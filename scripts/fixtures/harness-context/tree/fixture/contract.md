# Fixture contract

The source has one exported constant. The lifecycle is closed before the smoke.
Read source through the Read tool. Bash is limited to named metadata and entry
commands so every delivered file read has an observable route. A shell command
refused by this additional scope does not count as an actual file read.
