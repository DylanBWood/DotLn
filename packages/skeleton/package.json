{
  "name": "@dotln/skeleton",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "bin": {
    "dotln": "dist/src/dotln.js"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "tsc -b --force && node --test dist/test/*.test.js",
    "start": "node dist/src/cli.js"
  },
  "dependencies": {
    "@dotln/compiler": "0.3.0",
    "@dotln/kernel": "0.2.1"
  }
}
