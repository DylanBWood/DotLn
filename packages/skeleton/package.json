{
  "name": "@dotln/skeleton",
  "version": "0.11.0",
  "private": true,
  "type": "module",
  "bin": {
    "dotln": "dist/src/dotln.js"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "tsc -b --force && node --test dist/test/*.test.js",
    "start": "node dist/src/cli.js"
  },
  "dependencies": {
    "@dotln/compiler": "0.5.0",
    "@dotln/kernel": "0.2.1"
  }
}
