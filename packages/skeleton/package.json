{
  "name": "@dotln/skeleton",
  "version": "0.15.1",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "dotln": "dist/src/dotln.js"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "tsc -b --force && node --test dist/test/*.test.js",
    "start": "node dist/src/cli.js",
    "prepublishOnly": "node -e \"console.error('DOTLN_PACKAGE_PUBLISH_REFUSED: packages stay private until a separate publication decision'); process.exit(1)\""
  },
  "dependencies": {
    "@dotln/compiler": "0.9.1",
    "@dotln/kernel": "0.2.1",
    "typescript": "5.4.5"
  }
}
