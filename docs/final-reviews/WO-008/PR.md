## What this merges

WO-008, the `v0.4.0` composition-compiler order: a new pure `@dotln/compiler` workspace turns a typed `LoadoutGraph` into a normalized compiled program or structured diagnostics, and the walking skeleton's reactor now runs the compiled Seiri loadout in place of WO-003's hand-assembled object. Five linked supports lower to heterogeneous mechanisms (WorkOrder structure, permission guard, evidence schema, cadence plus statechart guard, verifier episode) with declared cost; the code DSL, function table, and statechart JSON views compile to one semantic hash; the compiled diff renders as the RPG item tooltip; and the 21 WO-003 decision traces are unchanged. `@dotln/compiler` is new at `0.1.0`, `@dotln/skeleton` advances to `0.4.0`, `@dotln/kernel` stays at `0.2.0`, no dependency is added, and nothing under `scripts/` or `packages/kernel/` changes.

- **Compiler (`packages/compiler/`, 12 TypeScript files, 4,210 lines):** the full graph type boundary; finite canonical normalization with negative-zero mapping and non-finite rejection; three codecs satisfying `decode(encode(x)) = normalize(x)`; the four compile steps; `SUPPORT INACTIVE`, `ACTIVE INACTIVE`, `INVALID GRAPH`, `DECLARED`, `HARD`, and `AMBIGUOUS SUPPORT CONFLICT`, `NON-COMMUTING SUPPORTS`, and `SEMANTICS UNSUPPORTED` diagnostics with concrete corrections; the nine-level precedence with winners applied to the emitted `AuthorityEnvelope` and WorkOrder operation lists; per-support cost emission; and the tooltip and view-hash renderers. Compiler v1 lowers one active mechanic, one participating link group, and at most one explicit pipeline, and rejects wildcard patterns rather than guessing.
- **Skeleton integration:** `reactor.ts` compiles the equipped graph and reads cadence, guard, verification subject, WorkOrder, and authority from the compiled program; `scenario.ts` takes the schedule ids from the reactor's decision and accepts an `equippedLoadout` option; `cli.ts` gains `--compiled-diff`. The frozen fixture `packages/skeleton/fixtures/wo003-decision-traces.json` was captured from the pre-edit tree and is genuine: a scratch build of `main` reproduces it exactly.
- **Evidence:** node tests rise from 110 to 154, all green; the exact `SUPPORT INACTIVE` text and its executable correction; three round-trip laws; three-view hash equality with a semantic edit that changes the hash; the nine-way conflict trace and all eight adjacent pairs; nine rejection cases; commuting-permutation identity; exact costs and mechanism types; exact tooltip, real unequip reversal, exact `--compiled-diff` and combined `--audit --compiled-diff` CLI output; purity under a throwing clock and RNG; and the 21-trace oracle.
- **Write-backs:** `02-domain-model.md` §LoadoutGraph v1 payload contract, `04-interfaces.md` §Editable-view v1 normalization and semantic hash, six adopted ledger entries, the compiler README, the skeleton README, the root README at `v0.4.0`, the capability table's `compiler.seiri-v1` row at level 1, and the publication index and locks.
- **Reviewed here, beyond the order:** the work-order map brought current; WO-018 amended with the single-slot harness-version attestation stall and the recorded `ultracode` = dynamic workflows + `xhigh` mapping; WO-108 extended to `packages/compiler` with the eight known surviving mutations as seed rows; both operator-directed during final review.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in this worktree on the verified tree and again on the reviewed tree: Prettier clean, the GitHub body-profile suite, all eight shell suites, `tsc -b --force`, 154 of 154 node tests, exit 0. `git diff --check` clean; `publication:check` 167 of 167 headings indexed with both audience locks `CURRENT`; `git status --porcelain scripts/ .claude/ packages/kernel/` empty.
- `node scripts/release.mjs check-surfaces`: `PASS release-block: observed v0.4.0`, `PASS component-version @dotln/compiler` (new package, no previous version required), `PASS component-version @dotln/kernel: src unchanged; observed 0.2.0`, `PASS component-version @dotln/skeleton: src changed; observed 0.4.0; previous v0.3.6 0.3.2`, and the body profile PASS over this package's `PR.md` and `RELEASE-NOTES.md`.
- Digests reproduced from a baseline build of `main` (`0344439`) and a scratch build of the delivered tree: default CLI 26 lines and 888 bytes on both sides, `sha256:25a406b0…` baseline and `sha256:88e66d23…` delivered, differing in exactly the banner line, and substituting the old version reproduces the baseline digest; the JSONL log 8,805 to 17,661 bytes with `sha256:9827759e…` on the delivered side; the compact 21-trace digest `sha256:ef9789a1…` identical on both sides and equal to the frozen fixture's, which deep-strict-equals both runs; the fixture file `sha256:ec53d1c8…`; the `--compiled-diff` output containing both receipt captures verbatim.
- Log delta itemized: 21 events with identical ids, types, and order; exactly three payloads differ, `WorkOrderEmitted` and `CommandPersisted` deep-equal modulo key order, and `LoadoutEquipped` replacing four fields with the thirteen-field graph; the live WorkOrder deep-strict-equals the baseline's.
- Mutation re-drill in a separate scratch copy, thirteen single-site mutations applied one at a time and rebuilt: the eight VER-001 survivors survive here too at 73 of 73 (tag check, ambiguous tie, active-inactive, duplicate ids, socket budget, pipeline dedupe, dropped `updateLaws`, hardcoded `promptTokens`); four controls die widely (inverted precedence 12 failures, constant hash 8, unsorted keys 5, dropped INTERRUPT 3); a changed reactor WorkOrder objective is caught by exactly one test.
- Direct probes: a linked support claiming `authority.repo.delete` = `allow` at `safety-invariants` compiles, enters `allowedEffects`, and leaves the tooltip's `Deletion remains operator-owned` line in place (VER-001 F2 reproduced); a tag-incompatible support and an equal-precedence tie each reject with the exact diagnostic (F3 and F4 fire correctly and remain untested).
- Subject integrity: a temporary-index tree of the working tree differs from the implementation-ready checkpoint only in the two control files, VER-001, and the operator's one-line `docs/discovery/environment.json` version edit; the control log is a strict append over `HEAD`; the verifier's report header equals its control-log actor by object comparison; the executor's and verifier's `xhigh` meet the declared `xhigh+` floors.
- Clean room: a pattern scan over the complete change set (5,846 added or new lines) found no credential, private address, internal host, tracker product name, or employer material; every relative link and anchor in the changed Markdown resolves (339 checked).

## Verification trail

- `docs/verifications/WO-008/VER-001.md`: pass; all six criteria re-derived independently, the frozen fixture proven genuine, 40 mutations drilled, nine non-blocking findings with a bounded checklist, and the attestation gate's stall disclosed rather than worked around.
- `docs/final-reviews/WO-008/FINAL-001.md`: pass; every VER-001 finding adjudicated with reasons, three documentation corrections applied and re-checked, the map brought current, the two operator-directed amendments recorded, method and instruments disclosed.

## Known open items

- **Safety-invariants is a label, not a floor.** Any linked support may claim the top layer, so an author-supplied graph can grant itself `repo.delete` over its own read-only support's hard denial, and the tooltip's RESTRICTIONS are authored strings that do not notice. Correct for the delivered Seiri loadout; a bounded follow-on before saved or community builds (VER-001 F2).
- **Eight undefended behaviors.** Five shipped rejections and three documented properties survive mutation with the suite green; WO-108's compiler extension seeds its kill matrix with them rather than adding tests here (VER-001 F3–F7).
- **One assertion guards the compiled WorkOrder content reaching the kernel.** The decision traces and CLI output do not carry WorkOrder text, so a changed objective is caught only by the AC6 shape test; a frozen WorkOrder oracle beside the trace fixture is a one-file candidate.
- **Harness-version attestation slot.** `environment.json` records one version per harness; the WO-008 verifier was refused until the operator recorded `2.1.260` by hand. WO-018 now carries the versions-list deliverable and the recorded `ultracode` label mapping.
- **Publish and release close.** The `gh` preflight fails closed inside the reviewer's sandbox, so the operator runs the publish command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, `npm run release -- close WO-008 --publish` from a fast-forwarded main checkout creates and pushes the annotated `v0.4.0` tag and its GitHub Release; the release-influence guard will still report the main checkout's local settings file until WO-018 lands.
