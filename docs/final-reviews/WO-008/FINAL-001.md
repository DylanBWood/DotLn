# WO-008 final review FINAL-001

**Subject:** branch `wo-008`, worktree `DotLn-wo008`, uncommitted working tree, 2026-09-03. `HEAD`, `main`, and `origin/main` are all `0344439`, the PR #24 merge that closed WO-016 and the commit behind the published annotated tag `v0.3.6`; the branch carries no local commits, so the entire subject is the dirty tree. At review open, after this review's own transition: 23 tracked modifications (882 insertions / 176 deletions against `HEAD` including the two `docs/control` files; 21 files, 868 / 167 without them) plus 17 untracked files totalling 4,964 lines: `packages/compiler/` (15 files, of which 7 source and 5 test files hold 4,210 TypeScript lines, plus a README, `package.json`, and `tsconfig.json`), `packages/skeleton/fixtures/wo003-decision-traces.json` (180 lines), and `docs/verifications/WO-008/VER-001.md` (481 lines). `scripts/`, `.claude/`, and `packages/kernel/` are unchanged; `package.json` changes only the `test` script to build and run the compiler suite; `package-lock.json` changes only the new workspace link and the skeleton version; `docs/discovery/environment.json` changes one line, the `claude-code` harness version `2.1.259` to `2.1.260`, recorded by the operator during verification (§Self-referential instruments).

**Authority:** `docs/work-orders/WO-008-composition-compiler.md`: `Model: any capable model` and `Effort: executor xhigh+; verifier xhigh+; reviewer any`, a `v0.4.0` minor release classification pinned by the planner rather than a placeholder, six acceptance criteria, an evidence gate, a write-back duty, a non-goal list, and the dated 2026-09-03 implementation evidence receipt. Against the activated text (`refs/dotln/checkpoint/WO-008/1`, whose tree equals `HEAD`'s plus the `Effort:` line the activation recorded, consistent with its `effortDeclarationValidated: true`), the order differs only by the appended receipt: 117 added lines and no removed line before this review's correction.

**Prior reports:** `VER-001` (pass; all six criteria re-derived from the delivered tree against a baseline build of `main`, the frozen fixture proven genuine, 40 mutations drilled with nine survivors, nine non-blocking findings with a bounded checklist, and two attestation-gate refusals disclosed rather than worked around). Complete sequence: one report, no repair episode.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes), **effort `max`**, in Claude Code 2.1.260 with sandboxed Bash; macOS 15.6 on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 installed in this worktree's own dependency tree. WO-008 declares `reviewer any`, so any attested effort including `unknown` would satisfy it; the value is stated because it is true. The effort value is **self-reported**: the harness's own `/model` readout in this session reported the selection of Fable 5.1 at `max` effort; `docs/discovery/environment.json` records no observed effective-session readback for `claude-code` and lists `max` among the documented `--effort` selector values; its version datum `2.1.260` was checked against `claude --version` on this machine rather than trusted.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.260","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-008 passes.** The order asked for a pure composition compiler: the full LoadoutGraph type boundary, link type-checking that fails with `SUPPORT INACTIVE` and concrete corrections, deterministic nine-level precedence with the winner visible in the trace, per-support declared cost, three editable views proven equivalent by one semantic hash, the compiled diff rendered as the RPG item tooltip, and the WO-003 scenario re-run on the compiled Seiri loadout with identical decision traces. The deliverable does that. `@dotln/compiler` has no dependencies and no I/O; an incompatible link returns the exact diagnosis and an executable correction; the nine layers match the architecture name for name and the trace exposes every overridden claim; a winning authority claim rewrites both the emitted envelope and the WorkOrder operation lists, so the kernel's `authorize()` and the trace agree; all five Seiri supports emit heterogeneous mechanisms with four cost fields; the three views compile to `fnv1a64:9ca8d0229c6bd8db`; the tooltip renders all six documented sections; and the 21 decision traces of the compiled run deep-strict-equal a fixture that a scratch build of `main` reproduces. Every digest, count, and capture in the receipt and in VER-001 was reproduced first-hand in this session, and the eight mutations VER-001 found undefended survive here too while four control mutations die. VER-001's nine findings are one overstated receipt sentence, two documentation citation errors, five undefended rejections and three undefended contract properties, and one design observation; three are corrected by this review inside the documentation lane, the eight undefended behaviors are handed to WO-108's compiler extension, and the design observation is nominated as a candidate. No code, contract, acceptance, schema, authority, or captured-evidence byte changed under this review.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **`scripts/resume.mjs` allocated this report's path, appended `FinalReviewRequested`, and records this verdict through the actor-gated `final-review-result` path.** It is not a deliverable of this order: `git status --porcelain scripts/ .claude/ packages/kernel/` is empty and `HEAD` is `origin/main`, so the script that ran is the one `v0.3.6` shipped. It is a fail-loud append-only log with a regenerated projection, and subject identity below was established with git objects rather than through it.
2. **The evidence file that gates actor attestations changed inside this change set, and it gates this review's own attestation.** `docs/discovery/environment.json` differs from `HEAD` by one line: `effortReadbackProbe.harnesses["claude-code"].version.value` moved from `2.1.259` to `2.1.260`. VER-001 §Attestation gate documents why: the probe recorded `2.1.259` at 14:12 on 2026-09-03, the verifier ran `2.1.260`, `effortHarnessEvidence()` requires an exact version match, and every `verification-result` attempt was refused regardless of `--source`. The verifier declined to edit the evidence that authorized its own verdict and asked the operator to record it; the checkpoint sequence shows the edit landed between the verify allocation (`/3`) and the verdict (`/4`), and the `VerificationCompleted` event carries `2.1.260`. The datum is a WO-019/WO-004 discovery artifact outside WO-008's scope, carried in the reviewed commit because the control log now depends on it; it was checked against `claude --version` here, and the structural fix (a versions list) is WO-018 deliverable 9 under the operator's amendment recorded in this review.
3. **`release check-surfaces` reports `component-version @dotln/compiler: src unchanged`.** As VER-001 disclosed, that row is incidental: `packages/compiler/` is entirely untracked, so the git-based "did `src/` change since the preceding tag" question has no tracked path to answer, and the other arm handled the new package honestly (`previous v0.3.6 not required`). The skeleton row detected the real change (`0.3.2` to `0.4.0`). This review did not rely on the compiler row for anything.
4. **This reviewer's own attestation is checked by the same mechanism.** A successful append is evidence that the header above equals the flags, not independent evidence about the subject; the inventory in §The order's own actors was compared by object comparison in this session, not by reading the projection.
5. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser, profile, and surface check** (`scripts/release-notes.mjs`, `scripts/github-body.mjs`, `release check-surfaces`), none of which this order changes. Their editorial adequacy is the operator's to judge at merge. Because `v0.4.0` is above the latest published tag, these notes become the `v0.4.0` edition at release close.

## Dispatch and subject integrity

`docs/control/current.md` named WO-008 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, drift `none`, and checkpoint `50b61418…` (`refs/dotln/checkpoint/WO-008/4`). Before invoking the transition I read the `resume` mapping in `package.json` (`node scripts/resume.mjs`, unchanged; only the `test` script differs from `HEAD`) and confirmed that `scripts/` is byte-identical to `HEAD` and `origin/main`. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-008/FINAL-001.md`, moved the phase to `final-review`, and minted checkpoint `d8ee81e7ce3fa482f0d50c60b7656a804d19ac7b` (`refs/dotln/checkpoint/WO-008/5`). `docs/control/resume.jsonl` is a strict append over `HEAD`: its first 116 lines are byte-identical to `HEAD`'s file, and the appended lines are `WorkOrderActivated` (with `effortDeclarationValidated: true`), `ImplementationReady`, `VerificationRequested`, `VerificationCompleted`, and this review's `FinalReviewRequested`, plus the `FinalReviewCompleted` the pass records. Every event carries `schemaVersion: 1`.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree at review open as tree `06e30e47aa12c50a83e139e75d03cfcd9f88376d`, and `git diff --stat` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                           |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`c788590c…`)              | every deliverable path (40 files, 5,845 insertions); the checkpoint's tree equals `HEAD`'s plus the work order's `Effort:` line             |
| `/2` implementation-ready (`606d2d53…`)    | `docs/control/current.md`, `docs/control/resume.jsonl` (+4), `docs/discovery/environment.json` (1 line), `docs/verifications/WO-008/VER-001.md` (added, 481) |
| `/3` verify allocation (`3d315108…`)       | `docs/control/current.md`, `docs/control/resume.jsonl` (+3), `docs/discovery/environment.json` (1 line), `docs/verifications/WO-008/VER-001.md` (added) |
| `/4` verdict (`50b61418…`)                 | `docs/control/current.md`, `docs/control/resume.jsonl` (+2)                                                                                 |
| `/5` final-review allocation (`d8ee81e7…`) | `docs/control/current.md`, `docs/control/resume.jsonl` (+1)                                                                                 |

Every deliverable blob, the 20 tracked non-control, non-discovery modifications and the 16 untracked deliverable files, is therefore identical across the implementation-ready, verify-allocation, and verdict checkpoints and the working tree; the verifier changed nothing in the deliverable, VER-001 has not changed since its verdict was recorded, and the only non-control change after implementation-ready is the operator's one-line discovery edit between `/3` and `/4`. All five checkpoints have `HEAD` as their sole parent.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. Beyond `node_modules`, the three build outputs, `.DS_Store`, and `*.tsbuildinfo`, there is no ignored path. `docs/intake/` exists only as three tracked zero-byte `.gitkeep` placeholders (`chats/`, `images/`, `notes/`), so it holds no raw material; VER-001's "absent from this worktree" is precise about material though not about the directory, and no verbatim-promotion screen is owed because this order has no ideation receipt. `.claude/` carries no change, tracked or untracked (its tracked `settings.json` is unchanged and the only other entry is an empty harness-owned `.cc-writes` directory that git does not see), and no `.claude/settings.local.json` exists in this worktree.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                                 | Report header                                                                                                                                                                                | Declared minimum | Result                          |
| -------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.0","model":"unknown","effort":"xhigh","source":"self-reported"}`                  | there is no executor report; the version agrees with the discovery record's `codex-cli` entry and `xhigh` with its persisted `model_reasoning_effort` selector; the model was not exposed    | `xhigh+`         | `xhigh` meets the floor exactly |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.260","model":"claude-opus-5[1m]","effort":"xhigh","source":"operator-attested"}`  | exactly one column-one header in VER-001, parsed and compared with the event actor as objects: equal; `operator-attested` because the session label `ultracode` was mapped by the operator | `xhigh+`         | `xhigh` meets the floor exactly |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.260","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`         | the header above                                                                                                                                                                             | `any`            | satisfied                       |

There is no `RepairCompleted` event; the sequence has no repair episode. The executor's `model: unknown` is honest rather than a gap in the floor: Codex CLI did not echo the model, the order declares `Model: any capable model`, and the operator dispatched it. The projection reads `Effort drift: none` because both completions so far are `xhigh`; this review's `max` adds the one distinct pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.260; model claude-fable-5-1; effort max; source self-reported`, `Effort drift: xhigh -> max`, and phase `closed`.

## Final evidence summary

### The unchanged `npm test`, twice, in this worktree

This worktree carries its own lockfile-resolved dependency tree (`node_modules/@dotln/kernel`, `@dotln/compiler`, and `@dotln/skeleton` are the workspace symlinks; Prettier 3.9.6; TypeScript 5.4.5), so the suite ran natively. First run, on the verified tree before any review artifact existed: `All matched files use Prettier code style!`, the GitHub body-profile suite, the eight shell suites (fixture temp-root, publication checker, backup-intake, resume, checkpoint, worktree, release surfaces, release close), then `tsc -b --force` over all three packages and `# tests 154 / # pass 154 / # fail 0 / # skipped 0 / # cancelled 0`, exit 0. Second run, after this review's corrections, the two operator-directed amendments, the refreshed publication locks, and the review package including the final bytes of this report were in place, with the same result (§Corrections applied by this review).

### Static and structural checks

- `git diff --check`: exit 0, no output, before and after this review's artifacts.
- `node scripts/check-publication.mjs`: `PASS index coverage: 167/167 product headings indexed`, `PASS dual-voice links`, both editions `CURRENT` (27 and 42 linked source sections); the two new `implemented` rows are indexed for the software-engineer voice; both source locks were refreshed by the executor and again by this review after the F8 wording landed in two locked sections.
- `node scripts/release.mjs check-surfaces` on the working tree: `PASS release-block: observed v0.4.0; expected v0.4.0 (work-order target v0.4.0; latest published v0.3.6)`, `PASS component-version @dotln/compiler: src unchanged; observed 0.1.0; previous v0.3.6 not required` (incidental, §Self-referential instruments), `PASS component-version @dotln/kernel: src unchanged; observed 0.2.0; previous v0.3.6 0.2.0`, `PASS component-version @dotln/skeleton: src changed; observed 0.4.0; previous v0.3.6 0.3.2; expected a different version`, and the profile rule PASS, reading `no current final-review bodies yet` before this review's bodies existed and naming `docs/final-reviews/WO-008/PR.md, docs/final-reviews/WO-008/RELEASE-NOTES.md` after.
- Forbidden and adjacent surfaces: `git status --porcelain scripts/ .claude/ packages/kernel/` is empty; `package.json` differs only in the `test` script; `package-lock.json` adds the `@dotln/compiler` workspace link and moves the skeleton to `0.4.0`; `npm ls --all` shows only `@types/node`, `prettier`, `typescript`, and the three local workspaces; `packages/compiler/package.json` declares no dependencies of any kind.
- The verifier's header equals its control actor by object comparison; VER-001 contains exactly one column-one `**Actor attestation:**` line.
- Every relative link and anchor in the 13 changed or new Markdown files at review open resolves (339 checked, 0 problems); repeated over the reviewed set including this package and the amended work orders, 427 checked, 0 problems.
- Clean room: a pattern scan over the complete change set (5,846 added or new lines) finds no credential or key shape, no private address, internal hostname, or email address, no commercial ticketing or ALM product name, and no employer, gateway, or managed-host description; the only URL is the GitHub Releases page already in the README.

### Output digests, reproduced from a baseline build

The baseline is `git archive 0344439` extracted into the session scratch root with a copy of this worktree's `node_modules` (whose relative `@dotln/*` links resolve to the scratch tree's own packages), built with `tsc -b --force`; the delivered tree was copied by `git ls-files -co --exclude-standard` (tracked and untracked files, excluding ignored paths) and built the same way. The baseline suite is 110 of 110 (29 skeleton); the delivered scratch suite is 154 of 154, and its compiler plus skeleton subset is 73 of 73.

| Surface                     | Baseline (`main`, `v0.3.6`)                        | Delivered tree                                     | Agrees with                                                                  |
| --------------------------- | -------------------------------------------------- | -------------------------------------------------- | ---------------------------------------------------------------------------- |
| default CLI                 | 26 lines, 888 B, `sha256:25a406b0…294a63befe`      | 26 lines, 888 B, `sha256:88e66d23…91b929ff`        | receipt and VER-001 on both sides; the WO-016 review's delivered value       |
| `--audit` CLI               | 1,082 lines, 30,687 B, `sha256:601cf325…a995f3b0`  | 1,630 lines, 47,940 B, `sha256:4fe47efa…0a4b06d9`  | baseline equals the WO-016 review's delivered value; delivered value is new  |
| `--compiled-diff` CLI       | not available                                      | 69 lines, 2,241 B, `sha256:a382aa0f…2580d121`      | contains both receipt capture blocks verbatim and contiguous                 |
| `--compiled-diff --audit`   | not available                                      | 1,673 lines, 49,293 B, `sha256:b3912cea…f348ded3`  | the exact-CLI test for the combined form                                     |
| JSONL log                   | 21 lines, 8,805 B, `sha256:44f92c90…c107ab22e2`    | 21 lines, 17,661 B, `sha256:9827759e…9f64829`      | receipt and VER-001                                                          |
| compact 21-trace digest     | `sha256:ef9789a1…86e21205`                         | `sha256:ef9789a1…86e21205`                         | receipt, VER-001, and the frozen fixture's own compact digest                |

The default CLI differs from `main` in exactly one line, the banner `v0.3.2` to `v0.4.0`; `sed` normalization of that line reproduces the baseline digest `25a406b0…`. The frozen fixture `packages/skeleton/fixtures/wo003-decision-traces.json` hashes to `sha256:ec53d1c8…707a173` as the receipt states, and the 21 traces extracted from the baseline build and from the delivered build are each `deepStrictEqual` to it, so the fixture is a genuine pre-edit capture and the compiled run reproduces it under strict equality.

### The log delta is three payloads, itemized

Decoding both JSONL logs and comparing every top-level field of every event: 21 events, identical `eventId`, `type`, `occurredAt`, `actorId`, `workstreamId`, `episodeId`, and `schemaVersion`; exactly three payloads differ. `WorkOrderEmitted` and `CommandPersisted` are `deepStrictEqual` and differ only in property order (canonical ordering from the compiler); `LoadoutEquipped` replaces the four-field placeholder (`identity`, `activeMechanic`, `normative`, `semantics`) with the thirteen-field typed graph. The live `WorkOrder` returned by `runScenario` is `deepStrictEqual` to the baseline's. The `--audit` delta is the same three payloads seen through the projections: with the banner removed, `diff` reports 15 hunks whose 61 removed lines are exactly the 11 lines of the old loadout payload and the 2 × 25 WorkOrder lines re-emitted in canonical key order, and whose 609 added lines are the 559-line graph payload plus those 50 re-ordered lines.

### Mutation re-drill

VER-001 drilled 40 mutations; this review did not inherit that. In a separate drill copy of the delivered scratch tree, each mutation below was applied alone as a source edit, the copy rebuilt with `tsc -b --force`, the 73-test compiler plus skeleton suite re-run, and the file restored byte for byte (a recursive `diff` of the drill sources against the pristine copy after the last mutant reported no difference; the worktree's sources were never touched). The unmutated control run was 73 of 73.

| #   | Mutation                                                                                     | Compiled | Result                                                                                                                                                                                   |
| --- | -------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `compile.ts`: the link type-check's `tagCompatible` forced true (VER-001 F3)                 | yes      | **survived**, 73 of 73                                                                                                                                                                   |
| 2   | `compile.ts`: the `AMBIGUOUS SUPPORT CONFLICT` tie branch never taken (F4)                   | yes      | **survived**, 73 of 73                                                                                                                                                                   |
| 3   | `normalize.ts`: `orderedSupportFacetIds` deduplicated (F5)                                   | yes      | **survived**, 73 of 73                                                                                                                                                                   |
| 4   | `normalize.ts`: `updateLaws` dropped in `normalizeIdentity` (F6)                             | yes      | **survived**, 73 of 73                                                                                                                                                                   |
| 5   | `compile.ts`: the active's missing capabilities treated as none (`ACTIVE INACTIVE`, F7)      | yes      | **survived**, 73 of 73                                                                                                                                                                   |
| 6   | `compile.ts`: `duplicateDiagnostics` never records a duplicate (F7)                          | yes      | **survived**, 73 of 73                                                                                                                                                                   |
| 7   | `compile.ts`: container socket-budget comparison raised out of reach (F7)                    | yes      | **survived**, 73 of 73                                                                                                                                                                   |
| 8   | `compile.ts`: `promptTokens: 0` hardcoded into `supportCosts` (F7)                           | yes      | **survived**, 73 of 73                                                                                                                                                                   |
| 9   | `compile.ts`: precedence comparator inverted (control)                                       | yes      | killed; 12 failures: the nine-way conflict, all eight adjacent-pair children, the authority-winner tests in both packages                                                                 |
| 10  | `render.ts`: INTERRUPT section dropped (control)                                             | yes      | killed by the exact tooltip test and both exact `--compiled-diff` CLI tests                                                                                                              |
| 11  | `normalize.ts`: canonical key sorting removed (control)                                      | yes      | killed; 5 failures: exact tooltip, verbatim hashes, three-view equality, both exact CLI tests                                                                                             |
| 12  | `normalize.ts`: semantic hash made constant (control)                                        | yes      | killed; 8 failures led by `a semantic edit changes the hash` and `equipping and unequipping changes the … semantic hash`                                                                  |
| 13  | `reactor.ts`: `compileWorkOrder` returns the compiled WorkOrder with a changed `objective`   | yes      | killed by exactly one test, `WO-008 AC6 the running scenario consumes the compiled Seiri loadout`; no trace, CLI, or milestone test noticed a different WorkOrder reaching the kernel     |

Rows 1 through 8 are the eight behaviors VER-001 reported as undefended; they are the five shipped rejections and three documented contract properties that WO-108's compiler extension now seeds its kill matrix with (§Operator-directed amendments). Row 13 is this review's own probe and reverses part of VER-001 F1 (§Adjudications).

### Direct probes against the built compiler

- **F2 reproduced.** A Seiri graph whose linked `seiri.repo-scope` support adds the claim `{target: "authority.repo.delete", value: "allow", layer: "safety-invariants", hard: false}` compiles with `ok: true`; the emitted envelope reads `allowedEffects: ["repo.inspect", "repo.delete"]` and `deniedEffects: ["repo.write"]`, the WorkOrder's `allowedOperations` gains `repo.delete`, and the rendered tooltip still lists `+ Deletion remains operator-owned` under RESTRICTIONS and never mentions `repo.delete`. The trace is honest: it records the hard `hard-permissions` denial from `seiri.read-only` as overridden by the soft `safety-invariants` allowance. Two hard claims at different layers reject as `HARD SUPPORT CONFLICT`, so the rule is that a lone hard claim yields to a higher soft layer while two differing hard claims never resolve.
- **F3 fires correctly.** A support with `supportedTags: ["destructive"]` linked to Seiri rejects with `SUPPORT INACTIVE: support "probe.destructive" supports tags [destructive], but active "seiri" exposes [observe, plan, schedule, verify].` plus a `link-compatible-active` and an `unequip-support` correction. Untested, as VER-001 said.
- **F4 fires correctly.** Two soft `voice` claims on `behavior.mode` with different values reject as `AMBIGUOUS SUPPORT CONFLICT: supports "probe.a" and "probe.b" make equal-precedence claims for "behavior.mode".`; the same two claims with equal values compile. Untested, as VER-001 said.
- **F5 measured.** A pipeline naming a `requires-pipeline` support twice compiles, the compiled program's `explicitPipelines` echo retains both occurrences, `decode(encode(x))` retains them, and the hash differs from the single-occurrence program; the phenotype semantics are identical because a string-rewrite transform is idempotent, so "applies that transform again" is mechanically true and observationally inert for the current transform kinds. A single non-commuting support also demands that the pipeline name it together with every other linked support pairwise, which is the doc's literal rule and is recorded below as a note.
- **F6 today.** The delivered `normalize` round-trips `updateLaws` intact; the finding is about the law's blindness, not the code.
- **Normalization strips unknown identity keys** (`normalizeIdentity` rebuilds the object field by field), so an unexpected input field neither survives into the graph nor changes the hash; the spread-based normalizers for containers, links, groups, pipelines, ambient effects, resources, axes, and three emission kinds would retain one. Not a criterion; a note for the saved-build rung.

## Acceptance criteria

| #   | Criterion                                                                                                                | Verdict | First-hand basis                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ------------------------------------------------------------------------------------------------------------------------ | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Incompatible link fails at compile time with `SUPPORT INACTIVE`, the exact missing capability, and concrete corrections  | met     | `compile.ts` read in full: the diagnosis is built from graph and environment data at the capability branch and the tag branch; the AC1 test pins the full diagnostic object and rendered corrections, and the second AC1 test proves the `provide-capability` correction is executable; `compileLoadout` returns `{ok: false, diagnostics}` before emission; the tag branch fires (probe) and is the one undefended half (F3).             |
| 2   | Equip/unequip changes the running program; three views hash equal; `decode(encode(x)) = normalize(x)` per view           | met     | Three codecs and three round-trip tests; `fnv1a64:9ca8d0229c6bd8db` reproduced from all three views in the scratch build; a semantic edit and an unequip each change the hash; `runScenario` with an unequipped support yields different decisions and constraints; severed compiled schedule ids change the emitted pulses; controls 11 and 12 above die on exactly these tests.                                                             |
| 3   | Nine-level precedence with visible winner; conflicting hard supports reject; non-commuting pair rejects or needs pipeline | met     | `COMPOSITION_PRECEDENCE` equals 03-architecture.md step 3 and the test's independent literal; the nine-way trace lists winner and eight overridden claims; all eight adjacent pairs; hard, declared, non-commuting, multi-pipeline, cross-group, wildcard, and unknown-layer rejections; the winning authority claim reaches the kernel's `authorize()`; control 9 kills 12 tests.                                                            |
| 4   | Per-support declared cost: mechanism type, prompt tokens, runtime cost, extra episodes                                    | met     | All five Seiri supports emit all four fields with five distinct mechanism types; the COST block reproduces the receipt verbatim; `promptTokens` is the one unfalsifiable field in the current fixture space (F7).                                                                                                                                                                                                                             |
| 5   | Compiled-diff preview renders as the RPG item tooltip, not only CLI text                                                  | met     | `renderCompiledDiff` is a pure function over compiled programs; header `Seiri / Sort / 整理`, six documented sections in order, then COST and SEMANTIC HASH as additive sections; the unequip preview is a real recompilation; exact tooltip, exact `--compiled-diff`, and exact combined CLI tests; control 10 dies.                                                                                                                         |
| 6   | WO-003 scenario re-runs on the compiled Seiri loadout with identical decision traces; delta itemized and justified         | met     | 21 of 21 traces `deepStrictEqual` to a fixture that the baseline build reproduces; default CLI byte-identical apart from the mandated banner; the log delta is exactly three payloads, two key-order only and one the intended graph replacement; the reactor takes WorkOrder, authority, cadence, guard, and verification subject from the compiled program (`reactor.ts` read in full).                                                    |

Evidence gate: test output captured in the receipt and reproduced here; every criterion maps to named tests that exist and run; the semantic-hash equality and the tooltip are pinned as frozen literals and reproduce verbatim. Scope discipline: types for the full graph, compile semantics bounded to one active, one participating group, one pipeline; three views only; PolarAxis typed and unevaluated; CLI text plus tooltip. Constraints: no dependencies, no I/O, new `packages/compiler/` in the workspace, no kernel change (none was needed and the receipt says so), no new dev-dependency. Non-goals respected.

## Write-back duty

- `docs/product/02-domain-model.md` §LoadoutGraph v1 payload contract: the pinned graph, container, link, group, pipeline, and support shapes and the bounded v1 lowering contract; field for field exact against `types.ts`; this review added the editorial-suffix sentence (F8).
- `docs/product/04-interfaces.md` §Editable-view v1 normalization and semantic hash: the three views, the codec law, canonical normalization, the hash algorithm and identifier, the compile signature; this review added the editorial-suffix sentence (F8).
- `docs/lineage/idea-ledger.md`: six `adopted` entries in a dated WO-008 section that states it does not predeclare the verdicts; 53 added lines and no removed line.
- `docs/planning/capability-table.md`: the dated WO-008 reassessment stages `compiler.seiri-v1` at level 1; this review corrected the purity citation (F9) and recorded the two verdicts in its preamble.
- `README.md`, `packages/skeleton/README.md`, `packages/compiler/README.md`, `docs/publication/audience-status-index.md` and both TOC locks: current.
- `docs/planning/work-order-map.md`: brought current by this review (§Corrections applied by this review).

## Verification-sequence soundness

VER-001 meets every duty the order and the guide place on it: it establishes subject identity through checkpoints, discloses the instrument and the incidental compiler row, confirms all six criteria with its own commands, builds the baseline and proves the fixture genuine rather than taking the receipt on report, itemizes the log delta per field, drills 40 mutations and discounts the equivalent ones, screens the change set for clean-room violations, names its findings with a bounded checklist that expands no scope, and refuses to edit the evidence that gates its own verdict. Every count it reports was re-derived here (154 and 73 tests, 167 headings, 27 and 42 sections, 21 events, 26/888 and 1,082 lines, all digests, 4,210 lines, three payloads, the eight survivors) and all agree. Two imprecisions, neither material: `docs/intake/` is present as tracked empty placeholders rather than absent; and F1's "cannot fail" is too strong for the reason in adjudication 1.

## Adjudications

### 1. "Proves the integration boundary" and the "tautological" assertion (VER-001 F1)

Confirmed in substance: the named test never calls `runScenario`, so the receipt sentence overstated it, and the sentence is **corrected** with a dated section in the work order. The characterization of the first assertion as unable to fail is only half right. Both sides derive from the same compile, so no compiled-content error can trip it, but they are two different functions, and mutation 13 above (a wrapper that alters the WorkOrder `objective` the reactor emits) was killed by that assertion and by nothing else. The assertion is therefore the only test that binds `compileWorkOrder` to the compiled program, and deleting it (checklist item 4) would remove the sole detector of that divergence; the checklist's alternative, running the scenario and asserting the live run consumed the compiled values, is the right shape and is a test change outside this review's authority. **Carried** as a candidate: a frozen WorkOrder oracle beside the trace fixture, since the traces and CLI output carry no WorkOrder text.

### 2. Self-granted deletion authority and the tooltip that does not notice (VER-001 F2)

Reproduced exactly (§Direct probes). Not a defect against WO-008: the compiler is a pure function over an author-supplied graph, no criterion asks for a claim-layer floor, and the trace records the override honestly. It is the most important thing this order has taught about the design, because "safety invariants" currently confers ordering rather than safety and the inspector's RESTRICTIONS are authored strings rather than a projection of the envelope, which 04-interfaces.md's "Honest status effects only" doctrine will not tolerate once graphs stop being author-trusted. **Nominated** in the work-order map as an unallocated composition candidate with no number, sequence, release, or activation authority.

### 3. Five undefended rejections and three undefended contract properties (VER-001 F3, F4, F5, F6, F7)

All eight confirmed by the re-drill (rows 1 through 8) and, for F3 and F4, by positive probes showing the guards fire correctly. Each is a test to add, which is code, and the operator directed during this review that WO-108's mutation probe extend to `packages/compiler` with these eight as seed rows rather than have the reviewer add tests. **Recorded** there (§Operator-directed amendments); no change to the deliverable.

### 4. The `V1`-suffixed names do not exist (VER-001 F8)

Confirmed: zero `V1`-suffixed identifiers in `packages/compiler/src`, twenty in 02-domain-model.md and five in 04-interfaces.md, with the shapes themselves exact. **Corrected** by one sentence in each document stating that the suffix is an editorial marker for the `schemaVersion: 1` payload and naming the unsuffixed exports; both publication locks were refreshed and `publication:check` is `CURRENT`.

### 5. The capability table cites the wrong test file (VER-001 F9)

Confirmed: `compiler.test.ts` contains no purity, determinism, or immutability assertion and `purity.test.ts` contains all of them. **Corrected**; the cell now attributes those properties to the purity test and links it.

### 6. The discovery-evidence edit inside the change set

The one-line `environment.json` change is not WO-008 work and was not made by the executor or the verifier; it is the operator's recording of the running harness version so that VER-001's verdict could be appended, as VER-001 §Attestation gate asked. It is carried in the reviewed commit because the control log depends on it, disclosed above, and its structural cause is now WO-018's problem to fix. **Ratified as carried evidence**; not a scope expansion.

### 7. The receipt's two Full Decision JSON digests

Not reproducible because the receipt states no serialization recipe, as VER-001 recorded. The underlying content is verified by a stronger route (strict trace equality, per-field log diff, strict WorkOrder equality). Inventing a recipe to make the digests reproduce would be fabricating evidence, so they are **left as recorded** and future receipts should state the command beside any digest.

### 8. Hard claims across layers

A lone hard claim at a lower layer yields to a soft claim at a higher layer; two differing hard claims reject. That is the nine-level precedence applied literally and AC3's "conflicting hard supports reject" holds; the uncomfortable consequence is adjudication 2. **Not a defect.**

### 9. A single non-commuting support demands a pipeline naming every other linked support

The pairwise check treats any pair with one `requires-pipeline` member as non-commuting, so the pipeline must name both, and 02-domain-model.md's sentence that omitted commutative supports "apply afterward" therefore only describes all-commutative groups that declare a pipeline anyway. Safe (it refuses rather than guesses), consistent with the doc's literal rule, and untested for the mixed case. **Recorded**; a note for the rung that adds a non-commuting support to a real loadout.

### 10. The kernel was not modified

The constraint allowed kernel changes "only for gaps this rung exposes, each noted in the result." `packages/kernel/src` is byte-identical to `v0.3.6`, `@dotln/kernel` stays `0.2.0`, and the receipt says no kernel change was needed. The reactor's new consumption of compiled `CadenceSpec` values, predicate references, and the authority envelope fit the kernel's existing types without adaptation. **Consistent with scope.**

### 11. The activation carried a pinned version and an Effort line

Unlike the six preceding orders, WO-008's H1 already read `v0.4.0` at activation and the executor forward-corrected nothing; the activation checkpoint's tree differs from `HEAD` by exactly the `Effort:` line, matching `effortDeclarationValidated: true`. **Ratified.**

### 12. The stale work-order map

The snapshot, boundary, slot, and target bullets and the WO-008 and WO-016 rows described WO-016 as awaiting merge and `v0.3.5` as the published boundary, true when written and false beside the published `v0.3.6` and this report. Following the practice of WO-013 through WO-016's reviews, they now record the `v0.3.6` close, VER-001 pass, FINAL-001 pass, the reviewed state awaiting operator merge, and the separately authorized release close that tags `v0.4.0`; the WO-018 and WO-108 rows record their amendments. **Corrected.**

## Corrections applied by this review

Three corrections in the documentation lane `docs/final-reviews/README.md` grants, plus the map, each proven contained by diffing against checkpoint 5 (only the named files changed, each with the intended hunks and nothing else after `prettier --write`), and followed by a re-run of the checks they could affect: the full `npm test` (which runs the pinned `prettier --check`), `git diff --check`, `publication:check` with refreshed locks, `check-surfaces`, and the link sweep, all clean.

1. **`docs/work-orders/WO-008-composition-compiler.md`:** acceptance-map item 6 reworded to say what its first named test does and which tests prove the running integration (adjudication 1), and a new dated section, `## Final-review evidence corrections — 2026-09-03`, recording the change with its finding number so the receipt's provenance stays legible.
2. **`docs/product/02-domain-model.md` and `docs/product/04-interfaces.md`:** one sentence each declaring the `V1` suffix editorial and naming the unsuffixed exports (adjudication 4); `docs/publication/everyday-ai-user-toc.md` and `software-engineer-toc.md` carry the refreshed source locks.
3. **`docs/planning/capability-table.md`:** the purity and immutability attribution moved to `purity.test.ts` with a link (adjudication 5), and the reassessment preamble now records VER-001 and FINAL-001 instead of "remains provisional".
4. **`docs/planning/work-order-map.md`:** the snapshot, boundary, slot, and target bullets, the WO-008, WO-016, WO-018, and WO-108 rows, the adjacent-track sentence about WO-108's base, and the new unallocated composition candidate (adjudication 12 and 2). No other row changed.

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5, and therefore to the tree VER-001 verified.

## Operator-directed amendments recorded in this review

The operator issued two instructions during this review, both outside WO-008's scope and both recorded in the reviewed commit as dated, provenance-marked sections of the affected work orders. They change no WO-008 deliverable and add no design idea, so no ledger entry is owed.

- **`docs/work-orders/WO-018-control-plane-consolidation.md`, "Operator amendment — harness-version attestation slot":** the observed problem that `environment.json` holds one version per harness and `effortHarnessEvidence()` matches it exactly, so every harness upgrade invalidates all effort attestation until discovery is re-run and fails only at the next completion command, with the evidence pointer to VER-001 §Attestation gate; the recorded operator mapping that the Claude Code session label `ultracode` means dynamic workflows plus `xhigh`; and two new required deliverables, a `versions` list with a matcher that accepts any listed observed version and appends rather than replaces, and the label mapping recorded in discovery with the refusal pointing at it while `resume` never converts a label to an effort value on its own.
- **`docs/work-orders/WO-108-mutation-probe.md`, "Scope extension — `packages/compiler`":** the target set grows to kernel + compiler + skeleton, the eight survivors above are tabulated as planted known-survivable seed rows with the four killed controls and the single-point detection of mutation 13 recorded beside them, two type-preserving operators (condition neutering, set-wrapping of an ordered array) are added, and the base moves to at or after the `v0.4.0` close.

## Findings

**None blocking.** No acceptance criterion is unmet and no behavioral or evidence defect was found in the deliverable. The items below are recorded and not scored.

1. **`safety-invariants` is a claimable label, and the tooltip's RESTRICTIONS are authored strings** (VER-001 F2): correct for the delivered loadout; nominated as an unallocated candidate for the rung at which graphs stop being author-trusted.
2. **Eight undefended behaviors** (VER-001 F3–F7): five shipped rejections and three documented properties survive mutation with the suite green; seeded into WO-108's compiler extension.
3. **One assertion guards the compiled WorkOrder content reaching the kernel:** mutation 13 is caught only by the AC6 shape test because traces and CLI output carry no WorkOrder text; a frozen WorkOrder oracle is a one-file candidate for the next skeleton order.
4. **A single non-commuting support requires a pipeline naming every other linked support** (adjudication 9): safe and documented literally, untested for the mixed case.
5. **The effort-attestation evidence holds one harness version per harness:** it stalled VER-001 until the operator edited discovery by hand; WO-018 now owns the versions list.
6. **The executor's model is `unknown` in the control log:** Codex CLI did not expose it and the order declares `Model: any`; recorded as the WO-019 mechanism working, not as a gap.
7. **The receipt's Full Decision JSON digests state no recipe** and cannot be reproduced; the content is verified by a stronger route.
8. **`docs/intake/` is present as tracked empty placeholders,** not absent as VER-001 wrote; no material is affected.

### Candidates considered and not sustained

- _AC2 fails because the round-trip law is vacuous._ Not sustained: the law as specified holds for each view, 04-interfaces.md says candidly how it is derived, and the hash tests give the equivalence teeth (a semantic edit and an unequip each change the hash; controls 11 and 12 die); F6 records the weakness honestly.
- _AC1 fails because the tag half of the type-check is untested._ Not sustained: AC1's wording is the missing capability, which is fully evidenced and mutation-resistant; the tag branch fires correctly on probe and is seeded into WO-108.
- _AC6 fails because the log is not byte-identical._ Not sustained: AC6 asks for identical decision traces, which hold under strict equality, and for any delta to be itemized and justified, which the receipt does and this review reproduced field by field.
- _The reactor recompiles the loadout on every decision._ Not a criterion: the compiler is pure and deterministic, and the recompilation is what makes equip and unequip change the running program.
- _The check-surfaces compiler row proves nothing._ True and disclosed; the guard's other arm handled the new package honestly and the skeleton row caught the real change.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step returned to the operator.** `gh` in this sandbox fails before its root command exists (`failed to create root command: failed to read configuration: open ~/.config/gh/config.yml: operation not permitted`, confirmed here with `gh --version` before the commit), so `npm run worktree -- publish` refuses at its `gh` preflight after the committed surface check and body validation and before any push, fail closed, as for WO-006 through WO-017 and WO-016. The reviewed commit is made here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation, and notes parse run against the exact commit; it refuses at the `gh` preflight and pushes nothing. The operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release, and the first minor.** After merge, and only under a fresh `resume: release close`, fast-forward the main checkout to the merge commit first (`git pull --ff-only origin main`), then run `npm run release -- close WO-008 --publish` from it, outside the sandbox and where `gh` is authenticated. Because `v0.4.0` is strictly above `v0.3.6`, the command runs `npm ci` and the evidence gate, assembles the five-section edition from the reviewed notes, creates and pushes the annotated `v0.4.0` tag, and creates the matching GitHub Release. The release-influence guard will still report the main checkout's local settings file until WO-018 lands; that warning is expected and true.
- **Attribution:** the reviewed commit carries a plain subject and no attribution trailer, per the operator's standing rule (`~/.claude/CLAUDE.md`) and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins.
- **Ledger duty:** discharged by the executor's six entries; this review's corrections are wording, and the two operator-directed amendments are work-order scope changes rather than design ideas, so no entry is owed.
- **Instruction conflict flagged, not acted on:** the execution guide asks reviewers to prefer instruments that fail loudly; this review's one non-loud instrument was its own effort attestation, which is a claim by construction, and the source label says so.

## Proposed PR

- **Title:** `:sparkles: WO-008: composition compiler v1; the Seiri link group compiles to heterogeneous mechanisms with declared cost, three editable views hash equal, and the skeleton runs the compiled loadout with identical decision traces; v0.4.0`, the `:sparkles:` shortcode being the gitmoji for introducing a new feature, which a new compiler workspace is.
- **Body:** `docs/final-reviews/WO-008/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commit (it also makes the map's "FINAL-001 pass" cell true); the reviewed state is committed with a plain subject. The operator then runs, from this worktree outside the sandbox:

```bash
npm run worktree -- publish WO-008 --title ':sparkles: WO-008: composition compiler v1; the Seiri link group compiles to heterogeneous mechanisms with declared cost, three editable views hash equal, and the skeleton runs the compiled loadout with identical decision traces; v0.4.0' --body-file docs/final-reviews/WO-008/PR.md
```

The helper runs `check-surfaces --committed WO-008` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-008`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, fast-forward the main checkout and run `npm run release -- close WO-008 --publish` from it; the expected outcome is the annotated `v0.4.0` tag and its GitHub Release.

## Method

Single reviewer, single session, no subagents. Read order: the execution guide's dispatch, closeout, discipline, and model-specific sections, the control projection and log, the work order in full including the receipt, VER-001 in full, the final-review and verification READMEs, the WO-016 final review with its PR body and notes for the package shape and publish precedent, then every changed file in full or by diff (all seven compiler source files and five test files in full, the reactor, scenario, and CLI diffs, both skeleton test diffs, the three manifests and lockfile, the compiler and skeleton READMEs, the two product-doc write-backs, the ledger additions, the capability table, the publication index, the README, and the fixture), with the publish, surface, notes, and body gates read from `worktree.mjs`, `release.mjs`, and `github-body.mjs` rather than inferred. Subject integrity was established through git objects. Evidence was re-established first-hand: two full `npm test` runs, the static, surface, publication, link, and clean-room checks, a baseline build of `main` and a scratch build of the delivered tree for the six digests and the strict fixture comparison, the per-field log diff and the audit-delta arithmetic, a thirteen-mutation re-drill in a separate drill copy, seven direct compiler probes, and the verifier header comparison. Authority used: three documentation corrections, the map update, the three package artifacts, and the two operator-directed work-order amendments; no edit to any code, test, contract, schema, or acceptance byte. The scratch trees lived in the session scratch root. Nothing was restored from a checkpoint, no destructive git command was used, and no remote, tag, or `gh` mutation was performed; the publish helper's own `gh` preflight after the reviewed commit fails closed, leaving the push and the PR to the operator.
