# WO-129 — FINAL-001

**Verdict:** PASS

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.270","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

Claude Code 2.1.270, observed with `claude --version`. The model is `claude-fable-5-1`, the exact model ID this session exposes. Effort `max` is the operator's `/model` selection at the start of this session, exposed to the tool environment as `CLAUDE_EFFORT=max`; a launch selector is not an effective-session readback, so the source is self-reported. The order declares the reviewer effort as `any`. This session took no part in the implementation, the repair or either verification.

## Subject and inputs

Branch `wo-129` at base `156f6ee`, which equals local `main` and, by `git ls-remote`, `origin/main` at review time, so no integration was needed. The subject is uncommitted: 37 modified tracked files (1,399 insertions, 252 deletions) plus 14 untracked files under `docs/control/orders`, `docs/evidence/WO-129`, `docs/verifications/WO-129` and `docs/final-reviews/WO-129`. `FinalReviewRequested` was recorded at 2026-09-13T04:06:23.726Z (checkpoint `refs/dotln/checkpoint/WO-129/9`, `ed815d9`). Between VER-002's completion checkpoint 8 (`3042a9c`) and checkpoint 9 only the three lifecycle projections changed. VER-001, VER-002 and the repair record are byte-identical to their completion checkpoints (4, 8 and 8).

Reviewed: the original order with its `## Execution record`; every cited section (07 §Discipline's process-budget, gate-input-protection and gate-execution bullets and §Goal-aligned decisions; 03 §Session lifecycle & resilience; `suiteEnvironment`, `observeSuiteInputs`, `suiteInputIdentity`, `suiteInputHash`, `cachePath`, `valid`, `explainSuiteFresh`, `saveSuiteSuccess` and `completeCoverage` in the suite evidence; `expandSuiteTasks` and `runGateChecks` in the runner; `prospectiveRealpath`, `suiteSuccessDirectory` and `suiteSuccessPath` in the shared gate evidence; `activeGateWriteRefusal` in the harness host; WO-126 D009, D012 and D014; WO-125 D003 to D005; the eighteen WO-125 transitions); the full subject diff, including the regenerated hooks and manifest, the transport change with its fixtures, the release surfaces and both publication locks; VER-001 and VER-002; D001 to D005, the repair record, the fixture transcripts, the WO-125 baseline, the host-gate capture and the authority and feedback editions (each JSON and JSONL file parsed); `package.json`; 08 §PRs and commits and §Release-note edition; and the WO-128 review artifacts as the publication series to size against.

## Independent checks

- Fixtures on the subject, run in sequence: `node --test scripts/test-suite-evidence.mjs` 15/15 (62.1 s), which includes the inventory declaration sweep, the Git-state matrix with the historical tag created, moved and deleted, the two-worktree identity fixture in both prefix spellings, the real-npm sibling gates, one explanation per input class with the five-path bound, the guard fixture and the version-2, unsealed, truncated and edited records; the three-role episode in `scripts/test-process-debt.mjs` 1/1 (12.4 s: 78 fresh / 0 reused, then 32 / 46 three times at three new exact trees); the linked-worktree cache-hook fixture in `scripts/test-harness.mjs` 1/1 (1.1 s); `scripts/test-gate-deadlines.mjs` 9/9 (1.2 s); the built skeleton worker and feedback-host tests 24/24 (27.4 s).
- Two real worktrees on this host: `observeSuiteInputs` on this worktree with its own `npm_config_local_prefix` and on the main checkout with its own gives `kernel` equal environment, toolchain, git, documents and declaration digests; source and runtime differ because main lacks the reviewed changes and carries an older build. An external prefix on this worktree changes the environment digest alone. Both checkouts resolve one cache directory under the main checkout's `.git`. Each observation covered about 1,800 files in 0.7 to 0.8 s.
- The shared cache holds 284 sealed version-3 records in 69 suite directories (24.5 MB). Every gate in the WO-129 series persisted its keyed successes except the repair's 03:20:31Z gate, which is VER-002 F1: the executor's three gates left 66, 23 and 23 records, VER-001's gate 69, VER-002's control run 1, VER-002's fresh gate at 03:50:20Z 69 and its composed gate at 03:57:46Z 23. The 03:20:31Z row carries the same checkout digest and the same 22-key environment projection as the three executor gates that persisted, and its 37 suite rows show 27 keyed, none reused and none failing, so the loss is not in the identity or the projection.
- Retained gate rows: seven full-gate aggregates from 02:11:14Z to 03:57:46Z. The host-gate capture's transition, before and first-after rows match the store and the control log: `ImplementationReady` at 02:11:36.804Z, tree `942ffdee` composed 75 / 3 at 647.8 s before it, tree `6e0aa8bb` composed 32 / 46 at 390.1 s first after it, with source and runtime digests unchanged and only the three control documents changed. One further composed row exists that no report cites: VER-002's second gate at 03:57:46Z, tree `2417fbf3`, 32 fresh / 46 reused, 376.3 s, in a Claude Code session after a document-only change.
- The built `harness-host.js`, `gate-evidence.mjs` and `harness-command.js` are byte-identical, by SHA-256, to the pinned snapshot `.runtime/harness/df7daacfd946b3ed` that the manifest and all nine generated hooks import at skeleton 0.15.4; each hook diff changes only the snapshot path, the version and two module hashes.
- The retired per-worktree cache path survives only in the order's observed-gap text, as history; no source, test or product document names it.
- Dependencies are unchanged: the lockfile changes only the skeleton version. `git diff --check` is clean. The adjacent queue has one item, completed. The clean-room screen of the WO-129 evidence, both reports, the order file and this review's outputs found no employer material, credentials, key material or mail addresses; the two checkout paths appear as they do throughout the order's evidence.

## Acceptance criteria

Criterion 1 holds: expanding the inventory gives 79 tasks, each with a declaration whose `git` field the runner copies onto the task; `index` declares `HEAD` and `refs/tags/*`, `plan` and `plan-refutation:current` declare `HEAD`, `authority-evidence` declares `refs/tags/v0.16.0`, the rest `none`; the seven `reuse: "live"` checks and `release:prepare` get no key, and an unknown suite name gets none.

Criterion 2 holds: the shared context is three digests (environment, toolchain, git); the checkout digest, CPU count and OS release sit in `executionFacts` on the row; `HEAD` and ref patterns are observed once into `gitState` and folded only into declaring suites' keys; the configuration observation is `--get-regexp` over `core.*`, `filter.*`, `init.templatedir` and `extensions.worktreeconfig`, with the exclude, attribute, user and npm configuration digests retained and the hook, filter, attribute and exclude guards still refusing reuse. The fixture proves a checkpoint ref, a branch, `user.name`, an identical-bytes commit, an unrelated tag, the historical tag's creation, move and deletion and an appended control event each change only the declaring suites' keys. The VER-001 repair gives `npm_config_local_prefix`, in either case, the `<worktree>` identity `PATH` already had, and the two-worktree probe above confirms it on this host.

Criterion 3 holds: records live under `<git common dir>/dotln/suite-success/<suite digest>/<inputHash>.json` at version 3 with the existing seal; the in-process sibling fixture and the real-npm fixture reuse across a linked worktree and reject one differing byte; the generated hooks refuse absolute, relative and shell-redirect writes into the cache from a linked worktree with or without an active gate; version-2, unsealed, truncated and edited records are ignored. F1 concerns retention on one host invocation, not this criterion's mechanism.

Criterion 4 holds: each fresh task prints `FRESH <task> (<policy>; <class: paths (+N more)>)` or `no prior success`, the aggregate row records `freshReasons` per executed task beside `executionMode`, `freshSuites` and `reusedSuites`, the retained host rows carry 78 and 32 reasons, and the fixture proves one explanation per class and the five-path bound.

Criterion 5 holds: the process-debt fixture builds the full 78-task inventory, records the three role transitions with checkpoint refs, and asserts `composed`, 32 fresh, 46 reused, a new exact tree and a retained row after each; reproduced.

Criterion 6 holds: a change to the runner, the suite evidence or the gate evidence changes every key; `reusable: false`, `reuse: "live"` and `--fresh` execute fresh with their policy named; `declarationVersion` is inside the `declaration` digest; the existing fixtures are kept, two adjusted to the new semantics (an unrelated tag now reuses; the F3 refusal regex admits the cache reason while the run-id check stands for every other destination).

Criterion 7 holds: the first full gate after `ImplementationReady` with no source change is `composed` with 46 reused tasks at 390.1 s, recorded in `host-gates.json` beside WO-125's ten 78-fresh rows in `wo125-baseline.json`, and both rows exist in the retained store with those values.

Criterion 8 holds except for the gate on the final bytes, which the completion command records: 07 §Discipline carries the identity rule, the tag input and the npm prefix sentence; D001 to D006 and their index rows exist; the follow-up register carries the WO-129 decision rows; the execution record uses the literal heading; `git diff --check` is clean; no dependency was added; application `v0.17.4` sits above the local `v0.17.3` with skeleton `0.15.4`.

## Review changes

- **D006 (deferral).** VER-002 F1 is carried as a follow-up whose first step is a diagnosis of the 03:20:31Z invocation's write context. The rejected options are recorded: failing the review over an unmet non-criterion, adding a persisted-success counter now (the observed invocation reported no failure to its own process, so a counter would have read 69), and failing closed on a non-writable cache against operator-review assumption 1. No source changed.

## Gate

The canonical gate for this review is `npm run harness -- evidence` on the tree containing this report, the PR body and the release notes; the runner's record for those bytes is the completion authority, and `final-review-result pass` refuses without a passing `npm run test:full` and `git diff --check` for the exact tree. This session's projected environment differs from every recorded gate's (its `GIT_CONFIG_VALUE_0..3` carry the checkout paths verbatim, and `CLAUDE_PID` is per session), so the gate re-executes all 78 tasks rather than composing; that is VER-002 O1 measured once more, not a defect in the order. The first attempt at this gate (04:19:26Z) failed in 26 s in the `release-surfaces` preflight: the reviewed-notes profile reads angle-bracket placeholders inside code spans in this review's own PR body and release notes as raw HTML, and no costly suite had started; the placeholders were rewritten as prose and the gate re-run. This report claims no gate result of its own.

## Observations (non-blocking)

1. VER-002 F1: one passing gate on the Codex host retained none of its 69 keyed successes, and nothing said so. The row for that gate is indistinguishable in checkout digest and environment projection from the executor's three gates that did persist, so the mechanism is in that session's write context, which this session cannot reproduce. D006 records the deferral and the reopening conditions; the next Codex-hosted gate followed by a cache census is the cheapest probe.
2. The reviewed environment projection (WO-126-D012) still bounds reuse to one session on Claude Code hosts: this session's direct projection has 24 keys, four of which carry checkout paths verbatim, and the executor's Codex rows have 22 with `npm_config_local_prefix` the only carrier, which the repair normalizes. Codex-hosted release closes on `main` can therefore compose from the reviewed worktree; Claude Code sessions pay a fresh gate each. This is outside the order's criteria and belongs with D003's reopening condition, as VER-002 O1 says.
3. The cache grows without pruning: 284 records and 24.5 MB after eleven gates, up from 191 and 16.6 MB at VER-002, and `explainSuiteFresh` validates every record in a suite's directory per fresh task. The order's Cost line ("the shared cache adds no per-gate work") understates this; the storage overhead is inside D003's reopening condition.
4. The cache guard resolves the Git common directory once per hook process for every write or shell tool call, gate or no gate, as D004 chose; one `git rev-parse` per hook invocation, memoized within the process.
5. `declarationVersion` is keyed only through the `declaration` digest; a bump with no other change has no dedicated fixture (VER-001 O4, VER-002 O4).
6. `gh` cannot read its configuration inside this session's sandbox, so the publish helper may refuse here; if it does, the handoff carries the exact command for the operator, and nothing outward-facing is attempted otherwise.

## Goal alignment

WO-129 exists to stop paying a full fresh gate for every lifecycle transition and to let a sibling worktree or the release close reuse work already done. Both are delivered and measured: 390 s and 376 s composed against 613 to 839 s fresh across two hosts, every fresh task explained, and identity equality across two real worktrees through real npm children. Passing sends the order to publication with F1 named, deferred with its diagnosis first, and reopenable on stated evidence. Failing, the NoOp for this decision, would spend a repair-and-verify cycle on a retention question no criterion asks, against a host context this session cannot reproduce. Rule beating: the pass rests on executed and reused counts from gates that persisted and on fixtures that execute real npm, not on hash equality alone. Seeking the wrong goal: the counts are executed tasks, not cache hits. Tragedy of the commons: the one lost gate is 717 s on one host, and the cache growth is measured and bounded by a reopening condition. Shifting the burden: no operator step is added; the deferred probe is a directory listing. Drift, escalation, policy resistance and success to the successful: no retry, warning or approval was added, and the declined alternatives are recorded in D006 rather than absorbed. Naive Interventionism: the review changed no source; a counter or fail-closed rule would touch the runner's completion path on every host. Reopen if a recorded gate at writable cache inputs fails to retain an executed passing suite, or if a gate at unchanged declared inputs reports `no prior success` for a task the preceding passing gate executed.

## Costs and publication

Tokens, measured with `node scripts/harness.mjs usage` (source `claude-transcript-message-usage`, scope `dispatch`): 134,311 at entry (observed 2026-09-13T04:00:12Z, 3 steps, 2 commands); 1,770,822 before this report was written (1,485,197 cached input, 229,798 cache-write input, 55,533 output, 294 uncached input; 69 steps, 68 commands; observed 04:12:13Z). The completion action records the handoff figure. Cost is null; the transcript records no price. The total includes about 104 s of fixture reproduction, the two-worktree probe and waiting; the independent checks added no gate run beyond the final canonical one. No equivalent alternative review was measured, so no efficiency comparison is claimed.

The reviewed state is committed as coherent commits (the declared-input identity, shared cache, explanations and cache guard with their fixtures; the transport minimum versions with their fixtures; the release preparation, runtime pins and evidence editions; the write-backs, evidence, reports and lifecycle records) and published with `npm run worktree -- publish WO-129 --title <reviewed title> --body-file docs/final-reviews/WO-129/PR.md`. Merge and release close remain the operator's.
