# WO-079 FINAL-001 — final review of `worktree integrate`

**Verdict: pass.** All four acceptance criteria hold at the reviewed subject,
judged against the order exactly as written. The verification sequence is a
single judgment, VER-001 (pass), and I judged it rather than inheriting it: I
reread the helper against each criterion, reproduced the fixture and the
refusal probe myself, and ran the full product gate on the staged tree. No
finding routes to repair. I made one bounded change inside this order's own
test file, recorded as
[WO-079-D005](../../evidence/WO-079/decisions.md#wo-079-d005), and reran the
whole gate after it.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"unknown","source":"self-reported"}

The harness version is live readback from `claude --version`. The model id is
this session's own reported identity. Effort is `unknown`: this harness exposes
no effective-effort readback and nobody supplied a value for this dispatch, so
the order's `reviewer any` line stays a recommendation rather than an
observation.

**Process cost:** entry 84107 tokens; handoff 3843399 tokens; source claude-transcript-message-usage

Scope `dispatch`; entry cutoff `2026-09-21T03:07:05.770Z`, handoff cutoff
`2026-09-21T03:18:38.127Z`. Both readings were taken before this report was
written and before the result transition, so both remain uncounted.
`costUsd` and `reasoningOutputTokens` are unavailable from this counter.
Subagents: 0 against the `docs/control/budgets.json` cap of 20, `countKind`
`exact-observed`, 0 unresolved admissions and 0 unlinked children. The fan-out
plan stated at entry was to judge this order in one writer session with no
spawn, so the whole budget remains and the unobserved remainder is unknown.

## Subject and evidence boundary

The subject is the uncommitted `wo-079` worktree at base `bca52bf2`, which is
`main` unchanged. `git log main..HEAD` is empty, so the whole order lives in the
index and working tree, as the no-branch-commits rule requires.

**No integration was required, and none was performed.** `main` has not moved
since the order was activated, so `git log HEAD..main` is empty and the second
lane's own helper had nothing to integrate. This review therefore does not
exercise the deliverable on itself; that is a limit of the evidence, recorded
below, not a gap in the deliverable.

Identity against the verified subject: `refs/dotln/checkpoint/WO-079/4` is the
capture VER-001 judged, and `refs/dotln/checkpoint/WO-079/5` the final-review
transition minted. The only tracked difference between checkpoints 4 and 5 is
`docs/control/current.md`, `docs/work-orders/README.md` and the appended
`FinalReviewRequested` line in `docs/control/orders/WO-079.jsonl` — control
projections and one lifecycle event, no source. Between checkpoint 5 and the
tree I staged, the only tracked differences are those same two projections,
regenerated by the transition. The order's untracked reports —
`implementation.md`, `VER-001.md` and `decisions.md` as it stood at dispatch —
hashed identically to their checkpoint-5 blobs; `decisions.md` has since gained
D005 from this review.

My own writes in this review are: the assertion change in
`scripts/test-worktree-integration.mjs`, decision D005 and its generated index
row, this report, the PR body and the release notes beside it, and the staging
and commits the pass authorizes.

## Instrument disclosure

Two instruments I used are themselves part of the subject, and one of them is
my own role text.

- **The reviewer skill is the deliverable.** This order changes the reviewer's
  integration instruction in `packages/skeleton/src/loadouts/contributor.ts:136`
  and regenerates it into `.claude/skills/dotln-reviewer/SKILL.md` and
  `.agents/skills/dotln-reviewer/SKILL.md`. The role text the harness delivered
  to me at dispatch is the changed text: it already reads "Handle authorized
  integration with `npm run worktree -- integrate WO-NNN` … then run the printed
  affected checks." I judged that line as a criterion-3 write-back and also ran
  under it. A reader should know that this review is the first execution of the
  instruction it approves.
- **`scripts/test-runner.mjs` is in the subject** — it gains the
  `worktree-integration` suite row and its protection sentence — and it is the
  gate I ran as evidence.
- **The `.claude/hooks/*.mjs` that governed this session were regenerated by
  this order.** While the gate was live they refused a `Monitor` call of mine as
  a possible write to gate inputs, naming the live run and offering
  `harness evidence --stop`. That refusal was correct; I waited rather than
  stopping the gate.

## Acceptance results

**Criterion 1 — the real-Git fixture: met.**
`scripts/test-worktree-integration.mjs` builds a bare local origin cloned from
the repository, a `main` checkout and a `wo-998` worktree, with three filed
orders, a merged sibling commit carrying a colliding `v9000.0.1` tag,
conflicting generated projections (the harness manifest, a console expected
fixture, an edition lock line, `CLAUDE.md`'s harness fragment, the decisions
index, the README release block), a conflicting authored file, a follow-up
register entry appended on each side, and untracked local bytes. Both the
fast-forward and the reviewed-commit paths are driven, and the assertions are
over content rather than exit status: the checkpoint is read back with
`git show <ref>:untracked-fixture.md`, the stash is identified by
`rev-parse refs/stash` before and after `--continue`, `HEAD` and `MERGE_HEAD`
witness that no reviewed commit was rewritten and no merge was committed, the
register entries are found by id and deep-equal their originals, and the
integrated tree is checked by `harness check`, `console-fixtures --check`,
`check-publication` and `work-orders index --check`.

After the change recorded as D005, the retime and the decision stub are now
asserted in **both** cases rather than only the fast-forward one. Standalone:
**6 tests, 6 passed, 0 failed, 35.95 s**. Inside the product gate the suite
passed at 53.13 s.

I read the helper against the criterion rather than only the assertions.
`scripts/lib/worktree-integration.mjs` contains no `stash pop`, no `stash drop`,
no `rebase` and no `commit`; its only `git add` calls stage resolved projections
so the unmerged stages clear. `mixedProjection` masks a generated fragment,
three-way merges the authored text around it, and restores the version from the
order's own heading, so authored prose keeps Git's ordinary conflict semantics
while the generated part is regenerated.

**Criterion 2 — the three refusals, no control event, no sibling gating: met.**
The fixture's refusal test snapshots `HEAD`, `git diff --binary HEAD`,
`git status --porcelain --untracked-files=all`, `refs/dotln`, `refs/stash` and
the raw index file, then asserts deep equality after each refusal: mismatched
worktree, unresolvable upstream, and ignored intake without a named backup. It
additionally covers a stale backup and a matching one. Because the snapshot
includes the tracked diff and control segments are tracked, "no control event is
appended" is covered by the same equality; structurally, the helper imports
`readControl` and `refreshControlProjection` only, and `refreshControlProjection`
writes a projection, not a segment. The unrelated third order's phase is varied
between `active` and `verifying` across the two cases, and neither is consulted.

Ordering matters here and is right: `verifyIntake` and the worktree-match test
run before any fetch, checkpoint mint or stash, so a refusal cannot leave
recovery material half-made.

**Criterion 3 — the write-backs: met, all four.**
Product 07 §Independent workflows and integration now runs the command and
describes the checkpoint, named stash, fetch, fast-forward-or-merge, stash
apply, the `--intake-backup` requirement, the nonzero exit and `--continue`, the
mixed-document re-merge, the register union, the reused producers, the draft
decision, and the statement that the helper runs no gate and records no event;
the 2026-09-16 checklist is retained and now reads "runs this list, now
implemented by `npm run worktree -- integrate WO-NNN`", with the 2026-09-17
amendment reading "the list remains its definition". That is exactly what the
criterion asks: the checklist names the command and stays as its definition.
`docs/PLAYBOOK.md` §Concurrency replaces the hand-written eight-step procedure
with the helper and `--continue`. The reviewer skill's integration line is
changed at the generator source and regenerated identically into both roots
(`diff -rq .claude/skills .agents/skills` is empty; the manifest carries the
matching whole-file hash for both). The ledger entry is discharged by the
standing pre-2026-09-09 substitution, which the work-order index marks
explicitly; D001–D005 and their five index rows carry it.

**Criterion 4 — gates, whitespace, dependencies: met.** Recorded below.

## Gates run at this review

`npm test -- --review` on the staged tree, after my last source edit:
**30 passed; 0 failed; 497.36 s; 74 fresh tasks**, exit 0. This is the passing
product row this review carries into the control event and the PR. It ran
outside the harness sandbox from the start, as the `skeleton` suite's
`needs: outside-sandbox` declaration requires; it is a full row, not a partial
inside-sandbox one.

`npm run test:docs`: **19 passed; 0 failed; 19.19 s**, rerun after this report,
the PR body and the release notes were written, and including the pinned
Prettier `format` check. An earlier run over the source-only tree passed the
same 19 in 18.58 s.

`git diff --check` and `git diff --cached --check`: both clean.

`npm run publication:check`: index coverage 272/272, both editions current
(30 and 45 linked sections). `node scripts/harness.mjs check`: 31 generated
surfaces. `node scripts/refute-plan.mjs check`: exit 0.

No new dependency. `devDependencies` are untouched; `package-lock.json` moves
only the internal `@dotln/skeleton` pin `0.33.0` → `0.33.1`, matched in
`packages/skeleton/package.json`, `packages/console/package.json` and
`packages/skeleton/src/version.ts`. The helper's intake check shells out to
`unzip`, which `scripts/backup-intake.sh` already required alongside `zip`.

Release surfaces agree: newest tag `v0.37.0`; the order heading, the README
claim and the dated roadmap note all read `v0.37.1`, a patch above the observed
baseline under the order's existing classification.

## The verification sequence

One judgment, VER-001, verdict pass. I judged it rather than accepting it, and
it holds up: it reproduced every criterion in its own session, ran the full
gate itself, probed the first refusal against this real repository with
before-and-after digests of status, `HEAD` and both ref namespaces, and checked
the register union question that decides whether the fixture is realistic —
that `planning-followups.mjs` mints new entry ids as content hashes rather than
positions, so cross-lane ids do not collide by construction. It also disclosed
that its own instruments were in the subject. A single passing verification is
a thinner sequence than this repository often produces, so I treated its seven
recorded limits as the place to look hardest, and two of them are worth the
reader's attention.

**L1, now closed by observation.** VER-001 recorded that the reviewed-merge case
asserted neither the retime nor the decision stub, and that the per-case JSON
the test prints — including `"retimed":"v9000.0.2"` — was a hardcoded literal
rather than a reading. That is a real defect in evidence output, in this order's
own new file, and it contradicts D001's own rule to assert preserved content
rather than successful subprocess exits. I moved both assertions to after
`--continue`, where the generators run in both paths, and made the printed line
read its values back from the integrated tree. Both cases now print
`"retimed":"v9000.0.2"` as an observation. Recorded as D005.

**L2, resolved by reading rather than left as an inference.** VER-001 noted that
in the fast-forward path `regenerate()` runs while an authored conflict may
still be unresolved, and inferred the failure mode without constructing it. The
inference is right and the design is self-correcting: an unresolved conflict
keeps `remaining` non-empty, so `receipt.complete` stays false and the command
exits 1 — the tree is never presented as integrated — and `--continue` runs
`resolveProjections` and `regenerate` again over the resolved bytes, so the
final projections are generated from authored-resolved content. Both generators
that could see conflicted bytes are idempotent across those two runs:
`decisionStub` returns early on its `<!-- integration <ref> -->` marker, and
`release prepare` is re-run in the fixture's own `--continue` and passes. Not a
finding.

L3 (a divergent same-entry register history asserted at unit level but not
driven through `resolveProjections`), L4 (the decisions-index conflict set up
but covered only by the following checks) and L7 (the order's Cost figures,
which nothing in the deliverable measures and no verified claim depends on)
stand as recorded limits of the shipped evidence. L5 is discharged: the
untracked `PR.md` VER-001 found was the generated process-meter block that
`release prepare` writes, and this review has replaced it with the reviewed PR
body around that block. L6 stands: the Codex executor's `ImplementationReady`
evidence carries the "Outputs not read at current bytes" advisory over 24 of
this order's own changed files, because Codex has no automatic read receipts —
advisory, not a finding, and the same structural gap WO-099 boarded up as D035.

## What I checked beyond the reports

The process-debt change is the part of the diff no report walks through, and it
is the kind of change that can hide a weakened test, so I checked it directly.
`scripts/test-process-debt.mjs` stops using `wo146-role-baseline.json` as the
current-bytes oracle and uses a new `wo079-role-baseline.json` instead. Both
historical snapshots survive: `wo145-role-baseline.json` still checks released
bytes at `historical.release`, `wo146-role-baseline.json` is unmodified in
`git status`, and the new test adds an assertion that did not exist before —
the WO-146 file's SHA-256 must equal `195026201a0d1535…`, which I confirmed
matches the file on disk. This is a baseline added beside its predecessors, with
a new byte-exactness check, not a baseline rewritten to make a failure go away.
D004 records the correction honestly, including that the gate was stopped before
its inputs were edited and recorded no check.

The ideation provenance checks out. The order's nomination cites the 2026-09-17
planning pass, whose §13 decision 4 records "WO-079 is rewritten as
`worktree integrate`", and §4 records the reopening observation in the operator's
own terms — integration still handled differently each time, every step
remembered rather than executed. The shipped scope is that rewrite and nothing
more: no lane generator, no scheduler, no history rewriting, no gate, no
lifecycle transition, matching the order's non-goals and D001's rejected list.

One observation about that receipt, which I am not routing as a defect. Its §4
prose and its §13 decision 4 both say WO-079 is "placed at pair 6", while its §4
sequence table lists WO-079 at pair 7 beside WO-069, with pair 6 as WO-110 and
WO-099; the order's own "Recommended placement" says pair 6 beside WO-099. The
executed order followed the table. Nothing depends on the discrepancy — the
order states the placement is "A recommendation, not a dependency token", and
`refute-plan check` and the plan's topology check pass — and a dated planning
pass is immutable historical evidence, so there is nothing here to repair. It is
recorded so a later reader of that receipt is not confused by it.

## Limits of this review

- **The helper was not exercised on a real integration.** `main` did not move,
  so this review had nothing to integrate and the deliverable's own first real
  use is still ahead. All evidence for it is the synthetic real-Git fixture. The
  implementation evidence says the same thing plainly, and D001's reopening
  condition is written for exactly that event: a real integration needing a
  mechanical step the command does not cover.
- **No time saving is measured or claimed.** The order's Cost section quotes
  final-review durations and a median for earlier integrations; nothing in the
  deliverable measures them, and I reproduced none of them.
- **One verification, not a converging sequence.** VER-001 passed first time.
  The disposition above says what I did about that rather than treating a single
  pass as equivalent to a sequence.
- **My own role text is in the subject**, as disclosed above.

## Disposition

Pass. The order set out to replace a remembered eight-step integration checklist
with one command that preserves recovery material and both histories, refuses
three named cases without touching the tree, appends no lifecycle event, and
hands the reviewer an explicit list of what still needs judgment. That is what
the code does, and the fixture proves it against real Git and the repository's
real generators. Nothing routes to repair. The reopening observations stand as
D001 states them.
