## What this merges

WO-126, the `v0.17.0` minor above the published `v0.16.0`. The operator's 2026-09-09 dispatch enumerated eleven failures of this repository's own lifecycle machinery observed during WO-042's final review and release close: a 37-step serial suite at every turn end and again at the tag, a reviewer owing 135 byte reads and 5,305,410 bytes of inherited and generated output after reviewing the diff, three Stop hooks that refused to end a turn without the whole suite and a recorded transition, a build that deleted the runtime the installed hooks import, a hand-written closeout script, attestation that degraded to `unknown` on a CLI patch bump, a prose-only anti-oscillation unit that described the wrong rule and left seventeen residue lines in the floor, a 184,668-byte planner read, a ledger that nothing read, and no meter over any of it. This order answers with three rules and their mechanisms. Hard enforcement lives in the four lifecycle transition commands and the pre-effect guards; Stop advises. A gate runs once per exact Git tree and its evidence is reused by hash. Growth needs a decision: `npm run meta` reads the control log, the evidence records, the manifest and the artifact sizes every order and fails the fast gate on a budget breach without a dated acceptance. Compiler `0.9.0`, skeleton `0.15.0`, console `0.1.3`; kernel `0.2.1`, the compiled-program contract `"1"`, `harness-v1`, `feedback-v1`, `verification-v1`, lifecycle legality and the event schema are unchanged. No dependency is added.

The operator expanded the order three times during execution and every expansion is in this diff: the retained public follow-up register with a paged feed at planning entry, the four-part gate-efficiency work (suite-input evidence reuse, forty named release cases under one cap, a sealed fixture template, live progress), and the D022 correction that made `npm run harness -- evidence` the single completion entry point. Eight ideation breakouts landed as labeled product candidates with their receipts.

- **Two gates with numbers (criterion 6).** `npm test` is the fast gate: build, the four package suites and the format, harness, index, publication, budget and meter checks under a 120-second budget, 109.6 seconds in the latest recorded idle cold run. `npm run test:full` runs every command of the previous chain through one runner with a build barrier, a CPU-derived cap, serialization groups, exclusive hook-heavy suites, generated-file preflights that stop expensive fixtures, split-candidate naming, negative-fixture lines under their suite, live progress and timeouts recorded as executed failures. The build compiles into staging and replaces each `dist` file atomically while the hooks import an immutable pinned snapshot; a fixture invokes an installed hook at every replacement boundary.
- **Evidence by tree hash (criterion 5).** Check rows carry the exact Git tree hash and duration; a required check is satisfied by any green run at the current tree from any session; executed suite successes whose reviewed inputs are unchanged compose into later gates with their original identity while the aggregate still covers the exact tree. The hot cache is bounded to 256 rows with tree-addressed archives and addressed failure logs.
- **Advisory Stop and hard transitions (criteria 3 and 4).** The observer records only paths the session authored; generated paths and files over 64 KB owe a check, not a read; inherited changes owe nothing. One Stop hook prints an advisory line and releases the writer. `implementation-ready`, `repair-complete`, `verification-result` and `final-review-result` refuse without the full gate, the diff check, authored-output reads or checks and no remaining work at the current tree.
- **The classifier and its floor (criterion 8).** Substring matching is replaced by invocation classification with quoting and expansion provenance: wrappers, shells, `eval`, pipelines, groups, package execution wrappers, assignments and inline Git configuration are followed; a literal denial floor covers every opaque program's arguments and input and the effect programs' own command-valued operands after only proven message and search data are removed; GitHub API bodies and write verbs are remote effects; names fold on case-insensitive hosts; every refusal names its classification reason. `plan start` is allowlisted and the tool-effect inventory refuses unclassified and spawn-class tools. Six of the seven verification failures were successive shapes of one class, a line the `v0.16.0` regexes refused that the classifier admitted; the floor rule closed it.
- **Attestation by version line (criterion 2).** The discovery record carries observed major.minor lines; `resume` accepts the declared effort for any patch on the line and records the actual version; `npm run discover -- harness` appends a probed row; session entry warns once off the line and observes the running version through the explicit input, the running executable, a verified ancestor's executable mapping or PATH. WO-042's segment is byte-identical.
- **Attribution (criterion 1).** The predicate matches session trailers and session URLs; the generated settings carry the attribution block; `worktree publish` refuses a matching title or body before any push.
- **Closeout (criterion 7).** `docs/control/local/` is a scripted lane with only `harness/**` disposable; `release close` reconciles intake into main with byte-verified copies and collision suffixes, previews with `--dry-run`, and reuses full-gate evidence for an identical tree; the release-close skill forbids closeout scripts.
- **Units and the floor (criterion 9).** Four prose-only units are kind `prose` at version 2 with no residue; three Stop units are advisory; version 1 is retained for replay; the anti-oscillation unit carries the operator's category rule and worked example in every role skill; the `CLAUDE.md` residue block is one line and the file shrank from 3,593 to 2,251 bytes.
- **Budget, meter and the lens (criteria 10, 11, 16, 17).** `docs/control/budgets.json` holds the operator's ceilings with token, dollar and PR-body caps unset by direction; the cold-start check compares each floor plus skill to the previous edition and its budget; the meter prints per-order and per-dispatch rows with previous-order deltas, one row per systems trap, and reopen candidates; status prints one health line; `release prepare` writes the table below; the first observation is the immutable baseline. Every role skill opens with the cost-before-adding-a-step line, every order carries a `Cost:` header, and the refuter holds a missing or unbalanced cost.
- **Headline titles and ledger scope (criteria 12 and 13).** `worktree publish` prints the last five merged subjects beside the proposed title and no numeric rule refuses a length. Decisions and corrections live in the order's decisions file with dispatch, evidence, rejected alternatives and reopening condition; `npm run meta` generates the decisions index and syncs the follow-up register; the ledger is for ideation and planning only and gained two sections with no deletion; the index marks the inherited ledger-duty substitution on 108 orders.
- **Planning and refutation (criteria 15 and 16).** The sequence moved to `docs/planning/sequence.md` and older rationale to the archive byte for byte; `planning: refute` and `planning: refute full` load the direct refuter skill, whose two commands print the canonical prompt and file the immutable receipt with carried verdicts and dispatch-to-file timing; the subject carries the cost line and a subject-hashed cost table.
- **Write-backs (criterion 14).** Products 02, 03, 04, 05, 07 and 08, the playbook, both readmes, the refutation readme, the capability table, the template and the publication index carry the changes; the unsourced no-telemetry sentences are replaced by the recorded-per-dispatch rule; both profiles' skills are regenerated.

## Evidence

Re-established first-hand at final review, not inherited:

- The built classifier and the pinned snapshot the hooks import agree on twenty probed shapes: bare, path-qualified and wrapped pushes deny; a shell fed a piped program, an interpreter one-liner, `npm exec -- npm publish`, `git rebase -x`, a `git -c diff.external` value and an assignment expanded into `find -exec` refuse with the classification reason; a `gh api` body is remote; a search pattern, a commit message, a `--grep` value and a read-only API call stay admitted; the lifecycle commands classify as `lifecycle.run`; a `for` loop refuses by design. The attribution predicate catches a `Claude-Session` trailer, a `Codex-Session` trailer, a `Session-URL` trailer, a bare session link, an AI coauthor and a generated-with footer, and admits a human coauthor.
- All 123 legacy work orders changed by exactly the two-line unavailable cost declaration and nothing else. The ledger gained 175 lines and lost none. WO-042's control segment is byte-identical. Only `.gitkeep` files are tracked under `docs/intake/` and no intake byte appears in the diff or the untracked set. No lint, type or formatter suppression appears in any added line. `package-lock.json` changes only by three workspace versions and the skeleton's compiler pin.
- The discovery record, compared as JSON before and after, differs only in the appended observed version lines for both harnesses and two appended Codex version rows; the rest is pretty-printing.
- `npm run meta -- --check` reports no budget breach; this session's own three loop refusals moved the guard-refusal count from 6 to 9, so the policy-resistance signal counts one refusal per tool use; the session hook observed `2.1.268` through the verified ancestor's executable mapping and printed no off-line warning.
- `git log main..HEAD` and `git log HEAD..main` are both empty, so no branch commit precedes this review and no integration was required. `git diff --check` is clean.
- The completion gate at the reviewed tree is `npm run harness -- evidence`, which runs `npm run test:full` and `git diff --check` and reuses executed suite successes; its rows are recorded by the `FinalReviewCompleted` evidence entry in the control segment rather than edited into the reports, per the write-once rule. This session ran under the bundle it reviews: the generated session hook resolved `resume: final review` to the reviewer role, the permission hook refused three loop-bearing commands and every refusal named its classification reason.

## Verification trail

- `VER-001` (fail): F1 to F9. Criteria 6, 8 and 18 not met, criterion 2 in part: a path-qualified program escaped every denied effect, the fast gate ran 139 to 144 seconds, the build had acquired a `python3` prerequisite, and the added authorship snapshot cost was undeclared.
- `VER-002` (fail): F10 to F16. Wrapper and compound forms still admitted, the attribution guard matched a substring, an unquoted expansion in the effect position was admitted, one contention failure, a five-second fast-gate margin and inert suite reuse under a rotating proxy credential.
- `VER-003` (fail): F17 to F20. A shell fed a piped program and interpreter one-liners, alias-defined pushes, a concurrent hook-state race and a version channel that could not match this host.
- `VER-004` (fail): F21 to F23. Groups, redirections, unlisted shells and interpreters, `gh api` bodies and the live version channel recording PATH.
- `VER-005` (fail): F24 to F28. Effect programs' own arguments and stripped assignments, no journaled refusal, case-folded names, lost release-case output and the preflight wait on the fast gate's critical path; the D022 correction landed in this repair.
- `VER-006` (fail): F29 to F30. Effect programs' command-valued operands and one refusal counted per denying hook.
- `VER-007` (pass): every criterion met; F31 (refused supported spellings, minor), F32 (`sed` and `less` command-valued operands on other hosts, minor) and F33 (full-gate contention on this host, moderate) recorded as non-blocking. Six bounded repairs sit between the reports, each with its receipt and decisions D007 to D028.
- `FINAL-001` (pass): [the report](FINAL-001.md) records the sequence, the checks re-established here, the disposition of F31 to F33 as decision D029 so they reach the planning follow-up register, and this review's own limits.

## Limits carried forward

- F31, F32 and F33 are carried as sourced follow-ups under [WO-126-D029](../../evidence/WO-126/decisions.md#wo-126-d029) with their reopening conditions; none opens a denied route on the observed host.
- The classifier is a bounded literal adapter. It does not decode scripts, encoded transforms, ambient Git configuration or the write-then-run route, and it refuses `for` loops, command substitution and some ordinary spellings conservatively; each costs a session a retry that the meter now sees.
- A read receipt witnesses delivery, not comprehension; a prose unit claims instruction delivery, not behavior; the console counts prevention only for hard units.
- Codex has no automatic hooks and relies on the explicit `harness begin`, `observe` and `delivered` adapter; its effort remains operator-attested. Claude's effort is self-reported because no readback row is recorded, and the committed Claude profile still declares `2.1.263` while the `2.1` line is observed through `2.1.268`.
- Token, dollar and PR-body ceilings remain unset by the operator's direction to collect usage first; the meter reports the observed values without a verdict.
- The planner startup-context comparison (FUP-0111) and the support-behavior gap recorded in D003 remain open candidates; twelve pinned runtime snapshots accumulate under `.runtime/harness/` and nothing prunes them.
- VER-001 names this machine's home-directory path once, as sixteen already-committed reports do; the immutable report was left as written.

<!-- dotln-process-meter:start -->
| Work | Phase ms / attempts | Gate ms | Read files / bytes | Observed tokens / USD | Declared prompt tokens | Corrections |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| WO-041 | 46,273,449 (Δ unavailable) / 8 | unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 0 (Δ unavailable) |
| WO-032 | 8,389,570 (Δ -37,883,879) / 3 | unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 0 (Δ 0) |
| WO-109 | 11,283,452 (Δ 2,893,882) / 3 | unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 0 (Δ 0) |
| WO-039 | 70,213,998 (Δ 58,930,546) / 13 | unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 0 (Δ 0) |
| WO-042 | 57,675,542 (Δ -12,538,456) / 5 | unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 0 (Δ 0) |
| WO-126 | 68,405,923 (Δ 10,730,381) / 15 | 21,908,580 (Δ unavailable) | 3 (Δ unavailable) / 43,318 (Δ unavailable) | 196,661,231 (Δ unavailable) / unavailable (Δ unavailable) | 504 (Δ unavailable) | 6 (Δ 6) |

Unavailable observations are not zero; unset ceilings are not approvals of a future limit.

| Dispatch | Wall ms (Δ) | Context bytes (Δ) | Commands (Δ) | Tokens (Δ) | Steps (Δ) | USD (Δ) / declared prompt tokens |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| WO-126/executor | 47,735,602 (30,780,538) | unavailable (unavailable) | unavailable (unavailable) | 79,941,302 (unavailable) | 595 (unavailable) | unavailable (unavailable) / 504 |
| WO-126/verifier | 20,670,321 (-16,528,879) | 1,394,107 (unavailable) | 825 (unavailable) | 116,719,929 (unavailable) | 933 (unavailable) | unavailable (unavailable) / unavailable |
| WO-126/reviewer | unavailable (unavailable) | unavailable (unavailable) | 116 (unavailable) | unavailable (unavailable) | 119 (unavailable) | unavailable (unavailable) / unavailable |
| WO-126/release-close | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) / unavailable |
| WO-126/planner | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) / unavailable |
| WO-126/refuter | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) / unavailable |
<!-- dotln-process-meter:end -->
