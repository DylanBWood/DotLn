# FINAL-001 — WO-126 Process debt: the lifecycle machinery costs more than the code it guards (v0.17.0)

**Verdict: pass.** All eighteen acceptance criteria, the three operator expansions recorded in the execution record and the eight ideation breakouts are met at the current subject, and the seven-report verification sequence is coherent: every finding of VER-001 through VER-006 is answered by the repair that follows it and re-established by the next report, and VER-007 passes with three findings it rates non-blocking. I re-established the load-bearing mechanisms first-hand rather than inheriting them: the classifier on the built module and on the pinned snapshot the hooks import, the attribution predicate, the legacy cost-header migration, the ledger and control-segment invariants, the discovery record's semantic changes, the meter and the version-line channel in my own session. No finding blocks this merge. VER-007's F31, F32 and F33 are carried forward as sourced follow-ups under decision D029, which this review records so they reach the planning follow-up register instead of staying inside a verification report; the observations below are recorded for their own sake and none is a defect in this order.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.268","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

This session runs Claude Code `2.1.268`, model `claude-fable-5-1`, at effort `max`, the model and effort the order names for every role and above the reviewer's `any` minimum; the operator selected them with `/model` before dispatching `resume: final review`. The version comes from `CLAUDE_CODE_EXECPATH` (`…/claude/versions/2.1.268`), `AI_AGENT` (`claude-code_2-1-268_agent`) and the session hook's own observation written to this session's state, `{"value":"2.1.268","channel":"ancestor-executable-lsof"}`, which is criterion 2's live channel. The observed Claude line in `docs/discovery/environment.json` is `2.1` at `newestPatch 2.1.263`; this version sits on the line, so the lifecycle gate records `effort max` where WO-042's reviewer on `2.1.265` recorded `unknown`, and the session hook printed no off-line warning. `CLAUDE_EFFORT=max` is exposed to this session and agrees with the launch claim, but the discovery record carries no Claude Code readback row, so the effort is self-reported rather than harness-readback. No account label was supplied.

## Subject and instrument

- Authority: [WO-126](../../work-orders/WO-126-process-debt.md) with its cited sections and appended execution record, including the operator's one-seam exemption, the budget clarification, the follow-up-register and planner-context expansions, the Node-only build decision, the VER-001 F6 cost correction, the four-part gate-efficiency expansion, the D010 invocation correction and the D022 completion-command correction; the two ideation receipts ([context continuity](../../evidence/WO-126/ideation.md), [implementation alternatives](../../evidence/WO-126/ideation-alternatives.md)) with the product sections they promote; the six repair receipts; and decisions [D001 to D028](../../evidence/WO-126/decisions.md), to which this review appends D029.
- Subject: the uncommitted tree on branch `wo-126` at `HEAD` `d0293dccb01d7303fe4b8e03a55dbcf1c40efd42`, the registered sibling worktree of the main checkout, with physical cwd and Git root agreeing. Against `HEAD` the tree carries 220 modified and 3 deleted tracked files (8,189 insertions, 3,376 deletions) and 89 untracked files before this review's own artifacts.
- Integration: `main` is also `d0293dc`. `git log main..HEAD` and `git log HEAD..main` are both empty, so no branch commit precedes this review and no integration was required or performed.
- Allocation: `FinalReviewRequested` at 2026-09-11T01:04:27Z, checkpoint `5a12bbd3a4b8ff3cab26f8468f18775d58533c67` (`refs/dotln/checkpoint/WO-126/31`). Status selected WO-126 in `final-review` with `final-review-result` as its sole legal next action.
- Verification sequence, all reports immutable: [VER-001](../../verifications/WO-126/VER-001.md) (fail, F1 to F9), [VER-002](../../verifications/WO-126/VER-002.md) (fail, F10 to F16), [VER-003](../../verifications/WO-126/VER-003.md) (fail, F17 to F20), [VER-004](../../verifications/WO-126/VER-004.md) (fail, F21 to F23), [VER-005](../../verifications/WO-126/VER-005.md) (fail, F24 to F28), [VER-006](../../verifications/WO-126/VER-006.md) (fail, F29 to F30), [VER-007](../../verifications/WO-126/VER-007.md) (pass, F31 to F33 recorded). Six bounded repairs sit between them, each recorded as `RepairRequested` and `RepairCompleted` with a receipt.
- Actor independence: implementation and all six repairs were attested to Codex CLI (`0.153.4`, then `0.154.0`), `gpt-6-astra`, effort `max`, operator-attested; VER-001 ran on Claude Code `2.1.266` with `claude-opus-5[1m]` at `max`, and VER-002 through VER-007 on Claude Code `2.1.267` and `2.1.268` with `claude-fable-5-1` at `max`. This review runs the same model and harness family as the last six verifications in a separate session; it is independent of the executor and of each verification session, not of the model.
- Instrument disclosure: this session runs under the bundle it reviews. The generated `UserPromptSubmit` hook resolved `resume: final review` to the reviewer role and injected the selected order path; the generated `PreToolUse` guard refused three of my Bash commands because they used `for` loops, each with the classification reason `Shell control syntax requires an explicit effect adapter`, and I rewrote them rather than weakening the guard; the write and read observers recorded my authorship, and the meter counted those three refusals. This report is direct evidence that the self-host works and is not evidence for the recorded feedback, authority or live smokes, which separate sessions produced.

## Completion actors against the declared minimums

The order declares executor `max+`, verifier `xhigh+` and reviewer `any`, with Claude Fable 5.1 at max or Codex gpt-6-astra at max named for every role. Every completed actor below meets its effort minimum; the status projection reports a single effort value, `max`, with no within-order drift. Headers quoted from the verification reports sit in table cells so that only this review's own header starts at column one.

| Completion | Control actor | Report header | Minimum | Result |
| --- | --- | --- | --- | --- |
| `ImplementationReady` 2026-09-10T00:51Z | codex-cli 0.153.4, gpt-6-astra, max, operator-attested | executor receipt names the same | executor max+ | met |
| `RepairCompleted` ×2 (VER-001, VER-002 repairs) | codex-cli 0.153.4, gpt-6-astra, max, operator-attested | repair-001, repair-002 receipts | executor max+ | met |
| `RepairCompleted` ×4 (VER-003 to VER-006 repairs) | codex-cli 0.154.0, gpt-6-astra, max, operator-attested | repair-003 to repair-006 receipts | executor max+ | met |
| `VerificationCompleted` VER-001 (fail) | claude-code 2.1.266, claude-opus-5[1m], max, self-reported | `**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.266","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}` | verifier xhigh+ | met; the model is not one the order names and the report discloses it |
| `VerificationCompleted` VER-002 to VER-006 (fail) | claude-code 2.1.267, claude-fable-5-1, max, self-reported | `**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.267","model":"claude-fable-5-1","effort":"max","source":"self-reported"}` in each | verifier xhigh+ | met |
| `VerificationCompleted` VER-007 (pass) | claude-code 2.1.268, claude-fable-5-1, max, self-reported | `**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.268","model":"claude-fable-5-1","effort":"max","source":"self-reported"}` | verifier xhigh+ | met |
| `FinalReviewCompleted` FINAL-001 (intended) | claude-code 2.1.268, claude-fable-5-1, max, self-reported | this report's header above | reviewer any | met |

## Acceptance re-established

| Criterion | What I checked and observed |
| --- | --- |
| 1 — attribution | Called `hasAiAttribution` from the built compiler on eight lines: a `Claude-Session` trailer, a `Codex-Session` trailer, a `Session-URL` trailer, a bare `claude.ai/code/session_` link, an AI coauthor and a generated-with footer match; a human coauthor and ordinary prose naming a session link do not. `.claude/settings.json` carries the declared `attribution` block; `worktree publish` screens the title and body through the same predicate before `ensureGh` and before any push, and I ran that predicate over this review's PR body, release notes and title. 08 §PRs and commits records the decision and the parsed-invocation rule. |
| 2 — attestation | Compared the discovery record before and after as JSON: the only semantic changes are the appended observed `2.1` line for Claude Code, the appended `0.152`, `0.153` and `0.154` lines for Codex and two appended Codex version rows from the bounded probe; the rest is pretty-printing. `effortHarnessEvidence` accepts any patch on an observed line; this session's `2.1.268` was observed through the verified ancestor's `lsof` text mapping and recorded `effort max`. WO-042's control segment has no diff against `HEAD`. |
| 3 — read obligation | Read `harnessOutputObligations` for my own session state: before this report it owes byte reads of exactly the paths I changed (the two control files written by the allocation, `decisions.md`, `PR.md` and `RELEASE-NOTES.md`, 99,782 bytes) and checks for the regenerated index (`suite:index`), the decisions index (`suite:meta`) and the 196,603-byte follow-up register, while the 264 inherited changes owe nothing. With this report the reviewer's obligation is six byte reads and three checks, against WO-042's 135 files and 5,305,410 bytes; the transition's evidence row records the exact figure. `.gitattributes` marks the generated index, every evidence JSON and JSONL, the console fixtures, `packages/*/dist`, the hooks, skills, manifest and settings. |
| 4 — advisory Stop | Only `finish.mjs` is wired at Stop; `protocolRefusal` returns a `systemMessage` for Stop and never `decision: block`; the writer is released in a `finally`. This session ended several turns with unmet obligations and was never refused. The four transition commands share `requireLifecycleEvidence`, which refuses without the full gate, the diff check, authored-output reads or checks and no remaining work at the current tree. |
| 5 — evidence by tree hash | `gateTreeHash` computes Git's own tree object identity over staged and untracked files with attribute-aware hashing; `findGateCheck` accepts any executed green row at the hash from any session and a failed attempt never erases a success; the hot cache holds 256 rows with tree-addressed archives and addressed failure logs; a timeout is recorded as an executed failure naming the suite and its elapsed time. VER-001 reproduced identical-tree reuse live (667 s to 0.62 s), VER-003 and VER-006 observed composed reuse across document-only changes in Claude sessions, and every `RepairCompleted` and `VerificationCompleted` row carries a `treeHash`. |
| 6 — two gates with numbers | The runner declares every command of the previous 37-step chain (fixture-proven against the activation `package.json`), a build barrier, a CPU-derived cap, the package serialization group, exclusive `harness-fixtures` and `process-debt`, generated-file preflights, split candidates above 30 s, negative-fixture lines under their suite, `--only`, `--serial`, `--fresh` and the document gate. Idle cold fast gates across the sequence ran 103.64 to 116.32 s against 120 s, 109.6 s in the latest recorded run; full gates ran 476 to 668 s cold and 189 to 230 s composed. The build stages under `.runtime/build-*` and replaces files atomically while the hooks import the pinned snapshot `a4a65a94d36a276d`, which is one of twelve retained under `.runtime/harness/`; the atomic-build fixture invokes an installed hook at every boundary. F33 records that the full gate can fail an intermittent suite under its own concurrency on this host; see below. |
| 7 — closeout | `classifyIgnoredMaterial` treats `docs/control/local/harness/**` and `.runtime/**` as disposable and the rest of `docs/control/local/` as retained and release-evidence-allowed; `worktree finish` previews reconciliation, excludes reconciled sources from the ignored-material refusal, prints the receipt, records the subject's gate checks into main and refuses anything but `--dry-run`; `release close` delegates to it, reuses full-gate evidence for an identical tree and previews with `--dry-run`. The closeout fixture covers subject and main harness state, `terms.txt`, new intake, a differing-byte collision, an unchanged preview and a symlink escape. `.gitignore`, `docs/README.md`, 03 and 07 say scripts read and write the lane, and the release-close skill forbids a closeout script. Not executed live, as in every verification; the release-close dispatch will exercise it. |
| 8 — planning branch and the guard | Classified twenty shapes on the built module and on the pinned snapshot with identical results: bare, path-qualified and `exec`-wrapped pushes deny; a shell fed a piped program, a `python3 -c` one-liner, `git rebase -x`, a `git -c diff.external` value and an assignment expanded into `find -exec` refuse as opaque programs containing a denied invocation; `npm exec -- npm publish` is `package.publish`; a `gh api` body is remote and a bare `gh api` read is admitted; `grep -rn 'git push'`, `git commit -m 'Refuse git push inside hooks'` and `git log --grep='git push'` stay `shell.run`; `npm run resume` and `npm run worktree -- publish` classify `lifecycle.run`; `ssh -V` is `transport.ssh`; `cat .env` is `credentials.access`; a `for` loop refuses. `plan start` is allowlisted in `metadataCommand` and creates `planning/<date>-<slug>` only from clean `main`; `harnessToolEffects` refuses unclassified and spawn-class tools. F31 and F32 are the two edges VER-007 left open and are carried forward. |
| 9 — units | `personalFeedbackUnits` derives version 2 from every retained version-1 unit: the four prose handlers carry `kind: prose`, no required evidence and the operator's Undesired and Desired sentences for anti-oscillation with the worked example; the three Stop handlers are `advisory`; every version-1 definition is retained in `retainedFeedbackUnitsV1` with `supersedes`. All twelve installed skills carry the rule and the example (I read the regenerated diffs of both profiles). `CLAUDE.md`'s residue block is one line naming the manifest and the file is 2,251 bytes against 3,593 at `v0.16.0`. |
| 10 — budget | `docs/control/budgets.json` holds the five cold-start ceilings, `fastGateMs 120000`, `readCapBytes 65536`, `sequenceBytes 8192`, `passRefutationMs 120000`, null token, dollar and PR-body caps with an explicit unset policy, and an empty acceptances list. `npm run meta -- --check` reports every cold-start profile within (reviewer 8,989 of 14,336 in the Claude profile), the sequence file at 3,735 of 8,192, the fast gate at 109,615 of 120,000 ms and no breach; `measureColdStarts` counts the installed floor plus skill only. |
| 11 — meter | `npm run meta -- --check` prints the five closed orders and WO-126 with phase durations and attempts, gate seconds, read obligations, observed tokens beside declared prompt tokens, cold-start bytes, manifest counts, artifact sizes, subject lengths, budget verdicts and the five trap rows; `resume status` ends with `Process health: no observed budget breach; WO-126 tokens 196,661,231; 0 reopen candidates; unset limits remain unset.`; `release prepare --local` wrote the table into `PR.md` and reported the `v0.17.0` target unchanged; `meta-baseline.json` is the immutable first observation. |
| 12 — headline titles | The reviewer skill I loaded carries the headline rule verbatim; `worktree publish` prints the last five merged subjects with their character counts beside the proposed title immediately before the push; no numeric rule refuses a length; the meter carries the series. This review's title is one clause of 101 characters written from the diff. |
| 13 — ledger scope | `decisions.md` holds D001 to D028 from the executor, repairs and operator corrections, each with dispatch, evidence, rejected alternatives and reopening condition, seven of them typed corrections in the misread, meant and changed form; this review appends D029. `npm run meta` regenerated `docs/lineage/decisions-index.md` with the new row and synced the follow-up register, which gained the decision as an untriaged entry, proving the collector. The ledger diff is 175 added lines under two ideation headings and no deletion; no lifecycle loadout directs a ledger append; the work-order index marks the inherited-duty substitution on 108 orders. |
| 14 — write-backs land | Read the diffs of 02 §Feedback compiler v1, 03 §Corpus policy and the control migration note, 04, 05, 07 §Read order, §Operator-opened planning pass, §Retained planning follow-ups, §Workflow closeout and releases, §Discipline and §Model-specific notes, 08 §PRs and commits, the playbook, both readmes, the refutation readme, the capability table, the template, the publication index and both edition locks: each carries the changes the criterion names, the unsourced no-telemetry sentences are replaced by the recorded-per-dispatch rule, and both profiles' skills are regenerated. |
| 15 — refutation is one phrase | The session hook resolves `planning: refute` and `planning: refute full` to the refuter role, whose skill is installed in both profiles and directs no read but the canonical prompt; `plan refute --direct` verifies committed equals workspace, computes the pass or full scope with carried verdicts by order and receipt hash and prints `planPrompt`; `plan receipt` validates, screens against local terms, files the immutable pair with a checked `review` object and the 120 s pass budget, commits with a plain subject and runs the plan check; the transport route reports the CLI's exit code and stderr. Twenty-three planning cases cover the direct route, a stale subject, a malformed result, a carried verdict and the cost holds. |
| 16 — the four questions | All 124 orders carry a `Cost:` header, the 123 legacy orders with exactly the two-line unavailable declaration and no other change (verified mechanically over every file); `buildPlanSubject` includes the line and a bounded subject-hashed cost table and refuses stale evidence; `validatePlanResult` holds a missing or unbalanced cost; every role skill opens with the cost line; the meter records wall-clock, context bytes, commands, tokens and steps per dispatch where exposed. |
| 17 — systems-trap lens | The meter prints one row per trap with the criterion's signals and previous-order deltas (`machineryShare 0.302`, `guardRefusals 9` after this session's three refusals, `operatorCorrections 6`, `coldStartBytes 12,046`, `subjectCharacters 91`); three consecutive worsening deltas raise a reopen candidate; 07 §Discipline names the lens and 05 preserves the eight-trap extension as a candidate. |
| 18 — final evidence | `git diff --check` is clean at the reviewed tree; no dependency is added and `package-lock.json` changes only by three workspace versions and the skeleton's compiler pin; no lint, type or formatter suppression appears in any added or new line; the manifest and all nine hooks pin `a4a65a94d36a276d`; feedback edition `002` is fresh under compiler `0.9.0`; the receipt records this order's phase durations and fast-gate seconds. The full gate at the tree carrying this report is `npm run harness -- evidence`, recorded by the `FinalReviewCompleted` evidence row rather than edited into this report, under the write-once rule (D014, D022). |

## Operator-expanded execution duties

- **Ideation breakouts.** Both receipts name their ignored captures by path and SHA-256, record the clean-room screen and the Shape-First treatment, and list what a reviewer must read. The promoted sections exist and are labeled candidates: 05 §Candidate — Context Continuity, §Candidate — all eight system traps, §Candidate — Tinkerer / Scientist with its adaptive follow-up, §Candidate — historical comparison after a forced alternative, §Candidate — retain unchosen alternatives, §Candidate — emoji shorthand; 04 §Candidate — browser and desktop hosts for a resident 3D view; 07 §Candidate — recurring review of implementation alternatives and §Candidate — planner startup context; 03's continuity paragraph; and the two ledger sections. Each claims no runtime, dependency, schedule or allocation, and the emoji table matches its receipt with token counts stated as unmeasured. I hashed nothing in intake: only `.gitkeep` files are tracked under `docs/intake/` and no intake byte appears in the diff or the untracked set.
- **Follow-up register.** `docs/planning/followups.json` retains stable identities with source revisions and dispositions; the paged feed is bounded to eight rows and 8 KB; local executor deferrals need a live public target; the metadata and document gates check freshness. This review's D029 entered the register through `npm run meta`, which is the collector working on a real decision.
- **Gate-efficiency expansion (D009, D010).** Reviewed input scopes, the sealed release template with forty named cases, live progress and the PATH deduplication are in the runner and its fixtures; the optimization receipt separates the confounded direct-invocation trial from the canonical npm measurements; composed reuse was observed live by VER-003, VER-006 and VER-007.
- **The D022 correction.** The shared executor, verifier and reviewer instructions name `npm run harness -- evidence` as the single completion command, and 07 §Discipline carries the rule with D014 and D018. This session followed it: no gate at any turn end, one completion gate at the final tree.
- **Budget clarification and the floor sentence.** Token, dollar and PR-body caps are null with the unset policy (D001); the hand-written floor sentence was replaced under the operator's explicit same-session authorization (D002).

## Open findings carried forward

Recorded as [WO-126-D029](../../evidence/WO-126/decisions.md#wo-126-d029) with reopening conditions; none opens a denied route on the observed host and none blocks the merge.

- **F31 (minor).** `git commit -am '…'`, `git tag -am`, `git commit -F -` with a heredoc, `git log --author=` and `--format=`, and `git grep -C2` or `--color` with a pattern naming a denied command are refused conservatively; the attached `-am'…'` form, `-a -m`, the bare pattern forms and every control VER-006 named are admitted. I reproduced the classification boundary in reading `floorOperands`: only the enumerated message and pattern spellings are removed. The cost is a retry that the meter now sees.
- **F32 (minor).** `sed '1e …'` and `less '+!…'` classify `shell.run` on both modules because `sed` and `less` are data programs and the floor is skipped for them; I confirmed the `sed` shape. The `v0.16.0` regex refused both lines, so this is a floor edge; on this host BSD `sed` rejects the `e` command and `less` on a pipe ignores `+!`, and the interactive route could not be tried without a pseudo-terminal. It executes on a host with GNU `sed`.
- **F33 (moderate).** VER-007's completion gate ran 78 tasks fresh in 601.5 s and `console` and `harness-fixtures` failed on fixed subprocess timeouts under load, the same two shapes the VER-006 repair receipt recorded; the composed retry the transition records reused the 35 green suites. D012 moved the hook-heavy fixtures into the exclusive group, and D022's retry-with-reuse absorbs the cost without making it visible. I judged this with VER-007: a recurring cost of the runner, not an unmet criterion, and the right home is the next planning pass with the reopening condition D029 states.

## Observations, not findings

- **The order's own `Cost:` header is superseded by its execution record.** The header still says the order adds one fast gate run per turn end; D022 removed that requirement and the execution record says so, while the header keeps its planning wording so the refuted subject hash is preserved (D004). A reader should take the execution record's cost correction and D022 as current.
- **VER-001 names this machine's home-directory path once.** Sixteen already-committed verification and final-review reports carry the same path, including WO-042's FINAL-001, so this is the repository's existing practice rather than a new exposure; the immutable report was left as written and this review avoids adding another instance. The operator may want a redaction rule for future reports.
- **The discovery record is now pretty-printed.** Its 823 added and 101 removed lines reduce, as JSON, to the appended version lines and two Codex rows; nothing observed was removed or reclassified.
- **Usage collection reads local transcript headers.** `npm run meta -- --collect` scans the first 64 KB of each Codex session file for the day range to find sessions whose `cwd` is this repository, and the Stop finalizer reads the Claude transcript named by the hook input; both record only counts into the ignored `docs/control/local/process/usage.jsonl`, and the meter publishes totals, not text. This is the operator's D001 direction and stays local, but it is the one place the harness reads outside the worktree.
- **Twelve pinned runtime snapshots sit under `.runtime/harness/`,** one per regenerated bundle across the order; they are disposable at teardown and nothing prunes them yet.
- **The generated work-order index and the PR meter block change at every transition.** I regenerated the index after allocation and will regenerate both after `final-review-result` records so the committed body carries the reviewer's own phase duration; both are generated outputs whose check the full gate satisfies.
- **This session's classifier refusals were all `for` loops.** Each cost one rewrite; the refusal names its reason, which is what VER-001 F3 and VER-002 F11 asked for.
- **A deduplicated Read produced the catch-all refusal.** When the Claude client short-circuited a repeat Read of `decisions.md` as unchanged, it still ran the PostToolUse hooks with a response carrying no file content, and the read observer refused with the `runtime-unavailable` class, which the client reported as a blocking hook error; the two real reads dispatched beside it were recorded with full byte ranges. No obligation was lost, the observer journaled the refusal, and the meter counts it as a guard refusal. A named reason for a content-free tool response would separate this from a destroyed runtime; recorded for the next planning pass, not repaired here.

## Executed evidence

- Classifier: `invocationEffects` over twenty shapes from `packages/skeleton/dist/src/harness-command.js` and from the pinned `.runtime/harness/a4a65a94d36a276d` copy, identical on every shape; `hasAiAttribution` from the built compiler over eight lines; the same predicate and `assertGitHubBodyProfile` and `parseReleaseNotes` over this review's PR body, release notes and title.
- Discovery: a JSON deep comparison of `docs/discovery/environment.json` at `HEAD` and in the working tree.
- Mechanical checks: `git diff --numstat` over all 123 legacy work orders (each exactly +2/−0) and a filtered diff proving no added line other than the cost header and its blank line; `git diff --numstat` on the ledger (175/0) and its added headings; `git diff --stat` on WO-042's segment (empty); `git ls-files docs/intake`; a suppression search over added tracked lines and every new source, test and document file; `wc -c CLAUDE.md` against `git show v0.16.0:CLAUDE.md`; `git log main..HEAD` and `HEAD..main`; `git diff --check`.
- Meter and lifecycle: `npm run meta -- --check` and `npm run meta` (writing form, for D029); `npm run resume --silent -- status --json` before and after allocation; `npm run resume -- final-review`; `npm run work-orders -- index` after allocation and after the artifacts; `npm run release -- prepare --local`; this session's harness state through `harnessOutputObligations`; the control segment's completion actors and evidence rows.
- Read at current bytes: the full tracked diff and every untracked addition outside the generated evidence JSON, including the classifier, harness host, gate evidence, usage observation, runner, suite evidence, release fixtures, meter, build, discovery, closeout, follow-up and direct-refutation modules, the compiler, skeleton, console and script diffs, the fixtures' named cases and the closeout and build fixtures in full, the regenerated hooks, skills, manifest and settings, all seven verification reports, the six repair receipts, both ideation receipts, the optimization receipt, the evidence README, D001 to D028, the product, planning, playbook, readme, ledger and publication diffs, the sequence, archive, follow-ups and template files, the budgets file and the control segment.
- Completion: `npm run harness -- evidence` at the tree carrying this report, whose `npm run test:full` and `git diff --check` rows the `FinalReviewCompleted` evidence entry records.

## Publication artifacts

[PR.md](PR.md) and [RELEASE-NOTES.md](RELEASE-NOTES.md) are prepared beside this report under the current publication contract, one physical source line per prose paragraph and list-item paragraph, checked with the repository's own `assertGitHubBodyProfile`, `parseReleaseNotes` and `hasAiAttribution` and formatted with Prettier. The release notes carry the five required sections; read-before-upgrading names the split gates, tree-hash evidence, advisory Stop, authorship-observed read obligations, the rewritten envelope with its two carried limits, session-trailer refusal, version-line attestation, the atomic build, the scripted closeout lane, the ledger scope change, the planning sequence and cost contract, the prose units and the moved feedback edition. The PR body carries the meter table that `release prepare` writes. The title is a headline written from the diff, `:zap: Gates run once per tree and Stop only advises, so an order stops paying hours for its machinery`, and the publish step prints the last five merged subjects beside it.

## Limits of this review

This session is not an independent instrument for the parts of the subject it runs on: the generated hooks, skills and instruction block judged my own tool calls while I reviewed them, and the meter counted my session. I paired the order's generators with direct classification on both modules, a JSON comparison of the discovery record, mechanical diff invariants and source reading, but the closeout helper, `plan start`, a real refutation, `npm run discover -- harness`, the Codex explicit adapter and `--only` and `--serial` were not executed live here; their evidence is the fixtures that passed in every verification gate and the earlier reports' live runs. VER-001 through VER-006 findings were not individually re-run beyond the fixtures that cover them and the twenty shapes I classified. The classifier remains a bounded literal adapter; a read receipt witnesses delivery, not comprehension; a prose unit claims instruction delivery, not behavior. This review runs the same model as six of the seven verifications, so it adds a separate session and a different role, not a different model. This review commits the reviewed state, pushes the `wo-126` branch and opens its pull request; it merges nothing, publishes no release and changes no account or tool setting. The branch is ready for operator review.
