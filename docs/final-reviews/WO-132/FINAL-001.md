# WO-132 FINAL-001 — the machinery stand-down: one product gate per order, keyed by the code it tests

**Verdict: pass.** The lifecycle now records a transition the moment its report exists and consults no gate; the one product gate runs fresh, once, keyed by tracked non-generated code, and its row is what the pull request, the tag manifest and the release close consume; attestation, version and effort are logged, never refused; the hooks refuse exactly a second writer and a live-gate input write; the refuter is a goal review; and the machinery's own suites left the default gate. Thirteen of fourteen criteria are met at the final bytes and the fourteenth is superseded by the operator's recorded execution amendment. Every finding of the four failing verifications is repaired and VER-005 rejected each counterexample at this code identity. This review found no defect that a criterion charges. The reviewer's single `npm test -- --review` ran at this code identity and passed; its row is reproduced below and carried in the completion event.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.272","model":"claude-fable-5-1","effort":"xhigh","source":"self-reported"}

Dispatch: `resume: final review`, recorded by the harness as `FinalReviewRequested` at 2026-09-15T16:38:57.167Z with FINAL-001 allocated at this path from checkpoint `refs/dotln/checkpoint/WO-132/21`; the dispatch was not repeated here. The harness version is `claude --version`. The model identifier is the session's own. The effort is self-reported: the session's `/model` output named `xhigh` and the environment exposes `CLAUDE_EFFORT=xhigh`, but neither is a vendor effective-effort readback, so none is claimed. The order declares `reviewer any`. This session held the worktree's only writer reservation (`writer --show`: reserved, alive); three read-only forked reviewers widened coverage over the docs and generated surfaces, the tests, and the release, worktree and refuter code, and every finding they returned was re-read by me at the source. No operator control was opened.

**Bypassed requirements: none.**

Instrument disclosure: the machinery under review also recorded this review. My dispatch, checkpoint and this report's completion event run through the changed `resume.mjs` and `lifecycle-evidence.mjs`; my gate row was written by the changed runner and keyed by the changed `gateCodeIdentity`; my writer reservation is the changed `writerIsolationFacts`; and the live-gate guard under criterion 9 judged my own commands during the gate. Where a claim rests on that machinery I say so and name the independent check beside it.

## Subject and integration

The subject is [WO-132](../../work-orders/WO-132-machinery-stand-down.md), criteria 1–14, with the 2026-09-15 execution record that superseded criterion 14's eight-hour cutoff, and the working diff on `wo-132` at base `0cef8fe1037fe14a8b0dedabc11de2ee326adb8f` (HEAD equals `main`; no branch commit preceded this review): 128 tracked files changed (8,020 insertions, 8,193 deletions), two new source files staged (`scripts/lib/evidence-sources.mjs`, the console `feedbackUnits-wo132.json` fixture), and the untracked control segment, evidence directory, five verification reports and this directory. The application target is v0.18.0 with compiler 0.10.0, skeleton 0.16.0 and console 0.1.6; kernel 0.2.1 is unchanged; the lockfile diff is first-party versions only; no dependency and no user setting is in the tree. `origin/main` at review time equals the base, so no merge, conflict or retiming was needed.

Code identity of the reviewed tree, computed by the built `gateCodeIdentity` before the gate: `8e24bc8c482b8a8f02830b201ac6d2b02c53c1baeb14660decd304ed678e2a43`, equal to the identity VER-005 gated and to repair 004's recorded final identity. The exact tree differed from VER-005's (`280065cc…` against `102da4ee…`) by the dispatch event, its projections and the index refresh alone, which is criterion 2 observed live.

Read scope: the order and every cited section of product 07, product 08 §PRs and commits and the planning diagnosis; `lifecycle-evidence.mjs`, `gate-evidence.mjs`, `harness-command.ts`, `harness-host.ts`, the compiler's `feedback.ts` and `harness.ts`, the contributor, feedback, plan-refuter and process-cost loadouts, `worker-protocol.ts`, `worker-transport.ts`, `resume.mjs`, `test-runner.mjs`, what remains of `suite-evidence.mjs`, `meta.mjs`, `evidence-sources.mjs`, `evidence-editions.mjs`, the three evidence scripts, `control-actor.mjs`, `control.mjs`, `harness-context.mjs`, `usage-observation.mjs`, `dotln.ts`, the console parser and blueprint, the package manifests, the releases and verifications READMEs, and the order's own diff; VER-001 through VER-005; the executor validation, the four repair records and decisions D001–D008; refutation receipt 013 and its dispositions; and the existing PR and release-note drafts. The forks read the remaining docs, the generated hooks, skills and manifest, every changed test and shell fixture, `release.mjs`, `worktree.mjs`, `release-records.mjs`, the plan helpers and the refutation protocol.

Ideation receipt: the order carries no operator-opened ideation section. Its nomination provenance is the operator's 2026-09-15 planning dispatch and three follow-ups in ignored intake (SHA-256 `19061ea6…`), the planning diagnosis, and refutation receipt 013, which judged WO-132 `machinery` with no hold and four known issues while holding on a WO-130 counterexample carried from receipt 012; the operator overrode that hold by direction in `f2c8d35`. This review held the implementation against the receipt's four known issues: (1) a merge tree whose code identity differs from the reviewed row now refuses at `release-records.mjs:216` and in manifest validation; (2) of the 23 tracked files under `.claude/` and `.agents/`, 22 are emitted from source and checked by `harness check`, and the remaining one, `.claude/settings.json`, is the host deny list whose declared attribution block that check also reads, so a change there is a permission-boundary change under the guide's own rule rather than a silent key miss for a product suite; (3) design-stage defects did move to the verifier, which found four across VER-001 to VER-004; (4) a verification pass without a product row is legal and occurred (VER-003 and VER-004 recorded no row at their identity), and the reviewer's gate then supplied the evidence release close requires.

## Verification sequence

| Report | Verdict | What it charged | Answered by |
| --- | --- | --- | --- |
| VER-001 (Claude, xhigh) | fail | F1 `npm run meta` captured only the first line of each wrapped Cost paragraph, so the promised removal and any shortfall were unreachable; F2/F3 the live-gate guard refused read-only `ls`, `head` and `grep` and no fixture could catch it | D004: paragraph-boundary extraction with a wrapped-authority regression; D005: the three utilities admitted with read and write fixtures |
| VER-002 (Claude, xhigh) | fail | F1 `>&word`, `1>&word` and `>>&word` recorded an `&`-prefixed destination that the unanchored ignore rules classified as scratch, admitting writes into `packages/*/dist` and `./node_modules` during a live gate; F2 the fixture targeted only tracked files | D006: descriptor-duplication branch resolving the real operand; hook-fixture denials against the protected destinations |
| VER-003 (Codex, max) | fail | F1 zsh's `>>&` with a numeric or `-` operand was treated as a descriptor operation and admitted | D007: append and duplication distinguished; seven commands pinned in parser and hooks |
| VER-004 (Claude, xhigh) | fail | F1 zsh clobber-override forms (`>!`, `>>!`, `>&!`, `&>!` and descriptor variants) recorded a `!`-prefixed destination and were admitted into the same targets | D008: a shared literal-operand boundary; any operand not beginning with a plain path character is opaque; ten subjects pinned in parser and hooks |
| VER-005 (Codex, max) | pass | rechecked every earlier finding at `8e24bc8c…`; product gate 252.17 s; fourteen criteria judged | this review |

VER-001 also measured three consecutive fresh product gates of 240.42, 243.16 and 243.62 s against the executor's 254.91, 250.50 and 246.16 s and the recorded 605 s baseline. No source byte changed after repair 004; I verified the identity equality above before running anything.

## Independent checks executed

- `git diff --check` and `git diff --cached --check`: clean before the gate and again after staging every reviewed output.
- `node scripts/harness.mjs check`: 24 generated surfaces pass for compiler 0.10.0 and skeleton 0.16.0; the Claude and Codex role skills are byte-identical in all six pairs and each carries the two kept invariants (fork-verified, re-read by me in the compiler template).
- `npm run publication:check`: both edition locks current. `npm run format:check`: clean. `node scripts/authority-evidence.mjs --check`: the plan-refuter migration verified with its authority envelope and effects unchanged. `node scripts/meta.mjs --check`: advisory only; three budget breaches print as planning input and nothing refuses.
- `npm test -- --list`: nineteen product and lifecycle suites, each with a specific `protects:` line. `--machinery --list`: exactly the fifteen suites criterion 5 names. `--review --list`: the nineteen plus the eleven machinery suites whose declared sources this order changed.
- Immutability: `git status` over the WO-126 through WO-131 and WO-044 evidence, verification and final-review directories is empty; the planning receipts are unchanged, only their README moved. The order file gained only its execution record.
- Live guard, observed against this session during my own gate: a `for` loop, a `sed -n` pipeline, a `wc -c <` redirect and `git ls-files` were refused with the gate's run id and the stop instruction; `ls`, `head`, `cat`, `grep` and the Read tool were admitted; writes to the scratch directory outside the worktree were admitted. That is the guard D005 and D008 describe.
- Source reading against the criteria: `requireLifecycleEvidence` throws only on a failed inline diff check and otherwise advises; `resume.mjs` still refuses an illegal phase and a missing or mismatched attestation header, and `parseActor` validates presence only; `findGateCheck` matches `npm test` by code identity and everything else by exact tree; `evaluateHarnessHook` returns a refusal only for a foreign live writer and for `activeGateWriteRefusal`, and an advisory with a journal row for every other judgment; the residual `branch === "main"` clause in `feedback.ts` is reachable only when no version 2 unit is compiled, which the equipped program is not; `canonicalWorkerArgs` and the CLI transport constructors write advisories for unrecorded efforts and versions and launch; `changedMachinery` selects by tracked diff against the base; `close` proves egress before `updateMain` and spawns no suite, `npm ci` or smoke row; `worktree publish` screens attribution and refuses a missing row or a differing identity before any push.

## The reviewer's gate

`npm test -- --review`, started 2026-09-15T16:47:47Z after the two new source files were staged and with no source edit by this review: **31 suites passed, 0 failed, 840.16 s, 74 fresh tasks, 0 reused**. That is the nineteen product and lifecycle suites plus the eleven machinery suites whose declared sources changed since the base; harness-fixtures (281.66 s) and process-debt (163.67 s) ran under exclusive scheduling, plan-refutation took 175.09 s and runner-fixtures 31.97 s, and the 43 release cases ran across four lanes. The row was recorded at 2026-09-15T17:01:49.122Z as `host-gate:8e24bc8c482b8a8f02830b201ac6d2b02c53c1baeb14660decd304ed678e2a43:npm test` with exact tree `280065cc1205fc0964228f3ac54cb78c1adfc4cc` and exit 0, and `findGateCheck` resolved it at the current code identity with no gate left live. This run is not a criterion-6 measurement: `--review` adds the machinery suites by design, and the six product-only rows above are the criterion's evidence. This report, the PR body and the release notes were written after the row, which criterion 2 permits; the completion event cites the row by code identity.

## Acceptance

| Criteria | Review result |
| --- | --- |
| 1: transitions stop gating | Every completion runs the inline diff check and advises; illegal phases refuse. Observed on the order's own control segment: seventeen events, none consulting a gate row. Met. |
| 2: one gate keyed by code identity | One identity across the exact trees of VER-005, my dispatch and my gate; docs, control, index and release-text edits leave it; a source edit moves it (runner fixture and my own computation). The PR body will carry the row. Met. |
| 3: one gate at final review | No transition is a gate precondition; the composed-evidence release case runs the whole lifecycle with exactly one gate; the reviewer's gate is the one this order paid at review. The verifiers' optional gates were role choices, as the order allows. Met. |
| 4: replica, keys, cache, probe and expansion leave | The three modules and their tests are deleted with no live reference; `suite-evidence.mjs` keeps only the child environment and coverage helpers; exclusive scheduling restored for the two heavy fixtures. Met. |
| 5: `protects:` lines; machinery on demand | Nineteen specific lines; fifteen machinery suites on demand and selected into `--review` by their own sources; no product test deleted (every deleted test covered removed machinery or removed refuter rules). Met. |
| 6: three fresh runs under 360 s | 254.91, 250.50, 246.16 s (executor) and 240.42, 243.16, 243.62 s (VER-001) recorded beside the 605 s baseline; release cases ran on four lanes. Met. |
| 7: attestation as given | Presence-only validation; `ultra` and `ultra code` become `xhigh` with `mode: subagents` and `raw`; the release fixture completes with an unrecorded version and `--effort ultra`; Codex subagents mode keeps multi-agent enabled. Met. |
| 8: one writer per worktree on any branch | The main conjunct left the live predicate and the hook; the generated-hook fixture runs build, bootstrap, history and the release helper on main under one reservation, refuses a second live writer and reclaims a dead holder. Met. |
| 9: hooks refuse exactly two things | The two invariants hold and fail closed after four redirect repairs; every other judgment delegates with an advisory and a journal row; both harnesses carry identical role text and the shared instruction block holds the residue. Limit carried from VER-001 F2: inside a live gate the guard also refuses opaque shell commands it cannot bound, which is wider than the sentence and narrower than the protection. Met with that stated limit. |
| 10: release close publishes only | Egress first, fast-forward, surface checks, dist build when missing, manifest with both trees, tag and Release, then best-effort finish and settle; no suite, `npm ci` or smoke row anywhere on the path; a differing merge identity refuses; `--dry-run` previews without mutation. Established by the executable fixture and source reading; live publication is the release-close dispatch's evidence. Met. |
| 11: refuter is a goal review | Four questions, the platform standard quoted verbatim from product 07, three verdicts, holds only from supported findings of a `misaligned` order, known issues with reopening observations, one judgment per pass unless observed evidence changed, dispositions bound to criterion text, historical receipts byte-identical. Met. |
| 12: cost reconciliation | The whole wrapped Cost paragraph reaches the printed line; a measured shortfall prints `PLANNING INPUT`; unavailable durations stay `unknown`; nothing refuses. Met. |
| 13: write-backs, immutability, no dependency | Product 02, 06, 07, 08, the playbook, the security document, the refutation, releases and verifications READMEs and the publication locks state the contract with the old mechanisms in dated historical sections; the decisions file lists every removed mechanism with source and reopening condition, the rollback commands and the gate rows; the migration note is dated; no dependency. Met. |
| 14: rollback fallback | Commands recorded in D001 before implementation; the cutoff removed by the operator's recorded amendment; nothing executed. Superseded. |

## Observations carried forward, none blocking

1. **Criterion 9's sentence is narrower than the guard.** The guard refuses during any live gate in the worktree, whoever started it, and refuses opaque shell commands whose write set it cannot bound (VER-001 F2, narrowed by D005's three utilities). `docs/AI-HARNESS-SECURITY.md` and product 02 phrase it as the reviewer's live `npm test -- --review`. The behavior is the safe one and is documented in product 07; the two sentences could name the wider scope.
2. **A weakened negative assertion.** `scripts/test-worktree.sh` now checks the published PR body by containment rather than byte equality, so the fixture no longer proves that an uncommitted body file's bytes are absent from the publication. The committed-bytes rule still holds in `readTrackedTextAtHead`; the test lost its counterexample. Nominated for the next machinery pass.
3. **Two assertions without a successor.** The deleted WO-039 main-branch test also asserted that `harness.mjs read-output .env` and its `&& pwd` composition were hook-refused. `credentials.access` is still classified and now delegates to the host, as criterion 9 directs; no in-repo test pins that path.
4. **Fixture limits stand as disclosed.** The composed-evidence release case asserts exit 0 on every lifecycle command and exactly one gate row but keeps no refusal counter, and its `npm test` is the synthetic one-gate script around the real runner record; two `assert.doesNotThrow` lines in `worker.test.ts` cannot fail on their own, though the adjacent assertions check the emitted arguments.
5. **Goal-review receipts pass the plan check across a moved subject hash** when their holds are disposed; the continuation check bounds what may have changed. This is what criterion 11 asks for and is not stated in the decisions file.
6. **Earlier observations unchanged.** Root Markdown is outside the code identity (VER-001 O1); untracked files are outside it (O2); `ultracode` as one word is recorded verbatim with no normalization (O3); `lifecycleRequiredChecks` is a dead export (O4); the machinery suites' `protects:` lines use the generic fallback, which criterion 5 requires only of `npm test` suites; the shared instruction block names the probe profile identifiers `claude-code-2.1.263` and `codex-cli-0.153.4`, which are manifest profile ids rather than the running versions.
7. **Future observations.** The next real order's one-gate control segment and a live publication from merged main remain the reopening observations the order's execution record names.

## Goal alignment and process cost

The order's contribution is the operator's flow: the recurring supervision cost of the lifecycle fell from eight gates in one day to one required gate per order, and that gate from 605 s to about 250 s, while the two protections that give the single row its meaning stayed hard. NoOp would have kept the per-transition cold gates and the refusal round trips. Policy resistance and escalation were the live risk in this pass: four consecutive redirect findings on one adapter, each answered by a narrower patch until D008's class-level operand rule; the review judged that rule sufficient because the fixture now targets the destinations the invariant protects rather than the spellings. Rule beating is checked by the fixtures' real hook and runner paths and by this review's own live observation of the guard. Shifting the burden is addressed by a close that refuses on a differing identity instead of rerunning. Commons cost and drift are judged against the recorded rows above. Success to the successful supplied no reason to keep the replica machinery. Seeking the wrong goal is checked against the operator's stated complaint, not receipt count. Naive Interventionism: product tests, immutable reports, the clean-room floor, host permissions and the two invariants are preserved.

Process cost, source `claude-transcript-message-usage`, scope dispatch: 138,795 total tokens at entry (2026-09-15T16:39:09Z); the handoff counter is reported in the response and the ignored receipt. Three forked reviewers reported 239,014, 275,253 and 301,420 tokens. Cost in dollars is unknown.

## Publication screening

No employer material, credential, internal URL, private identifier or secret is present; the clean-room screen found no stop condition. The only email addresses in the diff are a fixture placeholder and the operator's public author identity already tracked on main. Commit subjects and the PR text carry no AI attribution.

## Commit and publication plan

On recording this pass the reviewed state is committed as three coherent changes: the implementation with its tests, generated surfaces and component versions; the documentation write-backs; and the WO-132 evidence, verification, decision, final-review and control records. The PR opens with `npm run worktree -- publish WO-132` against the committed body beside this report, which carries the gate row. Merge and release close remain the operator's.
