# WO-021 final review FINAL-001

**Subject:** branch `wo-021`, worktree `DotLn-wo021`, uncommitted working tree, 2026-09-05. `HEAD`, `main`, and `origin/main` are all `aca01dd826a950475cae7e6f587eb8d85713029b`, the PR #33 merge of WO-030, which is also the commit the published annotated tag `v0.7.0` (tag object `064d304f…`) peels to locally and at `origin` (`git ls-remote origin main` agrees). The branch carries no local commits (`git log main..wo-021` is empty), so the entire subject is the dirty tree. At review open, after the dispatch transition and the index refresh the guide requires of a final-review actor: 61 `git status --porcelain -uall` entries, 42 tracked modifications (against `HEAD`, 779 insertions / 285 deletions including the two `docs/control` files and the generated `docs/work-orders/README.md`; 40 files, 742 / 264 without them) plus 19 untracked paths totalling 3,771 lines: six skeleton source leaves (`beacon-codebook.mjs` 102, `beacon-io.mjs` 261, `beacon-observe.ts` 104, `control-beacon-fs.mjs` 344, `control-beacon.ts` 81, `control-codebook.mjs` 184), two skeleton test files (`control-beacon.test.ts` 602, `control-beacon-cli.test.ts` 236), four scripts (`lib/beacons.mjs` 41, `lib/beacon-observe.mjs` 65, `test-beacon-fixture.mjs` 93, `test-control-beacons.mjs` 272), two benchmarks (`benchmark-beacon.mjs` 176, `benchmark-beacon-contention.mjs` 229), the order's own control segment `docs/control/orders/WO-021.jsonl` (five events), `docs/evidence/WO-021/` (`README.md` 182, `scan-costs.json` 122, `read-contention.json` 161), and `docs/verifications/WO-021/VER-001.md` (511). The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree taken after the dispatch and index refresh (`66138d29bed707f8b9b53378afd5ad009cd1aeaa`) differs from this review's allocation checkpoint `refs/dotln/checkpoint/WO-021/5` only in `docs/control/current.md`, the strictly appended segment, and the regenerated index, and from the verdict checkpoint `/4` additionally in VER-001 by exactly the passages its own disclosure 3 names (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, one dated sentence in the planning map (§Corrections applied by this review), and the lifecycle's own events.

**Authority:** `docs/work-orders/WO-021-control-plane-beacons.md`: `Model: any capable model`; `Effort: executor xhigh+; verifier xhigh+; reviewer any`; a `v0.8.0` minor classification completed at activation above the published `v0.7.0` under the operator's opt-out default, with the skeleton component advancing `0.6.0` → `0.7.0` and the kernel/compiler axes unchanged; nomination provenance from WO-020's write-back duty; direct-draft provenance (the operator supplied the complete public draft and directed its filing, the intake copy of the same turn is a compaction-safety sibling and not a source, and the clean-room screen found no stop condition); dependencies WO-020, WO-018, and WO-019, all closed with passing verdicts in the control fold; the cited sections; an objective; eleven scope-discipline bullets; deliverables; ten numbered acceptance criteria; an evidence gate; a write-back duty; non-goals; and one appended section dated 2026-09-05, the implementation receipt with its executor gate. Against the activated text (`refs/dotln/checkpoint/WO-021/1`, itself byte-identical to `main`'s copy) the order differs by 57 insertions and 3 deletions: the title's `(version assigned at activation)` became `(v0.8.0)`, the two-line placeholder release classification became the completed five-line one (an executor deliverable under the opt-out default), and the receipt was appended. The objective, scope discipline, deliverables, all ten criteria, the evidence gate, the write-back duty, and the non-goals are byte-identical to activation.

**Prior reports:** `VER-001` (pass; all ten criteria, the evidence gate, and every write-back reproduced independently: the six codebook constants re-derived in a separate language, the live beacon hand-decoded from the framing formula before any decoder ran, three of the verifier's own host envelopes driven through the real `--agent` command with per-event listings, a replay and tamper of its own log, both benchmarks re-run at full scale with every allocated-byte figure identical, a clean-room screen, and a relative-link sweep; eight non-blocking observations O1–O8 and four disclosures D1–D4, none requiring repair). Complete sequence: one report, no repair episode. The receipt inside the work order and the evidence README are executor evidence and were treated here as VER-001 treated them: claims under test.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes) in Claude Code 2.1.261 with sandboxed Bash; macOS on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, the system `python3` for one independent re-derivation, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 in this worktree's own dependency tree. Reasoning effort `max`: the session's model control reported it at launch, but `docs/discovery/environment.json` records `effectiveEffortReadback` for `claude-code` as `not found` with `harnessReadbackEligible: false`, so `harness-readback` is not admissible and the epistemic source is `self-reported`, exactly as VER-001's was. `2.1.261` was checked against `claude --version` on this machine and is the newest entry on the harness's observed `versions` list (observed 2026-09-04); `max` is on `sessionEffortSelector.values`; the discovery record is byte-identical to `main`, so the attestation is admissible on merged evidence alone. The order declares `reviewer any`, so any attested effort would satisfy it; `max` is stated because it is what the session reports. The executor was a different harness, model, and session (Codex CLI `0.153.2`, `gpt-6-astra`, `max`, operator-attested; `codex --version` reports `codex-cli 0.153.2` on this machine) and the verifier a different model and session on this harness (`claude-opus-5[1m]`), so the three roles are independent in session and actor.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-021 passes.** The order asked the control plane to drive its own car: every lifecycle transition projects the selected order's phase, verdict, and latest attested effort into a rebuildable beacon whose size decodes under an additive codebook v2 and whose mtime is the transition's own time; a read-only `constellation` sweeps every worktree by metadata alone and prints one row per beacon plus a bounded phase-group row; an agent's sweep is an Observe intent that must pass the existing authority guard before any beacon is read, records its refusal or exactly one `BeaconObserved`, and replays to the same staleness, absence, and clock-skew labels from the recorded sweep time; audiences are separated so the verifier directory holds host projections only and a restricted directory's random name appears only in the authorized `next` briefing; the group codebook is an injective mixed-radix count of members per phase in a sparse file where the host supports it; the cleanup classifier treats the beacon cache as disposable at its anchored root only; and the blueprint says all of that. The deliverable does that. This review re-established it first-hand rather than inheriting it: the live beacon written by this review's own dispatch is 72,341 bytes, which is `8192 + 1002*64 + ((1002*17 + 11) mod 64)` for (`final-review`, `pass`, `max`, `host-projected`) computed by hand before any decoder ran, and its mtime is the appended event's `recordedAt` to the millisecond; the live group cache is 1,235,671,522 bytes at zero allocated blocks, which is `4*13**6 + 3` framed, for one member in phase six; an independent Python enumeration reproduces 288 collision-free v2 states with maximum code 1150 and 81,833 bytes, 104 retained v1 states, 125,970 group vectors with maximum code 3,011,928,819 and 192,763,452,654 bytes, and pairwise disjoint size sets across the three families; four sweeps of this review's own authorship against the live repository (no grant, audience mismatch, matching grant, and a log path under `docs/intake/`) produced exactly `BeaconSweepRequested → CommandRefused`, the same, `BeaconSweepRequested → CommandPersisted → BeaconObserved → CommandResult`, and a refusal before any file existed, with every log at mode 0600 and every beacon inode, size, mtime, and ctime and the control segment byte-identical afterwards; replaying that log yields four decisions and ages `fresh, absent` with the `✦ fresh` and `○ absent` glyphs, re-declaring the recorded threshold as one millisecond turns the same captured metadata `stale`, and adding one byte to a recorded size makes replay refuse; `status`, `status --json`, and `times` left the three live beacon files and the segment byte-identical; kernel and compiler have no working-tree entry; the lockfile changes only the skeleton version; `npm test` is green twice; `git diff --check` is clean; `check-surfaces` passes the README block at `v0.8.0`, the three component rules, and the body profile. VER-001's eight observations are one purity-assertion shape change, one consumed framing tag, one collapsed error message, one per-transition storage probe, one untested-but-structural read-only path, one vocabulary mapping, one stray-file hazard in a hidden host directory, and one always-derived group row; this review adjudicates all eight as non-blocking, verifies the verifier's post-append edits byte for byte against the checkpoints, records observations of its own, applies one dated planning-map sentence, and changes no code, contract, acceptance, schema, authority, or captured-evidence byte.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment. Here the lifecycle helper is modified subject and the beacon it wrote is the thing being decoded.

1. **The resume helper is the deliverable and dispatched this review.** `scripts/resume.mjs` gained the emission hook in this order; `npm run resume -- final-review` allocated this path, minted checkpoint 5, wrote the very beacon this report decodes, will record this verdict, and produced every `status` reading quoted here. Compensating controls: subject identity was established with git objects (temporary-index trees and `git diff-tree`) rather than with the helper's own projection; the appended `FinalReviewRequested` line was read raw and its fields, ordinal, and strictly increasing `recordedAt` checked by hand; the `appendTransition` hook, the `next` case, the whole of `scripts/lib/beacons.mjs`, and every skeleton leaf it imports were read in full after the dispatch and before any result was recorded, and the `final-review` and `final-review-result` cases are byte-identical to `HEAD`; the beacon's size and mtime were computed from the framing formula and the event's `recordedAt` before any decoder was invoked. The control fold, store, index generator, release helper, notes parser, and body profile (`scripts/lib/control.mjs`, `control-store.mjs`, `work-orders.mjs`, `release.mjs`, `release-notes.mjs`, `github-body.mjs`) are byte-identical to `HEAD`, so those instruments are merged code.
2. **The codebooks certify themselves in the shipped tests.** The exhaustive round-trip and the normative-table equality test are written against the same modules they grade. Compensating control: an independent `python3` enumeration written for this review from the formulas in 02 §Beacon codebook v2 and §Beacon group codebook v1, sharing no code with the subject, reproduced every declared constant, the state and vector counts, zero collisions per family, and the cross-family disjointness of the v1, v2, and group size sets.
3. **Every fixture grades the deliverable with the deliverable.** `scripts/test-control-beacons.mjs`, the CLI test, the resume fixture's snapshot assertions, and `test-beacon-fixture.mjs` all import the shipped modules. Compensating controls: the four agent sweeps with this review's own envelopes against the live repository; the replay and two tampers of the resulting log; the read-only identity check on the live tree; the hand decode; the direct `classifyIgnoredMaterial` calls over eight anchored and lookalike paths.
4. **The worktree helper is partly subject.** `scripts/worktree.mjs` gained the `constellation` command and an `async` main in this order, while its `publish` branch and the release helper it delegates to are byte-identical to `HEAD`. The publish flow (`ensureClean`, `check-surfaces --committed`, tracked-body read, body profile, notes parse, `gh` preflight, push, `pr create`) was read from source rather than inferred.
5. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile** through `check-surfaces` and by calling `parseReleaseNotes` and `assertGitHubBodyProfile` directly; the first authoring pass was rejected for angle-bracket placeholders inside inline code, which the strict notes subset treats as raw HTML, and the placeholders were reworded before anything was recorded. Because `v0.8.0` is above the latest published tag, these notes become the `v0.8.0` edition at release close.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-021 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-05T16:36:32.455Z`, `elapsed` `{"implementation":32565591,"verification":863517}`, checkpoint `f48487ab71455c03bd5f1f38641b74d6ae6fab21` (`/4`), and `orders[]` with 23 entries; no projection-disagreement warning was emitted. Before invoking the transition this review read the `resume` mapping in `package.json`, the execution guide's dispatch contract and the harness runbook's notes on sandboxed transitions, the work order, and VER-001 in full; the complete diff of `scripts/resume.mjs` (whose only changes are the import, the emission hook in `appendTransition`, and the `next` briefing) and the new `scripts/lib/beacons.mjs`, `scripts/lib/beacon-observe.mjs`, and every skeleton source leaf they import were read immediately after the dispatch and before any result was recorded. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-021/FINAL-001.md` (the directory itself is created by the reviewer), moved the phase to `final-review`, appended `FinalReviewRequested` with `recordedAt` `2026-09-05T16:48:18.166Z` as the fifth line of `docs/control/orders/WO-021.jsonl` (the order was activated under the WO-030 layout, so it owns its own segment and `docs/control/resume.jsonl` is byte-identical to `HEAD`), minted checkpoint `1904baef23fefc6fe9601c9779da3bf1d1426bb6` (`refs/dotln/checkpoint/WO-021/5`), and emitted the beacon: `.control-beacons/public/` and `verifier/` each hold a 72,341-byte mode-0600 file at 144 blocks (73,728 bytes, exactly the logical size rounded up to the 4,096-byte block) whose padded JSON carries only the eight whitelisted fields, and `.control-beacons/groups/` holds the 1,235,671,522-byte group cache at zero blocks; all three carry mtime `1788626898.166` s, the transition's own stamp. The index refresh that followed changed only the WO-021 lines (`verified` → `final-review`, the pending `FINAL-001` row, and the `Now` line). The segment's five events are `WorkOrderActivated` (`07:13:50.216Z`, `effortDeclarationValidated: true`), `ImplementationReady` (`16:16:35.807Z`), `VerificationRequested` (`16:22:08.938Z`), `VerificationCompleted` (`16:36:32.455Z`), and this review's `FinalReviewRequested`; every event carries `schemaVersion: 1` and the stamps strictly increase. The projected `implementation` span (32,565,591 ms) and `verification` span (863,517 ms) are the arithmetic differences of the recorded stamps.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree after the allocation and index refresh as tree `66138d29bed707f8b9b53378afd5ad009cd1aeaa`, and `git diff-tree -r --name-status` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                                                                                                                                                      |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`90b1ec4d…`)              | every deliverable path (61 paths: 42 modified, 19 added, the segment among them because each checkpoint captures the tree immediately before its transition appends); the whole subject, the work order's own receipt included, is downstream of activation             |
| `/2` implementation-ready (`1d1b346a…`)    | `docs/control/current.md`, `docs/control/orders/WO-021.jsonl`, `docs/work-orders/README.md`, `docs/verifications/WO-021/VER-001.md` (added); nothing else, so every deliverable, every write-back, and the evidence directory were already in the readiness tree         |
| `/3` verify allocation (`ca863847…`)       | the same four paths                                                                                                                                                                                                                                                    |
| `/4` verdict (`f48487ab…`)                 | `docs/control/current.md`, `docs/control/orders/WO-021.jsonl`, `docs/work-orders/README.md`, and `docs/verifications/WO-021/VER-001.md` (modified: 29 insertions / 8 deletions, exactly the three passages and the new disclosure entry that VER-001's disclosure 3 names) |
| `/5` final-review allocation (`1904baef…`) | `docs/control/current.md`, `docs/control/orders/WO-021.jsonl`, `docs/work-orders/README.md`                                                                                                                                                                            |

Every deliverable blob is therefore identical across the implementation-ready, verify-allocation, verdict, and final-review-allocation checkpoints and the working tree; the verifier changed nothing outside its report except the regenerated index (VER-001 D1); VER-001 has not changed since this review was dispatched, and its only change since its verdict is the disclosed one, with the attestation header untouched. All five checkpoints have `HEAD` as their sole parent. The completion mints `/6`.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. `docs/intake/{chats,images,notes}` in this worktree each hold only `.gitkeep`, and `git status --porcelain --ignored` lists only `node_modules/`, the three `packages/*/dist/` outputs, and the anchored `.control-beacons/`; there is no `.claude/settings.local.json` here, and `.claude/settings.json` is tracked and byte-identical to `main`. `docs/observations/` does not exist (VER-001 D2 removed the verifier's logs, and this review removed its own, §Independent probes); `git check-ignore` confirms that path is not ignored, as 07 requires of deliberate evidence. `docs/decisions/` changed only by the appendable amendment to ADR-0002; `docs/discovery/environment.json`, `package.json`, and `docs/control/resume.jsonl` are byte-identical to `main`.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control segment                                                                                     | Report header                                                                                                                                                                                                                                                                                                                                                                                   | Declared minimum | Result                  |
| -------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ----------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.2","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`     | there is no executor report; the receipt states the operator selected the model and effort and that this is not effective readback; `codex --version` reports `codex-cli 0.153.2` on this machine, `0.153.2` is on the `codex-cli` observed `versions` list, and the persisted `model_reasoning_effort` selector records `max` for that version, so `max` was admissible at append               | `xhigh+`         | `max` exceeds the floor |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}` | exactly one column-one header in VER-001, parsed and compared with the event actor as JSON in this session: equal                                                                                                                                                                                                                                                                                | `xhigh+`         | `max` exceeds the floor |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`  | the header above                                                                                                                                                                                                                                                                                                                                                                                | `any`            | satisfied               |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: none` and the JSON form's `effortDrift` is `[{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.261; model claude-fable-5-1; effort max; source self-reported`, the same drift line, phase `closed`, a `finalReview` elapsed value beside the two existing values, and legal actions `release-close, next, activate`, and the emitted beacon should be 81,573 bytes (`code 1146` for `closed`, `pass`, `max`, `host-projected`) with the group cache at 16,063,628,754 bytes for one member in phase seven.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree after the dispatch transition and the index refresh, before any review artifact existed: `All matched files use Prettier code style!`, `GitHub body profile tests passed`, the shell suites (fixture temp-root, publication checker, backup-intake, resume including the seven segment `PASS` lines and the new `beacons: every lifecycle transition matches phase/verdict/effort/time; status and next preserve inode/ctime/size/mtime` line, checkpoint, worktree including `beacon cleanup: populated anchored projection directories removed; docs/intake/dist/x.md negative fixture preserved`, the four-line three-worktree constellation transcript, and the four `PASS` lines for garbage/absent decoding, verifier-only records with two independent 256-bit names, anchored disposal, and projection failure, release including the four real-Git concurrency `PASS` lines, and work-orders), `PASS index coverage: 225/225 product headings indexed` with both audience editions `CURRENT` (30 and 45 linked sections), `PASS docs/work-orders/README.md is current`, `tsc -b --force`, `# tests 196 / # pass 196 / # fail 0 / # skipped 0` with the diagnostics `v2: 288 states; MAX_V2_CODE=1150; dense bytes=81833; all 104 v1 states retain meaning`, `group: 125970 vectors, no collisions; 12 members across all 8 phases; logical maximum=192763452654`, `stat: v2 size=81833 blocks=160; group size=192763452654 blocks=0; filesystem block=4096`, `12 actual member files -> group counts=2,2,2,2,1,1,1,1; group blocks=0`, `refused: 0 metadata reads, 1 CommandRefused, 0 BeaconObserved; authorized: 1 sweep, 1 BeaconObserved; complete decisions and glyph replay identical`, and `authorized CLI: unreadable garbage beacon decoded; exactly one BeaconObserved; replay succeeded; lifecycle bytes unchanged`, and the identity corpus `# tests 8 / # pass 8 / # fail 0`, exit 0. Second run, after this review's map sentence and the three package artifacts were in place, with the same result.

### Static, surface, and structural checks

- `git diff --check` clean, before and after this review's additions.
- `packages/kernel` and `packages/compiler` have no working-tree entry, so both are byte-identical to the activation base and to `v0.7.0`; the only lockfile delta is `@dotln/skeleton` `0.6.0` → `0.7.0`; `package.json` is unchanged; no dependency was added, removed, or repinned. The `observe.*` lowering was the selected guard form, so criterion 10's kernel-byte-identity branch applies and holds.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.8.0; expected v0.8.0 (work-order target v0.8.0; latest published v0.7.0)`, `PASS component-version` with `src unchanged` for `@dotln/compiler` `0.2.0` and `@dotln/kernel` `0.2.1` and `src changed; observed 0.7.0; previous v0.7.0 0.6.0; expected a different version` for `@dotln/skeleton`, and the body-profile rule.
- A relative-link and anchor sweep written for this review over all 25 changed and new Markdown files resolved 654 targets; the only dangling one was `docs/final-reviews/WO-021/FINAL-001.md`, this report's own allocated path, linked from the regenerated index.
- The clean-room and identity screen over the full tracked diff and every untracked file found no absolute path, user name, address, internal hostname, non-public URL, commercial tracker or ALM product name, predecessor-system name, or managed-host or gateway description; the only URLs are the repository's own GitHub releases page and Apple's public Endpoint Security documentation cited by 09.
- The verifier's column-one header equals the `VerificationCompleted` actor by JSON comparison; the executor's `ImplementationReady` actor is the operator-attested Codex default the receipt states.
- `.prettierignore` excludes `docs/final-reviews/WO-*/`, `docs/verifications/WO-*/`, and `docs/work-orders/*.md`, so this package and the immutable reports are not formatter input, while the map sentence added by this review is and passes `prettier --check`.
- `.control-beacons`, `public`, `verifier`, and `groups` are mode 0700; every beacon file is mode 0600; each projection record parses to exactly `codebookVersion, effort, latestVerdict, phase, provenance, recordType, recordedAt, workOrderId` followed by newline padding only; the group file's first 64 bytes are all zero and it allocates no data block.

### Independent probes against the deliverable

- **Hand decode before any decoder.** For the dispatch's projection (`final-review` rank 6, `pass` rank 2, `max` rank 5, `host-projected` rank 0), `code = (((6*3 + 2)*6 + 5)*2 + 0)*4 + 2 = 1002` and `size = 8192 + 1002*64 + ((1002*17 + 11) mod 64) = 72341`; `stat` reported 72,341. For one member in phase six, `code = 4*13**6 + 3 = 19307239` frames to 1,235,671,522; `stat` reported 1,235,671,522. Both files' mtime is `1788626898.166`, and `Date.parse("2026-09-05T16:48:18.166Z")` is `1788626898166`.
- **Independent re-derivation.** A `python3` enumeration from the formulas in 02: 288 v2 states, maximum code 1150, maximum size 81,833 (the largest size in the enumerated set); 104 v1 states; 125,970 group vectors with maximum code 3,011,928,819 and maximum size 192,763,452,654; no collision within any family; the v1, v2, and group size sets pairwise disjoint; the live 72,341 and 1,235,671,522 and VER-001's quoted 29,365 and 51,682 all decode to the states those reports claim.
- **Live constellation.** `npm run worktree -- constellation` printed the WO-021 row `final-review | pass | max | host-projected | fresh | v2 | 72341 bytes`, an `absent` row for the main checkout (which has no projection directory), and `Group Beacon | … final-review=1 … | group-v1 | 1235671522 bytes (host cache; fresh)`, and wrote nothing.
- **Read-only identity on the live tree.** Inode, size, mtime, and ctime of all three beacon files, the control segment, and `current.md` were byte-identical across `resume status`, `resume status --json`, and `resume times` (exit 0).
- **Agent sweeps with this review's own envelopes.** Four `constellation --agent` runs: `allowedEffects: []` and an `audience: "verifier"` request holding only `observe.beacons.public` each exited 1, printed `Beacon sweep refused; CommandRefused recorded; no beacon metadata read.`, and left a log of exactly `BeaconSweepRequested, CommandRefused`; the matching grant exited 0, printed the constellation with the group row labelled `derived from this sweep; no matching cache read`, and left `BeaconSweepRequested, CommandPersisted, BeaconObserved, CommandResult` with one observation of size `72341` decoded `final-review` and one `absent`; a log path under `docs/intake/` refused with `sweep log must be a regular JSONL file under docs/observations` and created no file. All three logs were mode 0600. Across all four runs the three beacons' inode, size, mtime, and ctime and the control segment were byte-identical.
- **Replay and tamper.** `replayBeaconSweep` over the authorized log produced four decisions and ages `fresh, absent`, and `renderGlyphScene` shows `✦ fresh` and `○ absent`; rewriting only the recorded request's `staleAfterMs` to `1` replays the same captured metadata as `stale, absent`; adding one to the recorded `size` makes replay refuse with `BeaconObserved decoded fields differ from captured metadata`.
- **Classifier.** Direct `classifyIgnoredMaterial` calls: `.control-beacons`, `.control-beacons/public/a.beacon`, `.control-beacons/groups/g.beacon`, and `packages/skeleton/dist/x` are disposable; `docs/intake/.control-beacons/v`, `docs/intake/dist/x.md`, `nested/.control-beacons/v`, and `.control-beacons-other/v` are not.
- **Restoration.** `docs/observations/` was removed after the probes; the status entry count returned to 61 and the tree matched the one VER-001 passed.

### Reading

Read in full: the work order and its receipt; VER-001; the executor evidence README; the WO-030 final review, PR body, and notes as the package precedent; the execution guide's resume, closeout, freshness, discipline, and model-specific sections; the final-review README; the complete tracked diff (`.gitignore`, README, playbook, ADR-0002, ledger, the three planning documents, products 02, 03, 04, 06, 07, 09, 10, the three publication files, WO-009, WO-021, WO-022, the lockfile and skeleton manifest, tsconfig, `beacon-fs.ts`, `beacon.ts`, `reactor.ts`, `scenario.ts`, `scenario.test.ts`, the skeleton README, `paths.mjs`, `resume.mjs`, `worktree.mjs`, and the seven changed test scripts); every untracked source, test, fixture, and script leaf; the first 80 and 60 lines of the two benchmarks; the control segment; the generated index; and the harness security runbook's Claude section.

## Acceptance criteria

1. **Every transition emits; `status` and `next` do not — met.** The resume fixture snapshots `.control-beacons` (path, inode, size, mtime ns, ctime ns) before and after every `status`, `status --json`, and `next`, compares, then decodes the emitted file against the folded status and `current.md`'s phase line at every read-only assertion point across a lifecycle that allocates `VER-001`…`VER-004` and `FINAL-001`…`FINAL-003`. This review confirmed it on the live repository: the dispatch wrote the projection quoted above, and `status`, `status --json`, and `times` (VER-001 O5) left every byte and inode untouched.
2. **Three-worktree constellation, lifecycle order, metadata only — met.** The real-Git fixture prints exactly four lines in lifecycle order, proves both snapshots and all three segments byte-identical across the command, and decodes an `EACCES`-unreadable 0x78-filled file by size; a renamed file decodes `absent`. The live two-worktree constellation and the four agent sweeps above reproduced the shape and the read-only property.
3. **v1/v2 compatibility, collision-freedom, declared bounds, pre-allocation refusal — met.** The independent enumeration confirms every constant and the cross-family disjointness; the shipped suite proves the v1 reader labels v2 `unknown-codebook`, the v2 reader retains all 104 v1 meanings, the individual reader labels the group's tag 3 unknown, and the group decoder rejects individual sizes; four unrepresentable or over-ceiling sizes refuse before the destination directory exists; the maximum dense v2 file allocates 160 blocks, the declared ceil-to-block bound.
4. **Staleness, absence, clock skew, glyphs, authority independence — met.** `beaconAge` labels `stale` at exactly the threshold in nanoseconds, `fresh` one nanosecond earlier, `clock-skew` one nanosecond into the future, and `malformed` for non-numeric metadata; the reactor re-derives the label through `Cadence.After` with the origin rounded up to whole milliseconds; the shipped test runs the same envelope and event with observations in every age state against a throwing `Date.now` and gets an identical trace and authorization; this review's threshold tamper turned the live observation `stale` with the `◌ blurred/stale` glyph in VER-001's run and `stale, absent` here.
5. **`BeaconObserved` replays to the same decisions; WO-016 identity extends — met.** The identity test appends a live sweep to the walking-skeleton log and asserts equal decisions and glyph scenes; this review's replay reproduced four decisions and the same ages, and the size tamper refused.
6. **Group codebook exhaustive, bounded, twelve-member fixture, at most one block — met.** All 125,970 vectors round-trip without collision in both the shipped suite and the independent enumeration; a 13 in one slot, a `6 + 7` total, negative, fractional, and empty vectors, and codes above the bound refuse; twelve actual member files across all eight phases decode to `2,2,2,2,1,1,1,1` at `blocks=0`; the live group cache is at zero blocks.
7. **Cleanup with a populated beacon directory; intake still refuses — met.** The worktree suite asserts all three projection directories exist before `finish` succeeds and the planted `docs/intake/dist/x.md` still refuses elsewhere; the direct classifier calls above hold the anchor and the intake precedence.
8. **Authorization precedes every read; refusal records and observes nothing — met.** The CLI test `chmod 000`s the projection directory so any read would raise, and the refusal still completes; the pure test counts zero reads across five refusal reasons and sees `CommandPersisted` as the last durable event before the first read; this review's own no-grant and audience-mismatch envelopes recorded exactly one `CommandRefused` each with no observation, and the matching grant exactly one `BeaconObserved`. The selected form, `observe.beacons.<audience>` lowered through the existing `Act` guard, and its justification are in the receipt, 02, 03, and 10.
9. **Per-audience separation and restricted disclosure — met.** The fixture asserts every verifier and restricted record has exactly the eight whitelisted keys with `host-projected` provenance, that `emitControlBeacon` refuses `self-reported` provenance and an added `narrative` field, that the generic writer cannot target the control tree, that the restricted name is a 256-bit hex string distinct from the capability, appears in the authorized `next --beacon-session` briefing, and appears in none of `status`, `status --json`, `next`, `times`, or the segment, and that truncated, all-zero, wrong-worktree, and wrong-order capabilities exit 1 without echoing either secret. This review inspected the live records, modes, and the absence of any restricted path or capability in the changed files.
10. **Gate, hygiene, no new dependency, kernel untouched — met.** `npm test` green twice; `git diff --check` clean; no dependency change; kernel and compiler byte-identical; the `observe.*` form landed and the result records why.

## Evidence gate

| Gate item                                   | Where re-established by this review                                                                                                                                             |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| test-resume output                          | both `npm test` runs, including the new beacon line, plus the live read-only identity check                                                                                     |
| test-worktree output                        | both `npm test` runs, including the cleanup line, the three-worktree transcript, and the four beacon `PASS` lines                                                                |
| constellation transcript                    | live `npm run worktree -- constellation` and the authorized `--agent` output                                                                                                    |
| authorized and refused sweep transcripts    | four runs with this review's own envelopes, event listings per log                                                                                                              |
| verifier-directory and restricted assertions | fixture assertions in both runs plus live inspection of record keys, padding, and modes                                                                                        |
| replay identity output                      | this review's replay, threshold tamper, and size tamper, plus the WO-016 extension in the suite                                                                                 |
| group-beacon block count from `stat`        | live `blocks=0` at 1,235,671,522 logical bytes; the suite's `group size=192763452654 blocks=0`                                                                                   |
| v2 numeric and physical-size bound          | independent re-derivation; live 144 blocks at 72,341 bytes against the 73,728-byte ceil-to-block bound; the suite's `v2 size=81833 blocks=160`                                   |
| classifier negative fixtures                | direct calls over eight paths and the worktree suite's intake negative                                                                                                          |

## Write-back duty

All five duties are discharged and consistent with the code as read and with the behavior observed.

- **03 §Session lifecycle & resilience** gains the dated control-beacons paragraphs: emission after every successful append, disposable rebuildable directories, `status`/`times`/`next` emitting nothing, the warn-after-record failure rule, the metadata-only sweep, per-checkout group cache ownership, the twelve-member bound, and the perception paragraph with the audience and restricted-briefing rules. Each sentence matches `projectControlBeacon`, `emitControlBeacon`'s blanket catch, `sweepControlBeacons`, `emitGroupBeacon`, and `readGroupBeacon`.
- **04 §Agent projection** names `constellation` beside the terminal status region with its row shape and the `--agent` form, and §glyph grammar replaces WO-020's deferral with the four concrete glyphs, which match `renderBeaconGlyphs` character for character.
- **02** gains the normative v2 and group JSON blocks (equality-tested by the shipped normative-table test and reproduced independently here), the `ControlProjectionRecord` whitelist, the perception-and-age section, and the preserved function-table option, and its `BeaconObserved` glossary row now describes the delivered event.
- **WO-009** gains the note that the host projects evidenced beacons while a real worker emits only self-reported claims, with the `Cadence.After` contract and the instruction not to infer liveness or refresh host mtime from a worker claim.
- **Ledger** carries a dated section with the additive phase-count codebook `adopted`, the sparse lowering `adopted` as an observed local lowering, the measured-read-query finding `adopted`, state-selected function tables `preserved`, and payload-free perception and scan visibility `adopted` `preserved`, with provenance stated and no raw-intake promotion.
- Beyond the duty, and checked here for overstatement: 06's dated technical observation declines any operator-trial, token, or general-scalability claim; 07's resume-phrase and closeout sections document the command, the request envelope, the log containment and mode, the capability API, and the anchored disposable root; 09 states the same-user and privileged-observer limits with Apple's public documentation as the source; 10 records the codebook axis and the consumed framing tag; the playbook's paired-wave section adds the constellation guidance; ADR-0002's amendment records the four `.mjs` leaves and the `allowJs`/`checkJs` build within the decided constraints; the capability table's dated row claims level 1 with the correct blocking gate; the map and the usefulness checkpoint keep the operator comparison open; WO-022's input block records the bounded constants and the consumed tag; the skeleton README documents the command and the bounds; the README release block reads `v0.8.0` and passes `check-surfaces`; the publication index adds the three new 02 sections at `implemented`, matching how WO-020's codebook row entered, and both edition locks are `CURRENT`.

## Ideation receipt

This order carries no separate breakout receipt. Two provenance surfaces were inspected as subject. First, the order's **direct-draft provenance**: the operator supplied the complete public draft and directed its filing, the compaction-safety copy in ignored intake is a sibling record and not a source, the direction was confirmed during WO-020's repair, and the clean-room screen found no stop condition. This review did not read intake; the three intake directories in this worktree hold only `.gitkeep`, and nothing in the subject links to or quotes them. Second, the **execution-time follow-ups** the receipt records: the operator's questions about scale, concurrent reads, function-table selectors, and scan observability were synthesized into the ledger section, 02's preserved option, 09's scan-visibility paragraphs, 06's dated observation, and the usefulness checkpoint's measurement section, and the receipt states that no intake capture or direct-filing exception was used for them. Each promoted surface is labeled `adopted` or `preserved`, claims only what the committed JSON measurements support, links resolve, no settled decision was edited in place (ADR-0002 received an appendable amendment only), and the executor's own benchmarks were re-run at full scale by VER-001 with identical allocated-byte figures.

## Verification-sequence soundness

One report, no repair episode. VER-001 was allocated and recorded through the modified instrument and says so; it used compensating controls of the same kind this review used, and every one of its quoted numbers reproduced here or in the suite. Its post-append edits (D3) are exactly what it discloses, proven by diffing the verdict checkpoint against the allocation checkpoint: the disclosure count, the subject-state sentence, the AC1 wording, and the new disclosure entry with the renumbering it forced; no verdict, criterion, observation, measurement, or actor field changed and the header is untouched. Its observations are recorded with enough precision to adjudicate; its disclosures cover the index refresh, the created-and-removed logs, the post-append edits, and the by-design beacon churn. The sequence is sound and complete.

## Adjudications

### 1. The WO-016 reactor-purity assertion is now a module allowlist (VER-001 O1)

Non-blocking. This review re-read both admitted modules: `control-beacon.ts` has only type imports plus the pure glyph renderer, and `control-codebook.mjs` imports only `beacon-codebook.mjs`, which imports nothing. The guarantee moved from a package boundary to two named files, so a future `node:` import into either would not trip the test. Candidate for WO-022: assert transitively that no module reachable from the reactor imports a `node:` builtin. Not changed here because it is test code.

### 2. Framing tag 3 is consumed by the group family (VER-001 O2)

Non-blocking and visible: 10, WO-022's input block, the receipt, and 02 all say the individual reader labels tag 3 unknown and that individual v3 needs a wider framing radix or a directory-scoped tag. The constraint is an activation input for WO-022, as the order intended.

### 3. `emitControlBeacon` collapses every failure into one message (VER-001 O3)

Non-blocking. The blanket catch is the deliberate no-path-disclosure property criterion 9 requires, and the fixture proves the restricted name never leaks through it. A sanitized cause class without a path would help diagnosis; that is a small patch candidate, not a repair, because the transition records either way and the guide already tells the operator to inspect status and fix the projection rather than retry.

### 4. A storage probe runs on every transition (VER-001 O4)

Non-blocking. The order asked that the edge refuse against a re-observed filesystem limit rather than an assumed one, and the probe is that re-observation; on this APFS host it is allocation-free. A host that supports a 64 KiB hole but refuses the large truncate would warn on every transition while still recording each one. A cached probe with re-validation is a design candidate for a later order.

### 5. `resume times` is outside the fixture's snapshot assertions (VER-001 O5)

Non-blocking and now confirmed twice on the live repository. Adding `times` to the fixture bracket is a one-line test candidate for the next order that touches the resume suite; not applied here because final review may not change test code.

### 6. `none` in `current.md` versus `unknown` in the beacon (VER-001 O6)

Non-blocking. The two vocabularies denote the same absence: 02's whitelist declares the `latestVerdict` domain as `unknown`, `fail`, `pass`, and the fixture maps a null verdict to `unknown` explicitly. Criterion 1 is satisfied under that documented mapping.

### 7. A stray file in `.control-beacons/sessions/` would warn on every transition (VER-001 O7)

Non-blocking. The directory is hidden, host-managed, ignored, and disposable at its anchored root, and the transition still records. It joins WO-030's open item about a stray file in `docs/control/orders/` as one candidate: decide whether exactly the `.DS_Store` basename should be ignored where the classifier already treats it as disposable.

### 8. The `--agent` form never consults the group cache (VER-001 O8)

Non-blocking and correct by design: an authorized observation covers only the requested audience's projection directories, and the cache is a host convenience for the plain command. 04's sentence that the group row "labels its host-cache or sweep-derived source" is accurate for both forms; this review's authorized run printed the derived label. No edit.

### 9. The reactor routes beacon commands by two different discriminators (this review)

Non-blocking. `CommandPersisted` routes a beacon sweep by the command's `observe.beacons.` effect prefix, while `CommandResult` and `CommandRefused` route by `workstreamId === "ws_beacon_control"`. The shipped adapter fixes both, so every path is exercised and consistent, but a future host that persisted a sweep under another workstream would see its result or refusal fall through to the Seiri branches. Candidate for WO-022: route all three by the persisted command's effect.

### 10. The storage probe stages inside the projection root (this review)

Non-blocking. `probeBeaconStorage` creates a `.probe-*` directory under `.control-beacons/` and removes it in a `finally`; a process killed mid-probe would leave a hidden directory that no sweep enumerates and that the anchored classifier disposes of. Recorded so a stray `.probe-*` entry is not mistaken for a projection.

### 11. Publication runs into the sandbox's `gh` boundary (this review)

Operational, not a defect of the subject. In this sandbox `gh` cannot read its configuration (`open ~/.config/gh/config.yml: operation not permitted`), and `git ls-remote` succeeded while warning `failed to store` for the credential helper. The publish helper preflights `gh` after the surface checks and before any push, so a refusal at that step leaves the branch unpushed; the exact command is in §Publication package for the operator to run outside the sandbox, as WO-030's package already recorded.

### 12. The capability row's "staged pending independent review" is now behind the control state (this review)

Non-blocking. The dated `projection.control-beacon` row was written at implementation time and says so; after this review the phrase is historical, as WO-030's own row on `main` already is. Following that precedent, the row is not edited here; the next planning pass may refresh both.

### 13. The verifier's disclosures (VER-001 D1–D4)

D1, the dispatch staling the index: proper, and the same happened here at dispatch and will happen again after completion; the guide's refresh points handle it. D2, created and removed observation logs: this review did the same and verified the directory is absent afterwards. D3, three post-append edits: verified byte for byte against the checkpoints (§Dispatch and subject integrity); honest, bounded, and not a repair trigger. A forward-only hardening candidate: `final-review` dispatch could warn when the latest report's blob differs from its verdict checkpoint, so such an edit is surfaced mechanically rather than by disclosure. D4, beacon churn by design: the same is true of this review, whose dispatch rewrote the projection to `final-review` and whose completion will rewrite it to `closed`.

### 14. Smaller consequences recorded without action

The v1 writer in `beacon-fs.ts` now delegates to the shared `writeBeaconFile` with a fixed 65,536-byte ceiling and dense storage; the largest v1 codeword is 51,064 bytes, so no v1 state is affected, and WO-020's tests pass unchanged. The resume fixture now `git init`s its repositories because the writer requires a Git root with a gitignored projection directory, and its expected checkpoint warning became `not a valid object name HEAD`; the assertion still proves a transition records without a checkpoint. `scenario.ts` imports the pure glyph renderer for the scene, which keeps WO-016's single-reactor invariant. The activation checkpoint predates the segment file because checkpoints capture the tree before each append, which is why `/1` lists the segment as added.

### Candidates considered and not sustained

- **Whether the allowlist widening (O1) should fail the review.** No: both modules are pure today, the assertion still names them explicitly, and the kernel is byte-identical.
- **Whether the changed fixture warning text hides a regression.** No: the fixture became a Git repository without commits, and the message change follows from that.
- **Whether the publication rows for the three new 02 sections should be raised from `implemented`.** No: WO-020's codebook row entered at `implemented` after its own verification and review, so the convention for new rows stands.
- **Whether the `tsconfig` `allowJs`/`checkJs` change reaches the kernel or compiler.** No: only the skeleton config changed, and neither package has a working-tree entry.

## Corrections applied by this review

One, bounded and non-substantive, following the WO-030 review's precedent: the planning map's dated `WO-021 activation completion (2026-09-05)` paragraph gained a sentence recording that WO-021 passed independent verification and final review at this revision and awaits operator merge, with the recommended sequence, the paired-trial statement, and the usefulness comparison unchanged. `prettier --check` passes on the file and the second `npm test` run includes it. No code, test, contract, acceptance, schema, authority, or captured-evidence byte was changed.

## Deviations and open questions

- **No session trailer on the commit or PR body.** This session's harness supplied an attribution reminder asking that commit messages and PR descriptions end with a session link. The operator's standing instruction forbids AI attribution on commits and pull requests, the execution guide keeps commit messages plain, no commit in this repository's history carries such a trailer, and the repository is public; the reviewed-state commit and the PR body therefore carry none. The operator can amend the commit before merge if a trailer is wanted.
- **Claude Code attestations remain `self-reported`** because no effective-effort readback is recorded for the harness (WO-023 FINAL-001 adjudication 9 remains open).
- **The operator usefulness comparison has not run;** the capability row stays at level 1, and no read-performance, token, or scalability claim is made beyond the bounded measurements.
- **Same-user adversaries, provenance spoofing, privileged scan visibility, non-sparse hosts, durability beyond per-file rename, and worker liveness** are outside this order's boundary, as 09, the receipt, VER-001, and WO-009's note state.

## Publication package

- **PR title:** `:sparkles: WO-021: Control-plane Beacons; every lifecycle transition projects its order's phase, verdict, and attested effort into rebuildable metadata, a read-only constellation sweeps every worktree, authorized agent sweeps record replayable perception with staleness, and a bounded phase-group codebook counts members in a sparse file; v0.8.0`
- **PR body:** `docs/final-reviews/WO-021/PR.md`, committed with the reviewed state, one physical line per paragraph and list item.
- **Release notes:** `docs/final-reviews/WO-021/RELEASE-NOTES.md`, the five-section reviewed edition for `v0.8.0`, validated by `parseReleaseNotes` and the body profile.
- **Publish:** after `final-review-result pass` and the reviewed-state commit, `npm run worktree -- publish WO-021 --title '<the title above>' --body-file docs/final-reviews/WO-021/PR.md`. If the `gh` preflight refuses inside the sandbox (adjudication 11), the operator runs the same command from this worktree outside it; the helper pushes only `wo-021` and never merges.
- **Release close:** after merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints with the main checkout as the working directory; because `v0.8.0` is strictly above `v0.7.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.8.0` tag, and creates the matching GitHub Release. The subject worktree's populated `.control-beacons/` is disposable at its anchored root and will not block removal.

## Readiness

The branch is ready for operator review: the subject is the tree VER-001 passed, every criterion and gate item was re-established first-hand, the actor inventory satisfies the declaration, the suite is green twice on the reviewed tree, the surfaces pass, and the package is complete. After `final-review-result pass` the projection should show phase `closed`, `FINAL-001` pass, the reviewer's attestation as latest with no new drift pair, and legal actions `release-close, next, activate`; the index refresh that follows will mark WO-021 `final-reviewed` and release WO-009's and WO-029's conservative dependency blocks on it.
