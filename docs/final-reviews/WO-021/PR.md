## What this merges

WO-021, the `v0.8.0` minor above `v0.7.0`: Beacons for the control plane. Every appended lifecycle transition now projects the selected order's folded phase, latest verdict, and latest attested effort into a rebuildable beacon under the gitignored `.control-beacons/` root of the emitting checkout, with the file's exact byte size decoding under the new additive codebook v2 (288 states, maximum 81,833 dense bytes) and its mtime equal to the transition's own `recordedAt`; `status`, `times`, and `next` emit nothing, and a projection failure warns after the transition is already recorded rather than rolling it back. `npm run worktree -- constellation` sweeps every worktree from `git worktree list` by directory metadata and `lstat` alone, prints one lifecycle-ordered row per beacon and one group row, and writes nothing. The group row comes from a separate phase-group codebook: an injective mixed-radix count of members per phase for at most twelve host-projected members (125,970 vectors, no collisions), emitted as a sparse file that allocates no data block on this host and refuses before allocation where sparse support is not observed. An agent's sweep is an `Observe` intent lowered to `observe.beacons.public` or `observe.beacons.verifier` through the existing `Act` guard, so the kernel and compiler are byte-identical: the request is persisted, a refusal records `CommandRefused` before any beacon is read, and an allowed sweep records `CommandPersisted`, exactly one `BeaconObserved`, and `CommandResult` into an append-only mode-0600 log under `docs/observations/`; replay derives `fresh`, `stale`, `absent`, and `clock-skew` from the recorded sweep time and threshold (20 minutes by default), renders them as text glyphs, and refuses a log whose recorded decode disagrees with its recorded size, while no beacon state ever enters an authority decision. Projections are separated by audience: the verifier directory accepts only the eight-field host record, never a claim or narrative, and an optional host-issued session capability reveals its separate random restricted directory only in that order's authorized `resume next --beacon-session` briefing. The worktree-cleanup classifier treats `.control-beacons/` as disposable at its anchored root only; `docs/intake/**` still wins. Skeleton advances to component `0.7.0`; no dependency was added.

- **Every transition emits; `status` and `next` do not (criterion 1):** the resume fixture snapshots inode, size, mtime, and ctime around every read-only command and decodes the emitted file against the folded status at every point of a lifecycle with four verifications and three final reviews; the verifier and this review each confirmed it on the live repository, including `times`.
- **Three-worktree constellation by metadata only (criterion 2):** a real-Git fixture with two added worktrees prints exactly four lifecycle-ordered lines, leaves every beacon and segment byte-identical, decodes an unreadable 0x78-filled file by size, and reports a renamed file `absent`.
- **v1 retained, v2 collision-free, bounds declared, refusal before allocation (criterion 3):** the v1 reader labels v2 `unknown-codebook` and the v2 reader keeps all 104 v1 meanings; the verifier's and this review's independent enumerations reproduce every declared constant and prove the v1, v2, and group size sets pairwise disjoint; unrepresentable and over-ceiling sizes refuse before any file exists; the maximum dense v2 file allocates exactly its ceil-to-block bound.
- **Staleness, absence, skew, glyphs, and authority independence (criterion 4):** ages flip at the exact nanosecond boundaries, the reactor re-derives them through `Cadence.After` with a rounded-up origin, and the same envelope and event receive an identical trace and authorization with observations in every age state while `Date.now` throws.
- **Replay identity (criterion 5):** the WO-016 identity test appends a live sweep to the walking-skeleton log and asserts equal decisions and glyph scenes; both reviewers replayed their own logs and saw a size tamper refused.
- **Group codebook exhaustive and bounded (criterion 6):** every vector with total at most twelve round-trips, out-of-bound digits and totals refuse, twelve actual member files decode to `2,2,2,2,1,1,1,1` at zero blocks, and the live cache is at zero blocks.
- **Cleanup with a populated cache; intake still refuses (criterion 7):** `finish` removes a subject whose three projection directories are populated, the planted `docs/intake/dist/x.md` still refuses, and lookalike and nested `.control-beacons` paths are not disposable.
- **Authorization before every read (criterion 8):** the CLI test makes the projection directory unreadable and the refusal still completes; the pure test counts zero reads across five refusal reasons; the verifier's three envelopes and this review's three envelopes against the live repository recorded exactly one `CommandRefused` or one `BeaconObserved` each, and a fourth run with a log path under `docs/intake/` refused before any file existed.
- **Audience separation and restricted disclosure (criterion 9):** verifier and restricted records carry exactly the whitelisted fields with host provenance, a self-reported record and an added narrative field refuse, the restricted name is a 256-bit string distinct from the capability, appears only in the authorized briefing, and never in `status`, `next`, `times`, or the segment.
- **Suite, whitespace, dependencies, kernel (criterion 10):** `npm test` exit 0 for the executor, the verifier, and twice for this review; `git diff --check` clean; the lockfile changes only the skeleton version; `packages/kernel` and `packages/compiler` have no working-tree entry.
- **Write-backs:** 02's normative v2 and group tables, record whitelist, perception-and-age section, and preserved function-table option; 03's control-beacons paragraphs; 04's constellation row and the four glyphs; 06's dated technical observation; 07's command, request envelope, log containment, capability API, and anchored disposable root; 09's audience separation and documentation-backed scan-visibility limits; 10's codebook axis and consumed framing tag; the playbook, ADR-0002's tooling amendment, the capability table's dated row, the map, the usefulness checkpoint, WO-009's and WO-022's input notes, the skeleton README, the ledger's dated section, the publication index and both locks, and the README release block at `v0.8.0`.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in this worktree on the verified tree and again on the reviewed tree: Prettier clean, the body-profile unit suite, every shell suite including the new resume beacon line, the worktree cleanup line, the four-line three-worktree transcript and its four beacon `PASS` lines, and the four real-Git concurrency `PASS` lines, `check-publication.mjs` at 225 of 225 headings with both audience locks current, `index --check` current, `tsc -b --force`, 196 of 196 package tests with every beacon diagnostic the receipt quotes, 8 of 8 identity-corpus tests, exit 0. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.8.0; expected v0.8.0`, `PASS component-version` with `src unchanged` for kernel and compiler and `src changed; observed 0.7.0; previous v0.7.0 0.6.0` for the skeleton, and the body-profile rule over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity: a temporary-index tree of the working tree differs from the final-review allocation checkpoint only in the two control projections and the regenerated index; from the verdict checkpoint additionally in VER-001 by exactly the passages its own disclosure names; every deliverable blob is byte-identical from the implementation-ready checkpoint to the reviewed tree; all five checkpoints have `HEAD` (`aca01dd`, also `main` and `origin/main`) as their sole parent; `git log main..wo-021` is empty; the work order's text is byte-identical to activation apart from its completed version line and appended receipt; the verifier's report header equals its control-log actor by JSON comparison; the executor's `max` and the verifier's `max` meet the declared `xhigh+` floors.
- Direct probes: the dispatch's own beacon was decoded by hand from the framing formula (`72341` bytes for `final-review`, `pass`, `max`, `host-projected`; `1235671522` bytes for the one-member group) before any decoder ran, with mtime equal to the event's `recordedAt`; an independent Python enumeration reproduced all six declared constants and the cross-family disjointness; four agent sweeps with this review's own envelopes produced the refusal, the audience-mismatch refusal, the authorized four-event log with one observation, and a pre-file refusal for a log path under `docs/intake/`, with every log at mode 0600 and every beacon and the segment byte-identical afterwards; replay, a threshold tamper, and a size tamper behaved as specified; `status`, `status --json`, and `times` left the live projection untouched; 654 relative links across the changed Markdown files resolve, with this review's own allocated report path the only dangling target until the report existed; the clean-room and identity screen over all added and new lines found only the repository's own public URLs and Apple's public documentation; `codex --version` reports the executor's recorded `0.153.2`.

## Verification trail

- `docs/verifications/WO-021/VER-001.md`: pass; all ten criteria, the evidence gate, and every write-back reproduced independently by re-deriving the codebook in a separate language, hand-decoding the live beacon, driving the real sweep command with the verifier's own envelopes, replaying and tampering its log, and re-running both benchmarks at full scale; eight non-blocking observations and four disclosures with an empty repair checklist.
- `docs/final-reviews/WO-021/FINAL-001.md`: pass; the verifier's observations and disclosures adjudicated with reasons, the post-append edits proven bounded against the checkpoints, the reviewer's own observations recorded, one dated planning-map sentence applied and re-checked, method and instruments disclosed.

## Known open items

- **The WO-016 purity assertion is now a two-module allowlist** (VER-001 O1): both modules are pure today; WO-022 could assert transitively that nothing reachable from the reactor imports a `node:` builtin.
- **Framing tag 3 is consumed by the group family** (VER-001 O2): individual v3 needs a wider framing radix or a directory-scoped tag, as 10 and WO-022's input block record.
- **Projection failures collapse to one message** (VER-001 O3): deliberate no-path disclosure; a sanitized cause class is a patch candidate.
- **A storage probe runs on every transition** (VER-001 O4): allocation-free on this host; a cached, re-validated probe is a later design candidate.
- **`resume times` is outside the fixture's snapshot bracket** (VER-001 O5): confirmed read-only on the live repository by both reviewers; a one-line test candidate.
- **A stray file in `.control-beacons/sessions/`, like a stray file in `docs/control/orders/`, warns or refuses until removed** (VER-001 O7 with WO-030's open item): decide once whether exactly the `.DS_Store` basename should be ignored.
- **The reactor routes beacon commands by two discriminators** (this review): `CommandPersisted` by effect prefix, `CommandResult` and `CommandRefused` by workstream id; consistent for the shipped adapter, a unification candidate for WO-022.
- **The capability table's dated row still says "staged pending independent review"** (this review): historical after this review, as WO-030's row already is; the next planning pass may refresh both.
- **The operator usefulness comparison has not run**; the bounded measurements make no read-performance, token, or scalability claim, and same-user adversaries, privileged scan visibility, non-sparse hosts, and worker liveness stay outside the boundary.
- **`CLAUDE_EFFORT` is an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9): still open, so Claude Code attestations remain `self-reported`.
- **Publish and release close.** The bounded publisher `npm run worktree -- publish WO-021` runs the committed surface check and body validation before any push and then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses at that preflight the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.8.0` is strictly above `v0.7.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.8.0` tag, and creates the matching GitHub Release. The subject worktree's populated `.control-beacons/` is disposable at its anchored root and does not block removal.
