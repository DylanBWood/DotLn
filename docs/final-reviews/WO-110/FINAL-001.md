# WO-110 FINAL-001 — final review of the local-model transport

**Verdict: pass.** All four acceptance criteria hold at the reviewed subject, judged against the order exactly as written. The verification sequence is VER-001 (fail, three findings) and VER-002 (pass); I judged both, ran the order's own tests on my own build, probed the transport with six response shapes of my own, parsed both retained live packets independently, and reproduced the pinned comparison that the repair used to correct VER-001. No finding routes to repair. Three items I met are recorded in this order's decisions: [WO-110-D011](../../evidence/WO-110/decisions.md#wo-110-d011--final-review-the-reproduction-block-names-no-worktree-path), a bounded fix to the availability record's reproduction block, and [WO-110-D012](../../evidence/WO-110/decisions.md#wo-110-d012--final-review-a-bare-null-completion-body-is-labelled-transport-failed-boarded-up-for-the-next-order-that-touches-the-transport), a cosmetic failure label on one pathological body, boarded up with a named follow-up rather than changing verified source; and [WO-110-D013](../../evidence/WO-110/decisions.md#wo-110-d013--final-review-the-pr-meter-was-rendered-after-the-result-so-its-dispatch-table-is-empty-by-the-renderers-rule), which records why the PR body's meter carries the order row but no dispatch rows.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-fable-5-1","effort":"unknown","source":"operator-attested"}

Actor: Claude Code, `claude --version` on the operator's PATH reports `2.1.278 (Claude Code)`. The model is `claude-fable-5-1`, selected by the operator with `/model` at the start of this session. Effort is `unknown`: this harness exposes no effective-effort readback and nobody supplied a value for this dispatch, so the order's `reviewer any` recommendation is recorded as a recommendation, not as an observation. Dispatch: `resume: final review`, recorded by the harness as `npm run resume -- final-review`; I did not repeat it. Fan-out plan, stated before any spawn and unchanged at handoff: zero subagents against the cap of 20 in `docs/control/budgets.json`, because the subject is three source modules, one test file, one probe, its test, one evidence directory and two reports in one worktree, and every check I wanted to run is a short local command. Usage reports 0 admissions, `countKind: exact-observed`, 20 remaining; the unobserved remainder is unknown.

**Process cost:** entry 85962 tokens; handoff 3996593 tokens; source claude-transcript-message-usage

Both readings are `scope: dispatch` from `node scripts/harness.mjs usage 5de3cdb1-77d4-426a-8e2d-bf0c5b5d904a`; entry at 2026-09-20T20:09:18.341Z, handoff at 2026-09-20T20:22:53.475Z, which is this report's counter cutoff, taken after the gate and before the report was written, so the report and the result transition remain uncounted. Dollars and reasoning-output tokens are unavailable from this counter and are therefore unknown, not zero. Cached input is 3,671,650 of the handoff total.

## Subject, integration and evidence boundary

Reviewed the original [work order](../../work-orders/WO-110-local-model-transport.md); the complete verification sequence, [VER-001](../../verifications/WO-110/VER-001.md) and [VER-002](../../verifications/WO-110/VER-002.md); the executor's [implementation report](../../evidence/WO-110/implementation.md) with its repair section, [decisions](../../evidence/WO-110/decisions.md) D001 to D010 and the [experiment measurement](../../evidence/WO-110/experiment-measurement.json); the [availability record](../../discovery/local-model-transport-2026-09-20.md) and both smoke packets; and the ideation side: the order's nomination provenance (the operator's 2026-09-08 actor-kind list and WO-027's probe), the planning map's 2026-09-19 carry-ins for this order, and the latest planning receipt, [2026-09-20-planning-1a0fb634704921a3-021](../../planning/refutations/2026-09-20-planning-1a0fb634704921a3-021.md), whose WO-110 row is `aligned` with no findings.

Subject: branch `wo-110` at HEAD `920c5fd2` plus the staged working tree; `main`, `origin/main`, the merge base and the activation base are all `920c5fd2`, and the latest tag on it is `v0.35.1`, so there was nothing to integrate and no check to retime. No local commit precedes this review. The dispatch checkpoint is `refs/dotln/checkpoint/WO-110/9` (`b589309b`). The staged diff above the base is 67 files, 10,390 insertions and 533 deletions: 16 files with 1,468 insertions and 21 deletions in package source, tests, the probe and the package manifests; 14 files with 137 lines replaced in the generated `.claude` surfaces, which I reduced to their unique changed lines and found to be only `fnv1a64` hashes, the runtime snapshot id and the skeleton version, so no hook logic changed; 4 files with 642 insertions and 337 deletions in the regenerated console self-host fixtures; and the rest in documents, evidence, reports and control. The root `package.json` is untouched; `package-lock.json` changes only the skeleton version and the console workspace pin.

One event in this order's history needs stating plainly. [D007](../../evidence/WO-110/decisions.md#wo-110-d007--the-live-envelope-and-the-endpoints-unhonored-model-field) records that the live envelope, the row's amended reason string and the model-identity limitation were written after `implementation-ready` was recorded, under an explicit `operator override:`, with no lifecycle transition run or repeated. The control log bears that out: `ImplementationReady` at 18:34:57 UTC, the live packet at 18:38:40, `VerificationRequested` at 18:50:17. The amendment therefore sits inside the checkpoint VER-001 was dispatched on, and both verifiers judged it: VER-001 reports the 18:38 packet and VER-002 compared eighteen files against its own checkpoint. I judge the amended bytes as the subject, and I record that the override's authorization is the executor's attestation in D007, which is the only form in which either verifier or I can observe it.

## What I reproduced independently

I built the packages and ran the two WO-110 test files directly, outside the runner's selector: `node --test packages/skeleton/dist/test/local-model.test.js scripts/probes/local-model-transport-smoke.test.mjs` passed **15 of 15** in 102.74 ms of test time. The doubles are real loopback HTTP servers bound to `127.0.0.1:0`, so the transport's own request construction, bounded read and decoding ran unchanged; the envelope is asserted to be exactly the six fields and is re-validated with `parseWorkerResult`.

I then wrote six response shapes the tests do not cover and dispatched the pinned inspection request at each through my own loopback doubles: a 200 body of the JSON literal `null`; a 200 body with an empty `choices` array; a 200 body whose `choices` is an object; a 200 HTML body; a 503 body naming a missing model; and a 200 completion whose `content` is an array. Every one ended as a typed `WorkerFailure` with `alive()` false and nothing unbounded: `invalid-result` for the empty and non-array choices, the HTML and the array content; `model-unavailable` with detail `http-503` for the missing-model error; and `transport-failed` with detail `local endpoint read` for the bare `null`. That last label is the one cosmetic defect I found: `decodeLocalModelResult` reads `payload.choices` before its own `invalid-result` branches, so a `null` payload throws a plain `TypeError` that the dispatch wrapper classifies as a read failure. The actor maps every code to the same `worker-failed` observation, so the resident's outcome is identical; it is recorded in D012 with the follow-up named, not fixed here, because it is a source edit after the verified subject for a body no observed runner produces.

I parsed both retained packets myself. The [18:38 packet](../../discovery/local-model-transport-live-2026-09-20.json) has `outcome: "envelope"`, exactly the six field names, only the ten allowed shape keys, a `declaredRow` of `L-U1 unavailable`, a 14,558.64 ms dispatch, `model: "required-model"`, `effort: "high"` and `harnessVersion: "unknown"`; its serialized text contains no candidate path, classification or evidence string. The [18:10 packet](../../discovery/local-model-transport-2026-09-20.json) has `outcome: "unavailable"` with failure `model-unavailable` and the probe's `ECONNREFUSED`. Both `declaredRow` reasons are the row as it read when each run was made; the shipped `LOCAL_MODEL_ROW.reason` is the D007 amendment, and the packets are correctly left as observed.

I reproduced the comparison behind [D010](../../evidence/WO-110/decisions.md#wo-110-d010--ver-001-classified-release-surfaces-as-inherited-it-is-subject-caused-and-the-skeleton-component-is-bumped): `git diff --stat v0.35.1 920c5fd2 -- packages/skeleton/src` is empty, so the component-version rule passes at the clean base and the failure VER-001 called inherited was this subject's own unbumped skeleton source. VER-002's correction stands, and the bump to `0.32.0` is additive: an optional `local` slot on `ActorSpec` and `ActorResult`, a `local-model-http` member on `WorkerTransportName`, and an adapter replacing a module-local placeholder.

I read the whole source diff. The transport is one bounded `fetch` to an explicit IPv4 loopback origin with `redirect: "error"`, a 262,144-byte read bound, a deadline through `startDeadline`, and an `AbortController` that `kill()` and the deadline both drive; the profile fence answers on the request kind before validation; the wire carries `reasoning_effort: "none"` and no DotLn effort value, as [D003](../../evidence/WO-110/decisions.md#wo-110-d003--the-wire-shape-is-wo-137s-observed-rows-and-effort-stays-a-launch-claim) decided from WO-137's rows. `canonicalWorkerArgs` refuses `local-model-http` rather than falling into the Codex argv branch. The catalog's placeholder string `local-model is unavailable until WO-110` is gone, which discharges the first of the planning map's two 2026-09-19 carry-ins for this order; the second, fixing the WO-137 probe-client defects O1 to O3 before reusing `scripts/probes/local-runner-*.mjs`, was conditional on reuse, and the new smoke imports nothing from those clients, so the condition did not fire and those three observations remain recorded for the order that promotes them. The adjacent repair in [D006](../../evidence/WO-110/decisions.md#wo-110-d006--adjacent-repair-the-worker-lock-peer-fixture-publishes-its-result-atomically) is eight lines in a test fixture, a `.part` write and `renameSync`, and I agree with its diagnosis of a create-then-write race under load.

I scanned every new or changed file for credentials, keys and private identifiers: none. The one literal home-directory path was the `cd` line opening the availability record's reproduction block; that path is this worktree, which release close removes, so I replaced it with the playbook's placeholder form and recorded D011. The seven committed reports that carry the same literal path as an observation are immutable and were left alone.

## Acceptance results

| Criterion | Judgment |
| --- | --- |
| 1 — with a double endpoint, an inspection episode returns the six-field envelope and the result validates; an unavailable endpoint yields the typed unavailability | **Met, reproduced above.** Fifteen of fifteen tests on my build; my six additional probes all typed and bounded. A closed port and a `No models loaded` body both yield `model-unavailable`; the resident maps that to an `ActorUnavailable` NoOp through the unchanged `resident-host.ts` path, and the shipped `L-U1` row keeps the catalog reporting unavailable without contacting the operator's machine. |
| 2 — a live smoke against the operator's endpoint returns an envelope, recorded with shapes only, or an `unavailable` row | **Met, both branches recorded.** The 18:10 row is `unavailable`; the 18:38 row is a validated envelope, and `runTransportSmoke` sets `outcome: "envelope"` only after `parseWorkerResult` accepts the result. The projection carries field names, types, lengths, counts and latency and no model-authored text, which the smoke's own tests pin against a fixture summary that must never reach the packet. The live packet's `harnessVersion` is `unknown`; the dated narrative supplies the installed version separately. |
| 3 — write-backs land: 03 §Ports, `environment.md` addendum, ledger entry | **Met.** Product 03 §Ports names the third adapter and carries its paragraph; §Runtime primitive catalogs replaces the placeholder sentence. `docs/discovery/environment.md` carries the dated addendum with both time-indexed rows and the model-identity limit. The ledger duty is substituted under the pre-2026-09-09 rule: thirteen `WO-110-D0*` rows are in the decisions index and the work-order index marks the substitution. The skeleton README documents the `local` declaration, and the software-engineer edition's table of contents names the transport. `npm run publication:check`: PASS, 272 of 272 headings, both editions CURRENT, before and after my edits. |
| 4 — `npm test` green; `git diff --check` clean; no new dependency | **Met, re-run by me.** `npm test -- --review`: **28 suites passed, 0 failed, 261.70 s, 72 fresh tasks**, run after the whole subject was staged and outside a harness sandbox in force (the runner's own detector reported the `claude-code` marker with the probe write admitted). `git diff --check` and `git diff --cached --check`: clean, exit 0. Root `package.json` has no diff; the lockfile changes only the skeleton version and the console pin; no lint or type suppression enters the diff. |

Non-goals hold: the transport refuses `source-change`, `verification-evidence` and `plan-refutation` requests on their kind before any request is sent, and no document or record makes a model-quality claim. Operator-review assumption 1 held in the way the order anticipated: the endpoint is the operator's, the executor recorded `unavailable` when nothing was listening, and the operator started the runner for the one live run.

## The verification sequence

VER-001 failed the order on three findings, and all three were real at its subject: seven order-owned files unformatted, and the authority and feedback evidence editions stale against the changed generated surfaces and the changed actor sources. It also classified the `release-surfaces` failure and five preflight-blocked suites as inherited from base. That classification was wrong, and the repair caught it: the rule compares the previous tag against the working tree, the tag-to-base skeleton diff is empty, and the five suites were blocked, not failing. The executor recorded the correction in D010 with the evidence, and VER-002 checked it independently rather than carrying the sentence forward. I reproduced the empty diff myself and agree. The fail verdict was still the right call on the subject it judged; a verifier that had waved the formatter through because the runtime gate was green would have let a red current-subject check into review.

The repair minted the new authority and feedback editions once before the component bump was diagnosed, then had to mint them again, so both carry revision `001` and the revision-null editions remain as residue under [D009](../../evidence/WO-110/decisions.md#wo-110-d009--ver-001-f2-and-f3-new-wo-110-authority-and-feedback-editions-at-revision-001)'s follow-up. The implementation report says so under "ordering cost, recorded", and I have nothing to add except that the residue is the repository's normal shape and no manifest selects it.

VER-002's limits stand and I carry the substantive ones into the release text: n=1 on one warm runner, the packet's unknown runner version, the request's model field not identifying the answering artifact, cancellation evidence establishing client settlement rather than server-side inference cancellation, and `L-U1` remaining `unavailable` with WO-137's readiness contract inconclusive. Its verifier-authored probes (a body over the byte bound, stalls before and after headers, a redirect, an evidence request) and mine do not overlap, which is why I chose the shapes I did.

The repair's attestation records effort `unknown` with source `self-reported`, against the order's `executor xhigh+`; the transition logged the drift as the rules require and refused nothing. Both verification receipts carry `unknown; cause no-session`, which the cost-line rule admits.

## Corrections I made in this review

- The availability record's reproduction block opened with `cd /Users/dylanwood/Projects/DotLn-wo110`, the executor's worktree. Replaced with `cd <repository root>`, the playbook's form, and recorded in D011.
- The bare `null` body label is recorded in D012 as a defect met and not fixed, with the follow-up named for the next order that edits `local-model-transport.ts`. No source changed in this review.
- I recorded the result before refreshing the PR meter, and `release prepare` refuses once the order is closed, so the meter was rendered with the helper's own renderer at 20:29 UTC; that renderer omits dispatch rows for a closed order, and D013 keeps the block as rendered and names the ordering fix for the reviewer procedure or the renderer.

## Gates run at this review

- `npm run build --silent && node --test packages/skeleton/dist/test/local-model.test.js scripts/probes/local-model-transport-smoke.test.mjs` — 15 of 15 passed.
- Six reviewer-authored loopback probes from session scratch — all typed failures, one mislabel recorded above.
- `npm test -- --review` — **28 suites passed, 0 failed, 261.70 s, 72 fresh tasks**, started 2026-09-20T20:18:15Z and finished 2026-09-20T20:22:38Z by the shell clock, with the skeleton suite at 257.44 s, after staging the whole subject; no source changed afterwards.
- `npm run publication:check` — PASS, 272 of 272 product headings, both editions CURRENT, before and after my document edits.
- `node scripts/harness.mjs check` — 31 generated surfaces agree.
- `npx prettier --check` on both files I edited — pass.
- `git diff --check` and `git diff --cached --check` — clean.
- `npm run meta` — decisions index and follow-up register refreshed after D011 and D012; `npm run test:docs` — 19 passed, 0 failed, run after this report was written, after its first run refused an angle-bracket placeholder inside a code span of the release notes that I reworded; `npm run work-orders -- index` refreshed after the result.

No source changed in this review, so the review gate above is the product gate for this subject, and the document changes made after it are reports, decisions and generated documents, which do not invalidate code identity.

## Limits and disposition

**What the pass does and does not claim.** It claims a consumable third transport whose behavior is established against doubles and one live run: the envelope, the validation, the fences and the typed failures. It does not claim a qualified local model, a readiness grade, determinism, quality or throughput; the shipped row is `unavailable`, and the resident will not dispatch to this kind until a `ready` row is supplied. WO-138 remains the qualification order and still needs a `ready` WO-137.

**The model-identity gap is the finding that matters downstream.** The endpoint served `required-model` from whatever was loaded. The transport cannot bind an envelope to the artifact that produced it, and D007's follow-up puts that squarely on WO-138. The planning receipt's WO-138 row already records "admission precedes qualification" as a finding, with a reopening condition that fires if the catalog offers the local kind for a role WO-138 later grades `negative` or `inconclusive`; at this subject the catalog offers nothing live, so the condition cannot fire yet, and it should be watched when the row is promoted.

**Instruments under description.** The test runner, the generated harness surfaces and the smoke generator are all inside the subject. I ran the two test files outside the runner's selector, probed the transport with doubles of my own, and parsed the packets with a script of my own rather than through `envelopeShape`. The passing harness and evidence checks establish their declared bindings, not that a hook enforced a rule in a live session.

**The economy trial.** [D001](../../evidence/WO-110/decisions.md#wo-110-d001--tinkerer-economy-experiment-focused-skeleton-suite-iteration) is WO-145's second trial: adopted, 87 s of a 900 s budget, three focused runs of about 14.1 s against a 252.6 s full gate, on a stand-in test file because the order's own did not exist at pre-registration. It changed which declared commands the executor ran while iterating and nothing else; the full gate still ran before every handoff, and this review ran it again. The measurement is one order's shape, and WO-145's pre-registered reading of the three trials remains the judgment.

**Goal alignment.** Mission and critical path: a local, model-agnostic inspection actor is gate R's local option, landed as an interface the catalog consumes rather than as a script or a session, with content-addressed inputs and a declared row, so the platform checks hold. Rule beating was the live trap: an endpoint's schema claim is not validation, a served response is not the requested model, and working inference is not readiness; the subject refuses all three proxies and I checked each rather than trusting the report. Drift: I report the n=1 magnitude and the unknown runner version beside the met criteria. Escalation: this review adds no step, check, hook or receipt; D012 is a nomination inside an existing follow-up channel. Shifting the burden: the declared row lets the resident record why nothing happened without operator rescue, and the one defect I could fix inside the bound I fixed. Commons: one review gate, no live inference, zero subagents. Naive Interventionism: I changed no source, no product document, no closed report and no acceptance criterion, and I re-ran the publication check after my edits to prove it. NoOp: declining the order over a cosmetic label or an n=1 live row would leave a verified transport and its typed unavailability unmerged for no benefit, while the reopening conditions for every dated claim are already written.

## Release and handoff

Application **v0.36.0**, a minor release above local `v0.35.1`, retimed from the collided `v0.35.1` target under the order's existing minor classification; skeleton `0.32.0` under D010, with compiler `0.17.0`, kernel `0.6.0` and console `0.1.7` unchanged. The order heading, the README release claim and the roadmap's collision note all carry `v0.36.0`. The reviewed PR body and the five-section release notes sit beside this report.

This review authorizes committing the reviewed state, pushing the `wo-110` branch and opening its pull request, and nothing further. Merge, main, tag and Release remain the operator's and release close's.
