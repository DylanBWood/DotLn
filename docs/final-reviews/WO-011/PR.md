## What this merges

WO-011, the `v0.13.0` minor release above `v0.12.0`: ten recurring operator corrections from this repository's own lineage compiled into executable feedback checks, and the first bounded self-hosted step. The pure compiler lowers a complete FeedbackUnit declaration, with incident provenance, observable undesired and desired behavior, scope, trigger, mechanism and rationale, enforcement, required evidence, regression fixtures, conflicts, supersession, retirement and next-maturity conditions, onto a closed ten-handler vocabulary at the cheapest sufficient rung: scripts for attribution and suppression diffs, pre-effect guards for decision lineage, writer isolation and cleanup scope, a pure reactor branch for semantic correction, an evaluator for evidence judgments, and workflow gates for application evidence, output reads and complete scope. Nothing lowers to prose. The skeleton's boundary adapter evaluates host-derived facts and refuses before the effect runs; the source-comment check parses files with the already pinned TypeScript parser so regex, template and JSX text cannot impersonate a suppression directive; the shared typed reactor gains a feedback branch that authorizes one read-only audit through the kernel's `Program.Invoke`, `authorize` and durable `CommandPersisted` outbox and that turns a typed operator correction into a monotone policy tightening plus a durable diagnosis request, with no path from message wording. `dotln feedback-audit` runs that work order on this repository: it executes the twenty present/removal fixture subprocesses, pins seventeen source files and the report by content hash, and dispatches one blinded verifier episode over the WO-009 transports into an empty Git mount with model tools disabled; the retained live run used Claude Code `2.1.263` and completed on its second attempt after a refused first return. The kernel is byte-unchanged; the only dependency change is the pinned TypeScript package becoming a skeleton runtime dependency, recorded through ADR-0002's Amendments.

- **Criterion 1, the full FeedbackUnit shape and cheapest-sufficient mechanism:** the catalog declares exactly the roadmap's ten units, the compiler validates every field positively and refuses an unsupported or mismatched handler, an intake reference, duplicates and co-equipped supersession, the lowered rungs are 1, 1, 2, 2, 2, 3, 5, 6, 6 and 6 against product 02's hierarchy numbering, and each unit states why the rung below is insufficient.
- **Criterion 2, every regression fails when its mechanism is removed:** the removal is a test-only variable read by the fixture module alone; the audit gate requires the removal run to exit 1 with exactly one `ERR_ASSERTION` naming that test, so a build or subprocess failure cannot pass as a removal; all twenty runs are re-executed inside `npm test`, and one ablation was rerun directly at review.
- **Criterion 3, measured startup-context reduction with the method stated:** the WO-004 UTF-8 byte and physical-line method over the recorded two-file, 79-line, 3,530-byte baseline gives 6,025 bytes with the ten prose equivalents and 3,632 bytes with the compiled one-line residue, 2,393 bytes saved; the accounting was recomputed independently at verification and again at review, and the generator refuses a non-positive saving.
- **Criterion 4, one repository work order through compile, dispatch, execute and verify inside DotLn:** the retained audit stream carries `FeedbackAuditOpened`, `FeedbackAuditRequested`, `CommandPersisted`, `FeedbackAuditExecutionRequested` and `CommandResult` for the pinned subject and policy; the retained verification stream opens a separate workstream over exactly the seventeen pinned files plus the report, records the first attempt's `invalid-result` refusal and lease expiry, and admits the second attempt's evaluations with host-derived provenance; the pinned source and verifier subject recompute from the current tree, so the witness covers what ships.
- **Criterion 5, fail-conservative correction on semantic events only:** the correction reads only the event type and id, applies to operator-authored typed events only, can only remove wildcard and destructive effects, forbid scope expansion, preserve evidence and require diagnosis, is idempotent by event id, and its pending diagnosis blocks further dispatch and execution; worker-authored corrections and an operator message containing profanity leave the policy unchanged under replay equality.
- **Evidence gate and write-backs:** `scripts/feedback-evidence.mjs --check` reruns the fixtures and validates the retained streams inside `npm test`; every criterion maps to named tests in the executor's receipt; the domain model, architecture, roadmap, execution guide, IR compatibility, ADR-0002's Amendments, the legal inventory, planning map, publication index and both audience locks, root README release block, both package READMEs, discovery record, the work order's activation header and ideation receipt, and the ledger are updated consistently.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` on the verified tree after dispatch and again on the reviewed tree: Prettier clean, the body-profile suite, every shell suite, `check-publication.mjs` at 231 of 231 headings with both audience locks current, `index --check` current, `tsc -b --force`, 281 of 281 package tests including the eighteen WO-011 tests, 8 of 8 identity-corpus tests, the four current artifact-identity files, the four synthetic verification files, and ten passing regressions with ten removal failures and 2,393 fewer instruction bytes; exit 0 both times. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.13.0; expected v0.13.0`, `src changed` with advanced versions for the compiler (`0.6.0` above `0.5.0`) and the skeleton (`0.12.0` above `0.11.0`), `src unchanged` for the kernel at `0.2.1`, and the body profile over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity: a temporary-index tree of the working tree differs from the implementation-ready checkpoint only in the control projection, the appended control segment, the verifier's report and the regenerated index, so every deliverable blob the verifier judged is the one the executor declared ready; all five checkpoints have `HEAD` (`d6179e1`, also `main` and `origin/main`, the published `v0.12.0`) as their sole parent and their commit times match the segment's stamps to the second; `git log main..wo-011` was empty before the reviewed series; VER-001's bytes are identical in its verdict checkpoint and the committed tree; the verifier's report header equals its control-log actor by JSON comparison; the executor's `max` and the verifier's `max` meet the declared `xhigh+` floors.
- Direct probes: the source pin `sha256:410d1f83…`, policy hash `fnv1a64:d0000feb2622b0d4`, the 3,530, 2,495, 102, 6,025, 3,632 and 2,393-byte accounting and the verifier subject `sha256:6fb91aab…` recomputed from the current tree; the retained verification log re-projected to a `complete` matrix with both rows verified, zero stale evaluations and host-admitted provenance for the same command and input hash on both attempts, and its capsule path set equal to exactly the seventeen pinned files plus the report; the shipped `feedback-audit` command with the deterministic transport refused at a macOS `/tmp` store after its audit had run (module-level cause: the verifier mount's resolved path differs from the path given) and completed from a canonical store with both rows verified in one dispatch, then recovered on rerun with zero dispatches; one ablation rerun directly; ten reviewer-authored scanner cases and five attribution-predicate cases behaved as specified; a link sweep over the 22 changed Markdown files resolved 635 links; a clean-room term screen and a secret scan over every added line and new file found only the verifier's own screening sentence; the public gitmoji catalog confirms `:alembic:` as "Perform experiments".

## Verification trail

- `docs/verifications/WO-011/VER-001.md`: pass; all five criteria, the evidence gate, the scope discipline, the constraints, the activation completion, the write-back duty and the ideation breakout receipt reproduced independently outside the deliverable's own generator; six non-blocking observations and three disclosures, none requiring repair.
- `docs/final-reviews/WO-011/FINAL-001.md`: pass; the verifier's observations adjudicated with reasons, the reviewer's own observations recorded, the coherent two-commit series confirmed, three bounded documentation corrections applied and re-checked, method and instruments disclosed.

## Known open items

- **The attribution predicate does not match a session-URL trailer** (VER-001 observation 1): `hasAiAttribution` catches co-author trailers and generated-with footers, not a `Claude-Session:` or `Assisted-by:` trailer; the equipped invocation settings suppress that trailer at generation, the repository history has none, and this review added none to its commits or this body. Extending the byte predicate is a candidate, not a WO-011 obligation.
- **The pinned verifier subject excludes the evidence generator and the host tests** (VER-001 observation 2): the blinded verifier judged the seventeen implementation and fixture files plus the report; the repository verifier and reviewer read the rest.
- **One workstream kind per store** (VER-001 observation 4) and **an advisory profile disables the correction reactor** (VER-001 observation 5): standing structural constraints, consistent with product 02, worth naming before another branch or a non-hard profile is authored.
- **The clean-install claim is executor-reported** (VER-001 observation 6): neither the verifier nor this review reran `npm ci` in the worktree; the lockfile records TypeScript without `dev: true` under `@dotln/skeleton@0.12.0` and `npm ls typescript --omit=dev` resolves it, and release close runs `npm ci` before tagging.
- **The runbook's store path was refused on macOS** (FINAL-001 adjudication 8): corrected to the gitignored `.runtime/` root with the canonical-path rule stated; the CLI's refusal message remains deliberately generic (WO-010 FINAL-001 adjudication 8), so the mount-alias cause is only visible at module level.
- **A repository-relative store cannot be exercised inside the Claude Code sandbox** (WO-010 FINAL-001): the harness protects nested Git configuration under the project; run the runbook from an ordinary shell.
- **An unterminated block comment at end of file loses its last two characters in the scanner's body slice** (FINAL-001 adjudication 10): such a file is a syntax error and cannot be a green repair; recorded, not scored.
- **Live verification judged controlled fixture witnesses**, its launch model and effort are host-observed selections rather than effective readback, and `CLAUDE_EFFORT` remains an unrecorded readback candidate, so Claude Code attestations stay `self-reported`.
- **Publish and release close.** The bounded publisher runs the committed surface check and body validation before any push and then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses at that preflight the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.13.0` is strictly above `v0.12.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.13.0` tag, and creates the matching GitHub Release.
