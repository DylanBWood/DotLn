# WO-011 final review FINAL-001

**Subject:** branch `wo-011`, worktree `DotLn-wo011`, uncommitted working tree, 2026-09-06. `HEAD`, `main` and `origin/main` are all `d6179e189d35567955c0c3855d7e881180e1d0c2`, the PR #39 merge of WO-010 (`git ls-remote origin main` agrees), which is also the commit the published annotated tag `v0.12.0` (tag object `3eb0fc50aaff394ccd80bbf042948ef7c2e7a015`) peels to. The branch carries no local commits (`git log main..wo-011` is empty), so the entire subject is the dirty tree. At review open, before the dispatch transition: 34 tracked modifications (744 insertions, 68 deletions, including `docs/control/current.md` and the generated `docs/work-orders/README.md`) plus 25 untracked paths totalling 9,439 lines: six source leaves (`packages/compiler/src/feedback.ts` 643; `packages/skeleton/src/loadouts/feedback.ts` 160, `feedback-boundary.ts` 94, `feedback-source-comments.ts` 72, `feedback-audit.ts` 177, `feedback-selfhost.ts` 375), two scripts (`scripts/feedback-commit-msg.mjs` 23, `scripts/feedback-evidence.mjs` 118), three test files (`packages/compiler/test/feedback.test.ts` 227, `packages/skeleton/test/feedback-fixtures.test.ts` 486, `feedback-host.test.ts` 249), the order's own control segment `docs/control/orders/WO-011.jsonl` (four events at open, five after the dispatch), `docs/evidence/WO-011/` (`README.md` 122, `feedback.json` 533, `selfhost-audit.jsonl` 5, `selfhost-verification.jsonl` 958, the four `artifact-identity/` receipts totalling 3,847 and the four `verification/` files totalling 858), and `docs/verifications/WO-011/VER-001.md` (487). The tracked diff touches the reactor (233 insertions), the CLI, the verification host and protocol, the worker transport, both package manifests and READMEs, the compiler's index and version constant, the lockfile, `.prettierignore`, both evidence scripts' destinations, and fourteen documents. The subject is the tree VER-001 passed: a temporary-index `write-tree` of the working tree after the dispatch and the index refresh (`42717083ec3544b680696f78a3a8b04a68e2c8f6`) differs from the implementation-ready checkpoint only in the control projection, the appended segment, the verifier's report and the regenerated index.

## Actor

This review ran on the Claude Code CLI, version `2.1.263` (`claude --version` observed locally in this session), model `claude-fable-5-1` (Fable 5.1), at reasoning effort `max` selected by this session's model control. The value is self-reported: `max` is on this harness's `sessionEffortSelector.values` in `docs/discovery/environment.json`, `2.1.263` is on its observed `versions` list (recorded during WO-022's verification), and `effectiveEffortReadback` for `claude-code` remains `not found` with `harnessReadbackEligible: false`, so `harness-readback` is unavailable and `self-reported` is the honest label. The work order declares `reviewer any`, which `max` satisfies, and `Model: any capable model`. The executor was a different harness, model and session (Codex CLI `0.153.4`, `gpt-6-astra`, `max`, operator-attested) and the verifier a different model and session on this harness (`claude-opus-5[1m]`, `max`, self-reported), so this review is independent of both in session and in actor. No `--account-label` is supplied; the header carries no `accountLabel` and the completion will project `not-applicable`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-011 passes.** The order asked for exactly the roadmap's ten FeedbackUnits, each complete, each lowered to the cheapest sufficient rung, each with a regression fixture that fails when its mechanism is removed, a measured startup-context reduction with the method stated, one work order for this repository travelling compile, dispatch, execute and verify inside DotLn with evidence, and a fail-conservative correction reactor that triggers on typed semantic events and never on surface language. The deliverable does that. The compiler builds the `feedback-v1` profile by positive construction and pins it with a policy hash; the boundary adapter refuses before the effect; the shared reactor's feedback branch authorizes and dispatches the audit through the kernel and folds an operator correction into a monotone tightening with a durable diagnosis request; the audit host executes twenty fixture subprocesses behind a gate that a broken build cannot satisfy; the self-hosted driver pins seventeen files and the report, recovers without re-executing, and dispatches one blinded verifier into an empty mount; and the retained live streams pin exactly the bytes now in the tree. Every quantitative claim was recomputed here by a path that does not run through the artifact making it. Three bounded documentation corrections were applied; none touches code, a contract, acceptance, schema, compatibility, authority or prior evidence, and none touches a pinned source file, so the live witness remains valid. The branch is ready for operator review.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **The deliverable supplied most of the tools that measure it.** `compileFeedbackUnits`, `evaluateFeedback`, `feedbackBoundary`, `feedbackSourceComments`, `readFeedbackSource`, `runFeedbackRegressions`, `feedbackContextAccounting`, `runFeedbackSelfhost` and `scripts/feedback-evidence.mjs` are WO-011 deliverables. Compensating controls: the source pin, policy hash, instruction accounting and verifier subject were recomputed by calling the built modules directly and compared with the recorded values in `feedback.json` and both retained streams; one ablation was run as a raw `node --test` subprocess and its TAP read by eye; the retained streams were parsed with a hand-written script and the acceptance matrix re-projected through the WO-010 projection rather than read off the executor's summary; the shipped CLI was driven from clean stores and the module-level cause of its refusal obtained by calling the driver directly.
2. **The dispatcher published this review through the lifecycle script.** `npm run resume -- final-review` appended `FinalReviewRequested`, minted checkpoint 5 and rewrote the projection. `scripts/resume.mjs`, `scripts/work-orders.mjs`, `scripts/release.mjs`, `scripts/worktree.mjs`, `scripts/release-notes.mjs` and `scripts/github-body.mjs` are byte-identical to `HEAD` in this order (`git status --porcelain scripts/` lists only the two new feedback scripts and the two evidence scripts whose destination constants changed), so the control plane is not itself under review; even so, the appended line was read raw and its fields, ordinal and strictly increasing `recordedAt` checked by hand, and subject identity was established with git objects rather than the helpers' projection. No `DOTLN_BEACON_KEY_FILE` is configured, so Beacon emission took the unchanged dense v2 path.
3. **The self-hosted step consumes prior deliverables that this order modifies.** The WO-010 verification host, protocol and projection and the WO-009 transports carry the optional `feedback` profile, the fixed feedback-verifier limits and the observed-version list. Those diffs were read in full; the host test proves the profile's settings and limits and the kernel test suite that WO-010 established still passes (281 of 281, of which 18 are new).
4. **The executor's fixtures and receipts and the verifier's reproductions are claims.** All three new test files were read in full and their assertions compared with the acceptance criteria; the claims that could pass vacuously (that the removal run's failure is an assertion rather than a build error, that the attribution fixture drives a real Git hook, that the writer-isolation facts come from a real repository, that the verifier mount is empty) were confirmed to be positively asserted, and the first and last were reproduced here.
5. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile** by calling `parseReleaseNotes` and `assertGitHubBodyProfile` directly and through `check-surfaces`, which reports `PASS github-body-profile` over both files. Neither body contains an angle bracket. Because `v0.13.0` is above the latest published tag, these notes become the `v0.13.0` edition at release close.
6. **This session's harness instructs the reviewer to append a `Claude-Session:` trailer to commits and PR bodies.** The operator's standing rule in the user-level `CLAUDE.md` forbids AI attribution on commits and pull requests, the deliverable's `no-attribution` unit exists to enforce the same rule, and VER-001's first observation is that the byte predicate does not catch exactly this trailer. The rule wins over the harness default: no trailer is added anywhere in the reviewed series or the publication bodies, and the conflict is recorded here rather than acted on silently (adjudication 1).

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-011 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest with `accountLabel: "not-applicable"`, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-06T18:18:25.853Z`, `elapsed` `{"implementation":6527691,"verification":701763}`, checkpoint `b3a95d23d7344c76f86f1d290c47158ad6e47dc2` (`/4`), and `orders[]` with the known orders; no projection-disagreement warning was emitted. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation and printed the one-line briefing naming this report's path. Status then reported phase `final-review`, `finalReview: "FINAL-001"`, checkpoint `afcfdbcc42173d74f0b00e1cced1def23d5de513` (`/5`), `recordedAt` `2026-09-06T18:21:26.162Z` and legal action `final-review-result`; the projection's new WO-011 block agrees. `npm run work-orders -- index` was run after the dispatch, as the guide requires of a final-review actor; the regenerated index moves WO-011 to `final-review` and links this report as pending.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree after the allocation and index refresh as tree `42717083ec3544b680696f78a3a8b04a68e2c8f6`, and `git diff-tree -r` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                                                                                                                                 |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`1f031653…`)              | every deliverable path (59 files, 10,184 insertions, 69 deletions); the whole subject is downstream of activation                                                                                                                                  |
| `/2` implementation-ready (`68616dfb…`)    | `docs/control/current.md`, `docs/control/orders/WO-011.jsonl` (four appended events), `docs/work-orders/README.md`, `docs/verifications/WO-011/VER-001.md` (added); nothing else, so every deliverable blob the verifier judged is the one the executor declared ready |
| `/3` verify allocation (`26fa21b0…`)       | the same four paths, the segment carrying three appended events                                                                                                                                                                                   |
| `/4` verdict (`b3a95d23…`)                 | `docs/control/current.md`, `docs/control/orders/WO-011.jsonl` (two appended events), `docs/work-orders/README.md`                                                                                                                                 |
| `/5` final-review allocation (`afcfdbcc…`) | `docs/control/current.md`, `docs/control/orders/WO-011.jsonl` (one appended event), `docs/work-orders/README.md`                                                                                                                                  |

Every deliverable blob is therefore identical across the implementation-ready, verify-allocation, verdict and final-review-allocation checkpoints and the working tree; the verifier changed nothing outside its report except the regenerated index (VER-001 disclosure 2). VER-001's blob `69e03f4a7cd80cd4661bc0f30b95b4deed4241a9` is the same object in checkpoint `/4`, in checkpoint `/5` and in the working tree, so it was not edited after its verdict. All five checkpoints have `HEAD` as their sole parent, and their commit times (`16:15:42Z`, `18:04:30Z`, `18:06:44Z`, `18:18:25Z`, `18:21:26Z`) match the segment's `recordedAt` stamps to the second.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched (`git stash list` is empty). `docs/intake/{chats,images,notes}` in this worktree each hold only `.gitkeep`, and `git status --porcelain --ignored` lists no ignored path outside `.control-beacons/`, `node_modules/`, the three `packages/*/dist/` outputs, `.tsbuildinfo` files and `.DS_Store`. There is no `.claude/settings.local.json` here. The probe stores this review created live under the session temporary directory and `/tmp/claude/`, outside the repository, and were removed. `packages/kernel/` is untouched (`git status --porcelain packages/kernel/` is empty); `docs/decisions/0002-kernel-first-agentic-core.md` changes only by an appended Amendments entry.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                        | Report header and admissibility                                                                                                                                                                                                                                                                                                                                                                                               |
| -------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.4","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`    | there is no executor report; the evidence README and the discovery record state that the operator selected the model and effort under the 2026-09-04 default and that this is not effective readback; `0.153.4` is on the `codex-cli` observed `versions` list (WO-029, 2026-09-05) and the persisted `model_reasoning_effort` selector records `max` for that version, so `max` was admissible at append and meets `xhigh+` |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}` | exactly one column-one header in VER-001, parsed and compared with the event actor as JSON in this session: equal; neither carries `accountLabel`; the report's bytes are unchanged since its verdict checkpoint; `2.1.263` is on the `claude-code` observed `versions` list and `max` is on the documented session selector values; `max` meets `xhigh+`                                                                    |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`  | the header above; `reviewer any` is satisfied                                                                                                                                                                                                                                                                                                                                                                                 |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: none` and the JSON form's `effortDrift` is `[{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.263; model claude-fable-5-1; effort max; source self-reported; account not-applicable`, the same drift line, phase `closed`, a `finalReview` elapsed value beside the two existing values, and legal actions `release-close, next, activate`.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree after the dispatch transition and the index refresh, before any review artifact or correction existed, captured to a file without a pipe: `All matched files use Prettier code style!`, the GitHub body-profile suite, the shell suites (fixture temp-root, publication checker, backup-intake, resume, checkpoint, worktree, release and work-orders, each ending `passed`), `PASS index coverage: 231/231 product headings indexed` with both audience editions `CURRENT` (30 and 45 linked sections), `PASS docs/work-orders/README.md is current`, `tsc -b --force`, `# tests 281 / # pass 281 / # fail 0` for the three packages, `# tests 8 / # pass 8 / # fail 0` for the identity corpus, `Verified 4 current artifact evidence files in docs/evidence/WO-011/artifact-identity; original semantic hashes and frozen oracle unchanged.`, `Verified 4 synthetic verification evidence files; planted defect, repair, staleness and replay are green.`, `Verified ten passing regressions, ten removal failures, and 2393 fewer instruction bytes in the matched projection.`; exit 0. The last line is the order's evidence gate executing: it reran all twenty present and removal subprocesses and revalidated the retained live streams against the current pinned source and report.

Second run, on the reviewed tree after the three corrections and with this report, `PR.md` and `RELEASE-NOTES.md` present: the same lines and counts; exit 0. `git diff --check` was clean before and after.

### Static, surface and structural checks

`npm run release -- check-surfaces` on the working tree: `PASS release-block: observed v0.13.0; expected v0.13.0 (work-order target v0.13.0; latest published v0.12.0)`; `PASS component-version @dotln/compiler: src changed; observed 0.6.0; previous v0.12.0 0.5.0; expected a different version`; `PASS component-version @dotln/kernel: src unchanged; observed 0.2.1; previous v0.12.0 0.2.1; expected no bump required`; `PASS component-version @dotln/skeleton: src changed; observed 0.12.0; previous v0.12.0 0.11.0; expected a different version`; `PASS github-body-profile` over this package's two bodies. `parseReleaseNotes` accepted the notes with the five headings in order, and `assertGitHubBodyProfile` accepted both bodies when called directly. `npm ls typescript --omit=dev` resolves `typescript@5.4.5` under `@dotln/skeleton@0.12.0` and nothing else; the lockfile drops `dev: true` on that package and adds it to the skeleton's dependencies, and no other package moved. The public gitmoji catalog, fetched from the project's repository, lists `:alembic:` as "Perform experiments". `gh auth status` inside the sandbox fails to read `~/.config/gh/config.yml` (operation not permitted) while read-only `git ls-remote origin main` succeeds.

A link sweep over the 22 changed or new Markdown files resolved 635 relative links and anchors with GitHub slugging; the only unresolved target was this report's own path in the regenerated index, which now exists. A clean-room term screen and a secret scan over the 5,374 added lines and the new files found only VER-001's own sentence describing its screen; there is no commercial tracker name, predecessor-system name, managed-host or model-gateway description, internal hostname, credential, home path or private identifier; the only e-mail addresses are `example.invalid` fixtures and the public `noreply@anthropic.com` used to exercise the predicate, and every URL is public.

### Independent probes against the deliverable

1. **Recomputation.** `readFeedbackSource` over the current tree returns `sha256:410d1f837cac558d3b2176a94e9f8cf616f4a6c9830b12ff9be972527f200c95` for exactly seventeen files, the value recorded in `feedback.json`, the audit `CommandResult` and the audit's `FeedbackAuditOpened`; `personalFeedback().policyHash` is `fnv1a64:d0000feb2622b0d4`, the value in both streams; the lowered rungs are anti-oscillation 2, bounded-boy-scout-cleanup 2, concurrent-work-requires-worktrees 2, correctness-over-sycophancy 5, fail-conservative-correction 3, no-attribution 1, no-lint-type-disables-as-fixes 1, no-partial-completion 6, read-your-own-output 6 and verify-app-before-done 6, matching product 02's hierarchy numbering; `feedbackContextAccounting` over `environment.json`'s startup-context files returns 3,530, 2,495, 102, 6,025, 3,632 and 2,393 bytes; and the verifier subject over the seventeen files plus the report recomputes to `sha256:6fb91aab163c29b8700145fc3b673f6c51b05c8e694196a9a9670844865cfb11`, the revision in the retained `VerificationOpened`.
2. **Retained streams.** The audit stream holds five `feedback-host` events in order, `FeedbackAuditOpened`, `FeedbackAuditRequested`, `CommandPersisted`, `FeedbackAuditExecutionRequested` and `CommandResult` for `cmd_eade8c6d3b76891a`, over base commit `d6179e18…` with an authority envelope allowing only `feedback.audit`, denying `repo.write`, `repo.delete` and `publish`, and expiring after an hour. The verification stream holds 958 events: `VerificationHostConfigured` with `claude-cli-print`, `2.1.263`, `claude-sonnet-5`, `max`; `VerificationOpened` whose subject paths equal exactly the seventeen pinned files plus `docs/evidence/WO-011/feedback.json` and whose baseline holds the nine pinned files that existed at the base with both witnesses `unavailable`; two `WorkerAttemptStarted` with identical `inputHash` `fnv1a64:574dc236bc98ac20` and `limits` `{timeoutMs: 600000, maxBudgetUsd: "3.00"}`; 944 one-second heartbeats; `WorkerInterrupted` with reason `invalid-result` for `ep_verifier_1_attempt_1` after about 453 s, then `WorkerLeaseExpired`; and `ep_verifier_1_attempt_2` completing after about 494 s with `VerificationWorkerResultObserved`, `CommandResult`, `WorkerCompleted` and `VerificationContinuation`. Re-projecting the log gives phase `complete`, both rows `verified`, zero stale evaluations, zero findings, and each accepted evaluation carrying `provenance.kind: "host-admitted-verifier"` for `cmd_3b843b5108e71409` with that input hash. The raw log contains neither `VER-001` nor the evidence README's path; the ideation receipt is absent.
3. **The shipped CLI, deterministic transport.** `node packages/skeleton/dist/src/dotln.js feedback-audit --store /tmp/claude/… --transport fake` on macOS ran its twenty subprocesses and then printed `worker host refused; inspect the store and declared environment before retrying`, exit 1, leaving the audit store complete and the verifier store at `pending=1`, phase `verify`, both rows `incomplete`, four events. Calling `runFeedbackSelfhost` directly on a fresh `/tmp/claude/…` store throws `feedback verifier mount alias`: `/tmp` is a symlink to `/private/tmp`, and the driver refuses a mount whose resolved path differs from the path given. The same command over a realpath-resolved store under the session temporary directory completed: `WO-011-feedback-audit`, phase `complete`, both rows `verified`, ten fixtures, 2,393 bytes saved, one transport dispatch, about 4.7 s. Rerunning on the completed store returned the identical result with zero dispatches in about 0.7 s, so recovery reuses the saved audit and the completed verification. This session's `$TMPDIR` is itself `/tmp/claude-501`, so its unresolved form reproduces the same refusal.
4. **Direct ablation.** `node --test --test-name-pattern='^WO-011 regression read-your-own-output$'` over the built fixture: present, `ok 1`, `# pass 1 / # fail 0`; with `DOTLN_FEEDBACK_ABLATE=read-your-own-output`, `not ok 1`, `code: 'ERR_ASSERTION'`, `# pass 0 / # fail 1`. The variable is read by the fixture module and scrubbed by the audit host's subprocess environment; no production module consults it.
5. **Scanner cases written for this review.** A shebang followed by `@ts-nocheck`, a string containing `*/` followed by a real block directive, a regex character class containing `/` followed by a trailing `eslint-disable-line`, a JSX attribute-expression block comment, and CRLF line endings each yielded the directive; a nested template literal and a string inside JSX text each yielded nothing; a `.py` path is out of scope; a `// eslint-disable` inside a string in a `.mts` file yielded nothing. One curiosity: an unterminated block comment at end of file returns its body minus the last two characters (`@ts-ignor`), because the slice assumes a closing `*/`; such a file is a syntax error and cannot be a green repair (adjudication 10).
6. **Attribution predicate.** `Claude-Session: https://…` and `Assisted-by: Claude` trailers are not matched, a `Co-authored-by: Claude Opus` trailer and an emoji-prefixed `Generated with Claude Code` footer are matched, and the plain commit subjects and bodies used below are not matched (adjudication 1).

### Reading

The complete diff, all six new source leaves, both new scripts, the three new test files, the executor receipt, both retained streams' non-heartbeat events, VER-001 in full, the work order with its activation completion and ideation receipt, and every changed document were read. `compileFeedbackUnits`, `evaluateFeedback` with `reason`, `applyFeedbackCorrection`, `feedbackDecision`, `runFeedbackRegressions` and `runFeedbackSelfhost` were read clause by clause against the criteria.

## Acceptance criteria

1. **Each unit carries the full FeedbackUnit shape.** Met. The catalog declares exactly the roadmap's ten; the compiler bounds the inventory at ten independently, re-copies every field positively, requires `synthesized-from-public-lineage` treatment and public references, refuses references under `docs/intake/`, and refuses duplicates, handler mismatches and co-equipped supersession; the compiler test deletes each field in turn and expects refusal. The rung choices are the cheapest sufficient ones and each rationale holds: a text lint cannot decide which prior rejection applies, a static test cannot see live writer reservations, a guard cannot remember a correction across later effects.
2. **Each regression fails when its mechanism is removed.** Met, causally. Removal recompiles the profile without the unit, so `evaluateFeedback` finds no mechanism for that handler and the boundary permits the effect; the gate requires exit 1, `# pass 0`, `# fail 1`, a `not ok` line naming the test and an `ERR_ASSERTION`. Twenty runs re-executed twice here inside `npm test` and one rerun raw.
3. **Startup-context reduction measured against the WO-004 baseline, method stated.** Met. The method, baseline reference, common baseline, prose equivalent, residue and limits are recorded in the report and recomputed here; the generator refuses a non-positive saving; the stated limits are accurate and the construction is conservative.
4. **One work order through compile, dispatch, execute and verify inside DotLn.** Met with a live witnessed run whose four stages are separately visible in the retained streams, whose refused first attempt is preserved rather than trimmed, and whose pinned bytes are the bytes in the tree. The deterministic path was additionally driven end to end here through the shipped CLI, including recovery.
5. **Fail-conservative correction on semantic events only.** Met. Neither `applyFeedbackCorrection` nor the reactor branch reads message text; the trigger is the conjunction of `actorId === "operator"` and one of four typed events; the transition only removes wildcard and destructive effects, forces scope expansion off and evidence preservation and diagnosis on, and is idempotent; a pending diagnosis refuses both `FeedbackAuditRequested` and `FeedbackAuditExecutionRequested`; worker-authored corrections and an operator message with profanity leave the policy unchanged, under live and replayed decision equality.

## Evidence gate

Fixture runs with mechanism present and removed: captured in `feedback.json` and re-executed by the gate. The context-reduction measurement: recorded and recomputed. The witnessed self-hosted run: the two retained streams, validated against the current source and report by the gate and by this review's recomputation. Every criterion maps to a named test or witnessed run in `docs/evidence/WO-011/README.md`, and each mapping resolves to a test that exercises the claim.

## Write-back duty

Discharged. Product 02 gains §Feedback compiler v1 and the sentence that the decision-lineage guard is implemented for equipped boundaries; the ledger gains two dated append-only sections (the ideation batch and the implementation) with no rewrite of earlier entries; product 07 names the absorbed duties in §Model-specific notes, replaces the anti-oscillation sentence with a link to the equipped host, adds the disposable-backup rule and the corpus link; product 03, 06, 10 and 12, ADR-0002's Amendments, `docs/LEGAL.md`, the root README release block, both package READMEs, the discovery record, the planning map, the publication index and both locks, and the work order's header and receipt carry the same implemented boundary. The caveat that ordinary `resume` sessions still follow the guide and that the measurement does not authorize stripping rules from unrelated startup files is present.

## Ideation breakout receipt

Verified as subject. The source treatment is Clean Room with Shape-First Synthesis and public vocabulary, no direct-filing exception is claimed or implied, and canonical intake is named rather than duplicated. Products 03 and 12 and the planning map label the sibling repositories and integrations as operator-reported intentions, state what a pilot would have to witness, and select no stack, provider or authentication service; the corpus policy treats artifact count as an observation and refuses to infer a quota, a growth finding, a mandatory cleanup or a layout while protecting immutable evidence and recovery contracts; ledger statuses are `adopted` for promoted constraints and `preserved` for pilots and candidates, the later clarification narrows rather than overwrites and names the superseded fork suggestion, and the open items are listed as open. No ADR or resolution is relitigated; ADR-0002 is extended only through Amendments. The receipt carries no backup ZIP filename. The breakout introduced no executable helper.

## Verification-sequence soundness

One report, no repair episode, no re-verification. VER-001 read the original work order, the subject diff and the retained evidence, reproduced every criterion by a path outside the deliverable's generator where possible, disclosed its instruments, and recorded six non-blocking observations and no correction. Its header equals its control-log actor by JSON comparison and its bytes are unchanged since its verdict. Its observations are adjudicated below; none was found to conceal a defect.

## Adjudications

### 1. The attribution predicate does not match a session-URL trailer (VER-001 O1)

Sustained as a candidate, applied concretely here. `hasAiAttribution` matches co-author trailers and generated-with footers, and this review reproduced the miss on `Claude-Session:` and `Assisted-by:` trailers. The roadmap asks for a precise predicate and the criterion is met; the equipped invocation settings suppress the session trailer at generation, so the defence-in-depth pair holds where both rungs apply; and widening a hard-denial predicate in a pinned source file is a behavior change outside a reviewer's authority that would also invalidate the live witness. This review's own harness instructs exactly such a trailer; the operator's standing rule and the unit forbid it; none is added.

### 2. The pinned verifier subject excludes the evidence generator and the host tests (VER-001 O2)

Sustained as a note. The blinded verifier judged the seventeen implementation and fixture files plus the report, which is what its two criteria are about; the repository verifier and this review read the generator, the compiler tests and the host tests. The notes state the capsule's coverage.

### 3. `operatorPreference` is carried and unconsulted (VER-001 O3)

Correct by design and the stronger reading of correctness-over-sycophancy; `reason()` depends only on consistent independent witnesses for the current subject. Note only.

### 4. One workstream branch per store (VER-001 O4)

A standing structural constraint shared with WO-010's `state.verification`; the self-hosted path uses separate audit and verifier stores. Named in the notes' read-before-upgrading section.

### 5. Advisory enforcement disables the correction reactor (VER-001 O5)

Consistent with product 02, which requires another profile to declare its own correction policy; the shipped profile is hard. Note only.

### 6. `npm ci` is executor-reported (VER-001 O6)

Not rerun here for the verifier's reason: a cache miss would leave the worktree unbuildable mid-review, and release close runs `npm ci` before tagging in any case. The reachable facts were checked: the lockfile entry and `npm ls typescript --omit=dev`.

### 7. The verifier's disclosures (VER-001 D1–D3)

Mirrored above. The subject supplied the instruments, the control plane is a prior deliverable, and the live episode cannot be re-run; this review recomputed rather than trusted, dogfooded the unchanged lifecycle script, and validated the retained streams against current bytes.

### 8. The runbook's copy-paste command fails on macOS (this review)

Corrected. `packages/skeleton/README.md` gave `--store /tmp/dotln-feedback-audit` for the live command, the recording command and the status example. On macOS that store runs the full audit and is then refused at the verifier mount with the generic CLI sentence, exactly the class WO-010's final review corrected in the verification runbook. The three occurrences now use the gitignored `.runtime/feedback-audit`, and one sentence states the canonical-path rule, naming the `/tmp` symlink as the refused form and `/private/tmp/…` or a repository-relative directory as accepted. Writing `/private/tmp/…` as the headline path was considered and not sustained: correct on macOS, wrong elsewhere.

### 9. The CLI's refusal message hides the cause (WO-010 FINAL-001 adjudication 8)

Reproduced: the mount-alias refusal prints the same sentence as every other worker-host refusal, after the twenty subprocesses have already run. The candidate stands; a behavior change is outside a reviewer's authority.

### 10. The scanner's slice assumes a terminated block comment (this review)

An unterminated `/*` at end of file returns a body missing its last two characters, so a directive there would not match. Such a file does not compile and cannot present as a green repair, `@ts-nocheck` has nothing after it to affect, and every terminated form behaves correctly. Recorded, not scored.

### 11. The executor edited the scope-authority document (this review)

The work order's changes are the title, the release classification sentence and the `Cites` anchor, which are the activation completion the roadmap's retiming requires, plus the appended ideation receipt the execution guide requires. No acceptance criterion, constraint, non-goal or evidence-gate sentence changed.

### 12. Kernel, schema and contract axes (this review)

`packages/kernel/` is byte-unchanged; the event-envelope schema and the compiled-loadout contract stay at `1`; `feedback-v1` is a separate optional contract; the artifact-identity evidence proves every existing semantic hash unchanged under compiler `0.6.0`; the WO-010 verification edition regenerates identically under the new destination.

### 13. Precedence of the feedback branch (this review)

`seiriReactor` tests `FeedbackAuditOpened` or `state.feedback` before `state.verification`. Because the two workstreams live in separate stores and a store with `feedback` set routes foreign-workstream events to `feedback-outside-workstream` without folding them, no existing verification store changes behavior; adjudication 4 names the constraint.

### Candidates considered and not sustained

- **Extending `hasAiAttribution` to session-URL trailers** (adjudication 1): a behavior change in a pinned source file; candidate.
- **Writing `/private/tmp/…` as the headline runbook path** (adjudication 8): correct on macOS, wrong elsewhere.
- **Adding a diagnostic hint to the CLI refusal** (adjudication 9): a behavior change is outside a reviewer's authority.
- **Rerunning the live verifier** (adjudication 7): cost and time for no verdict-changing information, since the retained streams pin the current bytes.
- **Rerunning `npm ci`** (adjudication 6): risk to the worktree; release close covers it.
- **Failing the review over the in-sandbox `/tmp` refusal** (adjudication 8): the refusal is the product's canonical-path rule doing its job, the runbook is for an ordinary shell, and the canonical path completes.

## Corrections applied by this review

Three corrections, all in the documentation lane `docs/final-reviews/README.md` grants, each applied by exact string replacement with a script that refused unless the old string occurred exactly once, proven contained by `git diff` against checkpoint 5, and followed by the checks they could affect: `npx prettier --check` on the two Prettier-checked files, `git diff --check`, `node scripts/check-publication.mjs` (231 of 231, both locks current), `readFeedbackSource` (pin unchanged, since none of the files is pinned), and the full `npm test` (the second run). They are committed with the implementation because each documents behavior that implementation introduced.

1. **`packages/skeleton/README.md`:** the feedback-audit runbook's live command, recording command and status example use `.runtime/feedback-audit` instead of `/tmp/dotln-feedback-audit`, and one sentence after the status example states that the store lives under the gitignored `.runtime/` root and must be canonical, naming the macOS `/tmp` symlink as refused after the audit has run and `/private/tmp/…` or a repository-relative directory as accepted; the paragraph is rewrapped to the file's width (adjudication 8; 10 insertions, 5 deletions against the executor's text).
2. **`README.md`:** the release block's curly apostrophe in "repository's" becomes the straight apostrophe the file uses everywhere else (1 line).
3. **`docs/discovery/environment.md`:** one of two consecutive blank lines before the new `## WO-011 bounded harness observation (2026-09-06)` heading is removed (1 deletion).

The three files this review adds, this report, `PR.md` and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet, no guard was lost, and no behavioral or evidence defect was found in the deliverable that any criterion covers. The items below are recorded and not scored.

1. **The runbook's store path was refused on macOS** (adjudication 8): corrected; the canonical-path rule is now stated.
2. **The CLI refusal message is deliberately generic** (adjudication 9): the mount-alias cause is visible only at module level; candidate.
3. **The attribution predicate does not match a session-URL trailer** (adjudication 1): candidate; the equipped settings cover generation.
4. **The pinned verifier subject excludes the generator and the host tests** (adjudication 2): note.
5. **One workstream kind per store** (adjudication 4) and **an advisory profile disables the correction reactor** (adjudication 5): notes.
6. **The clean-install claim is executor-reported** (adjudication 6): release close runs it.
7. **The scanner's unterminated-comment slice** (adjudication 10): curiosity.
8. **A repository-relative store cannot be exercised inside the Claude Code sandbox** (WO-010 FINAL-001): the harness protects nested Git configuration under the project; run the runbook from an ordinary shell.
9. **The live verifier's effective model and effort are unknown** and **`CLAUDE_EFFORT` remains an unrecorded readback candidate** (§Actor), so Claude Code attestations stay `self-reported`.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step and the sandbox.** `gh` cannot read its configuration under this sandbox (`~/.config/gh` is denied), the same limit every reviewed order since WO-006 has met, while read-only `git ls-remote` to `origin` succeeds. The reviewed series is committed here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation and notes parse run against the exact commit; if it refuses at the `gh` preflight it pushes nothing and the operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute command the publisher prints: change directory into the main checkout and run this worktree's `scripts/release.mjs close WO-011 --publish` with node, outside the sandbox and where `gh` is authenticated. Because `v0.13.0` is strictly above `v0.12.0`, the reviewed subject helpers finish this worktree, run the closed-state gates, fast-forward main, run `npm ci` and the evidence gate, assemble the five-section edition from these notes, create and push the annotated `v0.13.0` tag, and create the matching GitHub Release. If Release creation fails after the tag push, the rerun is `npm run release -- close WO-011 --publish` from updated main.
- **Attribution:** adjudication 1 and instrument 6.
- **Ledger duty:** discharged by the executor's two dated sections; this review adds no ledger entry because every open item above is either already in VER-001 or is a candidate this report names for a patch or the next planning pass.
- **Intermediate commits are not individually gated.** The first commit's tree carries the pre-existing generated index over a changed work-order header, so `index --check` would refuse it in isolation; only the final tree is what `npm test`, `check-surfaces --committed` and the publisher judge.

## Proposed PR

- **Title:** `:alembic: WO-011: Ten operator corrections compiled into executable feedback checks and the first bounded self-hosted audit; v0.13.0`. The shortcode is the catalog's "Perform experiments" entry, chosen because the rung's stated exit is the strangler experiment that ADR-0001 decided and the roadmap names this order as its vehicle: the first work order that DotLn compiles, dispatches, executes and verifies against its own repository. `:safety_vest:` (validation) was WO-010's purpose and names only the boundary checks; `:goal_net:` (catch errors) names only what the fixtures do; `:building_construction:` would overstate a change that leaves the kernel and the decider boundary untouched. The title names the change and its version and leaves the inventory and validation to the body, per 08 §PRs and commits.
- **Body:** `docs/final-reviews/WO-011/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Commit series

Two commits, each with a plain subject and an explanatory body and no trailer, in this order, followed by a check that `git diff HEAD` is empty and that the final tree equals the post-result working tree:

1. **Implementation:** the compiler (`feedback.ts`, `index.ts`, `artifact-identity.ts`, `package.json`, `README.md` and `test/feedback.test.ts`); the skeleton (`loadouts/feedback.ts`, `feedback-boundary.ts`, `feedback-source-comments.ts`, `feedback-audit.ts`, `feedback-selfhost.ts`, `reactor.ts`, `dotln.ts`, `verification-host.ts`, `verification-protocol.ts`, `worker-transport.ts`, `package.json`, `README.md` and its two tests); `package.json`, `package-lock.json` and `.prettierignore`; the scripts (`feedback-commit-msg.mjs`, `feedback-evidence.mjs`, `artifact-identity-evidence.mjs`, `verification-evidence.mjs`); the root README release block; `docs/product/02`, `03`, `06`, `07`, `10` and `12`; ADR-0002's Amendments and `docs/LEGAL.md`; the discovery record; the planning map; the publication index and both edition locks; the ledger's two dated sections; the work order's completed activation header and ideation receipt; the executor evidence under `docs/evidence/WO-011/`; and the three corrections above.
2. **Independent review evidence and closed control state:** `docs/verifications/WO-011/VER-001.md`, this report, `PR.md`, `RELEASE-NOTES.md`, `docs/control/orders/WO-011.jsonl`, `docs/control/current.md`, and the regenerated `docs/work-orders/README.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commits (the committed surface check requires the closed control state, and it makes the index's check mark true); the index is regenerated after the result and checked; the reviewed state is committed as the series above. The publisher is then run from this worktree:

```bash
npm run worktree -- publish WO-011 --title ':alembic: WO-011: Ten operator corrections compiled into executable feedback checks and the first bounded self-hosted audit; v0.13.0' --body-file docs/final-reviews/WO-011/PR.md
```

The helper runs `check-surfaces --committed WO-011` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-011`, opens the PR against `main`, and prints the release-close handoff in the absolute subject-helper form. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, the operator runs that printed command with the main checkout as the working directory; the expected outcome is the annotated `v0.13.0` tag and its GitHub Release.

## Method

Everything above was re-established in this session inside the Claude Code sandbox: `npm test` twice, captured to files without a pipe; the read-only status projection and the transition; a temporary-index tree and `git diff-tree -r` against all five checkpoints; the segment read raw and compared as JSON with the verifier's header; the tag peel locally and on `origin`; the built modules called directly to recompute the pin, policy hash, accounting and verifier subject; the retained streams parsed with a script written for this review and re-projected through the WO-010 projection; the shipped CLI driven from a `/tmp` store and from a realpath-resolved store with the deterministic transport, the module-level diagnosis of the refusal, and the rerun for recovery; one ablation as a raw subprocess; the scanner and attribution probes as scripts in the session temporary directory; `check-surfaces`, the notes parser and the body profile called directly; the term screen, secret scan and link sweep as one-off scripts; a fetch of the public gitmoji catalog; and a full reading of the diff, the new files, the tests, the evidence and VER-001. No probe wrote into the repository; the corrections and the three package artifacts are the only files this review changed or added.
