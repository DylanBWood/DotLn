# WO-160 FINAL-001 — final review

**Verdict:** pass. WO-160 asked for the nine DotLn machinery defects the WO-111 cycle met to be fixed at their seam, each with a fixture, so the next order that meets one runs a command instead of improvising: an execution-amendment row can be withdrawn and an unmatched one is reported by row; `entropy subject` considers only filed reviews; `release prepare` names every file it wrote; non-envelope evidence JSONL is declared beside its order; the integrate helper makes its own hookless preservation and merge commits, records its phase, is admitted in `repairing` and `final-review` and adopts its own stash on a clean tree; `test:docs -- --against` labels each failing check `introduced` or `inherited`; `harness prune` lists and, under `--apply`, removes published-order integration stashes after writing their byte inventory; a required output the session itself wrote last counts as read; and completion advises when the current tree has no passing `test:docs` row. That is what the branch delivers. Three independent verifications judged it: [VER-001](../../verifications/WO-160/VER-001.md) failed criteria 7 and 8 with two regressions, the repair fixed all four, [VER-002](../../verifications/WO-160/VER-002.md) failed criterion 7 again on a reproduced race with Git's own `pack-refs --prune`, the repair adopted Git's packed-row rule with a real concurrent regression, and [VER-003](../../verifications/WO-160/VER-003.md) reproduced the repairs against real Git and passed all ten criteria. This review integrated `main`, which had moved by WO-161's `v0.50.0`, re-established each criterion on the integrated tree, ran the product gate there, and found no acceptance defect. Its own integration exercised items 3 and 5 against this repository, and its own red document gate exercised item 6.

**Subject:** [`docs/work-orders/WO-160-wo111-machinery-followups.md`](../../work-orders/WO-160-wo111-machinery-followups.md) on branch `wo-160`, uncommitted over HEAD `6f6aad9ad5da8a39c170f5472ebb71dd4a8dd08d` (`main` after WO-161), which this review reached by integrating `main` from the original base `852861d337fefb3f4c42694db237f60a6e9ea5c6`, the tree every verification judged. The dispatch checkpoint is `refs/dotln/checkpoint/WO-160/13` (`30ba714b`); the integration's hookless preservation commit is checkpoint `/14` (`aa37db33`).
- VER-003's recorded `reportHash` equals the report's current SHA-256 (`5b680f2d…`), and so do VER-002's (`5bbe4629…`) and VER-001's (`2744a564…`).
- Against VER-003's subject, the change outside `docs/` is exactly the integration: `main`'s WO-161 diff, the three version files retimed to skeleton `0.43.1`, the 14 regenerated hooks and the manifest (a new runtime snapshot id and file hashes), and the merged runner argument parsing. No WO-160 source line changed.
- The judged subject is the staged tree: 55 paths against `main` (2,377 insertions, 209 deletions) before this review's own records. The product gate recorded tree `a7b6173d21f8dedd79663221d05195af5f8ccbbb` and code identity `793f77c90961627739dbe9767685f20fc1ea1f44b0f724504975b83e37355f96`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-fable-5-1","effort":"xhigh","source":"claude-session-readback"}

Human actor: the operator selected Claude Fable 5.1 as the session model, dispatched `resume: final review` in a Claude Code session, and made no other choice during the review. The harness version is the host's own (`2.1.282`). Effort `xhigh` is the host's `CLAUDE_EFFORT` value read by this session: selected effort, not effective. The order asks for `reviewer any`. Fan-out plan, stated before any spawn: none. The subject diff is 2,377 lines across 55 files that three verifiers had already probed, so one reviewer reading the whole diff and running every check was the smaller plan that still covered the change. Harness readback: 0 subagents observed of the cap of 20 (exact-observed).

**Process cost:** entry 86260 tokens; handoff 7000553 tokens; source claude-transcript-message-usage

Both readings are `dispatch` scope, from `node scripts/harness.mjs usage 743c863c-b53c-4eec-b88d-aeb55ff5f25f`. The entry reading was observed at 2026-09-25T20:12:24.926Z. The handoff reading was observed at 2026-09-25T20:33:36.695Z (98 steps, 92 commands, 0 subagents of cap 20, exact-observed), after the judgment text was complete and before the result transition. Both include reused cached input. Reasoning tokens and dollar cost are unavailable from this counter, which means unknown, not zero. The largest waits were the product gate (570.89 s) and the integrate helper's two regeneration passes; this session spent the gate reading the subject through the live-gate list.

## Goal-aligned judgment

The order is machinery on the route to dependable unattended work, not a lettered critical-path gate: each item replaces a recurring rescue the WO-111 cycle paid by hand (a hand-typed hookless commit, a hand judgment that a red gate was inherited, a kernel fixture edit for an evidence stream, an orphaned planning row the gate passed by failing to match) with a command and a fixture. The judgment that matters is whether each command does in real use what its fixture claims. **Rule beating** decided how I reviewed: VER-001 and VER-002 each found a green fixture standing in for behaviour the real repository or real Git did not share (a packed `refs/stash`, a foreign write inside a tool window, a `pack-refs` interleaving), so I did not take VER-003's pass on its fixtures alone. I read the whole subject diff, and this review's own integration exercised items 3 and 5 against this repository: the helper ran in `final-review`, made its preservation commit hookless and recorded it, and `release prepare` named the one file it wrote; and when a draft of this review's release notes turned the document gate red, item 6 labeled that failure `introduced` against `6f6aad9a` without being asked. **Shifting the burden** is the outcome the order buys: the reviewer no longer types `core.hooksPath=/dev/null`, and the nine published integration stashes on this stack now have a command that can remove them with an inventory. **Fixes that fail** was checked at the two seams the repairs changed twice: the stash drop keeps Git's packed-row rule and its locks, and the observer credits only a named destination, so neither repair reopened the path the earlier one closed. **Naive Interventionism** bounds the integration: six bookkeeping conflicts, one component retime and one edition re-mint, nothing authored into behaviour; the observations the verifiers boarded stay on their register rows. **NoOp** would leave the next order improvising at the same nine seams and leave the planning gate passing row 22 by accident. The remaining lenses are immaterial here: one gate and no agents is the whole load (tragedy of the commons, escalation); the criteria and cold-start ceilings are unchanged and every role is within its ceiling (drift to low performance); the measured outcome is command use, not suite counts (seeking the wrong goal); the existing helpers were kept because their seams are exercised, not for their investment (success to the successful).

## Criteria

- **Criterion 1:** met. `plan amend-order WO-NNN --withdraw N "reason"` appends a schemaVersion 4 `PlanExecutionAmendmentWithdrawn` binding the row's four hashes; `readOverrides` refuses a withdrawal whose binding differs; `currentAmendments` treats a withdrawn row as inert and `requireMatchedAmendments` names an unmatched, un-withdrawn row by order and physical ordinal ([D002](../../evidence/WO-160/decisions.md#wo-160-d002)). The real log carries the row-22 withdrawal as row 25, and on the integrated tree `npm run plan -- check` exits 0. VER-001 and VER-003 reproduced the named failure and the withdrawal in disposable copies; VER-001's F3 (a legacy activation row refused after an admitted appendix) was repaired with the WO-159-shaped fixture ([D014](../../evidence/WO-160/decisions.md#wo-160-d014)). The `plan-refutation` suite passed in this review's gate.
- **Criterion 2:** met. `unconsumedReview` selects only receipts with an `EntropyReviewFiled` event, and `entropySubject` reports `latestFiledReview`. On the integrated tree `npm run entropy -- subject` names `REVIEW-003` with no `undefined`, and `entropy check` returns `status: ok` still listing the pre-mechanism `REFUTATION-001` and `REVIEW-001` ([D003](../../evidence/WO-160/decisions.md#wo-160-d003)). The source is unchanged since VER-001.
- **Criterion 3:** met, and exercised here. `applyReleasePreparation` returns its written paths and the meter write is listed only when bytes differ ([D004](../../evidence/WO-160/decisions.md#wo-160-d004)). During this review's integration the helper's `release prepare --local` printed `WO-160 target v0.51.0 remains current.` and `Files changed: docs/final-reviews/WO-160/PR.md`; VER-003's before/after hash snapshot established that exactly the named file changes, and the `prepare_independent` case asserts the message against the files. The release suites passed in this review's gate.
- **Criterion 4:** met. `evidenceJsonlDeclarations` reads `docs/evidence/WO-NNN/jsonl.json`, refuses escaping targets and empty reasons, and both censuses (`check-registrations` and the kernel `store-history` test) consult it ([D005](../../evidence/WO-160/decisions.md#wo-160-d005)). The two WO-111 entries live in `docs/evidence/WO-111/jsonl.json`, and the kernel fixture keeps only protocol streams. VER-003 reproduced the undeclared-stream failure and the declared pass with the kernel fixture byte-identical; the `registrations` document check and the kernel census passed in this review's gates.
- **Criterion 5:** met, and exercised here. The helper is admitted in `repairing` and `final-review`, records `phase`, makes its preservation checkpoint and any divergent merge commit with `-c core.hooksPath=/dev/null` and records both identities; a clean-tree `--continue` adopts only an unseen stash carrying this integration's name and base and refuses an ambiguous match ([D006](../../evidence/WO-160/decisions.md#wo-160-d006)). This review's `npm run worktree -- integrate WO-160` ran in phase `final-review`, recorded `preservationCommit` `aa37db33` equal to checkpoint `/14`, retained stash `0c2a9dee` under the name `WO-160 integrate 2026-09-25`, fast-forwarded the uncommitted branch so `mergeCommit` is `null`, and completed under `--continue` after the authored resolutions; no hook ran and nothing was typed by hand. The fixture covers the `repairing` phase, the divergent merge commit and the stranded-stash adoption, which this fast-forward did not reach; the `worktree-integration` suite passed in this review's gate. Product 07 §Independent workflows carries the sentence. The FUP-fd5f7c2b91095343 retarget is a close-stage write-back.
- **Criterion 6:** met. `classifyDocumentFailures` reruns each failing document check in a shared, hookless base clone at `--against REV` (default the merge base with `main`) and labels it `introduced`, `inherited` or `unknown`; the current failure stays red ([D007](../../evidence/WO-160/decisions.md#wo-160-d007)). The runner's argument parsing is the one WO-160 seam the integration touched: WO-161 renamed `--inside-sandbox` to `--confined-partial` on the same lines that add `--against`, and I merged both with both usage strings. The `runner` suite, which holds the WO-160 label fixture, passed in this review's gate, and VER-001 to VER-003 each reproduced the labels in disposable copies. It also ran in real use here: a draft of this review's release notes carried angle-bracket placeholders that the GitHub body profile refuses as raw HTML, so `release-surfaces` failed, its seven dependent document checks were left unexecuted, and the gate's default comparison reran the failing check at the merge base `6f6aad9a`, passed there, and printed `introduced release-surfaces against 6f6aad9a…` on the recorded row; the check stayed red until the placeholders were rewritten, and the next document gate was green.
- **Criterion 7:** met. `harness prune` lists integration stashes whose order has a published release with per-blob byte totals, retains unpublished, unnamed, worktree-owned, pending or unsupported ones with a reason, and under `--apply` writes the byte inventory and a ref, reflog and packed-refs recovery snapshot before dropping the exact entry under Git's own locks, keeping the packed row while entries remain ([D008](../../evidence/WO-160/decisions.md#wo-160-d008), [D014](../../evidence/WO-160/decisions.md#wo-160-d014), [D018](../../evidence/WO-160/decisions.md#wo-160-d018)). VER-002 reproduced the sequential behaviour equal to `git stash drop` in 46 checks and VER-003 the concurrent behaviour in 24 twins plus the race, against real Git 2.55.0. On this repository after integration the read-only preview lists nine published-order candidates totalling 3,379,702,472 bytes (WO-158, WO-111, WO-156, WO-154, WO-100, WO-151, WO-120, WO-147, WO-138) and retains eleven with a reason: this order's own `WO-160 integrate 2026-09-25`, because the order still has a registered worktree, and ten unnamed or unrecognized entries; the stack holds 20 entries. No stash was removed; the five WO-160 stash fixtures passed in this review's gate. O29 (an error-path in-place rewrite of unchanged `packed-refs` bytes) is boarded in [D021](../../evidence/WO-160/decisions.md#wo-160-d021).
- **Criterion 8:** met. `observeAuthorship` credits a read at the observed bytes only for a changed path that the tool named as a write destination and whose tool did not report failure; a change by another writer in a later tool window, on another path, or before an explicit observe still advises ([D009](../../evidence/WO-160/decisions.md#wo-160-d009), [D011](../../evidence/WO-160/decisions.md#wo-160-d011), [D014](../../evidence/WO-160/decisions.md#wo-160-d014)). VER-002 reproduced VER-001's two foreign-write probes as repaired and VER-003 confirmed the source unchanged; `packages/skeleton/src/harness-host.ts` is unchanged by the integration and the `process-debt` suite passed in this review's gate. The skeleton label carrying the change is `0.43.1` after the retime. The same-path, same-window limit is disclosed in D014 and boarded.
- **Criterion 9:** met. Completion reads the latest `npm run test:docs` row for the current tree and advises when it is missing, red, partial, unexecuted or unreadable, never blocking ([D010](../../evidence/WO-160/decisions.md#wo-160-d010); VER-001 F4 repaired in D014); the runner now records document-gate rows. The `process-debt` suite passed in this review's gate. Every WO-160 completion event carries `advisories: []` with a passing 21-suite row for its tree.
- **Criterion 10:** met. This review's `npm test -- --review` on the integrated tree passed 36 suites, 0 failed, 570.89 s, 80 fresh tasks, at tree `a7b6173d…` and code identity `793f77c9…`, recorded 2026-09-25T20:31:42.121Z with no confinement in force (`inForce: false`); `npm run test:docs` passed 21 of 21 twice, before and after this report; `git diff --check` and `git diff --cached --check` are clean. The lockfile changes only the two skeleton version lines against `main`, so there is no new dependency. Package source changes are the item-8 observer and its version refresh (D011), plus the item-4 kernel fixture move and its census test (VER-001 O19). No added line carries a lint or type suppression.

## Findings

None. No criterion is unmet and no waiver is recorded.

### Observations

- The integrate helper's first run reported its regeneration build as a pending runtime failure because the conflicted `package.json` files were unparsable during the stash apply; that is the helper working as designed (regeneration waits for authored resolution), and `--continue` completed it. No register row is needed.
- `node scripts/harness.mjs check` prints `local-terms list: unavailable` beside its 31 surfaces on this tree. `checkLocalTerms` returns that status when the optional, ignored `docs/control/local/terms.txt` is absent (`scripts/lib/terms.mjs:15`); it is an operator file outside this order's scope, and `harness check` still exits 0.
- README's "How this repository builds itself" lead now heads the WO-160 sentence, and the economy-experiment sentences follow as their own paragraph without a bold lead. Editorial only; the publication check is current.
- This review's own defect, fixed before any record: the first draft of `RELEASE-NOTES.md`, `PR.md` and this report spelled command placeholders as `<row>`, `<reason>`, `<date>` and `<rev>` inside code spans, which `release-notes.mjs` refuses as raw HTML in reviewed notes; all three now spell them `N`, `"reason"`, `YYYY-MM-DD` and `REV`. The `release-surfaces` preflight gates the other document checks, so one refusal left seven checks unexecuted on that row.

## Integration with `main`

`main` gained WO-161 (`v0.50.0`, tag present locally; compiler `0.19.1`, skeleton `0.43.0`; the host-confinement rename in the runner; WO-161's four editions; 97 files). The helper fast-forwarded the uncommitted branch from `852861d3` to `6f6aad9a` under its named stash and regenerated 21 projections; six authored conflicts remained, all bookkeeping: `packages/skeleton/package.json`, `packages/console/package.json` and `package-lock.json` (the skeleton bump retimed from `0.42.1` to `0.43.1` over `main`'s `0.43.0`, the same compatible bugfix), `scripts/test-runner.mjs` (WO-161's `--confined-partial` rename merged with item 6's `--against` parsing; no `--inside-sandbox` spelling remains), `docs/evidence/current.json` (authority `WO-160/002`, re-minted by this review on the integrated source; artifact identity, verification and feedback stay at `WO-161/001`) and `docs/product/06-roadmap.md` (both orders' activation notes plus a dated retime note). The application target `v0.51.0` remains current. Both bases, the resolved paths, the carried-forward claims and the checks on the integrated result are in [D022](../../evidence/WO-160/decisions.md#wo-160-d022). Outside `docs/` and the generated surfaces, the staged tree differs from VER-003's subject exactly by `main`'s WO-161 diff, the three version files and the merged runner lines. The stash and checkpoint 14 are retained.

## Verification sequence

Implementation ran on Codex CLI 0.157.0, `gpt-6-astra`, `ultra` (recorded `xhigh`, mode subagents), and closed under D011's operator-authorized item-8 exception with planning-log rows 24 and 26 binding the scope. [VER-001](../../verifications/WO-160/VER-001.md) (Claude Code 2.1.282, `claude-opus-5-5`, `ultracode` recorded `xhigh`/subagents, four read-only agents) failed criteria 7 and 8 on two reproduced defects (a packed `refs/stash` the prune could not remove yet listed; a foreign write credited as the session's read) and routed two regressions with them (a legacy activation row refused after an admitted appendix; an unreadable gate archive blocking completion). The first repair (Codex CLI 0.157.0, `gpt-6-sol`) fixed all four with fixtures ([D014](../../evidence/WO-160/decisions.md#wo-160-d014)) and boarded O1–O19 through the adjacent queue ([D015](../../evidence/WO-160/decisions.md#wo-160-d015)). [VER-002](../../verifications/WO-160/VER-002.md) (Claude Code 2.1.282, `claude-opus-5-5`, `xhigh`, one read-only agent) reproduced the four repairs and failed criterion 7 on F5: a non-top prune racing Git's own `pack-refs --prune` made `refs/stash` unreachable where `git stash drop` survives. The second repair (Codex CLI 0.157.0, `gpt-6-astra`) kept the packed row while entries remain with a real concurrent regression, repaired O22 and retimed the target to `v0.51.0` ([D018](../../evidence/WO-160/decisions.md#wo-160-d018) to [D020](../../evidence/WO-160/decisions.md#wo-160-d020)). [VER-003](../../verifications/WO-160/VER-003.md) (Claude Code 2.1.282, `claude-opus-5-5`, `xhigh`, no agents) reproduced F5 as fixed against real Git in the race and in 24 of 24 sequential twins, confirmed the pre-repair code fails the same probes, passed all ten criteria at code identity `60530a60…` and boarded O29 ([D021](../../evidence/WO-160/decisions.md#wo-160-d021)). Every verification ran on a different harness and model from the implementation and repairs. The sequence is consistent: each fail names reproduced behaviour, each repair names its finding, and the pass judges its recorded subject.

Ideation receipt: the [2026-09-25 off-ramps planning document](../../planning/off-ramps-5s-entropy-2026-09-25.md) §2 reconstructs the WO-111 failures and §5 names this order's nine items. It filed the order with "no package source changes and no edition re-mint"; the operator's D011 authorization during execution bounded one exception (the item-8 observer and its evidence refresh), recorded in the order's execution record and bound in the planning log by rows 24 and 26, and `plan check` passes on the integrated tree.

## Checks

Executed by this reviewer on the integrated tree, 2026-09-25:

| Check | Result |
| --- | --- |
| `npm run worktree -- integrate WO-160 --intake-backup …`, then `--continue` | complete; phase `final-review`; preservation `aa37db33`; stash `0c2a9dee`; 6 authored conflicts resolved by hand |
| `npm run release -- check-surfaces --local` | PASS on every surface |
| `node scripts/authority-evidence.mjs --write --edition WO-160 --revision 002`, then `--check` | recorded, then verified; 34 bundle comparisons |
| `npm run evidence:artifact -- --check`; `npm run evidence:verification -- --check` | verified at `WO-161/001` |
| `node scripts/feedback-evidence.mjs --check` | verified; WO-159's live audit carried by `WO-161/feedback-001` |
| `npm run evidence:console -- --check` | control, refutations and missing match |
| `node scripts/harness.mjs check`; `node scripts/harness-context.mjs --check` | 31 surfaces; executor 25,723/29,246, verifier 22,526/25,151, reviewer 23,608/24,576, release-close 14,632/16,384, planner 16,780/24,576, refuter 16,355 unset; every ceiling within |
| `npm run plan -- check`; `npm run entropy -- check`; `npm run entropy -- subject` | exit 0; `status: ok`; `REVIEW-003`, no `undefined` |
| `npm run publication:check` | PASS; both editions CURRENT (30 and 45 linked sections) |
| `git diff --check`; `git diff --cached --check` | clean |
| `node scripts/harness.mjs prune` (read-only preview) | 9 candidates (3,379,702,472 bytes); 11 retained with reasons; nothing removed |
| `npm run test:docs` | 21 passed, 0 failed, 24.06 s before the reports; 13 passed, 8 failed, 6.85 s on the first draft of the notes (`release-surfaces` refused raw HTML; seven dependents unexecuted; item 6 labeled it `introduced` against `6f6aad9a`); 21 passed, 0 failed, 24.16 s after the fix; rerun once more after this correction, the local gate receipts hold that row |
| `npm test -- --review` (Claude Code shell; no confinement in force) | **36 suites, 0 failed, 570.89 s, 80 fresh tasks, exit 0** |

## Handoff

Result: `final-review-result pass` with the gate row above. The reviewed state is committed as a series of coherent changes, the `wo-160` branch is pushed, and its pull request is opened with the contained PR body; nothing is merged, no `main` is pushed and no release or package is published. After the operator merges, the helper's post-merge release-close handoff applies. At close, FUP-fd5f7c2b91095343 and FUP-dcadda81305b4fb7 are retargeted through the adjacent queue and the boarded observations (D013, D015, D017, D019, D021) are nominated under FUP-dd20beb6b16ea86c, as the order's write-back duty names.
