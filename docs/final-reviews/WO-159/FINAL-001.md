# WO-159 FINAL-001 — final review

**Verdict:** pass. WO-159 asked for one launch site for every DotLn-launched Codex worker, verifier and probe, a per-episode home seeded only with what authentication needs, the operator's user-level Codex configuration byte-identical before and after each launch with the digest pairs on the receipt's protected surfaces, and a correction of the WO-054 claim that continuation changes no user configuration. That is what the branch delivers. `packages/skeleton/src/worker-transport.ts` now holds the only builder of a Codex `exec` argv (`codexExecArgv`, the sole owner of `--ephemeral --ignore-user-config`) and the episode launcher (`startCodexEpisode`), which creates a `0o700` home under system temp holding one symlink to the operator's `auth.json`, hands the caller an environment with that `CODEX_HOME`, and on `finish()` digests the user-level `config.toml` and its `[projects]` trust table again, counts the trust entries the CLI wrote into the isolated home, removes it and returns a digest-only record. The transport finishes the episode when the process settles and exposes the record; the worker, verification and source-change hosts write it as `codexIsolation` on their terminal events, the resident CLI observation carries it as `isolation`, and `assertCodexIsolationUnchanged` refuses an unequal pair or an unremoved home. The six named launch sites and a seventh live-model probe call the launcher. Live, on the operator's host with Codex CLI 0.156.1: `codex login status` exited 0 under an isolated home; one `gpt-6-sol` `xhigh` feedback verifier episode completed through the transport in 19,490 ms with equal pairs and both homes removed, and that same episode re-minted the feedback edition ([D012](../../evidence/WO-159/decisions.md#wo-159-d012)); a writer-shape probe with WO-111's source-change prefix observed the CLI writing its scratch trust entry into the isolated home while the user file stayed byte-identical, which turns WO-111 D011's attribution from an inference into an observation. VER-001 failed the first subject on two findings the repair fixed with fixtures (per-invocation homes in the multi-process probe rows; a receipt bound to its retained stream), and VER-002 passed the repaired subject. `main` did not move during the order, so nothing was integrated. The reviewer's product gate passed on its second run; the first run failed on a WO-143 lock-matrix subtest that exceeded its own budget while a sibling worktree's gate shared the host, recorded in [D020](../../evidence/WO-159/decisions.md#wo-159-d020). This review found no acceptance defect.

**Subject:** [`docs/work-orders/WO-159-codex-episode-isolation.md`](../../work-orders/WO-159-codex-episode-isolation.md) on branch `wo-159`, uncommitted at dispatch, judged at checkpoint `refs/dotln/checkpoint/WO-159/9` (`2b53e377`, tree `06db6436`), which is the tree VER-002 passed (checkpoint 8, `8ce86479`, tree `31f0e468`, the `VerificationCompleted` tree hash) plus the projections the final-review dispatch regenerated (`docs/control/current.md`, `docs/work-orders/README.md`, the `FinalReviewRequested` row); a diff of the checkpoint 9 tree against the staged index touches only those three files, and a diff of the checkpoint 8 tree against the index outside `docs/` is empty. Base `1f88850975c5aa7c2e8169c7267017d06d34a302` (the 2026-09-25 planning pass, #130): local `main`, `origin/main` by `git ls-remote` on 2026-09-25, and the worktree `HEAD` are all that commit, and `git log main..HEAD` is empty. The target is `v0.48.0` under the declared minor classification, the next minor above the local `v0.47.1` tag (WO-111), with `@dotln/skeleton` `0.40.0` → `0.41.0`, the console's pin and the lockfile following ([D011](../../evidence/WO-159/decisions.md#wo-159-d011)); `release prepare --local` reports it current. The staged diff against `main` is 79 files, 8,011 insertions and 476 deletions; outside `docs/` and `README.md` it is 41 files, 1,864 insertions and 449 deletions, of which the 14 `.claude/` hooks and the harness manifest are the bundle re-emission (snapshot `c07fee7f…` → `fa15c8d2…` and the `cli-actor-contract.js` runtime hash; no other line). The judged tree is the working tree with every WO-159 file staged; the reviewer gate ran at index tree `2fe97d33b9e11647b1668df894546d0213860674`, code identity `fbcb86c8921193b8b901db1b198ecde8251ef9dddf42a7085d372b8699986cd7`. Nothing was committed, pushed or published before the result transition.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-fable-5-1","effort":"xhigh","source":"claude-session-readback"}

Human actor: the operator dispatched `resume: final review` in a Claude Code session and made no other choice during the review. The harness version is from `claude --version`. The model is the session's selected model. Effort `xhigh` is the host's `CLAUDE_EFFORT` value read by this session: selected effort, not effective. The order asks for `reviewer any`. No subagent was planned or spawned: 0 of the cap of 20 (`harness usage`: exact-observed, no descendants). The subject's load-bearing claims each had an executor reproduction, an independent read-only review before minting (D010) and two independent verifications, and the instrument (the receipt check, the fake-Codex fixtures, the probe stub fixture) runs offline in seconds, so one reader could hold the whole diff and re-run every check. This session ran no provider episode and no live Codex launch.

**Process cost:** entry 86789 tokens; handoff 4010983 tokens; source claude-transcript-message-usage

Both readings are `dispatch` scope from `node scripts/harness.mjs usage 214e8892-c54b-424c-8386-b1a571b187d9`, the entry reading observed at 2026-09-25T05:35:20.997Z and the handoff reading observed at 2026-09-25T06:08:21.785Z, taken after the judgment text and the gate figures were complete and before the result transition; the only later edits were the document-gate figure in §Checks and this line. They include reused cached input. Reasoning tokens and dollar cost are unavailable from this counter, which means unknown, not zero. The largest waits were the two product-gate runs (564,676 ms and 492,013 ms). Final counters stay in ignored receipts and the handoff response.

## Goal-aligned judgment

The order is the containment precondition on the critical path: WO-066, WO-112 and WO-118, the first orders that run a worker against a repository the operator owns, could not start while every Codex launch wrote trust entries into the operator's tool settings, and WO-111 closed only on an operator-accepted deviation for that write (WO-111 D020). The mistakes most likely here were a fixture-only proof that the real binary does not honour, a receipt that could pass without the behaviour it names, and isolation that quietly breaks authentication. **Rule beating** was therefore the main lens. The fixture proves the mechanism; the live row proves the real CLI 0.156.1 honours `CODEX_HOME` the same way, and the trust probe proves the write the order exists to contain still happens and lands inside the isolated home. VER-001 F2 was the rule-beating case in the instrument itself: the check accepted a well-formed false stream digest; the repair derives the whole receipt from the retained stream, VER-002 reproduced the refusal independently, and this review's `--check` re-derives all nine fields. The receipt check is exported from the runtime, not a document convention, and it refuses `unknown` pairs as changed. **Drift to low performance:** the order raises the standard rather than normalising the WO-111 waiver: `criterion2: unmet` in WO-111's receipts is now answered by a launcher that prevents the write, and the WO-054 evidence file is corrected below its unchanged bytes instead of edited. **Shifting the burden:** the operator no longer removes trust entries by hand after each proof; the follow-up that WO-111 D011 minted at high priority is discharged at close. **Policy resistance / fixes that fail:** one builder replaces six argv copies, so the shapes cannot diverge; the isolated home and the receipt check act on the same record, and neither adds a runtime veto that a desktop rewrite of the same file could trip (D003 keeps the check on the receipt). **Escalation:** no new gate, agent, approval or ritual; the launcher is two exported functions and one optional field on existing events. **Tragedy of the commons:** one live episode served both the feedback re-mint and the live row (D001, D012: 24 s wall, 208,174 tokens); the trust probe was a one-word `low` episode; the repairs and this review spent no model episode. **Success to the successful:** the copy, the empty home and keyring storage were compared and declined on evidence (D002), not because the link had investment. **Seeking the wrong goal:** the objective is a byte-identical operator configuration at a real launch, and that is what the live digests show; the criteria do not ask for a WO-111 rerun and none was run. **Naive Interventionism:** the change's second-order effects are named: the home withholds the operator's `AGENTS.md`, rules, skills and memories, which the argv already ignored or disabled; a token refresh writes through the link in place at rust-v0.156.1, with the rename case as D002's reopening condition; an abnormal end leaves a directory holding one link, swept by the next launch once its launcher has exited and the age bound has passed, and listed by `harness prune`. **NoOp** would keep every Codex launch writing into the operator's configuration and keep the operator-repository orders blocked; it is rejected. Judged at handoff, the outcome supports the order's goal within its stated bounds, and the bounds (one inspection-shape live episode, the writer shape observed only by the one-word probe, Claude and Copilot launches not isolated, other host paths unknown) are recorded where the next reader will look.

## Criteria

| Criterion | Judgment and evidence |
| --- | --- |
| 1 — fixture: a fake `codex` that writes a trust entry leaves the user-level file byte-identical; equal pairs; home gone; auth bytes in no record, store or log | **Met.** `packages/skeleton/fixtures/codex-home-cli.mjs` reads `$CODEX_HOME/auth.json` and appends a trust entry to `$CODEX_HOME/config.toml`. The AC1 subtest in [`codex-episode.test.ts`](../../../packages/skeleton/test/codex-episode.test.ts) drives it through `CodexCliExecWorkOrderTransport` against a fixture user home: the user file's bytes are unchanged, both pairs equal, `isolatedTrustEntries` 1, the home gone, and the synthetic secret absent from the record, every store file and stderr. The same file covers the argv builder, the trust-table projection (inline root tables, literal keys, quoted brackets, multi-line arrays), home resolution, an unequal pair when the user file changes, an absent configuration, an unremovable home (recorded `homeRemoved: false` and refused by the check) and the stale-home rule. The two WO-159 test files pass 12 of 12 standalone and inside the reviewer's skeleton suite. |
| 2 — `git grep` for the shared flag over package sources and scripts returns only the launcher's file; the six sites call it | **Met with D004's corrected pathspec.** This review ran `git grep -l -e '"--ephemeral"' -- ':(glob)packages/*/src/**' scripts`: only `packages/skeleton/src/worker-transport.ts`. The order's literal `packages/*/src` pathspec matches no file (exit 1, nothing listed), which [D004](../../evidence/WO-159/decisions.md#wo-159-d004) records and the AC2 subtest encodes. Read at source: the transport's three shapes, `harness-probe.mjs`, `authority-probe.mjs`, `target-worker-smoke.mjs`, `local-model-role-qualification.mjs` and the seventh site `probe-codex-effort.mjs` ([D005](../../evidence/WO-159/decisions.md#wo-159-d005)) each bracket one spawn with one episode; `writing-worker-probe.mjs` wraps every invocation, including each concurrent session and the fresh recovery, in `isolated()`; `probe-worker-hosts.mjs` isolates its help-only invocations and reads its checklist from `CODEX_EXEC_SHARED_FLAGS`. The test file holds the literal as an expected argv and lies outside the criterion's paths. |
| 3 — live row through the transport on the operator's host, `codex login status` succeeding in isolation, equal digests, recorded with the launch claim | **Met for the recorded single-process episode.** [`live-codex.json`](../../evidence/WO-159/live-codex.json): login status exit 0 (ChatGPT) under an isolated home with the auth link; one `codex-cli-exec` `gpt-6-sol` `xhigh` feedback verifier episode, `WorkerCompleted`, 19,490 ms of a 600,000 ms deadline, 19 heartbeats, approval policy `never`; both launches' `config.toml` and trust-table pairs equal and both homes removed; the launch is labelled `launch-claim`, everything else `observed`. `node docs/evidence/WO-159/receipt.mjs --check` exited 0 in this review: the retained feedback verifier stream re-derives the receipt. The [trust probe](../../evidence/WO-159/trust-probe.json) adds the writer shape: one trusted entry naming the scratch target inside the isolated home, user pairs equal, 43 entries before and after. Not a WO-111 rerun, which is a non-goal. |
| 4 — the live receipts' protected-surface list includes both digest pairs; a fixture receipt with unequal pairs fails the check | **Met.** `protectedSurfaces.codexUserConfiguration` lists both launches' pairs and `validateReceipt` calls the runtime's `assertCodexIsolationUnchanged`. [`codex-isolation-receipt.test.ts`](../../../packages/skeleton/test/codex-isolation-receipt.test.ts) builds fixture receipts from real `WorkerHost` and `VerificationHost` stores through the fake CLI and refuses 26 forgeries by message, including unequal user-config and trust-table pairs, an unremoved home and a restated pair; after the F2 repair `matchSharedStream` refuses eight forgeries `validateReceipt` still admits, and VER-002 reproduced the 64-zero digest refusal independently. The worker, verification and source-change hosts record `codexIsolation` on `WorkerCompleted`/`WorkerResultObserved`, `WorkerInterrupted` and `WorkerResultQuarantined`; the CLI actor contract admits `isolation` only for a Codex episode and only in the closed shape. |
| 5 — a dated correction appended below the existing bytes of the WO-054 file; the WO-054 capability row reassessed | **Met.** The diff of [`codex-continuation.md`](../../evidence/WO-054/codex-continuation.md) is a pure append (`@@ -130,3 +130,27 @@`): a dated note naming the two overstated sentences, what they held for (the bundle's own emission, which still writes only its three owned files), what they did not (DotLn's Codex launches, including that order's own probe families), and the WO-159 observation; the note says the bytes above are unchanged, and the diff shows it. The capability table's dated reassessment adds a Codex launch containment row (level 1, target 2, efficiency unknown) and reassesses the WO-054 continuation row without rewriting the historical rows. |
| 6 — authority and verification editions re-mint deterministically; the feedback edition re-mints with one live self-host episode after every source edit settled; `current.json` selects them | **Met.** [`current.json`](../../evidence/current.json) selects `WO-159/001` for authority, verification and feedback; artifact identity stays `WO-154/001` ([D014](../../evidence/WO-159/decisions.md#wo-159-d014)). The reviewer gate's `harness-evidence` and `feedback-evidence` tasks passed on the staged tree; the feedback edition's live audit is the Codex episode (`carried: false`), recorded after the implementation's registered source edits settled, and the repair changed no registered source ([D018](../../evidence/WO-159/decisions.md#wo-159-d018)). The verification edition's four files are byte-identical to WO-154's. The harness bundle re-emission differs from `main` only in the snapshot id and one runtime hash. |
| 7 — `npm test` and `npm run test:docs` green; `git diff --check` clean; no new dependency | **Met on the second gate run.** Reviewer `npm test -- --review`: 37 suites, 0 failed, 492.01 s, 81 fresh tasks, exit 0, recorded 2026-09-25T05:59:47.772Z with no sandbox in force, at the tree and code identity above; the first run at the same identity failed one unrelated subtest under host load (§Findings). `npm run test:docs`: 21 passed, 0 failed, 23.05 s, 21 fresh tasks, exit 0. `git diff --cached --check` clean. The lockfile changes four lines (`@dotln/skeleton` `0.40.0` → `0.41.0` and the console's pin); no dependency was added. |

The **evidence gate** is satisfied: the fixture transcript is the AC1 subtest, the live row carries its digests, the feedback self-host episode is `docs/evidence/WO-159/feedback-001`, and `npm test` passed at this review. The **non-goals** hold: Claude Code and Copilot launches are not isolated and were not measured ([D009](../../evidence/WO-159/decisions.md#wo-159-d009) hands the question to planning); the operator's existing trust entries were not removed by the order (the operator removed them, [D013](../../evidence/WO-159/decisions.md#wo-159-d013)); no WO-111 rerun; the Entropy Reducer's two shapes moved to the builder without a byte change and its delegate route is untouched. **Operator-review assumption 1** was resolved in the operator's preferred direction: a symlink, never a copy ([D002](../../evidence/WO-159/decisions.md#wo-159-d002)). **Assumption 2** held: the operator saved the one-entry configuration after the live launches and before handoff, so the recorded census of 43 is the file as it stood at the row and no receipt spans the save (D013).

## Findings and readings

No acceptance defect. The readings below were checked at source and change no verdict.

- **The reviewer's first gate run failed on a test this order does not touch.** `npm test -- --review` at 2026-09-25T05:49:54Z exited 1 after 564,676 ms: the skeleton suite (377,741 ms) reported `not ok 223 - WO-143 once and loop restart at every acquisition filesystem boundary, including killed reclaimers` with `testTimeoutFailure` at 240,795 ms, seven of its eight kill/restart cells done; the other 447 skeleton subtests, including every WO-159 subtest, passed, and the row's four deadline diagnostics are classified `deadline-hit-cause-unestablished`. During that run the sibling `wo-158` worktree held a live gate record and the host load averages were 4.21, 6.41 and 6.93. The second run at 05:59:47Z, alone on the host, passed 37 suites with the skeleton suite at 294,403 ms, at the same tree and code identity. The failing subtest exercises resident lock acquisition, which the diff does not change (the resident host gains one optional field on a failed observation), so this is a machinery budget met under load, not an acceptance defect; [D020](../../evidence/WO-159/decisions.md#wo-159-d020) records both rows and names a low-priority planning follow-up on the matrix's single 240 s budget.
- **What isolation proves, exactly.** Proven: the user-level `config.toml` and its trust table are byte-identical across a launch, by fixture for the mechanism and live for an inspection-shape episode, a login check and a writer-shape probe. Observed and discarded with the home: the CLI's own logs, session state, memories, model cache, skills and installation id, which would otherwise have landed in the operator's `~/.codex` (D013, entry names only). Not claimed: other host paths, network effects, or Claude and Copilot launches (`FUP-e398c79e1b32e94b`).
- **The auth link, not a copy, answers receipt 028's first known issue in content.** [D002](../../evidence/WO-159/decisions.md#wo-159-d002) chose a symlink because the CLI's file store writes `auth.json` in place at rust-v0.156.1, so a refresh reaches the operator's file; a copy would strand a rotated token. A home left behind by an abnormal end therefore holds one link and no secret bytes. The directory itself is swept by the next launch once its launcher pid is gone and it is older than `STALE_CODEX_HOME_MS` (twice the longest DotLn episode deadline, so a detached Codex that outlives a killed launcher keeps its home), and `harness prune` lists it. A reused pid keeps a stale home invisible to both ([D006](../../evidence/WO-159/decisions.md#wo-159-d006) reopening); the detached-episode orphan itself is `FUP-35bc8736fb13c49a` (D010).
- **The trust write depends on the launch shape.** The inspection-shape live episode wrote nothing into its home (`isolatedTrustEntries: null`), while the source-change prefix wrote one entry naming its scratch target. The order's clause "observed and recorded, not assumed" held, and D013 states the consequence: WO-111's attribution is now an observation for the writer shape.
- **A home that cannot be built refuses.** `startCodexEpisode` throws `WorkerFailure("profile-refused", "isolated Codex home")` when the temp directory, its mode or the link cannot be made, and never falls back to the operator's home; a home that cannot be removed is recorded `homeRemoved: false` and refused by the receipt check. Receipt 028's second issue (a worker prompting or stalling without its user-level settings) is answered by the live row completing under `approval_policy never` inside its deadline; every argv keeps its explicit approval and sandbox arguments.
- **The receipt check is part of the instrument under review, and it has independent checks.** `validateReceipt` and `matchSharedStream` live beside the receipt and are the code the live builder used. Independent of them: the fixture receipts built from real host stores with the fake CLI, VER-001's and VER-002's in-memory false-hash reproductions (before and after the repair), and this review's `--check` run on the committed receipt.
- **Adjacent cleanup stayed within named paths.** [D007](../../evidence/WO-159/decisions.md#wo-159-d007) moved `harness-probe.mjs`'s retained-observation check ahead of the launch, so a rerun no longer spends a model episode before refusing. `package.json`'s `probe:workers` now builds first because the probe imports the built launcher. `scripts/test-runner.mjs` registers `worker-transport.ts` as a source of the `harness-probe` and `harness-fixtures` suites, which is why the reviewer gate ran 81 tasks against the executor's 71.
- **The release label was bound by amendment.** The 2026-09-25 planning pass filed WO-158 to WO-165 without the `(version assigned at activation)` placeholder, so the executor bound `v0.48.0` through `plan amend-order` ([D011](../../evidence/WO-159/decisions.md#wo-159-d011); `PlanExecutionAmended` row at 2026-09-25T04:33:50Z, source hash `33416089…` → `da0cc041…`); `plan check` exits 0 and `FUP-57741bfb0c1f7f35` asks the planner to restore the placeholder. The operator may still veto the label.
- **Register rows.** Open for the planner: `FUP-e398c79e1b32e94b` (whether a Claude launch leaves user-level state, medium), `FUP-35bc8736fb13c49a` (supervisor signal handling for a detached Codex episode, medium), `FUP-57741bfb0c1f7f35` (the heading placeholder, low) and D020's follow-up. Settled with reopening conditions VER-002 did not meet: `FUP-48329a515c2547af` and `FUP-d73389529a67a11a` (the two repairs). `FUP-3c34a8ffbf61376f`, WO-111's high-priority runtime follow-up, and its duplicate `FUP-f3281eb085ad87a7` settle at close through planning's `followups apply` ([D015](../../evidence/WO-159/decisions.md#wo-159-d015)); this worktree's queue holds no deferral onto them. A reviewer does not write register dispositions.
- **Independence across the sequence.** Implementation on Claude Code 2.1.282 / `claude-opus-5-5` at `xhigh` (`ultracode`, subagents); VER-001 on Codex CLI 0.156.1 / `gpt-6-sol` at `xhigh` (`ultra`, two read-only subagents); the repair on Claude Code / `claude-opus-5-5`; VER-002 on Codex CLI 0.156.1 / `gpt-6-sol`, no subagents; this review on Claude Code 2.1.282 / `claude-fable-5-1`. Every verification was independent of its implementation actor in model and harness, and this review used a third model.
- **Clean-room screen.** A grep over the staged additions for paths, accounts and credential shapes found only the receipt's own private-material pattern, fixture strings, and auth-file key names listed by name in D002; the receipts hold digests and counts, the trust probe records booleans and token counts, and the WO-054 note names public fixture families. No employer material, private path, credential or identifier entered the diff.

## Integration with `main`

None was needed. Local `main`, `origin/main` (`git ls-remote` on 2026-09-25) and the worktree `HEAD` are the same commit `1f888509`, the branch has no commits, and the sibling `wo-158` worktree has not published. `npm run worktree -- integrate WO-159` was not run: with identical bases it would stash and re-apply 79 uncommitted files to merge nothing and would need an intake backup archive. The projections its list names are current: the final-review dispatch regenerated the control projection and the work-order index; `harness check` reports 31 generated surfaces; `publication:check` reports both edition locks current; `release prepare --local` reports `v0.48.0` current with no files changed; `plan check` exits 0; and this review refreshed the index and `meta` after recording D020. No component version collides (`main` is at skeleton `0.40.0`; this order moves it to `0.41.0`) and no evidence edition changed since VER-002. Carried-forward claims: every claim VER-002 judged, on unchanged source bytes; the staged index differs from checkpoint 9 only in the three projection files above and from checkpoint 8 outside `docs/` not at all. The reviewer gate's code identity differs from VER-002's row because the tracked set changed, not the bytes: the fixture, the two test files, the evidence scripts and the reports were untracked at the verifier's run and are staged here. [D020](../../evidence/WO-159/decisions.md#wo-159-d020) records the bases and this assessment.

## Verification sequence

1. **Implementation** (Claude Code 2.1.282, `claude-opus-5-5`, `xhigh`, [record](../../evidence/WO-159/implementation.md)): the launcher, the transport and host records, seven adoptions, the fixture and its tests, the receipt builder and checker, the live row and the trust probe on Codex CLI 0.156.1, the WO-054 note, the capability reassessment, the product 03 status sentence, the three re-minted editions, the release assignment; an independent read-only review of 17 findings settled before minting (D010); D001–D015; `npm test` 27/0/71, 297.63 s.
2. **[VER-001](../../verifications/WO-159/VER-001.md)** (Codex CLI 0.156.1, `gpt-6-sol`, `xhigh`, two read-only subagents): fail. F1 major: the writing-worker probe's concurrent and resident-kill rows ran several `codex exec` processes under one home with one record per row; F2 minor: the receipt check accepted a well-formed false stream digest. D016, D017. Criteria 1, 3, 4, 5, 6 and 7 supported on that subject.
3. **Repair** (Claude Code 2.1.282, `claude-opus-5-5`, `xhigh`, [record](../../evidence/WO-159/repair.md)): `isolated(label, launch)` per invocation with labelled `codexEpisodes`, the stub logging each home, a fixture asserting 12 distinct homes for 12 invocations and binding every record to a launch, with a negative control at checkpoint 5 (9 of 12 distinct); `matchSharedStream` re-deriving the whole receipt from the retained stream and `--check` requiring it; D018, D019; `npm test` 27/0/71, 303.14 s.
4. **[VER-002](../../verifications/WO-159/VER-002.md)** (Codex CLI 0.156.1, `gpt-6-sol`, `xhigh`, no subagents): pass. Both repairs reproduced independently, including the 64-zero digest refusal; all seven criteria supported; `npm test` 27/0/71, 365.54 s; `test:docs` 21/0.
5. **This review** (Claude Code 2.1.282, `claude-fable-5-1`, `xhigh`, no subagents): the full staged diff read at source against the original order and the ideation receipt's three known issues, the criteria re-established with this session's own runs, no integration, the product gate on the staged tree, D020.

## Checks

All on the staged tree at index `2fe97d33…`, 2026-09-25, no sandbox in force:

- `npm test -- --review`, first run: 36 passed, 1 failed (`skeleton`: the WO-143 subtest timeout), 564.68 s, 81 fresh tasks, exit 1, recorded 05:49:54.351Z. Second run: **37 passed, 0 failed, 492.01 s, 81 fresh tasks, exit 0**, recorded 05:59:47.772Z, code identity `fbcb86c8921193b8b901db1b198ecde8251ef9dddf42a7085d372b8699986cd7`. Both rows are in the ignored checks record.
- `node --test` on the two WO-159 skeleton test files: 12 passed, 0 failed.
- `node docs/evidence/WO-159/receipt.mjs --check`: exit 0; the retained feedback verifier stream derives the receipt.
- Criterion 2 search with the corrected pathspec: one file; the literal pathspec lists nothing.
- `git diff --cached --check`: clean. `git diff --cached --stat`: 79 files, 8,011 insertions, 476 deletions.
- `node scripts/harness.mjs check --loadout contributor`: 31 generated surfaces. `npm run publication:check`: 275/275 headings indexed, both locks current. `npm run plan -- check`: exit 0. `npm run release -- check-surfaces --local`: all PASS. `npm run release -- prepare --local`: `v0.48.0` remains current, no files changed (it refreshed the process meter in `PR.md`).
- `npm run test:docs` with this report, the PR body, the release notes and D020 present: 21 passed, 0 failed, 23.05 s, 21 fresh tasks, exit 0.
- `git ls-remote origin refs/heads/main`: `1f888509`, equal to local `main` and `HEAD`.

## Handoff

On pass this review commits the reviewed state as a short series, pushes `wo-159` and opens its pull request with `npm run worktree -- publish WO-159`; it never merges, pushes `main`, publishes a release or changes settings. After the operator merges, `resume: release close` publishes `v0.48.0` from the main checkout and settles `FUP-3c34a8ffbf61376f` and its duplicate through planning's `followups apply` (D015). The operator may veto the `v0.48.0` label (D011) before merging.
