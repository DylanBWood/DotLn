# WO-148 FINAL-001 — one command now aims a resident at the live order it should watch; the three false guarantees VER-001 found are repaired and re-verified, the ideation receipt's base-drift issue is observed and regressed rather than carried, and the store still leaves nothing Git can see

**Verdict: pass.** All six acceptance criteria are met at the reviewed tree and no finding routes to repair. I did not take the two verifications' account on trust. I ran the shipped suite myself (14 cases, 0 failed, after adding one), ran the shipped `--check` against all four real retained WO-148 stores and read each refusal, scanned every changed file for a physical path, scanned the four authored scripts for suppression directives, confirmed the manifest and lockfiles are untouched, and built a disposable real-Git launchpad to reproduce the one known issue the ideation receipt called constructible but unobserved: after `main` advances and the order's branch integrates it, `--check` names the moved merge base, exits 1 and prints no launch line. That path had no fixture in the shipped suite, so review added one case to `scripts/test-resident-bind.mjs` and recorded the observation as [WO-148-D020](../../evidence/WO-148/decisions.md#wo-148-d020); nothing in `scripts/resident-bind.mjs` changed. `main` did not move while the order was in flight, so no integration was needed. The product gate ran once on the reviewed, staged tree, outside the harness sandbox from the start, and passed **24 suites, 0 failed, 263.22 s, 68 fresh tasks, exit 0**.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-fable-5-1","effort":"unknown","source":"claude-session-readback"}

**Process cost:** entry 84569 tokens; handoff 7085475 tokens; source claude-transcript-message-usage

The human actor is the operator, who dispatched `resume: final review` in this worktree after selecting the Fable 5.1 model for the session; `claude --version` reports 2.1.278 and the session reports its model as `claude-fable-5-1`. No effective effort readback exists in this harness and nobody supplied a reviewer effort (the order says "reviewer any"), so `effort` is `unknown`; the model is session readback, so the pair records `claude-session-readback`. Counters came from `node scripts/harness.mjs usage 7c8ad5fa-12a9-4a44-8800-574750e54d96`, scope `dispatch`, source `claude-transcript-message-usage`; `costUsd` and `reasoningOutputTokens` are null and therefore unknown, not zero. Before any fan-out I planned zero admissions against the root session's 20-agent `subagentCap`, because every check here is one command or one file and the single gate sets the pace; none was spawned, the counter reports 0 of 20 `exact-observed`, and the uncounted remainder is unknown. The dispatch briefing's observed-facts block reported every counter unavailable at 20:11:38Z; the usage command answered nine seconds later with the entry total above, so that block was a timing artifact of the session journal, not a missing counter. The handoff total was read at 20:31:09Z, after the gate and before this report's final bytes were written, so the result transition, the commit series and the publish that follow are not in it.

**Read scope.** The order in full, including its cost line, design, six criteria, non-goals and three operator-review assumptions; its cited sections — `MissionSource` and `assertMissionSource` in `mission-check-protocol.ts`, the mission fixture's actor shape, `CONTRIBUTOR_MISSION_POLICY` and its siblings in `contributor.ts`, `scripts/evidence-mission-check.mjs`, `parseWorktrees`, `readControl` and `eventsForOrder`, `workOrderAuthorityPath`, `docPath` and `docRelative`, `worktree start`'s branch naming, WO-099 D013 and D016, and the candidate document with its new disposition; product 03 §Operator-presence policy at its current bytes, the skeleton README's resident runbook, product 07 §Goal-aligned decisions, product 08 §PRs and commits and §Release-note edition; the ideation lineage, which is the 2026-09-21 standard pass §4 and its refutation receipt 022 with the four WO-148 known issues, and receipt 023, which carried WO-148's verdict unchanged; VER-001 and VER-002 in full; this order's `implementation.md`, `repair-001.md`, `live.json` and decisions D001 to D019; and the complete staged diff against `main`, including all 723 lines of the command, all 972 of its suite and all 234 of the live-row collector.

## Subject

| Item | Value |
| --- | --- |
| Work order | `docs/work-orders/WO-148-resident-binding.md`, minor, application `v0.40.0` |
| Branch / worktree | `wo-148` in the order's own worktree; physical cwd and Git root confirmed equal; this session is the sole registered writer. The path is not filed, because criterion 3 says so |
| Base | `main` at `c84db382efd665e52f20927b3885f4019c879f43`; `origin/main` was fetched during review and is the same object; the branch carries no commit, so the review base and the integration base are one commit and **no integration was required** |
| Verification base | VER-002 judged the staged tree at checkpoint 7 and completed at tree `69b26e37`. Between that completion and this review only `docs/control/current.md`, `docs/control/orders/WO-148.jsonl`, the generated work-order index and VER-002 itself changed. Review then added one test case, D020 and the regenerated decisions index and follow-up register; `scripts/resident-bind.mjs` and the live-row collector are byte-identical to VER-002's subject |
| Reviewed code identity | `22f57294958b6f052a7cba0145b1d329089a063b3cd8afba456c5f58fbab40bf` |
| Diff shape | 23 paths, 3,700 insertions, 32 deletions. Hand-authored source is four files: the command (723 lines), its real-Git suite (972 lines, 14 cases), the live-row collector (234 lines) and three lines registering the suite in `scripts/test-runner.mjs`. No `packages/*/src` file changed, so the order's "no kernel, resident-host, event, schema or hook change" holds by construction. Everything else is the product and runbook write-back, the candidate disposition, the version triple, the evidence and verification records and the regenerated projections |

## Acceptance, judged at the reviewed tree

| # | Criterion | Judgment |
| --- | --- | --- |
| 1 | From a fixture launchpad with an activated order in a worktree, `bind` writes a store the runtime accepts and `dotln status --store` reads, whose `mission-check` actor's `MissionSource` names that worktree, the authority path, the decisions path, the merge base, exactly the supplied surfaces and the vision path; `missionPinFromSource` equals a hand-built pin; the printed lines name the store | **Met, reproduced.** The first case builds a real launchpad and worktree, runs the actual command as a child process, decodes the store with `decodeResidentConfiguration`, asserts the single `mission-check` actor's `MissionSource` field for field, compares `missionPinFromSource` with `missionPin` over the pinned subject whole except for the per-pin salt and the ids it keys (asserted as shapes, and asserted to differ between two pins so a private ignored path cannot be recovered), runs the shipped `status` and `resident --once` against the store and finds `ResidentConfigured` without a dispatch, and asserts all seven printed operational lines name the store. I read that case line by line against the protocol's `MissionSource` and found every field the protocol admits either declared or, for `storyPath`, deliberately absent |
| 2 | Unknown order, closed order, missing worktree and no surface each refuse by name; `--check` against a closed order, a moved worktree or changed contract bytes names each mismatch, exits non-zero and refuses a launch line; a rebind writes a new store and leaves the old one byte-identical | **Met, reproduced and extended.** The refusal case covers no worktree, no surface, an unknown transport, an absolute surface, an unknown order whose branch exists, and a closed order. The check case drives an edited contract, a closed order, a moved worktree and a pruned worktree; the retargeting case mutates `decisionsPath`, `visionPath`, `thesisHeadings`, `decisionLimit`, `declaredSurfaces` (one surface whose text is the two bound ones joined) and an undeclared `storyPath` one at a time and withholds every launch line; a store stripped of its worker is named as no longer decodable; the rebind case proves `WO-999-1` unchanged to the byte and mtime; the moved-store case proves every printed command names the checked directory. Review added the moved-merge-base case (§Findings). Against this repository's four retained WO-148 stores, the shipped `--check` refused each as stale on phase (`active` → `final-review`) with exit 1 and no launch line; the first store also mismatched on contract bytes, and its two hashes are exactly the order's source hash before and after the version was assigned, which the planning check reports as this order's `release-assignment` update |
| 3 | No physical path enters a committed surface: the binding record and the store live in the ignored lane, and the evidence row redacts paths | **Met, reproduced.** `.gitignore` line 18 ignores `/docs/control/local/`; `assertIgnoredStore` asks `git check-ignore -q` about both files before creating the store, for every bind, and refuses by name when either is visible or Git cannot answer; the configured-root fixtures prove refusal without a rule (no lane directory left, Git status empty) and success with one. My scan of every staged file for `/Users/`, `/private/var/` and `/var/folders/` found three matches, all sentences in VER-001, VER-002 and D011 describing their own redaction checks; `live.json` carries counts, shapes and hashes only. This report follows the same rule |
| 4 | Live row: one real in-flight order bound, `--once` run from an outside terminal with the operator away; the row records launch, capsule path counts against the bound worktree, verdict and label; a verdict other than `on-mission` does not fail the order | **Met, unchanged since VER-001.** `live.json` records the fourth real bind of WO-148 at phase `active`, a detached actor-origin launch of Codex CLI 0.155.1 on profile `mission-check-v1` over `codex-cli-exec`, the operator away before and after, the capsule root equal to the bound worktree and its contract equal to this order, 20 changed paths all inside the 15 declared surfaces with 0 omitted, the base commit matching the binding, verdict `drift` with a hold raised and one correction recorded, label `observed`, and six stated limits. The two drift findings are explained in `implementation.md`: one was a real criterion-3 breach in the report, fixed; the other was the judge reading the superseded row, which no rerun can avoid. I did not rerun the 981-second observation: the repair changed no bind-time output except the pre-write refusal, and review changed no shipped code |
| 5 | Write-backs land: product 03, the skeleton runbook, the candidate disposition, the decisions file | **Met, and extended by this review.** Product 03 carries three dated paragraphs stating the command, what stays declared, what `--check` compares field by field, the relocated-store behaviour and the Git question asked before writing; the runbook names the command as the ordinary path and keeps the hand-written shape as the reference; the candidate document records its disposition and the two concerns answered by deferral; D001 to D019 record the design, the three corrections, the experiment, the two adjacent repairs, the three findings and their repairs. Review added D020. `publication:check` reports both editions current at 30 and 45 linked sections with 273/273 headings; `meta --check` passes with 20 rows |
| 6 | `npm test` green; `git diff --check` clean; no new dependency | **Met, executed here.** Gate below. `git diff --check` is clean in the working tree and the index. `package.json`, the lockfile and every package manifest are untouched; the four authored scripts carry no `eslint-disable`, `ts-ignore`, `ts-expect-error`, `prettier-ignore` or `biome-ignore`; `format:check` is clean |

## Gate and affected checks

Every reviewed tracked change and the three intended new source files were staged before the gate, and it ran outside the harness sandbox from the start.

- `npm test -- --review` — **24 suites, 0 failed, 263.22 s, 68 fresh tasks, exit 0**, recorded 2026-09-21T20:28:44.963Z at code identity `22f57294958b6f052a7cba0145b1d329089a063b3cd8afba456c5f58fbab40bf`, tree `28ef2e24aba84fe880692ab73d067dc5c08e202f`.
- The document edits this review then made are confined to `docs/`, which the code identity excludes, so the reviewed identity is unchanged at publication and the gate row resolves at the committed tree.
- `node --test scripts/test-resident-bind.mjs`: 14 tests, 0 failed, 4.07 s, at the formatted bytes. `npm run format:check`: clean. `npm run publication:check`: 273/273 headings, both editions current. `node scripts/release.mjs check-surfaces --local`: release block `v0.40.0` observed and expected, latest local tag `v0.39.0`, all four components unchanged with no bump required, every package publish refused as designed. `npm run plan -- check`: exit 0, with this order's `release-assignment v0.40.0` classified as a committed update under receipt 023. `node scripts/work-orders.mjs index --check`: current. `node scripts/meta.mjs --check`: passes after D020. `npm run adjacent -- list`: `adjacent-0001` and `adjacent-0003` completed, `adjacent-0002` deferred, nothing running and nothing next.

## The verification sequence

Two verifications and one repair, in order, and I judge each verdict correct.

**VER-001 failed the subject on three findings, and all three were real.** F1: `bindingMismatches` compared four source fields and joined the surfaces into text, so a store retargeted through the decisions window, the vision document or its theses passed as fresh; those fields select what the resident judges at every pulse, so a green check proved the wrong thing. F2: `checkBinding` discarded the directory it was given and printed launch lines for the path the store was bound at, so a successful check of a moved store handed the operator commands that could not open it. F3: bind followed a configured control root through `docPath` without asking Git whether the resolved lane was ignored, so a launchpad with `roots.control` moved and no matching rule got path-bearing files on a Git-visible surface, which is criterion 3 failed on a configuration the order claims to support. Each was reproduced by the verifier in a disposable launchpad rather than argued from the code, and each was boarded as a decision (D012 to D014) with the repair it asked for. Its `unknown; cause no-session` cost line is correctly caused: that Codex session had not begun a harness session.

**Repair 001 answered each finding at its boundary and no wider.** D015 compares all eight declared fields structurally, names an undeclared field, and decodes the store before printing a launch line; D016 carries the checked directory through every printed line and keeps the bound path as one provenance line; D017 asks `git check-ignore -q` about the exact two files before creating anything, for every bind. The defect-restored controls are the part I value most: with each defect put back one at a time, exactly the new case for it fails and every other case passes, which is what distinguishes a regression that holds from one that merely passes. D019 corrects the repair's own first claim that a passing `npm test` covered formatting; it did not, because the formatter lives in the document set, and the record keeps both gate runs.

**VER-002 passed the repaired subject, and I found nothing it missed.** It reran the full gate and the focused suite, probed every retargeted field independently, exercised all four real stores, proved both sides of the configured-root case, checked the manifest, suppressions, release surfaces, publication and metadata, and rescanned for paths. Its `unknown; cause harness-no-readback` cost line is honest about having a later reading and no entry total. Its criterion-2 row says a stale base refuses by name; the code path was there, but no fixture in the shipped suite moved the canonical merge base, which is the gap review closed rather than a defect in the verdict.

## The ideation receipt, and what happened to its four known issues

Refutation receipt 022 (`planning-9d2888b45f687bc6`, frozen 2026-09-21T07:14:25Z over the subject at `3a6aa592`) judged the standard pass that allocated the operator's 2026-09-20 candidate to WO-148: verdict `aligned-with-findings`, `holdReasons` empty, four WO-148 known issues, no observed failure and no vision contradiction. Receipt 023 carried that verdict unchanged by hash. Findings confer no operator decision authority, and none was treated as one. The standard pass's stop condition for this order, that it cannot bind a real order without a runtime change, did not trigger: the live row bound this order itself with no change under `packages/*/src`; the one declaration the store needed, the `actor.cli-worker` capability in its compilation environment, is store content and is recorded as D003.

1. **Base drift after integrate (criterion 2).** The receipt said `binding.json` records the merge base at bind time, `worktree integrate` moves it, and criterion 2's enumerated mismatches did not name the base. D005 and D006 record what `--check` compares and what the capsule diffs against, which is one of the receipt's two reopening conditions; the other, observing the mismatch, was not on the record and had no fixture. Review observed it in a disposable real-Git launchpad and added the case; D020 records both. The capsule keeps diffing against the base pinned at bind, which is exactly what makes a moved base visible as stale.
2. **The stale-binding refusal lives only in the command (criterion 2).** True, deliberate, and recorded three times: D005 names the resident-host refusal as a runtime change with replay consequences and this order's non-goal, product 03 says a running resident keeps judging its declared subject, and the runbook says the same. Reopens with WO-111's evidence or WO-142 D022's prune follow-up. Nothing in the delivered work contradicts the receipt, and nothing widens the order to answer it.
3. **The Contributor build is hard-wired; no build selector (criterion 1).** Also true and recorded: D003's reopening condition is a second build needing a bound resident, at which point the vision path, theses and decision window belong on the presence policy rather than in the command; product 03 states the store holds the Contributor build's graph and environment; the runbook keeps the hand-written shape as the reference for every other policy. An architectural narrowing named rather than hidden.
4. **The fixture suite's effect on `gateStepCount` (criterion 6).** The suite is placed in the named runner as `resident-bind`, with its protection sentence in the map, so the effect is now measurable. The meter reads the current count in the table below; the receipt's reopening threshold is the WO-069 row's figure.

| Signal | Value |
| --- | --- |
| `gateStepCount` at this review (`npm run meta`) | 67 |
| Receipt 022 reopening threshold | above 80 |

The candidate itself asked six decisions; the standard pass answered each and the delivered order records them as D001 (who owns the binding), D002 (store lifetime), D004 (surfaces), D005 (staleness), D006 (base) and the candidate's own delivered section (session presence and concurrency, both answered by deferral where the candidate put them).

## Findings, observations and limits

**No finding.** Nothing routes to repair.

**Observations.**

1. **The moved-merge-base path had no fixture, and now has one.** The shipped suite retargeted the store's own `baseCommit` field but never moved the canonical merge base the check recomputes. The added case binds, advances `main` by one commit, merges `main` into `wo-999` as `worktree integrate` does, asserts the named `base commit:` mismatch with exit 1 and no launch or export line, and asserts the following rebind records the new base. It is the reviewer's own source edit under the boy-scout bound, inside the order's named deliverable ("the command and its tests"), and it is why the gate below ran on 14 cases rather than 13. Recorded as D020.
2. **The order's cost line undercounts the store.** It estimates "a binding record of a few hundred bytes"; D002 measures 1,494 bytes for the record and 290,006 bytes for `resident.json` beside it, and this repository already holds four such stores from the live row's own binds. Recorded at implementation and correct; I did not amend the cost line, which is planner authority text, and prune eligibility stays the named non-goal.
3. **The collector's defaults name a transport that cannot currently run a mission check.** `scripts/evidence-resident-binding.mjs` defaults to `claude-cli-print` and a Claude model, and D009 shows every `claude-cli-print` mission check fails before any model call because the shared result schema emits duplicate enum items. The collector labels such a run `blocked` with the cause, so a row taken on the defaults stays honest, but it will be `blocked` until the D009 follow-up (`FUP-e2877e300cc8813a`) lands in its own order. The defect is in the shared schema, outside this order's declared scope, and the filed row used the unaffected Codex transport.
4. **One carried advisory is bookkeeping, not loss.** `adjacent-0002`'s deferred target names its intended order in prose rather than `FUP-e2877e300cc8813a`, because the item was disposed before `npm run meta` minted the identifier; the queue refuses mutation outside the executor and fixer phases and the planning check exits 0. D009 and the public follow-up both carry the defect, its reproduction and its reopening condition.
5. **Retained stores follow the worktree they were bound in.** D002's follow-up (`FUP-866699c54128edc1`) records that a bind run inside the bound worktree lands the store in that worktree's ignored lane and `worktree finish` preserves it into `main`'s lane as ordinary retained control material. This order's four stores are in that position. They are inert, ignored and inspectable; release close should expect them.
6. **The store's compilation environment carries a placeholder base commit.** `missionConfiguration` compiles with forty zeros as the environment's `baseCommit` and `repo: "project"`. The capsule's real base lives in the `MissionSource` and is what `--check` compares; I found no consumer of the environment's base in `resident-host.ts` or `resident-state.ts`, and the mission fixture and WO-099's live row also synthesize their environments. Harmless as delivered, and worth a real value if a later order makes the environment's base observable.
7. **The two verifications' cost lines are both correctly `unknown`.** Neither Codex session had a complete entry and handoff pair. Unavailable is not zero and blocked nothing, which is the rule.

**Limits.** This review establishes that the command binds what it says, refuses what it says and writes where it says; it does not establish the quality of the mission judge's verdict, which criterion 4 deliberately excludes and the live row's `drift` illustrates. The live row was not rerun. `--check` was exercised against real stores only in the stale direction, because this order's own phase has moved since every bind; the fresh direction is covered by the fixtures. The stale-binding guard remains one-sided (receipt issue 2). The `claude-cli-print` transport cannot run a mission check until D009's follow-up lands. `git check-ignore` is the only authority bind trusts for the lane, and a launchpad that keeps its control lane out of Git by another means would be refused (D017's reopening condition). I ran no economy experiment: this is the reviewer role, which the support does not equip.

## Goal alignment at handoff

The promised benefit was that watching a real order stops meaning a hand-written store: one command reads canonical state and the worktree, writes the store, prints the lines, and refuses a stale binding by name. The observed outcome is exactly that, on this order itself, four times, with the three boundary claims VER-001 found false now true and regressed. Against the traps, scaled to consequence: **rule beating** was the live one, three green readings that did not mean what they said, and is answered by comparing the whole declaration, decoding before printing, and asking Git before writing, plus the defect-restored controls that prove each regression fails for its own reason; **shifting the burden** is answered by the checked directory being the one every printed line names; **tragedy of the commons** is measured rather than assumed, at about 291 KB per store with four retained here and prune eligibility explicitly undecided; **escalation** adds one suite, no gate, hook, key, daemon or recurring check, and the meter's step count sits below the receipt's threshold; **success to the successful** is the Contributor-only binding the receipt named, recorded with its reopening condition rather than hidden; **policy resistance** is the one-sided guard, recorded as the limit; **drift to low performance** lowers no standard, since a store that does not decode gets no launch line; **seeking the wrong goal** is answered by the outcome being the mission check judging real work, which the live row shows. NoOp would have kept the boundary the candidate observed as empty and the only live row synthetic. Naive Interventionism is answered by the intervention being a launchpad script over an unchanged runtime, reversible by not running it. My own intervention was one test case, one decision, three review documents and the gate.

## Release-close request and publication

This dispatch authorizes committing the reviewed state, pushing the `wo-148` branch and opening its pull request. It does not authorize a merge, a push to `main`, a tag, a Release or a package publication. The PR body beside this report carries the meter table `release prepare` supplied; the five-section release notes follow the publication contract at one physical line per prose paragraph and disclose the new command, the ignored-lane precondition, the store size, the one-sided guard and the transport limitation. Commits are made in coherent series after the result is recorded, with plain subjects and no attribution. After the operator merges, release close consumes the committed gate row without running a suite.
