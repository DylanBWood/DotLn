# WO-049 FINAL-001 — Target-worktree harness bundle

**Verdict: pass.** All eight acceptance criteria are met on the reviewed tree. The reviewer product gate passed with 30 suites, zero failures and 74 fresh tasks in 793.31 s outside the sandbox, and the document gate passed with 17 suites and zero failures in 100.87 s. VER-001's blocking finding is repaired and independently verified by VER-002; no blocking finding remains. The verifier's non-blocking findings and limits are dispositioned below, one by a documentation correction at review and one by an explicit handoff to WO-053.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.273","model":"claude-fable-5-1","effort":"xhigh","source":"self-reported"}

Dispatch: `resume: final review`, 2026-09-16, recorded by the harness at `2026-09-16T22:26:01.245Z` with the subject preserved at `refs/dotln/checkpoint/WO-049/9` (`cdb672822d37809b2cb0001de20ad6214ed97778`). Claude Code 2.1.273 was observed with `claude --version`; the model is `claude-fable-5-1` as this session exposes it after the operator's `/model` selection; effort `xhigh` was read from `CLAUDE_EFFORT`. No effective-session readback channel is claimed. The selected order is [WO-049](../../work-orders/WO-049-target-worktree-bundle.md); the complete numbered verification sequence is [VER-001](../../verifications/WO-049/VER-001.md) (fail, F1 routed to repair) and [VER-002](../../verifications/WO-049/VER-002.md) (pass over the repaired tree); the executor's and repairer's decisions are [WO-049-D001–D008](../../evidence/WO-049/decisions.md), with the [implementation](../../evidence/WO-049/implementation.md) and [repair](../../evidence/WO-049/repair.md) notes. No ideation breakout was opened during this order, so the briefing's ideation-receipt clause has no subject here; the two operator scope expansions and corrections made during execution (the evidence-before-claims rule, D001 and D002; the Codex session readback, D005 and D006) are recorded in the decisions file with their same-day corrections, and `docs/intake/notes` in this worktree holds no capture for the closeout helper to reconcile. I read the full staged diff (62 modified tracked paths, 40 new paths), the compiler lowering and its validation, the emitter's exclude-ownership and preflight logic, the host guard, both profile arrays, the Codex session reader, the three new scripts, the changed fixtures, every product write-back, and every cited discovery row the profiles name.

## Review choices and integration

The verified branch base and `origin/main` fetched at review are both `4fbf77c0dd3b570b18c2be147bfbb2355ab07f46`; `git ls-remote` confirmed the remote head and that `v0.24.0` (WO-051) is origin's newest release tag with no `v0.25.0`. The repair pass had already integrated WO-051 by fast-forwarding the branch to that base and retimed the unpublished minor release to `v0.25.0` with skeleton `0.21.0` and compiler `0.12.0` (WO-049-D007), so there is no upstream delta, no merge, no text conflict and no version collision left for this review. `npm run release -- prepare` with origin tag observation reported that the `v0.25.0` target remains current and changed no file. Original verification, evidence and repair bytes are preserved; VER-001 judges the pre-repair subject and VER-002 the repaired one, and neither is rewritten. All eight acceptance claims carry forward with their original evidence and are re-established by the reviewer gate on the same tree.

Reviewer edits are confined to documentation. The repair had corrected the F2 rationale by rewriting the Non-goals sentence in place, which the planning continuation contract does not admit: the document gate's two plan suites refused with "existing work-order bytes changed", because a reviewed order may change only by its assigned release version and an appended execution-record section (07 §Operator-opened planning pass). I restored the filed Non-goals bytes and carried the same correction, unchanged in substance, as the order's appended `## Execution record`, which the contract admits; no criterion, objective, non-goal or dependency changed, and VER-002's statement that no criterion moved remains true of its subject. One product-document correction: the 06 §Release boundary activation note still said skeleton `0.20.0` carries the target guard execution, which was true at activation but stale once the repair retimed the skeleton to `0.21.0` because WO-051 published `0.20.0` with `v0.24.0`; one sentence now names that retime and cites D007. The everyday-AI-user publication lock was refreshed for it and `npm run publication:check` reports both editions current. The work-order index was regenerated after dispatch (its projection had read `verifying`), `npm run meta` reproduced the follow-up register byte-identically and left the decisions index with the repair's D007 and D008 rows, and `node scripts/harness.mjs check` found all 24 generated surfaces current, so no bundle emit was needed. No intake or recovery ref was discarded; the repair's named preservation stash is retained.

VER-001's findings are dispositioned as follows. **F1** (removal destroyed pre-existing exclude content; criterion 3) is repaired and reproduced by VER-002; my own read of `excludeRegistry` and `updateTargetExclude` confirms the block is located by exactly one anchored marker pair, its bytes are compared before any mutation, a suffix survives the splice, and the separator newline is reclaimed only at end of file when the stored prefix hash still matches. **F3** (capability provenance contradicted by its cited row) and **F4** (a backslash import root generated an unloadable URL) are repaired in `contributor.ts` and `harness.ts` and covered by the profile fixture and the compiler lowering test. **F5** (`ultracode` not normalized) is repaired in `parseActor` and recorded as D007 with VER-001 as its source, as the verifier asked. **F2** (the attribution pre-check can never decide a commit because the permission guard denies every opaque shell command first) remains, now accurately stated in the order's appended execution record, the skeleton runbook and D008; no criterion tests attribution, widening the shell classifier would be a design change beyond this order, and host-authorized execution is WO-052 and WO-053 work. It is handed on explicitly rather than carried as an open defect.

VER-002 L3, the live smoke of criterion 6 being the pre-repair record, is the limit I weighed longest. I did not re-run it: the recorder refuses to overwrite an existing result by design, and the smoke needs the real harnesses launched outside the sandbox by the operator. I judge the criterion met on the recorded evidence because what the smoke uniquely witnesses is not a function of the repaired bytes. VER-002's filtered diff shows the generated hooks moved only in runtime pins, snapshot path and policy hashes between the smoke and the repaired tree; the settings file and the instruction block derive from the permission matchers and the instruction template, which the repair did not touch; and the repair edited exclude ownership, import-root validation, capability reasons and effort normalization, none of which sits on a hook's decision path. The claim the smoke establishes (Claude Code in print mode under `--permission-prompts none --setting-sources project,local` loads the target's `.claude/settings.json`, fires the registered PreToolUse hooks and honours their deny; Codex exec fires none and echoes the block after an explicit read) therefore carries forward. What changed since is covered by VER-002's direct execution of the current generated hooks on every enumerated case, and by the same fixture in this gate. A live launch of the current bundle belongs with WO-053's live proof, which already owns the target's live source-change smoke; its recorder should be given a fresh output path rather than the retained records. VER-002 O7 also holds: the Codex record's `hookEvents: 0` counts journal rows only a generated hook could write, so it confirms rather than tests, and the no-Codex-hook claim rests on discovery rows X-W3, X-W4 and X-W10, which I read.

Two further reviewer observations, neither blocking. First, the Codex session reader's effort allowlist in `usage-observation.mjs` and its rendered `(xhigh + workflows)` label admit `ultra` and `ultra code` but not `ultracode`, the spelling F5 added to the attestation normalization in `resume.mjs`. If a Codex thread's `turn_context` ever carried that spelling, the readback would report effort `unknown` rather than a wrong value, which is the safe direction, and the executor's own thread recorded `ultra`; whether Codex writes the unspaced spelling is unobserved. Recorded for the planner, not routed. Second, the target settings file is written with `autoMemoryEnabled: false`, which answers for targets the privacy edge that WO-051's review handed to WO-053 for the writer launch array; that array is unchanged by this order.

The rationale in D003 remains applicable: an opt-in target lowering with immutable launchpad imports and off-target state is the prerequisite that WO-052, WO-072 and WO-075 hard-depend on and WO-053 cannot run without, and NoOp leaves that path blocked. For this review's own choices: NoOp on the smoke re-run wins because the launch is not available from this session and the carried claim is not a function of the repaired bytes; the 06 correction is the smallest reversible edit and has no consumer beyond the publication lock; the sandboxed product gate was skipped on the evidence, recorded by both verifications, that two WO-068 fixtures fail on a nested `sandbox-exec` inside this wrapper, saving one run of about four minutes; rule beating is addressed by reading the exclude-ownership and preflight code and the fixtures' assertions rather than trusting a green suite whose regenerated hooks and manifest are themselves in the diff; seeking the wrong goal is kept in view by treating this order as the emit, check and remove transport for a Git-invisible bundle, not as proof that anything confines a worker. The record observed that neither harness sandbox confines a sibling write; containment is the hook's write check, the Codex named profile and the host's diff check.

## Evidence

| Check | Reviewer result |
| --- | --- |
| `npm test -- --review`, outside sandbox | **30 passed, 0 failed; 74 fresh tasks; 793.31 s; recorded 2026-09-16T22:48:08.947Z** |
| `npm run test:docs` | **17 passed, 0 failed; 100.87 s** over the three reports and the two publication files, after the release-surfaces profile rejected angle-bracket placeholders in the release notes as raw HTML (reworded) and the plan suites refused the in-place Non-goals edit (moved to the execution record); a second run over the final report bytes passed with the same counts in 95.81 s |
| `npm run release -- prepare` (origin tags) | Target v0.25.0 remains current; no files changed; origin's newest tag is v0.24.0 |
| `git ls-remote origin` | `refs/heads/main` equal to the branch base `4fbf77c`; no `v0.25.0` tag |
| `npm run publication:check` after the 06 correction | PASS; 269/269 product headings indexed; both editions current (30 and 45 linked sections) |
| `node scripts/harness.mjs check` | 24 generated surfaces current; local-terms list unavailable |
| Filtered diff of `.claude/hooks/*.mjs` and `harness-manifest.json` vs `HEAD` | No line outside pins, snapshot path and hashes; skills text moves only by the D005 role-text sentences |
| `npm run meta`; `npm run work-orders -- index` | Follow-up register byte-identical; decisions index carries D001–D008; index projection now `final-review` |
| Prettier on the 16 changed or new source, test and fixture files and the 11 changed documents | Clean |
| `git diff --check`; new lint or type suppressions in added lines | Clean; none |
| `git log main..HEAD` before the review commits; `origin/main` | Empty; equal to the branch base |
| `git diff package.json` files and lockfile | `@dotln/compiler` 0.11.2 → 0.12.0, `@dotln/skeleton` 0.20.0 → 0.21.0 with its compiler pin; no dependency added or removed |
| `node scripts/harness.mjs writer --show` | One reservation, this session, alive |

Reviewer product code identity: `94932b56729151d47fbaf81c337f63b71fca8f378306eb22ed976d06aa75f8f5`; reviewed tree: `5df65f459087a569729c683ce9c1c8c49c6ae243`. The gate's evidence reference is `host-gate:94932b56729151d47fbaf81c337f63b71fca8f378306eb22ed976d06aa75f8f5:npm test`; its passing row is retained in the local gate record and carried into the final-review control event for publication. The reviewer transcript is retained under `docs/control/local/wo049-final-product.txt` (ignored). The three new scripts were untracked until this review staged them before the gate, as the role directs, so the verifier's code identity is a different key over the same source bytes.

| Criterion | Assessment |
| --- | --- |
| 1: fixture emits into a scratch repo outside this checkout with and without unrelated `.claude/` content; status empty before and after; one manifest entry and one anchored exclude line per file; `check --target` passes and refuses drift, a missing file and a missing exclude line; emit refuses an unowned `.claude/settings.json` by name | Met. `emitTargetUnlocked` validates every path before its first mutation (regular contained surface, `git ls-files` tracked refusal, unowned-existing refusal naming the path, ignore-negation refusal), then preserves the runtime and writes the exclude block, files, manifest and receipt; `checkTargetHarness` requires the receipt, the manifest's self entry, each file's hash, one anchored exclude line per file plus `/.dotln/`, untracked status, effective ignoring and the runtime pins. Fixture tests 2, 3 and 6 assert each clause; VER-002 reproduced it through the CLI in four scratch repositories including both refusal branches. |
| 2: a generated hook imports the launchpad runtime through the absolute snapshot root and returns the enumerated decisions; runtime removed returns `deny` naming the runtime, never an advisory | Met. The prelude imports the boundary and host modules through percent-encoded `file://` URLs under the snapshot and wraps them in a catch that answers `deny` with `DOTLN_TARGET_RUNTIME_REFUSED` and no `systemMessage`; `runTargetHarnessHook` binds the target by digest, the snapshot by real path and the pinned files by hash, and denies on any classification failure. Fixture test 5 runs the real generated hooks as child processes for the write-inside, remote, sibling, bundle, opaque-shell, second-writer and renamed-snapshot cases; VER-002 reproduced every case. The `feedbackBoundary` comparison is direct only for the writer-isolation unit (VER-002 O1); the permission decisions route through `harnessAuthorization` and `protect`, and the enumerated decisions are what the criterion asks for. |
| 3: `remove --target` deletes exactly the manifest-listed files and exclude lines; the unrelated file survives; a before-and-after tree diff proves it | Met after repair. Ownership is by one anchored marker pair whose current bytes are compared before mutation; the splice preserves a suffix and reclaims the separator only at end of file under a matching prefix hash. Fixture test 4 covers the appended user rule, the duplicated block (check, emit and remove all refuse without touching tree, exclude or receipt), the refreshed baseline across episodes and the changed-prefix case; tests 2 and 3 prove tree and exclude identity; VER-002 verified both F1 manifestations and four further ownership cases. |
| 4: committed Contributor bundle byte-identical to the activation base with relative imports preserved; `harness check` passes | Met under D003. The Contributor path in `lowerToHarness` is untouched apart from the early return for the target kind; the filtered diff of the committed hooks and manifest leaves only pins, snapshot and hashes; no `file://` appears in `.claude/hooks/`; `harness check` passes at 24 surfaces; `loadout.semanticHash` is unchanged. Literal byte identity does not hold because the compiler and skeleton versions, the runtime snapshot and the policy hashes moved, which D003 permits and criterion 8 requires (VER-001 O8, VER-002 O6). |
| 5: after an episode the target tree equals its post-emit state; journal and reservation appear in the launchpad lane keyed by the target; no absolute path under the target except hook import lines | Met. `harnessStateDirectory` resolves through an `AsyncLocalStorage` store that the target hook sets to the launchpad lane keyed by `digest(root)`, refusing symlinked components on the way; fixture test 5 asserts the lane's `writer/` directory and journal, the target tree's identity after the episode, and that no emitted line carries the scratch base path except the `await import(new URL("file://` lines; VER-002 reproduced it over 30 invocations. |
| 6: a recorded live smoke, operator-run outside the sandbox, with the stated flags, showing the Claude guard refusing a denied effect and admitting the allowed write, and a Codex exec launch echoing the block with no hook event, paths reduced to shapes | Met on the recorded evidence, carried forward. `live-claude.json` records `deniedRequest`, `guardDenied`, `siblingWritten: false`, `allowedWrite`, six hook records and exit 0 with `--permission-prompts none` and `--setting-sources project,local` in the command shape; `live-codex.json` records `blockEcho`, `hookEvents: 0` and exit 0; both reduce target and launchpad to shapes. Both predate the repair (VER-002 L3); the reasoning for carrying the claim and the WO-053 handoff are above. |
| 7: write-backs in 02 §Harness compiler v1, 03 §Platform and instance boundary, `scripts/harness.mjs` usage, the skeleton README, WO-033's umbrella note and the decisions file | Met. 02 describes the profile kind, its fail-closed prelude, the import root's omission from metadata and the observed-but-omitted distinction; 03 adds one sentence; the usage string documents `emit\|check\|remove --target`, `--runtime-root` and both profile ids and matches the parser; the skeleton README's Target worktree bundle runbook discloses the shell, settings-merge, empty-directory and interrupted-installation limits and F2; WO-033 phase 2 names WO-049 as its emit, check and remove half; D001–D008 carry sources, rejected alternatives and reopening conditions and are indexed. The compiler README, 06 and 07 carry the version and readback notes. |
| 8: `npm test` green; `git diff --check` clean; no new dependency; fresh artifact-identity evidence and regenerated pins | Met by the reviewer gate and checks above; `docs/evidence/current.json` selects the WO-049 authority, artifact-identity, verification and feedback editions and the gate's evidence suites pass over them; every pin agrees at compiler 0.12.0 and skeleton 0.21.0. |

The gate is partly an instrument under review: the regenerated hooks refused this session's own Bash and Monitor calls while the gate was live, which is the write invariant working as specified, and the evidence-source declarations that produced the feedback and authority editions are in the same diff. Agreement with the executor's, repairer's and verifiers' runs is therefore necessary but not sufficient; the criterion judgments above rest on the source reads and fixture assertions named beside them.

## Limits and outcome

No live launch of either harness was made at verification or review, and the retained live records witness the pre-repair bundle bytes. The fixtures establish emit, check and remove semantics, exclude ownership, the generated hooks' decisions and the off-target state; they establish nothing about how a model behaves under the bundle in a real episode, whether three PreToolUse guards suffice for a real target, or whether anything confines a worker (the record observed that neither sandbox does). The Codex profile carries no hook and no enforcement; its governance is WO-051's launch profile and the host's diff check. Opaque shell commands, including test scripts and Git commits, are denied by the target permission guard and need a host route (F2). The local-terms list is unavailable, so the clean-room screen is a manual read of the authored diff: the scratch and launchpad paths appear only as shapes or in fixtures, the live records reduce paths, and the shared-floor addition is generic. VER-002 L6 records that the verifier deleted the launchpad's ignored exclude registries while probing; no tracked file, evidence file or target was affected, the fixtures create their own, and no installed bundle on this machine depends on a deleted registry.

Process cost: entry usage observation at `2026-09-16T22:26:55.407Z` reported 396,935 total tokens for this dispatch (196 input, 337,153 cached input, 55,984 cache writes, 3,602 output; 14 steps, 11 commands; source `claude-transcript-message-usage`, scope `dispatch`, cost unavailable). Final counters belong in the ignored usage receipt and the handoff response. The dominant cost was gate wall-clock, run once; the sandboxed product gate was skipped on the recorded evidence rather than rerun.

The reviewed publication is [PR.md](PR.md) and the five-section [release notes](RELEASE-NOTES.md). The commit series separates the Codex session readback with the effort-selector normalization, the shared-floor evidence rule, the target-worktree bundle with its tests, runtime write-backs, version pins, regenerated bundle and evidence editions, and the lifecycle records with the three reports and publication files. This pass authorizes publication of the WO branch and its pull request only; merge and release close remain the operator's next handoff.
