## What this merges

WO-029, the `v0.9.0` minor above `v0.8.0`: pinned artifact identity for the author's personal runtime. Every successful `compileLoadout` now returns a separate, versioned `ArtifactIdentityV1` beside the unchanged `CompiledProgram`: the compiler contract version (`compilerVersion: "1"`), the executing compiler package version (`0.3.0`), the existing whole-program `semanticHash`, the exact compilation environment and authority expiry needed to reproduce that key, and one domain-separated `fnv1a64:` definition digest per participating manifest tuple `(componentKind, componentId, version)`, computed over the canonical JSON of `{ domain: "dotln:component-definition:v1", componentKind, definition }` with the definition taken from the normalized graph. The semantic-hash algorithm and preimage are byte-identical to `v0.8.0`: Seiri still hashes to `fnv1a64:9ca8d0229c6bd8db` and the Entropy Reducer fixture to `fnv1a64:c5ddbca75f1c4cee`, and every fixture digest is cross-checked against WO-101's independent no-BigInt FNV oracle. The skeleton host mints only the new `LoadoutEquipped` payload `{ payloadVersion: 2, graph, artifactIdentity }` under event-envelope schema 1; the reactor starts with no authority, recompiles an equip before storing graph, pin, environment, and authority together, and routes every later program-consuming event (presence, pulses, WorkOrder emission, command results, deletion attempts, refusals, termination, verification, and the new `CommandRedispatchRequested` recovery check) through one `withEquippedArtifact` comparison. Compile diagnostics and drift on any axis (compiler contract, compiler package, semantic hash, component definitions, compilation environment, authority expiry) become typed inert refusals (`ArtifactCompilationRefused`, `ArtifactIdentityDrift`, `ArtifactIdentityInvalid`, `ArtifactIdentityUnavailable`) with no intent, schedule, or authority, never a thrown error out of replay; an unknown schedule id becomes a durable `UnknownScheduleRefused` that leaves valid authority intact. Every successful consuming `DecisionRecorded` trace names the pinned semantic hash, both compiler-version axes, and the equip event. Historical logs whose equip payload lacks `payloadVersion` still replay with identity labeled unavailable and receive no retroactive assurance; the upgraded host appends exactly one idempotent `ArtifactIdentityEnforcementStarted` before accepting new input, after which a legacy-derived state cannot originate a program-consuming decision until an explicit v2 re-equip. The L0 receipt and governed-raw projections gain an `artifactIdentity` section whose rows are `matched`, `unverified`, `unavailable`, or `refused` with canonical event links, while continuing to state that cryptographic integrity, authenticity, and outer-confinement evidence are not collected. The compiler and kernel remain pure and dependency-free; the kernel is byte-identical; compiler advances to component `0.3.0` and skeleton to `0.8.0`; no dependency was added.

- **Exact identities and manifest membership (criterion 1):** two compiles of each fixture, one from a deep clone and a fresh environment object, produce byte-equal identities; the component keys equal the compiled manifest's participating tuples in order (6 for Seiri, 9 for the Entropy Reducer); reversing every set-like collection and every object's key order leaves the identity byte-equal; a body edit under an unchanged id and version moves exactly that definition hash and, when it changes compiled semantics, the whole-program hash.
- **Pinned domain, preimage, scheme, and an unchanged semantic hash (criterion 2):** `dotln:component-definition:v1`, `dotln-component-definition-fnv1a64-v1`, and the 214-byte canonical preimage of the `Quiet 雪` ambient vector are stated in 04 and in the compiler test and hash to `fnv1a64:3a5e415be9582bed` under the compiler, under WO-101's oracle, and under a third FNV-1a-64 implementation written for this review; the verifier rebuilt the compiler from the published `v0.8.0` commit and reproduced both whole-program keys byte for byte; this review reproduced both keys and all 15 definition hashes from its own canonical serializer; relocating a graph to another repository path changes only the whole-program key and requires an explicit re-equip.
- **v2 equip, atomic pin, no pre-equip authority (criterion 3):** `initialState()` has `authority: null`; a consumer before any equip refuses; the sole host factory emits exactly the three-key v2 shape and throws on a caller-supplied legacy draft; absence of `payloadVersion` is the only legacy discriminator, while `null`, `1`, `3`, `"2"`, and an extra key each refuse as `ArtifactIdentityInvalid`; a graph paired with a stale pin refuses as `ArtifactIdentityDrift` with no intent, schedule, or authority.
- **One comparison helper and typed refusals (criterion 4):** the reactor has exactly two runtime compile call sites, the equip decision and `withEquippedArtifact`; each drift axis was driven through every consumer type by the verifier and again by this review with the payload naming the axis and authority nulled; compile diagnostics refuse without throwing; two live runs produce byte-identical logs, replay reproduces the decisions, and the audit projection over the replayed log equals the live one.
- **Forward-only enforcement and untouched history (criterion 5):** the preserved pre-WO-029 fixture replays without throwing, ends with identity `null`, and projects only `unavailable` rows; the boundary appends once, drops unpinned authority, and is idempotent under replay; a legacy-derived state refuses forward input and a legacy-shaped equip after the boundary; an explicit v2 re-equip restores a pinned, authorized state; the frozen WO-003 trace oracle is unmodified (`ec53d1c8…` reproduced) and the new trace fields are carried by an explicit filtered delta rather than a rewrite.
- **Explicit unknown-schedule evidence (criterion 6):** after cutover an unknown id emits a durable `UnknownScheduleRefused` with valid authority retained; a test states that a reused id from an earlier equip is not detected without event stamping.
- **Audit projections (criterion 7):** the L0 receipt and governed raw both carry the `deterministic-content-equality-only` projection; a real appended log yields a `matched` equip row naming the pinned hash and a `refused` row carrying `drift: ["semantic-hash"]` whose links resolve to canonical events; a malformed refusal payload is rejected as an invalid audit source; the limitations deny cryptographic integrity, collision resistance, and authenticity, keep outer-confinement evidence separate, and add that graph and pin can be rewritten together and that a queued pulse carries no artifact stamp.
- **Gate, hygiene, purity, versions (criterion 8):** `npm test` exit 0 for the executor, the verifier, and twice for this review; `git diff --check` clean; no Node builtin, `process`, filesystem, clock, or randomness in compiler or kernel source; no dependency change; `check-surfaces` passes the README block at `v0.9.0`, compiler `0.3.0` and skeleton `0.8.0` changed, kernel `0.2.1` unchanged, and the body profile; the compiler's source version constant equals its `package.json`.
- **Write-backs:** 02's `Artifact identity v1` section, 03's promotion of the pinned-identity section from candidate to shipped with its limits, 04's pinned preimage, domain, scheme, and fixed vector, 06's dated activation completion, 09's association labels and `Deterministic artifact receipts` section, 10's identity and payload axes, both package READMEs, the planning map's dated paragraph and catalog row, the publication index rows and both edition locks, the ledger's dated section extending two adopted decisions, and the README release block at `v0.9.0`.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in this worktree on the verified tree and again on the reviewed tree: Prettier clean, the body-profile suite, every shell suite, `check-publication.mjs` at 227 of 227 headings with both audience locks current, `index --check` current, `tsc -b --force`, 221 of 221 package tests, 8 of 8 identity-corpus tests, and `artifact-identity-evidence.mjs --check` verifying all four generated evidence files, exit 0. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.9.0; expected v0.9.0 (work-order target v0.9.0; latest published v0.8.0)`, `PASS component-version` for compiler `0.3.0` (src changed, previous `0.2.0`), kernel `0.2.1` (src unchanged), and skeleton `0.8.0` (src changed, previous `0.7.0`), and the body-profile rule over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity: a temporary-index tree of the working tree differs from the final-review allocation checkpoint only in the two control projections and the regenerated index; every deliverable blob is byte-identical from the implementation-ready checkpoint through the verify-allocation, verdict, and final-review checkpoints to the reviewed tree; VER-001 is unchanged since its verdict; all five checkpoints have `HEAD` (`961601a`, also `main`, `origin/main`, and the commit `v0.8.0` peels to) as their sole parent; `git log main..wo-029` is empty; the work order's text is byte-identical to activation apart from its completed title version, release classification, and dated activation-completion header; the verifier's report header equals its control-log actor by JSON comparison; the executor's `max` and the verifier's `max` meet the declared `xhigh+` floors.
- Direct probes: 35 assertions of this review's own probe script against the built `dist` (pre-equip refusal, v2 equip pinning, stale-pin drift, five drift axes through nine consumer types, compile diagnostics, the `payloadVersion` discriminator, legacy replay, boundary idempotence, forward refusal, re-equip, unknown schedule, host-factory refusals, byte-identical live runs, replay and audit identity, the committed `scenario.jsonl` and `audit.json` equal to live output, a projected drift row, a rejected malformed refusal, relocation, and the rewritten-pair limit); a Python FNV-1a-64 and canonical-JSON implementation sharing no code with the compiler or WO-101 reproducing the RFC anchors, the pinned vector, both whole-program keys, and all 15 definition hashes; the CLI's default output byte-identical to `fixtures/wo029-cli.txt` apart from the version banner; 564 relative links across the 20 changed Markdown files resolving, with this review's own allocated report path the only dangling target until the report existed; a clean-room and identity screen over all 12,110 added and new lines finding only the repository's own releases URL; `codex --version` reporting the executor's recorded `0.153.4`.

## Verification trail

- `docs/verifications/WO-029/VER-001.md`: pass; all eight criteria, the evidence gate, and every write-back reproduced independently by rebuilding the compiler from the published base, re-deriving every hash through WO-101's oracle over the verifier's own canonical serializer, and driving the reactor and audit with the verifier's own probes; three non-blocking observations and four disclosures with no repair required.
- `docs/final-reviews/WO-029/FINAL-001.md`: pass; the verifier's observations and disclosures adjudicated with reasons, subject identity proven against all five checkpoints by git objects, the reviewer's own observations recorded, one dated planning-map sentence applied and re-checked, method and instruments disclosed.

## Known open items

- **The adjacency association fallback is retained but exercised by no fixture** (VER-001 O1): every log this repository now produces takes the `explicit-event-link` path; a synthetic historical-shape fixture is a test candidate for the next audit order.
- **`duplicate-boundary` is named in the evidence gate but is not a separate transcript row** (VER-001 O2): the property is proven by an executable test and by this review's probe, and the package records `canonicalBoundaryCount: 1` after two boundary calls inside the `legacy-before-boundary` row.
- **The `compilation-environment` drift axis cannot fire at the equip boundary** (VER-001 O3): the equip decision compiles with the pin's own declared environment, so the axis is reachable only for a stored pin or environment mutated after equip, which is the declared limit for a writer who can replace graph and pin together.
- **A refusal reason names the enforcement boundary in one raw-replay case where none exists** (this review): a program-consuming event before any equip sets the blocked flag, and a subsequent legacy-shaped equip is then refused with the reason `legacy equip after enforcement boundary` even in a log with no boundary; the refusal type is correct, the host driver always writes the boundary first, and only the wording is imprecise.
- **`CompileResult`'s success shape gains a required field** (this review): additive for every in-repo consumer and covered by the compiler's `0.2.0` to `0.3.0` minor; a third-party constructor of `CompileResult` literals would need the new field.
- **A reused schedule id from an earlier equip is not detected** without a per-pulse issuance stamp; signatures, membership proofs, per-pulse submissions, worker-result echo (WO-009), and verifier-artifact pinning (WO-010) remain deferred, and hash equality never authenticates a writer who replaces both graph and pin or excludes hidden adapter effects.
- **`CLAUDE_EFFORT` is an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9): still open, so Claude Code attestations remain `self-reported`.
- **Publish and release close.** The bounded publisher `npm run worktree -- publish WO-029` runs the committed surface check and body validation before any push and then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses at that preflight the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.9.0` is strictly above `v0.8.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.9.0` tag, and creates the matching GitHub Release. The subject worktree's populated `.control-beacons/` is disposable at its anchored root and does not block removal.
