# WO-029 final review FINAL-001

**Subject:** branch `wo-029`, worktree `DotLn-wo029`, uncommitted working tree, 2026-09-05. `HEAD`, `main`, and `origin/main` are all `961601a17174c14ac69afa24dbd62ca5432ad7e7`, the PR #34 merge of WO-021, which is also the commit the published annotated tag `v0.8.0` (tag object `7b494bea…`) peels to locally and at `origin` (`git ls-remote origin main` and `git ls-remote --tags origin v0.8.0` agree). The branch carries no local commits (`git log main..wo-029` is empty), so the entire subject is the dirty tree. At review open, after the dispatch transition and the index refresh the guide requires of a final-review actor: 54 `git status --porcelain -uall` entries, 34 tracked modifications (against `HEAD`, 942 insertions / 273 deletions including the two `docs/control` files and the generated `docs/work-orders/README.md`; 32 files, 905 / 252 without them) plus 20 untracked paths totalling 9,805 lines: the compiler's `src/artifact-identity.ts` (69), `test/artifact-identity.test.ts` (290), and fixtures `wo029-entropy-reducer.json` (2,074) and `wo029-identities.json` (139); the skeleton's `src/artifact-identity.ts` (351), `src/artifact-audit.ts` (190), `test/artifact-identity.test.ts` (653), and fixtures `wo029-cli.txt` (33) and `wo029-legacy-scenario.jsonl` (21); `scripts/artifact-identity-evidence.mjs` (316); the order's own control segment `docs/control/orders/WO-029.jsonl` (four events at dispatch, five after it); `docs/evidence/WO-029/` (`README.md` 82, `audit.json` 1,934, `baseline.json` 32, `negative-transcripts.json` 1,833, `release-surfaces.txt` 9, `scenario.jsonl` 28, `semantic-hash-inventory.json` 52, `test.log` 1,352); and `docs/verifications/WO-029/VER-001.md` (343). The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree taken after the dispatch and index refresh (`73d73340756590c7963d11194cad5455e6d75e5e`) differs from this review's allocation checkpoint `refs/dotln/checkpoint/WO-029/5` only in `docs/control/current.md`, the strictly appended segment, and the regenerated index, and from the verdict checkpoint `/4` in exactly the same three files, so VER-001 has not changed by a byte since its verdict was recorded (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, one dated sentence in the planning map (§Corrections applied by this review), and the lifecycle's own events.

**Authority:** `docs/work-orders/WO-029-pinned-artifact-identity.md`: `Model: any capable model`; `Effort: executor xhigh+; verifier xhigh+; reviewer any`; a `v0.9.0` minor classification from the local published `v0.8.0` base (`961601a`), completed by the executor on 2026-09-05 under the operator's standing opt-out release default because the control activation omitted a target, with compiler `0.3.0` and skeleton `0.8.0` advancing and kernel `0.2.1`, event-envelope schema `1`, and compiled-program contract `1` unchanged; nomination provenance from the operator's 2026-09-04 WO-023 ideation breakout, an ad hoc Fable planning response, and an independent repository audit, filed as a Shape-First synthesis with a passed clean-room screen; dependencies WO-008, WO-016, and WO-023 (all `closed` with passing verdicts in the control fold) and WO-101's independent FNV oracle (present at `corpus/harness/id-corpus-lib.mjs`); the cited sections; an objective; a five-bullet dated observed gap; eight scope-discipline bullets; deliverables; eight numbered acceptance criteria; an evidence gate; a write-back duty; non-goals; and four operator-review assumptions. Against the activated text (`refs/dotln/checkpoint/WO-029/1`, itself byte-identical to `main`'s copy) the order differs by 10 insertions and 3 deletions: the title's `(version assigned at activation)` became `, v0.9.0`, the two-line placeholder release classification became the completed five-line one, and the seven-line dated activation-completion header was added. The objective, observed gap, scope discipline, deliverables, all eight criteria, the evidence gate, the write-back duty, the non-goals, and the assumptions are byte-identical to activation.

**Prior reports:** `VER-001` (pass; all eight criteria, the evidence gate, and every write-back reproduced independently: the compiler rebuilt from the published `v0.8.0` commit to establish the pre-change whole-program keys, every definition hash and both semantic hashes re-derived through WO-101's independent no-BigInt oracle over the verifier's own canonical serializer, the reactor, audit, and factory driven by the verifier's own probes rather than the executor's helpers, a clean-room screen over 11,764 lines, and the executor's suite run only as a second signal; three non-blocking observations and four disclosures, none requiring repair). Complete sequence: one report, no repair episode. The executor's evidence README, its receipts under `docs/evidence/WO-029/`, and the activation-completion header inside the work order are executor evidence and were treated here as VER-001 treated them: claims under test.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes) in Claude Code 2.1.261 with sandboxed Bash; macOS on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, the system `python3` 3.11.7 for the independent hash re-derivation and link sweep, and the lockfile-resolved Prettier and TypeScript in this worktree's own dependency tree. Reasoning effort `max`: the session's model control reported it at launch, but `docs/discovery/environment.json` records `effectiveEffortReadback` for `claude-code` as `not found` with `harnessReadbackEligible: false`, so `harness-readback` is not admissible and the epistemic source is `self-reported`, exactly as VER-001's was. `2.1.261` was checked against `claude --version` on this machine and is the newest entry on the harness's observed `versions` list (observed 2026-09-04); `max` is on `sessionEffortSelector.values`; the `claude-code` discovery entries are byte-identical to `main` (this order's discovery diff touches only the `codex-cli` entries), so the attestation is admissible on merged evidence alone. The order declares `reviewer any`, so any attested effort would satisfy it; `max` is stated because it is what the session reports. The executor was a different harness, model, and session (Codex CLI `0.153.4`, `gpt-6-astra`, `max`, operator-attested; `codex --version` reports `codex-cli 0.153.4` on this machine) and the verifier a different model and session on this harness (`claude-opus-5[1m]`), so the three roles are independent in session and actor.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-029 passes.** The order asked for a forward-only pinned artifact-identity mechanism: a versioned identity record returned beside, never inside, the compiled program, carrying the compiler contract and package versions, the unchanged whole-program semantic hash, the exact compilation-environment preimage, and domain-separated definition digests for exactly the manifest's participating tuples; a host that mints only a v2 equip payload, recomputes before pinning, starts with no authority, and routes every program-consuming decision through one comparison whose failures are typed, inert, durable refusals rather than exceptions; a logged, idempotent enforcement boundary that keeps historical logs replayable without turning compatibility into an unpinned forward path; explicit unknown-schedule evidence; and audit projections that expose the receipt while still denying cryptographic integrity and authenticity. The deliverable does that, and the blueprint says so. This review re-established it first-hand rather than inheriting it: a third FNV-1a-64 implementation written in Python for this review, sharing no code with the compiler's BigInt recurrence or WO-101's 32-bit pairs, reproduced the RFC anchors, the pinned 214-byte `Quiet 雪` vector at `3a5e415be9582bed`, both whole-program keys (`fnv1a64:9ca8d0229c6bd8db`, `fnv1a64:c5ddbca75f1c4cee`) over its own canonical serialization of the compiled programs, and all 15 fixture definition hashes over its own serialization of the normalized definitions, with the manifest tuples equal to the identity tuples in order; 35 assertions of this review's own probe script against the built `dist` covered the pre-equip refusal, v2 pinning, the stale pin, five drift axes through nine consumer types, compile diagnostics, the `payloadVersion` discriminator, legacy replay, boundary idempotence, forward refusal, re-equip, the unknown schedule, host-factory refusals, byte-identical live runs, replay and audit identity, the committed canonical log and audit projection equal to live output, a projected drift row, a rejected malformed refusal, relocation, and the rewritten-pair limit; the CLI's default output is byte-identical to its new fixture apart from the version banner; the reactor has exactly two runtime compile call sites; the compiler diff touches no program construction and `normalize.ts` changes only an `export` keyword, so the semantic preimage is unchanged by construction as well as by measurement; kernel and compiler have no Node builtin, `process`, filesystem, clock, or randomness reference; `npm test` is green twice; `git diff --check` is clean; `check-surfaces` passes the README block at `v0.9.0`, the three component rules, and the body profile over this package. VER-001's three observations are one uncovered-but-functional historical fallback, one evidence-packaging naming gap, and one axis that is unreachable at equip by design; this review adjudicates all three as non-blocking, verifies that the verifier changed nothing but its own report and the generated index, records observations of its own, applies one dated planning-map sentence, and changes no code, contract, acceptance, schema, authority, or captured-evidence byte.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **The compiler and reactor certify what they mint.** `compileLoadout` produces the identity, `withEquippedArtifact` compares it, and the shipped suites grade both with the same modules. Compensating controls: a Python FNV-1a-64 and canonical-JSON implementation written for this review and sharing no code with the subject or with WO-101 reproduced every hash the fixtures pin, over material dumped from the built `dist` (normalized definitions, compiled programs, manifests); the reactor was driven by this review's own probe script through the raw `replay` entry point as well as through the host driver, so the host's own append loop was not the only witness.
2. **The evidence helper certifies its own generated files.** `scripts/artifact-identity-evidence.mjs --check` regenerates four files in memory and compares bytes. Compensating controls: this review regenerated the canonical scenario log and audit projection through `runScenario` and `projectAuditEvents` directly and compared them with the committed `scenario.jsonl` and `audit.json` byte for byte; the helper's `--check` mode was also read in full (it exits 1 on any mismatch and never repairs).
3. **The lifecycle helper dispatched this review but is not subject.** `scripts/resume.mjs`, `scripts/lib/`, `scripts/worktree.mjs`, `scripts/release.mjs`, `scripts/release-notes.mjs`, and `scripts/github-body.mjs` have no working-tree entry and are byte-identical to `HEAD`, so the dispatch, the checkpoint, the beacon emission, the index generator, the surface preflight, and the body validators are merged code. Subject identity was nevertheless established with git objects rather than with the helper's projection (§Dispatch and subject integrity).
4. **WO-101's oracle is a merged instrument.** `corpus/harness/id-corpus-lib.mjs` has no working-tree entry; it was used by the executor, the verifier, and the shipped tests, and this review added a third implementation rather than relying on it.
5. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile** by calling `parseReleaseNotes` and `assertGitHubBodyProfile` directly and through `check-surfaces`; both passed on the first pass with no angle brackets, HTML, or reserved markers. Because `v0.9.0` is above the latest published tag, these notes become the `v0.9.0` edition at release close.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-029 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-05T19:05:12.235Z`, `elapsed` `{"implementation":3709599,"verification":1244232}`, checkpoint `55aecb70e82192d7ab27ec4e4e3266eb08aefdf4` (`/4`), and `orders[]` with 24 entries; no projection-disagreement warning was emitted. Before invoking the transition this review read the `resume` mapping in `package.json`, the execution guide's dispatch contract, closeout, freshness, discipline, and model-specific sections, the harness runbook's notes on sandboxed transitions, the final-review README, the work order, VER-001, and the WO-021 package as precedent, and confirmed that no lifecycle script has a working-tree entry. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-029/FINAL-001.md` (the directory itself is created by the reviewer), moved the phase to `final-review`, appended `FinalReviewRequested` with `recordedAt` `2026-09-05T19:11:11.700Z` as the fifth line of `docs/control/orders/WO-029.jsonl` (the order owns its own segment and `docs/control/resume.jsonl` is byte-identical to `HEAD`), minted checkpoint `2e1905a9db62fb26109d3d06ddaed6ad58c73a48` (`refs/dotln/checkpoint/WO-029/5`), and emitted the merged WO-021 beacon: `.control-beacons/public/` and `verifier/` each hold a 72,341-byte mode-0600 file, the size the v2 codebook assigns to (`final-review`, `pass`, `max`, `host-projected`), and `groups/` holds the 1,235,671,522-byte group cache, all three with mtime `1788635471.700` s, which is `Date.parse("2026-09-05T19:11:11.700Z")`. The index refresh that followed changed only the WO-029 lines (`verified` → `final-review`, the pending `FINAL-001` row, and the `Now` line). The segment's five events are `WorkOrderActivated` (`17:40:23.786Z`, `effortDeclarationValidated: true`), `ImplementationReady` (`18:42:13.385Z`), `VerificationRequested` (`18:44:28.003Z`), `VerificationCompleted` (`19:05:12.235Z`), and this review's `FinalReviewRequested`; every event carries `schemaVersion: 1` and the stamps strictly increase. The projected `implementation` span (3,709,599 ms) and `verification` span (1,244,232 ms) are the arithmetic differences of the recorded stamps.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree after the allocation and index refresh as tree `73d73340756590c7963d11194cad5455e6d75e5e`, and `git diff-tree -r --name-status` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                                                                                                                              |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`926cdf7f…`)              | every deliverable path (54 paths: 34 modified, 20 added, the segment among them because each checkpoint captures the tree immediately before its transition appends); the whole subject, the work order's own header completion included, is downstream of activation |
| `/2` implementation-ready (`9070d207…`)    | `docs/control/current.md`, `docs/control/orders/WO-029.jsonl`, `docs/work-orders/README.md`, `docs/verifications/WO-029/VER-001.md` (added); nothing else, so every deliverable, every write-back, and the evidence directory were already in the readiness tree |
| `/3` verify allocation (`5e3bb19e…`)       | the same four paths                                                                                                                                                                                                                            |
| `/4` verdict (`55aecb70…`)                 | `docs/control/current.md`, `docs/control/orders/WO-029.jsonl`, `docs/work-orders/README.md`; VER-001 is not listed, so it is byte-identical to the blob its own completion checkpoint captured                                                  |
| `/5` final-review allocation (`2e1905a9…`) | `docs/control/current.md`, `docs/control/orders/WO-029.jsonl`, `docs/work-orders/README.md`                                                                                                                                                    |

Every deliverable blob is therefore identical across the implementation-ready, verify-allocation, verdict, and final-review-allocation checkpoints and the working tree; the verifier changed nothing outside its report except the regenerated index (VER-001 disclosure 1); VER-001 has not changed since its verdict. All five checkpoints have `HEAD` as their sole parent. The completion mints `/6`.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. `docs/intake/{chats,images,notes}` in this worktree each hold only `.gitkeep`, and `git status --porcelain --ignored` lists only `node_modules/`, the three `packages/*/dist/` outputs, and the anchored `.control-beacons/`; there is no `.claude/settings.local.json` here, and `.claude/settings.json` is tracked and byte-identical to `main`. `docs/observations/` does not exist and `git check-ignore` confirms that path is not ignored. `docs/decisions/`, `scripts/`, `packages/kernel/`, `packages/skeleton/src/cli.ts`, `packages/skeleton/src/loadouts/`, `corpus/`, `docs/control/resume.jsonl`, and the root `package.json` dependency blocks are byte-identical to `main`; `docs/discovery/environment.json` and `environment.md` differ from `main` only by the appended `codex-cli 0.153.4` observation and its nested persisted-selector history.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control segment                                                                                     | Report header                                                                                                                                                                                                                                                                                                                                                              | Declared minimum | Result                  |
| -------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ----------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.4","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`     | there is no executor report; the evidence README and the work order's activation-completion header state the operator-selected model and effort; `codex --version` reports `codex-cli 0.153.4` on this machine, the executor appended `0.153.4` to the `codex-cli` observed `versions` list with the persisted `model_reasoning_effort` selector observed as `max` at `18:14:17Z`, so `max` was admissible at append | `xhigh+`         | `max` exceeds the floor |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}` | exactly one column-one header in VER-001, `**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}`, parsed and compared with the event actor as JSON in this session: equal                                                                                                        | `xhigh+`         | `max` exceeds the floor |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`  | the header above                                                                                                                                                                                                                                                                                                                                                           | `any`            | satisfied               |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: none` and the JSON form's `effortDrift` is `[{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.261; model claude-fable-5-1; effort max; source self-reported`, the same drift line, phase `closed`, a `finalReview` elapsed value beside the two existing values, and legal actions `release-close, next, activate`, and the emitted beacon should be 81,573 bytes (code 1146 for `closed`, `pass`, `max`, `host-projected`).

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree after the dispatch transition and the index refresh, before any review artifact existed: `All matched files use Prettier code style!`, `GitHub body profile tests passed`, the shell suites (fixture temp-root, publication checker, backup-intake, resume, checkpoint, worktree, release, work-orders), `PASS index coverage: 227/227 product headings indexed` with both audience editions `CURRENT` (30 and 45 linked sections), `PASS docs/work-orders/README.md is current`, `tsc -b --force`, `# tests 221 / # pass 221 / # fail 0 / # skipped 0` including the nine `WO-029` compiler tests and the ten `WO-029` skeleton tests, the identity corpus `# tests 8 / # pass 8 / # fail 0`, and `Verified 4 WO-029 artifact evidence files; original semantic hashes and frozen oracle unchanged.`, exit 0. Second run, after this review's map sentence and the three package artifacts were in place, with the same result (§Readiness records its completion).

### Static, surface, and structural checks

- `git diff --check` clean, before and after this review's additions.
- `packages/kernel` has no working-tree entry, so it is byte-identical to the activation base and to `v0.8.0`. The compiler diff is `compile.ts` (the hash stored in a variable and the identity added to the success result), `index.ts` (one re-export), `normalize.ts` (the `export` keyword on `fnv1a64`), and `types.ts` (two new interfaces and the new success-result field); `normalizeCompiledProgram`, `canonicalStringify`, `semanticHash`, and every program-construction path are untouched, so the semantic preimage is unchanged by construction. A scan of `packages/compiler/src` and `packages/kernel/src` finds no `node:` import, `require`, dynamic `import()`, `process.`, filesystem call, `Date.now`, `Math.random`, or `fetch`. The lockfile changes only the compiler and skeleton versions and the skeleton's compiler dependency pin; root `devDependencies` are unchanged; neither compiler nor kernel declares a dependency; `package.json` gains only the `evidence:artifact` script and its `npm test` step.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.9.0; expected v0.9.0 (work-order target v0.9.0; latest published v0.8.0)`, `PASS component-version @dotln/compiler: src changed; observed 0.3.0; previous v0.8.0 0.2.0`, `PASS component-version @dotln/kernel: src unchanged; observed 0.2.1`, `PASS component-version @dotln/skeleton: src changed; observed 0.8.0; previous v0.8.0 0.7.0`, and the body-profile rule, first with `no current final-review bodies yet` and then over this package's `PR.md` and `RELEASE-NOTES.md`. `COMPILER_PACKAGE_VERSION` in `artifact-identity.ts` equals the compiler `package.json` version (`0.3.0`).
- A relative-link and anchor sweep written for this review over all 20 changed and new Markdown files, using GitHub's slug rules, resolved 564 targets; the only dangling one was `docs/final-reviews/WO-029/FINAL-001.md`, this report's own allocated path, linked from the regenerated index.
- The clean-room and identity screen over the full tracked diff and every untracked file (12,110 lines) found no absolute path, user name, address, internal hostname, non-public URL, commercial tracker or ALM product name, predecessor-system name, or managed-host or gateway description; the only URL is the repository's own GitHub releases page in the README block; the regular-expression hits were the substring `rally` inside `structurally` and `literally`, `Token` inside the `promptTokens` cost fields of the evidence JSON, and the words `corporate`, `gateway`, `employer`, and `token` inside VER-001's own clean-room paragraph that says they were not found.
- The verifier's column-one header equals the `VerificationCompleted` actor by JSON comparison; the executor's `ImplementationReady` actor is the operator-attested Codex default the receipts state.
- `.prettierignore` gains `docs/evidence/WO-029/{audit,negative-transcripts,semantic-hash-inventory}.json`, whose exact bytes the evidence helper owns, and already excludes `docs/final-reviews/WO-*/`, `docs/verifications/WO-*/`, and `docs/work-orders/*.md`, so this package and the immutable reports are not formatter input, while the map sentence added by this review is and passes `prettier --check`.
- The frozen WO-003 oracle `packages/skeleton/fixtures/wo003-decision-traces.json` has no working-tree entry and hashes to `ec53d1c841de5486cf656694228c83f67e8549993d0ac63c187d10728707a173` under `shasum -a 256`, the value the baseline, the inventory, and the skeleton test record.

### Independent probes against the deliverable

- **A third hash implementation.** A Python FNV-1a-64 over UTF-8 bytes, written for this review, reproduces the RFC 9923 anchors for `""`, `"a"`, and `"foobar"` (`cbf29ce484222325`, `af63dc4c8601ec8c`, `85944171f73967e8`) and hashes the 214-byte canonical preimage quoted in 04 and in the compiler test to `3a5e415be9582bed`, the value the compiler and WO-101's oracle also return. Over a dump of the normalized graphs, compiled programs, and identities taken from the built `dist`, the same script serialized every preimage with Python's own sorted-key, whitespace-free, non-ASCII-preserving JSON rather than the compiler's `canonicalStringify` and reproduced both whole-program keys and all 15 definition hashes (6 Seiri, 9 Entropy Reducer), with the manifest tuples equal to the identity tuples in order.
- **Reactor probes through the raw entry point and the host driver.** 35 assertions: `initialState()` has null authority and no pin; a pulse before any equip refuses `ArtifactIdentityUnavailable` (`no valid equip`) with null authority; a v2 equip through the host factory stores an identity equal to the compiler's, an authority equal to the compiled envelope, and an equip event reference, and the log begins with the boundary followed by a three-key v2 payload; a changed graph paired with the original pin refuses `ArtifactIdentityDrift` with `["semantic-hash","component-definitions"]` and no authority; each of `compiler-contract-version`, `compiler-package-version`, `semantic-hash`, `component-definitions`, and `authority-expiry` mutated in a stored pin refuses through all nine consumer event types with the payload naming exactly that axis, the source event id, and null authority; an empty active-mechanic list under a valid pin refuses `ArtifactCompilationRefused` with seven typed diagnostics and no throw; `payloadVersion` `null`, `1`, `3`, `"2"`, and an extra key each refuse `ArtifactIdentityInvalid`; the preserved legacy log replays without throwing, `verified: true`, identity `null` in every decision, and every audit row `unavailable`; appending the boundary twice adds one event, drops the legacy authority, refuses the next pulse as `ArtifactIdentityUnavailable`, leaves state unchanged under a replayed duplicate, refuses a legacy-shaped equip, and a v2 re-equip restores authority with two schedules on the next away transition, the extended log replaying to identical decisions; an unknown schedule id records a durable `UnknownScheduleRefused` with authority retained; the host driver throws on a legacy equip draft and on a caller-supplied boundary and appends nothing; two `runScenario` runs produce byte-identical logs, replay reproduces the decisions, the audit projection over the replayed log equals the live one, every one of the 10 consuming traces ends with the four pin inputs naming the live hash, contract, package version, and equip event, the L0 equip row is `matched` with links that resolve, governed raw carries the same projection, and the committed `scenario.jsonl` and `audit.json` equal the live output byte for byte; a real appended drift refusal projects a `refused` row with `drift: ["semantic-hash"]` and resolvable links, and a tampered refusal payload is rejected as `malformed artifact refusal`; relocation to `/relocated/repository` moves only the whole-program key (`fnv1a64:9ca8d0229c6bd8db` → `fnv1a64:a20fffda0d4e31e6`); a graph and pin rewritten together are accepted, which is the declared limit.
- **CLI.** `node packages/skeleton/dist/src/cli.js` prints 33 lines whose bytes equal `fixtures/wo029-cli.txt` apart from the version banner: 28 events beginning with `ArtifactIdentityEnforcementStarted` and ending with `SchedulesCancelled`, eleven `DecisionRecorded`, and `verified=true candidates=1`.
- **Comparison-path count.** `grep` over `reactor.ts` finds `compileArtifact(` at exactly two runtime sites, the equip decision and `withEquippedArtifact`; `compileLoadoutProgram` and `compileWorkOrder` survive only as exports for the scenario and CLI surface, and no other skeleton source calls them from a decision path.
- **Restoration.** The probe wrote only to the session scratchpad; the status entry count returned to 54 before this review's artifacts were added and the tree matched the one VER-001 passed.

### Reading

Read in full: the work order and its activation-completion header; VER-001; the executor evidence README; the WO-021 final review, PR body, and notes as the package precedent; the execution guide's resume, closeout, freshness, discipline, and model-specific sections; the final-review and verification READMEs; the harness security runbook; the complete tracked diff (`.prettierignore`, README, the two discovery records, ledger, map, products 02, 03, 04, 06, 09, 10, the three publication files, the work order, the lockfile and three manifests, `compile.ts`, `index.ts`, `normalize.ts`, `types.ts`, `audit.ts`, `reactor.ts`, `scenario.ts`, the compiler and skeleton READMEs, and the three changed skeleton tests); every untracked source, test, fixture, and script leaf; the identities fixture, the CLI fixture, the legacy fixture's event shapes, the inventory, baseline, and release-preflight receipts; the control segment; the generated index; and the notes parser, body profile, worktree publish path, and release surface rule in `scripts/`.

## Acceptance criteria

1. **Exact identities and manifest membership — met.** Two compiles of each fixture, one from a deep clone and a fresh environment object, are byte-equal; the identity's component keys equal the manifest's participating tuples in order (6 and 9); the compiler suite proves set-like and key-order reordering inert and a body edit attributable to exactly the edited definition, moving the whole-program hash only when compiled semantics change; this review's independent serializer reproduced every hash the fixture pins.
2. **Pinned domain, preimage, scheme, unchanged semantic hash — met.** Domain, scheme, and the full canonical preimage are asserted in the compiler test and stated in 04; the fixed vector reproduces under three implementations; VER-001 rebuilt the compiler from `961601a` and obtained both whole-program keys byte-identical to the current values, and this review confirms the same keys from the current build and the unchanged-preimage property from the diff itself; the receipt records `compilationEnvironment` and `authorityExpiresAt`; relocation changes only the whole-program key and the `relocation` transcript records the explicit re-equip.
3. **v2 equip, atomic pin, no pre-equip authority — met.** `initialState()` has `authority: null`; the sole host factory mints exactly the three-key v2 payload and rejects a legacy draft; absence of `payloadVersion` is the only legacy discriminator; a valid equip recompiles first and stores graph, pin, environment, equip reference, and authority together; a stale pin refuses canonically with nothing scheduled or authorized; rewriting graph and pin together is accepted and declared outside the assurance claim in code, tests, docs, and the audit limitations.
4. **One comparison helper, typed refusals, deterministic replay — met.** Two runtime compile sites; every event in the `consumesProgram` list, including stored-authority and continuation consumers and the recovery request, passes through `withEquippedArtifact`; each drift axis and a compile diagnostic produce schema-valid durable refusals with a trace instead of throwing; successful consuming traces carry the four `artifactIdentity.*` inputs; two live runs, replay, and the audit projection are identical.
5. **Forward-only enforcement and untouched history — met.** The legacy fixture is genuinely pre-WO-029 (its equip payload has no `payloadVersion`, and it holds no boundary and no refusal); it replays with identity `null` and `unavailable` rows; the host appends exactly one boundary before new input; a legacy-derived state cannot consume forward input or accept a legacy equip past it; the duplicate is idempotent; an explicit v2 re-equip restores a pinned state; the frozen oracle is unmodified and the scenario test carries the new fields as an explicit filtered delta.
6. **Explicit unknown-schedule evidence — met.** After cutover an unknown id records `UnknownScheduleRefused` with valid authority retained; the pre-boundary, unpinned in-memory observation is retained deliberately; the reused-id limitation is stated in a test, in 03, and in the audit limitations.
7. **Audit projections — met.** L0 and governed raw carry the `deterministic-content-equality-only` projection with `matched`, `unverified`, `unavailable`, and `refused` rows and canonical links; a malformed refusal is rejected; the limitations deny cryptographic integrity, collision resistance, and authenticity, keep outer confinement separate, and add the rewritten-pair and unstamped-pulse limits; governed raw's `unavailable` list names cryptographic integrity hashes and authenticity explicitly.
8. **Gate, hygiene, purity, versions — met.** `npm test` green twice; `git diff --check` clean; compiler and kernel pure; no dependency change; versions match the activation-time decision and `check-surfaces` confirms it.

## Evidence gate

| Gate item                                                                                                                                                                                                | Where re-established by this review                                                                                                                                                              |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| exact identity fixtures and independent-FNV comparison                                                                                                                                                   | the compiler suite in both runs; this review's Python re-derivation of all 15 definition hashes and both program keys                                                                            |
| before/after semantic-hash inventory                                                                                                                                                                     | `semantic-hash-inventory.json` byte-checked by the evidence helper in both runs; VER-001's base rebuild; the unchanged-preimage diff argument above                                              |
| stale-pin, diagnostic, version-drift, component-drift, pre-equip, legacy-before-boundary, legacy-after-boundary, duplicate-boundary, factory-shape, unknown-schedule, and rewritten-pair negative transcripts | the 14 rows of `negative-transcripts.json` byte-checked in both runs; each behavior reproduced by this review's probes; the duplicate-boundary property proven by test and probe (adjudication 2) |
| canonical log and audit receipt                                                                                                                                                                          | `scenario.jsonl` and `audit.json` equal to live `runScenario` and `projectAuditEvents` output in this review's probe                                                                              |
| unchanged frozen-oracle digest plus the explicit post-WO-029 delta                                                                                                                                       | `shasum -a 256` reproduced; the scenario test's filtered delta read and run in both suites                                                                                                       |
| full test output                                                                                                                                                                                         | `docs/evidence/WO-029/test.log` (executor), VER-001's run, and this review's two runs                                                                                                            |

## Write-back duty

All duties are discharged and consistent with the code as read and the behavior observed.

- **02 §Artifact identity v1** lists both interfaces exactly as `types.ts` declares them, names the v2 payload, the four trace inputs, the boundary payload, the only-absence discriminator this review tested, and the personal-profile framing.
- **03 §Pinned artifact identity** is promoted from candidate to shipped, names the single helper, the recovery decision, the no-authority-before-equip rule, the explicit-link association with its adjacency fallback, the path-dependence of whole-program identity, the unknown-schedule limit, and the independence of beacon-sweep authority; the deferred assurances paragraph now says WO-029 implements the bounded slice and does not claim them.
- **04 §Editable-view v1 normalization and semantic hash** pins the scheme, domain, exact preimage, fixed vector, and both fixture keys, and states that the semantic bytes are unchanged.
- **06** records the dated activation completion at `v0.9.0` without touching the retiming table; **09** adds the `explicit-event-link` label with its precedence and the `Deterministic artifact receipts` section, stating what the datum is and is not; **10** adds the identity schema, definition-hash scheme, and payload-version axes and denies any manifest-schema change.
- **Package READMEs** document the identity record, the preimage, the host factory, the boundary, the refusal family, the changed default timeline, and the unchanged Beacon v1 counter; the skeleton README flips the 0.6.0 and 0.7.0 lines from staged to shipped and adds the staged 0.8.0 line.
- **Map, publication, ledger, README.** The map's dated paragraph and catalog row, the publication index rows (`Artifact identity v1` and `Deterministic artifact receipts` added, `Pinned artifact identity` renamed and raised to `implemented`) with both edition locks `CURRENT`, the ledger's dated section extending the two adopted decisions without rewriting them, and the README release block at `v0.9.0`.

## Ideation receipt

This order carries no breakout of its own. Its nomination provenance is the operator's 2026-09-04 WO-023 ideation breakout, whose receipt lives in WO-023's work order and in the ledger section `WO-023 ideation breakout — pinned artifact identity (2026-09-04)`, both merged on `main` and already verified and reviewed under WO-023; the work order's header records the Shape-First synthesis and the passed clean-room screen. This review did not read intake; the three intake directories in this worktree hold only `.gitkeep`, and nothing in the subject links to or quotes them. The executor's discovery append (`codex-cli 0.153.4`, persisted selector `max`) is an observation under the model-specific notes' append-only rule, not ideation.

## Verification-sequence soundness

One report, no repair episode. VER-001 named its instruments and compensating controls, rebuilt the real base rather than trusting the executor's baseline, re-derived every hash through a second oracle and its own serializer, and drove the subject with its own probes; every number it quotes reproduced here or in the suite. Its disclosures cover the index refresh it performed, the weakening check it applied to the changed tests (renumberings, one association label strengthened, an exact-oracle equality replaced by the filtered delta that criterion 5 requires), the type-library noise in its scratch base build, and its own corrected probe inputs. Its report is byte-identical to the blob its completion checkpoint captured. The sequence is sound and complete.

## Adjudications

### 1. The adjacency association fallback is retained but exercised by no fixture (VER-001 O1)

Non-blocking. Every log this repository now produces takes the `explicit-event-link` path because the host interleaves a `DecisionRecorded` between the attempt and the refusal; the verifier confirmed the historical branch still works when its precondition holds. The label, its precedence, and the fallback are documented in 03, 09, and the skeleton README. Candidate for the next audit order: a synthetic historical-shape fixture that keeps the adjacency branch covered. Not changed here because it is test code.

### 2. `duplicate-boundary` is named in the evidence gate but is not a transcript row (VER-001 O2)

Non-blocking. The property is proven by the skeleton test that asserts state equality under a replayed duplicate and exactly one boundary in the log, reproduced by this review's probe, and the evidence package does carry the datum under another name: the `legacy-before-boundary` row records `canonicalBoundaryCount: 1` after `ensureIdentityEnforcement` was called twice. Evidence packaging, not a functional gap.

### 3. The `compilation-environment` drift axis cannot fire at the equip boundary (VER-001 O3)

Non-blocking and confirmed by reading: `equipDecision` compiles with the pin's own declared `compilationEnvironment`, so that field necessarily agrees at equip; it is reachable for a stored pin or environment mutated after equip, which is exactly what the `relocation` transcript and the skeleton's relocation test exercise. Consistent with the declared rewritten-pair limit.

### 4. A refusal reason names the enforcement boundary in one raw-replay case where none exists (this review)

Non-blocking. Any non-`UnknownScheduleRefused` refusal sets `artifactIdentityBlocked`, and `equipDecision` refuses a subsequent legacy-shaped equip whenever the boundary exists or that flag is set, always with the reason `legacy equip after enforcement boundary`. In a log driven through the raw `replay` entry point with no boundary, a program-consuming event before any equip followed by a legacy equip therefore yields the right type (`ArtifactIdentityUnavailable`) with an imprecise reason. The host driver always appends the boundary before the first input, so no log this host produces reaches the case. Wording candidate for a later patch.

### 5. `CompileResult`'s success shape gains a required field (this review)

Non-blocking. Every in-repo consumer reads the field additively and both compiler entry points populate it; only a third-party constructor of `CompileResult` literals would need to change. The compiler's `0.2.0` → `0.3.0` minor and the release notes' read-before-upgrading item cover it.

### 6. The Entropy Reducer's pre-change whole-program key has no record on `main` outside this order (this review)

Non-blocking. `git grep` finds `c5ddbca75f1c4cee` nowhere on `main`; 04 previously pinned only Seiri's key. The unchanged-preimage claim for that fixture rests on VER-001's rebuild of the published base, on the executor's baseline captured from the same commit, and on the diff argument that no program-construction path changed; this order's 04 write-back now pins both keys, so the next change has a merged record to compare against.

### 7. The audit projection rejects malformed receipts with plain errors (this review)

Non-blocking. `projectArtifactIdentities` throws `invalid audit source … malformed artifact refusal` and `… missing scoped reference` directly rather than through the audit module's `invalidAuditSource` helper; the message shape and the rejection behavior match, and this review observed the rejection. Cosmetic.

### 8. Publication runs into the sandbox's `gh` boundary (this review)

Operational, not a defect of the subject. In this sandbox `gh` cannot read its configuration (`open ~/.config/gh/config.yml: operation not permitted`), while `git ls-remote` succeeds with a credential-helper `failed to store` warning. The publish helper preflights `gh` after the surface checks and the body validation and before any push (`ensureGh` precedes the push inside `worktree.mjs`), so a refusal at that step leaves the branch unpushed; the exact command is in §Publication package for the operator to run outside the sandbox, as the WO-021 package already recorded.

### 9. The map's catalog row and the skeleton README's staged line are dated statements (this review)

Non-blocking. The catalog row says the order is active and to verify it; the README says component `0.8.0` is staged for `v0.9.0`. Both are true of this source until merge and release close, the generated index owns lifecycle status, and this order itself flipped the earlier staged lines to shipped, so the pattern for the next order is established. Following the WO-021 precedent, one dated sentence was added to the map's paragraph and the row was left as is.

### 10. The verifier's disclosures (VER-001 D1–D4)

D1, the dispatch staling the index: proper, and the same happened here at dispatch and will happen again after completion; the guide's refresh points handle it. D2, the weakening check on changed tests: re-read here; the audit renumberings preserve assertion structure, the association change is a strengthening, and the scenario test's filtered delta is the shape criterion 5 asks for against an oracle whose digest reproduces. D3, type-library errors in the scratch base build: confined to the verifier's scratch tree; the two pure functions it needed do not depend on ambient typings. D4, corrected probe inputs: harness errors recorded so the transcript is not misread as subject failures; the audit module's rejection of a blank `eventId` was correct behavior.

### 11. Smaller consequences recorded without action

The `.prettierignore` additions name three generated files whose bytes the evidence helper owns; `scenario.jsonl` and `test.log` are unknown-extension files Prettier already skips. `LiveReactorDriver` is now exported for the evidence helper and tests, which keeps WO-016's single-reactor invariant because the driver still replays through `seiriReactor`. `ScenarioResult.workOrder` may be `null` for a refused or incomplete replay, and the changed tests assert presence where a WorkOrder is expected. The `beacon-fs` CLI test now pins the new timeline fixture and the WO-020 fixture remains an unchanged historical artifact. The evidence README's reproduction sequence needs `npm run build` before `evidence:artifact -- --check` because the helper imports from `dist`; `npm test` satisfies that ordering by building first.

### Candidates considered and not sustained

- **Whether removing the module's default pre-equip authority is a regression.** No: criterion 3 requires no pre-equip authority, the CLI equips first, and the new fixture pins the new timeline.
- **Whether replacing the exact frozen-oracle equality with a filtered delta weakens evidence.** No: criterion 5 mandates the explicit delta, the oracle file is byte-unchanged with its digest reproduced, and every non-added decision still equals the oracle after the pin inputs are filtered.
- **Whether the missing `duplicate-boundary` transcript row fails the evidence gate.** No: the property is proven executable and the boundary count is recorded in the package (adjudication 2).
- **Whether the required `artifactIdentity` field on `CompileResult` should fail the review.** No: additive for every consumer, disclosed in the notes, covered by the compiler minor (adjudication 5).
- **Whether the publication rows for the new 02 and 09 sections should enter below `implemented`.** No: they describe shipped, verified behavior, matching how WO-020's and WO-021's rows entered.

## Corrections applied by this review

One, bounded and non-substantive, following the WO-030 and WO-021 precedent: the planning map's dated `WO-029 implementation (2026-09-05)` paragraph gained a sentence recording that WO-029 passed independent verification and final review at this revision and awaits operator merge, with the recommended sequence and the deferred items unchanged. `prettier --check` passes on the file and the second `npm test` run includes it. No code, test, contract, acceptance, schema, authority, or captured-evidence byte was changed.

## Deviations and open questions

- **No session trailer on the commit or PR body.** This session's harness supplied an attribution reminder asking that commit messages and PR descriptions end with a session link. The operator's standing instruction forbids AI attribution on commits and pull requests, the execution guide keeps commit messages plain, no commit in this repository's history carries such a trailer, and the repository is public; the reviewed-state commit and the PR body therefore carry none. The operator can amend the commit before merge if a trailer is wanted.
- **Claude Code attestations remain `self-reported`** because no effective-effort readback is recorded for the harness (WO-023 FINAL-001 adjudication 9 remains open).
- **No real out-of-process worker exists yet.** The receipt was proven only against the in-process fake adapters; WO-009 is the first consumer and is not authorized to start by this review.
- **Signatures, membership proofs, per-pulse submissions, pulse issuance stamps, worker-result echo, verifier-artifact pinning, and protection against a writer who replaces both graph and pin** are outside this order's boundary, as the work order, 03, 09, VER-001, and the audit limitations state.

## Publication package

- **PR title:** `:sparkles: WO-029: Pinned artifact identity; every successful compile returns a versioned identity record beside the program, the personal host pins it at a v2 equip, recomputes it before every program-consuming decision, turns drift and compile diagnostics into typed inert refusals, replays historical logs behind a logged enforcement boundary, and projects the receipt in L0 and governed raw; v0.9.0`
- **PR body:** `docs/final-reviews/WO-029/PR.md`, committed with the reviewed state, one physical line per paragraph and list item.
- **Release notes:** `docs/final-reviews/WO-029/RELEASE-NOTES.md`, the five-section reviewed edition for `v0.9.0`, validated by `parseReleaseNotes` and the body profile.
- **Publish:** after `final-review-result pass` and the reviewed-state commit, `npm run worktree -- publish WO-029 --title '<the title above>' --body-file docs/final-reviews/WO-029/PR.md`. If the `gh` preflight refuses inside the sandbox (adjudication 8), the operator runs the same command from this worktree outside it; the helper pushes only `wo-029` and never merges.
- **Release close:** after merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints with the main checkout as the working directory; because `v0.9.0` is strictly above `v0.8.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.9.0` tag, and creates the matching GitHub Release. The subject worktree's populated `.control-beacons/` is disposable at its anchored root and will not block removal.

## Readiness

The branch is ready for operator review: the subject is the tree VER-001 passed, every criterion and gate item was re-established first-hand, the actor inventory satisfies the declaration, the suite is green on the verified tree and again on the reviewed tree, the surfaces pass, and the package is complete. After `final-review-result pass` the projection should show phase `closed`, `FINAL-001` pass, the reviewer's attestation as latest with no new drift pair, and legal actions `release-close, next, activate`; the index refresh that follows will mark WO-029 `final-reviewed`.
