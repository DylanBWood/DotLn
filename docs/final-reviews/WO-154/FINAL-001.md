# WO-154 FINAL-001 — final review

**Verdict:** pass. A schema 2 feedback edition commits its verifier stream with every judged body replaced by a Git blob reference that `feedback-evidence --check` rebuilds byte for byte, records a behavioral identity over the judged files with release labels normalized beside a pins record, keeps or deterministically carries its live audit across a pins-only change, refuses a judged change under every bypass the regressions try, and leaves every earlier edition and receipt byte-identical. The repair of VER-001-F1 makes a recorded compiler release metadata at every replay layer, so a rebuilt compiler bump owes the deterministic `--carry` and never a live episode; VER-002 reproduced the rebuilt-label case and passed criteria 1 to 8. `main` moved by WO-153 (`v0.45.1`) during the order, so this review integrated it, resolved five authored conflicts without touching a source line, re-minted the authority edition deterministically over the integrated sources (the route [D004](../../evidence/WO-154/decisions.md#wo-154-d004--the-authority-edition-is-not-re-minted-it-is-not-stale) and WO-153 D007 both named), and passed the product gate on the staged integrated tree. No acceptance defect was found; the one register row the repair discharged but left open is disposed here ([D016](../../evidence/WO-154/decisions.md#wo-154-d016)).

**Subject:** [`docs/work-orders/WO-154-evidence-editions-by-reference.md`](../../work-orders/WO-154-evidence-editions-by-reference.md) on branch `wo-154`, uncommitted at dispatch, judged at checkpoint `refs/dotln/checkpoint/WO-154/9` (`55bfac61`), the tree VER-002 passed plus the two projections the final-review dispatch regenerated, then integrated with `main` at checkpoint `refs/dotln/checkpoint/WO-154/10` (`126a501d`). Original base `75743fd64031e83bb13c3873411fd5ca56cda1c1` (`v0.45.0`); fetched `main` `24aa7237a1dc50ee52ddbbffa0d30642ce1327a8` (`v0.45.1`, WO-153), which `git ls-remote origin refs/heads/main` returned on 2026-09-24. The judged tree is the working tree with every WO-154 file staged; the reviewer gate ran at tree `d3a32287a8a79c02e2d6d2e04cbd096c47f6da37`, code identity `f0bf5fb339fd685a85f42b8f711fcf13de78eca78c3df536d61b4a9cbbf08562`. Nothing was committed, pushed or published before the result transition.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.281","model":"claude-fable-5-1","effort":"xhigh","source":"claude-session-readback"}

Human actor: the operator dispatched `resume: final review` in a Claude Code session and made no choice during the review; the session ran autonomously under the reviewer's authority (commit the reviewed state, push the WO branch, open its PR; never merge, publish a release or push `main`). The harness version is `claude --version`. The model is the session's selected model. Effort `xhigh` is the value the host exports as `CLAUDE_EFFORT`, read by this session, which a claude-code attestation must carry from `claude-session-readback` (selected, not effective). The order asks for `reviewer any`. No subagent was planned or spawned: the subject diff is 53 source and document files that one reader can hold, so the review read every diff hunk itself, 0 of the cap of 20 with no descendants. The one live self-host episode this review consumed as evidence is the repair's; this session ran no provider episode.

**Process cost:** entry 86392 tokens; handoff 8280313 tokens; source claude-transcript-message-usage

Both readings are `dispatch` scope from `node scripts/harness.mjs usage 862d0d7e-b2e6-4195-97ec-7fc27c6b9e47`, observed at 2026-09-24T18:23:29.189Z and 2026-09-24T18:41:59.962Z. Reasoning tokens and dollar cost are unavailable from this counter, which means unknown, not zero. The largest single wait was the reviewer's product gate (293,334 ms). Final counters remain in ignored receipts and the handoff response.

## Goal-aligned judgment

WO-154 is enabling work on the route every later order takes: each close re-mints at least one edition, and each feedback edition used to add about 2.1 MB of copied source bodies and, when a registered source the verifier never judged changed beside a label-only judged subject, a live episode of two to five minutes and about USD 1.5 to 3.3. *Rule beating* was the review's main lens, because a green check is exactly what a wrong behavioral boundary would produce: the review read the identity, the drift comparison, the resolver and every refusal path at source, confirmed that `evidenceSourceContent` normalizes only component release labels so an external dependency pin in the lockfile is behavior (the skeleton test asserts it), and confirmed that the D010 case is refused three times over (behavior identity, forged identity, and the live audit's own per-file comparison with no identity layer). *Fixes that fail* is where VER-001 found the order short: a label-normalized comparison in one layer was undone by the replay layers beneath it; the repair moves the recorded release to the record at each layer instead of relaxing any of them, and the regression now rebuilds the compiler in a copy with its own workspace links, which is the condition the first fixture lacked. *Drift to low performance*: the standard is unchanged, a judged change or a changed regenerated report still owes a live episode; what changed is that a label no longer counts as behavior, and the recorded program is still compared with the current lowering apart from that label, which closes the hole a bare `policyHash` exemption would open. *Tragedy of the commons* and *shifting the burden*: 92 % of each edition's bytes and the label-only share of live episodes are removed from a shared cost every clone and every executor paid; the planner no longer prices a live episode into a component bump. *Escalation*: no new gate; the one new `--check` refusal (a compiler release moved the policy hash) names a deterministic command and replaces a silent console degradation. *Success to the successful*: schema 1 editions keep their exact path; the declined storage alternatives are recorded with reasons in D001 and the order. *Seeking the wrong goal*: the check still judges the audit, not the size; the rebuilt-compiler regression tests the stored-stream replay and the console, not only the checker's exit code. *Policy resistance*: the admission of a recorded compiler release is the compatibility decision WO-050 and WO-133 reserved; D014 and D015 reopen those decisions with the operator's confirmation rather than around them, and the WO-050 byte oracle keeps its bytes behind a hash-chained successor. *Naive Interventionism* argued against fixing anything at review: the one thing this review changed beyond integration bookkeeping is a register disposition. *NoOp* at review would publish an order whose integrated tree fails the authority check and whose register still shows the repaired finding open.

## Authority: the order text against its activated text

`git diff refs/dotln/checkpoint/WO-154/1 -- docs/work-orders/WO-154-evidence-editions-by-reference.md` shows two changes: the H1's `(version assigned at activation)` became `(v0.46.0)` ([D005](../../evidence/WO-154/decisions.md#wo-154-d005--release-assignment-v0460-skeleton-0390)), and the section **Operator authorization — 2026-09-24** was appended with the repair summary, the folded follow-up and criterion 8. [D011](../../evidence/WO-154/decisions.md#wo-154-d011--ver-001-f1-repair-a-compiler-release-is-recorded-metadata-at-every-replay-layer) records the operator's authorization during `resume: fix` of one more live feedback self-host episode and of folding in `FUP-0c86ada82f559914`, and `docs/control/plan-refutations.jsonl` holds its `PlanExecutionAmended` binding (receipt 027, decision D011, 2026-09-24T17:27:41Z, order hash `sha256:53601446…`). `npm run plan -- check` exits 0 on the integrated tree. Every original criterion, non-goal, dependency and operator-review assumption is unchanged. The order's own "one live episode" assumption was spent by D008; the second episode is the operator's, recorded in D011 and D013.

## The verification sequence

[VER-001](../../verifications/WO-154/VER-001.md) (Codex CLI 0.156.1, `gpt-6-astra`, `xhigh`, no subagents) passed criteria 1 and 3 to 7 on an independent fresh-fetch fixture (52 subject and 49 baseline references, the 2,177,908-byte stream rebuilt exactly, 328 historical files unchanged, 177,857 bytes against 2,231,901 by value) and failed criterion 2 with F1: after a compiler release-label change is *built*, `--check` and `--carry` refuse with `compiled policy drift`, because every replay layer recompiled the recorded program at the current label; the executor's fixture had missed it because its copy's symlinked `node_modules` resolved `@dotln/compiler` to the source checkout. The repair ([D011](../../evidence/WO-154/decisions.md#wo-154-d011--ver-001-f1-repair-a-compiler-release-is-recorded-metadata-at-every-replay-layer) to [D015](../../evidence/WO-154/decisions.md#wo-154-d015--wo-133-d004s-rejected-option-is-now-the-contract)) compares a recorded program or capsule with this compiler's lowering at the release it records (`isCompilerRelease` guards the label's shape), lets the verification slice adopt a persisted capsule that differs from the dispatched one only in that label, has `--check` name the deterministic carry when a compiler release moved the policy hash the console binds, folds in the pins-snapshot resolver, bumps `@dotln/compiler` to 0.18.0, and re-mints feedback as revision 002 with the authorized second live episode plus the other three editions deterministically. It also found more than the report named: the gate's stored-stream replay and five console tests failed on the same path, and the WO-050 byte oracle's four frozen refusals now replay to completion, recorded as a hash-chained successor manifest ([D014](../../evidence/WO-154/decisions.md#wo-154-d014--wo-154-is-the-compatibility-scope-wo-050-d002-reserved)). [VER-002](../../verifications/WO-154/VER-002.md) (Codex CLI 0.156.1, `gpt-6-sol`, `xhigh`, no subagents) reproduced the rebuilt-label case end to end in the new workspace-linked copy, replayed the carried streams (7 of 7), re-established the byte-identity, size and reference claims on revision 002 (189,014 bytes against 2,250,286 by value; the 2,196,293-byte stream rebuilt exactly), and passed criteria 1 to 8 with integration reserved for this review. Implementation and repair ran on Claude Code 2.1.281 / `claude-opus-5-5[1m]` at `xhigh`; the two verifications ran on Codex CLI with session readback on two different models; this review ran on Claude Code 2.1.281 / `claude-fable-5-1` at `xhigh`. Verification was independent of implementation in harness and model, and the review used a third model.

## Integration with `main`

`main` gained WO-153 (`v0.45.1`: a Codex dispatch whose session entry fails now says why; skeleton `0.38.1`; re-minted WO-153 authority and feedback editions; 42 files). `npm run worktree -- integrate WO-154 --intake-backup` naming the session scratch archive `DotLn-wo154-intake-20260924T182922Z.zip` checkpointed the work (`refs/dotln/checkpoint/WO-154/10`), retained the include-untracked stash `90cc6d5b` (`WO-154 integrate 2026-09-24`), fast-forwarded the uncommitted branch to `24aa7237` and re-applied the stash. Twenty generated projections were re-merged by the helper (the harness manifest and fourteen hooks, `README.md`, the control projection, the decisions index, the follow-up register by id union, one publication lock, the work-order index). Five authored conflicts were resolved by this review, none in a source file: `docs/evidence/current.json` (WO-154's four editions over WO-153's two), `packages/skeleton/package.json`, `packages/console/package.json` and both lockfile hunks (skeleton `0.39.0` over `0.38.1`: the minor bump declared at verification remains the next minor above what upstream consumed, so no retime; compiler `0.18.0` and console `0.1.11` were untouched by `main`), and `docs/product/06-roadmap.md` (both dated activation paragraphs kept, WO-154's first). `--continue` rebuilt the runtime, re-emitted the harness bundle (31 surfaces), refreshed the control projection, index, meta and publication locks, regenerated the selected console fixtures, and wrote the draft D016; `release prepare --local` reported `WO-154 target v0.46.0 remains current; no files changed`.

Evidence editions on the integrated tree: `feedback-evidence --check` prints `Live feedback audit docs/evidence/WO-154/feedback-002 judged the current source`, which is [D012](../../evidence/WO-154/decisions.md#wo-154-d012--the-merged-wo-153-is-integrated-at-final-review)'s prediction made exact: the only judged files `main` touched were the two label-bearing manifests, and the resolution kept the branch's bytes, so nothing moved relative to the live audit. Artifact identity and verification pass unchanged. Authority failed (`stale WO-154 revision 001 evidence: bundle-diff.json`): the hooks the edition compares were re-emitted after `main`'s `harness-host.ts` change, a registered authority source. That is the reopening condition D004 wrote for "the integrated tree at final review" and the outcome WO-153 D007 named ("the later branch re-mints over the integrated sources"), so this review re-minted authority deterministically as `docs/evidence/WO-154/authority/002` and selected it: `authority.json` is byte-identical to revision 001, and `bundle-diff.json` differs only in the fourteen regenerated hook hashes and the revision label; `--check` passes. No live episode was run. Carried-forward claims: every acceptance claim VER-002 judged is carried with its original evidence, because the integration changed no source file of this order; the staged diff of tracked code against `main` is the same 53 files, 2,326 insertions and 577 deletions the order had against its original base. [D016](../../evidence/WO-154/decisions.md#wo-154-d016) records the bases, resolutions, re-mint and this assessment.

## Criteria 1 to 8

Each row names what this review read or ran on the current, integrated bytes; VER-002's judgment is carried where the review found nothing to add.

| Criterion | Judgment | Evidence at this subject |
| --- | --- | --- |
| 1 — blob references and resolution | Pass | Revision 002's opening and persisted capsule carry `{path, blobHash, bytes}` entries (the two synthesized `.feedback-source/` projections add `derivedFrom`); `editionBodies.resolve` hashes working-tree candidates in process and sends only misses to one `git cat-file --batch`, rejects a body whose length or rebuilt hash differs, and `resolveEditionLog` names every unresolvable path in one `EditionReferenceError` (reviewer's read of `feedback-selfhost.ts`). The skeleton fixture proves not-yet-committed, committed-then-moved, derived, candidate-report and missing-path resolution; the CLI fixture forges a reference and gets `stale: unresolvable edition reference: packages/skeleton/src/worker-store.ts`. `--check` passes on the integrated tree. |
| 2 — behavior key, pins record, live-episode rule | Pass | `feedbackEditionIdentities` hashes every judged file with labels normalized (`behavior`) and the raw bytes of each label-bearing file (`pins`); `checkFeedbackEdition` binds both identities to the resolved opening, compares the live report with the regenerated one apart from `subject` and `policyHash`, and admits labels in the per-file comparison only when the pins moved; `--carry` writes only `edition.json` and `feedback.json` naming `carriedFrom`. The CLI fixture moves skeleton and console labels (`Retained …`), carries into `WO-999` (`no live episode`), and the rebuilt-compiler fixture does the same after a real build. D001 enumerates the 51 judged files plus the report and why a non-judged registered source is covered through the report. |
| 3 — D010 refusal under bypass | Pass | The skeleton case asserts D010's field-by-field comparison mechanically (only `subject` differs), then refuses the `worker-store.ts` change through the behavior identity, through a forged identity (`does not bind its live audit`), and with no identity layer at all through `validateSelfhostEdition` in both label modes; the schema 1 edition WO-157/002 refuses the same change with only its shape assertion differing. |
| 4 — history byte-identical, schema, console | Pass, integrated tree | `git diff --name-only HEAD -- docs/evidence docs/planning/refutations` shows only `current.json`; the only untracked evidence is under `docs/evidence/WO-154/`; VER-001 compared 328 historical files by blob. `FEEDBACK_EDITION_SCHEMA = 2`; schema 1 keeps `validateSelfhost`'s exact path in the script. The console manifest pins `WO-154/feedback-002` and reads through `feedbackEditionLog`; the WO-032 board case and fixture loader rebuild the pinned stream with `editionStream`; `console-fixtures --write` regenerated the selected expected outputs at integration. |
| 5 — bytes before and after, rule change | Pass | D009 and D013 record 177,857 and 189,014 bytes against 2,231,901 and 2,250,286 by value with the commands; VER-001 and VER-002 measured the same independently; product 03 §Corpus policy carries the after figure and the corrected premise (16 of 81 historical re-mints were label-only on the judged files, not "most"). Both revisions' `pins/` snapshots hash to their names (the CLI fixture's assertion). |
| 6 — receipt subject or split | Pass via the split | D003 records the separate seam (40 by-value reads, a receipt digest that covers the subject, the ER2-004 interaction) and mints `FUP-beb13d8d099d2917`; no receipt, `plan-receipts.mjs` or `plan-subject.mjs` byte changed; `npm run plan -- check` exits 0. |
| 7 — re-mint, gate, whitespace, dependencies, step count | Pass, integrated tree | `docs/evidence/current.json` selects authority `WO-154/002` (this review, deterministic), artifact identity `WO-154/001`, verification `WO-154/001`, feedback `WO-154/002` (the repair's one live `claude-cli-print` episode on `claude-sonnet-5` at `xhigh`, first attempt, USD 1.60); the four checks pass. The reviewer's gate below passes; `git diff --check` is clean; the lockfile changes three component versions and their workspace pins only; the product gate stays at 27 suites with the new cases riding `skeleton` and `evidence-sources` (the implementation and repair receipts report the per-suite cost). |
| 8 — rebuilt compiler release | Pass | `assertCompiledFeedback` and `assertVerificationTask` lower at the recorded release and refuse every other difference (compiler tests: a re-hashed earlier release passes; a foreign hash, a changed mechanism or subject, and `""`, `latest`, `0.17`, `17`, `null` labels fail). `persistedCompilation` adopts a persisted capsule only when the label differs, the rest of the command is identical and the capsule is its own release's lowering, and rebuilds the continuation from it; the skeleton test refuses a changed subject and an unhashed label and shows a well-formed foreign label being adopted and then failing to bind the recorded attempt. The CLI fixture builds the next compiler patch in a workspace-linked copy, sees `--check` name the carry, carries, re-checks, replays 7 of 7 stored streams and refuses a judged change. `liveAuditCandidates` supplies `pins/`, and `--record-selfhost` writes no Git object (no `hash-object -w` remains in the recorder). Historical 0.5.0 and 0.6.0 streams replay to complete in the skeleton suite. |

## Refutation 027's known issues for WO-154

The ideation clause of the dispatch resolves to [planning refutation 027](../../planning/refutations/2026-09-22-planning-12a29b8b07c6265d-027.md) (verdict aligned-with-findings for WO-154, three known issues, no hold), the 2026-09-22 planning ledger entry that filed WO-154 from REVIEW-002's ER2-001 and ER2-002, and D011's amendment binding. Each known issue's reopening observation was checked against the landed bytes:

- Criterion 2 (a lockfile change that alters a resolved dependency is behavior, yet the order calls the file "pins"): not met. `judgedBehavior` normalizes only component release labels; the skeleton test changes `typescript`'s pin in the lockfile projection and asserts the behavioral identity moves and `judgedDrift` names the lockfile. D001's reopening condition carries the boundary.
- Criterion 1 (an unresolvable reference is a refusal, so a shallow or blob-filtered clone stales every edition): accepted, not met on this subject. Operator-review assumption 1 makes the repository with its objects the evidence store; the implementation receipt's limits name reachability; every judged body lands in the same commit as the edition and the pins snapshot covers the bodies no commit would otherwise hold. D001's first reopening condition (a consumer without the Git objects) carries it.
- Criterion 6 (the receipt half is a second mechanism with an "or split" escape and no itemised balance): met as the recorded outcome. D003 takes the split and names the reason and the ER2-004 interaction; `FUP-beb13d8d099d2917` is the planner's row.

## Findings and bookkeeping by this review

No acceptance defect. One register correction and two readings:

- **Register (D016).** `FUP-ccaad908fd4c4376`, the follow-up D010 minted for the executor to resolve F1, had no disposition although D011 did the work and VER-002 criterion 8 passed on it; the repair disposed the folded `FUP-0c86ada82f559914` but not this one. Disposed at this review as `allocated` to WO-154 with the evidence and a reopening condition; no source or decision text changed.
- **Reading, no change.** `validateSelfhostEdition` in label-admitting mode iterates the current source files, so a judged path that leaves `FEEDBACK_SOURCE_PATHS` is not named by that function; a schema 2 check catches it earlier through the behavior identity's path list, and schema 1 checks never run in that mode. Consistent with D001; recorded so a later reader does not mistake it for a gap.
- **Reading, no change.** D007's decision text still describes `git hash-object -w`; its dated correction and D011 supersede it, and no object write remains in the recorder. Product 07's edition duty paragraph carries the new rule with WO-154 D001 and D011 as sources.

## Checks run by the reviewer

- `npm test -- --review` on the staged, integrated tree, outside the harness sandbox: **34 suites, 0 failed, 293.33 s, 78 fresh tasks, exit 0, tree `d3a32287a8a79c02e2d6d2e04cbd096c47f6da37`, code identity `f0bf5fb339fd685a85f42b8f711fcf13de78eca78c3df536d61b4a9cbbf08562`, recorded 2026-09-24T18:40:46.605Z with the sandbox not in force; no `dotln-gate-sandbox-*` root remained afterwards**.
- `git diff --check` clean before staging and at the result transition.
- `node scripts/feedback-evidence.mjs --check`, `node scripts/artifact-identity-evidence.mjs --check`, `node scripts/verification-evidence.mjs --check`: pass on the integrated tree before the gate. `node scripts/authority-evidence.mjs --check`: stale on the integrated tree; pass after the deterministic revision 002 re-mint.
- `node scripts/harness.mjs check --loadout contributor`: 31 generated surfaces. `node scripts/harness-context.mjs --check`: every set role within its ceiling (reviewer 23,071 of 24,576). `node scripts/check-registrations.mjs`: 327 JSONL, 21 stubs.
- `npm run plan -- check` exit 0; `npm run publication:check` PASS with both editions CURRENT; `npm run release -- check-surfaces --local` 0 FAIL (compiler, skeleton and console bumps each recognised against `v0.45.1`).
- `git ls-remote origin refs/heads/main`: `24aa7237`, equal to the fetched upstream; newest local tag `v0.45.1`.
- `git diff --name-only HEAD -- docs/evidence docs/planning/refutations`: `current.json` only.
- Reads at source: the whole subject diff (53 files), the three new files, both edition records, the WO-154 identity manifest, D001 to D016, both verification reports, the implementation and repair receipts, receipt 027's WO-154 entry and the ledger's filing entry.

## Handoff

The result is `final-review-result pass`. After the transition the reviewer refreshes the index and decisions index, commits the reviewed state as seam commits (the compiler and reactor admission of a recorded release with its tests; the recorder, resolver, identities, carry and the readers with their fixtures; the WO-050 oracle successor; the component bumps, re-minted editions, console re-pin and regenerated surfaces; the record), and runs `npm run worktree -- publish WO-154` with the committed [PR body](PR.md) and the [release notes](RELEASE-NOTES.md) beside this report. The helper's post-merge release-close handoff is returned to the operator verbatim. `FUP-beb13d8d099d2917` (the receipt subject) is the planner's row; the integration stash and checkpoints 1 to 10 are retained.

## Reproduction

From `/Users/dylanwood/Projects/DotLn-wo154` on branch `wo-154`:

```text
npm test -- --review
git diff refs/dotln/checkpoint/WO-154/1 -- docs/work-orders/WO-154-evidence-editions-by-reference.md
git ls-remote origin refs/heads/main
node scripts/feedback-evidence.mjs --check
node scripts/authority-evidence.mjs --check
node scripts/artifact-identity-evidence.mjs --check
node scripts/verification-evidence.mjs --check
node scripts/harness.mjs check --loadout contributor
node scripts/harness-context.mjs --check
node scripts/check-registrations.mjs
npm run plan -- check
npm run publication:check
npm run release -- check-surfaces --local
node --test --test-name-pattern=WO-154 scripts/test-evidence-sources.mjs
```

The rebuilt-compiler case is the second `WO-154` test in `scripts/test-evidence-sources.mjs`; it builds a workspace-linked copy, so it takes about 40 s.
