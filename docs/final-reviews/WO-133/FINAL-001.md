# WO-133 FINAL-001 — the stand-down residue is delivered, and the reviewer's own gate refuses it over one unregistered release case

**Verdict: fail.** One blocking defect, in the order's own criterion 3 deliverable: `scripts/test-release.sh` gains `release_case_runtime_refresh` and nothing adds `runtime_refresh` to the hard-coded case inventory at `scripts/test-release-fixtures.mjs:25`, so `runner-fixtures` fails its first assertion and `npm test -- --review` exits 1 at 29 passed, 1 failed, 828.592 s. Criterion 7 requires a green `npm test`; it is red at the reviewed code identity, so this review cannot pass. Everything else in the order is delivered and holds up: I read the whole diff against the criteria and re-derived the parts that carry weight, and the gate itself ran `release:case:runtime_refresh` to a pass in 21.77 s — the new fixture works, only its registration is missing. The defect is small and its repair is one line, but it is a source change to a declared machinery source and a reviewer may not write it and then certify its gate, so it returns through `resume: fix` and fresh independent verification. Nothing here is integration bookkeeping: `main` never moved, and there is no merge, retiming or carried-forward claim in this report. The one structural point worth the repairer's attention is *why* nobody saw it: `npm test` runs 19 product suites, `runner-fixtures` is machinery, and it was selected in my gate only because `test-runner.mjs`, `test-runner.test.mjs` and `evidence-editions.mjs` changed — `scripts/test-release.sh` is declared by no machinery suite at all, so adding a release case selects nothing that checks release cases. VER-001's F1 is confirmed at source and reproduced live in this session, and it is carried forward as a planner nomination rather than charged here, exactly as the verifier judged.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.273","model":"claude-opus-5[1m]","effort":"xhigh","source":"self-reported"}

Dispatch: `resume: final review`, recorded by the session hook as the `final-review` transition with FINAL-001 allocated at this path at 2026-09-16T05:21:12.239Z from checkpoint `refs/dotln/checkpoint/WO-133/5` (`d6b95d78…`). I did not repeat the recorded dispatch; `npm run resume -- briefing` projected it read-only. The harness version is `claude --version`. The model identifier is the one this session exposes for itself and agrees with the operator's `/model` selection, "Opus 5 (1M context)"; effort `xhigh` has an effective environment readback here, `CLAUDE_EFFORT=xhigh`. Because the model half is the session's own report rather than an operator statement made in this dispatch or an effective readback channel, the source is `self-reported` and not `operator-attested`; no effective model readback is claimed. This session held the worktree's only writer reservation throughout (`writer --show`, actor `2b6a587a…`, owner pid 17773, reserved 2026-09-16T05:21:11.516Z, `alive: true`). No subagent ran; every file named below was read by this session.

**Bypassed requirements: none.**

Instrument disclosure. The subject gates its own review. The hooks that held my writer reservation, refused my Bash calls during the live gate, journaled this session and suppressed its advisories are the regenerated hooks this order ships, running from the pinned snapshot `.runtime/harness/6b1dc0917ec592c3`. Every claim below that depends on that machinery says so. One consequence is load-bearing and appears twice in this report: the advisory suppression this order introduces was active in my own session, which is how I observed VER-001's F1 first-hand rather than only at source.

## Subject and base

The subject is [WO-133](../../work-orders/WO-133-stand-down-residue.md), acceptance criteria 1–7 as filed, its three operator-review assumptions and its recorded non-goals, together with the executor's four decisions D001–D004, of which D002 and D004 are same-day corrections, and the operator-authorized adjacent queue item `adjacent-0001`. The branch is `wo-133`, working tree uncommitted, preserved by final-review checkpoint 5.

**One base, and no integration.** `git rev-parse HEAD`, `main` and the last-fetched `origin/main` are all `030b1d91b74b9f1c9db2dfbdf997c913a0abb010`; `git log main..HEAD` and `git log HEAD..main` are both empty, and there are no branch commits before final review. Upstream did not move during any phase of this order, so none of product 07 §Independent workflows' integration steps applied: no merge, no regeneration for a new base, no follow-up register union, no retiming, no changed whole-tree hash and no carried-forward claim. VER-001 judged the same bytes this gate judged; `gateCodeIdentity` is `2d57d9e9bc1c33128c45f8e83654ddc7c9e4d04534893715385111d7d40eed0c` for the verifier's row, for my row and when recomputed on the current tree, and the reviewed tree hash `e217f969…` matches my gate row.

**Named limit on the base claim.** `git fetch origin main` is refused in this sandbox — `CONNECT tunnel failed, response 403` for `github.com:443` — so the `origin/main` value above is the last fetched ref in this worktree, not a live observation. Nothing in this review turns on it, and publication would have made its own authoritative remote check; that step is not reached on a fail.

Read scope: the order in full and its cited sections — the R1 replan §3.1 to §3.3, WO-132 criteria 7, 9 and 10, product 07 §Discipline (machinery stand-down and write once, run once) and §Independent workflows and integration, product 02 §Harness compiler v1, product 08 §PRs and commits and §Release-note edition; the complete diff of `packages/skeleton/src/harness-host.ts`, `packages/compiler/src/harness.ts`, `packages/compiler/src/artifact-identity.ts`, `packages/skeleton/src/loadouts/contributor.ts`, the new `packages/skeleton/src/version.ts` and `scripts/lib/harness-runtime.mjs`, `scripts/lib/harness.mjs`, `scripts/release.mjs`, `scripts/worktree.mjs`, `scripts/resume.mjs`, `scripts/refute-plan.mjs`, `scripts/test-runner.mjs`, `scripts/reactor-identity.mjs` and the new `scripts/fixtures/historical-compiler-loader.mjs`; every fixture diff — `scripts/test-harness.mjs`, `scripts/test-process-debt.mjs`, `scripts/test-release.sh`, `scripts/test-resume.sh`, `scripts/test-runner.test.mjs`, `packages/skeleton/test/executor-supports.test.ts`, `reactor-slices.test.ts` and `scenario.test.ts`; `packages/skeleton/src/evidence-editions.mjs` for the projection the selection reuses; `scripts/test-release-fixtures.mjs` and `scripts/lib/release-fixtures.mjs` for the failing assertion; every write-back; the generated `.claude/hooks/read-observer.mjs` in full against its compiler source; `packages/compiler/src/operator-control.mjs` for the SessionStart path; VER-001 in full at its current bytes; the executor's `decisions.md` and `validation.md`; the adjacent queue log; and the WO-050 final-review artifacts as the current publication precedent.

## Ideation receipt

The order carries no operator-opened ideation section and `docs/evidence/WO-133/` has no `ideation.md`, so the receipt is the order's own **Nomination provenance** header plus the operator-authorized adjacent item this execution opened. The nomination chain resolves literally in the delivered work. The R1 replan §3.2 diagnoses 344 advisory rows in one session caused by three fast-forwards past a 23:29Z build, and asks for four things — rebuild after a fast-forward that changes the pins, one advisory per session per cause, none from `PostToolUse` observers, and a `SessionStart` line naming the cause; all four are implemented and fixture-covered. §3.3 asks for the operator-attested fallback in role text and a refusal to downgrade a supplied value; the role text carries it in all twelve generated files and `parseActor` records supplied values verbatim while mapping the legacy token. §3.1 measures five of seven reviewer gates paying about 400 s for a version literal and asks for one module outside every machinery source list; `version.ts` exists, holds all three literals, and is declared by no machinery suite. The screenshot capture is ignored intake and was not mined; the order's synthesis is what the delivered work follows.

`adjacent-0001` is the second half of the receipt and it was run correctly. The queue log shows queue, intent announcement, an operator check-in recording "Proceed with fixture repair", start, and completion citing two executed checks — announcement before work, operator steering, evidence on completion, in that order, at `docs/control/local/adjacent-work.jsonl` seq 1–5 between 04:37:25Z and 04:50:34Z. Its scope is the compiler-patch fallout on version-bound test oracles, recorded in D004, and it stays inside the paths it declared.

## Verification sequence

| Report | Verdict | What it established | Limits it named |
| --- | --- | --- | --- |
| VER-001 (Claude Code 2.1.273, `claude-opus-5[1m]`, effort `xhigh`, self-reported) | pass | All seven criteria met with independent reproduction: the twenty-invocation stale-hook fixture, the two surviving refusals including one applied to the verifier's own session, `SessionStart` observed live on the healthy path, the six release scenarios at a 4.661 s worst case against a 120 s ceiling, all twelve generated role files carrying both new lines, the legacy source-token mapping in the canonical event, version-only selection against host-change selection with a grep pin, `harness check` at 24 surfaces, every write-back read as a diff, and `npm test` 19/0 at 251.66 s with `git diff --check` clean. Recorded F1 (the shared `classification` marker) as non-blocking and as-designed, plus O1–O6. | L1 no live stale-runtime observation; L2 criterion 2's stale half is fixture and documentation evidence; L3 criterion 4's post-merge observations open by construction; L4 timings are this host under load; L5 process cost. |

One verification was requested and it passed; there is no repair event and no earlier report, so the complete sequence is VER-001 alone. It changed no implementation, test, fixture or evidence file — its own account of writing only its report, `docs/control/current.md` and the work-order index is consistent with the tree I gated, whose code identity equals the one it measured. Its judgments therefore carry forward whole, with one exception that is not its fault and not a criticism of it: its `npm test` could not have caught the defect below, because plain `npm test` runs the 19 product suites and `runner-fixtures` is machinery, selected only by `--review`.

## The reviewer's gate

`npm test -- --review` on the staged tree: **29 suites passed, 1 failed, 828.592 s, 74 fresh tasks, exit 1**, recorded at 2026-09-16T05:41:43.874Z as `host-gate:2d57d9e9bc1c33128c45f8e83654ddc7c9e4d04534893715385111d7d40eed0c:npm test` with tree `e217f9693b4205b067cb5fcd9eb153bdc821db09` and 30 required suites. The single failure is `runner-fixtures` (FAIL, 32.16 s, 38 of 39 tests passed). Everything else passed, including the suites that carry this order's own claims: `harness-fixtures` 281.17 s, `process-debt` 158.94 s, `resume` 132.65 s, `release` with `release:case:runtime_refresh` at 21.77 s, `skeleton` 132.96 s, `plan-refutation` 167.32 s, and the four evidence suites against the WO-133 editions. `git diff --check` exits 0 in both the working tree and the index, and the gate changed no tracked file: `gateCodeIdentity` recomputed after the run still reads `2d57d9e9…`.

This is the order's own cost prediction meeting reality, and the reviewer's gate is the expensive case rather than the cheap one, correctly: WO-133 changes `harness-host.ts` behaviourally, so `--review` selected `harness-fixtures` and `process-debt` and the gate took 828.59 s. The saving the order promises applies to a later skeleton order whose only change is a version literal, and that remains the unobserved reopening condition. Nothing about this duration is a defect.

## Blocking finding

### F1 — the new release case is not registered in the release-case inventory, so `runner-fixtures` fails and criterion 7 is not met (blocking)

`scripts/test-release.sh` gains `release_case_runtime_refresh()` at line 1066, which is criterion 3's deliverable. `scripts/test-release-fixtures.mjs:23-29` asserts that the case list derived from that script equals a hard-coded expected inventory, and the order does not touch that file — `scripts/test-release-fixtures.mjs` is not in the diff at all. The assertion therefore fails with one extra element:

```
$ node -e 'import("./scripts/lib/release-fixtures.mjs").then(m => …)'
actual length 44 expected length 43
in test-release.sh but not registered: ["runtime_refresh"]
registered but absent from test-release.sh: []
```

I reproduced that directly, outside the gate and without running the suite, so it is deterministic rather than an ordering or load artefact. The gate's own failure output agrees, at `scripts/test-release-fixtures.mjs:23:1`, `ERR_ASSERTION`, with `+ 'runtime_refresh'` inserted between `stale_helpers` and `success` — the position the new function occupies in the script. The fix is to add `runtime_refresh` to the expected string at `scripts/test-release-fixtures.mjs:25`, after `stale_helpers`. That is one line, and it is still not mine to write: `scripts/test-release-fixtures.mjs` is a declared source of `runner-fixtures`, so changing it changes the tree the gate measures, and a reviewer who writes a source fix and then certifies its gate has certified their own work. Product 07 routes an actual acceptance defect through repair and fresh independent verification, which is what this is.

**Why three actors missed it, which matters more than the line itself.** `npm test` runs the 19 product suites; `runner-fixtures` is machinery. The executor ran `npm test` and `node --test scripts/test-runner.test.mjs` — the latter is only part of `runner-fixtures`, and the failing assertion lives in the sibling file `scripts/test-release-fixtures.mjs`. The verifier reproduced those same commands and inherited the same blind spot. `runner-fixtures` reached my gate only because `scripts/test-runner.mjs`, `scripts/test-runner.test.mjs` and `packages/skeleton/src/evidence-editions.mjs` changed, all of which are declared sources of it. **`scripts/test-release.sh` is declared by no machinery suite at all** — I checked every machinery row's `sources` against it and got none. So an order that adds a release case and touches nothing else selects no suite that checks release cases, and the inventory guard fails on whichever later order happens to change an unrelated declared source. Recommendation for the repair, inside this order's existing surface because it already edits `machinerySources`: declare `scripts/test-release.sh` under `runner-fixtures`. I record it as a recommendation, not a requirement, because it widens selection and that is the repairer's and the order's call.

## Finding carried forward from VER-001, confirmed and not charged here

### VER-001 F1 — every non-runtime advisory shares one per-session marker (non-blocking; as designed; planner nomination)

Confirmed at source. `runHarnessHook` initialises `reasonClass` to `config.kind === "permission" ? "authority" : config.kind` (`packages/skeleton/src/harness-host.ts:2863-2864`), and the suppression block at `:2987-2993` keeps that value only when it is one of `pins-differ`, `snapshot-missing` or `runtime-unavailable`, collapsing everything else — `authority`, `unreadable-state`, `finish`, `feedback`, `observe`, `session` — into the single literal `classification`, which becomes the marker key with the session id. The order's Design line names exactly four cause classes and `classification` is one of them, no acceptance criterion requires repeat non-runtime advisories to survive, and the executor disclosed the four classes in the product 07 write-back. So it is delivered as specified and it is not a defect this review charges, which is also how the verifier judged it.

It is worth the planner's attention because **this review session is the demonstration**, and the signal it silenced is the one about completion discipline. At 05:45Z my session's journal held 1,015 rows, 8 of them advisory rows carrying 3 distinct messages, and not one advisory was rendered in my terminal at any point:

```
05:21:00.317Z SessionStart session   (no advisory; pins healthy, returned {})
05:28:29.132Z PreToolUse   permission compiled authority does not permit settings.user   ← claimed the marker
05:28:29.303Z PostToolUse  observe    runtime-unavailable: Path outside the worktree     ← silent by design
05:28:32.213Z PreToolUse   permission compiled authority does not permit settings.user   ← suppressed
05:29:49.335Z PreToolUse   permission compiled authority does not permit settings.user   ← suppressed
05:32:15.439Z Stop         finish     pending no-partial-completion, verify-app-before-done ← suppressed
05:41:47.499Z PreToolUse   permission compiled authority does not permit settings.user   ← suppressed
05:41:47.684Z PostToolUse  observe    runtime-unavailable: Path outside the worktree     ← silent by design
```

Exactly one advisory marker file exists for this session, `1ae43c64…advisory`, created at 05:28:29.133Z by the first line. The action that claimed it was reading a file outside the worktree — an ordinary thing a reviewer does constantly — and it permanently silenced WO-132's `pending no-partial-completion, verify-app-before-done` support advisory for the remaining twenty minutes of a session whose entire job is to decide whether work is complete. That is a larger loss than the 344-row flood the order set out to remove, it is invisible in the release-facing README sentence "Runtime advisories appear once per session and cause", and it is not disclosed in the 07 write-back's list of four causes. **Planner nomination:** key the marker on the advisory's own identity — the support ids or a digest of the message — or exclude non-runtime causes from suppression entirely, since the measured flood is carried wholly by the three runtime causes. Reopening observation: a one-session regression emitting two distinct non-runtime advisories and expecting two visible messages. Until then, the order's own README and 07 sentences would read more truthfully if they said which advisories share the `classification` bucket.

## Acceptance

| Criterion | Review result |
| --- | --- |
| 1: one advisory per stale session naming `pins-differ` and the command, twenty journal rows, silent `PostToolUse` observers, a second session gets its own, both refusals intact | **Met.** Read at source: the suppression sits at the single output boundary in `runHarnessHook` after the `finally` that journals, so every row survives; `showHarnessAdvisory` returns `false` for `PostToolUse` and `protocolRefusal` returns `{}` there; the marker write is `{flag:"wx"}` with an `EEXIST` branch that fails open on a non-regular file, and `showHarnessAdvisory` catches a throwing claim and shows the message, so unwritable state never silences. The fixture asserts all of it including the second session and the unwritable-directory case. Both refusals passed in my gate, and the live-gate refusal fired against my own `sed`, `grep -r` and `$TMPDIR` calls for the duration of the run. |
| 2: `SessionStart` names the cause when pins differ and says nothing about the runtime when they match | **Met, and VER-001's L2 is now half closed by a live observation.** The first row in this session's journal is `{"recordedAt":"2026-09-16T05:21:00.317Z","event":"SessionStart","hookTiming":{"kind":"session","durationMs":120.49}}`, eleven seconds before the dispatch was recorded. Claude Code 2.1.273 does invoke the registered handler at the real `SessionStart` event, it decodes a `SessionStart` payload through the widened `decodeHarnessInput`, and with healthy pins it returned `{}` and dispatched nothing. What remains unobserved is only the rendering of a `SessionStart` `systemMessage`, because the healthy path emits none; the stale half stays fixture evidence, as L2 said. |
| 3: close and finish rebuild only when the fast-forward changed the pins, the manifest cites the rebuilt runtime, under 120 s | **Met in behaviour, and it is the criterion whose fixture is unregistered.** `ensureReleaseRuntime` now also builds when `harnessRuntimeCause(root)` is non-null and re-asserts the cause afterwards, and it runs after `updateMain` and before `prepareManifest`, so the manifest is built from the refreshed runtime; the below-latest branch calls `refreshHarnessRuntime` explicitly; `worktree finish` does the same against the main checkout and throws with the worktree preserved on a failed build. The six-scenario fixture asserts one build for changed and snapshot-missing, none for matching, `harnessRuntimeCause === null` afterwards, preservation on failure, and a 120 s ceiling; it passed in my gate at 21.77 s. F1 is that this case is missing from the inventory, not that the case is wrong. |
| 4: role text in all six roles, `resume` keeps supplied values and maps the legacy token | **Met.** `parseActor` maps only the legacy source token and records `--model` and `--effort` exactly as supplied; there is no path that rewrites a supplied value to `unknown`, which is the shape criterion 4 asks for. The generated-role fixture asserts both new lines across all six roles on both profiles, and the resume fixture asserts the canonical event carries `fixture.model/alpha` / `xhigh` / `operator-attested` from a legacy-token completion. The reopening observations are post-merge and open by construction; this review adds one datum on the Claude side, since this dispatch records a supplied model and effort rather than `unknown`. |
| 5: a version-only change selects no machinery suite, a host change still selects two, grep pin | **Met, re-derived independently.** I resolved every machinery row's declared sources against `packages/skeleton/src/version.ts` and got no match, and confirmed the three release literals appear in source only in `version.ts` — `harness-host.ts` re-exports and `contributor.ts` imports. The selection filter reuses `evidenceSourceContent`, which normalises only the compiler constant, the two skeleton literals and the package manifests, and falls back to selecting the suite whenever `git show` fails or the projection throws, so the conservative direction is preserved. The fixture proves both directions against a real repository. |
| 6: regenerated bundle and manifest pass `harness check`, write-backs land | **Met.** `node scripts/harness.mjs check` exits 0 at 24 generated surfaces and `npm run publication:check` exits 0 at 267/267 headings with both source locks current, both executed by me after the gate; and I read the regenerated `read-observer.mjs` line by line against `packages/compiler/src/harness.ts`: the embedded `showHarnessAdvisory`, the cause computation, the marker callback, the journal append that keeps the advisory text even when the response is `{}`, and the two new runtime pins for `compiler/dist/src/harness.js` and `skeleton/dist/src/version.js`. Every write-back the criterion names is present — 07 §Discipline and §Write once run once, 02 §Harness compiler v1, the playbook's release task, both component READMEs, `docs/README.md`, the decisions file with before-and-after observations and the honest "next real skeleton reviewer duration remains unobserved", and the decisions index. One accuracy gap, recorded rather than repaired: neither the README sentence nor the 07 list tells the reader that the support and authority advisories share the `classification` bucket. |
| 7: `npm test` green, `git diff --check` clean, no new dependency | **Not met.** `npm test -- --review` exits 1 with `runner-fixtures` failing, per F1. The other two halves hold: `git diff --check` is clean in the working tree and the index, and the lockfile diff moves only the internal `@dotln/compiler` and `@dotln/skeleton` workspace versions with no dependency added, removed or re-resolved. |
| Operator-review assumptions 1–3 | Accepted as stated and consistent with the delivered work. One 4 s build inside close or finish is what the fixture measures; the marker is per session under ignored harness state and a new session says it again once, which this session's marker file demonstrates; and assumption 3's escape hatch was not needed, because role text was in fact the right place — `parseActor` never discarded a supplied value. |

## Other observations, none blocking

1. **VER-001's O2 stands and is the sharper version of the same class as F1.** `scripts/lib/harness-runtime.mjs` accepts a pinned runtime file only if it matches `^packages/(?:compiler|skeleton)/dist/src/[a-z-]+\.(?:js|mjs)$` and returns `runtime-unavailable` otherwise. All 14 pins in each of the two installed profiles match today, which I checked directly against the manifest. A future pinned file whose basename carries a digit, an underscore or a capital would be read as `runtime-unavailable`, and `refreshHarnessRuntime` would then build once and throw `Built runtime still runtime-unavailable; checkout preserved`, failing every release close and worktree finish until the pattern is widened. The failure is loud and preserves the checkout, which is the right direction, but it is a path-shape assumption in the code that now gates both helpers that move main.
2. **`version.ts` is outside every machinery declaration by construction, which is the point and also the risk.** The file is two lines and its comment scopes it to release metadata. Nothing enforces that. VER-001's O3 names the reopening observation — any executable statement added to `version.ts` — and I agree it is latent rather than present. The now-dead `HARNESS_HOST_VERSION` and `skeletonVersion` projections in `evidence-editions.mjs` are harmless residue that a later order can drop.
3. **Advisory markers accumulate without cleanup.** One file per session per cause lands in `docs/control/local/harness/`; the verifier's 04:58Z marker and mine from 05:28Z are both still on disk. The directory is ignored local state and the files are tiny, so this is housekeeping, not a defect, but nothing prunes them.
4. **Read-only Bash stayed refused throughout my gate.** `sed -n`, a piped `grep -r` and any command expanding `$TMPDIR` were refused with `DOTLN_HARNESS_REFUSED: write may change gate inputs during active gate`. This is the conservative shape of the kept refusal, is pre-existing, matches VER-001's O5 and WO-132 VER-001's F2, and this order's non-goals exclude the classifier. I used the host's file reader instead and lost no coverage. Recorded so it is not read as a regression of this order.
5. **The adjacent fixture repair (D004) is defensible and disclosed.** `reactor-slices.test.ts` now recompiles the stored WO-050 correction program with the current compiler and generates the scenario log live, asserting slice ownership and predicate behaviour rather than byte identity; `scenario.test.ts` follows `currentEvidence` instead of hard-coded WO-046 and WO-048 paths. The 19-case byte oracle is preserved in `reactor-identity.mjs --check`, which now spawns a child under a test-only loader that substitutes exactly one constant in exactly one module and asserts it was invoked from `--check-historical` on the main thread; run outside that entry point, `--check-historical` fails loudly on the identity assertion. Production artifact validation is unchanged, and D004 plus the fixture README disclose the narrowing.
6. **Clean-room screen** over every added line in the authored surfaces: no employer material, credential, internal URL, private identifier or secret; the predecessor is referred to only as `v1` where it appears at all; no stop condition. The only hosts introduced are `example.invalid` in fixtures, this project's own releases page and registry URLs in the lockfile. **No lint or type suppression was added** — the diff introduces no `eslint-disable`, `@ts-ignore`, `@ts-expect-error`, `@ts-nocheck` or `prettier-ignore`, checked over every added line.
7. **`scripts/reactor-identity.mjs` and `scripts/fixtures/historical-compiler-loader.mjs` are declared by no machinery suite either.** They are exercised by a product suite in every gate, so nothing is currently unguarded, but it is the same declaration gap that F1 turned into a failure and is worth one look while the repairer is in `machinerySources`.

## Goal alignment and process cost

The mission contribution is real and mostly delivered: three of the four residue items — the runtime following main, the advisory reduction, the attestation fallback — remove recurring operator repairs on the route to the always-on runtime, and the fourth removes about 400 s from a reviewer gate whose only change is a version literal. NoOp loses on all four, as D001 argues. The trap that actually bit is rule beating, and it bit the order rather than the operator: the order's criterion 3 was satisfied by writing a fixture and running it directly (`bash scripts/test-release.sh --case runtime_refresh`, six scenarios, which is genuine evidence), while the guard that exists precisely to notice a new case going unregistered was never selected, because the selection rule keys on declared sources and `test-release.sh` is declared nowhere. An order that spends its own criterion 5 on making suite selection cheaper is the right place to notice that suite selection can also be too cheap. Naive Interventionism is answered well elsewhere — no new hook, key, cache, gate or check; the two refusals untouched; fail-open on the message and never on authority; reversible source changes. Drift to low performance is the honest cost of this cycle: an 828.59 s reviewer gate that ends in a one-line repair, plus the repair's own gate and a fresh verification to come.

Process cost, source `claude-transcript-message-usage`, scope `dispatch`, cutoff 2026-09-16T05:45:07.769Z: 12,119,383 total tokens — 162 input, 11,864,208 cached input, 206,549 cache write, 48,464 output — across 89 steps and 73 commands. Dollar cost is unavailable and therefore unknown; no context-byte counter was exposed. Recorded elapsed for the order before this dispatch: implementation 3,083,731 ms, verification 1,326,141 ms. Wall-clock in this dispatch was dominated by the 828.59 s gate. No subagent ran. The largest avoidable cost in this review was not mine to avoid: the gate had to run in full to produce the row criterion 7 needs, and it is also the only thing that found the defect.

## What repair needs

One bounded change and one recommendation, then a fresh independent verification of the repaired subject.

1. **Required.** Add `runtime_refresh` to the expected case inventory at `scripts/test-release-fixtures.mjs:25`, positioned after `stale_helpers` to match the function order in `scripts/test-release.sh`, and re-run `npm test -- --review` to a green row. Nothing else in the order needs to move; criteria 1–6 are met and their evidence stands at the current code identity.
2. **Recommended, same surface, repairer's call.** Declare `scripts/test-release.sh` under `runner-fixtures` in `machinerySources` so that adding a release case selects the suite that validates release cases. If it is declined, the reason belongs in a decision record with its reopening observation, because the gap is now documented and the next order to hit it will hit it the same way.

No behavioural code was written by this review. Nothing was committed, no branch was pushed, no pull request was opened, and `docs/final-reviews/WO-133/PR.md` remains the stub the executor's `release prepare --local` wrote; a five-section `RELEASE-NOTES.md` is deliberately not authored, because publication artefacts are for the pass path. The working tree is preserved exactly as verified, at checkpoint 5.
