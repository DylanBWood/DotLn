# WO-133 FINAL-002 — the repaired stand-down residue passes review at the same bytes two independent gates already measured

**Verdict: pass.** FINAL-001's one blocking defect is closed. `runtime_refresh` is now in the release-case inventory, and `scripts/test-release.sh` is a declared source of `runner-fixtures`, so a shell-only release case now selects the guard that checks it. My own `npm test -- --review` is **30 suites passed, 0 failed, 820.81 s, 74 fresh tasks, exit 0** at code identity `5778716b726ec3c195a2209795c9571d2b7b9f639a3e515db9ed9a5de849caea`. That is the same identity as the repairer's passing row (779.22 s) and VER-002's passing row (830.55 s). I read the complete subject diff against the original order, not only the repair delta, and all seven acceptance criteria are met. `main` has not moved since the order was activated: the live remote `main` is `030b1d9`, equal to `HEAD`, and no release above v0.22.0 exists remotely. So there is no integration, no retiming and no carried-forward claim. VER-001's F1 is confirmed a fourth time, in this review session, and is still a non-blocking planner nomination. It is delivered as the order's Design specifies, but the release notes must say plainly what it silences, and they do. One new observation, O1, concerns how that same catch-all cause class labels journal rows. Nothing routes to repair.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.273","model":"claude-opus-5[1m]","effort":"xhigh","source":"self-reported"}

Dispatch: `resume: final review`. The session hook recorded it as the `final-review` transition, allocating FINAL-002 at this path at 2026-09-16T13:40:16.455Z through VER-002 from checkpoint `refs/dotln/checkpoint/WO-133/11` (`3411c684…`). I did not repeat the recorded dispatch. The harness version comes from `claude --version`. Effort `xhigh` was read back from `CLAUDE_EFFORT` in this session. The model identifier is the one this session exposes for itself. Neither the operator in this dispatch nor an effective-readback channel supplied the model, so the source is `self-reported`, as in VER-001, FINAL-001 and VER-002. This session held the worktree's only writer reservation throughout (`writer --show`: actor `337b5142…`, owner pid 14913, reserved 13:40:15.482Z, `alive: true`). No subagent ran, and every file named below was read by this session.

**Bypassed requirements: none.**

Instrument disclosure. The subject reviews itself. The hooks that held my reservation, refused my read-only Bash calls for the 13.7 minutes my gate ran, journaled this session and suppressed its advisories are the regenerated hooks this order ships, running from snapshot `.runtime/harness/6b1dc0917ec592c3`. Every claim below that rests on that machinery says so.

## Subject and base

The subject is [WO-133](../../work-orders/WO-133-stand-down-residue.md): acceptance criteria 1–7 as filed, the three operator-review assumptions and the non-goals. It also covers decisions D001–D005 (D002 and D004 are same-day corrections; D005 is the repair) and the operator-authorized adjacent items `adjacent-0001` (fixture repair) and `adjacent-0002` (selection repair). The branch is `wo-133`, with an uncommitted working tree preserved by checkpoint 11. `git log main..HEAD` is empty.

**Base and integration.** `HEAD`, local `main` and `origin/main` are all `030b1d91b74b9f1c9db2dfbdf997c913a0abb010`. Unlike FINAL-001, this review observed the remote live: `git ls-remote origin refs/heads/main` returned `030b1d91…` at about 13:41Z, and the remote's highest application tag is `v0.22.0`. No sibling lane has merged or published, so v0.22.1, compiler 0.11.2 and skeleton 0.18.4 are still free. Under product 07 §Independent workflows and integration there is nothing to merge, regenerate, union or retime, and the patch classification recorded at activation stands unchanged.

Read scope: the order in full and its cited sections, which are R1 replan §3 (including §3.1–§3.3), WO-132's acceptance criteria (7, 9 and 10 among them), product 07 §Discipline and §Write once run once, product 02 §Harness compiler v1 and product 08 §PRs and commits and §Release-note edition. I read the complete diff of every source file: `packages/compiler/src/harness.ts` and `artifact-identity.ts`; `packages/skeleton/src/harness-host.ts`, `loadouts/contributor.ts` and the new `version.ts`; `scripts/lib/harness-runtime.mjs` (new) and `scripts/lib/harness.mjs`; `scripts/release.mjs`, `worktree.mjs`, `resume.mjs`, `refute-plan.mjs`, `test-runner.mjs` and `reactor-identity.mjs`; and `scripts/fixtures/historical-compiler-loader.mjs` (new). I read every fixture diff: `scripts/test-harness.mjs`, `test-process-debt.mjs`, `test-release.sh`, `test-release-fixtures.mjs`, `test-resume.sh` and `test-runner.test.mjs`, plus `packages/skeleton/test/executor-supports.test.ts`, `reactor-slices.test.ts` and `scenario.test.ts`. I also read the fixture `npm run build` double in `test-release.sh` (`make_repo`), `evidenceSourceContent` in `packages/skeleton/src/evidence-editions.mjs`, and the `worktree publish` helper. I read every write-back, including the generated role text, `.claude/settings.json`, the harness manifest's pin changes, the console fixture renders, the lockfile, both publication locks, `docs/evidence/current.json`, the decisions index and follow-up projections, and the work-order index. Finally I read VER-001, FINAL-001 and VER-002 in full, the executor's `decisions.md` and `validation.md`, the adjacent queue log, and the WO-047 final-review artifacts as the current publication precedent.

## Ideation receipt

The order has no operator-opened ideation section, and `docs/evidence/WO-133/` holds no `ideation.md`. The receipt is therefore the order's **Nomination provenance** plus the two operator-authorized adjacent items. The screenshot capture is ignored intake and was not mined. The delivered work follows the planner's synthesis, and that synthesis resolves directly in the diff.

§3.2 asks for four things: a rebuild after a fast-forward that changes the pins, one advisory per session per cause, none from `PostToolUse` observers, and a `SessionStart` line that names the cause. All four are implemented and fixture-covered. §3.3 asks for the operator-attested fallback in role text and a refusal to downgrade a supplied value. All twelve generated role files carry it, and `parseActor` records supplied values verbatim while mapping the legacy source token. §3.1 asks for the version literals in one module outside every machinery source list. `version.ts` holds the only source literal and no machinery suite declares it.

The adjacent queue log records both items in the prescribed shape: queue, intent announcement, operator check-in, start, and completion with executed checks. `adjacent-0001` ran at seq 1–5, 04:37:25Z–04:50:34Z ("Proceed with fixture repair"). `adjacent-0002` ran at seq 6–10, completing at 06:10:46.436Z ("Proceed with the bounded fix"). Both check-ins are actor-attested message receipts, not host inbox readback, and the log says so. D002's operator direction ("Register the existing handler for SessionStart") is likewise recorded by the executor. I cannot observe it independently from this session, and I accept it as the executor's sourced record.

## Verification sequence

| Step | Verdict | What it established | Limits it named |
| --- | --- | --- | --- |
| VER-001 (Claude Code 2.1.273, `claude-opus-5[1m]`, `xhigh`, self-reported) at code identity `2d57d9e9…` | pass | Criteria 1–7 independently reproduced: the twenty-invocation stale-hook fixture, both kept refusals, live healthy `SessionStart`, six release scenarios under the 120 s ceiling, twelve role files, the legacy-token mapping, version-only selection with a grep pin, `harness check`, every write-back, and `npm test` 19/0 at 251.66 s. F1 was recorded as as-designed, along with O1–O6. | L1–L5: no live stale runtime; criterion 2's stale half is fixture evidence; post-merge observations open; host-load timings; process cost. |
| FINAL-001 (same harness and model) at `2d57d9e9…` | **fail** | `npm test -- --review` 29/1 at 828.59 s. `runner-fixtures` failed because `runtime_refresh` was absent from the hard-coded inventory, and `scripts/test-release.sh` was declared by no machinery suite, so a release-case change selected nothing that checks release cases. Criteria 1–6 met; criterion 7 not met. | Recommended declaring the shell under `runner-fixtures`; confirmed VER-001 F1 live. |
| Repair, `resume: fix` (Codex CLI 0.154.0, `gpt-6-astra`, `max`, operator-attested) | RepairCompleted 06:11:17.208Z | D005: one inventory token, one declared source and a new isolated-Git regression, under `adjacent-0002`. Passing gate 30/0 at 779.22 s, identity `5778716b…`. | No component version, dependency, runtime behaviour or product document changed. |
| VER-002 (Claude Code 2.1.273, `claude-opus-5[1m]`, `xhigh`, self-reported) at `5778716b…` | pass | `npm test -- --review` 30/0 at 830.55 s with `runner-fixtures` passing. A negative control showed that the shipped table selects `["runner-fixtures"]` for a shell-only change and a table without the declaration selects `[]`. Source delta since the VER-001 subject is 3 files, +61/−1. Criteria 1–7 met; F1 reproduced a third time. | L1–L5 as VER-001, with a second Claude-side attestation datum. |

This is the complete numbered sequence: two verifications, one prior final review and one repair. VER-002 judged the bytes this review gated. Its identity, the repairer's and mine are all `5778716b…`, so its judgments carry forward without an integration caveat. No verifier changed implementation.

## The reviewer's gate

`npm test -- --review`, with intended new source files staged (`version.ts`, `harness-runtime.mjs`, `historical-compiler-loader.mjs`): **30 passed, 0 failed, 820.81 s, 74 fresh tasks, 0 reused, exit 0**. Recorded at 2026-09-16T13:57:30.384Z as `host-gate:5778716b…:npm test` with tree `0b4cd9438699dde0ea1560fdf862ac693682d642`. The suites carrying this order's claims passed at these aggregate durations: `harness-fixtures` 274.44 s, `process-debt` 159.85 s, `release` (all cases, including `runtime_refresh`) 274.75 s, `resume` 131.50 s, `worktree` 172.05 s, `skeleton` 132.41 s, `runner-fixtures` 33.47 s and `plan-refutation` 166.72 s. The four evidence suites passed against the WO-133 editions. `gateCodeIdentity`, recomputed after the run, is still `5778716b…`, so the gate changed no code-identity byte.

The duration is the expensive case, correctly so. This order changes `harness-host.ts` behaviourally, so `--review` must select `harness-fixtures` and `process-debt`. The saving the order promises belongs to a later skeleton order whose only change is a version literal, and that stays the reopening observation. Three passing gates at one identity (779 s, 831 s, 821 s) put the spread at host load, not scope.

## Executed checks

| Command or observation | Result |
| --- | --- |
| `git ls-remote origin refs/heads/main` and tag listing | `030b1d91…` = `HEAD`; highest application tag `v0.22.0` |
| `npm test -- --review` | 30/0, 820.81 s, 74 fresh, exit 0, identity `5778716b…` |
| `gateCodeIdentity` before and after the gate | `5778716b…` both times; equals VER-002's and the repairer's rows |
| `git diff --check`, and `git diff --cached --check` | exit 0, exit 0 |
| `node scripts/harness.mjs check` | exit 0; 24 generated surfaces |
| `npm run publication:check` | PASS; 30 and 45 linked source sections current; bootstrap checks pass |
| Source grep for `HARNESS_HOST_VERSION =` and `skeletonVersion: "` | the only source literal is `packages/skeleton/src/version.ts:2` (`"0.18.4"`); the other hits are the projection regex in `evidence-editions.mjs` and fixture strings in two test files |
| `harnessRuntimeCause(repo)`, three runs | `null`; 21.7, 17.1 and 16.2 ms over 1.25 MB of pinned built and snapshot bytes |
| `npm run resume --silent -- status --json`, timed twice | 0.41 s and 0.35 s real, including the new runtime preflight |
| This session's hook journal at about 13:59Z | 711 rows. The first is `SessionStart` at 13:40:08.986Z (128.34 ms, no advisory). 17 advisory rows in four kinds; one marker (`classification`, 13:43:52.075Z) |
| Added-line scan for `eslint-disable`, `@ts-ignore`, `@ts-expect-error`, `@ts-nocheck`, `prettier-ignore`, `biome-ignore` | none introduced |
| URL and address screen over tracked additions and untracked WO-133 evidence and reports | this project's releases page; the public hook reference cited by D003; `example.invalid` fixtures; lockfile registry URLs; and the no-attribution regression fixture string, identical in kind to WO-047's committed feedback edition |
| Product 02 write-back location | line 754, under `### Harness compiler v1` (line 712) |
| `git diff HEAD -- package-lock.json` | the `@dotln/compiler` and `@dotln/skeleton` workspace versions and the skeleton's pin on the compiler; no dependency added, removed or re-resolved |

## Acceptance

| Criterion | Review result |
| --- | --- |
| 1: twenty stale invocations yield one advisory naming `pins-differ` and the command, with twenty journal rows; observers silent; a second session gets its own; both refusals intact | **Met.** Read at source. `assertHarnessRuntime` throws cause-prefixed errors (`pins-differ:`, `snapshot-missing:`, `runtime-unavailable:`), and `runHarnessHook` derives `reasonClass` from them. The suppression sits after the `finally` that journals, so every row survives, and it deletes only a `systemMessage` beginning `DotLn advisory:`. A refusal's `permissionDecision`, a `Stop` refusal's `DotLn:` message and the dispatch context are therefore untouched. `showHarnessAdvisory` returns `false` for `PostToolUse` before any marker is claimed, and `protocolRefusal` returns `{}` there. The marker write is `wx` and fails open, because a throwing claim shows the message. The generated prelude computes the same causes from the pins and shares the same marker function. The WO-133 fixture asserts all of it, including a missing snapshot and unwritable marker storage. `harness-fixtures` and `process-debt` passed in my gate. The live-gate refusal was applied to my own Bash reads for the whole gate. |
| 2: `SessionStart` names the cause when pins differ and is silent when they match | **Met.** `lowerToHarness` registers `session.mjs` under `SessionStart`, and the fixture asserts it is byte-equal to the `UserPromptSubmit` entry. At `SessionStart`, `evaluateHarnessHook` asserts the built bytes and then the snapshot, and returns `{}` without dispatch. A third live observation now exists: this session's first journal row is the host's real `SessionStart`, 128 ms, no advisory, healthy pins. The rendering of a stale `SessionStart` message remains fixture evidence (VER-002 L2). |
| 3: close and finish rebuild once when the fast-forward changed the pins, the manifest cites the rebuilt runtime, under 120 s | **Met, and now registered.** `ensureReleaseRuntime` builds on a non-null `harnessRuntimeCause`, re-asserts, and runs after the fast-forward and before the manifest. The below-latest branch refreshes explicitly. `worktree finish` refreshes after `merge --ff-only origin/main` and before any removal, throwing with the worktree preserved. The fixture's build double rewrites the kernel runtime, and the close-changed scenario starts from an inconsistent kernel runtime that fails the cadence projection, so a successful publish shows the manifest was built from the rebuilt runtime. That is indirect, but it is sound evidence for "cites". Six scenarios; `runtime_refresh` is in the inventory (44 cases). `release` passed in my gate. |
| 4: role text in all six roles; `resume` keeps supplied values and maps the legacy token | **Met.** `contributor.ts` inserts both lines into every target role's procedure. `executor-supports.test.ts` asserts six roles on both profiles, and I read the lines in the generated `.claude` and `.agents` files, including the reviewer text this dispatch loaded. `parseActor` rewrites only the `--source` token `operator-selected-model-effective-effort-unavailable` and has no path that turns a supplied model or effort into `unknown`. The resume fixture asserts arbitrary labels on both harnesses and the canonical event. Post-merge first-completion observations remain open by construction (D001). |
| 5: a version-only change selects no machinery suite; a host change still selects two; grep pin | **Met.** `changedMachinery` filters changed files to declared machinery sources, then drops a file only when `evidenceSourceContent` projects base and current bytes equal. A failed `git show` or a projection error keeps the file, which is the conservative direction. `version.ts` matches no declared prefix. The fixture covers version-only, compiler-literal-only and host-change cases against a real repository through the selector, and the version-only case also through `--review --list`. The grep pin above holds. |
| 6: bundle and manifest pass `harness check`; write-backs land | **Met.** `harness check` exits 0 at 24 surfaces. The manifest pins now include `compiler/dist/src/harness.js` and `skeleton/dist/src/version.js`. Write-backs are present at current bytes: 07 §Discipline, which names the four cause classes; 07 §Write once run once, for the version module; 02 §Harness compiler v1; the playbook's release task and its finish sentence; both component READMEs; the `docs/README.md` `.claude/` change line; the decisions file with before-and-after observations (344 rows → one visible advisory of twenty in the fixture; 625–840 s gates → zero machinery suites selected, with the next real duration marked unobserved); and the decisions index. |
| 7: `npm test` green; `git diff --check` clean; no new dependency | **Met.** 30/0 at 820.81 s, exit 0; whitespace clean in tree and index; the lockfile moves only workspace versions. |
| Operator-review assumptions 1–3 | Accepted and consistent. The build double runs once, only on a mismatch. The marker is per session under ignored state, and a new session speaks again, which the fixture's second session and this session's own marker show. Assumption 3's escape hatch was not needed. |

## Finding carried forward, confirmed, not charged

### VER-001 F1 — every non-runtime advisory shares one per-session marker (non-blocking; as designed; planner nomination)

The source is unchanged. Only `pins-differ`, `snapshot-missing` and `runtime-unavailable` keep their own marker. Authority delegations, shell classification, unreadable state, the attribution pre-check and the `Stop` support advisory all map to the literal `classification`. The order's Design names exactly four cause classes and routes `protocolAdvisory`, the path all of those use, through the marker. No criterion requires repeat non-runtime advisories to stay visible, and product 07 discloses the four classes. It is delivered as filed. It also narrows WO-132 criterion 9's "one-line advisory" to the first occurrence per session, and the executor updated those WO-132 fixture assertions to read the journal instead. That is the supersession the order's Design implies, and it is disclosed, not hidden.

**Fourth session, same outcome.** In this review the first non-runtime advisory claimed the session's only marker at 13:43:52.075Z: a delegated authority notice on an ordinary tool call. The `Stop` advisory `pending no-partial-completion, verify-app-before-done` at 13:45:28.843Z was then journaled and not shown. VER-001, FINAL-001, VER-002 and this review have each had WO-132's completion-discipline advisory silenced for their whole dispatch.

What this review does about it is publication, not repair. The README release block says "Runtime advisories appear once per session and cause", which reads narrower than the behaviour. The release notes and PR body I author below state which advisories share the bucket and where the silenced text remains readable, so no reader of this release learns it from a missing message. Repair shape, if planning wants it, is unchanged: key the marker on the advisory's own identity, or exclude non-runtime causes from suppression, since the measured flood came entirely from the three runtime causes. Reopening observation: a one-session regression that emits two distinct non-runtime advisories and expects two visible messages.

## Observations, none blocking

**O1 — `runtime-unavailable` is also the catch-all class for any untyped hook error.** `runHarnessHook` classifies every error that is not `HarnessCommandRefused`, not `HarnessStateUnreadable` and not cause-prefixed as `runtime-unavailable`, and WO-133 now prefixes that class onto the message. This session's journal has six `PostToolUse` rows reading `runtime-unavailable: host facts or pinned runtime unavailable (Error: Path outside the worktree …); run node scripts/bootstrap.mjs …`. They came from ordinary reads of the session scratchpad outside the worktree, with healthy pins. They are silent by design and claimed no marker (the only marker is `classification`), so nothing visible went wrong here. The classification itself predates this order. Two consequences follow from WO-133's use of it, both latent. First, a count of `runtime-unavailable` journal rows, which is how this order's before-and-after advisory observation is measured, includes rows that have nothing to do with the runtime. Second, an untyped error on a non-observer event would still show the bootstrap instruction it always showed, but it would now also claim the session's `runtime-unavailable` marker and silence a genuine runtime advisory later in that session. Reopening observation: the first post-merge advisory count, which should separate outside-worktree observer rows before comparing with the 344-row baseline, or any non-`PostToolUse` journal row with an untyped error under this class.

**O2 — the new `resume` and `plan start` preflight costs about 20 ms.** Measured, not assumed. `harnessRuntimeCause` hashes both profiles' built and snapshot pins (1.25 MB) in 16–22 ms, and `resume status --json` still completes in 0.35–0.41 s. The order's "adds no recurring step" holds in substance: the check prints only on drift and never builds.

**O3 — VER-001 O2 stands.** `harness-runtime.mjs` accepts pinned paths only under `^packages/(?:compiler|skeleton)/dist/src/[a-z-]+\.(?:js|mjs)$`. All pins match today, including the two added by this order. A future pinned file outside `[a-z-]` would read as `runtime-unavailable`, and both helpers that move main would then build once and throw with the checkout preserved. That is loud and safe, but it is a path-shape assumption in the gate on close and finish.

**O4 — `version.ts` is unenforced release metadata.** It is outside every machinery declaration by design, and the evidence projection still normalizes the now-dead `HARNESS_HOST_VERSION` and `skeletonVersion` literals in `harness-host.ts` and `contributor.ts`. That residue is harmless and a later order can drop it. Reopening observation: any executable statement added to `version.ts`.

**O5 — D004's oracle narrowing is disclosed and bounded.** `reactor-identity.mjs --check` runs the 19 frozen cases in a child under a test-only loader. The loader asserts its entry point, mode and thread, and substitutes exactly one `COMPILER_PACKAGE_VERSION` declaration in exactly one module. `reactor-slices` now compiles current inputs, and `scenario.test.ts` follows `currentEvidence`. Production artifact validation is unchanged. The `skeleton` product suite spawns the oracle, so it ran in my gate, which is why D005's decision not to declare those two scripts under machinery is sound.

**O6 — advisory markers accumulate.** `docs/control/local/harness/` now holds four `.advisory` files, consistent with one per Claude session on this order. They are ignored and tiny, and nothing prunes them. Housekeeping only.

**O7 — the kept refusal held read-only Bash for the whole gate,** exactly as VER-001 O5, FINAL-001 observation 4 and VER-002 O6 describe. It is pre-existing and outside this order's non-goals. I used the file reader and lost no coverage.

**O8 — clean-room screen.** No employer material, credential, internal URL, private identifier or secret appears in any tracked addition or in the untracked WO-133 evidence, verification or final-review files. The predecessor appears only as `v1` where it appears at all. No stop condition.

## Goal alignment and process cost

The order removes four recurring costs on the route to the always-on runtime: the advisory flood, the manual rebuild after a close, lost operator attestations, and roughly 400 s of reviewer gate per version-only skeleton bump. NoOp loses on all four, as D001 records. Naive Interventionism is answered: no new hook file, key, cache, gate or check; the two refusals are untouched; the message fails open while authority is never touched; and every change is reversible. The trap this order tripped once was rule beating. Its release case was proven by running it directly, while the guard for registering new cases was never selected. D005 fixed that at the selection rule, not only at the missing token. The trap still live is seeking the wrong goal: the order measures visible advisory count, and F1 shows that fewer visible messages can also mean fewer of the ones a completion-deciding session needs. That is a planning question, and this review does not answer it by quietly editing behaviour.

Process cost: `node scripts/harness.mjs usage bd517f71-…`, source `claude-transcript-message-usage`, scope `dispatch`, cutoff 2026-09-16T13:59:02.407Z, before this report was written. 7,425,420 total tokens (104 input, 7,184,574 cached input, 207,925 cache write, 32,817 output) across 56 steps and 40 commands. Dollar cost and context bytes were not exposed, so they are unknown. Recorded elapsed before this dispatch: implementation 3,083,731 ms, verification 1,425,070 ms, repair 1,177,357 ms, final review (FINAL-001) 1,711,089 ms. The 820.81 s gate dominated wall-clock here. It was required, not duplicative: the order's evidence gate and WO-132 criterion 3 assign exactly one `npm test` to final review, and FINAL-001 showed that a narrower command misses what the review gate catches.

## Publication

On this pass the reviewed state is committed as coherent plain-subject commits with no attribution. `wo-133` is pushed and its PR opened through `npm run worktree -- publish WO-133` with the reviewed title and the committed [PR body](PR.md). The committed [release notes](RELEASE-NOTES.md) follow product 08's five-section edition. The PR body's process meter is the one `release prepare` wrote during repair. I recorded the result before refreshing it, and `release prepare` refuses a closed order, while the meter library's post-close render drops the per-dispatch rows. So the PR keeps the repair-time meter and says beneath it that it predates this review. Next time, run `release prepare` before `final-review-result`. The operator's phrase authorizes nothing beyond that: no merge, no push to main, no release, no package publication.
