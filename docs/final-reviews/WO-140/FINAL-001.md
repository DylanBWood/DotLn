# WO-140 FINAL-001 — final review of the gate sandbox preflight, partial identity and receipt cost line

**Verdict: pass.** All six acceptance criteria hold at the reviewed subject, with criterion 3 met through a sourced correction the executor recorded and I confirmed against the cited receipts. VER-001 is the only verification in the sequence and it passed; I reproduced its consequential claims rather than inheriting them. No finding routes to repair.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"xhigh","source":"operator-attested"}

Actor: Claude Code 2.1.278, model `claude-opus-5[1m]` selected by the operator with `/model` in this session, effort `xhigh` from the session's `CLAUDE_EFFORT`. Claude Code exposes no effective model or effort readback, so both are operator-attested rather than session-read. Dispatch: `resume: final review`, 2026-09-20, recorded by the harness as `npm run resume -- final-review`. One reviewer; no subagents spawned, 0 of the 20-agent cap, confirmed by the usage readback's `exact-observed` count.

**Process cost:** entry 83,789 tokens; handoff 4,677,985 tokens; source claude-transcript-message-usage

Both readings are `scope: dispatch` from `node scripts/harness.mjs usage 2988b23c-506b-4bb8-a76d-9fd6fa88a316`; entry observed at 2026-09-20T04:32:22.644Z, handoff at 2026-09-20T04:45:35.768Z, which is this report's counter cutoff. Reasoning tokens and dollar cost are unavailable from this counter and are therefore unknown, not zero. The difference is dominated by cached input across the 535.86 s gate wait.

## Subject and evidence boundary

Reviewed the original [work order](../../work-orders/WO-140-gate-sandbox-preflight.md), the complete verification sequence ([VER-001](../../verifications/WO-140/VER-001.md), the only one), the ideation receipt that nominated the order ([vision-into-use 2026-09-17](../../planning/vision-into-use-2026-09-17.md) §9, §10, §17 and its known-issue list), the executor's [decisions](../../evidence/WO-140/decisions.md) D001 to D008, [implementation evidence](../../evidence/WO-140/implementation.md) and [fixture transcripts](../../evidence/WO-140/fixture-transcripts.md), and the full uncommitted subject diff above `main` at `295766dd11b12d0cd819161a7c4f9ec58a82d7f2`.

Integration: `main`, `HEAD` and the merge base are the same commit, so there was nothing to integrate and no check needed retiming. The branch carried no commits before this review, as the contract requires.

I read the changed sources directly rather than through the reports: `scripts/lib/gate-sandbox.mjs` and `scripts/lib/receipt-cost.mjs` in full, the `runGateChecks` preflight and suite-table diff in `scripts/test-runner.mjs`, `partialGateCheck` and its use in `findGateCheck` and `reviewedProductGate`, the briefing and stamping changes in `scripts/resume.mjs`, `usageReadbackLine` and its two call sites, the loadout role text, product 07's two new passages, and the WO-140 fixtures in `scripts/test-runner.test.mjs` and `scripts/test-process-debt.mjs`.

## Acceptance results

| Criterion | Judgment |
| --- | --- |
| 1 — preflight, inherited marker, partial selection, outside unchanged | **Met.** The refusal is computed before the build, the diagnostics directory and every suite, and the fixtures assert that a refused run leaves no observed task, no recorded check and no active gate run. This review's own gate is the live inherited-marker case: it ran under `CLAUDECODE=1` with `skeleton` selected and did not refuse, and its recorded row carries `marker: claude-code`, probe `written`, `inForce: false`. `--only skeleton` omits the inside command because nothing would remain, and `--only outside --inside-sandbox` errors rather than passing empty; both are asserted. |
| 2 — every product-gate consumer rejects a partial row | **Met.** The fixture drives the real `runGate` to a partial row, then rejects that row, a copy relabelled `npm test`, and a relabelled copy with `partial` stripped, across `findGateCheck`, `requireLifecycleEvidence` and `reviewedProductGate`, and accepts the full run at the same code identity. I confirmed by source that worktree publication and release close both reach `reviewedProductGate`, so the two committed-history consumers are covered by that one guard. |
| 3 — declarations follow an environmental cause | **Met, through a sourced correction.** Only `skeleton` declares; `release` does not; an unknown need is refused at validation; the fake-marker fixture drives the preflight. The order's Cost paragraph and criterion 3 attribute one of WO-121 VER-001's in-sandbox suite failures to the `.claude/hooks/**` denial. I checked the cited receipts: that failure was `skeleton`'s nested `sandbox-exec`, and WO-121 FINAL-001 §L1 attributes the hooks denial to `git merge`, `git checkout --theirs` and `harness emit`, which are commands. [D001](../../evidence/WO-140/decisions.md#wo-140-d001) records the difference with evidence, rejected alternatives, a reopening condition and a named planner follow-up (`FUP-0e424834e4030880`). Declaring a suite to match the order's sentence would have removed a suite from every sandboxed run for no observed cause. |
| 4 — briefing, role text, cause codes | **Met.** This dispatch printed the session id and the exact usage command, and the command worked at entry and at handoff. The two new rules are present in all four generated role surfaces; I read them in the reviewer text loaded into this very session, which is the strongest available form of that check. Product 07 §Process budget carries the same three codes as `receipt-cost.mjs`. |
| 5 — the document check refuses a bare unknown in a new receipt | **Met.** VER-001's own `unknown; cause harness-no-readback` line passed the `verification-result` transition, which is executed evidence that the admitted form works on a real receipt. The fixture covers fenced examples, indented and list-item lines, comma-grouped counters, negated sources, case sensitivity, a second ambiguous code and stamped paths outside the receipt directories. `npm run test:docs` is a separate selection from the product gate and passed on its own: **19 suites, 0 failures, 15.83 s**, judging this report's own stamped cost line. |
| 6 — complete checks, no new dependency, generated surfaces | **Met.** `npm test -- --review`: **29 suites, 0 failures, 535.86 s, 73 fresh tasks, exit 0**, recorded 2026-09-20T04:43:33.164Z at code identity `05df9155882d003d481c14d85b83fe4621ea9fc8b144239098d65ce015379676`, run after staging the two new source modules. `node scripts/harness.mjs check`: 31 generated surfaces. `git diff --check`: clean in the worktree and the index. `node scripts/harness-context.mjs --check`: every role within its ceiling in both generated roots, reviewer 21,299 of 24,576 and verifier 20,081 of 20,480. The lockfile diff carries only skeleton `0.29.1` and the console pin; no dependency is added. |

## Why this review ran its own gate

VER-001 recorded a passing gate at code identity `49f6b472…`, with the two new modules untracked and read at pinned digests instead. Staging them changed the tracked source set and therefore the identity, so the verifier's row is not at the reviewed bytes; the row this review recorded, `05df9155…`, is, and it is the one `final-review-result` consumes. This is the ordinary reason the reviewer's gate follows staging, and VER-001 stated the obligation rather than substituting its own identity for it.

## Findings

None that route to repair. Three observations, each already owned and none blocking:

1. The order's criterion 3 wording remains uncorrected in the committed order, and the 2026-09-17 ideation receipt's known-issue list asserts that this wording was already fixed when it was not. The implementation follows the receipts, D001 records the divergence, and the planner follow-up is filed. The correct disposition is planning's, not repair's.
2. In-force behavior is established by real Seatbelt denials under the real markers and by an independent temporary-repository probe, not by a real sandboxed harness launch; none was available on this host. That a Codex Seatbelt sandbox refuses a nested `sandbox-exec` is a labelled inference, and Linux `CODEX_SANDBOX` propagation is unknown, where the harness is unrecognized and fails open as the order accepts. The implementation and VER-001 both state this limit plainly.
3. The recorded `sandbox` field carries an absolute worktree path. I confirmed this cannot reach a commit: `requireLifecycleEvidence` copies eight whitelisted keys into the control event, and `docs/control/local/` is gitignored.

## Goal alignment

Mission and critical path: a verifier's first gate attempt should be one that can pass, and a receipt that reports no number should say why. Both are on the path to verified deliverables, and both were measured gaps rather than supposed ones. Against the traps: rule beating is checked by rejecting relabelled and flag-stripped partial rows under any identity, and by refusing a cost line that mentions a code without recording it; seeking the wrong goal by declaring from receipts instead of from the order's sentence, which is the one place this order could have looked complete while being wrong; drift to low performance by refusing rather than silently skipping, and by keeping the full-suite requirement intact; shifting the burden by an actionable refusal that prints the command, leaving WO-121's F1 a defect rather than an exclusion; policy resistance and fixes that fail by failing open on every unrecognized condition, so the change cannot make a working setup worse; escalation and commons by one gate run and zero fan-out; success to the successful by running my own gate at the reviewed identity rather than inheriting the verifier's green row. Naive Interventionism favors unchanged outside behavior, untouched historical receipts and a closed cause list over a free-text field. NoOp keeps the repeated failed sandboxed gate and the bare "unknown", both of which the receipts show recurring. The claimed saving is one failed sandboxed gate per attended verification; what this order actually cost, two full executor gates plus this review's, is recorded in the implementation evidence and is larger than one release's saving, which the order's own cost paragraph should be read against.
