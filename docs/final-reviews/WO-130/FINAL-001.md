# WO-130 — FINAL-001

**Verdict:** PASS

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.270","model":"claude-opus-5","effort":"max","source":"self-reported"}

Reviewer: Claude Code `2.1.270` (`claude --version`), model `claude-opus-5` — the 1M-context variant whose exact identifier is `claude-opus-5[1m]` — at effort `max`, selected by the operator's `/model` command at the start of this session. The source is `self-reported`: Claude Code exposes a launch selector, not an effective-session readback. The order requires `any` effort of the reviewer.

## Subject and inputs

- Work order: [WO-130](../../work-orders/WO-130-declared-suite-inputs-replica.md), read in full including its operator-override known issue and its four execution records. Branch `wo-130`; `HEAD` is `6c9b39a`, the `main` tip, and the branch carries no commits before this review.
- Subject: the working tree at 39 modified tracked files (+2549 / −478) and 19 new files, including `scripts/lib/suite-replica.mjs`, the order's control segment, its evidence and authority editions, four verification reports and this review's own directory. I read the complete diff of every source and product file, the new module in full, and the WO-130 fixtures in `scripts/test-suite-evidence.mjs` and `scripts/test-process-debt.mjs`.
- Verification sequence, all four read: [VER-001](../../verifications/WO-130/VER-001.md) (fail: F1 blocking, F2 non-blocking, O1–O7), [VER-002](../../verifications/WO-130/VER-002.md) (fail: F1 blocking), [VER-003](../../verifications/WO-130/VER-003.md) (fail: F1 blocking, medium), [VER-004](../../verifications/WO-130/VER-004.md) (pass). VER-001 came from a Claude Code session on this host; VER-002 to VER-004 from Codex CLI `0.154.0` at max. No verifier report was treated as authority to widen scope.
- Ideation receipt: planning pass `planning-dc998fb93c27e337`. Receipts [009](../../planning/refutations/2026-09-12-planning-dc998fb93c27e337-009.md) and [010](../../planning/refutations/2026-09-12-planning-dc998fb93c27e337-010.md) held criterion 2 — an optional-file guard, a regular-file guard, and a declared flag that selects a read — and both are recorded as `accepted` in receipt [011](../../planning/refutations/2026-09-12-planning-dc998fb93c27e337-011.md)'s carried dispositions, because the order replaced validation-by-probing with execution inside the replica on every fresh run. Receipt 011 then held criterion 1 for a declared suite that hard-codes an absolute candidate path in its own source; the operator overrode that hold on 2026-09-12 in `docs/control/plan-refutations.jsonl` (`PlanHoldOverridden`, actor `claude-code 2.1.269` / `claude-fable-5-1` / max, receipt and capture SHA-256 recorded), directing that it be carried as a known issue. The order's execution record carries it with two reopening conditions. This review does not reopen the override and claims no universal filesystem boundary.
- Control history: 17 events, from `WorkOrderActivated` at 05:12:15Z through four verify/repair rounds to `FinalReviewRequested` at 20:57:43Z, each transition actor-attested. The `final-review` dispatch for this session was recorded by the generated session hook from the operator's phrase, and I did not repeat it.
- **Self-referential instruments.** The runner, replica constructor, input observer, shared cache, explanation code, the generated hooks and the lifecycle command that allocated this report are all part of the subject under review. My independence is a different session, a different harness from three of the four verifiers, an environment that is itself the one VER-001 failed in, and direct probes against the real repository — not avoidance of the machinery. Where a check below was produced by the code under review, I say so.

## Independent checks

1. **The exact command and environment VER-001 F1 failed.** This session's shell exports `GIT_CONFIG_COUNT=4` with four `safe.directory` entries naming both checkouts and `GIT_CONFIG_PARAMETERS='http.proxyAuthMethod=basic'` — the values VER-001 recorded. With nothing removed from the environment, `node --test scripts/test-suite-evidence.mjs` gives **26 passed, 0 failed, 111.95 s**. VER-001 reported 10 passed / 14 failed on the same file in the same environment. No `env -u` prefix was used anywhere in this review.
2. **The dispatch and composition fixtures.** `node --test --test-name-pattern='WO-130|three-role' scripts/test-process-debt.mjs`: **3 passed, 0 failed, 28.88 s**. These are the WO-129 three-role composition case that VER-001's final canonical gate failed with `fresh` against `composed`, the stub dispatch case, and the real-lifecycle case that drives the generated hook files against the actual lifecycle for both guarded refusals, the uncontended dispatch, the resumed briefing and `next`.
3. **A key-scope probe of my own, against the real repository rather than a fixture.** One `observeSuiteInputs` of this worktree (1,485 candidate entries, 2,380 installed entries, 874 ms) and then `replicaPlan`/`suiteInputHash` for the six reviewed scopes: every one narrows, with **zero narrowing refusals** and **zero projected environment values containing the candidate tree path**. `scripts/test-runner.mjs` is absent from the `kernel`, `compiler` and `skeleton` declarations (187, 188 and 242 declared files) and present in the three fixture declarations (308 each), which is the declaration boundary the order claims.
4. **Invalidation, measured the same way.** Mutating one entry of that snapshot and recomputing: a change to `packages/kernel/src/index.ts` re-executes all three package suites; `docs/product/02-domain-model.md` re-executes `kernel` and `skeleton` and leaves `compiler` reused; `corpus/harness/id-corpus-lib.mjs` re-executes only `compiler`; **`docs/product/07-execution-guide.md` — this order's own write-back — leaves all three reused**; and adding one argument to a suite command invalidates all three. That is criterion 4's "and nothing else" clause tested against real declared inputs.
5. **Cross-session reuse, measured.** For nine suites I computed this session's key and asked the shared cache for a matching success: **0 of 9 available**. The cache holds 69 records. This is VER-001 O6 and [D009](../../evidence/WO-130/decisions.md#wo-130-d009) observed once more from a third role, and it is why the gate below runs every task fresh.
6. **The retained host measurement, audited rather than trusted.** `replica-measurements.json` has 78 task rows, 47 with `executionRoot: "replica"` and 31 `candidate`, 40 release cases, and no non-zero exit; one verified read-only installed copy of 50,685,411 bytes in 1,099 files in 240.348 ms; 22,665.920 ms of per-replica setup; the fresh full gate at 798.414 s, exit 0, tree `5f536a49…`; and the source-change `npm test` at 51.917 s with 9 fresh and 3 reused tasks, 199,889 ms of package execution retained, restoring the fresh tree hash exactly. These are the executor's timings collected by the runner under review; I did not re-measure them, and their full and fast inventories differ, so they are not a speedup ratio.
7. **Screens.** `git diff --check` is clean. `package-lock.json` changes only skeleton's version; no dependency was added. No lint, type or coverage suppression directive appears anywhere in the diff. No AI attribution appears in any authored surface. Clean-room screen: no employer material, credentials, internal service identifiers or key material in the order, decisions, evidence, measurements, transcripts or verification reports; **no absolute home path occurs anywhere in the committed WO-130 surface**, including the `safe.directory` values VER-001 turned on, which it elided. No stop condition.

## Acceptance criteria

1. **Replica construction and the execution boundary — met.** `createReplicaContext` refuses a scratch root inside the candidate tree, copies the installed roots once per gate, verifies every copied file's digest and mode against the observation before making the copy read-only, and builds each replica under a nonce-named parent created `0o300`. The criterion-1 fixture asserts the replica's contents entry-for-entry against `replicaEntries`, the preserved `0o751` mode, an in-set symlink's contents, installed roots mounted as symlinks to the single copy, `src/main.js` absent, `EACCES` on listing the parent and on writing into the mounted `node_modules` from inside the child, and that the serialized `{cwd, argv, env}` contains no occurrence of the candidate tree path. My own check 3 confirms the environment half against the real repository. The accepted absolute-path residual of receipt 011 is unchanged and is not counted against this criterion.
2. **Guarded reads and the escaping link — met, and this is where the receipts held twice.** One synthetic suite per guard runs under `existsSync`, `existsSync && statSync().isFile()`, and a read selected by the declared flag `docs/product/02-domain-model.md`, across four candidate-tree states of `undeclared.txt` (absent, present, present as a directory, changed). All twelve runs exit 0, print `skipped`, and share one key: the file does not exist in any replica, so no guard can distinguish the states. Flipping the declared flag changes the key while the fresh replica run still reports `absent`, and deleting the undeclared file afterwards leaves that key and output unchanged. A fifth case refuses narrowing for a declared symlink whose target leaves the replica, for a leaking argument and for a leaking environment value. Receipts 009 and 010 are answered by construction, not by probing.
3. **Migrated scopes — met.** `kernel`, `compiler`, `skeleton`, `worktree`, `plan-refutation:fixtures`, `process-debt`, `release:prepare` and all 40 release cases execute in replicas; the audited measurement records each outcome with its setup cost, and no required scope was retained whole-tree. The remaining suites keep their whole-tree policy in the candidate tree, which is WO-131's scope.
4. **Key content — met.** The narrowed key is the declaration digest (carrying `version: 2`, the command, the load policy, the execution root and `replicaMechanismVersion`), the declared entries' bytes, types and listings, the installed roots, the toolchain, the host Git and npm configuration the replica's own children still read through `HOME`, and the projected replica environment with `<replica>` and `<suite-temporary>` placeholders. Checks 3 and 4 above establish both halves against real inputs, and the fixture adds mode, rename and mechanism-version invalidation plus the `FRESH` explanation naming the exact changed path. The host-configuration class is stricter than the criterion's list rather than looser: it is a real input of the suite's own `git` and `npm` children, and it can only force execution, never admit an unsound reuse.
5. **Loud failure without fallback — met.** The unconditional undeclared read passes in the candidate tree and then fails the gate with `Replica suite kernel failed; first unreadable path: … undeclared.txt. No candidate-tree fallback.` `executeSuite` is called once with the replica root; a setup or cleanup error becomes failed evidence rather than a candidate-tree rerun. Release preparation cannot carry an undeclared guarded read into the template its cases share.
6. **Operator-host measurements — met**, audited at check 6 and not re-measured. The record refuses the speedup reading in its own `limits` block.
7. **Write-backs and checks — met.** 07 §Discipline carries the replica paragraph, the injected-Git-configuration rule, the refused-narrowing contract and the `format` scope change, plus the new §"The phrase records its own dispatch"; `docs/evidence/WO-130/decisions.md` carries D001–D015 with dispatch, evidence, rejected alternatives and reopening conditions, including four same-day corrections in misread/meant/changed form; the decisions index has all fifteen rows and the follow-up register all fifteen `decision:` entries. `npm test` is green in this session's own environment — check 1 is the file VER-001 found red, and the canonical gate below is the whole of it. `git diff --check` is clean and no dependency was added.

## Earlier findings

- **VER-001 F1 (blocking) — closed.** The narrowing refusal on the injected Git configuration family, and the total loss of reuse behind it, are gone: the family is dropped from the reviewed projection in execution and key alike, and a refusal is now keyed under the pre-order whole-tree contract with the reason named on the task row and a key that can never coincide with a replica key. Reproduced here by checks 1, 2 and 3 in the failing environment.
- **VER-001 F2 (non-blocking) — closed.** Every fixture asserts replica creation, naming the refusal, before using a replica; the deterministic-repository case can no longer run `git` in the real worktree.
- **VER-001 O1, O2, O4, O5 — addressed** by the keyed refusal, the name-level drop that also removes `GIT_CONFIG_PARAMETERS` from gate children, `GIT_CEILING_DIRECTORIES` at the unlistable parent with its own fixture, and a direct assertion on the installed-copy change detector. **O3** (declared directory modes keyed but built with the umask) and **O6** (per-session environment) remain documented limits; O3 is strict in the safe direction and O6 is re-measured at check 5.
- **VER-002 F1 (blocking) — closed.** The prompt dispatch is now modelled as the equivalent `npm run resume -- <action>` invocation and judged by the same active-gate refusal and the same compiled writer-isolation unit the tool path uses; the real-lifecycle fixture asserts byte-identical refusal reasons, unchanged events, projection, checkpoint refs, report allocations and phase, and no reservation. Reproduced here at check 2.
- **VER-003 F1 (blocking) — closed.** A resumed dispatch is delivered exactly like a recorded one through the read-only `npm run resume -- briefing` projection, and a lifecycle that cannot project the briefing refuses the phrase instead of passing it silently. Reproduced here at check 2, and by this session itself: the dispatch that opened this review was recorded by the hook and its briefing named the allocated FINAL path.

## Review changes

- **None. One attempted write-back was reverted, and the reversal is itself evidence.** `docs/product/06-roadmap.md` §Release boundary carries no WO-130 activation-completion entry, although the standing release-assignment discipline records the base and classification there and every recent released order has one. I wrote that paragraph, and the canonical gate's publication preflight then failed in 29 s: the everyday-AI-user edition pins `06-roadmap.md#roadmap--application-release-ladder`, whose subtree contains §Release boundary, so one paragraph made a reader-facing edition's freshness lock stale. Keeping the paragraph would have required re-pinning a publication edition this order never touched. I reverted it — `docs/product/06-roadmap.md` is byte-identical to `HEAD`, and both editions check `CURRENT` — and carry the gap as an observation below instead. The subject this review passes is therefore exactly the subject VER-004 verified, with no reviewer edit in it.

## Gate

The canonical gate for this review is `npm run harness -- evidence` on the tree that contains this report, the PR body and the release notes; the runner's record for those exact bytes is the completion authority, and `final-review-result pass` refuses without a passing `npm run test:full` and `git diff --check` at that tree. Check 5 predicts what it will do: this session's projected environment differs from every recorded gate's, so all 78 tasks execute fresh rather than composing — VER-001 O6 and D009 measured a third time, not a defect of this order. This report claims no gate timing of its own beyond that record. The first attempt at this gate (21:08:06Z) failed in 29 s in the `publication` preflight on the stale lock described under "Review changes", with `git diff --check` clean in the same run and 24 costly suites stopped before they started; that is the preflight behaving as D009 records it, and the reverted paragraph is the whole of the repair.

## Observations (non-blocking)

- **The terminal receipt names no supports for the verifier and reviewer roles.** `equippedSupports` parses the briefing for `… is equipped`, and only the executor entry briefing enumerates supports; a `verify` or `final-review` dispatch therefore prints `equipped supports: none named by the briefing`, as this session's own dispatch did, even though the generated role skill equips Process Cost and Goal Alignment. The wording is honest and the fixture asserts it deliberately, but D013's stated goal is that the operator sees which supports are equipped, and for two of the six roles the receipt cannot show that. D013's own reopening condition — "a role's briefing names supports in a form the receipt does not parse" — is the right place for it.
- **Deadline diagnostics no longer survive the gate on disk.** The scheduler's peer file and deadline log moved from `docs/control/local/harness/deadlines/<timestamp>-<pid>/` into the per-gate replica scratch, which is correct — the gate must not write into the candidate tree it is fingerprinting — but the context is removed when the gate ends, so only the parsed `hit` rows retained in the gate record survive. Nothing in the order or 07 promises the transcripts themselves, and the gate row keeps what a reader needs; it is a change of retention that a future deadline diagnosis should know about.
- **Three of the six migrated scopes gain little reuse.** `worktree`, `process-debt` and `plan-refutation:fixtures` declare `packages/`, `scripts/` and the root configuration files — 308 files each — so almost any source change still re-executes them. That is correct for what they read and the order's benefit claim is scoped to the package suites, but it bounds how much of the gate's wall-clock this mechanism can remove; the heavy suites are exactly these three.
- **Cross-role reuse is still zero** (check 5). Until per-suite environment declarations land, every independent role pays a full fresh gate, which is most of this order's measured cost in practice. D009 files the proposal with its evidence.
- **The roadmap has no WO-130 activation-completion entry.** The standing release-assignment discipline records a completed activation target's base and classification in `docs/product/06-roadmap.md` §Release boundary, and WO-125, WO-128 and WO-129 each carry one; WO-130 assigns `v0.17.5` in its heading, the README claim, `docs/evidence/current.json` and the work-order index, but not there. It is not one of this order's acceptance criteria, and as "Review changes" records, adding it here would have pulled an unrelated publication edition's lock out of date. Release-close owns the release boundary and is the right place to add it, or the next order that touches a linked roadmap section can carry it with the re-pin.

## Goal alignment

Mission and critical path: the independently verified source-to-deliverable loop needs evidence that is dependable and gates that do not re-run work nothing changed. This order delivers the first half without qualification — an undeclared file cannot influence a narrowed run through any guard, because it is absent in every run — and the second half narrowly but really: the three package suites, 200 s of execution on the operator's host, now survive unrelated script changes, which check 4 confirms against this order's own documentation edit. **NoOp** was live at two points and rejected both times on evidence: passing after VER-001 would have handed the next role a red gate, and failing now would relitigate four rounds of closed findings that I reproduced myself. **Naive Interventionism** is respected: the scheduler, suite inventory, exact-tree aggregate, before/after input check, seals and publication controls are untouched, the whole-tree class still runs in the candidate tree, and my own attempted review change was withdrawn the moment it cost an unrelated publication surface its freshness. Against the eight traps at this scope: **rule beating** is the trap this order exists to answer and it is answered by construction rather than by a passing probe, since the fixtures cannot pass without the replica existing; **policy resistance** appeared exactly as VER-001 described — a correct refusal in one guard silently disabling reuse in another — and the repair makes a refusal keyed and named instead of total; **shifting the burden to the intervenor** was closed twice, first by removing the undocumented `env -u`, then by making the dispatch and its briefing harness-owned rather than remembered; **escalation** was avoided in D012 by routing the new dispatch through the guards that already existed instead of adding a third; **commons depletion** is one 50 MB copy per gate at 240 ms against 22.7 s of per-replica setup and 200 s of avoided package work on the measuring host, and a pure cost in any session that cannot reuse; **drift to low performance** is checked by the 78-task fresh gate rather than by focused fixtures alone; **success to the successful** is checked by comparing the replica path against the retained whole-tree contract, which a refusal still falls back to; **seeking the wrong goal** is the one to keep watching, because a green gate in one harness stood in for reuse in another until VER-001 measured it, and check 5 says that gap is still open in a different form.

## Costs and publication

Process cost, measured at report preparation: **16,988,737 total tokens** — 16,660,646 cached input, 288,203 cache-write, 39,724 output, 164 uncached input — over 104 steps and 103 commands; source `claude-transcript-message-usage`, scope `dispatch`, observed 2026-09-13T21:02:46.719Z from the harness session begun at 20:57:43.647Z. No separate entry observation was collected, so no entry-to-handoff delta is claimed; the completion command recollects a later cutoff. The dominant term is cached input re-read across one long reasoning window, and this is cumulative usage, not live context occupancy; wrapped command count and dollar cost are unavailable. The window includes useful work and waiting: 111.9 s of suite-evidence fixtures, 28.9 s of process-debt fixtures, about 3 s of direct key probes, a 29 s canonical gate stopped at its publication preflight by my own reverted paragraph, and the canonical gate that followed it. Tradeoff, chosen deliberately: rather than re-run the executor's fresh full gate and source-change comparison — roughly twenty minutes to re-observe numbers my own canonical gate corroborates on the fresh side, and which check 5 says my session could not reuse anyway — I spent about 140 s on the two fixture inventories that decide the four closed findings and about 3 s on three direct probes of the real repository's keys, which is the evidence a fixture cannot give. No equivalent alternative review was measured, so I make no efficiency claim.

Publication: local application target `v0.17.5` (patch) above annotated `v0.17.4`; skeleton `0.15.4` through `0.15.8`; compiler `0.9.1`, kernel and console unchanged; no new dependency. The reviewed [PR body](PR.md) and [release notes](RELEASE-NOTES.md) sit beside this report. Suite successes recorded before this change never match the new declaration and mechanism versions, so every inventoried suite executes once more after it lands. Publication beyond opening this order's pull request belongs to the separate release-close dispatch.
