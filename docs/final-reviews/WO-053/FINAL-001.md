# WO-053 FINAL-001 — the first live external source change passes review; both harnesses' receipts, the bounded writer repairs and the write-backs publish as v0.29.3

**Verdict: pass.** All six acceptance criteria hold on the current bytes, and
the claims that carry this order I re-derived myself rather than adopting the
verifier's word for them: the parent-handoff growth in each of the three
successful receipts equals the byte length of its own recorded envelope plus a
newline (532, 490 and 523), every one of the nine receipts resolves its
`loadoutHash` to one of the three filed loadout editions in the split the
README claims, the protected launchpad, target-main and sentinel snapshots are
byte-identical before and after in every successful episode, the killed-host
receipt records one `WorkerAttemptStarted`, one commit and a `SIGKILL` before
any durable source-change result, and no receipt carries an absolute private
path or credential shape. The three runtime repairs the operator's 2026-09-17
override admitted are the smallest change that makes the live episodes pass
and each one tightens or leaves unchanged the writer contract: a data-only
result tool is classified `read`, display prose can no longer stand in for a
structured result, and the fixture names its exact test executable. `main` did
not move during verification, so there is no integration to perform and this
report judges the same base VER-001 judged. The one product gate is re-run by
this review on the staged tree — **26 suites, 0 failed, 812.79 s, 70 fresh tasks, exit 0**. No blocking or substantive
finding survived review, and no behavioural code was written by it.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.275","model":"claude-fable-5-1","effort":"xhigh","source":"operator-attested"}

## Subject and base

Operator dispatch: `resume: final review`, recorded at `2026-09-18T00:19:57.036Z`
as `FinalReviewRequested`, allocating FINAL-001 through VER-001 and checkpoint
`refs/dotln/checkpoint/WO-053/5` (`150b366c`). The subject is
[WO-053](../../work-orders/WO-053-first-external-source-change.md) as filed at
dispatch — its objective, its six acceptance criteria, its non-goals, the
2026-09-17 operator override recorded in its classification and criterion 6, and
its `Execution readback (2026-09-17)` paragraph — together with the executor's
[D001–D003](../../evidence/WO-053/decisions.md). One root coding agent performed
this review; no subagent was used and no implementation was changed.

**One base.** VER-001 judged branch `wo-053` at
`8471dc7e42aadfa64206771dae09541d6b1942b6`. At this dispatch `HEAD`, local
`main`, `origin/main` after a fetch, and the merge base are all that same commit,
and the annotated tag `v0.29.2` points at it on origin. No sibling published
during verification, no merge was performed, and the preserve → merge → apply →
regenerate procedure product 07 names for a moved upstream did not apply. The
branch carries no commit: `git rev-list --count main..HEAD` is `0`, so every byte
of the subject is uncommitted work preserved through the five recovery
checkpoints, and this review commits it after the pass below.

**Extent.** Against that base the tree at entry changes 40 tracked files,
+553/−387 by `git diff --stat`, plus the five paths that were untracked at entry:
`docs/control/orders/WO-053.jsonl`, `docs/evidence/WO-053/`,
`docs/final-reviews/WO-053/`, `docs/verifications/WO-053/` and
`packages/skeleton/test/source-change-receipt.test.ts`. VER-001 counted +552.
Rather than guess which line accounts for the difference, I diffed the
verifier's checkpoint `refs/dotln/checkpoint/WO-053/3` against this dispatch's
`refs/dotln/checkpoint/WO-053/5` over every tracked path outside the report
directories: only `docs/control/current.md`, `docs/control/orders/WO-053.jsonl`
and `docs/work-orders/README.md` differ, all three regenerated by the lifecycle
transitions between the two dispatches. No source, test, receipt, fixture or
evidence byte differs from what VER-001 read.

**Ideation receipt.** The order has no order-local ideation record. Its
nomination provenance is the 2026-09-08 critical-path planning pass, gate D2,
which places WO-053 as the first external change after WO-052 and names the
second replan checkpoint R2 as taking its receipt together with WO-111's; the
planning map and the critical-path document now carry that checkpoint's one-line
WO-053 disposition, and I read both.

## Acceptance, judged on the current bytes

What I re-established myself is marked *re-derived*; what I checked by reading
the diff and the verifier's evidence is marked *confirmed*; nothing is carried
forward on VER-001's word alone.

**1 — per-harness writer smoke; host red/green, isolated one-file commit,
envelope-only handoff. Met, on both harnesses.** *Re-derived:* for
`claude-schema.json`, `codex-runtime.json` and `codex-recovery.json` I computed
`Buffer.byteLength(JSON.stringify(envelope.value) + "\n")` and compared it with
`parentHandoff.afterBytes − beforeBytes`; all three match exactly (532, 490,
523) with `beforeBytes` 0. `fixture.mjs` opens that transcript with the `wx`
flag and appends every child-stdout chunk to it, so the number is a measured
file size, not a recomputation. *Confirmed:* each of the three records
`boundary.value.unchanged: true` with `before` and `after` byte-identical across
`launchpad`, `targetMain` and `sentinel`; host `testBefore.exitCode` 1 and
`testAfter.exitCode` 0, both with null signal; `changedPaths` exactly
`["sum.mjs"]`; `commitCount` 1; the envelope's `observedCommit.sha` equal to the
host observation's `commit` and to the independent `gitEffect.value.commit`.
The recorded `launchpad.head` in all three is `8471dc7e…`, this branch's `HEAD`,
which ties the episodes to the checkout under review. The envelope carries the
six contract fields plus the two host observations `observedCommit` and
`observedDenials`; the loadout's `outputContract.required` names exactly the six.

**2 — killed-host recovery by commit identity, exactly one commit. Met.**
*Re-derived from `codex-recovery.json`:* `recovery.first.signal` is `SIGKILL`
with null exit code; `interruption.sourceObserved` and `sourceReceiptPresent`
are both false, so the kill preceded any durable host result; `second.exitCode`
is 0 after the real lease expiry; `recoveryByCommitWithoutRedispatch` is true;
`host.attempts` is 1 and `host.commitCount` is 1; `interruption.commit` equals
`host.outcome.observation.commit`. The event set contains `WorkerLeaseExpired`
and no `WorkerResultObserved`. `fixture.mjs` counts `attempts` from the decoded
store's `WorkerAttemptStarted` events, not from the deduplicated `eventTypes`
set, and its recovery wrapper throws on any second dispatch, so a redispatch
could not have produced this receipt.

**3 — every claim labeled. Met.** *Re-derived:* across all nine receipts every
non-metadata top-level fact carries `observed`, `launch-claim` or `unknown`;
`launch.effectiveReadback` is `unknown` in every one, so the installed version,
model and effort are never promoted past launch claims. `receipt.mjs` enforces
both, and requires an `unknown` fact to carry a null value and a reason.

**4 — a fixture pins the receipt shape. Met, and it rejects forgeries.**
*Confirmed by reading:* `receipt.mjs` pins `schemaVersion` 1, a closed top-level
key set, closed nested sets for `host`, `boundary`, `parentHandoff`, `recovery`
and `usage`, the successful-run invariants, the recovery invariants and the
privacy screen; `source-change-receipt.test.ts` requires a clean success per
harness and a successful requested recovery, and for every passing receipt
requires each of six mutations to throw — inflated `commitCount`, nulled
`testBefore.exitCode`, reddened `testAfter.exitCode`, and a rewritten commit sha
in the envelope, in `gitEffect`, and a rewritten branch — plus the extra-field,
label-deletion, private-path and forged-`functionalPass` rejections. *Executed:*
the test runs inside the `skeleton` suite of the gate below.

**5 — write-backs. Met, all five.** *Confirmed in the diff:* the roadmap's
source-to-deliverable rung opens with the live-evidenced sentence and keeps the
remaining vertical marked planned; the root README's release block replaces the
pending-proof sentence with one sentence pointing at the receipts; the
capability table's `worker.source-change` row reads `1 — demonstrable,
live-evidenced`, keeps target 2 and efficiency E0, and names what remains
unproven; the R2 checkpoint has a one-line WO-053 disposition in both
`work-order-map.md` and `critical-path-2026-09-08.md`, declining to infer an
export decision or a family waiver while WO-111 is outstanding; and, for an
order filed before 2026-09-09, the ledger duty is discharged by
`docs/evidence/WO-053/decisions.md` with D001–D003 in the decisions index and
the follow-up register, the substitution marked in the work-order index.

**6 — gate green, whitespace clean, no dependency change, bounded repairs,
inspection profile pinned. Met.** *Executed by this review:* `npm test -- --review` — 26 suites passed, 0 failed, 812.79 s (812,794 ms), 70 fresh tasks, exit 0, recorded `2026-09-18T00:39:01.270Z` for tree `7e4a0f17` at code identity `8df3152f`; the `skeleton` suite that carries this order's tests passed in 235.97 s. On the same tree `npm run publication:check`, `npm run format:check`, `node scripts/harness.mjs check`, `node scripts/console-fixtures.mjs --check`, `node scripts/authority-evidence.mjs --check` and `node scripts/feedback-evidence.mjs --check` all exit 0.
`git diff --check` and `git diff --cached --check` are clean. *Re-derived:*
`package-lock.json` changes exactly one line, the workspace's own
`@dotln/skeleton` version from `0.25.1` to `0.25.2`; no dependency entry moves.
*Confirmed:* the three runtime changes are the `StructuredOutput: "read"`
inventory entry in `harness-command.ts`, the Claude launch and decode change in
`worker-transport.ts`, and the `inspectionInstructions` field plus the
sharpened `outputInstructions` in `worker-protocol.ts`; the test double
`fixtures/writer-cli.mjs` gains the `missing-structured` and
`invalid-structured` negative behaviours and now emits non-JSON display text
beside `structured_output`, reproducing the live failure; the pinned inspection
baseline `fixtures/wo051-inspection-baseline.json` is not in the modified set.
The full diff introduces no `eslint-disable`, `@ts-ignore`, `@ts-expect-error`
or `@ts-nocheck` and no attribution trailer or footer.

## The bounded repairs under the operator override

The order as nominated forbade runtime change and deferred any defect to a
separate order. On 2026-09-17 the operator corrected that disposition during
`resume: next` (D002): the live episodes had found two blockers with no repair
owner, and atomic orders remain the default while a direct override keeps the
repairs for the defects impeding this proof inside it. The classification,
placement, design bullet, criterion 6 and non-goals in the order file were
edited to record that override, and the `Execution readback` paragraph was
appended; the pre-override wording is preserved at `main`. I judge the order as
it stood at dispatch, which is what VER-001 judged, and I read the six retained
failure receipts to confirm the override's premise rather than its narrative.

The premise holds. `claude-clean` and `claude-diagnostic` committed `sum.mjs`
but returned display prose where the transport required JSON (`invalid-result`,
`writer wire JSON`; the diagnostic's wire facts show 93 valid stream lines, a
successful terminal and a `non-json` result). `claude-fixed` and `claude-json`
returned without `structured_output`, and the executor's local journal read
showed the target guard refusing the native `StructuredOutput` submission as an
unknown tool; the public `claude-json` receipt keeps only the wire structure.
`codex-clean` returned a `blocked` envelope because the prompt authorized only
the three effectful commands while the harness needed to read; `codex-fixed`
reached inspection and editing, then reported exit 127 for a bare `node` the
worker environment could not resolve, with no commit. Each repair answers one
of those observations and nothing wider:

- `harnessToolEffects.StructuredOutput = "read"` admits a tool that carries
  data and cannot execute or mutate; the emitted-hook regression in
  `source-change-host.test.ts` now asserts the admission and that an unknown
  output tool still refuses, and the three effectful Bash routes are unchanged.
- The Claude launch moves from `stream-json --verbose --include-hook-events` to
  `--output-format json --json-schema <schema>`, and `decodeWriterResult` now
  rejects any terminal lacking `structured_output` or `permission_denials`
  instead of parsing `result` prose. This is the one change to a previously
  pinned canonical launch shape, and the pin in `writer.test.ts` is updated in
  the same diff; the denial count still comes from `permission_denials`, which
  the live `claude-schema` receipt confirms is present in aggregate mode.
- `inspectionInstructions` authorizes native reads or, when native tools are
  unavailable, a literal list of read-only shell commands run one at a time
  with no composition; `outputInstructions` now says the three effectful
  commands are the only ones authorized. This is prompt text. Enforcement
  remains the `dotln-writer` workspace-write profile with network disabled on
  Codex, the compiled guard on Claude, and the host's before/after checks; the
  skeleton README, the evidence README and the receipts all say so.
- The fixture supplies `process.execPath` as the exact focused-test executable
  and reduces it to `<node-executable>` in every public receipt.

I compared this with the alternatives D002 and D003 reject and with NoOp. NoOp
would have published a proof with two of three required live outcomes missing,
which is a drift to a weaker success definition. A tolerant parser that accepted
prose, or a broader shell allowlist, would have widened the contract to make the
evidence pass; the chosen repairs narrow it. The residual risk of intervening is
that the Claude launch shape changed under a pinned test, and that risk is
carried by the updated pin, the new negative double behaviours and the live
receipt on the new shape.

## Verification sequence

The sequence is one report. VER-001 (`2026-09-18T00:14:12.616Z`, verdict pass,
attested `claude-code 2.1.275`, `claude-opus-5[1m]`, effort `unknown`, source
`claude-session-readback`) verified the executor's `ImplementationReady`
(`2026-09-17T23:55:54.343Z`, attested `codex-cli 0.154.0`, `gpt-6-astra`,
`xhigh` with mode `subagents`, raw `ultra`, source `codex-session-readback`). No
repair event exists. Elapsed implementation is 3,354,678 ms and verification
1,009,072 ms by the control record.

VER-001's judgment stands on every point I re-derived. Its byte counts, its
loadout-hash split, its privacy grep, its reading of the `package-lock.json`
line and its account of the four runtime sources agree with the bytes. It
disclosed that the regenerated hooks governing its own session were part of its
subject and checked their diff for logic; I repeated that check by classifying
every changed line in the thirteen hook files and the manifest: each is a
snapshot directory, `skeletonVersion`, per-file `fnv1a64` pin, or the added
`StructuredOutput: "read"` inventory entry. Its verifier effort was recorded
`unknown` because no effort was supplied to that dispatch; that advisory is
accurate and does not touch the verdict. I have no correction to make to it.

## Findings, observations and limits

No blocking or substantive finding. The observations below are recorded so a
reader does not have to re-derive them, and none routes to repair.

- **Who launched the episodes.** Operator-review assumption 1 expected the
  operator to run the episodes from an outside terminal. The order's own
  `Execution readback` records that the authorized executor launched both
  harnesses from its unrestricted host terminal and states this is executor
  action, not a claim that the operator typed the commands. The containment
  claims rest on host observations — snapshots, test exits, Git reads — that
  do not depend on who invoked the fixture, and the receipts carry no launch
  identity beyond launch claims. This is a disclosed deviation from an
  assumption, not from a criterion.
- **What the proof bounds.** Protected-checkout and sentinel equality before
  and after establishes a bounded observed effect, not universal filesystem
  confinement and not fencing of a still-running orphan: the kill lands after
  the worker has exited. The parent-handoff measurement is the evidence
  collector's child-stdout sink; growth of the complete executor conversation is
  `unknown` in every receipt. The capability row, the README and the receipts
  state each of these limits, and the roadmap sentence claims only what they
  support.
- **Launch identity.** Harness version, model and effort in every receipt are
  launch claims with `unknown` effective readback, exactly as criterion 3
  requires. Cost figures are transport-reported counters for the successful
  episodes only; the six failed attempts add unrecorded cost and no efficiency
  claim is made anywhere in the subject.
- **Two early receipts lack the optional `wire` diagnostic** (`claude-clean`,
  `codex-clean`) because the executor added that reduction after them.
  `receipt.mjs` makes `wire` optional; those receipts remain valid, retained
  failures.
- **Retiming.** The order activated on the commit that carries the published
  `v0.29.2` tag, so the executor retimed the target to `v0.29.3` under the
  existing patch classification with a dated roadmap note and D003. That is
  the sibling-release bookkeeping product 07 names, not a finding; this
  review's `npm run release -- prepare` against origin's tag observation reports `WO-053 target v0.29.3 remains current; no files changed`.
- **Instruments in the subject.** The thirteen generated Claude hooks and the
  manifest are regenerated by this order and governed this session; their diff
  is pin-only, as above. `scripts/test-runner.mjs` is not modified by this
  order.

## Release, publication and the commit series

The classified release is application `v0.29.3` with skeleton `0.25.2`; kernel,
compiler and console versions, event schemas and dependency sets are unchanged.
The README claim, the order heading and the roadmap's activation-completion and
retiming notes agree on that target, and the fresh WO-053 authority and feedback
editions selected by `docs/evidence/current.json` bind the changed runtime while
artifact-identity and verification stay on their passing WO-122 editions.

No release dry run was attempted. This dispatch authorizes committing reviewed
state, pushing the `wo-053` branch and opening its PR; it does not authorize a
merge, a push to `main`, a tag, a Release or a package publication, and the
release helper's own preflight is the post-merge close's evidence. The PR body
beside this report is reviewer-authored and carries the process-meter table the
release-preparation step supplies plus the gate record above. That meter's
corrections column reports 2 for this order, counted from the session journal
by `correctionCounts`; the operator's 2026-09-17 correction is the one recorded
in D002, and I make no further claim about the journal's count. The five-section
release notes follow the publication contract with one physical source line per
prose paragraph and disclose the bounded scope of the proof. The executor's
provisional drafts of both files are replaced, not inherited. Correction, same
session: the first `worktree publish` attempt was refused by its own
`check-surfaces` preflight before any push, because the notes' redaction sentence
carried the literal angle-bracket placeholder token and the GitHub body profile
admits no raw HTML in reviewed notes; the sentence was reworded to describe the
token instead of quoting it, and this paragraph records that the earlier claim
that the notes already met the profile was wrong at the time it was written.

The PR title's gitmoji is `:sparkles:`, chosen from the catalog for a capability
that now exists where before it was pending, and the title is sized from this
diff against the whole merged series rather than the previous subject. The
commit series separates the runtime repair with its tests, double, README and
version bump; the regenerated bundle pins and fresh evidence editions; the live
proof with its fixture, receipts and loadout editions; the product and planning
write-backs; the lifecycle records; and the PR body with the release notes.
After the PR merges, the operator dispatches `resume: release close` in `main`.

No temporary repository or registered worktree was created. Recovery
checkpoints, ignored intake and local journals are retained. The writer
reservation (`actorId a93c2768…`, owner pid 17934, source `CLAUDE_PID`,
reserved `2026-09-18T00:19:56.375Z`, `alive: true`) is this worktree's single
reservation from entry through handoff and lapses with this process.

## Cost and instrument disclosure

Process cost at entry: 7 steps, 6 commands, `totalTokens` 170,206 (inputTokens
68, cachedInputTokens 146,566, cacheWriteInputTokens 22,364, outputTokens 1,208);
source `claude-transcript-message-usage`, scope `dispatch`, cutoff
`2026-09-18T00:20:19.337Z`; `reasoningOutputTokens` and `costUsd` null and
therefore unknown. At handoff: 63 steps, 54 commands, `totalTokens` 2,560,492 (inputTokens 456, cachedInputTokens 2,327,506, cacheWriteInputTokens 184,646, outputTokens 47,884), same source and scope, cutoff `2026-09-18T00:39:13.523Z`, read after the gate and before the result was recorded; the counters at the moment of handoff are in this dispatch's ignored receipt and in the response. The single review gate
dominates this review's cost and was not optional: the passing row is the
reviewed code identity the release manifest consumes, and both earlier rows were
recorded by other actors. Reading receipt bytes and recomputing three byte
counts and nine hashes cost seconds; relaunching live vendor episodes would have
cost minutes of paid inference per attempt and is outside this dispatch's
authority, so the recorded episodes were judged and not repeated.

The writer-reservation and gate-input guards that governed this session are
regenerated by this order, and they shaped the method as designed: every shell
command was refused by run id and pid while the review gate was live, so the
drafting during those minutes went through the file tools into the ignored
scratchpad and every Git-based check ran before or after the gate. No check was
skipped for that reason.

Actor disclosure, field by field. The harness version `2.1.275` was read back
from `claude --version`. The effort `xhigh` was read back from `CLAUDE_EFFORT`
in this process's environment. The model `claude-fable-5-1` is the model the
operator selected in this session immediately before the dispatch; no
effective-model readback channel is available, which is why the source is
`operator-attested` rather than a readback label. Node is `v22.2.0`, Git
`2.55.0`. The order recommends `reviewer any`, so no effort advisory applies.
Under review: the executor attested `codex-cli 0.154.0`, `gpt-6-astra`, `xhigh`
with mode `subagents` and raw `ultra`, source `codex-session-readback`; the
verifier attested `claude-code 2.1.275`, `claude-opus-5[1m]`, effort `unknown`,
source `claude-session-readback`. The live episodes recorded their own actors as
launch claims — `claude-fable-5` on Claude Code `2.1.274` and `gpt-6-astra` on
Codex CLI `0.154.0`, both at `xhigh` — with only the CLI versions read back
from the binaries.
