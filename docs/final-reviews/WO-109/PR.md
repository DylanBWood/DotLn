## What this merges

WO-109, a documentation and research order closing without a release below published `v0.14.0`. It establishes the Re-mining Well as a documented practice and finishes its first pilot draw: twenty root stones from the frozen intake snapshot and their ledger and product descendants, read through four lenses at declared scales, with every one of 416 coverage cells given a terminal disposition. The measured result is a strict local yield of zero against a threshold of two, so no identical second draw is recommended; the two historical tensions found (analogy-to-universal-mechanism overclaims in old image descriptions) are routed to a later ledger append rather than rewritten in place. Under the operator's 2026-09-07 scope amendments the draw also preserves 166 research records from public book and interaction research and a concept-registry proposal that the planning map now points to. The ledger, the product docs the draw compared against, and every runtime package are unchanged by the draw.

- **What the practice fixes.** Raw material stays in the main checkout's ignored intake; committed records carry opaque references and rewritten prose only. Strata are kept distinct so descendants of one source cannot corroborate it, and every unit ends in one of ten terminal dispositions including honest no-yield.
- **What the draw did not do.** No ledger append, product promotion, runtime, schema, or control-plane change; no supersession proposal; no automation. The mixed-note quarantine was dispositioned by the operator into granular generalization, and the verifier confirmed the committed output carries the portable capability and none of the private specifics.
- **Bookkeeping in this PR.** The omitted activation target is completed at `v0.13.3` with a dated roadmap note. The operator's 2026-09-07 correction against ratchet creep (one-way drift across consecutive artifacts, sized against their predecessors) is recorded in the execution guide, the publication doc, and the playbook as a separate commit.

## Evidence

- Every `npm test` step passed on the reviewed tree (`check-surfaces --local`, the publication and index checks, 302 of 303 package tests, the evidence checks, and the mutation self-tests) except the console board's live host-collection case, which timed out under host load twice (about 80 s against the collector's 60 s limit) and passes alone in 23 s; that case exercises no change in this PR and is the open hardening item WO-032 recorded. `git diff --check` clean; Prettier clean.
- Subject identity: the working tree at dispatch differed from the implementation-ready checkpoint only in control bookkeeping and the verifier's report; all five checkpoints have `497a24f` (`main`, `origin/main`, `v0.14.0`) as their sole parent.
- Screens on every changed file: 179 local links and anchors resolve; the operator's private clean-room term list finds zero word-boundary matches; no locator, hash, absolute path, or credential pattern appears in any draw artifact.

## Verification trail

- `docs/verifications/WO-109/VER-001.md`: pass; source manifest, output delta, backup, counts, citations, images, chats, notes, and the quarantine record independently re-established; two findings, both documentation.
- `docs/final-reviews/WO-109/FINAL-001.md`: pass; both findings closed, one operator correction recorded, the four-commit series confirmed.

## Known open items

- Founding reconciliation is partial and stays that way until operator or original-source provenance answers it: two unnamed founding notes, four image references with ambiguous associations, one image without a founding reference.
- A later in-slot integration order owns the ledger append of the two tensions, any promotion, and the registry decision; `next-moves.md` seeds it.
- The relocated duplicate intake copy beside the checkouts is the operator's to dispose of.
- Dropping the process-correction commit requires re-refreshing both edition source locks.
- The live host-collection test's 60 s collector timeout is load-sensitive (WO-032 open item); a per-command timeout, serial execution of that case, or a cheaper `release list` remains a hardening candidate.
- After merge, `resume: release close` runs the printed subject-helper command, which prints the no-release result and creates no tag or Release.
