# WO-111 FINAL-001 — final review

**Verdict:** pass. WO-111 asked for the first unattended live proof: with the operator marked away, the resident derives 5S work in a scratch repository under the progressive curve, completes and verifies each change, and stops on return. That happened, twice, and the retained raw stores say so. In each of two human-marked windows the WO-119 producer discovered all six seeded candidates, the resident derived three orders and advanced probe → widen → peak with changed-path counts 1/1/2, a live Codex worker made each change in an isolated worktree, and a fresh-transport verifier passed each one before the curve advanced. In the second window the human `back` edge arrived 252,399 ms before the next dispatch was due, with a fourth candidate eligible and one episode of budget left; replaying the retained log with and without that edge shows the returned resident has no due dispatch and rejects one, while the no-return control admits it. One clause of criterion 2 was not observed: both windows added scratch-target trust entries to the operator's user-level Codex configuration, a path outside the portfolio. VER-001 and VER-002 failed on it. The operator then explicitly accepted that disclosed deviation for this synthetic proof and kept its prevention in the independent runtime follow-up `FUP-3c34a8ffbf61376f` ([D020](../../evidence/WO-111/decisions.md#wo-111-d020)); VER-003 passed on that basis, and this review passes on the same basis. The order text, both receipt editions and both failed verdicts are unchanged: this is an operator's acceptance of a recorded exception, not a changed observation. `main` moved by WO-114 (`v0.47.0`) during the order, so this review integrated it, resolved one authored conflict in the roadmap without touching a source line, reproduced both corrected receipts byte for byte on the integrated runtime, and passed the product gate on the staged integrated tree. This review found no acceptance defect.

**Subject:** [`docs/work-orders/WO-111-unattended-live-proof.md`](../../work-orders/WO-111-unattended-live-proof.md) on branch `wo-111`, uncommitted at dispatch, judged at checkpoint `refs/dotln/checkpoint/WO-111/13` (`92fa79b0`), which is the tree VER-003 passed (RepairCompleted tree `1677a4b5`, checkpoint 10) plus the projections the final-review dispatch regenerated, then integrated with `main` at checkpoint `refs/dotln/checkpoint/WO-111/14` (`f5edffd5`). Original base `4b6a19cc50e98a437d35b2adf2d392fbf5b12139` (`v0.46.2`, WO-156); fetched `main` `76d28aff98d12d32aa956ee65d4bd0a91a12f691` (`v0.47.0`, WO-114), which `git fetch` observed before the integration and `git ls-remote origin refs/heads/main` returned after it on 2026-09-25. The target is `v0.47.1` under the declared patch classification; the second repair retimed it from `v0.46.3` when the sibling's `v0.47.0` became the observed baseline, and `release prepare --local` reported it current after the integration. The judged evidence blobs are the two corrected receipts `receipt-v2.json` `bba60339…` and `return-receipt-v2.json` `2ae97a1c…`, their event projections `events-v2.json` `3177fd44…` (1,731 rows, SHA-256 `30c90380…`) and `return-events-v2.json` `b14ac1bc…` (435 rows, `eed8729e…`), and the four original editions (`receipt.json` `dbe7d755…`, `return-receipt.json` `7d6c3f7a…`, `events.jsonl` `88d0dec4…`, `return-events.jsonl` `28166ae0…`); each is byte-identical to the VER-003 checkpoint (`refs/dotln/checkpoint/WO-111/12`). The order's only change outside `docs/` and `README.md` is two entries in the kernel test fixture `packages/kernel/test/fixtures/jsonl-protocols.json` (3 insertions, 1 deletion). The judged tree is the working tree with every WO-111 file staged; the reviewer gate ran at tree `c9355af397d4b092e7103888a78cb8b00b905d96`, code identity `03806e95fc3cc8dd2142119203fbd335526505a4b2315fb8c8ca5f19c5cfa61b`. Nothing was committed, pushed or published before the result transition.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-fable-5-1","effort":"xhigh","source":"claude-session-readback"}

Human actor: the operator dispatched `resume: final review` in a Claude Code session and made no other choice during the review. The harness version is from `claude --version`. The model is the session's selected model. Effort `xhigh` is the host's `CLAUDE_EFFORT` value read by this session: selected effort, not effective. The order asks for `reviewer any`. No subagent was planned or spawned: 0 of the cap of 20 (`harness usage`: exact-observed, no descendants). The subject is a documentary and evidence order whose every load-bearing claim already had two independent reproductions (the executor's collectors and VER-001's four-agent verification, then VER-002's and VER-003's re-checks), and the offline receipt reproduction runs in seconds, so one reader could hold the whole diff and re-run the instrument. This session ran no provider episode and relaunched no live window.

**Process cost:** entry 85373 tokens; handoff 5713997 tokens; source claude-transcript-message-usage

Both readings are `dispatch` scope from `node scripts/harness.mjs usage 21c79321-c9d9-4a4e-bc41-a07a4972d9df`, the entry reading observed at 2026-09-25T00:32:27.772Z and the handoff reading observed at 2026-09-25T00:55:00.533Z, taken after the judgment text and the gate figures were complete and before the result transition; the only later edit was the document-gate line in §Checks. They include reused cached input. Reasoning tokens and dollar cost are unavailable from this counter, which means unknown, not zero. The largest single wait was the reviewer's product gate (297,571 ms). Final counters stay in ignored receipts and the handoff response.

## Goal-aligned judgment

The order sits on the critical path's first thesis: the vision's first sentence names a runtime, and until this order no unattended work had ever been done by DotLn while the predecessor `v1` does it daily. Its value is the observation itself, not the three scratch fixes: that the compiled curve, the portfolio host, the live source-change primitive and the live verifier compose into one loop that runs without the operator and stops when the operator returns. The mistakes most likely here were a receipt that reads as more than the windows show, and a pass obtained by relabeling an observed failure. The first was VER-001's whole major and minor list and is repaired at source: every `observed` value in the v2 editions is computed from the retained logs by `repair-receipts.mjs`, which this review re-ran on the integrated runtime and which reproduced both editions byte for byte; the mission check's separate store and subject, the caller's abort, the snapshot coverage, the generator provenance and the unexercised in-flight clause are each disclosed in the receipt rather than in a report sentence. The second was attempted in the second repair (an amendment of criterion 2 and v3 receipt editions that reclassified the trust-entry write as accepted) and was rejected by the operator as rule beating; D019 restored the original criterion and withdrew the editions, and D020 recorded what the operator actually decided. **Rule beating** was therefore this review's main lens: the pass must rest on the operator's explicit decision, not on a changed standard, and the record must let a later reader see the difference. It does. The receipts still say `criterion2: "unmet; no operator exception recorded"` (they are the editions VER-002 judged and D020 keeps them unchanged), the order carries its original criterion, the D017 `PlanExecutionAmended` row stays in the append-only planning log as superseded history and `plan check` accepts the restored order as the original release assignment, and README, product 06 and D020 say in the same sentence that the clause was not observed and that the operator accepted the deviation for this synthetic proof. Product 07's [acceptance rule](../../product/07-execution-guide.md#discipline) is the authority: only an explicit operator decision lowers a standard already in force, and D020 is that decision, recorded with the operator's words and its reopening conditions. **Drift to low performance:** the standard is lowered for this scratch proof only; general containment is claimed nowhere, the capability table keeps `runtime.resident` at level 1 within stated bounds with criterion 2 unmet, and D020 reopens the moment a launch touches an operator-owned repository. **Shifting the burden:** the isolation defect is not left to manual settings cleanup; D011's follow-up owns the launcher fix for every Codex launch path, at high priority because it touches operator tool settings, and D014 keeps it independent of this order's outcome so neither waits on the other. **Policy resistance / fixes that fail:** the disposition and the runtime follow-up have disjoint scopes, so accepting the deviation here cannot undo the fix there. **Tragedy of the commons:** the repairs were offline replays of retained stores; no third live window, no model episode and no subagent was spent after VER-001, and this review spent none. **Escalation:** no new gate, agent or ritual was added; the order's evidence tooling is two callers and three collectors under `docs/evidence/WO-111/`, and the only package-area change registers two evidence streams in an existing test fixture. **Success to the successful:** an isolated-home rerun (D016) was compared and declined on evidence (the fresh isolated Codex home was not authenticated, and a rerun would cost another human presence window), not because the current launch path had investment. **Seeking the wrong goal:** the objective is a dependable unattended loop, and the observed loop is bounded, verified and stoppable; the windows were about 10.8 and 8.9 minutes, not the title's hour, and no net-value curve was measured, both of which the write-backs say. **Naive Interventionism:** this review changed no behavior; its only edits are the roadmap resolution, the integration decision and the record. **NoOp** at review would leave a passing, independently verified proof unmerged while WO-112, WO-118 and the owner-repository portfolio wait on its receipt. Judged at handoff, the outcome supports the order's goal within its stated bounds; its limits (one launch-isolation defect, no in-flight return exercised, no hour, no descending half of the curve, unknown tokens) are recorded where the next reader will look.

## Criteria

| Criterion | Judgment and evidence |
| --- | --- |
| 1 — producer discovery; at least three derived orders completed in ascending phase with verified results; first touching one file; reset on a failed verification | **Met.** VER-001 read the raw portfolio stores: one `discovery` script episode ran WO-119's `discovery-cli.js` over the target and returned six candidates with per-check exit codes and a corpus hash; each `PortfolioOrderActivated` carries that episode's id and a `portfolio:gardener-5s@1` source; WO-900 to WO-902 ran as live `codex-cli-exec` workers with separate fresh-transport verifiers whose authority denies `repo.write`; read-only `git diff base..commit` gives `src/main.js`, `src/main.js`, then `docs/guide.md` plus `loose/guide.md` (a rename), commits `a4dcfbab`, `e8413d5c`, `eb046ebc`, three `pass` verdicts with no findings. The second window repeats the pattern (1/1/2) and leaves the repeated-repair candidate eligible. This review's `repair-receipts.mjs check` on the integrated runtime rebuilt both v2 editions exactly from those stores, asserting six launch records per window and the progression fields. No verification failed, so the reset branch was not exercised; the compiled curve's reset-on-failure rule is WO-067's, not this order's. |
| 2 — main checkout hash, sentinel tree and every path outside the portfolio unchanged; no remote effect | **Measured parts met; the outside-portfolio clause was not observed and is accepted by the operator (D020).** Main HEAD `4b6a19cc…` before and after with a byte-identical recomputed snapshot; sentinel, target main, template and launchpad configuration unchanged; zero Git remotes on both targets and both launchpads before and after; each episode branch changed only its declared surfaces. Both v2 receipts record `outsidePortfolioChange.changed: true` for the user Codex configuration (trust entries keyed on the two random scratch roots only this order's seed created; attribution to the Codex launch is an inference from the keys, the in-window modification time and the inherited Codex home, labeled as such). `snapshotCoverage` names the ignored writes the checkout equality excludes (harness records in the ignored control lane, the first window's mission store, target `.git/info/exclude` worker blocks), and `outsideHostFilesystem` stays `unknown` for everything else. The pass rests on the operator's explicit acceptance in D020, confirmed in VER-003's session; §Findings reads that decision. |
| 3 — mission check fired at least once with its verdict recorded; return stopped dispatch within one tick with the in-flight episode handled per the phase | **Met.** One `mission-check` episode dispatched on the 900 s cadence in the separate mission store and returned `drift` over `contract:surfaces` (the WO-111 worktree's diff edited `docs/lineage/decisions-index.md`, which the typed surfaces omitted), with `MissionDriftObserved` appended; the v2 receipt names the judged subject (the DotLn worktree, not the scratch orders), the separate store and that the hold refused no work. Return: in the second window the human `back` is row 432 of 435; at that edge the replayed machine moves from `probe` to `present`; `due()` is null at `dueAt − 1`, `dueAt` (1790286813701) and `dueAt + 1000` with the edge and returns `dueAt` without it; a probe dispatch folded on the returned state is rejected and the control dispatch is accepted. The caller aborted 348 ms after `back`. Neither return had an episode in flight (`pendingEpisodeCount: 0` in both receipts), so the in-flight clause was not exercised; both receipts say so and cite the compiled rules (`discovery` kill, `probe`/`widen`/`peak` finish) and the fake-actor `resident.test.ts` fixture that covers them. In the first window the portfolio had already stopped on its exhausted three-episode budget, so its return causality is correctly `unknown`. |
| 4 — every claim labeled `observed`, `launch-claim` or `unknown`; budget consumed reported with its method | **Met.** Every top-level field in both v2 receipts carries a label. Budget: replay method stated, 3 of 3 episodes and 182,200 ms folded wall time in the first window, 3 of 4 and 183,249 ms in the second, ceilings from the recorded portfolio definition, `tokens: 0` with `actualEpisodeTokens` `unknown` because zero reported is not zero used. Six per-episode launch claims per window (three `fresh-cli` writers, three `fresh-transport` verifiers: Codex CLI 0.156.1, `gpt-6-sol`, `xhigh`) plus the mission launch, each with effective identity `unknown`. Presence authorship is a `launch-claim` grounded in D003's operator-message record, because the classifier assigns `human` by signal kind alone. |
| 5 — write-backs: README "What runs today", 06 dated rung, capability table (`runtime.resident`, `worker.source-change`), ledger, replan checkpoint answered in the planning map | **Met.** README §What you can run carries the runtime sentence with both receipts, the separate mission resident, the trust-entry deviation and D020, and the limits (no installed service, no net-value curve). Product 06 carries the dated "WO-111 live rung observation (2026-09-24)" with the same scope. The capability table's dated reassessment holds `runtime.resident` and `worker.source-change` at level 1 within stated bounds. The ledger duty is discharged by the decisions file and its index rows, the pre-2026-09-09 substitution the work-order index marks; the operator-authorized presence-curve clarification sits in newest-first position and `lineage` passes. `work-order-map.md` carries the dated "WO-111 R2 checkpoint disposition" linking the combined answer in `critical-path-2026-09-08.md` §Stop and replan points (cost/session comparison, integration-package growth, remaining questions); the WO-053 line now points to it. |
| 6 — `npm test` green; `git diff --check` clean; no runtime source, generated configuration or dependency change | **Met.** `npm test` passed at every phase: executor 27/0/71 (346.79 s), VER-001 (300.50 s), repair 1 (291.53 s), VER-002 (305.50 s), repair 2 (288.98 s), VER-003 (291.34 s); the reviewer's `npm test -- --review` passed on the staged integrated tree (§Checks). `git diff --check` is clean before staging, after integration and at the result transition. The order's diff outside `docs/` and `README.md` is the two fixture-registry entries; `packages/*/src`, runtime `scripts/`, `.claude/`, `.agents/`, the harness manifest, `package.json` and the lockfile are untouched by this order (their differences from the original base are `main`'s WO-114, consumed as they are). The `docs/evidence/WO-111/*.mjs` callers and collectors import the built runtime and are evidence tooling. |

The **evidence gate** is satisfied: the receipts (both editions, both windows) and one `npm test` at this review. The **non-goals** hold: no operator repository was touched (both targets are random scratch roots with zero remotes); nothing was published before this review's authorized pull request; no runtime fix was made (the isolation defect is boarded up, not patched). **Operator-review assumption 1** holds as a launch claim: the operator ran both windows outside the sandbox and supplied both away/back pairs, recorded in D003. **Assumption 2** held twice: VER-001 and VER-002 kept their receipts under this order's evidence and left the order open, and no receipt was withdrawn or overwritten.

## Findings and readings

No acceptance defect. The readings below were checked at source and change nothing.

- **What the pass rests on, exactly.** The outside-portfolio clause of criterion 2 is contradicted by the receipts. The lifecycle offered two honest routes: an isolated live rerun (D016) or an explicit operator decision. The operator declined the rerun (the fresh isolated Codex home was unauthenticated and a rerun needed another human presence window), rejected the paper amendment and the v3 editions (D019), and directed that the disclosed trust-entry side effect be treated as an accepted deviation for this synthetic proof with prevention in `FUP-3c34a8ffbf61376f` (D020), then confirmed the pass in VER-003's session. That is the explicit operator decision product 07 requires to lower a standard already in force, and it lowers it for this proof alone. A reader who wants the literal criterion will find it unmet in both receipts; a reader who wants the disposition will find D020 linked from README, product 06 and VER-003. D020's reopening conditions (an operator-owned repository, the follow-up's isolation test, any further outside-portfolio effect) are the right ones.
- **The correction chain is preserved, not hidden.** D013 withheld the first repair's handoff on an invented dependency and D014 corrected it. D016 routed VER-002's blocker to an isolated rerun and D017 then amended the criterion instead; D019 restored it and D020 recorded the operator's actual disposition. The D017 amendment row (2026-09-25T00:06:09Z, order hash `74a31ec3…`) remains in `docs/control/plan-refutations.jsonl` and `plan check` exits 0 on the restored order (release-assignment entry `v0.47.1`, current source hash `3d58f2c3…`). `repair-002.md` lists the six errors of that session in order. Nothing in the chain rewrote a receipt or a verdict.
- **Three register rows are superseded corrections.** `npm run meta` minted `FUP-f3281eb085ad87a7` (D016), `FUP-dcadda81305b4fb7` (D017) and `FUP-e468ee64ac00c70a` (D019) from their `reopens` objects. Each is superseded by the next decision and by D020, and none names work anyone should do. They are the planner's to dispose at the next pass; a reviewer does not write register dispositions. The scope-expansion edits also returned `FUP-0083`, `FUP-0108` and `FUP-0110` to needs-review, as `implementation.md` records; no disposition is inferred here.
- **Genuinely open follow-ups.** `FUP-3c34a8ffbf61376f` (D011, high): launch Codex worker and verifier episodes so the user configuration stays byte-identical, add a before/after digest to live receipts, correct `docs/evidence/WO-054/codex-continuation.md:116-117`, and check whether the Claude transport leaves the same class of state; the operator decides separately whether to remove the existing entries (the repair's read-only census found 36 DotLn scratch/probe entries among 43 trusted projects). `FUP-441469088613e52a`: the full rising-and-descending absence-curve experiment, a planning candidate with a falsifiable matrix and no timings. `FUP-7dbf4e832a8fb124` (D015): n1 is corrected in D018 and `repair-002.md` (46 → 102 recursively, 42 → 93 top-level; the historical planning pass keeps its bytes); n2, the second window's `snapshotCoverage` naming the first window's mission store, remains an acknowledged documentary slip in the retained v2 edition, because a truthful correction would need a v3 edition and the operator rejected new editions in this order.
- **The title's hour does not bind; the criteria do.** The windows lasted 648,313 ms and 536,565 ms. No numbered criterion sets a duration, the objective asks for "one bounded window" whose window and budget are the portfolio's, and the operator who supplied both `back` edges chose their length; D003 records the executor's correction of its own earlier sixty-minute claim. The write-backs say "not a full hour requirement". This review reads the title as descriptive, as VER-001 N1 left open.
- **"Ascending phase" is phase order, not file count.** Widen's ceiling equals probe's (one file), so scope grows only at peak, and nothing was merged so the post-peak re-discovery reports all six candidates again. The phases ascended; the ceilings are the portfolio author's data. The compiled fixture's 1/3/8 shape is a different portfolio.
- **The replay instrument is the runtime under examination.** The cancellation claim replays the retained log through `replayResident`, `residentMachine`, `portfolioSelection` and `residentRefusal` from the built skeleton. That is the right instrument for a claim about what this runtime's fold does, and VER-001's checker recomputed every pre-back value independently. It is not an independent policy model, and both receipts say so. This review ran it on `main`'s WO-114 runtime (skeleton `0.40.0`, which changed `resident-store.ts` and `resident-host.ts`) and both editions still reproduce exactly, so the carried-forward replay claims hold on the integrated build.
- **Two dated planning surfaces read as status.** `work-order-map.md`'s WO-111 disposition says "the document repair is ready for re-verification", and the capability table's rows, dated 2026-09-24 before D020, cite the containment failure without the disposition. Both are dated records that were true when written, and the R2 answer and reassessments they carry are unchanged by D020. The next planning pass owns any refresh; this review does not edit planner-owned documents.
- **VER-001 N4 stands.** The illustrative Blackjack +3 figures ($5, +$5 per loss, $45) peak near 8.5 losses, not twelve; every changed file disclaims them as the operator's recollection, not game mathematics, and the time-away-only mapping and the future automatic-selection placement are consistent across README, products 03, 05 and 06, ADR-0007, the inspirations row, the ledger and the planning candidate. The clean-room screen found no employer material, private path, credential or identifier in the new evidence, reports or diff lines (this review's grep over the WO-111 directories and the planning candidate, and VER-001's and VER-002's scans).
- **Independence across the sequence.** Implementation on Codex CLI 0.156.1 / `gpt-6-sol`; the first repair on `gpt-6-astra`; the second on `gpt-6-sol`; all three verifications on Claude Code 2.1.282 / `claude-opus-5-5[1m]` (VER-001 with four subagents in one workflow, the others with none); this review on Claude Code / `claude-fable-5-1`; all at `xhigh`. Every verdict was independent of its implementation actor in model and harness, and this review used a third model.

## Integration with `main`

`main` gained WO-114 (`v0.47.0`, minor: the resident publishes a `runtime-status-v1` file the console renders; skeleton `0.40.0`, console `0.2.0`; authority edition `WO-114/002` and feedback edition `WO-114/001`; regenerated harness hooks; 62 files). `npm run worktree -- integrate WO-111 --intake-backup` naming the session scratch archive `DotLn-wo111-intake-20260925T004000Z.zip` (3 files) checkpointed the work (`refs/dotln/checkpoint/WO-111/14`), retained the include-untracked stash `e15b9196` (`WO-111 integrate 2026-09-25`), fast-forwarded the uncommitted branch from `4b6a19cc` to `76d28aff` and re-applied the stash. Seven generated projections were re-merged by the helper (`README.md`, the control projection, the decisions index, the follow-up register by id union, both publication locks, the work-order index). One authored conflict, `docs/product/06-roadmap.md` §Release boundary, was resolved by this review: both dated paragraphs are kept, WO-111's activation and collision-retiming paragraphs above WO-114's activation paragraph, matching the section's newest-first order. No source file conflicted. `--continue` rebuilt the runtime, re-emitted the harness bundle (31 surfaces), refreshed the control projection, index, meta and publication locks, regenerated the selected console fixtures and wrote the draft D021; `release prepare --local` reported `WO-111 target v0.47.1 remains current; no files changed`.

Carried-forward claims: every acceptance claim VER-003 judged is carried with its original evidence, because the integration changed no receipt, event stream, collector, caller, write-back sentence or order byte of this order; the eight evidence blobs above are identical to checkpoint 12, and the staged diff against `main` outside `docs/` and `README.md` is the same two fixture-registry lines the order had against its original base. The replay-based claims were additionally re-established on the integrated runtime by `repair-receipts.mjs check`. No component version changed in this order, so there is no collision: `main`'s skeleton `0.40.0`, console `0.2.0`, compiler `0.18.0` and kernel `0.6.0` are consumed as they are. No registered evidence source changed; `docs/evidence/current.json` is `main`'s (authority `WO-114/002`, artifact identity `WO-154/001`, verification `WO-154/001`, feedback `WO-114/001`), so nothing is re-minted and no live episode ran. The affected checks the helper printed all pass on the integrated tree (§Checks). [D021](../../evidence/WO-111/decisions.md#wo-111-d021) records the bases, the resolution and this assessment.

## Verification sequence

1. **Implementation** (Codex CLI 0.156.1, `gpt-6-sol`, `xhigh`, [record](../../evidence/WO-111/implementation.md)): the seed generator, the live caller binding the existing resident, portfolio, source-change and verification hosts to two Codex CLI transports, the mission caller, two operator-marked windows, the original receipts and sanitized streams; D001–D010 including the operator's Blackjack +3 clarifications (D004, D006–D009) and the full-curve planning candidate; `npm test` 27/0/71, 346.79 s.
2. **[VER-001](../../verifications/WO-111/VER-001.md)** (Claude Code 2.1.282, `claude-opus-5-5[1m]`, `xhigh`, four subagents): fail. B1 the Codex trust entries (criterion 2), B2 the replan checkpoint not in the planning map, B3 a red document gate (ledger order; two unregistered JSONL streams); M1 the unexercised in-flight clause, M2 the misdescribed mission composition, M3 literal `observed` labels and missing per-episode launch claims; m1–m8. D011 boarded the runtime isolation defect as `FUP-3c34a8ffbf61376f`.
3. **First repair** (Codex CLI 0.156.1, `gpt-6-astra`, `xhigh`, [record](../../evidence/WO-111/repair.md)): the v2 editions by offline replay with per-episode launch claims, computed budgets and the no-return control; the two streams registered as non-`EventEnvelope` fixtures; the ledger reordered; R2 answered in the map and the critical-path plan; the living claims narrowed; the [trust diagnosis](../../evidence/WO-111/codex-trust-diagnosis.md); D012–D014; `npm test` 27/0/71, 291.53 s.
4. **[VER-002](../../verifications/WO-111/VER-002.md)** (Claude Code, `claude-opus-5-5[1m]`, `xhigh`, no subagents): fail. B1 carried, because the subject's own receipts record `criterion2: unmet`; n1 the mixed file enumeration and n2 the second window's snapshot list boarded as D015 (`FUP-7dbf4e832a8fb124`); the filed route corrected after filing at the operator's direction (D016).
5. **Second repair** (Codex CLI 0.156.1, `gpt-6-sol`, `xhigh`, [record](../../evidence/WO-111/repair-002.md)): the D017 amendment and v3 editions attempted, rejected by the operator and withdrawn (D019); D018 preserved the historical planning pass and recorded the corrected counts; D020 recorded the operator's acceptance of the disclosed deviation; retimed to `v0.47.1`; `npm test` 27/0/71, 288.98 s.
6. **[VER-003](../../verifications/WO-111/VER-003.md)** (Claude Code, `claude-opus-5-5[1m]`, `xhigh`, no subagents): pass on criteria 1, 3, 4, 5 and 6 and on criterion 2's measured parts, resting on D020 for the outside-portfolio clause; `npm test` 27/0/71, 291.34 s; `test:docs` 21/0.
7. **This review**: integration with `main`, the readings above, the receipt reproduction on the integrated runtime, and the product gate on the staged integrated tree.

## Checks run by the reviewer

- `npm test -- --review` on the staged integrated tree, run normally with no sandbox in force (the sandbox probe wrote): **28 suites, 0 failed, 297.57 s, 72 fresh tasks, exit 0, not partial, tree `c9355af397d4b092e7103888a78cb8b00b905d96`, code identity `03806e95fc3cc8dd2142119203fbd335526505a4b2315fb8c8ca5f19c5cfa61b`, recorded 2026-09-25T00:53:19.004Z**. `--review` added the `registrations` machinery suite to the 27 product suites because its declared source, the kernel fixture registry, changed.
- `git diff --check`: clean before staging, after integration and at the result transition.
- `node docs/evidence/WO-111/repair-receipts.mjs check` on the integrated runtime: exit 0; `receipt-v2.json` 1,731 rows, 6 launches, `check: true`; `return-receipt-v2.json` 435 rows, 6 launches, `check: true`; both editions regenerated byte for byte from the retained ignored stores, original stream digests asserted, the no-return control asserted.
- Blob identity of the eight evidence editions against `refs/dotln/checkpoint/WO-111/12` (VER-003): all identical.
- `npm run plan -- check`: exit 0; 27 receipts, 17 passes; WO-111's release assignment `v0.47.1` and its dated capability reassessment are listed as committed updates of receipt 027's subject; the D017 amendment row remains in the log as history.
- `npm run publication:check`: PASS; both editions CURRENT (30 and 45 linked sections); `node scripts/harness.mjs check`: 31 generated surfaces; `npm run release -- check-surfaces --local`: 0 FAIL; `npm run test:docs`: 21 passed, 0 failed, 23.18 s, 21 fresh tasks, exit 0, run after the cost line was filled with only this sentence still unfilled, and again with the same pass and fail counts on the final bytes before the result transition.
- `git ls-remote origin refs/heads/main`: `76d28aff`, equal to the fetched base, after the integration.
- Clean-room screen: `grep` for private path, credential, email and name patterns over `docs/evidence/WO-111/`, `docs/verifications/WO-111/`, `docs/final-reviews/WO-111/` and the planning candidate: the only matches are the two verification reports' descriptions of their own scans and the guard expression in `repair-receipts.mjs`. The intake archive stayed in the session scratch directory.
- Reads at source: the order; VER-001 to VER-003 in full; D001–D021; the implementation, both repair records and the trust diagnosis; both v2 receipts; `seed.mjs`, `baseline.mjs`, `run.mjs`, `mission.mjs` and `repair-receipts.mjs`; the whole authored diff against `main` (README, ADR-0007, products 03, 05 and 06, the ledger, the inspirations row, the capability table, the critical-path plan, the planning map, the fixture registry, the planning log, the publication locks, the follow-up register); the cited product sections (00 §The one-paragraph story, 03 §Operator-presence policy, the WO-053 receipt README, 07 §Goal-aligned decisions and §Independent workflows and integration, 08 §PRs and commits).

## Handoff

The result is `final-review-result pass`. After the transition the reviewer refreshes the index, commits the reviewed state as three commits and runs `npm run worktree -- publish WO-111` with the committed [PR body](PR.md) and the [release notes](RELEASE-NOTES.md) beside this report. The three commits are: the live proof (the seed generator, the callers, the collectors, both receipt editions and streams, the planning candidate and the fixture registration); the write-backs (README, products 03, 05 and 06, ADR-0007, the ledger and its index, the inspirations row, the capability table, the critical-path plan, the planning map, the order's target and the refreshed publication locks); and the record (decisions, the trust diagnosis, the implementation and repair records, the verification reports, this review, the control events, the planning-log row, the follow-up register and the work-order and decisions indexes). The helper's post-merge release-close handoff goes to the operator verbatim. Open after this order: `FUP-3c34a8ffbf61376f` (runtime launch isolation, high), `FUP-441469088613e52a` (the full absence-curve experiment) and `FUP-7dbf4e832a8fb124` n2; the three superseded correction rows and the three reopened register rows are named above for the planner. Checkpoints 1 to 14 and the integration stash are retained.

## Reproduction

From the `wo-111` worktree root:

```text
npm test -- --review
node docs/evidence/WO-111/repair-receipts.mjs check
for f in receipt-v2.json return-receipt-v2.json events-v2.json return-events-v2.json receipt.json return-receipt.json events.jsonl return-events.jsonl; do git rev-parse "refs/dotln/checkpoint/WO-111/12:docs/evidence/WO-111/$f"; git hash-object "docs/evidence/WO-111/$f"; done
npm run plan -- check
npm run publication:check
node scripts/harness.mjs check
npm run release -- check-surfaces --local
npm run test:docs
git ls-remote origin refs/heads/main
git diff main --stat -- packages scripts .claude
```
