# WO-150 FINAL-001 — the executor's economy experiment becomes default equipment on one boolean and one briefing line; the cost is 1,479 measured bytes per non-opted-out dispatch, and the reopening observation is deliberately one the passing fixtures cannot satisfy

**Verdict: pass.** All five acceptance criteria are met at the reviewed tree and no finding routes to repair. I did not adopt VER-001's account of the numbers. I recounted the cold start myself from the installed bytes (`CLAUDE.md` 6,113 + executor `SKILL.md` 18,214 = 24,327 against the 24,576 ceiling, within by 249), hashed all twelve installed role files and found each `.claude` file byte-identical to its `.agents` twin, walked the baseline fixture chain by hand (`wo150` → `wo079` → `wo146` → the immutable v0.33.2 `wo145` snapshot), reproduced both focused cases, and re-derived the three-trial reading from the WO-145, WO-110 and WO-099 experiment records rather than from this order's summary of them. The reading holds with room: the trials cost 163.546 + 87 + 2.73 = 253.276 s in total, and WO-110's adopted 238.436 s per skeleton test iteration clears that whole cost after two iterations in the next ten orders, with `regression: false` in all three records — so the pre-registered rule passes even on the strictest possible subset, one trial. Review added one measurement the order's cost line does not carry: D007's briefing line is 306 UTF-8 bytes, so the per-dispatch cost of the flip is 1,479 bytes, not 1,173. That is recorded as [WO-150-D008](../../evidence/WO-150/decisions.md) with a named follow-up and written back into product 05, because a recurring cost that only a report sentence carries is the thing the write-back duty exists to prevent. `main` did not move, so no integration was needed; the product gate ran once on the reviewed, staged tree and passed **28 suites, 0 failed, 497.64 s, 72 fresh tasks, exit 0**.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"unknown","source":"claude-session-readback"}

**Process cost:** entry 199064 tokens; handoff 12408783 tokens; source claude-transcript-message-usage

The human actor is the operator, who dispatched `resume: final review` in this worktree; `claude --version` reports 2.1.278 and the session reports its model as `claude-opus-5[1m]`. No effective effort readback exists in this harness and nobody supplied one, so `effort` is `unknown` rather than the order's "reviewer any"; the model is session readback, so the pair records `claude-session-readback`. Counters came from `node scripts/harness.mjs usage 5063ea9b-82af-4526-bf57-45c7c3a6c4ec`, scope `dispatch`, source `claude-transcript-message-usage`; `costUsd` and `reasoningOutputTokens` are null and therefore unknown, not zero. Before any fan-out I planned zero admissions against the root session's 20-agent `subagentCap`, because every check here is one file or one command and the single gate sets the pace regardless; none was spawned, the counter reports 0 of 20 `exact-observed`, and the uncounted remainder is unknown. The dispatch briefing's own observed-facts block reported background tasks, gate durations and every usage counter as unavailable at 16:52:59Z; the usage command answered a few seconds later, so that block's `counter-unavailable` was a timing artifact of the session journal, not a missing counter, and this report uses the command's readback.

**Read scope.** The order in full, including its cost line, design, five criteria, non-goals and two operator-review assumptions; its cited sections — `executor-supports.ts` (`tinkererEconomy`, `executorSupportDefaults`, `defaultExecutorSupportIds`, `executorSupportIds`), `executor-supports.test.ts`, the WO-145 role-baseline case in `scripts/test-process-debt.mjs`, WO-145 D001 to D003, WO-099 D008, product 05 §Candidate — Tinkerer / Scientist, `docs/control/budgets.json`, product 07 §Goal-aligned decisions; product 08 §PRs and commits and §Release-note edition; the ideation lineage, which is refutation receipt [023](../../planning/refutations/2026-09-21-planning-1457ac11ba15715d-023.md) and its six WO-150 known issues in the validated JSON result; VER-001 in full at its current bytes; this order's `implementation.md`, `decisions.md` D001 to D007, `cold-start.json`, the authority and feedback evidence editions including both self-host JSONL streams; the WO-110 and WO-099 experiment records read directly rather than through this order's citation of them; and the complete subject diff against `main`.

## Subject

| Item | Value |
| --- | --- |
| Work order | `docs/work-orders/WO-150-tinkerer-economy-default.md`, minor, application `v0.39.0` |
| Branch / worktree | `wo-150` at `/Users/dylanwood/Projects/DotLn-wo150`; physical cwd and Git root confirmed equal; this session is the sole registered writer |
| Base | `main` at `cd361f17fd2d80e21c1ee019945f3a81c36f34f4`; `git ls-remote`-backed `origin/main` is the same object and the branch carries no commit, so the review base and the integration base are one commit and **no integration was required** |
| Verification base | VER-001 judged the staged tree at `refs/dotln/checkpoint/WO-150/3` (`f5de3b90`); between that checkpoint and this review only `docs/control/current.md`, `docs/control/orders/WO-150.jsonl`, the generated work-order index and VER-001 itself changed, so no subject byte moved between verification and review |
| Reviewed code identity | `d1c9a28118ddd32c5fc1a2d54e0f6eabf4fe80873f343bbde801a47c2257f335` |
| Diff shape | 58 paths, 4,354 insertions, 341 deletions. Hand-authored source is five files: one boolean in `executor-supports.ts`, one branch in `scripts/lib/executor-readiness.mjs`, the two test cases, and the new `wo150-role-baseline.json` fixture. Everything else is the regenerated bundle (12 role files, 14 hooks, the harness manifest), the version and pin triple, the evidence editions and the document projections |

## Acceptance, judged at the reviewed tree

| # | Criterion | Judgment |
| --- | --- | --- |
| 1 | Defaults include `tinkerer-economy`; `contributorConfiguredProgram({})` equips it and `{ "tinkerer-economy": false }` removes it with every other support unchanged; the two generated skill roots are byte-identical | **Met, reproduced.** `executorSupportDefaults["tinkerer-economy"]` is `true` and `defaultExecutorSupportIds` carries six ids. The focused case passes and asserts, in both directions, that the opt-out removes this support alone: the component manifest gains and loses the entry, `executorSupportIds({ "tinkerer-economy": false })` equals the defaults minus that id, the executor procedure contains the paragraph exactly once on and never off, and every other support's presence in that procedure is equal on both sides while the authority envelope, work order, non-procedure facets and all five other roles stay deep-equal. I hashed all twelve installed role files myself: each `.claude/skills/dotln-*/SKILL.md` is byte-identical to its `.agents` twin. The objective's briefing clause is met by D007's branch, which the bare-dispatch case now requires on and forbids under the opt-out |
| 2 | Both roots measured for every role against the ceilings; the executor figure stated with its verdict; a breach raised under the standing route with a dated acceptance, never trimmed | **Met, recounted independently.** My own `wc -c` reproduces `cold-start.json` exactly: executor 24,327 of 24,576 (within by 249), verifier 21,061 of 25,151, reviewer 22,458 of 24,576, release-close 13,967 of 16,384, planner 15,033 of 24,576, refuter 15,690 with the ceiling unset; identical in both roots, with `CLAUDE.md` unchanged at 6,113 bytes and every non-executor delta zero. Nothing breached, so the empty `ceilingChanges` and `acceptancesAdded` are correct and `docs/control/budgets.json` is untouched — which is the right outcome, because the standing route raises a ceiling for a reviewed rule that needs it, not in advance of one |
| 3 | The WO-145 baseline case passes re-baselined: default-on files match this release's bytes, opt-out files match the previous default apart from the Origin comment | **Met, chain walked by hand.** `wo150-role-baseline.json` holds twelve current hashes and pins `wo079-role-baseline.json` by SHA-256; `wo079` in turn pins `wo146`, and the separate v0.33.2 release snapshot in `wo145-role-baseline.json` is untouched and still checked against Git history. The case now asserts every recorded `historicalSha256` in that chain instead of one. The opt-out reproduces all twelve previous-default files byte for byte including the Origin comment; default-on differs from the opt-out by the executor's 1,173 bytes and, in the other five roles, by the whole-loadout Origin line alone — which the case now asserts in both directions, so an equipment change that failed to move the semantic hash would fail |
| 4 | Write-backs land: product 05, the decisions file with the three trials' history and the operator's acceptance, the decisions index refreshed | **Met, and extended by this review.** Product 05 carries the dated WO-150 paragraph; D001 records the accepted flip and its four rejected alternatives, D002 the baseline strategy, D003 the cold-start decision, D004 the carried history, D005 the served order classes and the reopening observation, D006 the re-minted evidence editions, D007 the briefing repair. I re-derived D004's history from the source records: all three trials are dated 2026-09-20, all three `adopted`, all three `regression: false`; WO-145 and WO-110 carry `history` 2026-09-20/0 and WO-099 carries null/null, exactly because it ran unequipped, which its own follow-up states. Review added D008, the briefing-line measurement, and one sentence to product 05; `npm run meta` refreshed the index to eight rows and the follow-up register |
| 5 | `npm test` green; `git diff --check` clean; no new dependency | **Met, executed here.** Gate below. `git diff --check` and `git diff --cached --check` are clean. The manifest and lockfile change only the internal `@dotln/skeleton` 0.33.1 → 0.34.0 and the console's exact workspace pin; no external package moves, and the diff introduces no `eslint-disable`, `@ts-ignore`, `@ts-expect-error`, `prettier-ignore` or `biome-ignore` |

## Gate and affected checks

Every reviewed tracked change and the one intended new source file were staged before the gate, and it ran outside the harness sandbox from the start.

- `npm test -- --review` — **28 suites, 0 failed, 497.64 s, 72 fresh tasks, exit 0**, recorded 2026-09-21T17:03:53.545Z at code identity `d1c9a28118ddd32c5fc1a2d54e0f6eabf4fe80873f343bbde801a47c2257f335`, tree `4f7322fab02a68e6b858852aa3e3a86e620d724f`.
- The document edits this review then made are confined to `docs/`, which `gateCodeIdentity` excludes, so the reviewed identity is unchanged and `findGateCheck` still resolves this row at the current tree. I confirmed that rather than assuming it.
- `node scripts/harness.mjs check`: 31 generated surfaces. `npm run publication:check`: 273/273 headings, both editions current at 30 and 45 linked sections after I refreshed the two source locks my product-05 sentence invalidated. `npm run release -- check-surfaces --local`: every surface PASS, every package publish refused as designed. `npm run release -- prepare --local`: "WO-150 target v0.39.0 remains current; no files changed." `npm run plan -- check`: exit 0, with this order's `release-assignment v0.39.0` classified under receipt 023. `node scripts/work-orders.mjs index --check`: current. `npm run format:check`: clean. The two decision-record cases pass after D008.

**Why this gate ran 28 suites where the executor's and the verifier's ran 22.** `--review` adds the machinery suites whose declared sources changed since the base, and the reviewed code identity differs from both earlier runs' for a reason worth stating: `gateCodeIdentity` reads `git ls-files`, so `packages/skeleton/fixtures/wo150-role-baseline.json` was outside the identity while it was untracked and entered it when this review staged it. That fixture is a behavioral input to the process-debt case. The lifecycle anticipates this — it is why the reviewer's gate runs after the intended new source files are staged — and this run is the identity the release manifest will consume.

## The verification sequence

One verification, one verdict, no repair. VER-001 passed the subject and I judge that verdict correct. Its instrument disclosure is honest and its method is the right one: it declined to treat the subject's own generators and gate as proof of themselves, recomputed each role's bytes with `harnessInstallation()` against the checked-in budget, and reproduced the two focused cases. Its six criterion tables match what I found independently, and its three stated limits — the behavioral question the fixtures cannot answer, the 249-byte margin, the unavailable counters — are the right three. Its `**Process cost:** unknown; cause no-session` is correct and correctly caused: that Codex session had no harness session established, so the counters were genuinely unobservable rather than unread. I checked one thing VER-001 asserted without reproducing the arithmetic, the pre-registered reading, and it holds by a wider margin than the report claims; see §Observations.

## The ideation receipt, and what happened to its six known issues

Refutation receipt 023 judged the standard pass that activated this order: verdict `aligned-with-findings`, `holdReasons` empty, six WO-150 known issues, no observed failure and no vision contradiction. Findings confer no operator decision authority, and none was treated as one. All six are carried into the executed record rather than left in the receipt:

1. **Relevance assumed for derived and unattended orders.** D005 names the served classes, states that a runtime-derived or unattended dispatch's valid outcome is a kept-current decline naming the absent operator session, rejects a second in-code selection rule for three still-queued orders, and reopens if WO-100, WO-111 or WO-120 lands an unattended executor path.
2. **Rule beating: the fixture passes without any experiment.** D001 and D005 both say so in those words, and D005 sets the reopening observation at ten equipped dispatches each recording an experiment or an explicit decline, with fewer than half doing either reconsidering the flip. Product 05 states it as behavioral, not configurational.
3. **The 249-byte margin and the ceiling ratchet.** D003 records the margin as 1.013% of the ceiling, the smallest of any bounded role in either root, states that the next reviewed executor rule of any size breaches it, and sets its `reopenWhen` as the measurement `coldStartBytes.executor > 24576` rather than a plan to trim. The receipt's own reopening condition — that the measurement after WO-150 exceed the ceiling — is discharged by criterion 2 having re-measured it at 24,327.
4. **The 900 s and the horizon total are unmeasured.** D005 answers this directly: the experiment time is spent inside the executor dispatch and is therefore already inside the `elapsed.implementation` figure the meter reads from `docs/control/orders/WO-NNN.jsonl`; no criterion caps the horizon total and none is invented; the observables are the per-order 900 s bound in the paragraph and each experiment record's `cost.wallSeconds`. Per-order token accounting stays a declared non-goal.
5. **The history counts a trial that ran unequipped.** D004 labels each trial's provenance and shows the equipped-only subset still satisfying the pre-registered rule, which is exactly the receipt's stated reopening condition. My own re-derivation confirms it and strengthens it; see §Observations.
6. **No critical-path gate is named.** The product 05 write-back now says it plainly — "The outcome this serves is the operator-flow outcome in the vision, not a typed gate: the order unblocks nothing on the critical path" — which is the honest answer to the goal standard's question rather than an invented gate.

## Findings, observations and limits

**No finding.** Nothing routes to repair.

**Observations.**

1. **The per-dispatch cost is 1,479 bytes, not 1,173.** The order's cost line accounts one paragraph of compiled role text; D007 correctly added a matching `Executor entry duties` briefing line to meet the objective's briefing clause, and that line is 306 UTF-8 bytes of dispatch output on every non-opted-out executor dispatch. It changes no cold-start figure, because cold start is the instruction file plus the role skill a session loads before any dispatch output, and folding dispatch output into it would silently redefine every ceiling and every earlier acceptance. Recorded as D008 with a named follow-up for the planner — account a support's briefing bytes beside its role-text bytes when a cost line states the per-dispatch cost — and written into product 05. I did not amend the order's cost line: that is the planner's authority text and this dispatch carries no operator scope expansion.
2. **The pre-registered reading survives a stricter subset than D004 uses, and the arithmetic should be on the record.** The rule requires one adopted saving over the next ten orders to exceed the whole trial experiment cost, with no recorded regression. Total trial cost is 253.276 s (163.546 + 87 + 2.73). WO-110's adopted method saves a measured 238.436 s per eligible skeleton test iteration, so two iterations in ten orders clear the whole three-trial cost. All three records carry `regression: false`. D004 runs the test on the two trials it calls equipped; the conclusion holds on WO-110 alone.
3. **"Two trials equipped" is an approximation worth naming.** WO-110 was explicitly equipped under operator authorization and says so. WO-145 is the order that authored the support, and its own acceptance criterion 3 made its execution the first trial while the default was off; whether that dispatch's compiled role text carried the paragraph is not something this review observed, and WO-145 D001's instruction to equip the support "at each later trial" suggests it did not. The distinction changes no number and no conclusion, because of observation 2, and it is stated here rather than corrected in the record because I have not established that the record is wrong.
4. **The power-set cost of a sixth default id is real and recorded.** `defaultExecutorSupportIds` growing from five to six doubled the WO-042 composition loops from 32 to 64 combinations and the executor-supports file's runtime from 1.43 s to 2.76 s. D002 records it rather than narrowing the loop to hide it, which is the right call: the loop is an existing composition check and shortening it to pay for this flip would be trimming around a cost.
5. **The self-host alias hazard D006 records is a genuine trip wire, and I hit its neighbour.** The printed DotLn scratch path is under `/var/folders/…`, which macOS resolves to `/private/var/folders/…`; a plain `/tmp` redirect is refused outright under WO-144 for this role, as it was here on the first attempt. The refusal is correct in both cases and the fix is to use the resolved granted path. Named so the next role does not spend a turn on it.
6. **`docs/planning/followups.json` now holds ten revision rows for the tinkerer candidate heading.** The register's compatible-history design admits this and 48 other refs already carry more than one, so it is bookkeeping rather than a defect; a planner reading the register still sees one candidate with a long revision history.

**Limits.** This review establishes configuration, not behavior. The fixtures prove that the support is equipped by default, removable per order, isolated to the executor and byte-identical in both roots; nothing here shows that an executor will find an economy worth testing, which is precisely why D005's reopening observation is behavioral and why criterion 1 passing is not evidence that it will. No trial counted its iterations, so there is no measured per-order saving and none is estimated; the 238.436 s and 58.742 s figures are per eligible development iteration in one sample each, and the 0.21 s figure comes with one command removed. The token side of the pre-registered reading cannot be compared on any subset: one of three trials recorded a token count and that count is a broad-scope transcript counter, not a causal allocation. The 249-byte executor margin means the next reviewed executor rule of any size triggers the standing 4 KB route. I ran no economy experiment in this dispatch — the flip takes effect for dispatches after merge, and this is the reviewer role, which the support does not equip.

## Goal alignment at handoff

The promised benefit was that the executor's economy question becomes a standing prompt with a recorded answer instead of an operator reminder plus a manual per-trial equipment step — the step whose absence made WO-099's trial run unequipped and say so. The observed outcome is exactly that and no more: one boolean, one briefing line, an opt-out, and a decline-with-reason path that remains a valid outcome. Against the traps, scaled to consequence: **tragedy of the commons** is the live one and is measured rather than assumed, at 1,479 bytes and up-to-900 s per non-opted-out dispatch, against a 249-byte ceiling margin that D003 records as already thin; **rule beating** is the second, and is answered by a reopening observation the passing fixtures deliberately cannot satisfy; **policy resistance** is the receipt's sharpest point — WO-090 shortened the executor cold start and this order spends most of that shortening — and the honest answer is that the guard was re-measured, not moved, and that the next breach follows the reviewed route with the rule named; **escalation** adds no gate, agent, schedule or approval step; **drift to low performance** is bounded by unchanged outcome standards and by `regression` in every experiment record; **shifting the burden** moves the right way, since the change removes an operator reminder rather than adding one; **success to the successful** is countered by the flip being decided by a pre-registered rule and a dated acceptance rather than by prior investment; **seeking the wrong goal** is the one the product write-back now states outright, that this serves the operator-flow outcome and unblocks no typed gate. NoOp would have kept the support optional and preserved the WO-099 ambiguity. Naive Interventionism is answered by the intervention being one boolean in an existing switch mechanism, reversible per order and globally on the same line. My own intervention was one decision record, one product sentence, two refreshed locks and the gate.

## Release-close request and publication

This dispatch authorizes committing the reviewed state, pushing the `wo-150` branch and opening its pull request. It does not authorize a merge, a push to `main`, a tag, a Release or a package publication. The PR body beside this report carries the meter table `release prepare` supplied; the five-section release notes follow the publication contract at one physical line per prose paragraph and disclose the default flip, the opt-out, the 1,479-byte per-dispatch cost, the 249-byte margin and the behavioral reopening observation. Commits are made in coherent series after the result is recorded, with plain subjects and no attribution. After the operator merges, release close consumes the committed gate row without running a suite.
