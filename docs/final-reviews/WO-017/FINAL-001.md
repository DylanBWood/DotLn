# WO-017 final review FINAL-001

**Subject:** branch `wo-017`, worktree `DotLn-wo017`, uncommitted working tree, 2026-09-03. `HEAD` and `origin/main` are both `e943cb6cfb297656bbed268b857bd8a6bb25f606`, the PR #22 merge that closed WO-027; the latest published tag is `v0.3.4`, and the branch carries no local commits, so the entire subject is the dirty tree. At review open, after this review's own transition: 34 tracked modifications (1,931 insertions / 325 deletions against `HEAD` including the two `docs/control` files; 1,918 / 317 without them) plus two untracked paths, `packages/kernel/test/wo017-evaluable-kinds.test.ts` (173 lines) and `docs/verifications/WO-017/VER-001.md` (330 lines); 36 `git status --porcelain -uall` entries. The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree (`3db4d1722ed619364a4abd08625aaecaa5895acc`) differs from checkpoint `refs/dotln/checkpoint/WO-017/4` (the verdict) only in `docs/control/current.md` and the strictly appended `docs/control/resume.jsonl`, and from `/2` (implementation-ready) and `/3` (verify allocation) only in those two files plus VER-001 itself (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, three bounded documentation corrections (§Corrections applied by this review), and the lifecycle's own events.

**Authority:** `docs/work-orders/WO-017-kernel-truthfulness.md`: a `Model: any capable model` and `Effort: executor xhigh+; verifier xhigh+; reviewer any` header, a `v0.3.5` release classification, four dated observed problems reproduced against the built kernel, a scope-discipline clause that freezes the 13-step event count, ordering, and types, six acceptance criteria, an evidence gate, a write-back duty, a non-goal list, and an ideation breakout receipt dated 2026-09-03. Against the activated text (`refs/dotln/checkpoint/WO-017/1`, whose tree is `HEAD`'s), the order differs in exactly three hunks plus the appended receipt: the H1 and the release-classification paragraph, where the planner's "(version assigned at activation)" placeholder became `v0.3.5` with a disclosure of the activation-preflight miss and the two component versions; the non-goals line, where "WO-105's retired lane" became "retained by WO-105"; and the receipt section itself. Every observed problem, all six criteria, the evidence gate, the write-back duty, and the scope-discipline clause are byte-identical to the activated text (§Adjudications, item 1). The receipt is a genuine breakout: the operator opened a bare `ideation:` dispatch during implementation and preauthorized continuation, and this review digests the receipt and its promoted surfaces as subject (§Ideation breakout receipt).

**Prior reports:** `VER-001` (pass; all six criteria reproduced independently, the sixteen-row mutation drill re-run in a scratch copy with every mutation caught, the demo, audit, and event-log deltas itemized, the palette proven byte-identical, four non-blocking findings and three observations). Complete sequence: one report, no repair episode.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes), **effort `max`**, in Claude Code 2.1.259 with sandboxed Bash; macOS on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 installed in this worktree's own dependency tree. WO-017 declares `reviewer any`, so any attested effort including `unknown` would satisfy it; the value is stated because it is true. The effort value is **self-reported**: the harness's own `/model` output in this session reported the selection of Fable 5.1 at `max` effort, the harness system context carries `max` on the effort field and `claude-fable-5-1` on the model field, and `claude --version` returns `2.1.259`. `docs/discovery/environment.json` records `harnessReadbackEligible: false` for this harness and version, so `harness-readback` is unavailable and the mechanism would refuse it; the `--effort` session selector is documented locally with `max` among its values, which is selection evidence, not readback. This reviewer is a different model from the verifier (Claude Opus 5 under the same harness and version) and from the executor (GPT-5.6 Sol under Codex CLI 0.153.0). Every command in this report ran first-hand from this worktree inside the sandbox; nothing is quoted from the executor's or verifier's transcripts. No subagents were used. The machine header below carries the same five values this review passes to `final-review-result`, and it is the only line in this report that begins at column one with that label; every quoted actor sits in a table cell.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.259","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-017 passes.** The order asked that four kernel boundaries behave loudly and provably where they corrupted, ignored, or decorated, each with a test that binds, without moving the 13-step scenario's event count, order, or types, and with every output delta itemized and every unlisted byte proven identical. The deliverable does that: the store codec refuses every malformed shape with the physical line number; outbox replay completes a result-before-persist command with the `preceded-persist` trace and leaves nothing pending; the authority guard decides the expiry instant, evaluates named rules in a pinned first-failing order, evaluates revocation conditions as data with one clock and fail-closed refusal, and returns a trace for grants; the evaluable-kind subsets are data with a test over the whole constructor space; the skeleton consumes the corrected guard so that operator return really revokes inspection; and the demo differs from `main` in exactly the two decision payloads items 3d and 3e authorize. All six acceptance criteria are met on first-hand evidence, including a green `npm test` in this worktree, a baseline build of `main` for the output comparison, and a nine-mutation re-drill that agrees with VER-001's sixteen. The ideation receipt's checks all pass. Three bounded documentation corrections were applied, one of which discharges a non-goal the order asked to be recorded. Nothing acceptance-relevant was changed by this review.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **`scripts/resume.mjs` allocated this report's path, appended `FinalReviewRequested`, and records this verdict through the actor-gated `final-review-result` path.** It is not a deliverable of this order: `git diff --stat HEAD -- scripts/ .claude/ package.json` is empty and `HEAD` is `origin/main`, so the script that ran is the one `v0.3.4` shipped. It is a fail-loud append-only log with a regenerated projection, and subject identity below was established with git objects rather than through it.
2. **The evidence file that gates actor attestations was edited inside the subject.** `docs/discovery/environment.json`'s effort probe was re-observed by the executor (`observedAt` to `2026-09-03T14:12:17-04:00`, Codex CLI `0.152.1` to `0.153.0`), and that record is what lets `resume` accept the executor's own `xhigh` for Codex CLI 0.153.0. VER-001 (F2) checked the datum against the machine; this review did so again rather than trusting either the file or the verifier: `codex --version` prints `codex-cli 0.153.0`, `claude --version` prints `2.1.259` matching the unchanged sibling entry, and the persisted `model_reasoning_effort = "xhigh"` key is present in the Codex user configuration (key presence only; no other setting was read). The record is factually correct; the disclosure gap is adjudicated below.
3. **This reviewer's own attestation is checked by the same mechanism.** A successful append is evidence that the header above equals the flags, not independent evidence about the subject; the inventory in §The order's own actors was compared by object comparison in this session, not by reading the projection.
4. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser, profile, and surface check** (`scripts/release-notes.mjs`, `scripts/github-body.mjs`, `release check-surfaces`), none of which this order changes. Their editorial adequacy is the operator's to judge at merge. Because `v0.3.5` is above the latest published tag, these notes become part of the `v0.3.5` edition at release close.

## Dispatch and subject integrity

`docs/control/current.md` named WO-017 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, and drift `xhigh -> max`. Before invoking the transition I read the `resume` mapping in `package.json` (`node scripts/resume.mjs`, unchanged) and confirmed that `scripts/resume.mjs` and `package.json` are byte-identical to `HEAD` and `origin/main`. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-017/FINAL-001.md`, moved the phase to `final-review`, and minted checkpoint `271461a99a5f465350f4c9a90bb901b0f33d0183` (`refs/dotln/checkpoint/WO-017/5`). `docs/control/resume.jsonl` is a strict append over `HEAD`: the first 104 lines are byte-identical to `HEAD`'s file, and the appended lines are `WorkOrderActivated` (with `effortDeclarationValidated: true`), `ImplementationReady`, `VerificationRequested`, `VerificationCompleted`, and this review's `FinalReviewRequested`, plus the `FinalReviewCompleted` the pass records. Every event carries `schemaVersion: 1`.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree as tree `3db4d1722ed619364a4abd08625aaecaa5895acc`, and `git diff --stat` against each checkpoint reported:

| Checkpoint                            | Differences from the working tree                                                                                         |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`fb263995…`)         | every deliverable path; the checkpoint's tree equals `HEAD`'s, so the whole subject is downstream of activation           |
| `/2` implementation-ready (`0a71770f…`) | `docs/control/current.md`, `docs/control/resume.jsonl` (+4), `docs/verifications/WO-017/VER-001.md` (added, 330 lines) |
| `/3` verify allocation (`89056253…`)  | `docs/control/current.md`, `docs/control/resume.jsonl` (+3), `docs/verifications/WO-017/VER-001.md` (added)              |
| `/4` verdict (`3f056424…`)            | `docs/control/current.md`, `docs/control/resume.jsonl` (+2)                                                               |
| `/5` final-review allocation (`271461a9…`) | `docs/control/current.md`, `docs/control/resume.jsonl` (+1)                                                           |

Every deliverable blob, the 32 tracked non-control modifications and the untracked test, is therefore identical across the implementation-ready, verify-allocation, and verdict checkpoints and the working tree; the verifier changed nothing in the deliverable, and VER-001 has not changed since its verdict was recorded. All five checkpoints have `HEAD` as their sole parent.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. Beyond `node_modules` and the two build outputs `packages/kernel/dist/` and `packages/skeleton/dist/`, the ignored paths are four `.DS_Store` files and the two ignored intake files this order's receipt names, `docs/intake/notes/WO-017-expanded-ideation-2026-09-03.md` and `docs/intake/images/dual-survival.webp`; `git ls-files docs/intake` lists the three `.gitkeep` placeholders and nothing else; `.claude/` carries no change, tracked or untracked.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                                | Actor recorded in the control log                                                                                            | Report header                                                                                                                                                   | Declared minimum | Result                          |
| -------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------- |
| executor | `ImplementationReady`                                | `{"harness":"codex-cli","harnessVersion":"0.153.0","model":"gpt-5.6-sol","effort":"xhigh","source":"self-reported"}`         | there is no executor report; the recorded version and effort agree with the discovery record and with the machine as re-observed here                           | `xhigh+`         | `xhigh` meets the floor exactly |
| verifier | `VerificationCompleted`, VER-001 pass                | `{"harness":"claude-code","harnessVersion":"2.1.259","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}`   | exactly one column-one header in VER-001, parsed and compared with the event actor as objects: equal                                                            | `xhigh+`         | `max` exceeds the floor         |
| reviewer | `FinalReviewCompleted`, this review's completion     | `{"harness":"claude-code","harnessVersion":"2.1.259","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`     | the header above                                                                                                                                                | `any`            | satisfied                       |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: xhigh -> max`, the one distinct pair the completions produce; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.259; model claude-fable-5-1; effort max; source self-reported` with the same drift line and phase `closed`.

## Final evidence summary

### The unchanged `npm test`, twice, in this worktree

This worktree carries its own lockfile-resolved dependency tree (`node_modules/@dotln/kernel` and `@dotln/skeleton` are the workspace symlinks; Prettier 3.9.6; TypeScript 5.4.5), so the suite ran natively. First run, on the verified tree before any review artifact existed: `All matched files use Prettier code style!`, `GitHub body profile tests passed`, then the eight shell suites (fixture temp-root, publication checker, backup-intake, resume, checkpoint, worktree, release), then `tsc -b --force` and `# tests 99 / # pass 99 / # fail 0`, exit 0; `git status --porcelain -uall` was identical before and after. Second run, after this review's corrections and package were in place, with the same result (§Corrections applied by this review).

### Static and structural checks

- `git diff --check`: exit 0, no output, before and after this review's artifacts.
- `node scripts/check-publication.mjs`: `PASS index coverage: 159/159 product headings indexed`, `PASS dual-voice links`, both editions `CURRENT` (27 and 42 linked source sections); the four new candidate sections are indexed at status `vision` and both source locks were refreshed by the executor.
- `node scripts/release.mjs check-surfaces` on the working tree: `PASS release-block: observed v0.3.5; expected v0.3.5 (work-order target v0.3.5; latest published v0.3.4)`, `PASS component-version @dotln/kernel: src changed; observed 0.2.0; previous v0.3.4 0.1.0; expected a different version`, `PASS component-version @dotln/skeleton: src changed; observed 0.3.1; previous v0.3.4 0.3.0; expected a different version`, and the profile rule PASS, reading `no current final-review bodies yet` before this review's bodies existed and naming `docs/final-reviews/WO-017/PR.md, docs/final-reviews/WO-017/RELEASE-NOTES.md` after. A first draft of the notes was refused by the notes parser because angle-bracket placeholders inside inline code (`<detail>`, `<effect>`) read as raw HTML under the strict subset; the placeholders were reworded and the parser, the body profile, and `check-surfaces` were re-run.
- Forbidden surfaces: `git diff --stat HEAD -- scripts/ .claude/ package.json` is empty; `package-lock.json` changes only in the three workspace version fields (`@dotln/kernel` `0.1.0` to `0.2.0`, `@dotln/skeleton` `0.3.0` to `0.3.1`, and the skeleton's dependency pin); no dependency was added, and the kernel imports nothing outside its own sources.
- The verifier's header equals its control actor by object comparison; VER-001 contains exactly one column-one `**Actor attestation:**` line.

### Output deltas, reproduced from a baseline build

The baseline is `git archive HEAD` extracted into the session scratch root with a `node_modules` whose `typescript` and `@types` entries link to this worktree's installed copies and whose `@dotln/*` entries link to the scratch tree's own packages, built with `tsc -b --force`; the current tree was rebuilt the same way. `packages/skeleton/dist/src/cli.js` with and without `--audit`, and a scratch driver calling `runScenario` on the shipped fixture, were captured in both trees.

| Output                                | Baseline (`main`)     | Subject                 | Difference                                                                                                                                                                                                                                                                |
| ------------------------------------- | --------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CLI demo (timeline plus glyph scene)  | 26 lines              | 26 lines                | one line: the banner `v0.3.0` to `v0.3.1`                                                                                                                                                                                                                                 |
| audit projections (`--audit`)         | 1,010 lines           | 1,027 lines             | 31 differing lines: the banner; `evt_7` added as a source and evidence link of the allowed `authority-decision` record; that record's `decisionEvidence` upgraded to `authority-trace-and-command-persisted`; the two trace bodies below                                    |
| scenario event log                    | 21 events, 5 decisions | 21 events, 5 decisions | type sequence identical line for line; exactly two lines differ, `evt_7` (the program trace `["Invoke","Invoke"]` replaced by the authority grant with nine inputs) and `evt_12` (the `effect denied` refusal trace gains `state`, `rngState:17`, `predicates`, `policy`) |

Both differing lines carry the same `occurredAt` as their baseline counterparts (`1200000` and `1200003`), and `DecisionRecorded` events stay at five: the new revocation trace is pushed to `traces` only and is compared live-versus-replay without being logged, which is why the 13-step count holds.

### Mutation spot-check

VER-001 reproduced the executor's full sixteen-row drill; this review did not inherit that either. In a fresh scratch copy of the subject (tracked files plus the untracked test, linked to this worktree's dependencies the same way as the baseline), each mutation below was applied alone, the copy rebuilt with `tsc -b --force`, and the full node suite re-run. The unmutated copy passes 99 of 99.

| #   | Item | Mutation                                                                                                  | Caught by                                                                                                                                           |
| --- | ---- | --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 3a   | `now >= expiresAt` to `now > expiresAt`                                                                   | `WO-017 authority expiry` and the precedence matrix (2 failures)                                                                                    |
| 2   | 3b/c | move the `authority expired` rule after `authority revoked` (the survivor the order named)                | the precedence matrix alone (1 failure), which is the bind the order asked for                                                                      |
| 3   | 3b/c | swap the `effect denied` and `effect not allowed` rules                                                   | the precedence matrix plus four pre-existing AC4/AC6 tests (5 failures)                                                                             |
| 4   | 1    | restore the pre-WO-017 `trimEnd`-based `appendEvent` and `decodeLog`                                      | all six refusing `WO-017 store contract` tests (6 failures)                                                                                         |
| 5   | 2    | never remember an orphan result (`orphanResults.set` to `delete`)                                         | `WO-017 outbox ordering` (1 failure)                                                                                                                |
| 6   | 3e   | ignore `revocationConditions` entirely (an always-empty list)                                             | 15 failures across kernel authorization, audit, and the skeleton scenario                                                                           |
| 7   | 3d   | grant trace branch decorated with an opaque value instead of the effect                                   | the granted-trace test, two audit tests, and the live/replay identity test (4 failures)                                                             |
| 8   | 4    | `Every` accepts non-finite intervals again                                                                | `WO-017 Every finite interval` (1 failure)                                                                                                          |
| 9   | 4    | deferred Cadence throw replaced by a silent return                                                        | `WO-017 evaluable-kind data` (1 failure)                                                                                                            |

One first formulation of mutation 6 (short-circuiting the memoized evaluator) failed to compile under `exactOptionalPropertyTypes` rather than failing a test, the same shape as VER-001's row 16; it was reformulated as the always-empty list above so the bind is behavioral. Every mutation was caught by the test the order maps to its item, in agreement with VER-001.

### Ideation receipt evidence

- The filed _Dual Survival_ block in `05-pattern-library.md` (the fenced block after "filed under Direct Draft Fidelity:") is byte-identical to the transcription in the ignored intake note: 14 lines, `cmp` silent, MD5 `80d3b9bcb8f0e13b8c8ad0cb4db069b0` on both sides, the same digest VER-001 recorded.
- `this is truly desperate living` occurs exactly once each in the intake note, `05-pattern-library.md`, `00-vision.md`, and the ledger; `analysis:` is filed exactly in `04-interfaces.md`, `00-vision.md`, and `05-pattern-library.md`.
- The intake note (3,098 bytes) carries the four operator messages verbatim, including the correction that made exact wording the requirement, the clarification restricting exact treatment to the two phrase sources plus Westworld, and the recorded authorship and public-filing confirmation; the 7.5 MB source image stays in ignored intake and no committed surface embeds or links it.

## Acceptance criteria

| #   | Criterion                                                                                                                                          | Verdict | First-hand basis                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Store codec contract: `""` or newline-terminated JSON-object lines; both boundaries refuse with the line number; `eventId` from the well-formed count; five named shapes plus a round trip; domain-model row | met     | `parseLog` in `store.ts` read in full: empty string, missing-final-newline check naming `log.split("\n").length`, per-line blank, invalid-JSON, and non-object refusals; `appendEvent` routes through it. Seven `WO-017 store contract` tests cover the partial tail, blank interior line, whitespace-only log, unterminated complete object, non-object values, invalid JSON, and the byte-exact append; mutation 4 above breaks six of them. The **Event store** row in `02-domain-model.md` states the contract verbatim. |
| 2   | Outbox ordering: result-before-persist never leaves the command pending; trace `["result", "preceded-persist"]`; test yields zero pending           | met     | `replayOutbox` read in full: orphan results keyed by `commandId` from an `unknown` trace, applied on persist with the first orphan's trace rewritten in place, the one-argument form returning the same state. The named test asserts the completed entry with `resultEventId`, zero pending commands, the exact trace, and legacy equality; mutation 5 breaks it. Duplicate orphans reduce to `dedup` deterministically and never-persisted orphans stay `unknown`.                                                          |
| 3   | Authority guard (a) boundary decided both ways; (b) precedence matrix over adjacent pairs; (c) ordered named rules, order unchanged except (a) and (e); (d) grant trace recorded by the skeleton; (e) conditions as data with one clock, fail-closed, skeleton proof | met     | (a) `now = 9` authorizes and `now = 10` expires against `expiresAt = 10`. (b) seven rows, each constructed so both named conditions hold, checked by hand against the helper defaults (`allowedEffects: ["write"]`, `expiresAt: 999`, `now: 1`); mutations 2 and 3 break it. (c) the eight-entry `rules` array keeps the original seven names in order with `cannot evaluate revocation` inserted before `authority revoked`. (d) the authorized branch returns `authority-guard` / `["authorized", effect]` with `now:`, `authorityEnvelope:`, `evidence`, `revocations`, the condition inputs when consumed, and `resource:`; `scenario.ts` records it at `evt_7`. (e) `revocationConditions` over the full condition-by-event product with `{...predicateEnv, now: context.now}`, refusing on missing inputs, unknown refs, or a throw, with no short-circuit so a later error is never masked; the skeleton's `operator.return-event` condition refuses inspect `authority revoked` after return live and in replay, and mutation 6 breaks fifteen tests. |
| 4   | Evidence hardening: deferred throws pinned; `Sequence([])` to `Done`; `Every` refuses non-finite; binding `Backoff` exact; `replay` promotes `policy` and falls back `rngState`; kind lists exported as data with a complete test | met     | `EVALUABLE_CADENCE_KINDS` and `EVALUABLE_PROGRAM_KINDS` are `as const` arrays satisfying the kind unions; the new test enumerates all fourteen Cadence and eleven Program constructors and asserts throw-or-evaluate by membership, so neither list can drift in either direction; the four remaining tests pin exact values (`dueAt: 1250`, `rngState: 1015568748`, `trace: "Backoff:2:250"`; `rng:0` with the promoted policy). Mutations 8 and 9 break them. |
| 5   | Every item mapped to a named test; each item's observed-problem mutation, in a scratch copy, fails that test                                        | met     | VER-001's sixteen-row drill reproduced by the verifier and this review's nine-row spot-check above, every row caught by the mapped test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 6   | `npm test` green; skeleton event count, ordering, and types unchanged; every 3d/3e delta itemized and justified; remaining outputs identical; README map covers every new export | met     | 99 of 99 twice; 21 events with an identical type sequence and five decisions in both trees; the two payload deltas and the 31 audit lines itemized above and each traceable to items 3d and 3e; the demo timeline and glyph scene identical but for the banner; both `EVALUABLE_*_KINDS` mapped in the kernel README and required by `ac7-readme-map.test.ts` (surface floor raised from 56 to 58).                                                                                                                                                                                             |

Evidence gate: test output, the mutation drill, the before-and-after demo captures with an itemized structural diff and byte comparison of the unchanged outputs, and a clean `git diff --check` all exist first-hand in this review and in VER-001. Scope discipline: the event count, ordering, and types are unchanged; item 3d records the grant in the existing `DecisionRecorded` payload without adding an event; no dependency was added; the kernel performs no I/O and consults no ambient clock. Non-goals respected: no filesystem appender, no byte-offset sweep, no evaluation of deferred kinds, no change to the `commandId` or `eventId` schemes; the `Cadence.Evaluable` candidate the non-goals asked to be recorded is recorded by this review (§Corrections applied by this review).

## Write-back duty

- `02-domain-model.md`: the **Event store**, **Command**, and **AuthorityEnvelope** rows carry the codec contract, the ordering clause, and the exclusive expiry, eight-rule order, one-clock condition evaluation, fail-closed refusal, and returned grant trace; the kernel-version paragraph names component `0.2.0` staged for `v0.3.5`. A word-level diff shows those four spots as the only content changes in an 87-line hunk of Prettier table re-padding.
- `03-architecture.md`: failure-injection row 1 gains the ordering clause; the only other change is the ideation breakout's candidate section.
- Kernel README: both new exports mapped; the shallow-by-shape caveat added by this review.
- Ledger: the two entries the duty names, `Revocation becomes conditions as data` (`transformed`) and `The event-store codec has a fail-closed line contract` (`adopted`), exist in the 2026-09-02 entropy-review section with exactly those statuses and already name WO-017; the non-rewrite rule makes leaving them as written correct (VER-001 F4). The map row and this report now say that WO-017 pinned them.
- WO-105: a dated **Scope split** absorbs the malformed-line lane here, retargets the retained corpus to valid-object anomalies and byte-offset sweeps against the landed codec, and moves the dependency to WO-017 landed; the map records the same and the WO-103 dependency change.

## Ideation breakout receipt

The receipt names the review this closeout owes it, and this review performed it on the receipt, the six promoted product and lineage surfaces, and the three refreshed publication projections.

- **Exact phrase fidelity:** proven by byte comparison and digest above.
- **Direct-filing provenance:** the receipt, the ledger section, `00-vision.md`, and `05-pattern-library.md` each record that the operator authored the list, corrected an over-generalized first synthesis, and on 2026-09-03 authorized its exact text for public filing; the DBZ reference stays in lineage and reaches `03-architecture.md` only as the synthesized contributed-capacity mechanism, which is exactly the operator's clarification.
- **Clean room and secrets:** a pattern scan over the complete diff and the untracked test finds no credential, key, token, employer product name, internal hostname, corporate gateway, or managed-host description; the names in the palette are television personalities in a list the operator authored, not private individuals; every `v1` in the diff is a reactor version or an audit-record version, not the predecessor system.
- **Style, control, and authority stay separated:** the phrasebook support is forbidden from changing `Voice`, facts, authority, or evidence; the command lexicon is forbidden from bypassing platform safety, the Clean Room floor, permissions, evidence gates, or external-effect authority; the incident reporter produces only a local draft without a named, scoped, expiring, revocable envelope and treats an Issue as intake, not proof or repair authority; the contributed pool transfers no credential and subjects returned work to the same verification and merge authority as local work.
- **External-effect and privacy boundaries:** the incident packet is minimized locally with a preview manifest, raw logs stay separately retained diagnostics, and reversible fingerprints are excluded; the contributed pool names provider eligibility as environment truth to recheck and refuses to present several subscriptions as one account.
- **Principles 8 and 9:** model assignment stays per work order; the phrasebook's degraded-mode line cannot excuse or conceal silent model substitution; the pool rechecks eligibility rather than encoding it.
- **Links and locks:** all four candidate sections are titled `Candidate —` and indexed at `vision`; `publication:check` passes with both locks current.
- **Nothing presented as implemented:** confirmed on every promoted surface; the receipt's unresolved-choice list is intact and none is silently selected.

## Verification-sequence soundness

VER-001 meets every duty the order and the guide place on it: it establishes subject identity through checkpoints, discloses the instrument and the one place it touches the subject, confirms all six criteria with its own commands, reproduces the executor's drill rather than taking it on report and records honestly the two formulations the type checker rejected, itemizes the output deltas so they exist in a repository artifact, performs the receipt's review with a byte comparison, and names its findings with a bounded checklist that expands no scope. Every count it reports was re-derived here where cheap (99 tests, 159 headings, 21 events, five decisions, 26 demo lines, 31 audit lines, 1,027 audit lines, 14 palette lines and their digest, the eight-rule order, the sixteen-row drill by a nine-row sample) and all agree. No VER-001 statement is contradicted by this review's evidence.

## Adjudications

### 1. The executor amended the work order's authority text (VER-001 F3)

Three hunks against the activated text. The H1 and classification rewrite completed the activation step the helper skipped, as WO-024, WO-025, WO-019, and WO-027 each did before it: `v0.3.4` is the latest published tag locally and on origin, `v0.3.5` is the next patch strictly above it, the README release block carries `v0.3.5`, `@dotln/kernel` moves `0.1.0` to `0.2.0` and `@dotln/skeleton` `0.3.0` to `0.3.1` because both packages' sources changed, and `check-surfaces` proves all three rules. The draft had left the planner the option of the next application minor because item 3e adds optional fields; the executor chose a compatible application patch with a component minor for the kernel, which states the additive public API where consumers read it, and no criterion turns on the choice. The append-only activation event was neither replayed nor edited, and the correction is disclosed in place. The non-goals edit resolves a genuine contradiction: the draft called WO-105's byte-offset sweeps a "retired lane" while the write-back duty and WO-105's own scope split retain them. **Ratified.**

### 2. The recorded candidate and the shallow lists (VER-001 F1)

Confirmed at runtime in this session: `evaluateCadence(Cadence.Gate(Cadence.Burst(2, 1), ref))` throws `Cadence Burst evaluation is deferred` and `stepProgram(Program.Sequence([Program.All([])]))` throws likewise, although `Gate` and `Sequence` are listed. Shallow semantics are the authorized scope, and the test's completeness claim is exactly about root kinds; the gap was the missing candidate record the order's non-goals require and an unstated caveat that WO-018 could over-read. Both are documentation, so this review supplied them (§Corrections applied by this review) rather than sending a green deliverable through a repair cycle for a sentence and a ledger entry.

### 3. The discovery record edited in the subject (VER-001 F2)

The re-observation is factually correct (re-checked here, §Self-referential instruments, item 2) and it is a precondition of the executor's own attestation rather than ideation or scope. The gap is disclosure, and the durable places for it are this report and the PR body, both of which now name the two re-observed values; the receipt is about the ideation promotion and is left as the executor wrote it. **Accepted; disclosed here.**

### 4. No "landed" ledger entry (VER-001 F4)

The two entries the duty names existed at nomination with the statuses the duty gives and already name WO-017; appending a third entry that says the same thing would be the tidying the guide forbids. The distinction the verifier wanted, "WO-017 will pin this" versus "WO-017 pinned this", is now carried by the map row's VER-001 and FINAL-001 links and by this report. **No entry owed.**

### 5. The program trace replaced by the grant trace at `evt_7`

Item 3d says the granted trace is recorded "in the existing `DecisionRecorded` payload without adding an event". The executor read that as replacing the payload's trace, and the skeleton README says so ("the existing grant decision slot"). The replaced trace was `{reactorId: "program", branchPath: ["Invoke", "Invoke"], envInputs: []}`, which stated no input and no decision that the persisted command does not already state; the grant trace states the effect, the canonical time, the envelope, the consumed inputs, and the resource. The check the old trace stood in for, that the verification program produced an inspect intent, is retained as an assertion that throws in `runScenario`, and the replay path authorizes the same grant against the same envelope so live-versus-replay identity now holds over six traces including the unlogged revocation. **Ratified as the reading the order invites.**

### 6. The WO-103 compatibility amendment (VER-001 observation ii)

Not in the write-back duty. It is doc-only, it edits a draft order that has never been activated, and leaving it would have left a draft specifying a seven-rule oracle, a strict expiry boundary, and trace-less authorized cells that no longer exist, so the next executor would have built the corpus against the wrong contract. Unambiguous, low risk, covered by the same verification, and not obscuring the diff: within the bounded boy-scout policy, and the map records it. **Ratified.**

### 7. The pre-existing "during VER-001 repair" sentence (VER-001 observation iii)

The order's Direct-draft provenance paragraph says the operator confirmed the direction "during VER-001 repair"; WO-017 has no repair episode, and `git log -S` dates the sentence to the WO-007 episode's commit. Time-indexed, the sentence describes a confirmation that happened during a different order's repair window and is a phrasing ambiguity in immutable history, not a defect in this deliverable. **No edit.**

### 8. Audit linkage for a degenerate refusal (this review's observation)

The guard's refusal trace appends only the condition inputs that were actually supplied, so a `cannot evaluate revocation` refusal caused by a missing `state` or a missing `predicateEnv` emits a partial suffix (`rngState:`/`predicates` without `state`, or `state` alone). The skeleton's `semanticAuthorityInputsAreComplete` accepts only the empty suffix or the complete four-element form, so the denied audit record for that refusal stands without the trace link. Nothing is fabricated and the record keeps its reason and envelope reference; the projection is stricter than the emitter for a configuration that is itself a caller error. Not a defect against any criterion; carried as an open item for WO-103's oracle, whose cells now include missing-input environments.

### 9. `appendEvent` parses the whole log on every append (VER-001 observation i)

Correct and quadratic; irrelevant at demo scale; worth remembering when WO-009 lands a real appender. **Carried.**

### 10. The evidence gate's demo captures live in reports, not in the repository

The order assigns the itemized delta and `cmp` to the executor's session result, which is not a repository artifact. VER-001 and this report each reproduce the itemization first-hand, so the durable record exists twice. Not a defect; a planning note that the next kernel order could name a repository path for such captures.

## Corrections applied by this review

Three corrections, all in the documentation lane `docs/final-reviews/README.md` grants, proven contained by diffing against checkpoint 5 (34 insertions and 8 deletions across three files, identical with and without whitespace, so the formatter re-padded nothing) and followed by a re-run of the checks they could affect: the full `npm test` (which parses the kernel README's map and runs the pinned `prettier --check`), `git diff --check`, `publication:check`, and `check-surfaces`, all clean.

1. **`packages/kernel/README.md`:** four sentences after the evaluation-subset paragraph stating that both `EVALUABLE_*_KINDS` lists are exact by root kind and shallow by shape, that a listed combinator such as `Gate` or `Sequence` still throws `is deferred` when it wraps an unlisted child, that membership is not a promise about nested trees, and that the recursive subset type is recorded as a candidate. The map table is untouched and `ac7-readme-map.test.ts` still passes.
2. **`docs/lineage/idea-ledger.md`:** a new section directly below the header, `WO-017 final review — the deferred evaluable-subset type candidate (2026-09-03)`, with one `preserved` entry recording the `Cadence.Evaluable` and `Program.Evaluable` recursive subset type, the two runtime examples above, and the WO-018 consumer it would serve. This discharges the order's non-goal ("record as a candidate") and VER-001's checklist item 1. No existing entry was edited.
3. **`docs/planning/work-order-map.md`:** the snapshot line, the slot bullet, the suggested-targets bullet, and the WO-017 catalog row's state, preflight, and eligibility cells. They read "active", "implementation and evidence in progress", and "complete implementation evidence, then request independent verification", true when the executor wrote them and false beside a passing VER-001 and this report. Following WO-013, WO-024, WO-025, WO-019, and WO-027 FINAL-001 §Corrections, they now record VER-001 pass, FINAL-001 pass, the reviewed state committed and awaiting operator merge, and the separately authorized release close that tags `v0.3.5`. No other row changed.

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet and no behavioral or evidence defect was found in the deliverable. The items below are recorded and not scored.

1. **The evaluable-kind lists are shallow by shape** (VER-001 F1): caveat and candidate now recorded; a bounded order may pin a recursive subset type with a matching runtime predicate before WO-018's manifest reads the lists as tree-level truth.
2. **The discovery record was re-observed inside a kernel order** (VER-001 F2): correct, now disclosed. A future order whose executor must refresh `environment.json` to attest should name that refresh in its result or receipt.
3. **The activation-preflight gap is now five orders long** (WO-024, WO-025, WO-019, WO-027, WO-017): each activated with the planner's placeholder in the H1 and each executor forward-corrected it. For the operator's planning pass, not for this review.
4. **Audit under-links a caller-error refusal** (§Adjudications, item 8): a cell for WO-103's oracle.
5. **`appendEvent` is quadratic in log length** (§Adjudications, item 9): a note for WO-009.
6. **The executor's model and effort live in the control log and the verifier's report, not in a committed executor artifact.** The `ImplementationReady` event records them and VER-001 quotes them; that is the mechanism WO-019 built, and it worked, but there is still no executor-authored repository artifact for a code order. Recorded, as WO-013, WO-015, WO-024, and WO-025 recorded it.

### Candidates considered and not sustained

- _The replacement of the program trace at `evt_7` violates "without adding an event" or loses evidence._ Not sustained: §Adjudications, item 5; count, order, and types are unchanged, the replaced trace carried no information the persisted command does not, and the retained check is an assertion.
- _The kernel component should have taken a patch, not `0.2.0`._ Not sustained: the additive envelope field, two context inputs, the grant trace, and two exported constants are new public surface; pre-1.0 a component minor is the honest signal and `check-surfaces` requires only a different version.
- _The WO-103 edit expands scope._ Not sustained: §Adjudications, item 6; it changes a draft's oracle to match a contract this order ships and adds no deliverable.
- _The refusal trace's variable `envInputs` is decorative._ Not sustained: the inputs appear only when condition evaluation actually ran (the third rule), so the trace reports what the guard consumed, and the scenario's `evt_12` shows the consumed set while a first-rule refusal shows the base four.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step returned to the operator.** `gh` in this sandbox fails before its root command exists (`failed to read configuration: open ~/.config/gh/config.yml: operation not permitted`), so `npm run worktree -- publish` refuses at its `gh` preflight after the committed surface check and body validation and before any push, fail closed, as for WO-006, WO-007, WO-013, WO-024, WO-025, WO-019, and WO-027. The reviewed commit is made here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation, and notes parse run against the exact commit; it refuses at the `gh` preflight and pushes nothing. The operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, fast-forward the main checkout to the merge commit first (`git pull --ff-only origin main`), then run `npm run release -- close WO-017 --publish` from it, outside the sandbox and where `gh` is authenticated. Because `v0.3.5` is strictly above `v0.3.4`, the command runs `npm ci` and the evidence gate, assembles the five-section edition from the reviewed notes in first-parent order (WO-027's no-release notes ride this tag together with this order's), creates and pushes the annotated `v0.3.5` tag, and creates the matching GitHub Release.
- **Attribution:** the reviewed commit carries a plain subject and no attribution trailer, per the operator's standing rule (`~/.claude/CLAUDE.md`) and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins.
- **Ledger duty:** discharged by the executor's four breakout entries and the two nomination entries the duty names, plus this review's candidate entry.
- **Instruction conflict flagged, not acted on:** the execution guide asks that verifiers and reviewers prefer instruments that fail loudly; this review's one non-loud instrument was its own effort attestation, which is a claim by construction, and the source label says so.

## Proposed PR

- **Title:** `:bug: WO-017: kernel truthfulness; the store codec, outbox replay, and authority guard fail loudly and leave a trace, each bound by a test; v0.3.5`, the `:bug:` precedent being PRs #9 and #14 for boundary repairs, chosen over `:sparkles:` because the order's objective is correction at boundaries that misbehaved, with the additive API in service of that correction.
- **Body:** `docs/final-reviews/WO-017/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commit (it also makes the map's "FINAL-001 pass" cell true); the reviewed state is committed with a plain subject. The operator then runs, from this worktree outside the sandbox:

```bash
npm run worktree -- publish WO-017 --title ':bug: WO-017: kernel truthfulness; the store codec, outbox replay, and authority guard fail loudly and leave a trace, each bound by a test; v0.3.5' --body-file docs/final-reviews/WO-017/PR.md
```

The helper runs `check-surfaces --committed WO-017` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-017`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, fast-forward the main checkout and run `npm run release -- close WO-017 --publish` from it; the expected outcome is the annotated `v0.3.5` tag and its GitHub Release.

## Method

Single reviewer, single session, no subagents. Read order: the execution guide's dispatch, closeout, discipline, and model-specific sections, the control projection and log, the work order and its diff from the activated checkpoint, VER-001 in full, the final-review and verification READMEs, the WO-027 final review with its PR body and notes for the package shape and publish precedent, then every changed file's diff in full (kernel sources and tests, skeleton sources and tests, the product, ledger, planning, publication, and work-order documents, the discovery record, the manifests, and the lockfile), with the publish, surface, notes, and body gates read from `worktree.mjs`, `release.mjs`, `release-notes.mjs`, and `github-body.mjs` rather than inferred. The ignored intake note was read for the fidelity check and nothing from it was promoted. Subject integrity was established through git objects. Evidence was re-established first-hand: two full `npm test` runs, the static, surface, and publication checks, a baseline build of `main` for the demo, audit, and event-log comparison, a nine-mutation re-drill in a fresh scratch copy, the verifier header comparison, the palette byte comparison and digest, the machine re-observation behind the discovery record, and the clean-room scan. Authority used: three documentation corrections and the three package artifacts; no edit to any code, test, contract, schema, or acceptance byte. The scratch trees lived in the session scratch root. Nothing was restored from a checkpoint, no destructive git command was used, and no remote, tag, or `gh` mutation was performed; the direct `gh` invocation was `gh --version`, which failed before its root command existed, and the publish helper's own `gh` preflight after the reviewed commit fails the same way, leaving the push and the PR to the operator.
