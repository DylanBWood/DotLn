## What this merges

WO-028, the `v0.5.1` control-event time patch: the control log now records when each transition happened, and the times it never recorded are preserved before they can be lost. Every event that `scripts/resume.mjs` appends carries the host's UTC append time as an optional `recordedAt` field under the unchanged schema version `1`; the fold validates a present value and never reads it for ordering or legality, so a log whose timestamps are absent, reordered, or running backwards folds exactly as before. `status --json` and `docs/control/current.md` expose the latest event's time and the elapsed milliseconds of each phase's latest completed attempt, `"unknown"` where an endpoint predates the migration. A read-only `resume times` command labels every event's time as `recordedAt`, `recovered-from-local-checkpoint-ref`, or `unknown`, refusing rather than guessing when a needed local ref is missing or mismatched, and the executor ran it once against the activation log and committed the dated observation: 135 events, 120 recovered from 120 unpushed local refs, 15 unknown forever. Nothing was pushed, moved, deleted, or backfilled; no dependency, package version, or `src/` file changed. The order's 2026-09-04 operator-authorized ideation breakout is included as documentation only: the corrected Twelve Days of Christmas analogy, two preserved candidate supports, a manual operator guide for the shipped Entropy Reducer, and the operator's Codex-actor and release-assignment defaults.

- **`recordedAt` on every appended transition (criterion 1):** the append path stamps the host clock after `schemaVersion` and validates it before writing; the shipped suite drives all eight event types through the real dispatcher in one process and asserts canonical form, host-clock bounds, and non-decreasing order; a simulated bad clock refuses and leaves the log and projection byte-identical.
- **Time-blind fold (criterion 2):** `fold` calls `validateRecordedAt` per event and otherwise ignores the field; the masked `cmp` of `current.md` passes for absent, reordered, and backwards variants, and fifteen malformed shapes refuse with the event's ordinal.
- **Elapsed projection (criterion 3):** `scripts/lib/control-time.mjs` derives `recordedAt` and `elapsed` over append order, scoped to the current activation; `statusProjection(events)` is the common structured surface for the CLI, `worktree.mjs`, and `release.mjs`, and no script parses `current.md`.
- **`times` and the dated observation (criterion 4):** one physical JSON line per event, three labels, counts, refs read, the unpushed statement, and the second-precision caveat; missing, mismatched, foreign-scope, malformed, and non-commit refs refuse with empty stdout; `docs/discovery/control-event-times-2026-09-04.json` is byte-identical to the command's output over the first 135 events.
- **Checkpoint refs untouched (criterion 5):** the committed `for-each-ref` captures before and after the evidence run are byte-identical at 122 refs, and the post-readiness capture adds exactly `refs/dotln/checkpoint/WO-028/2`; the checkpoint suite's existing assertions are unchanged and the new cases are appended.
- **Migration notes and a regenerated projection (criterion 6):** dated notes in the architecture's §Session lifecycle & resilience and the execution guide's §Model-specific notes beside the WO-019 note; `current.md` was regenerated only by lifecycle transitions.
- **Green suite, clean whitespace, no dependency, size reported (criterion 7):** `npm test` exit 0, `git diff --check` clean, `package.json` and the lockfile unchanged; top-level `scripts/*.mjs` total 4,395 lines against 4,378 on `main` (+17) and `scripts/lib/` 285 against 150 (+135, the new module).
- **Write-backs:** architecture, execution guide (dispatch row and migration note), playbook, domain model, work-order map, ledger, docs map, README release block at `v0.5.1`, publication index and both audience locks, and the discovery record.
- **Ideation breakout (documentation only):** README, Principle 4, inspirations, and ledger correct the analogy while naming the superseded entry; vision, pattern library, and roadmap preserve the candidate review and baseline as `vision`; `docs/instance/entropy-reducer/README.md` documents the manual dispatch and the pending Shape-First correction; roadmap, execution guide, and playbook record the operator defaults; the receipt names the authority, source treatment, promoted surfaces, and unresolved choices.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in this worktree on the verified tree and again on the reviewed tree: Prettier clean, the body-profile unit suite, the shell suites, `check-publication.mjs` at 175 of 175 headings with both audience locks current, `tsc -b --force`, 173 of 173 package tests, 8 of 8 identity-corpus tests, exit 0. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.5.1; expected v0.5.1`, `PASS component-version` for `@dotln/compiler` `0.2.0`, `@dotln/kernel` `0.2.1`, and `@dotln/skeleton` `0.5.0` with `src unchanged`, and the body-profile rule over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity at review open: a temporary-index tree of the working tree differed from the verdict checkpoint only in the two control files; from the verify-allocation checkpoint only in those plus VER-001 and the verifier's one-line discovery observation; from the implementation-ready checkpoint additionally in the executor's post-readiness paragraph and ref capture; the activation checkpoint's tree equals `HEAD`'s; all five checkpoints have `HEAD` (`27e344b`, also `main`, `origin/main`, and the `v0.5.0` tag target) as their sole parent; `git log main..wo-028` is empty; the verifier's report header equals its control-log actor by JSON comparison; the executor's `max` and the verifier's `max` meet the declared `xhigh+` floors.
- Direct probes: the library's output over the first 135 events plus the CLI's trailing newline equals the committed observation byte for byte, and the observation, log-slice, and ref-capture hashes match the receipts; all 120 recovered times were re-derived from `git log -1 --format=%cI` on each event's named ref with zero mismatches and zero recorded-SHA mismatches; `fold` and the non-timing projection are deep-equal on the real 138-event log across five mutations of the field; nine malformed shapes refuse at the last ordinal; the live `verification` value of 889,287 ms equals the difference of the two recorded stamps; an isolated fixture repository drove the real CLI through six transitions and observed increasing `recordedAt` on every event, the three `times` labels, the SHA-mismatch and missing-ref refusals with empty stdout, the mixed projection, and the acceptance of this reviewer's attestation with a mismatched header refused; 401 relative links and anchors across the 20 changed Markdown files resolve; the clean-room and secret screen over all 1,985 added lines found nothing.

## Verification trail

- `docs/verifications/WO-028/VER-001.md`: pass; all seven criteria, the evidence gate, and the seven write-backs reproduced independently through two fixture repositories, direct library imports, a second-path re-derivation of every recovered time, byte-level hashing, and a ref-set comparison; five non-blocking findings and six disclosures with an empty bounded checklist.
- `docs/final-reviews/WO-028/FINAL-001.md`: pass; the verifier's findings and disclosures adjudicated with reasons, the reviewer's own observations recorded, the ideation receipt inspected against the guide's list, one bounded documentation correction applied and re-checked, method and instruments disclosed.

## Known open items

- **The dated observation is not reproducible by a bare `times` run** (VER-001 F1): reproduce it over the first 135 events, adding the CLI's trailing newline to the library output; inherent to a dated observation, and a backfill is a non-goal.
- **`checkpointRef` shape is validated only when recovery needs it** (VER-001 F2): consistent with `fold`, which has never validated ref shape; no behavioral risk observed.
- **A git failure during a required recovery loses the ordinal** (VER-001 F3): the contract holds and only the message is less specific; a bounded candidate for the next scripts order.
- **`at line N` is an event ordinal** (VER-001 F4): pre-existing WO-018 wording that diverges from the physical line only if the log ever contains a blank line.
- **One bounded narrowing of operator intent** (VER-001 F5): the success-under-growth review is attached to a material decision rather than to every edit; `preserved`, not `adopted`, so the operator can widen it at merge or later.
- **The module-entry guard does not resolve symlinks** (VER-001 D6): a CLI invoked through a symlinked directory path exits without acting; pre-existing and forward-only.
- **The compiled Shape-First support carries literal-first wording**: the manual guide states the operator's correction at dispatch; correcting the typed support, regenerating the residue, and testing the change is an unallocated follow-on named in the work-order map.
- **`CLAUDE_EFFORT` is an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9): still open, so Claude Code attestations remain `self-reported`.
- **Publish and release close.** The bounded publisher `npm run worktree -- publish WO-028` runs the committed surface check and body validation before any push and then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses at that preflight the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.5.1` is strictly above `v0.5.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.5.1` tag, and creates the matching GitHub Release.
