# WO-028 final review FINAL-001

**Subject:** branch `wo-028`, worktree `DotLn-wo028`, uncommitted working tree, 2026-09-04. `HEAD`, `main`, and `origin/main` are all `27e344b16d789010b01ae4fbd7c84296dd606575`, the PR #28 merge of WO-023 that the published annotated tag `v0.5.0` names (`git ls-remote --tags origin` resolves `refs/tags/v0.5.0^{}` to that commit); the branch carries no local commits (`git log main..wo-028` is empty), so the entire subject is the dirty tree. At review open, before this review's own transition: 26 tracked modifications (838 insertions / 111 deletions against `HEAD` including the two `docs/control` files; 24 files, 821 / 101 without them) plus eight untracked paths: `scripts/lib/control-time.mjs` (135 lines), `docs/instance/entropy-reducer/README.md` (82), `docs/discovery/control-event-times-2026-09-04.json` (148), the four files of `docs/discovery/control-event-time-evidence-2026-09-04/` (`fixture-transcript.txt` 7, `refs.before.txt` 122, `refs.after-tests.txt` 122, `refs.after-readiness.txt` 123), and `docs/verifications/WO-028/VER-001.md` (408); 34 `git status --porcelain -uall` entries. The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree (`5e5347daadadda1ffbd5fe2c888e9eb3525488df`) differs from checkpoint `refs/dotln/checkpoint/WO-028/4` (the verdict) only in `docs/control/current.md` and the strictly appended `docs/control/resume.jsonl` (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, one bounded documentation correction (§Corrections applied by this review), and the lifecycle's own events.

**Authority:** `docs/work-orders/WO-028-control-event-time.md`: `Model: any capable model`; `Effort: executor xhigh+; verifier xhigh+; reviewer any`; a `v0.5.1` patch classification assigned with explicit operator authority on 2026-09-04 after activation omitted the target; seven numbered acceptance criteria; a five-item evidence gate; a seven-item write-back duty; a non-goal list; four operator-review assumptions; one 2026-09-04 operator-authorized ideation breakout receipt (cumulative-context analogy, success under growth and routine observation, Entropy Reducer use) that names this review as owing it an inspection; and the executor's implementation evidence receipt. Against the activated text (`refs/dotln/checkpoint/WO-028/1`, whose tree equals `HEAD`'s by `git diff-tree`), the order differs only in its title's version suffix, the release-classification paragraph, and the two appended receipts; the objective, observed gap, design, deliverables, all seven criteria, the evidence gate, the write-back duty, the non-goals, and the operator-review assumptions are byte-identical to activation (adjudication 9).

**Prior reports:** `VER-001` (pass; all seven criteria, the evidence gate, and the seven write-backs reproduced through two fixture repositories built outside the repository and driving the real CLI, direct library imports on adversarial inputs, an independent re-derivation of all 120 recovered times through a second git formatting path, byte-level hashing, and a `for-each-ref` set comparison; five non-blocking findings F1–F5 and six disclosures D1–D6 with an empty bounded checklist). Complete sequence: one report, no repair episode. The executor's implementation evidence receipt in the work order is executor evidence and was treated here as VER-001 treated it: claims under test.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes) in Claude Code 2.1.261 with sandboxed Bash; macOS on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 in this worktree's own dependency tree. Reasoning effort `max`: the session's model control reported it at launch and the harness exposes it to the shell as `CLAUDE_EFFORT=max`, but `docs/discovery/environment.json` records `effectiveEffortReadback` for `claude-code` as `not found`, so `harness-readback` is not admissible and the epistemic source is `self-reported`, exactly as VER-001's was. `2.1.261` was checked against `claude --version` on this machine and is the newest entry on the observed `versions` list, appended by the verifier (adjudication 6); `max` is on `sessionEffortSelector.values`; so the attestation is admissible, and a dry run of the completion command in a fixture repository carrying a copy of the discovery record accepted exactly this header and refused a mismatched one. The order declares `reviewer any`, so any attested effort would satisfy it; `max` is stated because it is what the session reports.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-028 passes.** The order asked for time to become a recorded fact of the control log instead of a fact recoverable only from one machine: every appended transition stamped with the host's wall-clock append time, a fold that never reads it, a projection that shows per-phase elapsed time where both endpoints carry it and `unknown` otherwise, and a one-time labeled recovery of the times already lost to unpushed local refs, all without a schema bump, a new transition, a dependency, or an edit to any historical event. The deliverable does that. `recordedAt` is stamped after `schemaVersion` on every event the lifecycle appends and validated before the write; the fold validates a present value with its ordinal and is otherwise blind to it, which this review proved on the real 138-event log by deep-equality across five mutations of the field; `status --json` and `current.md` carry the latest time and the `elapsed` entries, and the live `verification` value of 889,287 ms is the arithmetic difference of the two recorded stamps; `resume times` prints the three labels, refuses a missing, mismatched, foreign-scope, malformed, or non-commit ref with an empty stdout and an untouched log, projection, and ref set, and reproduces the committed 135-event observation byte for byte once the CLI's trailing newline is accounted for; all 120 recovered times were re-derived here through a third git path with zero mismatches; the three committed ref captures hash as claimed and this session's own captures show the ref set growing only by the refs the order's transitions legitimately minted; the migration notes are dated and placed where the guide requires; and the suite is green, whitespace is clean, `package.json` and the lockfile are byte-identical to `v0.5.0`, and the size delta matches the receipt. The ideation breakout's promoted surfaces were digested as subject: the analogy correction is consistent across README, Principle 4, the inspirations register, and the ledger, the superseded entry is named and left intact, the two new supports are marked candidate everywhere they appear, the Entropy Reducer guide's five named exports and five links resolve and its claimed residue mismatch is real, and the clean-room screen over all 1,985 added lines found nothing. VER-001's five findings are one inherent property of a dated observation, two message-specificity observations, one pre-existing WO-018 wording, and one bounded reading of operator intent that the operator can widen; this review adjudicates all five as non-blocking, records four measured observations of its own, corrects one planning projection, and changes no code, contract, acceptance, schema, authority, or captured-evidence byte.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **The lifecycle scripts are the subject this time.** `scripts/resume.mjs` and the new `scripts/lib/control-time.mjs` are the deliverable, and they are the machinery that allocated this `FINAL-001` path, minted checkpoint 5, stamped `recordedAt` on the `FinalReviewRequested` event, and will record this verdict; their `statusProjection` is also what `worktree publish` and `release close` consult through `scripts/worktree.mjs` and `scripts/release.mjs`, both of which this order edits. The guide's rule applies directly: an append-only log with a regenerated projection fails loudly, so dogfooding is correct. Compensating controls, none of which trusts the shipped assertions: subject identity established with git objects, not the script; the real CLI driven end to end in a fixture repository outside the repository at a symlink-free path; `fold`, `statusProjection`, `controlTimeProjection`, `validateRecordedAt`, and `recoverControlTimes` imported directly and exercised on inputs constructed in this session, including the real log under mutation; every recovered time re-derived from `git log -1 --format=%cI` on the event's named ref, a third formatting path beside the executor's `%ct` and the verifier's `%cd` with `format-local`; SHA-256 of the observation, the log slice, and the ref captures; and `for-each-ref` set comparisons before and after every state-changing action of this review.
2. **`check-surfaces` ran through the subject's own code.** `release check-surfaces` lives in `scripts/release.mjs`, whose control-state reader now calls the changed `statusProjection(events)`. The body-profile rule and the notes parser (`scripts/github-body.mjs`, `scripts/release-notes.mjs`) are unchanged by this order. The five `PASS` rows were cross-checked by eye against the README block, the three package manifests, and the tag list, and `scripts/test-release.sh` passed in both `npm test` runs.
3. **The discovery record that admits actor attestations was edited inside the subject twice.** The executor appended the observed Codex CLI `0.153.2` persisted selector value `max` (with `observedAt`, the previous `xhigh` observation retained, and an explicit scope note that it is not effective-session readback), which is what admits the executor's `--effort max`; the verifier appended the observed Claude Code `2.1.261` (with `observedAt`), which is what admits the verifier's attestation and this reviewer's. This review confirmed `2.1.261` independently with `claude --version` and read the harness's `CLAUDE_EFFORT=max` from the environment; it cannot independently confirm the Codex persisted selector from a Claude session and relies on the receipt's operator attestation and the bounded discovery observation the executor recorded, both in the append-never-replace form the guide prescribes (adjudication 6).
4. **This reviewer's own attestation is checked by the mechanism that checked the verifier's.** A successful `final-review-result` append proves the header above equals the flags and nothing about the subject; the actor inventory below was compared by object comparison in this session, and the fixture dry run proved the refusal path is live.
5. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile** through `check-surfaces`, which listed both files in its `PASS github-body-profile` row. Because `v0.5.1` is above the latest published tag, these notes become the `v0.5.1` edition at release close.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-028 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-04T20:56:59.067Z`, and `elapsed` `{"implementation":"unknown","verification":889287}`; `docs/control/current.md` agreed line for line and no disagreement warning was emitted. Before invoking the transition this review read the `resume` mapping in `package.json` and the complete `scripts/resume.mjs`, `release.mjs`, and `worktree.mjs` diffs and the new library, as the guide requires. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-028/FINAL-001.md` (the directory itself is created by the reviewer), moved the phase to `final-review`, appended `FinalReviewRequested` with `recordedAt` `2026-09-04T21:08:16.677Z`, and minted checkpoint `1e9541338a022a0dc3c079008ba9c81e2091ed4e` (`refs/dotln/checkpoint/WO-028/5`). `docs/control/resume.jsonl` is a strict append over `HEAD`: its first 134 lines hash identically to `HEAD`'s file (`69473f41…`), and the appended events are `WorkOrderActivated` (with `effortDeclarationValidated: true` and no `recordedAt`, because activation predates the field), `ImplementationReady` (`20:38:52.814Z`), `VerificationRequested` (`20:42:09.780Z`), `VerificationCompleted` (`20:56:59.067Z`), this review's `FinalReviewRequested`, and the `FinalReviewCompleted` the pass records. Every event carries `schemaVersion: 1`, and the four stamped events increase.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree at review open as tree `5e5347da…` and again after the allocation as `cf1ad501…`, and `git diff-tree -r --name-status` against each checkpoint reported:

| Checkpoint                                 | Committer date              | Differences from the working tree                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------ | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`c65982b7…`)              | `2026-09-04T15:38:52-04:00` | every deliverable path (34 paths); the checkpoint's tree equals `HEAD`'s, so the whole subject, the work order's own amendments included, is downstream of activation                                                                                                                                                                             |
| `/2` implementation-ready (`0cd80824…`)    | `2026-09-04T16:38:52-04:00` | `docs/control/current.md`, `docs/control/resume.jsonl`, `refs.after-readiness.txt` (added), `docs/discovery/environment.json`, `docs/verifications/WO-028/VER-001.md` (added), and the work order (the executor's eight-line "Readiness recorded" paragraph describing event 136 and the post-readiness capture, written after the transition it describes) |
| `/3` verify allocation (`b003dcb9…`)       | `2026-09-04T16:42:09-04:00` | `docs/control/current.md`, `docs/control/resume.jsonl`, `docs/discovery/environment.json` (the verifier's one appended `2.1.261` line, VER-001 D4), `docs/verifications/WO-028/VER-001.md` (added)                                                                                                                                                  |
| `/4` verdict (`2d00111d…`)                 | `2026-09-04T16:56:59-04:00` | `docs/control/current.md`, `docs/control/resume.jsonl`                                                                                                                                                                                                                                                                                        |
| `/5` final-review allocation (`1e954133…`) | `2026-09-04T17:08:16-04:00` | `docs/control/current.md`, `docs/control/resume.jsonl`                                                                                                                                                                                                                                                                                        |

Every deliverable blob is therefore identical across the verify-allocation and verdict checkpoints and the working tree; the only post-readiness changes to the subject are the executor's own receipt paragraph and ref capture (before verification began) and the verifier's single discovery line (disclosed in VER-001 D4); VER-001 has not changed since its verdict was recorded. All five checkpoints have `HEAD` as their sole parent. The local ref set went from 122 refs in the executor's before-capture to 125 at review open (exactly `WO-028/2`, `/3`, `/4`), to 126 after the allocation (exactly `/5`); the completion mints `/6`.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. `docs/intake/{chats,images,notes}` in this worktree each hold only `.gitkeep`, and `git status --porcelain --ignored -- docs/intake` is empty, consistent with the receipt's statement that the canonical capture lives only in the main checkout; that capture and the backup archive were inspected by metadata only (3,381 bytes at `docs/intake/notes/WO-028-expanded-ideation-2026-09-04.md` in the main checkout; the archive `DotLn-intake-20260904T195218Z.zip` beside the repositories at mode `0600`), and neither was opened. `.claude/settings.json` is tracked and unchanged. Two empty `.claude/.cc-writes` directories exist on disk, one at the worktree root and one inside `docs/discovery/control-event-time-evidence-2026-09-04/`; they are harness write-tracking artifacts, git does not track empty directories, and `git add -A --dry-run` lists no path under either, so neither can enter the reviewed commit (adjudication 11).

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                                 | Report header                                                                                                                                                                                                                                                                                                                                                | Declared minimum | Result                  |
| -------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- | ----------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.2","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`             | there is no executor report; the receipt states the operator selected the model and effort, `0.153.2` is on the `codex-cli` observed `versions` list, and the persisted `model_reasoning_effort` selector now records `max` for that version with the previous `xhigh` observation retained, so `max` was admissible at append                                | `xhigh+`         | `max` exceeds the floor |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}`         | exactly one column-one header in VER-001, parsed and compared with the event actor as JSON: equal                                                                                                                                                                                                                                                             | `xhigh+`         | `max` exceeds the floor |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`          | the header above                                                                                                                                                                                                                                                                                                                                             | `any`            | satisfied               |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: none` and the JSON form's `effortDrift` is `[{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.261; model claude-fable-5-1; effort max; source self-reported`, the same drift line, phase `closed`, a `finalReview` elapsed value beside the verification value, and legal actions `release-close, next, activate`.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree before any review artifact existed: `All matched files use Prettier code style!`, `GitHub body profile tests passed`, the shell suites (fixture temp-root, publication checker, backup-intake, resume with its `time append`, `time projection`, `time validation`, and three time-blind `cmp` lines, checkpoint with its `time recovery` line, worktree, and release), `PASS index coverage: 175/175 product headings indexed` with both audience editions `CURRENT` (27 and 42 linked sections), `tsc -b --force`, `# tests 173 / # pass 173 / # fail 0 / # skipped 0`, and the identity corpus `# tests 8 / # pass 8 / # fail 0`, exit 0. Second run, after this review's correction and the three package artifacts were in place, with the same result.

### Static, surface, and structural checks

- `git diff --check` clean before and after this review's edits.
- `npm run release --silent -- check-surfaces`, twice: `PASS release-block: observed v0.5.1; expected v0.5.1 (work-order target v0.5.1; latest published v0.5.0)`; `PASS component-version` for `@dotln/compiler` `0.2.0`, `@dotln/kernel` `0.2.1`, and `@dotln/skeleton` `0.5.0`, each `src unchanged` against `v0.5.0`; `PASS github-body-profile`, first with no bodies and then listing `docs/final-reviews/WO-028/PR.md` and `RELEASE-NOTES.md`.
- `git diff --stat -- package.json package-lock.json packages/` is empty; the only new executable module is repository-local.
- Line counts: top-level `scripts/*.mjs` 4,395 against 4,378 on `main` (+17); `scripts/lib/` 285 against 150 (+135, all of it `control-time.mjs`). Both match the receipt and VER-001.
- The `.prettierignore` addition (`docs/discovery/control-event-times-*.json`) is the only reason the one-line-per-event observation survives `prettier --check`; the pattern is scoped to the dated observation family.
- `docs/lineage/idea-ledger.md`: the diff contains zero removed lines; the superseded `12-Days-of-Christmas restart framing` entry is present and unmodified. `docs/decisions/` is untouched. Under `docs/work-orders/`, only WO-028's file changed, so the operator-authored WO-020 and WO-021 drafts are untouched as the order requires.
- Every relative link and anchor in the 20 changed or new Markdown files resolves: 401 targets, zero missing files, zero bad anchors (GitHub slugs recomputed from the target headings).
- Clean-room and secret screen over all 1,985 added or new lines (the diff's `+` lines plus every untracked file): no credential shape, private key marker, internal hostname, email address, ticketing-product name, managed-host or gateway description, or profanity; the only `token` hits are the sentences stating that no token telemetry is added, and the only `v1` hit is `schema-v1` in the ledger, so the predecessor-system term did not re-enter.

### Independent probes against the deliverable

- **Observation reproduction (criterion 4, VER-001 F1).** `recoverControlTimes(repoRoot, events.slice(0, 135))` returns 20,149 bytes; the committed file is 20,150 bytes; the library output plus the `\n` that `main` appends on stdout equals the file exactly. SHA-256 of the file is `6fea01da8e80db4a83967a0a138b6b1ac6d6e9aef0e139df7a0518cebdf1b51e` and of the first 135 log lines `f58fb9fdfaf7b716d2bfd79ad965f33be651f80d087c27383ad7cfc4952dfc18`, both as the receipt states. A bare `npm run resume --silent -- times` over the current log differs at line 2 (`eventCount` 138, `recordedAt` count 3), as VER-001 F1 predicts.
- **Third-path re-derivation of every recovered time.** For each of the 120 `recovered-from-local-checkpoint-ref` rows, `git log -1 --format=%cI` on the event's named ref, converted with `new Date(...).toISOString()`, equals the observation's `time`, and `git rev-parse` of the ref equals the event's recorded `checkpointSha`: zero mismatches on either. The 15 `unknown` rows are exactly the events with neither field, breaking down as 1 `WorkOrderActivated`, 6 `ImplementationReady`, 3 `VerificationRequested`, 3 `VerificationCompleted`, and 2 `RepairRequested`, matching the work order's observed gap.
- **Ref captures.** `refs.before.txt` and `refs.after-tests.txt` are byte-identical (122 lines, SHA-256 `b633771fe846588cb2b78503788a30400d78154c58437bfb3755a21ebf81c024`); `refs.after-readiness.txt` (123 lines, `ac3b6247…`) differs from them by exactly the added `refs/dotln/checkpoint/WO-028/2` line; this session's own captures differ from the before-capture by exactly `/2`, `/3`, `/4` at review open and additionally `/5` after the allocation.
- **Fold time-blindness on the real log (criterion 2).** `fold()` and the non-timing portion of `statusProjection()` are deep-equal on the 138-event log across five mutations: `recordedAt` removed from every event, the stamped values reversed across events, every record's key order reversed, every stamp forced to `2099-01-01T00:00:00.000Z`, and every event including the historical ones given a synthetic increasing stamp. Nine malformed shapes injected at ordinal 138 (two fractional digits, `+00:00`, a space separator, month 13, September 31, `null`, a number, a leading space, a lowercase `z`) each refuse through both `fold` and `statusProjection` with `invalid recordedAt at line 138`. An absent key passes; an explicitly `undefined`-valued key refuses, which is the stricter and correct reading of "present".
- **Elapsed arithmetic and scoping (criterion 3).** The live `elapsed.verification` of 889,287 ms equals `Date.parse("2026-09-04T20:56:59.067Z") - Date.parse("2026-09-04T20:42:09.780Z")`; reversing the stamped values yields the signed `-196966`; a synthetic sequence with an interleaved event of another work order between an activation and its readiness ignores the foreign event.
- **The real CLI end to end (criteria 1, 3, 4).** In a fixture repository at a symlink-free path under the session temporary directory (VER-001 D6), carrying a copy of `resume.mjs`, `scripts/lib/`, and the discovery record: `activate`, `implementation-ready`, `verify`, `verification-result pass`, `final-review`, and `final-review-result pass` appended six events at `21:07:45.506Z`, `…46.767Z`, `…46.945Z`, `…48.213Z`, `…48.389Z`, and `…49.695Z`, every one canonical and increasing, each with its own checkpoint ref; `status --json` reported phase `closed` with `elapsed` `{"implementation":1261,"verification":1268,"finalReview":1306}` and `current.md` the three matching `Elapsed` lines; `times` over the untouched log counted 6 recorded, 0 recovered, 0 unknown with 0 refs read. Stripping `recordedAt` from the activation (which keeps its ref) and from the verify request (dropping its ref too) produced counts 4 / 1 / 1 with 1 ref read, the recovered value `21:07:45.000Z` equal to the ref's committer date `17:07:45-04:00`, a byte-unchanged log and projection, and after `next` the mixed projection `{"implementation":"unknown","verification":"unknown","finalReview":1306}`. Overwriting the activation's `checkpointSha` produced exit 1, `checkpoint SHA mismatch at line 1`, and zero bytes on stdout; deleting the ref produced exit 1, `missing local checkpoint ref at line 1`, and zero bytes on stdout. The same fixture accepted this reviewer's exact attestation and refused `final-review-result` when the header's effort differed from the flag.

### Reading

The complete diff of every changed file and every new file was read: the resume, release, and worktree scripts, the new library, both test suites, the control projection and log, the discovery record, the observation and its evidence directory, the work order with both receipts, VER-001, the README, the playbook, the docs map, the seven product documents, the inspirations register, the ledger, the map, the publication index and both editions, and the Entropy Reducer guide. The publish, surface, notes, and body gates were read from `worktree.mjs`, `release.mjs`, `release-notes.mjs`, and `github-body.mjs` rather than inferred, and the actor admissibility rules from `resume.mjs`. The ignored intake was not read.

## Acceptance criteria

**1. `recordedAt` on every appended transition. PASS.** Reproduced by the shipped suite in both runs and by the fixture's six real transitions; on the real log, events 136 through 139 carry the field and increase. The append path stamps and validates before writing; the bad-clock case leaves the log byte-identical.

**2. Time-blind fold; malformed refuses with its ordinal. PASS.** The masked `cmp` passed in-suite for absent, reordered, and backwards variants; the stronger real-log deep-equality across five mutations passed here; nine malformed shapes refuse at their ordinal.

**3. Elapsed projection in `status --json` and `current.md`; siblings unchanged. PASS.** Live values, the mixed fixture, and the arithmetic check above. `grep -rn "current\.md" scripts/*.mjs scripts/lib/*.mjs` matches only `resume.mjs`'s own path constant and warning string; `worktree.mjs` and `release.mjs` consume `statusProjection` and pass their suites.

**4. `times` labels, appends nothing, and matches the observation. PASS.** Three labels, both refusals with empty stdout, the byte-identical rerun, the byte-exact 135-event reproduction, and the third-path re-derivation of all 120 recovered times.

**5. No ref pushed, moved, or deleted; checkpoint suite additive. PASS.** The captures and this session's own set comparisons above; the `test-checkpoint.sh` diff is a pure append before the final success line.

**6. Dated migration note and hand-edit-free projection. PASS.** `03-architecture.md` §Session lifecycle & resilience carries the four-paragraph dated note; `07-execution-guide.md` §Model-specific notes carries the matching note after the WO-019 note; `current.md` has been regenerated only by lifecycle transitions, including this review's, and `status` emits no disagreement warning.

**7. Green tests, clean whitespace, no dependency, size reported. PASS.** As detailed above.

## Evidence gate

Every gate item exists and was reproduced: the fixture transcripts for criteria 1 through 4 (the committed transcript's seven lines appear verbatim in both of this session's `npm test` outputs); the `for-each-ref` pair (byte-identical, plus this session's own captures); the real-log `times` output and the committed observation (byte-identical over the first 135 events, hashes verified); the `cmp` results (passed in-suite, superseded by the real-log deep-equality); and `npm test`.

## Write-back duty

All seven are satisfied, as VER-001 found and this review re-read: the architecture note with `recordedAt` and the three labels; the guide's `resume: times` dispatch row and the `status`/`times` read-only sentence; the guide's dated migration note; the playbook in four places, including the "wall-clock spans, including waiting" caveat; the domain model's one-sentence separation of control `recordedAt` from kernel `occurredAt`; the map's WO-021 note in the slot narrative and the catalog row; and the ledger's `adopted` append-time item and `adopted`/`recovered` recovery item. `docs/README.md` additionally gained the phrase, the action list, and the instance-directory description.

## Ideation breakout receipt

Inspected against the guide's list for the eventual verifier and final reviewer:

- **Source treatment and provenance.** Ordinary Shape-First Synthesis is declared and is the right strategy for conversational operator ideation; Direct Draft Fidelity is correctly not claimed, so no direct-filing provenance is owed. The receipt names the canonical capture, the validated backup, the byte comparison that preceded removal of the worktree staging, and the absence of pending reconciliation; this review confirmed the worktree intake is placeholders only and the canonical file and archive exist, by metadata.
- **Traceability to operator intent.** The three subjects the receipt names each land on a surface: the corrected analogy (README, Principle 4, inspirations, ledger), success under growth with routine observation (vision, pattern library, roadmap, ledger, map), and Entropy Reducer use (the guide, pattern library, architecture, playbook, docs map, ledger). VER-001's fidelity check against the capture stands; this review did not reopen the intake.
- **Consistency with settled decisions and vocabulary.** No ADR, resolution, generated residue, or historical run record was edited; the residue's literal-first wording (`RESIDUE.md` lines 11 and 44) is exactly the mismatch the guide names, and the guide records it as a pending typed correction rather than editing the generated file. The vocabulary is the repository's own (`candidate`, `preserved`, `adopted`, `vision`).
- **Version and schema effects.** None beyond the `v0.5.1` assignment the operator made explicitly and the roadmap's recorded opt-out default; no schema, contract, or package changed for the breakout.
- **Internal cross-references.** All resolve (the 401-link check above), including the two new anchors the index, vision, and map cite.
- **Speculative choices not presented as settled.** Success under growth and the bounded system baseline are `Candidate —` headings, `vision` in the audience index, `preserved` in the ledger, and explicitly select no collector, scheduler, threshold, or cadence; the map lists both the Shape-First correction and the baseline as unallocated.
- **Executable helpers.** None was created for the breakout; the only new executable is the work-order library with automated coverage in both suites.
- **Neutral public language.** The roadmap's observation table uses neutral system measures; the operator's personal-routine framing survives only as a guard clause in the ledger explaining why the public language is neutral.

## Verification-sequence soundness

One verification, one pass, no repair episode. VER-001 was allocated at event 137 and its verdict recorded at event 138 with a header equal to the event actor; its elapsed span is 889,287 ms. The verifier changed exactly one subject byte outside its report, the appended discovery version line, disclosed in D4 and permitted by the guide's instruction to append a newly observed version rather than replace one; every other deliverable blob is identical across the verify-allocation and verdict checkpoints. The verifier's compensating controls were themselves reproduced or exceeded here rather than accepted: its two fixture repositories by this session's own, its `%cd` re-derivation by `%cI`, its four real-log mutations by five, and its hashes by recomputation.

## Adjudications

### 1. The dated observation is not reproducible by a bare `times` run (VER-001 F1)

Confirmed and bounded. Reproduction is `recoverControlTimes` over `events.slice(0, 135)` plus the newline `main` appends; the file's `eventCount` says so. A regenerated observation would be a backfill, which the order lists as a non-goal. Non-blocking; the reproduction recipe is carried into the PR body.

### 2. `checkpointRef` shape is validated only when recovery needs it (VER-001 F2)

Confirmed by reading `recoverControlTimes`: the shape, scope, and SHA checks sit inside the branch that runs only when `recordedAt` is absent and a ref is present. `fold` has never validated ref shape, and a stamped event's ref is irrelevant to its time. Non-blocking.

### 3. A git failure during a required recovery loses the ordinal (VER-001 F3)

Confirmed by reading `runGit`'s call site: the missing-ref, wrong-type, and SHA-mismatch paths are line-scoped, but a thrown git error propagates raw. The contract (refuse rather than guess) holds and the shipped no-git case covers the path that reads no ref. Non-blocking; a one-line wrap is a candidate for the next scripts order.

### 4. `at line N` is an event ordinal (VER-001 F4)

Pre-existing WO-018 behavior applied consistently to the new message; the observation file and the docs say "ordinal" while the message says "line", which coincide as long as the log has no blank lines, because `parseControlEvents` filters blank lines before indexing. Time-indexed as not a WO-028 defect. Non-blocking.

### 5. One bounded narrowing of operator intent (VER-001 F5)

The ledger and pattern library attach the success-under-growth review to a material decision and explicitly decline a mandatory handoff after every small edit. The entry is `preserved`, the section is a candidate, and nothing is settled by it; the operator can widen the reading at merge or in a later ideation. Non-blocking; recorded in the PR body's open items.

### 6. The verifier appended one discovery observation that this review also relies on (VER-001 D4)

The appended `2.1.261` line is the form the guide prescribes, carries an `observedAt`, replaces nothing, and changes no readback, selector, or eligibility value; `claude --version` on this machine confirms the value independently. This reviewer's attestation is admissible because of that line, which is disclosed above rather than treated as neutral. The executor's Codex selector observation is the same form and the same reliance for the executor's `max`. No action.

### 7. The module-entry guard does not resolve symlinks (VER-001 D6)

Byte-identical on `main`; this review avoided it by building its fixture at the resolved path. Forward-only and out of scope; a `realpathSync` on both sides is a candidate for the next scripts order.

### 8. The verification and this review exercised the deliverable on the real log (VER-001 D2, D3)

Events 137 through 139 and refs `/3` through `/5` exist because the verifier and this review used the lifecycle; that is dogfooding under the guide's rule, and the ref deltas were accounted for exactly. WO-028's own implementation duration is honestly `unknown` because its activation predates the field; the first `finalReview` duration in the log's history will be this review's own.

### 9. The work order's authority text was amended after activation (this review)

Against the activation checkpoint, the title gained `, v0.5.1`, the release-classification paragraph now records the operator's explicit assignment and the opt-out default, and the two receipts were appended, the last eight lines of the executor receipt after checkpoint 2. The receipt itself records the operator's authority and date for the assignment, which is the marker WO-023's review found missing there; the objective, criteria, gate, duties, non-goals, and assumptions are byte-identical to activation. No action; the operator's merge confirms the record.

### 10. A whole-hour coincidence between two of the order's own timestamps (this review)

The recovered activation time is `19:38:52.000Z` and the recorded readiness time is `20:38:52.814Z`, exactly 3,600 seconds apart at second precision. Both are host-clock stamps from separate processes (`worktree start` and the executor's readiness command), and independent file metadata agrees with each: the worktree's tracked files carry a 15:38 local modification time and the executor's post-readiness capture 16:39. A coincidence, checked so that a later reader does not suspect a derivation; no bearing on any criterion.

### 11. Empty harness directories inside the evidence tree (this review)

`docs/discovery/control-event-time-evidence-2026-09-04/.claude/.cc-writes` exists on disk, empty, beside the same artifact at the worktree root. It is untracked-invisible to git and cannot enter the commit. Recorded so a later session does not mistake it for reviewed content; nothing to change in the subject.

### 12. Smaller consequences recorded without action

`elapsed` entries persist after an order closes until the next activation clears them, which the architecture note states. Recovered times render `.000Z` by normalization, which the observation's `recoveredPrecision` field states. The playbook's actor table now names GPT-6 Astra for Codex while WO-023's control events keep `gpt-5.6-sol`; history is unchanged, and the guide's model-specific note dates the default. The observation's per-event lines are one physical line each by design, hence the `.prettierignore` entry.

### Candidates considered and not sustained

- **`statusProjection` now taking events is a breaking contract.** Not sustained: it is a helper of the repository's own scripts, not a package export; both callers changed in the same diff; the release notes still disclose it under "Read before upgrading".
- **The observation should include events 136 through 138.** Not sustained: a dated observation of the activation log is the deliverable, and a backfill is a listed non-goal.
- **`elapsed` should sum retries or exclude failed attempts.** Not sustained: the design names the latest completed attempt, the note says so, and the suite's retry case pins it.
- **Publishing timing widens the privacy threat model.** Not sustained as a defect: operator-review assumption 1 and the migration note make the public-profile choice explicit and the field optional; no other field was added.
- **The executor's post-checkpoint edit of the work order.** Not sustained: the eight lines describe the readiness transition and its capture, which can only be written after the transition; they change no authority text and were captured by checkpoint 3 before verification began.

## Corrections applied by this review

One correction, in the documentation lane `docs/final-reviews/README.md` grants, proven contained by diffing against checkpoint 5 (15 insertions and 11 deletions in one file) and followed by a re-run of the checks it could affect: the full `npm test` (which runs the pinned `prettier --check` and `check-publication.mjs` against the real repository), `git diff --check`, and `check-surfaces`, all clean.

1. **`docs/planning/work-order-map.md`:** the snapshot paragraph, the slot bullet, and the WO-028 row's evidence and disposition cells now record VER-001 pass, FINAL-001 pass, the reviewed state committed and awaiting operator merge, and the separately authorized release close that tags `v0.5.1`. No other row changed.

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet, no guard was lost, and no behavioral or evidence defect was found in the deliverable that any criterion covers. The items below are recorded and not scored.

1. **The dated observation reproduces only over the first 135 events, plus the CLI newline** (adjudication 1).
2. **`checkpointRef` shape is validated only when recovery needs it** (adjudication 2).
3. **A git failure during a required recovery loses the ordinal** (adjudication 3): candidate for the next scripts order.
4. **`at line N` is an event ordinal** (adjudication 4): pre-existing.
5. **The success-under-growth review is read as decision-attached, not edit-attached** (adjudication 5): `preserved`; the operator can widen it.
6. **The module-entry guard does not resolve symlinks** (adjudication 7): pre-existing; candidate.
7. **The compiled Shape-First support carries literal-first wording**: an unallocated typed follow-on named in the map; the guide states the dispatch correction.
8. **`CLAUDE_EFFORT` remains an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9).
9. **The executor's model and effort live in the control log and the verifier's report, not in a committed executor artifact.** Recorded, as every reviewed code order since WO-013 has recorded it.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step and the sandbox.** `gh --version` and `gh auth status` in this sandbox both fail before their root command exists (`failed to read configuration: open ~/.config/gh/config.yml: operation not permitted`), the same failure every reviewed order since WO-006 has met. The reviewed commit is made here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation, and notes parse run against the exact commit; if it refuses at the `gh` preflight it pushes nothing and the operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute command the publisher prints: change directory into the main checkout and run this worktree's `scripts/release.mjs close WO-028 --publish` with node, outside the sandbox and where `gh` is authenticated. Because `v0.5.1` is strictly above `v0.5.0`, the reviewed subject helpers finish this worktree through their own classifier and closed-state gate, fast-forward main, run `npm ci` and the evidence gate, assemble the five-section edition from these notes, create and push the annotated `v0.5.1` tag, and create the matching GitHub Release. If Release creation fails after the tag push, the rerun is `npm run release -- close WO-028 --publish` from updated main.
- **Operator confirmation at merge** of the bounded reading in adjudication 5, so the record shows whether the success-under-growth question is meant to follow every decision or only material ones.
- **Attribution:** the reviewed commit carries a plain subject and no attribution trailer, per the operator's standing rule (`~/.claude/CLAUDE.md`) and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins, as the WO-017, WO-018, and WO-023 reviews recorded.
- **Ledger duty:** discharged by the executor's two dated sections; this review adds no ledger entry because every open item above is either already in VER-001 or is a candidate this report names for the next order.

## Proposed PR

- **Title:** `:sparkles: WO-028: control-event time; every control transition records its host append time, status projects the latest completed phase durations, and a read-only times command labels historical times recovered from local checkpoint refs; v0.5.1`. The `:sparkles:` precedent is PR #28 for WO-023 and PR #25 for WO-008, chosen because the order adds a recorded field, a projection, and a command rather than restructuring existing behavior; `:recycle:` (PR #27, WO-018's consolidation) was considered and not chosen for that reason.
- **Body:** `docs/final-reviews/WO-028/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commit (the committed surface check requires the closed control state, and it also makes the map's "FINAL-001 pass" cell true); the reviewed state is committed with a plain subject. The publisher is then run from this worktree:

```bash
npm run worktree -- publish WO-028 --title ':sparkles: WO-028: control-event time; every control transition records its host append time, status projects the latest completed phase durations, and a read-only times command labels historical times recovered from local checkpoint refs; v0.5.1' --body-file docs/final-reviews/WO-028/PR.md
```

The helper runs `check-surfaces --committed WO-028` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-028`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff in the absolute subject-helper form. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, the operator runs that printed command with the main checkout as the working directory; the expected outcome is the annotated `v0.5.1` tag and its GitHub Release.

## Method

Single reviewer, single session, no delegation: every read, probe, and command named in this report was performed by the reviewer directly. Read order: the execution guide's dispatch, ideation, closeout, discipline, and model-specific sections; the control projection and log; the work order in full including both receipts; VER-001 in full; the final-review and verification READMEs; the WO-023 final review with its PR body and notes for the package shape and publish precedent; then the diffs of every changed file and every new file in full, with the publish, surface, notes, and body gates and the actor admissibility rules read at source. The ignored intake was not read. Subject integrity was established through git objects. Evidence was re-established first-hand: two full `npm test` runs, `check-surfaces` twice, `git diff --check` twice, the line counts against `main`, the observation and log-slice and ref-capture hashes, the byte-exact 135-event reproduction, the third-path re-derivation of all 120 recovered times, the real-log deep-equality across five mutations, nine malformed shapes, the elapsed arithmetic, the foreign-order scoping probe, the isolated fixture driving the real CLI through six transitions with both refusals and the attestation dry run, the 401-link resolution, the actor inventory by object comparison, the control-log hash comparison, and the clean-room and secret screen. Authority used: one documentation correction and the three package artifacts; no edit to any code, test, contract, schema, discovery field, verification report, or acceptance byte. Nothing was restored from a checkpoint, no destructive git command was used, and no remote, tag, or `gh` mutation was performed before the reviewed commit; the direct `gh` invocations were `gh --version` and `gh auth status`, both of which failed before their root command existed, and the one `git ls-remote --tags` used to confirm the published `v0.5.0` target was read-only.
