# WO-161 FINAL-001 — final review

**Verdict:** pass. WO-161 asked that every cold-start surface, the playbook and the ADRs say only true things about confinement on the operator's host: no host sandbox in Claude Code, Codex or Copilot, with DotLn's refusals and the host permission mode as the boundary; that the posture be recorded as a dated decision; and that the fail-open detector be named for what it does. [VER-001](../../verifications/WO-161/VER-001.md) found every criterion met but failed the order on its heading's promise, because six generated skills still carried the removed approval path's "need no Git escalation" clause and two hand-written surfaces still stated a sandbox-on posture as current. The [repair](../../evidence/WO-161/repair.md) fixed all eight findings at the generator and on the named surfaces ([D009](../../evidence/WO-161/decisions.md#wo-161-d009) to [D011](../../evidence/WO-161/decisions.md#wo-161-d011)), and [VER-002](../../verifications/WO-161/VER-002.md) passed all six criteria. This review read the whole subject diff, reproduced the grep, the byte table, the historical-authority immutability and the confined-row projection itself, re-ran every check and ran the product gate on the subject: 37 suites passed and none failed, at the code identity VER-002 judged. I found no new defect. `main` has not moved since activation, so this review integrated nothing.

**Subject:** [`docs/work-orders/WO-161-sandbox-vocabulary.md`](../../work-orders/WO-161-sandbox-vocabulary.md) on branch `wo-161`, uncommitted over HEAD `852861d337fefb3f4c42694db237f60a6e9ea5c6`, which is `main`, `origin/main` and the `v0.49.0` tag. The dispatch checkpoint is `refs/dotln/checkpoint/WO-161/9` (`1a886a36`).
- Against VER-002's result checkpoint `/8` (`099ed296`), the dispatch checkpoint differs only in `docs/control/current.md`, `docs/work-orders/README.md` and the `FinalReviewRequested` row.
- VER-001's recorded `reportHash` equals the report's current SHA-256 (`0d592ad7…`), and so does VER-002's (`2ac2feeb…`).
- The order's text differs from HEAD only by the `(v0.50.0)` heading label that `release prepare` wrote, so the six criteria judged here are the original ones.

The judged subject is the working tree: 66 tracked paths against HEAD (the detector module renamed, one fixture and one test file added, the rest modified) plus the order's untracked records under `docs/control/orders`, `docs/evidence/WO-161`, `docs/verifications/WO-161` and `docs/final-reviews/WO-161`. The product gate below recorded tree `4e659995f41f24246fc2cfaddb0c7f8af48ef8cc` and code identity `17655ddca8c6450bac73e611c0d97632f3ac4eabb8e40bdb555cc4768381a590`, the identity the repair's final gate and VER-002's gate recorded. No branch commit existed before this review.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-fable-5-1","effort":"xhigh","source":"claude-session-readback"}

Human actor: the operator dispatched `resume: final review` in a Claude Code session after selecting Fable 5.1 with `/model`, and made no other choice during the review. The harness version is from `claude --version`. The model is this session's model as the host reports it. Effort `xhigh` is the host's `CLAUDE_EFFORT` value read by this session: selected effort, not effective. The order asks for `reviewer any`. No subagent was planned or spawned: 0 of the cap of 20 (`harness usage`: exact-observed). The source and hand-written diff is small enough to read whole, the generated surfaces are judged by their deterministic checks, and two verifications had already covered the diff with four lenses and then none; one reviewer reading everything and re-running every check was the smaller plan.

**Process cost:** entry 86268 tokens; handoff 3845550 tokens; source claude-transcript-message-usage

Both readings are `dispatch` scope, from `node scripts/harness.mjs usage cfbb7e16-d621-47ec-bafe-c0bb3bcf69ca`. The entry reading was observed at 2026-09-25T18:27:37.267Z. The handoff reading was observed at 2026-09-25T18:44:56.877Z (86 steps, 78 commands, 0 subagents of cap 20, exact-observed), after the judgment text was complete and before the result transition. Both include reused cached input. Reasoning tokens and dollar cost are unavailable from this counter, which means unknown, not zero. The largest wait was the product gate (602.12 s), during which this session read the subject through the live-gate read list.

## Goal-aligned judgment

The order is truth-of-text work with a rename, not a lettered critical-path gate (receipt 028). Its mission contribution is removing a recurring operator rescue: agents reading, every session, that a sandbox and an approval path exist which the host does not have. The question for this review was whether the corrected text is true on this host and stays true by construction, without changing any refusal, host setting or historical record.

- **Rule beating** decided how I checked. Criterion 1's grep is a token test, and VER-001 showed it passing over a sentence that still implied the removed approval path. So I did not stop at the grep: I read every changed sentence in the generated skills and the floor, ran the approval and escalation grep VER-002 added, and read the generator so the sentences cannot regress without a process-debt assertion failing.
- **Seeking the wrong goal:** the measured outcome is what a cold start says, not the byte count. The bytes fell in every role anyway (executor 25,819 to 25,723; verifier 22,698 to 22,526; reviewer 23,780 to 23,608), and no ceiling moved.
- **Policy resistance / fixes that fail:** the rename keeps every recorded identity string (`npm test -- --inside-sandbox`, `needs: outside-sandbox`, the `sandbox` diagnostic field), so no historical gate row or consumer is invalidated, and the executable method script plus the runner fixture pin that the confined-row projection is unchanged.
- **Shifting the burden:** the fix is at the generators and regenerates the consumers; the WO-161 role baseline and the escalation-clause assertion mean a later role edit cannot quietly bring the old sentences back.
- **Naive Interventionism:** the discovery Seatbelt, the workers' `--sandbox workspace-write`, the 129 historical authority records and the user-level Claude setting are untouched, and the two out-of-scope sentences VER-001 met stay boarded with register rows rather than folded into this order.
- **NoOp**, withholding the pass, would keep a subject whose criteria, both verifications and this review's own reproduction all agree on, and leave every session reading the corrected text only in a branch.
- The remaining lenses are immaterial here. One gate and no agents is the whole load (tragedy of the commons, escalation). No ceiling or standard was lowered (drift to low performance). The detector was kept rather than removed because it still serves a genuinely confined host (success to the successful), which operator-review assumption 2 records.

## Criteria

- **Criterion 1:** met. `git grep -n -i sandbox -- CLAUDE.md .claude/skills .agents/skills` exits 1 with no output, and a plain `grep -rn -i` over the same paths agrees. At HEAD the same grep returns 11 lines: the residue line and the two loadout sentences in each of the executor, verifier and reviewer skills of both roots. The residue is now `CLAUDE.md:70` "Pre-effect envelope checks unavailable in this harness; host permission settings decide." The approval and escalation grep (`escalat|approval|outside command|confine`) returns only the new conditional product-gate sentence in the verifier and reviewer skills and the executor's Decision Receipts and Operator Check-In support sentences, none naming a host approval path. I reproduced the before and after bytes from `git show HEAD:` and the installed files, and they match [`vocabulary-before.json`](../../evidence/WO-161/vocabulary-before.json), [`vocabulary-after.json`](../../evidence/WO-161/vocabulary-after.json), [`cold-start.json`](../../evidence/WO-161/cold-start.json) and both verifications:

  | Role | Before (HEAD) | After | Sandbox-line bytes before → after |
  | --- | ---: | ---: | ---: |
  | Executor | 25,819 | 25,723 | 1,350 → 0 |
  | Verifier | 22,698 | 22,526 | 1,811 → 0 |
  | Reviewer | 23,780 | 23,608 | 1,811 → 0 |
  | Release close | 14,649 | 14,632 | 150 → 0 |
  | Planner | 16,797 | 16,780 | 150 → 0 |
  | Refuter | 16,372 | 16,355 | 150 → 0 |

  The figures are identical in `.claude/skills` and `.agents/skills`. The other every-session surfaces carry only true senses: the manifest names `packages/skeleton/dist/src/discovery-sandbox.js`, the discovery Seatbelt, and the generated hooks carry the retained `sandbox.disable` effect id. Outside hashes, snapshot ids and the compiler version, the fourteen regenerated hooks differ from HEAD only in `authorityEnvelopeId`.
- **Criterion 2:** met. The [playbook](../../PLAYBOOK.md) (lines 92–99), [`README.md`](../../../README.md) (lines 245–253), [product 07](../../product/07-execution-guide.md) (lines 269–277 and the renamed §Host-confinement preflight at 2398–2401) and ADR-0003 to ADR-0005 state the posture recorded in [`docs/AI-HARNESS-SECURITY.md`](../../AI-HARNESS-SECURITY.md) lines 48–64: no host sandbox in Claude Code, Codex or Copilot, with DotLn's refusals plus the host permission mode as the boundary. Each separates the retained Codex `workspace-write` plus `on-request` checkpoint procedure, and the playbook now says a full-access session has no outside-approval path. The [ADR-0003 amendment](../../decisions/0003-personal-ai-harness-security.md#amendments) names 2026-09-25 and the operator's direction, including Copilot, and names what it supersedes and what stays in force; ADR-0004 and ADR-0005 cross-reference it. The security note's 2026-09-25 reaffirmation and the docs index's dated §Host-posture correction point the same way. I screened every added line for the clean room: the host settings appear by posture only, and no setting file, credential, internal identifier or employer material was copied.
- **Criterion 3:** met. The detector is `scripts/lib/host-confinement.mjs` exporting `detectHostConfinement`, the flag is `--confined-partial` (`--inside-sandbox` now prints the usage error), the fixture root prefix is `dotln-host-confinement-`, and product 07's heading is §Host-confinement preflight with a dated note. `node scripts/test-runner.mjs --list` prints 27 suites and `--list --confined-partial` prints 25, the counts VER-002 showed byte-identical against a `git archive` extraction of HEAD; I carry that two-tree comparison forward with its evidence. On this host the gate below ran with no confinement in force and the full selection. For the simulated confined host, `node docs/evidence/WO-161/partial-method.mjs` exited 0 here: its no-build projection equals both [`partial-before.json`](../../evidence/WO-161/partial-before.json) and [`partial-after.json`](../../evidence/WO-161/partial-after.json) in `checkId` (`npm test -- --inside-sandbox`), the evidence-reference suffix, `needs` (`outside-sandbox`), `partial`, `excludedSuites` and `requiredSuites`. The committed runner fixture, with its build row, asserts `["build", "alpha"]` and the preserved identity constants, and passed in the gate's `runner-fixtures` suite. A search of `scripts` and the package sources and tests for the old names finds only the preserved identity constant and its assertion, and the race probe's own directory path.
- **Criterion 4:** met. `contributor.ts:222` reads `contributor.bounded`, the constraint reads "DotLn refusals and host permission settings define the boundary", the support title reads "Bounded contributor authority", and the `sandbox.disable` effect id is unchanged. `git diff HEAD --name-only -- docs/evidence` lists only `docs/evidence/current.json`, so all 129 tracked `authority.json` files, every one of which carries `contributor.sandboxed`, are byte-identical; WO-161's revisions `001` and `002` carry `contributor.bounded`. [`writer.test.ts`](../../../packages/skeleton/test/writer.test.ts) asserts the new label with the unchanged allowed and denied effects, the three outside-write grants and the one-writer limit; at HEAD no test asserted the old label, as VER-001 found. `executor-supports.test.ts` and `authority-evidence.mjs` reproduce the historical hashes `fnv1a64:06245f5c581212f1` and `fnv1a64:87aa6e1263d6d74d` by restoring exactly the three old labels, and the current build's identity is `fnv1a64:fcf61a6d699e7ffd`. The process-debt test pins the repaired role bytes through `wo161-role-baseline.json`, chained to WO-158's snapshot, and forbids the escalation clause in every generated role. `skeleton`, `process-debt` and `authority-evidence` passed in the gate.
- **Criterion 5:** met. `npm run harness -- check` reports 31 generated surfaces. `node scripts/authority-evidence.mjs --check`, `npm run evidence:artifact -- --check`, `npm run evidence:verification -- --check`, `node scripts/feedback-evidence.mjs --check` (carrying WO-159's live audit by reference) and `npm run evidence:console -- --check` all pass; `docs/evidence/current.json` selects authority `WO-161/002` and the other three editions at `WO-161/001` ([D004](../../evidence/WO-161/decisions.md#wo-161-d004), [D011](../../evidence/WO-161/decisions.md#wo-161-d011)). `node scripts/harness-context.mjs --check` exits 0 with every bounded cold start within its `docs/control/budgets.json` ceiling: executor 25,723 of 29,246, verifier 22,526 of 25,151, reviewer 23,608 of 24,576, release close 14,632 of 16,384 and planner 16,780 of 24,576; the refuter has no ceiling. Every role fell, so the standing 2026-09-17 route was not needed and no acceptance was recorded.
- **Criterion 6:** met. `npm test -- --review` passed 37 suites with 0 failed in 602.12 s (81 fresh tasks, exit 0), recorded 2026-09-25T18:44:18.416Z at tree `4e659995…` and code identity `17655ddc…`; the row's confinement field reads marker `claude-code`, probe written, `inForce: false`, so the full selection ran. `npm run test:docs` passed after this report was written (see Checks). `git diff --check` and `git diff --check --cached` are clean. The diff adds no lint or type suppression. The lockfile and manifests change only `@dotln/compiler` `0.19.0` → `0.19.1` and `@dotln/skeleton` `0.42.0` → `0.43.0` with their workspace pins, so no dependency was added.

## Findings

None requiring repair. Two items are recorded here rather than routed anywhere:

- **Open question for the operator, under product 07 §Precedence: the ADR Status lines and `## Original decision — 2026-09-01` headings.** The order directs "ADR-0003 gains an amendment (ADR-0004 and ADR-0005 a cross-reference)", and product 07 admits an ADR change only through its Amendments section. The repair also rewrote each ADR's Status line to point at the amendment and renamed each `## Decision` heading, and [D009](../../evidence/WO-161/decisions.md#wo-161-d009) records why: an unchanged "Accepted" label under a sandbox-on title would hide the current posture from a reader arriving at the top of the file. I judge this a documented deviation and not a defect. The decision bodies are byte-identical against HEAD, the annotations say only what the Amendments say, and `git grep` finds no inbound `#decision` anchor to these files outside historical records, so nothing decided was falsified and no link broke. VER-002 left the question to final review and the operator; it stays open here. The operator may confine the change to the Amendments sections in a later docs pass; D009's own reopening condition (an annotation contradicting its amendment) is the trigger to act sooner.
- **Note, no repair:** the Contributor permissions support still declares `semanticsAdded: ["Keep ADR-0003 through ADR-0005 posture"]`. Read against the amended ADRs, that posture is now the sandbox-off one, so the phrase stays true; it is compiled text, so changing it is a re-mint and belongs to a later order if the operator wants it reworded.

The boarded observations keep their register rows and sit on no criterion's path: [D007](../../evidence/WO-161/decisions.md#wo-161-d007) (`FUP-be91067a3842b44a`), the inherited `evidence-sources` machinery failure, whose five declared sources are unchanged from HEAD so the `--review` selection did not run it; and [D008](../../evidence/WO-161/decisions.md#wo-161-d008) (`FUP-422f1b3cdd4b7330`), product 02's "native sandbox/approval" sentence outside the order's surfaces. The race-probe directory `scripts/probes/gate-sandbox-race/` keeps its name; the order does not list it and both verifications noted it.

## Integration with `main`

None was needed. After `git fetch origin`, `origin/main`, local `main` and the worktree HEAD all read `852861d3`, the `v0.49.0` tag peels to it, and no commit exists in either direction. No merge, retime, stash or re-mint was performed and `npm run worktree -- integrate` was not run. `npm run release -- prepare` against origin's tags reports that `v0.50.0` remains current with no files changed, refreshing only the PR meter.

## Verification sequence

1. **Implementation** (Codex CLI 0.157.0, `gpt-6-astra`, `ultra` recorded `xhigh`/subagents; [D001](../../evidence/WO-161/decisions.md#wo-161-d001) to [D005](../../evidence/WO-161/decisions.md#wo-161-d005); [implementation record](../../evidence/WO-161/implementation.md)): the generator and residue edits, the detector rename, the envelope relabel, the hand-written corrections, the assertion updates, the editions and `v0.50.0`.
2. **[VER-001](../../verifications/WO-161/VER-001.md)** (Claude Code 2.1.282, `claude-opus-5-5`, `xhigh`; four read-only lenses): fail. Every criterion met, but the heading's promise was not: the "need no Git escalation" clause in six skills (F1), two hand-written surfaces stating a sandbox-on posture as current (F2), and six record and naming defects (F3 to F8). The operator accepted failing to repair them ([D006](../../evidence/WO-161/decisions.md#wo-161-d006)); D007 and D008 boarded the two out-of-scope items.
3. **[Repair](../../evidence/WO-161/repair.md)** (Codex CLI 0.157.0, `gpt-6-sol`, `ultra` recorded `xhigh`/subagents): F1 to F8 fixed on the order's existing surfaces, with a regression assertion for the escalation clause, the executable confined-row method, the activation-completion note, the Receipt 028 disposition and authority `WO-161/002` (D009 to D011).
4. **[VER-002](../../verifications/WO-161/VER-002.md)** (Claude Code 2.1.282, `claude-opus-5-5`, `xhigh`; no agents): pass at code identity `17655ddc…`, each finding rechecked on the file where it was found, with a four-case two-tree reproduction of the confined host.

Both verifications ran on a different harness and model from the implementation and the repair. This review shares the verifiers' harness and runs a different model from every earlier phase. Its reproduction and gate are its own evidence.

Ideation receipt 028 judged the order `aligned-with-findings` with two known issues, and [D010](../../evidence/WO-161/decisions.md#wo-161-d010) disposes of both: the generated gate sentence is conditional on the runner's runtime probe and asserts no host constant, so an exported instance's text is true on its own host; and the residue names the permission boundary while the dated ADR records this instance's posture. Neither reopening condition is observed on this subject.

## Checks

Executed by this reviewer on the subject, 2026-09-25:

| Check | Result |
| --- | --- |
| `git fetch origin`; `git log HEAD..origin/main` and the reverse | both empty; `origin/main` = HEAD = `v0.49.0` |
| `git grep -n -i sandbox -- CLAUDE.md .claude/skills .agents/skills`; plain `grep -rn -i` | no output, exit 1; 11 lines at HEAD |
| Cold-start bytes, `git show HEAD:` versus installed, both roots | the table under criterion 1 |
| `npm run build`; `npm run harness -- check` | 31 generated surfaces |
| `node scripts/authority-evidence.mjs --check` | verified; 34 bundle comparisons |
| `npm run evidence:artifact -- --check`; `npm run evidence:verification -- --check` | verified at `WO-161/001` |
| `node scripts/feedback-evidence.mjs --check` | verified; carries `WO-159/feedback-001` |
| `npm run evidence:console -- --check` | selfhost, control, refutations and missing match |
| `node scripts/harness-context.mjs --check` | exit 0; every ceiling within; refuter unset |
| `npm run publication:check` | PASS; both editions CURRENT |
| `npm run release -- check-surfaces --local` | 44 PASS, 0 FAIL |
| `npm run plan -- check` | exit 0 |
| `npm run release -- prepare` (origin tags) | `v0.50.0` remains current; no files changed besides the PR meter |
| `node docs/evidence/WO-161/partial-method.mjs` | exit 0; projection equals both saved files |
| `node scripts/test-runner.mjs --list` / `--list --confined-partial` / `--list --inside-sandbox` | 27 suites / 25 suites / usage error |
| `git diff HEAD --name-only -- docs/evidence` | only `docs/evidence/current.json` |
| Recorded `reportHash` of VER-001 and VER-002 | each equals the report's current SHA-256 |
| `npm test -- --review` (Claude Code shell; no confinement in force) | **37 passed, 0 failed, 602.12 s, 81 fresh tasks, exit 0**, recorded 2026-09-25T18:44:18.416Z; tree `4e659995…`, code identity `17655ddc…`; the 27 product suites plus the machinery suites whose declared sources changed, among them `process-debt` and `authority-evidence`; `skeleton` 293.21 s, `worktree-integration` 113.45 s |
| `git diff --check`; `git diff --check --cached` | clean |
| `npm run test:docs`, after this report and the PR body | **21 passed, 0 failed, 25.17 s, 21 fresh tasks**; rerun after this cell was filled: 21 passed, 0 failed, 24.34 s |

## Evidence gate, write-back, non-goals and assumptions

- **Evidence gate:** satisfied. The grep transcripts are [`sandbox-before.txt`](../../evidence/WO-161/sandbox-before.txt) and [`sandbox-after.txt`](../../evidence/WO-161/sandbox-after.txt), reproduced above; the simulated confined host is the committed runner fixture plus the executable method script; `npm test` ran once at final review, above. No live row, as the order specifies: the feedback edition carries WO-159's audit by reference.
- **Write-back duty:** discharged. Decisions D001 to D011 carry sources and reopening conditions and the index lists them; ADR-0003 carries the dated amendment; `docs/README.md` carries the dated §Host-posture correction and the Config log line; product 07's heading is renamed with a dated note. The register holds `FUP-6f7834c355df7feb` settled by the repair and `FUP-be91067a3842b44a` and `FUP-422f1b3cdd4b7330` allocated to the planner.
- **Non-goals held:**
  - No refusal or host permission mode changed: outside hashes, snapshot ids and the compiler version, the regenerated hooks differ from HEAD only in the envelope id, and `harness-host.ts` is untouched.
  - The discovery Seatbelt (`discovery-sandbox.ts`) and the worker transport's `--sandbox workspace-write` are not in the diff.
  - The operator's user-level Claude setting is outside the repository and was not touched; the security note still records the effective posture as operator-attested.
  - Historical evidence is byte-identical, above.
- **Operator-review assumptions:**
  - 1 held: the amendment records the operator's 2026-09-25 direction and this pass's intake, and no fresh capture was needed.
  - 2 held: the detector stays, fail-open and probed only when a selection needs the outside; only its names changed.

## Handoff

Result: `final-review-result pass`. On pass, this review commits the reviewed series, pushes `wo-161` and opens its pull request. It merges nothing and publishes no release, tag or package.
