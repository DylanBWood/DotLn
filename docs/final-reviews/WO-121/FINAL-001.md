# WO-121 FINAL-001 — presence origin passes review; the order is integrated onto a moved `main` and retimed to v0.27.0

**Verdict: pass.** All six acceptance criteria hold, and I re-derived on the integrated tree the three that carry the order rather than adopting the verifier's word for them: the classifier really is a pure function of `(signal, stamp)`, a stream of activity heartbeats from both a stamped and an unstamped worker really does leave the `away` phase and the cancellation count untouched, and a stalled actor really is written to `actors[…].status` and never to `present`. The verification sequence is honest and complete — VER-001 failed the order on a green-gate clause and found the cause, a repair closed it at its source, VER-002 reproduced the discharge in two environments — and nothing in it charges a criterion here.

What this review did beyond judging: `main` moved five commits during the order's verification, so this is an integrating final review. It merges `main`, resolves eighteen conflicts of which none is in a source file, retimes the unpublished release from `v0.26.0` to `v0.27.0` and the skeleton component from `0.22.0` to `0.23.0` because WO-119 published both labels while this order was being verified, regenerates the harness bundle, the projections and the evidence editions the merge invalidated, and re-runs the affected checks and one product gate on the result. Product 07 §Independent workflows and integration assigns exactly this list to this stage and states that a sibling's publication, a text conflict, a changed tree hash and a version collision are never findings, never a repair and never a new verification. No behavioural code was written by this review.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.274","model":"claude-opus-5[1m]","effort":"xhigh","source":"self-reported"}

Reviewer: this Claude Code session. Harness `2.1.274` read back from `claude --version` and agreeing with this session's `CLAUDE_CODE_EXECPATH` (`…/versions/2.1.274`) and `AI_AGENT` (`claude-code_2-1-274_agent`). Model `claude-opus-5[1m]` as the session exposes itself; because that half is the session's own report rather than an operator statement in this dispatch or an effective readback channel, the source is `self-reported` and no effective model readback is claimed. Effort `xhigh` has an environment readback here, `CLAUDE_EFFORT=xhigh`. The operator's phrase carried no `ultra` selector, so no mode or raw spelling is recorded and no subagent ran: every read, command and judgment below is this session's own.

Dispatch: `resume: final review`, recorded by the session hook as the `final-review` transition with FINAL-001 allocated at this path at 2026-09-17T01:16:28.556Z from checkpoint `refs/dotln/checkpoint/WO-121/9` (`8730c27c…`). I did not repeat the recorded dispatch. This session held the worktree's only writer reservation, confirmed at entry and again at handoff as the same actor `1152d11d…` and owner pid 48036, `alive: true`; the reservation was renewed at 01:39:57.287Z as the gate finished.

**Bypassed requirements: none.**

Instrument disclosure. The subject gates its own review, in two ways. The four presence hooks this order adds are wired in `.claude/settings.json`, so they executed on every tool call, prompt and stop of this dispatch; they recorded nothing and could not: I read back `DOTLN_RESIDENT_STORE` and `DOTLN_RESIDENT_EPISODE_ID` as unset in this session rather than assuming it, and `presence-heartbeat.ts:12-13` returns before any work when the store binding is absent. Separately, the hooks that hold my writer reservation and refuse writes during a live gate are the ones this order re-pins; they refused two of my commands outright — `git merge` and `harness emit`, both of which must rewrite `.claude/hooks/**`, which this sandbox denies — and refused every shell command I issued while my own gate was live, including `writer --show`. L1 names every command that therefore ran outside the command sandbox.

## Subject and the two bases

The subject is [WO-121](../../work-orders/WO-121-presence-signals-with-origin.md), acceptance criteria 1–6 as filed, its operator-review assumption, its recorded non-goals, and the executor's decisions D001–D004 with their same-day corrections. The branch is `wo-121`, working tree uncommitted, with no branch commit preceding this review.

**Two bases, and they differ.** VER-001 and VER-002 both judged the branch at base `1a2d3095cd84119c244edf554ec2b637504776df`, which was `main`, the merge-base and `HEAD` at the time. During this order's verification WO-119 was merged and its release closed, advancing `main` to `9ecedc5a341c74634a7cd901c033a3f9315c1416` — five commits, carrying PR #79, the `v0.26.0` tag, skeleton `0.22.0` and the WO-119 evidence and records. VER-002 saw this move and recorded it as its O2, correctly, as an observation for this stage rather than a finding. This report judges the integrated result and says below which claims were re-established and which are carried forward with their original evidence; it does not pretend the two verifications judged these bytes.

Work preservation: the pre-merge working state is `refs/dotln/integration/WO-121/pre-merge` (`0316f146…`) and the merged state is `refs/dotln/integration/WO-121/merged` (`7ab4a9d…`), beside the nine lifecycle checkpoints. Nothing was reset, cleaned or stashed; the twenty-nine files an aborted first merge attempt left untracked were verified byte-identical to `main`'s blobs and moved aside rather than deleted.

Read scope: the order and every cited section — ADR-0007, 03 §Operator-presence policy and the return-races candidate, `packages/compiler/src/harness.ts`, WO-067's and WO-044's orders, product 07 §Independent workflows and integration and §Goal-aligned decisions, product 08 §PRs and commits and §Release-note edition; the complete subject diff, including all of `presence-signals.ts`, `presence-heartbeat.ts`, the `resident-state.ts` fold, `resident-store.ts`, `resident-host.ts`, the actor and script-episode plumbing, the compiler's harness and presence-schema changes, every changed script and every changed fixture; the whole of `presence-signals.test.ts`; VER-001 and VER-002 in full at their current bytes; `decisions.md`, `implementation.md` and `repair-001.md`; the WO-119 material this merge integrates; and refutation receipts 003 and 004 as the nomination provenance.

## Integration

`git merge main` produced **eighteen conflicts, none in any source file**: the nine generated hooks and `harness-manifest.json`, `docs/control/current.md`, `docs/evidence/current.json`, `docs/lineage/decisions-index.md`, `docs/planning/followups.json`, `docs/product/06-roadmap.md`, both publication editions and `docs/work-orders/README.md`. Every source file the two orders share — `resident-state.ts`, `actor-contract.ts`, `script-episode.ts`, `feedback-audit.ts`, `evidence-sources.mjs`, `harness.mjs` — merged automatically, and I read each merged region rather than trusting the merge. The two surfaces that matter are coherent: WO-119's `scriptResultVerified(spec, payload)` refactor of the observed-outcome branch sits directly above WO-121's `actors[episode:id].status = "stopped"` retirement, and WO-121's stamp environment is applied to WO-119's renamed `sandbox`/`command` locals in the script supervisor. WO-119's discovery producer runs as a `script` actor, so it goes through the one adapter that carries the stamp; nothing else in the catalog is available.

Resolutions: the hooks and the manifest to `main` and then regenerated from source; `06-roadmap.md` keeping both orders' entries; `control/current.md` to `main`, which is correct because the `final-review-result` transition is the only legal writer of that projection; `decisions-index.md` and `followups.json` to `main` and then regenerated by `npm run meta`; both publication editions keeping this order's content with their locks recomputed; `work-orders/README.md` to `main` and then regenerated by the index command. The follow-up register is a true union, checked by entry id rather than by eye: 349 entries against a 348-entry union of both sides, the one addition being this review's own D005 row, and nothing from either side missing.

**Retiming.** The order staged application `v0.26.0`, compiler `0.13.0` and skeleton `0.22.0` above the then-observed `v0.25.0`. WO-119 consumed `v0.26.0` and skeleton `0.22.0`; the tag `v0.26.0` resolves to `9ecedc5`, the commit this review merged. `release check-surfaces --local` charged exactly that: `FAIL component-version @dotln/skeleton: src changed; observed 0.22.0; previous v0.26.0 0.22.0`. `npm run release -- prepare`, on origin's tag observation, retimed the application target to **`v0.27.0`** and wrote the order heading, the README claim and the dated roadmap note; the skeleton is retimed by hand to **`0.23.0`** in the package, in `HARNESS_HOST_VERSION` and in the lockfile workspace entry, because `release prepare` deliberately never alters component versions. Compiler `0.13.0` is unchanged and free: `main` publishes `0.12.0`. The classification is untouched — the same additive minor D003 declared — and D003 itself is preserved rather than rewritten, because it was correct at its recorded subject; the correction is [D005](../../evidence/WO-121/decisions.md#wo-121-d005), whose reopening condition D003 named literally.

**Evidence editions the integration invalidated, and how each was settled.** Re-emitting the bundle moved the recorded bundle diff, so `authority-evidence --check` refused revision 002 by name and instructed a new revision; revision **003** is recorded and selected, and 002 is preserved untouched. That nothing behavioural moved is a measurement, not an assertion: `authority/003/authority.json` is **byte-identical** to `authority/002/authority.json`, and only `bundle-diff.json` differs. The feedback edition went stale for a different and genuine reason: the merge changed the declared feedback source set itself — WO-119 adds four declared sources (`discovery.ts`, `discovery-actor.ts`, `discovery-sandbox.ts`, `work-candidate.ts`) and edits four more that WO-121 also touches — so the audited `subject` no longer describes the current sources. I diffed the recorded report against a freshly computed one and **exactly one field differs, `subject`**: the policy hash, all ten fixtures, the context projection and the maturity table are identical. `validateSelfhost` asserts the live log's subject equals the current source subject, so the existing logs cannot be carried forward and the shortcut is closed by design; edition **WO-121/003** therefore carries a fresh operator-run live `codex-cli-exec` audit, which is the same handling WO-039 and WO-048 recorded and the reason that step is not reviewer work.

## Ideation receipt

The order carries no operator-opened ideation section and `docs/evidence/WO-121/` holds no `ideation.md`, so the receipt is the 2026-09-08 critical-path pass and its refutation receipts. Receipt [003](../../planning/refutations/2026-09-08-critical-path-003.md) held the whole plan on this order's criterion 1 and named it the largest gap: "WO-121 explicitly treats non-resident-launched session heartbeats as human, although those sessions can keep working after away… launch provenance is not evidence that a person returned." The hold required three things: a negative fixture proving that activity from both a resident-launched and an operator-launched worker ends neither `away` nor cancellation, proof that an explicit `back` or a genuine human interaction does end `away`, and origin as a function of something other than the launch path. Receipt [004](../../planning/refutations/2026-09-08-critical-path-004.md) records the accepted disposition and passes the order as thesis-advancing with capability row `new:runtime.presence-origin`.

The implementation answers the receipt literally, not approximately. `classifyPresenceSignal(signal, stamp)` takes no launch path and no process input, and the module contains neither; the fixture at `presence-signals.test.ts:106-132` feeds every event × interaction × stamp combination under three different launch paths and asserts identical decisions, which is the hold's third requirement made structural. The negative stream at `:134-256` runs all four events stamped and the three activity events unstamped after an explicit `away`, then asserts `present === false`, unchanged machine state, generation and current episode, and `kills === 0`, for all six disposition combinations — the hold's first requirement. Both return signals end `away` in the same loop, and the stamped scripted prompt does not, which is the second. The receipt's own worry — that a labelled-origin fixture could pass while real activity falsely triggers return — is answered by `:532-606`, which runs the actually emitted hook files as processes and asserts exactly nine observations and `present === false`.

## Verification sequence

| Report | Verdict | What it charged | Answered by |
| --- | --- | --- | --- |
| [VER-001](../../verifications/WO-121/VER-001.md) (Claude Code 2.1.274, `claude-opus-5[1m]`, xhigh/`ultracode`) | fail | F1: `npm test` not green — `release:case:runtime_refresh` failed 3 of 3 runs, under the gate and isolated in and outside the sandbox, with `unable to read tree` and `hardlink different from source`; root-caused to automatic maintenance in the disposable fixture origin racing the integrator's local clone. Criteria 1–5 met and reproduced; O2 recorded a vacuous literal-only assertion in criterion 2's fixture | Repair 001 |
| Repair 001 (Codex CLI 0.154.0, `gpt-6-astra`, xhigh/`ultra`) | — | Disabled `maintenance.auto` and `receive.autogc` on the disposable repositories before first data and added `-c maintenance.auto=false` to all five clone sites; removed O2's assertion and its unused import; Trace2 evidence recorded the repack/clone overlap directly | [D004](../../evidence/WO-121/decisions.md#wo-121-d004) |
| [VER-002](../../verifications/WO-121/VER-002.md) (Claude Code 2.1.274, `claude-opus-5[1m]`, effort unknown) | pass | No finding. F1 discharged by reproduction rather than citation: the case passes 31.08 s in-sandbox and 22.00 s outside; `npm test` exits 0 outside the sandbox at 19 suites, 261.12 s. O2's removal verified not to weaken criterion 2. O2 of this report flagged the moved `main` for this stage | this review |

The sequence is sound and I did not re-litigate it. Two things in it deserve explicit endorsement. VER-001 recorded plainly that the race it found was pre-existing and unrelated to presence, while still failing the order because criterion 6 requires a green gate — the right call on both halves, and it labelled its pre-existence claim an inference (its L2) rather than dressing it as observation. VER-002 declined to inherit VER-001's prediction that an outside-sandbox run would clear the two WO-068 environmental cases and ran it, and it caught and disclosed its own method error (L4: a first comparison read `git show :<path>`, the index, on files that are modified but unstaged, making the comparison vacuous). Both are the behaviour the floor asks for.

## Acceptance, judged at the integrated tree

| # | Criterion | Result |
| --- | --- | --- |
| 1 | Origin classification; negative away stream; both return signals; stamped scripted prompt inert; launch-path purity; all pinned | **Met, re-established here.** The fixtures run 6 passed / 1 skipped on the integrated build, the skip being the native-script case under the nested sandbox, which `scriptAvailability()` skips cleanly by design. Purity is structural: `classifyPresenceSignal` admits only `(signal, stamp)`, and the installed profiles never produce a human heartbeat because `recordHarnessHeartbeat` writes `interaction: "unknown"` — the rule fails toward `actor`, as the order's design requires |
| 2 | Discretionary return handled per phase; explicitly requested foreground work continues | **Met.** Both return signals drive kill / finish / foreground; `kills === 1` only for `kill`, the machine returns to `present` with `current === null`, and in the `discretionary:false` case the episode completes across the return with `kills === 0`. The compiler refuses `discretionary:false` with anything but `inFlightOnReturn:"finish"`. VER-001's O2 is gone and the criterion never rested on it |
| 3 | A stalled actor is a liveness failure, not a presence change | **Met.** Stall is written only by `expireActorHeartbeats` into `actors[…].status`; `present` moves only on human origin and the human idle threshold. The fixtures separate stall at t=35, human idle at 40 and phase decay at 140 on independent clocks, prove replay is ambient-clock-free by throwing from `Date.now`, and assert stall during `away` with a `kill` that fails the test if called |
| 4 | The regenerated bundle differs from the base only in the heartbeat hooks and pins; `harness check` passes; the WO-039 hook fixtures pass | **Met, re-established against the new base.** Parsing each of the nine pre-existing hooks into wrapper and config and comparing against `main`: the only differences are the runtime snapshot id, the inline and configured pin entries with their hashes, `compilerPackageVersion` 0.12.0 → 0.13.0, `skeletonVersion` 0.22.0 → 0.23.0 and the derived `policyHash`. No rule text, no unit content, no logic line. `settings.json` is +20 lines with **zero** deletions; `.claude/skills/**` and the hand-written Clean Room floor are untouched. `harness check` passes at 28 generated surfaces |
| 5 | Write-backs land: 03 §Operator-presence policy, ADR-0007 §Amendments, ledger entry | **Met.** 03 gains the three origins, the event-kind rule and the explicit never-reads-launch-path/process-table/input-device rule, and cites WO-044's C-W10/X-W10 for the installed-profile fallback; I checked those discovery rows and they support the claim. ADR-0007 carries the dated amendment. The pre-2026-09-09 ledger substitution is marked in `docs/work-orders/README.md` and the index now carries D001–D005 |
| 6 | `npm test` green; `git diff --check` clean; no new dependency; a fresh feedback evidence edition | **Met at the integrated tree.** The gate row is below. `git diff --check` and `git diff --cached --check` are clean. The lockfile moves only the two first-party workspace versions, with no third-party dependency added, removed or re-resolved. The feedback edition is fresh twice over: the executor's live edition 002 for the order's own hook change, and edition 003 recorded here because integration moved the audited source set |

## Independent checks executed on the integrated tree

| Command or observation | Result |
| --- | --- |
| `npm test -- --review`, outside the command sandbox | **28 suites passed, 0 failed, 591.34 s, 72 fresh tasks, 0 reused**, exit 0; recorded 2026-09-17T01:39:55.390Z as `host-gate:2e27f968c12c8e74e10b4d225c6f73c8a5c59e4af5786e7671f734cff00cbf4f:npm test` at exact tree `1dd69bd1…` |
| `node scripts/harness.mjs evidence` | **Final evidence passed**, exit 0. The matched row is my own gate above, not a reused or inherited one; `git diff --check` executed at the same tree, exit 0, recorded 01:40:09.036Z |
| `npm run build` on the merged source | clean — the first evidence that WO-119's and WO-121's sources compose |
| `node --test …/presence-signals.test.js` on the integrated build | **6 passed, 1 skipped, 0 failed**; the skip is this order's own native-script case under the nested sandbox, which `scriptAvailability()` skips by design |
| `node scripts/harness.mjs check` | exit 0, 28 generated surfaces |
| Nine pre-existing hooks vs `main`, each parsed into wrapper and config | **0 differences** outside the runtime snapshot id, the inline and configured pin entries with their hashes, `compilerPackageVersion`, `skeletonVersion` and the derived `policyHash` |
| `.claude/settings.json` vs `main`; `.claude/skills/**`; the Clean Room floor | +20 lines with **0 deletions**; skills and instruction surfaces byte-unchanged |
| `npm run release -- check-surfaces --local` | PASS at every axis: release block `v0.27.0`, compiler `0.13.0` and skeleton `0.23.0` both "expected a different version", kernel and console "no bump required", every license pin, five refused publish dry-runs, and the PR body's one-line-per-paragraph profile |
| `npm run publication:check` | both editions CURRENT at 30 and 45 linked source sections |
| `authority-evidence`, `artifact-identity-evidence`, `verification-evidence`, `feedback-evidence` `--check` | all four pass |
| `authority/002/authority.json` vs `003` | **byte-identical**; only `bundle-diff.json` moved |
| `feedback-002/feedback.json` vs `003` | **exactly one differing leaf, `.subject`**; the live re-audit reproduced ten passing regressions, ten removal failures and the same 1,192-byte projection |
| `node scripts/console-fixtures.mjs --check` | five cases match; the selfhost fixture's pin to the immutable `feedback-002` path stays projectable under compiler 0.13.0, so no re-record was needed |
| Follow-up register, compared by entry id | 349 entries against a 348-entry union of both sides; **0 lost**, 1 added (this review's D005 row) |
| `git diff --check`, `git diff --cached --check` | clean, before the gate and after staging every reviewed output |
| `git log --oneline main..HEAD` | empty before this review's commits — no branch commit preceded final review |
| Clean-room and suppression screen over every added line | no employer material, credential, internal URL, private identifier or secret; the only external hosts are two public `git-scm.com` documentation links cited by D004, the npm registry in the lockfile, and an `example.invalid` fixture host; **no** lint or type suppression directive added |

## Observations carried forward, none blocking

1. **In every currently installed harness profile, no hook heartbeat can ever be human.** `recordHarnessHeartbeat` always writes `interaction: "unknown"`, because WO-044's C-W10 and X-W10 observe no typed/scripted discriminator, so criterion 1's human-prompt path is exercised by fixtures and by a future adapter, never by today's installed hooks. That is the order's own operator-review assumption and its declared fail-toward-`actor` rule, and 03 and the release notes say so plainly. It is worth an operator's attention because it means explicit `away`/`back` remain the only human signal in practice today.
2. **Ingress rejects a forged origin, which the criteria never asked for.** `decodePresenceObservation` recomputes the classification and refuses any observation whose recorded `origin` disagrees, so a hand-written or replayed event cannot assert `human`. Both verifications noted it; it survives the repair and the merge intact.
3. **The heartbeat budget is a resident configuration value with a 30 s default and no observed basis yet.** D002 says so and names the reopening condition — measured hook gaps. Nothing depends on the number today because stalls change no authority.
4. **VER-001's L2 and VER-002's L2 stand.** Neither established the release-fixture race's pre-existence by executing the case at `main`; the repair added Trace2 evidence of the repack/clone overlap, which is a mechanism observation rather than an error-signature inference, but no run at `main` exists. Nothing in this verdict depends on it: the case passes now, and the question bears only on how the repair's cost is attributed.
5. **Process cost is partly unknown, and both verifications said so.** VER-001's counters were unavailable because its dispatch was refused by the writer guard before the session record existed; VER-002's because the session exposed no identifier. This dispatch's counters are available and are reported in the response rather than claimed as a reduction. The process meter in `PR.md` is the table `release prepare` produced during this dispatch, so its reviewer row is a mid-dispatch reading; `release prepare` correctly refuses to run again once the order has passed, and the evidence handoff directs final counters to the response instead of an edited report.

## Limits

**L1 — five commands ran outside the command sandbox, and they are named.** `.claude/hooks/**` is on this sandbox's write-deny list, so four of them had to be: the aborted first `git merge`, the successful `git merge --no-commit main`, the `git checkout --theirs` that resolved the ten generated-bundle conflicts, and `node scripts/harness.mjs emit && check`. The fifth is the product gate itself, run outside so the two WO-068 native cases in L3 could execute rather than fail environmentally. That toll is the same one WO-047's and WO-048's reviews recorded, and WO-033's `worktree sync` remains the named deliverable that removes it. Every other command in this report ran inside the sandbox.

**L2 — the live feedback audit is the operator's, not mine.** A new edition requires a live `claude-cli-print` or `codex-cli-exec` verifier transport and a complete acceptance matrix; a reviewer cannot manufacture one, and WO-039's review recorded why regenerating it is not reviewer work. I recorded and selected the edition from the operator's run and verified it with `--check`.

**L3 — the two WO-068 native cases and this order's own native case need an outside-sandbox runner.** Cases 129 and 130 assert a fixed message and fail inside a nested `sandbox-exec`; WO-121's own native case skips cleanly under the same condition, which is the better-behaved shape and was VER-001's O3. The product gate above was therefore run where those cases can execute.
