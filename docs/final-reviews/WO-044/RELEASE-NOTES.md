## Release overview

This release records what the installed harnesses actually do as unattended writing workers, so the next orders on the external-change and resident chains design from observed rows instead of vendor documentation. Thirty-three labeled rows cover Claude Code 2.1.270 and Codex CLI 0.154.0 launched non-interactively with tools in a foreign Git worktree under that worktree's own settings, hooks and instruction files, and launched by a detached parent with no terminal; each row cites its command shape and a sanitized run file, and the record re-renders byte for byte from those files. It also returns the last step of the source-to-deliverable loop to the machine: release close proves network egress first, worktree removal explains every blocker with its remedy, a closed order's derived worktrees are settled, suite reuse survives permission residue, and a session can stop its own gate and record a cheap diff-only receipt on a failing verdict.

## Read before upgrading

Source release target v0.17.7 includes compiler 0.9.3 and skeleton 0.15.12. Kernel 0.2.1 and console 0.1.5, the domain-event schemas and third-party dependencies are unchanged; the lockfile diff is first-party component versions only.

Security and privacy: the recorded Claude Code posture in the harness security document now sets the user-level `sandbox.allowUnsandboxedCommands` to `true`, so a session may ask to run one command outside the sandbox and the permission mode reviews that ask; the sandbox itself stays on. That setting lives in the operator's own settings file, not in this tree, and the document records the undo. It was directed because regenerating the checked-in hook and skill files and launching an authenticated child harness could not run inside the sandbox at all.

Authority: the `Workflow` tool is admitted as a same-host spawn like `Agent` and `Task`; the harness's task-stop tools are classified as the session's own process control; and `node scripts/harness.mjs evidence --stop` is admitted while a gate holds the tree. A failing verification or review may record only `git diff --check` through `npm run harness -- evidence --fail`; the default command and every passing verdict still require the full suite gate. Every gate process exposes its run lineage to its children in `DOTLN_GATE_RUNS`; suites never see that variable and it never enters a suite key.

Removal and data: closing an order now removes derived detached worktrees that name the order when they are clean, have no active gate and hold no writer reservation, after preserving their non-disposable control and intake material into main's retained lane with byte proofs; anything else is kept and reported with its blocker and the exact removal command. A nested repository is classified explicitly: only `.git` with an unborn HEAD, no refs, no index entries and no objects is disposable scaffolding outside the intake lane, content under the control or intake lanes is preserved as a directory unit, and an unreadable repository is never assumed empty. The feedback verifier removes its own empty mount repository after each run. Nothing in the close deletes other ignored bytes.

Required operator action: release close and the post-merge helper need network egress to the GitHub host; `--dry-run` proves origin reachability before any local prerequisite and names the host, and a sandboxed agent session cannot publish, so run the close from an operator terminal with egress. Suite success records keyed from checkouts carrying non-executable permission residue re-key once on their next run; no migration is needed.

Known limits: authentication lifetime without an interactive login, the scheduler surface and idle cost are `unknown` or `unavailable` by the record's own labels, and Codex project hooks did not fire in exec mode without persisted hook trust. An abandoned writer transition lock or lease refuses registration and teardown until the operator removes the stale entry under the OS temporary directory; the refusal does not yet name that path. The gate reuse-key contract (a docs-insensitive `test:full` key, narrowed per-suite inputs, content-keyed evidence editions) is routed to a planning pass, not changed here.

## Substantive changes

- Discovery: `scripts/harness-probe.mjs --writing-worker claude|codex|report` creates a fresh scratch worktree outside the checkout for every launch, with a fixture file, a trivial test script, a project deny rule, a generated-shape PreToolUse hook that imports its runtime by an absolute `file://` URL and refuses a fixture command, and four instruction surfaces; it records field shapes only, links each fixture operation to its matched request and result, derives launch selectors from the executed arguments, removes its detached spools on every exit, and records detached resident SIGKILL and fresh recovery separately from native background availability. The 2026-09-14 record carries 23 observed, 8 unavailable and 2 ambiguous rows, an `environment.md` addendum and the planning map's checkpoint disposition.
- Release close and worktree teardown: origin reachability is proved before any local prerequisite in the dry run and the publish; the removal guard lists every blocking ignored entry with its lane and remedy; nested repositories are inspected through refs, index and object storage; derived worktrees are pruned, preserved and removed, or kept with a blocker on `finish`, on the new `worktree settle` action and from release close when the subject is already gone; writer registration and teardown exclude each other through a shared lease and an exclusive lock held outside the subject.
- Gate control: a session stops its own gate at the next boundary the runner or evidence command owns, with POSIX suites in their own process groups and a bounded pipe fallback so inherited descriptors cannot hold cancellation open; the lifecycle gate and the evidence runner read one `lifecycleRequiredChecks` definition; suite keys use Git's owner-executable bit alone.
- Harness guard and roles: the adapter-unavailable denial names the read tools and exact commands that remain admitted and accepts a compound only when every `&&` or `;` segment is allowlisted; `Workflow`, `TaskStop` and `KillShell` are classified; every generated role in both harnesses states that an unobserved value is `unknown`, `untested` or `blocked`; the release-close role and handoffs state the egress requirement; cold-start byte ceilings the shared line touches carry real margin.

## Progressive polish

Regenerated hooks, skills and the harness manifest for compiler 0.9.3 and skeleton 0.15.12; authority editions 001 through 005, a new artifact-identity edition, a new verification edition and feedback editions 001 through 003 recorded under WO-044 and selected in the current-evidence manifest; the console self-host fixture re-recorded from the selected feedback edition; refreshed publication source locks; the decisions index, follow-up queue and work-order index; the runner's pinned inventory moved to 82 tasks and 42 release cases; the root README release block names the shipped components; and the process-debt fixture teardown tolerates git's detached auto-gc with bounded retries.

## Evidence and compatibility

Reviewed baseline: `b38c06959d67c7a24b6d2cc8f8e9447fdbbd5db0`. The guarded post-merge helper binds v0.17.7's exact source commit and component versions in the annotated release manifest. [VER-001](https://github.com/DylanBWood/DotLn/blob/main/docs/verifications/WO-044/VER-001.md) through [VER-004](https://github.com/DylanBWood/DotLn/blob/main/docs/verifications/WO-044/VER-004.md) record the four failing verdicts and their repairs; [VER-005](https://github.com/DylanBWood/DotLn/blob/main/docs/verifications/WO-044/VER-005.md) passes with every earlier counterexample rejected at the final bytes; [FINAL-001](https://github.com/DylanBWood/DotLn/blob/main/docs/final-reviews/WO-044/FINAL-001.md) records the review's independent checks and the exact evidence boundary. The [writing-worker record](https://github.com/DylanBWood/DotLn/blob/main/docs/discovery/writing-worker-smoke-2026-09-14.md), its [index](https://github.com/DylanBWood/DotLn/blob/main/docs/discovery/writing-worker-smoke-2026-09-14.json) and [decisions D001 to D018](https://github.com/DylanBWood/DotLn/blob/main/docs/evidence/WO-044/decisions.md) carry the sources, alternatives and reopening conditions.

The supported Node line and the source-only publication controls are unchanged. No migration is required. Effort labels in the record are launch selectors, not effective-session readback; two synthetic limits disclosed by VER-003 stand as disclosed (distinct nested repositories retained at the same path merge as directories; the concurrency row's phrasing under a synthetic launch error); stale current-version headings in the compiler and skeleton package READMEs are deferred to a documentation planning pass.
