The installed harnesses are now observed, not assumed, as unattended writing workers, and a release close no longer costs an operator hour of hand recovery. Thirty-three labeled rows record what Claude Code 2.1.270 and Codex CLI 0.154.0 actually do when launched non-interactively with tools in a foreign Git worktree under that worktree's own settings, hooks and instruction files, and when a detached parent launches them with no terminal: which allowlist form admits editing, whether the project deny rule and a generated-shape hook fire in print mode, what the result envelope and hook input look like, what a SIGKILL leaves behind and how a fresh episode recovers, and which unattended surfaces the CLIs expose. Twenty-three rows are observed, eight unavailable and two ambiguous; every row cites its command shape and a run file reduced to field shapes, and the record re-renders byte for byte from those files. The target bundle, the writer transport profile and the resident launcher are designed from these labels.

Beside the record, the operator's scope expansions fold in the defects that surfaced during the WO-131 close. Release close now proves origin reachability before any local prerequisite and tells a sandboxed session it cannot publish. The worktree removal guard lists every blocking ignored entry at once with its lane and that lane's remedy, classifies a nested repository explicitly (only `.git` with an unborn HEAD, no refs, no index and no objects is disposable scaffolding; content under the control or intake lanes is preserved as a directory unit; an unreadable repository is never assumed empty), and the feedback verifier removes its own empty mount. Closing an order settles its derived detached worktrees: pruned when the directory is gone, preserved and removed when clean, idle and free of any writer reservation, otherwise kept with the blocker and the exact removal command. Suite success keys use Git's owner-executable bit alone, so main's permission residue no longer forks every handed-off success.

The gate is now something a session can finish itself. `node scripts/harness.mjs evidence --stop` is admitted while a gate holds the tree; every gate process names its run lineage to its children, the runner starts no further suite, ends running suites through their process group, and no check is recorded. A failing verification or review records only `git diff --check` through `npm run harness -- evidence --fail`, read from the one shared `lifecycleRequiredChecks` definition that the lifecycle gate also reads, while the default and pass paths keep the full gate. The generated adapter-unavailable denial names the read tools and exact commands that remain admitted and accepts a compound only when every segment is allowlisted; `Workflow` is admitted as a spawn like `Agent` and `Task`, and the harness's own task-stop tools are classified as the session's process control. Every generated role in both harnesses carries the rule that an unobserved value is `unknown`, `untested` or `blocked`.

Verification ran five times. VER-001 found eight defects in the first record and repairs (unborn-HEAD repositories discarded as empty, a live writer's worktree removed, descendants holding cancellation open, absent effects labeled as policy, recorded effort differing from the launched selector, detached spools retained, a mode key that differed from Git, and unproven resident SIGKILL evidence); VER-002 found the skeleton's stale compiler pin and a teardown race with git's detached gc; VER-003 found the documented `--fail` command rejecting its own flag; VER-004 found the README release block naming the previous release's components. Each was repaired with a regression and VER-005 rejected every counterexample at the final bytes. The final review re-rendered the record, ran the probe suite, the harness surface check, the release surface check and a privacy scan, and read the whole subject; see `docs/final-reviews/WO-044/FINAL-001.md`, `docs/verifications/WO-044/`, `docs/discovery/writing-worker-smoke-2026-09-14.md` and `docs/evidence/WO-044/decisions.md`.

Read before merging: the recorded Claude Code posture now sets the user-level `sandbox.allowUnsandboxedCommands` to `true` so a session can ask, with review, to run one command outside the sandbox; that setting lives in the operator's own settings file, not this tree, and `docs/AI-HARNESS-SECURITY.md` records the change and its undo. Authentication lifetime, the scheduler surface and idle cost remain unavailable or unknown by the record's own labels; Codex project hooks did not fire in exec mode without persisted trust. An abandoned writer transition lock refuses registration and teardown until an operator removes the stale entry under the OS temporary directory; the refusal does not yet name that path. The gate reuse-key contract is routed to planning rather than changed here. Compiler moves to 0.9.3 and skeleton to 0.15.12 for application v0.17.7; kernel, console, domain-event schemas and third-party dependencies are unchanged.

The process meter below is a pre-gate snapshot. Later timings and handoff usage remain local; refreshing this snapshot after validation is not a completion requirement.

<!-- dotln-process-meter:start -->
| Work | Phase ms / attempts | Gate ms | Read files / bytes | Observed tokens / USD | Declared prompt tokens | Corrections |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| WO-125 | 31,446,483 (Δ unavailable) / 9 | unavailable (Δ unavailable) | 5 (Δ unavailable) / 33,021 (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 0 (Δ unavailable) |
| WO-128 | 17,829,450 (Δ -13,617,033) / 3 | unavailable (Δ unavailable) | 11 (Δ 6) / 133,462 (Δ 100,441) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 3 (Δ 3) |
| WO-129 | 8,400,234 (Δ -9,429,216) / 5 | unavailable (Δ unavailable) | 6 (Δ -5) / 56,204 (Δ -77,258) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 2 (Δ -1) |
| WO-130 | 50,868,457 (Δ 42,468,223) / 9 | unavailable (Δ unavailable) | 3 (Δ -3) / 45,276 (Δ -10,928) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 4 (Δ 2) |
| WO-131 | 62,546,520 (Δ 11,678,063) / 5 | unavailable (Δ unavailable) | 13 (Δ 10) / 226,560 (Δ 181,284) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 2 (Δ -2) |
| WO-044 | 30,155,143 (Δ -32,391,377) / 8 | 7,015,801 (Δ unavailable) | 1 (Δ -12) / 17,087 (Δ -209,473) | 273,984,614 (Δ unavailable) / unavailable (Δ unavailable) | 657 (Δ unavailable) | 0 (Δ -2) |

Unavailable observations are not zero; unset ceilings are not approvals of a future limit.

| Dispatch | Wall ms (Δ) | Context bytes (Δ) | Commands (Δ) | Tokens (Δ) | Steps (Δ) | USD (Δ) / declared prompt tokens |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| WO-044/executor | 25,263,439 (-31,693,968) | 369,643 (unavailable) | 539 (unavailable) | 213,922,921 (unavailable) | 697 (unavailable) | unavailable (unavailable) / 657 |
| WO-044/verifier | 4,891,704 (765,925) | 103,476 (unavailable) | 245 (unavailable) | 60,061,693 (unavailable) | 269 (unavailable) | unavailable (unavailable) / unavailable |
| WO-044/reviewer | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) / unavailable |
| WO-044/release-close | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) / unavailable |
| WO-044/planner | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) / unavailable |
| WO-044/refuter | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) / unavailable |
<!-- dotln-process-meter:end -->
