# WO-044 FINAL-001 — harness truth for unattended writing workers, and a release close a session can finish itself

**Verdict: pass.** The 33-row writing-worker and unattended-launch record is derived byte for byte from its 22 sanitized run files and labels what it did not observe; the eight release-close, gate-control and role repairs the operator added by two scope expansions and two overrides are implemented with fixtures, and every finding of the four failing verifications is repaired at these bytes and independently rejected by VER-005. This review found no defect that a criterion charges. The completion event binds this judgment to the required passing full gate and diff check at the final pre-transition tree; this report does not claim the timing of a check that has not run at authoring time.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.272","model":"claude-fable-5-1","effort":"xhigh","source":"self-reported"}

Dispatch: `resume: final review`, recorded by the harness as `FinalReviewRequested` at 2026-09-15T03:33:03Z (2026-09-14 for the operator) with FINAL-001 allocated at this path; the dispatch was not repeated here. The harness version is `claude --version`; the model identifier is the session's own. The effort is recorded self-reported: the session environment exposes `CLAUDE_EFFORT=xhigh` and the operator's `/model` output named `xhigh`, but neither is a vendor effective-effort readback and the discovery registry holds none for 2.1.272, so no readback is claimed. The order declares `reviewer any`, so the minimum is met. No other writable agent was used; no operator control was opened.

**Bypassed requirements: none.**

## Subject and integration

The subject is [WO-044](../../work-orders/WO-044-writing-worker-harness-truth.md), criteria 1–8 as amended by the operator's execution record (items 9–16), and the working diff on `wo-044`: 71 tracked files (3,007 insertions, 789 deletions) plus the new probe module, its fixture and suite, the writer-teardown module, the dated discovery directory, the evidence editions, the control log, the five verification reports and this directory. The branch base is `b38c06959d67c7a24b6d2cc8f8e9447fdbbd5db0`; `git fetch origin main` in this session returned the same commit, so no merge, conflict resolution or release retiming was needed and no integration check had to be rerun. The application target is v0.17.7 with compiler 0.9.3 and skeleton 0.15.12; kernel 0.2.1 and console 0.1.5 are unchanged, the lockfile diff is six lines of first-party component versions, and no user setting is in the tree.

Read scope: the order and its cited sections; the discovery record, its index and run listing; the probe script, the probe module (1,902 lines, read in full), its runtime fixture and its stub suite; the release, worktree, resume, path-classification and intake-reconciliation helpers; the writer-teardown module; the gate-evidence, harness-host, harness-command, feedback-selfhost, contributor-loadout and compiler-harness sources; the runner, suite-evidence and lifecycle-evidence changes; every changed test and shell fixture; the product, security, discovery, planning and budget write-backs; the follow-up queue additions; the generated hook, skill and manifest diffs; VER-001 through VER-005; the evidence README, three repair records and decisions D001–D018; the process-meter body; and the publication contract. Generated surfaces and the console fixtures are judged through their generation checks, not by re-reading their bytes. The order carries no operator-opened ideation section; the ideation input resolves to its nomination provenance (the 2026-09-08 critical-path pass, gate C2, refutation receipts 002–004) and to the execution record's dated operator directions, which are the receipts this review holds the amendments against.

## Verification sequence

| Report | Verdict | What it charged | Answered by |
| --- | --- | --- | --- |
| VER-001 (Codex, max) | fail | F1 unborn-HEAD repositories discarded as empty; F2 live-writer worktree removed; F3 descendants hold cancellation open; F4 absent effects labeled as policy; F5 recorded effort differs from launched; F6 detached spools retained; F7 mode key differs from Git; F8 resident SIGKILL unproven | D012 repair: refs/index/object inspection, writer lease and lock, process-group cancellation with a pipe fallback, matched operation evidence and a settings control, per-launch selectors, spool removal in `finally`, owner-executable keying, detached resident kill and recovery rows |
| VER-002 (Claude, max) | fail | F1 skeleton pinned compiler 0.9.2 against a 0.9.3 workspace; F2 process-debt green decided by git's detached auto-gc | D013 repair: pin and lockfile at 0.9.3; bounded fixture-teardown retries with default maintenance left on |
| VER-003 (Codex, max) | fail | F1 `npm run harness -- evidence --fail` rejected its own flag | D017 repair: the paired option parser moved into the emit/check branch; the regression drives the real package command |
| VER-004 (Claude, xhigh) | fail | F1 the README release block named compiler 0.9.2 and skeleton 0.15.11 | D018 repair: the block names 0.9.3 and 0.15.12 |
| VER-005 (Claude, xhigh) | pass | rechecked every earlier finding at its bytes; 16 criteria met | this review |

The tree VER-005 gated differs from the tree reviewed here only by the `FinalReviewRequested` control event and the two phase projections it regenerates (`git diff --diff-filter=M` against the checkpoint taken at that event is those two files), plus this directory's outputs.

## Independent checks executed before the cutoff

- `git diff --check`: clean. `npm ls`: exit 0, the compiler pin coherent.
- `node scripts/harness.mjs check`: 24 generated surfaces pass, so the regenerated hooks, skills and manifest equal the emitted set for compiler 0.9.3 and skeleton 0.15.12.
- Record re-render: the 22 run files rendered into a scratch tree with `--writing-worker report --date 2026-09-14` reproduced the record, the JSON index, the `environment.md` addendum and the map disposition byte for byte. This proves derivation, not the truth of each launch.
- `node --test scripts/test-harness-probe.mjs`: 6 of 6 pass in 32 s, including the VER-001 counterexamples for ambiguous labels, spool removal and the unconfirmed background token.
- `npm run release -- check-surfaces --local`: every surface passes at the v0.17.7 target.
- Privacy scan of the run files, record, index, five reports, evidence README, repair records, decisions and the order for home paths, the user name, absolute path prefixes and UUIDs: the only matches are VER-004 and VER-005 naming their own scan patterns; no identifier reached a committed surface.
- Source reading against the criteria: the nested-repository test requires only `.git`, a symbolic unborn HEAD, no refs, no index entries and no loose, packed, alternate or garbage objects, and treats an unreadable or detached HEAD as non-disposable; intake protection outranks scaffolding; derived teardown reports every blocker with the exact removal command and removes only inside the writer lock after rechecking the writer, cleanliness and active gates; the shared `lifecycleRequiredChecks` is the single source for both the lifecycle gate and the evidence runner; the bootstrap hatch admits a compound only when every `&&` or `;` segment is allowlisted; `Workflow`, `TaskStop` and `KillShell` are classified as recorded.
- `npm run work-orders -- index` after dispatch: the index already reflected this phase; no other byte changed.

The final full suite gate and diff check for the exact tree filed here run through `npm run harness -- evidence` after this report, the PR body and the release notes are frozen; the completion event cites them.

## Acceptance

| Criteria | Review result |
| --- | --- |
| 1–2: labeled rows, hook and settings refusals | 33 rows (23 observed, 0 blocked, 8 unavailable, 2 ambiguous) across 17 Claude Code 2.1.270 and 16 Codex CLI 0.154.0 rows, each with its command shape and run file; C-W4 records the generated-shape hook firing and refusing in print mode, C-W3 records the settings deny with its matched control, and X-W3, X-W4, X-W5 and X-W10 state that no Codex hook event fired and what was launched. Met. |
| 3–4: write containment, instruction surfaces | Inside and sibling writes are recorded for both harnesses; both sibling rows refuse a containment claim because the sibling lies under the OS temporary directory, and the Codex named profile supplies the contrast. Marker echo establishes visibility, not automatic loading, and the rows say so. Met. |
| 5–6: kill rows, unattended rows | Operator-launched and detached resident SIGKILL rows exist for both harnesses with exit, signal, worktree state and a fresh recovery episode on the preserved worktree; authentication lifetime is `unknown` from one launch, the scheduler surface `unavailable` with `--help` as the command, idle cost `unavailable` with its blocker, concurrency observed at 3 of 3, and the no-terminal permission rows are `observed` for Claude with a matched denial and `ambiguous` for Codex. WO-068 designs from these labels. Met. |
| 7: write-backs | Pointer in product 03, addendum in `environment.md`, one-line disposition in the planning map, decisions D001–D018 in the index; the pre-2026-09-09 ledger substitution applies. Met. |
| 8: gate, whitespace, no user setting or dependency change | Whitespace clean; first-party versions only in the lockfile; the session's edit of the operator's settings file was denied and is recorded as operator-applied; the final canonical gate is cited by the completion event. Met. |
| 9: suite keys ignore non-executable bits | `canonicalMode` keys on Git's owner-executable bit alone; the regression covers seven non-executable modes and the release reuse fixture gives main's copy mode 0600. Met. |
| 10: every blocker with lane and remedy; nested repositories | Every ignored entry is classified once with its lane and that lane's remedy; empty scaffolding is disposable outside intake, content is preserved as a directory unit, and the feedback verifier removes its own mount. VER-003's same-path collision limit is carried forward, disclosed. Met. |
| 11: egress first | `ensureOriginReachable` precedes every local prerequisite in the dry run and the publish; the resume, worktree-publish and both role projections state the requirement; the `unreachable` case passes. Met. |
| 12: derived worktrees settled | Pruned, preserved-and-removed or kept with a blocker on `finish`, on `settle` and from release close when the subject is gone; the `derived` release case and the worktree fixture cover the clean, dirty, missing, retained, live-writer and unknown-writer cases. Met. |
| 13: adapter-unavailable denial names its hatch | The denial and the advisory carry the same text naming Read, Glob, Grep and the five exact commands; compound admission is every-segment. Observed live in this session: a `for` loop and a `$(...)` substitution were refused with their classification reason. Met. |
| 14: never-guess line in every role | Present once in all twelve generated role skills. Met. |
| 15: failing verdict records only the diff check | One shared `lifecycleRequiredChecks(verdict)`; the public `--fail` command reaches it and the regression drives the real package entry; the default and pass paths keep the full gate. Met. |
| 16: Workflow admitted as a spawn | Classified beside Agent and Task in source and in the installed permissions hook; the remote-spawn refusal remains. Met. |

## Observations carried forward, none blocking

1. **Abandoned writer transition lock or lease.** Registration holds a shared lease and teardown an exclusive lock under the OS temporary directory, keyed by the worktree's physical path; a process killed inside that brief window leaves a directory that makes every later registration or teardown refuse with "registration or teardown in progress (or lock requires inspection)". D012 and product 07 choose refusal over inferred reclamation, which is the safe choice, but the refusal does not name the directory to inspect. The recovery is to remove the stale entry under `dotln-writer-transition-<uid>` in the OS temporary directory from an operator terminal. Naming that path in the refusal is nominated for the next planning pass beside the reuse-key items already routed there; it is not changed here because a final review makes no undirected source change.
2. **Cancellation floor.** A stopped suite is reported only after the one-second SIGKILL escalation even when its direct child exits at once on SIGTERM, because the group kill must still sweep descendants. VER-002 measured 1,006 ms; the cost is bounded and deliberate.
3. **Disclosed verification limits.** VER-003's two synthetic limits stand as disclosed: distinct nested repositories retained at the same path merge as directories, and the concurrency row's phrasing under a synthetic launch error. D016 routes the gate reuse-key contract to planning. Adjacent item 0008 defers the stale package README versions.
4. **Security posture change is operator-side.** The recorded Claude Code user setting `sandbox.allowUnsandboxedCommands` moves to `true` in the posture document with its undo; the tree does not write the operator's settings. The release notes carry it under read-before-upgrading.

## Publication screening

No employer material, credential, internal URL, private identifier or secret is present; the clean-room screen found no stop condition. The record, run files and reports reduce paths, session identifiers and host names to shapes or placeholders. Commit subjects and the PR text carry no AI attribution.

## Completion boundary and cost

Report measurement cutoff: 2026-09-15T03:42:56Z. At entry the collector read 187,818 total tokens (135,191 cached input) at 4 steps; at the cutoff it read 2,704,689 total tokens (2,305,274 cached input, 360,806 cache writes, 38,093 output) over 86 steps and 82 commands, source `claude-transcript-message-usage`, scope `dispatch`, from `node scripts/harness.mjs usage <session>`. These are cumulative usage, not context occupancy. Monetary cost and reasoning tokens are unavailable from this collector.

The targeted checks above took under two minutes of suite time; reading the full subject was the dominant cost, and no equivalent alternative review cost was measured. Final gate duration and later counters stay in ignored receipts and the handoff response; completion recollects usage before appending. After the passing completion, the index refresh, the commit series and the reviewed publication create no further full-gate obligation.

Mission and critical path: the record is the observed input the target bundle, the writer transport profile and the resident launcher must be designed from (Principle 15), and the repairs return the last step of the source-to-deliverable loop to the machine. NoOp would leave v0.17.6 unpublishable behind a close that costs an operator hour and would design three orders from vendor documentation. Rule beating is checked by the byte-identical re-render, the matched settings control and the every-segment compound rule; shifting the burden is what egress-first, derived-worktree settlement and the session-owned gate stop remove; commons cost favors this single review pass over another sweep of ground four verifications already covered; drift and seeking the wrong goal are checked by the record labeling eight rows unavailable and two ambiguous rather than reaching for a value. Naive Interventionism favors passing the reviewed bytes unchanged and nominating the lock-path message separately. Reopen when a live row's label is contradicted by a later launch, when an abandoned transition lock is observed in practice, or when a derived worktree is removed that a blocker should have kept.
