# WO-051 FINAL-001 — Source-change writer request and transport profile

**Verdict: pass.** All six acceptance criteria remain met on the reviewed tree. The reviewer product gate passed with 25 suites, zero failures and 69 fresh tasks in 597.46 s outside the sandbox, and the document gate passed with 17 suites and zero failures in 150.86 s. No blocking finding remains; the verifier's two non-blocking findings are dispositioned below, one by a documentation correction and one by an explicit handoff to WO-053.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.273","model":"claude-fable-5-1","effort":"xhigh","source":"self-reported"}

Dispatch: `resume: final review`, 2026-09-16, recorded by the harness at `2026-09-16T20:29:00.250Z` with the subject preserved at `refs/dotln/checkpoint/WO-051/5` (`b75aabb72ca940a36517cf2d537a09e47ad6139b`). Claude Code 2.1.273 was observed with `claude --version`; the model is `claude-fable-5-1` as this session exposes it; effort `xhigh` was read from `CLAUDE_EFFORT`. No effective-session readback channel is claimed. The selected order is [WO-051](../../work-orders/WO-051-source-change-transport-profile.md); the complete numbered verification sequence is [VER-001](../../verifications/WO-051/VER-001.md) (pass); the ideation receipt is [ideation-local-models.md](../../evidence/WO-051/ideation-local-models.md); the executor's decisions are [WO-051-D001–D003](../../evidence/WO-051/decisions.md). I read the full staged diff (36 modified and 20 new paths), both changed transport sources against their unchanged inspection blocks, the new environment module, the writer suite, the four fixtures, the discovery record rows the arrays cite, the ignored raw ideation capture, and every promoted document.

## Review choices and integration

The verified branch base and `origin/main` fetched at review are both `0eacb54d070134127f36eebd380239d8c806b86a`; `git ls-remote` confirmed the remote head and that `v0.23.0` is origin's newest release tag. There is no upstream delta, no merge, no text conflict and no version collision, so no retime was needed: `npm run release -- prepare` with origin tag observation reported that the v0.24.0 target remains current and changed no file beyond the process-meter block in `PR.md`. Original verification, receipt and evidence bytes are preserved. All six acceptance claims carry forward with their original evidence and are re-established by the reviewer gate on the same tree.

Reviewer edits are confined to documentation. One product-document correction resolves VER-001 F2: 03 §Ports no longer calls the retained Codex overrides "non-shell hardening"; it now says the writer keeps the inspection profile's hardening minus the shell-tool disables and names `shell_environment_policy.inherit="none"` as untested with the three authorized commands until WO-053's live smoke. That sentence changes no launch array, fixture, contract or acceptance criterion. The two publication source locks were refreshed for it and `npm run publication:check` reports both editions current. The work-order index was regenerated after dispatch (its projection had read `verified`), `npm run meta` reproduced the decisions index and follow-up register byte-identically, and `harness check` found all 24 generated surfaces current, so no bundle emit was needed. No intake or recovery ref was discarded; the worktree-local ideation capture stays in place for the closeout helper's reconciliation.

VER-001 F1 is not carried into the Claude writer array at final review. Adding `--settings '{"autoMemoryEnabled":false}'` would change a pinned launch array and its fixture, which is a behavioral change a reviewer must not write and certify; the order's Objective enumerates the Claude flags without it and C-W2's recorded shape does not carry it. It is a real privacy edge under the cited 09 §Privacy and minimization, so it is handed to WO-053 explicitly: the live smoke should run the pinned array unmodified and observe whether the harness's automatic memory store is written for the worktree; if it is, a bounded order adds the flag with a re-pinned fixture. WO-051-D001's reopening condition ("WO-053's live proof contradicts a recorded launch row") already covers that outcome. O1 (a commit-message path the worker's own `git add -A` will stage) and O2 (the authority expiry inside the durable retry key) remain routed to WO-052 as the verifier recorded; both are host-ownership questions that this order's non-goals fence.

One further reviewer observation, non-blocking: a writer request whose effort is `ultra` normalizes to `xhigh` with subagents mode, but the Codex writer array still passes `--disable multi_agent` and `--disable multi_agent_v2`, because the writer filter removes only the two shell features while the inspection path keeps multi-agent enabled under subagents mode. This fails no criterion: the writer arrays are pinned to X-W1 and X-W2, which ran without a mode, and the fixture pins `xhigh`. It matters only if the operator's `ultra` selection is wanted for a live writer; WO-053 selects that launch and should either accept the disabled features or take the same subagents exception with a re-pinned fixture and its own evidence.

The ideation breakout is judged as part of the subject. The raw capture exists at the receipt's ignored path in this worktree and is not in the main checkout's intake, so reconciliation is pending and belongs to the closeout helper at release close, as the receipt states. The two ledger entries, the 06 candidate and the 07 candidate are rewritten syntheses of the operator's two messages, not the operator's wording: local-model experiments that may need operator setup in LM Studio, a broader comparison across models, repeated runs and prompt or support variation on one pinned order and build, and a work-order type that guides operator setup toward either a checked result or a failure artifact. Both candidates are `planned` rows in the audience index, retain the dated WO-027 deferral and WO-110's inspection-only scope, authorize no setup, download, launch, sequence change or allocation, and the 07 candidate states that a failure artifact never certifies the requested setup as successful. No helper, script or generator was created in the breakout. The clean-room screen finds personal product intent and no employer or secret material.

The rationale in WO-051-D001–D003 remains applicable: a validated writer request beside the untouched inspection contract advances critical-path gate D2 and unblocks WO-052, WO-122 and WO-053 without a live launch. For this review's own choices, NoOp on F1 wins over carrying the flag because the change is behavioral and unobserved; the 03 correction is the smallest reversible edit with no consumer; the sandboxed product gate was skipped on the recorded evidence that it fails two WO-068 fixtures at this code identity, saving one 300 s run; rule beating is addressed by reading the removed-line census and the fixtures' assertions rather than trusting a green suite whose bundle and hooks are themselves in the diff; seeking the wrong goal is kept in view by treating this order as the transport for `repo.write`, not as proof that a model changes source correctly or that anything confines it.

## Evidence

| Check | Reviewer result |
| --- | --- |
| `npm test -- --review`, outside sandbox | **25 passed, 0 failed; 69 fresh tasks; 597.46 s; recorded 2026-09-16T20:46:38.002Z**; the `--review` selection added the release, resume, worktree, work-orders, license, plan-refutation and GitHub-body machinery suites to the product suites |
| `npm run test:docs` | **17 passed, 0 failed; 17 fresh tasks; 150.86 s** over the three reports with only the document-gate figures unfilled; a second run over the final report bytes passed with the same counts |
| `npm run release -- prepare` (origin tags) | Target v0.24.0 remains current; no files changed; origin's newest tag is v0.23.0 |
| `npm run publication:check` after the 03 correction | PASS; both editions current (30 and 45 linked sections) |
| `node scripts/harness.mjs check` | 24 generated surfaces current; local-terms list unavailable |
| `npm run meta`; `npm run work-orders -- index` | Decisions index and follow-up register byte-identical to the executor's; index projection now `final-review` |
| Prettier on the ten changed or new source, test and fixture files and 03 | Clean |
| `git diff --check`; new lint or type suppressions in the diff | Clean; none |
| `git log main..HEAD` before the review commits; `origin/main` | Empty; equal to the branch base |
| `git diff package.json package-lock.json` | Only `@dotln/skeleton` 0.19.0 → 0.20.0; no dependency added or removed |
| `node scripts/harness.mjs writer --show` | One reservation, this session, alive |

Reviewer product code identity: `4f7d49a89bf88ea437ebce0e4ac0ae552cc047eab1fd311f963b78608f10082f`; reviewed tree: `bf4ad2853ef4951e80ee478c07c6c73548214eee`. The gate's evidence reference is `host-gate:4f7d49a89bf88ea437ebce0e4ac0ae552cc047eab1fd311f963b78608f10082f:npm test`; its passing row is retained in the local gate record and carried into the final-review control event for publication. The reviewer transcript is retained under `docs/control/local/wo051-final-product.txt` (ignored). The verifier's code identity (`b90aee57…`) remains its recorded subject; it differs from this one because the working-tree identity is computed over index-tracked paths and the new environment module, writer suite and four fixtures were untracked until this review staged them, as the role directs before the gate. No source byte changed between the two gates. That a verifier's gate identity can exclude untracked new source is recorded here as a process observation for the planner, not a finding against this order.

| Criterion | Assessment |
| --- | --- |
| 1: inspection arguments byte-identical to the activation base, pinned | Met. The diff removes exactly one `import type` line and one `.then(...)` call site from the two transport sources; `wo051-inspection-baseline.json` pins both arrays, the prompt and the four source blocks, asserted by test 1; `worker.test.ts` is unchanged and green. |
| 2: writer validator accepts and refuses as named; inspection refusals re-proven; arrays equal the record's rows with row ids; unavailable or ambiguous rows refuse by name | Met. Test 2 drives 31 refusal shapes through `dispatch` with zero launches; test 1 re-proves both inspection refusals; `wo051-writer-args.json` pins both arrays with their row ids and each flag carries its row in `sourceChangeArgs`; I compared every flag with C-W2, C-W3, C-W4, C-W8, C-W9, C-U2, X-W1, X-W2 and X-W8 in the discovery record; `sourceChangeProfile` throws naming C-W1, X-U2 and X-W3. |
| 3: environment refuses the launchpad, a non-worktree directory and an outside path; accepts a scratch worktree; existing fixtures pass | Met. Test 3 refuses six mounts including a prefix lookalike and a symlink alias, a symlinked message path and a nested launchpad, then accepts the real worktree; `canonicalPath` requires `realpathSync(path) === path`; existing environment and reactor fixtures pass in the gate. |
| 4: process doubles return the six fields plus `observedCommit` only when the double commits; `observedDenials` from C-W8's names or `unavailable` for Codex | Met. Tests 5 and 7 run `dirty` and `commit` for both harnesses against a real worktree and compare with live `rev-parse` and `status --porcelain`; the `spoof`, `wrong-id`, `failed-wire`, `nonzero` and `missing-denials` doubles reject; the tape declares itself synthetic. |
| 5: write-backs in 03 §Ports, the security runbook, the skeleton README and the decisions file | Met, with the 03 wording corrected at review; the runbook's two commands are the ones the verifier and I ran; D001–D003 carry sources, rejected alternatives and reopening conditions and are indexed. |
| 6: `npm test` green; `git diff --check` clean; no new dependency; regenerated bundle pins and a fresh feedback edition | Met by the reviewer gate and checks above; the manifest and nine hooks move only the skeleton version, snapshot id and version pin; `feedback-002` is a fresh edition over the final source identity and `docs/evidence/current.json` selects it. |

The gate is partly an instrument under review: the regenerated hooks refused my own Bash calls while the gate was live, which is the invariant working as specified, and the evidence-source declarations that produced the feedback edition are in the same diff. Agreement with the executor's and verifier's runs is therefore necessary but not sufficient; the criterion judgments above rest on the source reads and fixture assertions named beside them.

## Limits and outcome

Every writer episode is a process double and the wire tape is synthetic. The evidence establishes the argument arrays, the refusals, the parser's strictness and real Git observations against a real worktree; it establishes nothing about how a model behaves under the profile, whether three Bash patterns suffice for a real target, whether an empty inherited shell environment breaks the three commands under Codex, or whether either sandbox confines anything (C-W6 and X-W6 already observed that neither does). WO-052 owns the target host, message placement and episode orchestration; WO-053 owns the live proof and the F1, F2 and multi-agent observations above. No live source-change worker was launched at any phase of this order. The local-terms list is unavailable, so the clean-room screen is a manual read of the authored diff.

Process cost: entry usage observation at `2026-09-16T20:29:20.500Z` reported 176,616 total tokens for this dispatch (source `claude-transcript-message-usage`, scope `dispatch`, cost unavailable). Final counters belong in the ignored usage receipt and the handoff response. The dominant cost was gate wall-clock; the sandboxed product gate was skipped on the recorded evidence rather than rerun.

The reviewed publication is [PR.md](PR.md) and the five-section [release notes](RELEASE-NOTES.md). The commit series separates the implementation with its runtime write-backs and evidence editions, the document-only ideation breakout with its publication index and locks, and the lifecycle records with the three reports. This pass authorizes publication of the WO branch and its pull request only; merge and release close remain the operator's next handoff, and the closeout helper reconciles the worktree-local intake capture before removal.
