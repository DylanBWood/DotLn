# WO-026 final review FINAL-001

**Subject:** branch `wo-026`, worktree `DotLn-wo026`, uncommitted working tree, 2026-09-04 (local; the latest control events carry 2026-09-05 UTC stamps). `HEAD`, `main`, and `origin/main` are all `68b1ab2544765bfa2fdb063fe1be7d8f9f720843`, the PR #29 merge of WO-028 that the published annotated tag `v0.5.1` names (`git ls-remote --tags origin`, read-only, resolves `refs/tags/v0.5.1^{}` to that commit and the tag object to `8a079ab8…`, the same object the generated index records); the branch carries no local commits (`git log main..wo-026` is empty), so the entire subject is the dirty tree. At review open, before this review's own transition: 20 tracked modifications (530 insertions / 459 deletions against `HEAD` including the two `docs/control` files; 18 files, 514 / 447 without them) plus eleven untracked paths: `scripts/work-orders.mjs` (436 lines), `scripts/lib/control.mjs` (185), `scripts/lib/release-records.mjs` (174), `scripts/test-work-orders.mjs` (544), `scripts/test-work-orders.sh` (12), `docs/work-orders/README.md` (77), `docs/product/12-workstream-application.md` (178), `docs/planning/work-order-index-activation-2026-09-04.md` (83), `docs/planning/work-order-index-fixtures-2026-09-04.txt` (12), `docs/planning/work-order-index-references-2026-09-04.txt` (7), and `docs/verifications/WO-026/VER-001.md` (346); 31 `git status --porcelain -uall` entries. The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree differs from checkpoint `refs/dotln/checkpoint/WO-026/4` (the verdict) only in `docs/control/current.md`, the strictly appended `docs/control/resume.jsonl`, and the one generated index row that the verifier's post-verdict refresh and this review's post-dispatch refresh each moved forward (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, two bounded documentation corrections (§Corrections applied by this review), and the lifecycle's own events and index refreshes.

**Authority:** `docs/work-orders/WO-026-work-order-index.md`: `Model: any capable model`; `Effort: executor xhigh+; verifier xhigh+; reviewer any`; a `v0.5.2` patch classification completed on 2026-09-04 under the operator's release-assignment default after activation omitted the target; six numbered acceptance criteria; a five-item evidence gate; a write-back duty naming seven surfaces; a non-goal list; a 2026-09-04 operator-authorized ideation breakout receipt (end-user workstream application) that names this review as owing it an inspection; a 2026-09-04 operator-authorized tag-snapshot amendment; and the executor's implementation evidence receipt. Against the activated text (`refs/dotln/checkpoint/WO-026/1`, whose tree equals `HEAD`'s by `git diff-tree`), the order differs in its title's version suffix, the release-classification paragraph, criterion 1 (restated to permit exactly those edits and the receipts), and the three appended sections; the objective, the recorded rationale for not moving files, the design, the deliverables, criteria 2 through 6, the evidence gate, the write-back duty, and the non-goals are byte-identical to activation (adjudication 6).

**Prior reports:** `VER-001` (pass; all six criteria, the evidence gate, and the write-back duties reproduced independently, including a 152-comparison probe that folds every prefix of the real control log and eight mutations through both the pre-extraction `scripts/resume.mjs` at `68b1ab2` and the new `scripts/lib/control.mjs`, a byte comparison of `release list` between the old and new helpers, an independent 298-reference scan, direct corruption of the real index file to prove `--check` names the line, and a digest of the ideation receipt against the guide's list; three non-blocking observations and four disclosures with an empty repair checklist). Complete sequence: one report, no repair episode. The executor's implementation evidence receipt in the work order is executor evidence and was treated here as VER-001 treated it: claims under test.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes) in Claude Code 2.1.261 with sandboxed Bash; macOS 15 on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 in this worktree's own dependency tree. Reasoning effort `max`: the session's model control reported it at launch and the harness exposes it to the shell as `CLAUDE_EFFORT=max`, but `docs/discovery/environment.json` records `effectiveEffortReadback` for `claude-code` as `not found` with `harnessReadbackEligible: false`, so `harness-readback` is not admissible and the epistemic source is `self-reported`, exactly as VER-001's was. `2.1.261` was checked against `claude --version` on this machine and is the newest entry on the observed `versions` list; `max` is on `sessionEffortSelector.values`; the identical tuple was accepted by the completion mechanism for WO-028's `FinalReviewCompleted` (event stamped `2026-09-04T21:22:21.995Z`, present at `HEAD`), so the attestation is admissible. The order declares `reviewer any`, so any attested effort would satisfy it; `max` is stated because it is what the session reports.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-026 passes.** The order asked one question to be answered from evidence instead of from a hand-maintained table: which work orders are open, which are closed, and what closed them, without moving, renaming, or renumbering a single work-order file. The deliverable answers it. `scripts/work-orders.mjs` renders `docs/work-orders/README.md` from three labeled sources: every `WO-*.md` header read tolerantly (title, sole strict version or `unassigned`/`malformed`, Model, Effort, and the `WO-NNN` tokens of Depends on), a per-order reduction of the append-only control log through the one shared event switch that `scripts/resume.mjs` itself now consumes, and the earliest local annotated release manifest naming each order, with the manifest-free `v0.2.0` resolved from its reviewed record. `index --check` regenerates to memory against the recorded tag-object snapshot and names the first differing line; `npm test` runs it. This review reproduced the whole gate first-hand: `npm test` green on the verified tree and again on the reviewed tree, `git diff --check` clean, `check-surfaces` passing the README release block at `v0.5.2` with all three component sources unchanged, `check-publication` at 183 of 183 headings with both audience locks current, and `index --check` passing; 36 authority files produce 36 rows across Active 1, Open 14, Closed 19, and Historical 2; exactly one definition each of `runGit`, `containedRegularFile`, and `readJsonFile` and one event-type switch exist, all under `scripts/lib/`; `release list` from an isolated copy of `HEAD`'s scripts and from the current helper are byte-identical over the fifteen local release tags; 296 concrete work-order references across every tracked and untracked text file resolve to the 36 retained authority paths except the disclosed historical pointer in `WO-003/VER-001.md` and the synthetic fixture paths; 476 relative links and anchors across the 19 changed or added Markdown files resolve; and the clean-room and secret screen over every added line found nothing. The ideation breakout's promoted surfaces were digested as subject: the candidate document is labeled a candidate at its status line, at both link sites, and in all seven audience-index rows, states plainly what the repository does not implement, adds no acceptance criterion to any order, touches no decision record, and uses the approved generic vocabulary. VER-001's three observations are one missing roadmap sentence (corrected here), one latent widening of a sibling helper's fallback (confirmed byte-identical today and disclosed in the notes), and one conservative version label (recorded as a candidate); this review adjudicates all three as non-blocking, records its own observations, applies two bounded documentation corrections, and changes no code, contract, acceptance, schema, authority, or captured-evidence byte.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment. Four apply.

1. **The control plane's fold is the subject.** `scripts/resume.mjs` allocated this `FINAL-001` path, minted checkpoint 5, and will record this verdict, and its event fold now lives in the new `scripts/lib/control.mjs`, a deliverable of this order. A successful dispatch or completion event therefore proves nothing about the extraction. Compensating controls, none of which trusts the shipped assertions: subject identity established with git objects, not the script; the switch body in `control.mjs` read side by side with the deleted body in the `resume.mjs` diff (a move: the state fields, the eight cases, the attestation and checkpoint bookkeeping, and the error text are identical, with the only addition the optional `visit` observer that `foldWorkOrders` uses and `fold` does not pass); `release list` reproduced byte for byte from `HEAD`'s helper in an isolated copy; the resume, checkpoint, worktree, and release suites, which predate this order, green in both `npm test` runs; and the projection regenerated by each transition compared field for field with `status --json`.
2. **The generated index projects its own work order's row.** The row this review refreshed after dispatch reads `final-review` with `FINAL-001 (pending)`, which is the projection of the very event that dispatched this review. It was not treated as evidence; the section counts, the column count, the tag snapshot, and the closure attributions were checked against the files, the log, and the tag objects directly.
3. **`scripts/lib/release-records.mjs` now serves the release helpers.** `release.mjs`'s `check-surfaces`, which this review ran, imports its tag, manifest, and version helpers from the new library. The five `PASS` rows were cross-checked by eye against the README block, the three package manifests, and the tag list, and `scripts/test-release.sh` passed in both runs.
4. **`npm test` now contains `index --check`.** The suite's own green is partly the deliverable's green. The check was also run standalone, and its refusal paths were exercised by the fixture suite's real-CLI cases (deleted row, edited cell, missing snapshot, changed tag object), which this review read rather than inferred.

Following the guide, this review dogfooded rather than abstained: an append-only log with a fully regenerated projection and a byte-exact index diff fails loudly, so corruption would surface rather than produce a plausible record.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-026 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-05T00:04:09.414Z`, and `elapsed` `{"implementation":3740817,"verification":954799}`; `docs/control/current.md` agreed line for line and no disagreement warning was emitted. Both elapsed values are the arithmetic differences of the recorded stamps (`23:45:19.125Z − 22:42:58.308Z` and `00:04:09.414Z − 23:48:14.615Z`). Before invoking the transition this review read the `resume` mapping in `package.json` and the complete `scripts/resume.mjs`, `scripts/release.mjs`, and `scripts/test-release.sh` diffs and both new libraries, as the guide requires. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-026/FINAL-001.md` (the directory itself is created by the reviewer), moved the phase to `final-review`, appended `FinalReviewRequested` with `recordedAt` `2026-09-05T00:09:05.999Z`, and minted checkpoint `cebf84df28d851d9dd688d4c9f95a49827223bc4` (`refs/dotln/checkpoint/WO-026/5`). Per the tag-snapshot amendment, this review then ran `npm run work-orders --silent -- index`, which changed exactly the WO-026 row. `docs/control/resume.jsonl` is a strict append over `HEAD`: its first 140 events equal `HEAD`'s file line for line, and the appended events are `WorkOrderActivated` (`22:42:58.308Z`, `effortDeclarationValidated: true`), `ImplementationReady` (`23:45:19.125Z`), `VerificationRequested` (`23:48:14.615Z`), `VerificationCompleted` (`00:04:09.414Z`), this review's `FinalReviewRequested`, and the `FinalReviewCompleted` the pass records. Every event carries `schemaVersion: 1`, and all stamps increase.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree after the allocation and refresh as tree `a0534123…`, and `git diff-tree -r --stat` against each checkpoint reported:

| Checkpoint                                 | Committer date              | Differences from the working tree at review open                                                                                                                                                                                                                                       |
| ------------------------------------------ | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`fdd8aaeb…`)              | `2026-09-04T18:42:58-04:00` | every deliverable path (30 files, 2,235 insertions / 462 deletions to `/2`); the checkpoint's tree equals `HEAD`'s, so the whole subject, the work order's own amendments included, is downstream of activation                                                                          |
| `/2` implementation-ready (`830d9738…`)    | `2026-09-04T19:45:19-04:00` | `docs/control/current.md`, `docs/control/resume.jsonl`, `docs/verifications/WO-026/VER-001.md` (added), and the WO-026 index row (`active` → `final-review`, verification and final-review cells filled)                                                                                 |
| `/3` verify allocation (`400a38b6…`)       | `2026-09-04T19:48:14-04:00` | `docs/control/current.md`, `docs/control/resume.jsonl`, `docs/verifications/WO-026/VER-001.md` (added), the WO-026 index row (`ready-to-verify` → `final-review`)                                                                                                                       |
| `/4` verdict (`1b166f80…`)                 | `2026-09-04T20:04:09-04:00` | `docs/control/current.md`, `docs/control/resume.jsonl`, the WO-026 index row (`verifying` / `VER-001 (pending)` → `final-review` / `VER-001 (pass)` / `FINAL-001 (pending)`)                                                                                                             |
| `/5` final-review allocation (`cebf84df…`) | `2026-09-04T20:09:05-04:00` | `docs/control/current.md`, `docs/control/resume.jsonl`, the WO-026 index row (`verified` / `none recorded` → `final-review` / `FINAL-001 (pending)`)                                                                                                                                      |

Every deliverable blob is therefore identical across the implementation-ready, verify-allocation, verdict, and final-review-allocation checkpoints and the working tree, with two accounted exceptions: the verifier added its report before the verdict and refreshed the generated index after its dispatch and again after its verdict (rows at `/4` and `/5` above), exactly as the amendment prescribes; VER-001 itself has not changed since its verdict was recorded. All five checkpoints have `HEAD` as their sole parent. After this review's two corrections, the same temporary-index comparison against `/5` reports exactly the control files, the WO-026 index row, one line in `docs/planning/work-order-map.md`, and one line in `docs/product/06-roadmap.md`; the completion mints `/6`, and the post-completion refresh moves the WO-026 row into Closed before the reviewed commit.

## The order's own actors, inventoried

The final-review README requires the reviewer to list every executor-role completion actor, each completed verifier's header beside its control actor, and its own intended actor, check each against the declared minimum, and confirm the projection's drift. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role and event                                | Control-log actor                                                                                                            | Report header                                                                                                                                                        | Declared minimum | Result                                                                                                                                                                                           |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| executor, `ImplementationReady` (event 142)   | `{"harness":"codex-cli","harnessVersion":"0.153.2","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`       | none (there is no executor report); the work order's implementation evidence receipt states the same tuple in prose                                                  | `xhigh+`         | `max` ≥ `xhigh`; `0.153.2` is on the observed Codex `versions` list and its `max` persisted selector is recorded with `observedAt`, the operator-attested form the guide prescribes                |
| verifier, `VerificationCompleted` (event 144) | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}`   | `VER-001` carries exactly one column-one `**Actor attestation:**` line; parsed as JSON and compared by canonical serialization with the event actor: equal            | `xhigh+`         | `max` ≥ `xhigh`; `2.1.261` observed; `self-reported` is the honest source because readback is `not found`                                                                                        |
| reviewer, `FinalReviewCompleted` (intended)   | the header above                                                                                                             | this report                                                                                                                                                          | `any`            | satisfied by any attested effort; `max` stated as reported; the mechanism compares header and flags before append                                                                                 |

No `RepairCompleted` exists. `effortDrift` is `[{"effort":"max"}]`, a single value, and `current.md` renders `Effort drift: none`; the single-value list is correct because the executor and verifier both attested `max`. This review cannot independently confirm the Codex persisted selector from a Claude session and relies on the receipt's operator attestation and the bounded discovery observation, both in the append-never-replace form; the verifier's and this reviewer's Claude Code version were confirmed with `claude --version` on this machine.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

Run 1, on the verified tree after this review's post-dispatch index refresh, before any correction: exit 0. Prettier clean; the body-profile unit suite; the temp-root, publication, backup, resume, checkpoint, worktree, release, and work-orders shell suites, the last printing its eleven `PASS` groups and `PASS work-order index fixtures`; `check-publication.mjs` at 183 of 183 headings with both editions `CURRENT`; `PASS docs/work-orders/README.md is current`; `tsc -b --force`; 173 of 173 package tests; 8 of 8 identity-corpus tests. Run 2, on the reviewed tree after the corrections, the recorded pass, and the final index refresh: reported in §Ready to merge: handoff and the PR body, and required green before the commit.

### Static, surface, and structural checks

- `git diff --check` and `git diff --check HEAD`: exit 0 before and after the corrections.
- `node scripts/release.mjs check-surfaces`: `PASS release-block: observed v0.5.2; expected v0.5.2 (work-order target v0.5.2; latest published v0.5.1)`; `PASS component-version` for `@dotln/compiler` `0.2.0`, `@dotln/kernel` `0.2.1`, and `@dotln/skeleton` `0.5.0`, each `src unchanged` since `v0.5.1`; `PASS github-body-profile` with no current final-review bodies yet. The same command is rerun after this package is written so the body-profile rule sees `PR.md` and `RELEASE-NOTES.md`.
- `node scripts/check-publication.mjs`: 183 of 183 headings indexed, three identical claim links per voice, both editions `CURRENT`, before and after the roadmap correction (§Release boundary is not a linked edition section, so neither lock moved).
- `node scripts/work-orders.mjs index --check`: `PASS docs/work-orders/README.md is current`, standalone and inside the suite.
- Helper duplication (criterion 5): `grep` over `scripts/` and `packages/*/src` finds one `runGit` (`scripts/lib/git.mjs:7`), one `containedRegularFile` (`scripts/lib/paths.mjs:4`), one `readJsonFile` (`scripts/lib/paths.mjs:19`), and one `case "WorkOrderActivated"` (`scripts/lib/control.mjs:46`).
- Index shape (criterion 2): 36 `WO-*.md` files and 36 `| WO-` rows; Active 1, Open 14, Closed 19, Historical 2.
- `package.json`: the `test` chain gains `bash scripts/test-work-orders.sh` and `node scripts/work-orders.mjs index --check`; a `work-orders` script is added; `devDependencies` is byte-identical to `HEAD`; `package-lock.json`, `docs/decisions/`, and `.claude/` are untouched (zero `git status` entries).
- Prettier on the two corrected files: clean; the generated index is excluded by the existing `docs/work-orders/*.md` ignore rule, and this package is excluded by `docs/final-reviews/WO-*/`.

### Independent probes against the deliverable

- **`release list` is byte-identical across the extraction (VER-001 observation 2).** `git archive HEAD scripts package.json docs/releases` was unpacked into a fresh repository under the session temporary root, the fifteen tags and `main` fetched into it from this worktree, and `HEAD`'s `release.mjs list` run there; compared with the current helper's output in this worktree: identical, 16 lines, attributing `v0.2.1` to `WO-004,WO-012`, `v0.2.2` to `WO-101,WO-005`, `v0.2.3` to `WO-006,WO-015`, `v0.3.5` to `WO-027,WO-017`, and every other tag to its single order.
- **Closure attributions re-derived from tag objects (criterion 4).** `git cat-file -p` on the tag objects: `v0.2.1`'s manifest names `WO-012` and lists `docs/final-reviews/WO-004/` paths; `v0.2.2` names `WO-005` and lists `WO-101` paths; `v0.2.3` names `WO-015` and lists `WO-006` paths; `v0.3.5` names `WO-017` and lists `WO-027` paths. The index's `manifest workOrder.id` and `manifest changedFiles` labels match each case, and its snapshot line records `v0.5.1` as object `8a079ab8…`, the object `git ls-remote` reports on origin.
- **Reference resolution (criterion 1).** An independent scanner over every tracked and untracked text file found 296 concrete `docs/work-orders/WO-…md` references; 239 resolve to all 36 retained authority paths; the remaining distinct matches are synthetic fixture and template paths inside the shell and Node suites, plus the one historical dangling pointer (`docs/verifications/WO-003/VER-001.md` → `docs/work-orders/WO-002-verification.md`), present at `HEAD` twice in that immutable report and quoted, as a disclosure, in the activation comparison, the reference transcript, and VER-001. `git diff --name-status -M -C HEAD` reports modifications only, and under `docs/work-orders/` the only tracked change is the WO-026 authority; `codex-downtime-series.md` and the other 35 authorities are byte-identical to `68b1ab2`.
- **Link resolution.** 476 relative links and anchors across the 19 changed or added Markdown files, resolved with GitHub's anchor rules: all resolve except `docs/work-orders/README.md`'s link to this report's path, which this report creates.
- **Clean-room and secret screen.** A pattern screen over the 530 added tracked lines and every new file for commercial tracker and ALM names, corporate gateway and directory shapes, internal hostname suffixes, and credential and key patterns returned no match.
- **Intake state.** Main's canonical ignored capture named in the receipt exists at mode `0600` (604 bytes); this worktree's `docs/intake/` holds only the three `.gitkeep` files, so no worktree-local staging remains and closeout will not be blocked. This review did not read the capture; fidelity to the operator's words was established by VER-001's read and is relied on here (disclosed in §Ideation breakout receipt).
- **`gh` preflight in the sandbox.** `gh --version` and `gh auth status` both fail before their root command exists (`failed to read configuration: open ~/.config/gh/config.yml: operation not permitted`), the same failure every reviewed order since WO-006 has met.

### Reading

The complete diff of every changed file and every new file was read: the generator, both new libraries, the Node and shell fixture suites, the `resume.mjs`, `release.mjs`, and `test-release.sh` diffs, `package.json`, the README release block, the playbook, the docs map, the ledger's two new sections, the vision and interfaces links, the roadmap's migration paragraph and default sentence, the execution guide's dispatch and index paragraphs, the publication index, both edition locks, the staleness demonstration's recaptures 13 and 14, the work order with its three appended sections, VER-001 in full, the generated index, the candidate workstream document, the three planning evidence files, and the reduced map. The publish, surface, notes, and body gates were read from `worktree.mjs`, `release.mjs`, `release-notes.mjs`, and `github-body.mjs` rather than inferred, and the actor admissibility and final-review paths from `resume.mjs`. The ignored intake was not read.

## Acceptance criteria

**1. No work-order file moved, renamed, or deleted; bytes retained apart from the bounded exception; references resolve. PASS.** Reproduced by `git diff --name-status -M -C HEAD` (modifications only; under `docs/work-orders/` the WO-026 authority alone, plus the added generated README), by the byte identity of the other 35 authorities and the runbook to `68b1ab2`, and by the independent reference scan above. The authority's own edits are exactly the exception criterion 1 now states: the title's `v0.5.2`, the completed release classification, criterion 1 itself, and the three appended dated sections; the objective, design, deliverables, criteria 2 through 6, the evidence gate, the write-back duty, and the non-goals are unchanged since activation.

**2. Complete, unique, checked, deterministic. PASS.** 36 files, 36 rows, four sections summing to 36; every row has 11 cells (the fixture suite asserts this for its rows, and the real file was counted here); `index --check` passes on the working tree; the fixture suite deletes a row and edits a cell in a real generated file and asserts `stale at line N` names that row's line while leaving the file and the log untouched; two consecutive generations are byte-identical (fixture assertion, and VER-001 reproduced it on the real file). Its failure paths on the real file were exercised by VER-001 and not repeated here.

**3. Derivations in fixtures. PASS.** `scripts/test-work-orders.mjs` builds a repository at a path containing an ordinary space and a narrow no-break space, with a synthetic control log and simulated annotated tags, and asserts every required case: released as `workOrder.id` (`WO-030` → `v1.0.0`), released via `changedFiles` (`WO-031`), a closed no-release order (`WO-032`, `v0.5.0` below `v1.0.0`), a closed unreleased order (`WO-033`), an active order (`WO-034`), a draft blocked on an unclosed dependency (`WO-035`), dependency-ready drafts (`WO-036`, `WO-039`), two versions rendering `malformed` (`WO-037`), and no Depends on rendering `unknown` (`WO-038`). Beyond the minimum, and read here: `--check` is proven read-only against the log, the refs, and the index file; a new tag leaves the recorded check green with a newer-evidence notice while explicit refresh incorporates it without inventing a retroactive no-release close; a later-dated tag on an older commit cannot become a prior release; a malformed additional manifest is reported by `--check` and refuses `index`; missing or changed recorded tag objects refuse without rewriting the index; a failed final review and a fresh request have distinct verdicts and `foldWorkOrders(...).current` deep-equals `fold(...)`; unknown event types and invalid `recordedAt` refuse at the global ordinal; headers with `C#`, non-strict versions, missing H1, body-buried or duplicated fields, and empty Effort render `unknown`/`malformed` and never guess; duplicate ids, source and destination symlinks, and unknown CLI arguments refuse; and the sole manifest-free historical release requires its explicit record while malformed manifest JSON refuses.

**4. Real-repository closures. PASS.** The generated index renders `WO-003` `v0.2.0 (historical record)`, `WO-004` and `WO-012` `v0.2.1`, `WO-005` and `WO-101` `v0.2.2`, `WO-006` and `WO-015` `v0.2.3`, `WO-007` `v0.3.0`, and `WO-001` and `WO-002` `historical (time-indexed)`; the attribution kind of each was re-derived from the tag objects above. Every other order renders open with a computed dependency status that matches the pilot map's cell or is explained, token by token, in the dated activation comparison; VER-001 spot-checked seven of those and this review read all 36 rows of the comparison against the index. Later closures (`WO-008`, `WO-013`, `WO-016` through `WO-019`, `WO-023` through `WO-025`, `WO-027`, `WO-028`) render closed with their releases, including `WO-027` at `v0.3.5 (manifest changedFiles)` although its header says `v0.3.3`, which the tag object confirms.

**5. No duplicated helpers; unknown event refuses with its ordinal. PASS.** One definition of each named helper and one event-type switch, all inside `scripts/lib/` (grep above); the fixture proves `unknown control event type at line 9: UnrecognizedFutureEvent` and `invalid recordedAt at line 9` at the same global ordinal. The remaining `FinalReviewCompleted` occurrences are not parsers: `release.mjs`'s `passedFinalReview` predicate is unchanged from `HEAD`, `resume.mjs` constructs the event when appending, `control-time.mjs` holds WO-028's phase map, and the fixture builds synthetic events.

**6. Test chain, temp-root guard, whitespace, dependencies, `.claude/`. PASS.** The chain runs the new suite and the check and exits 0 twice; `scripts/test-work-orders.sh` sources `test-temp-root.sh`, resolves the `TMPDIR` base, creates an owned root with the unique `dotln-work-orders-test` prefix, installs the standard traps, and passes that root to the Node suite, which asserts the owner marker and a symlink-free absolute path; `git diff --check` is clean; `dependencies` is absent and `devDependencies` and the lockfile are unchanged; nothing under `.claude/` changed.

## Evidence gate

Fixture transcripts, the real-repository comparison with each discrepancy explained, `npm test`, `git diff --check`, and the reference-resolution output are all present and were reproduced. The eleven `PASS` lines and the closing `PASS work-order index fixtures` in the committed fixture transcript match this session's suite output line for line. The reference transcript's claims (242 concrete pre-existing references, 36 retained paths, 35 other authorities and the runbook byte-exact, one historical dangling pointer) agree with the independent scan above, whose slightly larger raw count includes the new files' own references. The activation comparison names the eight originally required closure mappings, every later closure, every additional dependency token, and the historical pointer.

## Write-back duty

The docs map line for `docs/work-orders/` names the stable addresses and the generated evidence index; the roadmap's §Work-order navigation and identity carries a dated 2026-09-04 migration paragraph adopting the evidence view and keeping metadata and scheduling as candidates under the retained anchor; the playbook's step 1 and the execution guide's §Operator resume phrases direct the reader to the index for evidence state and the map for recommendation, and both describe the refresh duty and the snapshot rule; the ledger gains the adopted entries (stable addresses, explicit tag observations) and the superseded entry (the pilot map's evidence columns) in a dated section, with no older entry rewritten (the diff is a pure insertion in newest-first order); the publication index gains the new document's seven rows, both edition locks were refreshed, and the staleness demonstration appends recaptures 13 and 14, recording the intermediate `da39d2…` lock rather than hiding it. The requested `codex-downtime-series.md` pointer was deliberately not written: the receipt records that editing it would breach the remaining file freeze, and its existing map link now reaches the index indirectly. That is a disclosed deviation from the write-back duty, not a silent omission (adjudication 7).

## Ideation breakout receipt

The 2026-09-04 receipt in the work order was inspected against the guide's list and the receipt's own required-review paragraph.

- **Authority and return.** The receipt states the operator authorized the full pipeline, return to the implementation, and a stop at readiness for verification; the executor's handoff obeyed that stop, and VER-001 read the canonical capture and confirmed the authorization language is the operator's. This review relies on that read and did not reopen the intake; the capture's existence, mode, and the absence of worktree staging were confirmed.
- **Source treatment and clean-room floor.** Ordinary Shape-First Synthesis; nothing lifted. The candidate document names no predecessor implementation, tracker, gateway, host, or credential; the host framing is the approved generic `constrained managed host`; the two prior workflow generations are described only as "both existing workflow generations"; the cross-repository example is synthetic and says so. The pattern screen over every changed and added file found nothing. The private early-use label was checked absent by VER-001; this review did not learn the label and therefore cannot leak it.
- **Candidate versus implemented.** The status line calls the document an operator-opened product exploration, not an implemented interface, deployment claim, or release rung; "What exists and what must be proved" states that the repository implements no end-user portfolio, repository coordinator, or live external execution service and that thresholds and the pilot task are unchosen; all seven audience-index rows are `vision`; both link sites (vision Horizon 1, interfaces candidate) say it allocates no rung and ships no console.
- **Settled decisions and boundaries.** `docs/decisions/` is untouched. The reference-profile framing follows ADR-0006: one owner's profile, other owners may equip differently, visible capability must match what is equipped. The document points at the existing platform/instance boundary, ports, terminal/console parity, and memory sections rather than inventing new ones; all four anchors resolve (link check above).
- **Authority and partial-delivery honesty.** Delivery executes "only when supported and authorized"; no atomic cross-repository merge or rollback is assumed; partial delivery stays visible with its next safe action; a green service check cannot make a failing client check green; a quiet view is not proof that a worker runs.
- **Publication.** Coverage 183 of 183; both locks current; recaptures 13 and 14 committed; source-base revisions unchanged.
- **No executable helper.** Consistent with the diff: the only new executables are the generator and its suite, which carry their own evidence.

## Verification-sequence soundness

One verification, one pass, no repair episode. VER-001 was allocated at event 143 and its verdict recorded at event 144 with a header equal to the event actor; its elapsed span is 954,799 ms. The verifier changed exactly one subject byte-region outside its report, the generated index's WO-026 row, refreshed after dispatch and after the verdict as the amendment prescribes and as the checkpoint table shows; every other deliverable blob is identical across checkpoints 2 through 5. The verifier's compensating controls were exceeded or independently repeated here rather than accepted: its old-versus-new fold probe by the side-by-side read and the `release list` byte comparison in an isolated copy, its reference scan by a second scanner, its anchor check by a full link resolution, and its clean-room screen by a second screen.

## Adjudications

### 1. The roadmap did not record WO-026's application of the release-assignment default (VER-001 observation 1)

Confirmed: §Release boundary's default paragraph ended with the WO-028 sentence. The guide's discipline bullet names that section as the place to complete a missing target, so the omission was a wording-completeness gap in the record, not a substantive one; the base and classification were already recorded in the work order, the README block, and the map. Corrected here with one sentence (§Corrections); the section is not linked by either audience edition, so `check-publication` stayed `CURRENT` and no lock or recapture was needed.

### 2. `release list`'s fallback attribution widened (VER-001 observation 2)

Confirmed by reading `listPublishedReleases`: when the first-parent walk between two releases finds no passing final review and no changed release notes, the fallback now pushes every order `manifestWorkOrders` derives (`workOrder.id` plus `docs/final-reviews/WO-NNN/` changed paths) where `HEAD` pushed `workOrder.id` alone. On the fifteen real tags the walk always finds at least one order, so the fallback never fires and the output is byte-identical (probe above). The widening makes the two consumers of a manifest agree on one attribution rule, which is the order's criterion-5 intent. Non-blocking; disclosed in the notes under Read before upgrading.

### 3. `WO-011` renders `malformed` in the Version column (VER-001 observation 3)

Confirmed by reading `parseHeader`: `strictVersionsIn` finds no strict version in "Feedback compiler v1 (ten units; application version assigned at activation)", and the `/\bv\d/` guard then reports `malformed` rather than `unassigned`, because a version-shaped token is present. The rule is conservative by design ("never guessed") and criterion 3 pins the two-version case, not this one. The label is the one real-repository cell a reader could mistake for a broken header, and the activation comparison compares phase, release, and dependency tokens rather than versions, so it does not call it out. Non-blocking; a distinct label for a non-application version token is a candidate for a later bounded order and is recorded in the PR body.

### 4. The local backup snapshot could not be located (VER-001 disclosure 3)

Immaterial and not searched here: the receipt calls the snapshot temporary local recovery, the canonical capture exists, and no reconciliation is pending.

### 5. Each transition's checkpoint predates the projection refresh (VER-001 disclosure 4; the amendment)

Confirmed by the checkpoint table: every refresh of the WO-026 row lands after the checkpoint that captured the transition it projects, so the immutable checkpoints show the previous row and the working tree the current one. This is the designed ordering; the next role checks the projection before its gate, which this review did (`index --check` passed on the refreshed tree before `npm test`). After the pass is recorded this review refreshes once more so the committed index shows the closed state, and the WO-028 row's dependency-token cell will read `WO-026: satisfied (closed)` at that refresh, an expected consequence of the literal-token rule.

### 6. The work order's authority text was amended after activation (this review)

Against the activation checkpoint, whose tree equals `HEAD`'s, the title gained `, v0.5.2`, the release-classification paragraph now records the default's application with its base and date, criterion 1 was restated to permit exactly those edits and the receipts, and three dated sections were appended: the ideation receipt (operator authority for the breakout), the tag-snapshot amendment (operator selection during execution, recorded with the reproducibility conflict it resolves), and the implementation evidence receipt. Criteria 2 through 6, the gate, the duty, and the non-goals are byte-identical to activation. The amendments name their operator authority and date; the tag-snapshot amendment changes what `--check` must do and the fixtures prove the amended behavior, so it is scope authority in the form the guide requires rather than executor discretion. No action; the operator's merge confirms the record.

### 7. The runbook pointer write-back was delivered indirectly (this review)

The write-back duty asked for a `codex-downtime-series.md` pointer; the receipt records that the file freeze in criterion 1 forbids editing it and that its existing map link now reaches the index. The freeze is the stronger and earlier rule, the runbook is an immutable series record, and the map's first paragraph links the index. Disclosed deviation; no action.

### 8. `tagContents` now addresses `refs/tags/<tag>` explicitly (this review)

`HEAD` ran `git cat-file -p <tag>` by bare name; the library runs it on `refs/tags/<tag>` through `runGit`. A branch or other ref sharing a tag's name can no longer shadow it, and a read failure now surfaces `runGit`'s message instead of `cannot read tag <tag>`. No suite asserted the old text; `test-release.sh` passes. A hardening, not a widening. No action.

### 9. `localReleaseRecords` refuses any local release-shaped tag with a malformed manifest (this review)

Outside snapshot mode every local annotated `vX.Y.Z` tag whose human layer begins `DotLn vX.Y.Z` must carry a well-formed manifest or `index` refuses with `malformed release attribution`; in snapshot mode an unrecorded malformed tag is only reported. Refusing rather than guessing is the order's rule, the fixture pins both behaviors, and a foreign tag of that exact shape is not a realistic local state. No action.

### 10. The generator's temporary file uses an exclusive create (this review)

`index` writes `README.md.tmp` with `wx` and renames it; a stale `.tmp` left by an interrupted run would make the next `index` fail with the raw `EEXIST` error rather than a named refusal, and the fixture asserts no `.tmp` remains after `--check`. Correct and safe; the message specificity is a candidate polish. No action.

### 11. Titles containing code spans render their backticks as `&#96;` in the index (this review)

`cell()` escapes backticks with the other Markdown-active characters so no header can inject table structure or formatting; the WO-028 and WO-023 rows therefore show literal backtick characters. Safe by design and stated in the index's limits; cosmetic. No action.

### 12. Smaller consequences recorded without action

`localTags` now carries `taggedAt` from `%(taggerdate:unix)`, `undefined` for lightweight tags, which `release.mjs` ignores. `WO-028`'s Closed row lists `WO-021` and `WO-026` as `not control-closed` because its Depends on paragraph names them as consumers; the limits paragraph and the activation comparison say the view is literal. After the pass, WO-026 renders `closed` and `unreleased` until `v0.5.2` exists, which is the honest local state the index's limits describe; once the tag exists, `--check` reports it as newer evidence until the next explicit regeneration, by the amendment's design. The reviewer's `CLAUDE_EFFORT` remains an unrecorded readback candidate (WO-023 FINAL-001 adjudication 9), so Claude Code attestations stay `self-reported`.

### Candidates considered and not sustained

- **Lifecycle transitions should regenerate the index.** Not sustained: the work order's design keeps the control plane lean and assigns refresh to roles; the amendment records the checkpoint/refresh ordering and every role in this order followed it.
- **Extracting `fold` into a library is a behavior change.** Not sustained: the switch body is a move, VER-001's 152-comparison probe and this review's byte-identical `release list` agree, and every pre-existing suite passes.
- **Recording tag object ids in a README is noise.** Not sustained: the ids are the reproducibility mechanism the operator selected; without them a committed release's own tests would fail the moment it is tagged.
- **`unreleased` after close misstates a release in progress.** Not sustained: the label reports local evidence honestly and the limits paragraph says release inclusion can follow a close.
- **The executor's post-readiness edits to the work order.** Not sustained: the implementation evidence receipt was captured by checkpoint 2 before verification began, and the diff between checkpoints 2 and 5 touches no authority byte.

## Corrections applied by this review

Two corrections, both in the documentation lane `docs/final-reviews/README.md` grants, proven contained by a temporary-index tree diff against checkpoint 5 (one line in each file) and followed by a re-run of the checks they could affect: Prettier on both files, `check-publication.mjs` (still 183 of 183 with both editions `CURRENT`), `git diff --check`, and the full `npm test` before the commit.

1. **`docs/product/06-roadmap.md` §Release boundary:** one sentence after the WO-028 precedent, recording that WO-026 applies the default as `v0.5.2` above published `v0.5.1` (adjudication 1).
2. **`docs/planning/work-order-map.md`:** the WO-026 row's Activation preflight cell now reads as the closed rows read, recording activation from published `v0.5.1`, the `v0.5.2` assignment under the default, and a pointer to the index for current evidence, in place of the present-tense statement that the index and ideation "are the review subject". No other row or paragraph changed.

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5 apart from the WO-026 index row that each transition and refresh moves.

## Findings

**None blocking.** No acceptance criterion is unmet, no guard was lost, and no behavioral or evidence defect was found in the deliverable that any criterion covers. The items below are recorded and not scored.

1. **`release list`'s fallback attribution widened to every order a manifest names** (adjudication 2): latent on the current tag set; disclosed.
2. **`WO-011` renders `malformed` for a product-generation token** (adjudication 3): conservative by design; a distinct label is a candidate.
3. **The runbook receives the index pointer indirectly through the map** (adjudication 7): disclosed deviation from the write-back duty under the file freeze.
4. **The generator's stale-temporary-file failure is unnamed** (adjudication 10): candidate polish.
5. **Code spans in titles render as literal backticks in the index** (adjudication 11): cosmetic.
6. **The index snapshot lags a new release tag until explicit regeneration** (adjudication 12): by design; the release close of `v0.5.2` will leave `--check` reporting newer evidence until the next order regenerates.
7. **`CLAUDE_EFFORT` remains an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9).
8. **The executor's model and effort live in the control log and the work order's receipt, not in a separate executor artifact.** Recorded, as every reviewed code order since WO-013 has recorded it.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step and the sandbox.** `gh` cannot read its configuration inside this sandbox, so the bounded publisher will refuse at its `gh` preflight after running the committed-tree surface check, body validation, and notes parse, pushing nothing. The reviewed commit is made here; the helper is then invoked once from this worktree inside the sandbox so that those checks run against the exact commit; the operator runs the same command from this worktree outside the sandbox (in this session, prefixing it with `!` runs it in the operator's shell).
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute command the publisher prints: change directory into the main checkout and run this worktree's `scripts/release.mjs close WO-026 --publish` with node, outside the sandbox and where `gh` is authenticated. Because `v0.5.2` is strictly above `v0.5.1`, the reviewed subject helpers finish this worktree, fast-forward main, run `npm ci` and the evidence gate (in which `index --check` passes against the committed snapshot), assemble the five-section edition from these notes, create and push the annotated `v0.5.2` tag, and create the matching GitHub Release. If Release creation fails after the tag push, the rerun is `npm run release -- close WO-026 --publish` from updated main.
- **Index regeneration after the tag.** The committed index records the snapshot through `v0.5.1`; after `v0.5.2` exists, `index --check` prints a newer-evidence notice and passes, and the next work order's executor regenerates the index before its evidence gate, attributing WO-026 to `v0.5.2`.
- **Attribution:** the reviewed commit carries a plain subject and no attribution trailer, and the PR body carries no session link, per the operator's standing rule (`~/.claude/CLAUDE.md`) and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins, as the WO-017, WO-018, WO-023, and WO-028 reviews recorded.
- **Ledger duty:** discharged by the executor's two dated sections; this review adds no ledger entry because every open item above is either already in VER-001 or is a candidate this report names.

## Proposed PR

- **Title:** `:sparkles: WO-026: work-order index; open, active, closed, and historical orders as a generated view over work-order headers, the shared control fold, and a recorded local release-tag snapshot, with every work-order file kept at its address; v0.5.2`. The `:sparkles:` precedent is PR #29 for WO-028 and PR #28 for WO-023, chosen because the order adds a generator, a checked projection, and a package script rather than restructuring existing behavior; `:recycle:` (PR #27, WO-018's consolidation) was considered for the helper extraction and not chosen because the extraction serves the new feature.
- **Body:** `docs/final-reviews/WO-026/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commit (the committed index must show the closed state, and `worktree finish` requires the closed control state); the index is refreshed once more; `npm test`, `git diff --check`, and `check-surfaces` are rerun on the reviewed tree; the reviewed state is committed with a plain subject. The publisher is then run from this worktree:

```bash
npm run worktree -- publish WO-026 --title ':sparkles: WO-026: work-order index; open, active, closed, and historical orders as a generated view over work-order headers, the shared control fold, and a recorded local release-tag snapshot, with every work-order file kept at its address; v0.5.2' --body-file docs/final-reviews/WO-026/PR.md
```

The helper runs `check-surfaces --committed WO-026` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-026`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff in the absolute subject-helper form. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, the operator runs that printed command with the main checkout as the working directory; the expected outcome is the annotated `v0.5.2` tag and its GitHub Release.

## Method

Single reviewer, single session, no delegation: every read, probe, and command named in this report was performed by the reviewer directly. Read order: the execution guide's dispatch, ideation, closeout, discipline, and model-specific sections; the control projection and log; the work order in full including its three appended sections; VER-001 in full; the final-review and verification READMEs; the WO-028 final review with its PR body and notes for the package shape and publish precedent; then the diffs of every changed file and every new file in full, with the publish, surface, notes, and body gates and the actor admissibility rules read at source. The ignored intake was not read. Subject integrity was established through git objects at every checkpoint. Evidence was re-established first-hand: two full `npm test` runs, `check-surfaces` twice, `check-publication` twice, `git diff --check` twice, the standalone `index --check`, the helper-duplication grep, the row and section counts, the actor comparison by canonical JSON, the strict-prefix check of the control log against `HEAD`, the `release list` byte comparison in an isolated copy of `HEAD`'s scripts, the tag-object re-derivation of four attributions, the 296-reference scan, the 476-link resolution, the clean-room and secret screen, the intake existence and mode check, and the read-only `git ls-remote` that confirmed the published `v0.5.1` target. Authority used: two documentation corrections and the three package artifacts; no edit to any code, test, contract, schema, discovery field, verification report, or acceptance byte. Nothing was restored from a checkpoint, no destructive git command was used, and no remote, tag, or `gh` mutation was performed before the reviewed commit; the direct `gh` invocations were `gh --version` and `gh auth status`, both of which failed before their root command existed.
