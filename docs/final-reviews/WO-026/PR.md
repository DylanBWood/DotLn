## What this merges

WO-026, the `v0.5.2` work-order index patch: the repository now answers "which work orders are open, which are closed, and what closed them" from evidence, in a generated index that `npm test` proves fresh, without moving, renaming, or renumbering a single work-order file. `scripts/work-orders.mjs` (package script `work-orders`) renders `docs/work-orders/README.md` from three labeled sources: every `WO-*.md` header read tolerantly (title, sole strict version or `unassigned`/`malformed`, Model, Effort, and the `WO-NNN` tokens of Depends on), a per-order reduction of the append-only control log through the one shared event switch that `scripts/resume.mjs` itself now consumes from the new `scripts/lib/control.mjs`, and the earliest local annotated release manifest naming each order, with the manifest-free `v0.2.0` resolved from its reviewed record. The index states the local tag set it was generated from and records each tag object's id; `index --check` regenerates to memory against that recorded snapshot, names the first differing line, requires every recorded tag to exist unchanged, and reports newer local release tags as newer evidence without failing, so tagging a reviewed commit cannot break that release's own tests. The human planning map keeps only what cannot be computed: recommendation, rationale, tracks, and activation preflight. The order's 2026-09-04 operator-authorized ideation breakout is included as documentation only: a candidate end-user workstream application document, its links from the vision and interfaces documents, a ledger section, and the publication index and lock refreshes. Nothing was moved, deleted, or backfilled; no dependency, package version, `src/` file, decision record, or lifecycle transition changed.

- **Files never move; references resolve (criterion 1):** `git diff --name-status` against `main` shows modifications only; under `docs/work-orders/` the only tracked change is the WO-026 authority's own bounded amendments (title version, completed release classification, restated criterion 1, three dated appended sections) and the only added file is the generated README; the other 35 authorities and the series runbook are byte-identical to `v0.5.1`; 296 concrete work-order references across every tracked and untracked text file resolve to the 36 retained paths, apart from synthetic fixture paths and one disclosed historical pointer in `WO-003/VER-001.md` that predates this order and is preserved, not rewritten.
- **Complete, unique, checked, deterministic (criterion 2):** 36 files, 36 rows, eleven cells per row, four sections (Active 1, Open 14, Closed 19, Historical 2 at verification; after this order's own recorded close the committed index reads Active none, Open 14, Closed 20, Historical 2); two generations are byte-identical; deleting a row or editing a cell fails `--check` naming that line while leaving the file, the log, and the refs untouched.
- **Derivations proven in fixtures (criterion 3):** a synthetic repository with a synthetic control log and simulated annotated tags covers released via `workOrder.id`, released via `changedFiles`, a no-release close, an unreleased close, an active order, a blocked draft, dependency-ready drafts, a two-version `malformed` header, and a missing Depends on rendering `unknown`; beyond the minimum, the suite proves `--check` is read-only, that a new tag leaves the recorded check green with a newer-evidence notice while explicit refresh incorporates it, that a later-dated tag on an older commit cannot become a prior release, that missing or changed recorded tag objects refuse, that failed and fresh final reviews have distinct verdicts, and that unknown events and invalid times refuse at their global ordinal.
- **Real-repository closures (criterion 4):** `WO-003` `v0.2.0` (historical record), `WO-004` and `WO-012` `v0.2.1`, `WO-005` and `WO-101` `v0.2.2`, `WO-006` and `WO-015` `v0.2.3`, `WO-007` `v0.3.0`, `WO-001` and `WO-002` historical (time-indexed); every later closure renders with its release, including `WO-027` at `v0.3.5` by changed final-review path although its header says `v0.3.3`; every open order's computed dependency status matches the pilot map or is explained token by token in the dated activation comparison.
- **One parser, one switch (criterion 5):** exactly one definition each of `runGit`, `containedRegularFile`, and `readJsonFile` and one event-type switch, all under `scripts/lib/`; `scripts/resume.mjs` re-exports `fold`, `parseControlEvents`, and `CONTROL_LOG_SCHEMA_VERSION` from `scripts/lib/control.mjs` so the WO-018 import surface is unchanged; `scripts/lib/release-records.mjs` holds the tag, manifest, version, and attribution helpers `release.mjs` previously defined inline.
- **Green chain, guarded fixtures, no dependency (criterion 6):** `npm test` runs `bash scripts/test-work-orders.sh` and `node scripts/work-orders.mjs index --check` and exits 0; the shell fixture uses the WO-013 temp-root guard; `git diff --check` is clean; `package.json` changes only the test chain and the new script; the lockfile and `.claude/` are untouched.
- **Write-backs:** docs map, roadmap §Work-order navigation and identity (dated migration paragraph adopting the evidence view; metadata and scheduling remain candidates), playbook step 1, execution guide §Operator resume phrases, ledger (adopted: stable addresses and explicit tag observations; superseded: the pilot map's evidence columns), publication index and both audience locks with staleness recaptures 13 and 14, README release block at `v0.5.2`; the series runbook keeps its bytes under the file freeze and reaches the index through its existing map link.
- **Ideation breakout (documentation only):** `docs/product/12-workstream-application.md` records the candidate desk-to-delivery journey, incremental parity checks against both existing workflow generations, cross-repository coordination, current capability limits, and open choices; it is labeled a candidate at its status line, at both link sites, and in all seven audience-index rows, adds no acceptance criterion to any order, and selects no schema, host, framework, provider, or command.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in this worktree on the verified tree and again on the reviewed tree: Prettier clean, the body-profile unit suite, the eight shell suites including the eleven work-order index fixture groups, `check-publication.mjs` at 183 of 183 headings with both audience locks current, `PASS docs/work-orders/README.md is current`, `tsc -b --force`, 173 of 173 package tests, 8 of 8 identity-corpus tests, exit 0. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.5.2; expected v0.5.2`, `PASS component-version` for `@dotln/compiler` `0.2.0`, `@dotln/kernel` `0.2.1`, and `@dotln/skeleton` `0.5.0` with `src unchanged`, and the body-profile rule over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity at review open: a temporary-index tree of the working tree differed from the verdict checkpoint only in the two control files and the WO-026 index row that each role's refresh moves; from the verify-allocation checkpoint additionally in `VER-001`; the activation checkpoint's tree equals `main`'s; all five checkpoints have `68b1ab2` (`main`, `origin/main`, and the `v0.5.1` tag target) as their sole parent; `git log main..wo-026` is empty; the verifier's report header equals its control-log actor by canonical JSON comparison; the executor's `max` and the verifier's `max` meet the declared `xhigh+` floors.
- Direct probes: `release list` from an isolated copy of `main`'s scripts and from the merged helper are byte-identical over the fifteen local release tags; four closure attributions were re-derived from the tag objects (`v0.2.1`, `v0.2.2`, `v0.2.3`, `v0.3.5`); 476 relative links and anchors across the 19 changed Markdown files resolve; 36 files produce 36 rows in four sections; the clean-room and secret screen over all 530 added tracked lines and every new file found nothing; the canonical ignored intake capture exists in the main checkout at mode `0600` and this worktree's intake holds only `.gitkeep` files.

## Verification trail

- `docs/verifications/WO-026/VER-001.md`: pass; all six criteria, the evidence gate, and the write-backs reproduced independently, including a 152-comparison probe folding every prefix of the real control log through the pre-extraction and extracted fold, a `release list` byte comparison, a 298-reference scan, direct corruption of the real index to prove `--check` names the line, and a digest of the ideation receipt; three non-blocking observations and four disclosures with an empty repair checklist.
- `docs/final-reviews/WO-026/FINAL-001.md`: pass; the verifier's observations adjudicated with reasons, the reviewer's own observations recorded, the ideation receipt inspected against the guide's list, two bounded documentation corrections applied and re-checked (the roadmap's release-default sentence for `v0.5.2`; the planning map's WO-026 preflight cell), method and instruments disclosed.

## Known open items

- **`release list`'s fallback attribution widened** (VER-001 observation 2): when the first-parent walk finds no order between two releases, the fallback now lists every order a manifest names rather than `workOrder.id` alone; latent on the current tag set, where the output is byte-identical.
- **`WO-011` renders `malformed` in the Version column** (VER-001 observation 3): its title's "v1" is a product-generation token, not an application version, and the conservative rule reports `malformed` rather than `unassigned`; a distinct label is a candidate for a later bounded order.
- **The series runbook receives the index pointer indirectly**: the write-back duty asked for a pointer in `codex-downtime-series.md`; the file freeze in criterion 1 forbids the edit, so its existing map link reaches the index.
- **The committed index snapshot lags a new release tag**: after release close tags `v0.5.2`, `index --check` reports it as newer evidence and passes; the next work order's executor regenerates the index and attributes WO-026 to `v0.5.2`.
- **Generator polish candidates**: a stale `README.md.tmp` from an interrupted run surfaces as a raw `EEXIST` error rather than a named refusal; titles containing code spans render their backticks as literal characters in the index by design.
- **`CLAUDE_EFFORT` is an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9): still open, so Claude Code attestations remain `self-reported`.
- **Publish and release close.** The bounded publisher `npm run worktree -- publish WO-026` runs the committed surface check and body validation before any push and then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses at that preflight the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.5.2` is strictly above `v0.5.1`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.5.2` tag, and creates the matching GitHub Release.
