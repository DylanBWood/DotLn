# WO-131 FINAL-001 — reusable gates and a finite completion path

**Verdict: pass.** VER-001's reuse failures are repaired and independently reproduced by VER-002. This review closes VER-002's recording gap and implements the operator's explicit final-session correction to the gate/report loop. The completion event binds this judgment to the required passing full gate and diff check at the final pre-transition tree; this report does not claim the timing of a check that has not yet run at authoring time.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.154.0","model":"gpt-6-astra","effort":"max","source":"operator-attested"}

Dispatch: `resume: final review`, 2026-09-14, followed by the operator's instruction to fix the recurring final-gate/report-edit/final-gate loop in this session. Codex CLI 0.154.0 was observed directly; model and effort use the repository's operator-attested default, not effective-session readback. No other writable agent was used.

## Subject and integration

The subject is [WO-131](../../work-orders/WO-131-remaining-suites-under-replica.md), criteria 1–11 as amended by 12–18, and the working diff on `wo-131`. The fetched base and current `origin/main` are both `151481f427e2456cf1762f4092edf8afba3a76ea`; no merge, conflict resolution or release retiming was needed. The application target remains v0.17.6, with compiler 0.9.2 and skeleton 0.15.11. Kernel, console, domain-event schemas and dependencies are unchanged.

Read scope included the order and cited sections; declaration, environment, replica, sandbox, scheduler and planning-reader changes; generated-hook compiler, operator control, bootstrap, host admission and publication handoffs; changed source tests and new sandbox/release fixtures; VER-001/VER-002, decisions, engineering review and ideation receipt; and the publication contract. Generated surfaces are checked through their generators and evidence editions. The retained WO-130 release failure and successful retry were resolved read-only from main's ignored `docs/control/local/retained/WO-130/`, an additional input absent from this worktree. They confirm the installed-link and closeout-admission repairs already present in the base are historical work, not newly claimed code changes.

## Findings and corrections

1. **VER-001 F1–F3: closed.** Kernel denial is recorded and applied when available but no longer determines reuse eligibility. Candidate and replica executions have distinct keys; narrowing refusals retain the complete candidate tree. Execution and key share each suite's environment projection and canonical PATH. VER-002's first independent gate ran 62.377 seconds with 17 fresh and 62 reused tasks when denial was unavailable, against VER-001's 807.236 seconds and zero reuse. Historical same-shell measurements keep their original conditions.
2. **VER-002 F1: closed here.** Copied its first gate's row from the host record into `gate-performance.json` as `independentVerification` and the evidence README. Checked tree, timestamp, duration, verdict, 17/62 split and fresh task list against the report. The comparable saving is 744.859 seconds, 92.27 percent. There are 63 declared tasks including uncached release preparation; all 62 cacheable tasks reused. This corrects the report's incidental wording that all 63 reused without changing the observation.
3. **Final-gate reporting loop: corrected under amendment 18 and D022.** The prior procedure lacked a stopping point: VER-002 added final timings and token totals after passing gates, repeatedly invalidating exact-tree evidence. Role instructions and Process Cost now require dated cutoffs before the final gate, retain later timings/counters in ignored receipts and the response, then proceed through output reads, completion and one index refresh. The evidence command prints that handoff after a pass. No path was excluded from hashing, no gate was weakened, and no post-gate validator was added. The operator expressly authorized this correction in final review; it is authored and tested here, not attributed to VER-002's independent judgment.
4. **Publication screening.** Both verification reports contained a raw local harness session identifier in their usage command. Original bytes remain in ignored recovery storage; public copies replace only the identifier with `<session>` and carry a dated notice. Verdicts, measurements, reproduction and checkpoint refs are preserved. No employer material, credentials, internal URLs or secrets are included. Commits and PR text carry no AI attribution.

## Acceptance and evidence

| Criteria | Review result |
| --- | --- |
| 1–3: declarations and composition | Every scheduled task is declared or explicitly retained. The six shell suites have replica scopes; live/current checks stay in the candidate tree. Reviewed exact reused-name/count assertions and before/after input identities. VER-002 re-executed runner and process-debt fixtures fresh. |
| 4, 7, 11, 15: measured payoff | The real cross-role row is filed in the required directory with unavailable denial. Cold, document and investment rows retain workload and cutoff labels; the fresh host result is 801.849 seconds against the 805-second ceiling. No cold speedup or total investment repayment is inferred from unlike runs. |
| 5, 13, 16: kernel denial | Reviewed parent/descendant denial, one-probe wrapping, both directions of cross-denial reuse and changed-input invalidation. Amendment 16 supersedes the prerequisite. A hard-coded absolute candidate read without denial remains the accepted residual under D019. |
| 6: write-backs and checks | D001–D022, execution guidance, evidence, follow-ups and indexes retain sources and reopening conditions. No dependency or suppression was added. The final full gate and diff check are required before the completion event. |
| 8–10: release close | Copied-link graph, atomic failed-copy handling, exact main admission set, surviving helper handoff and real-closeout composition fixture agree. VER-002 ran the whole release group fresh. The live WO-131 closeout row belongs to the post-merge role under operator-review assumption 3. |
| 12, 14: recovery | Generated hooks consult operator control before repository/runtime imports. Setup/dispatch failures are advisory at prompt submission; ordinary mutations retain writer/gate checks. Fixtures cover corrupt state, absent runtime/Git, held gates/writers, isolation and exit. General arbitrary-state recovery remains a candidate. |
| 17: engineering review | The six-order review ranks failures, applies bounded repairs and names residual work separately. Its cross-session diagnosis agrees with VER-002. |
| 18: finite completion | The strengthened process-debt fixture executes one full gate, then all four completion-evidence actions collect later usage without changing the tree or gate rows. It asserts one physical full-gate invocation and refuses completion after a report edit. The existing reuse test also invalidates changed source. Both targeted tests passed in 9.188 seconds. |

Additional executed checks before the final gate: build passed; the pinned runner baseline and both harness-context files matched historical Git sources byte for byte; authority revision 009 was generated and checked (four unchanged programs, four widening rejections, nine runtime denials, 27 bundle comparisons); artifact identity and feedback evidence passed; the new instructions were condensed before the final gate and every installed role fits its existing context budget; both publication source locks passed after the execution-guide lock refresh. The final full gate checks the affected inventories and current outputs after authoring and generation are complete.

The scheduler, replicas, cache and generated hooks are instruments under review. VER-002's fresh reproductions and this session's direct completion regression supplement the composed aggregate. The earlier independent verdict is carried forward for its actual subject; it did not judge the new final-review correction.

## Completion boundary and cost

Report measurement cutoff: 2026-09-14T17:09:57.904Z. The collector recorded 3,481,575 total tokens, including 3,315,968 cached input, source `codex-transcript-counter`, scope `dispatch`. These are cumulative usage, not context occupancy. The first usable entry reading was 125,976 tokens at 17:03:56.438Z after registration and initial routing. A first collection before a fresh counter was available failed and was repaired by the next observation. Wrapped command count and monetary cost are unavailable.

The targeted regressions took 9.188 seconds. Review, generation, preparation and waiting are included in usage; no equivalent alternative review cost was measured. Final gate duration and later counters remain in ignored receipts and the response. Completion recollects usage before appending. This report and the PR meter keep their pre-gate cutoff rather than copying their own validation cost. After passing completion, the generated index refresh and reviewed publication do not create another full-gate obligation.

Mission and critical path: dependable reuse and finite handoff remove recurring supervision from the independently verified source-to-deliverable route. D022 records NoOp and all eight traps. Policy resistance and rule beating require exact-tree evidence without recursive reporting; commons cost, escalation and shifting the burden oppose another rescue cycle; drift and seeking the wrong goal require measured completion; success to the successful does not protect the old ritual. Naive Interventionism favors existing receipts and a reversible ordering correction. Reopen on a missed dependency, cross-role reuse failure, failed live closeout, or a report again required to contain the cost of checking itself.
