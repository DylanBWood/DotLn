## What this merges

WO-023, the `v0.5.0` compiled Entropy Reducer: the repository's second founding identity now exists as typed instance content rather than as a prompt. One identity, one planning-reviewer role, a Shine active linked to Standardize and seven further supports, four masks, one authority envelope, one kernel Program, and one once-cadence are authored as compiler-v1 data under `packages/skeleton/src/loadouts/` and compiled through the landed `@dotln/compiler` into a thirteen-field WorkOrder whose operation lists equal the envelope's effect lists and whose `requiredEvidence` is raised to `["reproduction"]` by the Verify-First support. The reviewer's stance is a generated residue that a hand edit cannot survive, its output is held by typed validators before `report.emit`, and its first review ran once under the pinned Claude Fable 5.1 `max` actor and was challenged by a fresh blinded refutation that all seven findings survived. The pure compiler gains one bounded capability the loadout required: direct terminal prefix globs such as `repo.read*` now lower when no linked support carries an `authority.*` claim, while wildcard claim targets and wildcard-plus-claim graphs still refuse. `@dotln/compiler` advances to `0.2.0` and `@dotln/skeleton` to `0.5.0`; the kernel is untouched at `0.2.1`; no transport and no dependency were added. The order's activation-time retarget from the pre-WO-008 draft to the real compiler and its two 2026-09-04 operator-authorized ideation breakouts (pinned artifact identity, which filed the WO-029 draft; context projections and the application name's pronunciation guide) are included.

- **Typed definitions and the compiled WorkOrder (criterion 1):** `entropyReducerLoadout` returns the graph and `compileReviewerWorkOrder` returns the compiled program, semantic hash, WorkOrder, envelope, Program (`Sequence` of a census `Invoke`, an `All` over four lens `Invoke`s, a probe `Invoke`, two readiness `Emit`s, and `Await OperatorDisposition`), `Cadence.Once`, masks, the pinned reviewer requirement, the execution boundary, the validated lens briefs, and the residue, together with the exact compile inputs so the hash is reproducible.
- **Authority and validators (criterion 2):** under the compiled envelope the kernel refuses `repo.write`, `repo.write.file`, `git.mutate`, `git.mutate.commit`, `remote.*`, `settings.*`, and `decision.edit` as `effect denied` with a trace naming `auth_entropy_reducer`, grants the `repo.read*`, `probe.run:scratch*`, `intake.capture`, `delegate.readonly`, and `report.emit` families, refuses unanchored prefixes, and checks denies before allows; resource ceilings (`delegates` 4, `probes` 32), expiry, and the `OperatorStopRequested` revocation thread through `authorize`. `prepareReportEmit` validates findings (evidence label paired with a command or inspection reproduction, altitude 2–12, one-off or recurring standardization with a rung), proposal packets (safe single-segment ids, `docs/proposals/` path whose final segment is that id, episode match), the result envelope (fewer than 200 words), and a passing clean-room result before constructing the `report.emit` intent.
- **Generated residue (criterion 3):** `docs/instance/entropy-reducer/RESIDUE.md` is rendered from the graph with one paragraph per linked support carrying emission digests, plus identity dispositions and invariants, role obligations, active semantics and constraints, and the masks; `npm test` byte-compares it with a fresh render, and mutation tests prove every rendered field changes it while permutation does not.
- **Lens briefs (criterion 4):** the four default briefs validate against a six-field schema with a positive integer word budget and `noFix: true`; the ceiling is read from the compiled `delegates` limit; duplicate ids and intake-bearing or unsafe scopes refuse.
- **Recorded episode (criterion 5):** `runs/REVIEW-001.json` records episode `ep_entropy_reducer_20260904_001` on Claude Code 2.1.260, `claude-fable-5-1`, `max`, against a frozen scratch subject at base `e3e639f`, with the compiled program preserved in `REVIEW-001-COMPILED-PROGRAM.json` at `fnv1a64:b81d0609ee2e9fed`, seven typed findings, one proposal payload explicitly not filed, and a 178-word summary.
- **Blinded refutation (criterion 6):** `selectFindingsForRefutation` selects every measured finding and every by-inspection finding at six or fewer (a deterministic surface-and-severity round-robin sample of `max(6, ceil(n/3))` above), reports zero denominators as `not-applicable`, and `buildRefutationReport` keeps refuted and blocked findings attributable and out of the promoted set with no quota; `runs/REFUTATION-001.json` rebuilds byte for byte.
- **Confinement (criterion 7):** the review record carries byte-identical before/after status digests for source and scratch, 75 scratch deltas all under ignored `packages/*/dist/`, and a metadata-only intake inventory identical before and after; the refutation record shows zero deltas.
- **Compiler extension:** thirteen lines in `packages/compiler/src/compile.ts` compute the linked `authority.*` claims and refuse a wildcard only when one participates; the rewritten test covers active-origin and support-origin grants and denials, the inert unequipped case, wildcard-claim refusal, and mixed refusal; README, domain model, interface contract, ledger, and version move together.
- **Write-backs:** the pattern library's second founding identity, the playbook row, the architecture's first-live-use record with the WO-029 bootstrap exception, the `docs/instance/` map entry, four ledger sections, the roadmap's dated `v0.5.0` retiming with the five open drafts reciting rung names, ADR-0002's appended amendment, the README release block at `v0.5.0`, the capability table's dated note, the publication index with both edition locks refreshed, and the work-order map.
- **Ideation breakouts (documentation only):** the `WO-029` draft and the architecture's `Candidate — pinned artifact identity`; the architecture's candidate source-addressed context projections, preserved without a work order until a transport consumes one; the application-thesis heading now reads `προτείνω (protíno)` with the verb form, pronunciation guide, and unchanged file path recorded. Each receipt names the authority, source treatment, promoted surfaces, and unresolved choices.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in this worktree on the verified tree and again on the reviewed tree: Prettier clean, the body-profile unit suite, the shell suites, `check-publication.mjs` at 173 of 173 headings with both audience locks current, `tsc -b --force`, 173 of 173 package tests, 8 of 8 identity-corpus tests, exit 0. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.5.0; expected v0.5.0`, `PASS component-version @dotln/compiler: src changed; observed 0.2.0; previous v0.4.1 0.1.0`, `PASS component-version @dotln/kernel: src unchanged; observed 0.2.1`, `PASS component-version @dotln/skeleton: src unchanged; observed 0.5.0` (the pre-commit rule cannot see untracked source; the committed run at publish can), and the body-profile rule over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity at review open: a temporary-index tree of the working tree differed from the verdict checkpoint only in the two control files, and from the implementation-ready checkpoint only in those plus VER-001; the activation checkpoint's tree equals `HEAD`'s; all five checkpoints have `HEAD` (`e3e639f`, also `main`, `origin/main`, and the remote's `main`) as their sole parent; `git log main..wo-023` is empty; the verifier's report header equals its control-log actor by JSON comparison; the executor's `xhigh` and the verifier's `max` meet the declared `xhigh+` floors.
- Direct probes against the built modules: the tracked residue is 10,288 bytes at SHA-256 `b07bc753…` and byte-equal to a fresh render; the preserved compiled program hashes to the recorded `fnv1a64:b81d0609ee2e9fed` and the repaired subject at the same inputs to the pinned `fnv1a64:eefe230707d23735`; the refutation report rebuilds byte-equal with denominators 1 and 6 and seven promoted; the recorded output validates and its summary is 178 words; thirteen effect probes and a synthetic deny-before-allow envelope through the kernel's `authorize`; the compiler gate read in full and its four rules traced to lines; the clean-room and secret screen over the complete 8,492-line change set.

## Verification trail

- `docs/verifications/WO-023/VER-001.md`: pass; all eight criteria, the evidence gate, and the five write-backs reproduced independently through built-module probes, a twenty-three-mutation drill in an isolated copy, and recomputed hashes and selections; four advisory findings and three disclosures with a bounded checklist.
- `docs/final-reviews/WO-023/FINAL-001.md`: pass; the verifier's findings adjudicated with reasons, two further measured observations, both ideation receipts inspected against the guide's list, one bounded documentation correction applied and re-checked, method and instruments disclosed.

## Known open items

- **The activation-time amendment carries no dated operator marker** (VER-001 F1). The retarget executes the activation preflight the committed work-order map recorded for WO-023, the compiler extension follows from the activation text's own envelope patterns, and criteria 1 and 3 through 8 are byte-identical to activation, so nothing in the evidence changes; the record form is what is missing. Merging this PR is the operator's confirmation of the retarget, the compiler-source extension, and the non-goal rewrite.
- **The pre-commit `check-surfaces` cannot see untracked component source** (VER-001 F2): a `--untracked-files=all` fallback or an advisory label on the pre-commit run belongs to the next scripts order.
- **A bare `*` compiles in a claim-free graph with no diagnostic** (VER-001 F3): no shipped definition uses it; refusing an empty prefix with a fixture is a bounded follow-on.
- **The kernel's prefix glob is string-anchored** (VER-001 F4): `repo.readwrite` matches `repo.read*`; no current effect name falls in the gap; for the order that next adds effect names.
- **The lens-scope validator accepts glob-shaped and whitespace-padded scopes** (FINAL-001 adjudication 5): `docs/*`, `*`, `**`, and whitespace-padded intake spellings pass; the compiled Git-index constraint is the load-bearing guard, and a trim-and-resolve repair with those strings as fixtures is a follow-on.
- **`CLAUDE_EFFORT` is an unrecorded readback candidate** (FINAL-001 adjudication 9): the harness exposes the selected effort to the shell; a bounded discovery pass could record it and make `harness-readback` admissible for Claude Code sessions.
- **Publish and release close.** `gh` in this sandbox fails before its root command exists, so `npm run worktree -- publish` refuses at its `gh` preflight after the committed surface check and body validation and before any push. The operator runs the publish command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.5.0` is strictly above `v0.4.1`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.5.0` tag, and creates the matching GitHub Release.
