# WO-023 final review FINAL-001

**Subject:** branch `wo-023`, worktree `DotLn-wo023`, uncommitted working tree, 2026-09-04. `HEAD`, `main`, `origin/main`, and the remote's `refs/heads/main` (by `git ls-remote`) are all `e3e639fea0c7566e9aa9418dc231bea6b36f4dd7`, the PR #27 merge of WO-018 that the published annotated tag `v0.4.1` names; the branch carries no local commits (`git log main..wo-023` is empty), so the entire subject is the dirty tree. At review open, after this review's own transition: 32 tracked modifications (811 insertions / 235 deletions against `HEAD` including the two `docs/control` files; 30 files, 797 / 226 without them) plus eleven untracked paths: `packages/skeleton/src/loadouts/entropy-reducer.ts` (2,133 lines), `packages/skeleton/test/entropy-reducer.test.ts` (887), `packages/skeleton/test/entropy-reducer-artifacts.test.ts` (348), the six files under `docs/instance/entropy-reducer/` (`RESIDUE.md`, `REFUTATION-PLAN.md`, and the four run records), `docs/work-orders/WO-029-pinned-artifact-identity.md` (246), and `docs/verifications/WO-023/VER-001.md` (424); 43 `git status --porcelain -uall` entries. The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree (`d794a191d7c40c428576f7b701cb47a564e04022`) differs from checkpoint `refs/dotln/checkpoint/WO-023/4` (the verdict) only in `docs/control/current.md` and the strictly appended `docs/control/resume.jsonl`, and from `/2` (implementation-ready) and `/3` (verify allocation) only in those two files plus VER-001 itself (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, one bounded documentation correction (§Corrections applied by this review), and the lifecycle's own events.

**Authority:** `docs/work-orders/WO-023-compile-entropy-reducer.md`: `Model: any capable model for the implementation`, with the compiled loadout pinning its own review-episode actor to Claude Fable 5.1 at `max`; `Effort: executor xhigh+; verifier xhigh+; reviewer any`; a `v0.5.0` minor classification; eight numbered acceptance criteria; a nine-item evidence gate; a five-item write-back duty plus the compiler extension's move-together duty; a non-goal list; four operator-review assumptions; and two 2026-09-04 operator-authorized ideation-breakout receipts (pinned artifact identity; context projections and naming), each of which names this review as owing it an inspection. Against the activated text (`refs/dotln/checkpoint/WO-023/1`, whose tree equals `HEAD`'s by `git diff-tree`), the order differs by the activation-time retarget to the landed compiler and the two appended receipts (VER-001 F1; adjudication 1).

**Prior reports:** `VER-001` (pass; all eight criteria, the evidence gate, and the five write-backs reproduced through direct probes against the built modules, a twenty-three-mutation drill in an isolated copy outside the repository, and independent recomputation of every hash and selection; four non-blocking findings F1–F4 and three disclosures D1–D3 with a bounded checklist). Complete sequence: one report, no repair episode. The implementer-run review and refutation episodes under `docs/instance/entropy-reducer/runs/` are subject evidence, not verification, and were treated here as VER-001 treated them: claims under test.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes) in Claude Code 2.1.260 with sandboxed Bash; macOS on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 in this worktree's own dependency tree. Reasoning effort `max`: the session's model control reported it at launch and the harness exposes it to the shell as `CLAUDE_EFFORT=max`, but `docs/discovery/environment.json` records `effectiveEffortReadback` for `claude-code` as `not found`, so `harness-readback` is not admissible and the epistemic source is `self-reported`, exactly as VER-001's was (adjudication 9 records the readback candidate). `2.1.260` was checked against `claude --version` on this machine and is the newest entry on the observed `versions` list; `max` is on `sessionEffortSelector.values`; so the attestation is admissible. The order declares `reviewer any`, so any attested effort would satisfy it; `max` is stated because it is what the session reports.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.260","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-023 passes.** The order asked for the Entropy Reducer to exist as typed instance content rather than as a prompt: compiled through the real `@dotln/compiler` into a bounded WorkOrder and authority envelope, its stance emitted as a generated residue that a hand edit cannot survive, its output held by typed validators before `report.emit`, its first review run once under the pinned actor and challenged by a blinded refutation, and all of it without touching the kernel, adding a transport, or adding a dependency. The deliverable does that. One identity, one role, one Shine active linked to Standardize and seven further supports, four masks, one envelope, one Program, and one once-cadence exist as compiler-v1 data; the compile step returns thirteen populated WorkOrder fields whose operation lists are byte-equal to the envelope's effect lists and whose `requiredEvidence` is `["reproduction"]`, raised from the Verify-First support rather than written by hand; the kernel refuses every write, mutate, remote, settings, and decision family with a trace naming `auth_entropy_reducer` and grants the read, probe, capture, delegate, and report families with anchoring and deny-before-allow proven; the tracked residue is byte-identical to a fresh render and every rendered field changes it; the lens-brief validator enforces every field, the no-fix rule, uniqueness, and the compiled four-delegate ceiling; the recorded episode carries the pinned actor, seven typed findings, one unfiled proposal payload, and a 178-word summary; the blinded refutation selected all seven findings under the compiled rule and rebuilds byte for byte; the tracked-status pair and confinement inventory are consistent; the suite is green, whitespace is clean, and the lockfile changes only internal versions. The compiler extension is thirteen lines whose four documented rules were each traced to the line that enforces it. Every count, digest, and refusal in VER-001 that this review could reproduce was reproduced first-hand. VER-001's four findings are one process-record question for the operator, one pre-commit preflight blind spot in a script this order does not touch, one unenforced prose disclaimer about a bare `*` pattern that no shipped definition uses, and one pre-existing kernel matching semantic; this review adjudicates all four as non-blocking, records two measured observations of its own, corrects one planning projection, and changes no code, contract, acceptance, schema, authority, or captured-evidence byte.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **The lifecycle scripts are not the subject this time.** `git diff HEAD -- scripts/ package.json` is empty and `scripts/` carries no untracked file; `scripts/resume.mjs`, `worktree.mjs`, and `release.mjs` are the reviewed `v0.4.1` helpers. Before invoking the allocation transition this review still read the `resume` mapping in `package.json` and confirmed the empty diff, as the guide requires.
2. **The compiled loadout and the compiler are both the subject and the instrument of every probe of them.** `packages/skeleton/src/loadouts/entropy-reducer.ts` and `packages/compiler/src/compile.ts` are deliverables, and they are what any check of the residue, authority, validators, selection, and hashes must call. Compensating controls: the built modules were imported directly and exercised on inputs constructed in this session rather than through the shipped assertions; the compiler gate (`compile.ts:468-541`) was read in full and its four documented rules traced to the lines that enforce them rather than inferred from tests; the kernel's `authorize` rule order and `effectMatches` were read at source; the residue was compared by SHA-256 of the tracked bytes against a fresh render; the historical and repaired semantic hashes were recomputed from the preserved preimage and the recorded compile inputs; the refutation report was rebuilt from the recorded findings and attempts and compared as bytes.
3. **The discovery record that admits actor attestations is edited inside the subject** (one appended `codex-cli` observation, `0.153.2`, with an `observedAt`; nothing else in the file changes). The `claude-code` entry this review relies on is unchanged from `v0.4.1`. This review did not edit the record.
4. **This reviewer's own attestation is checked by the mechanism that checked the verifier's.** A successful `final-review-result` append proves the header above equals the flags and nothing about the subject; the actor inventory below was compared by object comparison in this session.
5. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile** (`scripts/release-notes.mjs`, `scripts/github-body.mjs`, and `release check-surfaces`), none of which this order changes. Because `v0.5.0` is above the latest published tag, these notes become the `v0.5.0` edition at release close.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-023 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, and drift `xhigh -> max`; `docs/control/current.md` agreed and no drift warning was emitted. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-023/FINAL-001.md`, moved the phase to `final-review`, and minted checkpoint `de1d472882970e16211466b3b5db8c81ce970fc2` (`refs/dotln/checkpoint/WO-023/5`). `docs/control/resume.jsonl` is a strict append over `HEAD`: its first 128 lines are byte-identical to `HEAD`'s file (`cmp` silent), and the five appended events are `WorkOrderActivated` (with `effortDeclarationValidated: true`), `ImplementationReady`, `VerificationRequested`, `VerificationCompleted`, and this review's `FinalReviewRequested`, plus the `FinalReviewCompleted` the pass records. Every event carries `schemaVersion: 1`.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree as tree `d794a191d7c40c428576f7b701cb47a564e04022`, and `git diff-tree -r --stat` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                                                                                            |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `/1` activation (`833c4f1a…`)              | every deliverable path (43 files, 7,528 insertions, 235 deletions); the checkpoint's tree equals `HEAD`'s, so the whole subject, the work order's own amendment included, is downstream of activation |
| `/2` implementation-ready (`7ee80a5d…`)    | `docs/control/current.md`, `docs/control/resume.jsonl` (+4), `docs/verifications/WO-023/VER-001.md` (added, 424 lines)                                                                                    |
| `/3` verify allocation (`787d0ccd…`)       | `docs/control/current.md`, `docs/control/resume.jsonl` (+3), `docs/verifications/WO-023/VER-001.md` (added)                                                                                                |
| `/4` verdict (`3775e58f…`)                 | `docs/control/current.md`, `docs/control/resume.jsonl` (+2)                                                                                                                                                  |
| `/5` final-review allocation (`de1d4728…`) | `docs/control/current.md`, `docs/control/resume.jsonl` (+1)                                                                                                                                                  |

Every deliverable blob, the 30 tracked non-control modifications and the ten untracked deliverable files, is therefore identical across the implementation-ready, verify-allocation, and verdict checkpoints and the working tree; the verifier changed nothing in the deliverable, and VER-001 has not changed since its verdict was recorded. All five checkpoints have `HEAD` as their sole parent.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. `git ls-files docs/intake` lists the three `.gitkeep` placeholders and nothing else; `git status --porcelain --ignored -- docs/intake` is empty, so no ignored intake file exists in this worktree, consistent with both receipts' statement that the canonical captures live only in the main checkout; `git ls-files --others --ignored --exclude-standard` shows nothing outside `node_modules/` and `packages/*/dist/`. `.claude/` carries no change, tracked or untracked. The main checkout's ignored intake was not opened.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                            | Report header                                                                                                                                                                                                                                                              | Declared minimum | Result                          |
| -------- | ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.2","model":"gpt-5.6-sol","effort":"xhigh","source":"self-reported"}`         | there is no executor report; `0.153.2` is the newest entry on the `codex-cli` observed `versions` list, appended by this subject with an `observedAt` in the append-never-replace form the guide prescribes, and the persisted `model_reasoning_effort` selector records `xhigh` | `xhigh+`         | `xhigh` meets the floor exactly |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.260","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}`   | exactly one column-one header in VER-001, parsed and compared with the event actor as JSON: equal                                                                                                                                                                          | `xhigh+`         | `max` exceeds the floor         |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.260","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`    | the header above                                                                                                                                                                                                                                                           | `any`            | satisfied                       |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: xhigh -> max` and the JSON form's `effortDrift` is `[{"effort":"xhigh"},{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.260; model claude-fable-5-1; effort max; source self-reported` with the same drift line and phase `closed`.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree before any review artifact existed: `All matched files use Prettier code style!`, `GitHub body profile tests passed`, the shell suites (fixture temp-root, publication checker, backup-intake, resume, checkpoint, worktree, and release with its notes, PR-target, surface, edition, and recovery fixtures), `PASS index coverage: 173/173 product headings indexed` with both audience editions `CURRENT` (27 and 42 linked sections), `tsc -b --force`, `# tests 173 / # pass 173 / # fail 0`, and the identity corpus `# tests 8 / # pass 8 / # fail 0`, exit 0. Second run, after this review's correction and package were in place, with the same result (§Corrections applied by this review).

### Static, surface, and structural checks

- `git diff --check`: exit 0, silent. `npm run format:check`: `All matched files use Prettier code style!`.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.5.0; expected v0.5.0 (work-order target v0.5.0; latest published v0.4.1)`; `PASS component-version @dotln/compiler: src changed; observed 0.2.0; previous v0.4.1 0.1.0; expected a different version`; `PASS component-version @dotln/kernel: src unchanged; observed 0.2.1`; `PASS component-version @dotln/skeleton: src unchanged; observed 0.5.0` (VER-001 F2 reproduced; adjudication 2); `PASS github-body-profile`, first with no current final-review bodies and again over this package's `PR.md` and `RELEASE-NOTES.md`.
- Dependency delta: `package-lock.json` changes only the internal workspace versions (`@dotln/compiler` 0.1.0 → 0.2.0, now also carrying its `name`, and `@dotln/skeleton` 0.4.0 → 0.5.0 with its exact compiler pin); no dependency was added or changed.
- Control log: strict append over `HEAD` proven by `cmp`; the verifier's report header equals its control-log actor by JSON comparison.
- Clean-room and secret screen over the complete change set (the `HEAD` diff plus every untracked file, 8,492 lines): no credential pattern, no employer-shaped term, no internal hostname; every `v1` match is the repository's own `compiler v1` and `v1 oracle` vocabulary; the run records alias local roots as `<main-checkout>`; the one absolute personal path is in VER-001's instrument disclosure and is adjudicated below (adjudication 8).

### Independent probes against the built modules

- **Residue.** The tracked `docs/instance/entropy-reducer/RESIDUE.md` is 10,288 bytes with SHA-256 `b07bc753bfcf92fcc104d1ebc391ca5cc6d1663dba741ce04a3861cf058b9512`; `referenceEntropyReducerResidue()` renders the same bytes.
- **Hashes.** `semanticHash` over the preserved `REVIEW-001-COMPILED-PROGRAM.json` is `fnv1a64:b81d0609ee2e9fed`, equal to the run record; `compileReviewerWorkOrder` at the record's exact compile inputs (the scratch repository path, base `e3e639f…`, the episode id, `dispatchedAt` 1788539400000, `episodeEndsAt` 1788546600000) is `fnv1a64:eefe230707d23735`, the repaired hash the artifacts test pins.
- **Refutation.** `buildRefutationReport(recorded findings, recorded attempts)` is byte-equal to `REFUTATION-001.json`'s `report`: measured denominator 1, inspection denominator 6, status `all`, seven promoted, none refuted, blocked, or unselected.
- **Recorded output.** `validateReviewerOutput` accepts `REVIEW-001.json`'s `reviewerOutput` against `wo_entropy_review_1` and `ep_entropy_reducer_20260904_001`; the summary is 178 words by `countWords` and by a naive whitespace split.
- **Authority under the compiled envelope** (kernel `authorize`, `now` inside the window): `repo.write`, `repo.write.file`, `git.mutate`, `git.mutate.commit`, `remote.push`, `settings.write`, and `decision.edit` all refuse `effect denied` with trace `["refused","effect denied"]` and `authorityEnvelopeId: "auth_entropy_reducer"`; `repo.read`, `repo.read.file`, and `report.emit` grant with trace `["authorized",<effect>]`; `repo.rea` and `xrepo.read` refuse `effect not allowed` (anchoring holds); a synthetic envelope with `allowedEffects: ["repo.*"]` and `deniedEffects: ["repo.write*"]` refuses `repo.write.file` as `effect denied`, which is the rule order at `packages/kernel/src/core.ts` (`effect denied` precedes `effect not allowed`); `repo.readwrite` grants (VER-001 F4 reproduced; adjudication 4).
- **Compiled evidence.** `workOrder.requiredEvidence` is `["reproduction"]` while `authorityEnvelope.requiredEvidence` is `[]`, which is correct because the kernel applies the envelope field to every effect.
- **Compiler gate.** The gate computes `linkedAuthorityClaims` from linked supports' claims whose target begins `authority.`; a linked support's wildcard claim target refuses unconditionally (`compile.ts:493-503`); a wildcard in the active's WorkOrder operations or envelope effects (`:505-518`) or in a linked support's permission-guard emission (`:519-535`) refuses only when `linkedAuthorityClaims` is non-empty; unlinked supports never enter the set. With the Entropy Reducer graph's operations and effects replaced by a bare `*` and empty denials, `compileLoadout` succeeds (`fnv1a64:2ac376cc8d8ed862`) (VER-001 F3 reproduced; adjudication 3).
- **Lens scope validator.** `validateLensBrief` refuses `docs/intake/` and accepts `docs/*`, `*`, `**`, `docs/int*`, `docs/intake ` (trailing space), `docs/intake` followed by a tab, ` docs/intake/` (leading space), and `docs/intakex/` (adjudication 5).

### Reading

The complete `HEAD` diff of all 32 tracked files; the loadout module, both test files, the residue, the refutation plan, both run records and the preserved compiled program, and the WO-029 draft in full; the work order in full including both receipts; VER-001 in full; the final-review and verification READMEs; the execution guide's dispatch, closeout, discipline, and model-specific sections and the playbook's final-review and publication sections; the WO-018 final review, PR body, and notes as the package precedent; `authorize` and `effectMatches` in the kernel; the compiler's diagnostics gate; the `publish` flow of `worktree.mjs`, the component-version rule of `release.mjs`, and the parser exports of `release-notes.mjs` and `github-body.mjs`.

## Acceptance criteria

| #   | Criterion                                                                                                                                                                     | Verdict | First-hand basis                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Identity, Role, active, link, supports, masks, envelope, Program, and Cadence as typed data; thirteen populated WorkOrder fields; operations equal envelope effects; `requiredEvidence` `["reproduction"]` | met     | `entropyReducerLoadout` returns the graph with one identity, one role, one active tagged observe/research/plan/verify/delegate/narrate, eight supports and eight links in one group; `compileReviewerWorkOrder` returns the thirteen fields, `allowedOperations` and `prohibitedOperations` byte-equal to the envelope lists, and the compiled `["reproduction"]` (probe above; AC1 tests read); the Program is `Sequence` of `Invoke`, `All` of four, `Invoke`, two `Emit`s, `Await` with `Cadence.Once`. |
| 2   | `authorize` refuses bare and family-member `repo.write` and `git.mutate` with a trace, grants bare and family-member `repo.read`, proves anchoring and deny-before-allow; the validator refuses a finding without `reproduction` before `report.emit` | met     | Thirteen effect probes and the synthetic deny-before-allow envelope above; `prepareReportEmit` validates before constructing the `report.emit` intent, and the AC2 tests refuse a missing, empty, and mismatched reproduction, a failed clean-room result, a duplicate id, an unsafe suggestion id, a foreign episode, and a 200-word summary; the kernel's rule order was read at source.                                                                                                                  |
| 3   | Residue generated; each support one paragraph; a semantic change changes it; a hand edit fails byte comparison; generation before the review baseline                        | met     | Byte and SHA-256 equality above; eight `<!-- support:… -->` markers each followed by one bold paragraph in link order; the artifacts tests mutate every support's semantics, two inert-looking fields, four emission bodies, a cost field, and the identity, role, and active stance, and each changes the bytes; permutation does not; the residue was read by the reviewer during `ep_entropy_reducer_20260904_001`, before the implementation-ready checkpoint.                                        |
| 4   | Lens brief schema enforced: missing files, questions, output shape, word budget, or no-fix fails; at most four briefs compile                                                | met     | `validateLensBrief` requires all six fields, a positive integer budget, `noFix === true`, and no extra fields; `validateLensBriefs` reads the ceiling from the compiled `resourceLimits.delegates`, refuses a fifth brief and duplicate ids; `compileReviewerWorkOrder` refuses an intake scope. Scope screening beyond the criterion is measured in adjudication 5.                                                                                                                                      |
| 5   | One review episode from the compiled order through operator-mediated manual dispatch into Claude Code with the pinned actor attested; typed findings, proposal payloads, summary under 200 words | met     | `REVIEW-001.json`: Claude Code `2.1.260`, `claude-fable-5-1`, `max`, the compiled program's hash and preserved preimage, seven findings, one packet explicitly `not authorized and not filed`, a 178-word summary; the recorded output validates; the transport preflight refusal and its one-keyword correction are in the record (D1; adjudication 6).                                                                                                                                                |
| 6   | Blinded refutation of every measured finding and of every by-inspection finding at six or fewer (stratified sample above); denominators, rule, reproduction, result, reason stated; zero denominator `not-applicable`; no quota; refuted findings attributable | met     | Selection and report rebuilt byte-equal above; `selectFindingsForRefutation` and the AC6 tests cover zero, six, seven, and nineteen-finding cohorts with order independence; `buildRefutationReport` keeps refuted and blocked rows attributable and out of the promoted set; the plan and the code agree; the record lists exactly what the refuter received and what it did not.                                                                                                                       |
| 7   | Compiled review order contains no tracked, control-plane, remote, or settings mutation; tracked status byte-identical before and after; scratch and intake deltas confined | met     | The envelope denies the four families (probes above); `REVIEW-001.json` records equal before/after SHA-256 for source and scratch status, 75 scratch deltas all under `packages/*/dist/`, and a metadata-only intake inventory identical before and after; the refutation record shows zero scratch deltas. The pair is attested, not recomputable after the authorized repair (D2).                                                                                                                       |
| 8   | `npm test` green; `git diff --check` clean; no new dependency                                                                                                                | met     | Both runs above; whitespace silent; lockfile delta is internal versions only.                                                                                                                                                                                                                                                                                                                                                                                                                 |

## Evidence gate

Compile output (`REVIEW-001-COMPILED-PROGRAM.json`, hashed here); the three refusal transcripts (`repo.write*`, `git.mutate*` with traces, and the validator's missing-reproduction refusal, all reproduced above and pinned by tests); the residue regeneration test (`entropy-reducer-artifacts.test.ts`, plus this review's byte comparison); the brief validation test (AC4 in `entropy-reducer.test.ts`, plus the probes above); the recorded episode with attestation (`REVIEW-001.json`); the refutation report (`REFUTATION-001.md` and `.json`, rebuilt here); the tracked-status pair (`REVIEW-001.json` `subject.sourceStatus*`); the scratch and ignored-intake delta inventory (`REVIEW-001.json` `confinement`). Every item exists and every reproducible item reproduced.

## Write-back duty

- `docs/product/05-pattern-library.md` names the Entropy Reducer as the second founding identity carrying Shine linked to Standardize beside the Repo Gardener's Sort, and its status line states what the skeleton host now compiles and what remains planned.
- `docs/PLAYBOOK.md` §Who does what carries the row: use for whole-repository entropy review, reproducible findings, and non-authoritative ProductSuggestion payloads; never for implementing, filing, promoting, or verifying its own suggestions or changing tracked, control, remote, or settings state.
- `docs/product/03-architecture.md` §First live Entropy Reducer use records the episode, the refutation, the unfiled proposal payload, and the direct-to-work-orders bootstrap exception for WO-029.
- `docs/README.md` maps `docs/instance/`.
- `docs/lineage/idea-ledger.md` adds four dated sections: the compiled loadout with its live receipt and the generated projection (adopted), the Sustain cadence (preserved), the review output not manufacturing authority (adopted); the `v0.5.0` forward retiming; the claim-free prefix-glob lowering as an extension of the precedence entry; and the two breakout sections. No older entry was rewritten.
- The compiler extension's move-together duty: `packages/compiler/README.md`, `02-domain-model.md` §Identity and composition, `04-interfaces.md` §Compiler contract, the ledger entry, the rewritten compiler test with active-origin, support-origin, mixed, inert, and wildcard-claim cases, and the component version `0.2.0` all state the same four rules that the gate enforces.
- Beyond the duty: the README release block at `v0.5.0`; the roadmap's dated retiming note and `v0.5.0` rung; ADR-0002's appended amendment (its permitted mechanism); the five open drafts reciting rung names in place of superseded versions; the capability table's dated note; the publication index (two new architecture rows, the retiming row, the renamed thesis row, two published rungs moved to `verified`) with both edition locks refreshed and `CURRENT`; the skeleton README; and the work-order map, whose reviewed state this review updates (§Corrections applied by this review).

## Ideation breakout receipts

Both receipts name this review as owing them an inspection, and the guide lists what that inspection covers. Performed on the receipts, the WO-029 draft, and every promoted surface.

- **Source treatment and reconciliation.** Both receipts declare Shape-First Synthesis under the saved bare-`ideation:` loadout, name the canonical capture only by path and SHA-256 in the main checkout's ignored intake, name an owner-only backup archive (the second with its own SHA-256), and state that the temporary staging copy was removed after byte validation. Those steps are operator-side and not reproducible from this worktree; what is externally observable is consistent with them: no ignored intake file exists here, no intake path is linked, staged, or diffed, and the promoted text names no employer material, credential, host, or private identifier.
- **Clean-room screen.** Run by this review over the promoted surfaces with the same term, secret, and path screens as the rest of the change set: nothing found.
- **Fidelity to operator intent, shape first.** The pinned-identity breakout keeps the useful relation, freeze compiled identity at equip, compare at consumption, fail closed on drift, and explicitly declines the literal signature analogy: the architecture candidate says neither hash is a signature, uniqueness guarantee, or authenticity proof, and WO-029's non-goals exclude signatures, keys, Merkle trees, per-pulse submission, and any cryptographic claim. The context-projection breakout keeps the lens shape as a lossy projection with an explicit receipt and declines to file a work order before a consumer exists, which the ledger records as `preserved` with the reason. The naming breakout records the verb form, the pronunciation guide, the related noun, and the unchanged file path.
- **Consistency with settled decisions and vocabulary.** `docs/decisions/` is untouched apart from ADR-0002's Amendments list; the semantic-hash preimage in `04-interfaces.md` is unchanged and WO-029 makes its preservation a criterion; ADR-0006's platform-versus-profile boundary is cited rather than reopened; the WO-029 draft carries the WO-019 `Model:` and `Effort:` lines in its leading metadata; "materialized capsule" and "on-demand expansion" are chosen to leave JIT/AOT as compatibility vocabulary.
- **Version and schema effects.** WO-029 assigns no version and no queue position; its `payloadVersion: 2` equip shape is its own future scope; the roadmap table lists it unassigned; the map's ordering note recommends it before WO-009 without prioritizing it. No control-log, event-envelope, or IR schema changes in this order.
- **Cross-references.** The two new `03-architecture.md` headings have index rows at status `vision` and `implemented`; the renamed `11-proteino.md` heading has its row and anchor updated; no reference to the old transliteration remains outside historical ledger prose (none, in fact, remains anywhere); `check-publication.mjs` reports 173/173 headings and both locks current.
- **Speculative choices not presented as settled.** Both architecture additions are headed or bolded `Candidate`; WO-029 is a draft whose activation, version, and relationship to a retimed WO-009 are stated as later decisions; the roadmap and map say filing granted none of them.
- **Executable helpers.** Neither receipt names one and the change set contains none; both state that no implementation code was created or changed during the breakout.

## Verification-sequence soundness

VER-001 meets every duty the order and the guide place on it: it establishes subject identity through checkpoints, discloses that the instrument is the subject and names three compensating controls, reproduces the executor's evidence rather than taking it on report, probes the compiled loadout and the compiler as built modules on adversarial inputs, runs its mutation drill in a copy outside the repository with `.git` excluded and says why, recomputes every hash and selection, performs the receipts' review, and records its findings with a bounded checklist that expands no scope. Every count it reports was re-derived here where cheap (173 and 8 tests, 173 headings, 27 and 42 sections, the 10,288-byte residue and its digest, both semantic hashes, the 178-word summary, the 75 scratch deltas, the 13-line compiler change, the 42 status entries) and all agree; every line citation this review followed resolves to the described code. Four statements are imprecise without bearing on the verdict, and this review restates them so a later reader is not misled:

1. "42 `git status --porcelain -uall` entries" is the count without VER-001 itself; it is 43 with the report, and the corroborating evidence is the checkpoint-to-checkpoint diff, which touches only the two control files and VER-001.
2. "The 8 067-line change set" is the change set before VER-001 existed; with the report it is 8,492 lines here.
3. The clean-room screen's "no … personal path" was true of that change set and is not true of VER-001's own instrument disclosure, which names an absolute local path (adjudication 8).
4. The verifier's control-log model label `claude-opus-5[1m]` is the harness's context-window-qualified display name rather than a bare model id; the discovery record does not constrain the model field, and the header and the event agree byte for byte, which is what the mechanism checks.

No VER-001 statement is contradicted by this review's evidence in a way that bears on its verdict.

## Adjudications

### 1. The work order was amended by the implementing session after activation without a dated operator marker (VER-001 F1)

Reproduced: the activation checkpoint's tree equals `HEAD`'s, so the whole 179-line work-order diff is post-activation: the H1 and release classification move to `v0.5.0`, the dependency, objective, definitions, scope-discipline, deliverables, and assumption text retarget from the pre-WO-008 hand-assembled fallback to the landed compiler, one paragraph authorizes the bounded compiler extension, criterion 2 is strengthened, the non-goal "the composition compiler (WO-008)" becomes "a second composition compiler or general wildcard-claim precedence", and the two receipts are appended. Three facts make this a record-form question rather than a scope question. First, the committed map at `HEAD` (the PR #26 planning pass) lists as WO-023's activation preflight, verbatim, "assign version and close disposition; pin provisional instance paths and reviewer availability; retarget the compile step to the landed `@dotln/compiler` instead of the hand-assembled `compile*` fallback the draft describes for the pre-WO-008 case", so the retarget executes an instruction the planner recorded before activation. Second, the activation text already declared the envelope's `repo.read*`, `probe.run:scratch*`, `repo.write*`, `git.mutate*`, `remote.*`, and `settings.*` patterns, and compiler v1 refused every one of them; retargeting to the real compiler therefore forced either the thirteen-line extension or a second hand-assembled path, which the same activation text and the planning pass both forbid. Third, criteria 1 and 3 through 8, the evidence gate, and the write-back duty are byte-identical to the activation text (VER-001 diffed them; this review read the diff), criterion 2 is strictly stronger and was verified in the stronger form, and the replaced non-goal had been made moot by WO-008's landing. What the record lacks is the dated amendment line the repository's convention uses elsewhere (WO-018's criteria 9 and 10; ADR amendments), and only the operator can supply that authority; this review does not write it. **Non-blocking;** the PR body asks the operator to confirm at merge. The forward-corrected H1 repeats the activation-preflight pattern earlier reviews recorded, and it is again for the planning pass rather than for this review.

### 2. `check-surfaces` reports `@dotln/skeleton: src unchanged` although this order adds 2,133 lines of skeleton source (VER-001 F2)

Reproduced. `scripts/release.mjs` decides with `git diff --no-renames --name-only <previous tag> -- packages/<dir>/src`, which cannot see untracked files. `scripts/` is outside this order's scope, the bump to `0.5.0` was performed anyway, and the `--committed` run inside `worktree publish` sees the tracked file after the reviewed commit. **Deferred** to the next scripts order: an `--untracked-files=all` fallback for the component's `src`, or a printed "advisory" qualifier on the pre-commit form.

### 3. The compiler admits a bare `*` pattern in a claim-free graph with no diagnostic (VER-001 F3)

Reproduced on this order's own graph. Under the kernel's prefix matching `*` grants every effect, and the ledger's disclaimer that this is not "a claim that `*` by itself is a safe profile" lives only in prose. Every guarantee the documentation states holds exactly, no shipped definition uses the pattern, and the gate's purpose, refusing wildcard-and-claim mixing, is enforced. **Deferred** as a bounded candidate: refuse an empty prefix in the claim-free gate and add the fixture beside the existing four. Recorded in the release notes as a known limitation.

### 4. The kernel's prefix glob is string-anchored, not segment-anchored (VER-001 F4)

Reproduced: `repo.readwrite` grants under the compiled envelope. `effectMatches` is pre-existing kernel behavior; the order's scope discipline says "kernel untouched" and its non-goals exclude general wildcard precedence; no effect name in the system falls into the gap. **Recorded** for whichever order next introduces effect names.

### 5. The lens-scope validator accepts glob-shaped and whitespace-padded scopes (this review)

Measured: `docs/*`, `*`, `**`, `docs/int*`, `docs/intake ` with a trailing space, `docs/intake` followed by a tab, and ` docs/intake/` with a leading space are all accepted, while `docs/intake/` and its separator and case variants refuse. The consequence is bounded: the compiled constraint limits census and delegate reads to paths named by the Git index and the residue says so, `docs/intake` holds only three tracked `.gitkeep` placeholders, and criterion 4 asks for the six fields, the no-fix rule, and the ceiling, all of which are enforced; the scope screen is the ER-004 repair's defense in depth. But the schema's `$comment` says the host validator "rejects broad … scopes", and `*` is as broad as a scope gets. **Non-blocking; deferred** as a candidate: trim, refuse glob metacharacters and interior whitespace, or resolve scopes against `git ls-files`, with the seven strings above as fixtures.

### 6. The recorded episodes examined the pre-repair subject (VER-001 D1)

Accepted. The record labels itself "pre-repair implementation review"; its compiled program is preserved and hashes to the recorded value; the repaired subject compiles at the same inputs to the pinned repaired hash; the divergence is explained by the ER-005 schema repair; all seven findings were repaired before implementation-ready, and VER-001 checked each repair against its finding, which this review re-read for ER-004 (separator and case normalization plus negative cases), ER-005 (no non-standard keyword; shared safe-id pattern; parity assertions), ER-006 (compile inputs carried in the artifact and the record), and ER-007 (stance derived into the residue with mutation tests). Criterion 5 asks for one recorded episode from the compiled order; it has one, honestly labelled.

### 7. A small internal helper model appears in both run records (VER-001 D3)

Accepted. The harness reports 17 and 20 output tokens from `claude-haiku-4-5` against 81,786 pinned-actor output tokens in the review, with no cache traffic, which is the shape of a harness-internal utility call rather than review content. Both records and the ledger disclose it and claim no substitution. The run records' `source` label `command-line-readback-and-invocation` is run-record vocabulary for the episode's actor; the order's lifecycle roles are attested separately in the control log, where the ladder vocabulary applies.

### 8. VER-001 names an absolute local path in its instrument disclosure

`/Users/dylanwood/Projects/DotLn/.git/worktrees/…` appears where the verifier explains why it excluded `.git` from its scratch copy. VER-001 is immutable evidence and this review does not edit it. Seven committed files already carry the same prefix in the same kind of prose (WO-004 VER-002, WO-007 VER-001, VER-002, FINAL-001, and its work order, WO-015 FINAL-001, and WO-018 VER-001), the identity it discloses is the repository's public GitHub identity, and the run records under `docs/instance/` correctly alias local roots as `<main-checkout>`. **Not sustained as a defect; recorded** so that a tightening of the convention, if the operator wants one, starts from the precedent rather than from this report.

### 9. The harness exposes the selected effort to the shell (this review)

`CLAUDE_EFFORT=max` is present in the session environment on Claude Code 2.1.260. The discovery record classifies `effectiveEffortReadback` for this harness as `not found`, so the attestation above is `self-reported`, correctly. A bounded discovery pass could observe this variable, record it as a session readback for the listed versions, and make `harness-readback` admissible for Claude Code sessions. **Recorded**; the record is not edited by this review.

### 10. Projections that are correct as time-indexed and left alone

The capability table's dated note says the compiler evidence is "pending WO-023's independent verification", which was true when written and is superseded by VER-001; the audience index keeps the `v0.5.0` rung at `planned` until release, the rule ER-003 established and the executor applied to the two published rungs; the WO-029 map row lists WO-023 as unsatisfied until merge. None is edited.

### 11. Smaller consequences recorded without action

The artifacts test pins the scratch repository path of the recorded episode because that path is part of the historical hash preimage, which is the honest way to keep the historical hash reproducible; the `package-lock.json` gains the compiler workspace's `name` field, an npm normalization with no dependency effect; the residue's four literal paragraphs (reading order, clean-room floor, execution boundary, output contract) are renderer constants under the same byte comparison as the data-derived text, so editing them in the generator changes the residue and a hand edit to the tracked file fails, which is what "editing the prompt is refused" means in practice.

### Candidates considered and not sustained

- _The compiler extension is out of scope because the draft's non-goal excluded the compiler._ Not sustained: adjudication 1; the activation text's envelope patterns required it, the committed map directed the retarget, the change is thirteen lines with fixtures for every documented rule, and the non-goal was rewritten to the narrower bound that still holds.
- _The hash divergence between the reviewed and repaired subjects means criterion 5's episode did not run from the compiled order._ Not sustained: adjudication 6; it ran from the compiled order of its time, whose preimage is preserved and hashes, and the repaired subject was independently verified.
- _`requiredEvidence` belongs on the envelope, not only the WorkOrder._ Not sustained: the kernel applies the envelope field to every effect, so a non-empty value would refuse `repo.read` too; the work order says this, and the compiled WorkOrder carries the requirement where the host validator reads it.
- _Adjudication 5 blocks criterion 4._ Not sustained: the criterion's stated requirements are met; scope screening is beyond it, and the compiled Git-index constraint is the load-bearing guard.
- _The rung row for `v0.5.0` should read `verified` now that VER-001 passed._ Not sustained: adjudication 10; the executor's rule keeps the active rung at `planned` until release, and the two published rungs show what the row becomes.

## Corrections applied by this review

One correction, in the documentation lane `docs/final-reviews/README.md` grants, proven contained by diffing against checkpoint 5 (11 insertions and 6 deletions in one file) and followed by a re-run of the checks it could affect: the full `npm test` (which runs the pinned `prettier --check` and `check-publication.mjs` against the real repository), `git diff --check`, and `check-surfaces`, all clean.

1. **`docs/planning/work-order-map.md`:** the snapshot paragraph, the slot bullet, and the WO-023 row's evidence and disposition cells now record VER-001 pass, FINAL-001 pass, the reviewed state committed and awaiting operator merge, and the separately authorized release close that tags `v0.5.0`. No other row changed.

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet, no guard was lost, and no behavioral or evidence defect was found in the deliverable that any criterion covers. The items below are recorded and not scored.

1. **The post-activation amendment lacks a dated operator marker** (adjudication 1): the operator confirms at merge; the record form, not the scope, is what is missing.
2. **The pre-commit `check-surfaces` cannot see untracked component source** (adjudication 2): deferred to the next scripts order.
3. **A bare `*` compiles claim-free with no diagnostic** (adjudication 3): deferred with a fixture candidate; a known limitation in the release notes.
4. **`effectMatches` anchors on the string, not the segment** (adjudication 4): recorded for a future effect-name order.
5. **The lens-scope validator accepts glob-shaped and whitespace-padded scopes** (adjudication 5): deferred with seven fixture strings.
6. **The harness's `CLAUDE_EFFORT` variable is an unrecorded readback candidate** (adjudication 9): for a bounded discovery pass.
7. **The executor's model and effort live in the control log and the verifier's report, not in a committed executor artifact.** Recorded, as WO-013, WO-015, WO-024, WO-025, WO-017, and WO-018 recorded it; the run records under `docs/instance/` are the first executor-authored evidence artifacts for a code order, though they attest the review episodes' actor rather than the implementer's.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step returned to the operator.** `gh` in this sandbox fails before its root command exists (`failed to read configuration: open ~/.config/gh/config.yml: operation not permitted`), so `npm run worktree -- publish` refuses at its `gh` preflight after the committed surface check and body validation and before any push, fail closed, as for every reviewed order since WO-006. The reviewed commit is made here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation, and notes parse run against the exact commit; it refuses at the `gh` preflight and pushes nothing. The operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute command the publisher prints: change directory into the main checkout and run this worktree's `scripts/release.mjs close WO-023 --publish` with node, outside the sandbox and where `gh` is authenticated. Because `v0.5.0` is strictly above `v0.4.1`, the reviewed subject helpers finish this worktree through their own classifier and closed-state gate, fast-forward main, run `npm ci` and the evidence gate, assemble the five-section edition from these notes, create and push the annotated `v0.5.0` tag, and create the matching GitHub Release. If Release creation fails after the tag push, the rerun is `npm run release -- close WO-023 --publish` from updated main.
- **Operator confirmation at merge** of the activation-time retarget, the compiler-source extension, and the non-goal rewrite (adjudication 1), so the record shows the authority rather than implying it.
- **Attribution:** the reviewed commit carries a plain subject and no attribution trailer, per the operator's standing rule (`~/.claude/CLAUDE.md`) and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins, as the WO-017 and WO-018 reviews recorded.
- **Ledger duty:** discharged by the executor's four dated sections; this review adds no ledger entry because every open item above is either already in VER-001's checklist or is a candidate this report names for the next order.

## Proposed PR

- **Title:** `:sparkles: WO-023: compiled Entropy Reducer; the second founding identity compiles through the real compiler with claim-free prefix-glob lowering, a generated residue, and one attested Fable review with blinded refutation; v0.5.0`, the `:sparkles:` precedent being PR #25 for WO-008's compiler, chosen because the order adds new instance content and a new compiler capability.
- **Body:** `docs/final-reviews/WO-023/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commit (the committed surface check requires the closed control state, and it also makes the map's "FINAL-001 pass" cell true); the reviewed state is committed with a plain subject. The operator then runs, from this worktree outside the sandbox:

```bash
npm run worktree -- publish WO-023 --title ':sparkles: WO-023: compiled Entropy Reducer; the second founding identity compiles through the real compiler with claim-free prefix-glob lowering, a generated residue, and one attested Fable review with blinded refutation; v0.5.0' --body-file docs/final-reviews/WO-023/PR.md
```

The helper runs `check-surfaces --committed WO-023` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-023`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff in the absolute subject-helper form. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, the operator runs that printed command with the main checkout as the working directory; the expected outcome is the annotated `v0.5.0` tag and its GitHub Release.

## Method

Single reviewer, single session, no delegation: every read, probe, and command named in this report was performed by the reviewer directly. Read order: the execution guide's dispatch, closeout, discipline, and model-specific sections; the control projection and log; the work order in full including both receipts; VER-001 in full; the final-review and verification READMEs; the WO-018 final review with its PR body and notes for the package shape and publish precedent; then the diffs of every changed file and every new file in full (the loadout module, both test files, the residue, the refutation plan, both run records, the preserved compiled program, the WO-029 draft, the compiler change and its test, the manifests and lockfile, the playbook, both READMEs, the six promoted product documents, the ADR amendment, the ledger, the roadmap, the capability table, the map, the publication index and editions, the discovery record, and the five retimed drafts), with the publish, surface, notes, and body gates read from `worktree.mjs`, `release.mjs`, `release-notes.mjs`, and `github-body.mjs` rather than inferred, and the kernel's `authorize` and the compiler's diagnostics gate read at source. The ignored intake was not read. Subject integrity was established through git objects. Evidence was re-established first-hand: two full `npm test` runs, the static, surface, and publication checks, the residue digest, both semantic hashes, the refutation rebuild, the recorded output's validation and word count, thirteen authority probes plus the deny-before-allow envelope, the bare-`*` compile, the nine scope strings, the actor inventory by object comparison, the control-log `cmp`, and the clean-room and secret screen. Authority used: one documentation correction and the three package artifacts; no edit to any code, test, contract, schema, discovery field, verification report, or acceptance byte. Nothing was restored from a checkpoint, no destructive git command was used, and no remote, tag, or `gh` mutation was performed; the direct `gh` invocations were `gh --version` and `gh auth status`, both of which failed before their root command existed, and the publish helper's own `gh` preflight after the reviewed commit fails the same way, leaving the push and the PR to the operator.
