# WO-027 final review FINAL-001

**Subject:** branch `wo-027`, worktree `DotLn-wo027`, uncommitted working tree, 2026-09-03. `HEAD` and `origin/main` are both `39c9f2aa6fd285d64f46028deec875e51e94088c`, the PR #21 planning merge that filed this order; the latest published tag is `v0.3.4`, and the branch carries no local commits, so the entire subject is the dirty tree. At review open, before this review's own transition: eight tracked modifications (97 insertions / 33 deletions against `HEAD`, including the two `docs/control` files) plus three untracked paths, `docs/discovery/local-inference.md` (413 lines), `docs/discovery/local-inference.json` (462), and `docs/verifications/WO-027/VER-001.md` (305); eleven `git status --porcelain -uall` entries, all under `docs/`. The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree differs from checkpoint `refs/dotln/checkpoint/WO-027/4` (the verdict) only in `docs/control/current.md` and the strictly appended `docs/control/resume.jsonl`, and from `/2` (implementation-ready) and `/3` (verify allocation) only in those two files plus VER-001 itself (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, one bounded wording correction (§Corrections applied by this review), and the lifecycle's own events.

**Authority:** `docs/work-orders/WO-027-local-inference-probe.md`: a `Model: any` and `Effort: executor xhigh+; verifier xhigh+; reviewer any` header, a `v0.3.3` no-release classification, an eight-item checklist over the seven-value `environment.json` label vocabulary, seven acceptance criteria, a six-item evidence gate, a five-surface write-back duty, an explicit non-goal list, and four operator-review assumptions. Two hunks differ from the activated text (`refs/dotln/checkpoint/WO-027/1`): the H1 and release-classification paragraph, where the planner's "(version assigned at activation)" placeholder became `v0.3.3` with a disclosure of the activation-preflight miss, and the Authority paragraph, which now names the five write-back surfaces and the scratch root outside the repository and records the operator's 2026-09-03 confirmation. Every other byte of the order, including all eight checklist items, all seven criteria, the evidence gate, the write-back duty, and the non-goals, is identical to the activated text (§Adjudications, item 1). No ideation breakout receipt exists for this order, so the dispatch's "ideation receipt" clause is vacuous here; the only intake material the order cites is the planner's compaction-safety capture, which this review screened (§Provenance and the clean-room screen).

**Prior reports:** `VER-001` (pass; all seven criteria confirmed first-hand, sixteen claims re-observed, the intake overlap screen run, eight observations for this review and the planner, none a defect). Complete sequence: one report, no repair episode.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes), **effort `max`**, in Claude Code 2.1.259 with sandboxed Bash; macOS on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 read from the main checkout's dependency tree, since this worktree has none installed. WO-027 declares `reviewer any`, so any attested effort including `unknown` would satisfy it; the value is stated because it is true. The effort value is **self-reported**: the harness's own `/model` output in this session reported the selection of Fable 5.1 at `max` effort, `claude --version` returns `2.1.259`, and the harness's persisted transcript for this session carries `claude-fable-5-1` on every model field, `max` on every effort field, and `2.1.259` on every version field. Unlike VER-001's session, the harness-provided system context and the persisted transcript agree on the model name here. Claude Code 2.1.259 exposes no effective-effort readback, so `harness-readback` is unavailable and the mechanism would refuse it. This reviewer is the same model as the verifier and a different model from the executor (GPT-5.6 Sol under Codex CLI 0.152.1). Every command in this report ran first-hand from this worktree inside the sandbox; nothing is quoted from the executor's or verifier's transcripts. No subagents were used. No `lms` command was run and LM Studio was not launched: the order grants one launch per runner shape, the executor spent it, and nothing in review requires another. The machine header below carries the same five values this review passes to `final-review-result`, and it is the only line in this report that begins at column one with that label; every quoted actor sits in a table cell.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.259","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-027 passes.** The order asked whether this machine can run one pinned small model through an installed runner with evidence about egress, determinism, and cost, and it accepted "unknown" as a labeled answer. The deliverable answers honestly on the `blocked` branch the order provides: the only installed runner is inventoried, an existing 7B Q8_0 artifact is pinned by byte size and SHA-256 with its embedded template and decoding parameters, the sole permitted service launch is recorded with its exact command, output, exit code, readiness polls, and the operating system's crash report for the runner process, the no-egress record states what it proved and what it did not, the proposed capability row sits beside an unchanged pinned table, and the decision packet defers the calibration order under a named condition with reason, evidence, cadence, and reversal. All seven acceptance criteria are met on first-hand evidence, including a green `npm test` in a byte-identical mirror of the tree, and every VER-001 re-observation this review repeated agrees. One bounded wording correction was applied to the planning map. Nothing acceptance-relevant was changed by this review.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **`scripts/resume.mjs` allocated this report's path, appended `FinalReviewRequested`, and records this verdict through the actor-gated `final-review-result` path.** It is not a deliverable of this order: `git diff --stat HEAD origin/main -- scripts/resume.mjs package.json` is empty and `HEAD` is `origin/main`, so the script that ran is the one `v0.3.4` shipped. It is a fail-loud append-only log with a regenerated projection, and subject identity below was established with git objects rather than through it.
2. **This reviewer's own attestation is checked by that mechanism.** A successful append is evidence that the header above equals the flags, not independent evidence about the subject; the inventory in §The order's own actors was compared by object comparison in this session, not by reading the projection.
3. **The sandbox this review ran in is one of the boundaries the order names, and it is not what the order says it is.** Checklist item 6 offers "the sandboxed Claude Code Bash boundary" as a boundary that denies network by construction. In this session that boundary permitted outbound HTTPS to all nine cited vendor pages. VER-001 observation 3 recorded the same, and this review confirms it rather than inheriting it. The executor did not use this boundary, so the deliverable's egress record is unaffected; the order's parenthetical is wrong for this configuration and is carried as an open item for the next probe.
4. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser, profile, and surface check** (`scripts/release-notes.mjs`, `scripts/github-body.mjs`, `release check-surfaces`), none of which this order changes. Their editorial adequacy is the operator's to judge at merge.

## Dispatch and subject integrity

`docs/control/current.md` named WO-027 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, and drift `xhigh -> max`. Before invoking the transition I read the `resume` mapping in `package.json` (`node scripts/resume.mjs`, unchanged) and confirmed that `scripts/resume.mjs` and `package.json` are byte-identical to `HEAD` and `origin/main`. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-027/FINAL-001.md`, moved the phase to `final-review`, and minted checkpoint `733e3b1e4d8331734438e309a8f33d3caaa04e93` (`refs/dotln/checkpoint/WO-027/5`). `docs/control/resume.jsonl` is a strict append over `HEAD`: `git diff --numstat` reports four added lines and zero removed before this review's transition (`WorkOrderActivated`, `ImplementationReady`, `VerificationRequested`, `VerificationCompleted`), five after it, plus the `FinalReviewCompleted` the pass records. Every event carries `schemaVersion: 1`, and the activation event carries `effortDeclarationValidated: true`.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree as tree `88c1c6434db3a69ea936ca342e4422f589d3be7d`, and `git diff-tree -r --stat` against each checkpoint reported:

| Checkpoint | Differences from the working tree |
| --- | --- |
| `/2` implementation-ready (`b8fea0fd…`) | `docs/control/current.md`, `docs/control/resume.jsonl` (+3), `docs/verifications/WO-027/VER-001.md` (added, 305 lines) |
| `/3` verify allocation (`0c16a4ad…`) | `docs/control/current.md`, `docs/control/resume.jsonl` (+2), `docs/verifications/WO-027/VER-001.md` (added) |
| `/4` verdict (`e4891d53…`) | `docs/control/current.md`, `docs/control/resume.jsonl` (+1) |

Every deliverable blob, both discovery files and the six tracked non-control modifications, is therefore identical across the implementation-ready, verify-allocation, and verdict checkpoints and the working tree; the verifier changed nothing in the deliverable, and VER-001 has not changed since its verdict was recorded. All five checkpoints have `HEAD` as their sole parent.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. Beyond `node_modules`, the only ignored path in the worktree is `packages/kernel/dist/tsconfig.tsbuildinfo`, which the closeout gate treats as disposable; `git ls-files docs/intake` lists the three `.gitkeep` placeholders and nothing else; `.claude/` carries no change, tracked or untracked. The repository has nine local tags, `v0.2.0` through `v0.3.4`.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role | Event | Actor recorded in the control log | Report header | Declared minimum | Result |
| --- | --- | --- | --- | --- | --- |
| executor | `ImplementationReady` | `{"harness":"codex-cli","harnessVersion":"0.152.1","model":"gpt-5.6-sol","effort":"xhigh","source":"self-reported"}` | there is no executor report; the discovery doc's header line states the same four values in prose and the JSON's `executor` object states them as data | `xhigh+` | `xhigh` meets the floor exactly |
| verifier | `VerificationCompleted`, VER-001 pass | `{"harness":"claude-code","harnessVersion":"2.1.259","model":"claude-fable-5-1","effort":"max","source":"self-reported"}` | exactly one column-one header in VER-001, parsed and compared with the event actor as objects: equal | `xhigh+` | `max` exceeds the floor |
| reviewer | `FinalReviewCompleted`, this review's intended completion | `{"harness":"claude-code","harnessVersion":"2.1.259","model":"claude-fable-5-1","effort":"max","source":"self-reported"}` | the header above | `any` | satisfied |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: xhigh -> max`, the one distinct pair the completions produce; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.259; model claude-fable-5-1; effort max; source self-reported` with the same drift line and phase `closed`.

## Final evidence summary

### The unchanged `npm test`, twice, in a byte-identical mirror

This worktree has no installed dependency tree, and a plain `npm test` here would resolve a global Prettier, as VER-001 observation 7 records; installing dependencies into the worktree is the operator's call, not a reviewer's. The suite therefore ran the way the executor and verifier ran it, and the shortcut was checked: `rsync` of the worktree to the session scratch root excluding `.git`, `node_modules`, and `packages/*/dist`; `diff -rq` between worktree and mirror with the same exclusions reports no difference; the mirror's `node_modules` is a symlink to the main checkout's tree, whose `package-lock.json` and `package.json` are byte-identical to this worktree's and whose installed Prettier is 3.9.6 and TypeScript 5.4.5. `npm test` in the mirror: `All matched files use Prettier code style!`, `GitHub body profile tests passed`, then the seven shell suites (fixture temp-root, publication checker, backup-intake, resume, checkpoint, worktree, release), each reporting passed, then `tsc -b --force` and `# tests 78 / # pass 78 / # fail 0`, exit 0. The run was repeated in a fresh mirror after this review's artifacts and the map correction were in place, with the same result.

### Static and structural checks

- `git diff --check`: exit 0, no output, before and after this review's artifacts.
- `node scripts/check-publication.mjs`: `PASS index coverage: 155/155 product headings indexed`, `PASS dual-voice links`, both editions `CURRENT`; the roadmap's added paragraph introduced no heading.
- `node scripts/release.mjs check-surfaces` on the working tree: `PASS release-block: observed v0.3.4; expected v0.3.4 (work-order target v0.3.3; latest published v0.3.4)`, `PASS component-version @dotln/kernel: src unchanged; observed 0.1.0`, `PASS component-version @dotln/skeleton: src unchanged; observed 0.3.0`, and the profile rule PASS, reading `no current final-review bodies yet` before this review's bodies existed and naming `docs/final-reviews/WO-027/PR.md, docs/final-reviews/WO-027/RELEASE-NOTES.md` after. The README's release block correctly still names `v0.3.4`: a no-release order leaves it alone.
- Forbidden surfaces: `git diff --stat HEAD -- package.json package-lock.json packages/ scripts/ .claude/` is empty; `package.json` and `package-lock.json` are byte-identical to the main checkout's; no dependency was added.
- `docs/discovery/local-inference.json` parses: `schemaVersion 0.1.0`, `workOrder WO-027`, `observedAt 2026-09-03T03:11:07-04:00`; its `labels` array equals `environment.json`'s element for element; a walk over every `classification` field found zero values outside that vocabulary; the eight `checklist` entries carry `observed, blocked, observed, blocked, blocked, blocked, observed, observed`, matching the doc's table.
- `git hash-object docs/planning/capability-table.md` is `02d8937c4d8b5396e4595dbb72ebf00245ef8935`, the object the doc names; `git diff --quiet main -- docs/planning/capability-table.md` reports it identical.
- The pinned `prettier --check . --ignore-unknown`, run from this worktree after the map correction: `All matched files use Prettier code style!`.
- `RELEASE-NOTES.md` (28 lines, longest 906 characters) parses through `parseReleaseNotes` into the five required sections and passes the GitHub-body profile; `PR.md` (37 lines, longest 600 characters) passes the profile; both are listed by the bare `check-surfaces` preview.

## Independent re-observations

Everything below was re-observed first-hand under this sandbox, read-only, without invoking `lms` or reading any settings value.

| Claim in the discovery doc | This review's observation | Agrees |
| --- | --- | --- |
| LM Studio bundle `0.4.13+1` | `plutil -extract CFBundleShortVersionString raw` on the app's `Info.plist` prints `0.4.13+1` | yes |
| `~/.lmstudio/bin/lms` is arm64 Mach-O, CLI commit `0b2a176`, wakes the desktop app with `--run-as-service` | `file` reports Mach-O 64-bit executable arm64; the literals `0b2a176` and `--run-as-service` are present in the binary | yes |
| Ollama, llama.cpp, MLX LM `not found` | `command -v` empty for `ollama`, `llama`, `llama-cli`, `llama-server`, `llama-bench`, `mlx_lm`, `mlx_lm.server`; no `/Applications/Ollama.app`; `import llama_cpp` and `import mlx_lm` both `ModuleNotFoundError` | yes |
| packaged llama.cpp backend through `2.31.2` | the highest llama.cpp backend directory version under the runner's extensions root is `2.31.2` | yes |
| Apple M3 Max, 16 cores, 48 GB | `system_profiler SPHardwareDataType` prints `Chip: Apple M3 Max`, `Total Number of Cores: 16 (12 performance and 4 efficiency)`, `Memory: 48 GB` | yes |
| the model root holds three publishers | three publisher directories | yes |
| pinned artifact `7,695,875,136` bytes, SHA-256 `49d60b64…30fbb` | `stat` prints 7695875136; `shasum -a 256` prints `49d60b64563d5e4e78211eb3dfb1af75c3c89d1158f141bc0bf38ec5eac30fbb` | yes |
| three matching main-process crash reports, the new one at 03:00:32 | three `LM Studio-*.ips` in the user DiagnosticReports directory, timestamps `02:08:46`, `02:09:44`, `03:00:32` (-0400), each `app_version 0.4.13+1`, `parentProc lms`, `EXC_BREAKPOINT`, `SIGTRAP`, `Trace/BPT trap: 5` (whitelisted fields only) | yes |
| a stale PID lock was left; its process is not alive | the lock file exists and `kill -0` on its PID fails | yes |
| v1 profile SHA-256 `e95491dd…` | the four printed clauses with a trailing newline hash to `e95491dd…` | yes |
| v3 profile SHA-256 `b61210f8…` | the four printed clauses hash to `cf35cca8…`; the recorded digest covers bytes the doc does not print | not reproducible; VER-001 observation 5, carried |
| nine `documented officially` URLs | all nine return HTTP 200 through this session's proxy | yes |
| capability table object `02d8937c…` | `git hash-object` prints `02d8937c4d8b5396e4595dbb72ebf00245ef8935` | yes |

Two VER-001 observations were also re-observed rather than inherited: eighteen `LM Studio Helper` and `LM Studio Helper (GPU)` reports exist with timestamps `03:00:28` through `03:00:32`, all `parentProc LM Studio` and `SIGABRT` (observation 1); and, models excluded, thirteen files under the runner's `.internal` root plus `bin/lms` and `settings.json` were modified between 02:55 and 03:05 on 2026-09-03 (observation 2; names only, no contents read).

## Provenance and the clean-room screen

The order's nomination provenance cites one intake artifact, the planner's compaction-safety capture `docs/intake/notes/WO-027-WO-028-primitives-and-data-planning-2026-09-03.md` (3,397 bytes), which exists only in the main checkout's single-copy intake; this worktree's `docs/intake/` holds the three `.gitkeep` placeholders and nothing else. No `ideation:` dispatch occurred during this order and no receipt exists; the ledger's WO-027 entry names its sources as the public work-order authority, public vendor documentation, and sanitized machine observations.

- **Overlap screen: synthesis, not lifting.** Word shingles between the capture and every added line (the diff's `+` lines plus both discovery files and VER-001, 1,277 lines): zero shared 10-word and 8-word shingles; two shared 6-word shingles, both technical literals that the order itself already prints, `apple m3 max 48 gb unified` and `printed waking up lm studio service...`.
- **Private identifiers: none.** Over the same 1,277 lines: no `/Users/` path, no username, no e-mail address, no non-loopback IP other than the public `1.1.1.1` test destination, no UUID shape, no hostname; the only `.local` hits are `runtime.local-inference`. The two matches the scan returned are VER-001's own sentences listing what it screened for. Hardware is disclosed no finer than chip family, core count, and memory; the OS point release is withheld.
- **Forbidden terms: none.** No commercial tracker or ALM product name; the words "employer", "gateway", and "managed host" occur only inside the repository's own screening language (the ledger's source line and VER-001's screen); every `v1` is a sandbox-profile revision, the `/api/v1/models` path, or `git status --porcelain=v1`, not the predecessor system (VER-001 observation 6, carried as a naming nuance).
- **Write-backs landed exactly where the duty names them, and nowhere else.** The `docs/README.md` map line for `docs/discovery/`; the `environment.md` §Corrections pointer paragraph before the "Gaps identified" list, which does not rewrite the historical audit; a newest-first ledger section above the 2026-09-03 planning entry with three `adopted` items and one `preserved` item, matching the duty's adopted-and-preserved shape; the dated roadmap discharge paragraph inside §Counterfactual profiling work orders, directly after the "deliberately unfiled" paragraph and before the hybrid local-first paragraph it preserves; and the work-order map's snapshot line, slot and floating bullets, and WO-027 row. The six tracked non-control modifications are exactly those five surfaces plus the forward-corrected work order.

## Verification-sequence soundness

VER-001 meets every duty the order and the guide place on it: it establishes subject identity through checkpoints, states its authority use and non-use, confirms the seven criteria with its own commands, re-observes sixteen claims including a bounded GGUF header read that reproduced the parameter count, vocabulary, token ids, and byte-equal chat template, runs the intake overlap screen, discloses the environment note behind criterion 7, and names its deferrals honestly. Its counts were re-derived here where cheap (the artifact digest, the three crash reports and eighteen helper reports, the residue set, the v1 and v3 profile digests, the nine URLs, the capability-table object, the 78 node tests) and all agree. Its one measurement this review did not reproduce exactly is the insertion and deletion count against `HEAD` (99/36 there, 97/33 here), which differs only by the state of the generated control projection at each measurement and does not describe the deliverable. No VER-001 statement is contradicted by this review's evidence.

## Adjudications

### 1. The executor amended the work order's authority text

Two hunks against the activated text. The H1 and classification rewrite completed the activation step the helper skipped, as WO-024, WO-025, and WO-019 each did before it: `v0.3.4` is the latest published tag locally and on origin, `v0.3.3` is strictly below it, and the order's expected class was the honest no-release path, so the value is substantively correct; the append-only activation event was neither replayed nor edited; the correction is disclosed in place, in the ledger's source line, and in the map row. The Authority rewrite resolves an inconsistency inside the planner's own text, where the activated clause permitted writes only under `docs/discovery/` and a scratch root while the write-back duty required edits to five other surfaces, by naming those surfaces explicitly and recording the operator's confirmation; it also moves the scratch root outside the repository, which criterion 4 accommodates by asking for the root by path class, and the packet gives `OS temporary root / dotln-wo027-probe.<random>` with five content classes. The amendment widens nothing beyond what the duty already demanded and lowers no criterion; the in-session operator authority is recorded there and in the map, which is as far as this review can verify it. Ratified.

### 2. The evidence gate on the `blocked` branch

The gate lists smoke transcripts, a determinism triple, and an egress record with a socket snapshot. None of the first two exists and the snapshot is empty, because the sole permitted launch crashed before readiness. Acceptance criterion 2 explicitly provides the `blocked` branch, "recorded `blocked` with the reproduced failure and the runner's own diagnostic", and the objective states that unknown is an acceptable result when every claim carries a label. The deliverable records `n=0` and `0 of 3` as data rather than inferring values from vendor documentation, records the launch transcript and exit code in place of a smoke, and records `observedSockets: []` with the reason. The "runner's own diagnostic" is the operating system's crash report for the runner's process, which VER-001 established is the only diagnostic that exists because no runner-owned log was written during the launch window; this review reads it the same way. The gate is satisfied in the shape the order allows.

### 3. VER-001 observation 1: helper crash evidence and the `ambiguous` causality label

Reproduced here: eighteen helper aborts in the five seconds before the main-process report, all `SIGABRT` with `parentProc LM Studio`. The deliverable's `ambiguous` label already refuses to attribute the crash to the runner alone, and its stated next experiment (one service lifetime under a boundary with independently attributable loopback and egress behavior) is the right first step; the observation sharpens the label and does not contradict it. The doc's phrase "the two earlier reports have different top frames" is imprecise, since VER-001 found they share one top frame that differs from the new report's; a prose nuance in a `blocked` item that changes no label. Accepted as an observation; the PR body carries it so the next probe launches under the outer boundary alone or the corrected profile before concluding that the runner crashes on service wake.

### 4. Observation 2: runner residue wider than the PID lock

Reproduced here by modification time. The order's "no config mutation" clause binds the executor's actions, and the executor read no settings value and wrote none; what the runner does to its own application data on wake is the runner's behavior, disclosed in VER-001 and now in the notes. Accepted; carried as a next-probe method note (hash the root, models excluded, before and after).

### 5. Observation 3: the Claude Code Bash boundary

Confirmed first-hand in this session: nine outbound HTTPS requests succeeded. The order's parenthetical is wrong for this configuration; the executor used a different boundary, so the deliverable's record is unaffected, and its limit-of-claim paragraph already refuses to attribute the external failures to any layer. Accepted; carried in the PR body and the notes as a caution for whoever runs the next experiment.

### 6. Observation 4: the conservative sub-label on unified memory

The item's single classification, `blocked`, is what criterion 1 checks and it is correct; the field-level `documented locally` on the 48 GB value under-claims what `system_profiler` observed. Correcting a label inside the verified deliverable is an acceptance-surface edit outside this review's wording lane, and an under-claim harms no reader. Accepted, not corrected.

### 7. Observation 5: the v3 digest

Reproduced: the four printed v3 clauses hash to `cf35cca8…`, not the recorded `b61210f8…`, while the v1 clauses hash exactly to the recorded value. The doc calls both sets "public-safe clauses", so the recorded digest is a claim about a file whose full bytes were projected out; nothing in the acceptance criteria turns on it. Accepted, carried: a future doc should print the whole profile or say what was projected.

### 8. Observations 6, 7, and 8

The `v1`/`v3` naming collides with `CLAUDE.md`'s reserved sense of `v1` only lexically; every usage is a profile revision, an API path, or a porcelain format, and the ledger line about "the corrected profile" is unambiguous in context; carried as a naming nuance for future docs, not corrected in the executor's ledger entry. The worktree's missing dependency tree is an operator call and was handled here by the mirror. The verifier's model-name discrepancy between system context and transcript did not recur in this session; `self-reported` remains the only honest source label until a readback exists.

## Corrections applied by this review

One correction, in the wording lane `docs/final-reviews/README.md` grants, proven contained by diffing against checkpoint 5 and followed by a re-run of the checks it could affect (the pinned `prettier --check`, `git diff --check`, and the second full mirror `npm test`, all clean).

1. **`docs/planning/work-order-map.md`: the snapshot line, the slot bullet, the sequence bullet, the floating bullet, and the WO-027 catalog row.** They read "pending independent verification", "active … pending independent verification", "is now active", and "occupies the active slot pending independent verification", true when the executor wrote them and false beside a passing VER-001 and this report. Following WO-013, WO-024, WO-025, and WO-019 FINAL-001 §Corrections item 1, they now record VER-001 pass, FINAL-001 pass, the reviewed state committed and awaiting operator merge, and the separately authorized no-release close that records the honest result with no tag. The pinned formatter re-padded the catalog table's columns to fit the longer cells, so the raw diff against checkpoint 5 is 46 insertions and 45 deletions while the whitespace-insensitive diff is 11 insertions and 10 deletions confined to those five spots; no other row's content changed.

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet and no behavioral or evidence defect was found in the deliverable. The items below are recorded and not scored.

1. **The order's boundary parenthetical is wrong for the Claude Code sandbox as configured** (VER-001 observation 3, confirmed here). Any future order that names a harness sandbox as a network-denying boundary should require the executor to prove the denial first-hand before a socket snapshot means anything, as this executor did with its own boundary.
2. **The next probe's cheapest trigger is not in the decision packet's cadence.** The packet reevaluates on a runner install or an operator-chosen LM Studio upgrade or fix, otherwise quarterly; the capability row's next-experiment cell names the cheaper first step, one relaunch under the outer boundary alone or under the corrected profile with a fresh one-launch authority, and VER-001 observation 1 gives the reason (a helper abort loop is consistent with a refused nested sandbox). The planner should read the row's cell together with the packet before deciding the disposition is exhausted. This is a planning note, not a defect: the packet's disposition, reason, evidence, cadence, and reversal are all present and internally consistent.
3. **The activation-preflight gap is now four orders long** (WO-024, WO-025, WO-019, WO-027): each activated with the planner's placeholder in the H1 and each executor forward-corrected it. For the operator's planning pass, not for this review.
4. **Naming and reproducibility nits in the deliverable** (VER-001 observations 4, 5, and 6): the under-claiming sub-label, the v3 digest that covers unprinted bytes, and the `v1`/`v3` profile names. None changes a classification or a criterion; a future discovery doc should print whole profiles and pick names that cannot collide with the reserved `v1`.
5. **Executor model and effort are in a committed artifact for this order**, the discovery doc's header line and the JSON's `executor` object, closing for this order the structural gap WO-013, WO-015, WO-024, and WO-025 recorded; the verifier's and this reviewer's are in their report headers and control events.
6. **The worktree has no dependency tree.** `npm test` cannot run natively here; the mirror is the proven path. Whether to install into work-order worktrees is the operator's call.

### Candidates considered and not sustained

- *The evidence gate is unmet because no smoke transcript or determinism triple exists.* Not sustained: §Adjudications, item 2; the order provides the `blocked` branch and the deliverable takes it honestly with `n=0`.
- *Item 2 should be `observed` because the OS reported the hardware.* Not sustained: the checklist asks for the values "as the runner reports them" and a thermal envelope; the runner never reported and `pmset -g therm` returned nothing; `blocked` with the OS values recorded as observed sub-fields is the honest single label.
- *The `v0.3.3` classification reuses a published tag name.* Not sustained: the release tooling's expected version is release truth, a target strictly below the latest tag is the explicit no-release outcome, `check-surfaces` passes with the README block at `v0.3.4`, and the close command's lower-target path prints the honest result and creates no tag; WO-101's `v0.2.0` evidence-only close is the precedent.

## Acceptance criteria

| # | Criterion | Verdict | First-hand basis |
| --- | --- | --- | --- |
| 1 | Every checklist item carries exactly one classification and a command or blocking reason; no unlabeled ambient claim | met | eight rows, one label each, mirrored in the JSON with zero out-of-vocabulary values; every vendor claim carries `documented officially` with a URL and all nine URLs return 200; the command log names every claim-producing probe |
| 2 | One runner shape observed end to end, or `blocked` with the reproduced failure and the runner's own diagnostic; crash-on-wake reproduced or corrected | met on the `blocked` branch | exact command, three-line output, exit 1, 120 polls; the 03:00:32 crash report re-read first-hand with `parentProc lms`; the two earlier reports match; §Adjudications item 2 |
| 3 | The no-egress record names boundary, method, sockets, and limit; no privacy or offline claim beyond the evidence | met | §6 read in full: outer boundary by key names, nested profile with clauses and digest (v1 reproduced), curl method, `observedSockets: []` with reason, an explicit limit paragraph; "offline" and "telemetry" occur only in the citation, the key-name scan, and the limit paragraph |
| 4 | Status pair differs only under `docs/discovery/`; nothing under `packages/`, `scripts/`, `.claude/`, `package.json`; no dependency; scratch root and downloads by path class | met | the recorded pair is identical; the forbidden-surface diff is empty; lockfile and `package.json` byte-identical to the main checkout; scratch `OS temporary root / dotln-wo027-probe.<random>` with five content classes; downloads none |
| 5 | Exactly one disposition with reason, evidence, cadence, and reversal | met | "defer under a named condition" in the doc and `decisionPacket.disposition` in the JSON, with all four fields, the predeclared evaluators, and an honest `blocked` 100-run cost |
| 6 | Proposed row present; pinned table byte-identical to `main` | met | §7 row read; `git hash-object` `02d8937c…`; `git diff --quiet main` on the table |
| 7 | `npm test` green with unchanged code; `git diff --check` clean; JSON parses with equal labels | met | mirror `npm test` exit 0 with 78/78, twice; `git diff --check` clean; labels element-for-element equal |

Non-goals respected: no calibration run, no transport adapter or model-slot lowering, no `ModelInputPlan` code, no runner install or upgrade, no benchmark loop, no vendor privacy claim, no capability-table edit, no roadmap rung. Operator-review assumptions held: LM Studio was the only runner shape; no artifact was downloaded; the operator was present for the launch (`operatorPresent: true`); the close takes the no-release path.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step returned to the operator.** `gh` in this sandbox fails before its root command exists (`failed to read configuration: open ~/.config/gh/config.yml: operation not permitted`), so `npm run worktree -- publish` refuses at its `gh` preflight after the committed surface check and body validation and before any push, fail closed, as for WO-006, WO-007, WO-013, WO-024, WO-025, and WO-019. The reviewed commit is made here; the operator runs the exact command from this worktree outside the sandbox.
- **No-release close.** After merge, and only under a fresh `resume: release close`, fast-forward the main checkout to the merge commit first (`git pull --ff-only origin main`), then run `npm run release -- close WO-027 --publish` from it, outside the sandbox and where `gh` is authenticated. The command performs the guarded worktree finish and synchronization, runs `check-surfaces` against the merged `HEAD`, and, because `v0.3.3` is below the latest `v0.3.4`, prints the honest result (`WO-027 closes v0.3.3, below latest release v0.3.4; no release tag is due`), creating no tag and no Release. These notes then ride the next tag that contains this work.
- **Attribution:** the reviewed commit carries a plain subject and no attribution trailer, per the operator's standing rule and the repository's hand-authored history.
- **Ledger duty:** discharged by the executor's entry; no entry is owed for this review's wording correction.
- **Next-probe method** (from VER-001 observations 1, 2, 3, and 5, confirmed here): launch under the outer boundary alone or the corrected profile before attributing the crash to `--run-as-service`; hash the runner's application-data root, models excluded, before and after; verify the chosen boundary's egress behavior first-hand; print whole profiles.

## Proposed PR

- **Title:** `:memo: WO-027: local inference probed as capability truth; the sole runner launch blocked on service wake, calibration deferred under a named condition`, a documentation-only deliverable (discovery evidence and write-backs; nothing under `packages/` or `scripts/`), the `:memo:` precedent being PR #21.
- **Body:** `docs/final-reviews/WO-027/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commit (it also makes the map's "FINAL-001 pass" cell true); the reviewed state is committed with a plain subject. The operator then runs, from this worktree outside the sandbox:

```bash
npm run worktree -- publish WO-027 --title ':memo: WO-027: local inference probed as capability truth; the sole runner launch blocked on service wake, calibration deferred under a named condition' --body-file docs/final-reviews/WO-027/PR.md
```

The helper runs `check-surfaces --committed WO-027` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-027`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, fast-forward the main checkout and run `npm run release -- close WO-027 --publish` from it; the expected outcome is the honest no-release result with no tag.

## Method

Single reviewer, single session, no subagents. Read order: the execution guide's dispatch, closeout, discipline, and model-specific sections, the control projection and log, the work order and its diff from the activated checkpoint, VER-001 in full, the final-review and verification READMEs, the WO-025 and WO-019 final reviews with their PR bodies and notes for the package shape and publish precedent, then every changed file's diff and both discovery files in full, with the publish and notes gates read from `worktree.mjs`, `release.mjs`, and `release-notes.mjs` rather than inferred. Subject integrity was established through git objects. Evidence was re-established first-hand: two full `npm test` runs in byte-identical mirrors, the static checks, the surface and publication checks, the JSON label walk, the artifact re-hash, the crash-report field projection, the residue window, the profile digests, the vendor URLs, and the two clean-room screens. Authority not used: no `lms` invocation, no LM Studio launch, no download, no install, no settings value read, no edit to any deliverable byte. The mirrors lived in the session scratch root and were removed. Nothing was restored from a checkpoint, no destructive git command was used, and no remote, tag, or `gh` mutation was attempted; the one `gh` invocation was `gh --version`, which failed before its root command existed.
