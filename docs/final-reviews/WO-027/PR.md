## What this merges

WO-027, the `v0.3.3` discovery work order on the honest no-release path below the published `v0.3.4`: a bounded probe of whether this machine can run one pinned small model through an installed runner, with every claim labeled from the `environment.json` vocabulary, and a dated decision that defers the local-inference calibration order under a named condition. Nothing under `packages/`, `scripts/`, or `package.json` changes; no dependency is added; the pinned capability table is byte-identical to `main`.

- **The question:** the roadmap's counterfactual-profiling section left a calibration order deliberately unfiled until the smallest runner and no-egress probe had run. This order is that probe: eight checklist items, one classification each, unknown accepted as a labeled answer.
- **Runner inventory** (`observed`): LM Studio `0.4.13+1` is installed with its `lms` CLI (arm64, commit `0b2a176`) and a documented headless server mode; Ollama, standalone llama.cpp, and MLX LM are `not found`. Static inspection of the CLI shows that on a connection miss every `lms` invocation wakes the desktop service, so every invocation is a process launch.
- **Pinned artifact** (`observed`): an existing 7B Q8_0 GGUF from a public publisher, pinned by byte size and SHA-256, with its embedded ChatML template, BOS and EOS tokens, 32,768-token context, and the intended fixed request (temperature 0, top-p 1, seed 424242, 80 output tokens, stop `<|im_end|>`) recorded as `untested`. No download occurred.
- **The sole launch** (`blocked`): the one permitted `lms server start` on loopback, under a nested `sandbox-exec` profile inside the executor's network-disabled sandbox, printed `Waking up LM Studio service...`, timed out after 120 readiness polls, and exited 1; the operating system wrote a crash report for the runner process (`parentProc lms`, `EXC_BREAKPOINT`, `SIGTRAP`) six seconds after launch, matching the planner's two earlier reports. Load time, first-token latency, throughput, cost per generation, and the determinism triple are all `blocked` with `n=0`, not inferred.
- **No-egress evidence** (`blocked`): the record names the outer boundary by key names, the nested profile by its clauses and digest, the curl method, the empty during-generation socket set with its reason, and an explicit limit of claim. External test connections failed under both profiles with no boundary diagnostic, so the denying layer is `ambiguous`; the launch profile also refused loopback, and a corrected profile later reached a verified loopback listener without the runner. No privacy or offline property is claimed for the model or the runner.
- **Capability row proposal** (`observed`): `runtime.local-inference` at maturity 0 with target 1, efficiency E0, baseline "blocked launch, `n=0`", next experiment one service lifetime under a boundary with independently attributable loopback and egress behavior, then the fixed three-request smoke with a during-generation socket snapshot. The pinned table is unchanged.
- **Decision packet** (`observed`): **defer under a named condition**, with reason, evidence, an event-driven-then-quarterly cadence, a reversal condition, the six evaluators a future order must predeclare, and an honest `blocked` for the 100-run cost. The hybrid local-first cell stays preserved, not scheduled.
- **Docs and write-backs:** the roadmap's dated discharge note, the environment audit's pointer, the `docs/` map line, a ledger entry with three `adopted` and one `preserved` item, and the work-order map row. The work order's heading carries `v0.3.3` and its authority paragraph names the write-back surfaces; both were forward-corrected by the executor under explicit operator authority after activation left the planner's placeholder in place, and both corrections are disclosed in place.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in a byte-identical mirror of this worktree linked to the lockfile-resolved dependency tree, on the verified tree and again on the reviewed tree: Prettier clean, the body-profile unit suite, all seven shell suites, `tsc`, 78 of 78 node tests, exit 0. `git diff --check` clean; the publication check `CURRENT` for both editions; the forbidden-surface diff (`package.json`, lockfile, `packages/`, `scripts/`, `.claude/`) empty; `git hash-object docs/planning/capability-table.md` equal to the object the packet names and identical to `main`.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.3.4; expected v0.3.4 (work-order target v0.3.3; latest published v0.3.4)`, both component rules PASS with sources unchanged, and the profile PASS over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity: every deliverable blob is identical across the implementation-ready, verify-allocation, and verdict checkpoints and the working tree; the control log is a strict append over `HEAD`; the verifier's report header equals its control-log actor by object comparison; the executor's `xhigh` and the verifier's `max` meet the declared `xhigh+` floors.
- Discovery claims re-observed read-only, without invoking `lms`: the bundle version, the seven absent runner executables and two absent Python modules, the CLI's architecture, commit, and wake flag, the coarse hardware, the artifact's byte size and SHA-256, the three main-process crash reports' whitelisted fields, the eighteen helper aborts in the same five seconds, the runner residue window, the v1 profile digest (reproduced) and the v3 digest (not reproducible from the printed clauses), the nine vendor URLs (all 200), and the JSON's label vocabulary (zero out-of-vocabulary classifications).
- Clean-room screen against the planner's intake capture: zero shared 10- and 8-word shingles, two shared 6-word technical literals; no private identifier, no forbidden term, every `v1` a profile revision, an API path, or a porcelain format.

## Verification trail

- `docs/verifications/WO-027/VER-001.md`: pass; all seven criteria confirmed first-hand, sixteen claims re-observed, eight observations for the planner and reviewer, none a defect.
- `docs/final-reviews/WO-027/FINAL-001.md`: pass; one bounded wording correction (the work-order map); the executor's authority amendments and the verifier's observations adjudicated with reasons; method and instruments disclosed.

## Known open items

- **The crash is not yet attributable to the runner alone.** Eighteen helper-process aborts preceded the main-process report by five seconds, and the launch ran under a nested seatbelt profile that also refused loopback; a helper abort loop is consistent with a refused nested sandbox. The next probe should launch once under the outer boundary alone or the corrected profile, with a fresh one-launch authority, before concluding LM Studio crashes on service wake. The decision packet's cadence names a runner install or upgrade; the capability row's next-experiment cell names this cheaper step.
- **The Claude Code Bash sandbox is not a deny-by-construction network boundary.** The order offers it as one; in the verifier's and reviewer's sessions it permitted outbound HTTPS. Whoever runs the next experiment must prove their boundary's egress behavior first-hand, as this executor did, before a socket snapshot means anything.
- **Runner residue.** Beyond the disclosed PID lock, the runner rewrote its CLI binary, `settings.json`, and thirteen internal state files on wake. Contents were not compared; a future run should hash the application-data root, models excluded, before and after.
- **Deliverable nits, carried:** the v3 profile digest covers bytes the packet does not print; the unified-memory sub-label under-claims what `system_profiler` observed; the `v1`/`v3` profile names collide lexically with the reserved `v1`. None changes a classification.
- **Activation preflight.** For the fourth consecutive order, activation left the planner's placeholder in the H1 and the executor forward-corrected it.
- **No-release close.** After merge, the separately authorized `resume: release close` runs `npm run release -- close WO-027 --publish` from a fast-forwarded main checkout; `v0.3.3` is below the latest `v0.3.4`, so the command records the honest no-release result and creates no tag or Release. These notes ride the next tag that contains this work.
- **Worktree dependency tree.** This worktree has none installed; `npm test` was run in a byte-identical mirror. Whether to install into work-order worktrees is the operator's call.
