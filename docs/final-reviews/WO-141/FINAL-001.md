# WO-141 FINAL-001 — observed facts, the nonblocking hedge advisory and the journal-derived correction counter

**Verdict: pass.** The subject meets all six acceptance criteria. No blocking finding requires repair. The reviewer ran the single product gate on the staged tree with every intended source file tracked, and reproduced criterion 1, criterion 2's true-negative half, VER-001's F1 and its O2 live in this session's own boundaries.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.274","model":"claude-opus-5[1m]","effort":"xhigh","source":"self-reported"}

## Subject and integration

Operator dispatch: `resume: final review`, recorded at `2026-09-17T21:37:08.042Z`, allocating FINAL-001. The subject is acceptance criteria 1–6 as filed, the execution record's two scope additions (the permission-trial ideation receipt and the current-evidence refresh), and the executor's decisions D001–D004 with their same-day correction. One root coding agent performed this review; no subagent was used and no implementation was changed.

No integration was required and none was performed. The merge base, `HEAD` and `main` are all `096323c916f6ac3af3e3b267ec3a27b117c97974`; `git log main..HEAD` is empty, so no branch commit preceded this review, and `git log HEAD..main` is empty. Limit: `git fetch --all` failed in this session with `fatal: failed to store: -67674`, a credential-store refusal, so "no newer sibling" is established against the local `main` ref and not against a fresh `origin/main` read. The publish step contacts origin and will surface a divergence if one exists; release close judges the eventual merged revision.

Recovery is preserved by `refs/dotln/checkpoint/WO-141/5` (`8c19cf046aaba0ab254a8d4806240e86bd9cec19`). No reset, restore, clean or stash drop was run, and ignored intake was not touched. The working tree the gate measured is the tree being committed.

The version staging is coherent at the current bytes: `README.md` prepares `v0.29.1`, the order heading carries `(v0.29.1)`, the dated roadmap record names application `v0.29.1` and skeleton `0.25.1`, and `packages/skeleton/package.json`, `version.ts` and the lockfile agree at `0.25.1`. `git diff main -- package.json` is empty and the lockfile carries only the internal skeleton bump, so no third-party dependency changed. Compiler, kernel and console versions are unchanged, which matches D003 after its recorded correction withdrew the compiler bump.

The adjacent repair recorded in D002 is sound and bounded. Its three repaired assertions in `scripts/test-process-debt.mjs` concern how presence hooks are wired into generated settings and what a presence hook returns; both are produced by `packages/compiler/src/harness.ts` and the generated `presence-*.mjs` bodies, and this diff changes neither — the presence hook files move only by their runtime snapshot pin. The fixtures were therefore already stale on `main` and the repair does not accommodate a WO-141 regression. The other assertion changes in that file and in `scripts/test-harness.mjs` track this order's own Stop and prompt-context behavior and are the expected consequence of criterion 1.

## Acceptance and executed evidence

| Criterion | Judgment and evidence |
| --- | --- |
| 1 — the three surfaces print the facts block with cause codes | Pass, with F1 and L2 named. Reproduced live in this session: the prompt-submit block at `2026-09-17T21:57:51.057Z` carried the clock, `Gate npm test: 1129806 ms; recorded 2026-09-17T21:57:50.831Z` scoped to this dispatch alone, and populated counters with `source claude-transcript-message-usage; scope dispatch; cutoff 2026-09-17T21:49:37.578Z`, while absent values printed `unknown` beside a cause code. `scripts/test-observed-facts.mjs` covers the fixture-journal form and the foreign-order exclusion; the Codex briefing surface remains fixture-only. |
| 2 — known hedge journals the observed value, unknown journals `unmeasured`, stated/`unknown`/fenced/quoted journal nothing, Stop never blocks or re-enters | Pass. VER-001's independent probe established the positive case and eight negatives, single delivery and silent re-entry. This review adds a second live true negative: the Stop hook fired unaided on this session's real final message at `2026-09-17T21:49:40.019Z` and journaled no hedge row; this session's journal holds 612 rows, 0 `HedgedQuantityObserved` and 0 `OperatorCorrectionReceived`. O4's comma-set-off escape is inside the order as written and under-counts rather than misattributing. |
| 3 — `operatorCorrections` derives from journal events, not decision records, with phase/order/unit splits | Pass. The decision-record derivation is gone from `scripts/lib/meta.mjs`; `correctionCounts` reads the journal, deduplicates legacy cumulative rows to one event each, and only the shifting-the-burden trap row carries the splits. `scripts/test-process-debt.mjs` pins both the two-corrections-plus-one-hedge count of 3 under the event's own order and phase and the refusal to retain a non-journal prior snapshot; `scripts/test-harness.mjs` pins one count per prompt across both configured hooks in either execution order. |
| 4 — Codex briefing and lifecycle commands print (a), run (b), count those scans, and the decisions record states the difference | Pass, fixture-only. `observedFactsReport` gates on `CODEX_THREAD_ID`, `codexObservationScope` selects the thread's own transcript and dispatch window, and `observationBoundary({codex: true})` journals dispatches, typed corrections and the handoff output. D001 records that Codex has no Stop hook and that a final answer written after the last lifecycle command waits for a later boundary. No Codex session was available to this reviewer either; L2 stands. |
| 5 — 07 §Discipline, FUP-0130 disposal, unchanged security runbook, regenerated bundle and manifest | Pass. 07-execution-guide.md carries the mechanism beside the no-guessing line, names all four units with their compiled ids, and states that it holds no turn, refuses no action and causes no Stop re-entry. FUP-0130's third disposition is `allocated`. `git status --porcelain docs/AI-HARNESS-SECURITY.md` is empty and no new refusal token appears in the diff. `node scripts/harness.mjs check` exits 0 at 28 generated surfaces with both new modules in the manifest and in `harnessInstallation`; `npm run publication:check` passes with 30 and 45 linked source sections current. |
| 6 — `npm test` green, `git diff --check` clean, no new dependency | Pass, with L1 named. `npm test -- --review` passed 30 suites, 0 failed, 1129.81 s, 74 fresh tasks, exit 0, outside this session's OS sandbox. `git diff --check` and `git diff --cached --check` both exit 0. No dependency changed. |

The successful reviewer gate was recorded at `2026-09-17T21:57:50.831Z` for tree `906cdfac61d26d58334010e5c74648ef892bcc91` at code identity `4b8375d24aaacd16eedc4894300edc909e5dfd51d7a26f201b1fe5c5b062bb72`. All intended source files — `packages/skeleton/src/observed-facts.ts`, `packages/skeleton/src/correction-observation.mjs` and `scripts/test-observed-facts.mjs` — were staged before that single gate, which is why this identity differs from the executor's and verifier's `e6671230…` and why the runner selected 30 suites rather than 19: with the new sources tracked, their declared machinery checks become affected. Report, control, release-prose and generated-index updates after this row do not change that identity.

Direct checks also passed: `node scripts/harness.mjs check`, `npm run publication:check`, and `npm run work-orders -- index`, which regenerated the index with no byte change.

L1 was reproduced in this session before the gate rather than assumed from VER-001. `node --test packages/skeleton/dist/test/discovery.test.js` inside this session's Seatbelt profile failed 8 of 10 cases with `discovery: check sandbox or launch refused` in 0.45 s. That 0.45 s probe replaced one full failing gate whose duration is unmeasured because it was never run; the comparable measured rows in this order's record are the verifier's sandboxed 322.78 s over 19 suites and this review's 1129.81 s over 30. The passing gate then ran once, outside the sandbox.

## Verification sequence and limits

The complete numbered sequence is VER-001, verdict pass, recorded at `2026-09-17T21:26:23.713Z`. There was no repair phase and no second verification; the control log holds exactly `WorkOrderActivated`, `ImplementationReady`, `VerificationRequested`, `VerificationCompleted` and `FinalReviewRequested`.

F1 is accurate, non-blocking, and is not routed to repair. `observeBackgroundTool` admits a dispatch only for `Agent`, `Task` and `spawn_agent`, so a `Bash` call with `run_in_background: true` journals nothing at dispatch. This review reproduces it in a second session and in the commonest shape: this reviewer started exactly one background task — the product gate — and the block at the next boundary read `Background tasks: unknown (no-session-task-observation)` while the same block carried that gate's measured duration. No numbered criterion is breached, because criterion 1 requires a cause code where a value is missing and the mechanism supplies one rather than inventing a dispatch time. It is nominated to planning with VER-001's repair shape: admit `Bash` into the dispatch branch on `input.run_in_background === true`, keyed on the task id the tool response already returns, with a regression that expects a `dispatchedAt`.

O1 is qualified by this session and the qualification favors the order. VER-001 observed that no Stop row was ever written unaided and that the usage half of the block was `unknown` at every one of its boundaries. In this attended reviewer session the Stop hook fired on its own when the turn ended, writing this session's only `finished` row at `2026-09-17T21:49:40.019Z` and the usage row whose cutoff `2026-09-17T21:49:37.578Z` then appeared in the next boundary's block with the token counters populated from `claude-transcript-message-usage` and `reasoningOutputTokens` and `costUsd` still printed as `unknown (counter-unavailable)`. O1 therefore describes a session that never ended a turn before handoff, not a host in which the Stop surface is inert; the hedge advisory does reach an agent in this harness.

O2 is reproduced live and remains cosmetic: this dispatch's briefing printed `Observed facts at 2026-09-17T21:37:08.275Z: unknown (session-identity-unavailable).` and the real block 390 ms later at `2026-09-17T21:37:08.665Z`. Both lines are true of their own source and nothing is invented, so no criterion is breached, but an order whose objective is that the agent has the value and nothing to estimate should not open by declaring the value unknown. It is nominated to planning together with O3, where a task that completes by notification keeps a growing elapsed counter against a `dispatched` label, and O5, where `PostToolUse` background rows carry no order or phase of their own.

The remaining items are limits, not failed claims: L1 the sandbox-nested gate, L2 the fixture-only Codex half of criteria 1 and 4, L3 live advisory delivery not exercised in the verifier's session, O4 the comma-set-off marker that escapes the clause scope. The order's own operator-review assumptions already fence the last of these: the counter counts what the journal can see, and an untyped correction without a journaled hedge is not counted.

## Release-close request and publication

No release dry run was attempted. This dispatch authorizes committing reviewed state, pushing the `wo-141` branch and opening its PR; it does not authorize a merge, a main push, a tag, a Release or a package publication, and the release helper's own preflight is the post-merge close's evidence. The PR body carries the current process-meter table supplied by the release-preparation step and the product-gate record above. The five-section release notes follow the publication contract, with one physical source line per prose paragraph, and disclose the Codex fixture-only limit, the background-shell dispatch gap and the measurement-only scope.

The PR title's gitmoji is selected from the [catalog](https://gitmoji.dev/) for tracking and measurement, and sized from this diff against the whole merged series rather than the previous subject. After the PR merges, the operator dispatches `resume: release close` in `main`.

No temporary repository or registered worktree was created. Recovery checkpoints, intake and the local journals are retained. The writer reservation is not released by an action of this session: `.claude/settings.json` wires `PreToolUse`, `PostToolUse`, `UserPromptSubmit`, `SessionStart` and `Stop` and no `SessionEnd`, `harness-host.ts` refuses a takeover only while `owner.pid` is alive, and `writer-teardown.mjs` coordinates worktree removal rather than a per-session release. The reservation therefore lapses when this process exits, and a later session in this worktree takes it by that liveness check.

Correction, same day, recorded under this order's own rule. The sentence above first read "this session releases its writer reservation at handoff". That was asserted from the presence of `writer-teardown.mjs` in the runtime installation list, without reading the module or checking how the reservation ends; the module coordinates worktree teardown and there is no session-end hook to release anything. What was meant was that the reservation does not outlive this session. The sentence now states the observed mechanism and names the three sources for it. The correction is recorded here rather than silently rewritten, and the release prose and PR body are re-pinned to the commit that carries it.

## Cost and instrument disclosure

Process cost at handoff: `inputTokens 74; cachedInputTokens 4209466; cacheWriteInputTokens 140640; outputTokens 23561; reasoningOutputTokens unknown; totalTokens 4373741; costUsd unknown`; source `claude-transcript-message-usage`, scope `dispatch`, cutoff `2026-09-17T21:58:24.199Z`; 51 steps and 43 commands from transcript tool-call metadata. The single 1129.81 s gate dominates that cost and was not optional: it is the reviewed identity the release manifest consumes, and the executor's and verifier's rows predate the staging that makes the new sources tracked. No process-cost reduction is claimed for this review; the 0.45 s sandbox probe avoided one failing gate.

Instrument disclosure: the machinery under review also observed this review, and every claim above that depends on it says so. The observed-facts block that criterion 1 introduces was injected into this session's prompt context at each boundary and is quoted above as evidence of its own criterion; the Stop path that criterion 2 introduces scanned this session's final message and wrote the usage row quoted above. My writer reservation (`actorId 98f50efd…`, owner pid 33324, reserved `2026-09-17T21:37:07.238Z`) is this worktree's single reservation and is intact. The live-gate refusal that WO-132 left standing blocked every `Bash` call for the duration of the gate, including read-only ones, so the interim gate output was read through the host's file reader instead.

Actor disclosure, because this order is about unmeasured claims: the harness version `2.1.274` was observed with `claude --version`, the effort `xhigh` was read back from `CLAUDE_EFFORT`, and the model `claude-opus-5[1m]` is this session's own statement of its identity with no effective-model readback channel, which is why the attestation source is `self-reported`. The compiled manifest's profile remains `claude-code-2.1.263`, eleven patch releases below the running CLI; no check above depended on that difference. The executor attested `codex-cli 0.154.0`, `gpt-6-astra`, `xhigh` (subagents, raw `ultra`), `codex-session-readback`; the verifier attested `claude-code 2.1.274`, `claude-opus-5[1m]`, `xhigh`, `self-reported`.
