# WO-070 FINAL-001 — final review

**Verdict:** pass. WO-070 asked that the seven build-free Beacon leaves get one home the control plane owns, that the skeleton import that home by name so no leaf is loaded once from source and once from `dist/`, and that a lifecycle transition in a copied root with no `packages/skeleton/src` emit a control Beacon that decodes. The implementation moved the leaves into the private, build-free `@dotln/beacons` workspace, rewired every consumer, split release-tag reading out of the gate-evidence import path so activation loads no skeleton source, made every root that assembles DotLn packages carry the workspace, and ran the operator-authorized live feedback episode and re-mint. [VER-001](../../verifications/WO-070/VER-001.md) passed all five criteria. This review read the whole subject diff, reproduced the leaf byte comparison, the module-identity check, the portability suite and the old-path search itself, re-ran every check, and ran the review gate on the subject at the code identity VER-001 judged. I found no defect. `main` has not moved since activation, so this review integrated nothing.

**Subject:** [`docs/work-orders/WO-070-beacon-portability.md`](../../work-orders/WO-070-beacon-portability.md) on branch `wo-070`, uncommitted over HEAD `af401170e0c9f197e9f6a1cd2cf1ea26d4d5c2e4`, which is `main` and `origin/main`; the `v0.51.0` tag peels to `64f9326f`, seven planning commits below. The dispatch checkpoint is `refs/dotln/checkpoint/WO-070/5` (`62d6dd58`).
- Against checkpoint 5, the tracked working tree differs only in the generated `docs/control/current.md` and `docs/work-orders/README.md` projections, and the order's control log carries the `FinalReviewRequested` row appended after it; the untracked verification report, decisions file, implementation record and feedback edition equal the checkpoint's blobs byte for byte.
- VER-001's recorded `reportHash` equals the report's current SHA-256 (`86bc79ac…`).
- The order's text differs from HEAD by the `(v0.51.1)` heading label that activation wrote and by the §Operator scope expansion section that [D010](../../evidence/WO-070/decisions.md#wo-070-d010) authorized and `plan amend-order` bound to receipt 030 (`PlanExecutionAmended`, normalized order hash `02a074d6…` over 5,684 characters, which `matchesExecutionAmendment` reproduces against HEAD and the current text); the five criteria judged here are the original ones, with criterion 5 judged after the re-mint as that section says.

The judged subject is the working tree: 80 tracked paths against HEAD (the seven leaves renamed from `packages/skeleton/src` to `packages/beacons/src`, four files added, 69 modified) plus the order's untracked records under `docs/control/orders`, `docs/evidence/WO-070`, `docs/verifications/WO-070` and `docs/final-reviews/WO-070`. Its code identity, computed here with `gateCodeIdentity`, is `3679af7623d491a2ad350194eab8698499461be262e4990a98d612e4fcb2cc67`, the identity the executor's final gate, VER-001's gate and this review's gate all recorded. No branch commit existed before this review.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.283","model":"claude-fable-5-1","effort":"xhigh","source":"claude-session-readback"}

Human actor: the operator dispatched `resume: final review` in a Claude Code session after selecting Fable 5.1 with `/model`, and made no other choice during the review. The harness version is from `claude --version`. The model is this session's model as the host reports it. Effort `xhigh` is the host's `CLAUDE_EFFORT` value read by this session: selected effort, not effective. The order asks for `reviewer any`. No subagent was planned or spawned: 0 of the cap of 20 (`harness usage`: exact-observed). The source diff is about a thousand lines and readable whole, the generated surfaces are judged by their deterministic checks, and VER-001 had already put two independent read-only agents on the diff; one reviewer reading everything and re-running every check was the smaller plan.

**Process cost:** entry 86391 tokens; handoff 4121608 tokens; source claude-transcript-message-usage

Both readings are `dispatch` scope, from `node scripts/harness.mjs usage 0b2bf899-28b8-422a-a8d4-707a65125771`. The entry reading was observed at 2026-09-26T00:24:27.060Z. The handoff reading was observed at 2026-09-26T00:44:31.050Z (76 steps, 69 commands, 0 subagents of cap 20, exact-observed), after the judgment text was complete and before the result transition. Both include reused cached input. Reasoning tokens and dollar cost are unavailable from this counter, which means unknown, not zero. The largest wait was the review gate (586.91 s); the paid live feedback episode belongs to the implementation dispatch (USD 1.6576648, [implementation record](../../evidence/WO-070/implementation.md)), and this review paid for no model call outside the session.

## Goal-aligned judgment

The order is the bounded prerequisite for exporting the control plane (WO-074, WO-075) on the critical path to an independently verified source-to-deliverable loop: a launchpad exported without `packages/skeleton` must still write and read its control Beacons. Receipt 030 judged it `aligned` on that basis. The question for this review was whether one module identity now exists and whether the copied root really emits, without any Beacon byte or guard changing.

- **Rule beating** decided how I checked. Criterion 2's grep test is scoped by [D006](../../evidence/WO-070/decisions.md#wo-070-d006) to the seven leaves and to `scripts/`, so it could pass while a leaf was still reachable twice by another route. I therefore imported each leaf by package name and by its source path in one process and compared the namespaces (7 of 7 identical), confirmed after a fresh build that neither `packages/skeleton/dist/src` nor `packages/beacons/dist` holds a copy, and searched every source, test, script, hook and JSON file for the old paths: only WO-042's historical mutation summary and two register summaries name them.
- **Seeking the wrong goal:** the outcome is the copied root emitting, not a passing suite name. I ran the portability suite myself; its second test asserts `packages/skeleton/src` absent both before and after the activation and decodes both audiences' Beacons from file size with the copied codebook.
- **Policy resistance / fixes that fail:** every guard stays. No fixture directory differs from HEAD, two leaves are byte-identical and the other five change only JSDoc type paths, and the immutable harness snapshot now carries the workspace ([D005](../../evidence/WO-070/decisions.md#wo-070-d005)) instead of resolving it through the checkout's `node_modules`, which the new snapshot test proves by importing the copied snapshot from outside every checkout.
- **Shifting the burden:** one predicate, `runtimeModuleDirectory`, decides what a package ships, so a launchpad, qualification root or fixture carries the workspace by construction rather than by operator rescue; [D007](../../evidence/WO-070/decisions.md#wo-070-d007) registers `beacons` as a release component so a later Beacon version bump is a label, not a paid live episode.
- **Drift to low performance:** the standard was the full product, machinery and document gate, which the takeover ran after Codex's segment had run none, and this review's own review gate.
- **Tragedy of the commons:** one paid live episode under D010's operator authorization; VER-001 recorded its own duplicate product gate as unnecessary cost; this review ran one gate and no agents.
- **Success to the successful:** the takeover re-derived Codex's workspace choice from the order and WO-033 rather than keeping it for its sunk work, and I judge the recorded reason below under assumption 1.
- **Escalation:** no new gate or approval; the scope expansion used the existing decision, amend-order and register surfaces.
- **Naive Interventionism:** consumers were enumerated from failing suites and a static trace; each repair is local and reversible; the reactor and the codebook algorithms are untouched.
- **NoOp**, withholding the pass, would keep the skeleton-source import in the lifecycle path and leave a subject that the executor's gates, VER-001 and this review's reproduction all agree on in a branch.

## Criteria

- **Criterion 1:** met. `git diff main --stat -- '*fixtures*' corpus` is empty, so no Beacon fixture changed. I reproduced the leaf table from HEAD blobs: `beacon-io.mjs` (`d25b6a75…`) and `beacon-provenance.mjs` (`f0b7f971…`) are byte-identical, and I read the other five diffs whole: every changed line retargets a JSDoc `import("./control-beacon.js")`, `import("./beacon.js")` or `import("./execution-environment.js")` type reference to `./types.d.mts`, plus one Prettier reflow of a type cast in `control-beacon-fs.mjs`; no constant, branch or newline changed. The skeleton suite, which holds the codebook v1 through group v1, perception and provenance tests, passed in this review's gate, and VER-001's focused run of the normative and exhaustive tests (104 v1 states, 288 v2 states, 125,970 group vectors, 589,824 representative v3 residues) stands with it.
- **Criterion 2:** met, as scoped by D006 to the seven leaves. `node --test scripts/test-beacon-portability.mjs` passes its grep scan, which walks `scripts/` recursively and rejects any skeleton source, skeleton dist or compiled-Beacons path to a leaf, and asserts each leaf exists only under `packages/beacons/src`. Independently: 7 of 7 leaves resolve to the same ESM namespace by `@dotln/beacons/<leaf>.mjs` and by source URL; `packages/skeleton/dist/src` holds none of the seven after `npm run build`; `packages/beacons/dist` does not exist; the built `reactor.js` imports `@dotln/beacons/control-codebook.mjs` by name; and no source, test, script, hook or JSON outside historical evidence names the old paths. The two non-Beacon source/dist pairs D006 names (`gate-evidence.mjs`, `usage-observation.mjs`) hold no module-level mutable state and are deferred with the plane/kit follow-up ([D009](../../evidence/WO-070/decisions.md#wo-070-d009), `FUP-a058e82c0bbd9b6d`); this report makes no whole-repository single-identity claim.
- **Criterion 3:** met. The suite's second test copies only `scripts/`, `packages/beacons` and `.gitignore` into a fresh temporary Git root with no `node_modules` and no `packages/skeleton`, activates WO-099 through `scripts/resume.mjs`, and decodes the public and verifier control Beacons with the copied codebook as `{codebookVersion: 2, phase: active, latestVerdict: unknown, effort: unknown, provenance: host-projected}` with mtime within one microsecond of the recorded transition. It passed here in 415 ms and in the gate's `beacon-portability` row. The split that makes activation load no skeleton source ([D003](../../evidence/WO-070/decisions.md#wo-070-d003)) is exact: `scripts/lib/release-tags.mjs` is HEAD's first 162 lines of `release-records.mjs` with the two gate-evidence imports removed and three private functions exported, and `release-records.mjs` re-exports the twelve former names and keeps its three, so its fifteen public names equal HEAD's ([D008](../../evidence/WO-070/decisions.md#wo-070-d008)); `dependencies.mjs` is the only consumer that moved to the new module.
- **Criterion 4:** met. Product 03's Layer diagram carries the row "Beacon metadata I/O (@dotln/beacons; build-free)" and a paragraph that separates the effect-edge reads and writes, the pure codebooks and the direct post-append projection from kernel-authorized perception sweeps; the skeleton README's §Control-plane Beacons explains both consumers; the pre-2026-09-09 ledger duty is discharged by [D002](../../evidence/WO-070/decisions.md#wo-070-d002) and its generated decisions-index row, and the work-order index marks that substitution on the order's row. Product 02 §Beacon codebook v1 names the workspace, ADR 0002 gains a dated WO-070 amendment, and product 06 records the activation completion with the compatibility impact. `npm run publication:check` passes with both editions CURRENT after the lock refresh.
- **Criterion 5:** met. `npm test -- --review` passed on the subject (42 passed, 0 failed, 586.91 s, 86 fresh tasks, exit 0, recorded 2026-09-26T00:42:27.004Z; see Checks). `git diff --check` and `git diff --check --cached` are clean. The diff adds no lint or type suppression. The lockfile adds only the `packages/beacons` workspace entry and its `node_modules/@dotln/beacons` link, the skeleton's exact `@dotln/beacons` pin and the `0.43.1` → `0.43.2` bump with the console's pin, so no third-party dependency was added; the internal workspace is the home the order's design expressly allows. The re-mint D010 authorized landed once: authority `WO-070/001` and feedback `WO-070/001` are selected, the feedback edition's live audit is its own (`carried: false`) and names the five component labels including `@dotln/beacons 0.1.0`, and all four edition checks pass on the subject.

## Findings

None requiring repair. Two observations are recorded here as notes, not defects, with the evidence for that classification:

- **`BeaconActionClass` in `packages/beacons/src/types.d.mts` is a hand-written seven-member union**, while the file derives its other codebook-backed unions from `BEACON_CODEBOOK` and `CONTROL_CODEBOOK` (D008). It mirrors the skeleton's equally hand-written `AuditActionClass`. Drift cannot pass silently in either direction: the staged strict build checks the skeleton's `BeaconState` against the leaf's parameter type, so a class added to the skeleton and not to the leaf fails `npm run build`, and `encodeBeaconState` throws `invalid beacon v1 fields` for any class the codebook does not list. Deriving the union from `BEACON_CODEBOOK.classes` would be tighter, but it is a source edit after D010's once-only mint and would need a fresh product gate for a type-only change; I leave it for the next order that touches the declarations.
- **The harness live smoke (`scripts/harness-live-smoke.mjs`) gained the workspace link and was not executed** by the executor, the verifier or this review; it runs live transports outside the gate. The two production builders changed with it are exercised by the `harness-probe` and `harness-fixtures` suites in the gate, and the snapshot test imports a copied snapshot from outside every checkout, so the link is covered by construction rather than by that smoke.

The boarded items keep their register rows and sit on no criterion's path: [D009](../../evidence/WO-070/decisions.md#wo-070-d009) (`FUP-a058e82c0bbd9b6d`), the plane/kit root resolution carried from WO-069 D005, D016 and D017, deferred at the operator's direction; [D011](../../evidence/WO-070/decisions.md#wo-070-d011) (`FUP-04bdf07955e1e24f`), the WO-154 evidence test that assumes the selected feedback edition is its own live audit, pre-existing on `main` and passing again now that the selection is live; and D005's recorded limit that the snapshot identity hashes only pinned dist files, so a Beacon-leaf-only change reuses an existing snapshot, as the unpinned dist codebook copies did before this order. WO-158-D006 (`FUP-09a2fb517f0ffbc4`) stays deferred to the next Beacon codebook revision, which this order is not.

## Integration with `main`

None was needed. After `git fetch origin`, `origin/main`, local `main` and the worktree HEAD all read `af401170`, and no commit exists in either direction. No merge, retime, stash or re-mint was performed and `npm run worktree -- integrate` was not run. `npm run release -- prepare` against origin's tags reports that `v0.51.1` remains current with no file changed besides the PR meter.

## Verification sequence

1. **Implementation**, two segments in one `resume: next` dispatch ([implementation record](../../evidence/WO-070/implementation.md)): Codex CLI 0.157.0, `gpt-6-sol`, `ultra` recorded `xhigh`/subagents, moved the leaves and recorded [D001](../../evidence/WO-070/decisions.md#wo-070-d001) to D003, then stopped in a provider outage before any gate; Claude Code 2.1.283, `claude-opus-5-5`, `xhigh`, continued at the operator's direction ([D004](../../evidence/WO-070/decisions.md#wo-070-d004)), reproduced each claim, ran the full gates, made every assembled root carry the workspace (D005), scoped criterion 2 (D006), registered the component (D007), applied the review-workflow repairs (D008), disposed the four carry-ins at the operator's direction (D009), ran the authorized live episode and re-mint (D010) and boarded the WO-154 test defect (D011).
2. **[VER-001](../../verifications/WO-070/VER-001.md)** (Codex CLI 0.157.0, `gpt-6-astra`, `ultra` recorded `xhigh`/subagents; two read-only agents at xhigh): pass on all five criteria at code identity `3679af76…`, with an independent focused run of the normative and exhaustive Beacon tests, a namespace-identity check, a TypeScript overlay probe showing the leaves remain type-checked, and a parsed lockfile comparison; no defect found or repaired. It recorded its own repeated product gate as unnecessary process cost after the operator's correction.

The verification ran on the implementation's first harness with a different model; this review runs on the implementation's second harness with a third model. Its reproduction, checks and gate are its own evidence.

The order was cut from WO-033's Beacon portability item at the operator's 2026-09-08 correction and first judged in receipt 003 of that day's critical-path pass. The latest ideation receipt, [030](../../planning/refutations/2026-09-25-planning-c93346fb3a92bb7f-030.md) of the 2026-09-25 standard pass (`aligned-with-findings` for the pass), judged WO-070 `aligned` with no findings: "Gate S enables a build-free exported control plane", with the expectation that "existing Beacon behavior must remain identical" and "no runtime fallback is added". The subject meets both: fixtures and codebooks are unchanged, and the copied root either emits or fails loudly. The receipt's unmeasured-cost finding on the order's legacy Cost line remains the planner's; D010's scope expansion is bound to that receipt by the `PlanExecutionAmended` row and `npm run plan -- check` exits 0.

## Checks

Executed by this reviewer on the subject, 2026-09-26:

| Check | Result |
| --- | --- |
| `git fetch origin`; `git log HEAD..origin/main` and the reverse | both empty; `origin/main` = `main` = HEAD = `af401170` |
| Working tree vs `refs/dotln/checkpoint/WO-070/5` | only `docs/control/current.md` and `docs/work-orders/README.md`; VER-001, decisions, implementation and feedback edition equal the checkpoint blobs |
| Recorded `reportHash` of VER-001 | equals the report's current SHA-256 (`86bc79ac…`) |
| Leaf SHA-256, `git show main:` vs `packages/beacons/src` | 2 identical, 5 differ; the five diffs read whole: JSDoc type paths and one Prettier reflow |
| `git diff main --stat -- '*fixtures*' corpus` | empty |
| `npm run build` | compiler, console, kernel, skeleton built; no leaf under `packages/skeleton/dist/src`; no `packages/beacons/dist` |
| Namespace identity, `@dotln/beacons/<leaf>.mjs` vs source URL, one process | 7 of 7 identical |
| Old-path search over source, tests, scripts, hooks and JSON | only WO-042's historical mutation summary and two register summaries |
| `node --test scripts/test-beacon-portability.mjs` | 2 passed, 0 failed, 461 ms |
| `release-tags.mjs` vs HEAD `release-records.mjs` lines 1–162 | only the two removed imports and three `export` keywords differ; fifteen public names preserved |
| `gateCodeIdentity` of the working tree | `3679af76…`, equal to the executor's and VER-001's gate rows |
| `npm run harness -- check` | 31 generated surfaces |
| `node scripts/authority-evidence.mjs --check` | verified; 34 bundle comparisons |
| `npm run evidence:artifact -- --check`; `npm run evidence:verification -- --check` | verified at `WO-161/001` |
| `node scripts/feedback-evidence.mjs --check` | live audit `WO-070/feedback-001` judged the current source; ten regressions, ten removal failures |
| `npm run evidence:console -- --check` | selfhost, control, refutations and missing match |
| `node scripts/harness-context.mjs --check` | exit 0; every ceiling within; refuter unset |
| `npm run publication:check` | PASS; both editions CURRENT (30 and 45 linked sections) |
| `npm run plan -- check` | exit 0 |
| `npm run release -- check-surfaces --local` | 51 PASS, 0 FAIL, including the beacons component, its workspace pin and its publish refusal |
| `npm run release -- prepare` (origin tags) | `v0.51.1` remains current; only the PR meter changed |
| Suppression search over the diff | 0 |
| `npm test -- --review` (Claude Code shell; no confinement in force) | **42 passed, 0 failed, 586.91 s, 86 fresh tasks, exit 0**, recorded 2026-09-26T00:42:27.004Z; tree `5251e1d3…`, code identity `3679af76…`; the row’s confinement field reads marker `claude-code`, probe written, `inForce: false`, so the full selection ran: the product suites plus the machinery suites whose declared sources changed, among them `evidence-sources`, `harness-probe`, `harness-fixtures`, `process-debt`, `runner-fixtures` and the four edition checks; `skeleton` 299.85 s, `worktree-integration` 112.86 s, `beacon-portability` 0.92 s |
| `git diff --check`; `git diff --check --cached` | clean |
| `npm run test:docs`, after this report and the PR body | first run 13 passed, 8 failed: the `release-surfaces` rule `github-body-profile` read the angle-bracket placeholders inside the notes’ code spans as raw HTML and seven suites stopped on that preflight; the PR body and notes were reworded; second run **21 passed, 0 failed, 27.90 s, 21 fresh tasks**; rerun after this cell was filled: 21 passed; 0 failed; 28.11 s |

## Evidence gate, write-back, non-goals and assumptions

- **Evidence gate:** satisfied. The transcripts are the implementation record's gate section and VER-001's reproduction commands; the ignored host gate rows at identity `3679af76…` record the executor's `npm test` (2026-09-26T00:00:44Z, 343,956 ms), VER-001's `npm test` (00:20:27Z, 293,618 ms) and this review's row above.
- **Write-back duty:** discharged. Criterion 4's three surfaces plus product 02, ADR 0002 and product 06; decisions D001 to D011 carry sources and reopening conditions and the index lists them; the register holds `FUP-a058e82c0bbd9b6d` and `FUP-04bdf07955e1e24f` open for the planner.
- **Non-goals held:**
  - Beacon semantics: no codebook constant, radix, phase list or framing changed; no fixture changed; the v3 key and storage paths are untouched.
  - The export itself (WO-074): no kit, overlay or export code is in the diff; the portability fixture copies files by hand.
- **Operator-review assumption 1:** held. The workspace home yields one identity (7 of 7 above). D002's recorded reason stands on evidence I checked: the bootstrap scripts need a path import that works before `npm ci`, the skeleton needs a name its TypeScript build resolves, and a private workspace gives both through npm's workspace link and the build's staging of build-free packages without emitting a copy, whereas a directory under `scripts/` would give the skeleton no package to import by name and a copy would be the second identity the order forbids.

## Handoff

Result: `final-review-result pass`. On pass, this review commits the reviewed series, pushes `wo-070` and opens its pull request. It merges nothing and publishes no release, tag or package.
