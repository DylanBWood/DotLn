# WO-085 FINAL-001 — final review

**Verdict:** pass. WO-085 asked that a product document cannot grow past a recorded ceiling, gain a dated receipt paragraph or a candidate heading, or link to a heading that does not exist without the document gate refusing; that a decision record cannot carry the operator's message as its dispatch; and that the written rule every role reads says to edit the changed sentence in place. The subject adds `scripts/docs-check.mjs` to `npm run test:docs`, reading `docs/control/doc-ceilings.json` and `docs/control/doc-baseline.json`, binds the rule into products 07 and 08 and `docs/README.md`, and repairs the three seams the check met in use: the index generator linked allocated reports before they existed, the integration decision stub emitted a dispatch the check refuses, and an imported path literal failed the configured-root suite. [VER-001](../../verifications/WO-085/VER-001.md) failed the pre-repair subject on two criterion-1 defects and routed five findings; the repair answered all five and integrated `main`; [VER-002](../../verifications/WO-085/VER-002.md) passed all five criteria and boarded ten non-blocking observations in [D015](../../evidence/WO-085/decisions.md#wo-085-d015). This review read the whole staged diff and both new scripts in full, counted the controls and the product bytes without the check's parser, ran the check, its fixtures and both gates on this final-review window, and confirmed `main` has not moved since the repair's integration. I found no defect that a criterion depends on and made no correction; one planning observation from the ideation receipt is recorded in [D016](../../evidence/WO-085/decisions.md#wo-085-d016).

**Subject:** [`docs/work-orders/WO-085-spec-receipt-boundary.md`](../../work-orders/WO-085-spec-receipt-boundary.md) on branch `wo-085`, uncommitted, over `main` at `6a5c323df4e2db298ec3ec0b16be61c6374b9cb8` (`v0.52.1`, WO-165). The repair integrated that base under [D010](../../evidence/WO-085/decisions.md#wo-085-d010) (preservation commit `refs/dotln/checkpoint/WO-085/6`, named stash `aabda54a` retained); `git fetch origin main` at this review found `origin/main`, `main` and the merge base all equal to it and `git log HEAD..main` empty, so no integration ran here. The dispatch checkpoint is `refs/dotln/checkpoint/WO-085/10` (`7ef6f9c0`); the working tree differs from it only by the dispatch's own `FinalReviewRequested` event, its two projections and the records this review files.
- VER-001's and VER-002's recorded `reportHash` values equal the reports' current SHA-256 (`20dfe6dd…`, `7c31940b…`).
- The order's text differs from `main` only by the `(v0.52.2)` heading label the integration's `release prepare` wrote; the five criteria are the original ones.
- The judged subject, staged, is 33 paths against `main` before this review's records: nine under `scripts/` (the check and its fixtures, the runner registration and gate stubs, the index generator and its tests, the integration stub and its test, the authority-evidence path), the two control files, the order's event log, products 05 to 08, `docs/README.md` and `README.md`, one planning document's anchor, the two publication locks, the generated index, control state, decisions index and register, and the order's evidence and verification records. Its code identity, recorded by this review's gate, is `26c200f1af7836611825290bfa9834b1efa179bda07da53f4f529527267aacd2` at tree `4728e33853a333ac0ba2b5c6f3531b4ca43b33b2`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.283","model":"claude-fable-5-1","effort":"xhigh","source":"claude-session-readback"}

Human actor: the operator dispatched `resume: final review` in a Claude Code session and made no other choice during the review. The harness version is from `claude --version`; the model is this session's model as the host reports it; effort `xhigh` is the host's `CLAUDE_EFFORT` value read by this session, selected rather than effective. The order asks for `reviewer any`. No subagent was planned or spawned: 0 of the cap of 20 (`harness usage`, exact-observed). The tracked diff is under 300 lines and the two new scripts are 36 KB, all read whole; twelve read-only agents across VER-001 and VER-002 had already probed and refuted the same subject; the remaining work was reproduction, gates and publication bookkeeping that one writer must own.

**Process cost:** entry 85725 tokens; handoff 3394011 tokens; source claude-transcript-message-usage

Both readings are `dispatch` scope from `node scripts/harness.mjs usage fa1941ca-8ed9-4eb9-bfe0-2d9d821a75e1`; the entry reading was observed at 2026-09-26T20:51:53Z and the handoff reading at 2026-09-26T21:20:35.230Z, after the judgment text was complete and before the result transition. Both count reused cached input; reasoning tokens and dollar cost are unavailable from this counter, which means unknown, not zero. The largest waits were the review gate (594.62 s) and the document gate runs; this review paid for no model call outside the session.

## Goal-aligned judgment

The order is not on the runtime critical path; it is operator-flow work on the deferred docs-reset row, judged `aligned-with-findings` by receipts [029](../../planning/refutations/2026-09-25-planning-8f567de659b31991-029.md) and [030](../../planning/refutations/2026-09-25-planning-c93346fb3a92bb7f-030.md) of the 2026-09-25 pass. Its promised benefit is that the 3.5× growth of `docs/product/` since 2026-09-01 stops at its inflow, so the next consolidation order is not undone in five days as WO-090's was. The question for this review was whether the gate now refuses the shapes the order names for a declared reason on every lifecycle window, without a bypass, a new ritual or a false refusal.

- **Rule beating** decided how I checked. The check passes the tree; VER-001 showed it can pass for the wrong reason (an undeclared PR-root rule, a hand-written marker pair). I counted the controls without the parser: 87 shapes, 368 link pairs with 412 occurrences, 743 dispatch fingerprints; the check reports exactly 412 declared occurrences, so nothing is admitted outside the inventory VER-002 keyed to closed orders' committed bytes. The fixtures now refuse marker pairs, leading byte-order marks and root-style PR links. Receipt 030's second known issue, that prose can relocate into exempt regions or a receipt can be written unbolded, remains constructible and stays boarded (D009 O1 and O2; D015 V1); its reopening observation, counted bytes falling while total bytes rise, has nothing to compare until a landed ceiling is edited against.
- **Policy resistance / fixes that fail:** receipt 029's first finding was that product 07 §Discipline makes every activation write a dated paragraph into 06 §Release boundary, which the check would refuse. The rewrite exempted that section until WO-086; the real roadmap now carries this order's two notes inside it (lines 54 and 61, in the section that runs from line 21 to line 847) and the check passes, so the standing duty and the new guard do not undo one another. Receipt 030's fourth known issue, that the first refused write-back would land on WO-164, WO-165, WO-162 or WO-163, is moot for WO-165 and WO-115, which merged before the check existed; the others activate after it and D009 carries the shape question.
- **Drift to low performance:** every ceiling is the landing bytes plus two per cent, a raise needs a resolving planning anchor and the fixture proves the refusal after a commit, and a consolidation lowers the ceiling in the same change. Receipt 030's first known issue, that the raise route makes the ceiling advisory by construction, is a standing observation whose reopening condition is the first raise without a same-change consolidation; none exists.
- **Escalation:** one check, document gate only, registered as a preflight; the F3 repair removed the false refusal every verifying and final-review window used to see. I observed the repaired behavior directly: `npm run test:docs` passed on this window before this report existed, with `FINAL-001 (pending)` as plain text. No approval step or ceremony was added.
- **Shifting the burden:** the check replaces the reviewer's hand link scan (WO-090 D007) and the workaround of filing a report early to run the gate. It adds one duty to whoever writes a PR body, which only `docs/README.md` states today (D015 V5); this review's body follows it and the reviewer's contract should say so, which D015's follow-up carries.
- **Tragedy of the commons:** fifteen documents parsed once per document gate (8.02 s in this run) and no product gate cost; the baseline is 172 KB of committed JSON the reader never opens.
- **Success to the successful:** D007 discarded the handwritten grammar for the pinned Prettier parser already installed rather than defending it; the new-dependency route stayed refused.
- **Seeking the wrong goal:** the ceiling is the proxy the operator can read and the order says so; the check judges the literal shapes and review still judges paraphrase, which D002 records rather than claiming a prose judge.
- **Naive Interventionism:** the consumers the check meets were enumerated and fixtured (release preparation's note, the index generator, the integration stub, the imported authority-evidence path); nothing under `packages/` changed; every edit is a document or a script and reversible.
- **NoOp** would keep the observed growth path and the unchecked links, and would leave `main`'s own `configuration-root` suite failing on the WO-165 literal that D014 repairs (V10).

## Criteria

- **Criterion 1:** met. On fixtures the check fails for a document over its ceiling by the exact overage (`scripts/test-docs-check.mjs`, the byte-ceiling case), a new dated receipt and a new candidate including duplicates and moved headings, a broken anchor and a missing file, and a dispatch without a control prefix or over 240 characters; the marker-pair, byte-order-mark and stored-PR-link cases added by the repair fail as VER-001 F2, F4 and F1 require. I ran the suite: 24 passed, 0 failed. On the current tree `node scripts/docs-check.mjs` prints the fifteen-row table and `PASS docs check: 15 product documents; 412 declared historical link occurrences; 0 failures`; my count of the baseline's declared occurrences is the same 412, so the pass rests on the declared inventory alone.
- **Criterion 2:** met. `doc-ceilings.json` names fifteen documents and `docs/product` holds fifteen, none extra. My byte count of each file, with 06's §Release boundary slice removed by heading lines, equals every `nonExemptBytesAtLanding`, and every ceiling equals `ceil(bytes × 1.02)`; the two values D012 reconciled after integration (04 at 59,662 and 07 at 184,704) match the integrated files. An omitted document fails with `missing ceiling` in the fixture. The activation-style paragraph passes under the real 06 §Release boundary: the real document carries this order's activation and collision-retiming notes there and the check passes, and the fixture covers a thousand-character note.
- **Criterion 3:** met. Product 07 §Documentation freshness and ownership keeps the edit-in-place rule and names `scripts/docs-check.mjs` in `npm run test:docs` and the ceilings file; product 08 §Freshness and ownership says "Edit the sentence that changed in place" and names the check and both control files; `docs/README.md` links the check and both controls and states the resolution rules the code applies. Each product edit replaced words inside an existing paragraph; the check confirms no new receipt or candidate shape in either document.
- **Criterion 4:** met. The order was filed on 2026-09-08 (`6ab27c46`), so its ledger duty is discharged by the decisions file and the index rows; the generated index marks the substitution and `docs/lineage/decisions-index.md` carries D001 to D015, with D016 joining at this review through `npm run meta`. Both publication locks changed and `publication:check` passes (276 of 276 headings, both editions current). `FUP-87ed701db7d7209e` carries a `duplicate` disposition targeting `FUP-71fc2efc208f597a`, whose `deferred` disposition names the historical operator-chat sweep and the role-text question, so the sweep stays named outside this order.
- **Criterion 5:** met. `npm test -- --review` passed on the staged subject (34 passed, 0 failed, 594.62 s, 78 fresh tasks, exit 0; see Checks). `npm run test:docs` passed on this window before the report existed (23 passed, 0 failed, 29.97 s) and again after this report, the PR body, the notes and D016 were written (see Checks). `git diff --check` and `git diff --cached --check` are clean; `package.json` and `package-lock.json` are unchanged; the diff adds no lint or type suppression and the records carry no attribution.

## Findings

None routed to repair, and no correction was needed within the boy-scout bound. Three observations from this review, each on a seam already boarded or now recorded:

- **R1, planning (recorded in D016).** Receipt 030's third known issue is met on its scope half. The link scan covers root-level Markdown and the configured document roots; `.claude/skills` lies outside both, and the six role skills cite `docs/product/07-execution-guide.md#Goal-aligned decisions` inside backticks, which the parser reads as inline code. WO-167 criterion 2 names the docs check reporting zero broken anchors as proof that every heading a role skill cites still resolves; today that report is silent about the skills, so the proof is vacuous for them. D016 routes the choice to planning before WO-167 activates; D009's O5 follow-up already holds the wider scope question.
- **R2, order text (boarded, D015 V7).** The order's Cost and Design still describe an exemption for generated blocks between markers; the repaired check has none because no generator writes into `docs/product`. The criteria do not depend on the exemption and D011's reopening condition names the first real producer.
- **R3, contract (boarded, D015 V5).** The check refuses a root-style `docs/…` link in a new PR body; only `docs/README.md` states the file-relative form. This review's PR body uses it, and the planning follow-up in D015 carries the writer-side rule.

## Verification sequence

1. **Implementation**, `resume: next` on 2026-09-26 ([evidence README](../../evidence/WO-085/README.md)): Codex CLI 0.157.1, `gpt-6-astra`, `ultra` recorded `xhigh`/subagents, two read-only helpers. It reused then superseded the publication heading helpers with the pinned Prettier Markdown AST (D001, D007), set the counted baselines and the lexical dispatch rule (D002), repaired two current anchors under operator approval and declared the 40 historical occurrences (D003), retargeted the carry-in register row (D004), assigned `v0.51.2` and the ceiling formula (D005) and fixtured the reproduced edge cases (D006).
2. **[VER-001](../../verifications/WO-085/VER-001.md)** (Claude Code 2.1.283, `claude-opus-5-5`, `ultracode` recorded `xhigh`/subagents; six read-only agents): fail. F1, a PR-root link rule admitting 308 broken links outside the inventory; F2, any hand-written marker pair exempting its contents; F3, the index linking allocated reports before they exist and failing the gate on every verifying and final-review window; F4, a leading byte-order mark disabling detection; F5, a wrong sentence in `docs/README.md`. D008 routed them and D009 boarded O1 to O14.
3. **Repair**, `resume: fix` (Codex CLI 0.157.1, `gpt-6-astra`): integrated `main` first under the operator's scope expansion (D010), repaired F1 to F5 with regressions and corrected D003's unsupported claim the same day (D011), reconciled the landing inventory with the integrated bytes for 04 and 07 only (D012), and repaired the two producers the integrated gate exposed: the integration decision stub's dispatch (D013, adjacent-0002) and the imported WO-165 path literal (D014, adjacent-0003).
4. **[VER-002](../../verifications/WO-085/VER-002.md)** (Claude Code 2.1.283, `claude-fable-5-1`, `ultracode` recorded `xhigh`/subagents; six read-only agents): pass on all five criteria at checkpoint 8; F1 to F5 reproduced as fixed on copies of the real documents with the real controls; the document gate green on a live verifying window; V1 to V10 boarded in D015 with a named follow-up.

Implementation and repair ran on Codex with `gpt-6-astra`; VER-001 on Claude Opus 5.5; VER-002 and this review on Claude Fable 5.1 in separate sessions. This review's reproduction, checks and gate are its own evidence and do not rest on VER-002's rows.

The order was cut from WO-035's spec/receipt item at the operator's 2026-09-08 correction, held on 2026-09-19 because its check would have refused `release prepare`'s roadmap note, and rewritten by the 2026-09-25 standard pass at the operator's item 4. Receipt 029 judged the pass `aligned-with-findings` with two WO-085 findings, the Release boundary conflict and the unspecified treatment of exempt text; the pass repaired both in the order text (§14) and receipt 030 judged the repaired order `aligned-with-findings` with four known issues, each with a reopening observation. Of those four, the sequencing issue is moot for the two orders that merged first, the raise-route and relocation issues have no triggering observation yet, and the anchor-semantics issue is met on its scope half and recorded as R1.

## Checks

Executed by this reviewer on the subject, 2026-09-26:

| Check | Result |
| --- | --- |
| `git fetch origin main`; `git rev-parse main origin/main HEAD`; `git log HEAD..main` | all three refs `6a5c323d`; no commit either way; no integration needed |
| Recorded `reportHash` of VER-001 and VER-002 | equal to the reports' current SHA-256 |
| Baseline and ceiling count without the check's parser | 87 shapes; 368 link pairs, 412 occurrences; 743 fingerprints; 15 of 15 ceilings equal `ceil(bytes × 1.02)` of the independent count |
| `node scripts/docs-check.mjs` | PASS; 15 product documents; 412 declared historical link occurrences; 0 failures; 06 exempt bytes 62,377 |
| `node --test scripts/test-docs-check.mjs` | 24 passed, 0 failed |
| `npm run test:docs` on this window before the report existed, 20:58:35Z | **23 passed, 0 failed, 29.97 s, 23 fresh tasks**; `FINAL-001 (pending)` rendered as plain text |
| `npm run publication:check` | PASS; 276 of 276 headings; both editions CURRENT |
| `npm run release -- check-surfaces --local` | PASS rows only, including the five publish refusals |
| `node scripts/meta.mjs --check`; `node scripts/harness.mjs check` | exit 0; 31 generated surfaces |
| Suppression and attribution search over the diff and the records | 0 and 0 |
| `npm test -- --review` (Claude Code shell; no confinement in force) | **34 passed, 0 failed, 594.62 s, 78 fresh tasks, exit 0**, recorded 2026-09-26T21:10:30.540Z; tree `4728e338…`, code identity `26c200f1…`; the 28 product suites plus the machinery suites whose declared sources changed since the base, among them `configuration-root`, `registrations` and `runner-fixtures`; `skeleton` 314.45 s, `worktree-integration` 113.45 s, `worktree` 81.81 s |
| `git diff --check`; `git diff --cached --check` | clean |
| `npm run test:docs` after this report, the PR body, the notes and D016 | first run, 21:21:35Z: **23 passed, 0 failed, 29.02 s, 23 fresh tasks**; rerun after this row was written: **23 passed, 0 failed, 29.35 s, 23 fresh tasks** (21:22:29Z) |

## Evidence gate, write-back, non-goals and assumptions

- **Evidence gate:** satisfied. The fixture transcripts are the executor's [`fixtures.txt`](../../evidence/WO-085/fixtures.txt) and [`docs-check.txt`](../../evidence/WO-085/docs-check.txt) for the pre-verification subject, labelled as such; the repaired subject's 24-case run is recorded in the evidence README, in VER-002 and in this report's Checks; the ignored host gate rows record the executor's, VER-002's and this review's gates, the last at the code identity above. No live row, as the order requires.
- **Write-back duty:** discharged. Products 07 and 08 and `docs/README.md` carry the rule and name the check; D001 to D016 carry sources and reopening conditions and the index lists them; the register row is retargeted; both publication locks are current.
- **Non-goals held:** no existing text was moved or folded (the diff under `docs/product/` is the two bound sentences, one anchor fragment and the two roadmap notes inside the exempt section); no committed decision record was rewritten, and the sweep stays in `FUP-71fc2efc208f597a`; no prose-quality judge; the one planning-document change is a single anchor fragment (D003, adjacent-0001), and the check's scope excludes `docs/planning/`.
- **Operator-review assumption 1:** held. Every ceiling is today's non-exempt bytes plus two per cent, and the raise guard is fixtured. **Assumption 2:** held. The evidence README and D001 to D016 hold this order's receipts, and the decisions index exposes them; nothing dated was added under a product heading outside the exempt section.

## Handoff

Result: `final-review-result pass`. On pass, this review commits the reviewed series, pushes `wo-085` and opens its pull request. It merges nothing and publishes no release, tag or package.
