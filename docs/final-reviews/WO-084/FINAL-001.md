# WO-084 FINAL-001 — Final review of the ledger order and index

**Verdict: pass.** All four acceptance criteria hold at the reviewed subject. The verification sequence is one report, VER-001, which passed; nothing it passed was contradicted by what this review read or ran, and its two central claims (section preservation and the refusals) were re-derived here with separate scripts. There is no blocking finding. VER-001's five observations are judged below and this review adds three. Two of VER-001's observations are judgments the executor made in the operator's vocabulary: three entries gained an inferred `adopted` tag, and the ledger header gained five executor-written status definitions. Both are consequences the order's own criteria force, both are disclosed, and both are put in front of the operator in the pull-request body, where the merge decision is made. This reviewer changed no source, test, ledger, product document or generated surface other than the work-order index refresh. One environment event is disclosed under Document gate: the operator upgraded the machine's Node to v26.9.0 between this review's product gate and its document gate, and one document suite outside this order fails on the new version.

Reviewed by a Claude Code session. The session hook recorded dispatch `resume: final review` and allocated this path; its `FinalReviewRequested` row is dated 2026-09-19T05:22:57.271Z. Harness version 2.1.278 is the output of `claude --version` in this session. Model `claude-fable-5-1` is the identifier the harness supplied in this session's system context after the operator's `/model` selection. Effort `xhigh` is `CLAUDE_EFFORT` in this session's shell, a host-supplied selection and not effective-session readback, so the source is `operator-attested`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-fable-5-1","effort":"xhigh","source":"operator-attested"}

## Subject and integration

Reviewed [WO-084](../../work-orders/WO-084-ledger-order-and-index.md) in full; the complete numbered sequence, [VER-001](../../verifications/WO-084/VER-001.md); decision [D001](../../evidence/WO-084/decisions.md); the [implementation evidence](../../evidence/WO-084/README.md) with its [migration manifest](../../evidence/WO-084/migration.json), [preservation record](../../evidence/WO-084/preservation.json) and [validation transcripts](../../evidence/WO-084/validation.txt); and the order's control log. Source read in full: `scripts/lineage.mjs` and `scripts/test-lineage.mjs`. Diffs read in full: `scripts/test-runner.mjs`, `scripts/test-runner.test.mjs`, the root README, `docs/README.md`, product 06 and 07, the planning map row, WO-109's citation, the order's title line, the decisions index row, the follow-up row and both publication locks. The generated [lineage index](../../lineage/README.md) and the ledger header were read in full. The ledger's 13,561-line diff was not read line by line; it was judged by this review's own preservation script over the baseline and current files, described under criterion 2. Three mechanisms the criteria depend on were read at source and not taken from the reports: `planningPasses` in `scripts/lib/plan-subject.mjs`, its callers `latestPlanningPass` in `scripts/lib/plan-direct.mjs` and `planningPassScope` in `scripts/lib/plan-receipts.mjs`, and the runner's suite classification. Cited sections read: WO-035's observed gap and ledger design item, Principle 12, product 07 §Goal-aligned decisions and product 08 §PRs and commits.

`git ls-remote origin` at review: `origin/main`, local `main` and this branch's `HEAD` are all `cb932c845354ec871fd34e14dec859bb869776ef`. Nothing was integrated, no conflict was resolved and nothing was retimed. `v0.31.1` is the newest tag locally and on the remote and no `v0.31.2` tag exists, so the staged patch target stands. The lane-pair sibling WO-142 has a worktree at the same base; if it publishes first, this order's version is retimed at integration under its existing patch classification, which is not a finding against either order. `package.json`, `package-lock.json` and every component `package.json` are unchanged. This reviewer wrote this report, `PR.md`, `RELEASE-NOTES.md` and the work-order index refresh.

**Ideation receipt.** The dispatch briefing names one; none exists for this order. `docs/evidence/WO-084/` holds no ideation record and the control log has no ideation row, so there was nothing of that kind to judge.

**Instrument disclosure.** Instruments from earlier orders observed this review: the generated Claude hooks reserved this session's writer and refused this session's shell calls during its own live gate, the gate-evidence machinery recorded the row below, and `harness usage` supplied the counts. `scripts/test-runner.mjs` is part of this subject, because it registers the two new suites, and it also ran the gate. `scripts/lineage.mjs` is under review; this review's preservation check does not import it, and its refusal probes call its `main` only as the thing being tested.

**Fan-out.** Plan stated before any spawn: none. The reviewable source is 423 new lines in two scripts plus an eleven-line runner diff, and the large diff is a mechanical move best judged by a script. Actual: 0. At 05:35:22Z `harness usage` reported count 0, cap 20, remaining 20, `exact-observed`, uncounted remainder unknown.

## The review gate

All twelve new files were staged first, including `scripts/lineage.mjs` and `scripts/test-lineage.mjs`, so the gate's code identity covers them and `--review` can see the changed runner.

`npm test -- --review` ran once, with no source edit before or after it: `22 passed; 0 failed; 483.22 s; 66 fresh tasks`, exit 0. The row was recorded 2026-09-19T05:34:02.311Z for tree `51553c817e86f14be7f26af29d14aedc6ff96d98` at code identity `d995b16627e482c2f9130984a5583a20ae34ec4aa728d9b669a5bbae588108ab`, execution mode fresh, 0 reused suites. Recomputing `gateTreeHash` and `gateCodeIdentity` after the gate returned the same two values. The selection is the 21 product suites plus the machinery suite `runner-fixtures` (34.73 s), which `--review` added because the runner changed. VER-001's row carries a different code identity, `10bbdf4b…`; this is an inference, not an observation: the identity is keyed by tracked code and the two new scripts were untracked when the verifier ran. The evidence for it is that `git diff` against `refs/dotln/checkpoint/WO-084/2` shows no difference under `scripts/`, `packages/`, `docs/lineage/`, `docs/product/` or `docs/evidence/`, and against checkpoint 4 only `docs/control/current.md`, the control log and the work-order index differ.

Checks run by this reviewer at the same subject, each exit 0: `node scripts/lineage.mjs index --check` (`PASS lineage index: 141 sections`), `node --test scripts/test-lineage.mjs` (7 of 7), `npm run plan -- check`, `npm run publication:check` (30 and 45 linked sections current), `node scripts/release.mjs check-surfaces --local`, `npm run harness -- check` (31 generated surfaces; local terms unavailable, so no terms assessment is claimed), `git diff --check` and `git diff --cached --check`. A search of every added line outside the ledger and of every new file found no new `eslint-disable`, `@ts-ignore`, `@ts-expect-error`, `@ts-nocheck` or `prettier-ignore`, and no attribution trailer, footer or session link; the single match is VER-001's own sentence reporting that search.

## Document gate

After the gate this reviewer ran `npm run work-orders -- index`, then wrote this report, the PR body and the release notes. `npm run release -- prepare` reported `WO-084 target v0.31.2 remains current; no files changed`, tag observation `origin`. None of these writes touches tracked non-generated code; the code identity was recomputed after them and compared with the gate row before the result was recorded.

**The machine's Node changed between the two gates.** fnm's `default` alias was repointed to a freshly installed v26.9.0 at 2026-09-19T05:37:31Z, from a long-lived shell outside this session; the operator said during this dispatch that the upgrade was theirs and was forced. The product gate row above was recorded at 05:34:02Z, before the change. The row has no Node field, so the version it ran under is not recorded. Inference, not observation: it was v22.2.0. The evidence is that `docs/discovery/environment.json` records Node 22.2.0 as observed, v22.2.0 is the only other Node on this shell's `PATH`, and at about 05:35Z this session's piped `node --test` printed TAP lines, which v26.9.0 in this same shell does not.

The first document run, under v26.9.0, ended `12 passed; 7 failed; 5.61 s`: two failures and five dependents skipped as failed preflight. The first failure was this reviewer's own: `release-surfaces` refused the release notes because `#L<n>` reads as raw HTML; the notes and the PR body now show a concrete link form, and the suite passes. The second is `feedback-evidence`: "feedback fixture gate failed: anti-oscillation present". `packages/skeleton/src/feedback-audit.ts` spawns `node --test` for each unit's fixture (line 189) and parses `# pass N` and `# fail N` from its output (line 205). Reproduced with the same spawn shape under v26.9.0, the fixture exits 0 and prints the spec reporter (`✔ …`, `ℹ pass 1`), so the TAP pattern finds nothing and the gate reads a pass as a failure. WO-084 touches neither that file nor its fixture, and the same suite at the same bytes passes under v22.2.0. It is a machinery incompatibility with the new Node. This branch's diff against `main` under `packages/` is empty, so `main` carries the same code; this review did not run it there. It is nominated below, not repaired here, because the fix is skeleton component source outside this order's patch scope.

The document gate was then run under v22.2.0, selected by a `PATH` prefix on that one command: `19 passed; 0 failed; 129.48 s; 19 fresh tasks`, with `lineage` at 0.11 s and `lineage-fixtures` at 0.88 s, and with `format`, `index`, `meta`, `release-surfaces` and `verification-evidence` reading this report, the PR body and the release notes. Under the new default v26.9.0 the suites that cover this order's check and this review's outputs were run one at a time and each passed: `lineage`, `lineage-fixtures`, `release-surfaces`, `format`, `index`, `meta` and `verification-evidence`. Not run under v26.9.0: the product gate and the other document suites. This reviewer changed no Node, fnm or shell setting. This section was completed after those runs; `format` and `release-surfaces` were rerun over the final bytes.

## Acceptance criteria

| # | Assessment | Result |
| --- | --- | --- |
| 1 | The check passes on the reset ledger. Against copies of the real ledger and index run through `main(["index","--check"], root)`, this review's ten probes gave one pass (unchanged) and nine refusals, each leaving the index bytes unchanged: a dated session appended at end of file ("below the founding corpus boundary"); the old Resolutions heading re-added ("belongs in resolutions.md"); a 2026-01-01 session inserted at the top ("out of newest-first date order", see R1); an undated session ("needs a valid YYYY-MM-DD date"); an entry whose only `adopted` sits later in its sentence ("unlabeled entry"); an `accepted` tag ("unknown status or provenance tag"); a provenance-only lead ("unlabeled entry"); a status unquoted in the header declaration ("Statuses declaration differs"); and a valid 2026-09-20 session without regeneration ("Stale docs/lineage/README.md"). These overlap VER-001's six and add four. **Counts.** VER-001 compared every status and all 141 rows with prettier's Markdown parser and found them equal: 970 entries, adopted 533, preserved 319, raw 16, superseded 4, transformed 63, rejected 26, deferred 1, candidate 7, open 3, specified 4. This review did not repeat that parse. It read the committed test's oracle, a multi-line lead regex that shares no code with `inspectLedger`, and ran it green. VER-001's note stands: a single-line grep undercounts because many leads wrap their tag, so the criterion's "grep" has to read whole leads. | Pass |
| 2 | This review's script splits `HEAD:docs/lineage/idea-ledger.md`, the current ledger and `resolutions.md` on `## ` lines without importing the subject. 142 baseline sections become 141 plus 1; the heading multisets are equal with no duplicate; 138 bodies are byte-identical, 2 differ only in trailing blank lines, and 2 differ only by the six lead edits in `migration.json`, which the script printed line by line. Resolutions sat at index 110 of 142 with 31 sections below it. In the result, dates are non-increasing before Images, every same-date group keeps its baseline relative order, and Images with Chat 011 to 001 are last. Planning-pass ids: `planningPasses` hashes heading bytes only, so equal headings give equal ids; VER-001 computed the 13 ids on both sides. Beyond the set, `latestPlanningPass` selects by date and the receipt chain, and its own comment says neither depends on where a section was filed; `planJudgmentScope` finds a pass's introducing commit with a `-S` pickaxe, which a move inside one file does not trigger. `npm run plan -- check` passes. The header changes beyond the dated note are judged under O2; none is entry prose. | Pass |
| 3 | The live pointers name the new path: product 07 §Operator-opened ideation mode item 5 and §Discipline, the planning map's do-not-relitigate row, WO-109's citation, the `docs/README.md` map, the ledger header's Provenance paragraph and migration note, and the generated index. This review's `git grep -i resolutions` over the tracked tree, plus the new files, finds no live document still pointing at an in-ledger Resolutions section. The remaining mentions are historical or immutable (WO-035's measurements, the phase-two and 2026-09-19 planning documents, receipts, reviews, evidence, the harness-context fixture baseline), WO-142's fence, and two lines of ledger entry prose that criterion 2 forbids changing. No document links into the ledger by line number outside the generated index. `CLAUDE.md` never mentioned Resolutions, which matches the order's re-observed gap, and its locked floor was correctly left alone. The ledger duty for this order filed before 2026-09-09 is discharged by [D001](../../evidence/WO-084/decisions.md#wo-084-d001) and its index row, as the work-order index states. | Pass |
| 4 | The gate row above. `lineage` (`index --check`, a preflight) and `lineage-fixtures` are registered `document: true`; the runner computes `product` as neither machinery nor document, selects `row.product` for plain `npm test`, and neither suite appears among the gate's 22. VER-001 observed both inside a green `npm run test:docs` at 0.12 s and 0.90 s. Both `git diff --check` forms are clean and no dependency changed. | Pass |

## Verification sequence

VER-001 is the only report and it passed without a repair cycle. It did not use the subject's parser as its oracle: counts came from prettier's bundled Markdown parser, preservation from a separate boundary split, and pass ids from the unchanged `planningPasses`. It bound its subject by comparing a temporary-index tree with checkpoint 2, so the source it judged is the source the executor handed off, and the same bytes reached this review. Its first `test:docs` run failed on a stale lifecycle projection it had not refreshed (O3); it said so, refreshed and reran green, which is the right handling and not a subject defect. Nothing in the sequence routes a finding against a sibling's staged version.

## Judgments on the recorded observations

- **VER-001 O1, three inferred `adopted` tags.** Confirmed as disclosed. The check requires a lifecycle tag, which is the header's founding rule ("one entry and a lifecycle status"), so each of the three needed one, and the order's criterion 1 cannot pass otherwise. The choice of `adopted` is supported by each entry's own section: in the 2026-09-08 section every other `operator-directed` entry already reads `` `adopted` `operator-directed` ``, and "Two NoOps of the first result are reversed by operator direction" records an operative reversal of the same kind; in the WO-022 section the metadata finding and the implementation choice both describe what that order built. The third edit is outside the "bold-lead sections" the order names, because the entry already had a backtick tag; it is inside criterion 1's requirement. Whether `adopted` is the disposition the operator intends is the operator's to say. The pull-request body lists the three.
- **VER-001 O2, the header changes beyond the dated note.** Criterion 2 reads "only section moves, tag normalization and the header note", and the header diff is wider than a note: the status declaration grows from five to ten, Provenance gains `operator-directed` and `operator-authorized`, and the Order paragraph drops the sentence placing Resolutions at the bottom and states the rules the check enforces. Each is forced by the order itself. The objective refuses "a status outside the declared set" while 41 existing entries carry the five undeclared statuses, so the set had to be declared, the entries rewritten, or the header left false; D001 rejects the rewrite, and declaring is the only option that changes no entry. The dropped sentence became false when Resolutions moved. Read against its own second clause, "no entry's prose changes", the criterion is met. What remains is wording: the five definitions are the executor's, for statuses the operator's sessions coined. They are quoted in the pull-request body so the operator reads them before merging. Not blocking.
- **VER-001 O3, the stale projection in its first document run.** A verifier-side omission, stated by the verifier, costing 34.22 s. This review refreshed the index after the gate and before its document run.
- **VER-001 O4, line widths.** Confirmed at product 07 line 610 and WO-109 line 84; the planning map's edited cell breaks that table's alignment. The `format` suite passes. Left unedited so the verified bytes stay the reviewed bytes; nominated below.
- **VER-001 O5, the index adds a step to every ledger write.** Confirmed, and it is the intended trade. One consequence VER-001 did not draw: no procedure names the command. `git grep` finds `lineage.mjs` in no product document, playbook page or role skill; only the ledger header, the generated index and the refusal message carry it. The refusal names the exact fix, so the first stale write explains itself, but a planning or ideation session learns it from a failed document run. Nominated below.
- **R1, the out-of-order diagnostic names the neighbour.** Inserting a 2026-01-01 section at the top is refused with "Line 47: section is out of newest-first date order". Line 47 is the existing 2026-09-19 section that follows the insertion; the misfiled section is at line 43. The loop compares each section with its predecessor, so it blames the first section that is newer than the one above it. The refusal is correct and the fixture test asserts only the message. A writer who files an old section high sees an existing section accused. Nominated below.
- **R2, the day is the ordering unit.** Same-date groups keep their prior relative order, and that order mixes two filing directions: sections once prepended run newest-first, and sections once appended below Resolutions run oldest-first. On 2026-09-08 the WO-039 fourth repair precedes the fifth, and on 2026-09-07 implementation, ideation, repair and third repair ascend. Within-day sequence is not in the heading bytes, so neither the migration nor the check could order it without inventing facts, and the header now says equal dates retain their existing order. Recorded as a limit of the result, not a defect.
- **R3, a stale lineage index skips dependent document suites.** `lineage` is a preflight, like `index` beside it, so a stale or invalid ledger skips the non-preflight document suites as failed preflight, as VER-001 saw with the work-order index. This matches the existing idiom and the order asks only that the check be in the chain. It raises the cost of the first forgotten regeneration from one failed suite to a short, mostly skipped run.

## Clean room over the whole subject

A screen of every added line outside the ledger and of every new file for home-directory paths, email addresses, URLs, credential patterns and the word "internal" matched only false positives: "internal whitespace" in the evidence, "internal contradictions" in the verbatim Resolutions text, the word "token" in an existing WO-043 heading carried into the index, and VER-001's description of its own scan. The moved ledger bodies were not rescanned; the preservation script shows them byte-identical to `main` apart from the six listed leads. No stop condition.

## Non-blocking items for the next planning pass

1. **Operator confirmation of three dispositions** (O1): `adopted` on the WO-022 metadata finding, the WO-022 implementation choice and the 2026-09-08 NoOp reversal.
2. **Five status definitions** (O2): `rejected`, `deferred`, `candidate`, `open` and `specified` are defined in the header in the executor's words. A correction is a header edit, not an entry edit.
3. **Name the index command where ledger writers start** (O5): product 07's ideation and planning procedures and the planner role text do not mention `node scripts/lineage.mjs index`.
4. **Diagnostic line** (R1): report the misfiled section, or both sections, when the date order breaks.
5. **Line widths and one table cell** (O4): bounded cleanup in product 07, WO-109 and the planning map.
6. **In-flight ledger writers.** A branch that adds a ledger section against the old layout merges into a reordered file; the check refuses a misplaced result and names a stale index. WO-142 is fenced off these paths; planning branches are not.
7. **Criterion wording.** Criterion 1's "equal a grep of the file" cannot be met by a line grep because leads wrap; a later order reusing the phrase should say "a parse of entry leads".
8. **Node 26.** The feedback audit's fixture gate depends on the default `node --test` reporter and fails every document run on v26.9.0; requesting the TAP reporter explicitly in that spawn is the narrow repair. Wider than that one suite: no product gate has been observed on v26.9.0, the gate row records no Node version, and `docs/discovery/environment.json` still says 22.2.0. Until this is handled, every session's `npm run test:docs` on this machine stops at `feedback-evidence` and skips the suites behind it.

## Goal and handoff

Mission contribution: none on the critical path, and D001 says so. This is documentation upkeep that improves operator flow in one specific way: the ledger's newest-first promise is true again, two operator ideation sections from 2026-09-18 are where a reader meets them, and the placement of every future section is a mechanical rule with an executable refusal instead of a judgment made at each write. The material choices in this review were the verdict, treating O2's header changes as met in substance, and making no reviewer edit.

Policy resistance and rule beating: the pass rests on a fresh gate at an identity that covers the new scripts, on a preservation check that shares no code with the subject, and on reading the three planning consumers, because criterion 2's receipt safety depends on mechanisms outside the new files. Drift to low performance: no write-back claims more than the source does; the evidence says "document suite" wherever the check is described and the roadmap paragraph says patch with no component change. Seeking the wrong goal: the status counts are navigation, and nothing in the outputs presents 970 entries as progress. Shifting the burden: the insertion decision leaves the writer, and item 3 above names the one place the burden could return, a session that does not know the command. Commons and escalation: no agents were spawned against a budget of twenty, one gate of 483 s was run, and width cleanups and a diagnostic line did not become a repair cycle. Success to the successful: VER-001's pass received no deference where this review could check for itself, and those checks agreed with it. Naive Interventionism: every ledger consumer found by search reads the file by heading or whole-file size, none by position, and the move is reversible from `main`; the reviewer declined the O4 edits because the lines assert nothing false and editing would have moved product 07 and its publication lock away from the bytes VER-001 judged. NoOp on the verdict, failing for O2's letter, would hold a verified, reversible migration behind five definitions the operator can correct in one header edit, and would leave 31 sections where the header says nothing goes.

Process cost: at entry (05:23:02Z) 79,895 total tokens; at 05:35:22Z, after the product gate, 5,692,683; at 05:46:53Z 11,915,554 total tokens, of which 11,643,388 were cached input, 206,879 cache writes and 65,133 output, across 95 steps and 57 commands. All three figures are dispatch scope from `claude-transcript-message-usage`; USD and reasoning tokens are unknown. 483 s was product-gate waiting, during which the live-gate refusal blocked this session's shell and the review continued by file reads, and about 135 s was document-gate waiting. Part of the cost after the product gate was diagnosing the Node change, which no part of this order caused; its share was not measured. The alternative was to record a failing document gate without knowing whether the subject broke it. A cheaper product gate was not available: staging the two new scripts changes the code identity, so no recorded row could match. The reviewer's own raw-HTML slip cost one 5.61 s document run. Final counters remain in ignored receipts and the handoff response.

On this pass the reviewer records the result, commits the reviewed state as a coherent series, pushes `wo-084` and opens its pull request. No merge, push to `main`, tag, release or package publication is performed.
