# WO-016 final review FINAL-001

**Subject:** branch `wo-016`, worktree `DotLn-wo016`, uncommitted working tree, 2026-09-03. `HEAD` and `origin/main` are both `a217d64d3db47b7c1ac25395524fc643db7f2c63`, the PR #23 merge that closed WO-017 and the commit behind the published annotated tag `v0.3.5`; the branch carries no local commits, so the entire subject is the dirty tree. At review open, after this review's own transition: 33 tracked modifications (2,970 insertions / 1,239 deletions against `HEAD` including the two `docs/control` files; 2,957 / 1,231 without them) plus six untracked paths, `packages/skeleton/src/reactor.ts` (754 lines), `docs/LEGAL.md`, `docs/decisions/0006-platform-mechanisms-instance-doctrine.md`, `docs/decisions/0007-presence-is-a-policy-input.md`, `docs/lineage/inspirations.md`, and `docs/verifications/WO-016/VER-001.md` (428 lines). The kernel, `packages/skeleton/src/audit.ts`, `scripts/`, `.claude/`, `package.json`, and `docs/discovery/` are unchanged; `package-lock.json` changes only in the skeleton's version field.

**Authority:** `docs/work-orders/WO-016-skeleton-reactor.md`: a `Model: any capable model` and `Effort: executor xhigh+; verifier xhigh+; reviewer any` header, a `v0.3.6` release classification, a dated observed problem, a two-stage scope discipline, nine acceptance criteria, an evidence gate, a write-back duty, a non-goal list, and eight dated 2026-09-03 receipts (ideation breakout, documentation scope expansion, implementation evidence, owner sovereignty, presence, intake lifecycle, local-settings closeout, and sources/legal) plus a post-ideation executor evidence addendum. Against the activated text (`refs/dotln/checkpoint/WO-016/1`, whose tree equals `HEAD`'s), the order differs in the executor's forward-correction of the planner placeholder to `v0.3.6` in the heading and classification paragraph, the eight receipts, and the addendum; the objective, stages, criteria, evidence gate, and non-goals are as activated.

**Prior reports:** `VER-001` (pass; all nine criteria reproduced independently, seven reactor mutations killed in a scratch copy, Stage 1 byte identity and the Stage 2 link-only delta proven by reconstruction in both directions, six non-blocking findings with a bounded checklist). Complete sequence: one report, no repair episode.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes), **effort `max`**, in Claude Code 2.1.259 with sandboxed Bash; macOS 15.6 on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 installed in this worktree's own dependency tree. WO-016 declares `reviewer any`, so any attested effort including `unknown` would satisfy it; the value is stated because it is true. The effort value is **self-reported**: the harness's own `/model` readout in this session reported the selection of Fable 5.1 at `max` effort, `docs/discovery/environment.json` (unchanged in this change set) records no observed effective-session readback for `claude-code` 2.1.259 and lists `max` among the documented `--effort` selector values, so `self-reported` is the only source the control plane accepts for this harness and the declared-effort gate compares the claim rather than an observation.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.259","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-016 passes.** The order asked that the walking skeleton become an instance of the kernel loop: one pure `Reactor<RuntimeState>` making every decision, a live host that only feeds events and executes intents against fakes, replay that is the kernel's own `replay()` over the same reactor, output proven byte-identical after Stage 1, and Stage 2's canonical links proven to be the only change. The deliverable does that. `seiriReactor` is the only caller of the four kernel deciders and imports only `@dotln/kernel`; the live driver steps every appended event through `replay()` one event at a time, so live and replay are one computation rather than two scripts; `replayScenario(log)` is decode, `replay()`, and a state projection with no branch of its own and no stub; the identity test compares complete `Decision` values and the semantic projections and pins enough absolute values that the re-drilled mutations die; the six payload tampers each fail a named child test; the negative fixture stays `○ unverified`; the CLI has exact coverage for both forms; and `scenario.ts` is 242 lines shorter. All eight output digests, the link-stripping and link-restoring reconstructions, and the seven-mutation drill were reproduced first-hand in this session and agree with VER-001. The eight receipts' documentation is honest about what is candidate, what is unresolved, and what remains unavailable; no code, acceptance, version, or captured evidence changed under them. VER-001's six findings are wording and hygiene; four are corrected by this review inside the documentation lane, one is a code candidate carried forward, and one is recorded and deliberately not applied.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **`scripts/resume.mjs` allocated this report's path, appended `FinalReviewRequested`, and records this verdict through the actor-gated `final-review-result` path.** It is not a deliverable of this order: `git status --porcelain scripts/ .claude/ package.json docs/discovery/` is empty and `HEAD` is `origin/main`, so the script that ran is the one `v0.3.5` shipped. It is a fail-loud append-only log with a regenerated projection, and subject identity below was established with git objects rather than through it.
2. **The evidence file that gates actor attestations is untouched.** `docs/discovery/environment.json` is byte-identical to `HEAD`; its `claude-code` version datum (`2.1.259`, `observed`) was checked against `claude --version` on the machine rather than trusted, and its `harnessReadbackEligible: false` for this harness is why the attestation above is `self-reported`.
3. **This reviewer's own attestation is checked by the same mechanism.** A successful append is evidence that the header above equals the flags, not independent evidence about the subject; the inventory in §The order's own actors was compared by object comparison in this session, not by reading the projection.
4. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser, profile, and surface check** (`scripts/release-notes.mjs`, `scripts/github-body.mjs`, `release check-surfaces`), none of which this order changes. A first draft of the notes was refused because a generic type written as `Decision<RuntimeState>` inside inline code reads as raw HTML under the strict subset; it was reworded and the checks re-run. Their editorial adequacy is the operator's to judge at merge. Because `v0.3.6` is above the latest published tag, these notes become part of the `v0.3.6` edition at release close.

## Dispatch and subject integrity

`docs/control/current.md` named WO-016 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, drift `xhigh -> max`, and checkpoint `9cdd8bd9…` (`refs/dotln/checkpoint/WO-016/4`). Before invoking the transition I read the `resume` mapping in `package.json` (`node scripts/resume.mjs`, unchanged) and confirmed that `scripts/` and `package.json` are byte-identical to `HEAD` and `origin/main`. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-016/FINAL-001.md`, moved the phase to `final-review`, and minted checkpoint `33fae2e084e8fd5b429a736aa850dda92c6ed94c` (`refs/dotln/checkpoint/WO-016/5`). `docs/control/resume.jsonl` is a strict append over `HEAD`: its first 110 lines are byte-identical to `HEAD`'s file, and the appended lines are `WorkOrderActivated` (with `effortDeclarationValidated: true`), `ImplementationReady`, `VerificationRequested`, `VerificationCompleted`, and this review's `FinalReviewRequested`, plus the `FinalReviewCompleted` the pass records. Every event carries `schemaVersion: 1`.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree at review open as tree `32805f49ff09bd8339becbae13f8829d44bf9c87`, and `git diff --stat` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                  |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ |
| `/1` activation (`221fbd70…`)              | every deliverable path; the checkpoint's tree equals `HEAD`'s, so the whole subject is downstream of activation    |
| `/2` implementation-ready (`250e1678…`)    | `docs/control/current.md`, `docs/control/resume.jsonl` (+4), `docs/verifications/WO-016/VER-001.md` (added, 428)  |
| `/3` verify allocation (`8e3aa13b…`)       | `docs/control/current.md`, `docs/control/resume.jsonl` (+3), `docs/verifications/WO-016/VER-001.md` (added)       |
| `/4` verdict (`9cdd8bd9…`)                 | `docs/control/current.md`, `docs/control/resume.jsonl` (+2)                                                        |
| `/5` final-review allocation (`33fae2e0…`) | `docs/control/current.md`, `docs/control/resume.jsonl` (+1)                                                        |

Every deliverable blob, the 31 tracked non-control modifications and the five untracked deliverable files, is therefore identical across the implementation-ready, verify-allocation, and verdict checkpoints and the working tree; the verifier changed nothing in the deliverable, and VER-001 has not changed since its verdict was recorded. All five checkpoints have `HEAD` as their sole parent. The delivered `scenario.ts` still hashes to `sha256:822634c0…92b106e7` and `reactor.ts` to `sha256:86244a1f…e0751d9b2`, the values the executor's payload-drill record and VER-001 both name.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. Beyond `node_modules`, the two build outputs, `.DS_Store`, and `*.tsbuildinfo`, the only ignored path is the intake note this order's receipts name, `docs/intake/notes/WO-016-expanded-ideation-2026-09-03.md` (5,680 bytes, last written 21:03 UTC); the owner-only ZIP beside the project, `DotLn-wo016-intake-20260903T214410Z.zip`, postdates that write, as VER-001 recorded. `.claude/` carries no change, tracked or untracked, and no `.claude/settings.local.json` exists in this worktree.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                          | Report header                                                                                                                         | Declared minimum | Result                          |
| -------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.0","model":"gpt-5.6-sol","effort":"xhigh","source":"self-reported"}`       | there is no executor report; the recorded version and effort agree with the discovery record's `codex-cli` entry                      | `xhigh+`         | `xhigh` meets the floor exactly |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.259","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}` | exactly one column-one header in VER-001, parsed and compared with the event actor as objects: equal                                  | `xhigh+`         | `max` exceeds the floor         |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.259","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`  | the header above                                                                                                                      | `any`            | satisfied                       |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: xhigh -> max`, the one distinct pair the completions produce; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.259; model claude-fable-5-1; effort max; source self-reported` with the same drift line and phase `closed`.

## Final evidence summary

### The unchanged `npm test`, twice, in this worktree

This worktree carries its own lockfile-resolved dependency tree (`node_modules/@dotln/kernel` and `@dotln/skeleton` are the workspace symlinks; Prettier 3.9.6; TypeScript 5.4.5), so the suite ran natively. First run, on the verified tree before any review artifact existed: `All matched files use Prettier code style!`, the GitHub body-profile suite, the eight shell suites (fixture temp-root, publication checker, backup-intake, resume, checkpoint, worktree, release surfaces, release close), then `tsc -b --force` and `# tests 110 / # pass 110 / # fail 0 / # skipped 0 / # cancelled 0`, exit 0. Second run, after this review's corrections and package were in place, with the same result (§Corrections applied by this review).

### Static and structural checks

- `git diff --check`: exit 0, no output, before and after this review's artifacts.
- `node scripts/check-publication.mjs`: `PASS index coverage: 165/165 product headings indexed`, `PASS dual-voice links`, both editions `CURRENT` (27 and 42 linked source sections); the six new candidate sections are indexed at status `vision` and both source locks were refreshed by the executor.
- `node scripts/release.mjs check-surfaces` on the working tree: `PASS release-block: observed v0.3.6; expected v0.3.6 (work-order target v0.3.6; latest published v0.3.5)`, `PASS component-version @dotln/kernel: src unchanged; observed 0.2.0; previous v0.3.5 0.2.0; expected no bump required`, `PASS component-version @dotln/skeleton: src changed; observed 0.3.2; previous v0.3.5 0.3.1; expected a different version`, and the profile rule PASS, reading `no current final-review bodies yet` before this review's bodies existed and naming `docs/final-reviews/WO-016/PR.md, docs/final-reviews/WO-016/RELEASE-NOTES.md` after.
- Forbidden and adjacent surfaces: `git status --porcelain scripts/ .claude/ package.json docs/discovery/ packages/kernel/` is empty; `package-lock.json` changes only in the skeleton's version field (`0.3.1` to `0.3.2`); `packages/skeleton/src/audit.ts` is byte-identical to `HEAD` by `cmp`; no dependency was added.
- The verifier's header equals its control actor by object comparison; VER-001 contains exactly one column-one `**Actor attestation:**` line.

### Output digests, reproduced from a baseline build

The baseline is `git archive a217d64` extracted into the session scratch root with a copy of this worktree's `node_modules` (whose relative `@dotln/*` links resolve to the scratch tree's own packages), built with `tsc -b --force`; the delivered tree was copied (tracked and untracked files, excluding `.git`, `dist`, `node_modules`, and `docs/intake`) and built the same way. `packages/skeleton/dist/src/cli.js` with and without `--audit`, and a scratch driver calling `runScenario` on the shipped fixture and `renderAuditProjections` over its log, were captured in both trees.

| Surface           | Baseline (`main`)                                   | Delivered tree                                      | Agrees with                                                              |
| ----------------- | --------------------------------------------------- | --------------------------------------------------- | ------------------------------------------------------------------------ |
| default CLI       | 26 lines, 888 B, `sha256:a27376e2…d2730874e`        | 26 lines, 888 B, `sha256:25a406b0…294a63befe`       | receipt Stage 1 and VER-001 on both sides                                |
| `--audit` CLI     | 1,027 lines, 28,866 B, `sha256:e928623b…37014b89d7` | 1,082 lines, 30,687 B, `sha256:601cf325…c7a995f3b0` | receipt Stage 1 and VER-001 on both sides                                |
| JSONL log         | 21 lines, 8,050 B, `sha256:6090a8e3…21a71ab11f`     | 21 lines, 8,805 B, `sha256:44f92c90…c107ab22e2`     | receipt Stage 1 and Stage 2 and VER-001                                  |
| audit projections | 999 lines, 27,976 B, `sha256:76c92943…76ef2785a65`  | 1,054 lines, 29,797 B, `sha256:45f12c35…0ce972de5a4` | receipt Stage 1 and VER-001; the receipt's Stage 2 value had 65 hex digits |

The default CLI differs from `main` in exactly one line, the banner `v0.3.1` to `v0.3.2`; `sed` normalization of that line reproduces the baseline digest `a27376e2…` for the default form and the receipt's recorded Stage 2 digest `b6fbb9c6…4988b39cd` for `--audit`. The Stage 1 claim in the receipt was measured before the manifest bump and is qualified accordingly (§Corrections applied by this review).

### The Stage 2 delta is link-only, proven in both directions

A scratch script stripped `causationId` and `correlationId` from both logs and compared them: byte-identical. It rendered the audit projections over both stripped logs with the delivered build (`audit.ts` being byte-identical to `main`): byte-identical. It restored the baseline's link values into the delivered log: the result equals the baseline log byte for byte, and its audit output equals the baseline audit output byte for byte. Counts: the log gains `causationId` on 17 events (0 to 17) and `correlationId` on 11 (3 to 14); the audit projections' link-field occurrences move from 0 to 27 and 9 to 27. With every line containing either link field removed, the two audit outputs differ in exactly two places: the correlation groups (the command group grows by four record ids and a new `evt_18` group holds two) and the single association value `derived-single-request-in-scope` to `explicit-event-link` on the verification record. The structural-refusal record still asserts `derived-same-episode-time-adjacency` in the audit suite, which is the retained limit the README, ledger, and capability table name.

### Mutation re-drill

VER-001 reproduced the executor's drill against the shared reactor; this review did not inherit that either. In the scratch copy of the delivered tree, each mutation below was applied alone to `packages/skeleton/src/reactor.ts`, the copy rebuilt with `tsc -b --force`, the 29-test skeleton suite re-run, and the file restored byte-for-byte (confirmed by `cmp` against the pristine copy after the last mutant; the worktree's `reactor.ts` was never touched).

| #   | Mutation                                                                  | Compiled | Result                                                                                                                                                                          |
| --- | ------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | grant `decisionIndex: 1` to `9`                                           | yes      | killed; 14 failures led by `WO-016 AC3`, `WO-016 AC6`, both `WO-016 AC8` CLI tests, and the three audit projection suites                                                       |
| 2   | `verified: … accepted === true` to `verified: true`                       | yes      | killed by `WO-016 AC7` and the `VerificationCompleted.accepted` child of `WO-016 AC4`                                                                                           |
| 3   | `candidates` to `[]` in the `CommandResult` fold                          | yes      | killed by `WO-016 AC3` and `WO-016 step 9`                                                                                                                                      |
| 4   | grant `now: env.now` to `now: 0`                                          | yes      | killed by `WO-016 AC3` and two audit suites                                                                                                                                     |
| 5   | `evidence` argument of the deletion `authorize()` call to `[]`            | yes      | **survived**, 29 of 29 green: an equivalent mutant, because `auth_seiri` declares `requiredEvidence: []` and the refusal fires on `effect denied` before evidence is consulted    |
| 6   | recorded `authorizationEvidence: evidence` to `[]`                        | yes      | killed by `WO-016 step 9 preserves deletion paths, evidence, and structural refusal`                                                                                            |
| 7   | forged `workOrder.baseCommit` in the granted state                        | yes      | killed by `WO-016 AC3`                                                                                                                                                          |

Row 5 is the second target VER-001's F4 named and reproduces its result exactly; row 6 is the target the executor's retained `R3.patch` shows the receipt's "dropped authorization evidence" mutant used (there as a replay-only wrapper around the reactor, so the executor's variant was killed by `WO-016 AC3` while the shared-reactor variant here is killed by `step 9`). The six payload tampers are children of the AC4 test that ran green in both `npm test` runs, and each child asserts both a decision inequality at its event and that the identity assertion throws, so a passing run is itself the detection proof; the executor's retained `RESULTS.md` records each of P1 through P6 failing exactly its own child.

### Ideation receipt evidence

- **Verbatim promotion:** the raw intake note (983 normalized words) and all 31 changed or new Markdown files were lowercased, stripped of punctuation, and compared for shared n-grams: zero overlaps at 8 words, and the only 6- and 7-word overlaps are the note's own filename cited as a path in the ledger, the work order, and VER-001. No Direct Draft Fidelity exception was requested for the batch, and none was needed.
- **The one exact reuse:** the executable-shelf quotation that now opens the README is byte-identical to the operator-authored passage in `00-vision.md` after blockquote normalization, and the vision still carries it; the documentation scope-expansion receipt records that provenance.
- **Links:** every relative link and anchor in the 31 changed or new Markdown files resolves (437 links checked with GitHub-style heading slugs; 0 problems), including the new anchors for the commander-mediated, animate-only, owner-sovereign, progressive-absence, intake-reconciliation, and unattended-portfolio candidates.
- **Clean room and secrets:** a pattern scan over the complete diff plus all six untracked files (7,481 lines) finds no credential or key shape, no RFC1918 address, internal hostname, or email address, no commercial ticketing or ALM product name, and no employer, gateway, or managed-host description; the only literal match is VER-001's own sentence explaining that `rally` inside "structurally" is not a hit. The predecessor system is `v1` throughout.
- **Candidate discipline:** every new product section is titled `Candidate —` and indexed at `vision` in `audience-status-index.md`; ADR-0006 and ADR-0007 are `Accepted` records with empty `Amendments` sections and each states that WO-016 adds no runtime mode, schema, wildcard, settings change, or package split; the ledger's ten new sections carry lifecycle statuses (`adopted`, `preserved`, `transformed`, `superseded`) and `git diff docs/lineage/idea-ledger.md` contains zero removed lines across 453 added ones.

## Acceptance criteria

| #   | Criterion                                                                                                                       | Verdict | First-hand basis                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `seiriReactor` exported as `Reactor<RuntimeState>`; sole caller of the four deciders                                            | met     | `reactor.ts` read in full: the export on line 615, the only `from` import `@dotln/kernel`, and all seven decider call sites inside it; `grep -rn` over `packages/skeleton/src` and `test` places none elsewhere; the AC1 test re-derives this from source text at run time.                                                                                                                                                                                                              |
| 2   | `replayScenario` is the kernel's `replay()` with no branching and no stubs                                                      | met     | The body is `replay(initialState(), decodeLog(log), seiriReactor, seiriPredicates)` then `projectResult`; no `switch`, `for`, `.find(`, or `fixture`; every returned field derives from the decoded log, folded state, or the reactor's decisions; `adapterEffects`, `adapterDispatches`, and `recoveredCommands` moved to `LiveScenarioResult`.                                                                                                                                       |
| 3   | Identity over the complete decision sequence plus timeline, glyph scene, work order, candidates, verified                       | met     | `semanticProjection` deep-strict-equals `decisions` (each `{state, intents, continuation?, schedules, trace}`), `timeline`, `glyphScene`, `workOrder`, `candidates`, `verified`, `cancelledScheduleIds`, and `activeScheduleIds`; the test also pins 21 decisions per side, the grant trace with `now:1200000`, the revocation branch, the five `DecisionRecorded` sources, and the candidate list.                                                                                    |
| 4   | Seven replay mutations and six payload tampers each fail a named test; the tautological trace-tamper test is replaced           | met     | Seven-row re-drill above; six named AC4 children each assert inequality at the tampered event and that identity throws; the old assertion that a forged trace payload leaves `traces` unchanged is deleted.                                                                                                                                                                                                                                                                             |
| 5   | Stage 1 `cmp` identity for log, CLI stdout, and audit projections                                                               | met     | The four baseline digests reproduce the receipt's Stage 1 values from an independent build; the delivered default CLI differs only by the mandated banner and normalizes to the baseline digest; the receipt is now worded to say so.                                                                                                                                                                                                                                                  |
| 6   | Every consequential event carries `causationId`; every audit delta itemized as link-only                                        | met     | 17 of 21 events carry a cause; the four without one are the exogenous task creation, loadout, and two presence changes; every cause points to a strictly earlier event (AC6 test pins all 21 pairs); the two-direction reconstruction and the non-link diff above itemize the delta to correlation groups and one association value.                                                                                                                                                  |
| 7   | `verified=true candidates=1` still printed; a negative fixture drives `verified=false` and `○ unverified`                       | met     | Both captured CLI outputs end `verified=true candidates=1`; the AC7 test's one-file referenced fixture pins `candidates: []`, `verified: false`, the `○ unverified` glyph, and `{accepted: false, candidateCount: 0}` in live, replay, and folded state; mutation 2 above breaks it.                                                                                                                                                                                                    |
| 8   | Exact automated CLI coverage for both forms                                                                                     | met     | `cli.test.ts` spawns the built `cli.js` with and without `--audit`, asserting exit 0, empty stderr, and exact stdout against a re-derived expectation that reads the version from `package.json`; mutation 1 above breaks both.                                                                                                                                                                                                                                                         |
| 9   | `scenario.ts` smaller than on `main`; delta reported                                                                            | met     | 506 lines against 748, the 242-line reduction the receipt reports; `reactor.ts` adds 754 lines of pure decision logic; the AC1 test asserts the direction (against a literal baseline, finding 2 below).                                                                                                                                                                                                                                                                                |

Evidence gate: before-and-after captures with `cmp`, the mutation drill with named failing tests, `npm test` green, and a clean `git diff --check` all exist first-hand in this review and in VER-001. Scope discipline: Stage 1 changed no output byte and Stage 2 changed only links and the one association they justify; the 13-step event sequence, types, and payload shapes are unchanged; `audit.ts` is untouched; the kernel was not modified because the order exposed no kernel gap, and the result says so; `RuntimeState` keeps `rngState` and `policy`. The four carried WO-003 findings are closed: no `fixture` parameter, exact CLI coverage, a negative outcome, and `renderGlyphScene(state)` deriving its six slots from folded flags. Non-goals respected: no transport, compiler, audit change, new Cadence or Program kind, envelope semantics change, SQLite, or UI.

## Write-back duty

- `docs/planning/capability-table.md`: a dated WO-016 reassessment re-checks `skeleton.fake-vertical` and `projection.skeleton-cli` against their blocking gates and keeps both at level 1, naming the still-empty `requiredEvidence`, the deterministic fakes, and the CLI's missing input/error contract rather than claiming the target level.
- `packages/skeleton/README.md`: the component advance, the reactor/host split, the link-first grouping the audit projections consume, the retained adjacency fallback, and the revised evidence list.
- `docs/lineage/idea-ledger.md`: the reactor entry `adopted`, the canonical-lineage entry `adopted`, and the link-first audit fold `preserved`, in a section that states the entries do not predeclare the verdicts.
- `docs/planning/work-order-map.md`: the audit causal-association hardening follow-on is nominated without number, sequence, release, or activation authority, beside the other three unallocated candidates the receipts name; this review brought the map's snapshot, slot bullet, and WO-016 row current (§Corrections applied by this review).

## Ideation breakout receipts

Eight receipts name the review this closeout owes them; this review performed it on each receipt, the promoted surfaces, and the refreshed publication projections.

- **Running worlds and commander tactics:** `11-proteino.md` frames continuity as modeled routines advancing "under a declared world-time and run policy" and explicitly declines to select hosting, pace, pause, persistence, or engine; the commander section says "No option acts merely because the commander suggested it"; the non-cognitive object section refuses anthropomorphism, says "a strong suggestion is not automatic installation," and requires the temporal rule to name its source events, equivalence, reach, reset, clock, lifetime, and effect before replay can reconstruct it; abrasive delivery is described as data, and no abrasive example is filed; the unresolved list is preserved and extended.
- **Documentation scope expansion:** the quotation is exact in both authorized locations; the fitted-gear comparison is the same in the README, vision, architecture skills passage, and roadmap diagnostic, with no mid-comparison switch to support links; the core bet says the bet "is not that this delicate judgment can be exhaustively formulated"; the displaced tagline is retained in the ledger as `preserved`; no historical entry was rewritten.
- **Owner sovereignty:** ADR-0006 keeps the repository's Clean Room and the author's loadout binding here, requires omission to be represented "as absence, not as the same capability under a weaker name," and denies that "unrestricted" can override a provider, harness, OS, destination, or service control plane; the platform/instance table in `03-architecture.md` says its third column is binding and its fourth is not implemented; no permissive mode, wildcard, toggle, package split, or manifest change entered the tree.
- **Presence:** ADR-0007 keeps the four outputs separate, requires real authority growth to be labeled rather than "relabeled as mere scheduling," makes ambient wall time a non-input and no declaration mean hold, and confines the skeleton to "one narrow fixture"; the README, principles 10 and 14, the vision's JARVIS and "what DotLn is not" passages, the domain model's `PresencePolicy` candidate row, the interfaces, pattern library, and architecture presence section all carry the same distinction; the ledger's resolution 6 supersedes the categorical clauses without rewriting them.
- **Intake lifecycle:** `docs/README.md`, the playbook, and the execution guide say plainly that backup archives only its own checkout and that reconciliation is manual; the note exists only in this worktree and is protected by the closeout refusal; the obligation to reconcile before removal is carried as open in the receipt, the ledger, the PR body, and this report.
- **Local settings:** the routing is asymmetric exactly as described in the playbook, execution guide, harness security guide, ledger, and WO-018's dated scope clarification: the exact root path only, never `.claude/**`, worktree copies still non-disposable, no executable change in WO-016, and the current warning honestly described as expected until WO-018 lands.
- **Sources and legal:** `inspirations.md` opens with the non-novelty posture, keeps five knowledge states, includes ambient and forgotten families and an exact-expression review register, and separates inspiration from copied expression, dependencies, and notices; `LEGAL.md` states no-license as the observed interim status, lists six decision gates, compares directions without selecting one, and closes by directing consequential choices to qualified counsel; both manifest claims are true on the machine (root and skeleton `private: true`, kernel not, no `license` field anywhere); the four dev-dependency licenses match the lockfile; no `LICENSE`, SPDX field, or publication guard was added, which is correct because the receipt holds them for the nominated follow-on.
- **Implementation evidence and addendum:** every claim was re-measured; the three wording defects VER-001 found are corrected below; the addendum's gates (110 tests, 165 headings, `check-surfaces`, `git diff --check`) reproduce.

## Verification-sequence soundness

VER-001 meets every duty the order and the guide place on it: it establishes subject identity through checkpoints, discloses the instrument and the one place it touches the subject, confirms all nine criteria with its own commands, reproduces the executor's captures from a baseline build rather than taking them on report, proves the link-only delta in both directions, drills the seven reactor mutations itself and records the equivalent mutant it found, screens the raw note for verbatim promotion, digests all eight receipts, and names its findings with a bounded checklist that expands no scope. Every count it reports was re-derived here (110 tests, 165 headings, 21 events, 26/1,027/999 and 26/1,082/1,054 lines, all eight digests, 17 and 11 new links, the 0-to-27 and 9-to-27 audit link counts, 453 added and 0 removed ledger lines, 24 against 5 over-width principle lines, 983 note words with zero 8-gram overlaps) and all agree. No VER-001 statement is contradicted by this review's evidence.

## Adjudications

### 1. The malformed Stage 2 audit digest (VER-001 F1)

Confirmed: the receipt's value has 65 hex characters and the measured SHA-256 of the delivered audit projections is the 64-character `45f12c35…0ce972de5a4`, reproduced here from an independent build. The first 63 characters agree, so this is a transcription slip, not a different artifact. **Corrected in place** and recorded in the work order's new dated final-review section.

### 2. "Stage 2 leaves the default CLI byte-identical" (VER-001 F2)

Confirmed literally false for the delivered tree by one line, the `v0.3.1` to `v0.3.2` banner that `check-surfaces` requires, and true when the executor captured it before the manifest bump. **Qualified in place** so the claim is reproducible from the delivered tree; AC5 is met on the substance because normalizing the banner reproduces the baseline digest.

### 3. ADR-0001 edited in place as well as amended (VER-001 F3)

Confirmed against `HEAD`: the executor appended a dated `## Amendments` section, which the sources receipt describes and the guide permits, and also rewrote the `## Consequences` paragraph, which the guide's "Settled is settled" rule does not permit ("never an in-place edit"). The amendment already carries the narrowing in full, so the in-place rewrite added no meaning the record lacked. **Restored** the `Consequences` paragraph to its `HEAD` bytes and kept the amendment; the ADR now differs from `HEAD` only by the appended section.

### 4. Two targets for "dropped authorization evidence" (VER-001 F4)

Confirmed by reading the executor's retained `R3.patch`: the drilled target was the recorded `authorizationEvidence` state projection, applied as a replay-only wrapper so that `WO-016 AC3` killed it, which is exactly what the receipt claims. The `authorize()`-argument variant survives (row 5 above) because the envelope requires no evidence and the refusal is decided by `effect denied` before evidence is consulted; that is an equivalent mutant, not an escaped defect. **The receipt now names its target and the equivalent variant.**

### 5. The AC1 size assertion pins a literal 748 (VER-001 F5)

Correct and cosmetic; it proves the AC9 direction today and stops tracking if the baseline moves. The fix is a test change, which is code, so it is outside this review's authority. **Carried** as a candidate for the next skeleton order.

### 6. Prose-width regression in the principles (VER-001 F6)

Confirmed: 24 non-table lines exceed 80 columns against 5 on `main`, the longest 104, and two more in the vision. Prettier passes under `proseWrap: "preserve"`. Rewrapping would change publication-locked source bytes in two audience editions and produce a large cosmetic diff in a document whose content this review is otherwise not touching. **Recorded, not applied.**

### 7. The receipt's "each fail AC3" claim for the replay-side mutants

VER-001's drill and this review's re-drill mutated the shared reactor, where the natural killers are the tests that pin absolute values, and both found `step 9` and `AC7` killing some rows rather than `AC3`. The executor's retained patches show a different formulation: R1 through R7 wrap `seiriReactor` inside `replayScenario` only, so live and replay diverge and `AC3` catches every one, which matches both the receipt and AC4's own wording ("the seven `replayScenario` mutations"). The two designs are complementary and both are recorded. **No correction owed.**

### 8. Live/replay identity holds by construction

`LiveReactorDriver.feed` steps each appended event through the kernel's `replay()`, and the kernel derives `env` from state and event inside its loop, so incremental and batch replay are one computation. That is precisely the structural property the objective asked for, and it means the identity assertion alone cannot kill a reactor mutation; the killing force is the pinned absolute values in the same test and the payload tampers, which the re-drill confirms empirically. **Ratified; a planning note for WO-008**, whose criterion 6 will swap a compiled loadout into this reactor and should keep pinning absolutes.

### 9. The executor's forward-correction of the release classification

The activation helper left the planner placeholder in the H1 and classification paragraph, the sixth consecutive order with that gap; the executor corrected it in place to `v0.3.6` and disclosed the correction. `v0.3.5` is the latest published tag locally and on `origin`, `v0.3.6` is the next patch strictly above it, the README block carries `v0.3.6`, the kernel is unchanged at `0.2.0`, the skeleton moves `0.3.1` to `0.3.2` because its sources changed, and `check-surfaces` proves all three rules. The append-only activation event was neither replayed nor edited. **Ratified.**

### 10. The stale work-order map cells

The snapshot line, slot bullet, and WO-016 row read "implementation-ready" and "independent verification pending", true when the executor wrote them and false beside a passing VER-001 and this report. Following WO-013, WO-024, WO-025, WO-019, WO-027, and WO-017 FINAL-001 §Corrections, they now record VER-001 pass, FINAL-001 pass, the reviewed state awaiting operator merge, and the separately authorized release close that tags `v0.3.6`. **Corrected.**

### 11. The kernel was not modified

The scope discipline allowed kernel changes "only for gaps this order exposes, each noted in the result." None was needed: `replay()`, `Reactor`, the predicate registry, and the four deciders carried the scenario as shipped by WO-017, and `git status packages/kernel/` is empty. The result names no kernel gap because there was none. **Consistent with scope.**

### 12. `renderGlyphScene` before any presence event

`main`'s event-sniffing renderer showed `○ dormant` before any `OperatorPresenceChanged` and `◌ dormant` after return; the state-derived renderer shows `◌ dormant` for the initial `returned` presence too. Only the final scene is rendered anywhere (CLI, tests), and for the shipped scenario and the negative fixture it is byte-identical. **Not a defect.**

## Corrections applied by this review

Four corrections, all in the documentation lane `docs/final-reviews/README.md` grants, proven contained by diffing against checkpoint 5 (the ADR, the work order, and the map are the only deliverable paths that changed, each with the intended hunks and nothing else after `prettier --write`), and followed by a re-run of the checks they could affect: the full `npm test` (which runs the pinned `prettier --check`), `git diff --check`, `publication:check`, and `check-surfaces`, all clean.

1. **`docs/decisions/0001-personal-clean-room-build.md`:** the `## Consequences` paragraph restored to its `HEAD` bytes; the dated `## Amendments` entry kept. The ADR's diff against `HEAD` is now the appended section only.
2. **`docs/work-orders/WO-016-skeleton-reactor.md`:** the Stage 2 audit digest corrected to the measured 64-character value; the Stage 2 default-CLI sentence qualified with the version-banner exception and its normalization result; the mutation-drill bullet naming the `authorizationEvidence` projection as the drilled target and the `authorize()`-argument variant as an equivalent mutant; and a new dated section, `## Final-review evidence corrections — 2026-09-03`, recording all three changes with their VER-001 finding numbers so the receipt's provenance stays legible.
3. **`docs/planning/work-order-map.md`:** the snapshot line, the slot bullet, and the WO-016 row's state and eligibility cells, as adjudication 10 describes. No other row changed.

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5, and therefore to the tree VER-001 verified.

## Findings

**None blocking.** No acceptance criterion is unmet and no behavioral or evidence defect was found in the deliverable. The items below are recorded and not scored.

1. **The audit fold still infers the structural-refusal association from adjacency.** By design of this order; the link-first fold with a labeled strict fallback is nominated in the map without a number.
2. **The AC1 size assertion pins the literal `748`** (VER-001 F5): a one-line code candidate for the next skeleton order.
3. **Twenty-four principle lines and two vision lines exceed 80 columns** (VER-001 F6): convention drift under `proseWrap: "preserve"`, recorded rather than rewrapped for the reason in adjudication 6.
4. **The `authorize()` evidence argument is dead for this fixture.** With `requiredEvidence: []` the guard never consults it, so no test can bind it; a fixture with a non-empty evidence requirement would make it load-bearing. A note for WO-008 or the authority corpus, not a defect here.
5. **The raw ideation note lives only in this worktree.** Backed up after its last write; manual reconciliation into the main checkout's intake is owed before the worktree is removed, and the close path refuses until then.
6. **The activation-preflight gap is now six orders long** (WO-024, WO-025, WO-019, WO-027, WO-017, WO-016): each activated with the planner's placeholder in the H1 and each executor forward-corrected it. For the operator's planning pass, not for this review.
7. **The executor's model and effort live in the control log and the verifier's report, not in a committed executor artifact.** The `ImplementationReady` event records them; that is the WO-019 mechanism working as designed, recorded as prior reviews recorded it.

### Candidates considered and not sustained

- _AC5 fails because the delivered default CLI is not byte-identical to `main`._ Not sustained: the one differing byte sequence is the banner that `check-surfaces` mandates, normalization reproduces the baseline digest, and the receipt now says so.
- _The reactor throws on unexpected input instead of deciding._ Not sustained: the throws (`away cadence did not produce a pulse`, `deletion unexpectedly authorized`, `operator return did not revoke inspect authority`) assert fixture invariants loudly at the point where a silent wrong decision would otherwise be recorded; replay of a foreign log fails rather than fabricating a decision.
- _`expectedInspectCommandId` is declared after the functions that use it._ Not sustained: it is a module-level `const` referenced only at call time after module evaluation, and every call site is exercised by the suite.
- _The map's WO-020 row still says "WO-016 merged, unsatisfied."_ Not sustained: it is true until the operator merges.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step returned to the operator.** `gh` in this sandbox fails before its root command exists (`failed to read configuration: open ~/.config/gh/config.yml: operation not permitted`), so `npm run worktree -- publish` refuses at its `gh` preflight after the committed surface check and body validation and before any push, fail closed, as for WO-006, WO-007, WO-013, WO-024, WO-025, WO-019, WO-027, and WO-017. The reviewed commit is made here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation, and notes parse run against the exact commit; it refuses at the `gh` preflight and pushes nothing. The operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, fast-forward the main checkout to the merge commit first (`git pull --ff-only origin main`), then run `npm run release -- close WO-016 --publish` from it, outside the sandbox and where `gh` is authenticated. Because `v0.3.6` is strictly above `v0.3.5`, the command runs `npm ci` and the evidence gate, assembles the five-section edition from the reviewed notes, creates and pushes the annotated `v0.3.6` tag, and creates the matching GitHub Release. The release-influence guard will still report the main checkout's local settings file until WO-018 lands; that warning is expected and true.
- **Attribution:** the reviewed commit carries a plain subject and no attribution trailer, per the operator's standing rule (`~/.claude/CLAUDE.md`) and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins.
- **Ledger duty:** discharged by the executor's ten sections; this review's corrections are wording and add no design idea, so no entry is owed.
- **Instruction conflict flagged, not acted on:** the execution guide asks that verifiers and reviewers prefer instruments that fail loudly; this review's one non-loud instrument was its own effort attestation, which is a claim by construction, and the source label says so.

## Proposed PR

- **Title:** `:recycle: WO-016: skeleton reactor; one pure reactor makes every decision live and in replay, derived events carry canonical links, and the docs separate platform from personal doctrine; v0.3.6`, the `:recycle:` shortcode being the gitmoji for restructuring code without changing its observable behavior, which is exactly Stage 1's proof, with Stage 2's links and the documentation promotions in service of it.
- **Body:** `docs/final-reviews/WO-016/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commit (it also makes the map's "FINAL-001 pass" cell true); the reviewed state is committed with a plain subject. The operator then runs, from this worktree outside the sandbox:

```bash
npm run worktree -- publish WO-016 --title ':recycle: WO-016: skeleton reactor; one pure reactor makes every decision live and in replay, derived events carry canonical links, and the docs separate platform from personal doctrine; v0.3.6' --body-file docs/final-reviews/WO-016/PR.md
```

The helper runs `check-surfaces --committed WO-016` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-016`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, fast-forward the main checkout and run `npm run release -- close WO-016 --publish` from it; the expected outcome is the annotated `v0.3.6` tag and its GitHub Release. Before this worktree is removed, the raw intake note must be reconciled into the main checkout's intake by hand.

## Method

Single reviewer, single session, no subagents. Read order: the execution guide's dispatch, ideation, closeout, discipline, and model-specific sections, the control projection and log, the work order in full including all eight receipts and the addendum, VER-001 in full, the final-review and verification READMEs, the WO-017 final review with its PR body and notes for the package shape and publish precedent, then every changed file in full or by diff (`reactor.ts` and the new `scenario.ts` in full beside `main`'s `scenario.ts`, the three test diffs, the skeleton README, the manifests and lockfile, the two new ADRs, `LEGAL.md`, `inspirations.md`, the ledger additions, the README, and the eleven product, two planning, three publication, one harness-security, one playbook, one docs-index, and one WO-018 diffs), with the publish, surface, notes, and body gates read from `worktree.mjs` and `release.mjs` rather than inferred. The executor's four retained scratch roots were read for the mutant patches and results; the ignored intake note was used only as the n-gram screen's input and nothing from it was promoted. Subject integrity was established through git objects. Evidence was re-established first-hand: two full `npm test` runs, the static, surface, and publication checks, a baseline build of `main` and a scratch build of the delivered tree for the eight digests, the two-direction link isolation and the non-link audit diff, a seven-mutation re-drill in the scratch copy, the verifier header comparison, the quotation comparison, the link and anchor sweep, the prose-width measurement, and the clean-room scan. Authority used: four documentation corrections and the three package artifacts; no edit to any code, test, contract, schema, or acceptance byte. The scratch trees lived in the session scratch root. Nothing was restored from a checkpoint, no destructive git command was used, and no remote, tag, or `gh` mutation was performed; the publish helper's own `gh` preflight after the reviewed commit fails closed, leaving the push and the PR to the operator.
