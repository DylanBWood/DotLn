## What this merges

WO-016, the `v0.3.6` skeleton-reactor order: the walking skeleton becomes an instance of the kernel loop. One pure `seiriReactor`, typed as the kernel's `Reactor` over `RuntimeState`, makes every decision the 13-step scenario makes, the live host owns only fakes and effects, and replay is the kernel's own `replay()` over the same reactor and log, so "same log, same decisions" is a structural property checked over complete `Decision` values. Every derived skeleton event carries a canonical `causationId` and, where one exists, a `correlationId`. The order also promotes an operator-opened ideation batch into documentation only. `@dotln/skeleton` advances to `0.3.2`; `@dotln/kernel` stays at `0.2.0`; no dependency is added and nothing under `scripts/` or `audit.ts` changes.

- **Stage 1, reactor without changing a byte of output:** `packages/skeleton/src/reactor.ts` (new, 754 lines) owns the four kernel deciders and imports only `@dotln/kernel`; `scenario.ts` falls from 748 to 506 lines and keeps the fakes, the append loop, and effect execution; `replayScenario(log)` is decode plus `replay()` plus a state projection with no event-type branch and no stubbed adapter fields. The Stage 1 capture reproduces `main`'s default CLI, `--audit` CLI, JSONL log, and audit projections byte for byte.
- **Stage 2, canonical links:** 17 events gain `causationId` and 11 gain `correlationId`; ids, types, order, and payloads are unchanged. Stripping both fields from the new log reproduces the old log and the old audit bytes; restoring the old link values into the new log reproduces both again. The only non-link audit change is the verification record's association, `derived-single-request-in-scope` to `explicit-event-link`; the structural-refusal record keeps its labeled adjacency fallback and `audit.ts` is byte-identical to `main`.
- **Evidence:** the identity test compares the complete decision sequence plus timeline, glyph scene, work order, candidates, verified flag, and schedule ids live versus replayed and pins the grant trace, the revocation branch, and the five recorded-decision sources; six payload tampers each fail a named child test; a negative fixture pins `verified=false` and `○ unverified` in live, replay, and glyph state; `cli.test.ts` asserts exact stdout and empty stderr for both public forms; a source-reading test proves decider ownership, branch-free replay, and the size reduction.
- **Carried WO-003 findings closed:** the unused `fixture` parameter, missing CLI output coverage, no negative outcome, and `renderGlyphScene` sniffing event types instead of folded state.
- **Write-backs:** the capability table keeps `skeleton.fake-vertical` and `projection.skeleton-cli` at level 1 with a dated reassessment; the skeleton README; the README release block at `v0.3.6`; ledger entries for the reactor, the links, and the retained audit limit; the work-order map nominates the link-first audit-hardening follow-on without number or sequence.
- **Ideation breakouts (documentation only):** ADR-0006 separates platform mechanisms from the author's personal-instance doctrine and the product docs label claims accordingly; ADR-0007 makes presence a policy input with four separate outputs and supersedes the categorical no-growth clauses without rewriting them; `προτείνω` gains running-world continuity, a candidate commander-mediated tactical loop, and capability-based object behavior as a replayable temporal rule; `docs/lineage/inspirations.md` and `docs/LEGAL.md` are new; ADR-0001 gains a dated amendment; the README leads with the operator-authored executable-shelf quotation and a compact thesis; the intake-lifecycle and local-settings receipts route their executable work to a nominated helper and to WO-018. Eight receipts in the work order name the authority, source treatment, promoted surfaces, and unresolved choices for each.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in this worktree on the verified tree and again on the reviewed tree: Prettier clean, the body-profile unit suite, all eight shell suites, `tsc -b --force`, 110 of 110 node tests, exit 0. `git diff --check` clean; `publication:check` 165 of 165 headings indexed with both audience locks `CURRENT`; `git status --porcelain scripts/ .claude/ package.json docs/discovery/` empty.
- `node scripts/release.mjs check-surfaces`: `PASS release-block: observed v0.3.6; expected v0.3.6`, `PASS component-version @dotln/kernel: src unchanged; observed 0.2.0`, `PASS component-version @dotln/skeleton: src changed; observed 0.3.2; previous v0.3.5 0.3.1`, and the body profile PASS over this package's `PR.md` and `RELEASE-NOTES.md`.
- Output digests reproduced from a baseline build of `main` (`a217d64`) and a scratch build of the delivered tree: all four baseline digests equal the receipt's Stage 1 values, and all four subject digests equal VER-001's measured values; the default CLI differs from `main` in exactly one line, the `v0.3.1` to `v0.3.2` banner, and normalizing that line reproduces the baseline digest. Stripping both link fields makes the logs and the audit outputs byte-identical; restoring the baseline links into the subject log reconstructs the baseline log and audit bytes by `cmp`.
- Mutation re-drill in a fresh scratch copy of the delivered tree, seven reactor mutations applied one at a time and rebuilt: wrong grant `decisionIndex`, forced `verified`, emptied candidates, grant `now := 0`, emptied recorded evidence, and a forged work order are each killed by the named tests; the `authorize()`-argument evidence variant survives, confirming VER-001's finding that it is an equivalent mutant under `requiredEvidence: []`.
- Subject integrity: a temporary-index tree of the working tree differs from the implementation-ready checkpoint only in the two control files and VER-001, so the verifier changed nothing in the deliverable and the deliverable did not change between implementation-ready and review; the control log is a strict append over `HEAD`; the verifier's report header equals its control-log actor by object comparison; the executor's `xhigh` and the verifier's `max` meet the declared `xhigh+` floors.
- Ideation receipts: zero exact 8-word overlaps between the raw intake note and the 31 changed or new Markdown files; the executable-shelf quotation is byte-identical in the README and the vision after blockquote normalization; every relative link and anchor in the changed Markdown resolves; the pattern scan over the complete change set found no credential, internal host, tracker product name, or employer material; every new product section is titled `Candidate —` and indexed at `vision`.

## Verification trail

- `docs/verifications/WO-016/VER-001.md`: pass; all nine criteria reproduced independently, seven reactor mutations killed, the link-only delta proven in both directions, six non-blocking findings.
- `docs/final-reviews/WO-016/FINAL-001.md`: pass; the verifier's findings adjudicated with reasons, three receipt-wording corrections and one ADR restoration applied and re-checked, the work-order map brought current, method and instruments disclosed.

## Known open items

- **The audit fold still infers the structural-refusal association from adjacency.** This order forbade changing `audit.ts`; the link-first fold with a labeled strict fallback is nominated in the work-order map without a number.
- **The AC1 size assertion pins the literal `748`.** It proves the direction today and stops tracking if the baseline moves; deriving it from `main` or dropping it in favor of the reported delta is a one-line candidate for the next skeleton order (VER-001 F5).
- **Twenty-four principle lines exceed 80 columns** after the layer labels were inserted; Prettier passes under `proseWrap: "preserve"`, and rewrapping would move publication-locked source bytes, so it is recorded rather than applied (VER-001 F6).
- **The raw ideation note lives only in this worktree's ignored intake.** It was backed up after its final entry; reconciling it into the main checkout's intake is manual and must happen before the worktree is removed, and the normal close path refuses until then.
- **The release-close guard will still report the main checkout's local settings file** until WO-018 lands its exact-path exemption; the warning is expected and true.
- **Publish and release close.** The `gh` preflight fails closed inside the reviewer's sandbox, so the operator runs the publish command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, `npm run release -- close WO-016 --publish` from a fast-forwarded main checkout creates and pushes the annotated `v0.3.6` tag and its GitHub Release.
