## What this merges

WO-030, the `v0.7.0` minor above `v0.6.0`: concurrent control state. The control plane now holds zero, one, or many in-flight work orders as independent lifecycles with attributable history. A newly activated order owns one append-only JSONL segment at `docs/control/orders/WO-NNN.jsonl`; an order activated in the historical `docs/control/resume.jsonl`, WO-030 itself included, finishes there, so no event moves and the event schema stays `1`. `foldSegments` in `scripts/lib/control.mjs` keeps one state per order and refuses a segment whose name disagrees with its activation, that carries a foreign `workOrderId`, or that is empty, naming the path and ordinal before any transition can append. The new `scripts/lib/control-store.mjs` reads the working tree or any committed revision through blobs alone, proves append-only byte prefixes per segment, binds selection to the `wo-NNN` branch or the `--work-order` flag, and orders the latest closed display by Git integration rather than by clock. `resume status --json` keeps its fourteen selected-order keys in the same order and appends `orders[]`; `current.md` renders every in-flight order and then the latest closed one; `next` names the other in-flight orders; `worktree finish`, `release close`, release-note attribution, and the generated index read committed segments and select the named order, so one order can finish and release while a sibling stays open. The real-Git fixture in `scripts/test-concurrent-control.mjs` drives three worktrees through serial merges, a finish and a no-publish and publishing release close while another order is verifying, a third branch created before the first merge, and per-release attribution, with the first-parent append-only proof passing at every step. Kernel, compiler, and skeleton `src/` are byte-identical to `v0.6.0`; no dependency was added; the authorized boy-scout change pins `"types": ["node"]` in the three package tsconfigs. The order's two operator-authorized ideation breakouts are included as documentation and planning only: the whole-or-split work-order candidate in the roadmap and the UIFA security, identity, logs, and traceability candidate in the roles document, with ledger entries and publication rows at `vision`.

- **Two concurrent orders, independent phases, byte-identical sibling (criterion 1):** the shipped fixture and the verifier's and this review's own real-Git worktrees each activated two orders, advanced one through readiness and verification, and found the sibling's segment and projection byte-identical; the ladders document's synthetic sequence folds to `WO-901 ready-to-verify; WO-902 active` under the new code and, rerun here through the code extracted from the `v0.6.0` tag, to the documented misattribution.
- **Corrupt segment refuses with path and ordinal; nothing appends (criterion 2):** all thirteen command forms refuse four mutations (foreign activation, foreign later event, missing activation, empty file) with the path and ordinal while every control byte and the projection stay unchanged; a segment whose order already belongs to the legacy file and an invalid segment name refuse too.
- **Legacy fold, `times`, and index equal their `v0.6.0` behavior (criterion 3):** the 153-event activation prefix hashes to the recorded `sha256 2b4c9374…`, the `v0.6.0` fold implementation to the recorded `sha256 4254c61a…`, and that implementation's per-order fold is JSON-identical to the committed comparison, to the new `foldWorkOrders`, and to `foldSegments`, recomputed here from the tag rather than read from the executor's file; the live `times` observation over the now 157-event log is byte-identical to the committed evidence over the first 153 rows with 120 named local refs read; the index generated by `main`'s generator and the branch's over identical control state differs only in the segment-source sentences and the tag snapshot's `controlSegments`.
- **Storage ownership; no order spans two files (criterion 4):** WO-030's own five events append to the legacy file because its activation lives there; new fixture orders each receive their own segment; a scan of every segment finds no order in two files; a reactivated closed order keeps its original file and reactivating an active order refuses without changing bytes.
- **Selection by branch, by flag, and its refusals (criterion 5):** on `main` with two open orders, bare `status` lists both, `status --json` and every transition refuse with `ambiguous work-order selection` naming them, an unknown id refuses naming them, and a duplicated flag refuses with usage; on a `wo-NNN` branch the phrases bind that order without a flag, the flag overrides, and `activate` of a different id refuses with `activation binds`; all reproduced in this review's own fixture.
- **Serial integration, finish, and release attribution (criterion 6):** merged states equal their pre-merge states field for field in the shipped fixture, the verifier's, and this review's (fourteen projected fields per order); `worktree finish` for WO-901 succeeds while WO-902 is open; the no-publish and publishing `release close` for WO-901 validate from the merged revision; the index attributes WO-901 to `v0.2.1` and WO-902 to `v0.2.2` with each tag's segment inventory; the third old-base branch keeps its segment and its append-only proof; the real repository's first-parent history passes the same proof.
- **Multi-order rendering; peers read only JSON (criterion 7):** `orders[]` carries `workOrder`, `phase`, `latestVerdict`, `recordedAt`, and `elapsed` for every known order; `current.md` renders elapsed phases per section; `worktree.mjs`, `release.mjs`, and `work-orders.mjs` contain no reference to `current.md`.
- **Write-backs (criterion 8):** architecture's session-lifecycle section with its dated migration note superseding "concurrent control-log writers are deferred"; the execution guide's `--work-order` rules and between-orders report; the playbook's five-step paired-wave procedure; the domain model's one sentence; the ladders document's discharged feasibility gate; the concurrent plan; the map; two dated ledger sections with zero removed lines; the regenerated index; and the capability table's dated `control.concurrent-orders` row.
- **Suite, whitespace, dependencies, line counts (criterion 9):** `npm test` exit 0 twice in this review; `git diff --check` clean; `package.json` and the lockfile byte-unchanged; `scripts/*.mjs` and `scripts/lib/*.mjs` total 6001 lines on `main` and 7227 here (+1226, of which 851 are test lines), reproduced row for row.
- **Ideation breakouts:** the intake capture in the main checkout hashes to the recorded `sha256 858b8458…` at mode 600, is gitignored and untracked, and the backup archive tests clean with the same member hash; the roadmap candidate states the permanent whole-route exclusion, the stable opaque identity contract, and the separation of hierarchy from scheduling; the roles candidate keeps the five adopted names, separates authentication, authorization, logs, and traceability, and stays under the platform/instance boundary; every link and anchor resolves.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in this worktree on the verified tree and again on the reviewed tree: Prettier clean, the body-profile unit suite, every shell suite including the seven segment `PASS` lines and the four real-Git concurrency `PASS` lines, `check-publication.mjs` at 222 of 222 headings with both audience locks current, `index --check` current, `tsc -b --force`, 187 of 187 package tests, 8 of 8 identity-corpus tests, exit 0. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.7.0; expected v0.7.0`, `PASS component-version` with `src unchanged` for all three components, and the body-profile rule over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity: a temporary-index tree of the working tree differs from the final-review allocation checkpoint only in the two control projections and the regenerated index; from the verdict checkpoint the same; from the verify-allocation and implementation-ready checkpoints additionally in VER-001; every deliverable blob is byte-identical from the implementation-ready checkpoint to the reviewed tree; all five checkpoints have `HEAD` (`7c1ea72`, also `main` and `origin/main`) as their sole parent; `git log main..wo-030` is empty; the work order's text is byte-identical to activation apart from its appended executor outcome and breakout receipt; the verifier's report header equals its control-log actor by JSON comparison; the executor's `max` and the verifier's `max` meet the declared `xhigh+` floors.
- Direct probes: the `v0.6.0` control modules extracted from the tag reproduce the committed legacy fold and the ladders sequence; the live `times` output matches the committed observation byte for byte over the legacy rows; an independent two-worktree real-Git probe reproduced branch-bound activation refusal, sibling byte-identity across two transitions, per-worktree phases, serial merges with only the generated projection conflicting, equal pre- and post-merge projections, the three-section overview, every ambiguous and unknown refusal, `next` naming in-flight orders, the `.DS_Store` and unactivated-branch boundaries, and the first-parent append-only proof; an independent audit of the generated index against the map's marked block and a `v0.6.0` fold agrees on order, labels, check marks, and the `Now` line; 721 relative links and anchors across 23 changed Markdown files resolve, the only dangling target being this review's own not-yet-written report; the clean-room and secret screen over all 3,400 added or new lines found only the receipts' own screening sentences and one public GitHub URL; script line counts reproduce; `codex --version` reports the executor's recorded `0.153.2`.

## Verification trail

- `docs/verifications/WO-030/VER-001.md`: pass; all nine criteria, the evidence gate, and every write-back reproduced independently against the `v0.6.0` tag, in the verifier's own real-Git fixtures, and by re-deriving the executor's numbers; seven non-blocking observations and five disclosures with an empty repair checklist.
- `docs/final-reviews/WO-030/FINAL-001.md`: pass; the verifier's observations and disclosures adjudicated with reasons, the reviewer's own observations recorded, the breakout receipt inspected against its required checks, two bounded documentation corrections applied and re-checked, method and instruments disclosed.

## Known open items

- **A stray non-segment file in `docs/control/orders/` refuses every `resume` command** (VER-001 O1): the refusal names the path and the direction is the safe one, but a Finder-created `.DS_Store` would block even read-only `status` until removed; WO-021 or a small patch should decide whether to ignore exactly that basename, which the worktree-finish classifier already treats as disposable.
- **A `wo-NNN` branch whose order is not yet activated cannot run bare `status`** (VER-001 O2): `worktree start` activates immediately, so the window is normally zero; `--work-order` on another order reads state.
- **The intake backup archive sits in `/private/tmp`** (VER-001 O3): its integrity and member hash were verified again here, and the canonical mode-600 capture in the main checkout is unaffected, but macOS purges that directory; the operator may move it beside the earlier archives in `~/Projects/`.
- **`resume.mjs` and `work-orders.mjs` invoked through a symlinked path exit silently without acting** (this review): their self-invocation guard compares the argument path resolved without symlinks against the module URL Node resolves with symlinks, so a `/tmp` alias on macOS never enters `main()`; the guard is byte-identical on `main`, the real worktree path is canonical, and the shipped suites pin real paths, so this is a pre-existing patch candidate outside this order.
- **`current.md` on `main` can be regenerated only through a legal `next` on some closed order or through a transition** (this review): the playbook's conflict step relies on a closed order existing, which is always true in this repository; `status` warns about a stale projection and never rewrites it.
- **Refusal hints omit `--work-order`** (this review): `requirePhase` prints the corrective command without the flag, so on `main` with several open orders the operator adds it; cosmetic.
- **The index reads control state at every local release tag eagerly** (this review): seventeen tags today, with no measured timing change; recorded for when the tag count grows.
- **The TypeScript 7.0.2 half of the tsconfig change is unverified here** (VER-001 O6): only the pinned 5.4.5 is installed and builds cleanly with the field.
- **The first measured paired wave has not run**: the capability table's dated row stays at level 1, and lane generation, admission control, per-order workflow variation, and beacon emission remain deferred as the order's non-goals state.
- **`CLAUDE_EFFORT` is an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9): still open, so Claude Code attestations remain `self-reported`.
- **Publish and release close.** The bounded publisher `npm run worktree -- publish WO-030` runs the committed surface check and body validation before any push and then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses at that preflight the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.7.0` is strictly above `v0.6.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.7.0` tag, and creates the matching GitHub Release.
