# WO-030 final review FINAL-001

**Subject:** branch `wo-030`, worktree `DotLn-wo030`, uncommitted working tree, 2026-09-05. `HEAD`, `main`, and `origin/main` are all `7c1ea72b244d57cb2eb4ef015076668ab99213f9`, the PR #32 merge of the 2026-09-05 planning pass that filed this order (`git ls-remote origin main` agrees), two commits above the published annotated tag `v0.6.0`, whose object `762620fe…` peels to `0323f7499d8bda5b15f9ba83f87373f1ba72aad1` locally and at `origin`. The branch carries no local commits (`git log main..wo-030` is empty), so the entire subject is the dirty tree. At review open, before the dispatch transition: 47 `git status --porcelain -uall` entries, 38 tracked modifications (against `HEAD`, 1,037 insertions / 329 deletions including the two `docs/control` files and the generated `docs/work-orders/README.md`; 35 files, 991 / 306 without them, measured after this review's corrections) plus nine untracked paths totalling 2,363 lines: `scripts/lib/control-store.mjs` (195 lines), `scripts/test-concurrent-control.mjs` (403), `scripts/test-control-segments.mjs` (359), `docs/verifications/WO-030/VER-001.md` (471), and `docs/evidence/WO-030/` (`README.md` 97, `legacy-fold.json` 626, `legacy-times.json` 166, `concurrency.txt` 38, `segments.txt` 8). The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree taken before the dispatch (`206c9f2658b9781b4495c2dd9a124a78b6829e02`) differs from checkpoint `refs/dotln/checkpoint/WO-030/4` (the verdict) only in `docs/control/current.md`, the strictly appended `docs/control/resume.jsonl`, and the regenerated index (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, two bounded documentation corrections (§Corrections applied by this review), and the lifecycle's own events.

**Authority:** `docs/work-orders/WO-030-concurrent-control-state.md`: `Model: any capable model`; `Effort: executor xhigh+; verifier xhigh+; reviewer any`; a `v0.7.0` minor classification above the published `v0.6.0`, pinned by the 2026-09-05 planning pass under the operator's opt-out default, with the root README release block named as an executor deliverable; planner-synthesized nomination provenance with a locally preserved compaction-safety capture; dependencies WO-018, WO-028, and WO-026, all closed and released (`v0.4.1`, `v0.5.1`, `v0.5.2`); a recommended placement before WO-021; the cited sections and helpers; an objective; a dated observed gap; a design section with segments, fold, selection, integration and release, checkpoints and time, and two declined alternatives; an authorized bounded boy-scout item (`"types": ["node"]` in three package tsconfigs); deliverables; nine numbered acceptance criteria; an evidence gate; a write-back duty; a non-goal list; four operator-review assumptions; and two appended receipts dated 2026-09-05: the executor outcome and the operator-opened ideation breakout receipt. Against the activated text (`refs/dotln/checkpoint/WO-030/1`) the order differs by 125 insertions and 0 deletions, all of them the two appended sections; the text at activation is byte-identical to `main`'s copy; the objective, observed gap, design, boy-scout item, deliverables, all nine criteria, the evidence gate, the write-back duty, the non-goals, and the assumptions are byte-identical to activation.

**Prior reports:** `VER-001` (pass; all nine criteria, the evidence gate, and every write-back reproduced independently: the `v0.6.0` control modules extracted from the tag and run over the real 153-event activation prefix, the `times` observation recomputed against real local refs, the index generated twice over identical state with `main`'s and the branch's generator, the verifier's own real-Git worktrees, merges, and a third old-base branch, a 33-commit first-parent append-only proof over the real repository, a link sweep, a capture-hash and backup check, and a clean-room screen; seven non-blocking observations O1–O7 and five disclosures D1–D5 with an empty repair checklist). Complete sequence: one report, no repair episode. The two receipts inside the work order are executor evidence and were treated here as VER-001 treated them: claims under test.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes) in Claude Code 2.1.261 with sandboxed Bash; macOS on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 in this worktree's own dependency tree. Reasoning effort `max`: the session's model control reported it at launch and the harness exposes it to the shell as `CLAUDE_EFFORT=max`, but `docs/discovery/environment.json` records `effectiveEffortReadback` for `claude-code` as `not found` with `harnessReadbackEligible: false`, so `harness-readback` is not admissible and the epistemic source is `self-reported`, exactly as VER-001's was. `2.1.261` was checked against `claude --version` on this machine and is the newest entry on the harness's observed `versions` list (observed 2026-09-04); `max` is on `sessionEffortSelector.values`; the discovery record is byte-identical to `main`, so the attestation is admissible on merged evidence alone. The order declares `reviewer any`, so any attested effort would satisfy it; `max` is stated because it is what the session reports.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-030 passes.** The order asked the control plane to hold zero, one, or many in-flight work orders as independent lifecycles with attributable history: one append-only segment per newly activated order, the historical log frozen in place with every ordinal kept, legality consulted only for the named order, selection bound to the `wo-NNN` branch or an explicit flag, a selected-order JSON projection that keeps its contract and adds an overview of every known order, a Markdown projection of every in-flight order, per-segment append-only and release attribution proofs, a proven serial integration of concurrent orders, and the write-backs that make the docs say so. The deliverable does that. `scanControl` keeps one state per order and `foldSegments` refuses a mis-owned, foreign, empty, or split segment with its path and ordinal before any transition can append; the new store reads the working tree or any revision through blobs alone and proves byte prefixes per file; selection binds the branch, honors the flag, and refuses ambiguity and unknown ids while changing no byte; `status --json` keeps its fourteen selected-order keys in their original order and appends `orders[]`; `current.md` lists every in-flight order and the latest closed one by Git integration order; `next`, `worktree finish`, `release close`, release-note attribution, and the generated index select the named order from committed segments. The `v0.6.0` tag's own fold over the real 153-event activation prefix is JSON-identical to the new fold and to the committed comparison; the live `times` observation is byte-identical to the committed evidence over those rows; the ladders document's synthetic misattribution reproduces under the tag's code and disappears under the new code; this review's own two-worktree real-Git probe reproduced branch-bound activation, sibling byte-identity, independent phases, serial merges with equal pre- and post-merge projections, every selection refusal, and the first-parent append-only proof; kernel, compiler, and skeleton sources are byte-identical to the base; no dependency was added; the suite is green twice; whitespace is clean. The ideation breakout receipt's promoted surfaces were digested as subject and are consistent with each other, with the settled decisions, and with the five adopted UIFA names; every speculative surface is labeled candidate or preserved. VER-001's seven observations are one operational hazard with the safe refusal direction, one selection boundary, one backup-location risk, one publication-status decision, one correct projection behavior, one unverifiable toolchain claim, and one proof-shape change that is strictly stronger; this review adjudicates all seven as non-blocking, decides the publication-status question, records seven observations of its own (one of them a pre-existing silent guard outside this order), corrects two documentation projections, and changes no code, contract, acceptance, schema, authority, or captured-evidence byte.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment. Unlike the WO-020 review, where the lifecycle helpers were merged code, here nearly every instrument is modified subject.

1. **The fold and the store are the deliverable and also dispatched this review.** `scripts/lib/control.mjs`, `scripts/lib/control-store.mjs`, and `scripts/resume.mjs` allocated this `FINAL-001` path, minted checkpoint 5, will record this verdict, and produced every `status` reading above. Compensating controls: the three control modules of the `v0.6.0` tag (`git show v0.6.0:scripts/lib/…`, the fold implementation hashing to the recorded `sha256 4254c61a…8221a84`) were run in this session over the real activation prefix and over the ladders sequence and compared with the new code by `JSON.stringify` equality; subject identity was established with git objects (temporary-index trees and `git diff-tree`) rather than with the helpers' own projection; the appended `FinalReviewRequested` line was read raw and its fields, ordinal, and strictly increasing `recordedAt` checked by hand; the `final-review` and `final-review-result` cases, `selectionArgs`, `selectWorkOrder`, `latestClosedOrder`, `appendTransition`, and `parseActor` were read in full before the transition ran.
2. **The executor's fixtures are executor-authored.** `scripts/test-control-segments.mjs` and `scripts/test-concurrent-control.mjs` grade the deliverable with the deliverable, and the committed `legacy-fold.json` and `legacy-times.json` were written by the executor. Compensating controls: the fold comparison was recomputed from the tag rather than read from the file; the `times` comparison was made against the live command over real local refs rather than the suite's Git stub; and an independent real-Git probe written for this review (one `main`, three worktrees, real branches and merges, no shipped test) reproduced criteria 1, 2 (through the refusals it exercises), 4, 5, 6's core, and 7.
3. **The index generator is modified subject and produced the index this review regenerated.** `scripts/work-orders.mjs` changed in this order, and `npm run work-orders -- index` ran here at dispatch as the guide requires, so the helper graded its own output. Compensating control: a script written for this review parsed the marked sequence block out of `docs/planning/work-order-map.md` and the checklist, `Now` line, and Active block out of the generated README, folded `docs/control/resume.jsonl` with the `v0.6.0` control module, and found order, labels, every check mark, the `Now` line, and the WO-030 block (`final-review`, VER-001 pass, FINAL-001 pending) in agreement. `scripts/test-work-orders.mjs` grades synthetic fixture repositories, so its assertions are not circular.
4. **The surface check, the publish helper, the notes parser, and the body profile are partly subject.** `scripts/release.mjs` changed (its control readers and close messages) while `scripts/release-notes.mjs` and `scripts/github-body.mjs` are byte-identical to `HEAD`; `scripts/worktree.mjs` changed in `finish` and its imports while `publish` is unchanged. The publish flow (`check-surfaces --committed`, tracked-body read, body profile, notes parse, `gh` preflight, push, `pr create`) and the close flow were read from source rather than inferred.
5. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile** through `check-surfaces` and by calling `parseReleaseNotes` and `assertGitHubBodyProfile` directly; both passed on the first attempt. Because `v0.7.0` is above the latest published tag, these notes become the `v0.7.0` edition at release close.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-030 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-05T06:26:56.588Z`, `elapsed` `{"implementation":4170929,"verification":1153091}`, checkpoint `85f62936780e43d23aa80f709aa36f36011b5151` (`/4`), and `orders[]` with 22 entries; no projection-disagreement warning was emitted. Before invoking the transition this review read the `resume` mapping in `package.json`, the complete `scripts/resume.mjs` and `scripts/lib/control-store.mjs` as they stand in the subject, and the `final-review` and `final-review-result` cases. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-030/FINAL-001.md` (the directory itself is created by the reviewer), moved the phase to `final-review`, appended `FinalReviewRequested` with `recordedAt` `2026-09-05T06:33:30.343Z` as line 157 of `docs/control/resume.jsonl` because WO-030's activation lives there, and minted checkpoint `b99a10aea90798e345a9918c2e32266df43dc16e` (`refs/dotln/checkpoint/WO-030/5`). `docs/control/orders/` does not exist in this checkout, as the design requires for an order whose activation predates the layout. The legacy file is a strict append over `HEAD`: its first 152 lines are byte-identical to `HEAD`'s file, and the five appended events are `WorkOrderActivated` (`04:54:10.350Z`, `effortDeclarationValidated: true`), `ImplementationReady` (`06:03:41.279Z`), `VerificationRequested` (`06:07:43.497Z`), `VerificationCompleted` (`06:26:56.588Z`), and this review's `FinalReviewRequested`; every event carries `schemaVersion: 1` and the stamps strictly increase. The projected `implementation` span (4,170,929 ms) and `verification` span (1,153,091 ms) are the arithmetic differences of the recorded stamps.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree after the allocation and index refresh as tree `8ff67f85e5a179f058cc2f97245f060bd986e210`, and `git diff-tree -r --name-status` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                                                                                                                                                           |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`f77e9fde…`)              | every deliverable path (47 paths: 38 modified, 9 added); the whole subject, the work order's own receipts included, is downstream of activation                                                                                                                              |
| `/2` implementation-ready (`045c4890…`)    | `docs/control/current.md`, `docs/control/resume.jsonl`, `docs/work-orders/README.md`, `docs/verifications/WO-030/VER-001.md` (added); nothing else, so the ideation breakout and its write-backs were already in the readiness tree, as the receipt states                    |
| `/3` verify allocation (`b4b484b7…`)       | `docs/control/current.md`, `docs/control/resume.jsonl`, `docs/work-orders/README.md`, `docs/verifications/WO-030/VER-001.md` (added)                                                                                                                                         |
| `/4` verdict (`85f62936…`)                 | `docs/control/current.md`, `docs/control/resume.jsonl`, `docs/work-orders/README.md` (regenerated by this review at dispatch)                                                                                                                                                |
| `/5` final-review allocation (`b99a10ae…`) | `docs/control/current.md`, `docs/control/resume.jsonl`, `docs/work-orders/README.md`                                                                                                                                                                                         |

Every deliverable blob is therefore identical across the implementation-ready, verify-allocation, and verdict checkpoints and the working tree; the verifier changed nothing outside its report except the regenerated index (VER-001 D1); VER-001 has not changed since its verdict was recorded. All five checkpoints have `HEAD` as their sole parent. `refs/dotln` held 144 refs after the allocation; the completion mints `/6`.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. `docs/intake/{chats,images,notes}` in this worktree each hold only `.gitkeep`, and `git status --porcelain --ignored` lists no ignored path outside `node_modules/`, the three `packages/*/dist/` outputs, and `.tsbuildinfo` files; there is no `.claude/settings.local.json` here. `.claude/settings.json` is tracked and byte-identical to `main`; `.gitignore`, `package.json`, `package-lock.json`, `docs/decisions/`, and `docs/discovery/environment.json` are byte-identical to `main`. One empty `.claude/.cc-writes` directory exists at the worktree root; git does not track empty directories, and the temporary-index `add -A` listed nothing under `.claude/`.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                         | Report header                                                                                                                                                                                                                                                                                                                                      | Declared minimum | Result                  |
| -------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ----------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.2","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`     | there is no executor report; the outcome states the operator selected the model and effort and that this is not effective readback; `codex --version` reports `codex-cli 0.153.2` on this machine, `0.153.2` is on the `codex-cli` observed `versions` list, and the persisted `model_reasoning_effort` selector records `max` for that version, so `max` was admissible at append | `xhigh+`         | `max` exceeds the floor |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}` | exactly one column-one header in VER-001, parsed and compared with the event actor as JSON in this session: equal                                                                                                                                                                                                                                    | `xhigh+`         | `max` exceeds the floor |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`  | the header above                                                                                                                                                                                                                                                                                                                                   | `any`            | satisfied               |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: none` and the JSON form's `effortDrift` is `[{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.261; model claude-fable-5-1; effort max; source self-reported`, the same drift line, phase `closed`, a `finalReview` elapsed value beside the two existing values, and legal actions `release-close, next, activate`.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree after the dispatch transition and the index refresh, before any review artifact or correction existed: `All matched files use Prettier code style!`, `GitHub body profile tests passed`, the shell suites (fixture temp-root, publication checker, backup-intake, resume including its time-blind `cmp` lines and the seven segment-suite `PASS` lines, checkpoint, worktree including `finish refused reopened merged order and preserved its closed subject`, release including the four real-Git concurrency `PASS` lines and the fixture Git log, and work-orders), `PASS index coverage: 222/222 product headings indexed` with both audience editions `CURRENT` (30 and 45 linked sections), `PASS docs/work-orders/README.md is current`, `tsc -b --force`, `# tests 187 / # pass 187 / # fail 0 / # skipped 0`, and the identity corpus `# tests 8 / # pass 8 / # fail 0`, exit 0. Second run, after this review's two corrections and the three package artifacts were in place, with the same result.

### Static, surface, and structural checks

- `git diff --check` clean before and after this review's edits.
- `npm run release --silent -- check-surfaces`, before and after the package artifacts existed: `PASS release-block: observed v0.7.0; expected v0.7.0 (work-order target v0.7.0; latest published v0.6.0)`; `PASS component-version` with `src unchanged; expected no bump required` for `@dotln/compiler` `0.2.0`, `@dotln/kernel` `0.2.1`, and `@dotln/skeleton` `0.6.0`; `PASS github-body-profile`, first with no bodies and then listing `docs/final-reviews/WO-030/PR.md` and `RELEASE-NOTES.md`.
- `package.json`, `package-lock.json`, and every `packages/*/package.json` are byte-identical to `main`; no dependency was added; the only `packages/` change is the one `"types": ["node"]` line in each of the three tsconfigs.
- `docs/lineage/idea-ledger.md`: `git diff --numstat main` reports 62 insertions and 0 deletions; the insertion is two dated sections placed newest-first above the 2026-09-05 planning-pass section. `docs/decisions/` is untouched. Under `docs/work-orders/`, only WO-030's file and the generated README changed.
- The work order's diff against its activation checkpoint is 125 insertions and 0 deletions, beginning at `## Executor outcome — 2026-09-05`.
- Every relative link and anchor in the 23 changed or new Markdown files resolved at review open: 721 targets, zero missing files, zero bad anchors (GitHub slugs recomputed from the target headings, fenced code excluded), the single dangling target being this report's own allocated path, exactly as VER-001 O5 predicts for a projection of control state.
- Clean-room and secret screen over all 3,400 added or new lines at review open (the diff's `+` lines plus every untracked file): no ticketing or ALM product name, predecessor-system name, managed-host or gateway description, internal hostname, private address, e-mail address, credential shape, or private-key marker; the only URL is the public GitHub Releases page already in the README, and the only term hits are the ledger's, the receipt's, and VER-001's own sentences describing their screens plus the substring "rally" inside "behaviourally" in VER-001.
- `codex --version` reports `codex-cli 0.153.2` on this machine, matching the executor's harness version in the control log.
- Script line counts against `main` at `7c1ea72…`, computed independently over every `scripts/*.mjs` and `scripts/lib/*.mjs`: 6,001 → 7,227 (+1,226), matching the executor's table row for row, with 851 of the added lines in the two new test files and the index fixture.

### Independent probes against the deliverable

- **Legacy fold recomputed from the tag (criterion 3, instrument 1).** `scripts/lib/control.mjs`, `control-time.mjs`, and `git.mjs` were extracted from `v0.6.0` (commit `0323f74…`) into the session temporary directory; the extracted `control.mjs` hashes to the `foldImplementationSha256` recorded in `legacy-fold.json`, and the first 153 lines of the live `docs/control/resume.jsonl` hash to its `sourceSha256`. The tag's `foldWorkOrders` over those 153 events is `JSON.stringify`-identical to the committed `orders` and `current`, to the new `foldWorkOrders`, and to `foldSegments` (22 orders; WO-030 `active`). The ladders sequence (activate WO-901; activate WO-902; implementation ready for WO-901) folds under the tag's `fold` to `WO-902 ready-to-verify`, under the tag's `foldWorkOrders` to `WO-901 active; WO-902 ready-to-verify`, and under the new `foldWorkOrders` to `WO-901 ready-to-verify; WO-902 active`.
- **`times` compared byte for byte (criterion 3).** `npm run resume --silent -- times` over the live 157-event log exited 0 with empty stderr; its first 153 event rows are `JSON.stringify`-identical to `legacy-times.json`'s 153 rows, `localRefsRead` is 120 in both, and the four later rows are WO-030's `recordedAt` events with no `segment` field, as legacy rows must be.
- **Two-worktree real-Git probe (criteria 1, 2, 4, 5, 6, 7).** A repository with a closed WO-900 in its legacy log, a `main`, and worktrees `wo-901`, `wo-902`, and later `wo-903`, driven only through the subject's `resume.mjs` and store: `activate WO-902` on branch `wo-901` refused with `activation binds WO-901; use --work-order WO-902 to override`; two WO-901 transitions (`implementation-ready`, `verify`) left WO-902's segment and `current.md` `sha256`-identical; each worktree's `status --json` reported its own order and phase (`WO-901 verifying`, `WO-902 ready-to-verify`) and held only its own segment; merging `wo-901` then `wo-902` into `main` conflicted only on the generated `current.md`, regenerated by `next --work-order WO-900`; the selected-order projections of both orders on merged `main` equalled their pre-merge worktree projections across all fourteen fields; bare `status` on `main` rendered three sections (WO-901, WO-902, then closed WO-900); `status --json` and `verify` on `main` refused with `ambiguous work-order selection; open orders: WO-901, WO-902`; `--work-order WO-999` refused with `unknown work order WO-999` and the same list; `next --work-order WO-900` named `Other in-flight orders: WO-901, WO-902`; a `.DS_Store` in `docs/control/orders/` made `status` refuse with `invalid control segment name at ordinal 1` (VER-001 O1); bare `status` on the unactivated `wo-903` branch refused with `unknown work order WO-903` while `--work-order WO-901` read state there (VER-001 O2); `addedSegmentEvents` passed over every first-parent commit of the fixture; `latestClosedOrder` on `main` returned WO-900; and `times` labeled the five segment rows with their file and local ordinals while the six legacy rows kept their global ordinals.
- **Index audit (instrument 3).** Nine sequence entries in the map's marked block match the README's proposed order in order and label; every check mark corresponds to a closed order in the `v0.6.0` fold (21 closed, none of the queued entries); the `Now` line reads `[WO-030] — final-review.`, equal to the fold's non-closed set; the Active block records VER-001 pass and FINAL-001 pending.
- **Intake capture and backup (receipt).** In the main checkout, `docs/intake/notes/WO-030-expanded-ideation-2026-09-05.md` hashes to the recorded `858b8458926d8bed33db88c1e2b8f5156413e9f00a7f33ad2e20af10b0462255`, is mode `0600`, is attributed by `git check-ignore -v` to `.gitignore:4 docs/intake/**`, and is unknown to git; the backup `/private/tmp/DotLn-wo030-intake-20260905T053955Z.zip` is mode `0600`, tests clean with `unzip -t`, holds the three `.gitkeep` placeholders and the capture, and its member hashes to the same value after extraction into the session temporary directory only. The main checkout is on `main`, clean, at `7c1ea72…`.

### Reading

The complete diff of every changed file and every new file was read: the two control modules and the new store, the resume, worktree, release, work-orders, and release-records changes, the two new suites and the five updated suites, the evidence README and transcripts, the work order with both receipts, VER-001, both READMEs, the playbook, the six product documents, the four planning documents, the ledger, the publication index and both edition locks, `.prettierignore`, and the three tsconfigs. The publish, surface, notes, and body gates were read from `worktree.mjs`, `release.mjs`, `release-notes.mjs`, and `github-body.mjs`, and the actor admissibility rules from `resume.mjs`. The ignored intake was hashed and stat'ed, never displayed or copied.

## Acceptance criteria

**1. Two concurrent orders, independent phases, byte-identical sibling; the ladders sequence folds correctly. PASS.** The shipped real-Git fixture, VER-001's fixtures, and this review's probe each show two worktrees with independent phases and a sibling segment and projection unchanged by the other order's transitions; the ladders sequence result was reproduced under the tag's code and the new code (§Independent probes).

**2. Corrupt segment refuses with path and ordinal; every transition refuses and appends nothing. PASS.** The shipped suite drives all thirteen command forms across four mutations and asserts every control byte and the projection unchanged; VER-001 repeated three mutations by hand; this review's probe reproduced the invalid-name refusal and confirmed by reading `foldSegments`, `controlFromSources`, and `main()` that every command folds before any branch of the switch can reach `append`.

**3. Legacy fold, `times`, and index equal their `v0.6.0` behavior. PASS.** Recomputed from the tag in this session, field for field and byte for byte; the index clause is VER-001's three-hunk comparison, which this review accepted after confirming the generator diff contains exactly those three textual changes plus the `controlSegments` snapshot field.

**4. Storage ownership; no order spans two files. PASS.** WO-030's own five events sit in the legacy file, `docs/control/orders/` does not exist here, the probe's new orders each received their own segment, and the shipped suite scans every segment and proves reactivation retains the original file.

**5. Selection by branch, by flag, and its refusals. PASS.** Every refusal and binding in the criterion reproduced in this review's probe with the exact messages, plus the `activate` binding refusal.

**6. Serial integration, finish, and release attribution. PASS.** Merged states equal pre-merge states in the shipped fixture, VER-001's, and this review's; finish while a sibling is open, the no-publish and publishing close, per-release attribution, and the third old-base branch are proven by the shipped real-Git fixture inside `npm test` twice here, whose Git log this review read; the first-parent append-only proof passed in this review's probe and, in VER-001, over the real repository's history.

**7. Multi-order rendering; peers read only JSON. PASS.** `orders[]` and the sectioned `current.md` were observed on the real repository and in the probe; `grep` finds no reference to `current.md` in `worktree.mjs`, `release.mjs`, or `work-orders.mjs`; `projectOrder`'s literal is byte-identical to `main`'s `statusProjection` body.

**8. Write-backs. PASS.** All present and accurate; see the section below.

**9. Suite, whitespace, dependencies, line counts. PASS.** As detailed above; the boy-scout change is exactly the three tsconfig lines.

## Evidence gate

Every named item exists and was reproduced: the fixture transcripts for criteria 1 through 7 (`docs/evidence/WO-030/segments.txt` and `concurrency.txt`, each matching the `npm test` output line for line in both runs, plus VER-001's and this review's own fixtures); the committed legacy-fold comparison (recomputed from the tag, identical) and the `times` comparison (recomputed against real local refs, byte-identical over the legacy rows); the integration fixture's Git log (printed inside both `npm test` runs and read); and `npm test`.

## Write-back duty

Criterion 8's surfaces all landed and are consistent with the code and with each other:

- **03 §Session lifecycle & resilience** describes segments, ownership by activation location, per-order legality, branch and flag selection, `orders[]`, the sectioned `current.md`, the Git-integration-order rule for the latest closed display, the checkout-scoped view, and a dated **Segment-layout migration (2026-09-05, WO-030)** block that names and supersedes "concurrent control-log writers are deferred" while retaining one writable agent per worktree; the WO-028 timing paragraph gains the per-order sentence and the `times` paragraph the segment and ordinal rules.
- **07 §Operator resume phrases** documents branch binding, `--work-order`, the one-open-order default, bare-`status` listing, the refusals, `orders[]`, the segment fold, and the between-orders `next` report; §Workflow closeout and §Discipline are consistent with it.
- **`docs/PLAYBOOK.md` §Concurrency** replaces the old limitation with the five-step paired-wave procedure and serial-integration rule and links the fixture; §Resume command surface and §When things break follow.
- **02-domain-model.md** gains the one-sentence control-plane paragraph; **10-ir-compatibility.md** records the layout as a storage axis; **06-roadmap.md** says "selected order" and records the ladder limitation as superseded and the per-order model as implemented.
- **`budget-window-work-order-ladders.md`** re-dates its observation as `Observed at v0.6.0` and discharges the feasibility gate for this slice; **`concurrent-work-orders-plan.md`** records the implementation update; the **map** updates WO-030, WO-010, WO-011, WO-021, WO-022, and the preserved-candidates bullet (two of its sentences corrected here, §Corrections).
- **Ledger:** two dated sections, 62 insertions, 0 deletions.
- **`docs/work-orders/README.md`** is regenerated and `index --check` passes; the **capability table** adds a dated `control.concurrent-orders` row at level 1, "staged pending independent review", in a new dated section that leaves earlier assessments untouched.
- Also landed and honest: `docs/README.md` (directory map with `docs/evidence/`, resuming section), the root `README.md` release block at `v0.7.0` with a rewritten summary and one added "Proven" bullet, `.prettierignore`, the publication rows and both edition source locks, and the three tsconfigs.

## Ideation breakout receipt

The receipt names the checks the verifier and final reviewer must make. Taking them in order, each was re-inspected here rather than inherited from VER-001:

- **Source treatment.** The canonical capture's hash, mode, ignore attribution, and untracked status, the empty worktree intake, and the backup's integrity and member hash are as recorded (§Independent probes). The receipt correctly declares ordinary raw ideation under the saved Clean Room loadout rather than a ready-to-file draft, and no intake byte appears in any committed surface.
- **Permanent whole-route exclusion.** 06's candidate states that starting the first child "permanently excludes execution of that original whole-order route, including after a child fails or is abandoned", that the aggregate status "must not claim that the original whole-order implementation ran", and that planning or previewing a split does not select it; the ledger entry repeats it.
- **Stable historical identities.** 06: a suffixed child label "does not replace the existing opaque `WO-NNN` identity contract, renumber historical orders, or establish priority"; the ledger: suffix labels "do not select a new ID grammar".
- **Hierarchy separated from scheduling.** 06: "Hierarchy and execution order answer different questions… A family of orders does not make all its work safe to run at once."
- **Acceptance and evidence coverage.** 06: the umbrella "maps its acceptance criteria to those increments and exposes uncovered work", with child review, integration, release, and completion "separately evidenced".
- **Five adopted UIFA names versus speculative additions.** The diff of 13 adds only the candidate section and one cross-reference sentence in the showrunner role; the candidate says "the five confirmed names above remain the adopted set" and that the one-hat and two-hat groupings are "possible groupings from this synthesis, not selected titles"; 02's diff touches no `Role` primitive.
- **Authentication, authorization, log, and audit distinctions; ADR-0006.** The candidate's table separates the four concerns by question; the prose adds that diagnostics do not establish authorization or prove acceptance, that a title confers no access or effect authority, and that the responsibilities remain implementation-selected "under the platform/instance distinction, with this repository's existing boundaries still in force"; 09's inserted paragraph reuses its concern boundaries without merging them; `docs/decisions/` is untouched.
- **Links, candidate status, publication freshness.** Both new sections are `vision` in the audience index; both edition locks are `CURRENT`; coverage is 222 of 222; every link and anchor resolves.
- **Preservation of the pre-breakout implementation.** The checkpoint table shows the readiness tree already contained the breakout's write-backs and that the deliverable is byte-identical from readiness to the reviewed tree; `npm test` passes twice; the receipt's statement that the breakout is not authority to implement either candidate is honored (no command, event type, schema field, or ADR was added by it).

## Verification-sequence soundness

One verification, one pass, no repair episode. VER-001 was allocated at event 155 and its verdict recorded at event 156 with a header equal to the event actor; its elapsed span is 1,153,091 ms. The verifier changed exactly one subject file outside its report, the regenerated index, disclosed in D1 and required by the guide; every other deliverable blob is identical across the verify-allocation and verdict checkpoints. The verifier's compensating controls were reproduced here rather than accepted: its tag extraction by this session's own, its `times` comparison by a live comparison, its fixtures by an independent probe, its index comparison by an independent audit, and its link sweep, hash checks, and clean-room screen by recomputation. Its D2 disclosure (a first `npm test` reading taken through a pipe, then re-run) is honest reporting, and both runs here were captured without a pipe.

## Adjudications

### 1. Any stray file in `docs/control/orders/` refuses every `resume` command (VER-001 O1)

Reproduced with a `.DS_Store` in this review's probe. The refusal direction is the safe one (an unknown file might hold events), the message names the path, the fix is deletion, and only the working-tree view is affected because `.DS_Store` is gitignored and revision views read blobs. The hazard is real on macOS once the directory exists, which happens at the next new activation. Non-blocking; the worktree-finish classifier in `scripts/lib/paths.mjs` already treats a `.DS_Store` basename as disposable, so ignoring exactly that basename in `readControl` would be a consistent small patch, or WO-021 may decide when it sweeps directories it does not own. Not applied here: it changes reader behavior and no verification covers it.

### 2. A `wo-NNN` branch whose order is not yet activated cannot run bare `status` (VER-001 O2)

Reproduced. `worktree start` activates immediately, so the window is normally zero; `--work-order` on another order reads state. A boundary, recorded.

### 3. The intake backup archive is in `/private/tmp` (VER-001 O3)

Verified again (integrity, four members, matching member hash, mode `0600`). The earlier archives sit in `~/Projects/` as `DotLn-intake-<stamp>.zip`. The sandbox cannot write outside this worktree and its temporary directory, so moving the archive beside its siblings is an operator action; the canonical mode-600 capture in the main checkout is unaffected.

### 4. Two publication rows were lowered from `verified` to `implemented` (VER-001 O4)

Decided and applied (§Corrections). `git log` over the audience index shows both rows `verified` continuously from 2026-09-02 (`76486da`) through the planning-pass commit, across WO-026's and WO-028's material changes to the same two sections, so lowering them was this executor's caution rather than a repository rule. The changed claims of those sections (branch and flag selection, the refusals, `orders[]`, the between-orders report, the closeout wording) are exactly what VER-001 and this review verified against behavior, so `verified` is the accurate classification at merge. The two lines were restored to their byte-exact `main` form; the formatter and `check-publication.mjs` pass.

### 5. The generated index links this report before it exists (VER-001 O5)

The same holds here after the `final-review` dispatch. Correct projection behavior; the link resolves as of this file.

### 6. The TypeScript 7.0.2 half of the boy-scout item is unverified (VER-001 O6)

Same finding here: only the pinned 5.4.5 is installed, the forced rebuild is clean with the field, and `@types/node` is the only `@types` package, so the field is inert today. The 7.0.2 claim remains an executor observation and is labeled as such in the notes.

### 7. The append-only proofs changed shape to a raw byte prefix (VER-001 O7)

Agreed after re-reading: `controlFromSources` parses every segment of both revisions before `addedSegmentEvents` compares prefixes, and `readIndex`'s `closeTime` compares a line-sliced prefix of a parsed committed segment, so the byte check is always downstream of a successful parse; the shipped suite proves an equivalent-JSON rewrite refuses. Strictly stronger than the per-event equality it replaced.

### 8. `resume.mjs` and `work-orders.mjs` invoked through a symlinked path exit silently (this review)

Found while building the probe under the macOS `/tmp` alias: both scripts run `main()` only when `pathToFileURL(resolve(process.argv[1])).href === import.meta.url`, but Node resolves the main module's symlinks while `resolve` does not, so every command exited 0 with no output and no effect. The guard is byte-identical on `main`, `/Users/dylanwood/Projects/DotLn-wo030` is canonical, the shipped suites assert `realpathSync(root) === root`, and `worktree.mjs` and `release.mjs` use a different entry guard. Pre-existing and outside this order; nominated as a small patch (compare real paths, or refuse loudly). The probe was rerun from the real path and every check passed.

### 9. `current.md` on `main` is regenerated only through a legal `next` on some closed order or a transition (this review)

The playbook's conflict step relies on a closed order existing, which is always true in this repository (21 closed) and was true in this review's probe; in a fresh repository with only in-flight orders the projection would stay stale, and `status` warns without rewriting it. A boundary, recorded.

### 10. `readIndex` reads control state at every local release tag eagerly (this review)

Seventeen `readControl(root, refs/tags/…)` calls per index run, replacing the previous lazy `taggedControl`; VER-001 measured 1.20 s against 1.25 s, so no cost today. Recorded for when the tag count grows.

### 11. Refusal hints omit `--work-order` (this review)

`requirePhase` prints `commandFor(action)`, which never carries the flag; on `main` with several open orders the operator adds it. Cosmetic.

### 12. `worktree finish` gained an `origin/main` closed-state guard (this review)

A strengthening with its own worktree-suite case (a reopened integrated order refuses and its closed subject is preserved). It is the committed-segment read the design section requires of finish and close, not a scope expansion, and it changes no precondition for an order that is closed in both places.

### 13. VER-001 counts twelve new `PASS` lines (this review)

This review counts eleven `PASS`-prefixed lines from the two new suites (seven and four); the worktree suite's new reopened-order line is not `PASS`-prefixed. Immaterial to any criterion.

### 14. The verifier's disclosures (VER-001 D1–D5)

D1 (index regenerated by the verifier) is the guide's requirement and left no other trace; D2 (the piped first reading, then a clean re-run) is honest and the clean result is what the report records; D3 (fixtures outside the checkout, no ref or remote touched) is the same method this review used; D4 (read-only hashing of local-only intake) is repeated here under the same discipline; D5 (the `v0.6.0` modules run under this checkout's Node) is the same method this review used and the criterion needs only same-input-same-fold, which holds. No action.

### 15. Smaller consequences recorded without action

The capability table's dated row, the ladders document's "staged for independent verification and final review", and the concurrent plan's "pending independent verification and final review" are dated 2026-09-05 statements about the source at that time and are left as written, as the WO-020 review left the capability table's identical phrasing; the next planning pass owns their refresh, and the map (the dated human projection the guide says to revalidate) is corrected here instead. `.prettierignore`'s new line keeps `legacy-times.json` byte-exact against the CLI output. The executor outcome's numbers (6,001 → 7,227 with 851 test lines; 187 and 8 tests) all reproduce. The `fatal: failed to store` line printed by `git ls-remote` is the sandboxed keychain helper, not a fetch failure; the remote refs were returned.

### Candidates considered and not sustained

- **`fold(events)` now returns the last event's order rather than a reset-on-activation single state.** Not sustained: the order retains `fold` for a single order's events, the tag comparison proves single-order equivalence, every caller passes single-order events or uses `foldWorkOrders`/`foldSegments`, and the multi-order behavior it replaced was the documented misattribution.
- **`statusProjection` re-serializes an event array through JSON.** Not sustained: only the array form used by tests takes that path; the JSON round trip is lossless for these events; lifecycle peers pass the store's object.
- **`latestClosedOrder` chooses by Git integration order, so the closed order shown on `main` is not the most recently closed by clock.** Not sustained: a display decision the architecture document states explicitly ("timestamps never order segments"), not a lifecycle input.
- **`validateManifest` now selects the manifest's order at `HEAD` and would refuse a manifest naming an order absent from control.** Not sustained: every forward manifest names a control-closed order, `v0.2.0` has no manifest by design, and the release suite's validate fixtures pass.
- **The candidate for split work orders reads like a scheduler.** Not sustained: it states that hierarchy and execution order answer different questions and implements nothing.

## Corrections applied by this review

Two corrections, both in the documentation lane `docs/final-reviews/README.md` grants, applied by exact string replacement with a script that refused unless each old string occurred exactly once, proven contained by diffing against checkpoint 5 (map: 11 insertions and 9 deletions across three sentences; audience index: 2 insertions and 2 deletions), and followed by a re-run of the checks they could affect: the pinned `prettier --check` on both files, `node scripts/check-publication.mjs` (222 of 222, both locks `CURRENT`), the full `npm test`, `git diff --check`, and `check-surfaces`, all clean.

1. **`docs/planning/work-order-map.md`:** the §Recommendation sentence that said WO-030's source "stages its fixture evidence for independent verification", the wave sentence that said "independent review and merge remain before the actual paired trial", and the preserved-candidates bullet that said "pending independent verification and final review" now record that WO-030 passed independent verification and final review at this revision and awaits operator merge, in the same form the WO-020 review used. No table row changed, so nothing reflowed.
2. **`docs/publication/audience-status-index.md`:** the rows for 07 §Operator resume phrases and §Workflow closeout and releases return from `implemented` to `verified`, restored as the exact bytes of `main`'s lines (adjudication 4).

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet, no guard was lost, and no behavioral or evidence defect was found in the deliverable that any criterion covers. The items below are recorded and not scored.

1. **A stray non-segment file in `docs/control/orders/` refuses every `resume` command** (adjudication 1): patch candidate or WO-021 input.
2. **An unactivated `wo-NNN` branch cannot run bare `status`** (adjudication 2): boundary.
3. **The intake backup archive sits in `/private/tmp`** (adjudication 3): operator may move it.
4. **Symlinked invocation of `resume.mjs` and `work-orders.mjs` is a silent no-op** (adjudication 8): pre-existing patch candidate.
5. **`current.md` on `main` needs a closed order or a transition to regenerate** (adjudication 9): boundary.
6. **Eager per-tag control reads in the index** (adjudication 10): recorded.
7. **Refusal hints omit `--work-order`** (adjudication 11): cosmetic.
8. **The TypeScript 7.0.2 claim is unverifiable here** (adjudication 6).
9. **The first measured paired wave has not run**, so `control.concurrent-orders` stays at level 1 with its declared gates.
10. **`CLAUDE_EFFORT` remains an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9).
11. **The executor's model and effort live in the control log and the outcome, not in a committed executor artifact**, as every reviewed code order since WO-013 has recorded.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step and the sandbox.** `gh` cannot read its configuration under this sandbox (the `~/.config/gh` path is denied), the same limit every reviewed order since WO-006 has met, while read-only `git ls-remote` to `origin` succeeds. The reviewed commit is made here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation, and notes parse run against the exact commit; if it refuses at the `gh` preflight it pushes nothing and the operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute command the publisher prints: change directory into the main checkout and run this worktree's `scripts/release.mjs close WO-030 --publish` with node, outside the sandbox and where `gh` is authenticated. Because `v0.7.0` is strictly above `v0.6.0`, the reviewed subject helpers finish this worktree through their own classifier, the closed-state gates in the subject and in `origin/main`, fast-forward main, run `npm ci` and the evidence gate, assemble the five-section edition from these notes, create and push the annotated `v0.7.0` tag, and create the matching GitHub Release. If Release creation fails after the tag push, the rerun is `npm run release -- close WO-030 --publish` from updated main.
- **Attribution:** the reviewed commit carries a plain subject and no attribution trailer, per the operator's standing rule (`~/.claude/CLAUDE.md`) and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins, as the WO-017, WO-018, WO-020, WO-023, and WO-028 reviews recorded.
- **Ledger duty:** discharged by the executor's two dated sections; this review adds no ledger entry because every open item above is either already in VER-001 or is a candidate this report names for a patch, WO-021, or the next planning pass.

## Proposed PR

- **Title:** `:sparkles: WO-030: Concurrent control state; one append-only segment per work order, lifecycle legality scoped to the named order, a selected-order status projection over every in-flight order, and a proven serial integration of two concurrent orders; v0.7.0`. The `:sparkles:` precedent is PR #31 for WO-020, PR #30 for WO-026, PR #29 for WO-028, and PR #28 for WO-023, chosen because the order adds a control-plane capability rather than restructuring existing behavior.
- **Body:** `docs/final-reviews/WO-030/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commit (the committed surface check requires the closed control state, and it also makes the map's sentences and the index's check mark true); the index is regenerated after the result and checked; the reviewed state is committed with a plain subject. The publisher is then run from this worktree:

```bash
npm run worktree -- publish WO-030 --title ':sparkles: WO-030: Concurrent control state; one append-only segment per work order, lifecycle legality scoped to the named order, a selected-order status projection over every in-flight order, and a proven serial integration of two concurrent orders; v0.7.0' --body-file docs/final-reviews/WO-030/PR.md
```

The helper runs `check-surfaces --committed WO-030` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-030`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff in the absolute subject-helper form. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, the operator runs that printed command with the main checkout as the working directory; the expected outcome is the annotated `v0.7.0` tag and its GitHub Release.

## Method

Everything above was re-established in this session inside the Claude Code sandbox: `npm test` twice, captured to files without a pipe; the read-only status projection and the transition; temporary-index trees and `git diff-tree -r` against all five checkpoints; the `resume.jsonl` prefix comparison against `HEAD`; the tag extraction and fold comparison, the `times` comparison, the two-worktree probe, the index audit, the link sweep, and the clean-room screen as scripts written for this review in the session temporary directory; `shasum -a 256`, `stat`, `git check-ignore`, and `unzip -t` on the main checkout's intake capture and the backup archive, with no intake byte displayed, copied, or committed; and a full reading of the diff, the receipts, VER-001, and the gates' source. The two corrections were applied with exact string replacement, checked with the pinned formatter and the publication checker, and diffed against checkpoint 5. No other file outside `docs/final-reviews/WO-030/` was changed by this review; the index regeneration and the control projection are the lifecycle's own effects.
