# WO-009 final review FINAL-001

**Subject:** branch `wo-009`, worktree `DotLn-wo009`, uncommitted working tree, 2026-09-05. `HEAD`, `main`, and `origin/main` are all `002593f6599b745a5d3e14ed8e2ac0d8fee37e87`, the PR #35 squash merge of WO-029, which is also the commit the published annotated tag `v0.9.0` (tag object `b2720f4b…`) peels to locally and at `origin` (`git ls-remote origin main` and `git ls-remote --tags origin v0.9.0` agree). The branch carries no local commits (`git log main..wo-009` is empty), so the entire subject is the dirty tree. At review open, after the dispatch transition and the index refresh the guide requires of a final-review actor: 46 `git status --porcelain -uall` entries, 25 tracked modifications (against `HEAD`, 565 insertions / 137 deletions including `docs/control/current.md` and the generated `docs/work-orders/README.md`; 23 files, 535 / 123 without them) plus 21 untracked paths totalling 5,325 lines: the skeleton's `src/worker-protocol.ts` (204), `src/worker-transport.ts` (378), `src/worker-host.ts` (328), `src/worker-store.ts` (171), `src/worker-worktree.ts` (97), `src/worker-status.ts` (118), `src/worker-demo.ts` (281), and `src/dotln.ts` (103); `test/worker.test.ts` (940); `fixtures/worker-cli.mjs` (69); `scripts/probe-worker-hosts.mjs` (142) and `scripts/test-live-workers.mjs` (190); the order's own control segment `docs/control/orders/WO-009.jsonl` (four events at dispatch, five after it); `docs/evidence/WO-009/` (`README.md` 77, `live.json` 71, `host-probe.json` 37, `deterministic.txt` 186, `full-suite.txt` 1,427, `live-diagnostic.json` 57, `claude-diagnostic.json` 25); and `docs/verifications/WO-009/VER-001.md` (419). The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree taken after the dispatch and index refresh (`171a5e8a329e85d51752259c69b342a5523f8bd5`) differs from this review's allocation checkpoint `refs/dotln/checkpoint/WO-009/5` only in `docs/control/current.md`, the strictly appended segment, and the regenerated index, and from the verdict checkpoint `/4` in exactly the same three files, so VER-001 has not changed by a byte since its verdict was recorded (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, one dated sentence in the planning map (§Corrections applied by this review), and the lifecycle's own events.

**Authority:** `docs/work-orders/WO-009-real-disposable-worker.md`: `Model: any capable model`; `Effort: executor xhigh+; verifier xhigh+; reviewer any`; a `v0.10.0` minor classification above the published `v0.9.0` base (`002593f`), completed by the executor on 2026-09-05 under the operator's standing opt-out release default because the control activation omitted a target, with skeleton `0.8.0 → 0.9.0` and compiler `0.3.0`, kernel `0.2.1`, event-envelope schema `1`, and compiled-program contract `1` unchanged; dependencies WO-008 (`closed`, pass) and WO-004's observed transport rows (present in `docs/discovery/environment.json` as the WO-004 addendum); the cited sections; an objective naming the two adapters and their canonical launch shapes; five scope-discipline bullets; constraints; six numbered acceptance criteria; an evidence gate; a write-back duty; and non-goals. Against the activated text (`refs/dotln/checkpoint/WO-009/1`, whose tree is identical to `main`'s) the order differs by 16 insertions and 5 deletions: the title's `(application version assigned at activation)` became `, v0.10.0`; the two-line placeholder release classification became the completed one; the eight-line dated activation-completion header was added; the roadmap cite was renamed from `§Application version pending` to `§v0.10.0`; and inside the scope-discipline bullet the placeholder "interval and lease expiry must be declared here when implemented" became the declared **1,000 ms** heartbeat, **5,000 ms** lease, and **180,000 ms** deadline with their meaning (four lines for one). The objective, constraints, all six criteria, the evidence gate, the write-back duty, and the non-goals are byte-identical to activation. The scope-discipline edit is adjudicated below (adjudication 9) as the completion the original text itself deferred to implementation, not a scope change.

**Prior reports:** `VER-001` (pass; all six criteria, the evidence gate, the write-back duty, and the scope and non-goal boundaries met; every claim reproducible without an authenticated model reproduced with the verifier's own synthetic vendor peer, its own row-2/4/6, worktree, status, and fail-closed drivers against the built `dist`, and out-of-band probes of the installed `claude 2.1.261` and `codex-cli 0.153.4` including negative controls for invented flag, profile, and feature names; the one non-reproducible limb, the two authenticated live completions and the nested-sandbox sense-boundary probe, corroborated by internal consistency against values the build computes; five non-blocking observations and three disclosures, none requiring repair). Complete sequence: one report, no repair episode. The executor's evidence README, the receipts under `docs/evidence/WO-009/`, and the activation-completion header inside the work order are executor evidence and were treated here as VER-001 treated them: claims under test.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes) in Claude Code 2.1.261 with sandboxed Bash and no override; macOS on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, the system `python3` for the link sweep, the clean-room screen, and the receipt arithmetic, and the lockfile-resolved Prettier and TypeScript in this worktree's own dependency tree. Reasoning effort `max`: the session's model control reported it at launch, but `docs/discovery/environment.json` records `effectiveEffortReadback` for `claude-code` as `not found` with `harnessReadbackEligible: false`, so `harness-readback` is not admissible and the epistemic source is `self-reported`, exactly as VER-001's was. `2.1.261` was checked against `claude --version` on this machine and is the newest entry on the harness's observed `versions` list (observed 2026-09-04); `max` is on `sessionEffortSelector.values`; the `claude-code` discovery entries are byte-identical to `main` (this order's discovery diff is one hunk adding the `wo009DisposableWorkerProbe` block), so the attestation is admissible on merged evidence alone. The order declares `reviewer any`, so any attested effort would satisfy it; `max` is stated because it is what the session reports. The executor was a different harness, model, and session (Codex CLI `0.153.4`, `gpt-6-astra`, `max`, operator-attested; `codex --version` reports `codex-cli 0.153.4` on this machine) and the verifier a different model and session on this harness (`claude-opus-5[1m]`), so the three roles are independent in session and actor.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-009 passes.** The order asked for the first real out-of-process worker: two `WorkOrderTransport` adapters over the CLIs discovery had already observed, each launched fresh with an explicit model, effort where the host exposes it and `unknown` where it does not, the narrowest locally observed settings boundary, ambient memory and session persistence disabled, no model tools, an isolated worktree, and schema-bound output; one real episode replacing only the fake executor in the shared demo with only the six-field envelope reaching the dispatching session; canonical failure rows 2, 4, and 6 without regressing rows 1, 3, and 5; a deterministic create-verify-cleanup worktree lifecycle in which a vanished worker destroys nothing; a `dotln status` projection that folds the store and introduces no state; and fail-closed handling of an unavailable model with no silent substitution in either direction. The deliverable does that, and the blueprint says so. This review re-established it first-hand rather than inheriting it: a synthetic vendor peer written for this review and distinct from both the executor's fixture and the verifier's, recording the argv, prompt bytes, and cwd it actually receives and salting its stdout wire fields and stderr with private canaries, drove both built transports through `runWorkerDemo`; 197 assertions covered the six-field envelope, the identical candidate, verification, and WorkOrder projection against the deterministic fake apart from the environment-dependent `repo` and `baseCommit`, the unchanged canonical event spine with only the seven worker lifecycle event types and their two paired `DecisionRecorded` records interleaved, the launch argv actually delivered to a spawned child for both hosts, the prompt's key set and pinned hash, a separate `dotln status --json` process observing one running episode and one pending command without changing a store byte, replay identity, worktree removal, re-query without dispatch, the pinned-selection refusal, row 6 (kill, retry refused before expiry, fresh distinct episode after expiry, recovered WorkOrder deep-equal), row 2 (crash after the durable receipt, recovery by query with no launch and one `CommandResult`), row 4 (observation persisted, `["WorkerResultObserved","quarantine","authority expired"]`, deep-equal state, pending command retained), both directions of `model-unavailable` on both transports with one launch, the exact `--model`, no fallback flag, and no vendor diagnostic in the store, twelve adversarial result shapes each ending in `invalid-result` with no `CommandResult` and no receipt, the blocked self-report retaining partial evidence without poisoning a later success, the CLI's missing-store, duplicate-option, `--beacons`, and gate refusals, and both version pins; a second probe of 9 assertions reproduced the two symlink refusals before any launch, the sanitized CLI message, and cleanup refusing an ignored file and never forcing. `npm test` is green on the verified tree and again on the reviewed tree; `git diff --check` is clean; `check-surfaces` passes the README block at `v0.10.0`, the three component rules, and the body profile over this package; the live receipts' command id, envelope byte lengths, and lease spans recompute from this build. VER-001's five observations are adjudicated below as non-blocking, its disclosures are shared or accepted, this review records observations of its own, applies one dated planning-map sentence, and changes no code, contract, acceptance, schema, authority, or captured-evidence byte.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **The host, transports, store, and status projection grade themselves.** `packages/skeleton/test/worker.test.ts` exercises `worker-*.ts` through the executor's own `fixtures/worker-cli.mjs`. Compensating controls: this review's synthetic peer (`review-cli.mjs`, written in the session scratchpad, not in the repository) shares no code with either fixture, records what the child actually receives rather than what `canonicalWorkerArgs` returns, and emits shapes neither fixture produces (an extra root key, a candidate path outside the inventory, a 321-character summary, a foreign work-order id, a multi-line summary, a duplicated candidate, a blocked self-report, a hanging child, an unavailable model); the probes drove the built `dist` through `runWorkerDemo`, `WorkerHost.run`, `WorkerWorktrees`, and the `dotln.js` CLI as a separate OS process. The shipped suite was run twice in full as a second signal, not as the proof.
2. **The two authenticated live completions and the nested-sandbox sense-boundary probe cannot be re-executed here.** This session's Bash runs sandboxed by policy, the work order and runbook both require an authenticated, non-sandboxed runner for `test:workers:live`, and `codex sandbox` cannot nest inside the active sandbox (VER-001 disclosure 1 reports status 71 at the probe's own assertion). Compensating controls: the receipts were cross-checked against values this build computes (§Independent probes), and VER-001 checked every flag, key, and feature name in both canonical shapes against the installed hosts with negative controls. No billable model invocation was attempted to work around a precondition this session cannot satisfy.
3. **The lifecycle helper dispatched this review but is not subject.** `scripts/resume.mjs`, `scripts/lib/`, `scripts/worktree.mjs`, `scripts/release.mjs`, `scripts/release-notes.mjs`, `scripts/github-body.mjs`, `scripts/work-orders.mjs`, and `scripts/check-publication.mjs` have no working-tree entry and are byte-identical to `HEAD`, so the dispatch, the checkpoint, the beacon emission, the index generator, the surface preflight, and the body validators are merged code. The two new scripts, `probe-worker-hosts.mjs` and `test-live-workers.mjs`, are subject, not instruments, and were read in full. Subject identity was established with git objects rather than with the helper's projection (§Dispatch and subject integrity).
4. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile** by calling `parseReleaseNotes` and `assertGitHubBodyProfile` directly and through `check-surfaces`. Because `v0.10.0` is above the latest published tag, these notes become the `v0.10.0` edition at release close.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-009 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-05T22:49:25.968Z`, `elapsed` `{"implementation":10530866,"verification":879688}`, checkpoint `514e9448670cb1fec1b9bbc212391139705fca0c` (`/4`), and `orders[]` with 25 entries; no projection-disagreement warning was emitted. Before invoking the transition this review read the `resume` mapping in `package.json`, the execution guide's dispatch contract, closeout, freshness, discipline, and model-specific sections, the final-review README, the work order, VER-001, and the WO-029 package as precedent, and confirmed that no lifecycle script has a working-tree entry. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-009/FINAL-001.md` (the directory itself is created by the reviewer), moved the phase to `final-review`, appended `FinalReviewRequested` with `recordedAt` `2026-09-05T22:51:21.620Z` as the fifth line of `docs/control/orders/WO-009.jsonl` (the order owns its own segment and `docs/control/resume.jsonl` is byte-identical to `HEAD`), minted checkpoint `4ff3a11881615e7357b2b91cca16ba9e3de63a6a` (`refs/dotln/checkpoint/WO-009/5`), and emitted the merged WO-021 beacon: `.control-beacons/public/` and `verifier/` each hold one 72,341-byte mode-0600 file, the size the v2 codebook assigns to (`final-review`, `pass`, `max`, `host-projected`), and `groups/` holds the 1,235,671,522-byte group cache, with mtime `1788648681` s, which is `Date.parse("2026-09-05T22:51:21.620Z")` truncated to seconds. The index refresh that followed changed only the WO-009 lines (`verified` → `final-review`, the pending `FINAL-001` row, and the `Now` line). The segment's five events are `WorkOrderActivated` (`19:37:21.067Z`, `effortDeclarationValidated: true`), `ImplementationReady` (`22:32:51.933Z`), `VerificationRequested` (`22:34:46.280Z`), `VerificationCompleted` (`22:49:25.968Z`), and this review's `FinalReviewRequested`; every event carries `schemaVersion: 1` and the stamps strictly increase. The projected `implementation` span (10,530,866 ms) and `verification` span (879,688 ms) are the arithmetic differences of the recorded stamps.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree after the allocation and index refresh as tree `171a5e8a329e85d51752259c69b342a5523f8bd5`, and `git diff-tree -r --name-status` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                                                                                                                                                                              |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`45251eb6…`)              | every deliverable path; the activation tree is byte-identical to `main`'s tree (`git diff-tree 002593f /1` is empty), so the whole subject, the work order's own header completion and the two placeholder completions included, is downstream of activation                                     |
| `/2` implementation-ready (`527ad0db…`)    | `docs/control/current.md`, `docs/control/orders/WO-009.jsonl`, `docs/work-orders/README.md`, `docs/verifications/WO-009/VER-001.md` (added); nothing else, so every deliverable, every write-back, the two scripts, and the evidence directory were already in the readiness tree               |
| `/3` verify allocation (`9ac70cd4…`)       | the same four paths                                                                                                                                                                                                                                                                            |
| `/4` verdict (`514e9448…`)                 | `docs/control/current.md`, `docs/control/orders/WO-009.jsonl`, `docs/work-orders/README.md`; VER-001 is not listed, and `cmp` against the blob `/4` holds reports it identical, so it is byte-identical to the report its own completion checkpoint captured                                     |
| `/5` final-review allocation (`4ff3a118…`) | `docs/control/current.md`, `docs/control/orders/WO-009.jsonl`, `docs/work-orders/README.md`                                                                                                                                                                                                    |

Every deliverable blob is therefore identical across the implementation-ready, verify-allocation, verdict, and final-review-allocation checkpoints and the working tree; the verifier changed nothing outside its report except the regenerated index (VER-001 disclosure 2); VER-001 has not changed since its verdict. All five checkpoints have `HEAD` as their sole parent. The completion mints `/6`.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash is empty and was not touched. `docs/intake/{chats,images,notes}` in this worktree each hold only `.gitkeep`, and `git status --porcelain --ignored` lists only `node_modules/`, the three `packages/*/dist/` outputs, and the anchored `.control-beacons/`; there is no `.runtime/` here (the executor's live gate wrote its stores under `/private/tmp`), there is no `.claude/settings.local.json`, and `.claude/settings.json` is tracked and byte-identical to `main`. `docs/control/resume.jsonl`, `docs/decisions/`, every existing `scripts/` file, `packages/kernel/`, `packages/compiler/`, `packages/skeleton/src/cli.ts`, `packages/skeleton/src/beacon*.ts`, `packages/skeleton/src/audit.ts`, and the root `package.json` dependency blocks are byte-identical to `main`; `docs/discovery/environment.json` differs from `main` only by the appended `wo009DisposableWorkerProbe` block and `environment.md` only by the appended dated addendum.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control segment                                                                                     | Report header                                                                                                                                                                                                                                                                                                                                                                                                                                                | Declared minimum | Result                  |
| -------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- | ----------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.4","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`     | there is no executor report; the evidence README states the operator-selected model and effort under the standing execution-guide default and says it is not readback; `0.153.4` is on the `codex-cli` observed `versions` list with the persisted `model_reasoning_effort` selector observed as `max` for that version (WO-029, `18:14:17Z`), so `max` was admissible at append; `codex --version` reports `codex-cli 0.153.4` on this machine | `xhigh+`         | `max` exceeds the floor |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}` | exactly one column-one header in VER-001, `**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}`, parsed and compared with the event actor as JSON in this session: equal                                                                                                                                                                                          | `xhigh+`         | `max` exceeds the floor |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`  | the header above                                                                                                                                                                                                                                                                                                                                                                                                                                             | `any`            | satisfied               |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: none` and the JSON form's `effortDrift` is `[{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.261; model claude-fable-5-1; effort max; source self-reported`, the same drift line, phase `closed`, a `finalReview` elapsed value beside the two existing values, and legal actions `release-close, next, activate`; with the WO-021 codebook unchanged in this diff, the emitted beacon should be 81,573 bytes (`closed`, `pass`, `max`, `host-projected`), the size WO-029's completion produced.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree after the dispatch transition and the index refresh, before any review artifact existed: `All matched files use Prettier code style!`, `GitHub body profile tests passed`, the shell suites (fixture temp-root, publication checker, backup-intake, resume, checkpoint, worktree, release, work-orders), `PASS index coverage: 227/227 product headings indexed`, `PASS docs/work-orders/README.md is current`, `tsc -b --force`, `# tests 236 / # pass 236 / # fail 0 / # skipped 0` including the fifteen `WO-009` skeleton tests and the unchanged `AC5 row 1`, `row 3`, and `row 5` tests in kernel and skeleton, the identity corpus `# tests 8 / # pass 8 / # fail 0`, and `Verified 4 WO-029 artifact evidence files; original semantic hashes and frozen oracle unchanged.`, exit 0; the counts equal the executor's `full-suite.txt` and VER-001's run. Second run, after this review's map sentence and the three package artifacts were in place, with the same result (§Readiness records its completion).

### Static, surface, and structural checks

- `git diff --check` clean, before and after this review's additions.
- `packages/kernel` and `packages/compiler` have no working-tree entry, so both are byte-identical to `v0.9.0`; `node:fs` and `node:child_process` appear only inside `packages/skeleton/src` (the new `worker-*.ts` and `dotln.ts` beside the pre-existing `cli.ts`, `beacon-fs.ts`, `beacon-io.mjs`, and `control-beacon-fs.mjs`) and nowhere in kernel or compiler, honoring the constraint that adapters and the host live outside both. The reactor diff adds two state fields (`workerEpisodeId`, `workerLeaseExpired`), the `workerResultGuard` and its traced `NoOp`, the `workerDispatchVersion` check on `CommandRedispatchRequested`, `WorkerAttemptStarted` and `WorkerLeaseExpired` folds, `WorkerResultObserved` admission, the same guard on a marked `CommandResult`, and `WorkerResultObserved` in the `consumesProgram` list so it passes through `withEquippedArtifact`; every other worker event falls to the reactor's `default: observed` branch and consumes no program. The scenario diff adds `persistEvent` to `LiveReactorDriver.append` (called before the in-memory log advances, so a refused store append leaves the reactor untouched), splits `runScenario` into `startScenario` and `finishScenario` with `feedOnce` reuse of a persisted suffix, and exports `projectScenario`, `restoreScenarioOpening`, and `ReactorStep`; `restore` replays without calling `persistEvent`. The lockfile changes only the skeleton version and its new `bin`; root `devDependencies` are unchanged; no package declares a new dependency; `package.json` gains the `dotln`, `probe:workers`, and `test:workers:live` scripts and no `npm test` step; `.gitignore` gains `/.runtime/`.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.10.0; expected v0.10.0 (work-order target v0.10.0; latest published v0.9.0)`, `PASS component-version @dotln/compiler: src unchanged; observed 0.3.0; previous v0.9.0 0.3.0`, `PASS component-version @dotln/kernel: src unchanged; observed 0.2.1; previous v0.9.0 0.2.1`, `PASS component-version @dotln/skeleton: src changed; observed 0.9.0; previous v0.9.0 0.8.0`, and the body-profile rule, first with `no current final-review bodies yet` and then over this package's `PR.md` and `RELEASE-NOTES.md`.
- A relative-link and anchor sweep written for this review over all 19 changed and new Markdown files, using GitHub's slug rules, resolved 662 targets; the only dangling one was `docs/final-reviews/WO-009/FINAL-001.md`, this report's own allocated path, linked from the regenerated index until the report existed.
- The clean-room and identity screen over the full tracked diff and every untracked file (6,577 lines) found no absolute personal path, user name, address, internal hostname, credential pattern, commercial tracker or ALM product name, predecessor-system name, or managed-host or gateway description; the only external URLs are the repository's own GitHub releases page and the public vendor configuration reference the discovery addendum cites; the first regular-expression pass hit only the substring `rally` inside `structurally` and `literally`. The evidence receipts carry no provider session id, raw transcript, or authentication material, and the live-gate script writes its stores outside the repository.
- The verifier's column-one header equals the `VerificationCompleted` actor by JSON comparison; the executor's `ImplementationReady` actor is the operator-attested Codex default the evidence README states.
- `.prettierignore` already excludes `docs/final-reviews/WO-*/`, `docs/verifications/WO-*/`, `docs/work-orders/*.md`, `docs/control/current.md`, and the two discovery records, so this package and the immutable reports are not formatter input, while the map sentence added by this review is and passes `prettier --check`.
- The three pinned durations are one source of truth: `HEARTBEAT_MS = 1_000`, `LEASE_MS = 5_000`, and `WORKER_TIMEOUT_MS = 180_000` in `worker-protocol.ts` equal the work order's header, 02, 03, the ledger, and the runbook, and the probe read them back from the built module.

### Independent probes against the deliverable

- **A third vendor peer and 197 assertions.** `review-cli.mjs` appends the argv, prompt bytes, and cwd it receives to a record file, writes `REVIEW_CANARY_STDERR_7f3a` to stderr on every run, embeds `REVIEW_CANARY_SESSION`, `REVIEW_CANARY_TRANSCRIPT`, `REVIEW_CANARY_THREAD`, and `REVIEW_CANARY_USAGE` in the Claude wire object and the Codex event stream beside the structured result, and emits nine behaviors. Driving both built transports through `runWorkerDemo` with `beacons: true` against stores under the real path of the session scratchpad: the envelope key set is exactly `episodeId, requiresHuman, resultId, status, summary, workOrderId`; `verified` and `candidates` equal the deterministic `runScenario(fixture)` projection and the recovered `WorkOrder` equals the fake's apart from `repo` and `baseCommit`; the fake's canonical event spine appears in the same order in the real log, and the only event types present in the real log but not the fake's are `WorkerHostConfigured`, `CommandRedispatchRequested`, `WorkerAttemptStarted`, `CommandReceipt`, `WorkerResultObserved`, `WorkerCompleted`, and `WorkerWorktreeRemoved`, with exactly two additional `DecisionRecorded` records (the redispatch gate and the admission); replay of the store equals the live projection; `worktrees/` is empty afterwards; the beacon directory exists; no canary appears in any store file; exactly one `WorkerAttemptStarted` carries `selectionSource: "host-launch"`, `effectiveModel`/`effectiveEffort` `unknown`, `heartbeatMs: 1000`, `leaseMs: 5000`, and a pinned `fnv1a64:` identity; the child's cwd is inside the detached worktree; the prompt's keys are exactly `artifactReceipt, episodeId, inventory, outputInstructions, resultId, workOrder`, its `artifactReceipt.semanticHash` equals the attempt's pinned hash, and it carries no transcript. On the argv actually delivered: `--model` is the exact requested value and `--fallback-model`, `--resume`, `--continue`, and `--dangerously-bypass-approvals-and-sandbox` are absent for both hosts; Claude receives `--effort high`, `--setting-sources project,local`, `--settings {"autoMemoryEnabled":false}`, `--tools ""`, `--output-format json`, and `--print --no-session-persistence --safe-mode --strict-mcp-config --disable-slash-commands --no-chrome --json-schema --max-budget-usd`, with the inline schema pinning `episodeId` to the attempt's id under `additionalProperties: false`; Codex receives no `--effort`, `exec --ephemeral --ignore-user-config --strict-config --json --output-schema`, `--cd` equal to the child's cwd, the named read-only profile and `network.enabled=false` overrides, and exactly 23 `--disable` pairs including `memories`, `shell_tool`, and `multi_agent`. During each run a separate `dotln.js status --store <dir> --json` process exited 0 reporting one running episode, one pending command, phase `running`, the requested model and effort, a lease span of exactly `LEASE_MS`, and store bytes identical before and after. A second `runWorkerDemo` over each completed store returned the same envelope with zero launches, and the other transport was refused with `no silent model substitution`.
- **Rows 6, 2, 4.** Row 6: a hanging child killed from `onRunning` left one pending command, phase `interrupted`, the recovered `WorkOrder` and live `program` continuation in the replayed state, and the clean detached worktree on disk; a retry before expiry was refused with `lease has not expired`; after `LEASE_MS + 1` a fresh run launched exactly one process, completed, and left two distinctly identified episodes (`lease-expired`, `completed`), an empty outbox, a `WorkerLeaseExpired` event, and a `WorkOrder` deep-equal to the killed attempt's. Row 2: a throw from `afterResultSaved` left one pending command, zero `CommandResult`, and one launch; recovery with a transport that throws on any dispatch completed with exactly one `WorkerResultRecovered`, one `CommandResult`, one `DeletionAttempted`, zero pending, the second attempt in `cached-result-query` mode with `producingEpisodeId` different from `workerEpisodeId`, and replay equal to the live projection. Row 4: through `WorkerHost.run` with host time advanced past authority between child completion and observation, `WorkerResultObserved` was persisted, its decision state deep-equal to its predecessor's, the trace `["WorkerResultObserved","quarantine","authority expired"]` with `authorityEnvelope:auth_seiri` in `envInputs`, the sole intent a `NoOp`, candidates empty, one pending command, a `WorkerResultQuarantined` event, no `CommandResult`, and replay decisions equal to the live ones.
- **Fail-closed and adversarial paths.** For each transport and each of `review-higher` and `review-lower`, an unavailable model produced `model-unavailable` after exactly one launch whose `--model` was the exact value with no fallback flag, one pending command, no `CommandResult`, a `WorkerInterrupted` payload carrying the reason and no stderr, and no canary in the store. For each transport, `extra-root-key`, `outside-path`, `long-summary`, `wrong-workorder`, `multiline-summary`, and `dup-candidate` each ended in `WorkerFailure` code `invalid-result` with one pending command, no `CommandResult`, and no `cmd_5ee0c6d8e207bd25.result.json` receipt. A `blocked` self-report returned a `blocked` envelope with `requiresHuman: false`, no candidates, `verified: false`, one pending command, and a `WorkerInterrupted` (`worker-incomplete`) payload retaining the one proposed candidate; after lease expiry a successful run verified, so the incomplete result did not poison the receipt cache. The CLI: status on a missing store exits 0 with an empty projection and creates nothing; a duplicated `--json`, `--beacons` on `status`, and `demo` without `DOTLN_LIVE_WORKERS=1` each exit 1 with the specific message and create nothing; `new ClaudeCliPrintWorkOrderTransport(runner, "2.1.260")` and `new CodexCliExecWorkOrderTransport(runner, "0.153.5")` throw `profile-refused`.
- **Second probe, 9 assertions.** A store directory that is itself a symlink is refused with `worker store may not be a symlink`, and a store under a symlinked parent (VER-001 O2's shape) with `worker directory may not alias a different root`, both before any launch; `dotln.js demo` over the aliased store exits 1 with the sanitized `worker host refused; inspect the store and declared environment before retrying` and no path in stderr. A file matched by an ignore rule (`core.excludesFile` on the fixture repository) makes `cleanup` refuse with `dirty worker worktree retained for inspection` and leaves the file in place; once removed, `cleanup` removes the worktree.
- **Live-receipt corroboration.** `expectedInspectCommandId` read from the built reactor is `cmd_5ee0c6d8e207bd25`, the command embedded in both live `resultId` values; the two envelopes re-serialize to exactly the recorded 346 and 334 bytes; both `leaseExpiresAt − lastHeartbeatAt` spans are exactly 5,000 ms; the episode ids `ep_seiri_1_worker_2` (Claude, killed then recovered) and `ep_seiri_1_worker_1` (Codex) are what the host's numbering produces for those histories; the Claude summary's aside that no tool access was available is what `--tools ""` implies. The earlier failed attempts in `live-diagnostic.json` and `claude-diagnostic.json` are labeled failures, not passes, and the evidence README says so.
- **Probe hygiene.** Both probes wrote only under the session scratchpad; a probe error of this review's own (the spine comparison first included `DecisionRecorded`, and the second probe first expected the worktree guard's message where the store guard fires first and first misplaced an ignore rule) was corrected in the probe, not in the subject; the working tree's status entry count remained 46 until this review's artifacts were added.

### Reading

Read in full: the work order and its activation-completion header; VER-001; the executor evidence README and every receipt under `docs/evidence/WO-009/`; the WO-029 final review, PR body, and notes as the package precedent; the execution guide's resume, closeout, freshness, discipline, and model-specific sections; the final-review README; the playbook's final-review steps; the complete tracked diff (`.gitignore`, README, the two discovery records, ledger, capability table, planning map, products 02, 03, 06, 10, 13, the three publication files, the work order, the lockfile and both manifests, `reactor.ts`, `scenario.ts`, `scenario.test.ts`, and the skeleton README); every untracked source, test, fixture, and script leaf; the control segment; the generated index; the canonical failure matrix in 03; and the notes parser, body profile, worktree publish path, and release surface rule in `scripts/`.

## Acceptance criteria

1. **One real episode replaces the fake executor; compact envelope — met.** Both real transports complete the shared demo with a spawned child; every other actor (structural deletion refusal, the fake verifier, operator-return cancellation) is the same code path the fake uses through `finishScenario`; the semantic projection equals the fake's; `dotln.ts` prints only `JSON.stringify(result.envelope)` on success and a fixed sanitized string on every failure; the six-field envelope is enforced by schema at the host and by `parseWorkerResult` again at the boundary; canaries in the vendor wire and stderr never reach the store.
2. **Rows 2, 4, and 6 with the canonical expected outcomes — met.** Reproduced by this review against 03's matrix text (row 2 "re-queries idempotently; never double-applies", row 4 "event is persisted; reactor decides quarantine/NoOp with trace naming the expired envelope; payload causes no state mutation", row 6 "lease expires; continuation + work order recoverable; a fresh episode resumes or a recovery episode inspects"); rows 1, 3, and 5 pass unchanged in both suites, and the fake scenario's frozen semantic trace is untouched.
3. **Deterministic worktree lifecycle — met.** Real `git worktree add --detach` with idempotent re-create, refusal of a path-escaping command id, a mismatched base, a symlinked path, a foreign common directory, and any tracked, untracked, or ignored dirt; `cleanup` re-verifies and never passes `--force`; every successful run leaves `worktrees/` empty and records one `WorkerWorktreeRemoved`; a killed worker leaves the worktree, the WorkOrder, and the pending command intact.
4. **`dotln status` renders the live in-flight projection — met.** Observed from a separate OS process during every real episode of both probes; `projectWorkerStatus` takes only an event array, and `WorkerLeaseExpired` is appended only by the host; a missing store yields an empty projection and no file.
5. **No silent model substitution in either direction — met.** One launch with the exact `--model`, no fallback flag, `model-unavailable`, a retained pending command, and a private diagnostic for both transports and both labels; the persisted `WorkerHostConfigured` selection refuses a different model, effort, or transport on a later run. The honest limit, stated by the executor, the verifier, 02, 03, and the discovery addendum, is that this proves adapter and store behavior, not provider-side identity; `effectiveModel` and `effectiveEffort` are recorded `unknown`.
6. **Observed canonical launch shapes — met.** The argv actually delivered matches 03 §Ports flag for flag; VER-001 checked all fifteen Claude flags and every Codex flag, config key, and feature name against the installed hosts with negative controls, and this review re-read `--help` output only through the recorded probe; `WorkerAttemptStarted` records transport, harness version, model, effort, selection source, both effective values as `unknown`, the profile, and the pinned identity; the bounded probe records Codex effort `unknown` and the adapter refuses any other Codex effort rather than mapping it onto a config override.

## Evidence gate

| Gate item                                                    | Where re-established by this review                                                                                                                                                                                                              |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| the live demo run captured, envelope included                | `docs/evidence/WO-009/live.json` (executor); corroborated here by the command id, byte lengths, lease spans, and episode numbering recomputed from this build; not re-executed (instrument 2)                                                    |
| rows 2/4/6 test output captured                              | `deterministic.txt` (35 of 35) and `full-suite.txt` (executor); the same tests in this review's two suite runs; each row reproduced by this review's own probe                                                                                     |
| the killed-worker recovery witnessed and recorded            | `live.json` `killedWorkerRecovered: true` with `WorkerLeaseExpired` asserted by the live gate script (executor); reproduced deterministically here against a real spawned child (row 6)                                                           |
| every criterion mapped to a named test or witnessed run      | the evidence README's table; all fifteen named `WO-009 …` tests exist in `worker.test.ts` and pass in both suite runs; every mapped behavior also reproduced by this review's probes                                                              |

## Write-back duty

All duties are discharged and consistent with the code as read and the behavior observed.

- **02 `WorkOrderTransport` and `Result envelope` rows** state the real `dispatch(request, now) → { receipt, completed, alive, kill }` contract, both adapter names, the first-class fake, the three envelope statuses, completion as a self-report, and `requiresHuman` as independent; the new paragraphs name the lifecycle events, the 1,000/5,000/180,000 ms values, and `WorkerResultObserved` admission; the actor-attestation paragraph now distinguishes the runtime attempt's launch claims from the control actor.
- **03** downgrades its status banner honestly to "implemented slices within a target architecture"; §Session lifecycle & resilience gains the disposable-inspection-host paragraphs with the lock, guard, receipt, and crash-window semantics this review verified; §Ports carries both canonical shapes, which this review compared flag for flag with `canonicalWorkerArgs`, the `fixture-inspection-v1` profile, and the broker boundary.
- **06** retitles the rung to `v0.10.0`, adds the dated activation completion beside WO-029's, and records an implementation receipt that explicitly declines to infer verification; **10** records the profile and payload version `1` as host protocol axes and denies any change to the event, equip, identity, compiled-program, compiler, or kernel axes; **13** moves WO-009 from "next planned" to "today" for the devops role.
- **Discovery.** The dated `environment.md` addendum and the `wo009DisposableWorkerProbe` block record observed launch acceptance, the still-absent Codex effort selector, the sense-boundary outcomes, and the `unknown` effective values, and leave the WO-001/004/019 observations intact.
- **Planning, publication, ledger, READMEs.** The capability table's dated reassessment stays at level 1 and keeps efficiency at E0; the map's dated paragraph and two catalog rows; the publication index row raised to `implemented` with both edition locks `CURRENT`; the ledger's three adopted entries appended without rewriting; the root README block at `v0.10.0`; the skeleton README's runbook with commands this review checked against `dotln.ts`'s parser.

## Ideation receipt

This order carries no ideation breakout: nothing in the subject names an intake batch, a breakout receipt, or a scope-expansion authority, and the ledger's `WO-009 implementation — disposable inspection episodes (2026-09-05)` section is an implementation entry extending the canonical lifecycle matrix. This review did not read intake; the three intake directories in this worktree hold only `.gitkeep`, and nothing in the subject links to or quotes them. The executor's two scripts are implementation, not breakout tooling, and were inspected as such (adjudications 6 and 7).

## Verification-sequence soundness

One report, no repair episode. VER-001 named its instruments and compensating controls, wrote its own vendor peer and drivers rather than re-running the executor's, probed the installed hosts with negative controls, ran the shipped suite only as a second signal, corroborated the live receipts it could not re-execute, screened the clean-room floor over the whole surface, disclosed the index refresh it performed and its own corrected probe errors, and stated what it does not establish. Every number it quotes reproduced here or in the suite; its column-one header equals its control actor; its report is byte-identical to the blob its completion checkpoint captured. The sequence is sound and complete.

## Adjudications

### 1. The worker prompt discloses the absolute worktree path (VER-001 O1)

Confirmed: in both probe runs `workOrder.repo` inside the delivered prompt begins with the store directory. Non-blocking. It is the path of the synthetic inventory the model is told to inspect, not a secret; the child already runs with that directory as its cwd; the work order's constraint is about secrets. Changing the prompt would change worker input, which is acceptance-relevant, so it is not a reviewer correction. Recorded in the notes' "read before upgrading" so the operator running the documented `.runtime/` stores knows the vendor receives a path under the checkout. A mount-relative `repo` is a candidate for a later privacy pass.

### 2. A `--store` path that traverses a symlink is refused generically (VER-001 O2)

Reproduced in both shapes: the store itself as a symlink refuses at `WorkerStore.acquire`, a symlinked parent refuses at `WorkerWorktrees`' alias guard, both before any launch, and the CLI reduces both to the fixed sanitized message. Non-blocking and fail-closed; the documented `.runtime/` path is unaffected. A more specific refusal string is a small candidate, as the verifier said.

### 3. The Claude launch writes a schema file it never references (VER-001 O3)

Confirmed by reading `dispatch`: `result.json` is always written into a private `mkdtemp` directory with mode 0600 and removed in `finally`, while Claude receives the schema inline via `--json-schema` and only Codex reads the file. Harmless redundant write; not a leak.

### 4. AC5's "either direction" is carried by the launch assertions (VER-001 O4)

Agreed. Both the shipped test and this review's probe loop over two model labels the synthetic peer ignores; the proof is one launch with the exact `--model` and no fallback flag, plus the persisted-selection refusal across runs. The executor's evidence states the limit plainly.

### 5. Both version pins are exact equality (VER-001 O5)

Confirmed: `2.1.260` and `0.153.5` refuse `profile-refused` at construction. Deliberate, tested, and documented in the runbook, 03, 10, and the discovery addendum. The operational consequence, a refusal after any routine CLI upgrade until re-probe and re-pin, is recorded in the notes' "read before upgrading".

### 6. The live-gate script is macOS-specific (this review)

`scripts/test-live-workers.mjs` creates its stores with `mkdtempSync("/private/tmp/dotln-live-worker-")`, a literal path that exists on macOS and not on Linux, chosen because `$TMPDIR` on macOS is a symlinked path the worktree alias guard refuses (adjudication 2). The gate is operator-run on the personal machine the discovery record describes, so this is a portability limit rather than a defect; `realpathSync(tmpdir())` would remove it. Candidate for the next touch of that script.

### 7. The bounded host probe checks eleven of the fifteen Claude flags and records a constant effort (this review)

`scripts/probe-worker-hosts.mjs` requires eleven Claude controls to appear in `--help` and omits `--disable-slash-commands`, `--mcp-config`, `--no-chrome`, and `--max-budget-usd`, and it records `claude.effort: "high"` as a literal rather than an observation. Non-blocking: the exact version pin (adjudication 5) is the operative guard, VER-001 checked all fifteen flags against the installed host out of band, and the probe's Codex branch does detect a newly appearing `--effort` selector. A re-probe should extend the Claude list so the recorded `host-probe.json` covers the whole canonical shape.

### 8. `.runtime/` is not on the release-close disposable list (this review)

The runbook documents stores under the newly ignored `/.runtime/` root. `release close` removes a subject worktree only when every ignored path is on the anchored disposable list (`node_modules/`, `dist/`, `.control-beacons/`, `.DS_Store`, `.tsbuildinfo`), so an operator who runs the documented demo inside a work-order worktree leaves a non-disposable ignored directory that blocks removal until deleted by hand. This worktree has no `.runtime/` (the executor's gate wrote under `/private/tmp`), the main checkout is unaffected because release close removes only the subject worktree, and the refusal names the offending path, so nothing is lost. Recorded in the notes and PR body; adding the root to the disposable list or pointing the runbook outside worktrees is a candidate for its own bounded order.

### 9. The executor completed a placeholder inside the work order's scope-discipline bullet (this review)

The activated text said the heartbeat interval and lease expiry "must be declared here when implemented"; the executor replaced that clause with the three declared values and their meaning, and the evidence README says the policy "complete[s] previously unassigned details within the work order's authorized scope." The values are one constant each in `worker-protocol.ts`, match 02, 03, the ledger, the runbook, and the attempt events, and were exercised by VER-001 and this review. Adjudicated as the completion the original text deferred, alongside the release-target completion the activation header records; no criterion, constraint, or non-goal changed.

### 10. `model-unavailable` is a heuristic label over vendor diagnostics (this review)

`decodeResult` classifies a non-zero exit as `model-unavailable` when stderr or stdout matches a model-not-found pattern and as `transport-failed` otherwise. Both branches fail closed with the command pending, no `CommandResult`, and no diagnostic bytes in the store, so the acceptance property holds under any vendor wording; only the failure label could drift. Non-blocking.

### 11. A quarantined completed result remains a durable receipt (this review)

`WorkerHost.run` publishes a validated `completed` result to the store before `WorkerResultObserved` admission, so a result quarantined for expired authority (row 4) stays cached; a later run re-queries it in `cached-result-query` mode and the reactor decides admission again. This matches 03's rule that the observation "happened" and that admission is the reactor's decision; recorded so a reader does not expect redispatch after an expiry.

### 12. The verifier's disclosures (VER-001 D1–D3)

D1, the unexecuted live gate and nested-sandbox probe, is shared by this review (instrument 2) and is a precondition of the runner, not a gap in the subject. D2, the index refresh after dispatch, is what the guide directs and this review did the same. D3, the verifier's own corrected probe inputs, reflects on the verifier's harness, not the subject; this review records its own equivalents in §Independent probes.

### 13. Publication runs into the sandbox's `gh` boundary (this review)

`gh auth status` in this session fails to read `~/.config/gh/config.yml` (`operation not permitted`), so `worktree publish` will refuse at `ensureGh` after the surface preflight and body validation and before any push. The operator runs the same command from this worktree outside the sandbox (§Publication package).

### 14. Smaller consequences recorded without action

`WorkerStore.append` re-reads and decodes the whole log to check the next event id, which is quadratic in log length and irrelevant at demo scale; efficiency remains E0 in the capability table. The child inherits the host's full environment (`env: process.env`) so the CLI broker can authenticate; 03, the runbook, and VER-001 all name this as the boundary, and no environment value is logged. `probe-worker-hosts.mjs --sandbox` and `test:workers:live` are reproducible commands rather than `npm test` steps, matching the constraint that live evidence is separated from the always-run suite. The publication rows enter at `implemented`, describing verified behavior, as WO-020, WO-021, and WO-029's did.

### Candidates considered and not sustained

- **Whether the scope-discipline edit is an unauthorized change to the work order.** No: the original clause was an explicit placeholder deferring the declaration to implementation (adjudication 9).
- **Whether zero heartbeats in this review's probe runs indicate the heartbeat never fires.** No: the synthetic peer completes in well under one second; the live receipts record 16 and 9 heartbeats, and the shipped lease-fencing test and this review's row-6 probe drive the timer.
- **Whether the unexecuted live gate fails the evidence gate.** No: the work order itself gates that suite on an authenticated, non-sandboxed runner; the captured receipt exists, is internally consistent with this build, and VER-001 proved that the controls it exercised are real host mechanisms.
- **Whether the redundant schema file is a leak.** No: private temp directory, mode 0600, removed in `finally` (adjudication 3).
- **Whether the `.runtime/` observation should fail the review.** No: it is an operator-workflow hazard with a named refusal and no data loss, outside the six criteria (adjudication 8).

## Corrections applied by this review

One, bounded and non-substantive, following the WO-030, WO-021, and WO-029 precedent: the planning map's dated `WO-009 implementation (2026-09-05)` paragraph gained a sentence recording that WO-009 passed independent verification and final review at this revision and awaits operator merge, with the recommended sequence and the deferred items unchanged. `prettier --check` passes on the file and the second `npm test` run includes it. No code, test, contract, acceptance, schema, authority, or captured-evidence byte was changed.

## Deviations and open questions

- **No session trailer on the commit or PR body.** This session's harness supplied an attribution reminder asking that commit messages and PR descriptions end with a session link. The operator's standing instruction forbids AI attribution on commits and pull requests, the execution guide keeps commit messages plain, no commit in this repository's history carries such a trailer, and the repository is public; the reviewed-state commit and the PR body therefore carry none, as WO-029's did. The operator can amend the commit before merge if a trailer is wanted.
- **The live gate and the sense-boundary probe were run only by the executor.** Neither the verifier nor this review could run them (instrument 2); the receipts are corroborated, not re-executed.
- **Provider-side model identity and effective effort are unproven by design.** Every effective value is recorded `unknown`; Codex effort is `unknown` because the installed host exposes no dedicated selector; Claude Code attestations remain `self-reported` because no effective-effort readback is recorded for the harness (WO-023 FINAL-001 adjudication 9 remains open).
- **Outside this order.** General mounts and Senses (WO-022), real verifier episodes (WO-010), feedback units (WO-011), a durable background scheduler, arbitrary external writes with exactly-once semantics, confinement of the same-user CLI broker, SQLite, background/workflow/MCP transports, and benchmarking remain separate work, as the work order, 03, VER-001, and the notes state.

## Publication package

- **PR title:** `:sparkles: WO-009: Real disposable worker; two version-pinned CLI transports (Claude Code print, Codex exec) replace only the fake executor in the shared inspection demo with an explicit model, the narrowest observed settings, no memory, no session persistence, no tools and schema-bound output, a single-writer JSONL host records 1 s heartbeats, 5 s leases and a 3 min deadline and publishes immutable result receipts before acknowledgement, detached worktrees are created, verified and removed without force, killed or expired workers leave recoverable pending work, dotln status projects the store read-only, and unavailable models fail closed with no silent substitution; v0.10.0`
- **PR body:** `docs/final-reviews/WO-009/PR.md`, committed with the reviewed state, one physical line per paragraph and list item.
- **Release notes:** `docs/final-reviews/WO-009/RELEASE-NOTES.md`, the five-section reviewed edition for `v0.10.0`, validated by `parseReleaseNotes` and the body profile.
- **Publish:** after `final-review-result pass` and the reviewed-state commit, `npm run worktree -- publish WO-009 --title '<the title above>' --body-file docs/final-reviews/WO-009/PR.md`. If the `gh` preflight refuses inside the sandbox (adjudication 13), the operator runs the same command from this worktree outside it; the helper pushes only `wo-009` and never merges.
- **Release close:** after merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints with the main checkout as the working directory; because `v0.10.0` is strictly above `v0.9.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.10.0` tag, and creates the matching GitHub Release. The subject worktree's populated `.control-beacons/` is disposable at its anchored root and will not block removal; no `.runtime/` exists here.

## Readiness

The branch is ready for operator review: the subject is the tree VER-001 passed, every criterion and gate item was re-established first-hand with a peer and probes of this review's own, the actor inventory satisfies the declaration, the suite is green on the verified tree and again on the reviewed tree, the surfaces pass, and the package is complete. After `final-review-result pass` the projection should show phase `closed`, `FINAL-001` pass, the reviewer's attestation as latest with no new drift pair, and legal actions `release-close, next, activate`; the index refresh that follows will mark WO-009 `final-reviewed`.
