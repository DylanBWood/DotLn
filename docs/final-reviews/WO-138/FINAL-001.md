# WO-138 FINAL-001 — the local-model role qualification pilot passes review as `inconclusive`: T2 candidate ranking is the one qualified role, its comparator was measured, and the order is integrated onto a moved `main` at its staged v0.38.1

**Verdict: pass.** WO-138 reports the outcome `inconclusive`, and under product 07 §Research and guided-operator work orders that is a complete close when the method criteria are met, which they are, all six, at the integrated tree. I did not adopt the verifier's account of the numbers: I re-derived one record from each of the six baseline cell groups by hand from the retained episode bytes, ran the retained independent audit over all 38 records, and read the packet, both product write-backs and `results.json` against each other. The local model's five T2 rankings are one identical ordering whose Spearman correlation with the operator's held-out order is 1 − 36/210 = 0.828571; the remote comparator matched the operator exactly once and produced the local ordering four times, so its median is the same 0.828571 and the ten-point comparator floor is measured, not assumed. Local T1 matches one field in ten in every repeat and local T3 matches ten rows in twelve in every repeat, so both miss their 0.9 floors and both fall outside ten points of a remote model that scored 1.0. The pre-registered floors were applied as written, the ranking was held out, the outcome logic maps one qualifying task to `inconclusive`, and nothing in the packet, the 03 catalog row or the 06 disposition promotes a capability level, claims no-egress, or qualifies private inputs, writing, implementation or verification. The one substantive defect this order met, VER-001's F1, was real and decision-consequential, was repaired by recollecting the whole matrix twice rather than by adjusting the experiment to its answers, and was independently confirmed closed by VER-002. This review adds integration: `main` moved five commits and published `v0.38.0` above the base this order started from, so the branch was fast-forwarded onto that base with the WO-079 helper in its first real use, its one authored conflict resolved, its projections regenerated, its staged `v0.38.1` confirmed still above the baseline, and the single product gate run on the result — **24 suites, 0 failed, 269.74 s, exit 0**. No finding survived review. Two defects met and not fixed are boarded up in the decision record with named follow-ups (D011 for the helper, D012 for the verifier's open advisory), and six further observations go to planning.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-fable-5-1","effort":"unknown","source":"claude-session-readback"}

**Process cost:** entry 84770 tokens; handoff 5689313 tokens; source claude-transcript-message-usage

The human actor is the operator, who dispatched `resume: final review` in this worktree after selecting the model with `/model` (Fable 5.1, whose session identifier is `claude-fable-5-1`) and supplied no effort; `claude --version` reports 2.1.278. No effective effort readback exists in this harness and nobody supplied one, so `effort` is `unknown` rather than the order's "reviewer any"; the model is session readback, so the pair is recorded `claude-session-readback` as the two verifications of this order recorded theirs. Counters were read with `node scripts/harness.mjs usage 1b2a8329-db9c-4d78-901c-823517ab9c67` at 2026-09-21T15:36:14.622Z and 2026-09-21T16:03:19.651Z, scope `dispatch`. Before any fan-out I planned zero admissions of the 20-agent root budget, because every read here is one file or one command and the gate sets the pace regardless; none was spawned, the counter reports 0 of 20 `exact-observed`, and the uncounted remainder is unknown. The operator's mid-review note that the worktree sync helper had never been used before is answered in §Integration, where its behavior is recorded as evidence.

**Read scope.** The order in full, including the amended activation preflight and the pre-registered floors; its cited sections: product 06 §Counterfactual profiling work orders and §Candidate — local-model usefulness experiments, product 03 §Runtime primitive catalogs, WO-110 and WO-119 as the sources of the transport and the T2 candidates, WO-052 VER-001 as the T1 source receipt, product 07 §Research and guided-operator work orders, §Goal-aligned decisions and §Independent workflows and integration, product 08 §PRs and commits and §Release-note edition; the ideation lineage the nomination names, the 2026-09-17 planning pass §3 (M5) and §13 (decisions 4 and 7) and the 2026-09-21 standard pass that activated the order under receipt 023; VER-001 and VER-002 in full at their current bytes; the packet, `implementation.md`, `repair.md`, `decisions.md` D001–D010, `audit.mjs`, `results.json`, both run manifests, the operator ranking and its request, the two repair attempt READMEs; the complete subject diff against `main`, which is the probe module (1,797 lines) and its test (606 lines) and fixture in full, the runner registration, the planning-continuation normalization and its test, both product write-backs, the order's version and classification lines and every regenerated projection; and eight episode records opened directly.

## Subject, and the two bases

| Item | Value |
| --- | --- |
| Work order | `docs/work-orders/WO-138-local-model-role-qualification.md`, activated 2026-09-21T12:41:33Z, patch, evidence-only, `v0.38.1` |
| Branch / worktree | `wo-138` at `/Users/dylanwood/Projects/DotLn-wo138`; physical cwd and Git root confirmed equal; this session is the sole registered writer |
| Probe | `scripts/probes/local-model-role-qualification.mjs`, SHA-256 `3d9d6aa05c47bc6561e6850ea49c236611a3b462486f8311e4119953411f6907`, equal to the sole `harnessBuildHash` across all 38 canonical records and both run manifests |
| Evidence | `docs/evidence/WO-138/`: 38 episodes (21 local, 17 remote), `inputs.json`, `results.json`, packet, decisions, implementation and repair records, `audit.mjs`, eight retained attempt sets (1.0 MB, 23,530 lines added) |
| Verification base | VER-002 judged the uncommitted worktree over `main` at `97cedbe0` at code identity `bb8ff2e3…`; between checkpoint 9 (`FinalReviewRequested`) and the pre-integration checkpoint 10 only `docs/control/current.md`, `docs/control/orders/WO-138.jsonl` and the generated index changed, so no subject byte moved between verification and this review |
| Integrated base | `main` at `87393bf6f1c7799df7c726d6b734ee3b4c766e25`, five commits ahead, carrying WO-071 (PR #108) and the annotated tag `v0.38.0`; `git ls-remote origin refs/heads/main` returns the same object, so `origin/main` equals local `main` |
| Code identity now | `14584332e2b757d63132099f36dedcdef154df14367770ca42970b1764bbaa0d`; it differs from VER-002's only because the fast-forward brought main's source (`scripts/lib/config.mjs`, `authority-grants.mjs`, `control.mjs`, `work-order-repository.mjs`, `resume.mjs`, `work-orders.mjs` and three test files) |

## Integration — the first real use of `worktree integrate`

**What the helper did, as observed.** `npm run worktree -- integrate WO-138` ran in the matching worktree root with no intake backup, because `docs/intake/` holds only three tracked `.gitkeep` placeholders and the helper's ignored-intake listing was empty. It minted `refs/dotln/checkpoint/WO-138/10` (`4ddc85ae…`, the whole working state including untracked paths), pushed the named stash `WO-138 integrate 2026-09-21` (`7249119e…`, include-untracked), fetched `origin`, fast-forwarded the commit-less branch from `97cedbe0` to `87393bf6`, applied the stash, auto-resolved five projections (`README.md`, `docs/control/current.md`, both publication locks and the work-order index), left one authored conflict, ran the regeneration list (build, harness bundle and manifest, control projection, `release prepare --local`, the D011 stub, `meta`, index, publication locks), printed the four affected checks, and exited 1 naming `docs/product/06-roadmap.md`. The conflict was the two dated activation paragraphs both inserted at the head of §Release boundary, WO-071's `v0.38.0` from `main` and WO-138's `v0.38.1` from the stash. I resolved it as a union with `v0.38.1` above `v0.38.0`, the order the section already uses, staged it, and ran `--continue`, which reran the same regeneration and exited 0 with "Integration mechanics complete. Recovery refs and stash retained." Nothing was popped, dropped, reset, restored or cleaned; the stash and checkpoint are listed above and remain.

**What integration changed.** `git diff --cached refs/dotln/checkpoint/WO-138/10` on the integrated, staged tree lists exactly main's 22 WO-071 files, the regenerated projections (`current.md`, the decisions index, the follow-up register, the two locks, the work-order index, the process-meter table in `PR.md`) and the D011 stub in `decisions.md`; over every subject path — the probe, its test and fixture, the 38 episodes, `results.json`, `inputs.json`, the packet, `03-architecture.md`, both verification reports and the three adjacent source files — it is empty. The follow-up register unioned by entry id at 491 entries and kept this order's four revision rows of the local-model candidate section beside main's WO-071 rows; the decisions index carries D001 to D012.

**Retiming.** None was needed and none happened: `release prepare --local` reported "WO-138 target v0.38.1 remains current; no files changed", because `v0.38.1` is the next patch above the published `v0.38.0`. Component versions are unchanged against `main` (compiler 0.17.0, console 0.1.7, kernel 0.6.0, skeleton 0.33.1) and `git diff --cached HEAD -- packages package.json package-lock.json .claude .agents` is empty, so there is no collision and no dependency change. The record is [D011](../../evidence/WO-138/decisions.md#wo-138-d011--integrate-main-at-87393bf6-and-keep-the-staged-v0381-target).

**Observations on the helper, recorded because this was its first real use.** (1) After the stash apply the helper set its stage to `applied` and ran every regeneration step, printing eight `Regenerated:` lines, while the roadmap still held conflict markers; the `--continue` pass regenerated everything again, so the locks, the register and the release note that reached the tree were computed on the resolved bytes, and I confirmed no conflict marker exists in any tracked file. The first pass is therefore provisional, and its output can mislead a reviewer who reads "Regenerated" as final. (2) The receipt's `release` string carries a trailing newline, so the D011 stub printed "local snapshot only.." with a doubled period. (3) The conflict sides are labelled `Updated upstream` and `Stashed changes`, which is Git's stash vocabulary and is accurate. None of these touched a subject byte. The first two are named in D011's `followup` for the planner; a reviewer never repairs the helper mid-review.

**Checks on the integrated tree.** `npm run publication:check`: 273 of 273 product headings indexed, both editions current at 30 and 45 linked sections. `node scripts/harness.mjs check`: 31 generated surfaces, no drift. `npm run release -- check-surfaces --local`: every license surface PASS, every package publish refused as expected, exit 0. `npm run format:check`: pass. `npm run plan -- check`: exit 0, with this order's workspace updates classified `release-classification-format` and `release-assignment v0.38.1` under receipt 023 and WO-071's `release-assignment v0.38.0` now committed. `node scripts/work-orders.mjs index --check`: current. `node docs/evidence/WO-138/audit.mjs`: 38 records, one build, `inconclusive`, `["T2"]`. The WO-138 suite direct: 13 of 13 in 2.57 s. `git diff --check` and `git diff --cached --check`: clean. The product gate is in §Gate.

## Acceptance, judged at the integrated tree

| # | Criterion | Judgment |
| --- | --- | --- |
| 1 | WO-137's record shows a successful live inference row; the pinned artifact is the one it recorded; every input is public or the seeded scratch repository; the packet states no no-egress claim and qualifies no private-material role; each of the 30 baselines has a recorded envelope or typed failure, none self-reported | **Met, carried forward and re-read here.** `docs/discovery/local-runner-2026-09-18.md` opens `inconclusive: live protocol behavior passed` with the no-egress boundary as its only reason and pins `Qwen3.6-27B-Q4_K_M.gguf` at SHA-256 `33625d8d…`, which is `PINNED_LOCAL.sha256` and the `artifactSha256` on every local record. Across the 38 records `sourcePaths` name only the WO-052 receipt, the WO-119 discovery fixture and the harness-probe hook fixture; no record contains a home path or a device identifier value (the runner inventory retains key names only). The packet says "No no-egress claim is made" and `results.json` carries `noPrivateInputQualification: true`. All 30 baselines are `validated-envelope`; validity is `validateAnswer`'s and agreement the oracles', never the model's `status` |
| 2 | Per task and model: schema-valid rate, oracle agreement, variability, median latency, tokens/s, interventions; tool-failure behavior described from the records | **Met, re-derived.** Six distributions, each 5 of 5 valid with zero interventions: local T1 0.1 in all five repeats at 11,152.439 ms and 17.897 tokens/s; remote T1 1.0 at 9,154.829 ms and 31.022; local T2 0.828571 in all five at 12,075.799 ms and 18.135; remote T2 [1, 0.828571 × 4], median 0.828571, at 16,596.313 ms and 30.586; local T3 0.833333 in all five at 25,131.474 ms and 15.280; remote T3 1.0 at 14,227.731 ms and 32.753. My hand checks: `baseline-local-T2-r1` ranks the operator's fourth candidate sixth and its fifth and sixth one place early, Σd² = 6; `baseline-codex-T2-r1` equals the operator's order; `baseline-codex-T2-r2` equals the local order; `baseline-local-T1-r1` matches only `verdict`; `baseline-codex-T1-r1` matches all ten fields; `baseline-local-T3-r1` labels J07 and J08 `read` where the fixture says `gate` and matches the other ten. The three injected cells are described from the records: T1 blocked with `requiresHuman: true` and wrote empty strings into ten fields, recorded as `invalid T1 criteriaMet`; T2 returned a valid `blocked` envelope naming the evidence-less candidate (`failing-lint`, the injected first candidate) with `requiresHuman: true` and no actual intervention; T3 classified the unknown row as `dispatch` instead of blocking, recorded as `unexpected input was not blocked` |
| 3 | One-factor cells identify each build by hash and change exactly one factor | **Met, carried forward.** Each factor record carries its `cellBuildHash` and the baseline group's single hash; `validateRecordInputs` recomputes every prompt hash from the registered variant, and the T3 definitions cell's changed `inputHash` is the content of that one named support, which a test asserts the baseline omits. Deltas: T1 field map 0 (0.1), T2 rubric −0.057142 local and +0.057143 remote, T3 definitions +0.166667 local (1.0) and 0 remote |
| 4 | Outcome exactly one of `ready`/`negative`/`inconclusive` against the floors as written; operator T2 time recorded; no capability promotion, no implementation or verification qualification | **Met.** `evaluateRecords` applies the order's thresholds unchanged (5 of 5, ≥ 0.9 for T1 and T3, ≥ 0.7 for T2, local ≥ remote − 0.1, ≤ 120,000 ms, zero interventions), refuses to qualify a task whose comparator returned fewer than five model outputs, and maps one qualifying task to `inconclusive` with the blocker "Only T2 met every pre-registered floor". `operatorRanking.elapsedSeconds: 60`, self-reported, inside the 13:04:26Z–13:09:14Z request window and before any model ranking existed (VER-002 checked the timestamps). `noCapabilityLevelPromotion: true`, `implementationOrVerificationQualified: false` |
| 5 | Live runs never inside `npm test`; the deterministic tests run over recorded envelopes and pass inside it | **Met, executed here.** Inference runs only under the CLI main guard; importing the module runs none; the test's only child is a synthetic gate process and its only server a loopback double; the suite is registered in `local-runner-double` and passed 13 of 13 directly and inside the gate below. The live window (15:05:06.798Z to 15:15:41.383Z) ended four minutes before the repair gate began (≈15:19:40.9Z, recorded 15:24:03.013Z), and both manifests record `retainedEpisodes: []` |
| 6 | Write-backs land (03's local kind roles dated, 06's candidate disposition, ledger entry); `npm test` green; `git diff --check` clean; no new dependency | **Met at the integrated tree.** `03-architecture.md` carries the dated "Observed 2026-09-21 — WO-138 role qualification after repair" row with its exclusions and reopening condition; `06-roadmap.md` carries the activation paragraph in §Release boundary and the dated `inconclusive` disposition in §Candidate — local-model usefulness experiments; the ledger duty is discharged by `decisions.md` and the decisions index rows, which the generated work-order index states as the substitution for an order filed after 2026-09-09. Every figure in both write-backs matches `results.json` row for row. Gate below; diff checks clean; `package.json`, `package-lock.json` and `packages/` unchanged against `main` |

## Gate

Every reviewed tracked change and every intended new source file was staged before the gate, and the gate ran outside the harness sandbox from the start.

- `npm test -- --review` — **24 suites, 0 failed, 269.74 s, 68 fresh tasks, exit 0**, recorded at 2026-09-21T16:02:21.436Z at code identity `14584332e2b757d63132099f36dedcdef154df14367770ca42970b1764bbaa0d`, tree `849078b8bf2f9debcb8c77a98ae37fd03871742d`. The executor's repair gate (15:24:03.013Z, 22 suites, 262,118 ms) and VER-002 both stand at code identity `bb8ff2e3…`; this run's identity differs because the fast-forward brought main's source, which is why `--review` selected the machinery suites whose declared sources changed since the base as well as the product suites. No live inference ran during this review, so the gate window is clean by construction.

## The decision packet, which is this stage's charge

Product 07 makes the reviewer's charge the decision packet and whether a speculative result was presented as settled. It was not. The packet's first sentence names the outcome class; the qualified role is stated as narrowly as the evidence allows ("rank T2's six public discovery candidates under the fixed read-only contract, with host validation of complete candidate coverage"); the comparator is described as "evidence, not a gold standard"; the single T3 support cell at 1.0 is reported as a candidate for a separately registered repeated qualification and not as a result; the retained matrices that were rejected or unmatched are named and linked, not averaged in; and the limits section says what five repeats of one cell do and do not establish. The 03 row and the 06 disposition say the same things in fewer words and add the reopening conditions. Two readings I hold more sharply than the packet does, neither of which changes a verdict: first, T1's "5 of 5 schema-valid" is partly an artifact of grammar-constrained decoding, since in all five local repeats the model wrote the receipt's coverage sentence into `criteriaMet`, where the 40-character bound truncated it, and a bare comma into the other eight fields, and the packet's sentence "length compliance did not fix accuracy" is true but does not say the content is degenerate; second, the T1 injected cell did block, and only its field contents were out of contract, which VER-002's A2′ already notes and D012 now records. The packet's numbers, its outcome logic and its refusals are correct.

## Verification sequence

VER-001 (`fail`) found the one defect that mattered: the probe's T2 schema emitted `uniqueItems`, the remote transport rejected all six remote T2 requests with HTTP 400 before any inference, and the collector's `??` on an empty string discarded the diagnostic, so the comparator floor was recorded as met without having been exercised. I judge that finding correct, decision-consequential and correctly routed to repair; its reproduction was independent of the subject's error handling, and its four advisories were reasonable. The repair (D006 to D010) removed the keyword while keeping the host's duplicate rejection, made the remote decoder retain bounded, credential-redacted diagnostics, made an unavailable comparator an explicit `null` that cannot qualify, published T1's existing 40-character bound in the schema, and then recollected the whole matrix under one formatted build twice: the first replacement (`unmatched-input-008`) reached the model on every remote call but failed the executor's own strengthened cross-transport check because a failing-test candidate's stderr digest embeds its scratch path, so the collector was changed to pin one `inputs.json` for both arms and validate every record's binding before scoring. The repair session ran 76 live episodes, 38 of them excluded, and the operator authorized the 17 additional remote launches beyond the session cap as a bounded exception; nothing in a prompt, oracle, ranking, floor or cell plan was tuned after trial evidence. VER-002 (`pass`) confirmed the repair three ways, including one bounded live call under the real T2 schema, re-derived every number with its own implementation, ran the planning and documentation suites, and raised four advisories, one of which (A1′) meets D007's own reopening condition. I judge both verdicts correct and the sequence complete. The verifier's live call and its instrument disclosure were bounded and honest; its non-routing of A1′ was the right cost judgment, and this review boards that advisory up in D012 rather than leaving it as a sentence.

## Findings, observations and limits

**No finding.** Nothing routes to repair.

**Observations, to planning.**

1. **Grammar-constrained validity is not contract understanding.** Local T1 returned five schema-valid envelopes whose fields are a truncated prose fragment and bare commas. A future T1 qualification should report field-content sanity beside schema validity, or the validity rate will keep flattering a model that never read the contract.
2. **The T1 minimum bound is undeclared** (VER-002 A1′). Boarded up as D012 with its named follow-up; not repaired here because the fix changes the build hash the closed matrix is bound to.
3. **The integration helper regenerates before authored conflicts are resolved**, and its receipt string prints a doubled period. First-use evidence, recorded in D011's follow-up; nothing reached a projection here because `--continue` regenerates again.
4. **Four revision rows of one roadmap section entered the follow-up register during one order** (D004's write-back, D006's withdrawal, D009's correction, D010's restoration). The register's compatible-history design admits this and the union kept them; it is bookkeeping, but a planner reading the register sees the order's corrections as four revisions of one candidate.
5. **The rounding asymmetry** VER-002 A3′ names (−0.057142 against +0.057143) is cosmetic and crosses no threshold; the local T2 rationale in `r1` ends in a stray non-Latin character at its 320-character cut, and rationales are unscored.
6. **The evidence JSON files are mode 0600 on disk** because the collector writes them privately; Git records only the executable bit, so the committed files are ordinary. Harmless, but an operator listing the directory will see it.

**Limits.** I ran no live inference on either transport and did not hash the GGUF; the artifact's identity rests on the runner inventory recorded at episode time and on WO-137's pin, as both verifications said. My hand re-derivation covers one record from each of the six baseline groups, all five local T1 repeats, and the T3 factor and injected cells; the retained `audit.mjs`, which I executed, and the verifier's independent implementation cover all 38. Latency, throughput and thermal observations are single-host, single-session values. No no-egress property is established or claimed, and no role that reads private material is qualified. The operator's 60 seconds is self-reported inside a 4 min 48 s wall-clock window and is recorded as such.

## Goal alignment at handoff

The promised benefit was a measured floor for the local actor kind consumed by the catalog and by the next qualification, replacing the standing assumption that local episodes stay inspection-only indefinitely and its opposite. The observed outcome is one narrow, host-checked read-only role and two named next qualifications, which is what M5 on the 2026-09-17 ladder asked the pilot's table to say. Against the traps: **rule beating** is answered by host-computed validity and agreement, a held-out ranking, a comparator that must return five model outputs before a comparison floor can pass, and typed failures that retain the model's actual output; **drift** by floors pre-registered in the order and applied unchanged through two recollections; **seeking the wrong goal** by keeping `inconclusive` when only one of three tasks qualified and by refusing a leaderboard; **success to the successful** by measuring the remote comparator under the same input, prompt, schema and build and reporting its misses too; **tragedy of the commons** was real and is visible (76 live episodes in repair beyond the 38 registered, three gates, one operator-authorized cap exception) and is bounded by the closed matrix; **escalation** added one probe, one fixture, one suite registration and no new gate, lane or dependency; **shifting the burden** is limited to one 60-second ranking and the executor's cleanup of the runner, both recorded; **policy resistance** appeared between the emitted schema, the host validator, the collector and the evaluator and was resolved at their existing interfaces. The platform lens holds: the result arrives as a catalog row, a packet, a machine-readable `results.json` and content-addressed episode records that a stranger to the session can recompute, as the audit and both verifications did. NoOp would have left an unsupported catalog assumption in place; integrating without regenerating would have left stale projections. My own intervention was bookkeeping only: a union of two paragraphs and two decision records.

## Release-close request and publication

This dispatch authorizes committing the reviewed state, pushing the `wo-138` branch and opening its pull request; it does not authorize a merge, a push to `main`, a tag, a Release or a package publication. The PR body beside this report carries the process-meter table the release preparation supplied and receives the product-gate record from the publish helper; the five-section release notes follow the publication contract, one physical line per prose paragraph, and disclose the `inconclusive` outcome, the single qualified role, the repair history, the two boarded-up defects and the evidence-only scope. Commits are made in coherent series after the result is recorded, with plain subjects and no attribution. After the operator merges, release close consumes the committed gate row without running a suite.

## Cost and instrument disclosure

Process cost at entry: 2 steps, 1 command, 84,770 total tokens, source `claude-transcript-message-usage`, scope `dispatch`, cutoff 2026-09-21T15:36:14.622Z; `costUsd` null and therefore unknown. At handoff, 2026-09-21T16:03:19.651Z: 96 steps, 90 commands, 5689313 total tokens; reasoning tokens and USD unknown, and the counter's own note says the wrapped command count may be incomplete. Subagents: 0 of 20, exact-observed, uncounted remainder unknown; the planned fan-out was zero and it stayed zero. The single product gate dominated the waiting and was not optional, because the integrated code identity is new and it is the identity the release manifest will consume; before spending it I ran the WO-138 suite, the audit, the four printed affected checks, the formatter, the planning check and the index check, which together took under two minutes and would have caught an integration failure cheaply. The integration itself, two helper passes and one union, took about four minutes of wall clock. The instruments here are the subject's own probe (whose `evaluate` path I did not rerun, since VER-002 did and the audit is independent of it), the retained audit script, the gate-evidence module for code identity, and the WO-079 helper for integration; the helper's behavior is reported above as observed rather than assumed, and the handoff counters are in this dispatch's ignored receipt and in the response.
