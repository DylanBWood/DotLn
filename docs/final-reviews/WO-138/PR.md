# WO-138

The local actor kind has its first measured, qualified role. A pilot ran three read-only inspection tasks with deterministic oracles against the pinned local Qwen3.6 27B artifact and one remote inspection transport, five repeats each under the same input, prompt, schema and probe build, and decided which roles the catalog may hand to the local kind. One qualifies: ranking the discovery producer's six candidates for a seeded repository against the operator's held-out order, where the local model returned five schema-valid rankings at Spearman 0.828571, the same median as the remote model, in a median 12.1 s with no operator intervention. Receipt abstraction (one field in ten) and hook-journal classification (ten rows in twelve) miss their pre-registered 0.9 floors and trail the remote model's 1.0, so the pilot's outcome is `inconclusive` and the catalog row says exactly that.

**What the row means and does not.** Product 03 gains a dated row qualifying the local kind, for committed public inputs only, to rank T2's six candidates under its fixed read-only contract with host-validated coverage. It is not a capability level, it makes no no-egress claim, and it qualifies no writing, implementation, independent verification or private-input role. Product 06 records the `inconclusive` disposition and the two next qualifications: a fresh fixed-schema accuracy qualification for T1, and a separately registered repeated qualification for T3 with its label-definition support, whose single cell reached 1.0 against a 0.833333 baseline.

**The evidence and the instrument.** `docs/evidence/WO-138/` holds the packet, 38 episode records that each name the probe build hash, the artifact SHA-256 from the runner's inventory and the hashes of their input, prompt and schema, the pinned inputs both transports consumed, `results.json`, both run manifests, the operator's ranking with its self-reported 60 seconds, an independent audit script, and eight retained attempt sets that explain their own exclusion. The probe under `scripts/probes/` performs no inference on import, refuses to run beside a visible product gate and discards any episode during which one starts, validates every returned object in host code, keeps invalid model output as a typed failure, and redacts credentials from remote diagnostics; thirteen deterministic tests over loopback doubles and the committed records run inside `npm test`.

**The repair history a reviewer should know.** VER-001 found that every remote T2 request had been rejected before inference because the emitted schema used `uniqueItems`, which that transport does not permit, and that the collector had discarded the diagnostic, so a comparator floor was recorded as met without being measured. The repair removed the keyword while keeping the host's duplicate rejection, retained remote error events, made an unmeasured comparator an explicit `null` that cannot qualify a task, declared the host's 40-character T1 bound in the schema, and recollected the full matrix under one build twice, the second time after the executor's own strengthened provenance check caught that a failing-test candidate's stderr digest embedded its scratch path and had drifted between the two arms. The operator authorized the 17 extra remote launches that took the repair session past its cap. Nothing in a prompt, oracle, ranking, floor or cell count was tuned after trial evidence. VER-002 confirmed the repair three ways, including one bounded live call, and passed.

**An adjacent planning repair.** The planning-continuation check now admits the equivalent article and sentence spellings of the evidence-only release header that release preparation writes, refusing a changed axis, description or article as before; six fixture combinations cover it (D008).

**Integration.** `main` moved five commits and published `v0.38.0` while this order was in flight. Final review fast-forwarded the branch onto it with `npm run worktree -- integrate WO-138`, the helper's first real use, resolved one authored conflict in the roadmap's release-boundary section as a union, regenerated the projections, and confirmed through release preparation that the staged `v0.38.1` still stands above the baseline. The helper's behavior is recorded as evidence in FINAL-001 and D011: it regenerates projections once before authored conflicts are resolved and again on `--continue`, which is harmless here and is named as a follow-up.

**Validation.** `npm test -- --review` passed **24 suites, 0 failed, 269.74 s, 68 fresh tasks, exit 0** at code identity `14584332` on the integrated tree, after every reviewed change and every new source file was staged. The retained audit reproduces all six distributions from the 38 records; the WO-138 suite passes 13 of 13; `publication:check` reports both editions current at 30 and 45 linked sections; `harness check` reports 31 generated surfaces with no drift; `release check-surfaces --local`, `format:check`, `plan check` and `work-orders index --check` pass; `git diff --check` is clean in the working tree and the index. Component versions and dependencies are unchanged.

**What a reviewer should know before merging.** The local T1 envelopes are schema-valid but degenerate (a truncated prose fragment and bare commas), so the validity rate is not contract understanding; the packet says accuracy did not improve, FINAL-001 says why. The T1 schema does not declare the host's minimum field length, and one retained unexpected-input cell was rejected on that gap after blocking correctly; it is boarded up as D012 because the fix changes the build hash the closed matrix is bound to. The remote comparator is one model over one transport, the T2 oracle is one person's ordering of one fixture, and the latency and throughput figures are single-host. The diff is large in lines because the 38 records and the retained attempts are JSON; the source change is one probe module, its test and fixture, one suite registration, and a nine-line normalization fix with its test.

Evidence: [FINAL-001](docs/final-reviews/WO-138/FINAL-001.md), [VER-001](docs/verifications/WO-138/VER-001.md), [VER-002](docs/verifications/WO-138/VER-002.md), [decision packet](docs/evidence/WO-138/decision-packet.md), [decisions D001 to D012](docs/evidence/WO-138/decisions.md), [release notes](docs/final-reviews/WO-138/RELEASE-NOTES.md), [the order](docs/work-orders/WO-138-local-model-role-qualification.md).

<!-- dotln-process-meter:start -->

Observation cutoff: 2026-09-21T15:46:13.498Z; source: canonical control events and the recorded gate, usage and harness observations collected by npm run meta.

| Work | Phase ms / attempts | Gate ms | Read files / bytes | Observed tokens / USD | Declared prompt tokens | Corrections |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| WO-110 | 7,158,026 (Δ unavailable) / 5 | unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 0 (Δ unavailable) |
| WO-099 | 16,656,405 (Δ 9,498,379) / 13 | unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 0 (Δ 0) |
| WO-079 | 4,418,943 (Δ -12,237,462) / 3 | unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 0 (Δ 0) |
| WO-069 | 8,532,758 (Δ 4,113,815) / 5 | unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 0 (Δ 0) |
| WO-071 | 4,477,821 (Δ -4,054,937) / 3 | unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | unavailable (Δ unavailable) | 0 (Δ 0) |
| WO-138 | 10,115,818 (Δ 5,637,997) / 4 | 527,591 (Δ unavailable) | 3 (Δ unavailable) / 24,969 (Δ unavailable) | unavailable (Δ unavailable) / unavailable (Δ unavailable) | 669 (Δ unavailable) | 1 (Δ 1) |

Unavailable observations are not zero; unset ceilings are not approvals of a future limit.

| Dispatch | Wall ms (Δ) | Context bytes (Δ) | Commands (Δ) | Tokens (Δ) | Steps (Δ) | USD (Δ) / declared prompt tokens |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| WO-138/executor | 9,257,502 (6,496,216) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) / 669 |
| WO-138/verifier | 858,316 (158,168) | 1,989 (unavailable) | 175 (unavailable) | 20,500,631 (unavailable) | 179 (unavailable) | unavailable (unavailable) / unavailable |
| WO-138/reviewer | 10,376 (-1,006,011) | unavailable (unavailable) | 68 (unavailable) | 84,770 (unavailable) | 69 (unavailable) | unavailable (unavailable) / unavailable |
| WO-138/release-close | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) / unavailable |
| WO-138/planner | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) / unavailable |
| WO-138/refuter | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) | unavailable (unavailable) / unavailable |
<!-- dotln-process-meter:end -->
