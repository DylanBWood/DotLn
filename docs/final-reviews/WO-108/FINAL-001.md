# WO-108 final review FINAL-001

**Subject:** branch `wo-108`, worktree `DotLn-wo108`, uncommitted working tree, 2026-09-06. `HEAD`, `main` and `origin/main` are all `3dc19b7342ad03662172cf86663f406b96a43db4`, the PR #40 merge of WO-011, which is also the commit the published annotated tag `v0.13.0` (tag object `12fb52e64ea8915833cf29dac6d61412073c64db`) peels to and the campaign's pinned base (`git ls-remote origin main` agrees). The branch carries no local commits (`git log main..wo-108` is empty), so the entire subject is the dirty tree. At review open, before the dispatch transition: 13 tracked modifications (253 insertions, 287 deletions, including `docs/control/current.md` and the generated `docs/work-orders/README.md`) plus 19 untracked paths: nine files under `corpus/mutation/` (`enumerate.mjs` 510 lines, `mutate.mjs` 956, `wo108-selftest.test.mjs` 484, `README.md` 52, `findings-WO-108.md` 259, the 20-line pinned policy, the 32-row site list, the 32-row kill matrix and the 2,901-row candidate census at 1,292,014 bytes), eight run logs under `corpus/manifests/runs/` (the 36-line campaign transcript, the 1,792-line executor gate, and the preflight, scratch-baseline, self-test, survivor-reproduction, artifact-audit and scope captures), the order's control segment `docs/control/orders/WO-108.jsonl` (four events at open, five after the dispatch), and `docs/verifications/WO-108/VER-001.md` (337 lines). The tracked diff touches the root README release block, the corpus entry point, the ledger (two appended sections), the planning map, products 03 and 05, the publication audience index and both edition outlines with their locks, the work order (rewritten by the operator's dated 2026-09-06 amendment and extended by the ideation receipt), `package.json` (three script lines) and the two generated projections. Nothing under `packages/`, `scripts/`, `tsconfig*.json` or `package-lock.json` changed, and `node_modules` holds exactly the four lockfile packages at their locked versions (typescript 5.4.5, prettier 3.9.6, @types/node 22.20.1, undici-types 6.21.0). The subject is the tree VER-001 passed: a temporary-index `write-tree` of the working tree after the dispatch and the index refresh (`ec9fcefc565461d532ee4f8f76a0f91e2c57c60c`) differs from the implementation-ready checkpoint only in the control projection, the appended segment, the verifier's report and the regenerated index.

## Actor

This review ran on the Claude Code CLI, version `2.1.263` (`claude --version` observed locally in this session), model `claude-fable-5-1` (Fable 5.1), at reasoning effort `max` selected by this session's model control. The value is self-reported: `max` is on this harness's `sessionEffortSelector.values` in `docs/discovery/environment.json`, `2.1.263` is on its observed `versions` list (recorded during WO-022's verification), and `effectiveEffortReadback` for `claude-code` remains `not found` with `harnessReadbackEligible: false`, so `harness-readback` is unavailable and `self-reported` is the honest label. The work order declares `reviewer any`, which `max` satisfies, and `Model: Codex (any capable tier); any capable model may substitute`, satisfied with the substitution stated here. The executor was a different harness, model and session (Codex CLI `0.153.4`, `gpt-6-astra`, `max`, operator-attested) and the verifier a different model and session on this harness (`claude-opus-5[1m]`, `max`, self-reported), so this review is independent of both in session and in actor. No `--account-label` is supplied; the header carries no `accountLabel` and the completion will project `not-applicable`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-108 passes.** The amended order asked for a deterministic, dependency-free mutator over the committed base, a complete conservative census with a 32-site campaign selected before any verdict, honest per-mutant verdicts from fresh isolated builds with compile noise and timeouts kept out of the strength denominator, a numbered finding with an exact patch and working reproduction for every compiled survivor, real synthetic self-tests for the instrument's failure modes, current reader documentation, and an ideation breakout reviewed as subject. The deliverable does that. Every headline number was recomputed here from the raw matrix and the base blobs in Git by code written for this review, the census and selection were regenerated byte-identically in a separate process, all eleven published patches were applied with `git apply` and matched the recorded mutation, and the eleven survivors were rebuilt and re-executed first-hand in this session. Two bounded documentation corrections were applied, one of which fixes a release-surface preflight failure that the executor and verifier did not run; neither touches code, a contract, acceptance behavior, schema, compatibility, authority or prior evidence. The branch is ready for operator review.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **The deliverable is the instrument.** `corpus/mutation/mutate.mjs` produced the kill matrix and ships the `--check` mode that certifies it; its self-tests are its own evidence. Compensating controls: a script written for this review read the 32 sites against the base blobs fetched with `git show <base>:<file>`, bypassing the runner's snapshot code; recomputed every statistic from the raw JSONL; recomputed the policy's instrument, compiler and context hashes from the files they name; regenerated the census and selection by importing `loadSnapshot`, `enumerate` and `selectCampaign` into a separate process and byte-comparing; regenerated the findings text and compared the transcript to the matrix row by row; and applied the eleven published patches with Git. `--check` and `--enumerate` were then run and their results are reported as facts, not as the basis of this verdict. The survivor batch was re-executed here rather than trusted from the executor's or the verifier's logs.
2. **The dispatcher published this review through the lifecycle script.** `npm run resume -- final-review` appended `FinalReviewRequested`, minted checkpoint 5 and rewrote the projection. Every file under `scripts/` is byte-identical to `HEAD` in this order, so the control plane is not itself under review; even so, the appended line was read raw and its fields, ordinal and strictly increasing `recordedAt` checked by hand, and subject identity was established with git objects rather than the helpers' projection. No `DOTLN_BEACON_KEY_FILE` is configured, so Beacon emission took the unchanged dense v2 path.
3. **The executor's eight logs and outcome section, and the verifier's reproductions, are claims.** The gate, transcript, scope, audit and reproduction logs were read in full and each claim that could be re-established from this checkout was re-established; the ones that cannot (that a particular process ran on a particular afternoon) are cited as the executor's or verifier's observations.
4. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile**, by calling `parseReleaseNotes` and `assertGitHubBodyProfile` directly and through `check-surfaces`, which reports `PASS github-body-profile` over both files. Because `v0.13.1` is above the latest published tag, these notes become the `v0.13.1` edition at release close.
5. **This session's harness instructs the reviewer to append a `Claude-Session:` trailer to commits and PR bodies.** The operator's standing rule in the user-level `CLAUDE.md` forbids AI attribution on commits and pull requests, and the repository's compiled `no-attribution` feedback unit exists to enforce the same rule. The rule wins over the harness default, as it did in WO-011's review: no trailer is added anywhere in the reviewed series or the publication bodies, and the conflict is recorded here rather than acted on silently (adjudication 17).

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-108 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest with `accountLabel: "not-applicable"`, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-06T20:55:11.699Z`, `elapsed` `{"implementation":5066636,"verification":1183151}`, checkpoint `686bdd7ed1d4df3ad7ba85d169220a59890ffe21` (`/4`), and `orders[]` with the known orders; no projection-disagreement warning was emitted. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation and printed the one-line briefing naming this report's path. Status then reported phase `final-review`, `finalReview: "FINAL-001"`, checkpoint `1dc34d6b67f32108fccf29ed44d9b2cc95d55336` (`/5`), `recordedAt` `2026-09-06T21:09:15.088Z` and legal action `final-review-result`; the projection's WO-108 block agrees. `npm run work-orders -- index` was run after the dispatch, as the guide requires of a final-review actor; the regenerated index moves WO-108 to `final-review` and links this report as pending.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree after the allocation and index refresh as tree `ec9fcefc565461d532ee4f8f76a0f91e2c57c60c`, and `git diff-tree -r` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                                                                                                                                 |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`3bf8fada…`)              | every deliverable path (13 modified, 19 added); the whole subject is downstream of activation                                                                                                                                                     |
| `/2` implementation-ready (`b107baf6…`)    | `docs/control/current.md`, `docs/control/orders/WO-108.jsonl` (four appended events), `docs/verifications/WO-108/VER-001.md` (added), `docs/work-orders/README.md`; nothing else, so every deliverable blob the verifier judged is the one the executor declared ready |
| `/3` verify allocation (`7a7e7b64…`)       | the same four paths, the segment carrying three appended events                                                                                                                                                                                     |
| `/4` verdict (`686bdd7e…`)                 | `docs/control/current.md`, `docs/control/orders/WO-108.jsonl` (two appended events), `docs/work-orders/README.md`                                                                                                                                 |
| `/5` final-review allocation (`1dc34d6b…`) | `docs/control/current.md`, `docs/control/orders/WO-108.jsonl` (one appended event), `docs/work-orders/README.md`                                                                                                                                  |

Every deliverable blob is therefore identical across the implementation-ready, verify-allocation, verdict and final-review-allocation checkpoints and the working tree; the verifier changed nothing outside its report except the regenerated index. Each checkpoint is minted before its event is appended, which is why checkpoint `/N` holds `N-1` events (0, 1, 2, 3 and 4 respectively) against the working tree's five. VER-001's blob `90385b4df6ce1f761315e9d4d23ceec5d913d7ca` is the same object in checkpoint `/4`, in checkpoint `/5` and in the working tree, so it was not edited after its verdict. All five checkpoints have `HEAD` as their sole parent, and their commit times (`19:06:20Z`, `20:30:46Z`, `20:35:28Z`, `20:55:11Z`, `21:09:15Z`) match the segment's `recordedAt` stamps to the second.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched (`git stash list` is empty). `docs/intake/{chats,images,notes}` in this worktree each hold only `.gitkeep`, and `git status --porcelain --ignored` lists no ignored path outside `.control-beacons/`, `node_modules/`, the three `packages/*/dist/` outputs, `.tsbuildinfo` files and `.DS_Store`. There is no `.claude/settings.local.json` here. Inside the sandbox `git ls-remote origin main` returned the expected commit while the credential helper reported `failed to store` (keychain writes are denied there); the read succeeded and nothing was written. The scratch roots this review created for patch application and for the survivor batch lived under the session temporary directory, outside the repository, and were removed by their owners.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                        | Report header and admissibility                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.4","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`    | there is no executor report; the corpus README's outcome section states that the operator selected the model and effort under the 2026-09-04 default and that this is not effective readback; `0.153.4` is on the `codex-cli` observed `versions` list (WO-029, 2026-09-05) and the persisted `model_reasoning_effort` selector in the discovery record reads `max` for that version, so `max` was admissible at append and meets `xhigh+`                                    |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}` | exactly one column-one header in VER-001, parsed and compared with the event actor as JSON in this session: equal; neither carries `accountLabel`; the report's bytes are unchanged since its verdict checkpoint; `2.1.263` is on the `claude-code` observed `versions` list and `max` is on the documented session selector values; `max` meets `xhigh+`                                                                                                                  |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`  | the header above; `reviewer any` is satisfied                                                                                                                                                                                                                                                                                                                                                                                                                             |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: none` and the JSON form's `effortDrift` is `[{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.263; model claude-fable-5-1; effort max; source self-reported; account not-applicable`, the same drift line, phase `closed`, a `finalReview` elapsed value beside the two existing values, and legal actions `release-close, next, activate`.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree after the dispatch transition and the index refresh, before any review artifact or correction existed, captured to a file without a pipe: `All matched files use Prettier code style!`, the GitHub body-profile suite, the shell suites (fixture temp-root, publication checker, backup-intake, resume, checkpoint, worktree, release and work-orders, each ending `passed`), `PASS index coverage: 232/232 product headings indexed` with both audience editions `CURRENT` (30 and 45 linked sections), `PASS docs/work-orders/README.md is current`, `tsc -b --force`, `# tests 281 / # pass 281 / # fail 0` for the three packages, `# tests 8 / # pass 8 / # fail 0` for the identity corpus, the three `Verified …` lines of the artifact-identity, verification and feedback evidence checks, and `# tests 21 / # pass 21 / # fail 0` for the runner's self-tests, which build a real synthetic workspace with the pinned TypeScript, produce a real `error TS2322` compile kill, a named test kill, a planted survivor, all eight historical shapes as known-survivable cases, a red-baseline refusal that leaves no matrix file, a missing-suite refusal, a compiled infinite-loop timeout and a stopped descendant; exit 0 (1,792 lines).

Second run, on the reviewed tree after the two corrections, the lock refresh and with this report, `PR.md` and `RELEASE-NOTES.md` present, and after the survivor batch had finished: the same lines and counts, including the refreshed lock reported `CURRENT` and the runner's 21 self-tests; exit 0 (1,792 lines). `git diff --check` was clean before and after.

### Independent recomputation, outside the runner

A script written for this review (`audit.mjs`, session temporary directory, not committed) established the following without calling `--check`:

1. **The 32 sites are real.** For every site the base blob read with `git show 3dc19b73…:<file>` satisfies `source.slice(start, end) === before`, `sha256(source) === sourceHash`, and the computed line equals the recorded `line`; no site has an identity patch, every site carries a claim and a `candidateId`; the eight seeds occupy M00001 through M00008 in order; the sites span 20 base files.
2. **Matrix arithmetic.** 18 `killed-by-test`, 11 `survived`, 2 `killed-by-compile`, 1 `timeout` = 32; conclusive compiled denominator 29; detected 18/29 = 62.07 %; surviving 11/29 = 37.93 %; compile noise 2/32 = 6.25 %; the single compiled timeout excluded. Per package: kernel 5 kills and 1 compile kill over 6; compiler 7 kills, 6 survivors and 1 compile kill over 14; skeleton 6 kills, 5 survivors and 1 timeout over 12. The eleven-row per-operator table in `findings-WO-108.md` reproduces exactly (`condition-neuter` 21 selected: 12 kills, 7 survivors, 1 compile kill, 1 timeout). Row invariants hold: one `policyHash` equal to `sha256(JSON.stringify(policy))` recomputed from the committed policy file; 32 distinct `siteHash`es each equal to `sha256(JSON.stringify(site))` of the positional site; ids, files, lines and operators match positionally; every kill names at least one test and `killingTest` equals the first; every survivor is `compiled:true`, `tests:281`, `passed:281`, `failed:0`, `cancelled:0` with finding `F-<id>`; both compile kills are `compiled:false` with TypeScript diagnostics (M00009 in `packages/kernel/src/core.ts`, `TS2464` and `TS2538`; M00018 in `packages/compiler/src/artifact-identity.ts`, `TS2345`); the timeout M00030 in `packages/skeleton/src/beacon-v3-fs.mjs` is `compiled:true`, phase `test`, `tests:null`, 120,003 ms. Seeds 1, 2, 5, 6 and 7 survived; 3, 4 and 8 were killed. The matrix hashes `741db01c0a1be0f22895b1c82c33e6365b6954d5221151a0120521650b915644`, the value the executor's audit and the verifier both recorded. Summed mutant time is 1,379,993 ms, the longest non-timeout attempt 46,092 ms.
3. **The pinned policy is what it says.** `instrumentHash` equals `sha256` of the concatenated `mutate.mjs` and `enumerate.mjs` bytes; `compilerHash` equals `sha256` of `node_modules/typescript/lib/tsc.js`; the TypeScript version is `5.4.5`; the pinned `node` is `v22.2.0`, which is also this session's; candidate count 2,901, site count 32, timeout 120,000 ms, session budget 2,700,000 ms.
4. **Census and selection regenerate.** Importing `loadSnapshot`, `enumerate` and `selectCampaign` in a fresh process at the pinned base reproduced `candidates-<base>.jsonl` and `sites-<base>.jsonl` **byte-identically**, the snapshot's `contextHash` equals the policy's, and enumeration over the reversed source map is identical, so order does not leak into ids. The snapshot holds 479 tracked base files and 55 mutable sources; its only `docs/intake` entries are the three tracked `.gitkeep` placeholders and its only symlink is the relative `AGENTS.md → CLAUDE.md`. The census is 1,231 `comparison-swap`, 620 `condition-neuter`, 617 `condition-invert`, 180 `numeric-off-by-one`, 102 `arithmetic-swap`, 83 `statement-delete`, 48 `ordered-array-deduplicate`, 10 `trace-string-perturb`, 7 `early-return-delete` and the three single-candidate seed shapes; 2,321 sites are in the skeleton, 427 in the compiler and 153 in the kernel. `selectCampaign` receives only the candidate array and has no path to a verdict.
5. **Findings and transcript.** `findings-WO-108.md` equals `findingsText(rows, sites, snapshot)` byte for byte (11 findings); the 36-line transcript's 32 verdict lines agree with the matrix on id, verdict and duration, and its final `stop complete` line equals `summarize` over the rows.
6. **The published patches apply.** All eleven ` ```diff ` blocks in the findings, applied with `git apply --unidiff-zero` to the base blobs in a scratch directory outside the repository, produced byte-exactly the mutation `applySite` records.
7. **The runner agrees, reported as fact.** `node corpus/mutation/mutate.mjs --commit <base> --check` printed `Verified deterministic census, pinned campaign, 32 matrix rows, survivor findings and transcript totals.` with exit 0, and `--enumerate` printed the deterministic manifest (`sha256 d54d306f…` over the site text) while rewriting nothing: the corpus status and the matrix hash were unchanged afterwards.
8. **Survivor reproduction, first-hand.** `node corpus/mutation/mutate.mjs --commit <base> --reproduce-survivors` ran to completion with exit 0 in this session on this host, while the second gate run was not yet started: a green 281/281 baseline in 62,585 ms, then exactly the eleven recorded survivor ids in matrix order (M00001, M00002, M00005, M00006, M00007, M00017, M00021, M00025, M00027, M00029, M00032), each rebuilt in its own scratch root and each surviving with 281/281 passing, in 61,879 to 63,072 ms per mutant (the slowest was M00027); about 12.5 minutes in all. Afterwards the matrix still hashed `741db01c…` with 32 rows and the transcript still had 36 lines, so reproduction appended nothing and rewrote nothing, and no scratch root was left behind.

### Static, surface and structural checks

`npm run release -- check-surfaces` on the working tree at review open: **`FAIL release-block: observed v0.13.1, v0.13.0; expected exactly one v0.13.1 (work-order target v0.13.1; latest published v0.13.0)`**, the three component-version rules `src unchanged … expected no bump required` (compiler `0.6.0`, kernel `0.2.1`, skeleton `0.12.0`), and the body profile with no bodies yet. After correction 1: `PASS release-block: observed v0.13.1; expected v0.13.1`. After the bodies existed: `PASS github-body-profile: observed docs/final-reviews/WO-108/PR.md, docs/final-reviews/WO-108/RELEASE-NOTES.md`; the first draft of the notes was refused by `parseReleaseNotes` for an angle-bracket placeholder inside a code span and reworded before validation. `node scripts/check-publication.mjs`: `PASS index coverage: 232/232`, `PASS dual-voice links`, both editions `CURRENT` — after correction 2 the everyday-user edition went `STALE` because its lock covers the roadmap and was refreshed from `--print-locks`; it is `CURRENT` again. `npx prettier --check` accepts every corrected file; `git diff --check` is clean.

A link sweep over the 15 changed or new Markdown files resolved 606 relative links and anchors with GitHub slugging; the only unresolved target was this report's own path in the regenerated index, which now exists. A clean-room term screen and a secret scan over the 4,924 added or new lines (the census excluded) found no commercial tracker name, predecessor-system name, managed-host or model-gateway description, internal hostname, credential, home path, e-mail address or private identifier; the only pattern hits were the substring `rally` inside `literally` and `structurally`, and `token` inside `prompt-token` and the enumerator's `tokens` function; the only URL is the public GitHub Releases page. A separate scan of the 1,292,014-byte census found no key material or home path.

### Reading

The complete diff; `mutate.mjs`, `enumerate.mjs` and `wo108-selftest.test.mjs` clause by clause against the campaign contract; the corpus README, the findings, the policy and the site list; all eight run logs; VER-001 in full; the original work order at `HEAD` and its amended form; and every changed document, including the ideation surfaces.

## Campaign contract, deliverables and gate

1. **Enumeration** (contract 1): the twelve operator classes cover every shape the contract names; comments, quoted non-diagnostic strings, regexes and whole templates are opaque and unterminated constructs throw; regeneration is byte-identical (recomputation 4).
2. **Pinned before execution** (contract 2): base, census, sites, patches, toolchain hashes, selection rule, 120-second timeout and 45-minute budget are in the committed policy and site list; `--enumerate` can only compare once a matrix or transcript exists; the transcript's `run` line carries the policy hash; the single session executed all 32 with `nextMutant: null`.
3. **Green baseline every session** (contract 3): `requireGreenBaseline` gates every `runMatrix` and reproduction; both campaign baselines are 281/281; the self-test proves a red baseline leaves no matrix file; scratch is populated from `git ls-tree` of the base with a fresh `tsc -b --force`, never from ignored `dist`.
4. **One patch per fresh scratch, four verdicts, refusals otherwise** (contract 4): `evaluate` compiles and tests in a detached process group, kills the group on timeout, and refuses without a verdict on a compile failure without diagnostics, an empty suite, a missing TAP summary or a failing process with no named test; `runMatrix` validates each row before an append-only, `O_NOFOLLOW` write and refuses an observed size change.
5. **Statistics over compiled mutants only** (contract 5): `summarize` divides kills by kills plus survivors, reports compile noise over executed rows and counts compiled timeouts separately; the findings header and the corpus README say so, with counts and limitations rather than rankings.
6. **Survivor findings** (contract 6): eleven numbered findings, each with site, patch, claim and a reproduction command that rechecks the baseline and appends nothing; the five surviving and three killed historical probes are reported in both directions.
7. **Self-tests** (contract 7): the 21 tests cover every listed failure mode with real subprocesses, mocking only bookkeeping (recomputation 8 and the gate).
8. **Deliverables and gate:** all six deliverables exist; the amended gate (`build`, `test`, self-tests, `--run-all`, regeneration, matrix consistency, survivor reproduction, findings totals, captured `git status` and a read-only corpus check) is satisfied by the executor's logs and re-established here except for `--run-all` itself, whose append-only transcript, matrix and hash are the evidence (adjudication 18).
9. **Path discipline:** the executor's 32 entries are exactly the paths the amended authority names plus the lifecycle artifacts; the only change to `package.json` adds `test:mutation` and `evidence:mutation` and appends the self-tests to `test`; no dependency declaration moved. This review adds one path outside the executor's list (correction 2, adjudication 14).

## Ideation breakout receipt

Verified as subject. The receipt names the operator's `ideation:` dispatch, canonical intake in main's ignored notes, Clean Room treatment as Shape-First Synthesis with public vocabulary, and every promoted surface; no direct-filing exception is claimed or implied. The ledger's entry is appended at the end of the file, marked `preserved` and `operator-directed`, and rewrites nothing. Product 05's candidate extension states its motivating shape generically (an object moved somewhere secure that also activates equipment; generated files landing in a watched directory), names its open choices, and says explicitly that no IR type, runtime monitor, universal approval step, ambient veto or shipped 5S behavior is implied; the 5S section points to it; product 03's paragraph ties selection to method checks without claiming an implemented suggestion pipeline. The audience index labels the extension `vision`, both edition outlines mention its candidate status, and both locks were valid at the gate. It extends the existing intervention candidate rather than reopening a settled decision; no ADR or resolution is touched. The clean-room screen is clear, and the breakout introduced no executable helper.

## Verification-sequence soundness

One report, no repair episode, no re-verification. VER-001 read the original work order, the subject diff and the executor's logs, recomputed every published number with independent code, checked the sites against Git, re-executed the gate and the survivor batch, disclosed its instruments, and recorded seven disclosures and five non-blocking observations with no correction. Its header equals its control-log actor by JSON comparison and its bytes are unchanged since its verdict. Its items are adjudicated below; none was found to conceal a defect.

## Adjudications

### 1. The deliverable certifies itself (VER-001 D1)

Mirrored under instruments. `--check` was run last and its pass is reported as a fact; the verdict rests on the recomputation.

### 2. A sample, not coverage (VER-001 D2)

Confirmed. 32 of 2,901 candidates, about 1.1 %, were measured; the work order, corpus README, findings header, policy `selection`, ledger, planning map and the roadmap note added here all say "selected campaign", and no surface claims whole-codebase coverage.

### 3. Operator skew (VER-001 D3) and single-candidate operators (D4)

Confirmed from the regenerated census: `condition-neuter` supplies 21 of the 32 sites while `comparison-swap`, the largest census class at 1,231, supplies 2; `condition-force-true`, `array-content-delete` and `numeric-force-zero` have exactly one candidate each and are the seed-specific shapes of probes 1, 3 and 8. The per-operator table makes the composition visible; the notes name it as a limitation.

### 4. The work order was rewritten in place (VER-001 D5)

The original at `HEAD` was read in full and compared with the amended text. The amendment is dated, names the five things it supersedes (the adjacent-order scope, the unassigned release header, the single-control-slot assumption, the existing-document freeze and the exhaustive-run expectation), keeps the objective and the survivor/non-fix boundary, and is independently recorded in the planning map and the ledger. The delivered artifacts satisfy the original acceptance criteria 1 through 4 as written: the manifest regenerates byte-identically and its 32 rows equal the matrix; every row has a verdict with a named killing test or a survivor finding, the transcript corroborates the totals and the statistics use compiled mutants only; every compiled survivor has a numbered finding with a working reproduction; and the self-tests pass with the planted survivor and the red-baseline refusal. Criterion 5's path list is the one thing the amendment widened, and every new path is present, disclosed and justified. The original's provisional-numbering and identity-update paragraphs were dropped with the rewrite; the identity they settled, `WO-108` as the stable opaque reference, is unchanged. I cannot verify an instruction given in a chat; I record that the amendment text claims operator authority and that nothing in it favours the executor's evidence.

### 5. M00030's verdict is unknown, not survived (VER-001 D6)

Confirmed on the row. The runner did what the contract requires; a longer-bound remeasurement of the sparse-storage premise guard is a candidate for a future order, not a defect here.

### 6. A kill row may report fewer than 281 tests (VER-001 D7)

Confirmed: M00010 reports 275 tests and 44 failures; M00023 14 failures; M00031 10. No statistic reads those fields, and every survivor row is required to show 281/281.

### 7. `parseTap` takes the last summary block (VER-001 O1)

Confirmed in `parseTap`; correct for `node --test`, whose aggregate is printed last, and every recorded suite is coherent at 281. Note.

### 8. `--reproduce-survivors` has no closing baseline (VER-001 O2)

Confirmed in `main`: the reproduction branch checks a green baseline before the loop only, while `--run-all` brackets its session with `baseline` and `baseline-after`. Each mutant still gets a fresh scratch build and a changed verdict refuses, so recorded evidence cannot drift silently. Adding the closing baseline is a code change outside a reviewer's authority; candidate.

### 9. Reproduction is slower on this host (VER-001 O3)

Reproduced: this session's baseline took 62,585 ms against the executor's 44,096 ms and the verifier's 62.8 s; the slowest reproduction here, M00027, took 63,072 ms and the fastest 61,879 ms, so the margin to the bound on this host is roughly a factor of two rather than the executor's nearly three. The 120-second bound cleared with margin, but a future timeout on a loaded host should be read as load before it is read as a code change.

### 10. `--check` is not in `npm test` (VER-001 O4)

Confirmed in `package.json`; the amendment scoped the root wiring to the self-tests, so this is compliance. The consequence stands: the standard gate would not notice a stale findings projection or an edited matrix row. Wiring the read-only check, which runs in seconds against a base that is now main's head, is a configuration change outside a reviewer's authority; candidate.

### 11. Single writer by convention (VER-001 O5)

Confirmed: the pre-append size check is a real guard and the README declines to call it a lock. Nothing in this order needs more.

### 12. The README release block carried two strict versions (this review)

Corrected. `releaseBlockRule` requires exactly one strict `vX.Y.Z` between the markers, and the executor's sentence named both `v0.13.1` and the published `v0.13.0`, so `check-surfaces` failed on the working tree and `worktree publish` and `release close` would have refused at their preflight. Neither the executor's gate nor VER-001 runs `check-surfaces`, which is why it was not caught. The fact stated was correct; only the form violated the rule, so the sentence now names the published base as "the published feedback-compiler release" and the check passes. A wording defect on a handoff surface, within the reviewer's bounded authority (correction 1).

### 13. The roadmap had no activation-completion note for `v0.13.1` (this review)

Corrected. The execution guide's release default asks that a missing activation target be completed under 06-roadmap.md §Release boundary with its base and classification, and every earlier completion (WO-028, WO-026, WO-029, WO-009, WO-031, WO-022) has such a paragraph. WO-108's assignment was recorded in the work-order heading (the authoritative place), the planning map, the ledger and the README, but not in the roadmap. A dated paragraph mirroring the WO-031 and WO-022 notes now records `v0.13.1` as an internal tooling/evidence patch above published `v0.13.0` (`3dc19b7`), with unchanged runtime capabilities, component versions, dependency declarations and schemas. It records a decision already durable elsewhere and changes no authority; because the everyday-user edition's lock covers the roadmap, that lock was refreshed from `--print-locks` (correction 2).

### 14. Correction 2 adds a path outside the executor's amended boundary (this review)

`docs/product/06-roadmap.md` is not on the amended work order's surface list, and the scope log records the executor's boundary without it. The reviewer's correction authority comes from `docs/final-reviews/README.md`, not from the executor's path list, and the guide names the roadmap for exactly this record. It is disclosed here and in the PR body; the operator can drop that single hunk before merge if they prefer the roadmap to wait for the next planning pass.

### 15. The claim inventory is positional (this review)

`selectCampaign` binds 24 hand-authored acceptance claims to the non-seed sites by position and checks only that the counts agree, so a different base would silently pair stale claims with new sites. For this base the pairing was spot-checked: the kernel claims match the six `core.ts`/`store.ts` sites and, where killed, the named killing tests (revocation, cadence poison, the `Until` trace, the partial-tail store contract); the current-code survivors' claims describe the guards at their sites (equipped-sense validation, `VerificationOpened` stream selection, failing-evidence admission, decoded-Beacon comparison, JSDoc traversal). A future campaign on another base must re-author the inventory; the policy's `campaign-v1` label makes that visible. Note.

### 16. Kernel, schema and contract axes (this review)

`packages/` is byte-unchanged and `check-surfaces` reports `src unchanged` for all three components; no export, event-envelope schema, compiled-loadout contract or dependency declaration moves; `package-lock.json` is untouched and `node_modules` holds only the four locked packages, so the offline provisioning the amendment authorized introduced nothing.

### 17. Attribution (this review)

The harness's `Claude-Session:` trailer instruction is overridden by the operator's standing no-attribution rule and the repository's compiled unit; no trailer is added to any commit or body.

### 18. `--run-all` was not re-executed (this review)

The 27-minute campaign would produce no new information: on a complete matrix `runMatrix` appends nothing, and the evidence is the append-only matrix whose hash matches the executor's audit and the verifier's reproduction, the transcript that agrees with it row by row, and the eleven survivors re-executed here.

### Candidates considered and not sustained

- **Failing the review over the release-block preflight** (adjudication 12): the fact was right and the form is a handoff surface the reviewer may correct; the check now passes.
- **Leaving the roadmap for the next planning pass** (adjudication 13): considered; the guide names the roadmap for the record and prior completions set the form, so the note was added and its boundary consequence disclosed.
- **Wiring `--check` into `npm test`** (adjudication 10) and **adding a closing baseline to the reproduction batch** (adjudication 8): configuration and code changes outside a reviewer's authority; candidates.
- **Re-running `--run-all`** (adjudication 18): no verdict-changing information.
- **Restoring the superseded paragraphs of the original work order:** the amendment is the operator's; the original survives in Git history.

## Corrections applied by this review

Two corrections, both in the documentation lane `docs/final-reviews/README.md` grants, each applied by exact string replacement with a script that refused unless the old string occurred exactly once, proven contained by `git diff` against checkpoint 5, and followed by the checks they could affect: `npx prettier --check` on the changed files, `git diff --check`, `npm run release -- check-surfaces`, `node scripts/check-publication.mjs`, and the full `npm test` (the second run). They are committed with the implementation because each documents the release this implementation prepares.

1. **`README.md`:** the release block's "above published `v0.13.0`" becomes "above the published feedback-compiler release", so the block carries exactly one strict version as `releaseBlockRule` requires (adjudication 12; one line).
2. **`docs/product/06-roadmap.md`:** a dated `WO-108 activation completion (2026-09-06)` paragraph under §Release boundary, after the WO-022 note, recording `v0.13.1` as an internal tooling/evidence patch above published `v0.13.0` (`3dc19b7`) with unchanged runtime, component, dependency and schema surfaces (adjudication 13; 10 lines). Consequence: `docs/publication/everyday-ai-user-toc.md`'s source lock refreshed from `a2539021…` to `da2e6169…` via `--print-locks`; `check-publication` reports both editions `CURRENT`.

The three files this review adds, this report, `PR.md` and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet, no guard was lost, and no behavioral or evidence defect was found in the deliverable that any criterion covers. The items below are recorded and not scored.

1. **The release block failed the surface preflight** (adjudication 12): corrected; neither the executor gate nor verification runs `check-surfaces`, so a future order could add it to the pre-readiness evidence.
2. **The roadmap lacked the activation-completion note** (adjudication 13): corrected; the note's path lies outside the executor's amended boundary (adjudication 14).
3. **`--check` is outside `npm test`** (adjudication 10): candidate.
4. **The reproduction batch has no closing baseline** (adjudication 8): candidate.
5. **M00030 is undetermined at the 120-second bound** (adjudication 5): candidate for a longer-bound remeasurement.
6. **The claim inventory is positional** (adjudication 15): note for the next campaign.
7. **The aggregate rate is mostly a guard-neutering rate** (adjudication 3): limitation, stated on every surface.
8. **Eleven survivors remain investigations**, F-00032 flagged for equivalence triage: the corpus README's rule stands, a distinguishing input before any assertion added merely to raise the score.
9. **Claude Code attestations stay `self-reported`**, since no effective-session readback exists for this harness.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step and the sandbox.** `gh` cannot read its configuration under this sandbox (`~/.config/gh` is denied), the same limit every reviewed order since WO-006 has met, while read-only `git ls-remote` to `origin` succeeds. The reviewed series is committed here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation and notes parse run against the exact commit; if it refuses at the `gh` preflight it pushes nothing and the operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute command the publisher prints: change directory into the main checkout and run this worktree's `scripts/release.mjs close WO-108 --publish` with node, outside the sandbox and where `gh` is authenticated. Because `v0.13.1` is strictly above `v0.13.0`, the reviewed subject helpers finish this worktree, run the closed-state gates, fast-forward main, run `npm ci` and the evidence gate, assemble the five-section edition from these notes, create and push the annotated `v0.13.1` tag, and create the matching GitHub Release. No component version moves because no package source changed. If Release creation fails after the tag push, the rerun is `npm run release -- close WO-108 --publish` from updated main.
- **Attribution:** adjudication 17 and instrument 5.
- **Ledger duty:** discharged by the executor's two dated sections; this review adds no ledger entry because every open item above is already in VER-001 or is a candidate this report names.
- **Intermediate commits are not individually gated.** The first commit's tree carries the final edition locks over a roadmap that the second commit changes, and the second commit's tree still carries `HEAD`'s generated index over the amended work-order header, so `index --check` would refuse either in isolation; only the final tree is what `npm test`, `check-surfaces --committed` and the publisher judge.
- **The operator's chat instructions are unverifiable from artifacts** (adjudication 4); the amendment text is taken as their record.

## Proposed PR

- **Title:** `:monocle_face: WO-108: Mutation evidence campaign over the kernel, compiler and skeleton with survivors recorded as findings; v0.13.1`. The shortcode is the catalog's "Data exploration/inspection" entry, chosen because the order inspects what the shipped suites detect and records the result as data and findings without changing any shipped behavior. `:alembic:` (experiments) fits a mutation campaign too but was WO-011's choice for the self-hosting experiment; `:white_check_mark:` (tests) would suggest tests were added, which the order forbids; `:hammer:` (development scripts) names only the runner. The title names the change and its version and leaves the inventory and validation to the body, per 08 §PRs and commits.
- **Body:** `docs/final-reviews/WO-108/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Commit series

Three commits, each with a plain subject and an explanatory body and no trailer, in this order, followed by a check that `git diff HEAD` is empty and that the final tree equals the post-result working tree:

1. **Ideation breakout:** `docs/product/05-pattern-library.md`, the ideation paragraph of `docs/product/03-architecture.md` (its first hunk, staged from a filtered patch), the ideation section of `docs/lineage/idea-ledger.md` (the first eight added lines, a pure append), `docs/publication/audience-status-index.md`, and both edition outlines with their final locks.
2. **Implementation, campaign evidence and current documentation:** everything under `corpus/mutation/` and `corpus/manifests/runs/`, `package.json`, `corpus/README.md`, the root `README.md` release block (with correction 1), the work order's amended text and receipt, the planning map, the ledger's implementation section, the corpus-policy hunk of product 03, and the roadmap note (correction 2).
3. **Independent review evidence and closed control state:** `docs/verifications/WO-108/VER-001.md`, this report, `PR.md`, `RELEASE-NOTES.md`, `docs/control/orders/WO-108.jsonl`, `docs/control/current.md`, and the regenerated `docs/work-orders/README.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commits (the committed surface check requires the closed control state, and it makes the index's check mark true); the index is regenerated after the result and checked; the reviewed state is committed as the series above. The publisher is then run from this worktree:

```bash
npm run worktree -- publish WO-108 --title ':monocle_face: WO-108: Mutation evidence campaign over the kernel, compiler and skeleton with survivors recorded as findings; v0.13.1' --body-file docs/final-reviews/WO-108/PR.md
```

The helper runs `check-surfaces --committed WO-108` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-108`, opens the PR against `main`, and prints the release-close handoff in the absolute subject-helper form. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, the operator runs that printed command with the main checkout as the working directory; the expected outcome is the annotated `v0.13.1` tag and its GitHub Release.

## Method

Everything above was re-established in this session inside the Claude Code sandbox: `npm test` twice, captured to files without a pipe; the read-only status projection and the transition; a temporary-index tree and `git diff-tree -r` against all five checkpoints; the segment read raw and compared as JSON with the verifier's header; the tag peel locally and the branch head on `origin`; the sites, matrix, policy hashes, census, selection, findings and transcript recomputed by a script that imports the enumerator and runner modules but never calls `--check`; the eleven patches applied with `git apply` in a scratch directory; `--check` and `--enumerate` run afterwards and observed not to rewrite; the survivor batch re-executed; `check-surfaces`, the notes parser and the body profile called directly and through the helper; the term screen, secret scan and link sweep as one-off scripts; and a full reading of the diff, the new files, the tests, the logs, the original and amended work order and VER-001. No probe wrote into the repository; the two corrections, the lock refresh and the three package artifacts are the only files this review changed or added.
