## What this merges

WO-108, the `v0.13.1` internal tooling/evidence patch above published `v0.13.0`: a deterministic, dependency-free mutation-evidence runner and the first measured campaign over the current kernel, compiler and skeleton. `corpus/mutation/enumerate.mjs` and `mutate.mjs` use Node builtins and the already provisioned TypeScript, Git and Node toolchain. At the pinned base `3dc19b7` they enumerate a conservative lexical census of 2,901 single-site candidates (comparison swaps, condition inversion and neutering, arithmetic swaps, numeric off-by-one, statement and early-return deletion, trace-string perturbation, ordered-array deduplication; comments, regexes and templates opaque), select a 32-site campaign before any verdict exists (the eight historical compiler probes from WO-008's drill, then six kernel, six current compiler and twelve current skeleton sites, each the median eligible site of a declared file and preferred operator), and measure each mutant in a fresh, provenance-validated scratch root populated from `git ls-tree` of the base with local workspace links, a forced `tsc -b` and the three shipped `node --test` suites under a 120-second bound. Verdicts are `killed-by-compile`, `killed-by-test` with the named tests, `survived`, or `timeout` with its phase; anything else refuses without a verdict, a red unmutated baseline refuses before any row, and the kill matrix is append-only with a resumable validated prefix. Result: 18 killed by test, 11 survived, 2 compile kills and 1 test-phase timeout. Over the 29 conclusive compiled mutants the shipped suites detected 62.1 % and missed 37.9 %; compile noise (6.25 %) and the timeout are reported separately and never counted as strength. Three of the eight historical probes that survived WO-008's drill are now caught (missing active capabilities, duplicate ids, zeroed prompt-token costs); five still survive and six current-code sites join them. Each of the eleven survivors is a numbered finding with its exact patch, the acceptance claim to investigate and a reproduction command; none is fixed and no assertion is added to any shipped suite. `npm test` gains the runner's 21 synthetic self-tests. The kernel, compiler and skeleton packages, their tests, the lockfile and every dependency declaration are byte-unchanged.

- **Determinism and pinning (contract 1 and 2):** the census and selection regenerate byte-identically from committed source alone, in a separate process and independent of source-map order; the policy pins the base, context hash, instrument and compiler hashes, Node and TypeScript versions, selection rule, timeout and session budget; after the first measured run every pinned artifact can only be compared, never rewritten.
- **Honest verdicts (contract 3 and 4):** every session and reproduction rebuilds an unmutated baseline first and refuses unless all 281 tests pass; each mutant compiles and runs in a detached process group that is killed on timeout; a compile failure without TypeScript diagnostics, an empty package suite, a missing TAP summary or a failing process without a named test are refusals, not kills; a survivor whose test count differs from the baseline is recorded as killed by a named runner assertion.
- **Statistics (contract 5):** kills divided by kills plus survivors over compiled conclusive mutants only, with compile noise over executed rows and compiled timeouts excluded and disclosed; package and operator breakdowns are counts of this selected campaign, not capability rankings.
- **Findings (contract 6):** `findings-WO-108.md` is a reproducible projection of the matrix with one finding per compiled survivor; all eleven patches apply with `git apply` and produce the recorded single mutation; `--reproduce` and `--reproduce-survivors` recheck the baseline and append nothing; `M00030` (the v3 Beacon sparse-storage guard) is recorded as undetermined at the bound, not relabelled.
- **Self-tests and gate (contract 7):** 21 tests build real synthetic workspaces with the locked TypeScript and prove a real `error TS2322` compile kill, a named test kill, a planted survivor, all eight historical shapes as known-survivable cases, a red-baseline refusal that leaves no matrix, a missing-suite refusal, a compiled infinite-loop timeout, a stopped descendant that never writes, scratch-provenance refusals, Git-applicable rendered patches, and append-only resume with partial-tail, duplicate, wrong-policy and mutated-field refusals; mocking is confined to bookkeeping. The read-only `--check` re-derives census, selection, matrix consistency, findings and transcript totals in seconds.
- **Documentation and ideation breakout:** the corpus entry point and product 03's corpus policy now distinguish generated evidence corpora from policy-governed incident material, closing WO-101's layout follow-up; the work order's dated operator amendment, the planning map, the ledger, the root README release block and a new roadmap release-boundary note record the `v0.13.1` assignment; the operator-opened ideation breakout adds product 05's candidate extension "implementation and delayed consequences" with its 5S pointer, a product 03 paragraph on preserving a suggestion's purpose into execution, a preserved ledger entry, and `vision`-labelled publication rows with refreshed edition locks. The candidate selects no runtime mechanism, IR type, monitor, schema or approval gate.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` on the verified tree after dispatch and again on the reviewed tree: Prettier clean, the body-profile suite, every shell suite, `check-publication.mjs` at 232 of 232 headings with both audience locks current, `index --check` current, `tsc -b --force`, 281 of 281 package tests, 8 of 8 identity-corpus tests, the artifact-identity, verification and feedback evidence checks, and 21 of 21 runner self-tests; exit 0 both times. `git diff --check` clean.
- Independent recomputation by a script that imports the enumerator and runner modules but never calls `--check`: all 32 sites verified against the base blobs read with `git show` (exact byte range, source hash, line); matrix arithmetic, per-package and per-operator tables, row invariants and the matrix hash `741db01c…` recomputed from the raw JSONL; the policy's instrument, compiler and context hashes recomputed from the files they name; census and selection regenerated byte-identically in a separate process; findings regenerated byte-identically; the 36-line transcript checked row by row against the matrix; all eleven published patches applied with `git apply --unidiff-zero` to the base blobs and matched the recorded mutation. `--check` and `--enumerate` then passed without rewriting any pinned artifact.
- Survivor reproduction: `--reproduce-survivors` re-executed here with exit 0: a green 281/281 baseline (62,585 ms) followed by exactly the eleven recorded survivor ids, each rebuilt in fresh scratch and each surviving at 281/281 (61,879 to 63,072 ms per mutant); the matrix hash, row count and transcript length were unchanged afterwards.
- `npm run release -- check-surfaces`: the README release block first failed the exactly-one-strict-version rule because the executor's sentence also named the published base; after the reviewer's wording correction it reports `PASS release-block: observed v0.13.1; expected v0.13.1`, `src unchanged … no bump required` for all three components, and the body profile over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity: a temporary-index tree of the working tree differs from the implementation-ready checkpoint only in the control projection, the appended control segment, the verifier's report and the regenerated index, so every deliverable blob the verifier judged is the one the executor declared ready; all five checkpoints have `HEAD` (`3dc19b7`, also `main`, `origin/main` and the published `v0.13.0`) as their sole parent and their commit times match the segment's stamps to the second; `git log main..wo-108` was empty before the reviewed series; VER-001's bytes are identical in its verdict checkpoint and the committed tree; the verifier's report header equals its control-log actor by JSON comparison; the executor's `max` and the verifier's `max` meet the declared `xhigh+` floors.
- Screens: a link sweep over the 15 changed Markdown files resolved 606 links; a clean-room term screen and secret scan over every added line and new file, plus a separate scan of the census, found nothing.

## Verification trail

- `docs/verifications/WO-108/VER-001.md`: pass; every clause of the amended contract, all six deliverables and the executable gate reproduced with independent code rather than the runner's own check, the sites checked against Git, the gate and the survivor batch re-executed; seven disclosures and five non-blocking observations, none requiring repair.
- `docs/final-reviews/WO-108/FINAL-001.md`: pass; the verifier's items adjudicated with reasons, the reviewer's own observations recorded, the three-commit series confirmed, two bounded documentation corrections applied and re-checked, method and instruments disclosed.

## Known open items

- **Eleven survivors are investigations, not defects.** A survivor may be equivalent or outside exercised inputs; `F-00032` (JSDoc traversal guard) is flagged for equivalence triage. A future evidence-hardening order should establish a distinguishing input before adding any assertion merely to raise the score.
- **`M00030` is undetermined.** Neutering the v3 sparse-storage premise guard reached the 120-second bound without a TAP report; it is excluded from the conclusive denominator rather than labelled. A longer-bound remeasurement is a candidate.
- **This is a 32-site sample of 2,901 candidates**, weighted toward guard neutering (21 of 32 sites; `comparison-swap`, the largest census class, supplies 2). Every surface says so; the 62.1 % figure describes these sites only.
- **`--check` is outside `npm test`** (VER-001 observation 4), by the amendment's scope; wiring the seconds-long read-only check is a cheap candidate. **`--reproduce-survivors` has no closing baseline** (observation 2). **The 24 current-claim labels are bound to sites by position** with only a count check, so a campaign on another base must re-author them (FINAL-001 adjudication 15).
- **The release-surface preflight is not part of the executor gate or verification**, which is how the README block reached final review failing it; adding `check-surfaces` to pre-readiness evidence is a candidate.
- **The roadmap note lies outside the executor's amended path boundary** (FINAL-001 adjudication 14): the reviewer added the standard activation-completion paragraph for `v0.13.1` under the guide's release default; the operator may drop that single hunk before merge.
- **Publish and release close.** The bounded publisher runs the committed surface check and body validation before any push and then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses at that preflight the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.13.1` is strictly above `v0.13.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.13.1` tag, and creates the matching GitHub Release. No component version moves.
