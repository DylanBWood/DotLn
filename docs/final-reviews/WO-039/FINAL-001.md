# FINAL-001 — WO-039 Harness lowering (v0.15.0)

**Verdict: fail.** The order's eight acceptance criteria are met on the base VER-004 judged, and I re-established every one of them first-hand. The review fails on a single bounded finding that is not acceptance and not bookkeeping: the branch base moved while this order was in flight, and the integration this review window owns cannot be completed without new live feedback evidence, which is executor work and which a reviewer must not author and then certify.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}

Claude Code reported `2.1.263`. The model identifier and `max` effort are this session's launch selection as the harness exposes it; no effective-session readback exists for this harness, so the source stays `self-reported`. The order requires reviewer effort `any`, so no minimum is at issue. No account label was supplied.

## Subject and instrument

- Authority: [WO-039](../../work-orders/WO-039-harness-lowering.md), its cited sections, the incremental-private-list amendment, and the [ideation receipt](../../evidence/WO-039/ideation.md). The verification sequence is [VER-001](../../verifications/WO-039/VER-001.md) (fail), [VER-002](../../verifications/WO-039/VER-002.md) (fail), [VER-003](../../verifications/WO-039/VER-003.md) (fail) and [VER-004](../../verifications/WO-039/VER-004.md) (pass), with four repair receipts.
- Subject: the dirty `wo-039` worktree at `HEAD` `8b55eca3b3427146e98d34c68f67f679dc59bea5`. Physical cwd and Git root agree, Git registers this worktree once, and this session holds the sole writer reservation, which it reclaimed from a recorded dead owner through the generated guard.
- Integration base: `main` and `origin/main` are both `1937b023187a813f3b8053a221f819a15cdad244`. `git merge-base wo-039 main` is `8b55eca`, `git log main..wo-039` is empty, and `git log wo-039..main` names twelve commits across two merged orders: WO-032 (`v0.14.0`, the read-only actor board, which adds the `packages/console` workspace) and WO-109 (`v0.13.3`, the re-mining practice).
- Allocation: `FinalReviewRequested`, checkpoint `7d06029a14376c1018491c06daaca8b11c6c4ad0`, `refs/dotln/checkpoint/WO-039/19`.
- Reviewed subject content hash: `sha256:8a7d861c1b4787fb9ef07a65db46402e89a6d24603fd5d800258ef7bf0088e8a`, identical to the hash VER-004's host receipt bound its checks to. Nothing in the implementation subject changed between that verification and this review.

**Instrument disclosure.** This session runs under the bundle it reviews. The generated `UserPromptSubmit` hook resolved `resume: final review` to the reviewer role and injected the selected order path, the generated `PreToolUse` guards judged every tool call, and the generated skill supplied this procedure. That is direct evidence that the self-host works and it is also a self-referential instrument: this report is not evidence for the live smoke records, which were produced by separate scratch sessions. No branch commit was made. Only this report and the control/index bookkeeping the allocation wrote exist outside the implementation subject.

## Acceptance re-established on the recorded base

| Criterion | What I checked and observed |
| --- | --- |
| 1 — observed harness truth | The [phase-zero record](../../discovery/harness-smoke-2026-09-07.md) carries Claude rows C1–C8 and Codex rows X1–X3, and every capability in `contributorProfiles` cites one of them by anchor. Codex's four hook events are `available: false` with a stated reason, and `assertHarnessProfile` refuses an unavailable capability that does not explain itself. I screened all four discovery JSON files for absolute paths, user names, session ids and host names and found none. |
| 2 — pure, deterministic lowering | `lowerToHarness` takes no I/O and no ambient clock or randomness; the compiler fixture freezes its inputs, stubs `Date.now` and `Math.random` to throw, and proves two identical bundles plus unchanged inputs. Every emitted file carries the loadout id and semantic hash, `verifyHarnessBundle` re-derives each file hash, and the one-changed-unit fixture proves only files naming that unit differ. The frozen Seiri and Entropy Reducer semantic hashes `fnv1a64:9ca8d0229c6bd8db` and `fnv1a64:c5ddbca75f1c4cee` are re-checked at compiler `0.7.0` by the artifact-identity evidence edition. |
| 3 — one compiled predicate | `parity()` in `scripts/test-harness.mjs` computes the verdict directly from `feedbackBoundary`, spawns the generated hook as a subprocess on the same synthetic payload, asserts the decisions agree, then rewrites the hook with an empty policy and asserts it allows. The finish fixture additionally asserts all three eligible Stop units are exercised. Seventeen host tests pass. |
| 4 — recorded live smoke | The four current role records and both writer scenarios pin the installed file hashes, the runtime, the loadout identity and `2.1.263`. `reviewer-009` resolves `.claude/skills/dotln-reviewer/SKILL.md`, refuses the fixture attribution effect, finishes its observer, records zero native Read refusals and an empty `outsideDirectedSet` and `attemptedOutsideDirectedSet`, reaches the correction unit through the role skill with `operator.correction-token.confirmed` named as the missing capability, and releases its reservation. One Bash route refusal is recorded rather than hidden. |
| 5 — committed self-host | I emitted the Contributor bundle independently into a fresh temporary directory: 24 files, zero byte differences against the installed `.claude/` and `.agents/` surfaces, and the marked `CLAUDE.md` block identical. `harness check` runs inside `npm test`; the drift fixture covers content, missing, unexpected, manifest, unowned-replacement and symlink cases. The config log carries five dated entries and ADR-0005 carries the dated amendment. `emitHarness` can only write the four allowed project surfaces, so no user-scope write is reachable. |
| 6 — directional context | `harness-context --check` recomputes the stored measurement from the instruction file and the skill rather than from a hand-written list, and all eight profile/role rows are strictly lower. The instruction file alone falls from 3,878 bytes / 78 lines to 3,593 / 64, which I confirmed against the file. The dominant saving is the 64,993-byte execution guide the old cold-start order mandated against the 2,296-byte cited section the reviewer skill now names. The comparison uses a matched synthetic WO-999 task on both sides and says so. |
| 7 — write-backs | Present and consistent: 02 §Harness compiler v1 with the lowering table and residue rule, 03's implementation slice, 07's rewritten cold-start read order and resume-phrase dispatch, 13's engineer and devops rows and both table cells, the README release block, dated `compiler.harness-v1` and `harness.self-hosted` capability rows, the publication index rows, both edition locks and eight dated ledger sections including the rejected alternatives. |
| 8 — gate and boundaries | `npm test` and `git diff --check` both exit 0 in this worktree, bound to the subject hash above. No runtime dependency is added; `package-lock.json` changes only by the two workspace versions; the kernel is untouched. The operator's local terms list is present, the screen passes over 78 selected surfaces, and its synthetic-term fixture refuses without echoing the term. |

The residue block is 1,432 UTF-8 bytes and its declared count matches the bytes it carries. Each of the ten units lowers to its declared rung or appears in the residue with a named missing capability, and the three judgment units appear as role procedure plus `facts.*` residue exactly as the order specifies.

## Finding

**F1 — the required integration invalidates the feedback evidence edition and needs new live evidence.** Severity: blocking. Not an implementation defect in WO-039.

Observed. `main` advanced by two merged orders while this order was in flight, so this final-review window owns the merge under 07 §Independent workflows and integration. WO-032 added the `packages/console` workspace. `FEEDBACK_SOURCE_PATHS` includes `package-lock.json`, and `feedbackSourceFile` strips `version` and `license` only at the lockfile root and the three known workspace entries `packages/kernel`, `packages/compiler` and `packages/skeleton`. The new `packages/console` and `node_modules/@dotln/console` entries therefore stay raw in the feedback subject.

Reproduction. Building the projected feedback subject from the current tree yields `sha256:4a153ea7fac3d51e328798eea62ee7c1bd3c288953b9fbf83c727c2ca9c7fa4c`, which is exactly the subject recorded in `docs/evidence/WO-039/feedback/feedback.json`. Building the same subject with `main`'s lockfile plus this order's two workspace version bumps yields `sha256:bf3cf4b14ba5d6a9d0a82cad636309618e6d797d1f3888e1bc08650fae1698e6`. `validateSelfhost` in `scripts/feedback-evidence.mjs` asserts the recorded audit's subject equals `readFeedbackSource(root).subject`, so on the integrated tree `node scripts/feedback-evidence.mjs --check --edition WO-039` fails with `selfhost audit source is stale`, and that step is inside `npm test`.

Why this is not reviewer work. Re-recording the edition requires `--record-selfhost`, which validates a live `claude-cli-print` or `codex-cli-exec` verifier transport and a complete acceptance matrix, so it needs a fresh live feedback audit and a fresh live independent verifier episode. The alternative remedy, generalizing the projection to every workspace, is behavioural code in `packages/skeleton/src/feedback-audit.ts`, which is itself in the feedback subject and so still needs a fresh live edition. The guide permits a reviewer to regenerate projections and reconcile independent manifest fields, and states that workspace release versions and license labels do not demand another live audit; a new workspace is neither, and a reviewer never writes a behavioural fix and certifies it. Selecting the `WO-032` edition instead does not help: this order changes `packages/compiler/src/artifact-identity.ts` and `packages/skeleton/src/reactor.ts`, which are raw members of the same subject.

Lineage. WO-041 recorded this exact hole as a known limitation of its projection: a workspace beyond the four known entries stays raw and over-invalidates. WO-039 did not introduce it and cannot be blamed for it; the finding is that the integration this window owns cannot be finished without discharging it.

Bounded remedy for the repair dispatch: merge `main`, then re-record the `WO-039` feedback evidence edition from a live audit and verifier on the integrated tree, then return through a fresh verification of the affected claim only. Whether to generalize the workspace projection is a separate candidate and should not be folded in silently.

## Integration bookkeeping the same dispatch should carry

Seventeen files are changed on both sides. These resolutions are routine and behaviour-preserving; I name them so the repair does not rediscover them.

- `package.json`: keep `main`'s console steps in `test` (the `rm -rf`, the `ls` guard and the `node --test` list) together with this order's `test-harness.mjs`, `harness.mjs check`, `harness-context.mjs --check` and `harness-evidence.mjs` steps, and keep `--edition WO-039` in both `test` and `evidence:feedback`.
- `package-lock.json`: `main`'s lockfile with this order's `packages/compiler` `0.7.0` and `packages/skeleton` `0.13.0` bumps and the skeleton's compiler dependency pin.
- `README.md` release block: this order's `v0.15.0` text wins. The retiming is already recorded in 06 and needs no second note: `v0.15.0` remains a minor above the `v0.14.0` baseline the integrated ancestry exposes, and `check-surfaces --local` then compares components against `v0.14.0` rather than `v0.13.3`, where compiler `0.6.0` to `0.7.0` and skeleton `0.12.1` to `0.13.0` both still satisfy the changed-source rule and `packages/console` needs no bump.
- Append-only documents merge by concatenation: `docs/lineage/idea-ledger.md`, `docs/planning/capability-table.md`, `docs/planning/work-order-map.md`, `docs/product/06-roadmap.md`, `docs/product/04-interfaces.md`, `docs/product/13-uifa-roles.md`, `docs/publication/audience-status-index.md`, `.prettierignore` and `packages/skeleton/README.md`.
- Regenerate rather than merge: `docs/work-orders/README.md` (`work-orders -- index`), `docs/control/current.md` (lifecycle projection), and both publication edition locks.
- Regenerate `docs/evidence/WO-039/harness-context.json`. WO-109 inserted a paragraph at line 247 of `docs/product/08-publication-compiler.md`, inside §PRs and commits, which is the reviewer role's directed range `242-279`; the stored measurement will otherwise drift and `harness-context --check` will fail. The `before` side is unaffected because it reads the frozen snapshot at `8b55eca`.
- `docs/product/07-execution-guide.md`: WO-109's discipline paragraph and this order's rewritten cold-start section touch different areas and both belong.

## Observations, not findings

- **O1 — the evidence gate disables the session's own harness while it runs.** During `npm test`'s `rm -rf packages/*/dist && tsc -b --force` window, every generated hook's dynamic import of the built runtime fails, so the wrapper's fallback denies every tool call with `DOTLN_HARNESS_REFUSED: built adapter unavailable`. I hit this first-hand in this session and recovered when the build finished. The behaviour is fail-closed and transient and no gate result is affected, but the skeleton runbook tells a reader to rebuild after a runtime edit without mentioning that the mandated gate blocks the session that runs it. Worth a sentence in the runbook.
- **O2 — the three Stop gates judge every turn end, not a completion claim.** Already recorded honestly in the ledger with two candidate remedies and named as a separate order. I reproduce the assessment: because no branch commit precedes final review, every session on an order inherits the whole order's diff as its output obligation, and the re-entry rule keeps that from looping. Nothing here changes my verdict.
- **O3 — the directed-set scanner is sensitive to where a prose line wraps.** `readDirectives` is line-based, and `CLAUDE.md` carries a bare `` `@skills` `` on a line that happens to contain no read keyword. I confirmed that joining that sentence onto one physical line makes the scanner emit a bare `@skills` selector, which `directedReads` rejects as unresolved, failing `harness-context --check` and therefore `npm test`. The failure is loud rather than silent, and `.prettierignore` now excludes `CLAUDE.md` so no formatter will reflow it, so this is a fragility to know about rather than a defect.
- **O4 — the reviewer's directed set omits the review outputs it must read.** The cold-start table in 07 says the reviewer's inputs include the current review, PR and release-note outputs, but no skill directive resolves them, so the measurement does not count them. The skill's operative instruction, read every changed output at its current bytes, is task-dependent and cannot be a static path. Criterion 6 constrains what the floor directs, and the floor directs nothing uncounted, so this passes as written; the 338 fixture bytes involved are immaterial to the comparison.

## Executed checks

- `npm run harness -- evidence` exited 0 in this worktree. Its `npm test` completed with 288 rebuilt runtime tests, 17 harness tests, 21 mutation tests, 8 identity-corpus tests, 9 license tests and 7 release-preparation tests, all passing, with zero failures and zero skips in every TAP total. `check-surfaces --local` reported `PASS release-block: observed v0.15.0; expected v0.15.0` against latest local tag `v0.13.3`, and both changed components passed their bump rule. Publication reported 237 of 237 headings indexed and both edition locks `CURRENT`. `harness check` reported 24 generated surfaces with the local-terms list present, and `harness-evidence` reported 4 current live role smokes, 2 writer-reservation smokes and 78 screened surfaces. Its subsequent `git diff --check` exited 0.
- Independent emission: `node scripts/harness.mjs emit --out <fresh temp directory>` produced 24 files with zero byte differences against the installation, and the marked `CLAUDE.md` block compared equal separately.
- Independent residue accounting: the block's declared 1,432 UTF-8 bytes equal the bytes it carries.
- Independent feedback-subject computation: the two hashes in F1, computed through the built `feedbackSourceFile` and `feedbackContentHash` without writing anything.
- Base observation: `git rev-parse HEAD main origin/main`, `git merge-base`, `git log main..wo-039` (empty), `git log wo-039..main` (twelve commits), `git worktree list --porcelain` (two worktrees, this one registered once), and `git tag --sort=-v:refname` (latest `v0.14.0`).

## Limits of this review

I did not merge `main`, so no claim here is made about the integrated tree beyond the two computed hashes and the file-level overlap. Every acceptance claim above is about the base VER-004 judged, which is byte-identical to the tree I checked. The live smoke records are revalidated recorded observations, not fresh observations of those harness sessions. This session's own governed behaviour is disclosed above as a self-referential instrument and is used as evidence for nothing.

## What happens next

No PR body or release notes are prepared, because nothing is published from a failed review and a prepared body would imply a merge decision this review did not make. The next legal dispatch is `resume: fix`, which owns the merge, the fresh live feedback edition, and the routine regenerations named above; the affected claim then returns through a fresh independent verification, and final review resumes. Every checkpoint ref, verification report, repair receipt and evidence file is preserved unchanged.
