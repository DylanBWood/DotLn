## What this merges

WO-039, the `v0.15.0` minor above the published `v0.14.0`. A saved build now governs the sessions that actually run here. The new compiler target `harness-v1` lowers a loadout, its equipped feedback units and its authority envelope into enforceable harness configuration; the new **Contributor** identity is this repository's own session build; and its emitted bundle is committed as 24 generated surfaces — `.claude/settings.json`, eleven hooks, ten role skills across two skills roots, `.claude/harness-manifest.json`, and the marked block of `CLAUDE.md`. `npm run harness -- check` refuses drift and runs inside `npm test`. Every generated hook imports the pinned built `feedbackBoundary` and evaluates the same compiled unit the equipped feedback host evaluates, so there is no second behaviour system and no hand-written rule file. Claude Code `2.1.263` receives hooks, skills, deny rules and the instruction block; Codex CLI `0.153.4` receives skills and the shared block only, with every unobserved capability named as unavailable in the residue rather than assumed. Compiler `0.7.0`, skeleton `0.13.0`, console `0.1.1`; kernel `0.2.1` unchanged. No runtime dependency, no kernel change, no lifecycle or event-schema change.

- **Phase zero before any rule (criterion 1):** `docs/discovery/harness-smoke-2026-09-07.md` records, per harness, which events fired, the payload fields each hook received as shapes, how a refusal is expressed and whether the harness honoured it, how a project skill resolved, what `--bare` skipped, and what remained unobserved; absolute paths, session ids and host names are reduced to shapes before commit. Claude rows C1–C8 and Codex rows X1–X3 are explicit anchors, and every profile capability the compiler consumes cites one; `assertHarnessProfile` refuses an unavailable capability that does not explain itself.
- **Pure, deterministic lowering with origin locality (criterion 2):** `lowerToHarness` performs no I/O and reads no clock or randomness; the fixture freezes its inputs, stubs `Date.now` and `Math.random` to throw, and proves two identical bundles with unchanged inputs. Every emitted file names its origin unit or facet and the loadout's semantic hash, `verifyHarnessBundle` re-derives each file hash, and the one-changed-unit fixture proves only the files naming that unit differ. The Seiri and Entropy Reducer semantic hashes `fnv1a64:9ca8d0229c6bd8db` and `fnv1a64:c5ddbca75f1c4cee` are unchanged from compiler `0.2.0` to `0.7.0` against the WO-101 independent oracle.
- **One compiled predicate (criterion 3):** `parity()` computes the verdict directly from `feedbackBoundary`, spawns the generated hook as a subprocess on the same synthetic payload, asserts the decisions agree, then rewrites the policy empty and asserts the hook allows. The finish fixture additionally exercises all three eligible `Stop` units. A missing built adapter or changed pinned runtime bytes refuse before any effect.
- **Recorded live smoke (criterion 4):** four role records and two writer scenarios pin the installed file hashes, the runtime, the loadout identity and `2.1.263`. Each role resolves its skill by resume phrase and refuses a fixture attribution effect; the live-holder scenario proves writer refusal; all six finish their read observers with zero refused native reads and an empty outside-directed set. The unconfirmed correction token remains named residue with the unit reached through the role skill.
- **Committed self-host and drift refusal (criterion 5):** independent emission into a fresh temporary directory produced 24 generated surfaces with zero differing files against the installation, `CLAUDE.md` included as a whole file. The drift fixture covers content, missing, unexpected, manifest and unowned-replacement cases; `emitHarness` can write only four project surfaces, so no user-scope write is reachable. The config log carries its dated entries and ADR-0005 its dated amendment.
- **Directional context, mechanically derived (criterion 6):** all eight role/profile rows are strictly lower in bytes and lines, from roughly 71,000 bytes and 994 lines to between 8,439 and 14,420 bytes and 130 to 188 lines; the instruction file alone falls from 3,878 bytes / 78 lines to 3,593 / 64. The directed-read set is derived from the instruction file and the skill rather than from an author's list, counts late reads, and uses a matched synthetic task on both sides. The residue block declares 1,432 UTF-8 bytes and carries exactly 1,432.
- **Write-backs (criterion 7):** product 02 §Harness compiler v1 with the lowering table and residue rule; 03's shipped implementation slice and what remains of the delivery order; 07's rewritten cold-start read order and resume-phrase dispatch; 13's engineer and devops rows; the README release block; dated `compiler.harness-v1` and `harness.self-hosted` capability rows; the publication index rows and both edition locks; the ledger entries with their rejected alternatives.
- **Gate and boundaries (criterion 8):** `npm test` and `git diff --check` exit 0 on the reviewed tree; no runtime dependency is added and the TypeScript pin stays `5.4.5`; the kernel, `scripts/resume.mjs` and lifecycle legality are unchanged. The operator's local terms list is present and its screen passes over the selected generated, discovery, live and context surfaces; its synthetic-term fixture refuses without echoing the term.

## Evidence

Re-established first-hand at final review, not inherited:

- Subject identity: the host-check receipt for this review binds to subject `sha256:9cf5d7bd4bbaa1ab2cde3fe8bbe6c982bafad5512195600e559b84f12f6b3e9a`, which is the same subject hash VER-005's receipt recorded. The implementation this review judged is byte-identical to the one that verification judged.
- Subject continuity: comparing checkpoint 19 (FINAL-001's) with checkpoint 25 (this review's) path by path, and subtracting `main`'s twelve commits, no WO-039 implementation source changed. The compiler target, the lowering host, the Contributor loadout, every harness script, all eleven generated hooks, all ten generated skills, the manifest, the discovery record and the live records are byte-identical across the two.
- Base: `HEAD`, `main` and `origin/main` are all `1937b023`; `git log main..wo-039` is empty; Git registers this worktree once; the latest local tag is `v0.14.0`.
- FINAL-001's blocking finding is closed: the fifth repair merged `main`, re-recorded the `WO-039` feedback edition from a fresh live audit and verifier, and returned through VER-005. `feedback-evidence --check --edition WO-039` passes inside `npm test` on this tree, and the integration bookkeeping FINAL-001 itemised is present and correct.
- Independent emission: `node scripts/harness.mjs emit --out DIR` into a fresh temporary directory produced the 23 installed files plus the manifest, which the command counts as 24 generated surfaces, with zero byte differences against the installation and the marked `CLAUDE.md` block compared equal separately.
- Independent residue accounting: the marked block's declared 1,432 UTF-8 bytes equal the bytes its seventeen lines carry.
- Independent measurement read: all eight rows of the stored context measurement are strictly lower in both bytes and lines, and the instruction file's 3,593 bytes / 64 lines were confirmed against the file itself.
- Self-host observed from inside: the generated `UserPromptSubmit` hook resolved this session's `resume: final review` to the reviewer role and injected the selected order path; the skill that supplied this procedure is byte-identical to the committed one; the generated `PreToolUse` guard refused two commands during this review, one of them correctly for naming the private harness settings directory. Both refusals are recorded rather than hidden.
- Actors: executor Codex CLI `0.153.4`, GPT-6 Astra, `max`, operator-attested; verifier the same selection through VER-005; reviewer Claude Code `2.1.263`, `claude-opus-5[1m]`, `max`, self-reported. The order requires reviewer effort `any`.

## Verification trail

- `VER-001` (fail): four findings — the `finish.mjs` Stop hook lacked boundary parity and removal proofs, the live read comparison ran inside an enforcing scope, the installed output-receipt gate could not be satisfied for oversized files, and a receipt overclaimed drift coverage.
- `VER-002` (fail): concurrent dead-owner recovery admitted two writers.
- `VER-003` (fail): a stale reclaimer removed a self-refreshed reservation, an unforced operator release removed a newly acquired one, and the live-holder smoke failed.
- `VER-004` (pass): all eight criteria, with every earlier finding closed by a passing regression.
- `FINAL-001` (fail): the branch base moved under two merged orders, so the integration this window owned could not complete without a fresh live feedback edition, which is executor work.
- `VER-005` (pass): the integrated subject, with the fifth repair's console re-pin and re-recorded edition judged independently.
- `FINAL-002` (pass): this review, with one non-blocking finding recorded and nominated.

## Known open items

- **A merged order's regression assertion is now vacuous (FINAL-002 F1).** The console's pinned `selfhost` fixture records exactly one verifier attempt, and it is the accepted one, so WO-032's assertion that every other recorded attempt shows `lease-expired` and takes no matrix link iterates over no rows; `lease-expired` appears in no committed fixture output and nowhere else in the suite. The cause is genuine — under compiler `0.7.0` the WO-011 verifier stream no longer projects its verifier actors, so the old case could not be kept — but the fifth repair receipt and the ledger both state that the criterion's substance is unchanged, and for the multi-attempt half it is not. A fixture-local synthetic store with two or more verifier attempts restores it with no live evidence and no behavioural change; that is executor work and is nominated rather than folded in here.
- **Three judgment units stay at rung 7.** `decision-lineage`, `evidence-judgment` and `cleanup-scope` remain role procedure with `facts.*` residue because the harness admits no semantic judgment fact.
- **The typed-correction hook is unbuilt** until the operator confirms an exact token; the unit is reached through the role skill and the residue names `operator.correction-token.confirmed`.
- **Codex hooks and settings permissions are unavailable here,** so that profile ships skills and the instruction block only and says so.
- **The three `Stop` gates judge every turn end rather than a completion claim,** recorded in the ledger with two candidate remedies and named as separate work.
- **The `PreToolUse` effect classifier matches the literal text `.env`,** so an ordinary `process.env.X` expression inside a shell command is refused as credential access. It fails closed and a generated deny may only tighten, so this is a sharp edge rather than a safety defect.
- **The evidence gate disables the running session's own harness** during its `rm -rf dist && tsc -b --force` window; the hooks deny fail-closed and recover when the build completes.
- **The live records are bounded role-entry smokes,** not complete delivery lifecycles, and the context measurement is a controlled comparison on a matched synthetic task, not an estimate of any real session's total cost.
- **Claude Code attestations remain `self-reported`;** no effective-effort readback is exposed for this harness.
- **Publish and release close.** `npm run worktree -- publish WO-039` runs the committed-surface, body, notes and sign-off checks before any push, then preflights `gh`; inside the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses there the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, from the main checkout.
