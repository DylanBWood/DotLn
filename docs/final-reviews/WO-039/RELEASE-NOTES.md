## Release overview

DotLn `v0.15.0` makes a saved build govern the sessions that actually run in this repository. A new compiler target, `harness-v1`, lowers a loadout, its equipped feedback units and its authority envelope into the configuration a harness enforces: settings deny rules and hook scripts at rung 2, on-demand role skills at rung 7, and a generated instruction fragment at rung 8 that holds only the residue nothing lower could carry, with a declared byte count. A new identity, the **Contributor**, is this repository's own session build, and its emitted bundle is committed here: `.claude/settings.json`, eleven `.claude/hooks/*.mjs`, five `.claude/skills/dotln-*/SKILL.md`, five `.agents/skills/dotln-*/SKILL.md`, `.claude/harness-manifest.json`, and the marked block of `CLAUDE.md`. `npm run harness -- check` refuses any drift between what the compiler emits and what is installed, and it runs inside `npm test`. Every generated hook imports the pinned built `feedbackBoundary` and evaluates the same compiled unit the equipped feedback host evaluates; there is no second predicate and no hand-written rule file. Claude Code `2.1.263` receives hooks, skills, settings deny rules and the instruction block; Codex CLI `0.153.4` receives skills and the shared instruction block only, with every unobserved capability named as unavailable in the residue rather than assumed. Compiler `0.7.0` and skeleton `0.13.0` carry the new contract and host; console `0.1.1` follows the self-hosted feedback edition; kernel `0.2.1` is unchanged. No runtime dependency is added, the kernel is untouched, and lifecycle legality, the event schema and the loadout, feedback and verification contracts are unchanged.

The measured result is the point. Under the previous configuration a cold start directed roughly 71,000 bytes and 994 lines per role, dominated by the 64,993-byte execution guide the read order mandated. Under the Contributor build the same synthetic task directs between 8,439 and 14,420 bytes and between 130 and 188 lines, depending on role and profile, and every one of the eight role/profile rows is strictly lower in both bytes and lines. The instruction file alone falls from 3,878 bytes / 78 lines to 3,593 / 64. This is a build replacing a biography, not a biography relocated into a skill: the measurement counts the instruction file, the role skill, and every file either of them directs the session to read anywhere in the role's procedure, including late reads, and it derives that set mechanically rather than from an author's list.

## Read before upgrading

- **The build now governs your session.** Opening a session in this repository loads generated hooks that judge tool calls before they take effect. A `PreToolUse` guard evaluates each call against the Contributor's authority envelope and refuses denied effects with `DOTLN_HARNESS_REFUSED`; `PostToolUse` hooks observe edits and reads; `Stop` hooks consult the control state's required checks, obligations and read receipts. The lifecycle script `resume.mjs` remains ground truth; the hooks are defense in depth and refuse only what it would refuse anyway.
- **Resume phrases dispatch through a skill.** A whole `resume:` phrase resolves to one generated role skill: executor (`next`, `fix`, `status`, `times`), verifier, reviewer, release close, plus a separate planner for the `planning:` and `ideation:` prefixes. `CLAUDE.md` §Start here names the skill per role and only files the measurement counts. The skill supplies procedure, input contract, artifact locations, evidence obligations and stopping rules; it duplicates no phase state and grants no authority.
- **The generated surfaces are compiler output, not hand-edited files.** Editing `.claude/hooks/`, `.claude/skills/`, `.agents/skills/dotln-*`, `.claude/settings.json`, `.claude/harness-manifest.json` or the marked `CLAUDE.md` block by hand makes `npm test` fail on drift. Change the loadout or a feedback unit and run `npm run harness -- emit`. The hand-written Clean Room floor stays outside the markers and is never generated.
- **Hooks require the built runtime.** Each generated hook dynamically imports pinned artifacts under `packages/*/dist`, and each pins their hashes. If the build is missing or its bytes differ, the hook refuses fail-closed with `DOTLN_HARNESS_REFUSED: built adapter unavailable`. `npm test` removes and rebuilds `dist` partway through, so a session running the gate is denied tool calls for that window and recovers when the build completes.
- **New root commands.** `npm run harness -- emit [--loadout contributor] [--profile id] [--out dir]`, `harness check`, `harness evidence` (the fixed required checks plus host receipts for the installed completion hooks), `harness read-output PATH [--offset n] [--length n]`, `harness writer --show|--release [--force]`, and `npm run terms -- check PATHS`.
- **The local-terms check is opt-in and honest when absent.** `docs/control/local/terms.txt` is operator-maintained, ignored, never committed and never hashed into any committed file. When it is absent the check reports `unavailable` and says so rather than passing silently; when it is present it must pass. A refusal prints only the file, line number and a count, never the matched text, so an echoed refusal cannot republish the term. No exhaustive inventory is required before work proceeds.
- **The feedback evidence edition and the actor board move to WO-039.** The compiler package bump changes the pinned policy and verification identities, so `npm test` and `npm run evidence:feedback` select `--edition WO-039`, and the console's `selfhost` fixture case follows the same edition through `SELF_HOST_EDITION` in `packages/console/src/collect.ts`. The WO-011, WO-041 and WO-032 editions are untouched historical bytes. Because the skeleton refuses an earlier edition's verifier stream as persisted compilation drift, the two selections must move together.
- **Project scope only.** `emitHarness` can write four project surfaces and nothing else. No user-scope setting, sandbox control, credential denial, default mode or allow grant is written or changed. A generated allow entry can never exceed the build's envelope and a generated deny can only tighten; ADR-0005 carries the dated amendment recording that project-scope harness configuration is now compiler output.

## Substantive changes

- **`harness-v1` in `packages/compiler/src/harness.ts`.** `HarnessProfile` declares an observed harness: hook events, refusal semantics, skills root, settings shape and instruction file, each capability carrying an evidence reference into the phase-zero record and, when unavailable, a stated reason. `assertHarnessProfile` refuses an unexplained unavailable capability, a non-repository path, an allow grant with no observed contract, and observed hooks that lack any pinned runtime artifact. `lowerToHarness(program, feedback, envelope, profile)` returns a `HarnessBundle`: files carrying `{path, contents, origin, rung}` where origin names the unit or facet ids, the loadout id and its semantic hash; a manifest with per-file hashes, compiler package version, profile and loadout identity; and a residue report naming every unit or facet that could not lower below rung 8, each with a reason and its missing capabilities. Lowering is pure, deterministic and dependency-free: no filesystem, process, clock or randomness, so a changed unit changes only the files that name it. `verifyHarnessBundle` re-derives every file hash, and `mergeHarnessFragments` composes the profile-qualified residue for the shared instruction symlink.
- **Lowering per declared mechanism.** `permission-guard` becomes settings deny entries plus a `PreToolUse` hook that re-checks host facts a settings matcher cannot express; `writer-isolation` becomes a `PreToolUse` hook resolving cwd, Git root and the worktree reservation; `suppression-diff` becomes a `PostToolUse` hook; `attribution` becomes both a `PreToolUse` check on commit commands and the commit-message hook; `application-evidence`, `complete-scope` and `output-review` become `Stop` hooks that consult required checks, obligations and read receipts; `decision-lineage`, `evidence-judgment` and `cleanup-scope` remain role procedure at rung 7 with `facts.*` residue, because the harness exposes no admitted semantic judgment facts; `semantic-correction` lowers to a `UserPromptSubmit` hook only when the operator confirms an exact token, and until then appears in the residue as `operator.correction-token.confirmed` with the duty carried by the role skill. Surface language is never a trigger.
- **The Contributor build (`packages/skeleton/src/loadouts/contributor.ts`).** Five roles selected by exact resume phrase, the ten personal feedback units equipped, the Clean Room active with its locked floor, and an authority envelope that states the ADR-0003 through ADR-0005 posture as allowed effects (`repo.read`, `repo.write`, `shell.run`, `git.local`, `lifecycle.run`) and denied effects (`credentials.access`, `transport.ssh`, `settings.user`, `sandbox.disable`, `remote.unapproved`, `package.publish`), with one writer per worktree.
- **The lowering host (`packages/skeleton/src/harness-host.ts`).** Hook evaluation, effect classification, the writer reservation with immutable nonce-named instances, session and read-scope observation, output read receipts with byte-ranged delivery, the control observation through the canonical read-only lifecycle command, and the fixed evidence runner. Reservation recovery acts only on the observed instance, so concurrent reclaimers of a dead holder admit exactly one writer, a self-refresh survives a stale reclaimer, and an operator release judges, retires and journals one observed reservation.
- **Commands and checks (`scripts/`).** `harness.mjs` with `emit`, `check`, `evidence`, `read-output` and `writer`; `lib/harness.mjs` with containment, ownership and obsolete-file rules; `harness-context.mjs` and `lib/harness-context.mjs` for the mechanical directed-read measurement; `harness-evidence.mjs`, `harness-live-smoke.mjs`, `harness-live-suite.mjs` and `harness-probe.mjs` for the live records; `terms.mjs` and `lib/terms.mjs` for the local screen; and `test-harness.mjs` with seventeen host tests covering boundary parity, unit removal, drift in five shapes, the local-terms fixture, frozen semantic hashes, whole-procedure context with late directives, the session-spanning read observer, the confirmed-token adapter, the commit-message adapter, completion tracking, and the four writer-reservation schedules.
- **Phase zero, recorded before any lowering rule was written.** `docs/discovery/harness-smoke-2026-09-07.md` and its JSON projection record, per harness, which events fired, the payload fields each hook received as shapes, how a refusal is expressed and whether the harness honoured it, how a project skill resolved, what `--bare` skipped, and what remained unobserved. Absolute paths, session ids and host names are reduced to shapes before commit. Every profile capability the compiler consumes cites a row by anchor.
- **Console (`packages/console` `0.1.1`).** `SELF_HOST_EDITION` declares the self-hosted feedback edition the board reads by default and derives the audit, verifier and maturity paths from it under edition-neutral store ids. The pinned fixture case is renamed `wo011` to `selfhost` and re-pinned to the WO-039 edition, and the AC2 test derives the accepted verifier attempt from the admitted matrix rather than naming one run's attempt number. The `uifa-board-v1` contract and both renders are unchanged.
- **Documentation.** Product 02 gains §Harness compiler v1 with the lowering table and the residue rule; product 03's §Agent enablement skills records the shipped slice and what remains of its delivery order; product 07 rewrites the cold-start read order and the resume-phrase dispatch; product 13 updates the engineer and devops rows; the README release block, two dated capability rows, the publication index and both edition locks, and the ledger record the work with its rejected alternatives.

## Progressive polish

- None. This release changes behaviour and configuration only; no cosmetic correction was applied at final review.

## Evidence and compatibility

- Source: the reviewed `wo-039` branch merged into `main` above `v0.14.0`; the exact commit is named by the annotated `v0.15.0` tag.
- Executor evidence: `docs/evidence/WO-039/README.md` (the acceptance map, live scope and limits), the phase-zero record and its per-harness JSON, the fixture and gate transcripts, the live role and writer records under `docs/evidence/WO-039/harness-live/`, the context measurement `harness-context.json`, and five repair receipts.
- Verification: `VER-001`, `VER-002` and `VER-003` (fail, with four then one then three findings, all closed by regressions), `VER-004` (pass), `FINAL-001` (fail, on the integration this window owned), and `VER-005` (pass, on the integrated tree). Final review: `FINAL-002` (pass).
- Compatibility: `@dotln/compiler` `0.6.0` to `0.7.0` and `@dotln/skeleton` `0.12.1` to `0.13.0` are minor bumps carrying the new contract and host; `@dotln/console` `0.1.0` to `0.1.1` is a patch that moves only the default evidence edition; `@dotln/kernel` `0.2.1` is unchanged. `package-lock.json` changes only by those workspace entries. No dependency is added or upgraded; the TypeScript pin stays `5.4.5`. Lifecycle phases, legal actions, the control-event schema and the loadout, feedback and verification contracts are unchanged.
- Known limitations: `decision-lineage`, `evidence-judgment` and `cleanup-scope` remain role procedure rather than enforced guards because no admitted semantic judgment fact exists, and the typed-correction hook stays unbuilt until the operator confirms an exact token; Codex hooks and settings permissions are unavailable here, so that profile ships skills and the instruction block only; the live records are bounded role-entry smokes, not complete delivery lifecycles, and the context measurement is a controlled comparison on a matched synthetic task rather than an estimate of any real session's total cost; the three `Stop` gates judge every turn end rather than a completion claim, which is recorded in the ledger with two candidate remedies; the `PreToolUse` effect classifier matches the literal text `.env` and so refuses an ordinary `process.env.X` expression inside a shell command, which fails closed and is safe but is a sharp edge; the evidence gate rebuilds `dist` and therefore denies the running session's own tool calls for that window; and console `0.1.1` re-pins its `selfhost` fixture to an edition that records exactly one verifier attempt, so WO-032's assertion that non-accepted attempts show `lease-expired` and take no matrix link is present in the test but exercises no row until a multi-attempt fixture is restored, which `FINAL-002` records and nominates as separate work.
