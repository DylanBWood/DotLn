# FINAL-002 — WO-039 Harness lowering (v0.15.0)

**Verdict: pass.** The order's eight acceptance criteria hold on the integrated tree and I re-established each one first-hand. FINAL-001's blocking finding is closed: `main` is merged, the feedback evidence edition was re-recorded from a live audit and verifier, and VER-005 judged the result independently. One bounded defect that is neither acceptance nor bookkeeping is recorded below as F1 and nominated for a separate order; it narrows a merged order's regression coverage and contradicts one sentence in the repair receipt and the ledger, but it fails no criterion of this order and breaks no gate.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}

Claude Code reported `2.1.263`. The model identifier and `max` effort are this session's launch selection as the harness exposes it; no effective-session readback exists for this harness, so the source stays `self-reported`. The order requires reviewer effort `any`, so no minimum is at issue. No account label was supplied.

## Subject and instrument

- Authority: [WO-039](../../work-orders/WO-039-harness-lowering.md), its cited sections, the incremental-private-list amendment, and the [ideation receipt](../../evidence/WO-039/ideation.md). The verification sequence is [VER-001](../../verifications/WO-039/VER-001.md) (fail), [VER-002](../../verifications/WO-039/VER-002.md) (fail), [VER-003](../../verifications/WO-039/VER-003.md) (fail), [VER-004](../../verifications/WO-039/VER-004.md) (pass), [FINAL-001](FINAL-001.md) (fail) and [VER-005](../../verifications/WO-039/VER-005.md) (pass), with five repair receipts.
- Subject: the dirty `wo-039` worktree at `HEAD` `1937b023187a813f3b8053a221f819a15cdad244`. Physical cwd and Git root agree, Git registers this worktree once, and there are no branch commits before this review.
- Integration: `HEAD`, `main` and `origin/main` are all `1937b023`. `git log main..wo-039` is empty. The merge FINAL-001 required was performed by the fifth repair, not by this review, and was verified by VER-005 before this window opened.
- Allocation: `FinalReviewRequested`, checkpoint `b5f5e00`, `refs/dotln/checkpoint/WO-039/25`.

**Instrument disclosure.** This session runs under the bundle it reviews, and that is evidence for the self-host but for nothing else. The generated `UserPromptSubmit` hook resolved `resume: final review` to the reviewer role, consulted canonical control state, and injected the selected order path in exactly the shape `evaluateHarnessHook` constructs at `packages/skeleton/src/harness-host.ts:1553`. The skill that supplied this procedure is byte-identical to the committed `.claude/skills/dotln-reviewer/SKILL.md`, Origin `fnv1a64:06245f5c581212f1`. The generated `PreToolUse` guards judged every tool call in this session and refused two of them; both refusals are recorded under Observations rather than hidden. This report is not evidence for the live smoke records, which separate scratch sessions produced.

## What changed since FINAL-001

FINAL-001 reviewed the implementation at checkpoint 19. I compared checkpoint 19 with this review's checkpoint 25 path by path and subtracted `main`'s own twelve commits. **No WO-039 implementation source changed.** `packages/compiler/src/harness.ts`, `packages/skeleton/src/harness-host.ts`, `packages/skeleton/src/loadouts/contributor.ts`, every `scripts/harness*.mjs`, `scripts/terms.mjs`, `scripts/lib/terms.mjs`, `scripts/test-harness.mjs`, all eleven generated hooks, all ten generated skills, the manifest, the discovery record and the live smoke records are byte-identical between the two checkpoints.

The repair-only delta is: the re-recorded `WO-039` feedback edition and its preserved pre-integration and draft siblings, the fifth repair receipt and its seven transcripts, VER-005, the control order log, and the console change described next.

## FINAL-001 F1 — closed

The finding was that `main` advanced by two merged orders, that `packages/console` therefore entered the raw feedback subject through `package-lock.json`, and that `validateSelfhost` would fail the gate on the integrated tree. The fifth repair merged `main`, re-recorded the edition from a fresh live audit and a fresh live verifier episode, and returned through VER-005. `node scripts/feedback-evidence.mjs --check --edition WO-039` now passes inside the green `npm test` I ran on this tree. The integration bookkeeping FINAL-001 named is present and correct: `package.json` keeps `main`'s console steps beside this order's harness steps with `--edition WO-039` in both places; `package-lock.json` carries `main`'s lockfile with the compiler `0.7.0`, skeleton `0.13.0` and console `0.1.1` workspace entries; the README release block claims `v0.15.0`; the append-only documents merged by concatenation; the index, control projection, edition locks and `harness-context.json` were regenerated.

## Acceptance re-established on the integrated tree

| Criterion | What I checked and observed |
| --- | --- |
| 1 — observed harness truth | The [phase-zero record](../../discovery/harness-smoke-2026-09-07.md) carries Claude rows C1–C8 and Codex rows X1–X3 as explicit `<a id>` anchors. I enumerated the ten anchors `contributorProfiles` cites and confirmed each resolves to a row that states what was observed. Codex's four hook events and its settings capability are `available: false` with stated reasons, and `assertHarnessProfile` refuses an unavailable capability that does not explain itself. |
| 2 — pure, deterministic lowering | `lowerToHarness` takes no I/O, clock or randomness; `COMPILER_PACKAGE_VERSION` is a source constant. Every emitted file carries the loadout id and semantic hash, and `verifyHarnessBundle` re-derives each file hash against the manifest. The frozen [semantic-hash inventory](../../evidence/WO-039/artifact-identity/semantic-hash-inventory.json) shows Seiri `fnv1a64:9ca8d0229c6bd8db` and Entropy Reducer `fnv1a64:c5ddbca75f1c4cee` unchanged from compiler `0.2.0` to `0.7.0` against the WO-101 independent oracle. The compiler and harness fixtures in the gate prove determinism, origin locality and manifest verification. |
| 3 — one compiled predicate | The generated hooks contain no predicate of their own: each imports the pinned built `feedbackBoundary` and passes it to `runHarnessHook`, and `evaluateHarnessHook` evaluates the compiled unit through `boundary(config.policy, fact, …)` at `packages/skeleton/src/harness-host.ts:1735`. `parity()` in `scripts/test-harness.mjs` spawns each generated hook on a synthetic payload, asserts its decision equals `feedbackBoundary`'s on the same host facts, then rewrites the policy empty and asserts it allows. Seventeen harness tests pass in the gate. |
| 4 — recorded live smoke | I read the six current records under [harness-live](../../evidence/WO-039/harness-live/) directly rather than inheriting their summaries. Each pins Claude Code `2.1.263`, the installed file hashes, the runtime, loadout `fnv1a64:06245f5c581212f1` and effective effort `xhigh`, and each reports `roleSkillResolved`, `requiredDeniedEffectRefused` and `fixtureCommitDidNotExecute` true, `observerFinished` true, an empty `outsideDirectedSet` and `attemptedOutsideDirectedSet`, zero native `Read` refusals, and `userScopeSettingsWritten` false. `correction` records the token unconfirmed with the unit reached through the role skill and `operator.correction-token.confirmed` as the missing capability. `readScopeMode` is `observe`, so an out-of-set read would be recorded rather than refused: that is what closes VER-001's methodological finding, because the empty set is now an observation rather than a product of enforcement. One Bash route refusal is recorded rather than hidden. These are revalidated recorded observations, not fresh observations of those sessions. |
| 5 — committed self-host | I emitted the Contributor bundle independently into a fresh temporary directory outside the repository and compared every emitted path with the installation: 24 generated surfaces — the 23 files the manifest lists plus the manifest itself — and zero differing files, including `CLAUDE.md` compared as a whole file rather than only its marked block. `harness check` runs inside `npm test`; the drift fixture at `scripts/test-harness.mjs:747` covers content, missing, unexpected, manifest and unowned-replacement drift, and `emitHarness` can only write the four allowed project surfaces, so no user-scope write is reachable. The config log carries its dated entries and ADR-0005 carries the dated amendment. |
| 6 — directional context | `harness-context --check` recomputes the stored measurement mechanically from the instruction file and the skill. I read the stored measurement directly: all eight profile/role rows are strictly lower in bytes and lines, and the instruction file alone falls from 3,878 bytes / 78 lines to 3,593 / 64, which I confirmed against `CLAUDE.md` itself. The comparison uses a matched synthetic WO-999 task on both sides and says so in its recorded method. |
| 7 — write-backs | Present and consistent: 02 §Harness compiler v1; 03 §Agent enablement skills with the dated WO-039 implementation slice and what remains of the delivery order; 07's rewritten cold-start read order and resume-phrase dispatch; 13's engineer and devops rows; the README release block; dated `compiler.harness-v1` and `harness.self-hosted` capability rows; the publication index rows and both edition locks; the ledger entries including the rejected alternatives. |
| 8 — gate and boundaries | `npm test` and `git diff --check` both exit 0 on this tree, with 368 tests passing across seven suites and zero failures, skips or todos. No runtime dependency is added and the TypeScript pin stays `5.4.5`; the kernel is untouched; `scripts/resume.mjs` and lifecycle legality are unchanged. The operator's local terms list is present and its screen passes; I did not read, print or hash the list or any match. |

I verified the residue accounting independently: the marked block declares 1,432 UTF-8 bytes and the seventeen residue lines it carries measure exactly 1,432 bytes. Each of the ten units lowers to its declared rung or appears in the residue with a named missing capability.

## F1 — a merged order's regression assertion is now vacuous (non-blocking, nominated)

**Severity: low, recorded rather than blocking.** It fails no WO-039 criterion, breaks no gate, and makes no shipped behavior wrong. I record it because the repair receipt and the ledger both state the opposite, and because a reviewer must not certify a claim they have shown to be unsupported.

**Observed.** WO-032 ships `packages/console/test/board.test.ts:319`, "WO-032 AC2 executor, verifier attempts, authority, hashes and actual accepted episode remain distinct". Its final loop asserts that every recorded verifier attempt other than the accepted one is in phase `lease-expired` and carries no link to the acceptance matrix. The fifth repair re-pointed the pinned fixture case from `wo011` to `selfhost`. The `selfhost` fixture records exactly one verifier episode, `ep_verifier_1_attempt_1`, and it is the accepted one, so that loop now iterates zero times. The previous `wo011` fixture recorded four attempts, three of them `lease-expired`.

**Reproduction.** `ep_verifier_` matches exactly one distinct episode id in `packages/console/fixtures/expected/selfhost.json`. The string `lease-expired` occurs zero times across all fifteen committed expected outputs — five cases in JSON, terminal and HTML — and appears in the console package only in `test/board.test.ts` and its build output, exactly once, inside that loop. `packages/console/fixtures/wo009.events.jsonl` contains no verifier episode. The deleted `wo011.json` contained three `lease-expired` rows. No other test or fixture exercises the invariant.

**Why the simple remedy is not available.** I reproduced the historical case to check whether it could simply be kept alongside the new one. All three WO-011 inputs still hash to the values `main`'s manifest records, and my reproduction harness is faithful — replaying the current `selfhost` case through it returns bytes identical to the committed golden. But replaying the `wo011` inputs under the current compiler no longer yields the recorded board: the four verifier actor rows are replaced by a single generic `verification-host` row whose facts read `unknown`. That is the documented behaviour for a historical edition, and it labels every missing fact rather than dropping it silently, so WO-032's "no unlabeled missing fact" invariant is intact. It does mean the old goldens cannot be restored, and the fifth repair's choice to remove the case was reasonable.

**What is unsupported.** [repair-005](../../evidence/WO-039/repair-005.md) says the AC2 test "checks every other recorded attempt as lease-expired without a matrix link; the criterion's substance is unchanged", and the ledger says "WO-032's criteria keep their substance". The first clause is true of the code as written and false of the pinned fixture, and the substance claim does not hold for the "verifier attempts" half of the criterion, which is now unexercised. The executor/verifier distinctness half — separate rows, different hash kinds, phases, allowed and denied effects, and links to build, receipts and matrix — is fully exercised, and the exact accepted episode is still pinned by the `selfhost` golden.

**Bounded remedy, nominated not folded in.** A fixture-local synthetic store with at least two verifier attempts, in the manner of the existing hand-authored `wo009.events.jsonl`, restores the invariant with no live evidence and no behavioural change. That is executor work; a reviewer must not author it and then certify it, and the guide reserves a failed review for a resolution that needs new behavioural code or an actual acceptance defect of the order under review. Neither applies, so this ships with the defect named on the committed surface and a candidate recorded.

## Observations, not findings

- **O1 — the generated permission guard classifies the literal text `process.env.` as credential access.** Its `permissionEffect` credential pattern is `/\.ssh(?:[\/\s]|$)|\.env(?:[\s.]|$)/` over the whole command string, so an ordinary Node expression such as `process.env.TMPDIR` inside a heredoc is refused as `credentials.access`. I hit this first-hand and rephrased rather than bypassed. The direction is right — the guard fails closed and a generated deny may only tighten — and the order's non-goals exclude a general effect-to-tool table, so this is an ergonomic sharp edge to know about, not a defect.
- **O2 — the same guard correctly refused a command naming `docs/control/local/harness`.** That is the private harness settings directory, which the envelope denies as `settings.user`. Recorded because it is the clearest first-hand evidence in this session that the compiled authority reaches real tool calls.
- **O3 — WO-032's published `v0.14.0` release notes and PR body describe a `wo011` fixture case and "the three lease-expired verifier attempts" that `v0.15.0` removes.** Those are historical reviewed artifacts and the publication contract keeps their original wording; the current state is recorded in this order's release notes instead.
- **O4 — the evidence gate disables the session's own harness while it runs.** During `npm test`'s `rm -rf packages/*/dist && tsc -b --force` window the generated hooks' dynamic import fails and the wrapper denies every tool call fail-closed. FINAL-001 recorded this as O1 and it is unchanged; I avoided it by running the gate as a single background job and not issuing tool calls that depend on the built runtime during the rebuild.
- **O5 — the three Stop gates judge every turn end rather than a completion claim, and this session hit all three.** Ending a turn mid-review drew `no-partial-completion`, `read-your-own-output` and `verify-app-before-done` refusals. The output gate named 222 of 223 outputs as missing current-byte reads, because 02 §Harness compiler v1 keeps output obligations at the session-entry revision across review commits and no branch commit precedes final review, so a reviewer inherits the whole order's diff. I confirmed that committing does not clear it: after the five reviewed commits the count fell only from 223 to 220, and the three that left were the `wo011` fixture outputs whose deletion the commit recorded, not files that became read. What lets a turn end is the documented re-entry rule, which records the unmet obligation once rather than looping. I measured the obligation rather than estimating it: against the session-entry revision `1937b023` it is 220 files totalling 8,793,272 bytes, of which 5,366,238 bytes sit in 23 files over 100 KB each — the three live verifier streams (854 KB, 732 KB and 730 KB), the ledger, and the gate transcripts. Discharging it through the Read tool is roughly 2.5 million tokens of delivery for files that are immutable recorded evidence, and 02 §Harness compiler v1 states plainly that read receipts witness delivered bytes, not comprehension. I therefore complete with that obligation recorded rather than discharged, exactly as FINAL-001 did, and I read at current bytes the outputs I authored and the diffs of everything I judged. The measured figure is offered as the missing quantity in the ledger's two candidate remedies: it is the cost of treating every turn end as a completion claim. This is honest behaviour of the shipped design, already in the ledger with two candidate remedies and named as separate work; it does not change my verdict.

## Executed checks

- `npm run harness -- evidence` exited 0 twice, once on the reviewed working tree and once again on the committed tree, each recording `npm test` exit 0 and `git diff --check` exit 0 with zero `not ok` lines. The first bound to subject `sha256:bc77ccac7850421ab53a69d5c017093cc7c7a4f610c8097b870ba84dbb2c3fb4` (transcript 116,131 bytes, SHA-256 `cd6e2dc7efb8197026de98e2e51d1390f05255e12cfd356d2f1b13fa57c5ad80`); the second, which is the one that binds to what is published, to `sha256:8430f7d0913c8cc1749216bb2accb970b324a45480ec0418310f9ad718af6d25` (transcript 116,113 bytes, SHA-256 `815df3881f442bac65bb880f536564a4c19cacff864aa3ea7bd4a5c0ab40880d`).
- The subject hash moved between those two runs without any file content changing. `harnessSubject` lists `git ls-files --cached --others`, so the three `packages/console/fixtures/expected/wo011.*` paths — deleted in the working tree but still tracked — were listed as `absent-or-link` before the commit and left the listing once the deletion was committed. Git records them as renames to `selfhost.*`. I re-ran the gate rather than let a receipt bound to the pre-commit listing stand in for the published tree.
- Suite totals, every one with `fail 0`, `skipped 0` and `todo 0`: 7 release-preparation, 9 license-surface, 288 kernel/compiler/skeleton, 18 console, 17 harness, 8 identity-corpus and 21 mutation tests — 368 in total. `check-surfaces --local` reported `PASS release-block: observed v0.15.0; expected v0.15.0 (work-order target v0.15.0; latest local tag v0.14.0)` and passed all four component rules, including the console patch required because its `src` changed. Publication reported 241 of 241 headings indexed with both edition locks `CURRENT`. `harness check` reported 24 generated surfaces with the local-terms list present, and `harness evidence` reported 4 current live role smokes, 2 writer-reservation smokes, 24 generated surfaces and 78 screened surfaces.
- Two earlier gate runs in this session did not reach the suites and are disclosed rather than discarded. The first stopped at `work-orders index --check` because I had not refreshed the generated index after my own `final-review` dispatch, which the skill requires; that was my procedural omission, not a subject defect, and regenerating it moved only the order's state, report links and latest attestation. The second stopped at `check-surfaces --local`, which refused my own draft release notes for raw HTML: the body profile rejects angle brackets, including inside code spans. Both were corrected and the run above is the authoritative one.
- Independent emission: `node scripts/harness.mjs emit --out DIR` into a fresh temporary directory outside the repository produced 24 generated surfaces with zero differing files against the installation.
- Independent residue accounting: the marked block's declared 1,432 UTF-8 bytes equal the bytes its seventeen lines carry.
- Independent fixture reproduction: replaying the pinned `selfhost` case through the built console returned bytes identical to its committed golden, which is the control that makes F1's `wo011` comparison trustworthy.
- Base and integrity: `git rev-parse HEAD main origin/main` all `1937b023`; `git merge-base`, `git log main..wo-039` empty; `git worktree list --porcelain` shows two worktrees with this one registered once; `git tag --sort=-v:refname` latest `v0.14.0`; and a path-by-path comparison of checkpoints 19 and 25 against `main`'s twelve commits.
- The first gate run of this session recorded subject `sha256:9cf5d7bd4bbaa1ab2cde3fe8bbe6c982bafad5512195600e559b84f12f6b3e9a`, identical to the subject VER-005's receipt bound its checks to, before my ledger and planning-map write-backs moved it to `bc77ccac`. The implementation this review judged is byte-identical to the implementation verification judged.

## Limits of this review

I did not perform the merge; the fifth repair did, and VER-005 judged its result. My integration claims rest on the path-by-path checkpoint comparison and the base observation above, not on re-doing the merge. The live smoke records are revalidated recorded observations, not fresh observations of those harness sessions. This session's own governed behaviour is disclosed above as a self-referential instrument and is used as evidence for the self-host only. I did not read the operator's local terms list, and no term, match, count or hash of it appears anywhere in this report. The context measurement is a controlled comparison on a matched synthetic task; it does not measure this review's own reading cost, which was substantial.

## What happens next

The reviewed state is committed on `wo-039` as five coherent commits, none carrying AI attribution. `npm run worktree -- publish WO-039` then passed every preflight it owns — the committed-surface check, both publication bodies, the license surfaces, and contribution sign-off for all five commits under the operator-author exemption — and refused at the `gh` preflight before any remote mutation, because this sandbox denies reading `~/.config/gh`. The branch is therefore **not pushed and no pull request is open**: `git ls-remote --heads origin wo-039` is empty and the branch sits five commits ahead of `origin/main`. The operator completes publication by running the same command from this worktree outside the sandbox:

```sh
npm run worktree -- publish WO-039 \
  --title ':passport_control: Compile the Contributor build into the harness configuration this repository runs on: permissions, hooks, role skills and bounded residue (WO-039, v0.15.0)' \
  --body-file docs/final-reviews/WO-039/PR.md
```

Nothing is merged, no release is published, and no account or repository setting is changed by this review. After the operator merges the pull request, `resume: release close` from `main` runs the exact post-merge handoff the publisher prints. Every checkpoint ref, verification report, repair receipt and evidence file is preserved unchanged.
