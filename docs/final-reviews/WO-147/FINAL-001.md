# WO-147 FINAL-001 — a host lock that vanishes between a contender's check and its read is re-inspected under the guard it already holds, so live contention waits instead of failing; the verifier's own late correction about stale evidence editions is discharged here rather than routed to repair, and the order is integrated onto a moved `main` at v0.40.1

**Verdict: pass.** All six acceptance criteria are met at the integrated tree and no finding routes to repair. I did not take VER-001's account on trust: I read the acquisition path and its two callers in full, traced both `ENOENT` sources (the raw Node error from `lstatSync` and the `StorePathError` that `atPath` now stamps with the underlying code) into the one guarded catch, read both new fixtures and all four amended WO-143 fixture families line by line against the criteria, checked D004's second-vanish argument against where `host.lock` is published and unlinked, and re-derived the ten-and-ten series from `fixtures.md` rather than from the summary of it. Two things happened in this review that the verification sequence did not anticipate. `main` moved by one merge (WO-148, published as `v0.40.0`), so the branch was fast-forwarded with the WO-079 helper, its target retimed to `v0.40.1` under the unchanged patch classification, and every acceptance claim carried forward on a subject whose bytes under `packages/` are identical to the verification checkpoint. And the verifier's post-pass correction [D008](../../evidence/WO-147/decisions.md#wo-147-d008--verifier-correction-ver-001-passed-a-subject-whose-evidence-editions-are-stale) was right: the review gate was red on stale authority and feedback editions because the worker store is a registered evidence source. I established that each staleness was identity-only, re-minted both editions as WO-147 revision 001 on the integrated tree with one live `claude-cli-print` self-host audit, re-pinned the console's self-host case, and recorded the choice and my disagreement with D008's proposed route as [D010](../../evidence/WO-147/decisions.md#wo-147-d010--final-review-re-mint-the-stale-authority-and-feedback-editions-inside-this-review-instead-of-failing-the-order-into-repair). The product gate ran once on the reviewed, staged, integrated tree and passed **28 suites, 0 failed, 277.17 s, 72 fresh tasks, exit 0**.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-fable-5-1","effort":"xhigh","source":"operator-attested"}

**Process cost:** entry 84842 tokens; handoff 6829123 tokens; source claude-transcript-message-usage

The human actor is the operator, who dispatched `resume: final review` in this worktree after selecting the Fable 5.1 model for the session; `claude --version` reports 2.1.278 and the session reports its model as `claude-fable-5-1`. The effort is the operator's `CLAUDE_EFFORT=xhigh` session selector, the same value VER-001 read; `docs/discovery/environment.json` records no effective effort readback for `claude-code`, so both values are what the operator supplied and the pair records `operator-attested` rather than a readback nobody has. Counters came from `node scripts/harness.mjs usage b8a190d2-7e4c-45dc-aebb-46a38d5228dd`, scope `dispatch`, source `claude-transcript-message-usage`; the entry cutoff was 2026-09-21T20:42:59.750Z; `costUsd` and `reasoningOutputTokens` are null and therefore unknown, not zero. Before any fan-out I planned exactly one admission against the root session's 20-agent `subagentCap`: the live feedback verifier episode that a fresh edition requires by construction, a `claude --print` child launched by the audit command. No `Agent` subagent was spawned, and the handoff readback's subagent count is reported in §Goal alignment at handoff. The dispatch briefing's observed-facts block reported every counter unavailable at 20:42:52Z; the usage command answered seven seconds later with the entry total above, the same session-journal timing artifact the WO-148 and WO-150 reviews recorded.

**Read scope.** The order in full, including its cost line, design, six criteria, non-goals and two operator-review assumptions; its cited sections — WO-143 D004 and D006, WO-143 FINAL-001's capability-row judgment and its O3, WO-069 D015, `acquire`, `acquireGuard`, `atPath`, `present` and `regularFile` in `worker-store.ts`, the `transaction` retry in `resident-store.ts`, the four WO-143 tests and the two WO-147 tests in `resident.test.ts`, the pause-point peer in `worker-lock-process.ts`, and product 03 §Operator-presence policy at its merged bytes; product 07 §Goal-aligned decisions and §Independent workflows and integration; product 08 §PRs and commits; the ideation lineage, which is the 2026-09-21 standard pass §5 and §8, Receipt 022's known-issue row for this order in the work-order map, and the refutation receipt's two WO-147 findings; VER-001 in full at its current bytes; this order's `implementation.md`, `fixtures.md` and decisions D001 to D008; the registered source lists in `scripts/lib/evidence-sources.mjs`, the WO-150 D006, WO-143 D002 and WO-047 FINAL-001 precedents for edition re-minting, and the feedback evidence script's `validateSelfhost`; and the complete staged diff against `main`.

## Subject

| Item | Value |
| --- | --- |
| Work order | `docs/work-orders/WO-147-resident-lock-contention.md`, patch, application `v0.40.1` (staged at `v0.39.1`, retimed in this review) |
| Branch / worktree | `wo-147` in the order's own worktree; physical cwd and Git root confirmed equal; this session is the sole registered writer |
| Original base | `main` at `c84db382efd665e52f20927b3885f4019c879f43`, the WO-150 merge the order was activated on |
| Integrated base | `main` at `452d7e72fda0ecaa4aad8f03f261602f18f51f7a`, one merge ahead (WO-148, PR #111, annotated tag `v0.40.0`); the branch carried no commit, so the helper fast-forwarded it and `git log HEAD..main` is empty |
| Verification base | VER-001 judged the staged tree at `refs/dotln/checkpoint/WO-147/3` (`e0236023`) and completed at checkpoint 4 (`2515f71a`). Between checkpoint 4 and the pre-integration checkpoint 6, only `docs/control/current.md`, `docs/control/orders/WO-147.jsonl`, the generated index, D008 and the follow-up register changed. `git diff --stat refs/dotln/checkpoint/WO-147/4 -- packages package.json package-lock.json` is empty at the integrated tree, and VER-001 is byte-identical to its checkpoint copy |
| Reviewed code identity | `d733e5981afbc0a90564cc9de1729e90d6b004c16c22fc9a096486930e410ccc` |
| Diff shape | 47 paths before this review’s three documents, 4,552 insertions, 357 deletions. Hand-authored source is three files: `worker-store.ts` (+17/−4), the pause-point peer `worker-lock-process.ts` (+20/−5) and `resident.test.ts` (+135/−12). Everything else is the skeleton `0.34.1` version and pin triple, the regenerated bundle (fourteen hooks and the manifest, snapshot-id and hash lines only), the two re-minted editions (five files, most of it the verifier stream), the re-pinned console self-host case (four files), product 03, the capability row, the roadmap note, the README claim and the order heading, the evidence, verification and control records, and the regenerated projections |

## Integration — `main` moved by one merge

**What the helper did, as observed.** `docs/intake/` holds three ignored files, so I archived them first with `npm run backup:intake` into the granted session scratch path and passed that archive to `npm run worktree -- integrate WO-147 --intake-backup`. The helper verified the archive against the current intake bytes, minted `refs/dotln/checkpoint/WO-147/6` (the whole working state, untracked paths included), pushed the named stash `WO-147 integrate 2026-09-21` (`82d21f0a…`), fetched `origin`, fast-forwarded the commit-less branch from `c84db382` to `452d7e72`, applied the stash, auto-resolved seven projections (`README.md`, `docs/control/current.md`, the decisions index, the follow-up register, both publication locks and the work-order index), found no authored conflict, and ran its regeneration list: build, harness bundle and manifest, control projection, `release prepare --local`, the D009 stub, `meta`, index, publication locks and the selected console fixtures. It exited 0 with "Integration mechanics complete. Recovery refs and stash retained." Nothing was popped, dropped, reset, restored or cleaned.

**What integration changed.** Against checkpoint 6, the integrated tree differs under `packages/`, `.claude`, product 03 and the capability table only by `main`'s own WO-148 lines: 58 added in product 03 §Operator-presence policy (three dated paragraphs about `resident-bind`, which Git placed beside this order's edited WO-143 paragraph without conflict; I read the merged section and the two read coherently) and 17 removed from the skeleton README. Every subject path is byte-identical.

**Retiming.** `release prepare --local` reported "Retimed WO-147: v0.39.1 → v0.40.1; updated its heading, README claim, and dated roadmap note", because the observed local annotated baseline is now `v0.40.0`. Component versions collide with nothing: `main` still carries skeleton `0.34.0`, this order's `0.34.1` stays the next patch, the console's exact pin follows it, and compiler `0.17.0`, kernel `0.6.0` and console `0.1.7` are unchanged. The record is [D009](../../evidence/WO-147/decisions.md#wo-147-d009--integrate-main-at-452d7e72-and-retime-the-unpublished-target-to-v0401), whose carried-forward claims I completed: all six criteria carry forward unchanged because the integrated tree holds the same bytes under `packages/`, `package.json` and `package-lock.json`, `main`'s WO-148 work touched no registered evidence source and no file this order changed except the merged product 03 section, and the retime moves only the application label.

**Checks on the integrated tree, before the gate.** `npm run publication:check`: 273 of 273 product headings indexed, both editions current at 30 and 45 linked sections (re-run after the capability-row edit and lock refresh below, same result). `node scripts/harness.mjs check`: 31 generated surfaces. `npm run release -- check-surfaces --local`: every surface PASS, every package publish refused as designed. `npm run plan -- check`: exit 0, with this order's `release-assignment` update classified. `node scripts/work-orders.mjs index --check`: current. `npm run format:check`: clean. `git diff --check` and `git diff --cached --check`: clean.

## The evidence editions — D008's defect, discharged here

**What I found.** Before any edit, `npm run test:docs` on the integrated tree failed `authority-evidence` ("stale WO-150 revision 001 evidence: bundle-diff.json") and `feedback-evidence` ("feedback evidence is stale"), with `plan`, `plan-refutation-current`, `console-docs`, `skeleton-docs` and `lineage-fixtures` blocked behind the first at 0.00 s: 12 passed, 7 failed, exactly as D008 recorded. The cause is the one D008 names: `package.json`, `package-lock.json`, `packages/skeleton/package.json` and `packages/skeleton/src/worker-store.ts` are in `commonSources`, so both editions pin them, and all four are in this order's diff. `main`'s WO-148 changed none of the registered sources, so the staleness is wholly this order's, and it was already present at verification: VER-001's criterion 6 named `npm test`, which is green, and did not run the review selection, which is not.

**Why each edition was stale, established before regenerating.** Authority: the newly generated `authority.json` is identical to WO-150 revision 001 in every top-level field; `bundle-diff.json` differs only in its comparison label and in the after-hashes of the 14 generated bundle files that embed the skeleton version and the `worker-store.js` hash. Deterministic, no live run. Feedback: the newly generated `feedback.json` differs from WO-150 revision 001 in exactly one field, `subject` (`sha256:e6f60a33…` to `sha256:497d0fd0…`); `policyHash`, all ten fixtures, the context projection, the maturity method and the maturity table are byte-identical. So feedback behavior is unchanged and only the audited source identity moved, and because `validateSelfhost` asserts the live log's subject equals the current source subject, the WO-150 self-host logs could not be carried forward. This is the WO-047 review's situation and its method.

**What I did.** `npm run build`; `node scripts/authority-evidence.mjs --write --edition WO-147 --revision 001`; `node scripts/feedback-evidence.mjs --write --edition WO-147 --revision 001`; then one live audit, `DOTLN_LIVE_WORKERS=1 dotln feedback-audit --store .runtime/feedback-audit-wo147-r001 --transport claude-cli-print --model claude-sonnet-5 --effort max`, the documented path WO-110, WO-140 and WO-150 used, into an ignored canonical-path store so the mount-alias refusal WO-150 D006 records could not trip. The audit reached phase `complete` with 10 fixtures and 1192 saved instruction bytes; the verifier episode `ep_verifier_1_attempt_1` ran 320.6 s from `WorkerAttemptStarted` to `WorkerCompleted` under a 600 s deadline and a USD 3.00 cap and returned `completed` with both criteria passing; the command's wall clock was 323 s. No token or dollar counter is exposed in the store, so that episode's cost is unknown, not zero. `--record-selfhost` then recorded both streams into the edition, `docs/evidence/current.json` now selects authority and feedback at WO-147 revision 001 with artifact-identity and verification left at WO-146, `node scripts/console-fixtures.mjs --record-current-selfhost` re-pinned the console's self-host case and `--write` regenerated it, and every check is green: `authority-evidence --check`, `feedback-evidence --check`, `console-fixtures --check` (five cases), and the document gate below.

**Why here and not through repair.** D008's follow-up says the only legal route is a failing final review, `resume: fix`, VER-002 and FINAL-002. That is true of the lifecycle mechanics if an executor must do the work, and I disagree that one must. No byte under `packages/` changed in this review; the editions record the reviewed source, they do not alter it; the authority edition is deterministic and the feedback edition's only moved field is the subject hash; the independent check a VER-002 would add is the same mechanical `--check` the review gate runs; and product 07 §Independent workflows and integration asks that bookkeeping not create a failed FINAL or a repair event. WO-047's final review did exactly this on an integrated tree. The alternative costs three dispatches and a second product gate over unchanged code. D010 records the decision, the rejected route, the live episode as this review's one admission, and a low-priority follow-up for the planner: an order that edits a registered evidence source carries one live self-host episode in its cost line, and the catalog's "no live model" dispatch dimension is wrong for that class, as WO-143 D002 also met in flight. D008's text and VER-001's recorded verdict are untouched; D008's register row is planning's to settle.

## Acceptance, judged at the integrated tree

| # | Criterion | Judgment |
| --- | --- | --- |
| 1 | Two real processes: the contender pauses after observing `host.lock` and before reading it, the holder releases, the contender resumes and acquires; no `ENOENT`, prior event bytes identical, `ResidentStore.transaction` completes on the contended path; a mutant with the re-inspection removed fails | **Met.** The fixture pauses the contender peer at `lstatSync` on `host.lock` after the call (that is `present(lock)`, the first inspection), the test awaits the holder's `release()` before letting the contender go, and asserts `acquired` true, `error` empty and the log bytes equal, on both arms; the transaction arm's holder owns `.resident-append/host.lock`, which is the lock `transaction` takes first, so the contended path is the real one. I traced both `ENOENT` sources into the one catch: `regularFile(lock)` throws Node's raw error with `code`, and `readFileSync` inside `atPath` now surfaces as a `StorePathError` carrying the same code (D007); anything else rethrows. VER-001 reproduced the discriminating claim by removing the `try`/`catch` from the compiled path and watching the fixture fail in 139.24 ms on raw `ENOENT`; the executor's own mutation failed in 193.86 ms. The direct-acquire arm's byte-identity assertion is trivially true on the empty log `acquire()` creates, as VER-001 recorded; the transaction arm carries the substantive one |
| 2 | Probe: a delayed claimant links into the retired target while retirement's `rmSync` is paused; either `ENOTEMPTY` reproduces and the repaired retirement completes with the guard intact and the claimant refused, or the decisions file records the probe and that the inference did not reproduce | **Met on the negative branch.** Twenty attempts pause the retiring peer before `rmSync`, assert `host.lock` already names the original's pid so the pause is after publication, let the delayed peer hard-link a valid successor claim into the retired target, assert the entry exists, then resume; `reproduced` is 0 of 20, the original acquires and the delayed claimant refuses every time. D003 records pause points, attempts and outcome and closes the inference as unobserved, with a reopening condition that names the stronger pause VER-001 showed is unreachable through this fixture's interception (Node's recursive removal makes no JS-level `fs` call). `acquireGuard` is unchanged |
| 3 | Every WO-143 boundary fixture obtains its dead pid at use and asserts deadness before and after; the named case passes ten runs under `npm test`'s concurrent load and ten alone, both recorded; a remaining failure is recorded with its cause | **Met.** The hoisted `const pid = deadPid()` is gone from the matrix and the guard-refusal matrix; each constructed case, each fault, each released-lock case, the malformed-state fixture, the concurrent dead-guard fixture and the retirement probe allocate at use, and `assertDeadPid` re-asserts `ESRCH` after every boundary. `fixtures.md` records ten `npm test -- --only skeleton` runs (260 to 313 s, 2,714 s) and ten isolated runs (154 to 210 s, 1,661 s), all passing: 6,880 boundaries with no live-pid observation, counted only on the final code after the D007 repair. VER-001 added one full canonical gate and one isolated run (38/48 boundaries per cell, 344 in total); this review's gate is one more concurrent observation |
| 4 | Live, unreadable and legacy guards refuse unchanged; a store written by v0.32.1 opens unchanged; the lock-cycle cost is re-measured | **Met.** The four WO-143 guard fixtures pass in VER-001, including the released-lock compatibility case; the retry is keyed to the filesystem code and not to a message, so a malformed lock, a non-regular lock, an `EACCES` read and the live-host refusal all still refuse. Two 400-cycle measurements after 20 warm-ups report medians 17.942 and 17.296 ms (p99 23.174 and 23.167) against WO-143 D006's about 19 ms, and VER-001 re-measured the cycle on its own build; no steady-state call is added when the lock does not vanish |
| 5 | Write-backs: a dated `runtime.resident` reassessment that removes the contention limit or states what remains, judged by verification and final review; product 03's lock paragraph; the decisions file | **Met after this review's edit.** Product 03 states the bounded re-inspection, that other missing or malformed state still refuses, and that boundary fixtures observe pid reuse rather than assume it. The executor staged the capability row hedged, and VER-001 correctly left the judgment to this dispatch. I have now judged it: the row keeps level 2 within the tested local process-crash scope, states that the scope now includes live contention between conforming local writers, removes the limit WO-143 FINAL-001 wrote in, and states what remains (an out-of-protocol same-user writer behind the guard, the inference closed as unobserved, `ESRCH` as the only establishment of death, the unchanged cycle cost, and WO-111's hour as the reopening observation). Decisions D001 to D010 are indexed; both publication locks were refreshed for the row |
| 6 | `npm test` green; `git diff --check` clean; no new dependency | **Met, executed here.** Gate below. `git diff --check` is clean in the working tree and the index. The manifest and lockfile change only the internal `@dotln/skeleton` `0.34.0` → `0.34.1` and the console's exact pin; the staged source introduces no `eslint-disable`, `@ts-ignore`, `@ts-expect-error`, `@ts-nocheck`, `prettier-ignore` or `biome-ignore` |

## Gate and affected checks

Every reviewed tracked change and every new file — the two editions, the re-pinned console case, D009, D010 and the refreshed projections — was staged before the gate, and the sandbox probe reported no harness sandbox in force, so it ran unsandboxed from the start.

- `npm test -- --review` — **28 suites, 0 failed, 277.17 s, 72 fresh tasks, exit 0**, recorded 2026-09-21T21:06:44.230Z at code identity `d733e5981afbc0a90564cc9de1729e90d6b004c16c22fc9a096486930e410ccc`, tree `fa15a0b435920e768a41c512adb3fdff67d372dc`.
- The review documents written after the gate are confined to `docs/`, which `gateCodeIdentity` excludes, so the reviewed identity is unchanged at publication.
- `npm run test:docs` after the documents: run 1 (with this report’s handoff placeholder still in its cost line) 13 passed, 6 failed, 4.16 s, because the `meta` suite refused the placeholder and the five suites that prepare behind it did not run; run 2, after the handoff counter was written, **19 passed; 0 failed; 25.78 s; 19 fresh tasks**; run 3 on the final bytes, after this sentence was written, is reported in the handoff response and matched run 2.
- The integrated-tree checks in §Integration, `authority-evidence --check`, `feedback-evidence --check` and `console-fixtures --check` all pass, and `npm run meta` reports no reopen candidate.

## The verification sequence

One verification, one pass, and one correction its author filed after recording it. I judge VER-001's six criterion judgments correct and independently grounded: it did not trust the changed fixture, it mutated the compiled path and watched the fixture fail; it did not accept the retirement probe's pause point, it patched `fs` and showed that Node's recursive removal makes no JS-level call, so the executor's pause is the only reachable boundary; it read the refusal surface in the source rather than inferring it from green runs; and it added one canonical gate and one isolated 344-boundary run to the stability record instead of re-running the series. Its instrument disclosure is honest and its `**Process cost:**` line is correctly formed. Where it was incomplete is exactly where D008 says: criterion 6 was judged on `npm test` alone, and the review selection that reads the registered evidence sources was red. D008 records the misread, what was meant and what changed, in the corrections form product 07 asks for, and leaves the report's bytes and verdict as made. I agree with its finding and its cause and disagree with its route; §The evidence editions and D010 say why. Nothing else in the sequence needed correction.

## The ideation receipt, and what happened to its two known issues

The 2026-09-21 standard pass allocated WO-143 D004 and WO-069 D015 to this order, and refutation receipt 022 (`planning-9d2888b45f687bc6`, over the subject at `3a6aa592`) judged it `aligned-with-findings` with two known issues, no observed failure and no vision contradiction; findings confer no operator decision authority, and none was treated as one.

1. **One re-inspection is not closed under a second vanish.** D004 dispositions it rather than adding a two-vanish fixture: the acquisition guard is held across both inspections and excludes every new conforming writer, publication of `host.lock` happens only under that guard, and `release()` unlinks only the holder's own lock, so a second vanish needs an out-of-protocol same-user writer, which the order excludes by name. I checked the argument against the code and it holds; the reopening condition names exactly that writer, and the capability row now carries it as what remains.
2. **Criterion 3's ten runs and their shape.** The receipt priced ten canonical gates at about 8.4 hours and asked the decisions file to record the run shape and wall clock. D006 and `fixtures.md` record ten `--only skeleton` runs and ten isolated runs with per-run seconds, 4,375 s in total, which is the reading the receipt preferred; VER-001 confirmed the runner schedules the skeleton suite in a multi-suite lane in a full gate, and covered that heavier shape once.

The receipt's commons observation had one blind spot that this review met rather than the planner: the catalog row's "two-process fixtures on the local filesystem; no live model" did not account for the live self-host episode a change to `worker-store.ts` requires. That is D010's follow-up.

## Findings, observations and limits

**No finding.** Nothing routes to repair.

**Observations.**

1. **The stale editions were a duty the order's own lineage documents.** WO-143 D002 records, for the same file, that "worker-store.ts is a declared behavioral source, so the existing live evidence no longer judges the current implementation", and re-minted with a live verifier. This order's executor did not, its `npm test` could not show it, and the verifier caught it after recording a pass. The review gate is where the lifecycle catches it by design, and it did. Cost of the miss in this dispatch: one 323 s live episode of unknown dollar cost and about ten minutes of checks; cost avoided by not routing it: three dispatches.
2. **The direct-acquire arm's byte assertion is vacuous.** VER-001 recorded it; I read it the same way. The `ENOENT` claim does not depend on it and the transaction arm's log carries real content, so I did not extend the fixture; a later edit that seeds the direct arm's log would make both assertions substantive.
3. **`StorePathError` now carries `code` for every wrapped error, not only the lock's.** D007 chose that over a special case, and I looked for other consumers: only the acquisition catch reads it, and it rethrows anything that is not `ENOENT`. A future reader of `code` elsewhere inherits a path-named error whose code is the underlying filesystem code, which is the right meaning.
4. **The helper's receipt string still prints a doubled period.** "local snapshot only.." reached the D009 stub, as WO-138 D011 already boards; I corrected it in the completed record and did not touch the helper mid-review.
5. **The order's cost line held.** No step, gate, hook, key or receipt was added; the meter's `gateStepCount` reads 66 at this review, and the two fixtures and the helper live in the existing resident suite as promised.
6. **The dispatch briefing's observed-facts block was a timing artifact again.** It reported every counter unavailable; the usage command answered seven seconds later. WO-148 and WO-150 recorded the same.

**Limits.** The proof is bounded to conforming local writers that honor the acquisition guard; a same-user process that creates or removes `host.lock` behind it is outside this order and the single re-inspection is not a fixed point against one. The retirement inference is closed as unobserved at the only pause point this fixture technique can reach, not disproved. `ESRCH` remains the only establishment of death and ambiguous liveness still refuses. This review did not re-run the ten-and-ten series; its gate is one further concurrent observation of the named case. The live self-host audit judged its pinned feedback capsule, not this order, which is what that evidence is for. WO-111's first unattended hour remains the reopening observation, and nothing here claims it. I ran no economy experiment: this is the reviewer role, which the support does not equip.

## Goal alignment at handoff

The promised benefit was the removal of the one recorded ownerless way a live-contended store ends a presence command or a resident transaction, ahead of WO-111's first unattended hour, with no new mechanism. The observed outcome is that and no more: one guarded re-inspection keyed to a filesystem code, discriminated by a fixture that fails without it, with the refusal surface, the reclaim writes and the transaction's retry pattern unchanged. Against the traps, scaled to consequence: **rule beating** was the live one and is answered twice, by the mutant that fails the fixture and by the staleness check that refused to let an older live audit stand for the changed store; **tragedy of the commons** is measured, at 4,375 s of stability evidence in implementation, one 323 s live episode here, and one gate per dispatch, with no recurring command added (D002); **drift to low performance** is checked by the re-measured cycle cost against WO-143's baseline; **shifting the burden** moves the right way, since a contended `ENOENT` no longer needs an operator rerun; **escalation** added no gate, hook, key or receipt, and this review declined to add three dispatches for a document; **success to the successful** is answered by D001's comparison against a transaction-level retry and a new mutex; **policy resistance** is bounded by the unchanged refusals; **seeking the wrong goal** is answered by asserting prior event bytes rather than a successful retry. NoOp would have left the race in the capability row as a stated limit and WO-111's hour exposed to it. Naive Interventionism is answered by the intervention being one bounded retry inside the path that already holds exclusion, reversible by removing the catch. My own intervention was integration bookkeeping, two regenerated evidence editions and one live episode, one capability row, two decisions, two locks, one console fixture, three review documents and the gate; no byte under `packages/` changed.

Process cost at entry: 84,842 total tokens at 2026-09-21T20:42:59.750Z, scope `dispatch`, source `claude-transcript-message-usage`, `costUsd` unknown. At handoff: 99 steps and 98 commands, 6,829,123 total tokens (808 input, 6,341,656 cached input, 352,868 cache write, 133,791 output) at 2026-09-21T21:11:57.117Z, scope `dispatch`, source `claude-transcript-message-usage`; reasoning tokens and USD are unknown, and the counter’s own note says the wrapped command count may be incomplete. Subagents: 0 of 20, `exact-observed`, 0 unresolved admissions, uncounted remainder unknown; the live verifier episode was a child process of the audit command and is not counted in that figure. The result transition, the commit series and the publish that follow are not in this total. The single product gate and the live audit dominated the waiting; both were required, the gate because the integrated identity is new and is the one the release manifest consumes, the audit because a fresh feedback edition cannot be recorded without a live verifier. Before spending the gate I ran the four printed affected checks, the planning check, the index check, the formatter and the whitespace checks, which together took under two minutes. One avoidable cost is mine: three document drafts were refused by the live-gate guard while the gate ran and had to be re-issued after it.

## Release-close request and publication

This dispatch authorizes committing the reviewed state, pushing the `wo-147` branch and opening its pull request. It does not authorize a merge, a push to `main`, a tag, a Release or a package publication. The PR body beside this report carries the meter table `release prepare` supplied; the five-section release notes follow the publication contract at one physical line per prose paragraph and disclose the re-inspection, what the proof does not cover, the retime, the re-minted editions and the live episode. Commits are made in coherent series after the result is recorded, with plain subjects and no attribution. After the operator merges, release close consumes the committed gate row without running a suite.
