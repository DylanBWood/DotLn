# WO-128 — FINAL-001

**Verdict:** PASS

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.270","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

Claude Code 2.1.270, observed with `claude --version`. The model is `claude-fable-5-1`, the exact model ID the session exposes. Effort `max` is the operator's `/model` selection at the start of this session, not an effective-session readback, so the source is self-reported. The order declares the reviewer effort as `any`. This session took no part in the implementation or the verification.

## Subject and inputs

Branch `wo-128` at base `5ce291b`, which equals local `main` and, by a read of the remote heads, `origin/main` at review time, so no integration was needed. The subject is uncommitted: 52 modified tracked files (790 insertions, 205 deletions at review start) plus 25 untracked files under `docs/control/orders`, `docs/evidence/WO-128`, `docs/verifications/WO-128`, `docs/final-reviews/WO-128`, `packages/skeleton/src` and `scripts`. `FinalReviewRequested` was recorded at 2026-09-13T00:06:13.686Z (checkpoint `refs/dotln/checkpoint/WO-128/5`, `bc8fbf3`). Between VER-001's completion checkpoint 4 (`15df12f`) and checkpoint 5 only the three lifecycle projections changed, and VER-001 is byte-identical to its completion checkpoint.

Reviewed: the original order with its `## Execution record`; every cited section (07 §Discipline's process-budget, write-once and gate-execution bullets and §Goal-aligned decisions; `suites`, `scheduleSuites`, `expandSuiteTasks` and `runGateChecks` in the runner; `suiteEnvironment` and `reusableResult` in the suite evidence; `recordGateChecks`; the collector's command budget; the harness fixture's 20 s sites; the runner fixture's timeout case; WO-125 VER-001 O6, VER-003 O3, VER-004 AC5 and FINAL-001 item 3; WO-126 D009, D012 and D026; WO-127 D004, D005 and D007; the map's console host-collection candidate); the full subject diff, including the four new modules, the regenerated hooks and manifest, the evidence editions and the release surfaces; VER-001; the evidence README, diagnosis, fixture results and D001–D009; the series, baseline, comparison and signal records; `package.json`; 08 §PRs and commits and §Release-note edition; and the WO-128 entry of planning receipt 011, the ideation receipt the dispatch names.

## Independent checks

- `node scripts/measure-gates.mjs --check` accepts series 002: five first-time passes at 689,520, 692,702, 694,561, 697,079 and 699,430 ms, each with 78 executed and 0 reused tasks, one tree hash, and a stored critical path equal to the recomputed one. The same check fails series 001 on its fifth run's exit code.
- The twelve failed gates were re-read from the main worktree's retained store with the subject's `readGateChecks` (4,522 rows, 127 full gates): the aggregate durations and failing suites match the diagnosis and total 8,385,737 ms; the two tree-changed gates carry every suite as failed, as the diagnosis says; the 16:08Z comparison gate passed at 476,304 ms.
- Per-task durations across the three retained data sets agree on what shared scheduling costs: `harness-fixtures` ran 133.7 s standalone, 134.4 s in the prior exclusive gate and 497–507 s in series 002; `process-debt` 64.4, 63.5 and 218–226 s; `plan-refutation:fixtures` 114.2, 187.5 and 524–534 s; the five series rows carry 2,674–2,711 s of task time against 1,229 s in the prior gate. Each row's critical chain is within 1.3 s of its wall-clock, so the recorded gate is one serial chain through `plan-refutation:fixtures` and the release cases.
- Fixtures on the subject: `scripts/test-gate-deadlines.mjs` 8/8 (1.1 s); `scripts/test-runner.test.mjs` 19/19 (8.1 s). After the F1 repair below, the deadline file passes 9/9 and the pair 28/28.
- VER-001 F1 reproduced: the table it describes, scheduled serially, gives `c` the skipped `a` as a predecessor and `gateCriticalPath` throws `Missing timeline predecessor: a`; at concurrency 2 the same table does not reach it. The expanded full-gate table has one group (`kernel`, `compiler`, `skeleton`; `console` is ungrouped) with one shared dependency list of the 13 preflights, so the crash needs a future group with mixed dependencies, as VER-001 said.
- Deadline sweep: every file with a numeric `timeout`, a `Date.now() +` bound or a long `setTimeout` that lacks the observer import is outside the gate: the build publication benchmark, the live harness smoke, suite and probe scripts, the Codex effort and worker-host probes, the live worker test, the compiler's 15 s hook metadata for external hosts, and a one-second sleep in the mutation self-test that is not a bound. Every gate-reachable timed call site imports `gate-deadlines.mjs`.
- `deadlineLimit(17_200, 60_000)` is 137,600 ms at four slots and 60,000 ms standalone (fixture). The comparison record's collector row has 44 standalone samples with a 10,480.5 ms maximum and 220 shared samples with per-run maxima of 60,410–61,751 ms, no hits, at most 3 peers and host load at most 0.55 per CPU; the old 60 s constant would have failed every accepted run.
- The three runtime modules `gate-deadlines.mjs`, `gate-evidence.mjs` and `harness-host.js` are byte-identical, by SHA-256, between `packages/skeleton/dist/src/` and the pinned snapshot `.runtime/harness/de21c0a913b9dd91` that the manifest and all nine generated hooks import at skeleton `0.15.3`; each hook diff changes only the snapshot path, the version and the file hashes (11 insertions, 7 deletions per hook).
- Dependencies are unchanged: the lockfile changes only the two versions and adds console's `name` field; console already imports skeleton's built modules. `git diff --check` is clean. The clean-room screen of the added tracked lines and the untracked files found no home paths, mail addresses, credentials or key material.

## Acceptance criteria

Criterion 1 holds: `historical-gates.json` lists the twelve gates with failing task, deadline finding, duration under load, inferred concurrent tasks and the only historical solo figures (the console's 17.2 s command and 23.2–25.5 s test from the WO-125 reports); the sites the disposed logs cannot establish are recorded as unknown.

Criterion 2 holds: the inventory covers every reachable fixed bound my sweep found, with its standalone maximum and its treatment; the collector bound is derived from the 17.2 s baseline through the declared factor with the 60 s floor, the harness 20 s sites use `max(20000, 1000 × factor)`, and the other finite bounds keep their values under the shared cap of four. A hit records the site, bound, duration, current peers, peer-observation source, host load and a classification; the slow doubles pass a manual-clock barrier at 700 of 800 ms and hit at 801 ms reading the changed peer set, pass a 300 ms child under its 2,000 ms derived bound, and fail a hung child that exits 0 on `SIGTERM` while keeping its diagnostic.

Criterion 3 holds: each gate row carries `taskTimeline` with start, end, peers at start, predecessors, class, factor and peer cap for all 78 tasks, plus `criticalPath`; the runner fixture proves the fields on scheduler output and the offline computation, the series checker recomputes all five stored paths, and the review adds the skipped-member case.

Criterion 4 holds as written: with criterion 2 in place, the two hook suites ran under the shared cap in five consecutive first-time passes on the operator's host, the flags are removed, and the wall-clock is recorded beside the 476,304 ms gate in the README, the diagnosis and product 07. The outcome contradicts the order's promised benefit (VER-001 F2): on the same source, exclusive scheduling ran the 78 tasks in 462.3 s against 666.5 s shared, and the retained rows agree. D010 records that correction and leaves the configuration to the operator, because the criterion prescribes removal and a review cannot amend the order.

Criterion 5 holds: series 002 is five consecutive outside-sandbox first-time passes on unchanged source; series 001's fifth-run failure was diagnosed as a fixture signal-delivery defect with a quiet-host negative control, repaired at the protocol, and the count restarted with the failed series retained. "Outside the sandbox" is attested by the series file's constant, not observed (VER-001 O2).

Criterion 6 holds except for the gate on the final bytes, which the completion command records: product 07's gate bullet and cold-gate candidate carry the write-backs; D001–D011 and their index rows exist; FUP-0054's duplicate disposition points to the settled candidate; the execution record uses the literal heading; `git diff --check` is clean; no dependency was added; application `v0.17.3` sits above the local `v0.17.2` with skeleton `0.15.3` and console `0.1.5`.

## Review changes

- **D010 (correction).** The diagnosis called the removal of exclusivity a measured benefit and the order's Cost line predicted up to 206 s saved per fresh gate; the same-source comparison measures about 204 s added. The diagnosis, the evidence README and product 07's gate bullet now name the cost and the open operator decision; the flags stay removed as criterion 4 prescribes, and neither reliability repair depends on the shared class.
- **D011 (repair of VER-001 F1).** `scheduleSuites` now adds only started tasks as a group predecessor, so a member skipped for a failed dependency is never a timeline edge, and a fixture schedules the F1 table serially, checks the skipped task is absent from every predecessor list and computes the chain `build`, `pre`, `c`, `b`, `d`. One predicate in a named path under the shared `runner-fixtures` check; scheduling order is unchanged.

## Gate

The canonical gate for this review is `npm run harness -- evidence` on the tree containing this report, the PR body and the release notes; the runner's record for those bytes is the completion authority, and `final-review-result pass` refuses without a passing `npm run test:full` and `git diff --check` for the exact tree. The review's runner change invalidates the suite inputs, so this gate re-executes rather than reuses. This report claims no gate result of its own.

## Observations (non-blocking)

1. The exclusivity configuration (D010) is the operator's decision: one exclusive sample passed with the repairs in place, which is not the five-run series criterion 4 demands, and restoring the two rows would reverse a prescribed outcome. A follow-up order or an amendment can measure the exclusive configuration under the same checker.
2. `scripts/measure-gates.mjs` writes "outside the sandbox" as a constant, so a series cannot record where it ran; the attestation is the operator's (VER-001 O2).
3. The `outside-declared-load` threshold of 2.0 per CPU is declared, not measured; the highest normalized load observed is 0.755 (VER-001 O3).
4. Every gate row's `deadlineDiagnostics` carries the four intentional negative-test hits, labeled cause-unestablished; a reader separates them by site. One diagnostic directory is kept per gate run with no retention (18 in this worktree). Local and ignored; no change requested.
5. Series 002 and the prior gate ran on different sources, with successful-call recording on in the series only, so the README's 45.8% figure is not a scheduling measurement; the F2 comparison is.

## Goal alignment

WO-128 removes the largest recorded waste on the route to the independently verified source-to-deliverable loop: twelve failed fresh gates and their reruns, 8,386 s in two days. The deadline half delivers that: five outside-sandbox fresh gates and VER-001's sandboxed one passed first time, the collector's derived bound absorbed a 61.75 s command that the old constant would have failed, and every hit now names its peers. Passing sends the order to publication with that reliability and with the exclusivity cost stated rather than absorbed. Failing, the NoOp for this decision, would spend a repair-and-verify cycle on an outcome criterion 4 rules out and on a latent crash the review repairs in one predicate. Rule beating and seeking the wrong goal: criterion 4's rule passed while its purpose went the other way, so D010 records the measured cost instead of treating five passes as the goal. Drift to low performance: a fresh full gate about 45% longer than the exclusive one is now the baseline; the number is on the record with the reversal left to the operator. Tragedy of the commons: a shared fresh gate consumes roughly twice the task time of an exclusive one, once per phase. Success to the successful: the workaround was re-measured, and the measurement favors it. Policy resistance, escalation and shifting the burden: no retry, raised constant or extra gate was added, and first-pass success removes the operator's rescue loop. Naive Interventionism: the review's two changes are a documentation correction and a predicate that only removes an edge; the isolated class and every original bound remain. Reopen if the operator restores exclusivity, a recorded gate row names a predecessor absent from its timeline, or a fresh gate fails on a deadline within the declared class.

## Costs and publication

Tokens, measured with `node scripts/harness.mjs usage` (source `claude-transcript-message-usage`, scope `dispatch`): 134,347 at entry (observed 2026-09-13T00:04:01Z, 3 steps, 2 commands); 3,022,053 before this report was written (2,691,869 cached input, 266,005 cache-write input, 63,695 output, 484 uncached input; 97 steps, 96 commands; observed 00:17:35Z). The completion action records the handoff figure. Cost is null; the transcript records no price. The total includes the fixture runs and waiting; the independent checks added no gate run beyond the final canonical one. No equivalent alternative review was measured, so no efficiency comparison is claimed.

The reviewed state is committed as coherent commits (the deadline observer, load classes, timeline and collector bound with their fixtures; the two fixture-protocol repairs; the release preparation, runtime pins and evidence editions; the write-backs, evidence, reports and lifecycle records) and published with `npm run worktree -- publish WO-128 --title <reviewed title> --body-file docs/final-reviews/WO-128/PR.md`. Merge and release close remain the operator's.
