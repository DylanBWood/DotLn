# WO-158 FINAL-001 — final review

**Verdict:** fail. WO-158 asked for four typed off-ramps with their own `resume` routes, a terminal `withdrawn` phase every projection knows, an override record appended at `operator override: off`, and a live `npm test` gate that admits a fixed read-only list while still refusing every redirecting or unlisted command. The control-plane routes, projections, fixtures, role text and editions hold on the integrated tree, and VER-001's four repairs hold. One acceptance defect remains on criterion 6, introduced by the F4 repair: `liveGateGit` in `packages/skeleton/src/harness-command.ts` never advances past a second Git prefix token, so `git --no-pager --no-optional-locks status` (the WO-142 prefix product 07 documents), `git -P --no-pager log` and `git --no-pager --no-pager log` loop forever inside the live-gate classifier. While a gate is live that hangs every generated pre-tool hook until the host's 15 s hook timeout; the listed read is not admitted, and a write chained after it goes unjudged wherever a hook timeout is fail-open, which the Copilot hook reference documents. I reproduced the loop myself. A reviewer does not write and certify a behavioral fix, so the finding returns to repair ([D027](../../evidence/WO-158/decisions.md#wo-158-d027)). `main` moved by WO-159 (`v0.48.0`) during the order; this review integrated it, retimed the collided skeleton label to `0.42.0`, re-minted the authority edition and carried WO-159's live feedback audit, and that integration stays staged for the repair.

**Subject:** [`docs/work-orders/WO-158-lifecycle-off-ramps.md`](../../work-orders/WO-158-lifecycle-off-ramps.md) on branch `wo-158`, uncommitted over base `1f88850975c5aa7c2e8169c7267017d06d34a302`. The dispatch checkpoint is `refs/dotln/checkpoint/WO-158/9` (`b6fe15fb`); against VER-002's result checkpoint `refs/dotln/checkpoint/WO-158/8` (`5d3f61ab`) it differs only in `docs/control/current.md`, `docs/work-orders/README.md` and the `FinalReviewRequested` row, and VER-002's recorded `reportHash` equals the report's current SHA-256 (`026e8d60…`). `npm run worktree -- integrate WO-158` checkpointed the work as `refs/dotln/checkpoint/WO-158/10` (`63329cf1`), retained the include-untracked stash `caf830d5bcf7569d05af801e866c41ef287399cf` (`WO-158 integrate 2026-09-25`) and fast-forwarded the uncommitted branch to `main` at `0c727915a0582120cbe12d41f48ada53ddd7e5ce`. The judged subject is the staged integrated tree: 103 paths against `main` (27 added, 76 modified). No branch commit exists.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-opus-5-5","effort":"xhigh","source":"claude-session-readback"}

Human actor: the operator dispatched `resume: final review` in a Claude Code session after selecting Opus 5.5 with `/model`, and made no other choice during the review. The harness version is from `claude --version`. The model is the session's selected model. Effort `xhigh` is the host's `CLAUDE_EFFORT` value read by this session: selected effort, not effective. The order asks for `reviewer any`. Two read-only review agents were planned and spawned before any gate, each judging a group of criteria (1 to 4; 5 and 6 with the grant seam), with no descendants: 2 of the cap of 20 (`harness usage`: exact-observed). They wrote nothing in the repository and probed only under the DotLn session scratch; the verdict rests on this reviewer's own reproduction.

**Process cost:** entry 82415 tokens; handoff 16239850 tokens; source claude-transcript-message-usage

Both readings are `dispatch` scope from `node scripts/harness.mjs usage 8948bb29-1c83-49bc-b9b3-e80288b31a06`: entry observed at 2026-09-25T14:53:14.646Z, handoff observed at 2026-09-25T15:21:15.746Z (2 subagents of cap 20, exact-observed), taken after the judgment text was complete and before the result transition. They include reused cached input and exclude the two review agents' own counters (231,878 and 170,039 tokens by their completion notices). Reasoning tokens and dollar cost are unavailable from this counter, which means unknown, not zero. The largest waits were the two review agents (918,731 ms and 927,251 ms, concurrent). No product gate ran.

## Goal-aligned judgment

The order is record-truth and throughput machinery, not a lettered critical-path gate (receipt 028): it moves recurring rescue out of prose into typed events and projections, and it lets sessions read while a gate runs. The judgment that matters is whether each route does what it claims without opening a new way around a standard. **Rule beating** decided this review: the live-gate fixture admitted `git --no-optional-locks --no-pager show`, the one prefix order the loop survives, so a green fixture stood in for a classifier that hangs on the documented order. **Fixes that fail** is the same event from the other side: the F4 repair closed the pager path and opened the hang on the same line. **Shifting the burden:** product 07 teaches sessions the exact prefix that hangs, so the defect would land on the next session that follows the guide during a gate. **Naive Interventionism** bounds the response: one line and a regression across prefix orders, not a redesign of the list; everything else the review met is boarded with register rows rather than folded into the repair. **NoOp** would publish a live-gate hook that a documented read stalls and that, on a fail-open host, stops judging chained writes. The remaining lenses are immaterial here: the fix consumes no shared budget (tragedy of the commons), raises no ceiling (drift to low performance), adds no process (escalation), and excludes no alternative on investment (success to the successful); policy resistance was checked where waiver, correction and report judgment meet, and they agree.

## Criteria

- **Criterion 1:** met. The four routes append their events through `appendTransition` with the actor attestation and a checkpoint, refuse outside their legal phases naming them, and `status --json`, `current.md` and the index project `legalOffRamps`, `waivedCriteria`, `withdrawal`, `corrections` and `overrideRecords` ([D002](../../evidence/WO-158/decisions.md#wo-158-d002)). `scripts/test-off-ramps.mjs` covers each event's legal and illegal phases and a withdrawn order a typed dependency reports as `unmet` with `detail: withdrawn`; a review agent re-ran it and the work-order and derived-order fixtures green in scratch, and folded every recorded segment and the legacy log under `main`'s and the staged `control.mjs` to identical projections for 103 orders. Limit ([D029](../../evidence/WO-158/decisions.md#wo-158-d029) d): `current.md` shows a withdrawn order only on its own `wo-NNN` branch.
- **Criterion 2:** met. `waive` and `withdraw` require a contained, ignored, untracked, non-empty capture whose SHA-256 matches `--capture-hash`; `waive` refuses an executor journal or an executor-held writer reservation and an undeclared criterion id; a verification passes with `unmet, waived by <ordinal>` and fails on the same line without the waiver ([D003](../../evidence/WO-158/decisions.md#wo-158-d003), [D004](../../evidence/WO-158/decisions.md#wo-158-d004), [D009](../../evidence/WO-158/decisions.md#wo-158-d009)). Limit (D029 a): under Codex, which has no hook-made reservation, removing `CODEX_THREAD_ID` from the command's environment records the waiver with `recordingSession.role: unobserved`; the refusal holds for every observed session and Codex enforcement is role text by design, so this is boarded, not an unmet criterion.
- **Criterion 3:** met. Every `resume` dispatch refuses from `withdrawn` except `activate`, which requires a changed authority and a new calendar-dated `**Reactivation (YYYY-MM-DD):**` note on or after the withdrawal (F1 repair, [D022](../../evidence/WO-158/decisions.md#wo-158-d022)); the index lists the disposition unchecked under Closed; the sequence topology and plan checks settle withdrawn edges ([D005](../../evidence/WO-158/decisions.md#wo-158-d005), [D007](../../evidence/WO-158/decisions.md#wo-158-d007)). Limit (D005's follow-up `FUP-3a0c4ea52f8d6d08`): a withdrawal reaches `main` only by an operator merge, because `worktree publish` requires a closed phase.
- **Criterion 4:** met. `correct` refuses `verdict` by name and never writes a report; a report path moves only to the same report id holding the bytes the result's recorded `reportHash` names (F2 repair, [D023](../../evidence/WO-158/decisions.md#wo-158-d023)); the fold projects corrected attestations with `correctedBy` and recomputed effort drift ([D008](../../evidence/WO-158/decisions.md#wo-158-d008)); a `claude-code` completion whose `--effort` disagrees with a readable `CLAUDE_EFFORT` is refused with the value. This order's own `VerificationCompleted` for VER-002 carries `reportHash` `sha256:026e8d60…`, equal to the file. Limit (D029 c): that refusal keys on the `claude-code` harness spelling (WO-157 code, unchanged).
- **Criterion 5:** met. The generated session hook appends `OperatorOverrideRecorded` at `operator override: off` with a hash-bound capture of the operator's prompts and prints the exact advisory when the runtime, a live gate, the writer reservation or a missing open order stops it; same-second exits take distinct exclusive captures (F3 repair, [D024](../../evidence/WO-158/decisions.md#wo-158-d024)); exit writes the normal mode before any runtime work, so recovery never blocks; Codex's `operator-control.mjs off` prints the advisory ([D012](../../evidence/WO-158/decisions.md#wo-158-d012)). A review agent re-ran the four WO-158 harness cases green. Limit ([D028](../../evidence/WO-158/decisions.md#wo-158-d028) c): a hook input the decoder refuses prints neither the exit message nor the advisory.
- **Criterion 6:** unmet. The list refuses redirects, heredocs, expansions, wrappers, `sed` write and execute commands, in-place flags, Git `-c`, `-C`, `--git-dir`, `--output`, `--ext-diff`, `--textconv`, `%G` and paged reads, and names the list in its refusal; but a Git stage with two prefix tokens after `git` loops forever in `liveGateGit` (F1 below), so a listed read hangs every pre-tool hook during a live gate and a chained write is not judged before the hook times out.
- **Criterion 7:** met. All twelve generated role skills carry the shared `Off-ramps (WO-158)` sentence, the verifier and reviewer the criterion-line form; product 07 §Operator recovery controls closes the recovery event shape with the four routes, the resume-phrase table gains four rows, and the live-gate vocabulary names the list. On the integrated tree `node scripts/harness.mjs check` reports 31 generated surfaces and `node scripts/harness-context.mjs --check` measures executor 25,819/29,246, verifier 22,698/25,151, reviewer 23,780/24,576, release-close 14,649/16,384, planner 16,797/24,576 and refuter 16,372 unset, VER-002's figures; no ceiling is breached ([D013](../../evidence/WO-158/decisions.md#wo-158-d013)).
- **Criterion 8:** met. On the integrated tree `docs/evidence/current.json` selects authority `WO-158/003` (re-minted deterministically by this review over both orders' sources), artifact identity `WO-158/001`, verification `WO-158/001` and feedback `WO-158/002` (WO-159's live audit carried: `only component release labels moved; no live episode`); all four checks pass ([D015](../../evidence/WO-158/decisions.md#wo-158-d015), [D026](../../evidence/WO-158/decisions.md#wo-158-d026)). The repair's source change will make authority stale again.
- **Criterion 9:** met at VER-002's subject and not re-established here. VER-002's `npm test` passed 27 of 27 suites and its `npm run test:docs` 21 of 21; on the integrated tree `git diff --cached --check main` is clean and the lockfile and manifests change only component versions and pins, with no new dependency. This review ran no product gate, because it failed on a reproduced defect and the repair changes a registered source; the gate belongs to the next final review.

## Findings

### F1 — criterion 6: the live-gate Git prefix loop never ends

**Severity:** high; it regresses the WO-139 live-gate write refusal on fail-open hosts and stalls every pre-tool hook on a documented read. **Expected:** a Git read on the list, in any prefix order, is judged promptly; a write chained after it is refused. **Observed:** `liveGateGit` advances its index only inside `unpaged ||= args[index++] !== "--no-optional-locks"`, and `||=` skips the right side once `unpaged` is true. Against the built runtime, `liveGateReads` returned in 1 ms for `git --no-optional-locks --no-pager status` and `git --no-pager log -1`, and ran until a 5 s alarm killed it (exit 142) for `git --no-pager --no-optional-locks status`, `git -P --no-pager log`, `git --no-pager --no-pager log` and `git --no-pager --no-optional-locks -c core.fsmonitor=false status --short`, the prefix [product 07](../../product/07-execution-guide.md) line 2235 documents. A review agent's scratch gate fixture saw the generated `permissions` and `concurrent-work-requires-worktrees` hooks killed at 20 s on `git -P --no-pager log; echo x > fixture.ts`; the hooks are registered with a 15 s timeout. `activeGateWriteRefusal` calls `liveGateReads` for every shell command while a gate is live, before any write path is judged. The loop entered with the F4 repair (absent at checkpoints 2 and 4, present at 6); the fixture spelled the prefixes in the one order it survives. Claude Code's outcome on a hook timeout was not observed; Copilot's is documented fail-open. **Repair:** [D027](../../evidence/WO-158/decisions.md#wo-158-d027)'s follow-up (`FUP-25e1b23c7b844eb1`): advance past every prefix token in any order, with a bounded regression across prefix orders through the three pre-tool hooks with a live gate, including a chained write to a gate input; then re-mint, rerun the affected suites and request a fresh verification.

### Boarded observations

None of these fails a criterion; each is recorded with a register row and is not required of the repair.

- [D028](../../evidence/WO-158/decisions.md#wo-158-d028) (`FUP-6996e331536d4389`), hook and grant seam: a granted outside-write root swapped for a symlink carries the grant to its target (medium; WO-144's session-scratch has the class, and D010's host-scratchpad resolves the same way); repository hooks and `%G` pretty formats are not in the configured-program check; a decoder refusal suppresses the override exit advisory; a post-append exception reports a recorded override as not appended; the list conservatively refuses `HEAD~1`, `stash@{0}` and quoted `<`/`>`.
- [D029](../../evidence/WO-158/decisions.md#wo-158-d029) (`FUP-7b4b41e2875f852d`), control-plane seam: the Codex environment evasion of the `waive` executor refusal; the harness-spelling scope of the effort refusal; `current.md` on a foreign branch; unjudged criterion-line forms and the `reportHash` read after the evidence await. D005's `FUP-3a0c4ea52f8d6d08` keeps the withdrawn-order publication gap, including a restart after the branch is deleted.

## Integration with `main`

`main` gained WO-159 (`v0.48.0`, tag present locally; skeleton `0.41.0`; the Codex episode launcher; WO-159's authority, verification and feedback editions; 81 files). Outside `docs/` and the generated surfaces, the staged tree differs from VER-002's subject exactly by `main`'s WO-159 diff, file for file, plus three version files; the files both orders touched (`scripts/test-harness.mjs`, `scripts/test-runner.mjs`, product 03) took WO-159's additive, disjoint lines. The helper regenerated 21 projections and reported two authored conflicts: `docs/product/06-roadmap.md` keeps both orders' notes plus a dated integration note, and `docs/evidence/current.json` selects the editions above. `release check-surfaces --local` then failed `component-version @dotln/skeleton: ... observed 0.41.0; previous v0.48.0 0.41.0`, so the skeleton label is retimed to `0.42.0` under D014's additive minor impact (manifest, console pin, both lockfile entries); compiler `0.19.0` is free; the application target stays `v0.49.0`. Rebuild and `harness emit` changed no generated surface. The carried-forward claims, the retime and the edition evidence are in [D026](../../evidence/WO-158/decisions.md#wo-158-d026). The integration is staged, uncommitted, for the repair; the stash and checkpoint 10 are retained.

## Verification sequence

[VER-001](../../verifications/WO-158/VER-001.md) (Codex CLI 0.156.1, `gpt-6-sol`, `ultra` recorded `xhigh`/subagents) failed criteria 3, 4, 5 and 6 on four reproduced defects: an impossible reactivation date, a report-path correction rebinding a pass to other bytes, same-second override captures overwriting each other, and a configured pager run by an admitted `git log`. The [repair](../../evidence/WO-158/repair.md) (Claude Code 2.1.282, `claude-fable-5-1`, `xhigh`) fixed each with a regression ([D022](../../evidence/WO-158/decisions.md#wo-158-d022) to [D025](../../evidence/WO-158/decisions.md#wo-158-d025)). [VER-002](../../verifications/WO-158/VER-002.md) (Codex CLI 0.156.1, `gpt-6-sol`, `xhigh`) reproduced the repairs and passed all nine criteria at code identity `1172bcf9…`. VER-002 judged its recorded subject correctly as far as its probes went; the prefix-order hang sits in the F4 repair's new loop, which neither verification probed with a second prefix token. Both verifications ran on a different model and harness from the implementation. The executor's own adversarial review (D020) had confirmed and fixed 15 defects before handoff.

Ideation receipt 028 judged the order `aligned-with-findings` with six known issues, and each reached a decision: pipelines judged stage by stage (D011), the capture's authorship observed through `recordingSession` (D003), the override's words captured and the no-`off` session boarded (D012), the effort refusal without a readback admitting (D008), cold-start headroom measured within ceilings (D013), and a reactivated order leaving `withdrawn` in every projection (D007). The criterion 6 known issue about pipelines is answered; this review's F1 is a different defect on the same criterion.

## Checks

Executed by this reviewer on the integrated tree, 2026-09-25:

| Check | Result |
| --- | --- |
| `liveGateReads` probe, built runtime, 5 s alarm | 2 forms return in 1 ms; 4 forms killed (exit 142), F1 |
| `npm run release -- check-surfaces --local` | FAIL skeleton `0.41.0` before the retime; no FAIL after |
| `node scripts/authority-evidence.mjs --check` | stale at `WO-158/002`; verified after the `WO-158/003` re-mint |
| `node scripts/feedback-evidence.mjs --check` | stale at `WO-158/001`; verified after carrying `WO-159/feedback-001` into `WO-158/002` |
| `npm run evidence:artifact -- --check`; `npm run evidence:verification -- --check` | verified at `WO-158/001` |
| `npm run evidence:console -- --check` | selfhost, control, refutations and missing match |
| `node scripts/harness.mjs check`; `node scripts/harness-context.mjs --check` | 31 surfaces; every role with a ceiling within it |
| `npm run publication:check` | PASS; both editions CURRENT |
| `git diff --cached --check main` | clean |
| `npm run test:docs`, after this report and D026 to D029 | 21 passed, 0 failed, 23.62 s |
| `npm test -- --review` | not run: the review fails on F1 and the repair changes a registered source |

## Handoff

Result: `final-review-result fail`. Nothing is committed, pushed or published. The next legal step is the operator's `resume: fix`: repair F1 under D027's follow-up on the staged integrated tree, re-mint the editions the change makes stale, then a fresh `resume: verify` and a new final review, which runs the product gate and the publication steps.
