# WO-031 final review FINAL-001

**Subject:** branch `wo-031`, worktree `DotLn-wo031`, uncommitted working tree, 2026-09-05 local time (the control events are stamped 2026-09-06 UTC). `HEAD`, `main`, and `origin/main` are all `272a1502f0049043f63b0858c9d869e8ce441842`, the PR #36 merge of WO-009 (`git ls-remote origin main` agrees), which is also the published annotated tag `v0.10.0` (tag object `2cb12ddbd670e74639411cd77a98277d6b6110a6` peels to it). The branch carries no local commits (`git log main..wo-031` is empty), so the entire subject is the dirty tree. At review open, before the dispatch transition: 38 `git status --porcelain -uall` entries, 28 tracked modifications (against `HEAD`, 695 insertions / 88 deletions including `docs/control/current.md` and the generated `docs/work-orders/README.md`; 26 files, 642 / 66 without them) plus ten untracked paths totalling 2,326 lines: `docs/control/orders/WO-031.jsonl` (4 lines at open), `scripts/lib/control-actor.mjs` (27), `scripts/lib/control-usage.mjs` (77), `docs/verifications/WO-031/VER-001.md` (387), and `docs/evidence/WO-031/` (`README.md` 79, `ideation.md` 99, `control-at-activation.json` 27, `resume-fixtures.txt` 23, `usage-at-activation.json` 1,471, `usage-at-activation.txt` 132). The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree taken before the dispatch (`b8bf2f062a2de59ff1a986c59118b69054ab9fcd`) differs from checkpoint `refs/dotln/checkpoint/WO-031/4` (the verdict) only in `docs/control/current.md`, the strictly appended `docs/control/orders/WO-031.jsonl`, and the regenerated index (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, one bounded documentation correction (§Corrections applied by this review), and the lifecycle's own events.

**Authority:** `docs/work-orders/WO-031-actor-usage-projection.md`: `Model: any capable model`; `Effort: executor xhigh+; verifier xhigh+; reviewer any`; a patch classification whose omitted target the executor's dated activation completion assigns as `v0.10.1` above the published `v0.10.0` (`272a150`) under the operator's 2026-09-04 release default, with component versions unchanged; planner-synthesized nomination provenance from the 2026-09-05 planning pass with a locally preserved compaction-safety capture; dependencies WO-019, WO-028, and WO-030, all closed and released (`v0.3.4`, `v0.5.1`, `v0.7.0`); a recommended free-lane placement; the cited sections and helpers; an objective; a dated observed gap; a design section covering the projection, the label grammar and precedence, the ignored mapping, and the projections that show a label; deliverables; seven numbered acceptance criteria; an evidence gate; a write-back duty; a non-goal list; three operator-review assumptions; and the appended 2026-09-05 operator-authorized ideation breakout section. Against the activated text (`refs/dotln/checkpoint/WO-031/1`, byte-identical to `main`'s copy) the order differs by 23 insertions and 3 deletions: the shortened H1, the classification line, the activation-completion paragraph, and the breakout section; the objective, observed gap, design, deliverables, all seven criteria, the evidence gate, the write-back duty, the non-goals, and the assumptions are byte-identical to activation.

**Prior reports:** `VER-001` (pass; all seven criteria, the evidence gate, and every write-back reproduced independently: the executor's arithmetic re-derived from the raw segments, an independent scanner and text parser over the real log, the validator exercised directly with 19 invalid and 6 valid forms, a standalone instrumented-filesystem check observing 20 intercepted calls, `git check-ignore` against the real repository, the timing refactor checked for equivalence, the six intake captures read in main, a link and clean-room screen, and the full gate; six non-blocking observations O1–O6 and four disclosures D1–D4 with no repair required). Complete sequence: one report, no repair episode. The receipts under `docs/evidence/WO-031/` and the breakout section inside the work order are executor evidence and were treated here as VER-001 treated them: claims under test.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes) in Claude Code 2.1.261 with sandboxed Bash; macOS on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 in this worktree's own dependency tree. Reasoning effort `max`: the session's model control reported it at launch and the harness exposes it to the shell as `CLAUDE_EFFORT=max`, but `docs/discovery/environment.json` records `effectiveEffortReadback` for `claude-code` as `not found` with `harnessReadbackEligible: false`, so `harness-readback` is not admissible and the epistemic source is `self-reported`, exactly as VER-001's was. `2.1.261` was checked against `claude --version` on this machine and is the newest entry on the harness's observed `versions` list (observed 2026-09-04); `max` is on `sessionEffortSelector.values`; the discovery record is byte-identical to `main`. The order declares `reviewer any`, so any attested effort would satisfy it; `max` is stated because it is what the session reports. `DOTLN_ACCOUNT_LABEL` is unset in this shell and no `--account-label` is supplied, so the header below carries no label and the completion event will project `not-applicable`, a live exercise of criterion 4 by the reviewer as well as the verifier.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-031 passes.** The order asked for one read-only projection that answers, from the control log alone, which actors did this repository's work, for how long, and in which phases, plus one optional opaque attestation field that lets the operator tell two accounts of the same harness apart without publishing who they are. The deliverable does that. `resume usage [--json]` folds every segment through the existing multi-order reader, pairs each phase request with its completion through a generator shared with status timing, groups completed attempts by harness, harness version, model, effort, account label, and phase and again per work order, keeps retries and failures as separate attempts, counts a missing endpoint under `unknown` rather than guessing, and changes no byte, mtime, projection, or Beacon on the real repository. The four completion commands accept `--account-label` with a per-shell environment default, refuse every invalid, empty, or line-breaking value before any append, let the flag win over an invalid environment, store the label last and only when supplied, and project `not-applicable` when absent without inferring anything; the mapping directory is anchored in `.gitignore` and referenced by no production script. The refactored `controlTimeProjection` is JSON-identical to the `v0.10.0` implementation over all 26 real orders; the recorded activation report plus this order's own two completed attempts reproduce the live totals to the millisecond; kernel, compiler, and skeleton sources are byte-identical to the base; no dependency was added; the suite is green twice; whitespace is clean. The ideation breakout receipt's promoted surfaces were digested as subject: they trace to six operator captures in main's ignored intake, contain no raw wording, introduce no numeric rule, and are consistent with the settled decisions; the guidance they add is committed as its own change in the reviewed series, as the receipt requires. VER-001's six observations are one stale documentation clause (corrected here), two small code-quality candidates, one test-strengthening candidate, one wording nit in an executor receipt, and one by-design absence; this review adjudicates all six as non-blocking, records seven observations of its own, corrects one documentation projection, and changes no code, contract, acceptance, schema, authority, or captured-evidence byte.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment. Nearly every lifecycle instrument this review used is modified subject.

1. **The dispatcher and the fold are the deliverable and also dispatched this review.** `scripts/resume.mjs`, `scripts/lib/control.mjs`, `scripts/lib/control-time.mjs`, and the two new modules allocated this `FINAL-001` path, minted checkpoint 5, rendered every `status` reading above, and will compare this report's header and record the verdict. Compensating controls: the `final-review` and `final-review-result` cases, `appendTransition`, `checkpoint`, `selectionArgs`, `parseActor`, `completionActor`, and `requireReportActor` were read in full before the transition ran; subject identity was established with git objects (temporary-index trees and `git diff-tree`) rather than with the helpers' own projection; the appended `FinalReviewRequested` line was read raw and its fields, ordinal, and strictly increasing `recordedAt` checked by hand; and the timing projection was compared against the `v0.10.0` module extracted from the tag.
2. **The usage report cannot be its own oracle.** Its numbers were reproduced by a scanner written for this review that reads the raw JSONL bytes and imports no WO-031 code, and its text form was re-parsed by a parser that shares nothing with the renderer (§Final evidence summary).
3. **The executor's fixtures are executor-authored.** `scripts/test-resume.sh` grades the deliverable with the deliverable. Compensating controls: the validator was exercised directly with a case list assembled here; the precedence rules were driven through the real completion commands in a throwaway repository built for this review; the privacy canary was re-run with a positive control that counts intercepted calls.
4. **The index generator is modified subject and produced the index this review regenerated.** `scripts/work-orders.mjs` gained the `Latest attestation` line, and `npm run work-orders -- index` ran here at dispatch as the guide requires. Compensating control: the `Now` line, checklist row, and Active block for WO-031 were read against `status --json` and the raw segment; `scripts/test-work-orders.sh` grades synthetic fixtures and is not circular.
5. **The surface check, the publish helper, the notes parser, and the body profile are unchanged instruments.** `scripts/release.mjs`, `scripts/worktree.mjs`, `scripts/release-notes.mjs`, and `scripts/github-body.mjs` are byte-identical to `HEAD`; the publish flow (`check-surfaces --committed`, tracked-body read, body profile, notes parse, `gh` preflight, push, `pr create`) was read from source.
6. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile** by calling `parseReleaseNotes`, `githubBodyProfileFailures`, and `assertGitHubBodyProfile` directly and through `check-surfaces`. The body profile passed both on the first attempt; the notes parser refused the first draft's `account <label>` because it treats an angle bracket as raw HTML even inside inline code, so that phrase and the PR body's `--account-label <label>` became prose, after which both files pass and `check-surfaces` observes them. Because `v0.10.1` is above the latest published tag, these notes become the `v0.10.1` edition at release close.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-031 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest with `accountLabel: "not-applicable"`, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-06T00:34:09.094Z`, `elapsed` `{"implementation":2743960,"verification":664336}`, checkpoint `aab6b7c1aef40dc3eccfc91b8af05cb537d9f8ea` (`/4`), and `orders[]` with 26 entries; no projection-disagreement warning was emitted. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-031/FINAL-001.md` (the directory itself is created by the reviewer), moved the phase to `final-review`, appended `FinalReviewRequested` with `recordedAt` `2026-09-06T01:34:13.085Z` as line 5 of `docs/control/orders/WO-031.jsonl`, the order's own segment because its activation lives there, and minted checkpoint `1b346d69b947f3db01b5fad70fffc328d20dba5e` (`refs/dotln/checkpoint/WO-031/5`) with neither a checkpoint nor a Beacon warning. The legacy `docs/control/resume.jsonl` (158 events) is byte-identical to `HEAD`, and the WO-009, WO-021, and WO-029 segments (6 events each) still hash to the digests recorded in `control-at-activation.json`. The WO-031 segment is a strict byte-prefix append over checkpoint 4's copy, and its five events are `WorkOrderActivated` (`2026-09-05T23:34:59.085Z`, `effortDeclarationValidated: true`), `ImplementationReady` (`00:20:43.045Z`), `VerificationRequested` (`00:23:04.758Z`), `VerificationCompleted` (`00:34:09.094Z`), and this review's `FinalReviewRequested`; every event carries `schemaVersion: 1` and the stamps strictly increase. The projected `implementation` span (2,743,960 ms) and `verification` span (664,336 ms) are the arithmetic differences of the recorded stamps. The index was regenerated after the dispatch (`Now: WO-031 — final-review`).

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree after the allocation and index refresh as tree `3b612d751f9db8a32c50d7d671e092403b43d76f`, and `git diff-tree -r --name-status` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                                                         |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`6b8321f4…`)              | every deliverable path (38 paths: 28 modified, 10 added); the whole subject, the work order's own appended sections included, is downstream of activation                  |
| `/2` implementation-ready (`2a2d7cae…`)    | `docs/control/current.md`, `docs/control/orders/WO-031.jsonl`, `docs/work-orders/README.md`, `docs/verifications/WO-031/VER-001.md` (added); nothing else, so the breakout and its write-backs were already in the readiness tree, as the receipt states |
| `/3` verify allocation (`43e7d899…`)       | `docs/control/current.md`, `docs/control/orders/WO-031.jsonl`, `docs/work-orders/README.md`, `docs/verifications/WO-031/VER-001.md` (added)                               |
| `/4` verdict (`aab6b7c1…`)                 | `docs/control/current.md`, `docs/control/orders/WO-031.jsonl`, `docs/work-orders/README.md` (regenerated by the verifier at dispatch and by this review at dispatch)       |
| `/5` final-review allocation (`1b346d69…`) | `docs/control/current.md`, `docs/control/orders/WO-031.jsonl`, `docs/work-orders/README.md`                                                                               |

Every deliverable blob is therefore identical across the implementation-ready, verify-allocation, verdict, and final-review-allocation checkpoints and the working tree; the verifier changed nothing outside its report except the regenerated index (VER-001 D2); VER-001 has not changed since its verdict was recorded. All five checkpoints have `HEAD` as their sole parent. `refs/dotln` held 167 refs before the allocation and 168 after; the completion mints `/6`.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. `docs/intake/{chats,images,notes}` in this worktree each hold only `.gitkeep`, and `git status --porcelain --ignored` lists no ignored path outside `.control-beacons/`, `node_modules/`, the three `packages/*/dist/` outputs, and `.tsbuildinfo` files; there is no `.claude/settings.local.json` here. `.claude/settings.json` is tracked and byte-identical to `main`; `package.json`, `package-lock.json`, `docs/decisions/`, and `docs/discovery/environment.json` are byte-identical to `main`; `.gitignore` differs by the anchored `/docs/control/local/` line and its comment. One empty `.claude/.cc-writes` directory exists at the worktree root; git does not track empty directories, and the temporary-index `add -A` listed nothing under `.claude/`.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                         | Report header                                                                                                                                                                                                                                                                                                                                               | Declared minimum | Result                  |
| -------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ----------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.4","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`     | there is no executor report; the evidence README states the operator selected the model and effort and that this is not effective readback; `codex --version` reports `codex-cli 0.153.4` on this machine, `0.153.4` is on the `codex-cli` observed `versions` list (observed 2026-09-05 during WO-029), and the persisted `model_reasoning_effort` selector records `max` for that version, so `max` was admissible at append | `xhigh+`         | `max` exceeds the floor |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}` | exactly one column-one header in VER-001, parsed and compared with the event actor as JSON in this session: equal; neither carries `accountLabel`                                                                                                                                                                                                              | `xhigh+`         | `max` exceeds the floor |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`  | the header above                                                                                                                                                                                                                                                                                                                                            | `any`            | satisfied               |

There is no `RepairCompleted` event; the sequence has no repair episode. The executor and verifier were different harnesses, models, and sessions, and this reviewer is a third model in a third session. The projection reads `Effort drift: none` and the JSON form's `effortDrift` is `[{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.261; model claude-fable-5-1; effort max; source self-reported; account not-applicable`, the same drift line, phase `closed`, a `finalReview` elapsed value beside the two existing values, and legal actions `release-close, next, activate`.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree after the dispatch transition and the index refresh, before any review artifact or correction existed: `All matched files use Prettier code style!`, `GitHub body profile tests passed`, the shell suites (fixture temp-root, publication checker, backup-intake, resume including its three time-blind `cmp` lines, the four new `usage` lines, the two new `account` lines, the mapping-path `check-ignore` line, the seven segment-suite `PASS` lines, and the Beacon line; checkpoint; worktree; release; and work-orders), `PASS index coverage: 228/228 product headings indexed` with both audience editions `CURRENT` (30 and 45 linked sections), `PASS docs/work-orders/README.md is current`, `tsc -b --force`, `# tests 236 / # pass 236 / # fail 0 / # skipped 0`, the identity corpus `# tests 8 / # pass 8 / # fail 0`, and `Verified 4 WO-029 artifact evidence files`, exit 0. The resume-suite region of that output is byte-identical to the committed `docs/evidence/WO-031/resume-fixtures.txt` (23 lines). Second run, after this review's correction and the three package artifacts were in place, with the same result.

### Static, surface, and structural checks

- `git diff --check` clean against `HEAD`; `package.json` and `package-lock.json` byte-unchanged, so no dependency was added.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.10.1; expected v0.10.1 (work-order target v0.10.1; latest published v0.10.0)`; `PASS component-version` for `@dotln/compiler` `0.3.0`, `@dotln/kernel` `0.2.1`, and `@dotln/skeleton` `0.9.0`, each `src unchanged; expected no bump required`; `PASS github-body-profile`, first over no current bodies and then over this package's `PR.md` and `RELEASE-NOTES.md`.
- Script line counts: `scripts/*.mjs` and `scripts/lib/*.mjs` total 8,806 lines on `main` and 8,958 here (+152, of which the two new modules are 104); `scripts/test-resume.sh` gains 236 lines and loses 2; each of the four sibling suites gains its one `unset DOTLN_ACCOUNT_LABEL` line.
- Link sweep: 616 relative links and anchors across the 21 changed or new Markdown files resolve with GitHub-style slugs; the only dangling target was this report before it existed.
- Clean-room and secret screen over all 3,795 diff and new-file lines for the terms `CLAUDE.md` prohibits (commercial tracker or ALM product names, the predecessor system's name, managed-work-host, corporate-gateway, or employer-policy descriptions), internal hostnames, e-mail addresses, and credential shapes: the only matches are the receipts' and VER-001's own screening sentences, and the only URLs are the public GitHub Releases page and `https://gitmoji.dev/`. Every label in the committed evidence is `not-applicable`.
- `docs/lineage/idea-ledger.md` changes by 75 insertions and 0 deletions; no older entry is rewritten.

### Independent probes against the deliverable

1. **Raw-byte scanner.** A script written for this review reads the five segment files as text, parses 181 events across 26 orders, pairs phases with its own start map per order (reset on activation, `unknown` when either endpoint is missing), and groups completions by the six dimensions. Its `totals` `{"attempts":90,"elapsedMs":81344609,"unknown":68}`, its 20 `byActor` groups in insertion order, and its 26 `byWorkOrder` entries are `deepEqual` to `usage --json`. Its own text parser recovers 102 rendered groups equal to the JSON, the `Total:` line, and all 26 work-order rows from `usage`. Zero stored events carry `accountLabel`; 44 completions lack an actor and group as `unknown` with account `not-applicable`.
2. **Timing refactor equivalence.** `scripts/lib` at `v0.10.0` was extracted with `git archive` (its `control-time.mjs` hashes to `sha256 266e322b…`; the subject's to `sha256 85740791…`). For each of the 26 orders, `controlTimeProjection(eventsForOrder(control, id))` under the old and new modules is JSON-identical. `completedPhaseAttempts` over every order yields 90 attempts, 22 known and 68 unknown, summing to 81,344,609 ms, which equals the recorded activation report's 77,936,313 ms plus this order's implementation (2,743,960 ms) and verification (664,336 ms) attempts exactly.
3. **Read-only on the real repository.** SHA-256, size, and mtime of `docs/control/resume.jsonl`, all four order segments, `docs/control/current.md`, `docs/work-orders/README.md`, and the public Beacon file were identical before and after `usage`, `usage --json`, and the refused `usage --work-order WO-031`; `git status --porcelain -uall` stayed at 38 entries. `usage --json` captured before the final-review dispatch is byte-identical to the capture after it, since `FinalReviewRequested` is not a completion.
4. **Positive-control privacy canary.** With eleven `node:fs` functions and `child_process.spawnSync` replaced through `syncBuiltinESMExports()` and the subject's `main` imported directly, `usage --json` and `usage` produced 63,938 bytes while the wrapper intercepted 60 calls (`existsSync`, `lstatSync`, `realpathSync`, `readFileSync`, `readdirSync`), none under `docs/control/local`, with no subprocess attempt and no write-function call (each would have thrown). The instrumentation demonstrably reaches the code under test, which is the check VER-001 O4 asks the suite to make self-checking.
5. **Validator exercised directly.** `validateAccountLabel` refused 23 forms, including every case criterion 3 names (`Test`, `a b`, `x@y`, a 17-character label, U+2028, U+2029), embedded and trailing LF, CR, tab, NUL, the empty string, leading and trailing spaces, a leading digit, a leading hyphen, an underscore, a non-ASCII letter, and every non-string type; it accepted `a1`, `claude-2`, `a`, a 16-character label, `a-1-2`, `z9`, and `undefined`. `projectActor` adds `not-applicable` at read time and returns `null` for a missing actor; `renderAttestation` renders the `unknown (raw: …)` form with the label last.
6. **Precedence through the real commands.** In a throwaway repository (the subject's `resume.mjs` and `lib/`, the repository `.gitignore`, the Beacon fixture, a fixture mapping file, and an order declaring `any` for every role) driven through the actual dispatcher with the human tuple: an exported `Test` and an exported empty label each refuse `implementation-ready` with the grammar message and leave the segment at one event; an exported `x@y` with `--account-label a1` records `a1` as the last actor key; an exported `claude-2` with a header carrying `claude-2` records `claude-2`; a header without a label while the environment exports `a1` refuses `final-review-result` naming the expected header and appends nothing; with the variable unset the same header is accepted, the stored actor has no label field, `status --json` projects `not-applicable`, and `current.md` renders `account not-applicable`. `usage` over that repository groups `a1`, `claude-2`, and `not-applicable` across three attempts.
7. **Ignore rule and private lane.** `git check-ignore -v docs/control/local/account-labels.md` matches `.gitignore:17` in this repository; `docs/control/local/` does not exist; `grep` over every non-test script, JSON, and shell file finds no `control/local` reference.
8. **Breakout traceability.** The main control-plane checkout was resolved with `git worktree list --porcelain`. All six captures the receipt names exist in its ignored `docs/intake/notes/` (mode 644; 426, 219, 161, 189, 106, and 99 bytes), are ignored by `.gitignore:4`, and main's tracked tree is clean. The three founding references are in range (285, 421, and 1,048 lines). This review read all six captures in full and copied nothing: each of the receipt's five decisions and the continuation directive traces to a specific message, the synthesis neither adds authority nor drops a requirement, the rejected word-count example becomes no numeric rule, and none of the raw surface language appears on any committed surface.
9. **Gitmoji catalog.** `https://gitmoji.dev/` was fetched: `:chart_with_upwards_trend:` is present as "Add or update analytics or track code", the fit for a usage projection, and `:sparkles:` remains "Introduce new features".
10. **The grounding VER-001 cited reproduces.** Subject-line word counts of the most recent `main` merges run 99, 59, 45, 37, 24, 39, and 37, and the 34 gitmoji-prefixed subjects on `main` use five shortcodes, `:sparkles:` 16 times and `:memo:` 10 times.

### Reading

The complete diff and every new file; both new modules line by line; every hunk of the five test suites; VER-001; the evidence README, the receipt, and the recorded usage reports; the seven product, planning, playbook, README, ledger, and publication write-backs against the code they describe; the `final-review`, `final-review-result`, `usage`, and projection-refresh paths in `scripts/resume.mjs`; and the publish and surface-check flows in `scripts/worktree.mjs` and `scripts/release.mjs`.

## Acceptance criteria

1. **Fixture grouping, retries, unknown endpoints, text parity. Met.** The shipped fixture supplies three orders (one in the legacy segment, two in order segments), two harnesses, two models, two labels, a failed verification and its retry, an activation without `recordedAt` for the pre-migration endpoint, a negative span, a zero span, an unattested completion, and an incomplete final review, and asserts `{"attempts":10,"elapsedMs":11000,"unknown":1}`, six exact actor/phase groups with their work orders, all three per-order groups, and the re-parsed text. VER-001 recomputed those numbers by hand. This review's raw-byte scanner (probe 1) proves the same grouping, retry, unknown, and parity properties on the real 90-attempt log.
2. **Read-only. Met.** The fixture `cmp`s every segment and a stale `current.md` across both forms and three refusals with `spawnSync` trapped; probe 3 proves the property by digest, size, and mtime on the real repository, and probe 4 proves no write function and no subprocess is reached. Structurally, `usage` skips branch selection and the latest-closed lookup, is excluded from the projection and Beacon refresh, and `repoRoot` comes from the module path.
3. **Label validation and precedence. Met.** Probe 5 covers the five named refusals and eighteen more; probe 6 drives the environment default, the flag override, the invalid and empty environment, and the header comparison through the real commands, all refusing before append. `parseActor`'s former arity check (`args.length !== allowedFlags.size * 2`) is replaced by a required-flag check; the parse loop still refuses unknown flags, non-string, empty, or `--`-prefixed values, and repeated flags, and a trailing odd argument fails as an unknown flag, so no gap opens.
4. **Historical absence, no inference. Met.** Zero of 181 stored events carry the field; `projectActor` defaults at read time; `dimensions()` in the usage module falls back independently; nothing in the three modules reads `harness`, `model`, or any other field to decide a label. VER-001's completion and this review's completion both exercise the absent case live.
5. **Ignored mapping, untouched private lane, real-log echo. Met.** Probe 7 for the ignore rule and references; probe 4 for the lane; probe 2 for the echo: the activation snapshot plus exactly this order's two completed attempts equals the live report to the millisecond, and four of the five recorded segment digests still match, the fifth being WO-031's own advancing segment.
6. **Write-backs. Met.** Every surface criterion 6 names landed and was read against the code: 07's flag, environment default, precedence, grammar, `not-applicable`, header position, and disclosure; 02's `accountLabel?` and one sentence; 09's candidate rewritten as adopted plus the re-identification paragraph; the playbook's per-terminal setup; the docs README's ignored directory and private-mapping section; the ledger's dated implementation section. Beyond the duty: 03's projection semantics, 06's efficiency caveat and activation completion, the map, the root README release block, the publication index row and both edition locks, and the generated index's `Latest attestation` line. Each behavioral claim tested here holds, including that an explicit flag wins over an invalid environment value.
7. **Gate green, diff clean, no dependency. Met.** §Final evidence summary.

## Evidence gate

- **Fixture transcripts (criteria 1–5).** `docs/evidence/WO-031/resume-fixtures.txt` reproduces byte-identically from this review's own first `npm test` run.
- **Real-log `usage` output.** Reproduced and cross-checked by probes 1 through 3.
- **`npm test`.** Exit 0, twice.

## Write-back duty

Discharged as listed under criterion 6. The verification README's actor key order, which criterion 6 does not list and which VER-001 O1 found stale, is corrected by this review (§Corrections applied by this review).

## Ideation breakout receipt

The receipt at `docs/evidence/WO-031/ideation.md` requires the reviewer to check traceability and internal links, confirm the intake handling, confirm coherent commit grouping at final review, and assess the prose for both unnecessary detail and missing substance.

**Traceability and intake handling.** Probe 8. The two captures the receipt says were staged in this worktree and removed after byte comparison are indeed absent here (the three intake directories hold only `.gitkeep`), and all six survive in main.

**Links.** The link sweep covers every changed Markdown file including the receipt, the ledger sections, the WO-011 incident paragraph, the new 08 section, and the publication index row; all resolve.

**Coherent commit grouping.** The receipt and the work order require the publication guidance to be committed separately from the actor-usage implementation, and the operator's own message asks for separate commits. This review commits the reviewed state as three commits (§Commit series): the breakout guidance as documentation on its own, the implementation with its tests, write-backs, and executor evidence, and the independent-review evidence with the closed control state. Six files carry hunks of both the breakout and the implementation (`07-execution-guide.md`, `PLAYBOOK.md`, `docs/README.md`, `02-domain-model.md`, the ledger, and the work order itself); their breakout-only intermediate blobs were built from the working-tree content by removing exactly the implementation insertions, each located by a unique anchor, and both halves of every split were read before staging.

**Prose assessment.** Proportionate. `08-publication-compiler.md` §PRs and commits owns the editorial guidance in four paragraphs; 07 and the playbook link it and add only their role-specific sentences, which is the right split. The guidance states what to do and what not to do without the quota the operator rejected, dates the supersession of the narrow gitmoji mapping, and scopes itself to new work so historical artifacts are not retroactively wrong. The continuation default is stated once per surface and the canonical-intake rule once per surface. Nothing reads as padding; nothing the operator asked for is missing. The WO-011 paragraph adds an incident to an existing unit rather than a new unit, as the receipt says.

**Clean-room and settled decisions.** The receipt records Clean Room with Shape-First Synthesis and public vocabulary, no direct-filing exception; the committed text is a rewrite, not a copy. No ADR or ledger resolution is reopened; the anti-oscillation material is framed as a manual application pending WO-011, not compiled enforcement.

## Verification-sequence soundness

One report, one verdict, no repair. VER-001's single column-one header equals its `VerificationCompleted` actor by JSON comparison; its verdict paragraph, criteria, evidence gate, observations, and disclosures are internally consistent and were reproduced or exceeded here. The verifier's report was written before its event, and the completion command compared the header before appending, as the mechanism requires. The executor ran on Codex CLI and the verifier on Claude Code in separate sessions, so verification was independent in both session and actor.

## Adjudications

### 1. `docs/verifications/README.md` documents the pre-WO-031 key order (VER-001 O1)

Sustained and corrected here. The completion command compares headers by exact string equality, so a labelled verifier following the README's old clause would write a header the command refuses. One clause now names `source`, then the optional `accountLabel` present only when the completion supplied one. This is documentation of shipped behavior, not a behavior change (§Corrections applied by this review).

### 2. Duplicated effort rendering (VER-001 O2)

Sustained as a candidate, not a finding. `renderEffort` in `scripts/resume.mjs` and the inline expression in `renderAttestation` compute the same `unknown (raw: …)` form and agree today; removing one is a code change outside this review's lane.

### 3. Fold-level label validation reports no ordinal (VER-001 O3)

Sustained as a candidate. `scanControl` calls `validateAccountLabel` without the `at line N` suffix its sibling `validateRecordedAt` carries, so a hand-edited invalid label refuses every `resume` command with the grammar but not the location. Failing closed is correct; the ordinal is a diagnosability improvement reachable only by editing a segment by hand.

### 4. The suite's privacy canary has no positive control (VER-001 O4)

Sustained as a test candidate. Probe 4 is this review's positive control and shows the instrumentation reaches the code today (60 intercepted calls); the suite would be self-checking with one assertion that the wrapper ran. Adding it is a test change and stays out of this review.

### 5. Two capture counts in one receipt (VER-001 O5)

Not sustained as a defect. "All six files were verified in main" and "the initial five captures are ignored in main" describe different subsets (the sixth is the later continuation note) and both are true; the receipt is executor evidence and is left as written.

### 6. `docs/control/local/` does not exist (VER-001 O6)

Not a defect. The design requires that no script create or read it; the three documentation surfaces tell the operator how to create it by hand.

### 7. The generated index gains a `Latest attestation` line for every order with a recorded actor (this review)

Not a finding. Criterion 6 asks that the index show a present label beside the attestation; the index had no attestation line before, so the executor added one through the shared renderer for every order whose control state records an actor. That is a visible change for eleven closed orders beyond WO-031 and is the proportionate way to satisfy the criterion; the release notes say so. The `[1m]` in `claude-opus-5[1m]` renders as `&#91;1m&#93;` through the index's pre-existing cell escaper, which is unchanged.

### 8. An empty or invalid exported `DOTLN_ACCOUNT_LABEL` blocks every completion in that shell (this review)

Not a finding. The playbook says an empty value is invalid and `unset` returns to omission; the refusal names the grammar and appends nothing. The cost is that the mistake surfaces at completion time rather than at export time; recorded in the notes and the PR.

### 9. The index links this report before it exists (this review)

Not a finding, as in the WO-030 review: the link is derived from the `FinalReviewRequested` event and resolves once the reviewer writes the file, which this review has now done.

### 10. `usage` sorts work orders lexicographically (this review)

Not a finding. Every id is three-digit, so lexical order is numeric order; `byActor` insertion order follows from that sort and is deterministic, which the scanner reproduced exactly.

### 11. `controlTimeProjection` now uses `Array.prototype.findLastIndex` (this review)

Not a finding. The pinned toolchain runs node v22.2.0 and the method has been available since node 18; the suite exercises it.

### 12. The attribution rule (this review)

The reviewed commits carry plain subjects and no attribution trailer, and the PR body carries no session link, per the operator's standing rule (`~/.claude/CLAUDE.md`, "Do not add AI attribution to commits or pull requests") and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins, as the WO-017, WO-018, WO-020, WO-023, WO-028, and WO-030 reviews recorded.

### 13. The verifier's disclosures (VER-001 D1–D4)

All four accepted. D1 (the activation snapshot's byte-for-byte claim is not re-runnable): probe 2's millisecond reconciliation and the four matching digests are the substitute and were reproduced here. D2 (the verifier regenerated the index): confirmed by the checkpoint table. D3 (labels unexercised under real concurrent accounts): still true; probe 6 exercises the mechanism through the real commands but not under two live provider accounts. D4 (wall-clock semantics documented, not validated): the disclaimer is present and consistent in the command output, 03, 06, the playbook, the evidence README, and now the notes.

### Candidates considered and not sustained

- **The removed arity check in `parseActor` opens a gap.** Not sustained: the parse loop refuses every malformed shape (criterion 3 above), and the fixture asserts eleven refused forms plus missing and duplicate flags with unchanged bytes.
- **`completedPhaseAttempts` keys its start maps by `event.workOrderId` even for an event without one.** Not sustained: the fold validates events before any projection, every caller passes `eventsForOrder`, and an `undefined` key can only hold starts nothing reads.
- **The executor's `DOTLN_ACCOUNT_LABEL=a1 npm test` run could have contaminated fixture evidence.** Not sustained: the five suites `unset` the variable before any fixture runs, the resume fixture sets and deletes its own value, and this review's two runs used an unset variable with identical results.
- **`status --json` now always carries `accountLabel` inside `latestAttestation`.** Not sustained as a compatibility break: it is an additive key whose absence in the stored event is preserved, the existing suite's expected status objects were updated in place, and the notes name it under read-before-upgrading.

## Corrections applied by this review

One correction, in the documentation lane `docs/final-reviews/README.md` grants, applied by exact string replacement with a script that refused unless the old string occurred exactly once, proven contained by `git diff` against `HEAD` (3 insertions and 2 deletions in `docs/verifications/README.md`), and followed by the checks it could affect: the pinned `prettier --check` on the file, the full `npm test`, and `git diff --check`, all clean. It is committed with the implementation because it documents the header order that implementation introduced.

1. **`docs/verifications/README.md`:** the canonical actor serialization now reads `harness`, `harnessVersion`, `model`, `effort`, optional `raw`, `source`, then the optional `accountLabel` that WO-031 added, present only when the completion supplied one (adjudication 1).

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet, no guard was lost, and no behavioral or evidence defect was found in the deliverable that any criterion covers. The items below are recorded and not scored.

1. **Fold-level label validation reports no ordinal** (adjudication 3): patch candidate.
2. **Effort rendering is computed in two places** (adjudication 2): patch candidate.
3. **The suite's privacy canary has no in-suite positive control** (adjudication 4): test candidate.
4. **An empty or invalid exported label blocks completions until unset** (adjudication 8): documented behavior.
5. **The two-terminal workflow is unexercised under two live accounts** (VER-001 D3).
6. **The index's new attestation line changes eleven closed orders' entries** (adjudication 7): intended.
7. **`resume.mjs` invoked through a symlinked path is a silent no-op** (WO-030 FINAL-001 adjudication 8): pre-existing, outside this order.
8. **`CLAUDE_EFFORT` remains an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9), so Claude Code attestations stay `self-reported`.
9. **The executor's model and effort live in the control log and the evidence README, not in a committed executor artifact**, as every reviewed code order since WO-013 has recorded.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step and the sandbox.** `gh` cannot read its configuration under this sandbox (the `~/.config/gh` path is denied), the same limit every reviewed order since WO-006 has met, while read-only `git ls-remote` to `origin` succeeds. The reviewed series is committed here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation, and notes parse run against the exact commit; if it refuses at the `gh` preflight it pushes nothing and the operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute command the publisher prints: change directory into the main checkout and run this worktree's `scripts/release.mjs close WO-031 --publish` with node, outside the sandbox and where `gh` is authenticated. Because `v0.10.1` is strictly above `v0.10.0`, the reviewed subject helpers finish this worktree through their own classifier, the closed-state gates in the subject and in `origin/main`, fast-forward main, run `npm ci` and the evidence gate, assemble the five-section edition from these notes, create and push the annotated `v0.10.1` tag, and create the matching GitHub Release. If Release creation fails after the tag push, the rerun is `npm run release -- close WO-031 --publish` from updated main.
- **Attribution:** adjudication 12.
- **Ledger duty:** discharged by the executor's three dated sections; this review adds no ledger entry because every open item above is either already in VER-001 or is a candidate this report names for a patch or the next planning pass.
- **Intermediate commits are not individually gated.** The two edition locks and the generated index are correct for the series' final tree; the first commit's tree carries the pre-existing lock bytes over changed product sections, and only the final tree is what `npm test`, `check-surfaces --committed`, and the publisher judge.

## Proposed PR

- **Title:** `:chart_with_upwards_trend: WO-031: Read-only actor usage projection and opt-in opaque account labels; v0.10.1`. The shortcode is the catalog's "Add or update analytics or track code" entry (probe 9), chosen because the order's main purpose is a usage projection rather than a generic feature; the title names the change and its version and leaves the inventory and validation to the body, per 08 §PRs and commits.
- **Body:** `docs/final-reviews/WO-031/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Commit series

Three commits, each with a plain subject and an explanatory body, in this order, followed by a check that `git diff HEAD` is empty and that the final tree equals the post-result working tree:

1. **Breakout guidance (documentation only):** `docs/product/08-publication-compiler.md`; the breakout hunks of `docs/product/07-execution-guide.md`, `docs/PLAYBOOK.md`, `docs/README.md`, and `docs/product/02-domain-model.md`; `docs/work-orders/WO-011-feedback-compiler.md`; the shortened heading and breakout section of `docs/work-orders/WO-031-actor-usage-projection.md`; the ledger's continuation and publication-discipline sections; the publication index row for the new 08 section; and `docs/evidence/WO-031/ideation.md`.
2. **Implementation:** `.gitignore`; the two new modules; `scripts/lib/control-time.mjs`, `scripts/lib/control.mjs`, `scripts/resume.mjs`, `scripts/work-orders.mjs`; the five test suites; `docs/product/03-architecture.md`, `06-roadmap.md`, `09-audit-resilience-privacy.md`; the implementation hunks of 07, the playbook, the docs README, and 02; the ledger's implementation section; the work order's classification line and activation completion; the root README release block; the planning map; both edition locks; the executor evidence (`README.md`, `control-at-activation.json`, `resume-fixtures.txt`, `usage-at-activation.json`, `usage-at-activation.txt`); and this review's `docs/verifications/README.md` correction.
3. **Independent review evidence and closed control state:** `docs/verifications/WO-031/VER-001.md`, this report, `PR.md`, `RELEASE-NOTES.md`, `docs/control/orders/WO-031.jsonl`, `docs/control/current.md`, and the regenerated `docs/work-orders/README.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commits (the committed surface check requires the closed control state, and it makes the index's check mark true); the index is regenerated after the result and checked; the reviewed state is committed as the series above. The publisher is then run from this worktree:

```bash
npm run worktree -- publish WO-031 --title ':chart_with_upwards_trend: WO-031: Read-only actor usage projection and opt-in opaque account labels; v0.10.1' --body-file docs/final-reviews/WO-031/PR.md
```

The helper runs `check-surfaces --committed WO-031` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-031`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff in the absolute subject-helper form. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, the operator runs that printed command with the main checkout as the working directory; the expected outcome is the annotated `v0.10.1` tag and its GitHub Release.

## Method

Everything above was re-established in this session inside the Claude Code sandbox: `npm test` twice, captured to files without a pipe; the read-only status projection and the transition; temporary-index trees and `git diff-tree -r` against all five checkpoints; the segment prefix comparison against checkpoint 4 and the legacy log against `HEAD`; the tag extraction and timing comparison, the raw-byte scanner and text parser, the digest, size, and mtime proof, the positive-control canary, the validator exercise, the throwaway-repository precedence probe, the link sweep, and the clean-room screen as scripts written for this review in the session temporary directory; `stat`, `shasum`, and `git check-ignore` on the six intake captures in the main checkout, read for traceability and not copied; a fetch of the public gitmoji catalog; and a full reading of the diff, the receipts, VER-001, and the gates' source. The one correction was applied with exact string replacement, checked with the pinned formatter, and diffed against `HEAD`. No other file outside `docs/final-reviews/WO-031/` was changed by this review; the index regeneration and the control projection are the lifecycle's own effects.
