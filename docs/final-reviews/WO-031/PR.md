## What this merges

WO-031, the `v0.10.1` patch above `v0.10.0`: a read-only actor usage projection and opt-in opaque account labels. `npm run resume --silent -- usage [--json]` folds every control segment through the existing multi-order reader, pairs each phase request with its completion, and reports every completed attempt grouped by harness, harness version, model, effort, account label, and phase, then per work order, with `attempts`, `elapsedMs` as the sum of known signed spans, `unknown` for attempts missing either timestamp, and each group's work orders. Retries and failures stay separate attempts; every span is wall-clock time that includes waiting and can overlap across concurrent orders, and the output says so. The four completion commands accept `--account-label` with `DOTLN_ACCOUNT_LABEL` as a per-shell default and the flag taking precedence; labels match `^[a-z][a-z0-9-]{0,15}$`, refuse before any append otherwise, are stored last in the actor JSON only when supplied, and project as `not-applicable` when absent, never inferred. The meaning of a label belongs in the newly ignored `docs/control/local/`, which no script reads. The shared phase-pairing generator that serves both status timing and usage keeps `status --json` timing JSON-identical to `v0.10.0` over all 26 real orders. Kernel, compiler, and skeleton sources are byte-identical to `v0.10.0`; no dependency was added. The operator-authorized ideation breakout is included as documentation only: PR and commit editorial guidance, relevance-first gitmoji selection, proportionate correction, resolving the main checkout for original intake, and the default that ideation continues to ready to verify.

- **Fixture grouping, retries, unknown endpoints, text parity (criterion 1):** the shipped three-order fixture asserts `{"attempts":10,"elapsedMs":11000,"unknown":1}`, six exact actor/phase groups with their work orders, all three per-order groups, and the text form re-parsed independently; the verifier recomputed every number by hand, and this review's raw-byte scanner, which imports no WO-031 code, reproduces the live report's totals, all 20 actor groups in insertion order, and all 26 work-order entries, with its own text parser matching 102 rendered groups.
- **Read-only (criterion 2):** the fixture `cmp`s every segment and a deliberately stale `current.md` across both output forms and three refused argument shapes with `spawnSync` trapped; on the real repository, `usage`, `usage --json`, and the refused `--work-order` form leave every segment, `current.md`, the generated index, and the public Beacon identical by SHA-256, size, and mtime, and `usage --json` before and after the final-review dispatch is byte-identical.
- **Label validation and precedence (criterion 3):** the validator refuses `Test`, `a b`, `x@y`, a 17-character label, U+2028 and U+2029, and 17 further forms in this review's direct exercise, and accepts `a1`, `claude-2`, and five further forms; a throwaway repository driven through the real completion commands refuses an invalid and an empty environment default before append, records `a1` from the flag over an invalid environment, records `claude-2` from the environment, refuses a report header lacking the label the environment supplies, and stores nothing when neither is set.
- **Historical absence, no inference (criterion 4):** zero of the 181 stored events carry `accountLabel`; the 44 completions without an attestation group as `unknown` with account `not-applicable`; `projectActor` defaults at read time and writes nothing back.
- **Ignored mapping and real-log echo (criterion 5):** `git check-ignore -v docs/control/local/account-labels.md` matches `.gitignore:17`; no production script references `control/local`; a positive-control canary intercepted 60 filesystem calls during both output forms with none under the private lane, no subprocess, and no write; the recorded activation report's 88 attempts and 77,936,313 ms plus this order's own implementation and verification attempts (2,743,960 and 664,336 ms) equal the live 90 attempts and 81,344,609 ms to the millisecond.
- **Write-backs (criterion 6):** the execution guide's flag, environment default, precedence, grammar, `not-applicable`, header position, and public-profile disclosure; the domain model's `accountLabel?`; the audit document's candidate rewritten as adopted with the two-accounts disclosure; the playbook's per-terminal setup; the docs README's ignored directory; the ledger's dated section; plus the architecture, roadmap, map, root README, publication index, edition locks, and generated index.
- **Gate, whitespace, dependencies (criterion 7):** `npm test` exit 0 twice in this review; `git diff --check` clean; `package.json` and the lockfile byte-unchanged.
- **Ideation breakout:** the six operator captures exist in main's ignored intake and were read for this review; the receipt's five decisions and the continuation directive trace to them faithfully in both directions with no raw wording on any committed surface; every relative link and anchor across the 21 changed Markdown files resolves; the new guidance is committed as its own change in the reviewed series.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in this worktree on the verified tree and again on the reviewed tree: Prettier clean, the body-profile unit suite, every shell suite including the resume suite's new usage, privacy, label, and projection lines and the seven segment `PASS` lines, `check-publication.mjs` at 228 of 228 headings with both audience locks current, `index --check` current, `tsc -b --force`, 236 of 236 package tests, 8 of 8 identity-corpus tests, the four frozen artifact checks, exit 0. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.10.1; expected v0.10.1`, `PASS component-version` with `src unchanged` for all three components, and the body-profile rule over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity: a temporary-index tree of the working tree differs from the final-review allocation checkpoint only in the two control projections and the regenerated index; from the verdict checkpoint the same; from the implementation-ready and verify-allocation checkpoints additionally in VER-001; every deliverable blob is byte-identical from the implementation-ready checkpoint to the reviewed tree; all checkpoints have `HEAD` (`272a150`, also `main` and `origin/main`, the published `v0.10.0`) as their sole parent; `git log main..wo-031` was empty before the reviewed series; the work order's criteria are byte-identical to activation; the verifier's report header equals its control-log actor by JSON comparison; the executor's `max` and the verifier's `max` meet the declared `xhigh+` floors.
- Direct probes: the `v0.10.0` `control-time.mjs` extracted from the tag produces JSON-identical `controlTimeProjection` output for all 26 orders; an independent raw-segment scanner and text parser agree with `usage --json` and `usage` to the number; digest, size, and mtime proofs of read-only behaviour on the real repository; a positive-control filesystem and subprocess canary; a direct exercise of the validator; a throwaway-repository precedence probe through the real dispatcher; 616 relative links and anchors across 21 changed Markdown files resolve; the clean-room and secret screen over all 3,795 subject lines found only the receipts' own screening sentences and two public URLs; `codex --version` reports the executor's recorded `0.153.4`; the gitmoji catalog confirms `:chart_with_upwards_trend:` as "Add or update analytics or track code".

## Verification trail

- `docs/verifications/WO-031/VER-001.md`: pass; all seven criteria, the evidence gate, and every write-back reproduced independently by re-deriving the executor's arithmetic from the raw segments, exercising the validator directly, a standalone instrumented-filesystem check, `git check-ignore` on the real repository, and the full gate; six non-blocking observations and four disclosures with no repair required.
- `docs/final-reviews/WO-031/FINAL-001.md`: pass; the verifier's observations and disclosures adjudicated with reasons, the reviewer's own observations recorded, the breakout receipt traced to its captures, the coherent commit series confirmed, one bounded documentation correction applied and re-checked, method and instruments disclosed.

## Known open items

- **An invalid hand-edited `accountLabel` refuses the whole control fold without naming its line** (VER-001 O3): failing closed is the right direction; the missing ordinal is a diagnosability gap reachable only by hand-editing a segment. Patch candidate.
- **Effort rendering is computed in two places** (VER-001 O2): `renderEffort` in `scripts/resume.mjs` and the inline expression in `scripts/lib/control-actor.mjs` agree today; a later change to one would silently diverge the drift line from the attestation line. Patch candidate.
- **The suite's privacy canary has no in-suite positive control** (VER-001 O4): the verifier's standalone run and this review's run (60 intercepted calls, none in the private lane) prove the instrumentation reaches the code today; a one-line assertion that the wrapper was invoked would make the fixture self-checking. Test candidate.
- **The two-terminal label workflow is unexercised under two live provider accounts** (VER-001 D3): correct as specified in fixtures and a throwaway repository; ergonomics untested in anger.
- **An empty or invalid exported `DOTLN_ACCOUNT_LABEL` blocks every completion in that shell** until corrected or unset, by design and as documented; noted because the refusal arrives at completion time rather than at export time.
- **`docs/control/local/` is not created by any tool** (VER-001 O6): by design; the operator creates it by hand when a mapping is wanted.
- **`resume.mjs` invoked through a symlinked path exits silently without acting**: pre-existing, recorded in the WO-030 review, outside this order.
- **`CLAUDE_EFFORT` remains an unrecorded readback candidate**, so Claude Code attestations stay `self-reported`.
- **Publish and release close.** The bounded publisher `npm run worktree -- publish WO-031` runs the committed surface check and body validation before any push and then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses at that preflight the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.10.1` is strictly above `v0.10.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.10.1` tag, and creates the matching GitHub Release.
