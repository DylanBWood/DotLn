## What this merges

WO-018, the `v0.4.1` control-plane consolidation: the four lifecycle scripts share one helper library, canonical control state is machine-readable and no script parses the Markdown projection, the release manifest's cadence grammar comes from the built kernel's type-exhaustive exports, closeout can no longer destroy ignored intake under a build-shaped directory name, the operator's root harness settings are permitted in main without becoming disposable anywhere, and the root test chain runs the real-repository publication check and the identity corpus it previously only claimed. `@dotln/kernel` advances to `0.2.1` for the additive `CADENCE_KINDS` export; `@dotln/skeleton` and `@dotln/compiler` sources are unchanged. The order's 2026-09-03 scope clarification, its 2026-09-03 harness-version amendment (criteria 9 and 10), and the documentation promoted from the 2026-09-04 Additional Opinion ideation breakout are included.

- **Shared helpers (criterion 1):** `scripts/lib/git.mjs` holds `runGit`, `failureOf`, `runGitPathList`, `ensureClean`, `parseWorktrees`, `mainWorktree`, and `removeMergedBranch`; `scripts/lib/paths.mjs` holds `containedRegularFile`, `parseJson`, `readJsonFile` (names the file on read and parse failure), `workOrderAuthorityPath`, and `classifyIgnoredMaterial`. All four lifecycle scripts import them and a grep proves one definition each. The clean-tree gate reconciled to the stricter `--porcelain --untracked-files=all`; the two allowlists reconciled to one classifier returning two policy answers, `disposable` for destructive cleanup and `releaseEvidenceAllowed` for the main-checkout release guard.
- **Classifier and data-loss fix (criterion 2, clarification):** nothing under `docs/intake/` is ever disposable; build outputs match only at the anchored root or `packages/NAME/` paths; the exact root `.claude/settings.local.json` is release-evidence-allowed but never disposable, and `.gitignore` gains exactly that anchored path. Fixtures plant `docs/intake/dist/x.md` and `docs/intake/node_modules/y` and a subject settings copy, prove `finish` refuses and every file survives, and prove a no-release close succeeds while main's settings file and intake both survive.
- **Machine-readable control state (criteria 3 and 4):** `resume status --json` emits the same twelve fields as `current.md`; both status forms are read-only and warn on stderr when the projection disagrees with the fold; `worktree finish` folds the subject's committed `HEAD:docs/control/resume.jsonl` and `release close` spawns the JSON status, with zero references to `current.md` in either; `fold` refuses an unknown event type with its one-based ordinal.
- **Manifest from the built kernel (criterion 5):** `builtKernelCadence` imports `packages/kernel/dist/src/index.js` in a child process and requires `CADENCE_KINDS`, `EVALUABLE_CADENCE_KINDS`, and the runtime `Cadence` constructors to agree bidirectionally with their discriminants before deriving `deferred` by difference; missing and inconsistent exports refuse with distinguishable messages before any tag object exists; the control-log schema range comes from the exported `CONTROL_LOG_SCHEMA_VERSION`; the regex parsers over the cadence union, the evaluator switch, and the schema literal, with their fixtures, are deleted (the pre-existing event-envelope regex over `types.ts` lies outside the order's scope).
- **The test chain exercises what it claims (criteria 6 to 8):** `npm test` runs `check-publication.mjs` against the real repository and the WO-101 identity corpus, the Program lane stays frozen outside the chain, `test-publication.sh` asserts against an independent `shasum -a 256`, the release suite's trace assertions carry `test -s` guards, and top-level `scripts/*.mjs` falls from 4,387 to 4,378 lines.
- **Harness-version attestation slot (criteria 9 and 10):** the discovery record carries an observed `versions` list per harness with the newest last; the matcher accepts any listed version; refusals name the versions on record; the `ultracode` session label is recorded as an operator-attested note meaning `xhigh` reasoning effort, the unrecognized-label refusal points at it, and `resume` never converts a label itself.
- **Reviewed helpers govern the first close:** `worktree finish` gates on the working directory being main rather than on the script residing there, so the still-present subject worktree's `release.mjs` and `worktree.mjs` perform the first post-merge close; `worktree publish` and `resume release-close` print that exact absolute command, and every equal-tag rerun reproduces evidence before re-deriving compatibility.
- **Write-backs:** the execution guide, playbook, releases README, corpus README, harness-security runbook, kernel and skeleton READMEs, README release block at `v0.4.1`, the ledger's adopted control-state-is-data entry and preserved `program`-field candidate, and the work-order map.
- **Ideation breakout (documentation only):** the candidate Additional Opinion support and two-result Second Opinion preset at status `vision` across the pattern library, domain model, architecture, interfaces, roadmap, and audit documents, with the ledger section, the map's unallocated candidate, and refreshed publication locks. The receipt in the work order names the authority, source treatment, promoted surfaces, settled boundaries, and unresolved choices.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in this worktree on the verified tree and again on the reviewed tree: Prettier clean, the body-profile unit suite, all eight shell suites, `check-publication.mjs` at 169 of 169 headings with both audience locks current, `tsc -b --force`, 154 of 154 package tests, 8 of 8 identity-corpus tests, exit 0. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.4.1; expected v0.4.1`, `PASS component-version @dotln/kernel: src changed; observed 0.2.1; previous v0.4.0 0.2.0`, `PASS component-version @dotln/skeleton: src unchanged; observed 0.4.0`, `PASS component-version @dotln/compiler: src unchanged; observed 0.1.0`, and the body-profile rule PASS over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity at review open: a temporary-index tree of the working tree differed from the verdict checkpoint only in the two control files, and from the implementation-ready checkpoint only in those plus VER-001; all five checkpoints have `HEAD` (`162cd12`, also `main` and `origin/main`) as their sole parent; `git log main..wo-018` is empty; the verifier's report header equals its control-log actor by object comparison; the executor's `xhigh` and the verifier's `xhigh` meet the declared `xhigh+` floors.
- Ten independent review lanes, one per criterion group, receipt duty, write-back duty, process duty, and adversarial code read, each finding refuted by three lenses before it was adjudicated; a fourteen-mutation re-drill in an isolated copy outside the repository; a thirty-path classifier probe; the symlinked-invocation reproduction behind VER-001's F1; the helper-duplication and projection-parse greps; the clean-room and secret scan over the complete diff.

## Verification trail

- `docs/verifications/WO-018/VER-001.md`: pass; all ten criteria, the five clarification items, and the evidence gate reproduced independently, an eighteen-mutation drill, four advisory findings and three notes with a bounded checklist.
- `docs/final-reviews/WO-018/FINAL-001.md`: pass; the verifier's findings adjudicated with reasons, the `finish` gate relocation and the equal-tag evidence rerun blessed explicitly, bounded documentation corrections applied and re-checked, method and instruments disclosed.

## Known open items

- **Symlinked absolute invocation of `resume.mjs` is a silent no-op** (VER-001 F1): the module-entry guard compares a lexically resolved `argv[1]` with the realpath-resolved module URL. No sanctioned invocation is affected; the one-line `realpathSync` repair is a follow-on order's, not a reviewer's.
- **The `ultracode` note's `classification` is outside the discovery record's declared `labels` vocabulary** (VER-001 F2): add `operator-attested` to that list in a follow-on change; restamping the note would silence the refusal hint that criterion 10 requires.
- **No destructive-cleanup fixture pins the intake precedence term alone** (VER-001 N3): the release-evidence fixture and direct probes pin it; a `docs/intake/**/.DS_Store` fixture is a candidate for the next suite change.
- **Publish and release close.** `gh` in this sandbox fails before its root command exists, so `npm run worktree -- publish` refuses at its `gh` preflight after the committed surface check and body validation and before any push. The operator runs the publish command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.4.1` is strictly above `v0.4.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.4.1` tag, and creates the matching GitHub Release.
