# WO-018 final review FINAL-001

**Subject:** branch `wo-018`, worktree `DotLn-wo018`, uncommitted working tree, 2026-09-04. `HEAD`, `main`, and `origin/main` are all `162cd12fef79f6038502f9b93a7b0a4c7a3a2af9`, the PR #26 planning-pass merge above the published annotated tag `v0.4.0`; the branch carries no local commits (`git log main..wo-018` is empty), so the entire subject is the dirty tree. At review open, after this review's own transition: 42 tracked modifications (1,723 insertions / 630 deletions against `HEAD` including the two `docs/control` files; 40 files, 1,709 / 621 without them) plus three untracked paths, `scripts/lib/git.mjs` (80 lines), `scripts/lib/paths.mjs` (70 lines), and `docs/verifications/WO-018/VER-001.md` (449 lines); 45 `git status --porcelain -uall` entries. The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree (`eaba2e7c533967ce9c7223fa825aaa588b6a8375`) differs from checkpoint `refs/dotln/checkpoint/WO-018/4` (the verdict) only in `docs/control/current.md` and the strictly appended `docs/control/resume.jsonl`, and from `/2` (implementation-ready) and `/3` (verify allocation) only in those two files plus VER-001 itself (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, four bounded documentation corrections (§Corrections applied by this review), and the lifecycle's own events.

**Authority:** `docs/work-orders/WO-018-control-plane-consolidation.md`: `Model: any capable model` and `Effort: executor xhigh+; verifier xhigh+; reviewer any`; a `v0.4.1` patch classification with `@dotln/kernel` `0.2.1`; ten numbered acceptance criteria (criteria 9 and 10 added by the 2026-09-03 operator amendment); the 2026-09-03 local-harness-settings scope clarification with five items; an evidence gate, a write-back duty, and a non-goal list; the 2026-09-04 Additional Opinion ideation breakout receipt with its required-review list; and the 2026-09-04 implementation evidence receipt, which this review treated as a claim under test. Against the activated text (`refs/dotln/checkpoint/WO-018/1`, whose tree equals `HEAD`'s), the order differs in exactly two hunks (147 added lines, 4 removed): the executor's forward-correction of the placeholder H1 and release classification to `v0.4.1`, and the two appended receipts. Nothing in the criteria, clarification, amendment, or non-goals changed after activation.

**Prior reports:** `VER-001` (pass; all ten criteria, the five clarification items, and the evidence gate reproduced in an isolated mutation harness, pure-function probes, and direct process observation; an eighteen-mutation drill; a thirty-four-path classifier probe; four advisory findings F1–F4 and three notes N1–N3 with a bounded checklist). Complete sequence: one report, no repair episode.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes) in Claude Code 2.1.260 with sandboxed Bash; macOS 15.6 on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 installed in this worktree's own dependency tree. The operator opened this session with the `ultracode` label, which is not an effort value; per the order's 2026-09-03 amendment and the `sessionLabelNotes.ultracode` record in `docs/discovery/environment.json`, that label attests reasoning effort **`xhigh`** with epistemic source **`operator-attested`**, and `resume` does not convert it, so the actor supplies both explicitly. WO-018 declares `reviewer any`, so any attested effort would satisfy it; the value is stated because it is what the recorded attestation says. `2.1.260` was checked against `claude --version` on this machine and is the second entry on the observed `versions` list this order introduces, so the attestation is admissible under criterion 9's own mechanism, which this review is the second live use of.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.260","model":"claude-fable-5-1","effort":"xhigh","source":"operator-attested"}

**Verdict: WO-018 passes.** The order asked the four lifecycle scripts to stop diverging, the control state to become machine-readable, the release manifest's cadence grammar to come from the built kernel, one data-loss gap to close, the test chain to exercise what it claims, the harness-version attestation slot to become a list, and the operator's local harness settings to be treated asymmetrically, all without losing a single existing guard. The deliverable does that. Twelve helpers exist once each under `scripts/lib/` and every lifecycle script imports them; `resume status --json` projects the canonical fold and neither `worktree.mjs` nor `release.mjs` reads a byte of the Markdown projection; the fold refuses an unknown event with its ordinal; nothing under `docs/intake/` can be classified disposable and build outputs match only at anchored paths; the exact root `.claude/settings.local.json` is release-evidence-allowed and never disposable; the manifest's evaluable and deferred cadence lists are derived from the built kernel's type-exhaustive `CADENCE_KINDS` export with bidirectional constructor agreement, and the control-log schema range from an exported constant; `npm test` runs the real-repository publication check and the identity corpus; the publication fixture asserts against an independent `shasum`; every trace assertion carries a non-empty guard; top-level `scripts/*.mjs` falls by nine lines; any observed harness version attests and refusals name the versions on record; and the `ultracode` note is recorded without any automatic conversion. Every count, digest, and refusal in the receipt and in VER-001 that this review could reproduce was reproduced first-hand, and every guard main had in the touched areas was traced to a surviving site or to a change a criterion names. VER-001's four findings are one deferred code defect on an unsanctioned invocation path, one vocabulary gap in a discovery record, one misordered index row, and one contract change the reviewer must bless; this review defers the first two, corrects the third, and blesses the fourth together with two further consequences of criterion 5 that no criterion names. No code, contract, acceptance, schema, authority, or captured-evidence byte changed under this review.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment. For this order the instrument is the subject more completely than for any earlier one.

1. **`scripts/resume.mjs` allocated this report's path, appended `FinalReviewRequested`, and records this verdict through the actor-gated `final-review-result` path, and it is a deliverable of this order** (criteria 3, 4, 9, and 10 change it; it is 992 lines against main's 906). Before invoking the allocation transition, this review read the `resume` mapping in `package.json` (`node scripts/resume.mjs`, unchanged) and the complete `git diff main -- scripts/resume.mjs`, so the code that ran was inspected rather than trusted. Three compensating controls were used rather than the instrument's own word: subject identity was established with git objects (§Dispatch and subject integrity); the fold, the projection, the classifier, and the authority-path check were imported as pure functions and probed on adversarial inputs by the review lanes; and every read-only status invocation was proven read-only by digest comparison of both control files before and after.
2. **`scripts/worktree.mjs` and `scripts/release.mjs` are the publish, surface, notes, and close gates this review relies on, and both are deliverables.** Their diffs against main were read in full, and their gates were exercised by the shell suites in isolated copies and by `check-surfaces` against this working tree. The `gh` preflight in `worktree publish` fails closed inside this sandbox (§Remaining deviations), so no push or PR mutation is performed by the instrument under review.
3. **`docs/discovery/environment.json`, the evidence file that gates actor attestations, was edited inside the subject by criteria 9 and 10.** That edit is what lets this review's own `2.1.260` attest. The datum was checked against the machine, and the file's one recorded inconsistency (VER-001 F2) is adjudicated below rather than repaired, precisely because this review must not edit the record that admits its own attestation.
4. **This reviewer's own attestation is checked by the same mechanism.** A successful append proves that the header above equals the flags, not anything about the subject; the inventory in §The order's own actors was compared by object comparison in this session, and the acceptance of this exact actor tuple was rehearsed by a review lane in an isolated copy before the live command ran.
5. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser, profile, and surface check** (`scripts/release-notes.mjs`, `scripts/github-body.mjs`, and `release check-surfaces`), of which only `release.mjs` is changed by this order, and not in those code paths. Because `v0.4.1` is above the latest published tag, these notes become the `v0.4.1` edition at release close.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-018 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, and a single-pair drift; `docs/control/current.md` agreed and no drift warning was emitted. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-018/FINAL-001.md`, moved the phase to `final-review`, and minted checkpoint `a45e84d74029060bca92af29d016ffa764d54fc3` (`refs/dotln/checkpoint/WO-018/5`). `docs/control/resume.jsonl` is a strict append over `HEAD`: the first 122 lines are byte-identical to `HEAD`'s file (`cmp` silent), and the appended lines are `WorkOrderActivated` (with `effortDeclarationValidated: true`), `ImplementationReady`, `VerificationRequested`, `VerificationCompleted`, and this review's `FinalReviewRequested`, plus the `FinalReviewCompleted` the pass records. Every event carries `schemaVersion: 1`.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git add -A`, `git write-tree`) captured the working tree as tree `eaba2e7c533967ce9c7223fa825aaa588b6a8375`, and `git diff --stat` against each checkpoint reported:

| Checkpoint                                   | Differences from the working tree                                                                                        |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `/1` activation (`5dcdcce4…`)                | every deliverable path; the checkpoint's tree equals `HEAD`'s, so the whole subject is downstream of activation          |
| `/2` implementation-ready (`daf3784b…`)      | `docs/control/current.md`, `docs/control/resume.jsonl` (+4), `docs/verifications/WO-018/VER-001.md` (added, 449 lines) |
| `/3` verify allocation (`8dca903c…`)         | `docs/control/current.md`, `docs/control/resume.jsonl` (+3), `docs/verifications/WO-018/VER-001.md` (added)             |
| `/4` verdict (`551398a3…`)                   | `docs/control/current.md`, `docs/control/resume.jsonl` (+2)                                                              |
| `/5` final-review allocation (`a45e84d7…`)   | `docs/control/current.md`, `docs/control/resume.jsonl` (+1)                                                              |

Every deliverable blob, the 40 tracked non-control modifications and the two untracked library files, is therefore identical across the implementation-ready, verify-allocation, and verdict checkpoints and the working tree; the verifier changed nothing in the deliverable, and VER-001 has not changed since its verdict was recorded. All five checkpoints have `HEAD` as their sole parent.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. The only ignored paths in this worktree are `node_modules/` and the three `packages/*/dist/` build outputs; `git ls-files docs/intake` lists the three `.gitkeep` placeholders and nothing else, and `git status --porcelain --ignored` shows no ignored file under `docs/intake/`, consistent with the receipt's statement that the worktree's provisional intake copy was reconciled to main and removed before verification. `.claude/` carries no change, tracked or untracked. In the main checkout, scanned read-only by path name only, 477 ignored entries (111 under `docs/intake/` and the exact root `.claude/settings.local.json` among them) all classify `releaseEvidenceAllowed`, so the post-merge close is not at risk from the reconciled guard.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                                  | Report header                                                                                                                                                                   | Declared minimum | Result                          |
| -------- | ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.0","model":"gpt-5.6-sol","effort":"xhigh","source":"self-reported"}`               | there is no executor report; `0.153.0` is the newest entry on the `codex-cli` observed `versions` list and its persisted `model_reasoning_effort` selector records `xhigh`        | `xhigh+`         | `xhigh` meets the floor exactly |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.260","model":"claude-opus-5[1m]","effort":"xhigh","source":"operator-attested"}`   | exactly one column-one header in VER-001, parsed and compared with the event actor as objects: equal                                                                           | `xhigh+`         | `xhigh` meets the floor exactly |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.260","model":"claude-fable-5-1","effort":"xhigh","source":"operator-attested"}`    | the header above                                                                                                                                                                | `any`            | satisfied                       |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: none` because every completion attests `xhigh`, and the JSON form's `effortDrift` is the single pair `[{"effort":"xhigh"}]`; this review's `xhigh` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.260; model claude-fable-5-1; effort xhigh; source operator-attested` with the same drift line and phase `closed`.

## Final evidence summary

### The unchanged `npm test`, twice, in this worktree

First run, on the verified tree before any review artifact existed: `All matched files use Prettier code style!`, `GitHub body profile tests passed`, then the eight shell suites (fixture temp-root, publication checker, backup-intake, resume, checkpoint, worktree, release), `PASS index coverage: 169/169 product headings indexed` with both audience editions `CURRENT` (27 and 42 linked sections), `tsc -b --force`, `# tests 154 / # pass 154 / # fail 0`, and the identity corpus `# tests 8 / # pass 8 / # fail 0`, exit 0. Second run, after this review's corrections and package were in place, with the same result (§Corrections applied by this review).

### Static, surface, and structural checks

- `git diff --check`: exit 0, no output, before and after this review's artifacts.
- `npm run release -- check-surfaces` on the working tree: `PASS release-block: observed v0.4.1; expected v0.4.1 (work-order target v0.4.1; latest published v0.4.0)`, `PASS component-version @dotln/kernel: src changed; observed 0.2.1; previous v0.4.0 0.2.0; expected a different version`, `PASS component-version @dotln/skeleton: src unchanged; observed 0.4.0`, `PASS component-version @dotln/compiler: src unchanged; observed 0.1.0`, and the body-profile rule PASS, reading `no current final-review bodies yet` before this review's bodies existed and naming `docs/final-reviews/WO-018/PR.md, docs/final-reviews/WO-018/RELEASE-NOTES.md` after.
- Helper singletons: a grep for each of `runGit`, `failureOf`, `runGitPathList`, `ensureClean`, `parseWorktrees`, `mainWorktree`, `removeMergedBranch`, `containedRegularFile`, `parseJson`, `readJsonFile`, `workOrderAuthorityPath`, and `classifyIgnoredMaterial` over `scripts/` returns exactly one definition each, all under `scripts/lib/`.
- Projection-as-API removal: `current.md` and `Phase: closed` occur zero times in `scripts/worktree.mjs` and `scripts/release.mjs`; main's `worktree.mjs:327` `includes("- Phase: closed")` and `release.mjs:283` `^- <label>:` regex are gone.
- Line count: top-level `scripts/*.mjs` totals 4,378 lines against 4,387 on main (`check-publication.mjs` −2, `release.mjs` −40, `resume.mjs` +86, `worktree.mjs` −53, four files unchanged), a fall of 9; `scripts/lib/` adds 150, so the whole `scripts/**/*.mjs` surface rises by 141 (VER-001 N2).
- Forbidden surfaces: `git diff main --stat -- docs/verifications docs/final-reviews` is empty (VER-001 is untracked and is the verifier's own artifact); `package-lock.json` changes only the kernel version and the skeleton's kernel pin; no dependency was added.
- The verifier's header equals its control actor by object comparison; VER-001 contains exactly one column-one `**Actor attestation:**` line.
- Phase sets and action lists: the `requirePhase` call sites, the eight `fold` cases, and the eleven `main()` cases are identical between main and the subject; `CONTROL_LOG_SCHEMA_VERSION` is `1`; the tag-manifest template is unchanged and has no `program` field.

### Independent review lanes and adversarial refutation

This review's reading of the change set was decomposed into ten independent lanes, each reading the work order, VER-001, and the final-review README first and then working one area of the subject: (A) criteria 1–2, (B) criteria 3–4 and F1, (C) criterion 5 and the WO-015 carried items, (D) criteria 6–8 and guard preservation across every shell suite, (E) criteria 9–10 and the scope clarification, (F) the ideation receipt's required-review list and the publication surfaces, (G) the write-back duty, non-goals, release classification, documentation consistency, and the clean-room screen, (H) process and attestation soundness including a spot-audit of VER-001's citations, (I) an adversarial read of every script change for regressions, and (J) a mutation drill. Every lane treated this worktree as read-only, proved its status runs read-only by digest, executed suites and mutations only in isolated copies whose git directory was verified inside the copy, and never read `docs/intake/`. Each lane's blocking and advisory findings were then handed to an adversarial refuter applying three lenses (reproduce it; is the severity right under the order's rules; is the proposed disposition really a non-substantive correction), and a completeness critic asked what no lane had covered. The critic, given every lane's findings and votes, found no blocking item and no file in the diff that no lane had examined; it named the elements only the reviewer can supply (this report, the PR title, the second full `npm test` and `check-surfaces` after the corrections, and the ordering that records the pass before the reviewed commit so the committed log folds to `closed`); it flagged two overclaims in this review's own draft notes, that the `types.ts` parser was deleted when only its cadence-union half was, and that the suites grew only by fixtures, both corrected before commit; it reconciled the lanes' severity disagreements to the refuters' verdicts; it confirmed by a names-only dry run that WO-018's own closeout gates (clean main, clean subject, no non-disposable ignored material in the subject, nothing release-refused in main) would pass today; and it verified on main that WO-017, WO-013, WO-024, and WO-025 are merged, so the order's dependency preconditions hold and its reconcile-if-WO-013-is-missing branch does not apply. In total the run used eighteen agents: ten lanes, seven batch refuters, and the critic.

Lanes A through I reported no blocking finding and agreed with VER-001's verdict and with one another; the disagreements between lanes were confined to severity labels, which the refuters settled (the map staleness and the `--silent` wording are notes, not advisories). The reproduced mutation results across lanes A–E, before lane J's drill, were: the any-segment revert to the classifier fails `test-worktree.sh` (at the nested `assets/dist` fixture, not the intake fixture, exactly as VER-001 N3 predicts); removing the intake term alone leaves `test-worktree.sh` green and fails `test-release.sh`; dropping `--untracked-files=all` is inert under default Git configuration but load-bearing under `status.showUntrackedFiles=no`, reproduced both ways; deleting the `fold` default fails `test-resume.sh` with `unknown control event was accepted`; changing `CONTROL_LOG_SCHEMA_VERSION` to `7` fails `test-release.sh` at the fixture's own replace precondition, and hard-coding `controlLog` to `{min:1,max:1}` in `release.mjs` fails the manifest assertion with `expected: { min: 7, max: 7 }`, which is the stronger oracle; removing the inconsistency throw fails `test-release.sh` with `inconsistent built kernel constants published (union-only)`; removing the missing-constants throw fails it too, mutely; altering the publication digest fails `test-publication.sh` at the `expected_lock` comparison; restricting the version matcher to the newest entry fails `test-resume.sh` at the listed-older-version attestation; making the settings path disposable fails `test-worktree.sh` with `subject harness settings were deleted`; and dropping the release exception fails `test-release.sh` at the `lower` fixture. Lane C additionally reproduced that omitting a kind from `CADENCE_KINDS` fails `tsc` with TS2345 and adding a bogus kind fails with TS2322, while a duplicated kind compiles and is caught at runtime by the kernel test and by `release.mjs`'s uniqueness check.

### Mutation drill

VER-001 ran an eighteen-row drill; this review did not inherit it. Lane J built a fresh isolated copy (rsync excluding `.git`, `docs/intake`, `node_modules`, and build outputs; `git init`; the copy's git directory verified before every mutation; the worktree's control digests identical before and after), ran the five touched suites green as a baseline (resume 9 s, worktree 14 s, publication 1 s, checkpoint 1 s, release 52 s; the release suite uses fixture kernel runtimes and a fake `tsc`, so it never builds the real kernel), and then applied each mutation alone, ran the named suite, and reset the copy. Where a suite failed under `set -e` without a diagnostic, an inherited `ERR` trap pinned the failing line.

| #   | Mutation                                                                          | Suite       | Result | Failing assertion                                                                                              |
| --- | --------------------------------------------------------------------------------- | ----------- | ------ | -------------------------------------------------------------------------------------------------------------- |
| 1   | `protectedIntake` always false                                                    | worktree    | missed | suite passes: the planted `docs/intake/dist/x.md` and `node_modules/y` stay protected by anchoring alone (N3)  |
| 1   | same                                                                              | release     | caught | `test-release.sh:595` `main checkout contains ignored material that can contaminate release evidence: docs/intake/…` |
| 2   | build outputs matched by any segment (the original bug)                           | worktree    | caught | `test-worktree.sh:420-422` `nested asset dist directory was treated as disposable`                             |
| 3   | root settings path dropped from `releaseEvidenceAllowed`                          | release     | caught | `test-release.sh:595` `… contaminate release evidence: .claude/settings.local.json`                            |
| 4   | root settings path made disposable                                                | worktree    | caught | `test-worktree.sh:408-410` `subject harness settings were deleted`                                             |
| 5   | `fold` `default:` throw deleted                                                   | resume      | caught | `test-resume.sh:512-514` `unknown control event was accepted`                                                  |
| 6   | `warnIfProjectionDisagrees` call removed from `status`                            | resume      | caught | `test-resume.sh:403` grep for the drift warning on stderr                                                      |
| 7   | version matcher accepts only the newest recorded version                          | resume      | caught | `test-resume.sh:187` `recorded observed versions: fixture-1, fixture-2` no longer emitted                      |
| 8   | `CONTROL_LOG_SCHEMA_VERSION` `1` → `7`                                            | release     | caught | `test-release.sh:800-809` the `success` fixture's own rewrite precondition; the manifest assertion is not reached |
| 9   | inconsistent-constants throw removed from `builtKernelCadence`                    | release     | caught | `test-release.sh:631` `inconsistent built kernel constants published (union-only)`                             |
| 10  | `finish` committed closed-state gate removed                                      | worktree    | caught | `test-worktree.sh:494-496` `hidden live control state bypassed destructive finish gate`                        |
| 11  | `ensureClean` drops `--untracked-files=all`                                       | all three   | inert  | worktree, resume, and release all pass; no fixture sets `status.showUntrackedFiles=no`                         |
| 12  | publication lock digest input altered                                             | publication | caught | `test-publication.sh:123` script digest differs from the independent `shasum` oracle                           |
| 13  | `readJsonFile` stops naming the file                                              | release     | caught | `test-release.sh:38` `invalid JSON in <path>` grep                                                             |
| 14  | `ensureNoIgnoredMaterial` drops `--ignored`                                       | worktree    | caught | `test-worktree.sh:364` `ignored secret was deleted`                                                            |
| 15  | manifest `controlLog` hard-coded to `{min:1,max:1}`, ignoring the import (extra)  | release     | caught | `test-release.sh:894-897` `deepStrictEqual` actual `{min:1,max:1}` expected `{min:7,max:7}`                    |

Thirteen of fourteen assigned mutations were caught by the named suite; the two misses are exactly the two VER-001 characterizes (its N3 and its inert `ensureClean` row), and the `1 → 7` row is caught by the fixture's precondition rather than by the manifest assertion, with row 15 supplying the genuine negative fixture for the export-to-manifest derivation (adjudication 12). Lane J also confirmed the `showUntrackedFiles=no` semantics in a scratch repository, and demonstrated end-to-end, in a probe clone with an eleven-line fixture that plants `docs/intake/.DS_Store`, that the shipped code refuses and preserves while mutation 1 destroys the subject worktree and branch, which is the destructive-side gap adjudication 9 defers. Fifteen of VER-001's eighteen rows were reproduced with identical outcomes; the three not assigned (missing-constants throw, `statusProjection` dropping `legalNextActions`, the NUL-termination guard) were covered by lanes C and I or by VER-001 alone.

### Ideation receipt evidence

- Every promoted surface is titled or labelled candidate, vision, or prospective: `05-pattern-library.md` `## Candidate — Additional Opinion` ending `Current resume control does not implement this candidate.`; `03-architecture.md` `### Candidate — opinion cohorts and sealed adjudication` ending `Current resume control v1 implements none of these cohort events or legal actions.`; the `02-domain-model.md` table headed `Candidate term`; `04-interfaces.md` `When the candidate Additional Opinion support is equipped` and `merely documenting this candidate does not make a repeated command legal`; `06-roadmap.md` `This recommendation has no version, work-order identity, or activation authority`; the map's `Unallocated workflow candidate`; both new audience-index rows at status `vision`.
- The operator resume phrase set in 07-execution-guide.md is identical on main and the subject; `04-interfaces.md` changes 29 insertions and no deletions inside the agent-projection section and leaves the phrase list untouched; `grep -ci opinion scripts/resume.mjs` is zero.
- The added lines of all eight promoted surfaces contain no public model, provider, or harness name; the only Markdown link they add resolves (`#candidate--influence-response-policies`); the pre-existing Eye Dr section is byte-unchanged and the candidate follows it.
- `docs/lineage/idea-ledger.md` gains 80 lines and loses none; the new section sits directly below the header per the file's rule with six entries at `transformed` or `preserved` and a second section recording the `program` manifest-field candidate as `preserved`.
- Publication: `check-publication.mjs` passes at 169/169 with both locks current; both new rows resolve to real headings by independent slug computation; the locks changed in this change set only because the candidate sections sit inside parent sections the editions link.
- Intake: `git ls-files docs/intake` lists three `.gitkeep` placeholders; no intake path is staged, diffed, or linked. The capture path is named in prose in the ledger and the work order, which is the ledger's established convention with many precedents on main; nothing from it was read by this review.

## Acceptance criteria

| #   | Criterion                                                                                                                                                                                    | Verdict | First-hand basis                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | One `runGit`, `failureOf`, `runGitPathList`, `ensureClean`, `parseWorktrees`, `mainWorktree`, `containedRegularFile`, `readJsonFile` (names the file), and one classifier under `scripts/lib/`; the four scripts import them; the two strictnesses and the two allowlists reconciled to one each with the choice stated | met     | Twelve single definitions (the eleven named plus `removeMergedBranch` and `parseJson`) with importers traced per script; every surviving `spawnSync` outside `lib` is a status-tolerant, binary, or non-git call, and every bare `JSON.parse(readFileSync(package.json))` on main is gone. The strict `--porcelain --untracked-files=all` won, and the receipt's reason was reproduced: under `status.showUntrackedFiles=no` main's `worktree.mjs` gate reports a tree with an untracked intake note as clean while the unified gate reports `?? docs/intake/notes/x.md`. The allowlists reconciled to one classifier exposing `disposable` (consumed only at `worktree.mjs:117`) and `releaseEvidenceAllowed` (consumed only at `release.mjs:148`); the release list is wider than the disposable list by exactly two terms, anything under `docs/intake/` and the exact root settings path, and the shared anchoring makes release influence stricter than main for nested build-shaped paths. `readJsonFile` names the file on read and parse failure, pinned by `test-release.sh`. |
| 2   | Nothing under `docs/intake/` is disposable; build outputs match by anchored path; the named fixture plants `docs/intake/dist/x.md` and `docs/intake/node_modules/y`, `finish` refuses, both survive; the disposables case stays green | met     | Thirty-six paths probed through `classifyIgnoredMaterial` by three lanes independently: every `docs/intake/…` candidate including `.DS_Store` and `.tsbuildinfo` descendants is non-disposable; `dist/`, `node_modules/`, and `packages/NAME/{dist,node_modules}/` are disposable while `a/b/dist/x`, `corpus/dist/y`, `docs/dist/n`, `.claude/dist/q`, `distortion/x`, `node_modulesX/y`, `./dist/x`, absolute, `..`-relative, and quoted candidates are not, refusing rather than deleting. The fixture is at `scripts/test-worktree.sh:384-405` and the disposables-leave case at `:438-460`, both green in isolated copies; the any-segment revert fails the suite. `runGitPathList` throws on a non-NUL-terminated list, so `core.quotePath` cannot reach the classifier, reproduced with a quoted and a tab-bearing path. |
| 3   | `status --json` emits the same fields as `current.md`; `worktree.mjs` and `release.mjs` read it and contain no substring or regex parse of `current.md`; `status` warns read-only on drift | met     | `render` emits twelve bullets and `statusProjection` twelve keys in the same order with `none` mapping to `null` and the unavailable checkpoint to `{unavailable:true}`; the live JSON matches `current.md` field for field; both forms are read-only by digest and `project` is gated by `action !== "status"`; the drift warning goes to stderr with exit 0 and the JSON stdout still parses, reproduced in an isolated copy with an edited and then a deleted projection. `release.mjs` spawns `resume.mjs status --json` for the live surface preview and folds `HEAD:docs/control/resume.jsonl` through the exported `fold` and `statusProjection` for close and validate; `worktree.mjs` folds the subject's committed `HEAD:docs/control/resume.jsonl` through the same exports for the `finish` gate and additionally requires `workOrder` to equal the work order being finished, which is what defeats the forged working-tree `- Phase: closed` fixture. The intent of "read it" is met by structured fields either way, and the committed-bytes form is the stronger guard. |
| 4   | `fold` refuses an unknown event type with its ordinal, consistent with `readEvents`; test added                                                                                              | met     | `scripts/resume.mjs:146-149` throws `unknown control event type at line N: <type or missing>`, one-based over non-blank lines exactly as `parseControlEvents` numbers malformed JSON; probed with an unknown type at position 3, a missing type, a null event, an empty array, and the real log. The fixture at `scripts/test-resume.sh:502-531` asserts the message, refuses a leaked stack, and `cmp`s both control files; deleting the `default:` clause fails it. Main's `fold` silently ignored unknown types. |
| 5   | Manifest cadence lists from the built kernel's exports; source-text parser and its fixtures deleted; "malformed cadence source" becomes "kernel constants missing or inconsistent" and still proves no `mktag` or push; control-log schema from a `resume.mjs` export | met     | `builtKernelCadence` (`release.mjs:577-653`) imports `packages/kernel/dist/src/index.js` in a child process and requires the complete `CADENCE_KINDS` tuple, unique identifiers, bidirectional agreement between the tuple and the runtime `Cadence` constructors with each factory's `kind` equal to its name, and a unique non-empty evaluable subset, then derives `deferred` by set difference; `built kernel cadence constants are missing` and `… inconsistent with the Cadence type union` are distinct refusals with distinct fixtures (`missing`; `union-only`, `constructor-only`, `inconsistent-evaluable`), each asserting a non-empty trace, no ` mktag`, no tag push, unchanged origin refs, and no candidate tag. `defineCadenceKinds` is compile-time exhaustive for coverage (omission and extras fail `tsc`; duplicates are caught at runtime). No `readFileSync` of `core.ts` and no regex over `resume.mjs` survive; `CONTROL_LOG_SCHEMA_VERSION` is imported at `release.mjs:41` and a fixture changes the export to `7` and asserts the manifest follows. The real built kernel's lists match the published `v0.4.0` manifest's lists and order. |
| 6   | `npm test` runs `check-publication.mjs` against the real repository and the identity corpus; the Program lane stays outside and is marked frozen                                              | met     | `package.json`'s `test` gained exactly `node scripts/check-publication.mjs` (before the build step; it imports only `lib/git.mjs`) and `node --test corpus/harness/wo101-id-corpus.test.mjs` (after the build; it imports the built kernel), both `&&`-chained; no `wo101-*` glob and no Program test in the chain; `corpus/README.md` marks the Program lane `explicitly frozen … outside npm test; promotion requires a separately reviewed change` and splits the commands. Both steps ran green in the first-hand `npm test`. |
| 7   | `test-publication.sh` asserts against an independent `shasum -a 256`; split refusal messages in the manifest deriver; `test -s` guards on the trace assertions in `test-release.sh`          | met     | `scripts/test-publication.sh:113-124` pipes the key, a NUL, the fixture section bytes, and a NUL into `shasum -a 256` and asserts both printed locks equal that value; the 64-character length check and the write-back-what-the-script-said pattern are gone; altering the checker's hashed input fails the suite. All four `GIT_TRACE` fixtures in `test-release.sh` carry `test -s` (main had none). The WO-015 review's third carried item, a `case` after `default:` classifying silently, is retired because no switch parser exists. |
| 8   | `scripts/*.mjs` lower than on main with the delta reported; every guard enumerated across `scripts/test-*.sh` still has a failing negative fixture                                          | met     | 4,378 versus 4,387 (−9), reported by the receipt and reproduced per file. Lane D diffed every suite against main line by line: every removed or changed assertion maps to a criterion (the cadence-parser fixture to criterion 5; four refusal substrings and the older-version success to criteria 9 and 10; the `mtime` read-only check to the stronger `cmp`-based `assert_status_read_only` under criterion 3), with two changes no criterion names, the inverted already-published-rerun assertion and the release-close hand-off text, adjudicated below; lane I mapped every `throw new Error` on main to a surviving site. The suites grew by fixtures: `test-release.sh` +175, `test-resume.sh` +166, `test-worktree.sh` +118, `test-publication.sh` +8, `test-checkpoint.sh` +2; `test-fixture-temp-root.sh`, `test-github-body.mjs`, `test-backup-intake.sh`, and `test-temp-root.sh` are byte-identical to main. |
| 9   | `version` becomes an observed `versions` list; the matcher accepts any listed version; recording appends; the refusal names the versions on record; fixtures for listed-older, unlisted, and unchanged `harness-readback` behavior | met     | Both harnesses carry two classified entries with the newest last, and both older values are genuine prior observations from main's history (`2.1.259` from WO-019, `0.152.1` from WO-019); the single hunk touches nothing else. `recordedHarnessVersions` filters to observed string entries and `effortHarnessEvidence` uses `includes`; both refusals embed `recorded observed versions: …`. Reproduced against the real file in a scratch fixture: `2.1.259` and `0.152.1` attest; `2.1.261` and `0.150.0` refuse naming the lists; `harness-readback` refuses because neither harness records an observed effective readback. Fixtures at `test-resume.sh:187-233`; the newest-only mutation fails at the listed-older attestation. Appending rather than replacing is a documented rule carried by 07-execution-guide.md and the playbook; no script writes the file. |
| 10  | The `ultracode` note beside the `claude-code` entry; the refusal for an unrecognized label points at it; `resume` never converts a label; the WO-019 unknown-plus-raw rule stands            | met     | `environment.json:141-149` carries the note with `attestedAt`, `meaning`, `reasoningEffort: "xhigh"`, `attestationSource: "operator-attested"`, and `automaticConversion: false`; `sessionLabelHint` fires only when all four gates hold and uses `Object.hasOwn` against prototype keys; zero `ultracode` occurrences in any `scripts/*.mjs` or `scripts/lib/*.mjs` (the 31 in `test-resume.sh` are fixture data); `--effort ultracode` is still refused with the hint and stored nowhere, reproduced against the real file; the `constructor`, `toString`, `__proto__`, and `malformed` labels refuse as `unknown (raw: …)` with no inherited guidance. The reviewer role's `any` minimum means a session-label effort would be stored as `unknown` plus `raw` rather than refused, which is the WO-019 rule working as designed; this review attests `xhigh` explicitly as the note directs. |

**Scope clarification (2026-09-03), all five items met.** `.gitignore` gains exactly the anchored `/.claude/settings.local.json` and no `.claude/**` glob, confirmed by `git check-ignore` on the siblings; nested copies are ignored on this machine only by the operator's global excludes and classify neither disposable nor release-allowed, so both guards refuse rather than delete or admit them. The classifier keeps release influence and destructive cleanup as two answers over shared parsing. The release fixture proves a no-release close succeeds while main's settings file and an intake screenshot both survive, retains the foreign ignored-file refusal, and refuses the sibling `.claude/other.local.json`; the worktree fixture proves a subject settings copy blocks removal and survives with its worktree and branch, alongside the intake precedence fixtures. The playbook, execution guide, and harness-security runbook carry the asymmetric treatment in present tense, replacing the "WO-018 will allow" text; no historical review is rewritten. No code reads, parses, hashes, or echoes the settings file; the guard compares a path string from `git ls-files -z`.

Evidence gate: every suite green first-hand; the new negative fixtures reproduced by mutation; `cmp` of both control files around every status pair and transition in `test-resume.sh`; the fixture's simulated release asserting the manifest's cadence fields equal the fixture kernel's constants, and the real built kernel's constants equal to the published `v0.4.0` manifest; `git diff --check` clean. Scope discipline: the phase sets, transition guards, and action lists are unchanged; no `recover` action, no `dotln status`, no `stat -f` change, no `program` field; nothing under `docs/verifications/` or `docs/final-reviews/` touched by the executor; the version changes match the classification exactly.

## Write-back duty

- 07-execution-guide.md and `docs/PLAYBOOK.md` document `npm run resume --silent -- status --json` as the first step of every dispatch, both forms as read-only, the drift warning, the anchored disposable list, the settings asymmetry, the two-form close, the observed `versions` list, and the `ultracode` note (§Model-specific notes and §Who does what respectively).
- `docs/releases/README.md` states that cadence compatibility comes from the built kernel's `CADENCE_KINDS` and `EVALUABLE_CADENCE_KINDS` with bidirectional constructor agreement, that the control-log schema range comes from the `resume.mjs` export, that committed release state is folded from the tagged bytes, and that equal-tag retries reproduce evidence first; this review adds the `validate` precondition (§Corrections applied by this review).
- `corpus/README.md` marks the promoted identity lane and the frozen Program lane; `docs/work-orders/codex-downtime-series.md` item (6) is reconciled to the same fact.
- The ledger's adopted entry `Control state is data, and a disposable projection is never an API` already existed on main under the 2026-09-02 entropy review; the subject references it rather than duplicating it and adds the `program` manifest-field candidate as `preserved`, which discharges the non-goal that asked for the record. The map records the version and the slot; this review updates its state (§Corrections applied by this review).
- The kernel README maps `CADENCE_KINDS`, states its compile-time exhaustiveness and the constructor-agreement requirement, and names component `0.2.1` staged for `v0.4.1`; the skeleton README states that `0.4.0` stays with only the kernel pin advancing; the README release block reads `v0.4.1`.

## Ideation breakout receipt

The receipt names the review this closeout owes it, and this review performed it on the receipt, the eight promoted surfaces, and the three refreshed publication projections; the evidence is in §Ideation receipt evidence above.

- **Source treatment and reconciliation:** Shape-First Synthesis is the saved bare-`ideation:` loadout; the promoted text is written in repository vocabulary and flags its one paraphrase of the operator's proposal as a documented transformation; the added lines name no model, provider, or harness; the byte-comparison and backup steps are operator-side and not reproducible from this worktree, which the receipt and VER-001 both state; what is externally observable (no intake path staged, diffed, or linked; the ignore rule intact; no ignored intake file in this worktree) is consistent with the receipt.
- **Fidelity to the two shapes:** "another peer episode over one immutable subject snapshot or seal the cohort and continue" carries the repeat-or-proceed shape; the ordered reduction path with a seal that freezes the admitted set, late results retaining a disposition, and adjudication as a separate authority carries the sealed one-way barrier.
- **Second Opinion versus the general cohort:** the two-result preset is distinguished from the finite batch bounded by count, concurrency, deadline, cost, and stop rules in the pattern library, the interfaces, and the ledger; recursive opinion-of-opinion fan-out is prohibited.
- **Replicates, variants, panels, critiques:** five modes are named and defined; same-model repeats are replicates, not independent votes; a critique that sees an earlier result is dependent by design; implementation variants pin their own produced artifact.
- **Worktree and single-writer safety, lossless dissent, Eye Dr precedence:** one writable agent per worktree from one pinned base with a separate integration branch and a single host-owned recorder; duplicate clusters retain every contributor; volume never creates authority; pairwise preference only below hard constraints with correlated runs named as confounders.
- **Settled boundaries:** `docs/decisions/` is untouched and ADR-0001's founding tournament is not reopened; independent verification survives ("cannot skip verification"); statechart and Program ownership is preserved ("an inner controller around an episode-producing stage, not a literal replay of that stage's outer lifecycle transition", with `Program.All` correctly described as grammar-only and deferred in the current kernel); attestation stays actual; disagreement is retained as data.
- **Nothing presented as current behavior or hidden scope:** confirmed on every surface; `04-interfaces.md` changes no current resume command; the unresolved choices are recorded as unresolved, with the receipt's list broader than the pattern library's explicitly non-exhaustive one.
- **Locks and links:** current and resolving; the one ordering defect (VER-001 F3) is corrected by this review.

## Verification-sequence soundness

VER-001 meets every duty the order and the guide place on it: it establishes subject identity through checkpoints, discloses that the instrument is the subject and names three compensating controls, reproduces the executor's evidence rather than taking it on report, probes the classifier and the fold as pure functions, characterizes the three uncaught mutations honestly, performs the receipt's review, and records its findings with a bounded checklist that expands no scope. Every count it reports was re-derived here where cheap (154 tests, 8 corpus tests, 169 headings, 27 and 42 linked sections, 4,378 and 4,387 and 4,528 lines, the suite deltas, 80 and 70 library lines, the 12 helpers, the 34 classifier paths, the 42 modifications) and all agree; every line citation resolves to the described code within two lines. Seven statements are imprecise without changing the verdict, and this review restates them so a later reader is not misled:

1. "No `ultracode` string appears anywhere in `scripts/`" is literally false: `scripts/test-resume.sh` contains 31 occurrences as fixture data. The substantive claim holds: no `scripts/*.mjs` or `scripts/lib/*.mjs` contains it, and the hint is data-driven from `environment.json`.
2. "`git status --porcelain -uall` returned the same 44 entries before and after" is the count before VER-001 itself existed; it is 45 with the report, and the corroborating evidence is the checkpoint-to-checkpoint diff, which touches only the two control files and VER-001.
3. The evidence table's "5 survived adversarial refutation" does not map onto the seven recorded items (F1–F4, N1–N3); the mapping is unstated.
4. "The suites grew only by added fixtures … with no restructuring" overstates uniformity: `test-release.sh` replaced the cadence-parser fixture and inverted the already-published-rerun assertion, and `test-resume.sh` swapped four refusal substrings, moved the successful readback fixture to the older version, and replaced the `mtime` check with `cmp`; none loses a guard, and each is adjudicated here.
5. "`harness-readback` still refusing because `harnessReadbackEligible` is false" is shorthand: no script reads that field; the refusal keys on `effectiveEffortReadback.classification` not being `observed`, as on main.
6. "No intake path is … referenced by a committed artifact": the capture path is named in prose in the ledger and the work order by the ledger's established convention; none is linked, staged, or diffed.
7. The `builtKernelCadence` span is `577-653`, not `577-651`, and the matcher is `476-481`; immaterial.

No VER-001 statement is contradicted by this review's evidence in a way that bears on its verdict.

## Adjudications

### 1. The module-entry guard is lexical, so an absolute symlinked invocation silently no-ops the CLI (VER-001 F1)

Reproduced three ways: a scratch symlink to this worktree, the macOS `/tmp` symlink, and, in an isolated copy, a state-changing `activate` that exited 0 with zero bytes on both streams and an unchanged log while the physical path refused with exit 1. Every sanctioned invocation is physical: `npm run resume` passes a relative argv and `process.cwd()` is physical even from a symlinked shell path; `worktree.mjs` spawns a path from `git worktree list`; `release.mjs` spawns from `import.meta.url`; the documented absolute close command derives from the same. The suites cannot present a symlinked path because `scripts/test-temp-root.sh:50` resolves every fixture base with `pwd -P`. The guard itself is required by criteria 3 and 5, which make `resume.mjs` both a module and a CLI. The one-line `realpathSync` repair is a behavior change in a `.mjs` file, which a final review may not apply. **Deferred** to a follow-on order with a fixture that invokes through a symlink; recorded as a known limitation in the release notes.

### 2. The `ultracode` note's `classification` is outside the discovery record's declared `labels` vocabulary (VER-001 F2)

Confirmed programmatically: `labels` declares seven values and the note is the only `classification` outside them; the diff never touches the `labels` line. VER-001 offers two repairs as interchangeable, and they are not: restamping `classification` would silence the criterion-10 hint in the real repository, because `sessionLabelHint` gates on that exact value, and `test-resume.sh` would not notice since it writes its own fixture record; appending `operator-attested` to `labels` is behavior-neutral, because no script reads `labels`. Even the safe repair is not a reviewer's to make: `labels` is the declared vocabulary of a schema-versioned machine-readable record, `docs/discovery/local-inference.json` records `labelsMatchEnvironmentJson: true` and WO-027's VER-001 states the two arrays were element-for-element equal as of 2026-09-03, so widening it here would silently date a sibling record and a prior report, and this review must not edit the file that admits its own attestation. Criterion 10 does not turn on the value. **Deferred**, with the safe repair named, to the next order that touches discovery.

### 3. One audience-index row is out of source order (VER-001 F3)

Reproduced by resolving all 22 architecture rows to heading lines: the sequence is strictly increasing except the new row at index line 66, whose heading is at `03-architecture.md:543`, between `the-agentic-communication-core` (515) and `composition-system` (605). `check-publication.mjs` compares key sets, and its locks hash only the edition-linked sections, so a pure move changes nothing mechanical; lane F proved the locks byte-identical before and after the move in an isolated copy. **Corrected** by this review (§Corrections applied by this review).

### 4. `worktree finish` now keys on the working directory, and every close hand-off names the subject helper (VER-001 F4; lanes A, D, H, I)

Main gated `finish` on the script residing in the main checkout (`repoRoot` from `import.meta.url`), which meant only main's stale copy of `worktree.mjs` could ever perform cleanup; the subject gates on `resolve(process.cwd())` equalling the main worktree resolved from the script's own repository, `release.mjs` spawns its sibling `worktree.mjs` with main as `cwd`, the `mainHasReleaseCommand` branch that printed `npm run release -- close` is gone, and `worktree publish` and `resume release-close` always print the absolute subject-helper form. This is what lets criterion 2's classifier and criterion 3's committed closed-state gate govern this order's own first post-merge close instead of the pre-fast-forward main helpers, which still carry the any-segment classifier; the receipt states that rationale, and the `stale_helpers` release fixture proves stale main helpers are never executed. The destructive scope is unchanged: the gate still confines cleanup to the script repository's main checkout, a wrong working directory refuses (fixture at `test-worktree.sh:90-92`), the subject path comes from `git worktree list` for the branch and never from `cwd`, and every pre-removal gate (clean main, clean subject, no non-disposable ignored material, committed closed state at the subject's `HEAD` for this exact work order, merged into `origin/main`) precedes `git worktree remove` and `git branch -d` in the same order as main. The two-form close is documented in the releases README, `docs/README.md`, the execution guide, and the playbook. **Accepted as a necessary consequence of criteria 2 and 3; blessed explicitly here so it is not inherited silently. No work-order amendment is required.**

### 5. Equal-tag recovery now reproduces release evidence before validating the published manifest (lanes C, D, I)

`runEvidence(root)` was inserted into the already-published branch of `close`, so a rerun against an equal version now runs `npm ci` and the full evidence gate, and `test-release.sh` replaced main's `test ! -e "$main/node_modules"` after the rerun with `test -d` plus `ci` and `test` counters incremented by exactly one, adding a fixture in which a stale planted `dist/src/index.js` is replaced before the compatibility import. No criterion names this, VER-001 is silent on it, and the removed assertion checked the absence of a side effect rather than a refusal, so no guard is lost. It is mechanically required by criterion 5: validating a published manifest re-derives compatibility from the built kernel, and trusting whatever ignored build output happened to exist would be exactly the untrusted-bytes failure the order set out to remove. The receipt and the releases README both disclose it, and the operator-visible cost is that a post-tag GitHub Release retry runs the evidence chain again. **Blessed** as a consequence of criterion 5, with the assertion inversion named here.

### 6. `release validate` now reads the built kernel and trusts whatever `dist` exists (lane D)

`validate` reaches `builtKernelCadence` through `validateManifest` and `baseManifest` and never runs evidence, so on an unbuilt checkout it fails with `cannot load built kernel cadence exports` and on a stale build it validates against stale bytes; main validated from source text and worked on any fresh clone. Reproduced in an isolated copy without `dist`. A consequence of criterion 5, not an unmet one; the suite cannot observe it because every `validate` call in `test-release.sh` runs after fixture evidence has populated `dist`. **Documented** by this review as a precondition in `docs/releases/README.md` (§Corrections applied by this review); a rebuild-or-refuse behavior for `validate` is a code follow-on.

### 7. `docs/README.md` prescribes `status --json` without `--silent` (VER-001 N1)

Reproduced: without `--silent` npm's banner precedes the JSON and `JSON.parse` fails on `>`; the two write-back targets the order names use the `--silent` form; `docs/README.md:103` is the only prescriptive occurrence lacking it. **Corrected** by this review.

### 8. The work-order map records the state at implementation handoff (lanes F and G)

The snapshot line, the slot bullet, and the WO-018 row say "awaiting independent VER-001" and "no verification or final-review result yet", true when the executor wrote them and false beside a passing VER-001 and this report. The map disclaims lifecycle authority, so nothing acceptance-relevant is wrong, and following WO-013, WO-024, WO-025, WO-019, WO-027, WO-017, and WO-008 FINAL-001 §Corrections it is the final review's pass that makes the "FINAL-001 pass" cell true. **Corrected** by this review.

### 9. Two guards without a discriminating destructive-side fixture (VER-001 N3)

Reproduced by lanes A and J: deleting the `!intake` term leaves `test-worktree.sh` green because the anchoring alone already protects `docs/intake/dist/x.md` and `docs/intake/node_modules/y`, while `test-release.sh` catches it; the only paths where the term is solely load-bearing on the destructive side are `.DS_Store` and `*.tsbuildinfo` under intake, which the direct classifier probes pin. Adding a `docs/intake/**/.DS_Store` fixture is a suite change and belongs to a follow-on order. **Deferred** as an optional fixture candidate.

### 10. The event-envelope schema version is still scraped from `types.ts` by regex (lanes C and D)

Pre-existing on main and outside criterion 5, which names the cadence lists and the control-log schema; the receipt's "no source-text cadence or resume-literal parser remains" and the releases README's "does not scrape either value" are accurate as scoped. **Recorded** as a candidate for a follow-on that exports the envelope schema version from the built kernel.

### 11. `release.mjs` discards the drift warning its child `status --json` emits (lanes B and I)

`parseControlState` consumes only the exit status and stdout, so the criterion-3 warning is captured and dropped when `check-surfaces` is the caller; `close` and `validate` fold committed bytes and never spawn the CLI. The criterion requires `status` to warn, which it does; relaying it is a follow-on. **Recorded.**

### 12. The `1 → 7` schema mutation is caught by the fixture's replace precondition, not by the manifest assertion (lane C)

VER-001's mutation table row is right that the suite fails, but the failure is mute and occurs before any manifest exists; the derivation itself is pinned by the fixture's `deepStrictEqual` against the imported export, which lane C proved by hard-coding `controlLog` in `release.mjs` and watching the assertion fail with `expected: { min: 7, max: 7 }`. Likewise the missing-constants refusal is pinned by a mute exact-message grep. **Disclosed** so the mutation table is read correctly; no change.

### 13. Smaller consequences recorded without action

- The anchored classifier makes the main-checkout release-evidence guard stricter than main for nested build-shaped ignored paths (`a/dist/x`, `corpus/dist/y`); named by criterion 2's anchoring; the main checkout holds none such today.
- `worktree.mjs` keeps an unused `readFileSync` import after the `mainHasReleaseCommand` branch was deleted, and `shellQuote` is now defined identically in `worktree.mjs` and `resume.mjs`; tidy candidates for the next scripts order, contrary in spirit but not in letter to criterion 1's named list.
- `harnessEvidence` still swallows an unreadable or malformed `environment.json` as "no recorded versions" (pre-existing); `resume release-close` falls back to the subject path when the main worktree cannot be resolved, which then fails safe at `release.mjs`'s working-directory check; the `finish` gate surfaces raw git text when the subject `HEAD` lacks a control log (fail-safe, no fixture); the two control-log readers still differ on interior blank lines (pre-existing); the manifest's `controlLog` range comes from the executing tool's sibling `resume.mjs` rather than the released root's copy, which coincide after the fast-forward and is what criterion 5 asks for.
- The `09-audit-resilience-privacy.md` additions name "an opinion cohort" without a candidate label inside a linked, `planned` section; the concept is defined as candidate on every other surface and the sibling isolated-episodes bullet is likewise unlabelled; a wording edit would change a linked section's bytes and cascade into a lock refresh, so it is left as is.
- The `ensureClean` and authority-path refusal texts changed wording (now naming the first dirty entry and the work-order id); no suite asserted the old strings.
- `test-resume.sh`'s one successful `harness-readback` attestation moved from the newest fixture version to the older one to satisfy criterion 9; the newest version's success is covered via `operator-attested`.
- VER-001 carries one absolute operator home path in its instrument disclosure, matching the practice of six merged reports and the disposition WO-007's VER-002 recorded; VER-001 is immutable and is committed as written.

### 14. The executor completed the planner-reserved release classification (the activation-preflight gap, seventh occurrence)

The activated text left the H1 at "version assigned at activation" and the classification at "assigned by the planner at activation"; the activation helper retained that placeholder, and the executor, under the operator's `resume: next` dispatch, rewrote both to `v0.4.1` with `@dotln/kernel` `0.2.1`, disclosing the forward-correction in place without touching the append-only activation event. VER-001 accepted the resulting versions without naming that a planner field was executor-edited. `v0.4.0` is the latest published tag locally and on origin, `v0.4.1` is the next patch strictly above it, the README block carries it, the kernel's `src/` changed and its version differs while the compiler's and skeleton's did not and theirs do not, and `check-surfaces` proves all four rules; no criterion turns on the choice and the order's own text called the expected class a patch. **Ratified**, as WO-024, WO-025, WO-019, WO-027, WO-017, and WO-008 FINAL-001 each ratified the same correction before it.

### 15. What could not be verified from the tree

The scope-discipline sentence "one isolated, evidenced change stage per numbered deliverable" describes the executor's working method; the repository records checkpoints per lifecycle transition, not per deliverable, and no per-deliverable commits exist by design, so this review records the sentence as unverifiable rather than met. The receipt's intake byte-comparison and backup are likewise operator-side.

## Corrections applied by this review

Four corrections, all in the documentation lane `docs/final-reviews/README.md` grants, proven contained by diffing against checkpoint 5 (15 insertions and 12 deletions across four files, of which the map's nine re-padded table lines are Prettier's) and followed by a re-run of the checks they could affect: the full `npm test` (which runs the pinned `prettier --check` and `check-publication.mjs` against the real repository), `git diff --check`, and `check-surfaces`, all clean.

1. **`docs/publication/audience-status-index.md`:** the row for `03-architecture.md#candidate--opinion-cohorts-and-sealed-adjudication` moved from after the session-lifecycle row to directly after the agentic-communication-core row, its source position; label, audience tags, and status unchanged; both edition locks byte-identical before and after, as `check-publication.mjs --print-locks` confirms (VER-001 F3).
2. **`docs/README.md`:** `npm run resume -- status --json` becomes `npm run resume --silent -- status --json`, matching the execution guide and the playbook (VER-001 N1).
3. **`docs/releases/README.md`:** one sentence after the `validate` command stating that it reads the built `@dotln/kernel` exports and needs `npm run build` or a prior evidence run first (adjudication 6).
4. **`docs/planning/work-order-map.md`:** the snapshot line, the slot bullet, and the WO-018 row's evidence and disposition cells now record VER-001 pass, FINAL-001 pass, the reviewed state committed and awaiting operator merge, and the separately authorized release close that tags `v0.4.1`; Prettier re-padded the table. No other row changed (adjudication 8).

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet, no guard was lost, and no behavioral or evidence defect was found in the deliverable that any criterion covers. The items below are recorded and not scored.

1. **Symlinked absolute invocation of `resume.mjs` is a silent no-op** (adjudication 1): deferred to a follow-on with the one-line `realpathSync` repair and a symlink fixture.
2. **The discovery record's vocabulary does not declare `operator-attested`** (adjudication 2): deferred; only the `labels`-append repair is behavior-neutral, and it dates a WO-027 observation.
3. **Two consequences of criterion 5 that no criterion names** (adjudications 5 and 6): the equal-tag evidence rerun is blessed; the `validate` precondition is documented; a rebuild-or-refuse `validate` is a code candidate.
4. **The intake precedence term lacks a destructive-side fixture of its own** (adjudication 9): optional fixture candidate.
5. **Residual source-text derivation** (adjudication 10): the event-envelope schema version; candidate.
6. **Drift warning not relayed by `release.mjs`** (adjudication 11): candidate.
7. **The activation-preflight gap is now six orders long** (WO-024, WO-025, WO-019, WO-027, WO-017, WO-008, and WO-018): each activated with the planner's placeholder in the H1 and each executor forward-corrected it. For the operator's planning pass, not for this review.
8. **The executor's model and effort live in the control log and the verifier's report, not in a committed executor artifact.** The `ImplementationReady` event records them and VER-001 quotes them; that is the WO-019 mechanism working, but there is still no executor-authored repository artifact for a code order. Recorded, as WO-013, WO-015, WO-024, WO-025, and WO-017 recorded it.
9. **Tidy candidates for the next scripts order** (adjudication 13): the unused `readFileSync` import in `worktree.mjs`, the duplicated `shellQuote`, the swallowed main-worktree resolution failure in `resume release-close`, the unrelayed drift warning, and the two suite fixtures adjudications 9 and 13 name (an intake `.DS_Store` on the destructive side; a `status.showUntrackedFiles=no` case for the strict clean gate).

### Candidates considered and not sustained

- _Criterion 3 is unmet because `worktree.mjs` folds committed bytes instead of reading `status --json`._ Not sustained: the criterion's purpose is structured fields with no projection parse, both scripts consume the exported projection, and the committed-bytes form is what defeats the forged-projection fixture.
- _The `finish` invocation-root change loses the "only main's helper may finish" guard._ Not sustained: adjudication 4; the guard moved from script residence to working directory, every destructive-path gate is unchanged in order, and the change is what lets the reviewed classifier govern the close.
- _The inverted rerun assertion violates scope discipline._ Not sustained as a defect: adjudication 5; it is a required consequence of criterion 5, disclosed by the receipt and the releases README, and loses no refusal.
- _The kernel should have taken a patch for a constant, not a component bump at all._ Not sustained: `CADENCE_KINDS` is new public surface and `check-surfaces` requires a different version when `src/` changes; `0.2.1` is the honest signal.
- _The 09 audit additions present the cohort as current behavior._ Not sustained: they describe audit-field policy for a concept every other surface labels candidate, and no reader of resume control is told it exists.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step returned to the operator.** `gh` in this sandbox fails before its root command exists (`failed to read configuration: … operation not permitted`), so `npm run worktree -- publish` refuses at its `gh` preflight after the committed surface check and body validation and before any push, fail closed, as for WO-006, WO-007, WO-013, WO-024, WO-025, WO-019, WO-027, WO-017, and WO-008. The reviewed commit is made here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation, and notes parse run against the exact commit; it refuses at the `gh` preflight and pushes nothing. The operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release in the new two-form shape.** After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute command the publisher prints: change directory into the main checkout and run this worktree's `scripts/release.mjs close WO-018 --publish` with node, outside the sandbox and where `gh` is authenticated. Because `v0.4.1` is strictly above `v0.4.0`, the reviewed subject helpers finish this worktree through their own classifier and closed-state gate, fast-forward main, run `npm ci` and the evidence gate, assemble the five-section edition from these notes, create and push the annotated `v0.4.1` tag, and create the matching GitHub Release. If Release creation fails after the tag push, the rerun is `npm run release -- close WO-018 --publish` from updated main, and it reproduces evidence before validating the equal tag.
- **Attribution:** the reviewed commit carries a plain subject and no attribution trailer, per the operator's standing rule (`~/.claude/CLAUDE.md`) and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins, as WO-017's review recorded.
- **Instruction conflict flagged, not acted on:** the session label under which this review ran describes dynamic workflows plus reasoning effort; only the latter is an effort value, and it is attested through the operator's recorded note rather than observed by the harness. The source label says so.
- **Ledger duty:** discharged by the executor's six breakout entries and the `program`-field candidate; this review adds no ledger entry because every open item above is either already recorded by VER-001's checklist or is a tooling tidy-up that the next scripts order should pick up from this report.

## Proposed PR

- **Title:** `:recycle: WO-018: control-plane consolidation; one helper library, machine-readable control state, the manifest's cadence grammar from the built kernel, and intake protected through closeout; v0.4.1`, the `:recycle:` precedent being PR #24 for WO-016's reactor consolidation, chosen because the order's objective is consolidation and evidence correction with no new runtime capability.
- **Body:** `docs/final-reviews/WO-018/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commit (it also makes the map's "FINAL-001 pass" cell true); the reviewed state is committed with a plain subject. The operator then runs, from this worktree outside the sandbox:

```bash
npm run worktree -- publish WO-018 --title ':recycle: WO-018: control-plane consolidation; one helper library, machine-readable control state, the manifest'"'"'s cadence grammar from the built kernel, and intake protected through closeout; v0.4.1' --body-file docs/final-reviews/WO-018/PR.md
```

The helper runs `check-surfaces --committed WO-018` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-018`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff in the absolute subject-helper form. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, the operator runs that printed command with the main checkout as the working directory; the expected outcome is the annotated `v0.4.1` tag and its GitHub Release.

## Method

Single reviewer, single session, with the reading and re-verification of the change set delegated to ten background review lanes and their refuters as described in §Independent review lanes and adversarial refutation; the session was interrupted once by a harness restart and resumed from the workflow's journal, and at the operator's direction the refutation stage was reduced from three refuters per finding to one batch refuter per lane, which changed the count of agents and not the lenses applied. Read order for the reviewer: the execution guide's dispatch, closeout, discipline, and model-specific sections, the control projection and log, the work order in full including both receipts, VER-001 in full, the final-review and verification READMEs, the WO-017 and WO-008 final reviews with their PR bodies and notes for the package shape and publish precedent, then the diffs of every changed file (the four lifecycle scripts and both library files in full, the kernel source and test changes, the manifests and lockfile, the playbook, execution guide, harness-security, releases, corpus, and package READMEs, the six promoted product documents, the ledger, the map, the publication index and editions, the discovery record, and the work order), with the publish, surface, notes, and body gates read from `worktree.mjs`, `release.mjs`, `release-notes.mjs`, and `github-body.mjs` rather than inferred. The ignored intake was not read. Subject integrity was established through git objects. Evidence was re-established first-hand: two full `npm test` runs, the static, surface, and publication checks, the helper-singleton and projection-parse greps, the line counts, the actor inventory by object comparison, the main-checkout release-guard scan by path name, and the clean-room scan; the lanes reproduced the classifier probes, the pure-function fold and projection probes, the symlink no-op, the `showUntrackedFiles` semantics, the `tsc` exhaustiveness, the five shell suites in isolated copies, and the mutation drill. Authority used: four documentation corrections and the three package artifacts; no edit to any code, test, contract, schema, discovery field, or acceptance byte. The scratch trees lived in the session scratch root; one lane's copy recipe left a self-referential `node_modules/node_modules` symlink inside this worktree's ignored `node_modules`, which the reviewer unlinked (the link only, never through it) before the second `npm test`, with both control digests and the 47-entry status unchanged by that cleanup. Nothing was restored from a checkpoint, no destructive git command was used, and no remote, tag, or `gh` mutation was performed; the direct `gh` invocation was `gh --version`, which failed before its root command existed, and the publish helper's own `gh` preflight after the reviewed commit fails the same way, leaving the push and the PR to the operator.
