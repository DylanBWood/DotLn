## What this merges

WO-038, the `v0.13.2` patch above the published `v0.13.1`: the 2026-09-06 license decision landed in every executable and human surface. The root and all three workspace manifests declare `license: Apache-2.0` and `private: true` (the kernel gains the flag the legal record had named as the open gap), each carries a `prepublishOnly` command that refuses `npm publish` with `DOTLN_PACKAGE_PUBLISH_REFUSED`, and the lockfile records the same licenses. A new `license-surfaces` preflight joins `check-surfaces`, `worktree publish`, `release close`, and `npm test`: it requires those fields and the exact guard on every manifest, refuses a `publishConfig`, pins the current workspace layout, checks the SHA-256 of `LICENSE`, `LICENSE-docs`, and `NOTICE` against both the files and the pins in `docs/LEGAL.md`, and runs offline npm dry runs against isolated copies of the manifests, counting only the specific refusal exit and marker. `worktree publish` also checks every commit in `origin/main..HEAD`, including commits reached through merges, for a Developer Certificate of Origin 1.1 `Signed-off-by` trailer matching the raw author, with the operator's exact pinned public identity exempt, and refuses before the GitHub CLI preflight, the push, or the PR. `CONTRIBUTING.md` states the outbound licenses, the DCO rule, the operator exemption, the clean-room boundary, and the review route. README, `docs/LEGAL.md`, the execution guide, the roadmap, the planning map, the ledger, and WO-033's export default agree with the manifests. Kernel, compiler, and skeleton sources are byte-identical to `v0.13.1`; no dependency, hook, or setting changes; no package is published.

- **Manifests and npm refusal (criterion 1):** all four manifests carry the fields; `npm publish --dry-run` from each real workspace directory exits 1 with exactly one marker line, reproduced independently by the verifier and by this review; npm 10.8.0's `publish --dry-run` returns success for a private package on its own, which is why the guard exists, and its shipped source shows the real publish path still refuses a private manifest inside `libnpmpublish` regardless of `--ignore-scripts`.
- **Preflight failure paths (criterion 2):** on a fixture built from the real license files, a missing `license`, a missing `private`, a changed `LICENSE` hash, a non-boolean `private`, a wrong-case SPDX identifier, and an added workspace pattern each fail with observed and expected values; the checker refuses to run the dry run whenever a manifest rule fails; a missing npm, an unrelated exit 1, and the marker with exit 0 do not count as refusals; the repository passes all 28 rules.
- **Sign-off enforcement (criterion 3):** the worktree fixture refuses an unsigned outside commit before `gh is required`, the push, or the PR, and accepts an operator commit without a sign-off; the verifier's twelve-scenario fixture covers matching and lowercase trailers, tabs and trailing whitespace, multiple trailers, a colon in the author name, a sign-off in the subject line, the operator's other public identity, an unsigned side branch behind an operator merge, and a missing base ref.
- **Documents agree (criterion 4):** `CONTRIBUTING.md` exists with the DCO rule, both licenses, no CLA, and all nine link targets resolving; README §Sources, license, and legal posture, `docs/LEGAL.md` §Current state and its decision gates, and the four manifests state the same posture.
- **Gate and scope (criterion 5):** `npm test` exit 0 twice in this review, `git diff --check` clean, the lockfile changed only by four `license` entries, no Git hook, and no `.claude/` or Git-config change.
- **Clean-room read (criterion 6):** this review read `CONTRIBUTING.md` against `CLAUDE.md`'s clean-room floor and records the result in FINAL-001: the document names the floor as a rule for contributors, carries no employer-derived term, host, API shape, or identifier, and introduces only the operator's already-public author identity as the pinned exemption.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` on the verified tree and again on the reviewed tree: Prettier clean, the nine license and contribution tests with two real npm refusals, every shell suite including the release suite's real-npm license fixtures and the worktree suite's sign-off refusal before any remote effect, `check-publication.mjs` with both audience locks current, `index --check` current, `tsc -b --force`, 281 of 281 package tests, 8 of 8 identity-corpus tests, the artifact-identity, verification-evidence, and feedback-evidence checks (the last rerunning all twenty causal subprocesses against the refreshed edition), and 21 of 21 mutation self-tests, exit 0. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.13.2; expected v0.13.2`, `src unchanged` for all three components, the body profile over this package's `PR.md` and `RELEASE-NOTES.md`, and all 28 `license-surfaces` rules.
- Direct probes: four `npm publish --dry-run` calls from the real workspace directories, each exit 1 with one marker line; `shasum -a 256` over the three license files equal to the legal record's pins and to the checker's constants; the nine `CONTRIBUTING.md` link targets and both in-repository anchors resolved by script; `packages/*/src` byte-identical to `v0.13.1` by `git diff`; no non-sample hook in the shared hooks directory and no `core.hooksPath`; npm 10.8.0's `lib/commands/publish.js` and `libnpmpublish` 9.0.8 read to settle where the private-package refusal runs.
- Subject integrity: a temporary-index tree of the working tree differs from the implementation-ready checkpoint only in the control projections, the regenerated index, and the added VER-001; from the verdict and final-review checkpoints only in the control projections and the index; every checkpoint has `main` (`01c2e32`, also `origin/main`) as its sole parent; `git log main..wo-038` was empty before the reviewed series.
- Actors: executor Codex CLI 0.153.4, GPT-6 Astra, `max`, operator-attested (`codex --version` reports 0.153.4 here); verifier Claude Code 2.1.263, `claude-opus-5[1m]`, `max`, self-reported, with the VER-001 header equal to its control event; reviewer Claude Code 2.1.263, `claude-fable-5-1`, `max`, self-reported; both `max` values exceed the declared `xhigh+` floors and the projection reports no drift.

## Verification trail

- `docs/verifications/WO-038/VER-001.md`: pass; all five executor criteria re-established with the verifier's own commands and fixtures, the sixth correctly left to final review; one disclosure (the checkers are the deliverable, with compensating first-hand checks) and six non-blocking observations.
- `docs/final-reviews/WO-038/FINAL-001.md`: pass; the verifier's observations adjudicated with reasons, npm's private-package ordering settled from its source, the second-identity limitation confirmed against `main`'s author history, the clean-room read recorded, one cosmetic correction applied and re-checked, the commit series and method disclosed.

## Known open items

- **The sign-off exemption is pinned to one of the operator's two public author identities** (VER-001 O2). Merge commits created in the GitHub UI are authored under the other identity, so GitHub's "Update branch" on an open work-order PR would put a commit on the branch that `worktree publish` refuses until it is rebased away or signed. Merging into `main` is unaffected. The check's base is the local `origin/main` remote-tracking ref, which the ordinary fetch keeps current. A follow-on may pin a second exact identity if the operator wants both exempt.
- **The sign-off email comparison is case-sensitive and the refusal names no identity** (VER-001 O3): a hand-typed trailer differing only in letter case is refused without a hint; `git commit -s` never produces one. Diagnosability nit.
- **The evidence-recovery wrapper has no automated test** (VER-001 O4): `docs/evidence/WO-038/feedback-audit-recovery.mjs` narrows only the verifier envelope's summary and passes result bytes through unchanged, confirmed by reading; the nominated feedback-refresh helper in the planning map is its tested replacement.
- **The verifier's summary bound is enforced by the host but absent from its prompt and schema** (VER-001 O5): outside this order; a ledger candidate and a planning-map candidate carry the fix.
- **`private: true` as a real-publish barrier is documented from npm's source, not from an executed publish**: an authenticated publish is out of bounds; the evidenced defenses are the private flag, the absent `publishConfig`, the exact lifecycle refusal, and the preflight that observes all three.
- **Publish and release close.** `npm run worktree -- publish WO-038` runs the committed surface check, the body and notes validation, and the sign-off check before any push, then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses there the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.13.2` is strictly above `v0.13.1`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.13.2` tag, and creates the matching GitHub Release.
