## What this merges

WO-042, the `v0.16.0` minor above the published `v0.15.0`. Until now a linked support could grant a compiled build authority its own active mechanic denied: a support's `CompositionClaim` chose its own precedence layer, `safety-invariants` included, and an `allow` winner was written straight into the emitted `AuthorityEnvelope` and WorkOrder operation lists. WO-008's VER-001 finding F2 reproduced exactly that on 2026-09-03, and since WO-039 lowered the envelope into this repository's live `PreToolUse` hook the path reached real permission decisions. Compiler `0.8.0` closes it. A linked support may now only narrow: a `deny` claim is legal at all nine layers, an `allow` claim is accepted only to restore an allowance the active already holds in both base lists, and a `permission-guard` emission obeys the same floor for the effects and operations it contributes. Widening needs an explicit `AuthorityGrant` that names its `grantId`, `version`, `grantedBy`, exact effects and operations, `repo` and `reason`, and that a host-owned registry outside the submitted graph independently admits. The tooltip's GRANTS and RESTRICTIONS are now a projection of the effective envelope rather than authored prose, with the authored strings kept under a labeled `AUTHORED NOTES (non-enforcing)` section. Compiler `0.8.0`, skeleton `0.14.0`, console `0.1.2`; kernel `0.2.1`, the compiled-program contract `"1"`, `harness-v1`, `feedback-v1`, `verification-v1`, lifecycle legality and the event schema are unchanged. No dependency is added.

The operator's 2026-09-08 ideation breakout, recorded in [ideation.md](../../evidence/WO-042/ideation.md), expands the same order with five independently equipped Contributor executor supports, an execution-safe planning-continuation gate, and one bounded adjacent repair. Those supports are the reason the regenerated bundle carries a new build identity rather than only new version pins.

- **The monotone floor (criteria 1 and 2).** `authorityDiagnostics` refuses four widening paths with `AUTHORITY WIDENING`, each diagnostic naming the support, the claim or emission id, the effect and the base list it violates, and offering both `unequip-support` and the new `declare-authority-grant` correction: the VER-001 F2 graph allowing `repo.delete` at `safety-invariants`, an allow for an effect absent from the base allowed list at `hard-permissions`, an emission adding an allowed effect, and an emission adding an allowed operation. Narrowing is untouched: a support `deny` at each of the nine layers removes the effect from `allowedEffects` and the operation from `allowedOperations` and appends both to the denied lists, a higher-precedence `allow` restores only a base allowance and records the losing denial in the conflict trace, and the kernel's runtime `authorize` agrees with the compiled lists on every fixture. Precedence, commutativity, pipelines and the hard-conflict rule are unchanged, and `safety-invariants` remains a legal layer for a support's `deny`.
- **Grants with provenance, admitted by the host (criterion 3).** `LoadoutGraph` gains the optional additive `authorityGrants` collection under schema version 1: absent normalizes to `[]`, entries sort by `grantId`, and all three editable views encode and decode it and hash equal. A grant in a submitted graph carries no authority by itself. `CompilationEnvironment.authorityGrantRegistry` is a separate host-owned input, and a graph grant is admitted only when a registry entry matches its `grantId`, `version`, `grantedBy`, `effects`, `operations` and `repo`; anything else rejects with `AUTHORITY GRANT UNADMITTED`. The repository adapter `compileRegisteredLoadout` reads the committed `packages/skeleton/loadouts/grants.json` for `operator` and `registered-repository` provenance and the ignored `docs/control/local/authority-grants.json` for `host-policy`. The committed registry is empty, so this merge adds no authority to anyone. Admitted grants apply after claims, are the only mechanism that may add an allowed effect or remove an exact base denial, and are recorded in the program's optional `grants` field, in `trace.authorityGrants` with the registry hash, and in the harness manifest. An exact grant that would need an exception to a broader wildcard denial rejects as `SEMANTICS UNSUPPORTED` rather than silently widening other authority.
- **Saved builds keep their bytes (criterion 4).** Normalization omits `grants` and the grant trace when no grant applied, so Seiri `fnv1a64:9ca8d0229c6bd8db`, the Entropy Reducer `fnv1a64:c5ddbca75f1c4cee`, the saved Contributor `fnv1a64:06245f5c581212f1` and the plan refuter `fnv1a64:9e1fa511376ca7ce` retain their full program bytes; their artifact identities differ only in `compilerPackageVersion`. The frozen WO-003 trace oracle and the WO-029 CLI fixture are byte-identical.
- **The tooltip cannot lie about the envelope (criterion 5).** `projectAuthorityInspection` returns GRANTS as the effective allowed effects, annotating a granted effect with its grant id and provenance, and RESTRICTIONS as the effective denied effects; `renderCompiledDiff` prints those and moves the authored strings under `AUTHORED NOTES (non-enforcing)`. `program.inspection` itself is unchanged, so no hash moves. An authored restriction naming an exact allowed-effect id, or an authored grant naming an exact denied one, rejects with `INSPECTION CONTRADICTION`. VER-001's one finding was that ordinary Unicode prose punctuation did not delimit that token; the repair replaced the ASCII exclusion list with one Unicode boundary rule around a complete escaped literal id, and the rule is written into product 04.
- **The lowered bundle still agrees (criterion 6).** The recorded [bundle diff](../../evidence/WO-042/bundle-diff.json) compares in two stages. Against activation, the unequipped saved build's 24 generated surfaces differ only in compiler and skeleton pins, derived runtime and policy hashes and the manifest's empty grant provenance; eleven files and the marked `CLAUDE.md` block are byte-identical. The declared support overlay then changes only the two executor skills, support origins and derived build identities, leaving the envelope, settings, deny matchers, host behavior and every other role's procedure fixed. `lowerToHarness` keeps its envelope-equality refusal, and a fixture bundle lowered from the grant program carries the grant and registry provenance in its manifest.
- **The two mutation survivors are killed (criterion 7).** The tag half of link compatibility (`F-00001`) and equal-precedence rejection (`F-00002`) sit inside the validation code this order rewrites, so the order authorized the two negative tests they lacked. Reproduced from current source, both baselines pass 328/328 and each mutant fails exactly its named new test at 327 passing.
- **Operator breakout: five atomic executor supports.** Adjacent Repair, Decision Receipts, Follow-up Queue, Intent to Act and Operator Check-In are separate support facets on the Contributor's Clean Room active, each equippable and removable on its own. All 32 default-switch combinations compile, round-trip through all three views and produce 32 distinct identities; removing all five restores `fnv1a64:06245f5c581212f1`. They carry no claim, no permission emission, no hook and no extra episode, and Observation and Recommendation remain mutually exclusive alternatives to Intent to Act. The additive `role-procedure` harness adapter binds an equipped prompt support to the role that already carries its compiled fragment, so the origin is recorded on that skill instead of appearing as false missing-adapter residue. These are rung-7 instructions; equipment tests do not prove model judgment.
- **Operator breakout: the planning gate and one adjacent repair.** The last planning receipt hashed every byte of each order and every dated capability row, so this order's own required release assignment and capability reassessment made the gate refuse its own worktree. The receipt now stays bound to its original committed subject, and a separate continuation comparison judges HEAD and the workspace against it, admitting only an assigned release placeholder, an appended `## Execution record`, and appended dated reassessments of existing capability ids. Objectives, criteria, dependencies, non-goals, sequence, vision, roles and existing rows still require a matching refutation, in the workspace and in HEAD alike. Separately, the actor board's default collector selected a WO-039 verifier stream that no longer replays under compiler `0.8.0`; console `0.1.2` moves the default to the WO-042 `feedback-002` edition and the regression now requires both default self-host streams to replay and project.
- **Write-backs (criterion 8).** Product 02 gains §Authority grants and trusted admission and the corrected sentence about winning claims; 03 bounds step 3 of the composition system; 04 gains §Authority inspection projection and the normalization rule for the optional `grants` field; 05 gains the executor-support, modifier-switch, intent-queue and resource-policy models; 06 records the release boundary; 07 replaces the discipline bullet under the operator's explicit supersession and restates the planning gate; 10 records the acceptance narrowing under contract `"1"`. The README release block, two dated capability reassessment rows, the publication index rows and both edition locks, the ledger with its rejected alternatives, and the planning map's now-allocated candidate are all current.

## Evidence

Re-established first-hand at final review, not inherited:

- `npm test` exits 0 on the reviewed tree, and `git diff --check` exits 0. The package suite passes 328/328, console 18/18, and every lifecycle, worktree, publication, release, harness, planning-continuation, evidence-edition and mutation self-test completes.
- `node scripts/authority-evidence.mjs --check`, `node scripts/authority-mutation-evidence.mjs --check` and `npm run harness -- check` exit 0. The authority transcript reports four unchanged saved programs, four widening refusals, nine narrowing layers agreeing with runtime `authorize`, admitted and reverted grants, and 24 bundle comparisons.
- I compiled the widening, narrowing, grant and contradiction fixtures directly against a fresh build rather than through the order's own evidence generator, and re-derived the four saved semantic hashes independently.
- The branch has no commits ahead of `main` before this review, `main` has none ahead of it, and no integration was required. The kernel package has no diff, `package-lock.json` changes only by the three workspace versions and the skeleton's compiler pin, and no lint, type or formatter suppression appears in any added line.
- This session ran under the bundle it reviews: the generated `UserPromptSubmit` hook resolved `resume: final review` to the reviewer role and injected the selected order path, and the generated `PreToolUse` guard refused a command that reached outside this worktree. That refusal is recorded rather than hidden, and this report is not evidence for the recorded live smokes, which separate sessions produced.

## Verification trail

- `VER-001` (fail): one moderate finding, F1, that Unicode prose punctuation bypassed the exact-token contradiction guard. Criteria 1 through 4 and 6 through 9 were met.
- Bounded repair: [repair-001.md](../../evidence/WO-042/repair-001.md) records the ten new directional Unicode fixtures failing before the change and passing after, the renewed authority, mutation, console and publication checks, and the refreshed edition locks.
- `VER-002` (pass): F1 resolved, all nine criteria and the operator-expanded duties met, no new finding.
- `FINAL-001` (pass): [the report](FINAL-001.md) records the acceptance sequence re-established here, the observations carried forward, and this review's own limits.

## Limits carried forward

- The compiler trusts the supplied active mechanic and the host registry. The registry hash is an equality receipt, not authentication; a grant's `reason` is explanatory text, not an admission field; and this is not hostile same-user isolation. Signatures and registered-repository authority profiles are WO-033's, not this order's.
- `compileRegisteredLoadout` is the only wired registry adapter, so the skeleton CLI, console and reactor compile paths still supply no registry and therefore reject any graph grant. That is fail-closed and matches the empty committed registry, but a future consumer of grants will need those paths wired.
- The contradiction guard compares explicit literal effect ids under the boundary rule product 04 states. It does not infer natural-language contradictions, and wildcard or time-scoped grants are out of scope for v1.
- The five executor supports are role instructions at rung 7. Their equipment, removal and projection are deterministic and tested; the judgment they ask for is not enforced. The follow-up queue is ignored worktree-local state whose chat and inbox observations are explicitly actor-attested.
- The committed harness profile still declares Claude Code `2.1.263`, which is the version the recorded live smokes used; this reviewing session ran `2.1.265`. Refreshing the observed profile and the recorded effort evidence is separate discovery work and is nominated rather than folded in here.
