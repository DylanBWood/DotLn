# FINAL-001 — WO-042 Authority provenance and monotone envelopes (v0.16.0)

**Verdict: pass.** All nine acceptance criteria and the operator-expanded execution duties are met, and I re-established the load-bearing ones first-hand rather than inheriting them from the verification sequence. VER-001's single finding is closed: the three Unicode punctuation cases it reproduced as accepted now reject, and the four documented negatives still compile. No finding blocks this merge. The observations below are recorded for their own sake and none of them is a defect in this order.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.265","model":"claude-opus-5[1m]","effort":"unknown","source":"self-reported"}

This session's environment exposes `AI_AGENT=claude-code_2-1-265_agent` and `CLAUDE_CODE_EXECPATH=.../versions/2.1.265`, so the running harness is Claude Code `2.1.265`; `claude --version` resolves to the newer installed `2.1.266`. The environment also exposes `CLAUDE_EFFORT=max`, which is this session's selected effort. `docs/discovery/environment.json` records observed Claude Code versions `2.1.259`, `2.1.260`, `2.1.261` and `2.1.263` only, so the lifecycle gate has no selector or readback evidence that would let `max` be attested for `2.1.265`, and the machine-readable effort therefore stays `unknown` rather than borrowing another version's row. Recording that discovery evidence is separate work and a reviewer should not add it in order to raise its own attestation. WO-042 requires reviewer effort `any`, so no declared minimum is at issue. No account label was supplied.

## Subject and instrument

- Authority: [WO-042](../../work-orders/WO-042-authority-provenance.md) with its cited sections and appended execution record, the operator-authorized [ideation breakout](../../evidence/WO-042/ideation.md), and the [VER-001 F1 repair record](../../evidence/WO-042/repair-001.md).
- Subject: the uncommitted tree on branch `wo-042` at `HEAD` `4ead1d0ea1f1fea049f8369d99c852da602f9278`. Physical cwd and Git root agree at `/Users/dylanwood/Projects/DotLn-wo042`, which Git registers as one worktree of `/Users/dylanwood/Projects/DotLn/.git`.
- Integration: `main` is also `4ead1d0`. `git log main..HEAD` and `git log HEAD..main` are both empty, so no branch commit precedes this review and no integration was required or performed.
- Allocation: `FinalReviewRequested` at checkpoint `c679916d9d2bc7a1e2151a5947629e7a249fe669`, `refs/dotln/checkpoint/WO-042/9`. Status selected WO-042 in `final-review` with `final-review-result` as its sole legal next action.
- Verification sequence: [VER-001](../../verifications/WO-042/VER-001.md) (fail, one moderate finding), a bounded repair recorded as `RepairRequested`/`RepairCompleted`, then [VER-002](../../verifications/WO-042/VER-002.md) (pass). Both reports remain immutable.
- Actor independence: the control log shows implementation, repair, VER-001 and VER-002 all attested to Codex CLI `0.153.4`, `gpt-6-astra`, effort `max`, operator-attested. VER-002 disclosed that as a limit on actor-level independence. This review is the first episode on a different harness and model, which is the one thing it can add that another Codex episode could not.
- Instrument disclosure: this session runs under the bundle it reviews. The generated `UserPromptSubmit` hook resolved `resume: final review` to the reviewer role and injected the selected order path, and the generated `PreToolUse` guard refused one Bash command whose working directory reached the main checkout outside this worktree. That refusal is recorded rather than hidden, and I re-ran the check with an absolute path from inside the worktree instead of weakening the guard. This report is direct evidence that the self-host works and is not evidence for the recorded live smokes, which separate sessions produced.

## Acceptance re-established

| Criterion | What I checked and observed |
| --- | --- |
| 1 — widening rejects | I compiled the VER-001 F2 shape directly against a fresh `packages/compiler/dist`: a linked support allowing `repo.delete` at `safety-invariants` while the active denies it returns `ok: false` with two `AUTHORITY WIDENING` diagnostics, one per base list, reading `support "s1" claim "s1.claim" allows "repo.delete" outside active "seiri" base authorityEnvelope.allowedEffects (absent or base-denied)`. The same allow at `hard-permissions` rejects identically. The recorded transcript adds the two emission paths. Every diagnostic carries `unequip-support` and `declare-authority-grant`, and the render spells the second one out as declaring a host-admitted grant *and* removing the widening claim or emission, so the correction is a complete route rather than a half one. A grant does not launder a widening support, because the floor is checked against the active's base lists, which grants never touch. |
| 2 — narrowing is accepted | A support `deny` of `repo.inspect` at `safety-invariants` compiles and moves the effect out of `allowedEffects` into `deniedEffects`, leaving `["repo.read"]` allowed. The transcript covers all nine layers, the restoration case where a higher-precedence allow returns only a base allowance while the losing denial stays in the conflict trace, and emission denials. Runtime `authorize` agrees with the compiled lists on every one. `applyAuthorityClaims` now also drops any effect present in both merged base lists before claims run, so a denial in the union cannot be outvoted by construction order. |
| 3 — grants widen with admitted provenance | Compiled directly: the grant fixture alone rejects with `AUTHORITY GRANT UNADMITTED`; supplying a matching `authorityGrantRegistry` compiles it, adds `repo.delete` to `allowedEffects`, records `fixture.delete/operator` in `program.grants` and a `registryHash` of `fnv1a64:54ee347b5b9d9531` in the trace. Changing one admission field (`version` 1 to 2) rejects; a foreign `repo` rejects with both `AUTHORITY GRANT UNADMITTED` and `INVALID GRAPH`. Malformed fields, duplicate ids, the three-view round-trip, removal restoring the pre-grant hash, and the wildcard-denial refusal are covered by the compiler and host fixtures. The committed registry `packages/skeleton/loadouts/grants.json` is `[]`, so this merge adds no authority to anyone. |
| 4 — hashes hold | Re-derived independently at compiler `0.8.0`: Seiri is `fnv1a64:9ca8d0229c6bd8db` and the saved Contributor is `fnv1a64:06245f5c581212f1`. A grant-free program carries no `grants` key and no `trace.authorityGrants`, which is the mechanism that keeps those bytes fixed. The Entropy Reducer, the plan refuter, the WO-003 trace oracle and the WO-029 CLI fixture are re-checked against the recorded activation baseline by `authority-evidence.mjs --check` and the artifact-identity edition inside the gate. |
| 5 — the tooltip projects the envelope | `projectAuthorityInspection` on the Seiri program returns `{"grants":["repo.inspect"],"restrictions":["repo.delete","repo.write"]}`, which is the effective envelope, and the rendered diff carries the authored strings under `AUTHORED NOTES (non-enforcing)` with `program.inspection` unchanged. VER-001's finding is closed: `"Never repo.inspect—ever."`, `"…–ever."` and `"……ever."` now reject with `INSPECTION CONTRADICTION`, as do curly-quote and possessive forms in both authored-note directions, while `repo.inspectExtra`, `prefixrepo.inspect`, `repo.inspect.child` and `repo.inspect:detail` still compile. Product 04 states the boundary rule those cases implement. |
| 6 — the lowered bundle agrees | I emitted the bundle independently into a fresh temporary directory with `node scripts/harness.mjs emit --out DIR`: 24 generated surfaces, `.agents` and `CLAUDE.md` byte-identical to the installation and `.claude` identical apart from the untracked `.cc-writes` runtime directory. The recorded [bundle diff](../../evidence/WO-042/bundle-diff.json) classifies both stages: against activation the unequipped saved build differs only in compiler and skeleton pins, derived runtime and policy hashes and the manifest's empty grant provenance, with eleven files and the marked `CLAUDE.md` block byte-identical; the declared support overlay then changes only the two executor skills, support origins and derived identities. `harness check` passes inside the gate, the WO-039 hook fixtures pass unchanged, and `lowerToHarness` keeps its envelope-equality refusal. |
| 7 — the mutation survivors are killed | The order authorized these two negative tests because both sites are inside the validation code it rewrites. The current transcript and source-bound summary record two unmutated baselines at 328/328 with each mutant failing exactly its named new test at 327 passing, and `authority-mutation-evidence.mjs --check` validates that record against the current executable source inside the gate. The historical WO-108 campaign is unchanged. |
| 8 — write-backs land | Present and consistent: product 02 §Authority grants and trusted admission plus the corrected sentence about winning claims and the AuthorityEnvelope provenance paragraph; 03 step 3 of the composition system; 04 §Authority inspection projection and the optional-`grants` normalization rule; 05 the executor-support, modifier-switch, intent-queue and orchestration/quality models; 06 the release boundary; 07 the replaced discipline bullet and the restated planning gate; 10 the acceptance narrowing under contract `"1"`. Two dated capability reassessment rows, the README release sentence, the publication index rows and both refreshed edition locks, three dated ledger sections carrying adopted *and* rejected options, the planning map's candidate marked allocated, and the work order's appended execution record are all in place. |
| 9 — gate and boundaries | `npm test` exits 0 on the reviewed tree; `git diff --check` exits 0. `packages/kernel` has no diff, `package-lock.json` changes only by the three workspace versions and the skeleton's compiler pin, no dependency is added, and no lint, type or formatter suppression appears in any added line. Fresh artifact-identity, verification and feedback revision 002 editions exist under `docs/evidence/WO-042/`. I ran `npm run terms -- check` over the twelve changed product, planning, publication and evidence prose surfaces plus this review's two publication artifacts: exit 0, `local-terms list: unavailable`, which is the required honest presence report. |

## Operator-expanded execution duties

The [breakout receipt](../../evidence/WO-042/ideation.md) is explicit operator authority, and the implementation follows the correction that mattered most: repair preference and decision paperwork stay two supports, not one. Adjacent Repair, Decision Receipts, Follow-up Queue, Intent to Act and Operator Check-In are separate facets with `claims: []`, `authorityChanges: []` and no emission other than `prompt-fragment`, so none of them can move the envelope. The equipment fixture proves all 32 default-switch combinations compile to 32 distinct identities, keep the envelope and WorkOrder identical to the unequipped build, round-trip through all three editable views, are order-independent, leave every non-executor role byte-identical, and restore `fnv1a64:06245f5c581212f1` when all five are switched off. Observation and Recommendation remain mutually exclusive alternatives to Intent to Act. The generated executor skill carries exactly the equipped fragments and the repair modifier, which I confirmed in the installed `.claude/` and `.agents/` skills.

The additive `role-procedure` adapter is the right shape for that projection. `lowerToHarness` admits it only when the named facet is an equipped `prompt-fragment` support in the component manifest, its text is one of the compiled `promptFragments`, and the named role's procedure already contains that text; otherwise it refuses. It supplies no hook, no effect and no settings entry, so the alternative — leaving those supports in the global missing-adapter residue, or duplicating their text into the shared instruction block — is correctly rejected.

The planning-gate repair preserves the distinction it exists to protect. The historical receipt stays bound to its own committed subject and is never rewritten; `checkPlanContinuation` then compares HEAD and the workspace against it and classifies each difference, admitting only an assigned release placeholder matching the exact activation form, an appended `## Execution record` constrained in its headings and bold-field lines, and appended dated reassessments whose work order is in the reviewed sequence and whose capability ids already exist. Its negative fixtures reject objective, criteria, non-goal, effort, heading, capability-id, date, malformed-version and existing-row drift, and one case proves a clean-looking workspace cannot hide drift already committed to HEAD.

The queued adjacent repair is in scope under the operator's correction and is bounded. The default collector selected a WO-039 verifier stream that compiler `0.8.0` refuses to replay; console `0.1.2` moves the default to the WO-042 `feedback-002` edition and the read-only collection test now requires both default self-host streams to replay and project while missing stores still report unavailable. The follow-up queue itself is ignored worktree-local state under `docs/control/local/`, gated to the executor and fixer phases, validated append-only, path-contained and symlink-refusing, and its chat and inbox observations are explicitly actor-attested rather than presented as host observations.

## Observations, not findings

- **The registry adapter is wired only where the tests use it.** `compileRegisteredLoadout` is the sole caller that supplies `authorityGrantRegistry`; the skeleton CLI, the console Builds panel, the reactor and the artifact-identity path all compile without one, so any graph grant they meet rejects as unadmitted. That is fail-closed and consistent with an empty committed registry and with WO-033 owning the first real consumer, but the wiring will be needed before grants are usable outside fixtures. Recorded, not folded in.
- **The context measurement's method changed and says so.** `measureHarnessContext` now gives both sides the current shared task files while keeping only the activation instruction and execution guide frozen, and the stored `method` string states exactly that. The change is necessary because the reviewer role's directed publication section moved, and the check still requires every row to be strictly lower. Disclosed rather than silent, which is the right handling.
- **The declared Claude profile is behind the installed CLI.** `contributorProfiles` declares `claude-code-2.1.263` and `harness-evidence.mjs` asserts that string on the recorded live smokes. Both are unchanged by this order — they come from WO-039 — but the installed CLI is now `2.1.266` and this session ran `2.1.265`, so the next live smoke will need the observed profile and `docs/discovery/environment.json` refreshed. Separately, this session exposes `CLAUDE_EFFORT`, an effort readback channel the discovery record does not describe for any version. Both belong in a bounded discovery pass, not here.
- **Effect ids are dotted, and the boundary rule assumes it.** The documented rule makes all Unicode punctuation a token boundary, so an effect id containing a hyphen or underscore would also be delimited by those characters inside a longer compound word. No current effect id has that shape and the behavior matches the promoted contract, so this is a property of the rule rather than a departure from it.
- **The generated work-order index needed regenerating after my own allocation.** `FinalReviewRequested` moved WO-042 from `verified` to `final-review`, which the index projects but lifecycle commands do not regenerate, so the first gate run failed on `docs/work-orders/README.md is stale at line 3`. I regenerated it and re-ran the gate green. This is the documented reviewer procedure, not a defect in the subject.
- **Provenance verified without reading intake.** I hashed the five referenced captures in the main checkout's ignored `docs/intake/notes/` without reading their contents: all five match the SHA-256 values recorded in the breakout receipt and the ledger, including the planning dispatch's `b17d1479741c2b97824cb70b196b1b1d0ce3602bbb0abb9ac7353441c1e82c0b`. No intake bytes appear anywhere in the diff, only `.gitkeep` files are tracked under `docs/intake/`, and the committed prose is synthesized rather than transcribed. The clean-room screen over the diff found no employer material, credential, internal service, private identifier or secret.

## Executed evidence

- `npm test` exits 0 on the reviewed tree. Its package run passes 328/328, console passes 18/18, and every format, license, publication, resume, checkpoint, worktree, release, work-order, harness, planning-continuation, evidence-edition and mutation self-test completes. `git diff --check` exits 0 against both the index and `HEAD`.
- Direct compilation against a fresh `packages/compiler/dist`, independent of `authority-evidence.mjs`: the two widening shapes, the narrowing case, the four grant admission cases, the projection, and fourteen contradiction-guard probes covering em dash, en dash, ellipsis, comma, parentheses, curly quotes and a possessive in both authored-note directions, plus the four documented negatives.
- Independent bundle emission into a fresh temporary directory: 24 generated surfaces, zero byte differences against the installation.
- Independent hash re-derivation: Seiri `fnv1a64:9ca8d0229c6bd8db` and the saved Contributor `fnv1a64:06245f5c581212f1` at compiler `0.8.0`, with `grants` and `trace.authorityGrants` absent from the grant-free program.
- `npm run terms -- check` over the changed prose and this review's two artifacts: exit 0, local list unavailable and reported as such.
- `npm run harness -- evidence` binds this review's host receipt to the current subject after re-running its own `npm test` and `git diff --check`.
- Reviewed at their current bytes: the full tracked diff, every untracked addition, both verification reports, the breakout and repair receipts, the evidence README, the bundle-diff classification, and the promoted product, planning, publication and ledger prose.

## Publication artifacts

[PR.md](PR.md) and [RELEASE-NOTES.md](RELEASE-NOTES.md) are prepared beside this report under the current publication contract, one physical source line per prose paragraph and list-item paragraph, which I checked with the repository's own `githubBodyProfileFailures` and with Prettier. The release notes carry the five required sections, and the read-before-upgrading section names the narrowed acceptance set, the two-statement grant rule, the changed tooltip shape, the changed executor procedure, the planning-gate change and the moved feedback edition. The PR title is sized against the whole merged series rather than the newest title, as 08 §PRs and commits requires after the 2026-09-07 correction.

## Limits of this review

This session is not an independent instrument for the parts of the subject it runs on: the generated hooks, skills and instruction block judged my own tool calls while I reviewed them. I paired the order's own generators with direct fixture compilation, independent emission, independent hash derivation and source reading, but a recorded live smoke, a recorded feedback audit and a recorded mutation run remain the executor's observations revalidated against their pinned subjects, not sessions I ran. The compiler still trusts the supplied active mechanic and the host registry; the registry hash is an equality receipt and not authentication; and the contradiction guard compares literal ids and claims no natural-language inference. The five executor supports are rung-7 instructions whose equipment is deterministic and whose judgment is not enforced. This review commits the reviewed state, pushes the WO-042 branch and opens its pull request; it merges nothing, publishes no release and changes no account or tool setting.
