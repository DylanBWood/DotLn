# WO-127 — FINAL-002

**Verdict:** PASS

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.154.0","model":"gpt-6-astra","effort":"max","source":"operator-attested","accountLabel":"not-applicable"}

Reviewed the original WO-127, its explicitly authorized preparation extension, the complete current subject diff, VER-001, VER-002 and FINAL-001. Codex CLI 0.154.0 was observed with `codex --version`; model and effort use the repository's operator-attested default. No blocking findings remain.

Acceptance criteria 1–3 are supported by the complete preservation plan, exclusive copies, source-set and destination-byte verification immediately before teardown, and refusal tests. Binary files, nested and empty directories, differing collisions and interrupted retries retain recoverable records. Main's active queue and terms remain separate from archival, and gate evidence retains its existing handoff. Previews and receipts omit private contents and content digests. The worktree integration fixture performs actual teardown; unknown ignored material, tracked destinations and symlink escapes remain refused.

Criterion 4 retains the cost-bearing source projection, matching acceptances, valid recorded ancestry and observation-after-subject bound. The removed whole-file timestamp predicate no longer rejects an execution-only commit or merge with unchanged planning inputs. Independent tests cover both valid transitions and stale or invalid evidence.

**FINAL-001 F1 — resolved, criterion 5:** The retained metadata trace ends inside synchronous stdin `readFileSync` at 29.958 ms, before hook evaluation. Production now awaits asynchronous stream consumption. The generated-process regression covers manifest-sized JSON, chunked multibyte UTF-8 and truncated-input refusal. The cross-commit fixture uses actual Write/Read observations and proves that changed current bytes invalidate a prior receipt. The twenty-second deadline and fail-closed parsing remain. This repairs the captured input path; the evidence does not establish a general Node defect or eliminate every possible cause of a timeout.

**FINAL-001 F2 — resolved:** The oversized architecture document declares its publication validator. The focused verifier/reviewer fixture requires current report delivery and the owning output checks, rejects missing, unrelated, failing or stale checks, and permits an accurate failed verdict without a green application gate. No lifecycle exemption was introduced; passing-result requirements remain intact.

Criteria 6–7 are supported by the factual documentation, generated role procedures and bounded preparation implementation. Canonical evidence builds, refreshes only the work-order index, decision index, follow-up register and generated harness, then captures the tree. Repeated preparation preserves unchanged bytes and modification times. Packages and fixtures wait for successful preflights. Immutable observations, planning-cost observations, publication source locks and release targets remain explicit inputs. The authority helper selects revision 002 without replacing either initial WO-127 snapshot. VER-001, FINAL-001 and historical WO-126 reports and evidence retain their original bytes.

VER-002 independently passed preservation 4/4 in 1492.205 ms, planning in 825.251 ms, hook/cross-commit 2/2 in 1731.074 ms, failure-handoff/preparation 2/2 in 2294.551 ms, and inventory/preflight 2/2 in 100.756 ms. Its canonical workload passed all 37 suites in 470637 ms, with a 61 ms diff check, for tree `973e6cbccb014dbaa52c98d32c1c21e8557e56a0`. The executor's repaired workload passed all 37 suites in 488156 ms with a 221 ms diff check. These results resolve the earlier failed review on changed implementation rather than treating an unchanged retry as a repair.

The independent final-review command is `npm run harness -- evidence`; `docs/control/local/wo127-reviewer-002-gate.log` and canonical completion evidence retain its actual final authored tree, result and timings. Completion requires that current full gate and diff check. The gate and generated procedures are themselves part of the reviewed subject; their source, assertions and boundaries were inspected independently. This fresh reviewer session records its own authored outputs and current-byte deliveries without adopting inherited implementation.

PR.md and the five-section RELEASE-NOTES.md now describe the completed repair and link VER-002/FINAL-002. Workflow preparation and preflight ordering are substantive changes; polish is limited to diagnostics and documentation refinement. The approved title remains `:bug: Unblock release close with automatic record preservation`. The source-only v0.17.0 target and skeleton 0.15.0 version remain appropriate. The controlling session records the reviewed verdict and performs the authorized Git/publication steps after writer handoff; post-merge release validation and cleanup remain required and are not claimed here.
