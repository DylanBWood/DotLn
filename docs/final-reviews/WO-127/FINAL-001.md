# WO-127 — FINAL-001

**Verdict:** FAIL

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.154.0","model":"gpt-6-astra","effort":"max","source":"operator-attested","accountLabel":"not-applicable"}

Reviewed the original WO-127, its full subject diff, every cited input and the complete verification sequence, VER-001. Codex CLI 0.154.0 was observed with `codex --version`; model and effort use the repository's operator-attested default. No effective-session readback is claimed.

**F1 — Blocking, acceptance criterion 5:** The canonical final-review workload reproduced the unresolved generated-hook timeout. Test 13, `WO-039 completion tracks outputs across commits and auxiliary prompts retain the active obligation`, stalled in `read-observer` while reading `<fixture>/.claude/harness-manifest.json`; the existing 20-second deadline terminated it with ETIMEDOUT, SIGTERM and empty stderr. The failing invocation is `scripts/test-harness.mjs:228`, called at line 1378. Reproduce through `npm run harness -- evidence`; the complete captured failure is `docs/control/local/wo127-reviewer-evidence.log`. Diagnose the stalled manifest read, repair the demonstrated failure path while retaining useful generated-hook integration, cross-commit and stale-byte coverage, then independently verify the repaired current workload. The cause is not established by this timeout record. An unchanged successful retry would not establish a repair.

**F2 — Corrected failure-handoff preparation:** The failed-result command refused current output obligations for `docs/product/03-architecture.md` and `docs/work-orders/README.md`. Inspection of the additional required input, `scripts/lib/lifecycle-evidence.mjs`, and its existing regression confirms FAIL already permits failed application evidence while enforcing current authored-output validation. The oversized architecture document lacked its owning `suite:publication` declaration; the index already declares `suite:index` but lacked a current successful check after the edits. The bounded correction adds `/docs/product/03-architecture.md dotln-check=suite:publication` to `.gitattributes`. The executor reports its focused regression passed in 1069 ms for both verifier and reviewer roles, covering the oversized document, generated index, report delivery and rejected missing, unrelated, failed or stale evidence. Current document validation runs through `npm run test:docs`, which records the owning suite checks, with actual `git diff --check` evidence and current-byte report delivery before the failed transition. No lifecycle exemption or green full gate is required to record this failure; passing-result requirements remain unchanged. The initial refusal appended no event and no guard was bypassed.

No additional blocking finding was identified in retention or planning-cost behavior. Retention fixtures exercise binary records, nested and empty directories, immutable previews, collisions, partial-copy retries, changed sources and refusal boundaries. The actual worktree fixture verifies teardown and separate gate-evidence handoff. Planning fixtures retain unchanged cost observations across execution commits and a merge while rejecting changed costs, mismatched acceptances, invalid ancestry and stale dates. The output-review regression retains every generated-hook process boundary and rejects receipts after current bytes change. The exact historical authority-command mapping preserves the complete prior suite inventory.

VER-001 independently passed four preservation cases in 1452.502 ms, the planning-cost case in 838.025 ms, and the affected harness case in 2972.077 ms including invocation overhead. Its canonical full gate passed all 37 suites in 470454 ms, with a 139 ms diff check, for tree `0ee3a76ff20322360c63d2a702579c89a1028709`. The reviewer gate passed 36 of 37 suites in 496194 ms and its diff check passed in 139 ms, for tree `749149e987a81bce716adf9c8e978f82f8a526c7`. The fixture's process deadline and assertions are unchanged; neither the failure nor earlier successful runs establish its root cause or a runtime repair. A retry launched immediately before the repair-handoff instruction arrived was interrupted with exit 130; its partial local log is retained as `docs/control/local/wo127-reviewer-evidence-002.log` and supplies no passing evidence. Failed-review completion requires a current diff check and preserves the failed reproduction. The operator requested useful tests and supplied no ban on reruns or in-process testing.

Review corrections add the missing WO-127 config-log entry and reconcile the README, Corpus policy and playbook with the already authorized opaque-byte archival. Control commands do not interpret the private account mapping; closeout copies and byte-verifies it without exposing contents or content hashes in receipts. The additional required input was docs/PLAYBOOK.md's account-label paragraph. No private mapping was opened in review and no retention code changed. PR.md and the five-section RELEASE-NOTES.md describe this repair only; immutable WO-126 reports and notes remain intact. The v0.17.0 source target and skeleton 0.15.0 version remain appropriate. No new dependency, authority expansion or product-scope decision was introduced.

The generated role instructions and gate inventory are part of the reviewed subject and also support this review. Their changes were inspected independently against implementation and executable evidence. Authorship and current-byte delivery use explicit Codex adapters. After the temporary executor handoff, a resumed observer incorrectly attributed the intervening executor edits to the old review session; that session and its journal are preserved. A fresh failed-handoff session explicitly adopts only the eleven prior reviewer-authored outputs and redelivers their current bytes, leaving the executor's metadata and fixture edits inherited. Meter observation at 2026-09-11T03:45:49.610Z recorded reviewer tokens and dollars as null; the observed writer-hook duration was 92.085083 ms. Gate runtimes are retained in evidence; the initial refused transition appended no event.

This review does not pass the branch for publication. Bounded repair, independent reverification and a new numbered final review remain required before publication and release close.
