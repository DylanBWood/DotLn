# WO-115 FINAL-002 — final review

**Verdict:** pass. WO-115 asked for one typed command contract listing exactly the commands that exist as terminal implementations, served by the resident over a loopback socket bound to the local user, executing exactly the terminal's implementation under the same compiled envelope, with the text host and a browser-capable client as equal consumers. The subject lands `console-commands-v1` with twenty-three IDs and no effect column, a `127.0.0.1` server whose bearer token sits in an owner-only descriptor, admission by the terminal permission hook's own classifier and decider, terminal-shaped refusals, console-actor receipts with content-addressed result bytes, digest-checked replay that never reruns an effect, and the `commands` and `invoke` actions of the text host plus the Node-free `@dotln/console/client` export. [VER-005](../../verifications/WO-115/VER-005.md) passed all five criteria after the sequence recorded below. This review read the whole subject diff, every filed report and decision, re-ran every deterministic check, ran the three focused suites and the review gate on the subject at the code identity VER-005 judged, and drove the real `dotln resident` binary through the shipped text host, a path no fixture starts. I found no defect requiring repair. [FINAL-001](FINAL-001.md) integrated `main`; `main` has not moved since, so this review integrated nothing.

**Subject:** [`docs/work-orders/WO-115-console-parity-contract.md`](../../work-orders/WO-115-console-parity-contract.md) on branch `wo-115`, uncommitted over HEAD `f73b7e184b38de9cab97b4e86c718b6006f6b19d`, which after `git fetch origin` is `main` and `origin/main`; the `v0.51.1` tag is the highest local and origin tag. The dispatch checkpoint is `refs/dotln/checkpoint/WO-115/24` (`6c869f97`).
- Against checkpoint 24 and against checkpoint 22 (VER-005's subject), the working tree's `packages/`, `scripts/`, `.claude/`, `README.md`, `docs/product/`, the package manifests and the lockfile are byte-identical. Against checkpoint 24 the tracked tree differs only in the generated `docs/work-orders/README.md` projection and the PR draft's process meter that `release prepare` refreshed here.
- VER-005's recorded `reportHash` equals the report's current SHA-256 (`b4864450…`).
- The order's text differs from HEAD only by the `(v0.52.0)` heading label that activation wrote; the five criteria judged here are the original ones, and no criterion is waived.

The judged subject is the working tree: 49 tracked paths against HEAD (5,062 insertions, 158 deletions: the contract, the loopback server, the two client modules and the console fixture added; the resident host, store, `dotln` entry, console CLI, status watcher, two test fixtures, two machinery test scripts, product 04, 06 and 07, four READMEs, the manifests, the lockfile, the publication index and locks, the evidence selection and the re-emitted hooks modified) plus the order's untracked records under `docs/control/orders`, `docs/evidence/WO-115`, `docs/verifications/WO-115` and `docs/final-reviews/WO-115`. Its code identity, computed here with `gateCodeIdentity`, is `bce54ec869793926a7d7db37d759d0e838daebe965ad1729a85982ea7b5b7949`, the identity the executor's final repair gate, VER-005's gate and this review's gate all recorded. No branch commit existed before this review.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.283","model":"claude-fable-5-1","effort":"xhigh","source":"claude-session-readback"}

Human actor: the operator dispatched `resume: final review` in a Claude Code session after selecting Fable 5.1 with `/model`, and made no other choice during the review. The harness version is from `claude --version`. The model is this session's model as the host reports it. Effort `xhigh` is the host's `CLAUDE_EFFORT` value read by this session: selected effort, not effective. The order asks for `reviewer any`. No subagent was planned or spawned: 0 of the cap of 20 (`harness usage`: exact-observed). The plan, stated before any spawn decision: the five earlier independent passes already put seven read-only agents on this subject (two each in VER-001, VER-002, VER-003 and FINAL-001, one in VER-004) and the implementation ran two adversarial review passes of its own; the source diff is about two thousand lines of source and eleven hundred of fixture, readable whole; so one reviewer reading everything, re-running every check and adding the one probe no fixture covers was the smaller plan.

**Process cost:** entry 86292 tokens; handoff 5664291 tokens; source claude-transcript-message-usage

Both readings are `dispatch` scope, from `node scripts/harness.mjs usage a960084a-b043-4b56-b5ba-1b37335e2de7`. The entry reading was observed at 2026-09-26T15:05:14.684Z. The handoff reading was observed at 2026-09-26T15:26:39.511Z (95 steps, 77 commands, 0 subagents of cap 20, exact-observed), after the judgment text was complete and before the result transition. Both include reused cached input. Reasoning tokens and dollar cost are unavailable from this counter, which means unknown, not zero. The largest wait was the review gate (528.58 s); this review paid for no model call outside the session.

## Goal-aligned judgment

The order is Gate U on the critical path: a UI host that can carry operator work without a second workflow state machine, the prerequisite for the gate V product exit. The latest ideation receipt, [030](../../planning/refutations/2026-09-25-planning-c93346fb3a92bb7f-030.md) of the 2026-09-25 standard pass, judged WO-115 `aligned-with-findings`: "NoOp leaves UI clients without a shared authorized command interface", with one known issue on criterion 3, that replay equality for effectful commands must mean reconstructing recorded results rather than executing the effects again, reopening if "replaying a recorded console invocation creates a second external or repository effect". The subject answers that issue by construction: `replayConsoleResults` reads receipts and content-addressed bytes and spawns nothing, and the bound fixture replays after the resident has stopped. The receipt's unmeasured-cost finding on the order's legacy Cost line remains the planner's. The question for this review was whether a UI can now do exactly what the terminal can, no more and no less, and whether the shared repairs the sequence accumulated left the gate honest.

- **Rule beating** decided how I checked. The parity inventory compares bytes on phase-independent refusals for all twenty-three IDs and executes success paths only for reads, the compiled diff and presence ([D009](../../evidence/WO-115/decisions.md#wo-115-d009--takeover-correction-of-the-fixtures-and-evidence-claims)), and every fixture starts the resident in process. I could not add lifecycle success pairs without effects on this repository, so I added the path the fixtures leave out: the shipped `dotln resident` binary, configured with the Contributor mission and a harmless script actor, driven through the shipped text host from a fresh process, stopped with `SIGHUP`. It listed twenty-three commands, matched three reads byte for byte against adjacent direct runs, refused a bad work-order id with the terminal's own bytes and an unknown command in terminal shape, exited 0 on `SIGHUP`, removed its descriptor, and replayed all five results with none incomplete and every receipt on the `console` actor.
- **Seeking the wrong goal:** the outcome is a UI running the terminal's commands under the terminal's authority, judged by result bytes, event deltas, receipts and reuse of the terminal classifier and decider, not by a passing suite name. The contract has no effect column to drift ([D005](../../evidence/WO-115/decisions.md#wo-115-d005--judge-console-commands-with-the-terminals-classifier-and-decider)), and the one place a console could have done more than a hooked terminal, prose the classifier cannot classify, is refused rather than admitted ([D010](../../evidence/WO-115/decisions.md#wo-115-d010--second-independent-review-fail-closed-classification-a-transport-that-outlasts-client-timeouts-and-stronger-fixtures)).
- **Policy resistance / fixes that fail:** every terminal guard stays. The entrypoints run unchanged with a fixed argv prefix and no shell; `resident-state.ts` is byte-identical to `main`, so the judged feedback edition is retained ([D008](../../evidence/WO-115/decisions.md#wo-115-d008--console-receipts-carry-digests-the-judged-resident-fold-is-unchanged)); the away phase's actor envelope no longer judges the operator's own commands, which had refused every read on a real bound store.
- **Shifting the burden:** a UI can invoke an existing command without the operator translating it; the watcher poll fallback ([D019](../../evidence/WO-115/decisions.md#wo-115-d019--one-shared-repair-for-the-integrated-gate-failures)) removes a gate rescue that WO-085 met on byte-identical source, and the fixture base repair ([D015](../../evidence/WO-115/decisions.md#wo-115-d015--repair-ver-002-g1-base-the-integration-fixture-on-the-sources-own-committed-revision)) removes the rescue every unintegrated sibling needed whenever `main` moved a leaf its scripts import.
- **Drift to low performance:** the standard was the complete product gate on the integrated subject; the exclusive scheduling the first integrated repair introduced was measured same-source and reverted, so the net subject leaves `main`'s scheduler untouched ([D023](../../evidence/WO-115/decisions.md#wo-115-d023--restore-measured-shared-scheduling-and-end-the-poll-on-watcher-errors)); no deadline was raised and no assertion removed anywhere in the sequence.
- **Tragedy of the commons:** the order consumed eleven dispatches and seventeen complete product gates on this host, the cost of two verifier findings, one integrated-gate failure with no established cause, and a measured scheduling decision; this review ran one gate, three focused suites, one probe and no agents.
- **Success to the successful:** the terminal entrypoints were kept over an in-process refactor by a recorded experiment ([D002](../../evidence/WO-115/decisions.md#wo-115-d002)), reopenable on measured cost; the exclusive scheduler got its own complete run and lost on the measurement.
- **Escalation:** no new gate or approval; the register settlement below uses the existing follow-up API, and the one file reordered here changes no decision text.
- **Naive Interventionism:** the consumers were enumerated (`dotln resident`, the text host, the browser client, the crash-child fixture); the resident fold, the terminal parsers and the compiled envelope are untouched; every change is local and reversible.
- **NoOp**, withholding the pass, would keep a subject five verifications and this review agree on in a branch, and keep the shared watcher fix out of the sibling worktrees that hit the same failure.

## Criteria

- **Criterion 1:** met. The console suite passed here directly (5 of 5, 16.5 s) and inside the review gate. Its inventory runs all twenty-three IDs directly and over loopback with phase-independent parser refusals, comparing exit code and exact stdout and stderr bytes and asserting an unchanged launchpad control digest and unchanged resident domain events both ways; `resume status` and `times` compare against adjacent terminal runs, the compiled diff and `dotln status` and `console status` compare exactly, and `presence away` and `back` compare bytes and the `OperatorPresenceObserved` and `ClockSampled` event deltas. Source read whole: `consoleCommandInvocation` fixes `process.execPath`, the kit-relative entrypoint and prefix, the launchpad cwd and a child environment stripped of host-session identity, and `execute` spawns it without a shell. My probe on the real binary matched `skeleton.compiled-diff`, `resume.times` and `resume.status` byte for byte against both adjacent direct runs. The D009 limit stands: lifecycle, intent, bind and emit success paths are not executed against this repository; their parity rests on the identical invocation and on the refusal pairs.
- **Criterion 2:** met. The same suite refuses unknown commands (including a name that only differs by case and one recorded by digest), a missing or wrong token, a foreign `Origin`, a `Host` other than `127.0.0.1:port`, a malformed body, a body over 1 MiB, the streaming `--watch` option, prose the classifier cannot classify, and `lifecycle.run` and `shell.run` under a current fixture envelope that admits `repo.read`; every console-originated refusal is exit 1, empty stdout and one `error:` line, and every transport refusal carries its HTTP status with that body. Source read whole: the server listens on `127.0.0.1` only, compares the token with `timingSafeEqual`, requires an owner-only `console/` directory and writes the descriptor with `wx` and mode 0600; the probe observed modes 700 and 600. A second OS account was not exercised; the boundary rests on those mode and ownership checks and the token refusals, as VER-003 recorded.
- **Criterion 3:** met. Every decoded request appends `ConsoleCommandInvoked` and `ConsoleCommandObserved` with actor `console`, including refusals, a command interrupted by a departing caller (exit 143), a request whose caller left while queued and a request still queued at orderly shutdown; result bytes are fsynced under `console/results/` before the receipt that names them. `replayConsoleResults` reconstructs every result from the receipts and the digest-checked bytes, rejects an orphan or duplicate result and names an invocation without a result; it spawns nothing. The fixture replays after shutdown and on a truncated log; the probe replayed five results with none incomplete. Requests remain in memory until their lane turn, so crash-durable queue admission is not established, as VER-002 disclosed.
- **Criterion 4:** met. Product 04 carries §Console parity contract v1 under §Terminal first, console equal, with the ID table, the classification rules, the refusal shapes, the receipt fields and the documented exclusions, and §Later console hosts now routes command invocation through it; product 07's resume-phrase section carries the one sentence; the console README carries §Resident command client and the skeleton README the resident's console paragraph; the README release block and paragraph, product 06's release-boundary note, the publication index row and both source locks follow. The pre-2026-09-09 ledger duty is discharged by [D001](../../evidence/WO-115/decisions.md#wo-115-d001) and the generated decisions-index rows, and the work-order index marks that substitution. The planning map's wave-5 candidate is register row `FUP-0061`, disposed `allocated` to WO-115 for its contract half. `npm run publication:check` passes with both editions CURRENT.
- **Criterion 5:** met. `npm test -- --review` passed on the subject (**37 suites, 0 failed, 528.58 s, 81 fresh tasks, exit 0**; see Checks). `git diff --check` and `git diff --check --cached` are clean. The diff adds no lint or type suppression. The manifests and lockfile differ from HEAD only by the internal bumps: `@dotln/console` `0.2.0` → `0.3.0` with its new `./client` export and its skeleton pin `0.43.2` → `0.44.0`, and `@dotln/skeleton` `0.43.2` → `0.44.0`; no dependency was added.

## Findings

None requiring repair. Five observations are recorded here as notes, not defects, with the evidence for that classification:

- **The decisions file listed D022 before D001** (VER-005 O1), so the generated index did too. This review moved that block to its chronological place before D023 and refreshed the index with `npm run meta`; no decision's text changed, and every anchor resolves as before.
- **Product 07's gate-scheduling paragraph holds the single word "fresh" on its own line** after a rewrap (VER-005 O2). It renders as ordinary paragraph text and passes Prettier; a rewrap would touch a locked publication section for no rendered change, so I left it.
- **The status watcher's 250 ms fallback read was described only in source and in D019 and D023.** The console README's §Live runtime status now says so in one sentence, so a reader of `console status --watch` knows why a missed directory notification cannot leave the host stale. The behavior itself is VER-005's judged subject and is unchanged.
- **`release.close` and `worktree.publish` are in the contract.** A client holding the token can push a work-order branch, open its pull request or publish a release exactly as the terminal's `resume: release close` phrase can, under the same `lifecycle.run` classification and the same compiled envelope ([D006](../../evidence/WO-115/decisions.md#wo-115-d006--contract-membership-add-the-release-close-helper-run-harness-directly-refuse-streaming)). That is the parity the order's objective names, and the token never leaves the owner-only descriptor; it is recorded so a reader does not mistake the loopback for a read-only surface.
- **The historical timeout cause is still unknown.** FINAL-001's two deadline failures did not recur in the executor's three later complete gates, VER-004, VER-005 or this review's gate, all on shared scheduling. D019 to D024 state this plainly, and the three register rows settled below carry reopening conditions for a recurrence.

The register: `FUP-db0a2c8b8a05d4ae` (the FINAL-001 F1 combined repair), `FUP-2ed6d5d710c207da` (D020's retained changes) and `FUP-e495acc8ebaebf05` (the VER-004 repair) had their repair and reopening conditions met by VER-005 and this review, as VER-005 O4 recorded, and are settled through `npm run plan -- followups --apply` with their evidence and reopening conditions ([D025](../../evidence/WO-115/decisions.md#wo-115-d025--final-review-passes-the-integrated-subject-and-settles-the-repair-rows)). The seven rows FINAL-001 settled and the duplicate link of `FUP-47287cef595c25e5` stay as recorded. `FUP-4656197433cb8b3d` (WO-114-D013's index-source hardening, deferred until "WO-115 or WO-117 activates") is the planner's: this order's executor did not carry it in, and this review widens nothing.

## Integration with `main`

None was needed. FINAL-001 integrated the subject from base `af401170` onto fetched `main` `f73b7e18` with `worktree integrate` ([D016](../../evidence/WO-115/decisions.md#wo-115-d016)), preserving checkpoint 14 and the named stash `WO-115 integrate 2026-09-26` (`d820b308`, still on the shared stack), and authority revision `WO-115/002` binds the integrated bundle. After `git fetch origin` here, `origin/main`, local `main` and the worktree HEAD all read `f73b7e18` and no commit exists in either direction, so no merge, retime, stash or re-mint was performed and `npm run worktree -- integrate` was not run. `npm run release -- prepare` against origin's tags reports that `v0.52.0` remains current with no file changed besides the PR meter. The carried-forward claims D016 named have since been re-judged on the integrated bytes by VER-004, VER-005 and this review, not carried.

## Verification sequence

1. **Implementation** ([record](../../evidence/WO-115/README.md)), two segments of one `resume: next` dispatch: a Codex CLI session activated the order, wrote the first implementation and [D001](../../evidence/WO-115/decisions.md#wo-115-d001) to D004, and stopped when its provider became unavailable; Claude Code 2.1.283, `claude-opus-5-5`, `xhigh`, took over the same worktree at the operator's direction, reproduced Codex's seven gate failures, and through two adversarial review passes replaced the hand-written effect column with the terminal classifier and decider ([D005](../../evidence/WO-115/decisions.md#wo-115-d005--judge-console-commands-with-the-terminals-classifier-and-decider)), settled contract membership (D006), the transport, refusal shape and lifecycle (D007), digest receipts with the fold unchanged (D008), phase-independent fixtures (D009), fail-closed classification and a transport that outlasts client timeouts (D010), and re-emitted the harness with authority edition `WO-115/001` (D011). Its complete `npm test` passed (27 suites, 320.63 s).
2. **[VER-001](../../verifications/WO-115/VER-001.md)** (Codex CLI 0.157.1, `gpt-6-astra`, `ultra` recorded `xhigh`/subagents; two read-only agents): fail. F1: a decoded request queued behind a long command got no headers, timed out after 301 s in native fetch and vanished from the receipts. The full gate also failed in the `worktree-integration` fixture, which cloned the moving `main` after WO-070 moved the Beacon leaves; the verifier named that a test-instrument limitation, not a console finding.
3. **Repair** (Claude Code, `claude-fable-5-1`, `xhigh`, [D013](../../evidence/WO-115/decisions.md#wo-115-d013--repair-ver-001-f1-open-every-decoded-requests-response-before-the-lane-and-keep-its-receipts)): every decoded request's response opens before the lane, the keep-alive runs across the wait, and a request whose caller left or whose resident is stopping is refused with a receipt; the fixture gained the two-request and shutdown blocks and was shown to fail on the unrepaired lane.
4. **[VER-002](../../verifications/WO-115/VER-002.md)** (same verifier configuration): F1 resolved by an independent sixty-second socket probe with two real keep-alive intervals; fail solely on criterion 5, G1, the mixed-revision fixture.
5. **Repair** (Claude Code, `claude-opus-5-5`, `xhigh`, [D015](../../evidence/WO-115/decisions.md#wo-115-d015--repair-ver-002-g1-base-the-integration-fixture-on-the-sources-own-committed-revision)): the fixture's origin `main` now points at the source's committed HEAD, so the working-tree overlays and the committed leaves they import come from one revision; 7 of 9 failing became 9 of 9, and the complete gate passed (27 suites, 321.67 s).
6. **[VER-003](../../verifications/WO-115/VER-003.md)** (same verifier configuration): pass on all five criteria at code identity `0e22ed0f…`, with a fresh complete gate (27 suites, 352.05 s).
7. **[FINAL-001](FINAL-001.md)** (Codex CLI 0.157.1, `gpt-6-astra`, `ultra`; two read-only agents): integrated `main` ([D016](../../evidence/WO-115/decisions.md#wo-115-d016)), resolving five authored conflicts and re-emitting authority edition `WO-115/002`; then fail on criterion 5, F1: the expanded review gate failed the WO-114 atomic-replacement watcher case at its 2 s deadline and the complete retry failed the WO-143 acquisition matrix at its 240 s deadline, both on unchanged tests with no established cause ([D017](../../evidence/WO-115/decisions.md#wo-115-d017--preserve-the-unchanged-watcher-timeout-and-obtain-fresh-product-evidence), [D018](../../evidence/WO-115/decisions.md#wo-115-d018--final-review-fails-the-integrated-product-gate)).
8. **Repair** (Codex CLI 0.157.1, `gpt-6-sol`, `ultra`; two read-only agents), expanded by the operator to the watcher failure seen in parallel orders ([D019](../../evidence/WO-115/decisions.md#wo-115-d019--one-shared-repair-for-the-integrated-gate-failures), [D020](../../evidence/WO-115/decisions.md#wo-115-d020--complete-the-bounded-repair-with-a-fresh-integrated-gate)): a 250 ms status read beside the filesystem watcher with a silent-notification regression, a crash-child fixture that skips console setup, and exclusive skeleton scheduling; the complete gate passed (28 suites, 444.80 s). The same-day correction in D019 records the operator's challenge to an over-read of the fixture measurement.
9. **[VER-004](../../verifications/WO-115/VER-004.md)** (Claude Code 2.1.283, `claude-opus-5-5`, `xhigh`; one read-only refuter): criteria 1 to 5 met on a fresh complete gate, but fail: F1, the scheduling change had no same-source comparison and product 07 was stale; F2, the poll outlived `close()` after a watcher error ([D021](../../evidence/WO-115/decisions.md#wo-115-d021--reverification-finds-an-unmeasured-gate-scheduling-change-and-a-poll-that-outlives-close)).
10. **Repair** (Codex CLI 0.157.1, `gpt-6-astra`, `xhigh`, [D022](../../evidence/WO-115/decisions.md#wo-115-d022--repair-ver-004-with-a-controlled-scheduling-comparison-and-watcher-cleanup), [D023](../../evidence/WO-115/decisions.md#wo-115-d023--restore-measured-shared-scheduling-and-end-the-poll-on-watcher-errors)): two complete gates differing only in skeleton's isolation declaration (312.441 s shared, 430.952 s exclusive, [comparison](../../evidence/WO-115/scheduling-comparison.json)); shared scheduling restored with scheduler assertions; the poll cleared on error with a regression; product 07 updated; final complete gate 311.97 s at identity `bce54ec8…`.
11. **[VER-005](../../verifications/WO-115/VER-005.md)** (Claude Code 2.1.283, `claude-opus-5-5`, `xhigh`; no agents): pass. It recomputed the comparison's code identities in memory rather than trusting the executor's snapshot, reran the native FSWatcher error-path probe against a pre-fix control, and ran a fresh complete gate (28 suites, 312.93 s) at `bce54ec8…` with both earlier deadline cases passing ([D024](../../evidence/WO-115/decisions.md#wo-115-d024--reverification-passes-the-measured-shared-scheduling-and-watcher-error-cleanup)). Its same-day corrections to two sentences of its own prose are recorded under D024 and change no finding.

Three harnesses and five models judged this subject across the sequence; this review runs on a fourth model. Its reproduction, checks, probe and gate are its own evidence.

## Checks

Executed by this reviewer on the subject, 2026-09-26:

| Check | Result |
| --- | --- |
| `git fetch origin`; `git log HEAD..origin/main` and the reverse | both empty; `origin/main` = `main` = HEAD = `f73b7e18` |
| Working tree vs `refs/dotln/checkpoint/WO-115/22` and `/24`, code paths | byte-identical (`packages`, `scripts`, `.claude`, `README.md`, `docs/product`, manifests, lockfile) |
| Recorded `reportHash` of VER-005 | equals the report's current SHA-256 (`b4864450…`) |
| `gateCodeIdentity` of the working tree | `bce54ec8…`, equal to the executor's final repair gate and VER-005's gate |
| `npm run build` | compiler, console, kernel, skeleton built |
| `node --test packages/console/dist/test/console-commands.test.js` | 5 passed, 0 failed, 16.51 s |
| `node --test packages/console/dist/test/runtime-status.test.js` | 8 passed, 0 failed, 1.53 s, including the error-path and silent-notification regressions |
| `node --test scripts/test-runner.test.mjs` | 41 passed, 0 failed, 14.21 s; skeleton shared, priority 80, one lane, factor 8 |
| Real-binary probe: `dotln resident` bound to the Contributor mission with a script actor, driven by `console commands` and `console invoke` from a fresh process, stopped with `SIGHUP` | descriptor 0600 in a 0700 directory; 23 commands; `skeleton.compiled-diff`, `resume.times`, `resume.status` byte-identical to both adjacent direct runs; `worktree.start BAD` byte-identical to the direct refusal; `portfolio.declare` refused `unknown console command`; resident exit 0; descriptor removed; replay 5 results, 0 incomplete; every receipt on actor `console` |
| `node scripts/harness.mjs check` | 31 generated surfaces |
| `node scripts/authority-evidence.mjs --check` | verified; 34 bundle comparisons |
| `node scripts/feedback-evidence.mjs --check` | retained live audit `WO-070/feedback-001`; ten regressions, ten removal failures |
| `npm run evidence:artifact -- --check`; `npm run evidence:verification -- --check` | verified at `WO-161/001` |
| `npm run evidence:console -- --check` | wo009, selfhost, control, refutations and missing match |
| `node scripts/harness-context.mjs --check` | exit 0; every ceiling within; refuter unset |
| `npm run publication:check` | PASS; both editions CURRENT (30 and 45 linked sections) |
| `npm run plan -- check` | exit 0 |
| `npm run release -- check-surfaces --local` | all PASS, including every package's publish refusal |
| `npm run release -- prepare` (origin tags) | `v0.52.0` remains current; only the PR meter changed |
| Suppression search over the diff and the new files | 0 |
| `npm test -- --review` (Claude Code shell; no confinement in force) | **37 passed, 0 failed, 528.58 s, 81 fresh tasks, exit 0**, recorded 2026-09-26T15:25:01.076Z; tree `f451c0bc…`, code identity `bce54ec8…`; the row’s sandbox field reads marker `claude-code`, probe written, `inForce: false`, so the full selection ran: the 28 product suites plus the nine machinery suites whose declared sources changed since the base, `harness`, `harness-fixtures`, `harness-evidence`, `registrations`, `runner-fixtures` and the four edition checks (`artifact-evidence`, `authority-evidence`, `feedback-evidence`, `verification-evidence`); `skeleton` 312.41 s shared, `worktree-integration` 106.92 s, `console` inside the run |
| `git diff --check`; `git diff --check --cached` | clean |
| `npm run test:docs`, after this report, the decision record, the register settlement, the PR body and the notes | **21 passed, 0 failed, 24.95 s, 21 fresh tasks** on the first run, before this cell was filled; the rerun after filling it precedes the result transition and is reported in the handoff response |

## Evidence gate, write-back, non-goals and assumptions

- **Evidence gate:** satisfied. The fixture transcripts are the executor's [console-commands.txt](../../evidence/WO-115/console-commands.txt) and [worktree-integration.txt](../../evidence/WO-115/worktree-integration.txt), the [scheduling comparison](../../evidence/WO-115/scheduling-comparison.json) and the [repair diagnostics](../../evidence/WO-115/repair-diagnostics.md); the ignored host gate rows at identity `bce54ec8…` record the executor's final `npm test` (2026-09-26T14:14:46Z, 311,970 ms), VER-005's `npm test` (14:58:39Z, 312,934 ms) and this review's row above.
- **Write-back duty:** discharged, as criterion 4 records; decisions D001 to D025 carry sources and reopening conditions and the index lists them in order.
- **Non-goals held:**
  - Drag-equip authoring: no authoring surface, saved-build or equip-preview verb is in the contract; product 04 says so and names the fork's shell over this contract.
  - Remote access: the server listens on `127.0.0.1` only and refuses a `Host` other than `127.0.0.1:port` and a non-local `Origin`.
  - Authentication beyond the local user: one random token in an owner-only descriptor; no account system.
- **Operator-review assumption 1** (loopback bound to the local user is the right first boundary for an offline app): held on the evidence available. Another local OS user can reach the port but cannot read the token; a browser shell receives it from its local host. A second OS account was not exercised by any pass, so the boundary rests on the mode and ownership checks and on the token refusals.

## Handoff

Result: `final-review-result pass`. On pass, this review commits the reviewed series, pushes `wo-115` and opens its pull request. It merges nothing and publishes no release, tag or package.
