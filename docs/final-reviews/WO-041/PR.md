## What this merges

WO-041, the `v0.13.3` control-plane patch above the published `v0.13.2`. Planning gets the blinded verifier that code already had: `npm run plan -- refute` compiles a subject from committed files only (the vision's thesis sections and exclusions, the five UIFA roles, the capability rows, and each marked order's title, objective, acceptance criteria, and non-goals), dispatches one read-only Entropy Reducer episode wearing the Contra-Auguste mask through an existing CLI transport with no model tools, validates a closed `plan-refutation-v1` result that names for every order whether it advances a named thesis and capability row, is machinery, or drifts, and writes an immutable receipt pair under `docs/planning/refutations/`. A forward-only gate inside `npm test` refuses a later planning pass without a receipt over the current subject or with an unanswered hold; a hold is discharged only by a changed named criterion with a fresh receipt or by an attributed override event in a separate control log that cites an ignored operator capture by SHA-256. The order also carries the operator's 2026-09-07 workflow correction, authorized as a scope amendment during the FINAL-001 repair: work orders implement and verify independently of every sibling's phase, routine integration is the final reviewer's work inside its window, and only affected claims need fresh evidence. Two helpers make that mechanical (`npm run release -- prepare` for collision retiming and `check-surfaces --local` at the head of `npm test` over ancestral tags), and the feedback audit now judges a labeled dependency projection of the skeleton manifest and lockfile so that release-only metadata no longer forces a live re-audit. `@dotln/skeleton` is `0.12.1`; compiler and kernel are unchanged; no dependency, lifecycle, or event-schema change.

- **Committed-only deterministic subject (criterion 1):** `scripts/lib/plan-subject.mjs` reads blobs by object id and hashes the sequence block, every listed order, the thesis sections and exclusions, the roles table, and the capability id and level cells; the fixture proves a one-byte change to any of the five families moves the hash and that planner narrative is excluded from the prompt.
- **Read-only compiled refuter (criterion 2):** `loadouts/plan-refuter.ts` pins six questions, allows only `repo.read*` and `report.emit`, denies write, git, remote, settings, and decision effects, and compiles through `compileLoadout`; the host authorizes both phases through the skeleton's single decider and dispatches from an empty scratch directory.
- **Closed result, structural holds, local-terms screen (criterion 3):** `validatePlanResult` rejects extra fields and unknown order ids, requires a thesis and row for `thesis-advancing` and a vision passage for `drift`, and constructs holds independently of the model's verdict; the receipt writer refuses on a local-terms match, proven with a synthetic term, and reports the list's absence as `unavailable`.
- **Recorded live run (criterion 4):** the committed receipt pair records `claude-cli-print`, harness `2.1.263`, `claude-fable-5-1`, `max`, verdict `pass` with zero holds over the ten-order sequence at `01c2e32`; the receipt discloses the self-referential instrument and its verdict on WO-041 is advisory.
- **Evidence gate (criterion 5):** `scripts/test-plan-refutation.mjs` runs eleven fixture groups inside `npm test`: the canned drift fixture fails without a disposition, override text inside a receipt is ignored, a fixture override event discharges, a fresh pass receipt over a subject changed inside the held criterion is admitted, one changed only elsewhere is refused, the third consecutive hold stops the pass, and every stale-standard case fails; the override command refuses without a capture hash and records the acting session's actor label.
- **Write-backs (criterion 6):** execution guide 07 (the mandatory refutation step and the receipt pair as a standard artifact), the 13 tester surface and roles table, the docs map, the README release block, the dated `control.plan-refutation` capability row, the publication index rows and both edition locks, and the ledger sections.
- **Gate and scope (criterion 7):** `npm test` exit 0 twice in this review, `git diff --check` clean, no new dependency, kernel and compiler `src/` byte-identical to `v0.13.2`.
- **Independent progress and evidence-scoped integration (criterion 8):** 07 §Independent workflows and integration, the playbook, the concurrent plan's lane rules, the phase-two plan, the map, and WO-033's sync-helper scope agree; no sibling-phase prerequisite exists anywhere, the marked sequence block hashes identically before and after, and every historical report is byte-identical to its checkpoint.
- **Release preparation and ancestral surfaces (criterion 9):** `release prepare` retimed this order's own target from `v0.13.2` to `v0.13.3` with the dated roadmap note; seven unit tests and two real-Git fixtures cover ordinary retiming, idempotence, malformed-input refusal without partial edits, preserved control segments and refs, and a sibling's non-ancestor tag being ignored by `check-surfaces --local` while a missing component bump still fails and publication still requires origin.
- **Feedback source projection (criterion 10):** `feedbackSourceFile` strips only `version` and `license` at the manifest root and the lockfile's root and four known workspace entries, labels the capsule `feedback-package-projection-v1` with its physical source path and omitted pointers, and throws on malformed input; the skeleton test proves stability under the exact upstream change that forced the repair and invalidation by every source path and by execution, dependency, publication, and unknown fields; the WO-041 edition was recorded live on the integrated base.
- **Full gate on the repaired subject (criterion 11):** `npm test`, `check-surfaces` against origin (33 rules, all PASS), the plan gate, publication (235 of 235 headings, both locks current), and `git diff --check` pass on the final tree; the sync record, first-pair receipt, and breakout receipt name what was measured and what remains manual.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` on the verified tree and again on the reviewed tree: Prettier clean; `check-surfaces --local` at the head (`latest local tag v0.13.2`, all component rules); 7 release-preparation, 9 license-surface, 285 package, 8 identity-corpus, and 21 mutation self-tests; every shell suite including the two new release fixtures; publication with both locks `CURRENT`; `index --check` current; `tsc -b --force`; all eleven plan-refutation fixture groups; the artifact, verification, and feedback evidence checks against the WO-041 edition; exit 0, zero `not ok` lines.
- `npm run release -- check-surfaces` against origin: `PASS release-block: observed v0.13.3` with latest published `v0.13.2`, skeleton `0.12.1` against previous `0.12.0` with source changed, compiler and kernel unchanged, the body profile over this package's `PR.md` and `RELEASE-NOTES.md`, and all license-surface rules.
- `npm run plan -- check`: the live receipt pair validates end to end against a subject rebuilt from `01c2e32`, which remains an ancestor of `HEAD`; enforcement `pending introduction commit`.
- Subject integrity: a temporary-index tree of the working tree differs from the VER-003 verdict checkpoint and from this review's dispatch checkpoint only in the control projection, the control segment, and the regenerated index; the nine plan-refutation modules, the receipt convention, the four shared skeleton edits, `resume.mjs`, `feedback-evidence.mjs`, and the live receipt pair are byte-identical to the VER-002 verdict checkpoint; VER-001, VER-002, VER-003, and FINAL-001 hash identically at their verdict checkpoints and now.
- Base: `HEAD`, `main`, and `origin/main` are all `a360d04`, the WO-038 merge named by `v0.13.2`; `git ls-remote` agrees and the remote tag set ends at `v0.13.2`; `git log main..wo-041` was empty before the reviewed series.
- Breakout provenance: the ideation receipt's capture in main's ignored intake hashes to the recorded SHA-256; the preserved sync stash exists under its recorded message and SHA; the sequence block hash is unchanged.
- Actors: executor Codex CLI 0.153.4, GPT-6 Astra, `max`, operator-attested (implementation and both repairs); verifier Claude Code 2.1.263, `claude-opus-5[1m]`, `max`, self-reported (VER-001 fail, VER-002 pass, VER-003 pass), each header equal to its control event; reviewer Claude Code 2.1.263, `claude-fable-5-1`, `max`, self-reported (FINAL-001 fail, FINAL-002 pass); every value meets its declared floor and the projection reports no drift.

## Verification trail

- `docs/verifications/WO-041/VER-001.md`: fail; two guards that did not guard (the local-terms screen missed separated and multi-line phrases; the deferral marker accepted orders outside the sequence), both reproduced independently.
- `docs/verifications/WO-041/VER-002.md`: pass; both repairs bound by module-swap fixtures; the original deliverable's criteria re-established.
- `docs/final-reviews/WO-041/FINAL-001.md`: fail; the release target already published by WO-038, changed skeleton source without a component bump, and a substantive integration after the base moved, routed through repair with an ordered checklist.
- `docs/verifications/WO-041/VER-003.md`: pass; the three FINAL-001 findings and breakout criteria 8 to 11 met on the integrated tree, with a mutation of the ancestral tag filter and an independent projection probe; criteria 1, 2, 3, and 5 carried forward on byte-identical modules.
- `docs/final-reviews/WO-041/FINAL-002.md`: pass; the package above, the breakout receipt digested, the commit series and method disclosed, and two cosmetic corrections applied.

## Known open items

- **The gate binds from the introduction commit.** Any later edit to the capability table, a sequence order file, a thesis section, or the roles table turns `npm test` red until a fresh refutation runs; the next planning pass should meet this deliberately (VER-001 O1, VER-003 O4).
- **The local operator terms list is unavailable in this checkout**, so the receipt writer's refusal is proven only by the synthetic-term fixture.
- **Two planning-pass headings on one date** resolve to the one nearer the top of the ledger (FINAL-001 O5).
- **`feedbackSourceFile` knows four workspace entries**; a fifth would stay raw in the subject, which over-invalidates (VER-003 O2).
- **A lightweight ancestral release tag aborts `check-surfaces` with an exception** rather than a FAIL line, and `--local` puts that path inside `npm test` (VER-003 O3).
- **`prepare --local` and `check-surfaces --local` observe different tag sets** by design: the whole local snapshot for retiming, ancestors only for the verification baseline (VER-003 O1).
- **One feedback edition per work order** is a writer limit; a superseded uncommitted edition is preserved in the recovery stash, never overwritten (FINAL-001 O1).
- **Claude Code attestations remain `self-reported`.** No effective-effort readback is recorded for the harness.
- **Publish and release close.** `npm run worktree -- publish WO-041` runs the committed surface check, the body and notes validation, and the sign-off check before any push, then preflights `gh`; inside the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses there the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.13.3` is strictly above `v0.13.2`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.13.3` tag, and creates the matching GitHub Release.
