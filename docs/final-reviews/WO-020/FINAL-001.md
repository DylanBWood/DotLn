# WO-020 final review FINAL-001

**Subject:** branch `wo-020`, worktree `DotLn-wo020`, uncommitted working tree, 2026-09-04 local date (the control log stamps this order's events on 2026-09-05 UTC). `HEAD`, `main`, and `origin/main` are all `45db219a6c7320808c8f25e82a684ffa57884964`, the PR #30 merge of WO-026 that the published annotated tag `v0.5.2` names (`git ls-remote --tags origin` resolves `refs/tags/v0.5.2^{}` to that commit; the local peeled tag agrees); the branch carries no local commits (`git log main..wo-020` is empty), so the entire subject is the dirty tree. At review open, after the dispatch transition and the index refresh the guide requires of a final-review actor: 31 tracked modifications (2,472 insertions / 652 deletions against `HEAD` including the two `docs/control` files and the generated `docs/work-orders/README.md`; 28 files, 1,839 / 583 without them) plus thirteen untracked files totalling 2,590 lines: `packages/skeleton/src/beacon.ts` (207 lines), `packages/skeleton/src/beacon-fs.ts` (241), `packages/skeleton/test/beacon.test.ts` (269), `packages/skeleton/test/beacon-fs.test.ts` (397), `packages/skeleton/test/beacon-observer.ts` (39), `packages/skeleton/fixtures/wo020-base-cli.txt`, `scripts/probe-beacon.py` (98), `docs/discovery/beacon-probe-2026-09-04.md` and `.json`, `docs/planning/beacon-usefulness-checkpoint.md`, `docs/planning/budget-window-work-order-ladders.md`, `docs/product/11-protino.md`, and `docs/verifications/WO-020/VER-001.md`; 44 `git status --porcelain -uall` entries. The subject is exactly the tree VER-001 passed: a temporary-index `write-tree` of the working tree (`229d4f49e183a9ee77a5a2076535089e1ac82e57`) differs from checkpoint `refs/dotln/checkpoint/WO-020/4` (the verdict) only in `docs/control/current.md`, the strictly appended `docs/control/resume.jsonl`, and the regenerated index (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, two bounded documentation corrections (§Corrections applied by this review), and the lifecycle's own events.

**Authority:** `docs/work-orders/WO-020-beacon-codebook.md`: `Model: any capable model`; `Effort: executor xhigh+; verifier xhigh+; reviewer any`; a `v0.6.0` minor classification from published base `v0.5.2` at `45db219…`, skeleton component `0.5.0` to `0.6.0`, recorded with the operator's approval of the version-banner exception; direct-draft provenance for the order text itself; nine numbered acceptance criteria; a six-item evidence gate; a write-back duty; a non-goal list; and twelve appended receipts dated 2026-09-04: the operator-opened four-part ideation receipt (documentation freshness, the Protíno filename, beacon value, a readable README), the stage-1 Beacon write-back receipt, the automation, resource-environment, budget-window/ladder, concurrent-workflow/compatibility/contribution, tenant-track, and constraint/admission ideation receipts, the stage-2 implementation and handoff evidence, and the Actor UI, UIFA, and naming-sound receipts. Against the activated text (`refs/dotln/checkpoint/WO-020/1`) the order differs by 517 insertions and 5 deletions: the title's version suffix, the release-classification paragraph, one clause of acceptance criterion 6 (adjudication 7), and the twelve appended receipts; the objective, shape, scope discipline, deliverables, criteria 1 through 5 and 7 through 9, the evidence gate, the write-back duty, and the non-goals are byte-identical to activation.

**Prior reports:** `VER-001` (pass; all nine criteria, the evidence gate, and every write-back reproduced independently: a Python decoder written from the blueprint alone, a `git archive` rebuild of the `v0.5.2` base to check the executor-supplied CLI fixture, an independent audit of the generated index against the map and a hand fold of the control log, a live-CLI mode-000 read, a 45,459-sample independent concurrent metadata observer, a zero-difference probe re-run, a 14-of-14 capture-hash check against the main checkout's ignored intake, a 12-word shingle comparison against the raw analogy, and a clean-room screen; five non-blocking observations O1–O5 and five disclosures D1–D5 with an empty repair checklist). Complete sequence: one report, no repair episode. The twelve receipts inside the work order are executor evidence and were treated here as VER-001 treated them: claims under test.

**Reviewer:** Claude Fable 5.1 (`claude-fable-5-1`, the model identifier this harness exposes) in Claude Code 2.1.261 with sandboxed Bash; macOS on arm64 (darwin 24.6.0), node v22.2.0, npm 10.8.0, git 2.55.0, and the lockfile-resolved Prettier 3.9.6 and TypeScript 5.4.5 in this worktree's own dependency tree. Reasoning effort `max`: the session's model control reported it at launch and the harness exposes it to the shell as `CLAUDE_EFFORT=max`, but `docs/discovery/environment.json` records `effectiveEffortReadback` for `claude-code` as `not found` with `harnessReadbackEligible: false`, so `harness-readback` is not admissible and the epistemic source is `self-reported`, exactly as VER-001's was. `2.1.261` was checked against `claude --version` on this machine and is the newest entry on the harness's observed `versions` list (appended by WO-028's verifier and merged with `v0.5.1`); `max` is on `sessionEffortSelector.values`; the discovery record is untouched by this order, so the attestation is admissible on merged evidence alone. The order declares `reviewer any`, so any attested effort would satisfy it; `max` is stated because it is what the session reports.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-020 passes.** The order asked for a beacon per evidenced episode: a file whose byte size is an exact codeword for the episode's latest consequential state under a published, versioned codebook, whose mtime is the time of the event that state was derived from, and whose content is the L0 receipt it summarizes; a pure encoder and decoder beside the audit folds; a separate filesystem edge that publishes atomically and reads with `stat` alone; a fake executor that writes its own explicitly self-reported claim before its result exists without touching the host's projection; an opt-in CLI whose default output stays byte-identical to the base apart from the assigned banner; a fresh, labeled host probe; the stage-1 write-backs; and a breakout receipt that keeps its sources distinct. The deliverable does that. The codebook is normative JSON in the domain model equality-tested against the shipped constant, and a decoder written here from that block and its prose alone assigns the same 104 codewords, the same largest size (51,064), and the same sweep over 0 through 102,128 (104 known, 101,292 malformed, 733 framed future versions, none guessed); live and replay files are byte-identical with equal nanosecond mtimes and the host content's nested receipt is the demo's terminal `schedule.cancel` observation; a mode-000 file and garbage at preserved size decode identically from `lstat`; the claim precedes `CommandResult` structurally and leaves the host bytes and mtime unchanged; 128 synchronized replacements in the suite, 45,459 samples in the verifier's observer, and 89,060 samples in this review's observer saw no off-lattice size or partial name; the base rebuilt from `HEAD` reproduces the committed fixture byte for byte and the current CLI matches it with only the banner substituted; the probe re-ran with zero differences at identical tool versions; all fourteen capture hashes and all six backup archives match their receipts; kernel, compiler, audit, and reactor sources are byte-identical to the base; no dependency was added; the suite is green twice; whitespace is clean. The twelve receipts' promoted surfaces were digested as subject and are consistent with each other, with the code, and with the settled decisions; every speculative surface is labeled candidate, preserved, or proposed. VER-001's five observations are one unreachable edge case, one crash-window ignore gap, one inferred filesystem boundary, one probe-script convenience, and one correct projection behavior; this review adjudicates all five as non-blocking, records seven measured observations of its own, corrects two documentation projections, and changes no code, contract, acceptance, schema, authority, or captured-evidence byte.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **The codebook decoder is the deliverable.** `packages/skeleton/src/beacon.ts` defines both `encodeBeaconState` and `decodeBeaconSize`, so the shipped round-trip test cannot show that the published codebook is decodable by a reader who has only the blueprint. Compensating control: a decoder written in Python in this session from the normative JSON block and the framing prose in `docs/product/02-domain-model.md#beacon-codebook-v1`, with no reference to the TypeScript, which enumerated the field space, swept every integer through twice the largest codeword, and decoded the live CLI output from `lstat` alone (§Independent probes). VER-001 wrote its own such decoder; this one is a second, independent implementation.
2. **The base-CLI fixture is executor-supplied.** Criterion 6's byte-identity is checked in the suite against the untracked `packages/skeleton/fixtures/wo020-base-cli.txt`. Compensating control: the base commit was extracted with `git archive HEAD` into the session temporary directory, given its own workspace symlinks (`node_modules/@dotln/*` pointing at its own `packages/`) and this worktree's devDependency tree (TypeScript 5.4.5 per the lockfile, which this order does not change), built with `tsc -b --force`, and run; its 888 bytes are `cmp`-identical to the committed fixture, as VER-001 also found.
3. **The index generator is modified subject and produced the index this review regenerated.** `scripts/work-orders.mjs` changed in this order, and `npm run work-orders -- index` ran here at dispatch as the guide requires, so the helper graded its own output. Compensating control: a Python script parsed the marked sequence block out of `docs/planning/work-order-map.md`, the checklist out of the generated README, and folded `docs/control/resume.jsonl` itself for `FinalReviewCompleted`/`pass`; order and labels agree, every check mark corresponds to an independently folded passing final review (none among the seven at review open), and the `Now` line names WO-020 at `final-review`. `scripts/test-work-orders.mjs` grades synthetic fixture repositories, not the live repository, so its assertions are not circular.
4. **The lifecycle scripts, the surface check, the publish helper, the notes parser, the body profile, and the discovery record are merged code, not subject.** `scripts/resume.mjs`, `scripts/lib/`, `scripts/release.mjs`, `scripts/worktree.mjs`, `scripts/release-notes.mjs`, `scripts/github-body.mjs`, and `docs/discovery/environment.json` are byte-identical to `HEAD`; under `scripts/` only `work-orders.mjs` and `test-work-orders.mjs` changed and `probe-beacon.py` was added. The instruments that allocated this `FINAL-001` path, minted checkpoint 5, will record this verdict, and validate this package are therefore the ones `v0.5.2` shipped.
5. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile** through `check-surfaces`, which first refused the notes for an angle-bracket placeholder inside a code span (the parser's raw-HTML rule does not exempt code spans) and, after rewording, listed both files in its `PASS github-body-profile` row. Because `v0.6.0` is above the latest published tag, these notes become the `v0.6.0` edition at release close.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-020 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-05T03:14:02.743Z`, `elapsed` `{"implementation":7107547,"verification":813288}`, and checkpoint `35e4964825d5ed20b5ef77852a8a0c1f7d824de7` (`/4`); no projection-disagreement warning was emitted. Before invoking the transition this review read the `resume` mapping in `package.json`, confirmed `scripts/resume.mjs` is unmodified, and read its `final-review` and `final-review-result` cases. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, allocated `docs/final-reviews/WO-020/FINAL-001.md` (the directory itself is created by the reviewer), moved the phase to `final-review`, appended `FinalReviewRequested` with `recordedAt` `2026-09-05T03:18:42.355Z`, and minted checkpoint `4c23c292a1cdf66d59048222867486d39836c451` (`refs/dotln/checkpoint/WO-020/5`). `docs/control/resume.jsonl` is a strict append over `HEAD`: its first 146 lines are byte-identical to `HEAD`'s file, and the five appended events are `WorkOrderActivated` (`00:42:45.245Z`, `effortDeclarationValidated: true`), `ImplementationReady` (`02:41:12.792Z`), `VerificationRequested` (`03:00:29.455Z`), `VerificationCompleted` (`03:14:02.743Z`), and this review's `FinalReviewRequested`; every event carries `schemaVersion: 1` and the stamps strictly increase. The projected `implementation` span (7,107,547 ms) and `verification` span (813,288 ms) are the arithmetic differences of the recorded stamps.

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree after the allocation and index refresh as tree `229d4f49…`, and `git diff-tree -r --name-status` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`2b4216b9…`)              | every deliverable path (44 paths: 31 modified, 13 added); the whole subject, the work order's own amendments included, is downstream of activation                                                                                                                                                                                                                                                                                                         |
| `/2` implementation-ready (`bd8203d8…`)    | `docs/control/current.md`, `docs/control/resume.jsonl`, `docs/work-orders/README.md`, `docs/verifications/WO-020/VER-001.md` (added), and the executor's post-readiness naming write-backs disclosed in the work order's Actor UI, UIFA, and naming-sound receipts: `docs/lineage/idea-ledger.md`, `docs/product/00-vision.md`, `docs/product/04-interfaces.md`, both audience outlines under `docs/publication/`, and `docs/work-orders/WO-020-beacon-codebook.md` |
| `/3` verify allocation (`1985c718…`)       | `docs/control/current.md`, `docs/control/resume.jsonl`, `docs/work-orders/README.md`, `docs/verifications/WO-020/VER-001.md` (added)                                                                                                                                                                                                                                                                                                                       |
| `/4` verdict (`35e49648…`)                 | `docs/control/current.md`, `docs/control/resume.jsonl`, `docs/work-orders/README.md` (regenerated by this review at dispatch)                                                                                                                                                                                                                                                                                                                              |
| `/5` final-review allocation (`4c23c292…`) | `docs/control/current.md`, `docs/control/resume.jsonl`, `docs/work-orders/README.md`                                                                                                                                                                                                                                                                                                                                                                       |

Every deliverable blob is therefore identical across the verify-allocation checkpoint, the verdict checkpoint, and the working tree; the only post-readiness changes to the subject before verification began are the executor's three later naming receipts and their write-backs, which the work order discloses as later than the implementation-ready checkpoint and which VER-001 digested; the verifier changed nothing outside its report except the regenerated index (VER-001 D1); VER-001 has not changed since its verdict was recorded. All five checkpoints have `HEAD` as their sole parent. `refs/dotln` held 138 refs after the allocation; the completion mints `/6`.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. `docs/intake/{chats,images,notes}` in this worktree each hold only `.gitkeep`, and `git status --porcelain --ignored` lists no ignored path outside `node_modules/`, `dist/`, `.tsbuildinfo`, and `.claude/settings.local.json`, consistent with the receipts' statement that every capture was reconciled to the main checkout and the provisional copies removed. `.claude/settings.json` is tracked and unchanged. One empty `.claude/.cc-writes` directory exists at the worktree root; it is a harness write-tracking artifact, git does not track empty directories, and `git add -A --dry-run` lists exactly the 44 subject paths and nothing under `.claude/` (adjudication 12).

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                         | Report header                                                                                                                                                                                                                                                                                          | Declared minimum | Result                  |
| -------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- | ----------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.2","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`     | there is no executor report; the stage-2 receipt states the operator selected the model and effort and that this is not effective readback, `0.153.2` is on the `codex-cli` observed `versions` list, and the persisted `model_reasoning_effort` selector records `max` for that version, so `max` was admissible at append | `xhigh+`         | `max` exceeds the floor |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}` | exactly one column-one header in VER-001, parsed and compared with the event actor as JSON in this session: equal                                                                                                                                                                                        | `xhigh+`         | `max` exceeds the floor |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.261","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`  | the header above                                                                                                                                                                                                                                                                                       | `any`            | satisfied               |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: none` and the JSON form's `effortDrift` is `[{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.261; model claude-fable-5-1; effort max; source self-reported`, the same drift line, phase `closed`, a `finalReview` elapsed value beside the two existing values, and legal actions `release-close, next, activate`.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree before any review artifact or correction existed: `All matched files use Prettier code style!`, `GitHub body profile tests passed`, the shell suites (fixture temp-root, publication checker, backup-intake, resume including its time-blind `cmp` lines, checkpoint, worktree, release, and work-orders), `PASS index coverage: 205/205 product headings indexed` with both audience editions `CURRENT` (30 and 45 linked sections), `tsc -b --force`, `# tests 187 / # pass 187 / # fail 0 / # skipped 0`, and the identity corpus `# tests 8 / # pass 8 / # fail 0`, exit 0. The four Beacon diagnostics printed exactly the executor's and verifier's values: `cmp exit=0; size=49764; mtimeNs=1200007000000; content byte-identical`; `readFile=EACCES at mode 000; garbage JSON at preserved size=37216`; `128 atomic replacements; concurrent sweeps=2086; observed both states; zero off-lattice sizes or temporary names`; `104 v1 states; maximum=51064; integers=0..102128; malformed=101292; future frames=733`. Second run, after this review's two corrections and the three package artifacts were in place, with the same result.

### Static, surface, and structural checks

- `git diff --check` clean before and after this review's edits.
- `npm run release --silent -- check-surfaces`, before and after the package artifacts existed: `PASS release-block: observed v0.6.0; expected v0.6.0 (work-order target v0.6.0; latest published v0.5.2)`; `PASS component-version` for `@dotln/compiler` `0.2.0` and `@dotln/kernel` `0.2.1` (`src unchanged`, no bump required) and for `@dotln/skeleton` (`src changed; observed 0.6.0; previous v0.5.2 0.5.0; expected a different version`); `PASS github-body-profile`, first with no bodies and then listing `docs/final-reviews/WO-020/PR.md` and `RELEASE-NOTES.md`.
- `git diff --stat HEAD -- packages/kernel packages/compiler packages/skeleton/src/audit.ts packages/skeleton/src/reactor.ts package.json` is empty; the lockfile's only change is the skeleton version; no dependency was added.
- `docs/lineage/idea-ledger.md`: the diff against `HEAD` contains zero removed lines; the insertion is eleven dated sections holding nineteen entries (7 `adopted`, 10 `preserved`, 2 `transformed`) placed newest-first above the WO-026 section. `docs/decisions/` is untouched. Under `docs/work-orders/`, only WO-020's file and the generated README changed, so the operator-authored WO-021 and WO-022 drafts are untouched.
- The Protíno path move is compatible: all 15 level-two headings of the former `11-proteino.md` survive in the pointer file and in `11-protino.md`, and the content between "Shape before machinery" and "Unresolved product choices" is byte-identical at 25,886 bytes; the audience index lists the sixteen old anchors as `deprecated` pointers.
- Every relative link and anchor in the 28 changed or new Markdown files resolved at review open: 651 targets, zero missing files, zero bad anchors (GitHub slugs recomputed from the target headings), the single dangling target being this report's own allocated path, exactly as VER-001 O5 predicts for a projection of control state.
- Clean-room and secret screen over all 4,444 added or new lines at review open (the diff's `+` lines outside the generated index and lockfile, plus every untracked file): no ticketing or ALM product name, predecessor-system name, managed-host or gateway description, internal hostname, private address, credential shape, or private-key marker; the only hits were the substring "rally" inside "behaviorally" and VER-001's own sentences describing its screen.
- `codex --version` reports `codex-cli 0.153.2` on this machine, matching the executor's harness version in the control log.

### Independent probes against the deliverable

- **Blueprint-only decoder (criterion 1, instrument 1).** The Python decoder enumerated the seven classes, their outcomes, four refusal counts, and two provenances to 104 codewords with no collision, largest 51,064, and round-tripped each; the sweep over 0 through 102,128 gave exactly 104 known, 101,292 malformed, and 733 framed future versions, totalling 102,129, matching the suite's diagnostic and VER-001. The smallest codeword of each class rises with class rank (8,284; 14,460; 20,572; 26,748; 32,860; 39,036; 45,148), which is the property that makes a size-sorted listing a lifecycle-grouped listing.
- **CLI end to end (criteria 2, 3, 6).** With no flags from an empty directory: exit 0, empty stderr, no file created, stdout equal to the committed fixture with `v0.5.0` replaced by `v0.6.0` on the first line. With `--audit --compiled-diff --beacons`: exit 0, empty stderr, exactly two files; the constellation printed the executor claim at 37,216 bytes (`verification/passed | 0 | self-reported | 1`) and the host state at 49,764 bytes (`external-effect/observed | 1 | host-projected | 1`), both with `1970-01-01T00:20:00` mtimes; the independent decoder read `lstat` alone and produced the same two states at `mtime_ns` 1,200,001,000,000 and 1,200,007,000,000; a second emission into a separate directory produced byte-identical files with equal `mtime_ns` and byte-identical stdout; after `chmod 000` a content read was denied while the decoder's result was unchanged; the host file's JSON is a `beacon-projection` with `refusalCount` 1 whose nested receipt is `schedule.cancel` (`external-effect` / `observed`, time 1,200,007) and the claim file's JSON is a `beacon-claim` with `claimedAt` 1,200,001, each padded with newlines only to its exact codeword size; no `.dotln-beacon-stage-*` residue remained.
- **Base rebuild (criterion 6, instrument 2).** The `HEAD` tree built and ran as described above: 888 bytes, `cmp`-identical to `packages/skeleton/fixtures/wo020-base-cli.txt`, first line `@dotln/skeleton v0.5.0`, no file created.
- **Independent concurrent observer (criterion 5).** A Python thread polled `listdir` plus `lstat` on one directory while the CLI emitted into it twelve times: 89,060 metadata samples, two distinct addresses, never more than two entries, ten distinct valid intermediate host states as the fold advanced through the demo (from `work-order-dispatch/emitted` at 8,284 bytes to `external-effect/observed` at 49,764), zero off-lattice sizes, zero non-`[a-f0-9]{64}.beacon` names, and no staging residue in the parent.
- **Probe re-run (criterion 7).** `python3 scripts/probe-beacon.py` reproduced the committed JSON transcript with zero differences at identical versions (macOS 15.6, Python 3.11.7, Node v22.2.0, npm 10.8.0, codex-cli 0.153.2), removed its temporary directory, and left `git status` at the same 44 entries.
- **Receipt claims (criterion 8).** All fourteen SHA-256 values recorded across the receipts match the corresponding files in the main checkout's `docs/intake/notes/` (the four 2026-09-02 captures at mode 644, the ten 2026-09-04 captures at mode 600); the six backup archives the receipts name exist beside the repositories at mode 600 with matching hashes; the budget-window receipt's synthetic fold claim reproduces in memory through `scripts/lib/control.mjs` (`activate WO-901; activate WO-902; implementation ready for WO-901` folds to `WO-902` at `ready-to-verify`). Intake files were hashed and stat'ed, never displayed or copied (§Method).
- **Unrecognized flag spelling.** `--beacons=DIRECTORY` exits 0 with empty stderr and creates nothing, confirming adjudication 10.

### Reading

The complete diff of every changed file and every new file was read: both Beacon modules, the CLI and scenario changes, the three test files, the fixture, the probe script and both probe records, the index generator and its test diff, the control projection and log, the work order with all twelve receipts, VER-001, both READMEs, the playbook, the eleven product documents, the two new planning documents, the ledger, the capability table, the map, the publication index and both editions, the `.gitignore`, and the two manifests. The publish, surface, notes, and body gates were read from `worktree.mjs`, `release.mjs`, `release-notes.mjs`, and `github-body.mjs` rather than inferred, and the actor admissibility rules from `resume.mjs`. The ignored intake was not read.

## Acceptance criteria

**1. Exhaustive round trip, malformed lattice, future codebook. PASS.** Reproduced by the suite in both runs and by this review's blueprint-only decoder; every integer through twice the largest codeword that is not a codeword decodes to `malformed` or, for the 733 framed future versions, to `unknown-codebook` with the version only; version 0 is reserved and malformed; the shipped decoder also refuses `-1`, `0.5`, `NaN`, `Infinity`, `Number.MAX_SAFE_INTEGER + 1`, and `-1n`.

**2. Live and replay byte-identical; audit inputs unchanged. PASS.** The suite's `cmp`, `readFileSync`, `st_size`, and `mtimeNs` checks; the pre- and post-derivation serialization of events and audit projection; two CLI emissions here byte-identical with equal `mtime_ns`; `audit.ts` and `reactor.ts` byte-identical to the base. The executor's edge-only claim is tested separately, as the criterion requires.

**3. Sweep decodes unreadable and garbled content. PASS.** The suite's mode-000 and garbage cases; this review's `chmod 000` on the live output decoded by an independent `lstat`-only reader; `sweepBeacons` uses `lstatSync` only and never opens a beacon.

**4. Premature claim does not change host evidence. PASS.** `FakeExecutor.dispatch` emits the claim before `this.effects += 1` and before the driver appends `CommandResult`, after the dedupe check so a re-dispatch emits nothing; the suite asserts the prefix ends at `CommandPersisted`, captures and re-asserts the host bytes and `mtimeNs`, renders both provenances, and proves the scenario result is deep-equal to a run without observation edges.

**5. Atomic publication. PASS.** Sibling staging outside the observed directory, exact size and nanosecond mtime verified before `renameSync`, cleanup in `finally`; the suite's 128 synchronized replacements (2,086 concurrent sweeps here) and this review's 89,060-sample observer.

**6. CLI behavior. PASS.** Opt-in, exactly one host projection and one claim, constellation from `stat` only in size order ending on the terminal host state, default output byte-identical to the base apart from the assigned banner (fixture reproduced from the base twice), no file without the flag; VER-001 exercised the documented in-repository invocation and every refusing boundary and this review re-read `validateBeaconDirectory` and the CLI's argument handling against them.

**7. Probe record. PASS.** Every observable carries a Principle 15 label, atomicity evidence, and sandbox readability; filesystem identity is `observed-not-exposed`; the size-for-v1 rationale keeps symlink, name, and xattr channels recorded, not adopted; the transcript reproduced with zero differences.

**8. Breakout receipt distinctions. PASS.** The stage-1 receipt separates the earlier analogy source, the capture index read as a locator only, and the two exact preserved planning turns, each with its hash; none is copied into a committed surface; every receipt names its ledger entries, changed surfaces, resolved and open choices, and required review; all hashes and archives verified.

**9. Suite, whitespace, dependencies, kernel. PASS.** As detailed above.

## Evidence gate

Every named item exists and was reproduced: the exhaustive round-trip output (suite diagnostic plus this review's independent sweep); the live-versus-replay `cmp` (exit 0 in-suite plus two CLI emissions compared by `cmp` here); the unreadable-content and claim-versus-evidence transcripts (suite diagnostics plus the live mode-000 decode); the atomicity test (plus 89,060 independent samples); the captured constellation (reproduced byte for byte, twice); and the probe transcript with versions (re-run with zero differences).

## Write-back duty

All stage-1 surfaces are present and consistent with the code: architecture's Beacon bullet in the blackboard list; the domain model's three rows and normative v1 block, whose arithmetic (`code = ((((a * 3 + o) * 4 + r) * 2 + p) * 4 + v)`, largest code 669, 51,064 bytes, `code >= 672` malformed, 64 unassigned outcome slots) this review re-derived by hand; interfaces' constellation row and staleness grammar with thresholds deferred to WO-021; audit's `M0 beacon` rung, access and omissions, and disclosure paragraph; the ledger's adopted, transformed, and preserved Beacon entries; the capability table's `projection.beacon` row at level 1 with its blocking gate; WO-021 nominated in the capability row, the roadmap, the skeleton README, and the map. The README release block, the skeleton README's component history and Beacon section (its two-row table matches the live constellation), the roadmap's `v0.6.0` line, and the publication index's `implemented` row for the codebook agree with the shipped behavior.

## Ideation breakout receipts

Inspected against the guide's list for the eventual verifier and final reviewer, receipt by receipt:

- **Source treatment and provenance.** Every receipt declares Clean Room's locked floor with Shape-First Synthesis and states that Direct Draft Fidelity does not apply; only the order text itself carries direct-draft provenance, recorded in its header. Each names its exact local-only capture with a SHA-256 (all fourteen verified), its canonical reconciliation, and its validated backup (all six verified). The stage-1 receipt reads the capture index as a locator and the two exact planning turns as sibling safety captures, not sources.
- **Traceability to operator intent.** Each receipt's named surfaces exist and say what the receipt says: freshness and ownership in the guide, the publication compiler, and the playbook; the Protíno move with mutable links updated in README, 00, 04, 06, 10, and the publication index; the usefulness checkpoint document, roadmap candidate, and map link; the marked sequence block, generator, tests, and generated README; the automation direction in 03 and the guide's discipline; the resource-pressure candidate in 03 with 02 and 06 cross-references; the ladder document with its Fable handoff, the 06 candidate, the map's preserved candidate, and the playbook's corrected concurrency sentence; the compatibility delegation in 10 and the contribution and tenant scenarios in 12; the constraint and admission material in 03, 06, and the handoff; Actor UI and UIFA with the sound associations in 00 and 04 and both audience outlines.
- **Consistency with settled decisions and vocabulary.** No ADR, resolution, older ledger entry, generated residue, or historical report was edited; the ledger diff has zero removed lines; the vocabulary is the repository's own (`candidate`, `preserved`, `adopted`, `transformed`, `planned`, `vision`). The receipt for the Protíno move supersedes exactly one earlier ledger choice by naming it rather than editing it.
- **Version and schema effects.** The skeleton's `0.6.0` under the operator-approved `v0.6.0` target; no event, control-log, IR, or codebook schema effect beyond the new versioned codebook itself, which is data with its own version axis as 10-ir-compatibility.md requires.
- **Internal cross-references.** All resolve (the 651-link check above), including the two new planning documents and the handoff anchor cited from 03, 06, 12, and the map.
- **Speculative choices not presented as settled.** The usefulness checkpoint opens with "No trial has run" and denies being an acceptance criterion or release gate; the ladder document calls itself a candidate trial with unproven benefit and records the one-order fold limitation; the resource-pressure, budget-window, concurrent-workflow, tenant-track, and constraint candidates are `Candidate —` headings that select no curve, unit, threshold, schema, storage engine, or work order; the map's preserved candidate is unallocated; UIFA is a proposed spoken name. No WO-020 acceptance criterion, lifecycle transition, or authority boundary is added or weakened by any of it, and no WO-021 control-plane, staleness, or group implementation appears in the code.
- **Executable helpers.** The only executable created for a breakout is the index generator change, named in the four-part receipt as implementation subject with its tests, independently audited by VER-001 and again here (instrument 3). `scripts/probe-beacon.py` belongs to the implementation stage and was re-run.
- **Neutral public language.** The generic tracker analogy carries no vendor term; the provider usage references in the ladder document are public documentation pages labeled as reference points, not account measurements; the operator's personal framing survives only in the ledger's guard clauses.

## Verification-sequence soundness

One verification, one pass, no repair episode. VER-001 was allocated at event 149 and its verdict recorded at event 150 with a header equal to the event actor; its elapsed span is 813,288 ms. The verifier changed exactly one subject file outside its report, the regenerated index, disclosed in D1 and required by the guide; every other deliverable blob is identical across the verify-allocation and verdict checkpoints. The verifier's compensating controls were themselves reproduced or exceeded here rather than accepted: its blueprint decoder by this session's own, its base rebuild by a second rebuild, its 45,459-sample observer by an 89,060-sample one, its probe re-run, hash checks, link sweep, and clean-room screen by recomputation, and its index audit by an independent script.

## Adjudications

### 1. `sweepBeacons` throws `ENOENT` on a directory that was never created (VER-001 O1)

Confirmed by reading `createBeaconWriter` (no `mkdir` until `emit`) and `sweepBeacons` (`readdirSync` throws). Unreachable from the shipped CLI, whose fixture always yields a host projection before the sweep. Non-blocking; a natural input to WO-021, which will sweep directories it does not own.

### 2. Staging is a sibling of the beacon directory, unignored for the in-repository invocation (VER-001 O2)

Confirmed by reading `emit`: `mkdtempSync(join(dirname(directory), ".dotln-beacon-stage-"))`, with `rmSync` in `finally`, and no matching `.gitignore` pattern. The design goal (no partial names inside the observed directory) is correct and the cleanup covers success and exception; only a hard kill between `mkdtemp` and `finally` leaves an untracked directory at the repository root. Non-blocking; one ignore line is a candidate for WO-021 or a patch. Not applied here: an ignore rule is part of the deliverable's environment rather than wording, links, or PR metadata, and the verification did not cover it.

### 3. One filesystem is assumed (VER-001 O3)

Inferred, not tested; `renameSync` across a mount returns `EXDEV` and nothing claims otherwise. Non-blocking; a boundary to name in WO-021.

### 4. `scripts/probe-beacon.py` hard-requires `codex --version` (VER-001 O4)

Confirmed by reading the version block, which calls `check_output` for `node`, `npm`, and `codex` outside the `attempt` wrapper. It reproduced here because the Codex CLI is installed; the sandbox printed a harmless PATH-alias warning on stderr that did not enter the transcript. Non-blocking.

### 5. The generated index links a report before it exists (VER-001 O5)

The same holds for this report after the `final-review` dispatch. Correct projection behavior; the link resolves as of this file.

### 6. The verifier's disclosures (VER-001 D1–D5)

D1 (index regenerated by the verifier) and D2 (the in-repository invocation exercised and cleaned) are the guide's own requirements and left no trace beyond the regenerated index; D3 (a deliberate stale-then-restore of the index) appended no event and changed no ref; D4 (read-only hashing of local-only intake) is repeated here under the same discipline; D5 (the base rebuild reused this worktree's devDependencies) is the same method this review used, and the lockfile pins the same toolchain the base pins. No action.

### 7. The work order's acceptance text was amended after activation (this review)

Against the activation checkpoint, the title gained `(v0.6.0)`, the release-classification paragraph replaced its "assigned by the planner at activation" placeholder with the `v0.6.0` minor assignment from `v0.5.2` and the sentence "The operator approved the truthful version-banner update", and criterion 6 gained the clause "except for the assigned package-version banner". The clause is the necessary consequence of the assigned version: a banner that prints the package version cannot stay byte-identical across a version bump, the exception is confined to that one line, and the suite enforces it exactly by substituting only that line in the base fixture. The stage-1 receipt directs review of "the version-banner normalization against the operator-approved release assignment", and the classification paragraph records the operator's approval and the roadmap's opt-out default as its authority, which is the marker earlier reviews asked for. Every other criterion is byte-identical to activation. Non-blocking; the operator's merge confirms the record.

### 8. "The operator confirmed this direction during VER-001 repair" (this review)

That sentence in the order's direct-draft provenance paragraph predates activation: `git log -S` places it in commit `76486da`, WO-007's audit-record baseline, when the WO-020 draft was filed during WO-007's repair of its own VER-001. WO-020 has no repair episode. Time-indexed as historical work-order text; not rewritten. No action.

### 9. The publication editions link new sections to document roots (this review)

The four new topics the editions add (work-order navigation, freshness and ownership in two documents, the usefulness checkpoint) are linked to their documents' root anchors because the editions' bootstrap base `f2a4b232…` predates the sections and the checker requires every link to resolve there; the stage-2 receipt records this and states that no checker was weakened. The link text names the section while the reader lands at the document top. Non-blocking; a later publication order can re-base.

### 10. `--beacons=DIRECTORY` is ignored (this review)

Tested: exit 0, empty stderr, nothing written. The CLI reads flags with `includes` and `indexOf`, so every unknown argument is ignored, a pre-existing pattern; the documented spelling is the one the suite exercises. Recorded without action.

### 11. Every appended event re-derives and rewrites the host beacon (this review)

`LiveReactorDriver.feed` calls `onEvents` after each append and `writer.project` emits every derived record, so the host file is replaced even when its state is unchanged; because size, mtime, and content derive from the receipt and not the wall clock, the replacement is byte-identical and the observer saw only valid states. Recorded; WO-021 may skip unchanged records.

### 12. Empty harness directory at the worktree root (this review)

`.claude/.cc-writes` exists on disk, empty; git does not track empty directories and the dry-run add lists nothing under it. Recorded so a later session does not mistake it for reviewed content.

### 13. Status labels for the three new candidate sections (this review)

The audience index labels the resource-pressure, usefulness-checkpoint, and budget-window candidates `planned`, while every other `Candidate —` section except the WO-026 navigation candidate is `vision`; the vocabulary rule requires the labels to stay visibly distinct but defines no boundary between them, the resource-environment receipt states the classification deliberately, and the publication check passes. Recorded for the next publication order; the executor's classification stands.

### 14. Smaller consequences recorded without action

The capability table's dated addition keeps `projection.beacon` at level 1 "staged pending independent review"; as with WO-023's addition, the dated text is not rewritten by review, and promotion follows the declared gates. The map's "Planning revision" line names WO-020 ideation, which remains the last human planning revision. `docs/README.md` describes directories, not files, so the two new planning documents need no map entry. The four 2026-09-02 intake captures are mode 644 in the main checkout while the ten 2026-09-04 captures are mode 600; the receipts claim 600 only for the latter.

### Candidates considered and not sustained

- **Beacon content discloses the whole L0 receipt to any reader with content access.** Not sustained: by design, and 09's M0 rung separates metadata access from content permissions and names the omissions and the leak.
- **A self-reported writer could forge a host-provenance size.** Not sustained as a defect: the provenance bit is documented as a channel label and not authentication, addresses differ by producer, and a keyed residue is WO-022's nominated subject and a listed non-goal.
- **The ledger now carries naming preferences (UIFA rhymes with FIFA; DotLn's .NET and Kotlin echo).** Not sustained: they are labeled naming context with no implementation effect, within the operator's ideation authority, and the products state them as proposed.
- **The ladder document cites external vendor documentation.** Not sustained: public product documentation labeled as reference points, not account measurements, and not employer material.
- **`implemented` in the publication index versus level 1 in the capability table.** Not sustained: the index labels a section's implementation status and the table a maturity level with declared promotion gates; both are accurate.

## Corrections applied by this review

Two corrections, both in the documentation lane `docs/final-reviews/README.md` grants, proven contained by diffing against checkpoint 5 (4 deletions in one file; 4 insertions and 3 deletions in the other) and followed by a re-run of the checks they could affect: the pinned `prettier --check` on both files, the full `npm test` (which runs the formatter and `check-publication.mjs` against the real repository), `git diff --check`, and `check-surfaces`, all clean.

1. **`docs/lineage/idea-ledger.md`:** the WO-020 insertion opened with five consecutive blank lines between the intro paragraph and its first section; collapsed to one. The ledger is formatter-ignored, so nothing else would have caught it; no entry text changed and the diff against `HEAD` still removes zero lines.
2. **`docs/planning/work-order-map.md`:** the WO-020 catalog row's activation-preflight cell, which still read as the pre-activation instruction ("active slot must close; assign version …"), now records the satisfied preflight in the same form as the WO-026 row, and the slot sentence in §Recommendation and rationale now says WO-020 passed independent verification and final review at this revision and awaits operator merge. The cell keeps the column's width, so the table did not reflow. No other row changed.

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet, no guard was lost, and no behavioral or evidence defect was found in the deliverable that any criterion covers. The items below are recorded and not scored.

1. **`sweepBeacons` on a never-created directory throws `ENOENT`** (adjudication 1): WO-021 input.
2. **Staging sibling is unignored for the in-repository invocation** (adjudication 2): candidate for one ignore line.
3. **One filesystem is assumed** (adjudication 3): boundary to name.
4. **The probe script requires the Codex CLI** (adjudication 4).
5. **The `--beacons=DIRECTORY` spelling is ignored** (adjudication 10): pre-existing argument handling.
6. **Every event rewrites the host beacon** (adjudication 11): deterministic; an optimization candidate.
7. **Document-root links in the editions** (adjudication 9): re-base in a later publication order.
8. **`planned` versus `vision` for the new candidates** (adjudication 13): for the next publication order.
9. **Criterion 6 amended after activation for the banner** (adjudication 7): operator-approved; the merge confirms.
10. **`CLAUDE_EFFORT` remains an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9).
11. **The executor's model and effort live in the control log and the receipts, not in a committed executor artifact**, as every reviewed code order since WO-013 has recorded.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step and the sandbox.** `gh auth status` in this sandbox fails before its root command exists (`failed to read configuration: open ~/.config/gh/config.yml: operation not permitted`), the same failure every reviewed order since WO-006 has met, while read-only `git ls-remote` to `origin` succeeds. The reviewed commit is made here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation, and notes parse run against the exact commit; if it refuses at the `gh` preflight it pushes nothing and the operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute command the publisher prints: change directory into the main checkout and run this worktree's `scripts/release.mjs close WO-020 --publish` with node, outside the sandbox and where `gh` is authenticated. Because `v0.6.0` is strictly above `v0.5.2`, the reviewed subject helpers finish this worktree through their own classifier and closed-state gate, fast-forward main, run `npm ci` and the evidence gate, assemble the five-section edition from these notes, create and push the annotated `v0.6.0` tag, and create the matching GitHub Release. If Release creation fails after the tag push, the rerun is `npm run release -- close WO-020 --publish` from updated main.
- **Operator confirmation at merge** of the post-activation criterion 6 clause (adjudication 7), so the record shows the banner exception was the operator's intent and not an executor's convenience.
- **Attribution:** the reviewed commit carries a plain subject and no attribution trailer, per the operator's standing rule (`~/.claude/CLAUDE.md`) and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins, as the WO-017, WO-018, WO-023, and WO-028 reviews recorded.
- **Ledger duty:** discharged by the executor's eleven dated sections; this review adds no ledger entry because every open item above is either already in VER-001 or is a candidate this report names for WO-021 or the next publication order.

## Proposed PR

- **Title:** `:sparkles: WO-020: Beacon codebook; each evidenced episode gets a beacon whose exact byte size decodes its latest consequential state through a published codebook, whose mtime is the derivation event's time, and which holds the matching L0 receipt; v0.6.0`. The `:sparkles:` precedent is PR #30 for WO-026, PR #29 for WO-028, and PR #28 for WO-023, chosen because the order adds a projection, an edge, and a CLI option rather than restructuring existing behavior.
- **Body:** `docs/final-reviews/WO-020/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commit (the committed surface check requires the closed control state, and it also makes the map's slot sentence and the index's check mark true); the index is regenerated after the result and checked; the reviewed state is committed with a plain subject. The publisher is then run from this worktree:

```bash
npm run worktree -- publish WO-020 --title ':sparkles: WO-020: Beacon codebook; each evidenced episode gets a beacon whose exact byte size decodes its latest consequential state through a published codebook, whose mtime is the derivation event'"'"'s time, and which holds the matching L0 receipt; v0.6.0' --body-file docs/final-reviews/WO-020/PR.md
```

The helper runs `check-surfaces --committed WO-020` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-020`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff in the absolute subject-helper form. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, the operator runs that printed command with the main checkout as the working directory; the expected outcome is the annotated `v0.6.0` tag and its GitHub Release.

## Method

Everything above was re-established in this session inside the Claude Code sandbox: `npm test` twice; the read-only status projection and the transition; temporary-index trees and `git diff-tree -r` against all five checkpoints; the `resume.jsonl` prefix comparison; the blueprint-only decoder, the base rebuild, the CLI probes, the concurrent observer, the probe re-run, the index audit, the synthetic fold, the link sweep, and the clean-room screen as scripts written for this review in the session temporary directory; `shasum -a 256` and `stat` on the main checkout's intake captures and backup archives, with no intake byte displayed, copied, or committed; and a full reading of the diff, the receipts, VER-001, and the gates' source. The two corrections were applied with exact string replacement, checked with the pinned formatter, and diffed against checkpoint 5. No other file outside `docs/final-reviews/WO-020/` was changed by this review; the index regeneration and the control projection are the lifecycle's own effects.
