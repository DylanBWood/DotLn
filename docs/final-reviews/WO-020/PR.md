## What this merges

WO-020, the `v0.6.0` minor above `v0.5.2`: Beacons. Every evidenced episode in the walking skeleton can now be read from one metadata sweep. With `--beacons` followed by a directory path, the skeleton writes one host-projected file per episode whose exact byte size is a codeword, published in the domain model as versioned data, for the episode's latest consequential action class, outcome, cumulative refusal count capped at three, provenance, and codebook version; whose mtime is the virtual time of the event the state was derived from; and whose content is the matching L0 receipt inside a rebuildable `BeaconProjectionRecord`. The fake executor separately writes an explicitly self-reported claim before its `CommandResult` exists, at its own stable address, and the constellation the CLI prints from `stat` alone shows both files in size order with their provenance decoded, ending on the demo's terminal `schedule.cancel` observation. The pure codebook, fold, and record encoder live in `packages/skeleton/src/beacon.ts` with no I/O; the filesystem edge in `packages/skeleton/src/beacon-fs.ts` validates the destination, stages each complete file in a private sibling directory, verifies exact size and nanosecond mtime, renames it atomically, and sweeps with non-following `lstat` only. Without the flag the CLI writes nothing and its output is byte-identical to `v0.5.2` apart from the `v0.6.0` banner. Kernel, compiler, reactor, and audit sources are byte-identical to the base; no dependency was added. The order's twelve operator-authorized receipts are included: documentation ownership, the Protíno path correction, the readable proposed sequence in the generated index, the beacon usefulness checkpoint, the automation direction, the resource-pressure, budget-window, concurrent-workflow, tenant-track, and constraint-admission candidates, the stage-1 Beacon write-backs, the stage-2 implementation evidence, and the Actor UI and UIFA naming write-backs, all as documentation and planning and none as a runtime, schema, acceptance, or authority change.

- **Exhaustive round trip, malformed lattice, future codebook (criterion 1):** the shipped test round-trips all 104 v1 states and sweeps every integer from 0 through 102,128 against an inline framing oracle (101,292 malformed, 733 framed future versions with no guessed state); the verifier's blueprint-only Python decoder and this review's second blueprint-only decoder reproduce the same field space and the same three counts.
- **Live and replay byte-identical; audit inputs unchanged (criterion 2):** `cmp` exit 0, size 49,764, `mtimeNs` 1,200,007,000,000 in the suite; serialized events and audit projection unchanged across derivation; two separate CLI emissions here were byte-identical with equal `mtime_ns`, and the host file's nested receipt is the `schedule.cancel` `external-effect/observed` entry at time 1,200,007.
- **Metadata-only reading (criterion 3):** a real mode-000 file refuses `readFileSync` with `EACCES` while the sweep is unchanged, and garbage content at the preserved size decodes identically; repeated by hand on the live CLI output with an independent decoder reading `lstat` only.
- **Premature claim isolation (criterion 4):** the claim is emitted before `CommandResult` and `VerificationCompleted` structurally, the host file's bytes and mtime are unchanged across it, both rows render with provenance decoded, a deduplicated re-dispatch emits no second claim, and the scenario result with observation edges is deep-equal to the baseline.
- **Atomic publication (criterion 5):** the suite's worker thread saw both states across 128 synchronized replacements with no off-lattice size or partial name (2,086 sweeps in this review's run); the verifier's independent observer took 45,459 samples and this review's took 89,060 across twelve CLI emissions, both with zero rejections and exactly two addresses.
- **CLI (criterion 6):** default output equals the committed `v0.5.2` fixture with only the banner substituted and creates no file; the fixture itself was reproduced twice from the base commit, by the verifier's `git archive` rebuild and again by this review's, byte for byte (888 bytes); `--beacons` writes exactly one host projection and one claim and prints the constellation in size order ending on the terminal host state; the documented in-repository invocation leaves `git status` unchanged; `docs/intake` paths, symlink aliases to them, non-ignored in-repository paths, a missing argument, and a duplicated flag all refuse before writing.
- **Probe record (criterion 7):** every observable carries a Principle 15 label, atomicity evidence, and sandbox readability; filesystem identity is honestly `observed-not-exposed`; the size-for-v1 rationale keeps symlink, name, and xattr channels recorded rather than adopted; `scripts/probe-beacon.py` reproduced the committed transcript with zero differences for the verifier and again for this review.
- **Breakout receipts (criterion 8):** the stage-1 receipt separates the earlier analogy source, the capture index read as a locator, and the two exact preserved planning turns; all fourteen recorded capture hashes match the main checkout's ignored intake (verified read-only by the verifier and again by this review) and the six named backup archives exist beside the repositories at mode 600 with matching hashes; every receipt names its ledger entries, changed surfaces, open choices, and required review.
- **Suite, whitespace, dependencies, kernel (criterion 9):** `npm test` exit 0 (187 package tests including the 14 new Beacon tests, 8 corpus tests, every shell suite, 205 of 205 headings, both audience locks current); `git diff --check` clean; root `package.json` unchanged and the lockfile changed only by the skeleton version; kernel, compiler, `audit.ts`, and `reactor.ts` byte-identical to `v0.5.2`.
- **Write-backs:** architecture (Beacon in the blackboard, automation direction, resource-pressure candidate), domain model (three rows and the normative v1 block), interfaces (constellation row, staleness grammar, Actor UI and UIFA), audit (M0 rung, access and omissions, disclosure), roadmap (`v0.6.0`, usability correction, usefulness and ladder candidates), execution guide and publication compiler (freshness and ownership), playbook (dispatch refresh, ownership, concurrency correction), IR compatibility (delegated compatibility choices, computed or cached views), workstream application (contribution and tenant scenarios, WO-026 delivered), vision (UIFA, name sounds), capability table (`projection.beacon` at level 1), map (marked sequence, usefulness and ladder links, candidate), ledger (eleven dated sections, nineteen entries), READMEs, publication index and both editions, the generated work-order index, and the two new planning documents.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` in this worktree on the verified tree and again on the reviewed tree: Prettier clean, the body-profile unit suite, the shell suites, `check-publication.mjs` at 205 of 205 headings with both audience locks current, `tsc -b --force`, 187 of 187 package tests, 8 of 8 identity-corpus tests, exit 0. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.6.0; expected v0.6.0`, `PASS component-version` for `@dotln/compiler` `0.2.0` and `@dotln/kernel` `0.2.1` (`src unchanged`) and for `@dotln/skeleton` `0.6.0` (`src changed`, bumped from `0.5.0`), and the body-profile rule over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity at review open: a temporary-index tree of the working tree differed from the verdict checkpoint only in the two control files and the regenerated index; from the verify-allocation checkpoint additionally in VER-001; from the implementation-ready checkpoint additionally in the executor's post-readiness naming write-backs (ledger, vision, interfaces, both audience outlines, and the work order's three later receipts); all five checkpoints have `HEAD` (`45db219`, also `main`, `origin/main`, and the `v0.5.2` tag target) as their sole parent; `git log main..wo-020` is empty; the verifier's report header equals its control-log actor by JSON comparison; the executor's `max` and the verifier's `max` meet the declared `xhigh+` floors.
- Direct probes: a decoder written from the domain model's JSON block and prose alone assigned 104 codewords with no collision, largest 51,064, and swept 0 through 102,128 to exactly 104 known, 101,292 malformed, and 733 future frames; the base commit rebuilt from `git archive HEAD` produced a CLI output `cmp`-identical to the committed fixture; the CLI run twice produced byte-identical files with equal `mtime_ns`, decoded from `lstat` alone by the independent decoder before and after `chmod 000`, with padding proven to be newlines only and the host content's nested receipt proven to be the `schedule.cancel` observation; a polling observer over twelve CLI emissions took 89,060 metadata samples and saw two addresses, ten valid intermediate host states, and zero off-lattice sizes or partial names, with no staging residue; the probe re-run differed from the committed JSON in nothing; all fourteen capture hashes matched the main checkout's intake; 651 relative links and anchors across the 28 changed Markdown files resolved at review open, the only dangling target being this review's own not-yet-written report; the generated index's sequence, labels, and progress marks agree with the map's marked block and an independent fold of the control log; the receipt's synthetic two-order fold reproduced (`WO-902` reported `ready-to-verify` after `WO-901` was marked ready); the clean-room and secret screen over all 4,444 added or new lines found nothing.

## Verification trail

- `docs/verifications/WO-020/VER-001.md`: pass; all nine criteria, the evidence gate, and every write-back reproduced independently with a blueprint-only decoder, a base rebuild, an index audit, live-CLI probes, a 45,459-sample observer, a probe re-run, capture-hash and shingle checks, and a clean-room screen; five non-blocking observations and five disclosures with an empty checklist.
- `docs/final-reviews/WO-020/FINAL-001.md`: pass; the verifier's observations and disclosures adjudicated with reasons, the reviewer's own observations recorded, the twelve receipts inspected against the guide's list, two bounded documentation corrections applied and re-checked, method and instruments disclosed.

## Known open items

- **`sweepBeacons` on a never-created directory throws `ENOENT`** (VER-001 O1): unreachable from the shipped CLI; WO-021's cross-worktree sweeps should decide whether an absent directory is an empty constellation.
- **Staging is a sibling of the beacon directory, and for the in-repository invocation that sibling is the repository root** (VER-001 O2): `.dotln-beacon-stage-*` is removed on success and on exception but is not gitignored, so a hard kill between staging and cleanup could leave an untracked directory; one ignore line is a candidate for WO-021 or a patch.
- **One filesystem is assumed** (VER-001 O3): a beacon directory at a mount boundary would fail with `EXDEV` rather than fall back; per-file atomicity only, no snapshot or durability claim.
- **`scripts/probe-beacon.py` requires the Codex CLI** (VER-001 O4), for its version line only.
- **`--beacons=DIRECTORY` is not a recognized spelling** and is ignored like every other unknown argument, a pre-existing CLI pattern; the documented form is `--beacons` followed by a directory path.
- **Every appended event re-derives and rewrites the host beacon**, deterministically and with identical bytes and mtime when the state is unchanged; WO-021 may skip unchanged records.
- **The publication editions reach the new sections through document-root links** because the pinned bootstrap base predates them; a later publication order can re-base.
- **The three new candidate sections carry the audience-index status `planned`** while the earlier candidate sections carry `vision`; the executor's classification stands, recorded for the next publication order.
- **Level promotion for `projection.beacon`** stays at 1 (demonstrable) until the declared control and staleness gates and the separate usefulness comparison exist; the capability table's dated addition is not rewritten by this review.
- **The work order's acceptance criterion 6 was amended after activation** to admit the assigned version banner, alongside the title suffix and the release-classification paragraph that records the operator's approval; the operator's merge confirms that record.
- **`CLAUDE_EFFORT` is an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9): still open, so Claude Code attestations remain `self-reported`.
- **Publish and release close.** The bounded publisher `npm run worktree -- publish WO-020` runs the committed surface check and body validation before any push and then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses at that preflight the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.6.0` is strictly above `v0.5.2`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.6.0` tag, and creates the matching GitHub Release.
