# WO-054 FINAL-001 — Final review of the worktree-snapshot profile and Codex continuation

**Verdict: pass.** All six acceptance criteria and the 2026-09-18 operator scope expansion hold at the reviewed subject. The three-report verification sequence is coherent: each failure was real, each repair addressed it by the recorded route, and nothing a verifier passed was contradicted by what this review read or ran. There is no blocking finding. One named evidence limit on the Codex adapter and seven smaller items are recorded for the next planning pass. This reviewer made one disclosed one-line source edit before the gate; it is described under "Reviewer edit" so the operator can reverse that call.

Reviewed by a Claude Code session. The session hook recorded dispatch `resume: final review` at 2026-09-18T20:58:47.857Z and allocated this path. Harness version 2.1.277 is the output of `claude --version` in this session. Model `claude-fable-5-1` is the identifier the harness supplied in this session's system context. Effort `xhigh` is `CLAUDE_EFFORT` in this session's shell, a host-supplied selection and not effective-session readback, so the source is `operator-attested`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.277","model":"claude-fable-5-1","effort":"xhigh","source":"operator-attested"}

## Subject and integration

Reviewed [WO-054](../../work-orders/WO-054-verification-over-real-worktree.md) in full with its scope expansion and execution record; the complete numbered sequence [VER-001](../../verifications/WO-054/VER-001.md), [VER-002](../../verifications/WO-054/VER-002.md) and [VER-003](../../verifications/WO-054/VER-003.md); decisions [D001–D007](../../evidence/WO-054/decisions.md); the [implementation record](../../evidence/WO-054/implementation.md), [repair-001](../../evidence/WO-054/repair-001.md), [repair-002](../../evidence/WO-054/repair-002.md), the [Codex continuation evidence](../../evidence/WO-054/codex-continuation.md) and the [ideation receipt](../../evidence/WO-054/ideation-2026-09-18.md). Source read in full: `packages/skeleton/src/verification-worktree.ts` and `packages/compiler/src/codex-continuation.mjs`. Diffs read in full: `packages/compiler/src/verification.ts`, `packages/compiler/src/harness.ts`, `scripts/lib/harness.mjs`, the verification host and protocol, the contributor loadout, the evidence-source and feedback-source lists, the runner, `.gitattributes`, `.prettierignore`, the version and lockfile moves, and the write-backs to product 02, 03, 06, 07 and 10, the root README and the skeleton README. Test suites were read for what they assert, not only that they exist. Generated surfaces, numbered evidence editions and console fixtures were judged through their checks, not by reading.

`git fetch origin main` at review: `origin/main`, local `main`, this branch's `HEAD` and their merge base are all `ce86fac6fc33eb008c03dce4cf423799c74f218d`. Nothing was integrated, no conflict was resolved and nothing was retimed. Application `v0.30.0`, compiler `0.14.0` and skeleton `0.26.0` stand under the order's minor classification; `package-lock.json` moves only `@dotln/*` workspace versions and no dependency was added. This reviewer wrote one line of `scripts/test-runner.mjs` (see "Reviewer edit"), this report, `PR.md`, `RELEASE-NOTES.md` and the work-order index refresh, and changed no other source, test, product document or generated surface.

**Instrument disclosure.** Instruments from earlier orders observed this review: the generated Claude hooks reserved this session's writer and refused this session's shell calls during its own live gate, the gate-evidence machinery recorded the gate row below, and `harness usage` supplied the counts. The instruments under review, the worktree-snapshot profile and the Codex continuation adapter, did not observe or assist this review. The adapter never ran in this Claude session.

**Fan-out.** Plan stated before any spawn: none. The new and changed source is about 2,100 lines, VER-001 had already run a five-agent adversarial pass over code that stayed byte-identical afterwards, and the surfaces no verifier judged were this reviewer's to inspect directly. Actual: 0. At 21:18:10Z `harness usage` reported count 0, cap 20, remaining 20, `exact-observed`, uncounted remainder unknown.

## The review gate

All 143 subject files were staged first, including the 76 new files, so the gate's code identity covers the new sources. That closes the gap VER-002 recorded as O5, where an identity match could not bind untracked sources and two verifiers had to fall back on timestamps.

`npm test -- --review` ran once, after the last source edit: `29 passed; 0 failed; 711.59 s; 73 fresh tasks`, exit 0. The row was recorded 2026-09-18T21:16:32.311Z for tree `7837eb27fe76d48154d8f6e3eab80299ebce593d` at code identity `aa8ef191992a0ca6d459bb023a5c01f46ac9c93d39a193110f6e43eefc528f8d`, execution mode fresh, 0 reused suites. `git write-tree` after the gate returns the same tree, and nothing is unstaged or untracked. The 29 required suites include `compiler`, `skeleton`, `codex-continuation`, `harness-fixtures`, `harness`, `harness-context`, `runner-fixtures` and the four evidence suites.

Checks run by this reviewer at the same subject, each exit 0: `node scripts/harness.mjs check` (31 generated surfaces), `node scripts/harness-context.mjs --check`, `npm run plan -- check`, `npm run publication:check`, `npm run meta -- --check`, `node scripts/release.mjs check-surfaces --local`, `node docs/evidence/WO-054/fixture.mjs --check`, `prettier --check . --ignore-unknown`, `git diff --check`, `git diff --cached --check`, the artifact-identity, authority, verification and feedback evidence checks, and `node scripts/console-fixtures.mjs --check`. The only advisory is the gate-time one already recorded as VER-003 O8.

## Document gate

With this report, the PR body and the release notes in place, `npm run release -- prepare` against `origin` reported the `v0.30.0` target current with no files changed beyond the refreshed PR meter. The first `npm run test:docs` then failed 12 passed, 5 failed in 26.38 s. The one real failure was `index`: `docs/work-orders/README.md is stale at line 3`. That was this reviewer's omission, not a subject defect: the procedure runs `npm run work-orders -- index` after dispatch, and this review had not yet done so, so the projection still showed the phase from before the dispatch. `plan`, `plan-refutation-current`, `console-docs` and `skeleton-docs` reported only "Required preflight or fixture preparation failed" behind it. After the index refresh, `npm run test:docs` passed 17 of 17 in 121.67 s with 17 fresh tasks, including the GitHub body profile over both review outputs and the format suite.

## Reviewer edit

`scripts/test-runner.mjs` described what the new `codex-continuation` suite protects as "Codex restores only its owned unfinished task after compaction and respects interruption". That label was written at implementation, when the adapter had a custom `Interrupt` hook and a stop-word parser; `refs/dotln/checkpoint/WO-054/2` carries the same text. Repair-001 removed both under the operator's D005 clarification and corrected the documents to make no interruption claim, but this string was missed. The suite's nine tests contain no interruption case, and the only occurrence of the word in the adapter is inside the context message it hands the model. The label was therefore an unsupported claim in a machine-reported description, the same class as VER-001 F7.

This reviewer changed it to "Codex restores only its owned unfinished task after compaction, continues once and yields to recovery controls", which is what the suite's tests assert. The phrase occurs nowhere else in the repository, no evidence edition or bundle pins the runner, and every edition check and `harness check` passed after the edit. The gate had to run after staging regardless, so the edit added no gate cost, and the row above is at the edited bytes.

Precedent cuts the other way and is stated here for that reason: WO-139's FINAL-002 nominated an inaccurate product 07 sentence instead of editing it. This case differs in that the text is a one-line description with no behavior, outside any followups-tracked section, and the operator's D005 direction had already settled what may be claimed. Failing the review for it would have bought a full fix and verify cycle for one string. The alternative, passing with the label wrong on `main`, would carry a known unsupported claim forward. If the operator prefers nomination, reverting the line needs a new gate.

## Acceptance criteria

| # | Assessment | Result |
| --- | --- | --- |
| 1 | `compileVerificationTask` applies `exactFields` to the snapshot, its contract, each test, each file, each evidence item, each `hostTest`, each criterion and the finding, and refuses with the path (`unexpected field $.subject.snapshot.contract.…`). The contract type is a `Pick` of six WorkOrder fields, so no worker-envelope field has a slot. `validateTransportRequest` now returns the refusal detail. Focused tests 1 and 2 assert the contract, diff, observed commit, `origin: host` witnesses and five injection paths; VER-001 reproduced twenty more. | Pass |
| 2 | `prepareWorktreeVerification` reads files from `ls-tree` at the observed commit, admits only `100644` and `100755` blobs, compares each file's bytes and mode with the Git object, requires a clean target, and refuses any overlap between the output directory and the target. Each named test gets a fresh files-only copy under `sandbox-exec` with a three-variable environment, a 30 s `SIGKILL` deadline and 64 KiB output bounds. The read mount is sealed read-only and `assertWorktreeSnapshot` re-inventories it before and after every test, at dispatch, on cached recovery, after the worker completes and before admission. Focused tests 3, 6, 7 and 8 cover tampering, recovery, confinement (outside read, outside write and TCP all `EPERM`) and self-modifying tests. | Pass |
| 3 | `node docs/evidence/WO-054/fixture.mjs --check` exits 0 with "worker pass, host contract failure, failing matrix". Focused test 4 asserts the row stays `failed` after an implementer event shaped `completed` with the summary "Everything passed"; test 5 asserts a passing verifier result cannot omit the adverse witness. The compiler derives `outcome` from `exitCode` and `signal` and refuses a witness whose stated outcome disagrees. | Pass |
| 4 | No WO-010 fixture, test or evidence file is in the diff. Legacy subjects keep positive selection; only the reserved names `snapshot` and `hostTest` are newly validated when present, which product 02 and 10 now state. The gate's `compiler` and `skeleton` suites passed. | Pass |
| 5 | Product 02 §Independent verification v1, product 03 §Ports, the skeleton README runbook and the product 10 compatibility note are present, and each claim checked against source matched: the bounds, the closed fields, the mode and blob restrictions, the timeout, the disclosed `sandbox-exec:` stderr heuristic, the statement that neither equality key authenticates a hostile writer, and the statement that named-test bytes come from the observed commit. The ledger duty for this order filed before 2026-09-09 is discharged by D001 and D002 with their index rows. | Pass |
| 6 | The gate row above; both `git diff --check` forms clean; no new dependency; `harness check` passes over 31 surfaces with the bundle pins regenerated; feedback edition `WO-054/004` is current and checks green. Both cold-start ceilings VER-002 found breached are raised by the documented route with dated acceptances naming the rule. | Pass |

## Operator scope expansion

The adapter meets each fence the expansion sets. It returns before any repository read unless the event is one of four and the input is a root session. It checks `analysis:` and `operator override:` first, again after its two read-only subprocesses, and again after writing the receipt and before blocking. It requires a matching session record, a role and expected event that agree, the canonical phase from `resume.mjs status --json`, an outstanding completion event counted from the recorded starting event, and a writer reservation whose `actorId` equals the SHA-256 of the hook's `session_id`. One continuation per compacted turn is enforced by an exclusive `wx` receipt. Its only subprocesses are `git rev-parse`, `resume.mjs status --json` and `harness.mjs writer --show`; it dispatches nothing and acquires or releases no writer. Every failure path returns `{}` or an advisory, leaving native stopping available. Emission refuses to replace a pre-existing `.codex/` file the manifest does not own, before any bundle write, and target-worker profiles strip the capability; both have regressions.

The evidence separates what is native from what is synthetic and claims no installed behavior: the repair probe ran the current generated bytes on Codex CLI 0.155.0 (its three surface hashes match this worktree, per VER-002), with synthetic status and ownership, and the documents say desktop activation is not established.

**Named limit, not blocking: the session-identity join is unestablished.** A production Codex role creates its record with `node scripts/harness.mjs begin` and a session argument the role supplies; the other scripts read `CODEX_THREAD_ID`. The adapter looks that record up by the SHA-256 of the hook payload's `session_id`. In every native probe the wrapper initialized the session and writer records from the hook's own `session_id` ("writer query returns the initialized native actor hash"), and the direct invocations against this worktree supplied the id by hand. The Codex role text names no id to pass to `begin`, and no WO-054 test or document asserts the two values are equal. Whether they are equal is unknown. If they differ, the adapter finds no record and returns `{}`: fail-safe, and a silent no-op in exactly the case the expansion exists for. It is not blocking because the order asks for native capability evidence and regression tests and forbids treating documentation as a claim of installed behavior, all of which holds; because D005 already reopens when "native hook activation is unavailable in the actual host"; and because the checked 0.155.0 loader gives linked worktrees their hook definitions from the root checkout, so activation cannot be observed from this branch before merge. The first diagnostic after merge, if a Codex session still idles after compaction, is to compare the hook's `session_id` with the id that session passed to `begin`. A sanitized equality observation in the native probe would settle it.

## Verification sequence

VER-001 failed correctly on F1, a generated hook that broke the repository format gate where no product suite could see it, and F2, a stop-word parser that missed ordinary wordings. VER-002 confirmed F1 repaired with a regression that fails when the exemption is removed, accepted F2 and F4 as resolved by removing the mechanism under the recorded D005 direction, and failed on N1, two cold-start ceilings breached and left advisory; VER-001 had missed N1 although the breach was present at its subject. VER-003 confirmed N1 settled by the documented route and re-ran the two machinery readers of the real budgets file. D004 is a candid record that the first F2 repair enlarged a speculative regex instead of asking what the operator's actual failure was. The F3 heuristic, the F5 event-boundary behavior and the F6 reserved names were resolved by narrowing the documents to the observed boundary, which this review confirmed against source. Nothing in the sequence routes a finding against a sibling's staged version.

## Ideation breakout

VER-003's author wrote these surfaces and did not judge them, so this review did. The [receipt](../../evidence/WO-054/ideation-2026-09-18.md), the ledger entry "Out-of-project file effects as an explicit grant", the dated paragraph in product 03 §Candidate — DotLn-owned authority with minimal native harness restrictions, and correction D007 were each read.

- **Clean room.** Public vocabulary only; no employer material, credential or private identifier; no raw intake text is copied and the direct-filing exception is not used. No stop condition.
- **Traceability.** The ledger entry names its capture and receipt; the product paragraph links the receipt; D007 cites product 07 §Operator-opened ideation mode, and that anchor and the ledger anchor both exist.
- **Accuracy.** The one checkable factual claim, that product 03 records neither native sandbox confining a sibling write (C-W6, X-W6), is true at `HEAD` lines 652 and 1533. The statement that planning branches admit external paths matches the compiled four-refusal sentence.
- **Nothing speculative presented as settled.** The ledger status is `preserved`, the product text sits in a Candidate section with four open choices listed, the tension with the planning-branch refusal is named as one for planning, and the operator's sandbox recollection is kept as recollection.
- **Scope.** Documents only. No schema, implementation, generated surface, setting or release classification changed, and the order text was not amended.

D007 records the verifier's handling as a failure under shifting the burden to the intervenor, with what was misread, what was meant and what changed. That is the required correction form.

## Clean room over the whole subject

A screen of all 143 subject files for home-directory paths, usernames, email addresses, credential patterns, session or thread identifiers and non-public URLs matched nothing this branch added. The UUIDs the screen found in the ledger are present at `HEAD`. The three Codex probe records carry model name, CLI version, booleans, hashes and fixture text, and no session, thread, turn or account identifier.

## Non-blocking items for the next planning pass

1. **Session-identity join** for the Codex adapter, as above. Add a sanitized equality observation to the native probe, or name the id in the Codex role text and assert it.
2. **Gate time.** This gate took 711,589 ms against the 120,000 ms `fastGateMs` budget; the two executor gates took 368,205 and 344,065 ms. Product 07's same-change route covers cold-start ceilings and not this metric (VER-003 O8).
3. **Ceiling step wording has two readings** (VER-003 O7): `budgets.json` says "to the measured bytes plus one 4 KB step", product 07 says "by one 4 KB step above the measured bytes", and all five acceptances used the previous ceiling plus 4,096. Write one reading down.
4. **Code identity and untracked sources** (VER-002 O5). Two verifiers bound new sources by timestamp because the identity covers tracked files only. Staging before the verifier's check, or a verifier-side temporary-index identity, would remove the inference. An instrument from an earlier order, outside this one.
5. **Same-turn typed stop** (VER-002 O6). The restored context tells the model the last message may be old steering and also that explicit interruption stays decisive. The operator assigned that judgment to the model in D005; it is recorded, not a finding.
6. **The `sandbox-exec:` stderr heuristic** (VER-001 F3) can downgrade a failing witness to unavailable. It cannot produce a pass and product 02 discloses it. D003 names the reopening condition: a real target that needs trustworthy launcher attribution.
7. **Intermediate evidence editions.** Editions 001 to 003 of four evidence families were never current and are committed as preserved history (VER-001 O3). A retention rule would bound this growth.
8. **Out-of-project file effects.** The operator set the ideation for priority at the next planning pass; the incident class recurred harmlessly in this review's own command construction, where an unused helper named a system temporary path. Nothing was written there, which this reviewer checked.

## Goal and handoff

Mission contribution: the critical path needs an independently verified source-to-deliverable loop, and this order moves verification from a synthetic repository to a real committed worktree with evidence the implementer cannot author. The material choices in this review were the verdict, the one-line label edit, and treating the session-identity join as a named limit and not a finding.

Policy resistance and rule beating: the pass rests on a fresh gate at an identity that covers the new sources, and on reading what the tests assert, because VER-002 showed every check can exit 0 while a documented duty is unmet. Drift to low performance: the documents were narrowed to observed boundaries in three places and none was widened; the ceilings rose with no efficiency claim, which D006 tracks. Seeking the wrong goal: repair-001 deleted an invested-in parser when the operator described the real failure, and this review held the last trace of that parser, the runner label, to the same standard. Shifting the burden: the session-identity limit is written with its first diagnostic so a recurrence does not start from nothing. Commons and escalation: no agents were spawned against a budget of twenty, and a one-string defect did not become a repair cycle. Success to the successful: the executor's own probes received no exemption; the question they could not answer, because their wrapper supplied the identity, is the limit recorded above. Naive Interventionism: the reviewer edit touches one description, changes no behavior, was checked against every pin, and is disclosed with the contrary precedent. NoOp on the verdict, failing for the evidence gap, would hold a verified profile behind a question that the checked loader makes unanswerable before merge.

Process cost: at entry (20:59:20Z) 196,694 total tokens; at 21:18:10Z 6,245,842 total tokens, of which 5,997,961 were cached input, 212,941 cache writes and 34,858 output, across 67 steps and 40 commands. Both figures are dispatch scope from `claude-transcript-message-usage`; USD and reasoning tokens are unknown. About 712 s was gate waiting, during which the live-gate refusal blocked this session's shell. No cheaper equivalent outcome was available: no recorded row could match once the new sources were staged. Final counters remain in ignored receipts and the handoff response.

On this pass the reviewer records the result, commits the reviewed state as a coherent series, pushes `wo-054` and opens its pull request. No merge, push to `main`, tag, release or package publication is performed.
