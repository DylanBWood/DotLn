# WO-055 FINAL-001 — Final review of the repair continuation

**Verdict: pass.** All six acceptance criteria hold at the reviewed subject. The verification sequence is one report, VER-001, which passed; nothing it passed was contradicted by what this review read or ran. There is no blocking finding. VER-001's five observations are judged below, this review adds three of its own, and all eight are recorded for the next planning pass. This reviewer changed no source, test, product document or generated surface.

Reviewed by a Claude Code session. The session hook recorded dispatch `resume: final review` at 2026-09-18T23:24:41.353Z and allocated this path. Harness version 2.1.277 is the output of `claude --version` in this session. Model `claude-fable-5-1` is the identifier the harness supplied in this session's system context after the operator's `/model` selection. Effort `xhigh` is `CLAUDE_EFFORT` in this session's shell, a host-supplied selection and not effective-session readback, so the source is `operator-attested`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.277","model":"claude-fable-5-1","effort":"xhigh","source":"operator-attested"}

## Subject and integration

Reviewed [WO-055](../../work-orders/WO-055-repair-continuation.md) in full; the complete numbered sequence, [VER-001](../../verifications/WO-055/VER-001.md); decision [D001](../../evidence/WO-055/decisions.md); the [implementation record](../../evidence/WO-055/implementation.md); the [portable proof](../../evidence/WO-055/repair-proof.json) and its [fixture](../../evidence/WO-055/fixture.mjs); and the order's control log. Source read in full: `packages/skeleton/src/repair.ts`, `packages/skeleton/src/repair-host.ts`, `packages/skeleton/test/repair.test.ts` and `packages/skeleton/test/repair-fixture.ts`. Diffs read in full: `reactor.ts`, `source-change-host.ts`, `worker-protocol.ts`, the feedback-source, evidence-source and harness runtime-file lists, the two adjusted architecture tests, the version and lockfile moves, and the write-backs to product 02 and 06, the root README, the skeleton README, the decisions index and `followups.json`. Two existing mechanisms the criteria depend on were read at source and not taken from the reports: kernel `stepProgram` (`Invoke`, `Await`, `Guard`, `Sequence`) and `SourceChangeWorktree.effect()`. Generated hooks, numbered evidence editions and console fixtures were judged through their checks, not by reading.

`git fetch origin main` at review: `origin/main`, local `main` and this branch's `HEAD` are all `d428180b3e7e89d2f0e70e74dda1d2a538d62078`. Nothing was integrated, no conflict was resolved and nothing was retimed. Application `v0.31.0` and skeleton `0.27.0` stand under the order's minor classification; `package.json` and `package-lock.json` move only the skeleton version and no dependency was added. This reviewer wrote this report, `PR.md`, `RELEASE-NOTES.md` and the work-order index refresh.

**Ideation receipt.** The dispatch briefing names one; none exists for this order. `docs/evidence/WO-055/` holds no ideation record and the control log has no ideation row, so there was nothing of that kind to judge.

**Instrument disclosure.** Instruments from earlier orders observed this review: the generated Claude hooks reserved this session's writer and refused this session's shell calls during its own live gate, the gate-evidence machinery recorded the row below, and `harness usage` supplied the counts. The repair host under review did not observe or assist this review.

**Fan-out.** Plan stated before any spawn: none. The subject is 1,336 new lines in four source and test files plus a 370-line reactor diff, small enough to read directly, and VER-001 had already probed it 40 ways at bytes that have not changed since. Actual: 0. At 23:48:36Z `harness usage` reported count 0, cap 20, remaining 20, `exact-observed`, uncounted remainder unknown.

## The review gate

All 23 new files were staged first, including the four new source and test files, so the gate's code identity covers them.

`npm test -- --review` ran once, with no source edit before or after it: `28 passed; 0 failed; 1070.80 s; 72 fresh tasks`, exit 0. The row was recorded 2026-09-18T23:46:35.290Z for tree `c409ac877d25d90ae352ecb9e3514469aca4dd95` at code identity `5c22a602c45d0736c65a9d5a650420c2278289f8248d6de852c2db922bdf5187`, execution mode fresh, 0 reused suites. Recomputing `gateTreeHash` and `gateCodeIdentity` after the gate returned the same two values. The `skeleton` suite passed in 360.93 s and its log shows all six WO-055 tests: the pure AC1 derivation, AC2, AC3, both AC4 SIGKILL cuts and the host-level AC1 refusal.

Checks run by this reviewer at the same subject, each exit 0: `npm run build`, `node docs/evidence/WO-055/fixture.mjs --check`, `npm run plan -- check`, `npm run publication:check`, `node scripts/release.mjs check-surfaces --local`, `npm run harness -- check` (31 generated surfaces; local terms unavailable, so no terms assessment is claimed), `npm run evidence:verification -- --check`, `git diff --check` and `git diff --cached --check`. A search of the changed source and the new files found no new `eslint-disable`, `@ts-ignore`, `@ts-expect-error`, `@ts-nocheck` or `prettier-ignore`, and no attribution trailer, footer or session link.

## Document gate

After the gate this reviewer ran `npm run work-orders -- index`, then wrote this report, the PR body and the release notes. `npm run release -- prepare` against `origin` reported the `v0.31.0` target current with no files changed beyond the refreshed PR meter. `npm run test:docs` then passed 17 of 17 in 129.10 s with 17 fresh tasks, including `format`, `index`, `meta`, `publication`, `release-surfaces`, the planning checks and the four evidence suites over these outputs. None of these writes touches tracked non-generated code, so the gate's code identity is unchanged; it was recomputed after them and still reads `5c22a602…`.

## Acceptance criteria

| # | Assessment | Result |
| --- | --- | --- |
| 1 | `deriveRepairOrder` resolves each evidence id only to a witness with the finding's criterion and the subject's revision, and requires a failing witness whose `observed` and `expected` equal the finding's. Surfaces are the finding's likely surfaces plus those witnesses' `codeSurfaces`; each must pass `repositoryPath` and lie inside the original surfaces by exact match or `surface/` prefix. Each reproduction must match the exact-command pattern and equal a named test. The derived order spreads the original WorkOrder, keeps the envelope object, the operation lists, the base and the contract, clears `knownFacts` and `decisions`, and sets `round + 1`. No references, an outside path and an outside command each return `NeedsHuman` naming the input. A grant must equal a host registry record in full, name the order's repo, use no wildcard, stay inside the original effects and operations, and cover exactly the extra scope; its id enters `grantIds` and the envelope is unchanged. The host-level test asserts status `needs-human`, a null order, zero writer launches and no `repo.write` command in the log; VER-001 compared the persisted `RepairOpened` envelope bytes and reproduced 28 further cases. | Pass |
| 2 | The focused test gives one writer launch and two verifier launches, both capsules carrying `repairContract(original)` and the original criteria, the writer prompt free of the `IMPLEMENTER_NARRATIVE_SENTINEL` facts, `git diff --name-only` from the failed commit to the repair equal to `fixture.txt`, and replay equal to live state. The diff limit is host-enforced: `SourceChangeWorktree.effect()` measures from `requested.baseCommit`, which is now the failed revision, and refuses any committed path outside the derived surfaces. The reactor's verify-result check refuses a capsule whose contract, criteria, tests, base or revision differ from the original's and the observed commit. | Pass |
| 3 | The round program ends in `Guard(repair.verified)` then `Guard(repair.remaining)`, where remaining is `round < (roundLimit ?? 2)`. Two wrong repairs give status `exhausted`, round 2, two writer launches, three verifier launches, a terminal `RepairExhausted` whose finding equals the state's, next action `human` and no `source-3` store. The fold refuses exhaustion without a last finding. VER-001's table over limits 0, 1 and 3 shows the field is a limit and not a constant. | Pass |
| 4 | Both SIGKILL cuts pass in the gate: after the child commit and before the parent observation, and after snapshot preparation. Recovery restores the persisted program by replay, `SourceChangeHost.run()` returns the saved observation from the child receipt without dispatch, and a saved verification result is reused. VER-001 added six cut points after each durable parent event; each resumed to `complete` with exactly one writer launch. Kernel `Invoke` re-offers its intent until a matching `CommandResult` arrives, which is why a repeated `RepairTick` after resume cannot advance or duplicate a dispatch. | Pass |
| 5 | Product 02 §Independent verification v1 describes the mechanism as built: resolution against pinned witnesses, containment, exact registry grants that cannot add effects, `executionBaseCommit`, the persisted `Sequence` of `Invoke`, `Await` and `Invoke` with `Guard` branches, the ownership acknowledgement before awaiting, `RepairExhausted`, `NeedsHuman` and the WO-056 boundary. Each of those claims was checked against source and matched. The skeleton README gains the component `0.27.0` paragraphs with a runnable reproduction, product 06 records the activation, and the root README names `v0.31.0`. The ledger duty for this order filed before 2026-09-09 is discharged by D001 and its index row, as the work-order index states. | Pass |
| 6 | The gate row above; both `git diff --check` forms clean; no new dependency; `harness check` passes over 31 surfaces with `repair.js` added to the pinned runtime files; `docs/evidence/current.json` selects the WO-055 editions and the gate's four evidence suites passed against them. | Pass |

## Verification sequence

VER-001 is the only report and it passed without a repair cycle. It reproduced the executor's focused suite, fixture check and full gate on its own build, then went past them where the implementer's tests were thin: 28 derivation and grant cases including the prefix-sibling path `fixture.txtx`, shell composition in a command, a foreign subject revision and a grant that differs from its registry record; six kill points after each durable parent event; and four round-limit settings. It bound its subject by comparing its temporary-index tree with checkpoints 2 and 3, which differ only in control files and the generated index, so the source it judged is the source the executor handed off. The same source reached this review: no source file has changed since. Nothing in the sequence routes a finding against a sibling's staged version.

## Judgments on the recorded observations

- **VER-001 O1, cited test files are repair surfaces.** Confirmed at `repair.ts`: witnesses contribute the criterion's `codeSurfaces`, so the derived surfaces here are `contract-test.mjs`, `fixture-test.mjs` and `fixture.txt`. This is what the order asks for ("the files the finding names plus the tests it cites") and product 02 says so. The consequence stands: a repair writer may edit a named test that re-verification then runs. The command names and the contract cannot change, and test bytes are already subject content under WO-054's snapshot model, where the verifier sees them in its diff. Not blocking for a doubles proof; it matters for WO-056's live loop and WO-066.
- **VER-001 O2, a malformed path names `finding`.** Confirmed. Through the host the case is unreachable because `parseEvidenceResult` applies the same `copyFinding` check first, and the criterion's outside-path case names the path.
- **VER-001 O3, `roundLimit` placement.** The order's Design line says "a WorkOrder field"; the field is on `RepairOriginal`, beside the WorkOrder. No acceptance criterion names the type, the operator-review assumption asks only that the field be per order, and VER-001's table shows per-order behavior with the default of two. Placing it on the repair original kept the compiler contract at `0.14.0`, consistent with the order's classification note of no schema change beyond the new event types. Product 02 and the README describe the actual placement. Judged met in substance. The order text is not amended: that needs an operator scope decision this review does not have.
- **VER-001 O4, stale skeleton README heading.** The heading reads `0.24.0` and was already stale on `main` at `0.26.0`. Outside this order; nominated below.
- **VER-001 O5, the `Await` timeout is declarative.** Confirmed in kernel `stepProgram`: an unmatched `Await` returns itself in `waits` and no branch evaluates the `After 180000 ms` timeout. A stalled child relies on the WO-052 and WO-054 host deadlines, leases and locks. The order requires no timeout branch.
- **R1, the writer prompt changed for every source-change writer.** `writerPrompt` now includes `surfaces` when the command payload carries them, and its output instruction reads "Edit only the declared surfaces within the worktree mount" where it read "Edit only within the declared worktree mount". This reaches WO-052 and WO-053 writers, not only repairs. It narrows the instruction to what `effect()` already enforces, no test pinned the old wording, and the gate's skeleton suite passed. The implementation record does not call it out, so the release notes do.
- **R2, one unused import.** `repair.test.ts` imports `writeFileSync` and never uses it. No lint gate covers it. Left unedited so the verified source stays byte-identical; nominated below.
- **R3, requested grants and the registry arrive together.** `RepairHostOptions.grants` carries both lists, so "separately admitted" means separate from verifier output, which is the refuted risk: no field of a finding can supply or select a grant. It does not mean two independent host inputs. The implementation record and VER-001 already state that the registry is trusted and unauthenticated.

## Clean room over the whole subject

A screen of the changed tracked lines and every new file for home-directory paths, usernames, email addresses, credential patterns and URLs matched nothing in source or authored documents. Its only hits are inside the generated `feedback/selfhost-verification.jsonl` edition, which captures this repository's own files: public npm-registry and funding URLs from `package-lock.json` and the `example.invalid` fixture string from an existing test. No stop condition.

## Non-blocking items for the next planning pass

1. **Protect cited tests from a repair** (O1). For WO-056 and WO-066, consider excluding a criterion's test files from the repair surfaces unless the finding names them, or judging a repair that touches its own oracle differently.
2. **`Await` timeout** (O5). The live loop needs either a kernel timeout branch or a recorded decision that host deadlines are the only stall bound.
3. **`roundLimit` wording** (O3). The order's Design line and the built placement differ; record one of them as the contract before WO-066 reuses the derivation.
4. **Writer prompt wording** (R1) reached earlier orders' writers through this one. The next live source-change run is the first observation of it with a real model.
5. **Skeleton README heading** (O4) and the unused test import (R2): bounded cleanup.
6. **Gate time.** This gate took 1,070,798 ms against the 120,000 ms `fastGateMs` budget; VER-001's took 519 s and the executor's 525 s for 20 suites. WO-054's final review recorded the same class at 711,589 ms.
7. **Malformed-path naming** (O2) and the single-input grant shape (R3) are recorded limits, not defects.
8. **Probe scripts.** VER-001's three probe scripts live in that session's scratchpad and are quoted, not committed. Its results are reproducible from the descriptions; the six kill points would be cheap to keep as tests.

## Goal and handoff

Mission contribution: the critical path needs an independently verified source-to-deliverable loop, and this order closes the step between a negative verification and the next attempt, which until now was an operator handoff between two hosts. The material choices in this review were the verdict, leaving the source unedited, and treating the `roundLimit` placement as met in substance.

Policy resistance and rule beating: the pass rests on a fresh gate at an identity that covers the new sources, on reading what the six tests assert, and on reading `effect()` and `stepProgram` directly, because AC2's diff limit and AC4's no-second-dispatch both depend on mechanisms outside the new files. Drift to low performance: no claim in the write-backs is wider than the source, and the proof says "doubles" wherever it appears. Seeking the wrong goal: launch counts are evidence here only because the tests pair them with committed diffs, contract equality and replay. Shifting the burden: each observation above carries the consumer that will meet it first. Commons and escalation: no agents were spawned against a budget of twenty; two cleanups and a wording mismatch did not become a repair cycle or a second gate of nearly eighteen minutes. Success to the successful: VER-001's pass received no deference where this review could check source itself, and those checks agreed with it. Naive Interventionism: WO-054's final review made a one-line reviewer edit; this one declines the analogous edit because the unused import asserts nothing false, unlike that label, and editing would have moved the source away from the bytes VER-001 judged. NoOp on the verdict, failing for O3's wording, would hold a verified mechanism behind an order-text question that no criterion asks.

Process cost: at entry (23:25:07Z) 260,397 total tokens; at 23:48:36Z 9,478,007 total tokens, of which 9,250,327 were cached input, 199,929 cache writes and 27,621 output, across 75 steps and 37 commands. Both figures are dispatch scope from `claude-transcript-message-usage`; USD and reasoning tokens are unknown. About 1,071 s was gate waiting, during which the live-gate refusal blocked this session's shell and the review continued by file reads. No cheaper equivalent outcome was available: no recorded row could match once the new sources were staged. Final counters remain in ignored receipts and the handoff response.

On this pass the reviewer records the result, commits the reviewed state as a coherent series, pushes `wo-055` and opens its pull request. No merge, push to `main`, tag, release or package publication is performed.
