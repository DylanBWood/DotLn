## What this merges

WO-010, the `v0.12.0` minor release above `v0.11.0`: independent verification as blinded verifier episodes, claim-typed evidence, focused repair continuation and a living acceptance matrix. The pure compiler builds a `verification-v1` capsule by positive construction, so a verifier or repairer receives exactly the acceptance criteria, the pinned diff, an explicit host-read repository snapshot, the host's own witnesses and, for a repairer, one complete blocking finding; implementer narrative, transcripts and result prose have no compiled path, and a test proves it under a throwing clock and random source. The skeleton's shared typed reactor gains a verification branch: the workstream opens with baseline and candidate snapshots and a host authority envelope, dispatch runs through the kernel's `Program.Invoke`, `authorize` and the durable `CommandPersisted` outbox to the WO-009 transports, and acceptance admits only a host-actor result with `verificationResultVersion: 1` for the pending command from a producing episode the workstream started, under a live lease and a current grant, over an unchanged subject. The result schemas are closed: a pass needs every required check witnessed passing and no adverse witness for that check anywhere in the subject; a failure needs a failing witness and a complete finding whose observed and expected values match one; a repair proposal may touch only the finding's likely surfaces and must change something. Each blocking finding compiles a focused repair WorkOrder, one repair is applied at a time, the fold derives affected criteria from the changed source bytes rather than from an implementer-supplied impact list, affected evidence is marked stale while old evaluations and unaffected rows are preserved, and a fresh blinded verifier rechecks the changed subject. `projectAcceptanceEvidenceMatrices` is a pure replay that both `dotln status` formats fold, and `dotln verify-demo` drives the synthetic planted-defect loop from the command line. The kernel is byte-unchanged; no dependency was added.

- **Criterion 1, the planted defect caught, repaired in a fresh episode and re-verified green through the kernel's event loop:** from a clean store the shipped CLI records 31 events; the first verifier fails `AC-selection` with observed `["unused.tmp","used.tmp"]` against expected `["unused.tmp"]` from a real Node subprocess over the pinned interpreter, a fresh repairer proposes only `policy.json`, the host applies it and a third episode passes; the three episodes run in three distinct worktrees, `decideProgram` emits exactly three intents, no command is left pending, and replaying the log through `seiriReactor` reproduces every live Decision.
- **Criterion 2, the implementer never certifies itself, structurally:** the compiled capsule's key sets are exactly the domain model's pinned field lists and `prohibitedOperations` includes `transcript.read` and `self.certify`; the verifier's sentinel and this review's own sentinel each reach the host log exactly once, on `ImplementationReported` by the implementer, and never a capsule; an identical accepted result re-emitted by the implementer actor, a forged all-pass result, a pass asserted over a failing witness, a synthetic `CriterionVerified` type and a repair result retyped as a verification are all ignored or refused with the rows unchanged.
- **Criterion 3, full VerificationFinding shape and focused repair WorkOrders:** the recorded finding carries every pinned field; the compiler refuses an incomplete or non-blocking finding and a surface outside the criterion; the protocol refuses a fabricated observation; two blocking findings compile two distinct repair plans; proposals outside the surface, adding files, changing nothing or returning nothing are refused; a zero repair bound stops at `attention` with the criterion still failed.
- **Criterion 4, substantive repair marks affected evidence stale, visibly:** the staleness record is `policy.json` affecting `AC-policy` and `AC-selection` while `AC-description` keeps its verified status, evaluation and revision; mid-repair `dotln status` prints `AC-selection behavior stale` and the JSON carries `stale: true` on the old evaluation; status reads leave the store bytes unchanged.
- **Criterion 5, the matrix lives and updates through the workstream:** there is no matrix file; rows advance `incomplete` to `verified/failed/verified` to `stale/stale/verified` to all `verified` on each appended event; every evaluation carries host-derived provenance, the producing episode, the subject revision and the stale flag; the projection reproduces identically with the clock and randomness stubbed to throw.
- **Evidence gate and write-backs:** the recorded loop, mid-repair and final projections are re-derived byte for byte by `scripts/verification-evidence.mjs --check` inside `npm test`; every criterion maps to named tests in the executor's receipt; the domain model, architecture, roadmap, IR compatibility, planning map, publication index and both audience locks, staleness demonstration, root README release block, both package READMEs, the work order's activation header and the ledger are updated consistently.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` on the verified tree after dispatch and again on the reviewed tree: Prettier clean, the body-profile suite, every shell suite, `check-publication.mjs` at 230 of 230 headings with both audience locks current, `index --check` current, `tsc -b --force`, 263 of 263 package tests including the eighteen WO-010 tests, 8 of 8 identity-corpus tests, the four current artifact-evidence files and the four synthetic verification evidence files verified; exit 0 both times. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.12.0; expected v0.12.0`, `src changed` with advanced versions for the compiler (`0.5.0` above `0.4.0`) and the skeleton (`0.11.0` above `0.10.0`), `src unchanged` for the kernel at `0.2.1`, and the body profile over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity: a temporary-index tree of the working tree differs from the implementation-ready checkpoint only in the control projection, the appended control segment, the verifier's report and the regenerated index, so every deliverable blob the verifier judged is the one the executor declared ready; all five checkpoints have `HEAD` (`1a81e00`, also `main` and `origin/main`, the published `v0.11.0`) as their sole parent and their commit times match the segment's stamps to the second; `git log main..wo-010` was empty before the reviewed series; VER-001's bytes are identical in its verdict checkpoint and the committed tree; the verifier's report header equals its control-log actor by JSON comparison; the executor's `max` and the verifier's `max` meet the declared `xhigh+` floors.
- Direct probes: the shipped `verify-demo` from a clean canonical store returned the same envelope the executor's smoke receipt and VER-001 record (`verification_3`, `ep_verifier_3_attempt_1`, `result_cmd_a55351b8ec012a11`, completed), `status --json` printed exactly the five top-level keys the domain model describes with the matrix at `complete` and the stale history preserved, a rerun on the completed store returned the identical envelope with the 31-line log byte-identical and no worktree left, a store under macOS `/tmp` was refused while the identical command under `/private/tmp` completed, a reviewer-chosen sentinel reached the log once and no capsule, a link sweep over the 18 changed Markdown files resolved 593 links, a clean-room term screen and a secret scan over every changed and new file found only pre-existing boundary prose and the publication locks, and the public gitmoji catalog confirms `:safety_vest:` as "Add or update code related to validation".

## Verification trail

- `docs/verifications/WO-010/VER-001.md`: pass; all five criteria, the evidence gate, the activation completion and every write-back reproduced independently by driving the shipped API with the verifier's own sentinel and forged results, enumerating the capsule's key sets, re-running the loop end to end from a clean store and re-running the full gate; two bounded documentation corrections, four non-blocking observations and three disclosures, none requiring repair.
- `docs/final-reviews/WO-010/FINAL-001.md`: pass; the verifier's corrections applied and its observations adjudicated with reasons, the reviewer's own observations recorded, the coherent two-commit series confirmed, four bounded documentation corrections applied and re-checked, method and instruments disclosed.

## Known open items

- **Live model verification is unwitnessed** (VER-001 not verified): every witness is `synthetic-fixture`, the verifier in every run is the deterministic fixture projection, and the `live` evidence source has no producer; the launch path exists but no authenticated model was invoked. The mechanism, not model judgement, is what this release proves.
- **A repository-relative store cannot be exercised inside the Claude Code sandbox** (FINAL-001 adjudication 1): the nested fixture repository's `git init` is refused by the harness's protection of Git configuration and hooks under the project, before any product guard runs; the WO-009 runbook's demo performs the same nested init. The runbook now uses the gitignored `.runtime/` root and names the canonical-path rule; run it from an ordinary shell.
- **The CLI's refusal message is deliberately generic** (FINAL-001 adjudication 8): every worker-host refusal prints the same sentence, so a symlinked store path and a sandbox denial are indistinguishable at the command line; a diagnostic hint that names no path is a candidate, not a correction.
- **`verificationReactor` has no production caller and `repairPlans` is never pruned** (VER-001 O2 and O3): the compatibility projection is exercised by tests only, and compiled plans for resolved findings stay in state without being read by dispatch.
- **The WO-022 artifact-identity receipts are no longer byte-checked** (VER-001 O1): the helper checks the current edition under WO-010; a dated migration note in the WO-022 evidence README records this, the WO-029 note continues to point at WO-022, and a hash pin for historical receipts remains a patch candidate.
- **`CLAUDE_EFFORT` remains an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9), so Claude Code attestations stay `self-reported`.
- **Publish and release close.** The bounded publisher runs the committed surface check and body validation before any push and then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses at that preflight the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.12.0` is strictly above `v0.11.0`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.12.0` tag, and creates the matching GitHub Release.
