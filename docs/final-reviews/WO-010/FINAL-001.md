# WO-010 final review FINAL-001

**Subject:** branch `wo-010`, worktree `DotLn-wo010`, uncommitted working tree, 2026-09-06. `HEAD`, `main`, and `origin/main` are all `1a81e008611731883d43eb19743c5dcb9205b062`, the PR #38 merge of WO-022 (`git ls-remote origin main` agrees), which is also the commit the published annotated tag `v0.11.0` (tag object `539021143175b03184fc8f3ea580a1b6491ab4ea`, the same object locally and on `origin`) peels to. The branch carries no local commits (`git log main..wo-010` is empty), so the entire subject is the dirty tree. At review open, before the dispatch transition: 54 `git status --porcelain -uall` entries, 32 tracked modifications (against `HEAD`, 32 files, 949 insertions / 90 deletions including `docs/control/current.md` and the generated `docs/work-orders/README.md`; 30 files, 919 / 77 without them) plus 22 untracked paths totalling 8,148 lines: six source leaves (`packages/compiler/src/verification.ts` 387; `packages/skeleton/src/verification-protocol.ts` 423, `verification-host.ts` 372, `verification-demo.ts` 528, `verification-fake.ts` 121, `verification.ts` 114; 1,945 in all), the subprocess double `packages/skeleton/fixtures/verification-cli.mjs` (42) and `scripts/verification-evidence.mjs` (80), two test files (`packages/compiler/test/verification.test.ts` 164, `packages/skeleton/test/verification.test.ts` 755; 919), the order's own control segment `docs/control/orders/WO-010.jsonl` (four events at open), `docs/evidence/WO-010/` (`README.md` 64, `cli-smoke.txt` 22, `events.jsonl` 31, `matrix.json` 387, `stale-status.json` 423, `stale-status.txt` 17, and the four `artifact-identity/` receipts totalling 3,847), and `docs/verifications/WO-010/VER-001.md` (366). The subject is the tree VER-001 passed: a temporary-index `write-tree` of the working tree taken after the dispatch and the index refresh (`5bc5b05724119548892bd90ed8e3e430310445c8`) differs from the implementation-ready checkpoint only in the control projection, the appended segment, the verifier's report and the regenerated index (§Dispatch and subject integrity), and VER-001's own bytes are identical in its verdict checkpoint, in this review's allocation checkpoint and in the working tree. This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, four bounded documentation corrections (§Corrections applied by this review), and the lifecycle's own events.

## Actor

This review ran on the Claude Code CLI, version `2.1.263` (`claude --version` observed locally in this session), model `claude-fable-5-1` (Fable 5.1), at reasoning effort `max` selected by this session's model control. The value is self-reported: `max` is on this harness's `sessionEffortSelector.values` in `docs/discovery/environment.json`, `2.1.263` is on its observed `versions` list, and `effectiveEffortReadback` for `claude-code` remains `not found` with `harnessReadbackEligible: false`, so `harness-readback` is unavailable and `self-reported` is the honest label. The session environment again exposes a `CLAUDE_EFFORT=max` variable; that remains the unrecorded readback candidate WO-023's review named, and it does not change the label. The work order declares `reviewer any`, so no floor applies; the executor (Codex CLI `0.153.4`, `gpt-6-astra`, `max`, operator-attested) and the verifier (Claude Code `2.1.263`, `claude-opus-5[1m]`, `max`, self-reported) were different sessions, and this reviewer is a third model in a third session. No `--account-label` is supplied, so this header carries no `accountLabel` and the recorded event will project `not-applicable`. This review needed no discovery change.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-010 passes.** The order asked for blinded verifier episodes as a first-class mechanism: claim-typed evidence mapped into a living acceptance matrix, typed findings driving a focused repair in a fresh episode, substantive repair marking affected evidence stale, and an implementer that structurally cannot certify itself. The deliverable does that. The pure compiler builds a `verification-v1` capsule by positive construction, so a verifier or repairer receives exactly the criteria, the pinned diff, an explicit host-read snapshot, the host's witnesses and, for a repairer, one complete blocking finding, and a test proves the construction pure and narrative-free under a throwing clock and random source. The shared typed reactor's verification branch drives dispatch through the kernel's `Program.Invoke`, `authorize` and the durable `CommandPersisted` outbox, and admits only a host-actor result with `verificationResultVersion: 1` for the pending command, from a producing episode the workstream itself started and that is not an implementer episode, under a live lease and a current grant, over an unchanged subject. The result schemas are closed: a pass needs every required check witnessed passing and no adverse witness for that check anywhere in the subject, a failure needs a failing witness and a complete finding matching it, and a repair may touch only the finding's likely surfaces and must change something. Each blocking finding compiles its own focused repair WorkOrder, one repair is applied at a time, the fold derives affected criteria from the changed source bytes, affected evidence is marked stale while old evaluations and unaffected rows are preserved, and a fresh blinded verifier rechecks the changed subject; a human disposition, a failure without a blocking finding and an exhausted repair bound all leave `attention` rather than a verified completion. The matrix is a pure replay that both `dotln status` formats fold, and the shipped `dotln verify-demo` runs the planted-defect loop from a clean store to `complete` with 31 events and no pending command. The kernel is byte-unchanged, no dependency was added, the suite is green twice, whitespace is clean, and the release surface passes at `v0.12.0`. VER-001's two bounded corrections are applied, its four observations and three disclosures are adjudicated below as non-blocking, and this review records observations of its own, of which one (a repository-relative store cannot be exercised inside this harness's sandbox) is a harness limit rather than a defect. Nothing in this review changes code, a contract, acceptance behavior, a schema, a compatibility axis, an authority boundary, or a captured-evidence byte.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **The deliverable is a verification mechanism, and this report is a human-role reviewer judging a machine verifier.** The fixture verifier (`fixtureVerificationResult`) derives its verdicts from the same host witnesses the matrix consumes, so a green loop proves the host, kernel, blinding, admission and staleness machinery, not model judgement. Compensating controls: the loop's own completion was not accepted as evidence for the structural criteria; the admission code (`foldVerificationEvent`, `parseEvidenceResult`, `compileVerificationTask`) was read clause by clause against criteria 2 and 3; the verifier's adversarial constructions were re-read against that code; and this review planted its own sentinel through the built modules (§Independent probes).
2. **The dispatcher published this review through the lifecycle script.** `npm run resume -- final-review` appended `FinalReviewRequested`, minted checkpoint 5 and rewrote the projection. `scripts/resume.mjs` is byte-identical to `HEAD` in this order, so the instrument is not itself under review; even so, the appended line was read raw and its fields, ordinal and strictly increasing `recordedAt` checked by hand, and subject identity was established with git objects rather than the helpers' projection. No `DOTLN_BEACON_KEY_FILE` is configured, so Beacon emission took the unchanged dense v2 path.
3. **The compiler and skeleton modules under review are the modules used to observe them.** Compensating controls: the shipped CLI was driven from clean stores at canonical paths and its raw JSON parsed by hand; store bytes were compared by SHA-256 across reads and reruns; the sentinel probe scanned the persisted log with a plain string search rather than through any projection.
4. **The executor's fixtures and receipts and the verifier's reproductions are claims.** The eighteen WO-010 tests were read in full and their assertions compared with the acceptance criteria; the two claims that could pass vacuously (that the implementer narrative really reaches the host log, and that the adverse witness is a real subprocess observation rather than a constant) were confirmed to be positively asserted and were reproduced here.
5. **The index generator, the surface check, the publish helper, the notes parser and the body profile are unchanged instruments.** `scripts/work-orders.mjs`, `scripts/release.mjs`, `scripts/worktree.mjs`, `scripts/release-notes.mjs` and `scripts/github-body.mjs` are byte-identical to `HEAD`; the only script changes are the new `scripts/verification-evidence.mjs` and the evidence-directory constant plus comment in `scripts/artifact-identity-evidence.mjs`.
6. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile** by calling `parseReleaseNotes` and `assertGitHubBodyProfile` directly and through `check-surfaces`, which reports `PASS github-body-profile` over both files. Neither body contains an angle bracket, which the notes parser treats as raw HTML even inside inline code. Because `v0.12.0` is above the latest published tag, these notes become the `v0.12.0` edition at release close.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-010 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest with `accountLabel: "not-applicable"`, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-06T14:57:31.236Z`, `elapsed` `{"implementation":36346888,"verification":833654}`, checkpoint `6254c4c653a6a84f03ccdf64516ef0da9ee36ece` (`/4`), and `orders[]` with the known orders; no projection-disagreement warning was emitted. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, printed `Final-review docs/work-orders/WO-010-independent-verification.md, the complete verification sequence, and ideation receipt; write docs/final-reviews/WO-010/FINAL-001.md.`, moved the phase to `final-review`, appended `FinalReviewRequested` (`throughVerificationId: "VER-001"`, `finalReviewId: "FINAL-001"`) with `recordedAt` `2026-09-06T15:20:28.495Z` as line 5 of `docs/control/orders/WO-010.jsonl`, and minted checkpoint `3086a8cf2751044abc68eddc473a1d1955fcc8d4` (`refs/dotln/checkpoint/WO-010/5`) with neither a checkpoint nor a Beacon warning. The legacy `docs/control/resume.jsonl` and every other order segment are byte-identical to `HEAD`. The WO-010 segment's five events are `WorkOrderActivated` (`2026-09-06T04:35:18.594Z`, `effortDeclarationValidated: true`), `ImplementationReady` (`14:41:05.482Z`), `VerificationRequested` (`14:43:37.582Z`), `VerificationCompleted` (`14:57:31.236Z`), and this review's `FinalReviewRequested`; every event carries `schemaVersion: 1` and the stamps strictly increase. The projected `implementation` span (36,346,888 ms) and `verification` span (833,654 ms) are the arithmetic differences of the recorded stamps. The index was regenerated after the dispatch (`Now: WO-010 — final-review`; the checklist row reads `final-review`; the Active block links `FINAL-001 (pending)`).

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree after the allocation and index refresh as tree `5bc5b05724119548892bd90ed8e3e430310445c8`, and `git diff-tree -r` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                                                                                                                              |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`ef686e3a…`)              | every deliverable path (54 files, 9,098 insertions); the whole subject is downstream of activation                                                                                                                                             |
| `/2` implementation-ready (`f654cb61…`)    | `docs/control/current.md`, `docs/control/orders/WO-010.jsonl` (four appended events), `docs/work-orders/README.md`, `docs/verifications/WO-010/VER-001.md` (added); nothing else, so every deliverable blob the verifier judged is the one the executor declared ready |
| `/3` verify allocation (`94ead192…`)       | the same four paths, the segment carrying three appended events                                                                                                                                                                                |
| `/4` verdict (`6254c4c6…`)                 | `docs/control/current.md`, `docs/control/orders/WO-010.jsonl` (two appended events), `docs/work-orders/README.md`                                                                                                                              |
| `/5` final-review allocation (`3086a8cf…`) | `docs/control/current.md`, `docs/control/orders/WO-010.jsonl` (one appended event), `docs/work-orders/README.md`                                                                                                                               |

Every deliverable blob is therefore identical across the implementation-ready, verify-allocation, verdict and final-review-allocation checkpoints and the working tree; the verifier changed nothing outside its report except the regenerated index (VER-001 D2). VER-001's blob `ea8a9458509c41c6d3fad74a6069bc1c22a48329` is the same object in checkpoint `/4`, in checkpoint `/5` and in the working tree, so unlike WO-022's report it was not edited after its verdict. All five checkpoints have `HEAD` as their sole parent, and their commit times (`00:35:18`, `10:41:05`, `10:43:37`, `10:57:31`, `11:20:28` local, UTC minus four hours) match the segment's stamps to the second.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. `docs/intake/{chats,images,notes}` in this worktree each hold only `.gitkeep`, and `git status --porcelain --ignored` lists no ignored path outside `.control-beacons/`, `node_modules/`, the three `packages/*/dist/` outputs, `.tsbuildinfo` files and `.DS_Store`; the `.runtime/` probe stores this review created were removed again, and the directory itself no longer exists. There is no `.claude/settings.local.json` here; `.claude/`, `packages/kernel/`, `docs/decisions/` and the legacy control log are byte-identical to `main`. `package.json` changes only by appending `node scripts/verification-evidence.mjs --check` to the test chain and adding the `evidence:verification` script; `devDependencies` are untouched, and `package-lock.json` changes only the two component version numbers and the skeleton's compiler dependency pin. No `docs/observations/` directory exists in this worktree.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                        | Report header                                                                                                                                                                                                                                                                                                                                                                                                                                              | Declared minimum | Result                  |
| -------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ----------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.4","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`    | there is no executor report; the evidence README states the operator selected the model and effort under the 2026-09-04 default and that this is not effective readback; `0.153.4` is on the `codex-cli` observed `versions` list (observed 2026-09-05 during WO-029) and the persisted `model_reasoning_effort` selector records `max` for that version, so `max` was admissible at append                                                                  | `xhigh+`         | `max` exceeds the floor |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}` | exactly one column-one header in VER-001, parsed and compared with the event actor as JSON in this session: equal; neither carries `accountLabel`; the report's bytes are unchanged since its verdict checkpoint; `2.1.263` is on the `claude-code` observed `versions` list since WO-022's verification, and `max` is on the documented session selector values                                                                                              | `xhigh+`         | `max` exceeds the floor |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`  | the header above                                                                                                                                                                                                                                                                                                                                                                                                                                           | `any`            | satisfied               |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: none` and the JSON form's `effortDrift` is `[{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.263; model claude-fable-5-1; effort max; source self-reported; account not-applicable`, the same drift line, phase `closed`, a `finalReview` elapsed value beside the two existing values, and legal actions `release-close, next, activate`.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree after the dispatch transition and the index refresh, before any review artifact or correction existed, captured to a file without a pipe: `All matched files use Prettier code style!`, the GitHub body-profile suite, the shell suites (fixture temp-root, publication checker, backup-intake, resume, checkpoint, worktree, release and work-orders, each ending `passed`), `PASS index coverage: 230/230 product headings indexed` with both audience editions `CURRENT` (30 and 45 linked sections), `PASS docs/work-orders/README.md is current`, `tsc -b --force`, `# tests 263 / # pass 263 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0` with the eighteen WO-010 tests present as `ok 33` through `ok 35` (compiler) and `ok 214` through `ok 228` (skeleton), the identity corpus `# tests 8 / # pass 8 / # fail 0`, `Verified 4 current artifact evidence files in docs/evidence/WO-010/artifact-identity; original semantic hashes and frozen oracle unchanged.`, and `Verified 4 synthetic verification evidence files; planted defect, repair, staleness and replay are green.`, exit 0. The counts equal the executor's and the verifier's recorded counts exactly. Second run, after the four corrections were in place and before this report, `PR.md` and `RELEASE-NOTES.md` existed (all three live in the Prettier-ignored review directory and are read by no test), started `2026-09-06T15:34:07Z` and finished `15:38:43Z` with the same output line for line apart from timings, exit 0. The publisher's committed surface check and the index check judge the committed tree again after the result is recorded.

### Static, surface, and structural checks

`git diff --check` exits 0 before and after the corrections. `npm run release -- check-surfaces` reports `PASS release-block: observed v0.12.0; expected v0.12.0 (work-order target v0.12.0; latest published v0.11.0)`, `PASS component-version @dotln/compiler: src changed; observed 0.5.0; previous v0.11.0 0.4.0`, `PASS component-version @dotln/kernel: src unchanged; observed 0.2.1`, `PASS component-version @dotln/skeleton: src changed; observed 0.11.0; previous v0.11.0 0.10.0`, and `PASS github-body-profile`, first with `no current final-review bodies yet` and then over this package's `PR.md` and `RELEASE-NOTES.md`. `git diff --stat main -- packages/kernel` is empty. A 64-hex scan across the diff's added lines and every new file outside the artifact-identity receipts returned only the two publication source locks and the two intermediate lock observations that the staleness demonstration records. A term screen for tracker products, internal hostnames, gateways and employer wording across the 46 changed and new text files returned only pre-existing generic boundary prose (the clean-room sentences in the ledger's history, 03, 06, the planning map and the root README) and regex false positives on ordinary words, and the only URL in any added line is the public GitHub Releases page. A link sweep over the 18 changed Markdown files resolved 593 relative links and anchors with a GitHub-faithful slugifier; the only miss was this then-unwritten report, which the regenerated index links as pending. No GitHub token variable is set in the session. `npx prettier --check` passes on the three Prettier-checked files this review corrected; the smoke receipt is a `.txt` file outside Prettier's scope.

### Independent probes against the deliverable

1. **The shipped loop from a clean canonical store.** `node packages/skeleton/dist/src/dotln.js verify-demo --store /private/tmp/claude/…` printed `{"workOrderId":"verification_3","episodeId":"ep_verifier_3_attempt_1","resultId":"result_cmd_a55351b8ec012a11","status":"completed","summary":"Synthetic fixture episode completed.","requiresHuman":false}`, the envelope the executor's smoke receipt and VER-001 both record. `status --json` on that store printed exactly the five top-level keys `acceptanceEvidenceMatrices`, `episodes`, `runningEpisodes`, `pendingCommands`, `recentEvents` (474 lines); the one matrix has keys `workstreamId`, `subjectRevision`, `phase`, `baseline`, `rows`, `evidence`, `findings`, `staleness`, phase `complete`, rows `AC-policy` verified with two evaluations of which the first is `stale: true`, `AC-selection` verified with two evaluations of which the first is stale, `AC-description` verified with one, one staleness record `{changedSurfaces: ["policy.json"], criterionIds: ["AC-policy","AC-selection"]}`, and the finding `finding_verification_1_AC-selection` at `resolved`; neither `phase` nor `criteria` is a top-level key (adjudication 2). The human form printed `running=0 pending=0`, the three episodes `ep_verifier_1_attempt_1`, `ep_repairer_2_attempt_1` and `ep_verifier_3_attempt_1` each `completed fake`, `pending commands: none`, and the `acceptance ws_independent_verification phase=complete` block with one row per criterion. The store's `events.jsonl` had the same SHA-256 before and after two status reads. A second `verify-demo` on the completed store returned the identical envelope, the 31-line log was byte-identical, and the `worktrees` directory was empty.
2. **The macOS symlink refusal (VER-001 checklist 1).** `--store /tmp/claude/…` exited 1 with `worker host refused; inspect the store and declared environment before retrying`, leaving `events.jsonl` with the single `VerificationHostConfigured` event and an initialized `repository`; the identical command at `/private/tmp/claude/…` completed as in probe 1; `ls -ld /tmp` shows `/tmp -> private/tmp`. The refusal is `WorkerWorktrees`' aliasing guard (`realpathSync(directory) !== resolve(directory)`), a WO-009 line untouched by this order.
3. **A repository-relative store inside this sandbox (this review).** `--store .runtime/verification` was refused with the same generic message after `VerificationHostConfigured`. Driving `runVerificationDemo` directly with the same absolute directory showed the cause: `git init --quiet` failed with `Operation not permitted` while copying the hook template into the nested `.git/hooks/`, and with `GIT_TEMPLATE_DIR` empty it failed while writing the nested `.git/config` (`could not set 'core.repositoryformatversion'`). That is the harness's protection of Git configuration and hooks under the project (`docs/AI-HARNESS-SECURITY.md`), raised inside `git init` before any product guard runs; `realpath` of the checkout equals its path, so the aliasing guard is not involved, and the same nested `git init` at `/private/tmp/claude/…` succeeded where the sandbox permits it. `worker-demo.ts` line 53 performs the same nested `git init` for the WO-009 inspection demo, so the pre-existing `.runtime/claude` runbook shares this in-sandbox limit. Outside the sandbox the nested init has no such restriction. The probe stores were removed.
4. **Blinding with a reviewer-chosen sentinel.** A script in the session temporary directory imported the built `verification-demo.js`, `verification-fake.js` and the kernel, ran `runVerificationDemo({implementerNarrative: "FINAL_REVIEW_BLINDING_SENTINEL_Q4Z8"})` into a canonical temporary store, and scanned the persisted events as plain strings: 31 events, phase `complete`, all rows `verified`; the sentinel appears in exactly one event, `evt_3 ImplementationReported` by actor `implementer`, and in none of the three `CommandPersisted` capsules (roles `verifier`, `repairer`, `verifier`); every capsule's top-level keys are exactly `compilerPackageVersion`, `contractVersion`, `criteria`, `finding`, `inputHash`, `role`, `subject`, `workOrder`; `prohibitedOperations` is `repo.write`, `repo.delete`, `network`, `transcript.read`, `self.certify`; `pendingCommands(replayOutbox(log))` is 0. The assertion is not vacuous: the marker demonstrably reaches the log.
5. **The verifier's header and the segment.** The single column-one header in VER-001 parses as JSON equal to the `VerificationCompleted` actor; the five `recordedAt` values strictly increase; the tag snapshot in the regenerated index lists `v0.11.0`, whose object `53902114…` peels to `HEAD` locally and on `origin`.
6. **The gitmoji catalog** fetched from `gitmoji.dev/api/gitmojis` (75 entries) lists `:safety_vest:` as "Add or update code related to validation.", `:white_check_mark:` as "Add, update, or pass tests.", `:goal_net:` as "Catch errors.", `:monocle_face:` as "Data exploration/inspection." and `:clown_face:` as "Mock things."

### Reading

The complete tracked diff (32 files) and all 22 new files were read in full: the compiler's pinned types, line and path validators, `copyCriterion`, `copyFinding`, `copySubject`, `compileVerificationTask`, `assertVerificationTask`, `changedVerificationSurfaces` and `affectedVerificationCriteria`; the skeleton's request and result types, `parseEvidenceResult` with every admission check, the result schemas, transport validation, prompt and result selection; the reactor's verification state, kernel-driven `dispatch`, `foldVerificationEvent` for every event type, `verificationDecision` and its trace; the driver, host and dry-run admission; the demo's fixture creation, witnesses, real subprocess, worktree mounting, data-only repair application and crash-window recovery; the fake transport and the subprocess double for both wire formats; the compatibility projection, replay, matrix projection and draft helper; the generic transport and store changes, the status projection and renderer, the CLI's new command; the evidence script; the eighteen WO-010 tests and the two modified WO-016 and WO-009 tests; the executor's receipts; VER-001; and every product, planning, publication, README, ledger, work-order and evidence hunk. The work order, its five criteria, evidence gate, write-back duty, constraints and non-goals were read against each of those.

## Acceptance criteria

Each criterion was reproduced independently by VER-001 (whose reproductions are quoted in the PR body) and re-read here against the code, the tests and this review's own probes.

1. **A planted defect caught by a blinded verifier, repaired in a fresh episode, re-verified green, end to end through the kernel's event loop.** Met. `dispatch` builds the capsule, wraps it in a `Program.Invoke` whose completion emits `VerificationContinuation`, and requires `decideProgram` to yield exactly one intent; the host persists `CommandPersisted` only after `authorize` grants it; the result is folded through `decideProgram` again and the continuation drained. Probe 1 and probe 4 reproduce the 31-event loop with three physically distinct episodes, `ep_repairer_2_attempt_1` running in its own worktree, no pending command at the end, and the executor's suite additionally asserts three distinct `cwd` values and byte-identical replayed Decisions. The defect is a real subprocess observation over `select.mjs`, not a constant.
2. **The implementer episode never certifies itself, structurally.** Met. Blinding is positive construction at every nested boundary (`copySubject` rebuilds each file and witness by named field), and the compiler test compiles a subject and criteria carrying the sentinel in six extra places under a throwing clock and random source. Certification is closed by the fold: every non-host actor is dropped before any branch, a `CommandResult` is accepted only for the pending command with `verificationResultVersion: 1`, a producing episode in `pending.attempts` that is not in `implementerEpisodes`, a live lease, a current grant and an unchanged subject revision, and any refused result is recorded in `refusedResults` without touching a row. The repairer's schema has no `evaluations`, and on application its episode joins `implementerEpisodes`, so a later result claiming it is refused by the same gate.
3. **Full VerificationFinding shape; each blocking finding compiles a focused repair WorkOrder.** Met. `copyFinding` requires every field and refuses empty lists, `compileVerificationTask` refuses a non-blocking finding or a surface outside the criterion, and `parseEvidenceResult` additionally requires the finding's references to belong to the failed evaluation and its observed and expected pair to match an adverse host witness. The fold compiles one `repair_<dispatch>_<findingId>` plan per blocking finding with `allowedOperations: ["repair.propose"]`; the executor's suite shows two findings producing two plans, and a zero bound stops at `attention` after exactly one attempt.
4. **Substantive repair marks affected evidence stale, visibly.** Met. `VerificationSubjectSubmitted` requires the new files to equal the proposal applied to the old subject byte for byte, requires at least one changed surface, and marks every prior evaluation on an affected criterion `stale: true` while leaving unaffected rows untouched; `affectedVerificationCriteria` selects every criterion when a changed surface is undeclared. Probe 1 shows the stale history preserved in the final JSON, and the executor's mid-repair test drives `dotln status` on a paused store and proves the store bytes unchanged.
5. **The AcceptanceEvidenceMatrix lives and updates through the workstream.** Met. There is no matrix file; `projectAcceptanceEvidenceMatrices` replays each workstream that a host-actor `VerificationOpened` names through the same `seiriReactor` the live driver uses, `projectWorkerStatus` folds it, and the executor's test snapshots row status on every appended event. Provenance, producing episode, subject revision and stale flag are host-derived on acceptance and cannot be supplied by a worker.

## Evidence gate

Present, and reproduced. `docs/evidence/WO-010/events.jsonl` holds the 31-event loop, `matrix.json` and `stale-status.json`/`.txt` the final and mid-repair projections, and `scripts/verification-evidence.mjs --check` re-derives all four from a fresh run inside `npm test`; this review's own runs reproduced the same envelope, the same `commandId` and the same row history. The executor's README maps each criterion to named tests, and the eighteen tests are present in both gate runs. `git diff --check` is clean and the release surface passes.

## Write-back duty

All discharged, and consistent with the code. 02 §Independent verification v1 pins `AcceptanceCriterion`, `VerificationEvidence`, `Evaluation` with its matrix additions, `VerificationFinding`, the capsule and its input key, the admission conditions, the staleness semantics and the matrix projection contract; each field list and each admission sentence was compared with the source and with probe 1. 02's actors table updates the `WorkOrderTransport` row for request-selected results. 03 §Ports gains the `VerificationAdapter` realization and narrows "the current personal profile" to "the personal inspection profile" now that two profiles exist. 06 renames the rung heading to `v0.12.0` and records the activation completion and receipt. 10 adds the `verification-v1` capsule contract and `verificationResultVersion: 1` axes and states that the capsule key is not a `CompiledProgram` semantic hash. The publication index gains the contract row and the renamed roadmap anchor, both audience routes carry refreshed text and source locks, and the staleness demonstration records the recapture. The planning map, the root README release block, both package READMEs, the work order's activation header and the ledger's dated section (three adopted entries and the shared-reactor correction recorded as its own entry) are updated.

## Ideation breakout receipt

None. The order had no `ideation:` breakout; no receipt exists under `docs/evidence/WO-010/`, and the dispatcher's generic mention of a receipt has nothing to resolve to. The work order is planner-authored: it entered the repository at `2bd4e60` (2026-08-31, the WO-002 closeout), received its `Effort:` line at `7f5181f` (2026-09-04), and was last changed before activation at `c752b77` (the WO-023 merge). The executor's activation completion changed the title, added the dated activation-completion paragraph and renamed the `Cites` anchor; the objective, scope discipline, constraints, criteria, write-back duty, evidence gate and non-goals are byte-identical (adjudication 9). The executor's ledger section states that no intake was copied or mined, and the term screen is consistent with that statement.

## Verification-sequence soundness

The sequence is `WorkOrderActivated` → `ImplementationReady` → `VerificationRequested` → `VerificationCompleted (pass)` → `FinalReviewRequested`, with no repair episode. VER-001 is WO-010's first and only independent verification; it read the work order, the executor's evidence as claims and the full subject; it reproduced every criterion by an independent method rather than by re-running the executor's fixtures alone; it disclosed the instruments it shared with the subject; its bytes are unchanged since its verdict; and its verdict is consistent with everything this review observed.

## Adjudications

### 1. The runbook's copy-paste command fails on macOS (VER-001 checklist 1)

Confirmed by probe 2. Applied: the three commands in `packages/skeleton/README.md` §Independent verification now use `.runtime/verification`, the gitignored repository-relative root the WO-009 runbook already uses, and one sentence names the canonical-path rule with the `/tmp` symlink as the macOS example and `/private/tmp` or a repository-relative directory as the accepted forms (§Corrections applied by this review). Probe 3 records the limit of this review's evidence: inside this sandbox the repository-relative form is refused at the nested `git init` by the harness, not by the subject, and the aliasing guard it was chosen to avoid was checked directly (`realpath` of the checkout equals its path). Writing `/private/tmp/…` as the headline path was considered and not sustained because it is macOS-specific.

### 2. The smoke receipt presents a reduced projection as command output (VER-001 checklist 2)

Confirmed by probe 1: the real output has five top-level keys and 474 lines, and neither `phase` nor `criteria` is top-level, while every recorded value is correct. Applied: four `#` comment lines above the block, dated as a final-review note, state that it is a reduced projection, name the five real top-level keys, and give the JSON paths of `phase` and of each `criteria` entry. The recorded values are untouched. Replacing the block with the real output was considered and not sustained because the final projection is already preserved byte for byte in `matrix.json`.

### 3. WO-022's artifact-identity receipts lost their gate (VER-001 O1)

Confirmed: `scripts/artifact-identity-evidence.mjs` now writes and checks `docs/evidence/WO-010/artifact-identity/`, and the four WO-022 files are referenced by no code while `docs/evidence/WO-029/baseline.json` remains the pinned source of the frozen oracle. The established precedent is the dated note WO-022's review added to the WO-029 README. Applied: the same dated migration note in `docs/evidence/WO-022/README.md`. The WO-029 note continues to point at WO-022; rewriting it was considered and not sustained because it is a dated historical note and the forward trail WO-029 → WO-022 → WO-010 is now complete. A hash pin for the historical files remains a patch candidate.

### 4. `verificationReactor` has no production caller (VER-001 O2)

Confirmed by grep: the compatibility projection in `packages/skeleton/src/verification.ts` is imported only by the test file; production paths use `VerificationDriver` and `projectAcceptanceEvidenceMatrices`. It wraps the shared reactor rather than deciding anything itself, which is the WO-016 boundary the ledger records. A note, not a defect.

### 5. `repairPlans` accumulates and is never pruned (VER-001 O3)

Confirmed in the fold: the array is only ever appended (`[...state.repairPlans, ...repairPlans]`), and `dispatch` recompiles its capsule from the first open blocking finding rather than reading a plan. Bounded by the repair count and the finding count; worth knowing before a later rung consumes it. Recorded.

### 6. Blank lines in the ledger section (VER-001 O4)

Confirmed: two blank lines preceded the new `## WO-010 implementation` heading and one blank line separated the final `Preserve the established single decider boundary` entry from the list above it, unlike every neighbouring section. Applied: one blank line before the heading and none inside the list. The entries' bytes are unchanged; `docs/lineage/` is outside Prettier, so the gate could not have caught this.

### 7. The verifier's disclosures (VER-001 D1–D3)

D1 (the subject is the instrument) and D3 (the dispatch published through the ordinary control path with no Beacon key) are the same conditions this review met and compensated for (§Self-referential instruments). D2 (the verifier refreshed the index) is the guide's expected refresh; the nine-line delta it describes is visible between checkpoints `/2` and `/4`, and this review refreshed the index at dispatch and will refresh it again after its result.

### 8. The CLI's refusal message hides the cause (this review)

`dotln.ts` prints the same sentence for every `WorkerFailure` so that unexpected external diagnostics cannot leak paths or authentication details, which is a deliberate WO-009 choice. The cost, visible in probes 2 and 3, is that a symlinked store path, a sandbox denial and a genuine profile refusal are indistinguishable at the command line. A diagnostic hint that names a refusal class without a path would be a code change and is therefore a candidate, not a correction.

### 9. The executor edited the scope-authority document (this review)

Confirmed: `git diff` on the work order shows only the title, the dated activation-completion paragraph and the `Cites` anchor rename to the roadmap heading the executor renamed. The execution guide's Discipline section makes release assignment opt-out and instructs the executor to complete a missing activation target and record the base and classification; the same dated completion is recorded in 06. Authorized; recorded so the touch is visible.

### 10. The dispatcher names an ideation receipt (this review)

The transition message asks for "the complete verification sequence, and ideation receipt" for every order; §Ideation breakout receipt records that none exists for WO-010 and why.

### 11. The attribution rule (this review)

The reviewed commits carry plain subjects and no attribution trailer, and the PR body carries no session link, per the operator's standing rule (`~/.claude/CLAUDE.md`, "Do not add AI attribution to commits or pull requests") and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins, as the WO-017 through WO-022 reviews recorded.

### 12. Kernel, schema and contract axes (this review)

`packages/kernel/` is byte-identical to `main`, so the constraint that kernel and compiler purity are unchanged holds by construction on the kernel side; on the compiler side `verification.ts` imports only `artifact-identity.js`, `normalize.js` and a type, and the WO-016 guard forbids Node built-ins in the skeleton's two pure leaves. The event-envelope schema stays `1`, the compiled-program contract stays `1`, and the artifact identity keeps every semantic hash while `compilerPackageVersion` advances to `0.5.0`, which 10 records.

### 13. The verifier's not-verified list (VER-001)

Live model verification, live-integration claims, visual and network claim types, comparison and rating projections, source-writing repairers, independent code review, post-PR loops, a hostile same-user writer and a genuinely unavailable interpreter remain outside what any run in this order witnessed. Every surface this review read (the roadmap, both READMEs, the ledger, both audience routes, the executor receipt and the release notes) labels the loop synthetic and the live path unwitnessed; no overclaim was found.

### Candidates considered and not sustained

- **Replacing the smoke receipt's second block with the 474-line real output** (adjudication 2): the labeled reduction is the smaller honest change and the full projection is already recorded.
- **Rewriting the WO-029 migration note to name WO-010** (adjudication 3): a dated note is history; the chain is complete through WO-022's new note.
- **Writing `/private/tmp/…` as the headline runbook path** (adjudication 1): correct on macOS, wrong elsewhere.
- **Failing the review over the in-sandbox `.runtime` refusal** (adjudication 1, probe 3): the failure is raised by the harness inside `git init`, before any product code decides anything, and the identical nested init succeeds where the sandbox permits it.
- **Adding a diagnostic hint to the CLI refusal** (adjudication 8): a behavior change is outside a reviewer's authority.

## Corrections applied by this review

Four corrections, all in the documentation lane `docs/final-reviews/README.md` grants, each applied by exact string replacement with a script that refused unless the old string occurred exactly once, proven contained by `git diff` and `git status`, and followed by the checks it could affect: `npx prettier --check` on each Prettier-checked file, `git diff --check`, and the full `npm test` (the second run). They are committed with the implementation because each documents behavior that implementation introduced.

1. **`packages/skeleton/README.md`:** the three runbook commands use `.runtime/verification` instead of `/tmp/dotln-verification-demo`, and a new sentence after the block states that the store lives under the gitignored `.runtime/` root and that its path must be canonical, naming the macOS `/tmp` symlink as the refused form (adjudication 1; 4 insertions, 4 deletions against the executor's text).
2. **`docs/evidence/WO-010/cli-smoke.txt`:** four dated comment lines above the status block state that it is a reduced projection, name the five real top-level keys, and give the JSON paths of `phase` and the `criteria` entries; no recorded value changed (adjudication 2; 4 insertions).
3. **`docs/evidence/WO-022/README.md`:** a dated migration note (2026-09-06, WO-010) after the implementation-choices paragraph records that the evidence helper now writes and checks the current edition under `docs/evidence/WO-010/artifact-identity/`, that the four WO-022 files are retained historical bytes no longer regenerated or byte-checked, and that `docs/evidence/WO-029/baseline.json` remains the pinned oracle (adjudication 3; 7 insertions).
4. **`docs/lineage/idea-ledger.md`:** one blank line removed before the new section heading and one removed inside its list (adjudication 6; 2 deletions among the executor's added lines).

The three files this review adds, this report, `PR.md` and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet, no guard was lost, and no behavioral or evidence defect was found in the deliverable that any criterion covers. The items below are recorded and not scored.

1. **The runbook's store path was refused on macOS** (adjudication 1): corrected; the canonical-path rule is now stated.
2. **A repository-relative store cannot be exercised inside the Claude Code sandbox** (adjudication 1, probe 3): harness protection of nested Git configuration and hooks under the project; shared with the WO-009 runbook; run the runbook from an ordinary shell.
3. **The smoke receipt presented a reduced projection as output** (adjudication 2): labeled.
4. **The four WO-022 receipts are no longer byte-checked** (adjudication 3): dated note applied; hash pin is a patch candidate.
5. **The CLI refusal message is deliberately generic** (adjudication 8): diagnostic candidate.
6. **`verificationReactor` has no production caller** (adjudication 4) and **`repairPlans` is never pruned** (adjudication 5): notes.
7. **Live model verification is unwitnessed** (adjudication 13): the launch path exists; the `live` evidence source has no producer.
8. **`CLAUDE_EFFORT` remains an unrecorded readback candidate** (§Actor), so Claude Code attestations stay `self-reported`.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step and the sandbox.** `gh` cannot read its configuration under this sandbox (`~/.config/gh` is denied), the same limit every reviewed order since WO-006 has met, while read-only `git ls-remote` to `origin` succeeds. The reviewed series is committed here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation and notes parse run against the exact commit; if it refuses at the `gh` preflight it pushes nothing and the operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute command the publisher prints: change directory into the main checkout and run this worktree's `scripts/release.mjs close WO-010 --publish` with node, outside the sandbox and where `gh` is authenticated. Because `v0.12.0` is strictly above `v0.11.0`, the reviewed subject helpers finish this worktree, run the closed-state gates, fast-forward main, run `npm ci` and the evidence gate, assemble the five-section edition from these notes, create and push the annotated `v0.12.0` tag, and create the matching GitHub Release. If Release creation fails after the tag push, the rerun is `npm run release -- close WO-010 --publish` from updated main.
- **Attribution:** adjudication 11.
- **Ledger duty:** discharged by the executor's dated section; this review adds no ledger entry because every open item above is either already in VER-001 or is a candidate this report names for a patch or the next planning pass.
- **Intermediate commits are not individually gated.** The first commit's tree carries the pre-existing generated index over a changed work-order header, so `index --check` would refuse it in isolation; only the final tree is what `npm test`, `check-surfaces --committed` and the publisher judge.

## Proposed PR

- **Title:** `:safety_vest: WO-010: Independent verification as blinded verifier episodes, claim-typed evidence, focused repair and a living acceptance matrix; v0.12.0`. The shortcode is the catalog's "Add or update code related to validation" entry (probe 6), chosen because the order's main purpose is making the validation of acceptance claims a compiled, host-admitted mechanism; `:white_check_mark:` would suggest this repository's own tests changed, `:goal_net:` names only the caught defect, and `:monocle_face:` names inspection rather than verification. The title names the change and its version and leaves the inventory and validation to the body, per 08 §PRs and commits.
- **Body:** `docs/final-reviews/WO-010/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Commit series

Two commits, each with a plain subject and an explanatory body, in this order, followed by a check that `git diff HEAD` is empty and that the final tree equals the post-result working tree:

1. **Implementation:** the compiler (`verification.ts`, `index.ts`, `artifact-identity.ts`, `package.json`, `README.md`, and `test/verification.test.ts`); the skeleton (`verification-protocol.ts`, `verification-host.ts`, `verification-demo.ts`, `verification-fake.ts`, `verification.ts`, `reactor.ts`, `dotln.ts`, `worker-protocol.ts`, `worker-status.ts`, `worker-store.ts`, `worker-transport.ts`, `fixtures/verification-cli.mjs`, `package.json`, `README.md`, and its three tests); `package.json`, `package-lock.json` and `.prettierignore`; the scripts (`verification-evidence.mjs`, `artifact-identity-evidence.mjs`); the root README release block; `docs/product/02`, `03`, `06`, `10`; the planning map; the publication index, both edition locks and the staleness demonstration; the ledger's dated section; the work order's completed activation header; the executor evidence under `docs/evidence/WO-010/` (all ten files, including the labeled smoke receipt); and this review's `docs/evidence/WO-022/README.md` note.
2. **Independent review evidence and closed control state:** `docs/verifications/WO-010/VER-001.md`, this report, `PR.md`, `RELEASE-NOTES.md`, `docs/control/orders/WO-010.jsonl`, `docs/control/current.md`, and the regenerated `docs/work-orders/README.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commits (the committed surface check requires the closed control state, and it makes the index's check mark true); the index is regenerated after the result and checked; the reviewed state is committed as the series above. The publisher is then run from this worktree:

```bash
npm run worktree -- publish WO-010 --title ':safety_vest: WO-010: Independent verification as blinded verifier episodes, claim-typed evidence, focused repair and a living acceptance matrix; v0.12.0' --body-file docs/final-reviews/WO-010/PR.md
```

The helper runs `check-surfaces --committed WO-010` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-010`, opens the PR against `main`, and prints the release-close handoff in the absolute subject-helper form. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, the operator runs that printed command with the main checkout as the working directory; the expected outcome is the annotated `v0.12.0` tag and its GitHub Release.

## Method

Everything above was re-established in this session inside the Claude Code sandbox: `npm test` twice, captured to files without a pipe; the read-only status projection and the transition; temporary-index trees and `git diff-tree -r` against all five checkpoints; the segment read raw and compared as JSON with the verifier's header; the tag peel locally and on `origin`; the shipped CLI driven from clean stores at a canonical path, at the macOS `/tmp` symlink and at a repository-relative path, with the module-level diagnosis of the last; the sentinel probe as a script written for this review in the session temporary directory; `check-surfaces` before and after the package artifacts existed; the notes parser and body profile called directly; the 64-hex scan, the term screen and the link sweep as one-off scripts; a fetch of the public gitmoji catalog; and a full reading of the diff, the new files, the receipts, VER-001 and the gates' source. The four corrections were applied with exact string replacement, checked with the pinned formatter where it applies, and diffed against `HEAD`. No other file outside `docs/final-reviews/WO-010/` was changed by this review; the index regeneration and the control projection are the lifecycle's own effects, and every probe store was removed.
