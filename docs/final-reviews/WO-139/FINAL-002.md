# WO-139 FINAL-002 — Final review after the FINAL-001 repair

**Verdict: pass.** All seven acceptance criteria hold at the reviewed subject, both FINAL-001 findings are repaired and independently reproduced as repaired, and the two operator-authorized scope expansions behave as their decisions claim. No blocking finding. Eight non-blocking items are recorded for the next planning pass; none is an acceptance-criterion failure and none is repaired here.

Reviewed by a Claude Code session, dispatch `resume: final review` recorded by the session hook at 2026-09-18T17:50:05.950Z. Harness version 2.1.277 is the harness host's `versionObservation` for this session (channel `ancestor-executable-lsof`). Model `claude-fable-5-1` is the identifier the harness supplied in this session's system context. Effort `xhigh` is the value this session's hook input carried and its journal recorded. These are the actor's readback of harness-supplied values, not an independent effective-session attestation.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.277","model":"claude-fable-5-1","effort":"xhigh","source":"self-reported"}

## Subject and integration

Reviewed [WO-139](../../work-orders/WO-139-subagent-cap.md) in full, the complete numbered sequence [VER-001](../../verifications/WO-139/VER-001.md), [FINAL-001](FINAL-001.md) and [VER-002](../../verifications/WO-139/VER-002.md), decisions [D001–D006](../../evidence/WO-139/decisions.md), the [evidence README](../../evidence/WO-139/README.md), [repair-001](../../evidence/WO-139/repair-001.md), and the authored diff against `main`: `subagent-budget.ts` in full, the host, pin-list, loadout, compiler boundary, budgets, lifecycle, handoff, planning-continuation and planning-receipt changes, the probe library and its hook fixture, and the write-backs to product 06 and 07, the security runbook, the capability table and the README. Generated surfaces were judged through their checks, not by reading.

`git fetch` at review: `origin/main`, local `main` and this branch's base are all `494e6825ae4c249da97671379c26d4a38a84b153`. Nothing was integrated, no conflict was resolved and nothing was retimed; the latest remote tag is `v0.29.4`, so `v0.29.5`, compiler `0.13.2` and skeleton `0.25.4` stand under the patch classification. The sibling `wo-054` worktree is unpublished at the same base. This reviewer changed no source, test, product document or generated surface. The only files written are this report, `PR.md`, `RELEASE-NOTES.md` and the post-dispatch index refresh.

**Instrument disclosure.** The generated hooks under review reserved this session's writer, initialized and charged this session's subagent counter, and refused one of the helper's shell reads during a live gate (the pre-existing gate refusal). The helper attributed that gate to another session's `npm test`; this session's own document gate ran in the background in the same window, and the retained local state does not identify the run, so its owner is unknown. `harness usage`, also under review, supplied the counts quoted below.

**Fan-out.** Plan stated before the first spawn: none at entry (0/20 observed), then revised and stated to one read-only helper with no descendants, judging the WO-139 tests as one group of thirteen claims. Actual: one. Observed after the spawn and at handoff: 1/20, `exact-observed`, unresolved overlap 0/0, uncounted remainder unknown. Every helper claim recorded below was checked against source or by execution first.

## The review gate

`npm test -- --review` was not rerun. `gateCodeIdentity` recomputed at the reviewed tree is `24742080e4a9713d9a94824623e69bc69d9c887138c68f6184cf837231e1140d`, and `findGateCheck` returns the row recorded 2026-09-18T17:37:52.807Z: exit 0, 32 suites, 76 fresh tasks, 0 failed cases, the same identity. A second passing row from the repair (17:05:58Z) carries the same identity. The five new source files are staged, and no source edit follows the row. The gate is keyed by tracked non-generated code, so a rerun would buy the same evidence for about twenty minutes of waiting; the time went to independent probes instead. The tradeoff is that this review did not itself observe the 32 suites execute.

## FINAL-001 findings

**F1, cap module pinned: repaired.** `scripts/lib/harness.mjs:95` declares `packages/skeleton/dist/src/subagent-budget.js`; that line is the file's only change. The focused fixture "WO-139 cap-module-only changes refresh the runtime and snapshot damage is detected" passed here (3 of 3 WO-139 cases, 14.95 s). The installed snapshot `bb9522b84d7a517d` named by `.claude/hooks/permissions.mjs` holds `subagent-budget.js` and `harness-host.js` with the same SHA-256 prefixes as the built modules (`15fff614…`, `378aa197…`). `node scripts/harness.mjs check`: 28 generated surfaces, exit 0.

**F2, advisory provenance: repaired.** The `WeakSet` tag is applied at the two creation sites (SessionStart initialization and permission admission) and the throttle bucket keys on membership; `runHarnessHook` assigns the awaited response directly, so identity survives. FINAL-001's focused command passed 2 of 2 here (it was 0 of 2).

## Acceptance criteria

| # | Assessment | Result |
| --- | --- | --- |
| 1 | Probe 2 records field shapes and anonymous equality labels only; the README names root `session_id`, child `agent_id` and direct `tool_use_id`. The probe requires `DOTLN_LIVE_HARNESS=1`, runs in an isolated temporary repository with four tools and refuses to overwrite an earlier row. | Pass |
| 2 | Scratch probe of the built module: with cap 3 the third Agent is admitted and the fourth refused with `DOTLN_SUBAGENT_CAP_REFUSED: count 3, cap 3; docs/control/budgets.json subagentCap would be exceeded…`; a repeated `tool_use_id` does not debit twice; Workflow at zero remaining is refused and an admitted Workflow leaves the count at 0; `null` admitted 25 of 25 and still counted 25, rendered `25/disabled`; an absent key and an absent file both give 20; cap 0 refuses the first spawn; missing counter, malformed counter and an invalid cap each admit with the cause named. | Pass |
| 3 | Same probe: children a, b, c count 1, 2, 3 at their first call, a fourth child is refused on each of two attempts, and an already counted child is still admitted at the cap. A child seen before a direct admission is never matched to it (count 2, exact); a later child yields `minimum-observed` with an advisory; joining the admission to a different identity raises the count to 3, exact. Live: the helper's first tool call through the installed hooks recorded its hashed `agent_id`, equal to the hash joined from the Agent result. | Pass |
| 4 | Live `harness usage` returned count, cap, remaining, count kind, overlap and `uncountedRemainder: "unknown"`. The Stop summary string is asserted through the generated `finish` hook by the fixture that passed here. | Pass |
| 5 | The batching rule is in the loadout and in this session's delivered reviewer skill. `harness check` and `harness-context --check` exit 0; `publication:check` passes. The host has four policy refusal sites (gate, planning, subagent, foreign writer) and this diff adds exactly one `protocolRefusal` call; the other refusal-shaped sites are dispatch admission, input contract and target worker, all pre-existing. 07 §Discipline, the runbook and the compiled boundary sentence say four, sourced to WO-135 and WO-139. The verifier ceiling rose one 4 KB step with a dated acceptance naming the rule. | Pass |
| 6 | D003 and the README carry the five-row per-harness, per-path table; 07's total-cap candidate names the observed paths and a reopening condition. | Pass |
| 7 | The gate row above; `git diff --check main` and `git diff --cached --check` exit 0; package metadata moves only compiler `0.13.2` and skeleton `0.25.4`; no new dependency. | Pass |

**Concurrency.** The order's unit case runs eight processes at cap 1, which cannot show lost updates. A scratch stress run against the built module did: five rounds of 24 to 32 concurrent processes gave 24 of 24 counted under `null` (twice), exactly 5 admitted and 19 refused at cap 5 (twice), and exactly 12 of 32 at cap 12, with no fail-open advisory in any round.

**Scope expansions.** D002: `node scripts/test-plan-refutation.mjs --check-only` reports `authorized-execution-amendment` for WO-053 bound to WO-053-D002, `pending-capability-history-repair` for `worker.source-change` on the committed side and the dated reassessment on the workspace side, and WO-139's `release-assignment` v0.29.5. Code read: an amendment matches only when both the normalized source hash and the hash of the first `orderLength` bytes match, any remainder must pass the strict execution-appendix rule, amendments are excluded from criterion dispositions and so cannot discharge a hold, and every recorded amendment is revalidated on each gate check. D005: the release is prepared before the append, a pre-append failure propagates before the `try`, and the `finally` releases only the current session's actor. The originating operator messages for D002 and D005 are not available to this session; their authority is taken from the decisions record, as in VER-001 and FINAL-001.

**Clean room.** A screen of the WO-139 evidence, verification, review and lifecycle records for local paths, raw session identifiers, account strings and session links matched only a system-path literal inside quoted source in the feedback editions. No stop condition.

## Document gate

`npm run test:docs` before this review's outputs: 12 passed, 5 failed. The one real failure was `release-surfaces`/`github-body-profile` on FINAL-001's draft release notes (the heading `Read-before-upgrading`); `plan`, `plan-refutation-current`, `console-docs` and `skeleton-docs` reported only "Required preflight or fixture preparation failed" behind it. That draft was a failed review's output, not the verified subject, and this review replaced it. This review's own first two drafts of the notes failed the same profile on angle-bracket placeholders inside code spans, which it reads as raw HTML; both were reworded. With this report, the PR body and the release notes in place, `npm run release -- prepare` against `origin` reports the `v0.29.5` target current with no files changed beyond the refreshed PR meter, and `npm run test:docs` passes 17 of 17 in 122.68 s.

## Non-blocking items for the next planning pass

1. **Pin list is hand-maintained (VER-002 N1, confirmed).** An independent recomputation of the relative-import closure from the 38 pinned modules reaches 57; 19 are unpinned, including `loadouts/feedback.js` imported by the host. `main` has the same list minus the cap module. Derive the list from the entry points or assert closure coverage in the fixtures.
2. **No generated-hook regression for child counting.** The only generated-hook call carrying `agent_id` (`scripts/test-harness.mjs:514`) runs after the fixture deletes the counter and asserts the gate refusal. Removing the host's `agent_id` forwarding would fail no test. The behavior is established by the unit cases and two live observations, not by a regression.
3. **Weak assertions in the planning fixtures.** The forged-binding loop uses `assert.rejects` with no matcher, `orderLength` is never forged, the capability-repair negatives match only the generic prefix, and the same-ID check is not exercised independently of row preservation.
4. **Unit concurrency case cannot show lost updates** at cap 1; the stress probe above did. Untested branches: missing `tool_use_id`, `resume` of a known or unknown agent, and the handoff's runtime-missing and reservation-remains errors.
5. **`SubagentStart` (VER-001 O1).** Probe 2 observed it with `session_id` and `agent_id` before each child's first tool call; no hook is registered for it and its ability to deny is untested. It belongs to the open total-cap candidate.
6. **Three inaccurate 07 sentences.** Agent joins were observed immediately for background spawns (VER-001 and this review), not "only after the child finishes"; `PlanExecutionAmended` has no actor field though the text says actor-attested (O3); a non-criterion byte change fails without naming its order (O4). The doubled "and" in both refusal lists and the 17,477-byte figure (now 17,503) are cosmetic.
7. **Counter behaviors worth a decision.** A host denial or failed launch after admission is never refunded and the count still reads `exact-observed` (D003 records the limit, not the label); the refusal text prints a minimum count without "at least"; an abandoned lock directory leaves the fail-open advisory for the rest of that session.
8. **Amendment binding is permanent.** Every recorded amendment revalidates its structured decision block on every gate check, so a later edit to that block, or removal of that decisions file, turns the planning check red. This is the designed strictness; it needs a recorded route for a same-day decision correction.

The helper also raised a "starvation" case: one unresolved direct admission, then three workflow children, then the direct agent's own child refused at cap 3. Checked by hand against `observation`: four distinct agents exist there, so a refusal is correct; the count is a minimum and never exceeds the actual total, so overlap cannot refuse a session that is within its cap. Which agent is refused depends on arrival order. Not a defect.

## Goal and handoff

Mission contribution: fewer operator interventions to bound agent fan-out and to hand a worktree to the next role, on the path to independently verified execution. Material choices in this review were the verdict, relying on the identity-keyed gate row, and nominating rather than editing the inaccurate 07 sentences. Policy resistance and rule beating: the pass rests on executed behavior of the built and installed modules, not on a green generator count, which is what FINAL-001 showed to be insufficient. Drift and seeking the wrong goal: the release text says "observed admission cap" and lists the uncounted paths rather than claiming a total. Commons and escalation: one grouped helper against a budget of twenty, not one agent per claim. Burden shifting: the eight items go to planning as named candidates instead of becoming a recurring manual check. Success to the successful: the existing pin list and the executor's own fixtures received no exemption; both were re-derived. Naive Interventionism: a reviewer edit to 07 would change a followups-tracked candidate section after verification for a wording gain, so it is nominated, not made. NoOp, failing the review for coverage gaps, would hold a working and verified cap behind test refinements that no criterion requires.

Process cost: at entry (17:50:23Z) 140,233 total tokens; at 18:03:54Z 8,357,076 total tokens, mostly cached input. Both are dispatch scope from `claude-transcript-message-usage`; USD and reasoning tokens are unknown. The helper reported 131,929 tokens and 530 s of its own. Final counters remain in ignored receipts and the handoff response.

On this pass the reviewer records the result, commits the reviewed state as a coherent series, pushes `wo-139` and opens its pull request. No merge, push to `main`, tag, release or package publication is performed.
