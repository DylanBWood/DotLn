# WO-067 FINAL-001 — the compiled PresencePolicy: an operator-away curve that can only narrow the base

**Verdict: pass.** A loadout graph may now carry an optional `presence` collection: ordered phases over four named axes, each with an entry cadence or condition, a scope, a required-capability list, a return disposition and an envelope that is validated against the final compiled base and lowered to an effective envelope, a gated kernel cadence, a statechart gate and an observed availability. Every dimension of that envelope is a narrowing, so anything the phase authorizes the base also authorizes; a phase that would widen refuses with `AUTHORITY WIDENING`, an unknown rule refuses naming its field, a graph without the collection compiles to the same bytes and hash as before, the three editable views carry it, and the tooltip derives the curve from compiled values. The seven fixture behaviours the order names run against the emitted table with the real kernel authorizer and cadence evaluator. All six acceptance criteria and the operator-directed adjacent snapshot repair are met at the final bytes; VER-001 passed with no findings and killed nine of nine mutations; this review re-read the whole subject at source, ran its own checks, confirmed the operator-review assumption on the fixture's reading, and found no defect that a criterion charges. The reviewer's single `npm test -- --review` ran at this code identity and passed; its row is reproduced below and carried in the completion event.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.272","model":"claude-fable-5-1","effort":"xhigh","source":"self-reported"}

Dispatch: `resume: final review`, recorded by the harness as `FinalReviewRequested` at 2026-09-15T18:39:53.914Z with FINAL-001 allocated at this path from checkpoint `refs/dotln/checkpoint/WO-067/5` (`60ff042b…`); the dispatch was not repeated here. The harness version is `claude --version`. The model identifier is the session's own; the operator's `/model` output this session set `Fable 5.1`. The effort is self-reported: the environment exposes `CLAUDE_EFFORT=xhigh`, which is not a vendor effective-effort readback, so none is claimed. The order declares `reviewer any`. This session held the worktree's only writer reservation (`writer --show`: reserved, alive, reserved at the dispatch). No subagents were used; every file named below was read by this session. No operator control was opened.

**Bypassed requirements: none.**

Instrument disclosure: the hooks this order regenerated served this session. They recorded the dispatch, reserved the writer, and during my own gate refused a `sed | grep` pipeline and a `for` loop while admitting `grep`, `cat`, `git add` before the gate and the host Read tool. That is the live-gate guard WO-132 shipped, running from the snapshot the adjacent repair installed, and it is also part of the subject: the regenerated hooks point at `.runtime/harness/f1f7fc0e67e4664f` and the previous snapshot `7b3c264bb69b6f3f` remains on disk. Where a claim rests on that machinery I say so.

## Subject and integration

The subject is [WO-067](../../work-orders/WO-067-presence-policy-compiled.md), criteria 1–6 as filed, the operator-review assumption, and the 2026-09-15 bounded adjacent repair section, on branch `wo-067` at base `7aeccb1` (`HEAD` equals local `main`; `git rev-list --count main..HEAD` is 0; no branch commit preceded this review). The working diff is 43 tracked files (782 insertions, 347 deletions) plus nine untracked paths: two compiler source files, two test files and one fixture, now staged, and the control segment, evidence directory, verification directory and this directory. The application target is v0.19.0 with compiler 0.11.0; skeleton 0.16.0 and console 0.1.6 keep their source versions and the skeleton's compiler pin follows; kernel 0.2.1 is byte-unchanged (`git diff HEAD -- packages/kernel` is empty). The lockfile diff is the compiler version and the skeleton's pin to it; no dependency and no user setting is in the tree. A first `git fetch` from this sandbox was refused by the network filter; the retried fetch reached the remote and `origin/main` is `7aeccb1`, equal to the base, so no merge, conflict or retiming is needed.

Read scope: the order and every cited section: ADR-0007, product 02 §Events and decisions and §LoadoutGraph v1 payload contract, product 03 §Operator-presence policy and its candidate, product 04 §RPG view, product 07 §Goal-aligned decisions, product 08 §PRs and commits, and the planning map rows for the PresencePolicy candidate; `presence-schema.ts` and `presence.ts` in full; the diffs to `types.ts`, `compile.ts`, `normalize.ts`, `render.ts`, `views.ts`, `index.ts`, `artifact-identity.ts`, both package manifests and the lockfile; both new test files and the fixture in full; `scripts/lib/harness.mjs`, `scripts/test-harness.mjs` and `scripts/lib/evidence-sources.mjs`; one regenerated hook, the harness manifest and the console fixture manifest diffs, with the three console renders summarized by changed-line class; every product, ADR, README, publication, decisions-index, followups, control and evidence-edition diff; the evidence README, decisions D001–D003, both TAP transcripts, the control segment, VER-001, the existing meter-only PR draft, and the WO-132 final-review artifacts as the current publication precedent.

Ideation receipt: the order carries no operator-opened ideation section and the evidence directory has no `ideation.md`. Its nomination provenance is ADR-0007, product 03's candidate, the planning map's PresencePolicy candidate, and the operator's 2026-09-08 mid-pass direction during the critical-path planning pass, whose five correction messages are preserved verbatim in ignored intake (`docs/intake/notes/2026-09-08-critical-path-planning-correction.md`, SHA-256 `4b3a9b27…`) and whose plan records them. Two rows of that plan bind this order: the operator's "operator away" cron whose changes start small and widen became "a compiled presence policy: phases with change-size ceilings and envelope narrowings, advance on verified success, reset on failure or at the peak, cancel on return" (WO-067, WO-100, WO-111), and the external review's fourth finding, that `cancel-on-return` was unscoped, was answered by scoping cancellation to discretionary phases and letting a requested foreground task continue. The compiled `return` transition carries a kill or finish action only when the phase is discretionary and always carries `preserve-foreground`, and the fixture's requested foreground task completes under its own envelope after return in both race orderings. The receipt is answered.

## Verification sequence

| Report | Verdict | What it established | Limits it named |
| --- | --- | --- | --- |
| VER-001 (Claude Code 2.1.272, claude-opus-5[1m], xhigh) | pass | Fresh `npm test` 19 groups 0 failed (240.90 s); `node --test scripts/test-harness.mjs` 26 of 26 including the adjacent-repair regression; `harness check` 24 surfaces; `publication:check`; `git diff --check`; kernel and lockfile diffs; prettier over changed files; 24 hostile compile probes refusing or admitting as product 02 specifies; a mutation run on a scratch mirror killing 9 of 9 deliberate breaks of the lowering; criteria 1–6 and the repair each judged met | O1 tooltip pinned by regex, not a golden render; O2 non-discretionary phases also leave on return, unexercised; O3 `scope.changeSize`/`budget` not compared with the effective envelope; O4 the fixture host interprets emitted guard names; O5 a `Calendar` cadence refuses naming the first disallowed key rather than `.kind` |

One verification was requested and it passed; there is no repair in the sequence. The verifier changed no implementation, and the exact tree it gated differs from mine by the dispatch event, its projections, the index refresh and the staging of the five new files, which are outside the code identity until staged and inside it now.

## Independent checks executed

- `node scripts/harness.mjs check`: 24 generated surfaces pass for compiler 0.11.0 and skeleton 0.16.0; the regenerated hooks and manifest match emission from the changed `harness.mjs`.
- `npm run publication:check`: both edition locks current (30 and 45 linked source sections); the new `02#compiled-presence-policies` row resolves.
- `npm run format:check`: every matched file uses Prettier style. `git diff --check HEAD`: clean.
- `git diff HEAD -- .claude/harness-manifest.json` contains no changed `semanticHash` line, so no compiled program hash moved; the changed `feedbackPolicyHash` and the console render's two "Recorded build hash" values embed `COMPILER_PACKAGE_VERSION`, and every other changed console line is the recorder's relabel to the WO-067 feedback edition, its evidence paths, grant-expiry timestamps and receipt identifiers.
- `node scripts/authority-evidence.mjs --check`: three unchanged programs, the WO-132 plan-refuter migration, four widening rejections, nine runtime denials and 27 bundle comparisons verified.
- Semantic-hash probe against the built compiler: the fixture compiles to `fnv1a64:b4f9b111da86c248` with its policy, to `fnv1a64:c6bc2feeb35764b0` with the collection removed, and to `fnv1a64:7e2c9259bc3fa7d7` after one phase's `changeSize.files` moves from 1 to 2. Presence enters the hash and a phase edit moves it, which product 10's new paragraph states; the WO-042 requirement that a fork's actor behaves the same under the same hash therefore holds for policy data too.
- Clean-room screen over the new source, tests, fixture, order, reports and every added product line for URL, e-mail and credential patterns: nothing beyond the repository's own public link.
- `npm test -- --review --list`: the nineteen product and lifecycle suites plus the eight machinery suites whose declared sources this order changed (`authority-evidence`, `harness-fixtures`, `harness`, `harness-context`, `harness-evidence`, `artifact-evidence`, `verification-evidence`, `feedback-evidence`).

## Source review

The schema (`presence-schema.ts`) closes every object against a key list at every level and fails with a field path before normalization can drop anything: identifiers are non-empty text without control characters, numbers are finite and non-negative, ceilings and versions are safe integers, effect patterns admit `*` only as a terminal character and never whitespace, `entry` has exactly one of `cadence` or `condition`, cadence kinds are `Once`, `After`, `Every` (positive interval), `Gate` and `Until` to a nesting depth of 32, `axes` must be exactly the four, `curve`, `returnRule` and `decay.expires` admit only the one value each this subset compiles, `present` and `expired` are reserved phase identifiers, duplicate policy or phase identifiers refuse, and a non-discretionary phase must declare `finish`. Policies sort by identifier and phases keep their authored order.

The lowering (`presence.ts`) is computed against the emitted base program, after grants and claims, so the ceiling is the final compiled envelope. An allowed effect is admitted only when some base allow pattern covers it and no base deny pattern covers it; a resource limit is compared with the base's own-property value, or with 0 when the base has no such resource; expiry cannot extend. The effective envelope takes the narrowing's allow list, limits and expiry where given and the base's otherwise, and unions denials, required evidence, revocation event types and revocation conditions, so for every rule in the kernel's pinned order (invalid effect, expiry, revocation, denial, absence from the allow list, missing evidence, exhaustion) the narrowed envelope refuses whenever the base does. A terminal-prefix allowance equal to the base keeps the base's exact denials, which is how a base-equal `repo.*` peak still refuses `repo.delete`. The statechart is `present`, the phases and `expired`; `absence` enters the first phase; `verified-success` moves to the next phase or, at the peak, back to the first with `reset-progress`; `failure` returns to the first; `return` cancels pending dispatch, resets progress, kills or finishes a discretionary episode per the phase and preserves the foreground; `idle-expired` expires the phase and enters `expired`, which only a `return` leaves. Each phase's cadence is `Gate(entry, phase-ready)`, with a condition entry lowered to `Gate(After(0), condition)`; the idle cadence is `After(idleMs)`; a missing capability emits `NoOp` with a reason. The compiler emits and never schedules.

`compile.ts` appends the collection to the program only when non-empty and refuses when any widening diagnostic exists; `normalize.ts` omits an empty collection from the normalized graph; `views.ts` carries `presence-policy` rows, `context.presence` and, through the code DSL's full-graph normalization, the collection itself; `render.ts` appends the derived PULSE and INTERRUPT lines to the authored inspection on the single diff renderer that the console and the skeleton CLI both call. `scripts/lib/harness.mjs` adds the compiler's artifact-identity module to the runtime pins with a comment naming why; the `test-harness.mjs` regression bumps only the compiler version, re-emits, asserts a new snapshot beside the retained old bytes, and then invokes the emitted writer hook and requires an admitted decision that actually reserves its writer. `evidence-sources.mjs` names the two new compiler files as common behaviour sources, so evidence editions re-key on a presence change; that is a shared-check change within the order's own surface.

## The reviewer's gate

`npm test -- --review`, started 2026-09-15T18:47:57Z after the five new files were staged and with no source edit by this review: **27 suites passed, 0 failed, 807.17 s, 70 fresh tasks**. That is the nineteen product and lifecycle suites plus the eight machinery suites whose declared sources this order changed, the harness fixture file that carries the adjacent-repair regression among them. The row was recorded at 2026-09-15T19:01:26.135Z as `host-gate:1104b4f72410ac29d28e52b976d1f61c97f59463b9d523fd3bd3914f8e00921f:npm test` with exact tree `9917c2eb8c188ce1c8f1bdd86305538591e84718` and exit 0, and `findGateCheck` resolved it at the current code identity after the gate with no gate left live. The identity differs from the one the executor’s and VER-001’s fresh gates recorded (`efaa647d…`) because the five new source, test and fixture files were untracked then and are staged now; no tracked byte differs between the two, and those two earlier passing rows (253.66 s and 240.90 s) stand beside this one. This report, the PR body and the release notes were written after the row, which the contract permits; the completion event cites the row by code identity.

## Acceptance

| Criterion | Review result |
| --- | --- |
| 1: three-phase fixture; per-phase narrowing; executable cadences; tooltip pins the curve | `probe`, `widen`, `peak` compile with file ceilings 1, 3 and 8 under base 8; every phase keeps the base's `repo.delete` denial, `verified-input` evidence, `AuthorityRevoked` revocation and expiry 100000; the skeleton test proves "narrowed authorizes ⇒ base authorizes" with the real authorizer for five effects per phase and exhausts each ceiling exactly; every emitted cadence is `After` or `Gate` and evaluates with `evaluateCadence`; the tooltip carries the policy and per-phase lines under PULSE and INTERRUPT from compiled values (pinned by regex, see O1). Met. |
| 2: widening refuses `AUTHORITY WIDENING`; unknown curve or return rule refuses naming the field | Eight envelope attempts refuse in the compiler test, including a literal denied effect, `repo.*` over an exact list, an ungranted effect, a raised limit, a positive new resource, an extended expiry and the `toString`/`constructor` prototype keys; VER-001 added bare `*`, `__proto__`, `hasOwnProperty` and a `repo.delete` under a base wildcard. `automatic-grant` refuses as `[0].curve`; `ignore` refuses as `[0].returnRule`; fifteen other malformed inputs refuse naming their key. Met. |
| 3: the curve proven as data | The committed transcript shows `NoOp:present` while present; an unverified and a foreground outcome leave `probe` and a verified one enters `widen`; `failure:probe`; two full `probe → widen → peak → probe` loops; both race orderings end `present` with `in-flight:kill` and the foreground task completing under its own envelope; a `widen` episode finishes across a return and blocks overlapping dispatch; no expiry at 99, expiry at 100, and a return-then-absence edge to rearm; an admitted portfolio grant with no adapter emits `NoOp: … unavailable external capability: adapter.portfolio`. VER-001's nine mutations each failed at least one of these. Met. |
| 4: committed hashes exact; three views round-trip | Seiri with `presence: []` equals Seiri without it at `fnv1a64:9ca8d0229c6bd8db`; no manifest `semanticHash` line moved; no existing test file changed and the hash-pinning suites pass at `HEAD`'s expectations; code DSL, function table and statechart JSON each compile deep-equal to the direct compile; normalization is idempotent; reversing phase order changes the result. My probe adds that a policy and a phase edit each move the hash. Met. |
| 5: write-backs | Product 02 (`presence?` in the payload type, the new `#### Compiled presence policies` subsection, the glossary row moved to compiled subset); 03 (the compiled-subset paragraphs under §Operator-presence policy); 04 (the presence-curve bullet under the RPG view); 10 (the WO-067 version-axes paragraph); ADR-0007 §Amendments (dated note); the ledger duty discharged by D001–D003 and their three decisions-index rows, as the pre-2026-09-09 rule allows and the work-order index marks; the audience-status row and both TOC locks, checked current. Each statement was compared with the code above and none overstates it. The roadmap and the root README release block are additional. Met. |
| 6: `npm test` green; `git diff --check` clean; no new dependency; kernel unchanged | The reviewer's gate above; VER-001's fresh product gate; `git diff --check` clean before and after staging; lockfile diff first-party only; kernel diff empty. Met. |
| Bounded adjacent repair (2026-09-15, D003) | The pin, the regression that runs the emitted hook, the regenerated surfaces at `f1f7fc0e67e4664f`, the retained `7b3c264bb69b6f3f`, and a passing `harness check`. The operator's direction is quoted in the order and in D003. Met. |
| Operator-review assumption 1: the curve reading; the fixture is editable data | Confirmed against the order's own design text and the ideation receipt's row: `probe` is one file under `repo.inspect` alone, discretionary and killed on return; `widen` is three files in one surface under the full base allow set, finished on return; `peak` is eight files across both fixture surfaces, equal to the base ceiling, finished on return; then reset and loop. The fixture is left as authored and remains data the operator can edit; the comparison with the predecessor's actual cron is the operator's own, since the clean room admits only the operator's recorded description of it. |

## Observations carried forward, none blocking

1. **VER-001 O1–O5 stand.** The tooltip is regex-pinned; a non-discretionary phase leaves on return without a kill or finish action and no fixture exercises one; `scope.changeSize` and `scope.budget` are host-counted policy data not compared with the effective envelope, so a phase can declare a ceiling above its limit and the tooltip shows both; the fixture host interprets emitted guard names; a `Calendar` entry refuses naming `.entry.cadence.expression`. WO-068 should decide which ceiling wins when scope and envelope disagree, and should register the three predicate identifiers the compiler emits.
2. **`expired` is left only by a return edge.** A host that keeps observing absence after idle expiry cannot rearm the policy; a recorded return followed by fresh absence is required. That is D001's "fresh absence is needed to rearm" and product 03 states it, but WO-068 should surface `expired` in status so an unattended run that went quiet is visible rather than silent.
3. **A single-phase policy loops on itself.** With one phase, `verified-success` transitions to the same phase with `reset-progress`, which is the peak rule applied to the first phase. Legal and unexercised.
4. **Compile-time availability is a snapshot.** `availability` reflects the compilation environment's capabilities; product 02 says the host must recheck before dispatch. Carried for WO-068.
5. **Executor attestation.** `ImplementationReady` recorded `codex-cli` 0.154.0 with model and effort `unknown` against the order's `executor xhigh+` recommendation, and advised thirty-one outputs not read at current bytes; the evidence README states why readback was unavailable. The contract logs and never refuses, and the verifier and this review read those outputs. The refreshed meter records the executor phase at 2,427,029 ms, the verification at 988,464 ms and this review at handoff. Planning input, not a charge.
6. **Evidence editions now re-key on the two presence files.** `evidence-sources.mjs` is a shared check; the change is inside this order's surface and is named here so the next evidence edition's key move is expected.

## Goal alignment and process cost

This order is the first link of the unattended chain (WO-067 → WO-068 → WO-099, WO-100, WO-111): the resident host can now consume an inspectable, narrowing-only absence contract instead of prose, and a fork's actor behaves identically under the same hash. NoOp would have left the resident gate without that contract and kept the predecessor's behaviour as an unverifiable description. Rule beating was the material trap for this review, so the judgment rests on reading the lowering at source, the verifier's mutation run, the real kernel authorizer and cadence evaluator in the fixture, and my own hash probe rather than on transcript shape. Policy resistance is answered by computing every narrowing against the final base so no phase can undo a support denial or an admitted grant's boundary; escalation and commons cost were bounded to one reviewer gate and the checks above, with no new process; the adjacent repair pinned one module and added one regression rather than replacing the snapshot architecture (D003), and it removes a recurring operator rescue after every compiler release; kernel cadences won over a new schedule language on compatibility, not investment (D001); drift is held by the unchanged hash-pinning suites; the outcome judged is data a resident can execute, not fixture or receipt counts. Naive Interventionism: the collection is additive and opt-in, removable by deleting it, and every existing program's bytes and hash are exact.

Process cost, source `claude-transcript-message-usage`, scope dispatch: 73,535 total tokens at entry (cutoff 2026-09-15T18:40:09.886Z); 3,106,027 total tokens and 56,694 output tokens at the last observation before writing (cutoff 2026-09-15T19:01:49.492Z; 52 steps, 41 commands); the handoff counter is reported in the response and the ignored receipt. Cost in dollars is unknown. Wall-clock waiting was the single reviewer gate; no subagent ran.

## Publication screening

No employer material, credential, internal URL, private identifier or secret is present; the predecessor is named only as such and only through the operator's own description; the clean-room screen found no stop condition. Commit subjects and the PR text carry no AI attribution.

## Commit and publication plan

On recording this pass the reviewed state is committed as four coherent changes: the adjacent harness snapshot repair on its own, since its two source files and their regression review independently; the presence implementation with its tests and fixture, the compiler 0.11.0 release with the skeleton pin and lockfile, the regenerated hooks and manifest that both the release and the repair require, the console self-host repin and the evidence-source list; the product, ADR, README and publication write-backs; and the WO-067 control, evidence, verification, decision-index, followups, final-review and index records. The PR opens with `npm run worktree -- publish WO-067` against the committed body beside this report, which carries the gate row. Merge and release close remain the operator's.
