## What this merges

WO-022, the `v0.11.0` minor release above `v0.10.1`: perception as a compiled, authorized, mounted capability. Three support facets on the perception slot, Beacon Sight, Fine Spectrum, and Composition, compile in the pure compiler against host-derived mount capabilities and declare a measured cost of one context line per sweep, no authority change, and `BeaconObserved` evidence. The skeleton's host profile `beacon-perception-v1` is the mount authority record: the reactor authorizes the existing `observe.beacons.public` or `observe.beacons.verifier` effect, then the perception gate recompiles the declared senses and refuses with a `CommandRefused` naming the envelope when Beacon Sight or its mount is missing, before the first filesystem call. The sparse twin runs the same decision and renders zero or exactly one sweep action. The mounted reader performs only known-name status calls on the declared addresses, the edge whitelist and replay validation reject anything beyond the codebook fields, Fine Spectrum alone unlocks sub-second time, and Composition alone unlocks the separately mounted phase-group codeword. Individual Beacon codebook v3 adds a non-secret 8-bit key epoch and a 16-bit keyed residue carried only in the size codeword, with a sparse-required zero-tail content contract whose full 1,236,950,589,434-byte maximum is probed on the destination volume before any emission; an external mode-0600 key outside every checkout signs and checks it, rotation retains no old key and refuses at epoch 255, and the labels `residue-matched`, `forged-provenance`, `unverifiable-provenance`, and `unauthenticated-legacy` describe a weak host-consistency check rather than authorship. Restricted Beacon directories are search-only at rest. A deterministic `MountedBeaconVerifier` completes the walking skeleton from a detached capsule that a test proves contains no implementer prose, physical path, or capability. The kernel is byte-unchanged; no dependency was added.

- **Criterion 1, compile-time gate:** `SUPPORT INACTIVE` names `beacons.individual.metadata` with a `provide-capability` correction when the mount is absent; with grants all three supports compile active with the supplied envelope unchanged and one, two, or three context lines per sweep.
- **Criteria 2 and 3, affordance and guard:** nine refusal shapes (no sense, no mount, duplicate senses, no allowed effect, explicit denial, expiry, exhausted `beaconSweeps`, missing evidence, and a profile declaring a model tool) render no sweep action, persist exactly `BeaconSweepRequested` then `CommandRefused` with the envelope id, and make zero metadata reads; the equipped, mounted, authorized role renders exactly one action.
- **Criterion 4, exact fields:** over a real 0111 mount with garbage content, an extended attribute, an unrelated file, and mode 000, the observation keys are exactly `address`, `size`, `mtimeMs`, `mtimeNs` when sensed, `decoded`, `provenanceCheck`, and `fineSpectrum`; the persisted log contains no content, extra name, attribute name, physical path, capability, or key byte; tampered replays throw.
- **Criterion 5, blinded verifier:** the host log provably retains the implementer narrative while the verifier's capsule has exactly `candidatePaths`, `episodeId`, `inventory`, and `perception` and contains neither the narrative nor the mount path; `VerificationCompleted` is accepted and the episode replays identically.
- **Criteria 6, 7, and 8, provenance and channels:** listing a 0111 directory fails with `EACCES` while a known-name `lstat` succeeds; a current-epoch residue off by one decodes `forged-provenance`, a never-issued epoch `unverifiable-provenance`, and v1/v2 `unauthenticated-legacy`; Beacon Sight alone yields whole-second time and `not-sensed` fine and group channels while the group directory is unreadable, and all three senses yield exact `mtimeNs`, one group observation, and three rendered lines; canonical control JSONL re-derives a byte-identical v3 size and mtime through the production emitter; rotation to 255 then refuses without touching the key file or any Beacon.
- **Criterion 9, arithmetic and storage:** the field product is 4,831,838,208, `MAX_V3_CODE` is 19,327,352,831, and `MAX_V3_LOGICAL_BYTES` is 1,236,950,589,434, recomputed independently; 589,824 representative residue round trips and every inherited-field combination at boundary epochs pass; the full maximum lands in at most eight 512-byte blocks with its holes and exact mtime preserved through atomic rename while a `Buffer.alloc` witness sees no allocation above 64 KiB; every unsupported premise refuses with no destination or staging file.
- **Criterion 10 and write-backs:** `npm test` green, `git diff --check` clean, no dependency, no key material in any committed file; the domain model, architecture, interfaces, audit, execution guide, IR compatibility, roadmap, roles lens, planning map, publication index and locks, README release block, package READMEs, work-order activation header, and ledger are updated consistently.

## Evidence

Re-established first-hand at final review inside the Claude Code sandbox, not inherited:

- `npm test` on the verified tree after dispatch and again on the reviewed tree: Prettier clean, the body-profile suite, every shell suite including the three-worktree constellation with its new provenance column and the restricted-directory teardown fixture, `check-publication.mjs` at 229 of 229 headings with both audience locks current, `index --check` current, `tsc -b --force`, 245 of 245 package tests including the nine WO-022 tests, 8 of 8 identity-corpus tests, and the four current artifact-evidence files verified; exit 0. `git diff --check` clean.
- `npm run release -- check-surfaces`: `PASS release-block: observed v0.11.0; expected v0.11.0`, `src changed` with advanced versions for the compiler and skeleton, `src unchanged` for the kernel, and the body profile over this package's `PR.md` and `RELEASE-NOTES.md`.
- Subject integrity: a temporary-index tree of the working tree differs from the final-review allocation checkpoint only in the two control projections and the regenerated index; every deliverable blob is byte-identical from the implementation-ready checkpoint through the reviewed tree except the verifier's one recorded discovery line; all five checkpoints have `HEAD` (`2851225`, also `main` and `origin/main`, the published `v0.10.1`) as their sole parent; `git log main..wo-022` was empty before the reviewed series; the verifier's report header equals its control-log actor by JSON comparison; the executor's `max` and the verifier's `max` meet the declared `xhigh+` floors.
- Direct probes: the shipped `projectBeaconSparseTwin` and `observeBeaconSweep` run against this worktree's real `.control-beacons/public/` directory returned no action unequipped or unmounted, one action equipped, an authorized sweep of exactly four events, the live 72,341-byte codeword decoding to `final-review`, `pass`, `max`, `host-projected` with `unauthenticated-legacy`, a nanosecond timestamp equal to the host's `lstat`, no physical path in the log, two rendered lines, an identical replay, and whole-second time with no `mtimeNs` when Fine Spectrum was unequipped; a fresh key outside the repository produced an authenticator equal to an independent `createHmac` over the documented preimage, a tampered residue read `forged-provenance`, an unknown epoch `unverifiable-provenance`, the v3 size round-tripped, and a key path inside the repository was refused; a 64-hex scan over the diff and every new file found only the two publication locks; a clean-room term screen over every changed file found only pre-existing generic boundary prose; 602 relative links across the 21 changed Markdown files resolve; the gitmoji catalog confirms `:passport_control:` as "Work on code related to authorization, roles and permissions".

## Verification trail

- `docs/verifications/WO-022/VER-001.md`: pass; all ten criteria, the evidence gate, the activation preflight, and every write-back reproduced independently by rebuilding the framing and field arithmetic from the normative text, recomputing the residue from the raw key with `node:crypto`, driving the shipped API against a self-built host fixture, an external `Buffer.alloc` witness on the full maximum, and the full gate; six non-blocking observations and five disclosures, none requiring repair.
- `docs/final-reviews/WO-022/FINAL-001.md`: pass; the verifier's observations and disclosures adjudicated with reasons, the reviewer's own observations recorded including a post-verdict wording edit to VER-001 that the verdict checkpoint preserves, the coherent two-commit series confirmed, one dated documentation note applied and re-checked, method and instruments disclosed.

## Known open items

- **VER-001 was edited after its verdict was recorded** (FINAL-001 adjudication 8): the verdict checkpoint `refs/dotln/checkpoint/WO-022/4` holds the 393-line report the completion command checked; the committed 394-line report changes a disclosure count from four to five, adds one sentence separating the verification's own additions from the subject, and reflows the actor paragraph. The attestation header, verdict, criteria, observations, and disclosures are byte-identical. Recorded as a process deviation from report immutability, not a defect in the deliverable; a lifecycle guard that pins the report digest at completion is a planning candidate.
- **WO-029's four artifact-evidence receipts are no longer byte-checked** (VER-001 O1): the helper now checks the current edition under WO-022; a dated migration note in the WO-029 evidence README records this, and a hash pin for the historical files is a patch candidate.
- **Effort attestation refuses on a harness patch newer than the recorded discovery versions** (VER-001 D5): the verifier recorded `2.1.263` first-hand in `docs/discovery/environment.json`, the one line in this PR authored by the verifier rather than the executor; the generic gap is a planning candidate.
- **The restricted directory is owner-writable during the emission window** (VER-001 O6): inside the documented same-user limit.
- **The agent constellation text no longer prints age or group-count rows** (VER-001 O2): intended by criterion 4; the human form retains them.
- **`snapshotBeacons` skips directories without owner read** (VER-001 O3): an in-place rewrite of an existing inode inside a 0111 directory is the bounded blind spot of that test helper.
- **A provenance sentence in the work order reads as a forward reference** (VER-001 O5): "confirmed that direction during VER-001 repair" is operator-authored text that predates this order's activation and describes an earlier order's repair; the scope authority is not edited by a verifier or reviewer.
- **The mounted verifier is deterministic**, the constellation CLI form was exercised through its fixture and this review's module-level probe rather than a new observation log on this repository, and the end-to-end warning on a genuinely dense volume with a configured key was not observed on real dense storage; native model verification remains WO-010.
- **Publish and release close.** The bounded publisher runs the committed surface check and body validation before any push and then preflights `gh`; in the Claude Code sandbox `gh` cannot read its configuration, so if the helper refuses at that preflight the operator runs the same command from this worktree outside the sandbox. After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute subject-helper command the publisher prints, with the main checkout as the working directory; because `v0.11.0` is strictly above `v0.10.1`, it runs `npm ci` and the evidence gate, assembles the edition from these notes, creates and pushes the annotated `v0.11.0` tag, and creates the matching GitHub Release.
