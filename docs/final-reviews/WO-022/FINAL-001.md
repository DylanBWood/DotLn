# WO-022 final review FINAL-001

**Subject:** branch `wo-022`, worktree `DotLn-wo022`, uncommitted working tree, 2026-09-05 local time (the control events are stamped 2026-09-06 UTC). `HEAD`, `main`, and `origin/main` are all `285122570696f58023b8ebdb0ea2c39ab8a06ed8`, the PR #37 merge of WO-031 (`git ls-remote origin main` agrees), which is also the commit the published annotated tag `v0.10.1` (tag object `ca5d3fab28ecfa00cb7d0738d8e6a0be3018569d`) peels to. The branch carries no local commits (`git log main..wo-022` is empty), so the entire subject is the dirty tree. At review open, before the dispatch transition: 64 `git status --porcelain -uall` entries, 44 tracked modifications (against `HEAD`, 44 files, 939 insertions / 187 deletions including `docs/control/current.md` and the generated `docs/work-orders/README.md`; 42 files, 901 / 166 without them) plus 20 untracked paths totalling 6,793 lines: eight source leaves (`packages/compiler/src/senses.ts` 219; `packages/skeleton/src/beacon-perception.ts` 269, `beacon-perception-fs.ts` 64, `beacon-verifier.ts` 90, `execution-environment.ts` 111, `beacon-provenance.mjs` 156, `beacon-v3-codebook.mjs` 94, `beacon-v3-fs.mjs` 160; 1,163 in all), three test files (`packages/compiler/test/senses.test.ts` 87, `packages/skeleton/test/senses.test.ts` 617, `senses-v3.test.ts` 408; 1,112), the order's own control segment `docs/control/orders/WO-022.jsonl` (four events at open), `docs/evidence/WO-022/` (`README.md` 101, `focused-tests.txt` 65, `host-probe.json` 106, and the four `artifact-identity/` receipts totalling 3,847), and `docs/verifications/WO-022/VER-001.md` (394). The subject is the tree VER-001 passed, with one qualification recorded in adjudication 8: a temporary-index `write-tree` of the working tree taken after the dispatch and the index refresh (`3f71e8d14374ad7dee6d8c6df6a3149cd120e421`) differs from the verdict checkpoint `refs/dotln/checkpoint/WO-022/4` in `docs/control/current.md`, the strictly appended segment, the regenerated index, and a 12-insertion, 11-deletion wording edit to VER-001 itself that the verifier made after its verdict was recorded (§Dispatch and subject integrity). This review adds this report, its PR body, this order's `RELEASE-NOTES.md`, one dated documentation note (§Corrections applied by this review), and the lifecycle's own events.

## Actor

This review ran on the Claude Code CLI, version `2.1.263` (`claude --version` observed locally in this session), model `claude-fable-5-1` (Fable 5.1), at reasoning effort `max` selected by this session's model control. The value is self-reported: `max` is on this harness's `sessionEffortSelector.values` in `docs/discovery/environment.json`, `2.1.263` is on its observed `versions` list because the verifier recorded it first-hand during VER-001 (VER-001 D5), and `effectiveEffortReadback` for `claude-code` remains `not found` with `harnessReadbackEligible: false`, so `harness-readback` is unavailable and `self-reported` is the honest label. The work order declares `reviewer any`, so no floor applies; the executor (Codex CLI `0.153.4`, `gpt-6-astra`, `max`, operator-attested) and the verifier (Claude Code `2.1.263`, `claude-opus-5[1m]`, `max`, self-reported) were different sessions, and this reviewer is a third model in a third session. No `--account-label` is supplied, so this header carries no `accountLabel` and the recorded event will project `not-applicable`. This review needed no discovery change of its own.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-fable-5-1","effort":"max","source":"self-reported"}

**Verdict: WO-022 passes.** The order asked that a sense become a thing a loadout equips, an envelope grants, an environment mounts, and an audit records: a role without the sense cannot compile the sweep, cannot see the affordance, is refused with an event if it tries, and reads nothing; a role with the sense learns exactly the codebook fields and nothing else. The deliverable does that. Three support facets on the perception slot compile in the pure compiler against capabilities the host derives from its mount record, declare one measured context line per sweep, no authority change, and `BeaconObserved` evidence, and report `SUPPORT INACTIVE` with the exact missing capability and a concrete correction when the mount is absent. The reactor authorizes the existing `observe.beacons.public` or `.verifier` effect and then runs a perception gate that recompiles the declared senses; a missing sense or mount refuses with a trace and a `CommandRefused` naming the envelope before the first filesystem call, and the sparse twin runs the same decision to render zero or exactly one sweep action. The mounted reader performs only known-name `lstat` calls on the declared addresses; the edge whitelist and replay validation reject content, extra names, extended attributes, physical paths, capabilities, key bytes, and any sub-second value when Fine Spectrum is not equipped; Composition alone unlocks the separately mounted group codeword, and unequipped channels say `not-sensed` rather than `absent`. Individual codebook v3 adds an 8-bit non-secret epoch and an exact 16-bit keyed residue carried only in the size codeword, with declared `bigint` maxima, an algebraic injectivity argument that is equality-tested against the normative block, a sparse-required zero-tail contract whose full 1,236,950,589,434-byte maximum is probed on the destination volume before any emission, an external mode-0600 key that never enters a prompt, fixture, log, or checkout, rotation that retains no old key and refuses at 255, and labels that every surface describes as a weak host-consistency check rather than authorship. Restricted directories are 0111 at rest. A deterministic mounted verifier completes the walking skeleton from a detached capsule that a test proves excludes the implementer's narrative, the mount path, and the capability. The kernel is byte-unchanged, no dependency was added, the suite is green twice, whitespace is clean, and the release surface passes at `v0.11.0`. VER-001's six observations and five disclosures are adjudicated below as non-blocking; this review records four observations of its own, of which one (the post-verdict edit to VER-001) is a process deviation that does not alter any claim, applies one dated documentation note, and changes no code, contract, acceptance behavior, schema, compatibility, authority, or captured-evidence byte.

## Self-referential instruments, disclosed

07-execution-guide.md §Discipline requires naming the instruments that are part of, or adjacent to, the deliverable under judgment.

1. **The dispatcher published this review through the code under test.** `npm run resume -- final-review` appended `FinalReviewRequested`, minted checkpoint 5, and emitted WO-022's Beacon through the modified `emitControlBeacon` and `projectControlBeacon`. Compensating controls: the appended line was read raw and its fields, ordinal, and strictly increasing `recordedAt` checked by hand; subject identity was established with git objects rather than the helpers' projection; and the published public codeword was decoded in two independent ways (§Final evidence summary), giving `final-review`, `pass`, `max`, `host-projected` at exactly the transition's `recordedAt`. With no `DOTLN_BEACON_KEY_FILE` configured this is the unchanged dense v2 path, which is itself the documented no-key behavior.
2. **The compiler, skeleton, and lifecycle Beacon emitter under review are the modules used to observe them.** Compensating controls: the codebook constants were checked against the arithmetic in 02 rather than against the encoder's inverse; the residue was recomputed for a fresh key with `createHmac` over the documented preimage rather than through `openBeaconKey().sign`; the shipped sweep was driven against this worktree's real public Beacon and its result compared with the host's own `lstat` and with a direct `decodeSignalSize` of the host size; and `npm test`, which includes the reactor import boundary test and the normative-block equality test, ran first-hand twice.
3. **The executor's fixtures are executor-authored and the verifier's reproductions are verifier-authored.** Both were treated as claims. The nine WO-022 tests were read in full, their assertions compared with the acceptance criteria clause by clause, and the two claims that could pass vacuously (that the host log contains the implementer narrative, and that the extra name is really inside the mount) were confirmed to be positively asserted.
4. **The index generator, the surface check, the publish helper, the notes parser, and the body profile are unchanged instruments.** `scripts/work-orders.mjs`, `scripts/release.mjs`, `scripts/release-notes.mjs`, and `scripts/github-body.mjs` are byte-identical to `HEAD`; `scripts/worktree.mjs` changes only in the `finish` path (restricted-directory disposal), and the `publish` path (`check-surfaces --committed`, tracked-body read, body profile, notes parse, `gh` preflight, push, `pr create`) was read from source and is unchanged.
5. **The reviewer authored `RELEASE-NOTES.md` and `PR.md` and validated them with the repository's own parser and profile** by calling `parseReleaseNotes` and `assertGitHubBodyProfile` directly and through `check-surfaces`, which reports `PASS github-body-profile` over both files. Both passed on the first attempt; neither body contains an angle bracket, which the notes parser treats as raw HTML even inside inline code. Because `v0.11.0` is above the latest published tag, these notes become the `v0.11.0` edition at release close.

## Dispatch and subject integrity

`npm run resume --silent -- status --json` named WO-022 at phase `verified` with `final-review` as the only legal action, latest verdict VER-001 pass, the verifier's attestation as latest with `accountLabel: "not-applicable"`, drift `[{"effort":"max"}]`, `recordedAt` `2026-09-06T03:43:04.998Z`, `elapsed` `{"implementation":4179858,"verification":1410848}`, checkpoint `8cc67a8ec1e995592eaba357aff1956a4bb5c2fb` (`/4`), and `orders[]` with the known orders; no projection-disagreement warning was emitted. `npm run resume -- final-review` ran inside the ordinary sandbox with no approval escalation, printed `Final-review docs/work-orders/WO-022-senses.md, the complete verification sequence, and ideation receipt; write docs/final-reviews/WO-022/FINAL-001.md.`, moved the phase to `final-review`, appended `FinalReviewRequested` (`throughVerificationId: "VER-001"`, `finalReviewId: "FINAL-001"`) with `recordedAt` `2026-09-06T03:49:17.375Z` as line 5 of `docs/control/orders/WO-022.jsonl`, and minted checkpoint `5caf2de57144ad3655af3f2a7b41c4bccc22e63b` (`refs/dotln/checkpoint/WO-022/5`) with neither a checkpoint nor a Beacon warning. The legacy `docs/control/resume.jsonl` and every other order segment are byte-identical to `HEAD`. The WO-022 segment's five events are `WorkOrderActivated` (`2026-09-06T02:06:42.998Z`, `effortDeclarationValidated: true`), `ImplementationReady` (`03:16:22.856Z`), `VerificationRequested` (`03:19:34.150Z`), `VerificationCompleted` (`03:43:04.998Z`), and this review's `FinalReviewRequested`; every event carries `schemaVersion: 1` and the stamps strictly increase. The projected `implementation` span (4,179,858 ms) and `verification` span (1,410,848 ms) are the arithmetic differences of the recorded stamps. The index was regenerated after the dispatch (`Now: WO-022 — final-review`; the checklist row reads `final-review`; the Active block links `FINAL-001 (pending)`).

Subject identity was established with git objects, not with the lifecycle script: a temporary index (`GIT_INDEX_FILE`, `git read-tree HEAD`, `git add -A`, `git write-tree`) captured the working tree after the allocation and index refresh as tree `3f71e8d14374ad7dee6d8c6df6a3149cd120e421`, and `git diff-tree -r` against each checkpoint reported:

| Checkpoint                                 | Differences from the working tree                                                                                                                                                                                                                                     |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/1` activation (`b205f329…`)              | every deliverable path; the whole subject is downstream of activation                                                                                                                                                                                                 |
| `/2` implementation-ready (`32dd893a…`)    | `docs/control/current.md`, `docs/control/orders/WO-022.jsonl`, `docs/work-orders/README.md`, `docs/discovery/environment.json` (the verifier's one recorded line, VER-001 D5), `docs/verifications/WO-022/VER-001.md` (added); nothing else, so every deliverable blob the verifier judged is the one the executor declared ready |
| `/3` verify allocation (`3133e651…`)       | the same five paths                                                                                                                                                                                                                                                   |
| `/4` verdict (`8cc67a8e…`)                 | `docs/control/current.md`, `docs/control/orders/WO-022.jsonl` (two appended events), `docs/work-orders/README.md`, and `docs/verifications/WO-022/VER-001.md` (12 insertions, 11 deletions; adjudication 8)                                                          |
| `/5` final-review allocation (`5caf2de5…`) | `docs/control/current.md`, `docs/control/orders/WO-022.jsonl` (one appended event), `docs/work-orders/README.md`                                                                                                                                                       |

Every deliverable blob is therefore identical across the implementation-ready, verify-allocation, verdict, and final-review-allocation checkpoints and the working tree; the verifier changed nothing outside its report except the regenerated index (VER-001 D2) and the one discovery line it disclosed (D5). VER-001's current bytes equal checkpoint `/5`; they differ from checkpoint `/4` as adjudication 8 records. All five checkpoints have `HEAD` as their sole parent, and the checkpoint commit times (`23:16:22`, `23:19:34`, `23:43:04`, `23:49:17` local) match the segment's stamps to the second.

No destructive git command ran in this worktree; nothing was restored from a checkpoint; the shared stash was not touched. `docs/intake/{chats,images,notes}` in this worktree each hold only `.gitkeep`, and `git status --porcelain --ignored` lists no ignored path outside `.control-beacons/`, `node_modules/`, the three `packages/*/dist/` outputs, `.tsbuildinfo` files, and `.DS_Store`; there is no `.claude/settings.local.json` here, and the tracked `.claude/settings.json` is byte-identical to `main`. `package.json`, `docs/decisions/`, and `packages/kernel/` are byte-identical to `main`; `package-lock.json` changes only the two component version numbers and the skeleton's compiler dependency pin. No `docs/observations/` directory exists in this worktree.

## The order's own actors, inventoried

`docs/final-reviews/README.md` requires, before a pass, every executor-role completion actor, each completed verifier's report header beside its control actor, and this reviewer's own intended completion actor, each checked against the declared role minimum, with the projection's drift confirmed. The declaration is `executor xhigh+; verifier xhigh+; reviewer any`.

| Role     | Event                                            | Actor recorded in the control log                                                                                          | Report header                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Declared minimum | Result                  |
| -------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ----------------------- |
| executor | `ImplementationReady`                            | `{"harness":"codex-cli","harnessVersion":"0.153.4","model":"gpt-6-astra","effort":"max","source":"operator-attested"}`      | there is no executor report; the evidence README states the operator selected the model and effort and that this is not effective readback; `0.153.4` is on the `codex-cli` observed `versions` list (observed 2026-09-05 during WO-029) and the persisted `model_reasoning_effort` selector records `max` for that version, so `max` was admissible at append                                                                                                                                       | `xhigh+`         | `max` exceeds the floor |
| verifier | `VerificationCompleted`, VER-001 pass            | `{"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}`  | exactly one column-one header in VER-001, parsed and compared with the event actor as JSON in this session: equal; neither carries `accountLabel`; the header is byte-identical in checkpoint `/4` and in the current file, so the post-verdict edit (adjudication 8) did not touch it; `2.1.263` was admissible only because the verifier recorded it (D5), and `max` is on the documented session selector values                                                                                    | `xhigh+`         | `max` exceeds the floor |
| reviewer | `FinalReviewCompleted`, this review's completion | `{"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-fable-5-1","effort":"max","source":"self-reported"}`   | the header above                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `any`            | satisfied               |

There is no `RepairCompleted` event; the sequence has no repair episode. The projection reads `Effort drift: none` and the JSON form's `effortDrift` is `[{"effort":"max"}]`; this review's `max` adds no new pair, so after completion the projection should read `Latest attestation: harness claude-code; version 2.1.263; model claude-fable-5-1; effort max; source self-reported; account not-applicable`, the same drift line, phase `closed`, a `finalReview` elapsed value beside the two existing values, and legal actions `release-close, next, activate`.

## Final evidence summary

### `npm test`, twice, first-hand, in this worktree

First run, on the verified tree after the dispatch transition and the index refresh, before any review artifact or correction existed, captured to a file without a pipe: `All matched files use Prettier code style!`, the GitHub body-profile suite, the shell suites (fixture temp-root, publication checker, backup-intake, resume including its Beacon line `every lifecycle transition matches phase/verdict/effort/time`, checkpoint, worktree including the three-worktree constellation now ending each row with `unauthenticated-legacy` and the new `issueBeaconSession` plus `emitControlBeacon` fixture before `finish_worktree`, release, and work-orders), `PASS index coverage: 229/229 product headings indexed` with both audience editions `CURRENT` (30 and 45 linked sections), `PASS docs/work-orders/README.md is current`, `tsc -b --force`, `# tests 245 / # pass 245 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0` with the nine WO-022 tests present as `ok 29` and `ok 203` through `ok 210`, the identity corpus `# tests 8 / # pass 8 / # fail 0`, and `Verified 4 current artifact evidence files in docs/evidence/WO-022/artifact-identity; original semantic hashes and frozen oracle unchanged.`, exit 0. The counts equal the executor's and the verifier's recorded counts exactly. Second run, after this review's dated note and the three package artifacts were in place and before the result was recorded, with the same result; the tree it judged differs from the committed series only by the lifecycle's own effects (the appended completion event, the control projection, and the regenerated index), which the publisher's committed surface check and the index check judge again on the committed tree.

### Static, surface, and structural checks

`git diff --check` exits 0. `npm run release -- check-surfaces` reports `PASS release-block: observed v0.11.0; expected v0.11.0 (work-order target v0.11.0; latest published v0.10.1)`, `PASS component-version @dotln/compiler: src changed; observed 0.4.0; previous v0.10.1 0.3.0`, `PASS component-version @dotln/kernel: src unchanged; observed 0.2.1`, `PASS component-version @dotln/skeleton: src changed; observed 0.10.0; previous v0.10.1 0.9.0`, and `PASS github-body-profile` over this package's `PR.md` and `RELEASE-NOTES.md`. `git diff --stat main -- packages/kernel` is empty. A 64-hex scan across the diff's added lines and every new file outside the artifact-identity receipts returned only the two publication source locks. A term screen for tracker products, internal hostnames, gateways, and employer wording across every changed and new file returned only pre-existing generic boundary prose (the clean-room floor sentences in 03, 04, 07, 09, the planning map, and the roles document) and no new URL other than the public GitHub Releases page. A link sweep over the 21 changed Markdown files resolved 602 relative links and anchors; the only misses were this then-unwritten report, which the regenerated index links as pending, and three Greek-headed anchors that the sweep's ASCII slugifier cannot compute and that `check-publication.mjs` verifies. `npx prettier --check` passes on the one file this review corrected.

### Independent probes against the deliverable

1. **The shipped sweep against this worktree's real public Beacon.** A script in the session temporary directory imported the built `beacon-observe.js`, `beacon-perception.js`, `control-beacon-fs.mjs`, and `control-codebook.mjs`, built a `beacon-perception-v1` profile with one mount on `.control-beacons/public/` and the single address `controlBeaconAddress("WO-022")`, and an envelope allowing `observe.beacons.public` with one `beaconSweeps` unit. `projectBeaconSparseTwin` returned `[]` with `senses: []`, `[]` with no environment, and exactly `[{"action":"sweep",…}]` when equipped and mounted. `observeBeaconSweep` with `beacon-sight` and `fine-spectrum` was authorized, persisted exactly `BeaconSweepRequested`, `CommandPersisted`, `BeaconObserved`, `CommandResult` to a temporary log, and returned one observation whose keys are exactly `address`, `decoded`, `fineSpectrum`, `mtimeMs`, `mtimeNs`, `provenanceCheck`, `size`: size `72341`, decoded `{codebookVersion: 2, phase: "final-review", latestVerdict: "pass", effort: "max", provenance: "host-projected"}`, `provenanceCheck: "unauthenticated-legacy"`, `fineSpectrum: "sensed"`, `mtimeMs` `2026-09-06T03:49:17.375Z` (the dispatch's `recordedAt`), and `mtimeNs` equal to the host's own `lstat`. A direct `decodeSignalSize` of the host's size agreed. The log does not contain the physical directory path; `renderBeaconPerception` produced two lines; `replayBeaconSweep` reproduced the decisions. With `beacon-sight` alone, `mtimeMs % 1000` was `0`, `mtimeNs` was absent, and `fineSpectrum` was `not-sensed`. Nothing under the repository was written.
2. **The residue, recomputed independently.** `createBeaconKey` into a canonical temporary directory outside every checkout returned epoch `0`; `openBeaconKey().sign` for `final-review`, `pass`, `max`, `host-projected` at WO-022's address produced an authenticator equal to the first two big-endian bytes of `createHmac("sha256", key)` over `dotln-beacon-v3`, NUL, the address, NUL, the decimal v2 ordinal `250` (derived here as `(encodeControlBeacon(v2) − 8192) / 64 / 4`), NUL, `0`; `check` returned `residue-matched`, an authenticator incremented by one `forged-provenance`, and epoch `9` `unverifiable-provenance`. `encodeV3Beacon` of the signed state decoded back to the same fields through both `decodeV3BeaconSize` and the generic `decodeSignalSize` (which selected version 3). `createBeaconKey` at a path inside this repository threw with `outside` in its message and created no file. A first attempt through the sandbox's symlinked temporary path was refused by the helper's canonical-path rule before any file was created, which is the documented strictness rather than a defect.
3. **The verifier's header and the segment.** The single column-one header in VER-001 parses as JSON equal to the `VerificationCompleted` actor; the five `recordedAt` values strictly increase; the tag snapshot in the regenerated index lists `v0.10.1` with object `ca5d3fab…`, whose commit is `HEAD`.
4. **The gitmoji catalog** fetched from `gitmoji.dev/api/gitmojis` lists `:passport_control:` as "Work on code related to authorization, roles and permissions", `:sparkles:` as "Introduce new features", `:lock:` as "Fix security or privacy issues", and `:safety_vest:` as "Add or update code related to validation".

### Reading

The complete diff (44 tracked files) and all 20 new files were read in full: the compiler support definitions and loadout; the environment profile and declaration; the perception gate, refusal, whitelist, validation, and renderer; the mounted reader; the reactor's perception branch and observed-event validation; the v3 codebook, key helpers, and sparse writer; the control Beacon host changes (0111 provisioning, session mount, keyed emission, disposal, legacy decoder split, phase rank and group counts admitting v3, the constellation's provenance column); the scenario's injectable verifier and driver guard; the mounted verifier; the scripts (key selection from the command-scoped environment variable, the agent constellation's renderer swap, the evidence edition relocation, the fixture module list, the unreadable-directory skip, the teardown restorer); the nine WO-022 tests and every modified WO-021, WO-016, and WO-029 test; the executor evidence, host probe, and focused transcript; VER-001; and every product, planning, publication, README, ledger, and work-order hunk. The work order, its ten criteria, evidence gate, write-back duty, and non-goals were read against each of those.

## Acceptance criteria

Each criterion was reproduced independently by VER-001 (whose reproductions are quoted in the PR body) and re-read here against the code, the tests, and this review's own probes.

1. **SUPPORT INACTIVE without the grant; active with it; declared cost.** Met. `compileBeaconSenses` returns the ordinary `SUPPORT INACTIVE` diagnostic from `compileLoadout` with `missingCapabilities: ["beacons.individual.metadata"]` and a `provide-capability` correction; Composition without a group mount names the group capability alone; with grants the supplied envelope is preserved and `supportCosts` sums one line per equipped sense with `promptTokens: 0`. Equipping without Beacon Sight is `ACTIVE INACTIVE` because the sweep active requires it.
2. **Sparse twin affordance.** Met. `projectBeaconSparseTwin` feeds the same `BeaconSweepRequested` payload through the same reactor decision as the live sweep, so the affordance and the guard cannot disagree by construction; probe 1 saw zero actions unequipped and unmounted and exactly one equipped.
3. **Guard refusal, event, zero reads.** Met. `observeBeaconSweep` appends the request, reads the folded authorization, appends `CommandRefused` and returns before `readMountedBeacons` is ever called; the test's throwing read adapter counted zero invocations across nine refusal shapes; the refusal payload carries `authorityEnvelopeId`. For an envelope lacking the effect the refusal is the kernel's, whose trace names the input class while the event names the envelope id; the new perception refusal names `authorityEnvelope:<id>` in its trace directly. The durable evidence names the envelope in both paths.
4. **`BeaconObserved` is exactly the codebook fields.** Met. `projectSenseObservation` is a positive whitelist; `validatePerception` rejects any extra key, a `mtimeNs` or sub-second `mtimeMs` without Fine Spectrum, non-numeric shapes, an address set that differs from the mounted declaration, and a decoded state that disagrees with the size; the reactor runs the same validation on replay. The test exercises garbage content, a real extended attribute, an unrelated file inside the mount, and mode `0000`, and asserts the absence of the marker, the extra name, the attribute name, the path, the capability, and the key from the persisted log.
5. **Mounted verifier completes the skeleton, blinded.** Met. `MountedBeaconVerifier` builds its capsule from candidate paths, the fixture's inventory fields, and the sensed perception, then detaches it with `JSON.parse(JSON.stringify(…))`; the test asserts positively that the host log contains the narrative and that the capsule's keys are exactly `candidatePaths`, `episodeId`, `inventory`, `perception` with neither the narrative nor the mount path; `finishScenario` reaches `verified: true` and replays identically.
6. **Search-only directory; forged provenance; legacy labels.** Met. `issueBeaconSession` creates the directory at `0111` and `emitControlBeacon` restores `0111` in `finally`; `readdirSync` fails `EACCES` while a known-name `lstat` succeeds; a current-epoch residue off by one decodes as a well-formed v3 record whose check is `forged-provenance`; v1 and v2 decode `unauthenticated-legacy` with their fields unchanged (probe 1 saw the live v2 so labelled).
7. **Fine Spectrum and Composition gate their own fields.** Met. Without Fine Spectrum the observation has no `mtimeNs` and a whole-second `mtimeMs`; without Composition `groups` is `not-sensed` and the reader skips the group mount entirely (the test proves it by making the group directory unreadable); with Composition an expected but missing group file is an `absent` observation. `renderBeaconPerception` emits one, two, or three lines.
8. **Replay, rotation, unknown epoch, exhaustion.** Met. The residue is a pure function of the record, key, and epoch; the canonical control JSONL re-derives an identical size and mtime through the production emitter at a new inode; rotation increments the epoch, leaves only `hostkey` in the key directory, and makes earlier Beacons `unverifiable-provenance`; a never-issued epoch is `unverifiable-provenance` under both keys; rotation at 255 throws `epoch exhausted` with the key file and the published Beacon unchanged.
9. **Injectivity, boundaries, maximum file.** Met. The product `8 × 3 × 6 × 2 × 256 × 65536 = 4,831,838,208`, `MAX_V3_CODE = 4 × product − 1 = 19,327,352,831`, and `MAX_V3_LOGICAL_BYTES = framedSize(MAX_V3_CODE) = 1,236,950,589,434` are recomputed by the test from the cardinalities and, in VER-001, from the normative text without importing the constants. The injectivity argument in 02 is sound: the check residue is strictly below the stride, so Euclidean division recovers the code, and successive quotient/remainder pairs at radices 65,536, 256, 2, 6, 3, and 8 recover bounded digits; the claim is correctly scoped as an algebraic proof plus 589,824 representative round trips. The maximum-size fixture writes the full declared maximum into at most eight 512-byte blocks with holes at three offsets and the exact mtime preserved through atomic rename, under a `Buffer.alloc` witness; every unsupported premise refuses before any file exists.
10. **Gate green, no dependency, no key committed.** Met. §Final evidence summary; `package.json` is byte-identical to `main`; the key helpers refuse any path inside a Git checkout and the tests create their keys in canonical temporary directories and remove them.

## Evidence gate

Present, in the committed tests' TAP diagnostics (`docs/evidence/WO-022/focused-tests.txt`) and reproduced in this review's first-hand gate run: the compile-fixture output with its `SUPPORT INACTIVE` message and correction; the affordance capture (`unequipped actions=[]; equipped actions=[{"action":"sweep",…}]`); the refusal transcript with nine cases and zero reads; the blinding assertion output; the forged-provenance transcript; the directory permission transcript (`listing=EACCES, known-name lstat=allowed`); and the replay verification output. The v3 evidence records the field cardinalities and maxima, the numeric and filesystem limits, maximum logical bytes and allocated blocks (`8/8` on this host), hole preservation through rename, malformed-boundary results, epoch exhaustion, and the unsupported-host no-file refusal. The activation preflight is satisfied by `docs/evidence/WO-022/host-probe.json`, a fresh observation of the sparse premise, the read-denied and search-only behaviors, the name and symlink length boundaries, the extended-attribute exposure, and Node's microsecond `utimes` truncation with the centered correction the writer implements.

## Write-back duty

All discharged, and consistent with the code. 04 §RPG view gains the three senses as the first helmet supports and §Agent projection gains `projectBeaconSparseTwin` and `renderBeaconPerception`. 03 §Composition system gains perception as the step-2 capability example, §Candidate isolated execution environments gains `beacon-perception-v1` beside `fixture-inspection-v1`, and the status line and the transport paragraph read `v0.11.0`. 02 gains the sense rows with capabilities and measured costs, the normative v3 block (equality-tested by `senses-v3.test.ts`), the field formula and injectivity argument, the preimage, the label semantics stated as a weak signal rather than authorship, the sparse-required content contract, the group paragraph's tag-3 clarification, and the forward-migration paragraph. 09 §Privacy and minimization gains the 0111 behavior, the note that content denial hides neither existence nor mtime, the mount as the boundary, and the same-user limit. 06, 07, 10, 13, the planning map, the publication index (the new v3 row) with both refreshed source locks, the root README release block, both package READMEs, and the work order's completed activation header are updated. The ledger's 2026-09-06 section carries the three required entries: adopted (a sense as a compiled, authorized, mounted capability), recovered (read denial does not hide metadata), and preserved (separate OS-user isolation as the only stronger local boundary), plus the v3 adoption and the implementation choices.

## Ideation breakout receipt

None. The order had no `ideation:` breakout; no receipt exists under `docs/evidence/WO-022/`, and the dispatcher's generic mention of a receipt has nothing to resolve to. The work order's own provenance is a direct-filed operator draft: its header records the nomination (the operator's second question in the 2026-09-02 beacon planning session), the direct-draft filing instruction, the compaction-safety copy in main's ignored intake, and that the clean-room screen found no stop condition; the ledger's 2026-09-02 release-surfaces section records that WO-016 through WO-022 were supplied by the operator as complete public drafts with direct filing instructions. The file entered the repository at `76486da` (2026-09-02) and was last changed at `2119345` (the WO-021 merge) before this order's executor completed its activation header. The executor's ledger section states that no intake was copied or mined, and the term screen above is consistent with that statement.

## Verification-sequence soundness

The sequence is `WorkOrderActivated` → `ImplementationReady` → `VerificationRequested` → `VerificationCompleted (pass)` → `FinalReviewRequested`, with no repair episode. VER-001 is WO-022's first and only independent verification; it read the work order, the executor's evidence as claims, and the full subject; it reproduced every criterion by an independent method rather than by re-running the executor's fixtures alone; it disclosed the instruments it shared with the subject and the one line it added outside the subject; and its verdict is consistent with everything this review observed. One qualification, adjudication 8, concerns the report's bytes after its verdict rather than its findings.

## Adjudications

### 1. WO-029's evidence receipts lost their gate (VER-001 O1)

Confirmed: `scripts/artifact-identity-evidence.mjs` now writes and checks `docs/evidence/WO-022/artifact-identity/`; `docs/evidence/WO-029/{audit.json, negative-transcripts.json, semantic-hash-inventory.json, scenario.jsonl}` are referenced by no code, while `baseline.json` remains the pinned source of the frozen oracle and every semantic hash and the compiler test pins every historical identity field. Nothing proved is now unproved; a hand edit to the four historical receipts would go undetected. The execution guide's prescription for exactly this situation is a dated migration note where a reader will hit it, never a back-fill, and `docs/evidence/WO-029/README.md` line 65 was the sentence a reader would hit. Applied (§Corrections applied by this review). A hash pin for the historical files is a patch candidate.

### 2. The agent constellation text lost its age and group rows (VER-001 O2)

Confirmed and intended. Age is derived from `sweptAt`, not a codebook field, and the group count is a Composition channel; `renderBeaconPerception` is the criterion-4 projection. The reactor still computes `age` into replayed state and the human `worktree constellation` retains both rows and gains a provenance-check column. Recorded in the release notes as a changed output shape.

### 3. `snapshotBeacons` skips directories without owner read (VER-001 O3)

Confirmed; the 0111 design requires it. The directory's own mtime and ctime still change on atomic-rename publication, so the residual blind spot of that test helper is an in-place rewrite of an existing inode. Bounded; recorded.

### 4. The executor edited the scope-authority document (VER-001 O4)

Confirmed: `git diff` on the work order shows only the title and the Release classification paragraph. The execution guide's Discipline section makes release assignment opt-out and instructs the executor to complete a missing activation target and record the base and classification; the same dated completion is recorded in 06. The objective, scope discipline, criteria, evidence gate, write-back duty, and non-goals are byte-identical to activation. Authorized; recorded so the touch is visible.

### 5. A provenance sentence in the work order reads as a forward reference (VER-001 O5)

Confirmed: "The operator confirmed that direction during VER-001 repair" was last changed in the WO-021 merge, before this order was activated, and describes that order's repair. It is operator-authored text in the scope authority; neither the verifier nor this review edits it. Recorded so a reader does not look for a WO-022 repair that never happened.

### 6. The restricted directory is owner-writable during emission (VER-001 O6)

Confirmed in `emitControlBeacon`: `chmod 0700`, write, `finally chmod 0111`. 09 already places same-user processes outside the boundary and names separate OS-user isolation as the stronger option; this is inside the declared limits. Recorded so it is not later read as an undisclosed gap.

### 7. The verifier's disclosures (VER-001 D1–D5)

D1 and D3 (the instrument published the dispatch; the subject is the tooling) are the same conditions this review met and compensated for (§Self-referential instruments). D2 (the first gate run failed on the stale index the verifier's own dispatch produced) is the guide's expected refresh; this review refreshed the index at dispatch before its first run and will refresh it again after its result. D4 (the artifact-evidence check verifies the new edition, not WO-029's receipts) is adjudication 1. D5 (the one-line `versions` entry for `claude-code 2.1.263` in `docs/discovery/environment.json`) was weighed as the verifier asked: the datum is first-hand (`claude --version` prints `2.1.263 (Claude Code)` in this session too), the shape is the file's own precedent, the addition sits inside the probe's recorded scope, `sessionEffortSelector` and `effectiveEffortReadback` are unchanged, and the refusal it answered named that corrective explicitly; the alternative, attesting `--effort unknown`, is refused by the declared `verifier xhigh+` floor and would have misstated the effort actually run. It rides in this PR as the single verifier-authored line, is committed with the review evidence rather than the implementation, and the generic gap it exposes is named below as a planning candidate.

### 8. VER-001 was edited after its verdict was recorded (this review)

The verdict checkpoint `refs/dotln/checkpoint/WO-022/4`, minted at `23:43:04` local immediately before `VerificationCompleted` was appended, holds a 393-line VER-001. The file's modification time is `23:44:25`, and its current 394-line bytes equal checkpoint `/5`. `git diff` between the two checkpoints shows exactly three hunks: the verdict paragraph's "four disclosures" became "five disclosures", which matches the report's own five-item disclosure list; the subject-state bullet gained "At dispatch," and one sentence stating that the report, the index refresh, and the discovery line are the verification's own additions rather than subject; and the Actor paragraph was reflowed without a word changing. The `**Actor attestation:**` header, the verdict, all ten criterion paragraphs, the evidence gate, the write-back duty, the six observations, the five disclosures, and the not-verified list are byte-identical. Applying today's standard, which existed when the report was written: verification artifacts are immutable and the report necessarily exists before its event, so a change after the completion command checked it is a process deviation by the verifier. It does not alter any claim, finding, or attestation; the pre-edit bytes are preserved in a local checkpoint ref and described here; and the edit made the report consistent with itself. Adjudicated non-blocking and recorded. Because nothing in the current lifecycle detects such an edit except a reviewer's checkpoint comparison, a completion-time digest of the report in the control event, or a refusal to proceed when the report's bytes have changed since its verdict, is a planning candidate.

### 9. The constellation CLI was not run against this repository in this review (this review)

`worktree constellation --agent` requires a host-request file and appends to `docs/observations/`, which the guide classifies as deliberate evidence rather than a disposable cache; creating a new observation log in the subject during final review would add an artifact the verifier never saw. The CLI path is covered by `control-beacon-cli.test.ts` with the new request shape, and probe 1 drove the same built modules against this worktree's real public directory. Recorded as a limit of this review's evidence, not a finding against the deliverable.

### 10. Kernel, schema, and contract axes (this review)

`packages/kernel/` is byte-identical to `main`, so the "kernel changes only where the guard needs an Observe branch and only if WO-021 did not already choose the `observe.*` namespace" clause resolves to no change, as the ledger records; the perception gate is layered in the skeleton reactor after the kernel's `authorize`. The event-envelope schema stays `1`, the compiled-program contract stays `1`, and the artifact identity keeps every semantic hash while `compilerPackageVersion` advances to `0.4.0`, which 10 records.

### 11. The attribution rule (this review)

The reviewed commits carry plain subjects and no attribution trailer, and the PR body carries no session link, per the operator's standing rule (`~/.claude/CLAUDE.md`, "Do not add AI attribution to commits or pull requests") and the repository's hand-authored history; the harness's per-session attribution reminder conflicts with that rule and the operator's rule wins, as the WO-017, WO-018, WO-020, WO-023, WO-028, WO-030, and WO-031 reviews recorded.

### Candidates considered and not sustained

- **Editing the work order's provenance sentence (adjudication 5)**: the scope authority is operator-authored and direct-filed; a reviewer's wording change there is not a handoff correction.
- **Failing the review over the post-verdict edit (adjudication 8)**: the findings, header, and verdict are unchanged and the prior bytes are preserved; a fresh verification would re-derive the same report for a count correction.
- **Splitting the artifact-evidence relocation into its own commit**: compiler `0.4.0` requires the new edition and the compiler test pins the version, so the relocation cannot be reviewed or gated apart from the version advance.
- **Treating `beaconSessionMount`'s physical path as a disclosure**: it is a trusted-host API returning the host's own mount record; the briefing remains the only textual disclosure, and the tests prove the path never reaches a log or capsule.

## Corrections applied by this review

One correction, in the documentation lane `docs/final-reviews/README.md` grants, applied by exact string replacement with a script that refused unless the old string occurred exactly once, proven contained by `git diff` against `HEAD` (10 insertions and 3 deletions in `docs/evidence/WO-029/README.md`), and followed by the checks it could affect: `npx prettier --check` on the file, the full `npm test`, and `git diff --check`, all clean. It is committed with the implementation because it documents the relocation that implementation introduced.

1. **`docs/evidence/WO-029/README.md`:** the sentence stating that the four generated files "are byte-checked by the evidence helper" now says they were byte-checked at that edition, followed by a dated migration note (2026-09-06, WO-022) that the helper writes and checks the current edition under `docs/evidence/WO-022/artifact-identity/`, that the four WO-029 files are retained historical bytes no longer regenerated or byte-checked, and that `baseline.json` remains the pinned source of the frozen oracle and every semantic hash (adjudication 1).

The three files this review adds, this report, `PR.md`, and `RELEASE-NOTES.md`, are artifacts the process requires of a passing review, not corrections. Every other deliverable path is byte-identical to checkpoint 5.

## Findings

**None blocking.** No acceptance criterion is unmet, no guard was lost, and no behavioral or evidence defect was found in the deliverable that any criterion covers. The items below are recorded and not scored.

1. **VER-001 changed after its verdict** (adjudication 8): process deviation, prior bytes preserved in checkpoint `/4`; lifecycle guard candidate.
2. **The four WO-029 receipts are no longer byte-checked** (adjudication 1): dated note applied; hash pin is a patch candidate.
3. **Effort attestation refuses on a harness patch newer than the recorded discovery versions** (adjudication 7, VER-001 D5): planning candidate.
4. **The restricted directory is owner-writable during the emission window** (adjudication 6): documented limit.
5. **The agent constellation text lost its age and group rows** (adjudication 2): intended.
6. **`snapshotBeacons` cannot see an in-place rewrite inside a 0111 directory** (adjudication 3): bounded test-helper limit.
7. **The work order's provenance sentence describes WO-021's repair** (adjudication 5): wording in operator-authored scope authority.
8. **The constellation CLI produced no new observation log on this repository in this review** (adjudication 9): evidence limit.
9. **The end-to-end lifecycle warning on a genuinely dense volume with a configured key was not observed on real dense storage** (VER-001 not-verified): the refusal path is exercised in fixtures.
10. **`CLAUDE_EFFORT` remains an unrecorded readback candidate** (WO-023 FINAL-001 adjudication 9), so Claude Code attestations stay `self-reported`.

## Remaining deviations and open questions

None requires a change to the subject.

- **Publish step and the sandbox.** `gh` cannot read its configuration under this sandbox (`~/.config/gh` is denied), the same limit every reviewed order since WO-006 has met, while read-only `git ls-remote` to `origin` succeeds. The reviewed series is committed here, and the helper is then invoked once from this worktree inside the sandbox so that its committed-tree surface check, body validation, and notes parse run against the exact commit; if it refuses at the `gh` preflight it pushes nothing and the operator runs the same command from this worktree outside the sandbox.
- **Release close is a tagging release.** After merge, and only under a fresh `resume: release close`, the operator runs the exact absolute command the publisher prints: change directory into the main checkout and run this worktree's `scripts/release.mjs close WO-022 --publish` with node, outside the sandbox and where `gh` is authenticated. Because `v0.11.0` is strictly above `v0.10.1`, the reviewed subject helpers finish this worktree through their own classifier and the new restricted-directory disposal, the closed-state gates in the subject and in `origin/main`, fast-forward main, run `npm ci` and the evidence gate, assemble the five-section edition from these notes, create and push the annotated `v0.11.0` tag, and create the matching GitHub Release. If Release creation fails after the tag push, the rerun is `npm run release -- close WO-022 --publish` from updated main.
- **Attribution:** adjudication 11.
- **Ledger duty:** discharged by the executor's dated section; this review adds no ledger entry because every open item above is either already in VER-001 or is a candidate this report names for a patch or the next planning pass.
- **Intermediate commits are not individually gated.** The first commit's tree carries the pre-existing generated index over changed work-order headers, so `index --check` would refuse it in isolation; only the final tree is what `npm test`, `check-surfaces --committed`, and the publisher judge.

## Proposed PR

- **Title:** `:passport_control: WO-022: Senses as compiled, authorized, mounted perception with weak keyed v3 Beacon provenance; v0.11.0`. The shortcode is the catalog's "Work on code related to authorization, roles and permissions" entry (probe 4), chosen because the order's main purpose is making perception an authorized, role-equipped, host-mounted permission rather than a generic feature; `:sparkles:` would name the novelty and `:lock:` would overstate the residue. The title names the change and its version and leaves the inventory and validation to the body, per 08 §PRs and commits.
- **Body:** `docs/final-reviews/WO-022/PR.md`, committed alongside this report and `RELEASE-NOTES.md`.

## Commit series

Two commits, each with a plain subject and an explanatory body, in this order, followed by a check that `git diff HEAD` is empty and that the final tree equals the post-result working tree:

1. **Implementation:** the compiler (`senses.ts`, `index.ts`, `artifact-identity.ts`, `package.json`, `README.md`, and its two tests); the skeleton (`beacon-perception.ts`, `beacon-perception-fs.ts`, `beacon-verifier.ts`, `execution-environment.ts`, `beacon-provenance.mjs`, `beacon-v3-codebook.mjs`, `beacon-v3-fs.mjs`, `beacon-observe.ts`, `control-beacon-fs.mjs`, `control-beacon.ts`, `control-codebook.mjs`, `reactor.ts`, `scenario.ts`, `worker-protocol.ts`, `package.json`, `README.md`, and its five tests); `package-lock.json`; the scripts (`artifact-identity-evidence.mjs`, `lib/beacon-observe.mjs`, `lib/beacons.mjs`, `test-beacon-fixture.mjs`, `test-control-beacons.mjs`, `test-worktree.sh`, `worktree.mjs`); `.prettierignore`; the root README release block; `docs/product/02`, `03`, `04`, `06`, `07`, `09`, `10`, `13`; the planning map; the publication index and both edition locks; the ledger's dated section; the work order's completed activation header; the executor evidence under `docs/evidence/WO-022/` (`README.md`, `focused-tests.txt`, `host-probe.json`, and the four `artifact-identity/` files); and this review's `docs/evidence/WO-029/README.md` note.
2. **Independent review evidence and closed control state:** `docs/verifications/WO-022/VER-001.md`, this report, `PR.md`, `RELEASE-NOTES.md`, the verifier's `docs/discovery/environment.json` line, `docs/control/orders/WO-022.jsonl`, `docs/control/current.md`, and the regenerated `docs/work-orders/README.md`.

## Ready to merge: handoff

Closeout from here, per the playbook: `final-review-result pass` is recorded before the reviewed commits (the committed surface check requires the closed control state, and it makes the index's check mark true); the index is regenerated after the result and checked; the reviewed state is committed as the series above. The publisher is then run from this worktree:

```bash
npm run worktree -- publish WO-022 --title ':passport_control: WO-022: Senses as compiled, authorized, mounted perception with weak keyed v3 Beacon provenance; v0.11.0' --body-file docs/final-reviews/WO-022/PR.md
```

The helper runs `check-surfaces --committed WO-022` on the committed tree, validates `RELEASE-NOTES.md` and the body against the profile, preflights `gh`, pushes only `wo-022`, opens the PR pinned to `github.com/DylanBWood/DotLn`, and prints the release-close handoff in the absolute subject-helper form. The operator retains merge authority. After the merge, and only under a fresh `resume: release close`, the operator runs that printed command with the main checkout as the working directory; the expected outcome is the annotated `v0.11.0` tag and its GitHub Release.

## Method

Everything above was re-established in this session inside the Claude Code sandbox: `npm test` twice, captured to files without a pipe; the read-only status projection and the transition; temporary-index trees and `git diff-tree -r` against all five checkpoints and checkpoint-to-checkpoint diffs; the segment read raw and compared as JSON with the verifier's header; the tag peel; the sweep probe against the real public Beacon, the residue recomputation, and the v3 round trip as a script written for this review in the session temporary directory; `check-surfaces` before and after the package artifacts existed; the notes parser and body profile called directly; the 64-hex scan, the term screen, and the link sweep as one-off scripts; a fetch of the public gitmoji catalog; and a full reading of the diff, the new files, the receipts, VER-001, and the gates' source. The one correction was applied with exact string replacement, checked with the pinned formatter, and diffed against `HEAD`. No other file outside `docs/final-reviews/WO-022/` was changed by this review; the index regeneration and the control projection are the lifecycle's own effects.
