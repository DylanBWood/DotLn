# Entropy Reducer refutation 001

Review episode `ep_entropy_reducer_20260904_001` examined the frozen pre-repair
WO-023 subject at semantic hash `fnv1a64:b81d0609ee2e9fed`. A fresh Claude Code
2.1.260 episode using `claude-fable-5-1` at `max` received only each finding ID
and its command or inspection steps. It did not receive the first review's
conclusion, severity, surface, evidence references, proposals, or summary.

The measured denominator was 1 and the inspection denominator was 6. The
compiled rule selected the measured finding and all six inspection findings.
All seven survived; none was refuted, blocked, or left unselected. The complete
review output, attempts, attestation, model usage, baseline hashes, confinement
inventory, and derived report are preserved in `REVIEW-001.json` and
`REFUTATION-001.json`.

| Finding | Refutation result | Repair in the implementation subject                                                                                                                                                               |
| ------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ER-001  | survived          | Replaced stale WO-009/WO-011/pattern-workshop version citations with activation-time roadmap references; retained the valid scenario re-export citation.                                           |
| ER-002  | survived          | Added the first-live-use architecture record and the required reviewer/residue/Sustain lineage entry.                                                                                              |
| ER-003  | survived          | Corrected published roadmap statuses and kept active WO-023 at `planned` until independent lifecycle verification.                                                                                 |
| ER-004  | survived          | Normalized repeated separators and case before intake-scope comparison; added negative cases.                                                                                                      |
| ER-005  | survived          | Removed the non-standard schema keyword, declared host-only relational checks, shared the safe-ID pattern, aligned non-empty items and evidence/reproduction pairing, and added parity assertions. |
| ER-006  | survived          | Added exact compile inputs to the compiled reviewer result and run receipt; WO-029 now makes environment preimage and path sensitivity explicit.                                                   |
| ER-007  | survived          | Derived identity dispositions/invariants, role obligations, active semantics, and active constraints into the residue; narrowed filesystem-confinement language; added mutation tests.             |

With the same recorded compile inputs, the repaired subject compiles to
`fnv1a64:eefe230707d23735`. The change from the reviewed hash is expected and
explained: ER-005 changes the compiled output-contract and lens-brief schemas.
The historical review hash remains immutable evidence of what Fable examined.
Its exact compiled-program preimage is preserved in
`REVIEW-001-COMPILED-PROGRAM.json`, and the artifact test independently
recomputes the historical hash from those bytes. The repaired hash is pinned by
the same test for the subject handed to independent verification.

The reviewer's one `ProductSuggestionPacket` remains payload evidence only. It
was not filed because neither the reviewer nor this refutation episode had
proposal-filing authority. The harness reported a tiny internal Haiku helper
usage in both episodes (17 and 20 output tokens respectively); the returned
review and refutation content was generated by the pinned Fable actor, so no
model substitution is claimed.
