# WO-006 — Publication bootstrap (two contents, one staleness loop), v0.2.3

**Model:** any capable model.

**Release classification:** `v0.2.3` patch — documentation-publication
artifacts, an internal staleness proof, and dev-only formatting tooling, not
exported runtime capability. Reclassify before implementation if scope expands
beyond that boundary.

**Depends on:** the existing Node/npm toolchain; the publication proof runs
against reviewed blueprint sources, the formatting expansion adds only an exact
dev dependency, and the personal harness record is sanitized documentation
rather than repository-enforced configuration.

**Authorized expansions (2026-09-01):** During execution the operator directed
this work order to continue and explicitly expanded its scope thirteen times:
(1) synthesize the Malcolm Check ideation and correct the durable `ideation:`
dispatch contract; (2) add Prettier with sensible defaults and apply it where
formatting is meaningful across the repository; (3) align the personal Codex
sandbox/approval posture with the Claude setup, document current
enable/verify/undo procedures for every actual harness, and make the recurring
checkpoint warning actionable; (4) extend the Malcolm Check with observable
restraint accounting that separates selection disposition from guard, operator,
harness, and OS intervention without claiming hidden reasoning; and (5) explore
custom external rule-source imports, their mapping preview, alternative
implementation paths, and public versus organization-specific source classes.
(6) define a candidate Embodied Explorer role that learns bounded composite
movement skills in simulation; (7) make incremental progress on work-order
identity, eligibility, recommendation, and purpose/requirement navigation; (8)
nominate a future source-grounded Team Topologies research pass without adopting
book content from memory; and (9) make increasing the chance of operator flow
state the project mission, with dependable workflow as a means rather than the
end; (10) preserve continuity-critical interaction as a binary quench candidate
without reducing forceful messages to tone markers or pinning a fixed heat-rule
list; (11) derive a bounded Flow Steward obstacle-removal role from three named
public essays without importing their human stereotypes or management authority;
and (12) generalize the quench discussion into temporal and relational response
interpretation, while preserving RxJS `expand()` as a bounded generated-lowering
hypothesis; and (13) clarify the brain/body/hands, harness, orchestration,
transport, execution-environment, and human-attention boundaries; preserve
LangGraph plus container/VM isolation as bounded future probes; and adopt a
foundation-first strategy that accommodates external innovation through
primitives and composition before adding special cases. These additions remain
documentation, process guidance, and dev tooling; they add no exported runtime
capability and do not change the patch classification.

**Required VER-001 correction (not a scope expansion):** The verifier reported
that Claude's persistent `defaultMode` still caused fresh sessions to start in
Manual even though the operator had selected and disclosed Accept Edits. The
verifier correctly left personal settings untouched. The first repair pass then
retained and documented Manual, contrary to the stated repair outcome. This pass
must persist the complete intended posture—Accept Edits plus fail-closed
sandboxed-Bash auto-allow and the existing explicit denies—remove the eleven
accidental checkout-local Bash grants, and prove startup behavior in a fresh
session. The operator's mid-verification changes explain the moving initial
state; they do not excuse the repair agent's failure to resolve the reported
regression.

**Required repair-continuation correction (not a scope expansion):** The faulty
repair had already recorded `RepairCompleted`, leaving control in
`ready-to-verify` with a checkpoint that predated the operator's correction.
When the operator identifies an incomplete repair before a new verifier is
allocated, `resume: fix` must be able to reopen the preserved failure source,
and the corrected executor must record a new `repair-complete` checkpoint. This
path is legal only when that source remains; an initial implementation-ready
subject cannot manufacture repair authority.

**Operator deferral for the VER-002 repair (2026-09-01):** The operator directed
this repair not to inspect, mutate, or reverify personal Accept Edits or
checkout-local allow entries and will use automode for now. Any reconciliation
of the current personal posture, startup evidence, and the ADR/runbook claims
belongs to a separately activated dedicated work order. VER-003 must preserve
VER-002's factual disclosure but score none of those deferred axes in this
repair.

**Cites (read these sections):** 08-publication-compiler.md §Source model,
§Audience editions, §Outline first, §Authority and honesty rules, §Bootstrap
path (steps 1–4 are the original scope); 00-vision.md §Mission and
§Inspirational sources; 07-execution-guide.md; 01-principles.md Principles 2, 3,
4, 5, 6, 7, 9, 10, 11, 13, 14, 15, and 18; 04-interfaces.md §External
rule-source mapping preview; 05-pattern-library.md §Candidate — Embodied
Explorer, §Candidate — temporal interaction interpretation, §Candidate —
continuity-critical flow and binary quench, and §Candidate role — Flow Steward;
03-architecture.md §Runtime primitive catalogs and §Candidate — isolated
execution environments; 04-interfaces.md §Terminal first, console equal;
06-roadmap.md §Work-order navigation and identity and §Post-1.0 horizons;
10-ir-compatibility.md §Candidate — external rule-source import plans and
§Behavioral toolbox; `docs/planning/work-order-map.md`; ADR-0002 §Amendments;
ADR-0003; ADR-0004; ADR-0005.

**Original objective:** Run steps 1–4 of the publication bootstrap path and
nothing past them except the operator-authorized expansions recorded above: (1)
a minimal audience/status index over the existing blueprint sections; (2) the
base DotLn outline, an implementation-overlay template, and two sharply
different tables of contents from the same sources — an everyday AI-user book
and a software-engineer book; (3) one shared concept rendered in both voices
with identical claim links; (4) a deliberate source-claim change proving both
editions become stale. The key test is traceability, not prose quality.

**Scope discipline (one step at a time):**

- Headings and links are the claim graph — no `Claim` IR, no `PublicationPlan`
  type promotion (08 defers both until two editions prove the outline
  insufficient; an outline is allowed to be sufficient).
- Status labels come from 08's fixed vocabulary (vision / specified / planned /
  implemented / verified / blocked / deprecated) and must not blur.
- The staleness check uses the cheapest sufficient mechanism: a small script or
  a documented manual diff procedure — not a pipeline.
- Gitignored intake is never quoted or linked (clean-room rule; 08 §Source
  model).

**Deliverables:** `docs/publication/` (new directory): the audience/status
index, base outline, overlay template, the two tables of contents, and the
dual-voice sample; a map line in `docs/README.md`; the authorized ideation
write-backs and receipt; exact Prettier dependency, configuration, commands, ADR
amendment, and repository-wide supported-file formatting; the dated sanitized
AI-harness security runbook, its documentation-map and dispatch links, the
personal Claude/Codex setting alignment, ADR-0003, and the actionable checkpoint
warning plus bounded repair-reopen transition and fixture; the follow-up Malcolm
restraint-accounting write-backs; and the rule-import candidate write-backs; the
corrected Claude startup posture and ADR-0005; the Embodied Explorer candidate;
the provisional work-order map and navigation semantics; the unmined Team
Topologies research nomination; and the operator-flow mission propagated through
product and publication surfaces; the binary-quench, Flow Steward, and
temporal-interaction candidates; the bounded RxJS `expand()` lowering
hypothesis; and the brain/hands, orchestration, attention-interface,
isolated-execution, LangGraph, and foundation-first synthesis. The expansions
add no importer, connector, simulator, scheduler, coordination framework,
classifier, RxJS or LangGraph runtime dependency, container/VM runner, image,
environment provider, deployment default, or runtime schema.

**Acceptance criteria (all required)**

1. Every section of every current `docs/product/` document (currently 00–10)
   appears in the index with audience tags and a status label.
2. The two tables of contents differ sharply in sequence, vocabulary, and depth,
   cite only shared committed sources, and differ in no fact, limitation,
   permission, or evidence (08's audience rule).
3. The dual-voice sample explains one concept in both voices with identical
   claim links to blueprint headings.
4. The staleness loop is demonstrated: change one source claim, show both
   editions flag stale, revert; the demonstration is captured.
5. No publication artifact directly derives from, quotes, or links
   `docs/intake/`; the separately authorized ideation first becomes clean-room
   product source, which the current-heading index may then cover.
6. The ideation breakout is clean-room rewritten into the ledger, pattern
   catalog, audit guidance, and operator/model workflow docs, and the receipt
   gives the verifier the complete expanded subject.
7. Prettier is an exact dev dependency with documented exclusions for surfaces
   where formatting would corrupt raw, append-only, generated, hash-sensitive,
   or immutable evidence; every other tracked file type it supports is
   formatted, `format:check` passes, and the full test suite stays green.
8. The harness runbook inventories only actual current harnesses, distinguishes
   personal settings from repository enforcement, records the tested date and
   versions, gives enable/verify/undo steps for Claude and Codex without
   secrets, documents their command and Git-metadata asymmetry, and makes a
   failed recovery checkpoint understandable without advertising stale recovery
   data. Claude's effective baseline is persistent `acceptEdits`, sandbox
   enabled, sandboxed-Bash auto-allow enabled, fail-closed startup, unsandboxed
   fallback disabled, retained credential/transport denies, and zero
   checkout-local allow entries; targeted readback and a fresh-session startup
   check must both support that claim.
9. Malcolm evaluation counts only observable typed decisions and control/effect
   records, keeps selection disposition separate from enforcement/effect
   outcome, treats authority requests as nonterminal, requires attestation for
   OS attribution, uses `unknown` rather than inferring hidden reasoning, says
   `participated` absent causal evidence, and notes that end-to-end collection
   is not implemented.
10. External rule-source import remains a candidate compatibility-planning
    surface: it preserves source provenance, previews construct-level mapping
    fidelity and multiple implementation paths, separates suggestions from
    selection authority, qualifies public-rule scope, treats private relational
    semantics as observed rather than assumed, and makes no implemented-import
    or effortless-integration claim.
11. The Claude settings fault sequence is recorded without shifting the repair
    failure onto the operator: the operator introduced moving state and
    disclosed it; the verifier reported the unresolved startup regression and
    correctly did not mutate personal settings; the repair retained Manual and
    failed its assigned correction; ADR-0005 supersedes only the affected
    ADR-0004 decision.
12. Embodied Explorer remains a post-1.0 candidate with authoritative embodiment
    facts separated from its uncertain learned body/capability model and
    Identity; its senses, motor intents, supports, tool/scaffold promotion, and
    physical-safety boundary stay typed, bounded, evidence-based, and explicitly
    unresolved rather than becoming runtime claims.
13. Work-order navigation distinguishes workflow-legal next, eligible now, and
    recommended next; treats existing IDs as opaque stable references rather
    than a queue or family code; derives lifecycle claims from evidence; and
    pilots purpose, dependency, execution-profile, and affected-surface metadata
    without renumbering history or pinning scheduler IR.
14. Team Topologies appears only as a provenance-aware future research
    nomination. No book-derived pattern, counterpattern, taxonomy, architecture
    choice, citation, or schedule is claimed before an authorized
    source-grounded pass.
15. The durable mission and everyday publication route state that dependable
    workflow is a means to increase the chance that the operator remains in a
    genuine psychological flow state. They neither promise nor infer that state
    and do not let flow weaken authority, evidence, privacy, or recoverability.
16. Continuity-critical flow is a candidate interaction contract with binary
    `conducting | quenched` phase semantics. Applicable versioned heat
    conditions may be instantaneous or temporal and OR into that phase without a
    severity score; the recalled count of nine and typography markers are not
    canonical triggers, and recovery requires evidence without widening
    authority or claiming the operator's mood.
17. Flow Steward remains a bypassable, revocable support role that preserves
    operator ownership, removes only evidenced obstacles within existing
    authority, keeps authorized metawork off-thread, exposes its own cost, and
    cannot become an approval gate, global manager, surveillance layer,
    objective owner, guard remover, self-assigner, or self-verifier. Public
    source transfers and rejected stereotypes/psychological claims are explicit.
18. Temporal interaction interpretation covers both operator and system events,
    keeps reply attachment correctable and source-linked, makes recurrence
    counts/windows rule-specific, treats the _Outliers_ association as unmined,
    and does not promote inferred attachment, latent/unreported intent, or a
    psychological state to fact, authority, or objective retargeting. RxJS
    `expand()` remains a bounded implementation hypothesis under authoritative
    statechart/IR semantics, with termination, cycles, cancellation, ordering,
    concurrency, duplicate, resource, and teardown behavior left for an
    executable fixture.
19. `resume: fix` can reopen `ready-to-verify` only while a prior verification
    or final-review failure source remains, appends another `RepairRequested`
    against that same source, and requires a new `repair-complete` checkpoint.
    It refuses the initial implementation-ready state, allocates no verification
    report, and leaves the independent verifier as the legal next actor.
20. The final ideation batch keeps model/brain, tools/hands, harness/body,
    orchestration/host, transport, execution environment, attention interface,
    authority, and evidence distinct while preserving the familiar projection.
    LangGraph is only a conformance/lowering candidate; container and VM
    isolation is only an attested environment probe; worktrees are not described
    as hostile-code containment; and the foundation-first accommodation strategy
    adds no dependency, runner, image, provider, schema, deployment choice,
    WO-009 expansion, or claim about the operator's psychology.

**Evidence gate:** the captured staleness demonstration, index coverage check
(no untagged section), targeted sanitized harness-setting checks plus fresh
Claude startup-mode evidence and `claude doctor`, zero checkout-local Claude
allow entries, checkpoint degradation and repair-reopen fixtures, review of the
restraint-attribution categories and current implementation limit, work-order
map evidence audit, source/transfer-boundary review for the Flow Steward,
temporal/quench invariant review, clean `format:check`, and passing full test
suite, recorded in the result.

**Write-back duty:** the roadmap's v0.2.3 entry already points here; the
`docs/README.md` map line lands in the same change; ledger duty for any
transformed publication idea.

**Non-goals:** manager / IT-director / VP-development / CFO editions (08 gates
them on this loop working); rendering pipelines, sites, or printable output;
Claim IR; publishing any edition anywhere; installing or implementing LangGraph,
Docker, a container/VM runner, an environment provider, an image, a remote
worker, or a new orchestration/runtime schema.

## Ideation, tooling, and harness expansion receipt — 2026-09-01

- **Authority:** the operator explicitly authorized all four expansions and
  instructed the executor to continue the original publication work.
- **Raw intake batch:** local-only
  `docs/intake/notes/WO-006-expanded-ideation-2026-09-01.md`; it is neither
  quoted nor linked by a committed publication artifact.
- **Ledger additions:** “The Malcolm Check separates available moves from
  justified moves,” “`ideation:` dispatches the complete durable pipeline by
  default,” and “Restraint accounting separates selection-layer restraint from
  external intervention” in `docs/lineage/idea-ledger.md`.
- **Product/process surfaces:** candidate pattern in
  `docs/product/05-pattern-library.md`; derived audit guidance in
  `docs/product/09-audit-resilience-privacy.md`; dispatch semantics in
  `docs/product/07-execution-guide.md`, `docs/PLAYBOOK.md`, and `CLAUDE.md`;
  current heading coverage and refreshed edition locks in the publication
  index/projections.
- **Tooling surfaces:** exact Prettier dev dependency and lockfile entry,
  `.prettierrc.json`, `.prettierignore`, package scripts, the ADR-0002
  amendment, and the formatter-authored repository diff.
- **Harness security surfaces:** `docs/AI-HARNESS-SECURITY.md`, its
  `docs/README.md` map entry, the dispatch guidance in `docs/PLAYBOOK.md` and
  `docs/product/07-execution-guide.md`, and the actionable degradation message
  plus fixture assertions in the resume helper. The operator made and disclosed
  the initial Claude changes; this receipt does not treat that moving snapshot
  as the final intended mode. The later correction is recorded separately below
  and in ADR-0005. For Codex, the executor added an explicit human reviewer to
  the existing sandbox configuration and removed one obsolete, exact WO-002
  Git-stash auto-allow rule. No personal configuration contents enter the
  repository.
- **Decision/schema/runtime effects:** ADR-0002 records the dev-tool choice; no
  domain schema or runtime dependency changes; no executable Malcolm Check
  implementation. ADR-0003 records the personal harness safety boundary, known
  asymmetry, and rollback authority without turning those settings into product
  claims; ADR-0004's later supersession is recorded in ADR-0005. Restraint
  attribution remains a derived candidate view over existing record concepts; no
  telemetry schema or collector is pinned. At the time of this dated receipt,
  the resume change altered only degradation guidance, not transition or
  checkpoint semantics; the later required repair-continuation correction adds
  the bounded reopen path described by AC19. The publication checker remains
  original WO-006 scope.
- **Formatting exclusions:** gitignored intake; the append-only ledger and
  JSONL; settled decision bodies; work-order scope authorities; corpus
  fixtures/manifests; generated control/discovery projections; immutable
  verification, final-review, and release records; dependencies; build outputs;
  binaries; and unsupported file types. The active WO-006 authority remains in
  the formatted surface, and targeted ignore comments preserve established
  roadmap heading anchors while the rest of that current product source is
  formatted.
- **Unresolved ideation choices:** exact mask/personality rendering, trigger
  taxonomy, compiled `PatternDefinition` shape, graduation evidence, and
  safeguards against reflexive caution; the restraint-funnel schema, correlation
  key, collection boundary, denominators, and calibration corpus.
- **Required review:** verify clean-room rewriting, ledger append-only
  traceability, consistency with Principles 5 and 10 and existing composition
  precedence, the capture-only exception and resume authority, absence of
  accidental runtime claims, the dependency amendment and formatter exclusions,
  complete supported-file formatting, updated index coverage, sanitized and
  locally accurate harness instructions, no credentials or private identifiers,
  the one-invocation unsandboxing tradeoff, the new checkpoint-warning
  assertions, observable restraint accounting without chain-of-thought claims,
  and every original WO-006 acceptance item.

## Rule-import ideation breakout receipt — 2026-09-01

- **Authority:** the operator opened the complete `ideation:` pipeline for
  custom user rule-source imports and then explicitly pre-authorized resuming
  WO-006 repair after the breakout.
- **Raw intake batch:** local-only
  `docs/intake/notes/WO-006-custom-rule-imports-2026-09-01.md`; committed
  surfaces synthesize it without quoting or linking it as publication content.
- **Ledger addition:** “External rule imports are previewed compatibility plans,
  not blind ingestion” in `docs/lineage/idea-ledger.md`.
- **Product surfaces:** candidate import-plan semantics in
  `docs/product/10-ir-compatibility.md` and the side-by-side mapping preview in
  `docs/product/04-interfaces.md`; the publication index and both source locks
  were refreshed before repair evidence was recaptured.
- **Decision/schema/runtime effects:** no ADR is changed by this breakout, no
  connector or import schema is pinned, and no executable importer, live data
  access, sync, compliance evaluator, or runtime capability is added. No
  application, schema, artifact, or transformation version advances; the
  `v0.2.3` documentation/tooling patch classification remains unchanged.
- **Clean-room transformation:** the committed design generalizes the named
  storage example to source-specific relational rule stores and contains no
  organization identifier, private schema, credential, host, or proprietary API
  shape. Public rule catalogs are candidate exercises only after provenance,
  license, jurisdiction, effective date, official status, and updates are
  established.
- **Unresolved choices:** import-plan type shape, connector and discovery
  contract, rule/fact/temporal mapping taxonomy, conflicts and precedence,
  materialization versus delegated evaluation, refresh and bidirectional sync,
  source-revision invalidation, data movement, calibration corpus, and evidence
  thresholds for “seamless.”
- **Required review:** confirm source preservation, visible exact/adapted/lossy/
  unmapped/blocked/unverified states, alternative paths without auto-selection,
  read-only and minimized private-source discovery, honest public-rule scope, no
  accidental legal-compliance or implementation claim, current publication
  coverage/locks, and continued satisfaction of all prior WO-006 acceptance
  criteria and VER-001 repairs.

## Claude startup correction receipt — 2026-09-01

- **Authority:** this is completion of the settings defect already reported by
  `VER-001`, followed by the operator's explicit instruction to make the whole
  posture correct rather than merely restore one value. It is not a tenth scope
  expansion.
- **Accountability:** the operator changed Claude state at several points during
  verification and disclosed every issue, producing a moving snapshot. The
  verifier correctly reported the resulting mismatch and declined to mutate
  personal settings. The first repair agent was expected to reconcile that
  report but instead persisted/documented Manual and recorded repair complete.
  Responsibility for that unresolved repair defect lies with the repair pass,
  not with the verifier and not with the operator's disclosed intervention.
- **Correct effective posture:** fresh sessions start in Accept Edits;
  filesystem sandbox, sandboxed-Bash auto-allow, and fail-closed startup are on;
  unsandboxed fallback is off; credential-path and direct SSH/SCP/SFTP denies
  remain; the checkout-local permission allow list is empty.
- **Changed surfaces:** the authorized personal user and checkout-local settings
  were corrected without copying their private contents into the repository.
  ADR-0005 supersedes ADR-0004 Decision 4 only; the sanitized runbook and
  repository configuration log describe the effective result and rollback.
- **Required review:** compare targeted setting readbacks with ADR-0005, start a
  genuinely fresh Claude session without a command-line permission override,
  verify its displayed mode, and run `claude doctor`. Do not test the denies by
  reading a credential or attempting an external connection.
- **Executor evidence:** targeted readback matched all pinned values, retained
  fourteen deny entries, and found zero allow entries across the applicable
  user/local/worktree layers. A new unoverridden `claude` process displayed
  `accept edits on`; its `/sandbox` view selected sandboxed Bash with
  auto-allow. `claude doctor` exited zero with no invalid-setting warning; its
  unrelated Keychain-writability warning was left untouched.

## Embodied Explorer ideation breakout receipt — 2026-09-01

- **Authority:** the operator opened the complete `ideation:` pipeline for a
  role that explores its embodiment, composes rudimentary sensing and movement
  into larger skills, experiments near its purpose, and may propose tools or
  scaffolds that enhance its purpose.
- **Raw intake batch:** local-only
  `docs/intake/notes/WO-006-embodied-explorer-2026-09-01.md`; committed surfaces
  synthesize it without importing private or employer material.
- **Ledger/product surfaces:** the Embodied Explorer ledger entry, candidate
  pattern in `docs/product/05-pattern-library.md`, post-1.0 simulation fixture
  in `docs/product/06-roadmap.md`, and behavioral-toolbox horizon in
  `docs/product/10-ir-compatibility.md`.
- **Decision/schema/runtime effects:** no actor kind, role schema, simulator,
  controller, physical adapter, or capability claim is implemented or pinned.
  The candidate separates embodiment truth, learned capability belief, and
  Identity; created tools remain inactive until separately promoted.
- **Unresolved choices:** first body/world, observation boundary, control rate,
  reset and stop semantics, novelty objective, learning schedule, skill
  representation, compatibility/forgetting, tool boundary, and evidence
  curriculum.
- **Required review:** confirm bounded authority, independent evidence,
  simulation-first scope, explicit physical safety boundary, and the absence of
  claims that curiosity, repetition, fluent self-report, or code generation
  proves competence.

## Work-order navigation ideation breakout receipt — 2026-09-01

- **Authority:** the operator requested incremental progress toward seeing what
  is next, complete, or possible, and toward replacing number-range folklore
  with useful work-order purpose and requirement distinctions.
- **Raw intake batch:** local-only
  `docs/intake/notes/WO-006-work-order-navigation-2026-09-01.md`.
- **Ledger/product surfaces:** the work-order identity ledger entry,
  `docs/product/06-roadmap.md` candidate semantics,
  `docs/planning/work-order-map.md`, execution-guide and playbook routing, and
  the stable-ID clarification in `docs/work-orders/codex-downtime-series.md`;
  `docs/README.md`; and the superseding identity notes in WO-101, WO-102,
  WO-103, WO-105, WO-107, and WO-108.
- **Decision/schema/runtime effects:** existing IDs remain stable; no order is
  renumbered, activated, prioritized by ID, or declared complete from this map.
  No scheduler, registry schema, or allocation policy is pinned.
- **Unresolved choices:** single versus multi-track grouping, metadata home,
  default meaning of complete, in-slot/out-of-slot presentation, computable
  versus operator preflight, and whether human aliases improve scanning.
- **Required review:** audit every catalog claim against the control log and
  numbered evidence, keep dependency separate from preflight and worker role
  separate from model/harness, and confirm legal, eligible, and recommended
  answers never collapse into one another.

## Team Topologies research ideation breakout receipt — 2026-09-01

- **Authority:** the operator nominated Team Topologies for a source-grounded
  idea and counterpattern mining pass soon; the operator did not authorize
  memory-based adoption or assign a work-order number.
- **Raw intake batch:** local-only
  `docs/intake/notes/WO-006-team-topologies-research-2026-09-01.md`.
- **Durable surfaces:** the preserved ledger entry, the explicitly unmined
  inspirational-source nomination in `docs/product/00-vision.md`, and the
  unallocated research-candidate row in the provisional work-order map.
- **Decision/schema/runtime effects:** no book content, coordination pattern,
  taxonomy, architecture decision, citation, schedule, or runtime change is
  adopted in this pass.
- **Required review:** confirm the language describes only the intended inquiry,
  requires an authorized copy and provenance, considers both transfers and
  non-transfers from human teams to disposable agents, and remains honest about
  its unallocated state.

## Operator-flow mission ideation breakout receipt — 2026-09-01

- **Authority:** the operator rejected the former lead tagline as too narrow and
  clarified that dependable flow is correct but insufficient: the project aims
  to raise the likelihood that the operator remains in an actual flow state.
- **Raw intake batch:** local-only
  `docs/intake/notes/WO-006-flow-over-babysitting-mission-2026-09-01.md`,
  including the later flow-state refinement.
- **Ledger/product surfaces:** the adopted/transformed ledger entries, mission
  in `docs/product/00-vision.md`, observable interruption-cost objective in
  `docs/product/04-interfaces.md`, README lead, base publication outline,
  everyday-user route, and current-heading audience/status index.
- **Meaning:** dependable surrounding work is a mechanism. The desired outcome
  is a higher chance that the operator can keep the idea and implementation
  direction present without being pulled into repetition, procedure,
  coordination, polling, or recovery. “Give an agent a build, not your
  biography” remains architectural shorthand, not the mission or lead tagline.
- **Limits:** the product cannot guarantee, detect, or certify a person's
  psychological state, and flow never authorizes hidden scope, skipped evidence,
  privacy loss, unsafe continuation, or self-verification.
- **Required review:** ensure the outcome/means hierarchy survives every
  projection, interruptions remain decision-ready and material, engaged creation
  and walk-away operation both remain valid, publication claim links satisfy
  their base/lock contract, and no interface metric is mislabeled as proof of
  flow.

## Continuity-critical quench ideation breakout receipt — 2026-09-01

- **Authority:** the operator opened the complete `ideation:` pipeline after
  observing that a prior classifier reduced forceful corrections to expletive
  and capitalization markers and missed the response obligation those messages
  were directing.
- **Raw intake batch:** local-only
  `docs/intake/notes/WO-006-superconductor-quench-2026-09-01.md`, including the
  clarification that the recalled nine conditions were incidental rather than a
  desired fixed taxonomy.
- **Ledger/product surfaces:** the adopted continuity-critical-flow ledger entry
  and binary-quench candidate in `docs/product/05-pattern-library.md`, plus
  current publication-index coverage.
- **Decision/schema/runtime effects:** the candidate reuses existing explicit
  interaction context, FeedbackUnits, guards, corrections, statecharts,
  continuations, and evidence concepts. It adds no classifier, universal heat
  list, phase score, kernel primitive, schema, or runtime implementation.
- **Unresolved choices:** mode entry/exit, phase scope, condition applicability
  evidence, condition-specific versus universal response, recovery contract,
  cascade fixture, and false-positive correction.
- **Required review:** confirm that every applicable heat condition has equal
  binary phase effect without a severity ranking; typography is not the event;
  quench describes the interaction rather than the operator's mood; active idea
  and continuation survive; and recovery, authority, safety, and verification
  claims remain evidence-bound.

## Flow Steward literature ideation breakout receipt — 2026-09-01

- **Authority:** the operator opened the complete `ideation:` pipeline for an
  obstacle-removing coordinator/metaworker/translator inspired by three named
  public essays.
- **Raw intake batch:** local-only
  `docs/intake/notes/WO-006-flow-steward-literature-2026-09-01.md`.
- **Reviewed public sources:** Joel Spolsky's
  [“Controlling Your Environment Makes You Happy”](https://www.joelonsoftware.com/2000/04/10/controlling-your-environment-makes-you-happy/)
  and [“Two Stories”](https://www.joelonsoftware.com/2000/03/19/two-stories/),
  and Rands's
  [“Managing Nerds”](https://randsinrepose.com/archives/managing-nerds/).
- **Ledger/product surfaces:** the adopted Flow Steward ledger entry and
  candidate role under the continuity-critical pattern in
  `docs/product/05-pattern-library.md`, plus current publication-index coverage.
- **Transfer boundary:** predictable action/result mechanics, trusted ownership,
  recurring-friction removal, protected creation time, and optional credible
  support transfer as inspiration. Happiness claims, personality types,
  stereotypes, employee-management authority, organizational punishment,
  surveillance, and unilateral obstacle classification do not.
- **Decision/schema/runtime effects:** existing temporary roles,
  reactors/statecharts, continuations, disposable sidecars, and mediator
  projections are possible lowerings. No actor kind, resident agent, manager,
  global coordinator, authority source, observation schema, or implementation is
  added.
- **Unresolved choices:** named role versus support facet/projection; obstacle
  classes; auto-remediation envelope and rollback; observation/retention
  boundary; notification policy; reusable-improvement proposal path; and
  evidence needed to claim reduced friction.
- **Required review:** prove objective ownership and authority monotonicity,
  bypassability and failure isolation, protected-friction survival, no orphan or
  self-generated work, source-preserving translation, intervention-cost
  accounting, operator control, and the absence of inferred mental-state claims.

## Temporal-response and RxJS `expand()` ideation breakout receipt — 2026-09-01

- **Authority:** the operator clarified that quench came from a larger temporal
  response model involving repeated behavior inside a window, replies to older
  prompts, and movement through communication modes, then separately nominated
  RxJS `expand()` as a quiet MVP candidate.
- **Raw intake batch:** local-only
  `docs/intake/notes/WO-006-temporal-response-semantics-2026-09-01.md`.
- **Ledger/product surfaces:** the adopted temporal-interaction ledger entry,
  the preserved `expand()` lowering hypothesis, and the candidate in
  `docs/product/05-pattern-library.md`; the quench candidate now explicitly
  accepts instantaneous or temporally proved conditions.
- **Source boundary:** the operator's _Outliers_ association is preserved as an
  unmined research pointer. The only transferred technical source in this pass
  is the
  [official RxJS `expand()` reference](https://rxjs.dev/api/index/function/expand),
  which supports recursive projection but not DotLn's proposed semantics.
- **Decision/schema/runtime effects:** no event-envelope field, clock policy,
  reply resolver, window algebra, mode taxonomy, RxJS dependency/version,
  lowering, or executable fixture is pinned. Existing canonical statechart/IR
  and zero-runtime-dependency decisions remain authoritative.
- **Unresolved choices:** minimum observation envelope, event versus ingestion
  clock, late arrival, attachment correction, mode vocabulary, privacy and
  retention, rule-specific density evidence, fixture corpus, and whether a
  deterministic reducer, generated RxJS, or both is the useful lowering.
- **Required review:** keep immutable transport relation, declared semantic
  target, and inferred attachment distinct; never let temporal adjacency choose
  silently; apply common machinery to both actors without equating their
  semantics or authority; permit an unresolved attachment; require deduplicated
  evidence that a repeated action violates the same scoped, versioned contract;
  and keep `expand()` bounded, cancellable, cycle-safe, deterministic where
  promised, resource-limited, unable to invent external events/work/effects, and
  separate from temporal/quench meaning.

## Brain/hands, isolation, and foundation ideation breakout receipt — 2026-09-01

- **Authority:** the operator opened the complete `ideation:` pipeline for the
  brain-and-hands agent model, harness versus orchestration, isolated VM/Docker
  execution, LangGraph, and a foundation-first strategy for accommodating
  outside innovation; the operator then directed the repair to close.
- **Raw intake batch:** local-only
  `docs/intake/notes/WO-006-brain-hands-isolation-and-foundation-2026-09-01.md`.
- **Reviewed public sources:** Dan McAteer's
  [“The Evolution of the Agent Harness”](https://www.latent.space/p/attention-interface),
  the official
  [LangGraph JavaScript overview](https://docs.langchain.com/oss/javascript/langgraph/overview)
  and
  [runtime/framework/harness comparison](https://docs.langchain.com/oss/javascript/concepts/products),
  plus Docker's official
  [container/VM boundary overview](https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-a-container/).
  The World of Warcraft comparison remains the operator's experiential analogy,
  not a sourced claim about why a market succeeded.
- **Ledger/product surfaces:** the newest ledger section; vision foundation and
  source notes; architecture responsibility table, LangGraph accommodation
  probe, and isolated-environment candidate; the domain-model name
  disambiguation; the interface's human-attention projection; audit evidence
  guidance; the post-1.0 probe nomination; and publication-index coverage.
- **Transfer boundary:** model/brain, tools/hands, harness/body, durable
  orchestration/host, transport, execution environment, authority, evidence, and
  operator-attention projection remain separate even when a vendor bundles them.
  The attention interface packages material choices; it does not infer mood or
  flow. Stronger models may remove redundant scaffold only after conformance
  evidence and cannot absorb external authority, audit, recovery, identity, or
  provenance.
- **Decision/schema/runtime effects:** no LangGraph or container dependency,
  runner, image, VM/microVM provider, Docker configuration, environment schema,
  deployment topology, model route, canonical-state change, or WO-009 scope
  change. `ExecutionEnvironmentProfile` is only a working candidate name.
- **Unresolved choices:** whole-harness-inside versus
  brain-outside/hands-inside; local provider availability; container/VM/microVM
  risk profiles; attestation and teardown records; secret/model egress
  brokering; LangGraph semantic fit; the first accommodation fixture; and
  measurable quality of the smallest intent-to-evidence loop.
- **Required review:** reject ontology collapse, framework-name equivalence,
  worktree-as-security claims, containment-as-authority, self-certification,
  silent attention-policy learning, unsourced market causation, and feature
  checklist accretion. Require local discovery and negative containment fixtures
  before selecting any environment default, and decision/event parity before
  accepting any orchestration lowering.

## Final repair evidence receipt — 2026-09-01

- **Full executable gate:** `npm test` passed after the final code and product
  changes. That run included the Prettier check; publication, intake-backup,
  resume, checkpoint, worktree, and release fixtures; a forced TypeScript build;
  and all 68 kernel/skeleton tests with zero failures.
- **Publication gate:** the final current check reports 134/134 product
  headings, three identical dual-voice links, and both editions `CURRENT` at 27
  and 42 linked source sections. Section 7 of the staleness demonstration
  records the final stored locks, the deliberate two-edition stale result, the
  exact inverse patch, and restored source hash.
- **Harness gate:** sanitized targeted readback shows persistent
  `permissions.defaultMode = acceptEdits`, zero allow entries in all applicable
  layers, fourteen retained user-level denies, sandbox enabled, sandboxed-Bash
  auto-allow enabled, fail-closed startup enabled, and unsandboxed fallback
  disabled. A genuinely fresh unoverridden Claude 2.1.257 process displayed
  `accept edits on`; its `/sandbox` view selected sandboxed Bash with
  auto-allow. `claude doctor` exited zero with no invalid-setting warning. Its
  unrelated Keychain-writability warning remains outside scope and untouched.
- **Lifecycle gate:** the operator's renewed `resume: fix` reopens the preserved
  VER-001 source only because the earlier incomplete repair had already been
  marked complete. The repaired helper and fixture prove that an ordinary
  implementation-ready state cannot do this. This was a dogfood bootstrap: the
  bounded reopen code and much of the correction were already present in the
  working tree before the second `RepairRequested` event made use of that new
  path; checkpoint 5 lacked the reopen clause and checkpoint 6 contained it.
  That cycle contains the new `RepairRequested` checkpoint and a subsequent
  `RepairCompleted` checkpoint; the generated control projection is the
  authority for the final hash rather than a self-referential value copied into
  this receipt.
- **Clean-room/diff gate:** only the three `.gitkeep` placeholders under
  `docs/intake/` are tracked; raw captures remain local-only. The committed
  synthesis contains no personal filesystem path, credential, private
  identifier, employer implementation detail, unreviewed _Outliers_ mechanic, or
  private rule-store shape. `git diff --check` is clean.
- **Separation:** VER-001 remains immutable and visible. The repair executor did
  not allocate or write VER-002 and does not certify acceptance. After the fresh
  repair-complete checkpoint, independent re-verification is the recommended
  next action; another bounded `fix` remains legal only if the operator finds a
  further defect before dispatch.
