# WO-098 — Rule migration batch 1b: six more units including one role-skill unit and one cadence unit complete the selection rule, the whole-set measurement is reported, and the batch template names batch two (version assigned at activation)

**Cost:** Legacy declaration unavailable: added and removed wall-clock, context bytes, commands, tokens and steps were not measured. No reduction is claimed; the next planning refutation must judge this missing cost evidence (WO-126, 2026-09-09).

**Model:** any capable model. State the model and effort actually run in the
result (07-execution-guide.md §Model-specific notes).
**Effort:** executor xhigh+; verifier xhigh+; reviewer any.
**Release classification:** minor. Six compiled units, the measurement and
the template. Assigned at activation under the standing opt-out default.
**Nomination provenance:** the second half of WO-040's batch one, cut into
a bounded child at the operator's 2026-09-08 correction. Planner-synthesized
draft. Opaque identifier, not a priority. Clean-room screen: applies with
force.
**Depends on:** WO-097 merged (the first half's retirements and
bookkeeping).
**Recommended placement:** after WO-097; the same surfaces. A
recommendation, not a dependency token.

**Cites (read these sections):** `docs/work-orders/WO-040-rule-migration-batch-one.md`
(the selection rule, the measurement, the template); `docs/evidence/WO-039/README.md`
(criterion 6's method); `docs/work-orders/WO-097-rule-migration-batch-1a.md`.

**Objective:** Six more units including at least one role-skill unit (rung
seven) and one cadence-shaped unit, so that across the twelve every
lowerable category has a member and at least eight sit at rung one or two;
at least one `reference` recorded beside them; the whole-set measurement
(shapes by status and governance mode before and after; every role's
directed-load total strictly lower; the list of remaining sentences with
reasons; the verifier's restatement search); the batch template recorded;
batch two's candidates named in the ledger.

**Observed gap (dated 2026-09-08, `main` at `33e2c25`):**

- The success metric the vision names has never been reported as a number.

**Design (scope discipline):**

- The umbrella's measurement rules unchanged; directional and derived.

**Deliverables:** the units, the reference, the measurement, the template,
the write-backs below.

**Acceptance criteria (all required)**

1. Six units with the full shape and regression fixtures, at least one at
   rung seven and one cadence-shaped; across the twelve the selection rule
   holds; one `reference` recorded and never counted as a unit.
2. The measurement is directional and derived: mechanism count higher and
   prose count lower after the batch; every role's directed-load total
   strictly lower; the reverse-mapping fixture proves coverage; the
   verifier's restatement search is recorded and any found restatement
   reverts the shape to `prose`.
3. The template is recorded in the result and the ledger names batch two's
   candidates.
4. Write-backs land: 02 §Feedback compiler v1 (any handler extension), 06,
   13 engineer and tester rows, README, a dated capability-table row
   `feedback.migration`, ledger entry; publication locks.
5. `npm test` green; `git diff --check` clean; no new dependency; a fresh
   feedback evidence edition (operator-run live audit).

**Evidence gate:** the transcripts; the measurement; `npm test`; the
evidence edition.

**Write-back duty:** as listed in criterion 4.

**Non-goals:** batch two onward (the recorded candidate, one batch per
wave).

**Operator-review assumptions**

1. Employer-specific shapes stay excluded by default.
