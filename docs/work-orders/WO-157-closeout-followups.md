# WO-157 — Closeout follow-ups, one order: every defect boarded up by the orders closed on 2026-09-22 is fixed in one operator-authorized order — the integrate helper survives intent-to-add, deferred queue items get public targets, the source-change host counts files and refuses deletions, target publication runs no target hook, the skeleton admits a grant-bearing identity, the resident's binding is compiled under its registered profile with a default model, refused live episodes carry their reason, receipts say what their witnesses observe, Claude Code attestations read the selected effort, registries follow the import graph, allocation events fold across section changes, and the gate-sandbox teardown is deterministic (version assigned at activation)

**Model:** any capable model. The one live row is the re-mint's feedback
self-host episode. State the model and effort actually run
(07-execution-guide.md §Model-specific notes).
**Effort:** executor xhigh+; verifier xhigh+; reviewer any.
**Release classification:** minor. Skeleton validators, the source-change
and publication hosts, the worker refusal record, the resident's binding, a
control-fold contract, the executor's role text and the discovery probe
change; the editions re-mint once. Assigned at activation under the standing
opt-out default.
**Cost:** adds, across the fifteen items below, one pre-receipt check and a
guarded stash in the integrate helper; one sentence in the executor's
Follow-up Queue support and a reviewer-phase `retarget` command in the
adjacent queue; a committed-path count and a deletion/type-change refusal in
the source-change host; a writer-profile probe and a hooks-disabled push (or
a hook-configuration snapshot refusal) in target publication; the optional
`authorityGrantRegistry` in `isCompilationEnvironment`; the resident's
portfolio binding compiled under the bound repository's registered
`authorityProfile`, with a default model and effort per transport; the typed
refusal reason in the recorded `invalid-result` refusal and the CLI line; a
confinement witness whose summary matches what it observes; a refutation
statement rule; a claude-code selected-session readback row in the discovery
probe and an attestation source that cites it; a registered inventory that
follows the import graph (or a check that a protocol change stales the
edition); a document-gate row over committed JSONL streams and suite stubs;
a versioned authority check on `WorkOrderIdentityAllocated` with
order-scoped failure; a deterministic gate-sandbox teardown; and one fixture
per item. Registered sources edited (`scripts/lib/evidence-sources.mjs`,
checked 2026-09-22): `packages/skeleton/src/artifact-identity.ts`,
`worker-transport.ts`, `verification-protocol.ts`, `verification-host.ts`,
`dotln.ts`, `loadouts/executor-supports.ts`, `docs/discovery/environment.json`
and `scripts/lib/evidence-sources.mjs` itself; the executor confirms the
list against the registry at activation. One re-mint of every affected
edition (authority, artifact identity, verification, feedback, harness as
the registry selects) with one live feedback self-host episode (WO-147 D010),
after every registered-source edit has settled. Removes: the operator
recovery every stranded integration needs (WO-100 D017); the recurring
adjacent-queue advisory (WO-064 D011, WO-152 D011); the class of
undiagnosable refused live episodes (WO-152 D009: two refusals, USD 2.72, no
cause); the `unknown` written for a supplied effort on Claude Code receipts
(WO-152 D012; WO-100's status projection reports `xhigh -> unknown`); a
fold-wide control refusal waiting on a section-list change (WO-120 D007); a
product-gate failure with no owner (WO-063 D005); and the security limit and
uncounted ceiling that stand between the portfolio and an operator
repository (WO-064 D010, WO-100 D016). Wall-clock, tokens and context bytes
of the order itself are unknown until run; the executor records them per
seam group.
**Nomination provenance:** the sixteen decision records named in the items,
each boarded up by an order closed on 2026-09-22 (WO-063, WO-120, WO-152,
WO-151, WO-064, WO-100) and read by the 2026-09-22 closeout follow-ups
planning pass from the follow-up register. **Operator authorization:** the
dispatch asked for at most one order, run next and solo, carrying whatever
is useful before any future order; the operator's mid-pass correction
directed that everything go into a single work order, one time, authorized
by them, and then that the order be explicit enough for the executor and
carry enough acceptance criteria for the verifier. All three messages are
captured verbatim in ignored intake (SHA-256 in the ledger section). That
authorization sets aside the one-seam convention for this order and admits
the cross-seam edits and the single re-mint. Opaque identifier, not a
priority. Clean-room screen: no stop condition.
**Depends on:** WO-100 merged (the portfolio, the ceiling and the
integration that met D017; closed, v0.44.0); WO-064 merged (target
publication and the grant-bearing identity path; closed, v0.43.0); WO-151
merged (the receipt witnesses; closed, v0.42.0); WO-152 merged (the worker
refusal record and the effort attestation; closed, v0.41.1); WO-120 merged
(the allocation event; closed, v0.41.0); WO-063 merged (the teardown
observation; closed, v0.40.3).
**Recommended placement:** a one-entry slot at the head of the sequence,
before WO-153 and WO-154, solo, at the operator's direction. It edits
registered sources and re-mints the editions, so it must not run beside
WO-154 or WO-155, which do the same. A recommendation, not a dependency
token.

<!-- dotln-dependencies:start -->
[
  {
    "workOrderId": "WO-100",
    "relation": "satisfied-by-close",
    "reason": "the portfolio, the source-change ceiling and the integration that met the stranded receipt"
  },
  {
    "workOrderId": "WO-064",
    "relation": "satisfied-by-close",
    "reason": "target publication and the grant-bearing identity path"
  },
  {
    "workOrderId": "WO-151",
    "relation": "satisfied-by-close",
    "reason": "the confinement witness and refutation statement"
  },
  {
    "workOrderId": "WO-152",
    "relation": "satisfied-by-close",
    "reason": "the refused-episode record and the effort attestation"
  },
  {
    "workOrderId": "WO-120",
    "relation": "satisfied-by-close",
    "reason": "the allocation event whose authority check is versioned"
  },
  {
    "workOrderId": "WO-063",
    "relation": "satisfied-by-close",
    "reason": "the gate-sandbox teardown observation"
  }
]
<!-- dotln-dependencies:end -->

**Cites (read these sections):** the decision records named in each item
(`docs/evidence/WO-100/decisions.md` D006, D007, D016, D017;
`docs/evidence/WO-064/decisions.md` D006, D010, D011;
`docs/evidence/WO-152/decisions.md` D009, D011, D012;
`docs/evidence/WO-151/decisions.md` D001, D017, D020, D021;
`docs/evidence/WO-120/decisions.md` D007; `docs/evidence/WO-063/decisions.md`
D005); the source lines each item names below; 07-execution-guide.md
§Independent workflows and integration, §Retained planning follow-ups,
§Gate sandbox preflight and §Model-specific notes; 02-domain-model.md
§Artifact identity v1; 06-roadmap.md §Candidate — unattended work-order
portfolio (the always-on default paragraph); the map's section "Candidates —
returns from the WO-064 and WO-100 closes (recorded 2026-09-22)"; the
[closeout follow-ups planning document](../planning/closeout-followups-2026-09-22.md)
§4 (why one order) and §12 (the operator's correction).

**Objective:** the fifteen boarded-up defects below are fixed, each with a
fixture that fails against the current source and passes after, in one
order with one re-mint, so that no follow-up from the 2026-09-22 closes is
left for a later order to rediscover, and the verifier can check every item
from this text alone.

**Observed gap (dated 2026-09-22, `main` at `ad5bb1d7`):** each item's
observation is stated in the item. The planning pass re-checked four of
them on that revision: `git stash push --include-untracked` fails on an
intent-to-add entry on Git 2.55.0 (` A` in `git status --porcelain`);
`scripts/adjacent-work.mjs` refuses mutation outside the selected order's
`active` and `repairing` phases, so the retarget the D011 records asked of
a later executor cannot be performed; `CLAUDE_EFFORT=xhigh` is exported in a
Claude Code 2.1.280 session while `docs/discovery/environment.json` records
no readback at 2.1.263 and WO-100's status projection reports effort drift
`xhigh -> unknown`; one abandoned `dotln-gate-sandbox-*` root dated
2026-09-21 22:00 sits in the temporary directory.

## The items

Each item states what was observed, the paths, the fix, the fixture and
what the verifier checks. Where a decision record offered two routes, the
default is named and the alternative admitted; the executor records the
route and reason in the decisions. Line numbers are as of `ad5bb1d7`.

### Group A — closeout machinery

**Item 1 — the integrate helper survives intent-to-add (WO-100 D017).**
Observed: `worktree integrate` writes its pending receipt at stage
`preserved` and then runs `git stash push --include-untracked`; an entry the
executor had marked with `git add -N` (so `git diff --check` could see new
files) made the stash fail with "Entry '...' not uptodate. Cannot merge.";
nothing was stashed or merged and HEAD was unchanged, but a fresh run
refused as pending and `--continue`, which handles only untracked conflicts
or a `merging` stage, could not move past `preserved`; the reviewer
recovered by hand under an operator authorization. Paths:
`scripts/lib/worktree-integration.mjs` lines 386–446 (entry, the pending
refusal, the `--continue` branch) and 495–520 (the receipt `save()` at
stage `preserved`, then the stash push); `scripts/test-worktree-integration.mjs`
(the case "three required refusals leave the tree, index, HEAD and recovery
refs unchanged" at line 437 and "untracked stash collision remains explicit
and recoverable until continuation" at line 529 are the patterns). Fix:
before `createCheckpoint` and the first `save()`, read intent-to-add entries
(the ` A` rows of `git status --porcelain --untracked-files=all`, or
`git diff --name-only --ita-invisible-in-index`); default route: refuse in
the helper's existing refusal shape, naming every path and the remedy
`git add -- <paths>`, leaving tree, index, HEAD, recovery refs and receipt
file unchanged; admitted alternative: stage them and record
`stagedIntentToAdd: [paths]` in the receipt. Independently, mint the receipt
only after the stash push succeeds (the checkpoint ref may exist earlier),
or delete the receipt when the stash push fails and rethrow with Git's
stderr, so a fresh run after any pre-stash failure proceeds. Replace the
interim sentence in product 07 §Independent workflows and integration with
the helper's rule. Fixture: a new case in
`scripts/test-worktree-integration.mjs` with one `git add -N` entry: the
helper refuses (or stages) before any write; after a refusal the receipt
file is absent, `git stash list` is empty, HEAD and `git ls-files --stage`
are unchanged; the case fails against the current source. Verifier: run the
`worktree-integration` suite; in a scratch worktree with a `git add -N`
entry run `npm run worktree -- integrate WO-NNN` and observe the refusal
text and the unchanged state; read the product 07 paragraph.

**Item 2 — deferred queue items get public targets (WO-064 D011, WO-152
D011).** Observed: the executor disposes a deferred adjacent item before
`npm run plan -- followups --sync` has minted the decision's public FUP
identifier, so the item keeps a literal target (`planning`, or prose) and
`ImplementationReady` and `RepairCompleted` carry an advisory on every later
order; the records asked the next executor to retarget the closed order's
queue, which the queue cannot do: `scripts/adjacent-work.mjs` lines 24–36
select the order from the current branch and refuse mutation outside its
`active` and `repairing` phases. Paths:
`packages/skeleton/src/loadouts/executor-supports.ts` (the Follow-up Queue
support text; a registered source, regenerated into
`.claude/skills/dotln-executor/SKILL.md` and `.agents/skills/dotln-executor/SKILL.md`);
`scripts/adjacent-work.mjs` lines 24–36; `scripts/lib/adjacent-queue.mjs`
(command kinds `queue`, `revise`, `announce`, `dispose`, `direct`,
`check-in`, `start`, `complete` at lines 93–250). Fix: (a) the Follow-up
Queue support says: after recording a decision that names a `followup`, run
`npm run plan -- followups --sync` and dispose the deferred item onto the
minted `FUP-` identifier, never onto prose; (b) a `retarget` command kind,
admitted in the final-review phase as well as `active` and `repairing`,
that changes only a `deferred` item's `target` to an existing `FUP-`
identifier (checked against `docs/planning/followups.json`) and refuses any
other field, status or a non-existent identifier. Fixture: in the adjacent
queue's test file, a retarget of a deferred item to an existing FUP
succeeds in the final-review phase, a retarget to prose or to a missing FUP
refuses, and a retarget that touches another field refuses; the regenerated
bundle matches the generator. Verifier: `node scripts/harness.mjs check
--loadout contributor` is green; read the regenerated executor skill's
Follow-up Queue line; run the fixture; run `npm run adjacent -- apply
--file` with a retarget request in a scratch control state and observe the
result.

### Group B — hosts and the skeleton

**Item 3 — the source-change host counts files and refuses deletions (WO-100
D016).** Observed: WO-052 checks each changed path only by surface prefix
(`packages/skeleton/src/repair.ts` lines 85–90, consumed by
`packages/skeleton/src/source-change-host.ts` at lines 114 and 131); nothing
counts a Shine or Standardize change's files against the envelope's `files`
ceiling (`packages/skeleton/src/portfolio.ts` lines 43–44 and 81, phase
ceilings validated at 211–223), and a deletion or a file turned into a
directory passes without `repo.delete`; `portfolio.ts` line 43 reads
"Host-counted files the order may touch" although no host counts them. Sort
is unaffected because the host requires exactly the move. Fix: after the
writer's commit and before WO-054, read `git diff --name-status <base>
<commit>` in the host; refuse when the path count exceeds the envelope's
`files` ceiling; refuse a `D` entry, a type change (`T`, or a path whose
entry changes between blob and tree) unless the envelope carries
`repo.delete`; the refusal names the paths and the ceiling; reword the
comment to what is enforced. Fixture: a Shine fixture whose writer replaces
a file with a directory of files inside the surface is refused; a fixture
whose change exceeds `files` is refused; an in-ceiling Standardize change
still passes; a Sort move is unchanged. Verifier: run the portfolio and
source-change suites; read the refusal messages in the fixture output; read
the comment.

**Item 4 — target publication runs no target hook (WO-064 D010).** Observed:
`publishTargetOrder` (`scripts/lib/target-publish.mjs`, the push at line
462: `git -C <repo> push --no-follow-tags origin <commit>:refs/heads/<branch>`)
runs from the target's Git root with no hook override, so a `pre-push` or
`reference-transaction` hook in the target's common Git directory runs in
the operator's process with the operator's Git and `gh` credentials after
every refusal has passed; whether a sandboxed target writer (the
`target-worker-claude` and `target-worker-codex` profiles that `harness emit
--target` installs) can create such a hook is not established, since the
writer commits in a linked worktree whose objects and refs live in that
common directory. Fix: first, a recorded probe under each writer profile
that attempts to write `hooks/pre-push`, `config` and a file under the
common Git directory from the worktree, filed in `docs/evidence/WO-157/`
with the observed refusal or success; then, unless both profiles are
disproved, the publish push runs with hooks disabled (for example
`git -c core.hooksPath=<empty directory> push ...`), or refuses when the
hooks directory listing, `core.hooksPath` or other hook-relevant
configuration differs from a snapshot taken before the writer ran; the guard
lands as defence in depth even if the probe disproves both, unless the
operator declines it in a decision. Fixture: in the target-publication
suite, plant a `pre-push` hook in the fixture target that writes a sentinel
file; publish; assert the sentinel is absent and the push succeeded (or, on
the snapshot route, that a planted hook after the snapshot refuses the
publication with the hook named). Verifier: read the probe record; run the
suite; plant a hook by hand in a scratch target and run one publication
against a scratch remote as WO-064's smoke did, observing that the hook did
not run.

**Item 5 — the skeleton admits a grant-bearing identity (WO-064 D006).**
Observed: `compileLoadout` keeps the whole host environment, including an
optional `authorityGrantRegistry`, in `ArtifactIdentityV1.compilationEnvironment`
and the reactor recompiles from that field on equip, but
`isCompilationEnvironment` (`packages/skeleton/src/artifact-identity.ts`
line 61; consumers at 102 and 146) admits exactly five keys, so every
identity compiled with a registry is refused: `SourceChangeHost` throws
"compiled artifact identity is invalid" and `LiveReactorDriver.equip`
records `ArtifactCompilationRefused`; WO-064 avoided the path by compiling
its writer without publication grants (WO-064 D002). Fix: admit the optional
`authorityGrantRegistry`, validated with `normalizeAuthorityGrants`
(`packages/compiler/src/authority.ts`); state the optional field in product
02 §Artifact identity v1 (line 520). Fixture: equip a registry-bearing
identity through `LiveReactorDriver.equip` and dispatch it to a
`SourceChangeHost`; no `ArtifactCompilationRefused`; a malformed registry
still refuses. Verifier: run the skeleton suite; read product 02; confirm
the artifact-identity edition was re-minted (criterion 16).

### Group C — the resident's binding

**Item 6 — the resident's binding is compiled under its registered profile
(WO-100 D006).** Observed: the configuration loader validates a portfolio
binding under the bound repository's registered `authorityProfile`
(`scripts/lib/config.mjs` lines 64, 200, 376–379, 448), but a hand-written
`resident.json` bypasses that check; `scripts/resident-bind.mjs` (usage at
lines 91–92, flags at 128–129 and 188–196) and `decodePortfolioBinding`
(`packages/skeleton/src/portfolio.ts` line 274) do not compile the binding's
environment under the profile; the resident host consumes the ports at
`packages/skeleton/src/resident-host.ts` lines 92–106. Fix: `resident-bind`
builds the portfolio binding from the loaded `portfolios` entry through the
loader and compiles its environment under the bound repository's registered
`authorityProfile`; `resident-bind --check <store>` refuses a hand-written
binding whose environment does not match that profile, naming the
mismatch. Fixture: a hand-written binding with a mismatched profile is
refused by `--check`; a loader-built binding passes; the refusal names the
repository and profile. Verifier: run the resident-binding suite
(`scripts/evidence-resident-binding.mjs` and its cases); run `--check` by
hand against a mismatched store.

**Item 7 — always-on agents have a default model (WO-100 D007).** Observed:
`resident-bind` refuses without `--model` and `--effort` (lines 188–189);
no default exists for the resident's workers and judges or the portfolio's
live hosts; product 06's always-on paragraph records the operator's
candidate values (GPT-6 Luna or the latest Claude Sonnet, Sonnet 5 until
Sonnet 5.5, at `xhigh`) and says WO-111 owns adopting it, which WO-111's
non-goals forbid as a runtime change. Fix: `--model` and `--effort` become
optional; the default per transport is `gpt-6-luna` for `codex-cli-exec`
and the latest Claude Sonnet (`claude-sonnet-5` today) for
`claude-cli-print`, both at `xhigh`; the binding records `modelSource:
"default"` or `"operator"` so receipts can tell them apart; product 06's
paragraph moves from candidate to shipped with the values and the reopening
condition (Sonnet 5.5 available, a model withdrawn, an operator changes a
role default). Fixture: a bind without the flags records the transport's
default and `modelSource: "default"`; with the flags it records
`"operator"`; an unknown transport still refuses. Verifier: run the suite;
bind by hand without flags and read the store; read product 06.

### Group D — live episodes and receipts

**Item 8 — refused live episodes carry their reason (WO-152 D009).**
Observed: `packages/skeleton/src/verification-protocol.ts` (lines 160 and
341) and `worker-transport.ts` (lines 613, 636, 659, 789, 806) raise
`WorkerFailure("invalid-result", <reason>)` with a closed vocabulary of
contract reasons, but `verification-host.ts` lines 355–385 record only the
code in the `WorkerInterrupted` payload (`reason: code`) and rethrow
`new WorkerFailure(code)` without the detail, and `dotln.ts` line 274 prints
`worker refused: ${error.code}`; two refused live verifier episodes on
WO-152 cost USD 2.72 and left no diagnosable cause. Fix: `WorkerFailure`
carries the typed `detail`; the `WorkerInterrupted` payload records it beside
the code; the CLI line prints `worker refused: invalid-result (<detail>);
pending work is retained`; raw model output stays discarded; the detail
vocabulary is closed (the existing strings). Fixture: a fake transport
returns an invalid result with a named reason; the stored event and the CLI
line carry it; an unknown detail string is refused by the recorder.
Verifier: run the verification and skeleton suites; run one fake-transport
refusal through `dotln` and read the line; confirm the feedback edition was
re-minted (criterion 16).

**Item 9 — the confinement witness says what it observes (WO-151 D020).**
Observed: `scripts/lib/entropy-review.mjs` renders "Tracked status
byte-identical across the episode" (lines 546 and 586) from
`trackedStatusByteIdentical` (lines 1061–1071), which runs `git status
--porcelain --untracked-files=no`, so a shell command that creates an
untracked file in the source repository or edits an ignored one leaves the
claim true; and `scratchDelta.deltaCount` (lines 1163–1166) is
`Math.abs(after.count - before.count)`, a net count, so a frozen copy that
gains and loses the same number of paths reads as zero; both hashes are in
the receipt, only the summary overstates. Fix (default route: widen): add an
untracked-path observation to the source-repository witness with a subject
rule that survives the dispatching session's own writes, and record the
scratch delta as added and removed path sets rather than a net count, under
the conditional-rendering rule WO-151 D016 established; admitted
alternative: narrow the rendered claim to "tracked-path status and the
frozen copy's path-and-size inventory". Fixture: mutate the frozen copy by
adding one path and removing another; the receipt records both sets (or, on
the narrow route, the summary text names only what is observed and the
fixture asserts that wording). Verifier: run the entropy suite; read one
receipt rendered by the fixture.

**Item 10 — the refutation statement adds evidence (WO-151 D017).**
Observed: the worker instruction (`scripts/lib/entropy-review.mjs` line
1000) asks for the schema object plus a truthful statement, and both live
refuters returned their attempts payload as the statement, which the host
recorded and rendered (lines 556 and 612, truncated at 4,000 characters), so
the statement added nothing the typed report did not carry. Fix (default
route): for the refutation route, drop the separate statement and render the
typed report's reasons and evidence references in its place; admitted
alternative: tighten the instruction to prose naming what was read and run,
and refuse a statement that parses as JSON. Fixture: on the chosen route, a
refutation receipt renders without a statement paragraph (or a JSON
statement is refused with the reason). Verifier: run the entropy suite; read
one rendered refutation receipt from the fixture.

**Item 11 — Claude Code attestations read the selected effort (WO-152
D012).** Observed: `docs/discovery/environment.json` records claude-code
`effectiveEffortReadback` as `not found` with `harnessReadbackEligible:
false`, last observed at CLI 2.1.263, and carries no selected-session
readback row for claude-code although copilot-cli has one; at 2.1.278 and
later the session exports `CLAUDE_EFFORT` and `~/.claude/settings.json`
persists `modelSettings.<model>.effortLevel` (the planning pass observed
`CLAUDE_EFFORT=xhigh` at 2.1.280); the generated role text tells Claude Code
roles there is no readback to look for, which is how WO-152's reviewer
recorded `unknown` for a supplied `xhigh`, and WO-100's status projection
reports the same drift. The attestation `source` field written by lifecycle
dispatches carries `operator-attested`, `codex-session-readback` and
`harness-session-model-statement` today (observed in
`docs/control/orders/WO-100.jsonl`); the executor locates its validator.
Fix: `environment.json` gains `claude-code.selectedSessionReadback`
(classification `observed`, channels `CLAUDE_EFFORT` and the persisted
`effortLevel`, scope "selected, not effective", observed at 2.1.280); the
attestation vocabulary admits `claude-session-readback`, used when
`CLAUDE_EFFORT` is present, with the operator-selected value still refused
if it disagrees; the role text's readback sentence is corrected to say Claude
Code reads `CLAUDE_EFFORT`. Fixture: a dispatch with `CLAUDE_EFFORT=xhigh`
records source `claude-session-readback` and effort `xhigh`; without the
variable it records `operator-attested`; a supplied value never becomes
`unknown`. Verifier: run the resume suite; dispatch a read-only status in a
session with the variable set and read the attestation line; read the
regenerated role text; confirm the feedback edition re-mint carried the probe
file (criterion 16).

### Group E — registries and the fold

**Item 12 — registries follow the import graph (WO-151 D001).** Observed:
`packages/skeleton/src/entropy-review-protocol.ts` and
`mission-check-protocol.ts` change the compiled behaviour of registered
sources (`verification-protocol.ts`, `worker-transport.ts`) without being
registered in `scripts/lib/evidence-sources.mjs`, so an edition can stay
green while a protocol moves. Fix (default route): register the two files
and add a check, run by each edition's `--check`, that fails when a
registered source imports a sibling module that is neither registered nor
on an explicit exclusion list; admitted alternative: derive the registered
set by walking imports from the registered roots. Fixture: in a temporary
copy, change `entropy-review-protocol.ts` and observe the feedback edition
check fail; an excluded import does not fail. Verifier: run the edition
checks; run the fixture.

**Item 13 — new artifacts and suites owe a registration (WO-151 D021).**
Observed: `docs/control/entropy-reducer.jsonl` had to be declared in the
kernel's committed-JSONL protocol registry
(`packages/kernel/test/fixtures/jsonl-protocols.json`) and `entropy.mjs`
added to the stub script list the runner's CLI-selection fixture writes
(`scripts/test-runner.test.mjs` line 621), and nothing failed until the
artifact was tracked or the enumerating suite was selected. Fix: a
document-gate row in `test:docs` asserting that every committed `.jsonl`
under `docs/` is an `EventEnvelope` stream or listed in the registry, and
that every suite in `scripts/test-runner.mjs` whose task names a script has
a stub in the CLI-selection fixture's list. Fixture: plant an unregistered
`docs/**/*.jsonl` in a temporary copy and observe the row fail; remove one
stub and observe the row fail. Verifier: run `npm run test:docs`; run the
fixture.

**Item 14 — allocation events fold across section changes (WO-120 D007).**
Observed: folding the control log (`scripts/lib/control.mjs` line 60, the
`WorkOrderIdentityAllocated` case; line 285) re-validates every allocation
event's embedded authority against the section list hard-coded in
`scripts/lib/derived-contract.mjs` (lines 86–110, `sections` and
`checkGeneratedSections`), so a future change to that list would make
historical events unfoldable and refuse every control read
repository-wide. Fix: the event records the canonical digest (or a version)
of the section list it was written under; the fold validates against the
list that digest names, keeping the current list as the default for events
without one; a genuinely corrupt authority still refuses, and the failure is
scoped to that order's segment so other control reads continue. Fixture:
fold an allocation event written under a superseded section set and observe
it fold; fold a corrupt authority and observe an order-scoped refusal that
leaves the other orders readable. The map's WO-113 catalog row carries a
dated note that this order versions the check ahead of WO-113's section
contract. Verifier: run the control-segment suites; read the two fixture
outputs.

### Group F — gate reliability

**Item 15 — the gate-sandbox teardown is deterministic (WO-063 D005).**
Observed: `sandboxFixture` (`scripts/test-runner.test.mjs` line 1304) makes
a `dotln-gate-sandbox-*` repository under the temporary directory and its
`t.after` (line 1307) removes it with `rmSync`; on WO-063's first review
gate the teardown failed the whole runner-fixtures suite with `ENOTEMPTY`
after its assertions had passed, and one such root dated 2026-09-21 22:00
survives in the temporary directory. Fix: identify which process repopulates
or holds `<fixture>/.git/objects/pack` while the hook removes the tree (a
lingering child of the fixture's own `runGate`, or Git background
maintenance in the fixture repository), and make the teardown deterministic
at that cause (for example waiting for the child to exit, or initialising
the fixture with `gc.auto=0` and `--no-auto-maintenance`), not by blanket
retry; add an abandoned-root check so that no `dotln-gate-sandbox-*` root
survives a completed `npm test`. Fixture: reproduce the race under
concurrent suite load and show the repaired teardown completing; the
abandoned-root check fails against a planted root. If the race cannot be
reproduced under load, record the diagnosis and the evidence tried; the
abandoned-root check still lands. Verifier: run `npm test` once and list
the temporary directory afterwards; run the fixture; read the diagnosis.

## Executor procedure

1. Work the groups in order A to F; commit per group with a plain subject
   naming the items; record one decision per item with the route chosen,
   the before-fixture failure and any deviation from the item text.
2. Item 4's probe comes first inside group B, because its result selects the
   push route.
3. After every registered-source edit has settled: `npm run build`;
   `node scripts/harness.mjs emit --loadout contributor` and
   `node scripts/harness.mjs check --loadout contributor` (items 2 and 11
   change role text); `node scripts/harness-context.mjs --check` (if a
   cold-start ceiling breaches, the standing 2026-09-17 route applies: raise
   by one 4 KB step in `docs/control/budgets.json` with the rule named, never
   trim); then re-mint each edition the registry selects for the edited
   sources: `node scripts/authority-evidence.mjs` and its `--check`;
   `npm run evidence:artifact`; `npm run evidence:verification`;
   `node scripts/feedback-evidence.mjs --write` then `--record-selfhost
   <store> --edition WO-157` with one live `claude-cli-print` or
   `codex-cli-exec` verifier episode, then `--check`. Bump the skeleton
   component and the console's exact pin as WO-151 D001 did; the release
   classification is minor.
4. Run `npm test -- --review` before handoff, because this order edits
   `scripts/test-runner.mjs`'s inputs and the machinery suites (product 07
   §Gate sandbox preflight; WO-100 D014).
5. Report per group: wall-clock, commands and, where the harness exposes
   them, tokens; unavailable counters are unknown.

**Acceptance criteria (all required)**

1. Item 1 holds: the fixture with an intent-to-add entry fails against the
   current source and passes after; a refusal leaves receipt, stash, HEAD
   and index unchanged (or the staged paths are recorded); a pre-stash
   failure leaves no receipt that a fresh run cannot proceed past; product
   07's interim sentence is replaced.
2. Item 2 holds: the regenerated executor skill carries the
   sync-before-dispose sentence and `node scripts/harness.mjs check
   --loadout contributor` is green; the `retarget` command changes only a
   deferred item's target to an existing FUP identifier, in the final-review
   phase as well as the executor's, and refuses prose, a missing identifier
   or any other field, with fixtures for each.
3. Item 3 holds: the over-ceiling and the file-to-directory fixtures are
   refused with the paths and ceiling named; the in-ceiling Standardize and
   the Sort fixtures still pass; the `portfolio.ts` comment matches the
   enforced rule.
4. Item 4 holds: the probe record for both writer profiles is filed under
   `docs/evidence/WO-157/`; the planted `pre-push` hook does not run during
   the fixture publication (or the snapshot route refuses it by name); the
   guard is present unless a decision records the operator declining it.
5. Item 5 holds: a registry-bearing identity equips and reaches a
   `SourceChangeHost` without `ArtifactCompilationRefused`; a malformed
   registry still refuses; product 02 §Artifact identity v1 states the
   optional field.
6. Item 6 holds: `resident-bind --check` refuses a hand-written binding
   whose environment does not match the bound repository's registered
   `authorityProfile`, naming the mismatch, and passes a loader-built one.
7. Item 7 holds: a bind without `--model` and `--effort` records the
   transport's default (`gpt-6-luna` for `codex-cli-exec`, the latest Claude
   Sonnet for `claude-cli-print`, `xhigh`) with `modelSource: "default"`;
   with the flags it records `"operator"`; product 06 states the shipped
   default and its reopening condition.
8. Item 8 holds: an `invalid-result` refusal's stored `WorkerInterrupted`
   payload and the `dotln` CLI line carry the typed detail; raw output is
   still absent from both; an unknown detail is refused.
9. Item 9 holds: the receipt records added and removed scratch paths and an
   untracked-path observation (or the narrowed wording), and the fixture
   that adds one path and removes another proves it.
10. Item 10 holds: a refutation receipt on the chosen route renders without
    a statement paragraph, or refuses a JSON statement with the reason.
11. Item 11 holds: with `CLAUDE_EFFORT` set a lifecycle dispatch records
    source `claude-session-readback` and the variable's effort; without it,
    `operator-attested`; no supplied value becomes `unknown`;
    `environment.json` carries the claude-code selected-session readback row;
    the regenerated role text says Claude Code reads the variable.
12. Item 12 holds: changing `entropy-review-protocol.ts` in a temporary copy
    fails the feedback edition check; an excluded import does not.
13. Item 13 holds: the new `test:docs` row fails on a planted unregistered
    `docs/**/*.jsonl` and on a removed suite stub, and passes on the tree.
14. Item 14 holds: an allocation event under a superseded section set folds;
    a corrupt authority refuses with an order-scoped error while other orders
    stay readable; the WO-113 note is on its catalog row.
15. Item 15 holds: the abandoned-root check fails against a planted root and
    passes after a full `npm test`; the race is reproduced and shown fixed,
    or the diagnosis and evidence tried are recorded with the check still
    landed.
16. Every edition the registry selects for the edited sources is re-minted
    once, after the last registered-source edit, with one live feedback
    self-host episode recorded; every edition `--check` passes on the branch
    and on the integrated tree; the skeleton component and console pin move.
17. Every fixture named above fails against the current source (recorded per
    item in the decisions) and passes after; existing suites pass unchanged
    except where an item changes their expectation, which the decisions
    name; `npm test -- --review` is green before handoff.
18. Write-backs land: products 02, 06 and 07 as named in the items; the
    decisions record the route chosen per item and the reopening condition
    each map item carries; the map's 2026-09-22 closes section and the
    register rows already read "allocated to WO-157", and the reviewer checks
    each row's reopening condition against the landed fix.
19. `npm test` green; `git diff --check` clean; no new dependency; cold-start
    ceilings within budget or raised by the standing route with the rule
    named.

**Evidence gate:** the fixture transcripts per item; the probe record for
item 4; the live self-host episode and the edition checks; `npm test --
--review` at handoff and `npm test` once at final review.

**Write-back duty:** as listed in criterion 18; the ledger is reserved for
planning synthesis.

**Non-goals:** the discharged in-order directives of the closed orders
(settled in the register); the reviewer-on-a-cadence and meter-column
candidates (WO-151 D007 and the map's Entropy Reducer section); the
efficiency redesign of the cold-start ceiling route (WO-054 D006, the
operator's reserved pass); any change to the portfolio's derivation,
WO-111's proof or the sequence; new request kinds or transports.

**Operator-review assumptions**

1. The operator's authorization for one cross-seam order is recorded in the
   provenance above and the ledger; the refutation judges the order under
   that authorization rather than the one-seam convention.
2. Item 4's probe may show that neither writer profile can write the
   target's common Git directory; the guard still lands unless the operator
   declines it in a decision.
3. Item 15 may end in a recorded diagnosis if the race cannot be reproduced
   under load; the abandoned-root check still lands.
4. Item 7's Claude default is `claude-sonnet-5` until Sonnet 5.5 is
   available; the executor records the model id actually bound.
