# WO-127 — Preserve retained worktree state and unblock release close (v0.17.0)

**Model:** Codex gpt-6-astra at max or Claude Fable 5.1 at max; independent verification and final review follow the existing lifecycle.
**Effort:** executor max+; verifier xhigh+; reviewer any.
**Release classification:** patch. Repair the unpublished WO-126 release boundary; retain its v0.17.0 target and source-only distribution.
**Cost:** Adds bounded closeout enumeration and byte verification for retained local records, plus focused regression cases; removes manual archival and a full release gate that rejects unchanged planning inputs after execution commits. Focused preservation fixtures took 1517.638 ms and the planning-cost regression took 875.693 ms; retain measured observations without inventing caps.
**Nomination provenance:** The operator's 2026-09-11 release-recovery dispatch explicitly requests fixing the failed release gate, automatically preserving retained control state, completing publication and cleaning up the worktree. Synthesized from that dispatch; personal-project sources only, with no clean-room stop condition.
**Depends on:** WO-126 merged, satisfied.
**Recommended placement:** Immediate patch for the pending v0.17.0 release; no unrelated planned order is activated.
**Cites (read these sections):** 07-execution-guide.md §Workflow closeout and releases and §Discipline; 03-architecture.md §Corpus policy; scripts/lib/intake-reconciliation.mjs; scripts/lib/paths.mjs; scripts/worktree.mjs (finish); scripts/lib/plan-subject.mjs (cost-table validation); scripts/test-process-debt.mjs (closeout fixtures); scripts/test-plan-refutation.mjs (planning-cost fixtures); scripts/test-harness.mjs (invoke and output-review fixture); packages/skeleton/src/loadouts/contributor.ts (release-close procedure); scripts/authority-evidence.mjs (edition selection); scripts/test-runner.mjs (authority-evidence suite); docs/publication/everyday-ai-user-toc.md and software-engineer-toc.md (source locks).

**Objective:** Complete guarded release close without manually moving retained worktree records or rejecting unchanged planning-cost inputs merely because execution or integration commits are newer.

**Additional required inputs:** scripts/test-runner.test.mjs (prior-command inventory and preflight scheduling); FINAL-001 (traced timeout and failed-handoff preparation); packages/skeleton/src/harness-host.ts (stdin transport); .gitattributes (owning document validator); scripts/lib/evidence-preparation.mjs, scripts/harness.mjs, scripts/lib/harness.mjs, scripts/lib/meta.mjs and scripts/work-orders.mjs (owned projection preparation). The operator's later 2026-09-11 steering explicitly adds prevention of recurring stale-output failures at each stage.

**Observed gap (2026-09-11):** WO-126's retained adjacent-work record and five retained directory entries needed manual archival under main's ignored retained/WO-126 lane. The subsequent release gate passed 35 of 37 suites in 618.729 seconds, then refused planning cost evidence because its timestamp predates later work-order execution commits; a harness read-observer subprocess also hit its twenty-second timeout. The failed evidence remains local and immutable.

**Design (scope discipline):** Extend the existing preservation helper and guarded finish path. Archive non-disposable ignored control records under main's docs/control/local/retained/WO-NNN, keeping relative directory structure and separating them from main's active state. Existing bytes win; differing collisions preserve both. Validate the entire plan, copy without overwriting, verify bytes, and retain sources until guarded teardown. Continue refusing unknown ignored material and symlink escapes. Validate planning-cost freshness against the recorded cost-bearing subject, retaining content, ancestry and timestamp checks without treating allowed execution-only edits as new planning inputs. Diagnose the harness timeout and use the existing evidence-reuse procedure; do not weaken assertions or add blanket retries.

**Deliverables:** Helpers, focused fixtures, generated release-close skills, affected closeout documentation, sourced decisions and executed evidence.

**Acceptance criteria (all required)**

1. Retained control files and nested directories are automatically archived before teardown, including adjacent-work, feedback evidence, process and prototypes. Main's active state and terms.txt are unchanged; harness gate evidence is still handed off through the existing mechanism.
2. Dry-run shows the complete intake and retained-control preservation plan and changes no file, directory, ref or control state. Receipts contain no private file contents or private content digests.
3. Existing archive collisions preserve both versions; identical retries reuse the preserved bytes. A partial failure leaves sources recoverable, and a retry can complete. Symlinks and path escapes refuse before teardown, and unknown ignored material remains protected.
4. Planning-cost evidence remains valid after execution-only edits or a merge with unchanged cost-bearing inputs. Changed cost-bearing inputs, mismatched acceptances, invalid/non-ancestor revisions and observations older than their recorded subject remain refused. Focused tests reproduce the live failure and these boundaries.
5. Investigate the observed harness timeout and preserve tests that establish useful DotLn behavior, including cross-commit receipts, stale current bytes and generated-hook integration. Repair demonstrated defects without weakening assertions or adding blanket retries; validate the full release workload and state exactly what the observations establish.
6. Update the closeout docs and generated skills, retain sourced decisions, run the required full gate and git diff --check, then use independent verification and final review. Following merge, run the canonical release-close helper and confirm publication and worktree cleanup.
7. Prepare owned deterministic projections before the canonical evidence tree is captured. Repeated preparation leaves unchanged outputs untouched. Keep immutable observations and explicit publication/release inputs intact. All expensive package tests and fixtures wait for preflight success; failures do not produce passing evidence.

**Evidence gate:** Focused preservation and planning-cost fixtures; affected harness fixture; npm run harness -- evidence; canonical release-close verification after merge.
**Write-back duty:** Affected product documentation and docs/evidence/WO-127/decisions.md, with the decisions index and publication projections refreshed through their generators.
**Non-goals:** Changing published tags or Releases, editing immutable WO-126 reports, deleting retained material, weakening ignored-material classification, publishing packages or bypassing review.
**Operator-review assumptions:** The v0.17.0 release remains pending. Worktree cleanup follows successful reviewed integration and verified preservation; it never discards an unmerged repair.

## Execution record

The preservation helper now plans intake and retained control archival together, validates containment and ignored destinations before copying, uses exclusive collision copies and rechecks the source set and exact destination bytes before teardown. Archive receipts omit content and content digests. Main's active terms and queue stay intact. The existing harness evidence merge remains separate.

Focused preservation tests passed: four cases, 1517.638 ms, covering binary files, nested and empty directories, dry-run immutability, active state, cross-type collisions, interrupted and corrupt copies, retries, changed sources, symlink escapes and unignored destinations. The worktree integration fixture passed with automatic archival and actual teardown; the final gate also checks retained evidence handoff and unchanged refs during preview.

The planning-cost regression passed in 875.693 ms, including execution-only commits, a later merge, changed costs, mismatched acceptances, invalid and non-ancestor revisions, and stale observation dates. The current planning gate now passes without changing the existing cost table or immutable planning receipts. The operator asked whether the tests remain useful for DotLn. I incorrectly attributed restrictions on reruns and in-process testing to that instruction; D005 corrects the attribution. Every generated-hook invocation is retained; the fixture additionally proves changed current bytes invalidate the earlier cross-commit receipt. The twenty-second subprocess deadline remains intact. Traced isolated runs exercised 167 hook processes without reproducing the stall; these are diagnostic observations, not proof of a runtime timeout repair.

The pending source target remains v0.17.0. Skeleton's existing unpublished 0.15.0 version already differs from the preceding published release; no additional component retiming is needed for this repair. Updated role instructions save sourced cost observations in evidence and suppress recurring missing-cost narration, following the operator's explicit correction.

The changed generated skills require a fresh authority bundle snapshot. The existing authority evidence helper now accepts an explicit work-order edition and refuses to overwrite differing existing evidence; the gate selects WO-127. WO-126 snapshots remain intact. Both publication edition locks were refreshed after checking their linked source sections.

The first complete source-stable full workload passed 36 of 37 suites in 475.600 seconds, including retained control, actual worktree teardown/evidence handoff, planning-cost and generated-hook fixtures. Its sole failure was the runner inventory expecting the prior authority command without the new edition argument. The inventory now explicitly maps that historical command to the WO-127 edition, preserving the check for every prior command; its focused regression passed in 61.002 ms. The canonical gate is rerun after this correction.

## FINAL-001 repair

Final review reproduced the hook timeout after the first independent green workload. The fifth traced isolated repetition then stopped inside the synchronous fd-0 read at 29.958 ms, before evaluation. The hook now consumes its piped JSON asynchronously. The cross-commit regression uses real Write and Read hook observations of its authored file; a separate generated-process case checks a manifest-sized message, chunked multibyte UTF-8 and truncated-input refusal. Both passed in 2019.103 ms. A minimal 100-process pipe probe did not reproduce the original stall; the diagnosis is bounded to the captured generated-hook read, not an unsupported general Node defect.

Failed-result validation already supported targeted checks. The architecture document now declares its publication validator, and a focused fixture covers both verifier and reviewer with oversized documents, a generated index, current report delivery and missing, failing or stale checks. The original failed review was recorded through the canonical command; passing-result requirements remain intact.

Following the operator's additional request, canonical evidence prepares the work-order index, decision index, follow-up register and generated harness before fingerprinting. These generators do not rewrite unchanged bytes. Package tests now wait for the same preflights as fixtures. Historical authority snapshots remain unchanged; the repaired runtime and role procedure use explicit WO-127 authority revision 002. Immutable observations, publication source locks and release targets are not silently refreshed by preparation.
