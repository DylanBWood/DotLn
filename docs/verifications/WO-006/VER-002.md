# WO-006 verification report VER-002

**Subject:** branch `wo-006`, uncommitted working tree, 2026-09-01. Base and
`HEAD` are both `f2a4b232e1868691398964433c7e373fca4b84bb`; the branch carries
no local commits. The subject is 73 tracked modifications plus eleven untracked
additions (`.prettierrc.json`, `.prettierignore`, `docs/AI-HARNESS-SECURITY.md`,
`docs/decisions/0003-…`, `0004-…`, `0005-…`, `docs/planning/work-order-map.md`,
`docs/publication/`, `docs/verifications/WO-006/`,
`scripts/check-publication.mjs`, `scripts/test-publication.sh`) — 10,862
insertions and 3,189 deletions, the large majority authorized repository-wide
Prettier reformatting. Three of those lines are this verification's own
`VerificationRequested` append and its regenerated projection.

**Authority:** `docs/work-orders/WO-006-publication-bootstrap.md`, now carrying
twenty acceptance criteria, thirteen operator-authorized scope expansions, two
required corrections, and nine breakout receipts plus a final repair evidence
receipt.

**Prior reports:** `docs/verifications/WO-006/VER-001.md` (verdict `fail`; one
blocking finding, one material, two non-blocking). Two repair episodes ran
between it and this report.

**Verifier:** Claude Opus 5 (1M context) in Claude Code at `ultracode` effort;
WO-006 permits any capable model. The lead verifier ran the full evidence gate
directly and independently re-derived every headline number, then dispatched
fourteen read-only parallel audits — the three publication-core criteria, the
staleness loop, the checker as an instrument, clean-room and receipts, Prettier,
the harness runbook and ADRs, Malcolm and rule-import, Embodied Explorer and
Team Topologies, work-order navigation, mission and flow, the `resume: fix`
transition, the brain/hands boundary batch, scope/classification/write-back, and
control-log integrity. Every candidate finding was re-checked by three
independent adversarial refuters on distinct lenses (correctness,
scope-and-authority, reproduces), each instructed to default to refutation. Only
the lead verifier wrote to this worktree.

**Verdict: WO-006 does not pass.** All four `VER-001` findings are repaired,
including the blocking one; all twenty acceptance criteria are met in substance;
the executable evidence gate is fully green and every headline number in it was
independently re-derived, with the complete `npm test` additionally re-run
end-to-end by a second agent in an isolated copy. Two material findings and
three minor ones stand out of 33 candidates raised across fourteen audits; 29
were refuted on 99 adversarial votes, including three the lead verifier had
personally adjudicated as findings and then withdrew on the panel's evidence.

Both material findings are honesty defects in a work order whose entire subject
is traceable, unblurred publication:

- one index row labels a section `verified` while eight of fourteen cadences and
  five of eleven program constructors it pins throw `evaluation is deferred`;
- three affirmative statements in delivered documents are false against the tree
  they describe, each accurate when written and never written back after a later
  authorized pass.

Every repair is small: one status label, one negative assertion in a shell
fixture, and five prose corrections. None touches shipped behaviour, a settled
decision body, or a numbered immutable record.

The reason this is `fail` rather than pass-with-notes is the execution guide's
own line: "'We were still building it' explains absent **process** scaffolding.
It never excuses a **behavioral or evidence** defect." A `verified` label
contradicted by a runtime throw, and three delivered statements contradicted by
the tree, are claims the repository itself refutes — the same class `VER-001`
blocked on, in the deliverable built to detect exactly that. They should be
corrected before final review rather than after.

## Dispatch and subject integrity

`npm run resume -- verify` allocated this exact report path and advanced the
control state from `ready-to-verify` to `verifying`, recording checkpoint
`8584dc2e59c095f1d51ce10ab4df40f220fd72d8` (`refs/dotln/checkpoint/WO-006/8`).
No older checkpoint was used.

`docs/control/resume.jsonl` is a strict append: its first 38 lines are
byte-identical to `HEAD` and nine WO-006 events follow. Re-folded independently,
the sequence
`WorkOrderActivated → ImplementationReady → VerificationRequested (VER-001) → VerificationCompleted(fail) → RepairRequested → RepairCompleted → RepairRequested → RepairCompleted → VerificationRequested(VER-002)`
is legal at every step, including the second `RepairRequested` — the reopen path
AC19 describes. Every `checkpointSha` named in those events resolves to a real
object under `refs/dotln/checkpoint/WO-006/`.

`VER-001` is provably immutable. Its SHA-256 is
`7a55c341def6306f5136331b6604959755dd8758a092e36c6f8584bfe3d6ed88` in the
working tree and identical in checkpoints 3, 4, 5, 6, 7 and 8 — every checkpoint
taken since it was written. No numbered immutable record was modified:
`git diff HEAD -- docs/verifications docs/final-reviews docs/releases` touches
only the three directory-convention READMEs, and their word streams are
byte-identical to `HEAD` (Prettier prose reflow only, not one word changed).
`git diff --check` is clean. Only the three `.gitkeep` placeholders under
`docs/intake/` are tracked.

## Evidence gate

Every item the work order names, run by the lead verifier in this worktree. All
exit codes zero:

```text
$ npm run publication:check
PASS index coverage: 134/134 product headings indexed
PASS dual-voice links: 3 identical claim links per voice
CURRENT everyday-ai-user-toc.md: 27 linked source sections match
CURRENT software-engineer-toc.md: 42 linked source sections match
PASS publication bootstrap checks

$ npm run format:check              All matched files use Prettier code style!
$ bash scripts/test-publication.sh  publication tests passed
$ bash scripts/test-backup-intake.sh
$ bash scripts/test-resume.sh
$ bash scripts/test-checkpoint.sh
$ bash scripts/test-worktree.sh
$ bash scripts/test-release.sh
$ tsc -b --force && node --test packages/*/dist/test/*.test.js
# tests 68 / pass 68 / fail 0
$ node --test corpus/harness/*.test.mjs
# tests 22 / pass 22 / fail 0
```

**Independently re-run.** The full `npm test` was additionally executed
end-to-end by a second, independent agent in an isolated `rsync` copy of the
working tree (`node_modules` copied, `.git` and `docs/intake` excluded), which
reproduced every stage: `format:check` clean, all six shell suites exit 0,
`tsc -b --force` exit 0 producing nine kernel and one skeleton bundles, and
`# tests 68 / # pass 68 / # fail 0`. `claude doctor` was also run first-hand
(non-interactively, with a kill guard): exit 0, one warning only — "macOS
Keychain is not writable" — exactly the unrelated warning the work order's
receipt describes.

**Disclosed deviation, unchanged from VER-001.** The six shell suites
`mktemp -d` under `/private/tmp` and guard cleanup on that exact prefix. The
verifying harness's sandbox denies writes to `/private/tmp` while permitting
`/private/tmp/claude-501`, so the suites ran from a mirror in which only the two
`/private/tmp/dotln-*` string literals per script were relocated. The per-script
relocation delta was printed and inspected — it is exactly two lines per file
and touches nothing else — and the five code-under-test files
(`scripts/resume.mjs`, `scripts/release.mjs`, `scripts/worktree.mjs`,
`scripts/check-publication.mjs`, `scripts/backup-intake.sh`) were proved
byte-identical to the working tree with `cmp` before the run. This exercises
every assertion in all six suites; it does not exercise the literal
`/private/tmp` path choice. An unmodified `npm test` by the operator would close
the residual gap.

## Independent re-derivation (not trusting the deliverable's own instruments)

Three instruments used as evidence here are themselves WO-006 deliverables, so
per the execution guide's disclosure duty each headline number was re-derived by
a mechanism that is not the instrument.

- **`scripts/check-publication.mjs`** is simultaneously the AC1/AC4 guard and a
  deliverable. Its `134/134` was re-derived with a separately written parser and
  fence tracker over `docs/product/00-*.md … 10-*.md`: 9 + 1 + 7 + 17 + 13 +
  23 + 26 + 8 + 11 + 10 + 9 = **134** headings, against **134** index rows
  parsed out of `audience-status-index.md` — one-to-one, no missing, extra, or
  duplicate. Every status label is inside 08's seven-value vocabulary (`vision`
  31, `specified` 39, `planned` 50, `verified` 7, `implemented` 7; zero
  out-of-vocabulary). The dual-voice sample's three claim links each appear
  exactly twice — once per voice, byte-identical.
- **`format:check`** is AC7's own gate. Formatter conformance was proved instead
  by re-printing every changed file from `HEAD` through the pinned Prettier
  3.9.6 and comparing byte-for-byte: **41 files are exactly a pure re-print of
  `HEAD`**, including every `packages/` source and test file and every corpus
  harness module. The only `packages/` file outside that set,
  `packages/skeleton/README.md`, differs solely by joining a hyphen-broken
  `live-versus-\nreplayed` — its whitespace-stripped character stream is
  byte-identical (`2fb17c3a…` both ways), so the change is a Markdown rendering
  fix, not a semantic edit. The ignore partition was recomputed through
  Prettier's own API: **112 supported tracked files = 47 ignored + 65
  formatted + 0 unformatted**, and each ignored path maps to a category the
  receipt documents (hash-sensitive corpus manifests, generated projections,
  settled decision bodies, immutable verification/final-review/release records,
  the append-only ledger, work-order scope authorities). `AGENTS.md` is a
  symlink to `CLAUDE.md` and is skipped by Prettier's globbing by design;
  `CLAUDE.md` itself is formatted.
- **`scripts/resume.mjs`** allocated this report's own `VER-002` path and is
  modified by this diff. Its transition semantics were exercised in an isolated
  fixture repository rather than inferred from the live projection — see AC19
  below. The append-only log with a regenerated projection is the
  safe-to-dogfood shape the guide describes: corruption would have shown rather
  than producing a plausible record, and none did.

Two further repository-wide checks, run first-hand:

- **Clean-room.** An 8-gram verbatim-copy detector over all ten local intake
  notes (2,453 distinct shingles) against all 87 changed or added text files
  found exactly **one** shared run of eight or more words, in three committed
  files: _"give an agent a build, not your biography"_ — the operator's own
  coined phrase, which the operator-flow receipt deliberately preserves as
  "architectural shorthand, not the mission or lead tagline." No other verbatim
  overlap exists. No forbidden term, employer identifier, internal hostname,
  credential, email, username, or absolute personal path appears anywhere in the
  new surface.
- **Link integrity.** All **376** relative Markdown links and heading anchors
  across all **83** repository Markdown files resolve. The repository-wide
  reformat broke zero links and lost zero heading anchors.

**Coverage.** All 84 changed paths partition exactly, and two agents computed
the split independently and agreed: of the 73 tracked modifications, **41** are
byte-identical to a pure Prettier re-print of `HEAD`; **19** are substantive and
Prettier-formatted; **13** are Prettier-ignored or unsupported and were all
hand-edited (the control projection and JSONL, the ADR-0002 amendment, the
ledger, six `WO-1xx` work orders plus the downtime series, and the two shell
fixtures). That is **32** substantive modifications plus **11** untracked
additions = **43** authored or new paths. Every one of those 43 falls inside an
audited dimension — the eleven product documents, the seven work orders plus the
downtime series, the ledger, the four operator/model workflow docs, the three
new ADRs and the ADR-0002 amendment, the runbook, the work-order map, the seven
`docs/publication/` artifacts, the two publication scripts, the three
control/lifecycle scripts, the two control-plane files, the two package files,
`.prettierignore`, and this report's own directory. Nothing in the subject went
unopened.

**Patch classification holds.** `package-lock.json` adds exactly one package —
`prettier@3.9.6`, marked `dev: true` — and removes none. No `rxjs`, `langgraph`,
`langchain`, `docker`, or other runtime dependency appears in `package.json` or
the lockfile; no Dockerfile, compose file, image reference, or environment
schema was added. Every `packages/` source and test file is a pure Prettier
re-print of `HEAD`. No exported runtime capability was added, so `v0.2.3`
remains correct.

## Status of the four VER-001 findings

**Finding 1 (blocking) — the Claude posture the runbook documents.** Repaired on
every axis the work order pins except one the operator has suspended. Targeted
sanitized readback across all applicable layers:

| Axis                               | AC8 / runbook             | Actual                    | Layer               |
| ---------------------------------- | ------------------------- | ------------------------- | ------------------- |
| `permissions.defaultMode`          | `acceptEdits`, persistent | `acceptEdits`, persistent | user                |
| `sandbox.enabled`                  | `true`                    | `true`                    | user                |
| `sandbox.autoAllowBashIfSandboxed` | `true`                    | `true`                    | user                |
| `sandbox.failIfUnavailable`        | `true`                    | `true`                    | user                |
| `sandbox.allowUnsandboxedCommands` | `false`                   | `false`                   | user                |
| credential/transport denies        | retained, fourteen        | fourteen deny entries     | user                |
| checkout-local `permissions.allow` | zero                      | **13**                    | main-checkout local |

The first six axes match exactly, including the persistent `acceptEdits` whose
absence was the substance of VER-001's blocking finding. ADR-0005 supersedes
ADR-0004 Decision 4 only, ADR-0004 partially supersedes ADR-0003 Decision 2
only, and all three ADRs are new files in this change — no settled decision body
was rewritten in place. ADR-0002's only change is a strictly appended
§Amendments entry (`git diff` shows additions, zero deletions).

The seventh axis diverges: the main checkout's local settings carry thirteen
`permissions.allow` entries (eleven `Bash`, two `WebFetch`) where AC8 and the
runbook claim zero. The repair nonetheless did its job — probing for the exact
`Bash(shasum -a 256 docs/product/08-publication-compiler.md)` rule `VER-001`
named returns false, so the original eleven were genuinely cleared, and the user
layer carries no `allow` key at all. A refuter sampled the file seven times at
thirty-second intervals and found the count, mtime and inode static: the
thirteen accumulated in one ~100-second burst during this verification's own
subagent fan-out — the two `WebFetch` grants are for the public sources my own
auditors fetched to check AC20's citations — and then stopped. **The operator
separately directed during this run that the accept-edits and allow-entry checks
be belayed, disclosing that per-command approval under parallel fan-out was
untenable and that the session had been moved to auto mode.** Per the execution
guide's precedence rule that instruction governs. This report records the
machine state factually and scores the axis neither way. The runbook already
handles the case correctly in prose — it timestamps its zero-count claim to the
post-correction check and instructs the reader to "investigate any nonzero count
locally without copying rule contents into repository evidence" — so no rule
contents appear here. Its §Current posture table row nonetheless reads as a
present-tense invariant; that is the surface a future pass should soften if the
operator wants the claim to survive drift.

**Finding 2 (material) — the staleness capture recorded superseded locks.**
Repaired, and the repair is the execution guide's prescribed remedy rather than
a back-fill. `docs/publication/staleness-demonstration.md` now opens with a
dated **Evidence discontinuity** note, preserves the earlier same-day
transcripts instead of rewriting them, and adds §7 as the current capture.
Reproduced first-hand in an isolated clone of the repository at the base
revision with the working tree overlaid:

```text
$ shasum -a 256 docs/product/08-publication-compiler.md
eb71f5d6…    (matches the recorded pre-probe hash)
$ node scripts/check-publication.mjs
PASS index coverage: 134/134 …  CURRENT 27 …  CURRENT 42 …    exit 0
# apply the exact §2 patch
$ shasum -a 256 docs/product/08-publication-compiler.md
e5a7aff8…    (matches the recorded probe hash)
$ node scripts/check-publication.mjs
STALE everyday-ai-user-toc.md: source lock ceb1d6b8…; current 4518fd70…
STALE software-engineer-toc.md: source lock 1f6e1a73…; current 88eaf2d3…
FAIL 2 edition(s) are stale                                     exit 1
$ node scripts/check-publication.mjs --expect-stale
PASS expected-stale proof: 2/2 editions flagged stale           exit 0
# exact inverse patch
$ shasum -a 256 docs/product/08-publication-compiler.md
eb71f5d6…    restored;  normal check CURRENT again              exit 0
```

All four hashes in §7 reproduce byte-exactly, and the two stored locks it prints
are the two the shipped editions carry and `--print-locks` reports. The
delivered tree contains no probe change. AC4's substance and its capture clause
are both met — with the residual noted in finding 1 below.

**Finding 3 (non-blocking) — `headings()` fence tracker.** Repaired correctly.
`closesFence()` now requires the closing line to consist only of fence
characters of the same kind and at least the opener's length, and
`fenceOpener()` rejects a backtick opener whose info string contains a backtick
— both CommonMark-correct. `scripts/test-publication.sh` pins it with a
` `-delimited block containing an inner ` ` ``` line followed by real headings,
asserting `11/11` coverage and failing loudly if a fenced heading leaks.

**Finding 4 (non-blocking) — `slugBase()` stripped underscores.** Repaired
correctly. `stripInlineMarkdown()` now strips `_`/`__` only in emphasis position
(word-boundary lookarounds, iterated to fixpoint) and `slugBase()` retains
`\p{Pc}`, so `## The resume_state field` slugs to `the-resume_state-field` as
GitHub does. The fixture pins four `resume_state` shapes — plain, nested-bracket
link, reference-style link, and one with adjacent `_care_` emphasis — plus the
`Echo` / `Echo 1` / `Echo-1` duplicate-anchor ladder.

## Findings

### 1 — Material. One index row carries a status the index's own rule and 08's honesty rule forbid

`docs/publication/audience-status-index.md` states its rule in its own preamble
(lines 4–7): "A row classifies the material claim made by that section, not
every future possibility mentioned beneath it. Mixed sections use the least
mature status that avoids presenting the whole section as more complete than its
material claims." The work order's scope discipline says status labels "must not
blur"; 08 §Authority and honesty rules 2 says the same: "Implementation status
cannot blur."

Line 38 of the index reads, verbatim:

```text
| [Events and decisions (the kernel loop)](../product/02-domain-model.md#events-and-decisions-the-kernel-loop) | everyday-ai-user, software-engineer | verified  |
```

That section normatively pins an eleven-constructor Program grammar
(`docs/product/02-domain-model.md`, "Pinned Program payloads … are exactly:")
and a fourteen-variant Cadence AST, including behavioural clauses such as
`Compensate`'s "scope: the paired program only". In the shipped kernel:

```text
$ grep -n 'case "' packages/kernel/src/core.ts
  Cadence:  Once After Every Gate Until Backoff          (6 of 14)
  Program:  Done Emit Invoke Await Guard Sequence        (6 of 11)
$ grep -rn 'is deferred' packages/kernel/src/*.ts
  core.ts:101  throw new Error(`Cadence ${cadence.kind} evaluation is deferred`);
  core.ts:196  throw new Error(`Program ${program.kind} evaluation is deferred`);
```

Eight of fourteen Cadence variants (`Burst`, `Calendar`, `Window`, `While`,
`Sequence`, `Merge`, `Race`, `Repeat`) and five of eleven Program kinds
(`Choose`, `All`, `Race`, `Repeat`, `Compensate`) throw on contact —
`corpus/harness/wo101-deferral-pins.test.mjs` pins exactly those five by name as
"WO-101 DEFERRED PROGRAM PINS". The section body discloses one unrelated
deferral ("A general expression AST is deferred until a consumer requires one")
and no deferral for the grammars themselves.

The type surface is complete and genuinely verified; the evaluation semantics
the section states are 45% and 57% deferred. That is the mixed case by the
index's own definition, and `verified` is the most mature label in the
vocabulary. The other six `verified` rows check out. This is the one row where
the blueprint's most load-bearing section is described, in the one work order
whose subject is publication honesty.

Three adversarial refuters re-checked this on distinct lenses; it survived, and
the reproduction lens reproduced every step "string-for-string". `implemented`
is where the stated rule points.

### 2 — Material. Three delivered statements are false against the tree they describe

Each of these was accurate when written and was invalidated by a later
authorized pass that never wrote back. The adversarial panel refuted every
attempt to hang a specific acceptance criterion on them individually, and I
accept those refutations — but the completeness critic, which saw those
refutations, re-raised them as a set at material severity, and I agree with that
adjudication. The governing rule is not an acceptance criterion; it is
`CLAUDE.md` §2 — "When you finish a piece of work that changes a decision,
update the doc in the same pass" — together with the execution guide's
evidence-gate discipline. Three affirmative statements in delivered documents
are now false:

- `docs/work-orders/WO-006-publication-bootstrap.md:617` — "Section 6 of the
  staleness demonstration records the final stored locks." Section 6 records
  `bdd5c35b…`/`5d85534b…` at 133/133; the shipped editions carry
  `ceb1d6b8…`/`1f6e1a73…` at 134/134, in **Section 7**. Confirmed by
  `--print-locks` and by `grep` over both editions. Mitigating: the
  demonstration resolves the pointer itself at its line 8–12, which names §7 as
  the current evidence.
- `docs/publication/staleness-demonstration.md:8` — "Sections 1–5 preserve
  earlier same-day captures." §6's locks are superseded too; the note is off by
  one section.
- `docs/product/06-roadmap.md:539` — "four bounded same-release additions",
  where the authority records thirteen. The rung's classification claim
  (documentation, process, dev tooling; no exported runtime capability; no
  release-boundary move) remains true of all thirteen, and the write-back duty
  at WO-006:235 only requires that the entry "already points here", which it
  does. A fourth, related inaccuracy sits one level down and is recorded as
  finding 4.

**Repair.** Four prose edits: correct §6 to §7, widen the discontinuity note to
§1–6, scope or correct the resume-change bullet, and bring the roadmap rung's
expansion narrative in line with the authority it points at.

### 3 — Minor. The repair-reopen fixture does not assert the guard AC19 turns on

AC19 requires that the reopen "requires a new `repair-complete` checkpoint," and
the evidence gate names the repair-reopen fixture explicitly. The behaviour is
correct — exercised first-hand in an isolated fixture repository:

```text
# ready-to-verify, no failure source
$ node scripts/resume.mjs fix
error: cannot perform action in phase ready-to-verify; run: npm run resume -- verify   exit 1
# ready-to-verify, VER-001 preserved as failure source
$ node scripts/resume.mjs fix
Repair …/WO-099.md using docs/verifications/WO-099/VER-001.md; read both artifacts.  exit 0
  → appends RepairRequested with sourceFindingId VER-001; creates no VER report
# now in `repairing`
$ node scripts/resume.mjs verify
error: cannot perform action in phase repairing; run: npm run resume -- repair-complete  exit 1
$ node scripts/resume.mjs repair-complete && node scripts/resume.mjs verify
Verify …; write the immutable report to docs/verifications/WO-099/VER-002.md.
```

The guard at `scripts/resume.mjs:407-415` is precise — `fix` still requires
`needs-fix`, or `ready-to-verify` with **both** a `failureSourceId` and a
`failureSourcePath` — and the appended event takes its source fields directly
from that state, so "same source" is structurally guaranteed.

`scripts/test-resume.sh:44-79` covers the refusal without a failure source, the
`verify, fix` projection after reopen, the reopened `fix` naming `VER-002`, the
`Phase: repairing` advance, non-reuse of verification numbers, and the
final-review failure-source path. It never asserts the third line above. Three
refuters independently mutated the guard out of `scripts/resume.mjs` in scratch
copies and confirmed the suite still passes. One negative assertion closes it. A
second cheap hardening: nothing inspects `sourceFindingId` in the appended
JSONL, so the durable record's own shape is unpinned even though the code
guarantees it.

### 4 — Minor. A work-order-map cell inverts a precondition it summarises

`docs/planning/work-order-map.md:72` records WO-108's execution profile as
"green baseline is an execution refusal condition."
`docs/work-orders/WO-108-mutation-probe.md:115-117` says the opposite: a red
baseline "invalidates every verdict, so the mutator refuses to start unless the
unmutated suite passes first." The map borrows WO-108's own "green-baseline
refusal" shorthand and loses its sense, so the cell reads as _green ⇒ refuse_
where the rule is _not green ⇒ refuse_. A navigation aid that inverts a
precondition misdirects the executor who relies on it. The neighbouring WO-006
cell also files two workflow-legal transitions under the "eligibility" heading —
the one distinction AC13 most insists on keeping separate.

### 5 — Minor. The lifecycle gate does not disclose its own bootstrap

The work order's Lifecycle gate presents the second repair cycle as an ordinary
reopen and says "The repaired helper and fixture prove that an ordinary
implementation-ready state cannot do this." The proof is real — I reproduced it.
The sequence still needs saying out loud. Checkpoint
`refs/dotln/checkpoint/WO-006/5` (`RepairCompleted`, 17:07) does **not** contain
the reopen clause in `legalActions()`; checkpoint 6 (`RepairRequested`, 19:33)
**does**, along with 1,478 insertions across 30 files. The ordering is not
optional — `fix` could not have been legal from `ready-to-verify` unless the
capability already existed in the tree — but the transition that authorised the
episode was made legal by code written before the episode opened, and much of
the repair had already landed when the opening event was appended. The execution
guide's disclosure duty covers exactly this shape. This report discloses it; the
receipt should too.

## Observations — raised, refuted, and recorded anyway

Twenty-nine candidates were refuted by the adversarial panel. The lead verifier
agrees with the refutations and does **not** treat these as findings, but
records the ones a final reviewer will encounter on the same surfaces.

- **The checkout-local allow list holds thirteen entries, not zero, and it is
  this verification's own footprint.** AC8 and the runbook claim zero. A refuter
  established what the count actually is: the eleven entries `VER-001` named
  were genuinely cleared (probing for the exact
  `Bash(shasum -a 256 docs/product/08-publication-compiler.md)` rule returns
  false), the user layer has no `allow` key at all, the WO-006 worktree has no
  local settings file, and the thirteen present entries — eleven `Bash`, two
  `WebFetch` for the exact public sources this verification's own agents fetched
  — accumulated in a single ~100-second burst during this verification's
  subagent fan-out and then stopped; sampled seven times at 30-second intervals
  the count, mtime, and inode were all static. **The operator separately
  directed during this run that the accept-edits and allow-entry checks be
  belayed**, disclosing that per-command approval under parallel fan-out was
  untenable and that the session had been moved to auto mode. Per the execution
  guide's precedence rule that instruction governs; this report records the
  machine state factually and scores nothing. A future pass may still want the
  runbook's §Current posture row to read as a dated observation rather than a
  present-tense invariant, since verification itself moves the count.
- **The mission rewrite was challenged as reversing an `adopted` ledger idea,
  and it does not.** `docs/product/04-interfaces.md` at `HEAD` carried
  "Automating that layer is explicitly out of bounds: it is the fun part."; the
  working tree replaces it with "the operator chooses whether shaping that layer
  directly is part of their desired creative work", and `idea-ledger.md:1103`
  still records **Operator-in-the-fun-loop** as `adopted`. The lead verifier
  initially carried this as a material ledger-duty violation. The panel refuted
  it 3–0 on evidence I accept: the ledger entry's _adopted commitment_ is the
  clause after its em dash — "hand-authoring identities/patterns via reviewed
  markdown is a first-class permanent path; the compiler must accept
  hand-written sources and never require the GUI" — and **both clauses survive
  verbatim** in the working tree. The quoted operator phrase is recorded
  provenance, not the commitment. No ledger duty is triggered. Recording the
  correction here because the surface looks alarming and the next reviewer will
  hit it too.
- **ADR-0004 is a new committed decision record named by no receipt and absent
  from the Deliverables list.** Raised as an AC6 defect and refuted 3–0: AC6's
  receipt clause is bound to the _ideation breakout_, ADR-0004 is a
  harness-security record governed by AC8 and AC11 (both satisfied), and no
  criterion makes the Deliverables paragraph an exhaustive file inventory.
  `scripts/test-publication.sh` is likewise unnamed, covered in substance by
  receipt 1's "The publication checker remains original WO-006 scope."
- **The first receipt's "the resume change alters only degradation guidance, not
  transition or checkpoint semantics" was challenged as denying AC19's
  transition change.** Refuted 6–0, decisively: the sentence sits inside a dated
  batch receipt, it was literally true when written — `VER-001` independently
  records "The `scripts/resume.mjs` delta is a single string literal" — the
  reopen code first appears at checkpoint 6, well after, and the transition is
  disclosed three other ways in the same document (the "Required
  repair-continuation correction" section, Deliverables line 111, and AC19
  itself).
- **Neither ADR-0003 nor ADR-0004 carries a backward supersession marker.** Read
  alone, ADR-0003 still presents auto-allow as a rejected alternative. Refuted:
  no criterion requires a back-pointer, `VER-001` authorised "an Amendments
  entry **or** a superseding record", and the repair took the second route
  correctly. Cheap to improve, not a defect.
- **The recorded harness snapshot has aged.** The runbook pins "Claude Code
  2.1.257"; the machine reports `2.1.258`. A dated snapshot is not falsified by
  a later patch bump.
- **`CompatibilityPlan` is a dangling reference.**
  `docs/product/10-ir-compatibility.md:137` relates the import-plan schema to
  `CompatibilityPlan`, defined nowhere in the repository. Harmless in a
  candidate surface that pins no schema; a reader will still look for it.
- **Latent checker edge cases, the same class as `VER-001` findings 3 and 4.**
  `slugBase()`'s trailing `.trim()` drops leading and trailing hyphens GitHub
  keeps; `headings()` reads ATX lines inside block quotes and inside HTML
  comment blocks; `inlineText()` unescapes `\_` before emphasis stripping. No
  current product heading triggers any of them, and all 134 present anchors were
  independently confirmed correct. Recorded so a future author does not resolve
  a failure by writing the broken link the instrument asks for.
- **One assertion in `scripts/test-publication.sh` cannot fail**, and the
  fixture kills only 4 of 16 seeded mutants of the checker. The
  `grep -Fq 'hidden'` guard inspects output a passing run never emits; a real
  fence leak is caught by the `11/11` assertion plus `set -e` instead. The two
  `VER-001` repairs are properly pinned; the checker's other guards are not.
- **The Team Topologies receipt names a work-order-map "row"** that is a prose
  bullet outside the catalog table. Accurate in substance, imprecise in noun.
- **Reciprocal to `VER-001`'s note on AC5.** The criterion still carries the
  narrowed wording the party under verification introduced. A final reviewer
  should confirm the operator intended the rewrite rather than an appended note;
  the original survives at `HEAD`.

## Acceptance criteria

| #   | Criterion                                                                                                                       | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Every product section indexed with audience tags and a status label                                                             | **Pass with note.** 134 headings enumerated independently; 134 index rows; one-to-one anchor match, no missing, extra, or duplicate; all tags and all seven status values inside 08's vocabulary. One row's label blurs `verified` against `implemented` — finding 1, which fails the work order's separate scope-discipline clause rather than this criterion's own text.                                                                              |
| 2   | Two editions differ sharply, cite shared sources, differ in no fact/limitation/permission/evidence                              | **Pass.** 27 vs 42 linked source sections, sharply divergent sequence and register, all links resolve currently and at the base revision. Selection differences are what 08 permits; no fact, limitation, permission, or evidence diverges.                                                                                                                                                                                                             |
| 3   | Dual-voice sample, one concept, identical claim links                                                                           | **Pass.** Exactly three links, each appearing once per voice, byte-identical and in identical order.                                                                                                                                                                                                                                                                                                                                                    |
| 4   | Staleness loop demonstrated and captured                                                                                        | **Pass.** Reproduced end-to-end first-hand in an isolated clone; §7's four hashes and both stored locks match byte-exactly. VER-001 finding 2 is repaired by dated discontinuity note plus recapture, not by back-fill. Residual pointer staleness is finding 2.                                                                                                                                                                                        |
| 5   | No publication artifact derives from, quotes, or links intake                                                                   | **Pass.** 8-gram detector over 2,453 intake shingles against all 87 changed files found one shared run — the operator's own coined phrase, deliberately and explicitly preserved by receipt.                                                                                                                                                                                                                                                            |
| 6   | Ideation clean-room rewritten; receipts give the complete expanded subject                                                      | **Pass.** Rewriting, ledger append-only discipline (319 insertions, zero deletions), and every ledger entry the receipts name all check out. A challenge that ADR-0004 is unreceipted was refuted 3-0: AC6's receipt clause is bound to the ideation breakout, and ADR-0004 is governed by AC8 and AC11, both satisfied.                                                                                                                                |
| 7   | Prettier exact, exclusions documented, everything else formatted, suite green                                                   | **Pass.** Exact `3.9.6` dev pin with lockfile integrity; ADR-0002 §Amendments strictly appended with an unedited decision body; 112 supported tracked files = 47 documented-ignored + 65 formatted + 0 unformatted; reformat proved semantically inert by independent re-printing.                                                                                                                                                                      |
| 8   | Harness runbook accurate, sanitized, dated, enable/verify/undo, asymmetry, checkpoint warning                                   | **Pass on the pinned axes; one axis operator-suspended.** Six of seven axes match exactly, including the persistent `acceptEdits` that VER-001 blocked on. The checkout-local allow count diverges (13, not zero) and the operator directed that this check be belayed — recorded above as a disclosed deviation, not scored. The criterion's fresh-session startup half is **not assessable** non-interactively; two agents tried. See Open questions. |
| 9   | Malcolm accounting observable-only, disposition separate from enforcement, `unknown`/`participated`, collection not implemented | **Pass.** Every clause satisfied across 05, 09, and the ledger; the two axes carry disjoint category lists stated identically in all three; no runtime-claim leakage.                                                                                                                                                                                                                                                                                   |
| 10  | External rule-source import stays a candidate compatibility-planning surface                                                    | **Pass.** Provenance preserved, mapping states visible and never auto-selected, suggestion separated from selection authority, public-rule scope qualified, no implemented-import or legal-compliance claim, clean-room intact.                                                                                                                                                                                                                         |
| 11  | Claude settings fault sequence recorded without shifting failure onto the operator                                              | **Pass.** The sequence is recorded accurately and attributes the unresolved repair to the repair pass. ADR-0005 supersedes ADR-0004 Decision 4 only; ADR-0004 partially supersedes ADR-0003 Decision 2 only; no settled decision body was rewritten in place.                                                                                                                                                                                           |
| 12  | Embodied Explorer stays a bounded post-1.0 candidate                                                                            | **Pass.** Embodiment truth, learned capability belief, and Identity stay separate; physical-safety boundary explicit; no actor kind, role schema, simulator, controller, or adapter exists in code.                                                                                                                                                                                                                                                     |
| 13  | Work-order navigation separates legal / eligible / recommended; IDs opaque; claims evidence-derived                             | **Pass with note.** The three answers stay distinct and the map is dated and labelled provisional; no ID was renumbered. One catalog cell inverts its source and one mislabels a lifecycle answer — finding 4.                                                                                                                                                                                                                                          |
| 14  | Team Topologies appears only as a provenance-aware research nomination                                                          | **Pass.** No book-derived pattern, counterpattern, taxonomy, architecture choice, citation, or schedule; no work-order number or schedule assigned.                                                                                                                                                                                                                                                                                                     |
| 15  | Mission states dependable workflow as a means to raise the chance of operator flow                                              | **Pass.** The outcome/means hierarchy survives every projection; nothing promises, detects, measures, or certifies the state; no interface metric is labelled proof of flow. A challenge that its propagation into 04 reversed an `adopted` ledger idea was refuted 3-0 — see Observations.                                                                                                                                                             |
| 16  | Continuity-critical flow is a candidate with binary `conducting \| quenched` semantics and no severity score                    | **Pass.** Conditions OR into the phase without ranking; typography is explicitly not the event; the recalled nine are explicitly not canonical; recovery is evidence-bound and claims nothing about the operator's mood.                                                                                                                                                                                                                                |
| 17  | Flow Steward stays bypassable, revocable, and authority-preserving                                                              | **Pass.** Cannot become an approval gate, global manager, surveillance layer, objective owner, guard remover, self-assigner, or self-verifier; source transfers and rejected stereotypes are explicit.                                                                                                                                                                                                                                                  |
| 18  | Temporal interpretation covers both actors; `expand()` stays a bounded hypothesis                                               | **Pass.** Attachment stays correctable and source-linked, counts and windows stay rule-specific, _Outliers_ stays unmined, and no RxJS dependency exists in `package.json` or the lockfile.                                                                                                                                                                                                                                                             |
| 19  | `resume: fix` reopens only against a preserved failure source; refuses initial ready state; allocates no report                 | **Pass on behaviour — finding 3 on its guard.** Every clause exercised first-hand in an isolated fixture. The shipped regression fixture does not assert one of them.                                                                                                                                                                                                                                                                                   |
| 20  | Brain/hands, harness, orchestration, transport, environment, attention, authority, evidence stay distinct                       | **Pass.** LangGraph stays a lowering candidate, containers stay an attested probe, worktrees are never called containment, `ExecutionEnvironmentProfile` stays a working candidate name, and no dependency, runner, image, provider, schema, or deployment choice was added.                                                                                                                                                                            |

## Scope, classification, and clean-room

Scope discipline holds against every named prohibition: no `Claim` IR and no
`PublicationPlan` type in fact or in name; 08's exact seven-value status
vocabulary with zero out-of-vocabulary labels; no manager, IT-director, VP, or
CFO edition; no renderer, site, or printable output; nothing published anywhere.
`scripts/check-publication.mjs` is a read-only checker that emits no artifact —
the cheapest sufficient mechanism the scope rule authorises, not the pipeline it
forbids.

The `v0.2.3` patch classification holds, proved rather than asserted: every
`packages/` source and test file is byte-identical to a pure Prettier re-print
of `HEAD`, `package-lock.json` adds exactly `prettier@3.9.6` marked `dev: true`
and removes nothing, no version, export, or `files` field changed anywhere, and
no `rxjs`, `langgraph`, `langchain`, or container dependency, runner, image, or
environment schema exists anywhere in the tree.

Write-back duty is discharged: `docs/README.md` maps `docs/publication/`,
`docs/AI-HARNESS-SECURITY.md`, `docs/planning/` including the provisional
work-order map, and `docs/decisions/` at the repository's directory-level
convention; the ledger append landed in its prescribed position with 319
insertions and zero deletions; all 376 cross-file links and anchors resolve.

## Required next actions

1. `npm run resume -- verification-result fail`.
2. `npm run resume -- fix`, then repair findings 1 through 5 reading both this
   report and the original work order:
   - relabel the `02 §Events and decisions` index row from `verified` to
     `implemented` (or add the missing deferral disclosure to the section and
     justify `verified` explicitly);
   - add one negative assertion to `scripts/test-resume.sh` proving `verify` is
     refused while the phase is `repairing`, and ideally one asserting
     `sourceFindingId` in the appended event;
   - correct the §6 pointer to §7, widen the discontinuity note to §1-6, scope
     or correct the resume-change receipt bullet, and bring the roadmap rung's
     expansion narrative in line with the authority;
   - fix the WO-108 green-baseline cell and the WO-006 eligibility label in the
     work-order map;
   - disclose the reopen bootstrap in the lifecycle gate. None touches a settled
     decision body, a numbered immutable record, or shipped behaviour.
3. `npm run resume -- repair-complete` — the repair episode is not finished
   until this is recorded.
4. A fresh independent `VER-003` before any final-review attempt.

## Open questions and caveats

- The six shell suites ran against a relocated temp root, as disclosed in the
  evidence gate. An unmodified `npm test` by the operator would close the
  residual gap.
- The harness posture changed during this verification and the operator
  disclosed it, explicitly suspending the accept-edits and checkout-local-allow
  checks and moving the session to auto mode. This report records the machine
  state factually and scores neither axis. A refuter further established that
  the thirteen checkout-local allow entries are this verification's own fan-out
  footprint, not a surviving repair defect. The settings files are gitignored
  and outside the committed subject.
- **AC8's fresh-session startup check could not be discharged.** The criterion
  requires "targeted readback **and** a fresh-session startup check". The
  readback is done and matches on every pinned axis. The startup check is not
  reachable non-interactively: `claude config list` spawned without a TTY exits
  1 with "Not logged in", and no non-interactive surface renders the
  permission-mode indicator. Two independent agents attempted it and both
  recorded `not-assessed`. The persisted layering is consistent with the claim
  (user-layer `defaultMode: "acceptEdits"` with no overriding layer) and
  `claude doctor` reports no invalid setting, but the displayed
  `accept edits on` indicator rests on the executor's receipt alone. Recorded
  here rather than scored, so a final reviewer knows AC8 is evidenced on
  readback and doctor, not on a witnessed startup.
- `docs/intake/` was read only to run the verbatim-copy detector for AC5. No
  intake content is reproduced in this report.
- The work order carries `Model: any capable model`, so no model constraint was
  violated. This verification ran on Claude Opus 5 (1M context) at `ultracode`
  effort.
- Three of this verification's own instruments — `scripts/resume.mjs`,
  `scripts/check-publication.mjs`, and `format:check` — are WO-006 deliverables.
  Each headline number they produce was independently re-derived by a different
  mechanism, as recorded above. None of them can fail silently in the way the
  guide's abstention rule contemplates: the append-only log with a regenerated
  projection shows corruption rather than hiding it, and the checker's two
  numbers were reproduced by a separate parser and a separate slugger.
