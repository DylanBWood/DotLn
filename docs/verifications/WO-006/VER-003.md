# WO-006 verification report VER-003

**Subject:** branch `wo-006`, uncommitted working tree, 2026-09-01. Base and
`HEAD` are both `f2a4b232e1868691398964433c7e373fca4b84bb`; the branch carries no
local commits. The subject is 91 changed paths — 73 tracked modifications and 18
untracked additions, 14,395 insertions and 3,189 deletions — the large majority
authorized repository-wide Prettier reformatting. Four of those lines are this
verification's own `VerificationRequested` append and its regenerated projection.

**Authority:** `docs/work-orders/WO-006-publication-bootstrap.md`, carrying twenty
acceptance criteria, thirteen operator-authorized scope expansions, two required
corrections, an operator deferral for this repair, ten ideation breakout receipts,
a Claude startup correction receipt, and a final repair evidence receipt.

**Prior reports:** `docs/verifications/WO-006/VER-001.md` (verdict `fail`; one
blocking, one material, two non-blocking findings) and `VER-002.md` (verdict
`fail`; two material and three minor findings). Three repair episodes have run —
two against `VER-001`, one against `VER-002`.

**Verifier:** Claude Opus 5 (1M context) in Claude Code at `ultracode` effort;
WO-006 permits any capable model. The lead verifier ran the complete evidence gate
directly, independently re-derived the load-bearing numbers with instruments that
are not the deliverable, and dispatched eleven read-only parallel audits — the
four VER-002 repair dimensions, the publication core, Prettier and clean-room
receipts, the harness runbook and ADR chain, the Malcolm/rule-import/Explorer/Team
Topologies batch, the mission/quench/steward/temporal batch, the brain-hands and
scope/classification batch, and control-plane integrity. Every candidate finding
was re-checked by independent adversarial refuters on three distinct lenses
(correctness, scope-and-authority, reproduces-and-materiality), each instructed to
default to refutation. Only the lead verifier wrote to this worktree.

**Verdict: WO-006 passes.** All five `VER-002` findings are repaired, including
both material ones, and each repair was confirmed first-hand rather than accepted
on the repair pass's word. All twenty acceptance criteria are met, with the single
exception of the AC8 clause the operator explicitly deferred, which this report
records without scoring, as the work order directs. The executable evidence gate
is fully green at every stage. Seventeen candidate findings were raised across the
eleven audits; thirteen were refuted on adversarial review and four survived, all
of them minor. **No material finding stands, and none of the four is blocking.**

The four survivors are recorded below as recommended hardening for the final
reviewer or a follow-on order. The reason this is `pass` rather than
`fail-with-notes` is the same execution-guide line that made `VER-002` a fail,
applied honestly in the other direction: `VER-002` failed on *behavioral and
evidence* defects — a `verified` label a runtime throw contradicted, and three
delivered statements the tree refuted. Nothing in that class survives here. What
remains is a co-located-disclosure preference already discharged by the alternative
remedy `VER-002` itself offered, a navigation aid one lifecycle transition behind,
a mutation-coverage gap in a fixture, and one missing field in two of ten receipts.
Those are notes, not defects the repository refutes.

## Dispatch and subject integrity

`npm run resume -- verify` allocated this exact report path and advanced the
control state from `ready-to-verify` to `verifying`, recording checkpoint
`6b0c486d8a3e37d4e423c17a13c5284d66631079`
(`refs/dotln/checkpoint/WO-006/12`). No older checkpoint was used.

`docs/control/resume.jsonl` is a strict append. Its first 4,996 bytes — HEAD's
exact length, 38 lines — are byte-identical to `git show HEAD:…` under `cmp`, and
thirteen WO-006 events follow. Re-folded independently, without `scripts/resume.mjs`,
the sequence

```text
WorkOrderActivated → ImplementationReady
  → VerificationRequested(VER-001) → VerificationCompleted(fail)
  → RepairRequested(VER-001) → RepairCompleted
  → RepairRequested(VER-001) → RepairCompleted
  → VerificationRequested(VER-002) → VerificationCompleted(fail)
  → RepairRequested(VER-002) → RepairCompleted
  → VerificationRequested(VER-003)
```

is legal at every step, and the independent fold reproduces `docs/control/current.md`
exactly on phase, latest verification, latest verdict, legal next actions, and
latest checkpoint. All twelve `checkpointSha` values resolve to real commit objects
whose named `checkpointRef` points at exactly that sha, and every checkpoint's
parent is `HEAD`.

Numbered immutable records are byte-frozen. `VER-001.md` has SHA-256 prefix
`7a55c341def6` and 23,956 bytes at checkpoints 3 through 12 and in the working
tree — every checkpoint since it was written. `VER-002.md` has prefix `c45a94082b59`
at checkpoints 9 through 12 and in the working tree, and is absent (empty) before
checkpoint 9, as expected. `git diff HEAD -- docs/verifications docs/final-reviews
docs/releases` touches only the three directory-convention READMEs, whose
whitespace-stripped character streams are byte-identical to `HEAD` — Prettier prose
reflow, not one word changed. `git diff --check` is clean. Only the three `.gitkeep`
placeholders under `docs/intake/` are tracked.

**The repair delta is exactly what it claims to be.** Diffing checkpoint 9's tree
(the state at which `VER-002` was completed) against the full current working tree —
tracked and untracked, through an isolated temporary index that never touched the
real one — yields exactly eight changed paths and nothing else:

```text
docs/control/current.md                            6 +/-   (regenerated projection)
docs/control/resume.jsonl                          4 +     (appended events)
docs/planning/work-order-map.md                   52 +/-   findings 4
docs/product/06-roadmap.md                        20 +/-   finding 2c
docs/publication/audience-status-index.md         18 +/-   finding 1
docs/publication/staleness-demonstration.md        2 +/-   finding 2b
docs/work-orders/WO-006-publication-bootstrap.md  30 +/-   findings 2a, 2d, 5 + deferral
scripts/test-resume.sh                             6 +     finding 3
```

No stray edit, no touched decision body, no touched numbered record.

## Evidence gate

Every item the work order names, run by the lead verifier in this worktree. All
exit codes zero.

```text
$ npm run format:check
All matched files use Prettier code style!

$ npm run publication:check
PASS index coverage: 134/134 product headings indexed
PASS dual-voice links: 3 identical claim links per voice
CURRENT everyday-ai-user-toc.md: 27 linked source sections match
CURRENT software-engineer-toc.md: 42 linked source sections match
PASS publication bootstrap checks

$ bash scripts/test-publication.sh      publication tests passed
$ bash scripts/test-backup-intake.sh
$ bash scripts/test-resume.sh           resume tests passed
$ bash scripts/test-checkpoint.sh
$ bash scripts/test-worktree.sh
$ bash scripts/test-release.sh
$ tsc -b --force && node --test packages/*/dist/test/*.test.js
# tests 68 / pass 68 / fail 0
$ node --test corpus/harness/*.test.mjs
# tests 22 / pass 22 / fail 0
```

**Disclosed deviation, unchanged from `VER-001` and `VER-002`.** The six shell
suites `mktemp -d` under `/private/tmp` and guard cleanup on that exact prefix. The
verifying harness's sandbox denies `mkdtemp` under `/private/tmp` while permitting
`/private/tmp/claude-501`, so the suites ran from a minimal mirror containing only
`scripts/`, `.gitignore`, and `docs/releases/tag-manifest.template.json` — the sole
repository files any suite reads. The relocation delta was printed and inspected:
it is **exactly two lines per suite**, substituting `/private/tmp/dotln-` with
`/private/tmp/claude-501/dotln-` in the `mktemp` template and its matching cleanup
`case` guard, and touches nothing else. All five code-under-test files
(`scripts/resume.mjs`, `scripts/release.mjs`, `scripts/worktree.mjs`,
`scripts/check-publication.mjs`, `scripts/backup-intake.sh`) plus `.gitignore` and
the tag-manifest template were proved byte-identical with `cmp` before the run.
This exercises every assertion in all six suites; it does not exercise the literal
`/private/tmp` path choice. An unmodified `npm test` by the operator would close
the residual gap.

## Independent re-derivation (not trusting the deliverable's own instruments)

Three instruments used as evidence here are themselves WO-006 deliverables. Per the
execution guide's disclosure duty each load-bearing number was re-derived by a
mechanism that is not the instrument.

- **`scripts/check-publication.mjs`** is simultaneously the AC1/AC4 guard and a
  deliverable. Its `134/134` was re-derived twice, independently: once by the lead
  verifier with a hand-written CommonMark enumerator with fence tracking, and once
  by an auditor whose enumerator additionally handled `~~~` fences, info-string and
  closing-fence-length rules, and setext headings, and which computed anchors with
  the **real `github-slugger` 2.0.0** rather than any approximation. Both reach
  9 + 1 + 7 + 17 + 13 + 23 + 26 + 8 + 11 + 10 + 9 = **134** headings against **134**
  index rows: one-to-one, zero missing, zero extra, zero duplicate, zero unresolved
  anchors. Robustness was checked too — zero setext, blockquoted, indented, or
  HTML-comment-hidden headings exist, so 134 is complete. Every status is inside
  08's seven-value vocabulary (50 `planned`, 39 `specified`, 31 `vision`, 8
  `implemented`, 6 `verified`; `blocked` and `deprecated` unused, which is
  permitted) and every row carries at least one valid audience tag.
- **The source-lock mechanism** was reimplemented from scratch — subtree extraction
  plus sorted-key, NUL-delimited, normalized-section hashing — and reproduces both
  recorded digests exactly (`ceb1d6b8…` over 27 links, `1f6e1a73…` over 42). Its
  sensitivity was proved on a mirror: changing one word inside a cited
  `01-principles.md` subtree flips **both** editions to `STALE`.
- **`format:check`** is AC7's own gate, and its one silent axis is coverage, so
  coverage was measured rather than assumed. Recomputed through Prettier's own API
  at the pinned 3.9.6 over `git ls-files --cached --others --exclude-standard`:
  **151 files walked = 66 ignored + 9 unsupported + 76 formatted + 0 unformatted**.
  Every ignored path maps to a category the receipt documents. Semantic inertness
  was proved by re-printing each `HEAD` blob through the pinned formatter and
  comparing bytes: **41 files are exactly a pure re-print of `HEAD`**, including
  every `packages/` source and test file and every corpus harness module.
- **`scripts/resume.mjs`** allocated this report's own path and is modified by this
  diff. Its transition semantics were exercised in isolated fixture repositories
  rather than inferred from the live projection, and the control log was verified
  through git object bytes rather than through the script. See §AC19 and
  §Self-referential instruments.

Two further repository-wide checks:

- **Clean-room.** A token-normalized 8-gram detector over all ten local intake notes
  (2,533 tokens, 2,407 distinct shingles) against all 91 changed or added text files
  found exactly **four** shared runs, all of the same phrase — the operator's own
  coined slogan _"give an agent a build, not your biography"_ — in `00-vision.md`,
  the ledger, `VER-002.md`, and the work order. **Zero land in `docs/publication/`.**
  The direction of copying is provably repository→note, not note→repository: the
  slogan was already committed at the base revision. No forbidden term, employer
  identifier, internal hostname, credential, email, username, or absolute personal
  path appears anywhere in the new surface.
- **Link integrity.** All **377** relative Markdown links and heading anchors across
  all 94 repository Markdown files resolve. The one apparent miss is a false
  positive: a verbatim index row quoted inside a ```` ```text ```` fence in the
  immutable `VER-002.md`. Separately, all **246** links across the seven
  `docs/publication/` artifacts resolve, and every edition link also resolves at the
  declared base revision `f2a4b232`.

**Patch classification holds, proved rather than asserted.** `package-lock.json`
adds exactly one package — `prettier@3.9.6`, `dev: true`, with `resolved` and
`integrity` — and removes none (17 insertions, 0 deletions). `package.json` adds
three scripts and extends `test`; the only dependency line added is the exact
Prettier pin. No `rxjs`, `langgraph`, `langchain`, or container dependency,
Dockerfile, compose file, image reference, runner, environment provider, or runtime
schema exists anywhere in the tree. No version, export, or `files` field changed.
`v0.2.3` remains correct.

## Status of the five VER-002 findings

**Finding 1 (material) — the `verified` index row.** Repaired.
`docs/publication/audience-status-index.md:38` now reads `implemented`. The change
is provably single-valued: comparing checkpoint 9's file with the current one after
normalizing table padding shows **exactly one differing cell**, all 134 link targets
identical, and the preamble and section headers byte-identical. The remaining
diff is column re-padding on the eight rows of that one table (`verified` is 8
characters, `implemented` is 11), which keeps the file Prettier-clean.

`implemented` is the correct landing point under the index's own rule and the
ladder the index actually uses. The index preamble requires "the least mature status
that avoids presenting the whole section as more complete than its material claims";
08 rule 2 requires the seven labels stay visibly different. In this index, `verified`
is reserved for sections whose claim carries a passing numbered verification —
the `v0.2.0`, `v0.2.1`, and `v0.2.2` rungs all have passing `VER` plus `FINAL`
artifacts plus a cut annotated tag. `implemented` is what the ladder assigns to
`v0.1.0 → WO-002`, whose `VER-002` reads "does not pass" and which has no final
review. The kernel-loop section sits in exactly that position: type surface complete
and executably pinned (`packages/kernel/test/ac7-readme-map.test.ts:346-383` asserts
the exact 11 Program and 14 Cadence namespace members), evaluation partial. The
deferral facts were re-derived first-hand — `packages/kernel/src/core.ts` handles 6
of 14 Cadence kinds with a default throw at `:101`, and 6 of 11 Program kinds with a
default throw at `:196` — and `corpus/harness/wo101-deferral-pins.test.mjs:16-43`
pins exactly the five deferred Program kinds by exact message.

The full sweep `VER-002` directed was also performed: all 14 non-`{vision,
specified, planned}` rows were checked against repository state, and the other 13
hold. One candidate — the `verified` on 08 §Release-note edition — was raised at
material severity and **refuted 3–0**; see Observations.

**Finding 2 (material) — three false statements plus a fourth scoping item.** All
four repaired, each verified against the tree it describes rather than accepted:

- `WO-006:627-629` now says "**Section 7** of the staleness demonstration records
  the final stored locks." TRUE: `--print-locks` reports `ceb1d6b8…` and `1f6e1a73…`,
  and those two values appear in §7 (`staleness-demonstration.md:252-253`), not §6
  (`bdd5c35b…`/`5d85534b…`).
- `staleness-demonstration.md:8` now says "Sections **1–6** preserve earlier
  same-day captures." TRUE: reading §1–§7, the recorded lock values in §2, §4, §5
  and §6 are all superseded; only §7's are current.
- `06-roadmap.md:539-540` now says "**thirteen** bounded same-release additions,
  fully enumerated in WO-006." TRUE and complete: the work order's Authorized
  expansions paragraph enumerates exactly thirteen, and the roadmap's compressed
  list maps one-to-one onto them.
- `WO-006:289-293` now scopes the resume-change bullet — "**At the time of this
  dated receipt**, the resume change altered only degradation guidance …; the later
  required repair-continuation correction adds the bounded reopen path described by
  AC19." TRUE, and it now discloses rather than denies the AC19 transition change.

The roadmap edit did not disturb its `<!-- prettier-ignore -->` heading anchors: all
twelve ignore comments still immediately precede their headings, and the repo-wide
link check confirms every roadmap anchor still resolves.

**Finding 3 (minor) — the repair-reopen fixture's missing assertion.** Repaired, and
the repair was proved effective by mutation testing rather than by reading. The six
added lines at `scripts/test-resume.sh:66-71` assert (i) `verify` refused while the
phase is `repairing`, (ii) the refusal names `npm run resume -- repair-complete`,
(iii) the refused call appended no event, and (iv) `sourceFindingId` and
`sourceReportPath` on the reopen `RepairRequested`. In an isolated mirror with
`resume.mjs` proved byte-identical, eleven mutants were seeded. The kill matrix,
repaired suite versus the pre-repair suite extracted from checkpoint 9:

| Mutant                                                | repaired | pre-repair |
| ----------------------------------------------------- | -------- | ---------- |
| weaken the `verify` guard to accept `repairing`        | KILLED   | survived   |
| drop `sourceFindingId`                                 | KILLED   | survived   |
| drop `sourceReportPath`                                | KILLED   | survived   |
| wrong `sourceReportPath`                               | KILLED   | survived   |
| refused `verify` still appends an event                | KILLED   | survived   |
| refusal names the wrong corrective command             | KILLED   | survived   |
| five further AC19 mutants (guard removal, fix without a failure source, verify appends before its guard, RepairRequested without repair-complete, fix allocates a report) | KILLED | KILLED |

The three mutants `VER-002`'s refuters used to demonstrate the gap are now all
killed. One residual mutant survives and is recorded as finding 3 below.

**Finding 4 (minor) — the inverted work-order-map cell.** Repaired.
`work-order-map.md:72` now reads "**red** baseline is an execution refusal
condition", matching `WO-108-mutation-probe.md:115-117` ("the mutator refuses to
start unless the unmutated suite passes first"). The WO-006 cell's mislabelled
lifecycle answer is also fixed: its Planning-disposition cell now reads
"**lifecycle:** follow the control projection" rather than filing two
workflow-legal transitions under "eligibility". A cell-by-cell comparison of the
whole catalog table against checkpoint 9 shows exactly five changed cells, and an
audit of every remaining row against its source work order, verification artifact,
final review, and tag message found all cells faithful.

**Finding 5 (minor) — the undisclosed lifecycle bootstrap.** Repaired.
`WO-006:638-648` now states: "This was a dogfood bootstrap: the bounded reopen code
and much of the correction were already present in the working tree before the
second `RepairRequested` event made use of that new path; checkpoint 5 lacked the
reopen clause and checkpoint 6 contained it." Verified first-hand: checkpoint 5's
`case "fix":` reads `requirePhase(state, "needs-fix")` with no reopen clause, and
checkpoint 6's carries the bounded `ready-to-verify` + failure-source condition.
Because `checkpoint()` runs before `append()` (`scripts/resume.mjs:287`),
checkpoint 6's snapshot proves the code existed before the event that used it.

## Findings

All four are minor. None blocks final review.

### 1 — Minor. The kernel-loop section still states its full grammars with no deferral note at the point of claim

Now that `audience-status-index.md:38` reads `implemented`, a reader following that
row lands on `docs/product/02-domain-model.md` §Events and decisions, which presents
the 14-kind Cadence AST (`:25`) and the 11-node Program grammar (`:30-31`) with no
statement that 8 of those 14 and 5 of those 11 throw `evaluation is deferred`.
`grep -n 'defer' docs/product/02-domain-model.md` returns only `:44` (a different
subject — the general expression AST) and `:88` (ProductSuggestion lifecycle). The
disclosure exists and is accurate, but one hop away and unlinked:
`packages/kernel/README.md:27-32` and `06-roadmap.md` §v0.1.0. Both editions link
this section (everyday once, engineer twice), and the row carries the
`everyday-ai-user` tag.

**Why this is minor, not a repair failure.** `VER-002` itself offered these as
alternatives — "relabel the row … **or** add the missing deferral disclosure to the
section and justify `verified` explicitly". The repair took the first branch, which
discharges the finding as written. A refuter also established, correctly, that 08
rule 3 binds *editions*, and 08 §Source model casts the product blueprint as an
input rather than an edition, so the rule does not directly reach this file. The
label is now honest; the limitation is simply not co-located with the claim it
qualifies.

**Suggested hardening.** One sentence in §Events and decisions naming which six
Cadence kinds and which six Program kinds are evaluable at kernel 0.1.0, with the
rest exported as types only. It is a lock input for both editions, so
`npm run publication:check` must be re-run and both locks refreshed afterwards.

### 2 — Minor. The work-order map's lifecycle guidance is one transition behind the log it defers to

The repair rewrote the map's "Now and after this close" block and the WO-006 catalog
row to advance them one lifecycle step, but authored that text before the
transitions it anticipates were recorded. `work-order-map.md:20-23` reads "After
green executor evidence and `repair-complete`, independent re-verification becomes
legal. **Recommended next:** dispatch an independent verifier to append VER-003",
and the row's Activation-preflight cell (`:60`) reads "already active; finish
bounded repair evidence and record `repair-complete`". Both actions are done —
`resume.jsonl:50-51` records `RepairCompleted` (checkpoint 11) and
`VerificationRequested(VER-003)` (checkpoint 12), and `current.md` reads
"Phase: verifying … Legal next actions: verification-result".

This is structurally the same staleness pattern the repair was correcting, now
re-introduced one step later. Two mitigations keep it minor and one refuter refuted
it outright on authority: AC13 imposes no phase-currency duty; the map is explicitly
dated and labelled provisional and instructs the reader to revalidate before
activation; and the same row already carries the staleness-proof answer,
"lifecycle: follow the control projection". Any text that names the next transition
goes stale the moment that transition fires — including this report.

**Suggested hardening.** Make the block transition-agnostic rather than
step-numbered: drop the "dispatch an independent verifier to append VER-003"
sentence and the row's "finish bounded repair evidence and record `repair-complete`"
instruction, and let the already-present "the current control projection names the
legal transition for its exact phase" carry the answer.

### 3 — Minor. One AC19 mutant still survives the repaired fixture

AC19 requires the reopen to append `RepairRequested` "against that same source". The
two new JSONL assertions pin `sourceFindingId` and `sourceReportPath` at a point in
the fixture where the preserved failure source happens to *equal* the latest
verification (both `VER-002`), so they do not exercise the clause that makes them
different. Reproduced in an isolated mirror with `resume.mjs` proved byte-identical:
replacing `scripts/resume.mjs:419-420`'s `state.failureSourceId` /
`state.failureSourcePath` with `state.latestVerificationId` /
`state.latestVerificationPath` leaves the suite passing (`resume tests passed`,
exit 0). That mutant is a real defect — it would record a *passed* verification as
the repair source whenever the failure source is a final review, since a final
review is only reachable from phase `verified`. The fixture's only final-review-source
coverage (`scripts/test-resume.sh:83`) greps stdout, which is rendered from
`state.failureSourcePath` (`resume.mjs:422`) and is therefore unaffected.

The shipped code is correct; only the fixture's discrimination is incomplete. This
is the same class as `VER-002` finding 3 — one step further in.

**Suggested hardening.** Extend the reopen coverage to the final-review branch,
where the failure source differs from the latest verification: after
`scripts/test-resume.sh:83`, add a `repair-complete` plus reopened `fix`, then
assert `'"sourceFindingId":"FINAL-002"'` and the matching `sourceReportPath` on the
appended event. One added reopen kills the mutant.

### 4 — Minor. Two of the ten ideation breakout receipts omit the Unresolved-choices field

`docs/product/07-execution-guide.md:156-158` pins the receipt contract: a receipt
"names the raw intake batch, ledger entries, product/decision/schema surfaces
changed, **unresolved choices**, and required review." Eight of the ten ideation
breakout receipts carry an explicit **Unresolved choices** bullet. Two do not — the
Team Topologies research receipt (`WO-006:435`) and the Operator-flow mission
receipt (`:453`). (The Claude startup correction receipt and the Final repair
evidence receipt are not ideation breakout receipts, so the field does not bind
them.)

Both compensate partially in prose — Team Topologies states the whole nomination is
unmined and unallocated; Operator-flow carries a **Limits** bullet — so no claim is
false, and AC6's substance is met: this verifier did receive the complete expanded
subject and was able to score AC14 and AC15 on it. The gap is uniformity against a
written field list at the two receipts where the open questions arguably matter
most.

**Suggested hardening.** One bullet each: for Team Topologies, the copy/provenance
route, which transfers are testable for disposable agents, and the work-order
allocation; for Operator-flow, whether and how interruption cost is measured without
inferring the operator's state.

## Observations — raised, refuted, and recorded anyway

Thirteen candidates were refuted by the adversarial panels. The lead verifier agrees
with the refutations and does **not** treat these as findings, but records the ones a
final reviewer will meet on the same surfaces.

- **The second `verified` row was challenged at material severity and refuted 3–0.**
  `audience-status-index.md:174` labels 08 §Release-note edition `verified`, and an
  auditor argued the live generator honours neither 08's "changed behavior, not
  commit trivia" item nor its "Progressive polish" aggregate. The refutations are
  decisive and I accept them: `VER-002` performed exactly this sweep and cleared the
  row ("The other six `verified` rows check out", `VER-002.md:354`); the row and
  `scripts/release.mjs` are both byte-identical since that adjudication, and
  `release.mjs` is a pure Prettier re-print of `HEAD` — pre-existing behaviour
  WO-006 neither authored nor changed, already covered by the passing WO-004 and
  WO-012 verifications; the "polish" clause is conditional on polish-class churn
  that `v0.2.2` did not contain; and one repro step mis-transcribed a line number
  (486 for 490). Relitigating a prior immutable adjudication on unchanged evidence
  is not warranted. Recorded because the surface looks alarming next to finding 1's
  subject and the next reviewer will hit it.
- **The eight deferred Cadence kinds have no executable deferral pin**, unlike the
  five deferred Program kinds that `wo101-deferral-pins.test.mjs` pins by exact
  message. Silently adding, removing, or renaming a Cadence deferral would fail no
  suite. The evidence base under the `implemented` label is asymmetric. A natural
  candidate for WO-102.
- **Eleven roadmap heading anchors depend on a render-invisible double space** held
  in place only by adjacent `<!-- prettier-ignore -->` comments. Running the repo's
  own Prettier on such a heading collapses it and silently rewrites the anchor. Those
  anchors back eleven index rows plus a link both editions use. The guard works
  today; it is one deleted comment from breaking two locks.
- **No executable assertion pins the immutable numbered records against the
  formatter.** With `--ignore-path /dev/null`, Prettier reports style issues in
  `VER-001.md`, so `.prettierignore:22` is load-bearing rather than incidental, and
  nothing in the six suites asserts that `npm run format` leaves
  `docs/verifications/WO-*/` byte-identical. Refuted as unpinned to any criterion;
  cheap hardening.
- **The advertised checkpoint-restore command can orphan a numbered record.**
  `checkpoint()` runs before `append()`, so the checkpoint an event names never
  contains that event; `git checkout <ref> -- .` therefore reverts `resume.jsonl`
  past the restoring transition while the `VER-NNN` file survives on disk. Reproduced
  in a fixture. Refuted as unpinned — the projection's own text and the execution
  guide both frame the checkpoint as pre-transition — but a final reviewer should
  know the affordance behaves this way.
- **`RepairCompleted` records `sourceVerificationId` from the latest verification**
  unconditionally (`resume.mjs:429`), so a final-review-sourced repair pairs
  `RepairRequested{FINAL-001}` with `RepairCompleted{VER-002}`. AC19 says nothing
  about that payload and nothing reads the field.
- **Three prose imprecisions in delivered documents, each true in substance.** The
  repaired discontinuity note's "their recorded lock values" is true of §2, §4, §5
  and §6 but not §1 and §3, which record no lock. `WO-006:627-629` says §7 records
  "the exact inverse patch" where §7 states the patch's *effect* and the patch text
  lives in §2. `00-vision.md:269` names a paragraph by a phrase that paragraph does
  not use. None was raised as a finding; all are one-word fixes if touched.
- **`packages/skeleton/README.md` carries one non-formatter prose edit** inside what
  a receipt calls "the formatter-authored repository diff" — a hyphen-broken
  `live-versus-` / `replayed` joined by hand. Refuted: no criterion requires a
  pure-formatter diff, and `VER-002`'s scoped claim ("every `packages/` **source and
  test** file") remains true.
- **`docs/product/02-domain-model.md` required 18 hand-inserted pipe escapes**
  before the reformat, without which Prettier reshapes a normative table row. The
  Formatting-exclusions receipt names only the roadmap ignore comments as a manual
  safety measure. Refuted as unpinned to any criterion.
- **`.prettierignore` excludes future work orders** (`WO-007`…`WO-011`,
  `WO-102`…`WO-108`) under an "immutable evidence" rationale they do not yet satisfy.
  Benign today; each future order will need its own re-inclusion the way WO-006 has
  one.
- **The software-engineer edition carries no self-lossy label** where
  `base-outline.md:89` and `:103` require every edition to label itself a lossy
  projection, and the everyday edition does (`everyday-ai-user-toc.md:14`). It
  changes no fact, limitation, permission, or evidence, so AC2 is met.
- **Two carried-forward `VER-002` observations still stand.** The Team Topologies
  receipt names a work-order-map "row" that is a prose paragraph; and
  `CompatibilityPlan` at `10-ir-compatibility.md:137` is a notation mismatch with
  02's defined "Compatibility plan" term rather than the dangling reference
  `VER-002` described. Both refuted, both harmless.
- **`VER-002`'s refuted fun-loop challenge was re-verified from scratch, not
  deferred to.** The ledger entry `Operator-in-the-fun-loop` (`idea-ledger.md:1103`)
  has its adopted commitment in the clause after the em dash, and both clauses
  survive verbatim in the working tree. No ledger duty is triggered. I reach the same
  conclusion `VER-002`'s panel did, independently.

## Acceptance criteria

| #   | Criterion                                                                                                                       | Result                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Every product section indexed with audience tags and a status label                                                             | **Pass.** 134 headings enumerated twice independently (once with the real `github-slugger`); 134 index rows; one-to-one, zero missing, extra, duplicate, or unresolved. All tags valid, all statuses inside 08's vocabulary. The `verified` row `VER-002` blocked on now reads `implemented`; finding 1 is a co-located-disclosure note, not a label defect. |
| 2   | Two editions differ sharply, cite shared sources, differ in no fact/limitation/permission/evidence                              | **Pass.** Divergence quantified: 12 chapters in 7 Parts vs 17 in 8 Modules, 27 vs 42 source anchors, prose vocabulary Jaccard 0.233, shared-source order Spearman 0.534. Every FACT/LIMITATION/PERMISSION/EVIDENCE claim compared: no divergence. All 246 publication links resolve, and every edition link also resolves at the base revision. |
| 3   | Dual-voice sample, one concept, identical claim links                                                                           | **Pass.** One concept, two voices, exactly three claim links appearing once per voice, byte-identical and in identical order.                                                                                                                                                                                 |
| 4   | Staleness loop demonstrated and captured                                                                                        | **Pass.** Reproduced end-to-end in an isolated mirror; §7's hashes and both stored locks match `--print-locks` byte-exactly, and the lock mechanism was reimplemented independently and proved sensitive. The §6→§7 pointer and the §1–6 discontinuity note are now correct.                                  |
| 5   | No publication artifact derives from, quotes, or links intake                                                                   | **Pass.** 8-gram detector over 2,407 intake shingles against all 91 changed files: four shared runs, all one operator-coined slogan, **zero in `docs/publication/`**, and the copy direction is provably repository→note.                                                                                     |
| 6   | Ideation clean-room rewritten; receipts give the complete expanded subject                                                      | **Pass with note.** All ten intake batches exist; all named ledger entries resolve; the ledger append is 319 insertions / 0 deletions inserted at its prescribed position; every backticked surface a receipt names is in the changed set. Two receipts omit the Unresolved-choices field — finding 4.        |
| 7   | Prettier exact, exclusions documented, everything else formatted, suite green                                                   | **Pass.** Exact `3.9.6` dev pin, 17 lockfile insertions / 0 deletions; ADR-0002 §Amendments strictly appended; 151 files = 66 documented-ignored + 9 unsupported + 76 formatted + 0 unformatted; 41 files proved pure re-prints of `HEAD`. The formatter provably cannot reach the control log, the projection, or any numbered record. |
| 8   | Harness runbook accurate, sanitized, dated, enable/verify/undo, asymmetry, checkpoint warning                                   | **Pass on the assessable half; second half operator-deferred.** The runbook inventories exactly the two live harnesses, separates personal settings from repository enforcement, dates its snapshot, gives enable/verify/undo for both, documents the command and Git-metadata asymmetry, and makes the failed checkpoint understandable without advertising stale data — each claim checked against `scripts/resume.mjs` and the checkpoint fixture. Zero secrets, credentials, private identifiers, or personal paths. The posture/startup clause is deferred — see below. |
| 9   | Malcolm accounting observable-only, disposition separate from enforcement, `unknown`/`participated`, collection not implemented | **Pass.** Every clause satisfied across 05, 09, and the ledger; the two axes carry disjoint category lists stated identically on all three surfaces; no collector, schema, or runtime claim exists.                                                                                                          |
| 10  | External rule-source import stays a candidate compatibility-planning surface                                                    | **Pass.** Provenance preserved, all six mapping states visible, three implementation paths with explicit no-auto-selection, suggestion separated from selection authority, all six public-rule qualifiers present, private semantics framed as hypothesis-to-test, and "seamless" explicitly demoted to an evidence claim. |
| 11  | Claude settings fault sequence recorded without shifting failure onto the operator                                              | **Pass.** All four clauses recorded and the attribution is fair: the operator's disclosed moving state, the verifier's correct restraint, and the repair pass's own failure are each stated plainly. ADR-0005 supersedes ADR-0004 Decision 4 only; ADR-0004 partially supersedes ADR-0003 Decision 2 only; all three are new files. |
| 12  | Embodied Explorer stays a bounded post-1.0 candidate                                                                            | **Pass.** Embodiment truth, learned capability belief, and Identity stay separate; the physical-safety boundary is explicit; no actor kind, role schema, simulator, controller, or adapter exists in code.                                                                                                   |
| 13  | Work-order navigation separates legal / eligible / recommended; IDs opaque; claims evidence-derived                             | **Pass with note.** The three answers stay distinct and separately labelled; no ID renumbered; every evidence-state cell audited against its numbered artifact and tag message. The inverted WO-108 cell and the mislabelled WO-006 answer are both fixed. Finding 2 is lifecycle staleness, not a category collapse. |
| 14  | Team Topologies appears only as a provenance-aware research nomination                                                          | **Pass.** Every committed mention is a nomination or a description of one; no book-derived vocabulary leaked (a targeted grep for the source's taxonomy returns only two unattributed generic uses of "cognitive load"); no work-order number, slot, or release promise.                                       |
| 15  | Mission states dependable workflow as a means to raise the chance of operator flow                                              | **Pass.** The outcome/means hierarchy survives every projection; nothing promises, detects, measures, or certifies the state; no interface metric is labelled proof of flow. The fun-loop ledger challenge was re-verified from scratch and does not hold.                                                    |
| 16  | Continuity-critical flow is a candidate with binary `conducting \| quenched` semantics and no severity score                    | **Pass.** Conditions OR into the phase without ranking; typography is explicitly not the event; the recalled nine are explicitly not canonical; recovery is evidence-bound and claims nothing about the operator's mood.                                                                                     |
| 17  | Flow Steward stays bypassable, revocable, and authority-preserving                                                              | **Pass.** Cannot become an approval gate, global manager, surveillance layer, objective owner, guard remover, self-assigner, or self-verifier; the public source transfers and the rejected stereotypes and psychological claims are both explicit.                                                            |
| 18  | Temporal interpretation covers both actors; `expand()` stays a bounded hypothesis                                               | **Pass.** Common machinery over both actors without equating their authority; attachment correctable and source-linked; counts and windows rule-specific; _Outliers_ explicitly unmined; `expand()` bounded with its behaviours left to a fixture; no `rxjs` in `package.json` or the lockfile.               |
| 19  | `resume: fix` reopens only against a preserved failure source; refuses initial ready state; allocates no report                 | **Pass.** Every clause exercised first-hand in isolated fixtures, and the guard read in source. The fixture now kills all six mutants `VER-002` named or implied; one further mutant survives — finding 3.                                                                                                    |
| 20  | Brain/hands, harness, orchestration, transport, environment, attention, authority, evidence stay distinct                       | **Pass.** All nine layers stay distinct under a five-row responsibility table; LangGraph stays a lowering candidate; containers stay an attested probe; worktrees are never called containment; the WoW comparison stays an analogy, never a market-causation claim; no dependency, runner, image, provider, schema, or deployment choice was added. |

## Operator-deferred axes — recorded, not scored

The work order records, at `WO-006:68-74`:

> The operator directed this repair not to inspect, mutate, or reverify personal
> Accept Edits or checkout-local allow entries and will use automode for now. Any
> reconciliation of the current personal posture, startup evidence, and the
> ADR/runbook claims belongs to a separately activated dedicated work order.
> VER-003 must preserve VER-002's factual disclosure but score none of those
> deferred axes in this repair.

This verification complied. Neither the lead verifier nor any auditor read
`~/.claude/settings.json` or any `.claude/settings.local.json`, ran `claude config`,
`claude doctor`, or any settings readback, or counted allow entries. The following
are recorded so the disclosure survives into the record, and are **scored neither
way**:

1. **AC8's second sentence** — "Claude's effective baseline is persistent
   `acceptEdits` … and zero checkout-local allow entries; targeted readback **and** a
   fresh-session startup check must both support that claim" — is not assessed here.
   `VER-002` found six of seven readback axes matching exactly and the seventh
   (checkout-local allow count) diverging at thirteen, which a `VER-002` refuter
   established was that verification's own subagent-fan-out footprint rather than a
   surviving repair defect. `VER-002` also recorded the fresh-session startup half
   as **not assessable** non-interactively, on two independent attempts.
2. **`docs/AI-HARNESS-SECURITY.md` §Current posture** still presents its
   "no checkout-local allow entries" row as a present-tense invariant rather than a
   dated observation, which `VER-002` advised a future pass should soften. The prose
   below the table does timestamp its evidence to the post-correction check on
   2026-09-01. The repair correctly did not touch this file.
3. **Two receipts** — the Claude startup correction receipt's Executor-evidence
   bullet (`WO-006:376-384`) and the Final repair evidence receipt's Harness gate
   (`:629-637`) — still assert "zero allow entries" and a witnessed
   `accept edits on` startup. The repair correctly left them untouched.
4. **ADR-0003 and ADR-0004 carry no backward supersession marker**; read in
   isolation, ADR-0004 Decision 4 still reads as current and ADR-0003 still lists
   auto-allow as a rejected alternative. `VER-001` authorised "an Amendments entry
   **or** a superseding record" and the repair took the second route, so AC11 is
   satisfied; the readability gap belongs to the same dedicated work order.

I confirmed by tree diff that the repair touched none of the surfaces these axes
govern — `git diff refs/dotln/checkpoint/WO-006/9^{tree} <worktree> -- docs/decisions/
docs/AI-HARNESS-SECURITY.md docs/README.md docs/PLAYBOOK.md scripts/resume.mjs
scripts/test-checkpoint.sh` is empty — so `VER-002`'s factual disclosure on these
axes stands unaltered and unrebutted in the record.

## Scope, classification, and clean-room

Scope discipline holds against every named prohibition. No `Claim` IR and no
`PublicationPlan` type was promoted: the only `PublicationPlan` occurrences outside
the prohibition itself and the two prior reports are `08-publication-compiler.md:90`
and `idea-ledger.md:969`, **both pre-existing at `HEAD`** and both framed
conditionally ("If repeatability or traceability outgrows Markdown, promote only…").
08's exact seven-value status vocabulary is used with zero out-of-vocabulary labels.
No manager, IT-director, VP, or CFO edition exists — `docs/publication/` holds
exactly the seven authorized artifacts. No renderer, site, or printable output; and
nothing was published anywhere. `scripts/check-publication.mjs` is a read-only
checker that emits no artifact — the cheapest sufficient mechanism the scope rule
authorises, not the pipeline it forbids — and it is not vacuous: deleting one
indexed row yields `FAIL … 1 missing` and exit 1, mutating a product source yields
`STALE … FAIL 1 edition(s) are stale` and exit 1, and depriving it of its git
baseline makes it refuse rather than report `CURRENT`.

Write-back duty is discharged: `docs/README.md` maps `docs/publication/`,
`docs/AI-HARNESS-SECURITY.md`, `docs/planning/` including the provisional work-order
map, and `docs/decisions/`; the roadmap's `v0.2.3` entry points at WO-006; the ledger
append landed at its prescribed position with 319 insertions and zero deletions; and
the config log in `docs/README.md` carries a line for each `CLAUDE.md` and settings
change.

## Self-referential instruments — mandatory disclosure

Three instruments this verification relied on are themselves WO-006 deliverables
under verification. Per the execution guide, each is disclosed and was verified
independently rather than trusted.

- **`scripts/resume.mjs`** allocated this report's path, appended every event
  audited, and generated the projection checked. Its bytes changed **twice during
  the WO-006 lifecycle**: SHA-256 prefix `f13ba02fd2db` at checkpoint 1 (the
  `HEAD`/WO-005 version), `2530108a5a3e` at checkpoints 2–5, and `8ed93057c8f4` from
  checkpoint 6 onward, which is the current working tree. The AC19 reopen event was
  produced by code first appearing at checkpoint 6. It fails loudly: illegal
  transitions throw, append nothing, and mint no ref, which
  `scripts/test-checkpoint.sh:21-23` asserts. Dependence on it was neutralized by
  re-folding the state machine independently and by verifying append-only through
  git object bytes rather than through the script. This is the safe-to-dogfood shape
  the guide describes — corruption would have shown rather than producing a plausible
  record, and none did.
- **`scripts/check-publication.mjs`** is absent at `HEAD` and wholly new. It fails
  loudly and is demonstrably not vacuous (see §Scope). Both its headline numbers were
  reproduced by separate parsers and a separate slugger, and its lock mechanism was
  reimplemented from scratch and proved sensitive.
- **The Prettier `format:check` gate** is AC7's own guard. It fails loudly on style,
  but its coverage axis is silent, so coverage was measured directly rather than
  inferred, and every ignored path was mapped to a documented category.

None of the three falls in the narrow abstention case the guide contemplates — where
the instrument's correctness is the question *and* its failure would be silent. Each
was still checked by a mechanism that is not itself the instrument.

## Required next actions

1. `npm run resume -- verification-result pass`.
2. `resume: final review` — the legal next actor. The final reviewer reads
   `WO-006`, then `VER-001`, `VER-002`, and this report in order.
3. The four minor findings are recommended hardening, not repair conditions. If the
   operator prefers them closed before final review, `resume: fix` remains available
   against this report as the failure source; nothing in them touches a settled
   decision body, a numbered immutable record, or shipped behaviour.
4. The deferred harness/posture reconciliation needs its own activated work order,
   as `WO-006:68-74` directs. It should carry the four items in §Operator-deferred
   axes as its subject.

## Open questions and caveats

- The six shell suites ran against a relocated temp root, as disclosed in the
  evidence gate. Exactly two lines per suite differ and all code under test was
  proved byte-identical, but the literal `/private/tmp` path choice is not exercised.
  An unmodified `npm test` by the operator would close the residual gap.
- AC8's fresh-session startup half and the checkout-local allow count are
  operator-deferred, not merely unassessed. `VER-002`'s factual disclosure on both is
  preserved above and scored neither way.
- `docs/intake/` was read only to run the verbatim-copy detector for AC5. No intake
  content is reproduced in this report.
- Three of the seventeen adversarial refuter calls failed with an API error rather
  than returning a verdict, leaving two-vote panels on three findings. Two of those
  three findings survived on a 1-of-2 majority (findings 1 and 3 above); the lead
  verifier independently re-adjudicated both, accepted the surviving severity as
  minor, and states here that neither was promoted on panel arithmetic alone.
- The work order carries `Model: any capable model`, so no model constraint was
  violated. This verification ran on **Claude Opus 5 (1M context) at `ultracode`
  effort**, in Claude Code 2.1.x with sandboxed Bash. The work order's `Model:` line
  pins no effort field and no executable check compares configured to documented
  effort; that gap is recorded here rather than assumed away.
- This report is the third in the sequence and does not edit, replace, or supersede
  `VER-001` or `VER-002`, both of which remain byte-frozen and were proved so above.
