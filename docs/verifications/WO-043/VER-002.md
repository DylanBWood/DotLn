# WO-043 verification VER-002

**Verdict: fail.** Every original acceptance criterion holds, and VER-001 F1 is
repaired. The actual Codex and Claude measurements reproduce exactly from their
source transcripts. Four findings need repair:

1. **F2 (low):** the `scope expand:` and `conversation only:` contract is not
   in the Contributor's generated instructions, although product 07 says it
   is.
2. **F3 (medium):** the repair reworded the locked, hand-written floor without
   recorded operator authorization.
3. **F4 (low):** 07's cold-start table omits the goal-card read and
   contradicts the refuter skill.
4. **F5 (medium):** WO-043's committed meter understates executor tokens by
   about 44%.

- **Work order:** [WO-043](../../work-orders/WO-043-typed-dependency-truth.md),
  application target `v0.17.1` (patch). The expanded acceptance obligations
  come from its dated 2026-09-11 execution record.
- **Subject:** branch `wo-043` at base `36267f6` with the uncommitted
  implementation and repair (`RepairCompleted` tree `52d01c6`, checkpoint
  `refs/dotln/checkpoint/WO-043/6`, `8575449`). This dispatch's
  `VerificationRequested` event (2026-09-11T16:19:59.282Z, checkpoint
  `refs/dotln/checkpoint/WO-043/7`, `17b93b6`) and the index refresh after it
  changed only control and generated projections.
- **Dispatch:** operator `resume: verify`; `npm run resume -- verify`
  allocated this path.

## Actor

Claude Code `2.1.268`, observed with `claude --version`. The model is
`claude-opus-5[1m]`, the exact model ID the session exposes. Effort is `max`,
the session's selected effort (`CLAUDE_EFFORT=max`). That value is a launch
setting, not a recorded effective-session readback, so the source is
self-reported. This session took no part in the implementation or the repair.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.268","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}

## Method and independence

The following components are all under verification, and they also recorded
this work: the lifecycle script, the index generator, the harness hooks, the
usage collector and the gate runner. They allocated this report, supplied
status, refreshed the index, measured this session's tokens and ran the gate.

To check the measurement claims without relying on the collector, I used inline
read-only Node readers that do not import it. They parse the raw local Claude
and Codex transcripts and apply each source's counting rule. This report
publishes no transcript text or session identifier.

## Original acceptance criteria

### 1–5. Parser, projection, index, status and activation, migration — hold

None of the files behind these criteria changed between VER-001's subject
(checkpoint 3) and repair completion (checkpoint 6):

- `scripts/lib/dependencies.mjs`
- `scripts/work-orders.mjs`
- `scripts/test-work-orders.mjs`
- `scripts/test-dependency-resume.mjs`
- `scripts/lib/plan-dependency-migration.mjs`
- every migrated work-order file

`scripts/resume.mjs` changed only to append the executor entry briefing to
`next` and `fix`, so VER-001's reproductions still apply. The gate's `index`,
`resume`, `work-orders-fixtures`, `plan-refutation`, `release`, `worktree` and
`console` suites passed again. This dispatch's `status --json` projects typed
dependencies for WO-043: WO-026 and WO-030 are `met`, and nothing blocks.

### 6. Write-backs — hold; VER-001 F1 is repaired

`docs/publication/audience-status-index.md` again marks "Work-order navigation
and identity (candidate)" as `planned`. Its diff from `HEAD` no longer touches
that row (D005). Both edition source locks were refreshed. The new
"Goal-aligned decisions" row uses the conservative `specified`. The gate's
`publication` suite passed.

### 7. Gate and fences — hold

`npm run harness -- evidence` exited 0 on tree `ddcc246`:

- `npm run test:full` passed all 37 suites in 632.73 s, with 78 fresh and 0
  reused tasks (recorded 2026-09-11T16:35:02Z);
- `git diff --check` passed.

`package.json` changes only the edition literal in the `evidence:feedback`
script. The lockfile changes only workspace versions, so no dependency was
added. The package changes stay inside the set the execution record
authorizes:

- the skeleton's Contributor loadout and supports, harness host, usage
  collector, evidence-edition resolver, transport version list and tests;
- the compiler's role-set adapter and version constant;
- the console's collector, README and fixtures.

The only control changes are `docs/control/orders/WO-043.jsonl` and
`docs/control/current.md`. Outside `packages/`, the repair also rewrote the
locked floor; see F3.

## Expanded obligations (2026-09-11 execution record)

### Command prefixes — partly met (F2)

Product 07 documents both prefixes as first-party commands that create no
lifecycle event:

- `conversation only:` answers without changing scope, phase or obligations;
- `scope expand:` adds the stated work together with its receipt.

The process-debt fixture sends both prompts during an active executor session.
The role, order, expected event, start time and event count all stay
unchanged. The contract is not projected into the generated instructions; see
F2.

### Mandatory token measurement — holds (committed meter: F5)

One collector serves four callers: `harness usage`, the Claude Stop hook,
`meta --collect` and all four completion actions.

- **Session match.** It keys the session by SHA-256. It requires exactly one
  transcript whose name, recorded ID and physical worktree all match.
- **Window.** It counts from the dispatch start.
- **Refusals.** It refuses absent, stale and foreign counters.
- **Failure handling.** A failure at Stop time is advisory, and the writer is
  still released. A completion action refuses outright.

The fixtures cover:

- both transcript formats;
- repeated Claude message IDs;
- a wrong session and a wrong worktree;
- a stale window and missing counters;
- transcript text and identity leakage;
- own-session use on `main`.

I reproduced the actual measurements with the independent readers:

- **Claude, VER-001 session.**
  - The probe window, 14:10:36Z to 14:52:40Z, gives 5,278,902 tokens: 718
    input, 4,846,932 cache reads, 300,168 cache writes and 131,084 output.
    That equals the probe.
  - VER-001 first reported its token usage as null, although the transcript
    already carried these counts. It was corrected after recording and now
    states this figure. That correction is VER-001's only change since its
    completion event.
  - Extending the window to 15:00:22.269Z gives 10,029,006 tokens across 42
    messages, which equals the D011 reconciliation.
  - The session has one ID, one working directory and no sidechain rows.
  - D011's 16,938,893 is the sum of the two latest rows before reconciliation:
    10,029,006 and 6,909,887.
- **Codex, repair session.** Each recorded executor row equals the counter
  delta over its window: 2,436,959; 9,540,897; 23,933,250 (the completion-time
  measurement at 16:15:44.786Z); and 24,580,001. The probe's 3,482,818 also
  reproduces.
- **Codex, implementation session.** The two rows recorded before the repair,
  2,724,493 and 13,319,888, reproduce for their recorded windows. Both windows
  end at or before 06:14:38Z, earlier than `ImplementationReady` (see F5).
- **This session.** The entry measurement of 84,820 reproduces.
- **Counting rules.** In all 366 Codex counters, the total equals input plus
  output, because cached input is already counted inside input. No counter
  decreases. Claude totals add cache reads and cache writes to input and
  output.
- **Privacy.** I scanned 1,128 tracked and untracked non-ignored files for the
  four session IDs and their SHA-256 values. I also scanned the 201 changed
  files for home-directory and transcript paths. There were no hits. Hashed
  keys stay in the ignored local usage file, and the meter projection strips
  them.

### Automatic executor supports — hold

Ordinary `next` and `fix` messages now append the installed support duties and
the current queue (`scripts/lib/executor-readiness.mjs`). Equipped fragments
come before the first subject read in the executor procedure, and the support
test asserts that order. `implementation-ready` and `repair-complete` refuse
while an adjacent item is queued or running.

The fixture covers:

- bare `next` and `fix`;
- removal of individual supports;
- `status --json` staying read-only;
- queued and running refusals;
- explicit disposition.

In the live queue, adjacent-0001 and adjacent-0002 were each announced,
checked in and completed before handoff.

### Process Cost and Goal Alignment — hold (07's cold-start table: F4)

Each support is compiled once and projected once into all six roles, in both
harnesses. Each fragment appears exactly once in each of the 12 generated
skills.

- **Adapter.** The compiler's role-procedure facet accepts an explicit
  `roleNames` set alongside the legacy `roleName`. It refuses empty, duplicate,
  unknown and ambiguous targets, and the fixtures exercise all four refusals.
- **Removal.** Removing either support leaves the other support, the authority
  envelope and the work-order contract unchanged. Host measurement does not
  depend on either switch.
- **Goal card.** 07 §Goal-aligned decisions cites product 00, the
  critical-path plan and the selected order. It names the eight traps, Naive
  Interventionism and NoOp, and it puts accuracy over agreement in every phase.
- **Correctness.** The shared-role test asserts that Correctness over
  Sycophancy is present in every generated role.

D009, D010 and D013 record the comparisons.

### Current-evidence selection, transport and versions — hold

- **Selection.** `docs/evidence/current.json` and
  `packages/skeleton/src/evidence-editions.mjs` select the authority,
  artifact-identity, verification and feedback evidence. The four evidence
  commands and the console both use this selection. Neither the gate nor the
  package scripts contains a current work-order literal.
- **Selection safety.** Explicit historical pins still resolve, and a
  malformed or escaping entry is refused. The prior 37-command inventory
  assertion passes.
- **Transport.** The only transport change adds `2.1.268` to the Claude
  version list.
- **Versions.** The application stays at `v0.17.1`. Compiler is `0.9.1`,
  skeleton `0.15.1` and console `0.1.4`; the kernel is unchanged.
- **Records.** Decisions D005–D013 exist, each with its row in the decisions
  index. The Tinkerer request is carried by `FUP-fc4158d3207e495d` and
  product 05, and nothing is implemented for it.

## Findings

### F2 — command contract not in generated instructions (low)

- **Obligation:** the execution record, "2026-09-11 repair and operator
  expansion", says to "project the contract into the Contributor's generated
  Codex and Claude instructions".
- **Observed:** a search for "conversation only" across `.claude`, `.agents`,
  the skeleton and compiler sources, `scripts/lib` and `CLAUDE.md` finds only
  `CLAUDE.md`.
  - The contract sits at lines 42–43, in hand-written text above the
    `dotln-harness` marker.
  - No generated skill, generated marker block or Contributor source contains
    it.
  - Product 07 still says "The contract is carried in the Contributor's
    generated instructions for both harnesses".
  - D006 places the prefixes in "the shared floor and generated role
    instructions". The floor half holds; the generated half does not.
- **Expected:** the Contributor emits the contract into its generated Codex and
  Claude instructions, where the harness check regenerates it. Otherwise, a
  recorded operator decision amends the obligation and the wording in 07 and
  D006.
- **Reproduction:**
  - `grep -rl "conversation only" .claude .agents packages/skeleton/src packages/compiler/src scripts/lib CLAUDE.md`
  - `sed -n 40,50p CLAUDE.md`
  - `grep -n "generated instructions for both harnesses" docs/product/07-execution-guide.md`
- **Impact:** both harnesses load `CLAUDE.md` (`AGENTS.md` is a symlink to
  it), so the prefixes work in every session today. The gap is that the
  contract sits outside the compiled, regenerated Contributor output that the
  obligation names, and 07 claims the opposite.

### F3 — locked floor reworded without recorded authorization (medium)

- **Observed:** `CLAUDE.md` (which `AGENTS.md` symlinks) was unchanged at
  checkpoint 3, so every edit to it is repair work.
  - Beyond adding lines 42–46, the repair reworded the existing Clean Room,
    intake, Shared memory and Start here paragraphs.
  - The file shrank from 2,251 to 2,224 bytes. That keeps Codex
    release-close (floor plus skill) at 6,135 bytes, just under its
    6,144-byte ceiling. The repair receipt says no budget acceptance was
    requested.
  - No execution-record entry or decision authorizes rewording the floor.
    D006 covers only carrying the new prefixes, and the execution record's
    permitted edit set does not include `CLAUDE.md`.
- **Expected:** the floor changes only with explicit operator authorization
  of the exact text.
  - The Contributor declares that "the hand-written Clean Room floor stays
    locked", and the file's own heading names it the locked floor.
  - WO-126 changed a floor sentence only after the operator authorized the
    exact text (WO-126-D002).
- **Substance:** I found no weakened rule. The only loosened phrase is "read
  cited sections only", which became "Read cited sections".
- **Repair:** either restore the existing wording and meet the ceiling another
  way, or record the operator's authorization of the exact new text. The
  ceiling can be met through F2's generated projection or a requested budget
  acceptance.
- **Reproduction:**
  - `git diff -- CLAUDE.md`
  - `git show HEAD:CLAUDE.md | wc -c`
  - `wc -c CLAUDE.md .agents/skills/dotln-release-close/SKILL.md`
  - `grep -n "hand-written Clean Room floor" packages/skeleton/src/loadouts/contributor.ts`

### F4 — 07's cold-start table omits the goal card and contradicts the refuter skill (low)

- **Observed:**
  - All 12 generated skills direct the read "Goal Alignment: Read
    `docs/product/07-execution-guide.md#Goal-aligned decisions`", a 5,280-byte
    section.
  - 07's cold-start table (lines 14–21) has a column headed "All directed
    inputs, including later procedure". That column lists the goal-card read
    for no role.
  - The table's refuter row still says "no map, guide, ledger or
    earlier-receipt prerequisite". The regenerated refuter procedure now says
    "The shared goal card supplies purpose; the canonical prompt is the sole
    subject evidence."
- **Expected:** the table names the goal-card read for every role and states
  the refuter's new input boundary.
- **Context:** the harness-context measurement already counts 07 lines 39–107
  for every measured role, so only the product table is stale.
- **Reproduction:**
  - `sed -n 14,21p docs/product/07-execution-guide.md`
  - `sed -n 9,10p .claude/skills/dotln-refuter/SKILL.md`

### F5 — WO-043's committed meter understates executor tokens (medium)

- **Observed:** `docs/evidence/WO-043/meta.json`, observed at
  2026-09-11T15:40:29Z, gives the executor 22,860,785 tokens. The PR meter
  table generated from it shows the same figure. That total adds two partial
  rows:
  - **Implementation, 13,319,888.** The row covers only 05:49:31Z to
    06:14:38Z. The same session's counters, from that start to
    `ImplementationReady` (14:08:33Z), give 16,773,516.
  - **Repair, 9,540,897.** This is the 15:34:26Z reading. The handoff
    measurement was 23,933,250 at 16:15:44Z, recorded locally after the
    snapshot.
  - By the counters, the two dispatches used 40,706,766 tokens, so the meter
    is about 44% low.
- **Expected:** the committed cost evidence covers each completed dispatch.
  D011's append-only, source-reproduced reconciliation can replace the partial
  implementation row, using the local Codex transcript. `meta.json` can then
  be refreshed after the handoff measurement.
- **Reproduction:** compute counter deltas over those windows in the two local
  Codex transcripts. The `startedAt` and `observedAt` fields of the WO-043
  usage rows in `meta.json` show both windows.
- **Severity:** medium, because this is the token evidence the operator made
  mandatory.

Re-verification can be limited to F2–F5 and the gate.

## Costs and limits

- **Runtime:** the dispatch was recorded at 2026-09-11T16:19:59.282Z. The first
  gate run took 632.7 s, with every task run fresh. Later runs on the report
  tree reuse unchanged suites.
- **Tokens:** measured with `node scripts/harness.mjs usage` (source
  `claude-transcript-message-usage`, scope `dispatch`).
  - At entry: 84,820.
  - At the `verification-result` command (16:56:50.976Z): 8,029,965.
  - For comparison, VER-001's session used 10,029,006 tokens on a narrower
    subject.
- **Cost:** null; the transcript records no price.
- **Goal alignment of the verdict:** failing sends four bounded repairs back
  before final review. Passing would certify an unmet obligation, an
  unauthorized floor edit, a stale product contract and understated cost
  evidence.
- **Not covered:**
  - I did not rerun the live Claude feedback audit. The gate's
    `feedback-evidence` suite checks its recorded evidence.
  - I did not re-derive the 85-order migration, which is byte-identical to
    VER-001's subject.
