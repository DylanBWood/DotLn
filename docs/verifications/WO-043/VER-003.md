# WO-043 verification VER-003

**Verdict: pass.** VER-002's four findings are repaired, VER-001 F1 stays
repaired, and every original acceptance criterion still holds. Every recorded
token figure behind the F5 repair reproduces exactly from its source counters.
The full gate ran fresh and green. The five observations below are
non-blocking; two of them name an owner for later action.

- **Work order:** [WO-043](../../work-orders/WO-043-typed-dependency-truth.md),
  application target `v0.17.1` (patch). The expanded obligations come from its
  dated 2026-09-11 execution record, including the VER-002 repair entry.
- **Subject:** branch `wo-043` at base `36267f6` with the uncommitted
  implementation and its three repairs. The VER-002 repair completed at
  2026-09-11T17:56:10.444Z (`RepairCompleted` tree `f352a8e`, checkpoint
  `refs/dotln/checkpoint/WO-043/10`, `05992af`). The executor then refreshed
  the meter (observed 17:56:24.687Z), the prepared PR table and the index, and
  released the writer at 18:07:51Z. This dispatch's `VerificationRequested`
  event (18:10:10.211Z, checkpoint `refs/dotln/checkpoint/WO-043/11`,
  `159e5dd`) and the index refresh after it changed only control and generated
  projections. The first gate run observed tree `1fa612d`, before this report
  existed.
- **Dispatch:** operator `resume: verify`; `npm run resume -- verify`
  allocated this path.

## Actor

Claude Code `2.1.268`, observed with `claude --version`. The model is
`claude-opus-5[1m]`, the exact model ID the session exposes. Effort is `max`,
the session's selected effort. That value is a launch setting, not a recorded
effective-session readback, so the source is self-reported. This session took
no part in the implementation or any repair.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.268","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}

## Method and independence

The following components are under verification, and they also recorded this
work:

- the lifecycle script allocated this report and supplied status;
- the index generator refreshed the index;
- the harness hooks and writer lease governed this session;
- the usage collector measured this session's tokens;
- the gate runner ran the gate;
- the generated verifier skill directed this procedure.

To check claims without relying on them, I used three instruments of my own:

- **Counter replay.** Two inline read-only Node readers that do not import the
  collector. One replays the cumulative counters in the three local Codex
  transcripts for this worktree (457 counters). The other sums Claude
  per-message usage, de-duplicated by message ID, in the three local Claude
  transcripts. Both print numbers only.
- **Checkpoint comparison.** Commit-to-commit diffs and blob hashes between
  checkpoints 3, 7, 9, 10 and 11. They avoid the untracked-file pitfall that
  the evidence README describes.
- **Build probe.** The freshly built Contributor, compiled in five support
  variants and compared role by role with the 12 generated skills.

The harness refused a helper-script file outside this report, shell loops and
command substitution, so the readers ran as inline scripts. This report
publishes no transcript text, session identifier or private path.

## Prior findings

### VER-001 F1 — still repaired

`docs/publication/audience-status-index.md` still marks "Work-order navigation
and identity (candidate)" as `planned` (line 170). The "Goal-aligned
decisions" row is `specified`.

### F2 — repaired

- **Mechanism.** `packages/skeleton/src/loadouts/contributor.ts` inserts one
  session-command line after each role's first procedure line in the
  generated target (`targetRoles`), before supports are projected.
- **Build probe.** I compiled five programs: the default, no supports, Process
  Cost off, Goal Alignment off, and both off. In each, all six roles carry the
  line exactly once.
- **Generated files.** All 12 generated skills contain every procedure line of
  the default build and match its descriptions. Each contains `scope expand:`
  exactly once. At checkpoint 9, only `CLAUDE.md` contained it; now
  `CLAUDE.md` does not.
- **Test.** The support test asserts the line in four programs and in the
  emitted role files. I did not rerun the test against checkpoint 9, but there
  it could not pass, because no generated file carried the line.
- **Product 07.** Its statement that the Contributor's generated instructions
  carry the contract for both harnesses is now true. The generated line
  compresses 07's table:
  - `scope expand:` adds scope and receipt;
  - `conversation only:` answers without pausing work;
  - effect limits stay, and only an explicit pause or stop interrupts;
  - neither appends an event.

  The omitted nuances, such as quoted examples and ideation capture, stay in
  07.
- **Live use.** During this dispatch, the operator asked one
  `conversation only:` question. I answered it and continued. The order's
  control segment gained no event beyond this dispatch's request. This is a
  self-observation, not independent evidence.

### F3 — repaired

- **Floor.** `git diff --exit-code HEAD -- CLAUDE.md` exits 0: the whole
  hand-written floor, 2,251 bytes, matches `HEAD`. `AGENTS.md` still symlinks
  to it.
- **Ceiling.** `node scripts/harness-context.mjs --check` exits 0. Ten measured
  role files are within their ceilings, and two have no ceiling set. Codex
  release-close (floor plus skill) is 6,132 bytes against its 6,144-byte
  ceiling.
- **Compaction.** The ceiling is met by compacting only the generated
  release-close target; see observation 1.

### F4 — repaired

Product 07's cold-start table now names §Goal-aligned decisions for every role.

- **Executor row.** It limits the reads to `next`/`fix` and keeps
  `status`/`times` as read-only reports. That matches the executor skill's
  first line.
- **Refuter row.** It matches the refuter skill: the goal card supplies
  purpose, and the canonical prompt is the sole subject evidence.

### F5 — repaired

Every figure the repair relies on reproduces exactly from the counters. In all
457 Codex counters, totals never decrease, the total equals input plus output,
and no cache-write tokens appear.

- **Implementation session.** From 05:49:31.124Z to 14:08:32.202Z the counters
  give 16,773,516 tokens: 16,704,221 input (16,188,160 of them cached) and
  69,295 output.
  - No counter falls between that reading and `ImplementationReady` at
    14:08:33.644Z.
  - The superseded partial rows, 2,724,493 and 13,319,888, reproduce. They are
    retained, with two `supersedes` references.
- **First repair.** Counting from 15:02:58.012Z:
  - 23,933,250 at the handoff measurement (16:15:44.786Z);
  - 24,580,001 at 16:16:53.384Z;
  - the intermediate readings, 2,436,959 and 9,540,897, also reproduce.
- **Second repair.**
  - Session start to 17:32:07.860Z gives 267,527.
  - No tokens fall between that point and the harness entry at 17:32:15.900Z.
  - Entry to 17:56:09.195Z gives 8,234,134.
  - The receipt's readings of 113,733, 3,065,882 and 3,333,409 reproduce.
- **Totals.**
  - Completed handoffs: 16,773,516 and 23,933,250 make 40,706,766.
  - With the post-handoff reading: 16,773,516 and 24,580,001 make 41,353,517.
  - The corrected snapshot: 13,319,888 and 9,540,897 make 22,860,785.
- **Committed meter.**
  - Executor: 16,773,516, 24,580,001, 267,527 and 8,234,134 make 49,855,178.
  - Verifier: 10,029,006, 14,390,210, 166,081 and 0 make 24,585,297.
  - The two Claude session figures reproduce from their transcripts: 42 and
    44 unique messages, one session ID each and no sidechain rows. Observation
    3 covers the envelope rows.

Observation 2 describes the boundary each row uses.

## Original acceptance criteria

### 1–5. Parser, projection, index, status and activation, migration — hold

- **Unchanged since VER-001.** I compared checkpoints 3 and 11 over these files
  and found no difference:
  - `scripts/lib/dependencies.mjs`
  - `scripts/work-orders.mjs`
  - `scripts/test-work-orders.mjs`
  - `scripts/test-dependency-resume.mjs`
  - `scripts/lib/plan-dependency-migration.mjs`
  - `scripts/lib/release-records.mjs`
  - the two shell suites
- **Unchanged since VER-002.** Between VER-002's subject (checkpoint 7) and this
  dispatch (checkpoint 11), `scripts/`, the compiler, the console and the
  kernel are unchanged. `docs/work-orders/` changed only in WO-043's execution
  record and the generated index.
- **Status.** This dispatch's `status --json` projects typed dependencies for
  WO-043: WO-026 is met at `v0.5.2`, WO-030 is met at `v0.7.0`, and nothing
  blocks.
- **Gate.** The gate's `index`, `resume`, `work-orders-fixtures`,
  `plan-refutation`, `release`, `worktree` and `console` suites all passed
  fresh.

### 6. Write-backs — hold

The F1 row holds. The repair refreshed the software-engineer source lock, and
the `publication` suite passed. D014 has its decisions-index row and follow-up
record.

### 7. Gate and fences — hold

`npm run harness -- evidence` exited 0 on tree `1fa612d`:

- `npm run test:full` passed all 37 suites in 622.7 s, with 78 fresh and 0
  reused tasks (recorded 2026-09-11T18:26:16Z);
- `git diff --check` passed.

The repair changed no `package.json` and no lockfile. Its package changes,
`contributor.ts` and its test, stay inside the Contributor edits that the
execution record authorizes. The only control changes are WO-043's segment and
`docs/control/current.md`.

## Expanded obligations since VER-002

VER-002 found these obligations sound:

- automatic executor supports;
- the Process Cost and Goal Alignment projection;
- current-evidence selection;
- transport and versions.

The repair touched these surfaces only through:

- the Contributor target and its test;
- the generated skills and manifest;
- authority evidence edition 002, and the selection manifest that points to it.

Each of the 12 skills still carries the Goal Alignment read exactly once. The
`skeleton`, `harness`, `harness-context`, `authority-evidence`, `process-debt`
and `meta` suites all passed fresh.

## Observations (non-blocking)

1. **The release-close compaction keeps its duties, less precisely.** I found
   no dropped release duty, so D014's reopening condition is not met.
   - **Kept:**
     - the cwd and Git-root check in main, and the status read;
     - the three `Read` directives and the exact handoff;
     - the helper after removal or partial publication, and `--dry-run`;
     - refusal reporting, and no forced teardown or repeated transition;
     - every publication prohibition.
   - **Moved:** "Only resume: release close authorizes this" now appears only
     in the skill description. The phrase gate still holds through three
     mechanisms:
     - the prompt hook's exact intent (`.claude/hooks/session.mjs:110`);
     - the floor's dispatch line;
     - lifecycle legality.
   - **Less precise:**
     - The literal `npm run resume -- release-close` became "main's exact
       release-close/worktree-publish handoff". The command is still reachable
       through status legal actions, 07 §Operator resume phrases and the
       reviewer's returned handoff.
     - The helper's named checks became "release prerequisites".
     - "main's" was dropped from `terms.txt` and from the retained lane.
   - **Identity:** the loadout's policy deltas still carry the pre-repair
     procedures, so the semantic hash (`4d9dc4e4…`) is unchanged. Only the
     target hash (`b3a645b7…` to `3b588a0b…`) identifies the change, as D014
     and the code comment state.
   - **Cosmetic:** the doc comment for `contributorRolesFor` now sits above
     `sessionCommands` (`contributor.ts:380`).
2. **Codex and Claude windows start at different points.**
   - **The rule.** The collector counts a Codex dispatch from its explicit
     harness entry; 07 says the adapter measures "after session entry". It
     counts a Claude session from its first message.
   - **The gap.** The first two executor sessions spent 887,976 and 879,894
     tokens before their entries, and those intervals are outside the meter.
     The second repair's equivalent interval, 267,527 tokens, is inside it.
   - **Effect on totals.** Through each executor handoff:
     - counting every dispatch from session start gives 50,976,297;
     - counting every dispatch from entry gives 48,940,900;
     - the meter shows 49,855,178.
   - **Effect on the 07 claim.** The committed figures are exact for their
     stated windows. So is 07's claim that post-handoff readings bring those
     dispatches to 41,353,517, but only for entry-bounded windows. Counted
     from session start, they total 43,121,387.
   - **Why this is not a finding.** The contract defines entry-bounded
     measurement, and VER-002 specified these windows. A uniform boundary rule
     is a planning decision.
3. **The verifier meter row includes an audit worker.** The row adds the
   feedback audit's 166,081-token envelope and a 0-token attempt. Both were
   recorded as `verifier` at 15:52Z, during the first repair. The tokens are
   real, but they do not belong to a lifecycle verification dispatch.
4. **Post-handoff work sits outside the snapshot.**
   - After their meter readings, the three executor sessions used 782,762,
     193,782 and 1,263,988 more tokens. A local row at 18:07:36Z reads
     9,369,456 for the second repair's window.
   - `meta.json` names its cutoff, 17:56:24.687Z. The prepared PR table does
     not. Yet the receipt says both are at "their explicit observation
     cutoff", and 07 now requires a meter snapshot to name its cutoff.
   - The reviewer regenerates PR.md.
5. **Two earlier reports changed after completion, as disclosed.**
   - VER-001 differs from its completion checkpoint only in its token line
     (`288c334` to `580ddec`).
   - VER-002's current bytes (`eace486`) replace the F2-only report stored at
     its completion checkpoint (`d983854`). They equal checkpoints 9 and 10.
     The evidence README records the operator's direction for the rewrite.
   - Neither verdict changed.

## Goal alignment of the verdict

WO-043 is enabling work on the path to the external-source runtime. It
provides machine-checked dependency gating, dependable role instructions and
trustworthy cost evidence. Passing sends the order to final review, with the
residual accounting boundaries disclosed.

- **NoOp (fail again).** Another executor and verifier cycle would cost more
  than the discrepancy it fixes. The meter puts the last repair at 8,501,661
  tokens and each verification at 10 to 14 million. That spend would adjust
  about 3.5% of one figure, and the right boundary for that figure is a policy
  choice. The escalation and commons lenses weigh against it.
- **Rule beating and drift.** The pass rests on the independent replay,
  checkpoint comparison and build probe, not on the green gate alone. The
  observations carry their numbers, so no weaker standard is normalized.
- **Burden and success to the successful.** Each actionable observation names
  an owner: planning for the boundary rule, and the reviewer for PR.md. I
  considered failing on observation 2 rather than deferring to the existing
  collector design. I rejected that because the contract states the
  entry-bounded rule explicitly.
- **Seeking the wrong goal.** The benefit is correct activation gating and
  accurate instructions and evidence, not report volume.
- **Reopen if** the operator defines dispatch usage as whole-session usage, or
  a cost decision depends on the boundary between executor and verifier.

## Costs and limits

- **Runtime.** The dispatch was recorded at 2026-09-11T18:10:10.211Z, and
  `VerificationCompleted` records the exact elapsed verification time. The
  first gate run took 622.7 s, all fresh. The run on the report tree reuses
  unchanged suites.
- **Tokens.** Measured with `node scripts/harness.mjs usage` (source
  `claude-transcript-message-usage`, scope `dispatch`):
  - at entry, 84,548;
  - before writing this report (18:29:07Z), 2,332,212, over 74 steps and 55
    commands.

  The completion action records the handoff figure. For comparison, VER-002
  measured 8,029,965 at its result command, on a broader re-verification.
- **Cost.** Null; the transcript records no price.
- **Not covered:**
  - I did not rerun the live Claude feedback audit; the `feedback-evidence`
    suite checks its recorded evidence.
  - I did not re-derive the 85-order migration, which is byte-identical to
    VER-001's subject.
  - I did not exercise remote tags or publication.
