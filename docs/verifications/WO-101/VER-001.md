# WO-101 verification report VER-001

**Subject:** branch `wo-101`, uncommitted working tree, 2026-09-01. Base,
`HEAD`, and `origin/main` were all
`329820e14c55a46a987b9d788449eca14e42b6db`; subject before this report was
24 new files under `corpus/`, the explicitly authorized work-order correction,
and lifecycle state.
**Authority:** `docs/work-orders/WO-101-program-and-hash-corpus.md`.
**Prior reports:** none.
**Verifier:** Codex, GPT-5 family; the reasoning-effort setting was not surfaced
to this session. WO-101 permits any capable Codex tier. Read-only parallel
audits covered the Program lane, ID lane, and manifest/artifact lane; only the
primary verifier wrote to this worktree.

**Verdict: WO-101 passes.** All five acceptance criteria pass. The exact
evidence gate, the repository baseline, independent fixture and manifest
checks, and direct source review are green. There are no blocking or
non-blocking implementation findings and no kernel/spec divergence. One
non-blocking evidence caveat about historical preflight timing is recorded
below.

## Dispatch and subject integrity

`npm run resume -- verify` allocated this exact report path and advanced the
control state to `verifying`. The transition could not create its optional
recovery checkpoint because this environment exposes the shared Git index
read-only; it failed loudly, recorded `checkpointUnavailable`, and proceeded
as designed. No older checkpoint was used.

Immediately before this report, the work-order correction diff hashed
`ba6d4aa4eb421acf5d65f6fc148aa525ea524d79093fe4de1e15c49d66a66a11`,
and the sorted path/content digest for all 24 files under `corpus/` was
`c63f5fd2bddd402c364979663509fe387c29d0c0b417214923d298c3d6996ee1`.
The corpus tree occupied 6,859,752 bytes; the nine manifested fixtures account
for 6,744,107 of those bytes. Fresh install, build, repository tests, generator
checks, and harness tests left the subject status unchanged. No package,
kernel, script, root-test, configuration, or product/ledger file changed.

The only existing-file change outside standing lifecycle state is the
work-order correction at `docs/work-orders/WO-101-program-and-hash-corpus.md:11-18`.
It records the operator's explicit in-place authority correction rather than
rewriting activation history. Every implementation file is within the paths
allowed at work-order lines 74-94.

## Executable evidence

Dependency and repository-baseline checks on the pinned base all exited 0:

```text
npm ci
added 5 packages

npm run build
tsc -b

npm test
backup-intake tests passed
resume tests passed
checkpoint tests passed
worktree tests passed
release tests passed
# tests 68
# pass 68
# fail 0
```

The exact WO-101 gate from work-order lines 157-161 then exited 0:

```text
npm run build &&
node corpus/harness/generate-program-corpus.mjs --seed wo101-seed-20260901 --check &&
node corpus/harness/generate-id-corpus.mjs --seed wo101-seed-20260901 --check &&
node --test corpus/harness/wo101-*.test.mjs

program: 7 fixture files; 7,784 ASTs; 38,920 evaluation inputs
Invoke: 168 event cells + 1 no-event cell
Await: 672 event cells + 4 no-event cells
IDs: 2,054 stableHash inputs + 2,048 commandId tuples
# tests 22
# pass 22
# fail 0
```

`git diff --check` also exited 0. The running toolchain exactly matched the
manifest: Node v22.2.0, npm 10.8.0, TypeScript 5.4.5, Darwin 24.6.0 arm64.
The committed implementation transcript at
`corpus/manifests/runs/WO-101-329820e14c55a46a987b9d788449eca14e42b6db.log`
contains the build, both `--check` summaries, and the complete 22-test TAP run;
its summaries agree with the manifest.

## Acceptance criteria

| # | Verdict | Evidence |
|---|---|---|
| 1 | **pass** | Bounds and atoms are pinned at `corpus/manifests/WO-101.json:18-215`, the versioned predicate registry and digest at lines 216-240, constructor counts at lines 241-301, and exact Cartesian coverage with no missing, duplicate, or unexpected cells at lines 302-406. |
| 2 | **pass** | All 5,764 committed vectors replay against built kernel output. The nine independently recomputed fixture row counts, byte sizes, and SHA-256 values match `WO-101.json:507-567`; there are no expected-fail rows and `corpus/manifests/findings-WO-101.md` accurately records no divergence. |
| 3 | **pass** | `corpus/harness/wo101-program-corpus.test.mjs:79-142` checks committed and full-enumeration repeat identity, input immutability, poisoned ambient sources, and residual round-trips. `wo101-id-corpus.test.mjs:34-133` checks every hash/ID vector, 128 NFC/NFD pairs, 256 namespace pairs, and 128 empty-episode fallback pairs against both implementations. |
| 4 | **pass** | Both recorded-seed `--check` commands regenerated and byte-compared their lane plus the manifest successfully. `generator-runtime.mjs:24-34` and `wo101-support.mjs:127-154` show that check mode reads and compares exact bytes rather than rewriting fixtures. |
| 5 | **pass** | `corpus/harness/wo101-deferral-pins.test.mjs:9-26` is the single marked file and pins the exact error through both `stepProgram` and `decideProgram` for `Choose`, `All`, `Race`, `Repeat`, and `Compensate`. Direct source search found no evaluation case for those kinds; `packages/kernel/src/core.ts:53` retains the common deferral path. |

## Program-corpus audit

The enumerator declares maximum size/depth 4 and Sequence arities 0/1/2 at
`corpus/harness/program-corpus-lib.mjs:13-20`, freezes the atom and predicate
alphabets at lines 22-97, and constructs exact-size closure at lines 161-205.
An independent recurrence reproduces its layer counts:

```text
size 1:    2
size 2:   30
size 3:  462
size 4: 7290
total:  7784
```

All six implemented constructors occur at the root and throughout the set.
The full regenerated JSONL-equivalent artifact is digested in memory at lines
260-290, while the harness actually evaluates all 38,920 AST/context inputs;
it does not infer full coverage from the committed sample.

The matching alphabet at lines 293-347 spans 2 event types, 3 correlation
states, 4 command-id states, and 7 payload shapes, including null, array,
scalar, missing result, non-string result, and unknown result. Lines 349-403
materialize every combination for Invoke and all four optional-pattern modes
for Await. The tests at `wo101-program-corpus.test.mjs:144-220` independently
derive exact dispatch/advance/throw/stationary or advance/wait outcomes and all
four ProgramStep channels. Twelve Sequence cases at lines 222-240 cover empty
normalization, Done absorption, nested residual behavior, channel preservation,
and tail order. Guard tests at lines 242-255 exercise both versioned predicates,
both branches, absence of an event, and the exact unknown-reference error.

## Hash-identity audit

The reference hash at `corpus/harness/id-corpus-lib.mjs:41-51` uses unsigned
32-bit high/low arithmetic and Buffer UTF-8 encoding. Its source imports no
kernel implementation and contains neither BigInt calls nor BigInt literals;
`wo101-generators.test.mjs:48-68` enforces that separation and checks the
literal hand-derived `ws:a:0:0 -> cmd_6b46f97735167cca` byte trace.

The recurrence constants and all six planted anchors were independently
checked against RFC 9923 section 8.3
(`https://www.rfc-editor.org/rfc/rfc9923.html#section-8.3`), including the
three `strlen+1` NUL-inclusive block values. Number intermediates in the pair
recurrence remain below JavaScript's exact-integer limit. Every one of the
2,054 stable-hash rows and 2,048 command-id rows agrees with the shipped BigInt
implementation at `packages/kernel/src/core.ts:66-67`.

The generated families cover Unicode scalar boundaries, valid and malformed
surrogate shapes, combining forms, multibyte text, all C0 controls plus DEL,
embedded colons, empty and long IDs, numeric-string edges, and seeded mixed
UTF-16. Tests at `wo101-id-corpus.test.mjs:65-187` verify the claimed family
counts and targeted behaviors. The namespace regression keeps the entire
preimage after the first three characters identical, changing only `ep:` to
`ws:`, and reproduces the incident anchor exactly.

## Verification-instrument disclosure

The generators, exact-byte checker, manifest builder, and their tests are
WO-101 deliverables and were also used by its evidence gate. This report does
not treat their successful self-use as sufficient proof. Separate checks
included direct inspection of the shipped kernel and generator sources,
independent enumeration combinatorics, independent fixture row/byte/SHA-256
recalculation, transcript-to-manifest comparison, mathematical review of the
32-bit-pair recurrence, and published RFC anchors. The unrelated repository
suite also passed before the corpus gate.

## Scope, clean-room, deviations, and open questions

- Clean-room review found only the public RFC URL in the generated material;
  no credential, private host, employer identifier, proprietary API shape, or
  internal service detail is present. `docs/intake/` was not inspected.
- The implementation transcript fully captures the executable evidence gate,
  but it does not durably show the separate `npm ci` and root `npm test`
  preflight or prove their historical order before corpus generation. The
  activation correction already records that preflight followed activation.
  The verifier reran install, build, and all 68 repository tests at the same
  pinned base before the final corpus gate; this is a non-blocking evidence
  caveat, not a behavioral defect.
- The verification-request checkpoint was unavailable because of the
  read-only Git index. The lifecycle log records that honestly; no recovery
  point was used.
- The work-order authority explicitly waives ledger duty, so no lineage entry
  was written. This is the required skipped-duty note, not an omission.
- `corpus/README.md:30-32` and `WO-101.json:581-584` retain the required open
  question: a later authorized documentation pass must sync
  `docs/product/03-architecture.md` section Corpus policy with this generated
  corpus layout.
- WO-101 remains an internal tooling/evidence-only `v0.2.0` close. Because it
  is below the latest published `v0.2.1` tag, release close must record the
  honest no-release disposition and must not create or push a tag.
