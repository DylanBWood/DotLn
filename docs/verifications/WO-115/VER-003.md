# WO-115 — VER-003

**Verdict:** pass. VER-002 G1 is resolved by the coherent-revision fixture and
a fresh complete product gate. VER-001 F1 remains resolved. No new finding.

**Subject:** The uncommitted WO-115 tree following repair checkpoint 10,
allocated by `resume: verify` on 2026-09-26. The executed product gate records
code identity `0e22ed0f73bcbb8355fac73b9b1a85ddd55b76dec056b781911c8b8fe9290d1a`.
Comparing current file bytes, including untracked source, with checkpoint 8
confirms that `scripts/test-worktree-integration.mjs` is the only changed file
under `packages/` and `scripts/` since VER-002. This verifier changed no
implementation, acceptance criterion or earlier report.

Verifier: Codex CLI 0.157.1, gpt-6-astra, selected effort `ultra`, normalized
to xhigh with subagents, from current-session readback. Two read-only
gpt-6-astra reviewers at xhigh independently examined the integration fixture
and console contract; the fixture reviewer was reused for write-backs. The
root alone ran the gate and wrote this report. The transport exposes no hard
dollar-cap control; the suggested USD 5 cap was unenforced.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.157.1","model":"gpt-6-astra","effort":"xhigh","mode":"subagents","raw":"ultra","source":"codex-session-readback"}

**Process cost:** entry 42649 tokens; handoff 3625809 tokens; source codex-transcript-counter

Scope: this root dispatch. Entry cutoff 2026-09-26T03:41:39.175Z;
report-preparation cutoff 2026-09-26T03:49:00.154Z. These cumulative transcript
counters include repeated and cached input, not unique context. Dollar cost
and child-agent token totals are unknown. The harness reports zero observed
admissions with unknown uncounted remainder; explicit accounting is two
reviewers, one reused, no descendants, against cap 20. Later counters stay in
ignored receipts and the handoff response.

## Acceptance judgment

**Criterion 1:** met

The fresh console suite passed in 23.88 seconds. Its inventory invokes all
23 contract IDs directly and over loopback with phase-independent parser
refusals, comparing exit code and exact stdout/stderr bytes and asserting
unchanged launchpad control state and resident domain events. Additional
fixtures compare read projections, compiled diff, presence-away/back event
deltas and the text host. Queued success retains terminal-byte parity.
Source inspection confirms fixed Node entrypoints and argv, the same terminal
parsers, working directory and console-child environment, without a shell.

Successful lifecycle, intent, bind and emit operations are supported by that
shared dispatch and the refusal inventory rather than executed success-path
pairs, as recorded in D009. Resume reads compare adjacent terminal runs and
can compare refusals when local state prevents a successful read. This report
does not convert either limit into broader success evidence.

**Criterion 2:** met

The passing fixtures exercise unknown commands, missing/incorrect tokens,
foreign Origin and Host, malformed/oversized requests, unclassifiable commands
and denial by a current compiled envelope. Console refusals have exit 1,
empty stdout and the documented error line. Source inspection confirms
127.0.0.1 binding, an owner-only directory and descriptor, and reuse of the
terminal's `permissionEffect` and `harnessAuthorization` instead of another
authority table. Another OS account was not exercised; the local-user boundary
is supported by filesystem ownership/mode checks and token-refusal fixtures.

**Criterion 3:** met

Fixtures verify console-actor invocation/result receipts and replayed bytes,
including unknown-command refusals, interrupted running commands, queued
departure and orderly shutdown. Incomplete admitted invocations are named.
Source inspection confirms result blobs are persisted before receipts and
replay checks digest, length, actor and invocation/result pairing. The
console receipt transactions leave the resident host's change baseline alone.

VER-001 F1 remains resolved: headers and keepalive precede serial admission,
and a decoded request whose caller leaves or whose resident stops receives a
recorded refusal when its lane turn arrives. The current full gate reran that
regression. VER-002's independent two-heartbeat probe is retained historical
evidence on byte-identical console sources; it was not rerun or claimed as a
fresh probe here. Crash-durable queue admission, arbitrary client deadlines
and unlimited duration are not established. Requests remain in memory until
admission, as VER-002 disclosed.

**Criterion 4:** met

Read the order's cited product sections, terminal sources, product 04's
contract/exclusions, product 07's resume sentence, console and skeleton README
write-backs, evidence and D001–D015/index rows. The planning map marks the
wave-5 candidate's contract half allocated to WO-115. The pre-2026-09-09
ledger duty is discharged by the decisions file and index under the verifier
procedure. Portfolio declaration, saved-build selection and equip preview
remain documented exclusions because they have no terminal verb. The current
release prose and package versions agree on the staged release; normal
upstream integration and any version retiming remain final-review work.

**Criterion 5:** met

`npm test` exited 0: 27 suites passed, zero failed, 71 fresh tasks, 352.053
seconds, recorded 2026-09-26T03:48:49.343Z in the canonical ignored
`docs/control/local/harness/checks.json`. The worktree-integration suite passed
in 113.367 seconds, console in 23.879 seconds and skeleton in 348.187 seconds.
This is a fresh complete selection, not a partial run or a reused repair row.
`git diff --check` passed. Manifest/lockfile inspection shows component version
changes, the existing skeleton dependency pin and the browser-client export,
with no new dependency. Source comparison found no new lint/type suppression.
The 13 changed generated hooks differ from HEAD only in digest values.

## VER-002 G1 — resolved

**Expected:** The full product gate judges coherent source inputs and passes
without changing real upstream state or weakening the integration scenarios.
**Observed:** The repair at `scripts/test-worktree-integration.mjs:145` points
only the temporary bare origin's `main` at the source's committed HEAD before
cloning the fixture worktree. The existing working-tree overlays and their
committed package-source imports now agree. No current package-source `.mjs`
change triggers D015's stated reopening condition.

The fixture still creates its own independent upstream and local commits,
authored/generated conflicts and colliding tag. Assertions still require
preserved recovery data and histories, real generators, release retiming,
correct reviewed-merge parents, pre-write refusals and recoverable stash
failures. The independent fixture review found no scenario that requires
real-main-only content. The full gate passes all nine integration tests;
no assertion was removed or skipped by this repair.

**Reproduction:** From this worktree on Node v26.9.0, run `npm test`.
The existing focused command is `node --test scripts/test-worktree-integration.mjs`;
its repair-era before/after transcript remains in
[worktree-integration.txt](../../evidence/WO-115/worktree-integration.txt).
This verification's independent execution was the complete gate above.
[D015](../../evidence/WO-115/decisions.md#wo-115-d015--repair-ver-002-g1-base-the-integration-fixture-on-the-sources-own-committed-revision)
records the repair and reopening conditions; the fresh passing gate discharges
D014's named G1 follow-up. There is no waiver or remaining finding.

## Goal judgment and limits

The verification plan retained D014/D015's goal comparison: Gate U needs a
dependable terminal-to-console boundary and independent evidence on the
selected subject. Policy resistance and shifting the burden favor coherent
fixture inputs over repeated operator rescue after unrelated main changes.
Commons and escalation are bounded by two batched read-only reviews and one
fresh full gate, with no repeat of the unchanged 60-second probe. Drift and
rule beating are checked by the original criteria and retained integration
assertions. Success to the successful does not justify retaining the broken
fixture base; seeking the wrong goal is avoided by judging command behavior
and the full gate rather than test count alone. Naive Interventionism favors
the one-fixture repair while preserving real integration ownership. NoOp would
leave the previously unmet gate obligation unresolved.

The observed outcome supports that plan: console regressions remain green and
the previously failing suite now passes within the complete gate. No resource
saving or universal transport guarantee is claimed. The generated harness and
resident store under verification also recorded this work; their receipts are
not independent proof of their own correctness. Final review owns integration
and checks on the integrated subject, preserving these original report bytes.
