# WO-115 — VER-002

**Verdict:** fail. VER-001 F1 is resolved; criterion 5 remains unmet because
the full product gate fails in the existing mixed-revision integration fixture.
No new console implementation defect was found.

**Subject:** The uncommitted repaired WO-115 tree following checkpoint 6,
allocated by `resume: verify` at checkpoint 7. The executed product gate records
code identity `17dd1d7221120bc18e6b3c37119b8a9e14f34993a880c8af658afea2e5400e0d`.
No implementation, acceptance criterion or prior report was changed.

Verifier: Codex CLI 0.157.1, gpt-6-astra, selected effort `ultra`, normalized
to xhigh with subagents, from current-session readback. Two read-only
gpt-6-astra reviewers at xhigh checked transport/replay and contract/authority;
the root alone ran probes and wrote these artifacts. The transport has no hard
dollar-cap control, so the suggested USD 5 cap was unenforced.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.157.1","model":"gpt-6-astra","effort":"xhigh","mode":"subagents","raw":"ultra","source":"codex-session-readback"}

**Process cost:** entry 39273 tokens; handoff 2061305 tokens; source codex-transcript-counter

Scope: this root dispatch. Entry cutoff 2026-09-26T03:14:02.514Z;
report-preparation cutoff 2026-09-26T03:20:06.153Z. Counters include repeated
and cached input, not unique context. Dollar cost and child token totals are
unknown. The harness reported zero observed admissions and an unknown uncounted
remainder; explicit accounting is two reviewers, one reused, no descendants,
against cap 20. Later counters remain in ignored receipts and the response.

## Acceptance judgment

**Criterion 1:** met

The fresh console suite passed in 22.71 seconds. Its inventory invokes all
23 command IDs directly and through loopback with phase-independent parser
refusals, comparing exit code and stdout/stderr bytes, unchanged launchpad
control state and resident domain events. Successful read/compiled-diff,
presence-away/back and text-host paths pass. Queued terminal-result parity was
independently reproduced below. Successful lifecycle, intent, bind and emit
operations remain supported by identical entrypoints/argv/environment and
refusal pairs rather than executed success-path pairs, as disclosed in D009.

**Criterion 2:** met

Fixtures cover unknown commands, missing/incorrect tokens, foreign Origin and
Host, malformed/oversized bodies, classification failure and denial by a
current compiled envelope. Refusals have exit 1, empty stdout and the documented
error line. Source confirms fixed Node argv without a shell, 127.0.0.1 binding,
owner-only connection storage, and reuse of the terminal classifier and decider.
Actual access from a second OS account was not tested; that boundary rests on
filesystem ownership/modes and token-refusal tests.

**Criterion 3:** met

Fixtures verify console-actor invocation/result receipts, digest-checked
replay, incomplete admitted invocations, and retained refusals for callers
leaving while queued and requests pending at orderly shutdown. The independent
socket probe replayed both results with no incomplete entries. Headers precede
serialized admission; crash-durable queue admission is not established. A
resident killed before admission can lose an in-memory queued request. This
limit is not recurrence of F1's ordinary timeout/disconnect loss.

**Criterion 4:** met

Read the work order's cited product sections, product 04's contract/exclusions,
product 07's resume sentence, console README, evidence and D001–D013/index rows.
The map marks the contract half allocated to WO-115. The pre-2026-09-09 ledger
duty is discharged by the decisions file/index under the verifier procedure.
Portfolio declaration, saved-build selection and equip preview have no terminal
verb; the documented exclusions avoid inventing UI-only commands.

**Criterion 5:** unmet

`npm test` exited 1: 26 suites passed, one failed, 71 fresh tasks, 318.53
seconds; cutoff 2026-09-26T03:20:06.092Z. Console passed in 22.71 seconds and
skeleton in 315.63 seconds. `git diff --check` passed. Manifest/lockfile diffs
contain component versions, the existing skeleton dependency pin and the client
export, with no new dependency. Source comparison found no new lint or type
suppression directives. G1 is the sole remaining unmet obligation; no waiver
is recorded.

## VER-001 F1 — resolved

`ConsoleLoopback.accept` now flushes headers and starts keepalive before the
serial wait; `run` records departed/stopping requests instead of dropping them
before admission. The revised fixture covers queued success, cancellation and
shutdown replay. Both reviewers found those paths consistent with the repair.

The root independently used the production `ResidentHost`, `missionConfiguration`
and native fetch. Three directories under `os.tmpdir()` held a resident, an
empty status store and a FIFO at `<blocked-store>/runtime-status-v1.json`.
A first `console.status` request blocked on the FIFO. After its invocation
receipt appeared, the probe queued `dotln.status --store <empty-store>`, read
two body chunks, cancelled the blocker, checked terminal bytes, stopped the
resident and checked replay:

```json
{
  "headersMs": 1,
  "heartbeats": [
    { "elapsedMs": 30003, "bytes": 1 },
    { "elapsedMs": 60003, "bytes": 1 }
  ],
  "elapsedMs": 60557,
  "terminalParity": true,
  "replay": [
    { "command": "console.status", "exitCode": 143 },
    { "command": "dotln.status", "exitCode": 0 }
  ],
  "incomplete": []
}
```

Both chunks were whitespace. The eventual exit code and stdout/stderr base64
equalled an adjacent direct execution using `consoleCommandInvocation` and
`consoleChildEnvironment`. No transport timeout was shortened or implementation
modified. This establishes prompt headers and two real keepalive intervals,
not unbounded duration or a repeat of the original 301-second timeout.

## G1 — full gate mixes revisions in its integration fixture

**Severity:** medium (P2), test-instrument blocker. **Criterion:** 5.

**Expected:** The complete `npm test` run is green at the judged subject.
**Observed:** `worktree-integration` reports two passed and seven failed.
Its ignored canonical output is
`docs/control/local/harness/check-output/afc93d035d849acfd52e35e6a5bd557c5dd9f774af0dc6637afd57501bea8a15.log`.
Line 66 names missing `packages/skeleton/src/control-beacon-fs.mjs`, imported
by `scripts/lib/beacons.mjs` in the temporary fixture.

**Reproduction/evidence:** Run `npm test` with this subject and current local
`main`. `scripts/test-worktree-integration.mjs:133–175` clones tracked history,
checks out `main`, then overlays this subject's scripts/shared library; lines
245–249 overlay built packages. `git cat-file -e` confirms the old Beacon path
exists in subject HEAD and is absent from `main`, whose WO-070 change moved
those leaves. This is the cause recorded in VER-001 and the repair handoff,
not a console regression or version collision.

The prior coherent-revision nine-test rerun is historical diagnostic evidence
only; it was not repeated or presented as this session's full gate. No fixture,
branch/ref or implementation was changed to obtain a green result.

**Disposition:** [WO-115-D014](../../evidence/WO-115/decisions.md#wo-115-d014--reverification-resolves-f1-and-retains-the-full-gate-failure)
records the fixture-coherence follow-up. A pass requires fresh full-gate
evidence or a separately authorized canonical waiver of criterion 5. Neither
is supplied here. Normal final-review integration retains ownership of sibling
integration; this verifier does not perform it early or reopen the resolved
console repair.

## Judgment and limits

Gate U needs reliable command results and inspectable evidence. The extra
60-second probe establishes behavior beyond the shorter regression fixture;
the full run exposes the remaining gate obligation. D014 records the
system-trap, Naive Interventionism and NoOp comparisons. Two batched reviews,
one full gate and one bounded socket probe support this judgment; no resource
saving is claimed. The generated harness and resident store under verification
also recorded this work, so their receipts are not independent proof of their
own correctness.
