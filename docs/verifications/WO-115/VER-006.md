# WO-115 — VER-006

**Verdict:** pass — on the current tree all five criteria are met, with the console subject byte-identical to what VER-005 judged and a complete product gate passing at this code identity; the lifecycle repair's claims (a) to (e) hold except one low-severity fold gap no publication consumer reaches (Finding 1).

**Subject:** branch `wo-115`, HEAD `13f09a1ebf5c15ffc334e74dae520aea4dc22263` ("Keep the control fold free of skeleton source and leave the role skill sentence unchanged", on `a659ab80` and `9c7ca0ad`), plus uncommitted control, index and report records under `docs/`, all outside the identity. Code identity from `gateCodeIdentity(process.cwd())` at the moment of writing: `81d0244ca535c662f94ac4b0e418debd6ff50935686227a7af36e6d639586518`, equal to HEAD's and to VER-006's allocated checkpoint `refs/dotln/checkpoint/WO-115/28` (`45a721a3`). My brief named `a659ab80` (identity `e7625cc9…`); the subject moved to `13f09a1e` during this verification and I judged the moved tree.

Verifier: Claude Code 2.1.283, model `claude-opus-5-5`, effort `xhigh` from the session's `CLAUDE_EFFORT` readback, a subagent of the final reviewer's session spawned at the operator's direction; no agents spawned, writes only under os.tmpdir(). Executed: `node --test scripts/test-runner.test.mjs` (41/41), `node scripts/test-off-ramps.mjs` and an extended scratch copy of it, `node --test scripts/test-beacon-portability.mjs` and the WO-145 role-baseline test of `scripts/test-process-debt.mjs` at `a659ab80` (both failed) and at `13f09a1e` (both passed), `node --test packages/console/dist/test/console-commands.test.js` (5/5, 16.9 s), scratch probes of the identity, the fold, `foldWorkOrders` and `reviewedProductGate` on temporary repositories, `git check-attr` and `git diff --check`. Read only: the root session's gate rows in `docs/control/local/harness/checks.json`, VER-005, FINAL-002, D025–D026 and the source; I ran no `npm test`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.283","model":"claude-opus-5-5","effort":"xhigh","source":"claude-session-readback"}

**Process cost:** unknown; cause harness-no-readback. A subagent has no session usage readback of its own, so no token count is claimed.

## Acceptance judgment

**Criterion 1:** met

`git diff 0ec69ecd` (VER-005's checkpoint 22) against the working tree is empty for `packages/skeleton/src/console-*`, `packages/console/src`, `packages/console/test`, both package manifests, the root manifest and the lockfile, so VER-005's and FINAL-002's parity judgments carry; the console suite passed here directly (5/5) and inside the gate at `81d0244c`. The D009 limit stands: lifecycle, intent, bind and emit success paths are not executed against the repository.

**Criterion 2:** met

The same suite's refusal fixtures run on unchanged sources; a second OS account was not exercised, as VER-003 recorded.

**Criterion 3:** met

The same suite's receipt and replay fixtures run on unchanged sources; replay reads receipts and content-addressed bytes and reruns nothing, as VER-005 and FINAL-002 judged.

**Criterion 4:** met

Product 04 carries `console-commands-v1` under §Terminal first, console equal (lines 104, 162); product 07 line 150 carries the sentence; the console README carries §Resident command client (line 49); the planning map records the WO-115 allocation (lines 715-718); D001 and the index rows discharge the ledger duty. Since checkpoint 22 these surfaces are unchanged except one console README sentence and product 07's repair paragraph.

**Criterion 5:** met

Gate row recorded 2026-09-26T16:56:22.882Z: `npm test` at identity `81d0244c…` (tree `02f60c7e`), 39 suites, 0 failed, exit 0, 598.8 s, 83 fresh and 0 reused, not partial, sandbox not in force. `git diff --check` is clean on the working tree and on `main..HEAD`; against `main` the manifests and lockfile differ only by the console 0.3.0 and skeleton 0.44.0 bumps and the console `./client` export, so no dependency was added; the diff since checkpoint 22 adds 0 suppressions.

## Lifecycle repair

**(a) verified.** In a scratch repository a marked README edit left the identity unchanged in working-tree and revision mode, while an unmarked README edit and a Markdown input edit moved it in both; adding a mark moves the identity because `.gitattributes` is counted, and revision mode read the revision's own attributes (an uncommitted unmarking left `c1` and `c2` unchanged). `git check-attr` reports `dotln-documentation: set` for exactly the `packages/compiler`, `packages/console`, `packages/skeleton` and `corpus` READMEs and unspecified for `packages/kernel/README.md` (read at `ac7-readme-map.test.ts:154` and `kernel.test.ts:484`), `corpus/mutation/README.md` and `packages/skeleton/fixtures/wo050/README.md`. No test, script or package source reads the four marked READMEs, and `.prettierignore` excludes `*.md`. Recomputed identities reproduce D025: `bce54ec8…` at checkpoint 22, and `dbb52211…` at checkpoint 25 and at `88d87c6f`.

**(b) verified, except one partial.** `OFF_RAMP_PHASES.correct` is the open phases plus `closed` (`resume.mjs:228`); closed admits only `productGate` (`resume.mjs:1541`; `productGate` plus `reportPath` was refused). The extended fixture refused binding a failing, partial, non-empty-`excludedSuites`, unexecuted or non-`npm test` row, a passing row at a later working-tree identity whose checkpoint identity differed, and a `VerificationCompleted` subject; the repository fixture refuses a missing row, a non-review subject, a changed working tree, a repeat and a re-point; a review whose own event carried a gate (`host-gate:later:npm test`) refused a later binding. Success records `previous: {"productGate":"none"}` and the eight-field row in `evidence.productGate`. The fold refuses hand-appended invalid, partial, unexecuted or non-`npm test` rows, a mismatched `evidenceRef`, missing evidence and a declared subject type other than `FinalReviewCompleted`. `validProductGate` refuses partial rows through the local `partialRow` (`control.mjs:41`, `:55`), which matched skeleton's `partialGateCheck` on 49 `partial`/`excludedSuites` shapes with 0 mismatches; `control.mjs` imports no `gate-evidence.mjs`, and the WO-070 copied-control-plane test passes 2/2. Partial: the fold accepts a binding that declares `FinalReviewCompleted` while its ordinal names a non-review event or a failing review (Finding 1).

**(c) verified.** On temporary repositories `reviewedProductGate` ignored an uncommitted binding, accepted it once committed, returned the latest committed binding of the review's ordinal, ignored a binding aimed at another ordinal, kept the gate after a committed marked-README edit, and refused after a committed source change and after a later binding at another identity. `worktree publish` calls it at `worktree.mjs:410` before any push; `release close` calls it through `releaseEvidence` (`release.mjs:1293`) with `sourceRevision` after `updateMain`, or `origin/main` in a dry run (`release.mjs:1749`).

**(d) verified.** `foldWorkOrders` (`control.mjs:492-497`) kept closeOrdinal 2 and the review's closeRecordedAt after a closed correction; re-activation cleared both, and a second pass set 5 and its own time.

**(e) verified.** A grep over `scripts`, `packages/*/src`, `corpus` and `.claude/hooks` finds only `release-records.mjs:115` (`reviewedProductGate`) reading `evidence.productGate` from control events; `lifecycle-evidence.mjs` and `resume.mjs:1804` write it and test fixtures set it. `meta.mjs:720` reads an order's last `evidence` object only for `readCount`/`readBytes`, which no producer writes to control events, so its output is unchanged.

## Findings

1. Low — `scripts/lib/control.mjs:130-134` checks only the declared `event.subject.type`; the `RecordCorrected` fold case resolves the real subject at `control.mjs:355` but checks only its `workOrderId`. Reproduction: fold `[WorkOrderActivated, FinalReviewCompleted pass, RecordCorrected{subject:{ordinal:1,type:"FinalReviewCompleted"}, valid row}]` is accepted, and so is a binding on a failing review. Not blocking: `resume correct` cannot write such an event, and `reviewedProductGate` binds only the actual latest passing review's ordinal (the mis-aimed binding was ignored). D026's statement that the earlier verification finding (a binding aimed at a non-review event) is repaired is therefore only partly true. Fix: require the event at the subject ordinal to be a passing `FinalReviewCompleted`; if deferred, the root session records it in D026 or a new decision with a named follow-up.

Observations (not requiring repair):

- O1, resolved in the subject: at `a659ab80` the first complete gate (identity `e7625cc9…`, 16:42:19Z, exit 1) failed `beacon-portability`, because `control.mjs` statically imported `./gate-evidence.mjs`, which re-exports `packages/skeleton/src` that the WO-070 copied control plane lacks, and `process-debt`, because the role sentence moved `.agents/skills/dotln-executor/SKILL.md` to `c9e9661c…` against the pinned `8216a475…` (`test-process-debt.mjs:4544`). I reproduced both; `13f09a1e` fixes both and both pass now. D026 records only the role-baseline reason; the portability break and the failed row belong in a same-day correction note.
- O2: D026's rejected option "Exclude every Markdown file" cites `packages/console/fixtures/inputs/*.md` as counted inputs, but those paths are `dotln-generated` (`check-attr`: set) and already outside the identity; the kernel README is the operative example.
- O3: the fold does not enforce a single binding; a hand-appended second binding re-points at publication, still bounded by the published-revision identity check.
- O4: the comment at `scripts/test-off-ramps.mjs:909` ("A closed order takes no off-ramp.") is stale.

## Limits

- I did not run `npm test`; criterion 5 rests on the root session's row, which I read, and on my targeted reruns.
- `worktree publish` and `release close` were not executed; claim (c) rests on their call sites and on `reviewedProductGate` probes. The `resume correct` probes ran in copied fixtures, not on this repository's control log.
- The stale-`main` release-close warning, console lifecycle success paths (D009) and a second OS account were not exercised.
