# WO-115 — VER-005

**Verdict:** pass. Both VER-004 findings are resolved:
- **F1:** the repair ran a same-source comparison of shared and exclusive
  skeleton scheduling and kept the shared configuration. Product 07 records
  the measured tradeoff.
- **F2:** the status poll now stops when the watcher errors, so no callback
  follows `close()`.

A fresh complete product gate passed at the handoff code identity, and
criteria 1–5 are met.

**Subject:** the uncommitted, integrated WO-115 tree at checkpoint 22, allocated
by `resume: verify` on 2026-09-26. Its sources under `packages/`, `scripts/`,
`.claude/`, `README.md`, `docs/product/`, and the package manifests and
lockfile are byte-identical to checkpoint 22. Between checkpoint 18 (the
VER-004 subject) and checkpoint 22, only four source files changed:
- `packages/console/src/runtime-status.ts`
- `packages/console/test/runtime-status.test.ts`
- `scripts/test-runner.mjs`
- `scripts/test-runner.test.mjs`

`docs/product/07-execution-guide.md` also changed. The executed gate records
code identity
`bce54ec869793926a7d7db37d759d0e838daebe965ad1729a85982ea7b5b7949`, the same
identity as the executor's final repair gate. This verifier changed no
implementation, product document, acceptance criterion or earlier report.

Verifier: Claude Code 2.1.283, `claude-opus-5-5`, effort `xhigh` from the
session's `CLAUDE_EFFORT` readback. No subagents were planned or spawned; the
harness observed 0 admissions against cap 20. The root ran every check and
wrote these artifacts. The transport exposes no hard dollar-cap control, so
the suggested USD 5 cap was unenforced.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.283","model":"claude-opus-5-5","effort":"xhigh","source":"claude-session-readback"}

**Process cost:** entry 81966 tokens; handoff 11731396 tokens; source claude-transcript-message-usage

Scope: this root dispatch. The entry cutoff is 2026-09-26T14:52:19.691Z and
the report-preparation readback is 2026-09-26T15:01:49.481Z. These cumulative
transcript counters include cached input; they measure neither unique context
nor dollar cost. The 312.9 s gate accounts for most of the wall-clock. Later
counters stay in ignored receipts and the handoff response.

## Earlier findings

**VER-004 F1 (scheduling change without a same-source comparison): resolved.**
The repair reran the product gate twice with only skeleton's isolation
declaration changed: shared in 312.441 s and exclusive in 430.952 s. It then
restored the shared configuration and updated product 07. This verifier
checked the comparison rather than trusting it:
- **Canonical rows.** `docs/control/local/harness/checks.json` holds both
  comparison rows. Both exited 0, with the durations the evidence cites.
  `scripts/test-runner.mjs:1334-1350` records exit 0 only when code identity
  is unchanged at the gate's end, so neither run's code changed mid-gate.
- **Identity reproduction.** A scratch script replicated `gateCodeIdentity`
  over the 529 selected paths without writing to the repository:
  - Current sources give `bce54ec8…`.
  - Current sources plus checkpoint 20's scheduler test and a runner declaring
    `exclusive: false` give `bcf5d037…`. That runner hashes to the comparison's
    `a8dee07a…`, and the identity matches the shared row exactly.
  - The same inputs with checkpoint 20's `exclusive: true` runner (`2dd260ce…`)
    give `9eba426a…`, the exclusive row.

  The two measured runs therefore differed by one code line, and both
  included the current watcher fix. The executor's wider 3,473-path snapshot
  also covers non-code paths; its maps stay in ignored scratch, and this
  verifier did not reproduce them.
- **Retained configuration.** `git diff HEAD -- scripts/test-runner.mjs` is
  empty, so the net subject keeps main's scheduler.
  `scripts/test-runner.test.mjs` only adds assertions:
  - skeleton is not exclusive
  - skeleton has priority 80, one reserved lane and load factor 8
  - exclusive suites start with no active peer

  `node --test scripts/test-runner.test.mjs` passed 41 of 41 in 14.06 s.
- **Product 07.** Lines 2499–2513 of
  `docs/product/07-execution-guide.md` state the rows and the 118.511 s
  difference. They also state the priority, lane and load-factor effects,
  which match `scripts/test-runner.mjs:882-891` and `:1068-1076`, and the
  pair's limits. `npm run publication:check` passes.

**VER-004 F2 (the poll outlives `close()` after a watcher error): resolved.**
`runtime-status.ts:107-114` now clears the interval in the error handler.
The scratch probe from VER-004 was rerun against the build this gate
compiled. It drives Node v26.9.0's native error path with
`_handle.onchange(-5, …)`, which nulls the handle and emits no close event.
After `close()`, the probe atomically replaces and then removes the status
file.

| Module | Callbacks after `close()` |
| --- | --- |
| Current build | 0 `changed`, 0 `unavailable` |
| Pre-fix control copy | 1 `changed`, 1 `unavailable` |

The control is the same built file with only the error-path `clearInterval`
removed. `node --test packages/console/dist/test/runtime-status.test.js`
passed 8 of 8 in 1.51 s. That includes the new regression (605.7 ms), the
silent-notification recovery (252.9 ms) and atomic replacement (12.0 ms).

**FINAL-001 F1, VER-001 F1 and VER-002 G1:** FINAL-001 F1 is resolved on
this gate's evidence, and the other two stay resolved:
- **FINAL-001 F1 (no passing integrated gate).** Its two deadline failures
  did not recur under shared scheduling. The WO-143 acquisition matrix
  (`ok 224`) passed inside skeleton. The watcher cases passed inside console
  while it ran beside skeleton, worktree-integration and work-orders-fixtures.
- **VER-001 F1 and VER-002 G1.** Their sources are unchanged since checkpoint
  18. Their regressions ran in this gate: the queued-request fixtures inside
  console, and worktree integration passed in 113.65 s.
- **Cause.** The historical timeout cause remains unestablished, as D019–D023
  state.

## Acceptance judgment

**Criterion 1:** met

The console suite passed in 21.968 s inside the complete gate. It includes
the 23-ID parser/refusal parity inventory, run both directly and over
loopback, plus these fixtures:
- read projection
- compiled diff
- presence delta
- text host
- queued result

The contract, loopback, client and terminal-dispatch sources are
byte-identical to the subjects VER-003, FINAL-001 and VER-004 judged. The
success-path limit recorded in D009 still applies: lifecycle, intent, bind and
emit successes rest on shared dispatch, not on executed success-path pairs.

**Criterion 2:** met

The same console suite passed its refusal fixtures on unchanged sources:
- unknown commands
- a missing or wrong token
- a foreign Origin or Host
- malformed and oversized requests
- unclassifiable prose
- denial by a current compiled envelope

Another OS account was not exercised; the limit VER-003 recorded stands.

**Criterion 3:** met

The console suite's receipt and replay fixtures passed on unchanged sources.
They cover disconnects, queued departure, shutdown and truncated logs.

**Criterion 4:** met

The write-back surfaces judged met by VER-003, FINAL-001 and VER-004 are
unchanged:
- product 04
- product 07's resume-phrase sentence
- the console and skeleton READMEs
- decision records D001–D020 and their index rows
- the planning map's wave-5 allocation

Product 07's gate-scheduling paragraph changed, and this report judges it
under F1 above.

**Criterion 5:** met

`npm test` exited 0 at 2026-09-26T14:58:39.491Z: 28 suites passed, 0 failed,
72 fresh tasks, 0 reused, 312.934 s. The result is recorded in the canonical
ignored `docs/control/local/harness/checks.json`, and the runner transcript is
kept in this session's scratch. Skeleton ran shared on one lane at load factor
8 in 310.035 s. `git diff --check` and `git diff --cached --check` are clean.

Manifests and the lockfile differ from HEAD only by the internal version bumps:
- console 0.3.0, with its `./client` export
- skeleton 0.44.0

No dependency was added. No lint or type suppression appears in the repair
diff. Prettier reports the repaired sources, the product 07 paragraph and the
WO-115 evidence files as formatted.

## Observations (not defects)

- **O1:** `decisions.md` places D022 before D001, so the generated index lists
  it there too. Its anchor resolves, and no check or rule requires
  chronological order.
- **O2:** product 07 line 2514 holds the single word "fresh" after a rewrap.
  It renders as ordinary paragraph text and passes Prettier.
- **O3:** skeleton-inclusive shared gates have now passed three times on this
  host, in 312.4 s, 312.0 s and 312.9 s. These are not WO-132's three
  consecutive same-source rows: an exclusive run separates the first from the
  others, and it ran at a different identity. This verification makes no
  WO-132 claim.
- **O4:** the three follow-up rows below have their stated repair or
  reopening conditions met by this evidence. As D024 records, their register
  disposition is left to final review, which settled this order's earlier
  rows through the canonical API:
  - `FUP-db0a2c8b8a05d4ae`
  - `FUP-e495acc8ebaebf05`
  - `FUP-2ed6d5d710c207da`

## Goal judgment and handoff

[D024](../../evidence/WO-115/decisions.md#wo-115-d024--reverification-passes-the-measured-shared-scheduling-and-watcher-error-cleanup)
records the decision, evidence, rejected alternatives and reopening condition.
It also compares the outcome with all eight system traps, Naive
Interventionism and NoOp.

The chosen plan cost one fresh gate, two focused suites and two scratch
scripts, with no subagent. The scripts were the identity recomputation and
the native error-path probe, each run on the current build and on a pre-fix
control. Recomputing the identities made the comparison's same-source claim
reproducible, rather than resting on the executor's own snapshot. The
error-path probe exercised Node's real handle rather than the test double.
The retained shared scheduler is main's existing configuration, so the net
subject lands no scheduling change.

The generated harness and resident store under verification also recorded
this work, so their receipts are not independent proof of their own
correctness. Record the passing verification and hand off
`resume: final review`.
