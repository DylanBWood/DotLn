# WO-155 — VER-001

**Verdict:** pass. Criteria 1 and 3–6 are satisfied, and criterion 2 is
declined under criterion 1, as the order allows. My own probe reproduced the
observation behind that decline. One minor display defect in the new meter
row is recorded, with a follow-up, as
[WO-155-D006](../../evidence/WO-155/decisions.md#wo-155-d006). No repair
finding.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-opus-5-5[1m]","effort":"xhigh","source":"claude-session-readback"}

Independent Claude Code verifier, dispatched by `resume: verify` on 2026-09-24.
The model ID comes from the session's system context, the effort from
`CLAUDE_EFFORT`, which reports the selected effort, not the effective one. The
CLI version comes from `claude --version`. One writer owned the worktree. I
launched two read-only subagents, a fresh Explore worker and a fresh
general-purpose worker, to observe floor loading. The harness counted them as
2 of the 20-agent cap (exact-observed), with no descendants and no writes. The
transport exposes no hard USD 5 provider cap, so that limit was not enforced.
The two probes are the only model episodes this verifier started.

**Process cost:** entry 80752 tokens; handoff 9030678 tokens; source claude-transcript-message-usage

Both readings have dispatch scope and include reused cached input tokens.
Entry cutoff: 2026-09-24T19:24:23.364Z; handoff sample cutoff:
2026-09-24T19:34:27.312Z. The host's task notifications report 15,021 and
32,401 subagent tokens for the two probes. Whether the dispatch total includes
them is unknown. Dollar cost is unavailable. The host refused one attempt at
an unsandboxed full gate before it ran, so it cost no gate time.

## Subject and method

The selected [WO-155](../../work-orders/WO-155-single-source-floor.md) is
uncommitted work on `a403671d` (tag `v0.46.0`). This verification's entry
checkpoint is `refs/dotln/checkpoint/WO-155/3` (`4333ed20`), and the current
product-code identity is `46fe97a1…`. I read the order, the executor's
[implementation record](../../evidence/WO-155/implementation.md),
[D001–D005](../../evidence/WO-155/decisions.md), the
[loading observations](../../evidence/WO-155/skill-loading.json), both
cold-start measurements, the full diffs of `scripts/harness-context.mjs`,
`scripts/lib/harness-context.mjs`, `scripts/lib/process-budget.mjs`,
`scripts/lib/meta.mjs` and the two new `scripts/test-process-debt.mjs` cases,
the product 06/07 additions, the release-label and publication-lock diffs,
and both re-minted editions. I made no implementation, product-document or
branch-commit change. My repository writes are this report, D006 and the
generated projections the lifecycle refreshes.

## Acceptance findings

| Criterion | Observed result and evidence |
| --- | --- |
| 1 — completeness check | **Pass.** [skill-loading.json](../../evidence/WO-155/skill-loading.json) records six observed paths (the root and a fresh worker each for Codex, Claude and Copilot). The Claude Explore and Copilot task workers lacked the floor, so criterion 2 is recorded as declined, with a reopening condition, in D003. I reproduced the Claude rows in this session. **Claude role dispatch:** `CLAUDE.md` was in this session's context before `dotln-verifier` loaded, so floor before skill is true. **Fresh Explore worker:** it reported no floor, could not quote the floor's `Capabilities and residue:` line or its Clean Room sentence, and found the refusals paragraph first at line 23 of `dotln-refuter/SKILL.md`, the same line as the executor's row. So floor before skill is false. **Fresh general-purpose worker:** it quoted the floor's canary line exactly (`.claude/harness-manifest.json; planning: refute[ full] selects dotln-refuter.`) and named `CLAUDE.md`, so floor before skill is true. This row is not in the executor's record. It does not change the decline, because one path without the floor is enough, but it narrows D003's Claude reopening condition to the worker type. The refuter text asks for "a fresh background agent" and does not fix a type, and the Explore type has the Skill tool, so the omission is on a supported path. I did not reproduce the Codex or Copilot rows. |
| 2 — deduplication | **Declined under criterion 1, and the fallback holds.** `git diff HEAD` is empty for `CLAUDE.md`, `.claude/`, `.agents/`, `packages/compiler`, and `packages/skeleton/src` and `fixtures`. The paragraph occurs exactly once in `CLAUDE.md` and once in each of the 12 skills. `AGENTS.md` remains a symlink to `CLAUDE.md`. The paragraph is emitted at `packages/compiler/src/harness.ts:1042`, not `contributor.ts`, as D003 corrects. No new role oracle is needed for unchanged output. |
| 3 — trend and budgets | **Pass.** `harness-context` emits schema 3 with `comparisonEdition` `v0.46.0` and, per profile, `bytes`, `ceiling`, `previousBytes`/`delta` and `lastAcceptance` {date, revision, bytes, delta, cause}. I recomputed every figure independently with `git cat-file -s` and a separate script. Each role's latest unscoped acceptance is found at the first first-parent `budgets.json` revision containing it: executor at `75743fd6`, 25,183 bytes (Δ 0); verifier at `f24b5d72`, 21,061 (Δ +832); reviewer at `37a729ca`, 21,299 (Δ +1,772); release-close at `d428180b`, 12,437 (Δ +1,576). Planner and refuter have no acceptance, and the CLI reports that cause. The v0.46.0 totals equal the current totals for all six roles. No acceptance reason was reworded after introduction, so each located snapshot is its introduction commit. `npm run meta -- --check` prints the same rows under the drift heading and left the working tree unchanged. `budgets.json` has no diff. `--help` prints the usage line. `--bogus` exits 1 with the usage line. The no-flag and `--check` JSON are byte-identical, and neither exits nonzero, which matches "advisory, never refuse". The fixture shows `--check` printing the advisory for a breach. One minor display defect remains: see VER-001-N1. |
| 4 — before/after | **Pass.** [Before](../../evidence/WO-155/cold-start-before.json) (schema 2, v0.16.0 comparison) and [after](../../evidence/WO-155/cold-start-after.json) (schema 3) record the same per-role bytes, and D005 lists them. The order's expected executor figure of about 22,745 was conditional on criterion 2, and D005 claims no reduction. I reproduced the after-state bytes. |
| 5 — release and editions | **Pass.** Skeleton `0.39.0 → 0.39.1`, with the console pin and lockfile following it. `current.json` selects the WO-155 authority 001 and feedback-001 editions. The authority record is byte-identical to WO-154/002. Its `bundle-diff.json` differs only in its `comparison` label. Feedback-001 is schema 2, with pins naming skeleton 0.39.1 and a live, not carried, audit whose stream opens the ten feedback units at base `a403671d`. D004 records why each edition changed: the order's explicit duty and one requested episode, not staleness, since all four prior checks passed. The skeleton move has no skeleton source change. The order's premise ("the generated bundle bytes change") did not hold under the fallback, but criterion 5 states the move unconditionally, and D004 records that. |
| 6 — checks and write-backs | **Pass.** The executor's complete `npm test` row is recorded at the current code identity: 33 suites (review mode), exit 0, not partial, 562.8 s, recorded 19:19:06Z. My in-sandbox `npm test -- --inside-sandbox` passed 25 of 25 suites (69 fresh tasks, 135.24 s); the runner marks it partial because it excluded `skeleton` and `portfolio`. The review-only suites that carry this order's tests passed separately: `process-debt`, `harness-context` and `meta` (`npm test -- --review --only <suite>`, each 2 of 2). The two WO-155 cases also passed directly under `node --test --test-name-pattern=WO-155`. `git diff --check` is clean. `scripts/test-runner.mjs` is unchanged, so the two new cases add no gate step. The dependency sets are unchanged, and the lockfile diff touches only the two skeleton labels. D001–D005 carry evidence and reopening conditions. |

## VER-001-N1 — unset ceiling rendered as "unavailable"

**Severity:** minor (presentation). **Expected:** the meter separates an unset
ceiling from an unavailable observation. Its own legend says "Unavailable
observations are not zero; unset ceilings are not approvals of a future
limit", and its budget rows print `ceiling unset`. **Observed:** the new
drift rows print `.claude/skills/refuter: 15,736 bytes; ceiling unavailable;
… ; unset`, because `renderMeta` passes a null ceiling through `display()`.
**Reproduction:** `npm run meta -- --check`, drift rows for the refuter in
both roots. The JSON is correct (`"ceiling": null`, verdict `unset`), and no
verdict depends on the label. Because a verifier may not edit the
implementation and the defect is not substantive, it is boarded up in
[WO-155-D006](../../evidence/WO-155/decisions.md#wo-155-d006) with a named
low-priority follow-up, registered as `FUP-e5a6ca7dbe6270ea`.

## Executed checks and limits

- Two read-only Claude probes (Explore and general-purpose), each answering
  from startup context before any tool call. A canary quote tells real floor
  presence apart from recall. These are model self-reports of delivered
  context, not loader internals.
- `node scripts/harness-context.mjs` with no flag, `--check`, `--help` and
  `--bogus`; `npm run meta -- --check`; independent Git recomputation of
  every edition and acceptance baseline.
- `npm test -- --inside-sandbox`: 25 passed, 0 failed, 135.24 s; partial,
  excluded `skeleton` and `portfolio`, not product-gate evidence.
- `npm test -- --review --only process-debt`: 2 passed, 69.21 s. `--only
  harness-context`: 2 passed, 0.66 s. `--only meta`: 2 passed, 0.67 s.
  `node --test --test-name-pattern=WO-155 scripts/test-process-debt.mjs`:
  2 of 2 passed.
- `git diff --check`: clean.
- `npm run test:docs`: 21 passed, 0 failed, 30.37 s, with this report and
  D006 present.
- The host denied an unsandboxed `npm test`, and I did not retry it. The
  product-gate evidence is the executor's complete row at the identical code
  identity. The order's evidence gate runs `npm test` once at final review.
- The Codex and Copilot loading rows, and the live feedback episode, are the
  executor's observations. I inspected their records but did not re-run
  them. The provider's effective model and effort are unknown.
- The baseline "previous edition" is the highest reachable release tag.
  While an order's work is unreleased, that is the prior release. At a
  tagged commit with no working changes, the delta is zero by construction.
  D001 and product 07 state this rule. The acceptance lookup keys on the
  whole entry, including `reason`, so rewording a reason later would move
  the snapshot to the rewording commit. That does not happen today, and
  D001's reopening condition ("a history comparison is ambiguous") covers
  it.
- Instrument disclosure: `npm run meta` and `harness-context` are under
  verification. I used them only as outputs to check against independent
  Git recomputation. The usage readback in the cost line comes from
  `scripts/harness.mjs usage`, which this order did not change.

This result supports the order's goal. The two decisions most likely to go
wrong were checked against evidence: removing floor text a supported worker
receives, and reporting a byte saving the fallback never earned. **NoOp**
would leave the fixed v0.16.0 comparison and no acceptance trend. **Naive
Interventionism** would have deduplicated despite the Explore observation.
**Drift to low performance** now shows, per role, as growth since each
acceptance: +832, +1,772 and +1,576 bytes. **Escalation** is avoided: the
ceilings, the acceptances and the advisory route are unchanged, with no
seventh acceptance. **Rule beating** is avoided because baselines come from
repository snapshots, never from reason prose. **Policy resistance** and
**fixes that fail** are reduced because the CLI and the meter share one
measurement. **Tragedy of the commons:** the history query is one `ls-tree`
per snapshot, which is D002's measured economy. **Shifting the burden:**
baseline selection is mechanical rather than hand-maintained. **Success to
the successful:** per-file reads were replaced on evidence, not kept by
habit. **Seeking the wrong goal:** the byte target stayed below floor
completeness, which the order ranks first.
