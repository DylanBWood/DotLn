# WO-070 verification — VER-001

**Verdict:** pass

Verified on 2026-09-26 UTC against the original acceptance criteria in
`docs/work-orders/WO-070-beacon-portability.md`, including its authorized
evidence-edition expansion. Subject: the uncommitted WO-070 tree above
`af401170e0c9f197e9f6a1cd2cf1ea26d4d5c2e4`; executable code identity
`3679af7623d491a2ad350194eab8698499461be262e4990a98d612e4fcb2cc67`.
No earlier verification report exists for this order.

Actor: Codex CLI 0.157.0, gpt-6-astra, selected ultra effort normalized to
xhigh with subagents, from the current-session readback. Two independent
read-only agents ran at xhigh, with no descendants; this root wrote the
report. The transport exposes no hard dollar cap, so the optional USD 5 cap
was unenforced.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.157.0","model":"gpt-6-astra","effort":"xhigh","mode":"subagents","raw":"ultra","source":"codex-session-readback"}

**Process cost:** entry 39258 tokens; handoff 2060504 tokens; source codex-transcript-counter

Counters are dispatch-scoped observations, entry cutoff
2026-09-26T00:14:34.856Z and handoff cutoff 2026-09-26T00:20:30.480Z.
They include repeated input, not unique context; dollars are unknown. The
harness observed zero subagents with unknown uncounted remainder; the explicit
session count is two against the cap of twenty, not zero. No complete combined
root-and-child token total is claimed.

## Judgment and scope

The copied control plane emits a decodable control Beacon without the
skeleton package. This supplies the bounded prerequisite for exporting the
control plane on the source-to-deliverable critical path; it does not complete
the export. The seven build-free leaves now share one physical module home.

Goal-alignment judgment: preserve existing Beacon encodings and guards
(policy resistance and drift); test actual emission and module identity
(rule beating and seeking the wrong goal); choose the home from current
consumer evidence (success to the successful); remove packaging rescue
(shifting the burden). Commons and escalation favor reusing the executor's
passing evidence with focused independent checks. Naive Interventionism favors
the bounded move and unchanged algorithms over redesigning the reactor or
codebooks. NoOp retains the skeleton dependency and misses this order's
portability outcome. Reopen on a distinct module identity, changed fixture
bytes, or a supported copied root that cannot emit.

The verifier initially repeated the full product gate and proposed a machinery
sweep. The operator corrected that over-expansion during this dispatch:
executor, verifier and final reviewer should not each rerun every test.
The stop command found no active gate because the product run had just
completed. No machinery or document sweep was repeated. That duplicate
293.62-second gate was unnecessary process cost; the targeted checks and
existing passing evidence should have determined the verification scope.

## Acceptance criteria

**Criterion 1:** met

Expected unchanged Beacon fixture behavior, codebook v1 through group v1,
perception and provenance. Direct HEAD-to-current comparisons show
`beacon-io.mjs` and `beacon-provenance.mjs` byte-identical; the other five
leaves change JSDoc type paths and formatting only. Fixture directories have
no diff. Existing test assertions remain intact; changes retarget imports
and copy the new workspace into temporary roots. The product skeleton suite
passed. An independent focused run also passed eight normative/arithmetic
tests: 104 v1 states, 288 v2 states, 125,970 group vectors, and 589,824
representative v3 residue round trips. This is not an exhaustive enumeration
of the entire v3 state space.

Reproduction:
`node --test --test-name-pattern='normative|104 v1|decoder refuses|v2 exhaustively|group exhaustively|v3 data' packages/skeleton/dist/test/beacon.test.js packages/skeleton/dist/test/control-beacon.test.js packages/skeleton/dist/test/senses-v3.test.js`.

**Criterion 2:** met

Expected one home for the seven Beacon leaves, as scoped by the order's
objective and WO-033's cited Beacon portability item. The recursive grep
test rejects their old skeleton source/dist paths and Beacon dist paths;
it passes after the fresh build. Independently importing each leaf through
its direct source URL and `@dotln/beacons/<leaf>.mjs` returns the identical
ESM namespace. No skeleton dist copy exists. Existing non-Beacon source/dist
pairs remain outside this bounded move, as recorded in D006 and the
operator-deferred follow-up D009; this report makes no whole-repository
single-identity claim.

Reproduction:
`node --test --test-name-pattern='grep scan' scripts/test-beacon-portability.mjs`
passed one test.

**Criterion 3:** met

Expected a real lifecycle transition in a temporary root with no skeleton
source, followed by a successful Beacon decode. The second test in
`scripts/test-beacon-portability.mjs` copies scripts and the Beacon workspace
without node_modules or the skeleton, activates WO-099, and checks public
and verifier Beacons. Both decode as v2 active, unknown verdict/effort,
host-projected, with mtime within the existing sub-microsecond host bound
of the recorded transition. The `beacon-portability` suite passed in
0.83 seconds in the verifier's product run. Its absence assertions and
copied-module decoder were inspected independently.

**Criterion 4:** met

The architecture's Layer diagram and accompanying prose locate the
build-free workspace, distinguish pure codebooks from filesystem effects,
and describe direct lifecycle projection. The skeleton README's
Control-plane Beacons section explains both consumers. D002 in
`docs/evidence/WO-070/decisions.md` and its generated decisions-index row
discharge the pre-2026-09-09 ledger duty under the verifier skill. The
additional domain-model, ADR and roadmap write-backs agree with the move.

**Criterion 5:** met

`npm test` passed: 28 suites, zero failures, 72 fresh tasks, 293,618 ms,
recorded 2026-09-26T00:20:27.210Z. The ignored host gate row records the
same code identity as the subject above:
`host-gate:3679af7623d491a2ad350194eab8698499461be262e4990a98d612e4fcb2cc67:npm test`.
Both `git diff --check` and `git diff --cached --check` passed. A parsed
lockfile comparison confirms all external dependency entries unchanged;
the new dependency is the internal workspace expressly allowed by the
order's design, not a third-party addition. No new lint/type suppressions
were found in the added lines.

## Additional evidence and limits

The consumer review found no actionable defect in build staging, runtime
snapshot copying, authority/qualification roots, release-tag extraction,
or evidence-source inventories. A read-only TypeScript diagnostic probe
found zero baseline diagnostics; a virtual file overlay changing the new
control phase type to number produced five errors inside the leaves plus
consumer errors. Thus the moved JavaScript remains checked without requiring
a mutation of the subject. This confirms the consequential D008 claim,
not its earlier exact count of four diagnostics.

The executor's `implementation.md` reports passing machinery and document
runs. The document gate's host row was read and matches this code identity;
the machinery summary is attributed to executor evidence, not an independent
rerun. The authorized live feedback episode was not repeated. The live smoke
remains unrun. Snapshot identity still omits Beacon-only changes (D005).
D009 retains the deferred plane/kit work; D011 already boards up the carried
feedback-edition test limitation with a named follow-up. No new unresolved
defect was found or repaired in this verification.

Instrumentation disclosure: lifecycle Beacon projection and runtime package
assembly are themselves under review and also supported this dispatch and
its gate. The isolated copied-root fixture, direct source comparisons,
namespace identity checks and independent type probe provide observations
beyond those instruments' own success claims. Generated harness surfaces
are judged through their generation/check evidence; no automatic Codex hook
coverage is claimed.
