# WO-133 VER-002 — independent verification of the repaired stand-down residue

**Verdict: pass.** FINAL-001's blocking F1 is repaired and the repair holds under
an independent gate: `npm test -- --review` is **30 passed, 0 failed, 830.545 s,
74 fresh tasks, exit 0** at code identity
`5778716b726ec3c195a2209795c9571d2b7b9f639a3e515db9ed9a5de849caea`, with
`runner-fixtures` — the suite that failed the review — passing in 33.49 s. The
missing inventory entry is registered (`releaseCases` derives 44 cases and the
expected list now matches, `runtime_refresh` sitting between `stale_helpers` and
`success`, the position the function occupies in the script). The repair's second,
recommended half is real and not decorative: with `scripts/test-release.sh`
declared under `runner-fixtures`, a shell-only change selects exactly
`runner-fixtures`; with that one declaration removed from an otherwise identical
suite table, the same change selects nothing. I ran that negative control myself.
Acceptance criteria 1–7 are met, and criterion 7 — the half FINAL-001 marked not
met — is met at the reviewed identity.

The repair is also correctly bounded. The only source changed since the VER-001
subject is three files (`scripts/test-release-fixtures.mjs` one line,
`scripts/test-runner.mjs` one line, `scripts/test-runner.test.mjs` one new
regression), so criteria 1–6 rest on bytes that have not moved since VER-001 and
that I re-executed here in full rather than carried forward on trust. No component
version, dependency, runtime behaviour or product document changed in the repair.

VER-001's F1 is **not** repaired and was not routed to repair: it remains
as-designed, and it reproduced a third time — in this session, live. It stays a
planner nomination.

Subject: [WO-133](../../work-orders/WO-133-stand-down-residue.md), branch
`wo-133`, uncommitted working tree preserved by
`refs/dotln/checkpoint/WO-133/9` (`9d339993586c52208bdc6561be1e1e8dd3006555`).
The subject is acceptance criteria 1–7 as filed, the repaired subject recorded by
`RepairCompleted` at `2026-09-16T06:11:17.208Z`, decision D005, and FINAL-001's
required item 1 and recommended item 2. This operator's `resume: verify`
allocated VER-002 at `2026-09-16T06:12:22.370Z`. This verifier changed no
implementation, created no branch commit (`git log --oneline main..HEAD` is
empty), ran no dispatch the harness had already recorded, and made no lifecycle
transition other than recording this result. `gateCodeIdentity` was
`5778716b…` before and after every check below — the same identity as the
repairer's passing gate row at `06:09:38.705Z`, so their green run and mine
measure the same bytes.

Verifier: Claude Code 2.1.273, observed with `claude --version`; model
`claude-opus-5[1m]` as this session exposes it; effort `xhigh` read back from
`CLAUDE_EFFORT`. No effective-model readback channel is claimed, so the source is
`self-reported` rather than `operator-attested`. Repairer attestation under
verification: codex-cli 0.154.0, gpt-6-astra, max, `operator-attested`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.273","model":"claude-opus-5[1m]","effort":"xhigh","source":"self-reported"}

Instrument disclosure: the machinery under verification also recorded this
verification. My `npm test -- --review` row was written by the gate-evidence path
this order leaves in place; my writer reservation (`actorId 8cd8eac4…`, owner pid
36945, reserved `06:12:21.607Z`) is this worktree's single reservation; the
advisory marker criterion 1 introduces claimed a `classification` marker for my
session at `06:14:43.729Z` and shaped what I saw for the rest of it; and the
live-gate refusal criterion 1 requires to survive was applied against my own Bash
calls for the fourteen minutes the gate ran. One read-only helper session located
write-back wording while Bash was refused; every write-back sentence quoted or
relied on below I afterwards read myself at current bytes.

## Executed checks

| Command or observation | Result |
| --- | --- |
| `npm test -- --review` (06:16:35Z–06:30:25Z) | **30 passed, 0 failed, 830.545 s**, 74 fresh tasks, exit 0; tree `92c1aaa4…`, ref `host-gate:5778716b…:npm test` |
| `runner-fixtures` inside that gate | **PASS 33.49 s** — the suite FINAL-001 recorded as FAIL |
| `node --test scripts/test-release-fixtures.mjs` | 3 passed, 0 failed, 0.749 s |
| `releaseCases(repo)` via `scripts/lib/release-fixtures.mjs` | 44 cases; `… createrecovery stale_helpers runtime_refresh success …` |
| `bash scripts/test-release.sh --case runtime_refresh` | 6 of 6 scenarios passed, 13.54 s wall; worst scenario close-changed 1,958 ms; close-matching 1,894 ms; finish-changed 541 ms; finish-matching 382 ms; finish-snapshot 540 ms |
| `node --test --test-name-pattern "release shell changes select their inventory guard" scripts/test-runner.test.mjs` | 1 passed, 0 failed, 0.428 s |
| Negative control: `changedMachinery` on a synthetic Git repo, shell-only change | declared → `["runner-fixtures"]`; the same table with `scripts/test-release.sh` removed → `[]`; no other machinery suite declares it |
| `expandSuiteTasks(["runner-fixtures"])` | `node --test scripts/test-runner.test.mjs scripts/test-release-fixtures.mjs scripts/test-gate-deadlines.mjs` — the new regression and the inventory guard are both in the selected suite |
| `npm test -- --review --list` | 30 suites selected, including `release`, `harness-fixtures`, `process-debt`, `runner-fixtures` |
| `node scripts/harness.mjs check` | exit 0; 24 generated surfaces |
| `npm run publication:check` | PASS; 30 and 45 linked source sections current |
| `git diff --check` | clean before and after the gate, exit 0 |
| `git log --oneline main..HEAD` | empty — no branch commits before final review |
| `git diff HEAD -- package-lock.json` | one internal component version (`@dotln/compiler` 0.11.1→0.11.2); no dependency added, removed or re-resolved |
| `git diff refs/dotln/checkpoint/WO-133/3 -- packages scripts .claude .agents README.md docs/product docs/PLAYBOOK.md package*.json` | 3 files, +61/−1 — the whole source delta since the VER-001 subject |
| Added-line scan for `eslint-disable`, `@ts-ignore`, `@ts-expect-error`, `@ts-nocheck`, `prettier-ignore` | none introduced |
| `gateCodeIdentity` before and after my gate | `5778716b…` unchanged; equals the repairer's 779.219 s passing row |
| Live `SessionStart` probe, this worktree, healthy pins | `{}` — no runtime message, no dispatch |
| Live `harnessRuntimeCause(repo)` | `null` |
| Live hook journal, this session at 06:32Z | 1,107 rows; 24 advisory rows; 5 distinct messages; one marker file |
| `grep` pin over source for the three release literals | `HARNESS_HOST_VERSION = "0.18.4"` only in `packages/skeleton/src/version.ts`; `harness-host.ts` re-exports it, `contributor.ts` imports it for both profiles' `skeletonVersion` |
| All twelve generated role files (`.claude/skills`, `.agents/skills`) | each carries the operator-attested fallback line and the publication-check line |
| `npm run work-orders -- index` | regenerated; projection now reads `verifying` / VER-002 pending |

My gate at 830.545 s sits between the reviewer's 828.592 s and the repairer's
779.219 s at the same 74 fresh tasks; the spread is host load, not scope.

## The repaired defect

FINAL-001 F1 was that `scripts/test-release.sh` gained
`release_case_runtime_refresh()` while the hard-coded inventory at
`scripts/test-release-fixtures.mjs:25` still listed 43 cases, so the guard
asserted 43 against 44 and `runner-fixtures` failed.

**Required item 1 — met.** The expected string now carries `runtime_refresh`
after `stale_helpers`. I derived the inventory from the shell script directly and
got 44 cases in that order, and the guard passes standalone in 0.749 s as well as
inside the gate.

**Recommended item 2 — taken, and it works.** `scripts/test-release.sh` is now a
declared source of `runner-fixtures`. I did not take the fixture's word for this.
In a temporary Git repository containing only that script I added one release
case and called `changedMachinery` twice: once with the shipped suite table, once
with a table identical except that `scripts/test-release.sh` is stripped from
`runner-fixtures.sources`. The first returns `["runner-fixtures"]`, the second
returns `[]`. That is FINAL-001's diagnosis reproduced and closed: before the
repair a release-case addition selected no suite that checks release cases; after
it, it selects exactly the one suite that owns the inventory, and no other.

The new regression in `scripts/test-runner.test.mjs` asserts the same thing
through both the selector and the `--review --list` entry point, and asserts the
expansion reaches `scripts/test-release-fixtures.mjs`. It runs inside
`runner-fixtures`, which the declaration now selects — the guard and its selector
are in the same suite, so the next shell-only change exercises both.

D005 records the decision, the two rejected alternatives, NoOp, and a reopening
condition covering both directions (a release-case change evading the guard, and
the new declaration costing unrelated machinery work). It is projected into the
decisions index and the followups file.

D005 declines FINAL-001's observation 7 — declaring `scripts/reactor-identity.mjs`
and `scripts/fixtures/historical-compiler-loader.mjs` — on the ground that they
already execute in every product gate. I checked that rather than accepting it:
`packages/skeleton/test/reactor-slices.test.ts:43` spawns
`scripts/reactor-identity.mjs --check`, and the `skeleton` package suite is a
product suite selected in every run. The distinction from F1 is the right one:
there, the *cases* ran every gate in the product `release` suite while the
*inventory guard* sat unselected in machinery; here the check that would catch a
change runs unconditionally.

## Acceptance criteria

| # | Criterion | Verdict | Evidence |
| --- | --- | --- | --- |
| 1 | One advisory per stale session, twenty journal rows, silent observers, second session, refusals intact | **met** | `harness-fixtures` passed in my gate, carrying the WO-133 stale-hooks fixture and the unwritable-marker case; both kept refusals passed, and the live-gate refusal fired against my own Bash calls for the gate's duration. Source unchanged since VER-001 verified it; the suppression still sits at the single output boundary after the journal append, and `showHarnessAdvisory` returns false for `PostToolUse`. |
| 2 | `SessionStart` names the cause when pins differ, is silent when they match | **met**, with a named limit | fixture asserts both halves inside `harness-fixtures`; the matching half I observed live — the registered handler returned `{}` with `harnessRuntimeCause` `null`. The stale half stays fixture evidence (L1, L2). |
| 3 | Close and finish rebuild only when the fast-forward changed the pins; manifest cites the rebuilt runtime; under 120 s | **met** | I ran the six-scenario case directly: one build for changed and snapshot-missing, none for matching, `harnessRuntimeCause === null` re-asserted afterwards, failure preserving the worktree; worst scenario 1,958 ms against the 120 s ceiling. `ensureReleaseRuntime` builds on a non-null cause and re-asserts, running after `updateMain` and before `prepareManifest`; `worktree finish` calls `refreshHarnessRuntime` against the main checkout and throws with the worktree preserved. The fixture is now registered, which is what F1 was about. |
| 4 | Role text in all six roles; `resume` keeps supplied values and maps the legacy token | **met**, with a named limit | twelve generated files carry both lines, including the verifier text this dispatch loaded; `parseActor` records `--model` and `--effort` verbatim and maps only `operator-selected-model-effective-effort-unavailable` to `operator-attested`, with no path rewriting a supplied value to `unknown`; the `resume` suite passed in my gate. Post-merge observations remain open (L3). |
| 5 | Version-only change selects no machinery suite; host change still selects two; grep pin | **met** | `runner-fixtures` passed, carrying both the existing version-only regression and the new release-shell regression; I confirmed independently that the three literals exist only in `version.ts` and that no machinery suite declares any prefix of it. The new `scripts/test-release.sh` declaration does not touch this: it is disjoint from `version.ts`. |
| 6 | Bundle and manifest pass `harness check`; write-backs land | **met** | `harness check` exit 0 at 24 surfaces; `publication:check` PASS. I read each write-back at current bytes: 07 §Discipline (the four cause classes, the journal-row rule, the observers, fail-open marker storage, `SessionStart`, the two helpers' rebuild, the attestation fallback, the publication-check line) and 07 §Write once run once (the version module), 02 §Harness compiler v1, the playbook's release task and its worktree-finish sentence, both component READMEs, `docs/README.md`, the decisions file, and the decisions index including D005. |
| 7 | `npm test` green; `git diff --check` clean; no new dependency | **met** — the half FINAL-001 found not met | 30 passed, 0 failed, 830.545 s, exit 0 at identity `5778716b…`; `git diff --check` clean before and after; the lockfile moves one internal component version only. |
| Operator-review assumptions 1–3 | unchanged and consistent | assumption 3's escape hatch was not needed; role text was the right place. |

## Findings

None blocking. Nothing from this dispatch routes to repair.

### Carried forward — VER-001 F1, every non-runtime advisory shares one per-session marker (non-blocking; as designed; planner nomination)

Unchanged at source and reproduced a third time, now in this session. The marker
key is the session id plus a cause that is the reason class only for
`pins-differ`, `snapshot-missing` and `runtime-unavailable`; everything else —
`authority`, `unreadable-state`, `finish`, `feedback`, `observe`, `session` —
collapses into the single literal `classification`.

My journal at 06:32Z held 1,107 rows, 24 of them advisory rows carrying five
distinct messages, and exactly one marker file existed for my session, claimed at
`06:14:43.729Z` by a shell-classification advisory on my first compound Bash
command. Everything after it was written and not shown:

```
06:14:43.729Z PreToolUse  feedback     command classification: Shell control syntax …  ← claimed the marker
06:17:44.011Z PreToolUse  permission   compiled authority does not permit settings.user ← suppressed
06:18:30.624Z Stop        finish       pending no-partial-completion, verify-app-…      ← suppressed
06:20:08.536Z Stop        finish       pending no-partial-completion, verify-app-…      ← suppressed
06:24:17.965Z Stop        finish       pending no-partial-completion, verify-app-…      ← suppressed
06:28:10.438Z Stop        finish       pending no-partial-completion, verify-app-…      ← suppressed
```

Three consecutive sessions on this order — the VER-001 verifier, the FINAL-001
reviewer and me — have each had WO-132's completion-discipline advisory silenced
for their whole dispatch by an unrelated first advisory. In mine it was silenced
four times while I was deciding whether this work is complete. It remains
delivered as the order specifies: the Design section names `classification` as one
of four cause classes, no acceptance criterion requires repeat non-runtime
advisories to survive, and product 07 discloses the four classes. Judging that
design is planning's call. Repair shape if planning wants it, unchanged from
VER-001 and FINAL-001: key the marker on the advisory's own identity, or exclude
non-runtime causes from suppression, since the measured flood is carried wholly by
the three runtime causes. Reopening observation: a one-session regression emitting
two distinct non-runtime advisories and expecting two visible messages.

## Observations

**O1 — the release-facing wording still does not say which advisories share the
`classification` bucket.** `README.md` says "Runtime advisories appear once per
session and cause"; product 07 lists the four cause classes but not that
`authority`, `unreadable-state`, `finish`, `feedback`, `observe` and `session` all
land in the fourth. FINAL-001 recorded the same gap. The repair's scope was the
blocking defect, so this correctly stayed untouched; it belongs with the F1
nomination.

**O2 — VER-001's O2 stands.** `scripts/lib/harness-runtime.mjs` accepts a pinned
runtime file only if it matches
`^packages/(?:compiler|skeleton)/dist/src/[a-z-]+\.(?:js|mjs)$` and returns
`runtime-unavailable` otherwise. All pins match today. A future pinned file whose
basename carries a digit, an underscore or a capital would read as
`runtime-unavailable`, and `refreshHarnessRuntime` would build once and throw,
failing every release close and worktree finish until the pattern widens. Loud and
checkout-preserving, which is the right direction, but it now gates both helpers
that move main. Reopening observation: the first pinned runtime file outside
`[a-z-]`.

**O3 — `version.ts` is outside every machinery declaration by construction.** That
is criterion 5's point and its latent risk; the file is two lines of release
metadata and nothing enforces that. Reopening observation: any executable
statement added to it. Unchanged from VER-001 O3.

**O4 — the new declaration has a small recurring cost, and D005 owns it.** Every
future change to `scripts/test-release.sh` — a 1,564-line script that release work
touches often — now also selects `runner-fixtures` (33.49 s in my gate) in a
reviewer gate. That is the price of closing the gap, it is bounded, and D005's
reopening condition names it. No normalisation applies to a `.sh` file in
`changedMachinery`, so any byte change selects the suite; the projection failure
path selects conservatively, unchanged.

**O5 — advisory markers accumulate without cleanup.** Three `.advisory` files now
sit in `docs/control/local/harness/` — the VER-001 verifier's (04:58Z), the
reviewer's (05:28Z) and mine (06:14Z). Ignored local state, tiny files, nothing
prunes them. Housekeeping, not a defect; same as FINAL-001's observation 3.

**O6 — read-only Bash was refused throughout my own gate.** For fourteen minutes
`sed`, `grep` through a pipe, `ps` and `node -e` were all refused with
`DOTLN_HARNESS_REFUSED: write may change gate inputs during active gate`. This is
the conservative shape of the kept refusal, pre-existing, matching VER-001 O5,
FINAL-001 observation 4 and WO-132 VER-001 F2; this order changes no classifier
and its non-goals exclude it. I used the host's file reader for those reads and
lost no coverage. Recorded so it is not read as a regression of this order.

**O7 — clean-room screen.** Every line added by the repair is project-internal:
one inventory token, one declared source path, and a fixture that creates a
temporary Git repository with `fixture@example.invalid`. No employer material,
credential, internal URL, private identifier or secret; no stop condition.

## Limits

**L1 — no stale-runtime observation in a live host session.** Every claim about
what a stale worktree shows comes from fixtures and from source. Producing the
stale state live would mean damaging this worktree's pinned runtime, which a
verifier must not do. My live observations cover the healthy path only.

**L2 — criterion 2's stale half is fixture and documentation evidence**, as in
VER-001 L2. FINAL-001 closed half of it by observing a real `SessionStart` journal
row in a live session; I observed the handler returning `{}` under healthy pins.
The rendering of a `SessionStart` `systemMessage` under stale pins remains
unobserved, because the healthy path emits none. First real Claude session on
merged main settles it.

**L3 — criterion 4's post-merge observations are open by construction.** This
dispatch adds a second Claude-side datum: my completion records
`claude-opus-5[1m]` / `xhigh` rather than `unknown`. The Codex side after merge and
the first post-merge close remain the reopening observations, as does the next
skeleton reviewer's gate duration for criterion 5's cost claim.

**L4 — timings are this host under load.** My gate ran alongside nothing else of
mine, but the release case I ran directly overlapped no suite either; the recorded
durations are observations of these runs, not estimates of an isolated gate. The
only timing a criterion depends on is criterion 3's 120 s ceiling, measured at
1,958 ms worst case.

**L5 — process cost.** `node scripts/harness.mjs usage bf2c5472-…` at
`2026-09-16T06:32:34.078Z`, source `claude-transcript-message-usage`, scope
`dispatch`, cutoff that timestamp and therefore excluding this report's own write:
7,428,576 total tokens (146 input, 7,276,927 cached input, 119,604 cache writes,
31,899 output) across 88 steps and 67 commands. Dollar cost is unavailable and
therefore unknown; no context-byte counter was exposed. The dominant cost was the
830 s review gate. I judged it necessary rather than reproducing only the failing
suite: criterion 7 asks for a green `npm test` at the reviewed identity, and the
whole reason this order returned from final review is that a narrower command
missed the defect. The three targeted reproductions and the negative control cost
under a minute between them and are what make the repair's second half verified
rather than assumed. One read-only helper session ran while the gate held Bash;
its output changed no file and I re-read its subject myself.
