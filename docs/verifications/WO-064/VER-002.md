# WO-064 VER-002 — independent verification after VER-001 repair

**Verdict:** pass. The repaired target publish path refuses a same-head acceptance matrix whose criteria differ from the published WorkOrder, before a remote call. The five acceptance criteria are supported by the focused fixtures, the full product gate, the recorded operator smoke and the write-back checks, within the limits below.

**Subject:** [WO-064](../../work-orders/WO-064-target-publish.md) on `wo-064`, repository HEAD `15fa8a7953707d6caa768a0edc01b6b6678238a9`, staged at `refs/dotln/checkpoint/WO-064/6` (`863eaf9f67295d78eb195cde5d667d727319a3fd`). I judged the current authored `scripts/lib/target-publish.mjs`, `scripts/github-body.mjs`, `scripts/github-repository.mjs`, `scripts/worktree.mjs`, their fixtures and runner registration, and the WO-064 write-backs. [VER-001](VER-001.md) is the prior failed verification. I made no implementation or fixture changes.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-6-sol","effort":"xhigh","source":"codex-session-readback"}

Human actor: the operator dispatched `resume: verify` in this attended Codex session. The dispatch briefing read back the current model, effort and CLI version. No subagent was launched: observed count 0 of cap 20, with unknown unobserved remainder. A USD 5 hard provider cap was unavailable on this transport, so no cap is claimed.

**Process cost:** entry 52649 tokens; handoff 1779946 tokens; source codex-transcript-counter

Both readings are dispatch-scoped `node scripts/harness.mjs usage 01a0ca7a-15f9-79e0-94d0-92cc8db8daac` observations. The entry cutoff was 2026-09-22T18:57:31.986Z; the handoff reading cutoff was 2026-09-22T19:05:15.209Z. Dollar cost and wrapped command count are unavailable, not zero. Final counters remain in ignored receipts and the response.

## Goal-aligned judgment

The benefit is a governed source-change branch becoming a target pull request whose body accurately states the WorkOrder's acceptance evidence. VER-001 showed rule beating and seeking the wrong goal: a matching revision could carry unrelated verified criteria. D009 binds descriptions as an exact multiset at the host and generator seams and renders them in contract order; the focused negative case and the full gate now support that narrower claim. Policy resistance is limited by reusing the compiled authority, kernel guard and outward lint. Commons cost is one additional fixture episode and the observed gate time, without a dependency or agent. The passing negative case resists drift to low performance. No new retry or process loop adds escalation; the existing artifact generator is retained because its source and output were checked, not merely because it is incumbent (success to the successful). A mismatched supplied store refuses with a named reason, so the operator does not have to handwrite or diagnose an outward PR body (shifting the burden). Naive Interventionism favors the bounded seam check over changing the skeleton's verification projection in this order; D009 names when a stronger identity binding should reopen. NoOp would leave VER-001's false acceptance claim in a PR.

## VER-001 finding F1 — repaired

**Criterion and expected result:** Criterion 1 requires the generated body to describe the current WorkOrder's contract and acceptance evidence. A matrix for the same head but another criterion set must not assert verification of this WorkOrder, and refusal must precede remote calls.

**Observed and reproduction:** `acceptanceStatuses()` in `scripts/github-body.mjs` now compares matrix row descriptions to the WorkOrder acceptance criteria as an exact multiset, returns statuses in contract order and rejects a missing, extra or altered criterion. `generateTargetPullRequest()` rejects a mismatch. `acceptanceMatrix()` in `scripts/lib/target-publish.mjs` applies the same check to the replayed store before `ensureGh`, `git push` or `gh pr create`. The direct generator case repeats VER-001's one-criterion `Input is rejected safely` versus same-head `Unrelated behavior works` with `verified`: it throws. The host fixture replays a same-head verification opening with unrelated criteria and observes refusal, no `gh` invocation, no pushed branch and no publication log. A reordered matching matrix publishes the WorkOrder criteria in contract order with three `incomplete` statuses. `node --test scripts/test-github-body.mjs` passed 16/16; `node --test scripts/test-target-publish.mjs` passed 5/5.

**Judgment:** F1 is closed for the recorded subject. This description binding matches the compiler's snapshot coverage check in `packages/compiler/src/verification.ts`. It does not claim that a revision or criterion text cryptographically identifies a WorkOrder; D009 records the condition for a stronger projection binding.

## Acceptance evidence

| Criterion | Observed evidence and judgment |
| --- | --- |
| 1, grant, lint and target publication | The five target fixtures exercise no grant, a host-policy grant, an edited contract, a moved branch, absent local terms, a `vocabulary.local` finding, a matrix for another revision, and VER-001's same-head mismatch. Each refusal precedes `gh` calls and a pushed branch. The granted path pushes only the observed commit, calls the `gh` stub for one PR with the byte-pinned generated body, records one `PullRequestOpened`, and a retry pushes nothing. **Pass.** |
| 2, outward body | The pinned body passes the outward lint; the fixture excludes launchpad vocabulary, DotLn paths, local store paths, test commands and worker narrative. The generator cases check literal Markdown escaping, status correspondence and lint refusal for launchpad terms. **Pass for exercised output.** |
| 3, live smoke | [smoke.json](../../evidence/WO-064/smoke.json) records an operator-run empty scratch GitHub repository, the observed branch pushed, an open PR with matching head, base, title and pinned body shapes, and one matching event. [smoke.mjs](../../evidence/WO-064/smoke.mjs) shows the queries and comparisons that produced those shapes. The repository and PR identity are intentionally omitted, so I verified the record and script, not the remote independently. The writer was a deterministic double, and the scratch grant was explicit host input to the same publish function the CLI calls. **Pass within those recorded limits.** |
| 4, write-backs | Product 07 §Workflow closeout and releases describes target publication and its repaired matrix binding; product 02 §Actors and episodes defines `PullRequestOpened`; the capability table has a dated `delivery.pull-request` row that names VER-001 and D009. This pre-2026-09-09 order's ledger duty is discharged by [decisions.md](../../evidence/WO-064/decisions.md) and its generated index row. `npm run publication:check` passed 274/274 product heading links and both publication views. **Pass.** |
| 5, gate and dependencies | `npm test` passed 26 suites, 0 failed, 70 fresh tasks in 327.06 s at this subject. After the report was filed, `npm run test:docs` passed 20 tasks, 0 failed in 30.05 s. `git diff --check` exited 0. `package.json` and `package-lock.json` have no diff; no dependency was added. **Pass.** |

## Instrument and limits

`scripts/test-runner.mjs` is in the subject because it registers the target publish suite and recorded the full gate. I inspected its registration diff and ran both target suites directly; those direct runs passed. The operator smoke is shape evidence, without a verifier readback of the private remote. D006 separately boards up the skeleton's refusal of registry-bearing artifact identities; this publish path uses a grant-free writer compilation, and that existing limit is not a new finding. A crash after PR creation and before `PullRequestOpened` remains the executor's disclosed manual-inspection window. This verdict judges the current staged subject and grants no authority to publish, merge or release a target.
