# WO-148 VER-002 — independent verification

**Verdict:** pass. The repaired implementation satisfies all six acceptance
criteria. VER-001's three findings were independently rechecked at the current
subject: every stored mission-source field now participates in freshness,
successful checks of moved stores print the checked path, and bind refuses a
Git-visible configured control lane before writing either path-bearing file.
No new substantive defect was found.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-5.6-sol","effort":"xhigh","source":"codex-session-readback"}

Human actor: the operator dispatched `resume: verify` in an attended Codex
session. The current-session readback supplied the harness, version, model and
effort. No subagent was spawned; the explicit fan-out plan was one verifier and
one writer in this worktree, so the observed count is 0 of the cap of 20 and
the unobserved remainder is unknown.

**Process cost:** unknown; cause harness-no-readback

The first entry reading reported every token counter unavailable. After the
explicit verifier session began, a later pre-report reading observed 2,885,611
total tokens from `codex-transcript-counter`, dispatch scope, cutoff
2026-09-21T20:03:56.344Z. Because no entry total exists, there is no complete
entry/handoff pair; unavailable is not zero and did not block the verdict.

## Subject

- Work order: `docs/work-orders/WO-148-resident-binding.md`.
- Branch `wo-148`; `HEAD` and `origin/main` are both `c84db382efd6`. The
  implementation, evidence and projections remain uncommitted, as required
  before final review.
- Verification dispatch: `VER-002`, checkpoint 7. The original order bytes are
  unchanged from VER-001's checkpoint. VER-001 and its boarded D012–D014
  findings were read before judgment; the repair is recorded by D015–D019 and
  `docs/evidence/WO-148/repair-001.md`.
- The current command, fixtures, live-row collector and row, product/runbook
  write-backs, decision record, release surfaces and generated indexes were
  read. This report is the only implementation-adjacent file authored by this
  verifier.

## Goal-aligned outcome

The delivered command makes WO-099's mission check usable on an actual active
order without hand-writing a compiled store, while retaining explicit surfaces,
immutable stores and an operator-selected transport. NoOp would preserve the
three false guarantees VER-001 reproduced: a fresh result over a retargeted
subject, successful instructions for a missing old path, and a supposedly
private store visible to Git. The repair is the smallest useful intervention:
compare the complete declaration, print the checked directory, and ask Git
before writing.

That directly answers rule beating and shifting the burden to the intervenor.
The other system-trap lenses do not change the judgment: the repair adds no
authority, gate, hook, key, scheduler or recurring resident work; it does not
derive surfaces, rebind automatically, change the resident host or decide
pruning; and it preserves the existing command and store mechanism instead of
creating a competing path. The command is an externalizable interface with a
declared store/binding contract, consumed by this repository's fixture and live
row, and its output is usable without reconstructing the authoring session.

## Instrument disclosure

- `scripts/test-runner.mjs` and `scripts/test-resident-bind.mjs` are part of the
  subject and produced green results. I did not treat those results as
  self-authenticating: I inspected the implementation and fixtures, ran a
  separate field-by-field mismatch probe, and exercised all four retained real
  WO-148 stores through the shipped `--check` command.
- `scripts/evidence-resident-binding.mjs` produced criterion 4's live row and is
  also part of the subject. Its source and the filed row are byte-identical to
  VER-001's recorded subject; the repair changes checking and a pre-write ignore
  refusal, not the capsule, store shape or resident launch path.

## Executed evidence

| Check | Result |
| --- | --- |
| `npm test` | **Pass: 23 suites, 0 failed, 269.43 s, 67 fresh tasks.** The `resident-bind` suite passed inside it in 5.58 s. |
| `node --test --test-reporter=spec scripts/test-resident-bind.mjs` | **Pass: 13 tests, 0 failed, 4.67 s.** |
| Independent prior-finding probe | Pass: changing `decisionsPath`, `decisionLimit`, `visionPath`, `thesisHeadings`, `declaredSurfaces` or adding `storyPath` produced a named store mismatch; generated launch lines used only the supplied checked path. |
| Four retained WO-148 stores | Each decoded successfully, exited 1 for the expected stale phase, and printed no launch line. |
| Configured-root privacy cases | The focused suite proved both sides: an unignored configured lane refused before creating its local directory and left Git status clean; the same root with its local lane ignored bound successfully, both files matched `git check-ignore`, and Git status stayed clean. |
| `git diff --check` | Pass before this report. |
| Dependency and suppression scan | No manifest or lockfile changed; no lint, type or formatter suppression directive exists in the three authored scripts. |
| `node scripts/release.mjs check-surfaces --local` | Pass: application `v0.40.0`, minor classification, no component bump required, workspace pins and publication surfaces valid. |
| `node scripts/check-publication.mjs` | Pass: 273/273 product headings indexed; both edition source locks current. |
| `node scripts/meta.mjs --check` | Pass at the pre-report cutoff. |
| Current changed-file path scan | No actual operator worktree, session or scratch path appears. The two matches are VER-001 and D011 describing their own `/Users/` redaction checks. |

## Prior findings

### F1 — resolved — complete mission-source freshness

**Observed now.** `checkBinding` decodes `resident.json` with
`decodeResidentConfiguration`. `bindingMismatches` compares `root`,
`contractPath`, `decisionsPath`, `baseCommit`, `declaredSurfaces`,
`decisionLimit`, `visionPath` and `thesisHeadings`; arrays are structural, and a
source field absent from the binding is named. The integration fixture mutates
each consequential field independently and withholds every launch line. The
independent direct probe reproduced the same refusal.

**Expected.** A successful freshness check must cover every stored declaration
that selects the dispatch-time mission subject and the store must still be a
configuration the runtime accepts.

**Severity after repair:** none. VER-001's high-severity false-green path is no
longer reproduced.

### F2 — resolved — relocated stores print the checked path

**Observed now.** `checkBinding` returns the resolved checked directory, and the
success path supplies it to every launch, tick, presence, status, export and
recheck line. The original bound path appears once as provenance. The real-Git
fixture moves a store, checks the new directory and asserts every actionable
line names the new directory while none names the directory it left.

**Expected.** If relocation is accepted, every actionable line must open the
store that was checked.

**Severity after repair:** none. VER-001's moderate unusable-instructions path
is no longer reproduced.

### F3 — resolved — the configured lane must be ignored before bind writes

**Observed now.** `assertIgnoredStore` asks `git check-ignore -q` about the
future `resident.json` and `binding.json` paths before creating the store. A
visible path or a Git query error refuses by name. The negative configured-root
fixture leaves no local lane and a clean Git status; the positive fixture binds
only after the matching ignore rule exists.

**Expected.** A command that writes physical worktree and store paths must prove
that both files are outside Git's visible surface before either is created.

**Severity after repair:** none. VER-001's high-severity privacy failure is no
longer reproduced.

## Acceptance criteria

| # | Verdict | Independent evidence |
| --- | --- | --- |
| 1 | **Met.** | The real-Git fixture writes a runtime-decodable store, checks the complete `MissionSource`, hand-built pin equivalence, shipped status/once reads and every printed operational line. |
| 2 | **Met.** | Unknown/closed orders, absent worktree/surfaces, stale phase/worktree/contract/base and retargeted stored-source fields refuse by name without launch output; rebind creates the next store without changing the first. |
| 3 | **Met.** | Default and configured lanes are checked with Git before writing; negative and positive configured-root cases pass, filed live evidence remains redacted, and the current changed-file scan finds no actual physical path. |
| 4 | **Met.** | The unchanged `live.json` records one real in-flight bind and shipped away/once path, actor-origin Codex launch, 20/20 changed paths inside 15 declared surfaces, matching base, returned `drift` verdict and hold, label `observed`, with explicit limits and no physical path. |
| 5 | **Met.** | Product 03, the skeleton runbook, candidate disposition and D001–D019 are present; release, publication and metadata checks pass. |
| 6 | **Met.** | Current full product gate 23/23 green; `git diff --check` clean; no dependency, manifest or lockfile change. |

## Limits and handoff

- Criterion 4's 981-second live observation was not rerun. The collector and
  filed row are byte-identical to VER-001's subject, and the repair does not
  change the constructed capsule, binding record or resident dispatch. The new
  bind-time behavior is only the pre-write ignore refusal, exercised on both
  configured-root sides by the current fixture.
- D009's `claude-cli-print` duplicate-enum defect remains a separately boarded
  shared-schema follow-up. It is outside this order's declared no-schema-change
  scope; the criterion 4 row used the unaffected `codex-cli-exec` transport.
- The carried planning advisory about `adjacent-0002` using prose rather than
  its synthesized FUP identifier does not lose the follow-up: D009 and the
  public follow-up both name it. Its queue-field cleanup requires a later fixer
  or planner phase and is not an acceptance failure of this binding command.
- No automatic rebinding, surface derivation, pruning, daemon or resident-host
  refusal is claimed. Final review remains a separate operator dispatch.
