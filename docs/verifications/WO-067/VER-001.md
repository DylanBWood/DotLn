# WO-067 VER-001 — independent verification of the compiled PresencePolicy

**Verdict: pass.** All six acceptance criteria and the bounded adjacent repair are met at the current subject. A fresh `npm test` passed (19 groups, 0 failed, 240.90 s). The harness test file containing the adjacent-repair regression passed separately (26 of 26). Twenty-four hostile compile probes of my own refused or admitted exactly as the order and product 02 specify. A mutation run on a scratch mirror of the built compiler killed all nine deliberate breaks of the lowering, so the fixtures exercise the behaviour, not only its shape. There are no findings. O1–O5 below are observations and limits for the reviewer and WO-068, not defects.

Subject: [WO-067](../../work-orders/WO-067-presence-policy-compiled.md), branch `wo-067`, uncommitted working tree preserved by `refs/dotln/checkpoint/WO-067/3` (`f95cd86449da50f1ecab17f963d5a3ab4b912ab6`). `HEAD` is `7aeccb1`, equal to `main`; `git rev-list --count main..HEAD` is 0. The subject is acceptance criteria 1–6 as filed plus the 2026-09-15 bounded adjacent repair section. The operator's `resume: verify` allocated this report at 2026-09-15T18:22:25.138Z. This verifier changed no implementation and created no commit. Its only repository writes are this report, the `npm run work-orders -- index` projection and the lifecycle result record. Probes and mutations ran on scratch copies outside the worktree.

Verifier: Claude Code 2.1.272, observed with `claude --version`; model `claude-opus-5[1m]` as this session exposes it. Effort: the operator's `/model` output set `xhigh`, and the observed `CLAUDE_EFFORT` readback is `xhigh`. No other effective-session readback is claimed.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.272","model":"claude-opus-5[1m]","effort":"xhigh","source":"self-reported"}

Instrument disclosure: the Claude hooks this order regenerated served this session. Its adjacent repair installed runtime snapshot `.runtime/harness/f1f7fc0e67e4664f`, and those hooks recorded the dispatch, reserved this session's writer (pid 44589), and refused my Bash commands while my own `npm test` gate was live. That is incidental corroboration of criterion-level repair behaviour, and it is also the instrument under verification. No subagents were used.

## Executed checks

| Command or observation | Result |
| --- | --- |
| `npm test` (this session, completed before 18:32Z) | **19 passed, 0 failed; 240.90 s; 62 fresh tasks; exit 0** |
| `node --test scripts/test-harness.mjs` (not an `npm test` group) | 26 passed, 0 failed; 195.5 s; `ok 1 - WO-067 compiler-only release installs a fresh compatible harness snapshot` |
| `node scripts/harness.mjs check` | 24 generated surfaces checked |
| `npm run publication:check` | pass; 264/264 product headings indexed; both TOC locks current |
| `git diff --check HEAD` | clean, exit 0 |
| `git diff HEAD -- packages/kernel` | 0 lines — kernel unchanged |
| `git diff HEAD -- package-lock.json` | only `@dotln/compiler` 0.10.0→0.11.0 and the skeleton's pin to it — no new dependency |
| `npx --no-install prettier --check` over changed source, fixtures and product docs | all files formatted |
| Verifier probe script (24 compile cases, cadence walk, compatibility, full render) | all as specified — see criteria 1, 2, 4 |
| Verifier mutation run (scratch mirror, compiler + skeleton presence tests) | baseline 11/11 pass; **9/9 mutations killed** |
| `ls .runtime/harness/` | previous snapshot `7b3c264bb69b6f3f` retained beside new `f1f7fc0e67e4664f` |

## Criteria

### 1 — three-phase fixture compiles with per-phase narrowing, executable cadences and a pinned tooltip: **met**

- `packages/skeleton/fixtures/wo067-presence.json` compiles to phases `probe`, `widen` and `peak`, with effective file ceilings `[1, 3, 8]` under base `8`. Every phase retains the base's `repo.delete` denial, `verified-input` evidence, `AuthorityRevoked` revocation and `expiresAt` 100000 (`packages/compiler/test/presence.test.ts:44`).
- Narrowing is asserted per phase with the real kernel authorizer. For five effects in every phase, "narrowed authorizes ⇒ base authorizes" holds. The file resource is exhausted at exactly the phase ceiling, and missing evidence refuses (`packages/skeleton/test/presence.test.ts:407`).
- Cadences: my walk of every emitted phase cadence and `idleCadence` found only `After` and `Gate`, and both are in `EVALUABLE_CADENCE_KINDS` (`packages/kernel/src/core.ts:60`). The schema admits only `Once`, `After`, `Every`, `Gate` and `Until` (`packages/compiler/src/presence-schema.ts:89`). My probes confirmed `Calendar`, `Backoff` and `Window` entries refuse. The skeleton test evaluates every phase cadence with `evaluateCadence` (`:462`).
- Tooltip: the render I reproduced places `+ Presence fixture.progressive v1: progressive; absence → probe; verified success → next; failure or peak → probe; axes=…` plus one line per phase under **PULSE**. Under **INTERRUPT** it places `+ Presence fixture.progressive: cancel-on-return; return precedes dispatch/outcome; …` plus per-phase kill/finish lines. `renderCompiledDiff` is the only tooltip renderer; the console (`packages/console/src/builds.ts:173`) and skeleton CLI (`packages/skeleton/src/cli.ts:93`) both call it, so no second surface omits the curve. Limit: see O1.

### 2 — widening refuses with `AUTHORITY WIDENING`; unknown curve or return rule refuses naming the field: **met**

These phase envelopes all refused with `AUTHORITY WIDENING`:
- a literal base-denied effect, `repo.*` over an exact base list, and bare `*`
- `repo.delete` under a base `repo.*` that denies it
- an ungranted `portfolio.publish`
- raising `files`, or creating a positive `newResource`, `__proto__` or `hasOwnProperty` limit (the committed test adds `toString` and `constructor`)
- `expiresAt` 100001 against base 100000

The following were admitted:
- `expiresAt` equal to the base, an empty allow set, and an empty envelope at the peak (equal to the base)
- a new resource at 0
- `repo.de*` under a base `repo.*`, which still inherits the `repo.delete` denial

The curve and return-rule cases refused with a field path:
- an unknown curve: `INVALID GRAPH: presence: invalid [0].curve`
- a missing curve: the same message
- an unknown return rule: `… [0].returnRule`

### 3 — the curve proven as data: **met**

The committed transcripts (`docs/evidence/WO-067/presence-fixtures.tap`) show:
- **Hold:** `NoOp:present` while present.
- **Verified-only advance:** an unverified outcome and a foreground outcome leave `probe`; a verified outcome advances to `widen`.
- **Failure reset:** `failure:probe`.
- **Peak reset and loop:** two full `probe → widen → peak → probe` loops.
- **Return races:** both orderings; `in-flight:kill` in `probe`, `in-flight:finish` in `widen` with no overlapping dispatch; the requested foreground task completes under its own separate envelope.
- **Decay:** no expiry at 99, expiry at 100, and a fresh return/absence edge needed to rearm.
- **Unavailable adapter:** `NoOp:… unavailable external capability: adapter.portfolio` under an admitted portfolio grant.

These ran green in my gate and again in the mutation baseline. The mutation run shows each behaviour depends on the emitted lowering (`packages/compiler/dist/src/presence.js` in the mirror). Each of these nine mutations failed at least one presence test:
1. expiry check removed
2. own-property resource guard replaced by `in`
3. base denials dropped from the effective envelope
4. peak stays at peak
5. failure advances
6. kill replaced by finish
7. NoOp availability suppressed
8. idle deadline doubled
9. allowed-effect widening check removed

Limit: see O4.

### 4 — committed semantic hashes exact; three editable views round-trip `presence`: **met**

- Seiri compiles to `fnv1a64:9ca8d0229c6bd8db` with and without `presence: []`, and the program carries no `presence` key. A fixture compile is deterministic (`fnv1a64:b4f9b111da86c248` twice).
- No existing test file under `packages/*/test` is modified (`git diff --stat HEAD`). The unchanged hash-pinning suites therefore pass at `HEAD`'s expectations in my gate. That covers the planRefuter `fnv1a64:4c45aa92547659f9` already migrated on `main` by WO-132 AC11, which [authority edition 002](../../evidence/WO-067/authority/002/authority.json) records as its source.
- The console selfhost repin changes two values, labelled `feedback-v1 policy hash` and `verification-v1 input hash`. Both embed `COMPILER_PACKAGE_VERSION` (`packages/compiler/src/feedback.ts:196`), and its `loadoutHash` cell is `null`. No `semanticHash` line in `.claude/harness-manifest.json` changed. No program hash moved.
- Views: serialized code DSL, function table and statechart JSON each compile deep-equal to the direct compile. Normalization is idempotent; `[]` and absent normalize identically; reversing phase order changes the result (`packages/compiler/test/presence.test.ts:283`).

### 5 — write-backs: **met**

| Duty | Where it landed |
| --- | --- |
| 02 §LoadoutGraph v1 payload contract | `presence?: readonly PresencePolicy[]` in `LoadoutGraphV1`, a new `#### Compiled presence policies` subsection, and the PresencePolicy row moved from candidate to compiled subset |
| 03 §Operator-presence policy | new "Compiled subset (WO-067, 2026-09-15)" paragraphs |
| 04 §RPG view | new "Presence curve (WO-067)" bullet |
| 10 §Separate version axes | new WO-067 paragraph |
| ADR-0007 §Amendments | a 2026-09-15 note |
| Ledger duty (order filed before 2026-09-09) | discharged by `docs/evidence/WO-067/decisions.md` D001–D003 and their three decisions-index rows; the work-order index row marks the substitution |
| Publication | new audience-status row for `02#compiled-presence-policies`; both TOC locks updated; `publication:check` passes |

The 06 roadmap release note is an additional write-back.

### 6 — `npm test` green; `git diff --check` clean; no new dependency; kernel unchanged: **met**

See the executed checks table.

### Bounded adjacent repair (2026-09-15, WO-067-D003): **met**

- `scripts/lib/harness.mjs:81` adds `packages/compiler/dist/src/artifact-identity.js` to the runtime pins.
- The regression at `scripts/test-harness.mjs:280` bumps only the compiler version and re-emits. It asserts a new snapshot while the old snapshot's bytes are preserved, then invokes the emitted writer hook and requires it to be allowed without `unavailable`/`bootstrap` and to actually reserve its writer. It passed in my run.
- The nine regenerated hooks point at `f1f7fc0e67e4664f`; the prior snapshot remains on disk; `harness check` passes.

## Observations (non-blocking)

- **O1 — tooltip pinning is partial.** The compiler test pins the tooltip by regex: the PULSE and INTERRUPT header lines, the probe ceiling and two dispositions. It is not a golden render. My full render shows the complete curve, so the criterion's behaviour holds. A later render change to the widen or peak authority lines would not fail a test.
- **O2 — non-discretionary phases also leave on return.** I compiled a non-discretionary `probe`. Its `return` transition is `cancel-pending, reset-progress, preserve-foreground → present`: no kill or finish, and the in-flight episode continues. This is consistent with hold (no phase while present) and with "requested foreground continues". No committed fixture exercises a non-discretionary phase.
- **O3 — scope is policy data, not checked against the base.** `scope.changeSize` and `scope.budget` are not compared with the base envelope (`packages/compiler/src/presence-schema.ts:202`, `presence.ts` has no check). A phase could declare a host ceiling above its effective `resourceLimits`; authorization still caps at the envelope, but the tooltip would show both numbers. Per ADR-0007 scope grants no effect, so this is not a widening. WO-068 should decide which ceiling wins when they disagree.
- **O4 — the fixture host interprets emitted data.** `current-policy-episode-while-absent`, the idle in-flight exclusion and `returnPrecedence` are enforced by the test host, keyed by the compiled guard names and flag. Resident enforcement is WO-068, as the order's non-goals and the evidence README state.
- **O5 — refusal names the offending key.** A `Calendar` entry refuses naming `.entry.cadence.expression`, the first key the admitted kinds do not allow, rather than `.kind`. It still names a field. This is outside criterion 2, which concerns curve and return rule.

The operator-review assumption (the probe → widen → peak → reset reading) remains for the final reviewer; this report does not confirm it on the operator's behalf.

## Goal alignment and cost

This order is the first link of the unattended chain (WO-067 → WO-068 → WO-099). A verified compiled policy lets the resident host consume inspectable, narrowing-only absence behaviour instead of prose. NoOp would leave that chain without an independently checked contract.

Rule beating was the material trap for this verification, so the mutation run was chosen to show the evidence cannot pass without the lowering. Escalation and commons cost were bounded:
- one fresh product gate
- one harness test file outside that gate
- scratch-only probes

The other lenses did not change a judgment here.

Usage at the last observation before writing (source `claude-transcript-message-usage`, scope `dispatch`, cutoff 2026-09-15T18:32:28.910Z): 2,625,735 total tokens, 42,101 output tokens, 59 steps, 31 commands; cost unavailable. Wall-clock spent waiting included the 240.90 s gate and the 195.5 s harness test file. Final counters belong to the ignored receipts and the handoff response.

## Reproduction

```sh
npm test
node --test scripts/test-harness.mjs
node scripts/harness.mjs check
npm run publication:check
git diff --check HEAD
git diff HEAD -- packages/kernel package-lock.json
```

The verifier probe and mutation scripts were scratch files, not committed. Their cases are enumerated under criteria 1–4 so any verifier can reconstruct them against `packages/compiler/dist`.
