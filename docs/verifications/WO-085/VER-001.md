# WO-085 VER-001 — independent verification: fail

**Subject:** the uncommitted `wo-085` worktree at HEAD `f73b7e184b38de9cab97b4e86c718b6006f6b19d` (equal to `main`), after `resume: next` and before repair. The `verify` dispatch allocated this report and checkpoint `refs/dotln/checkpoint/WO-085/3` (`4068d8e4`). The judged working tree differs from that checkpoint only by the dispatch's own `VerificationRequested` event and its two projections (`docs/control/current.md`, `docs/work-orders/README.md`); every untracked file hashes equal to the checkpoint except the order's event log. The product-gate code identity is `8ae4123ef427539d122bdbca4150792f502b11f7708576375d06ee910ac97b96`, the same identity the executor's final gates recorded. I judged the five criteria of [WO-085](../../work-orders/WO-085-spec-receipt-boundary.md) against the original order, its Objective and Design, the cited WO-090-D007 and WO-153-D008 records, the current implementation and its fixtures, and disposable probes.

**Verifier:** Claude Code 2.1.283, Claude Opus 5.5 (`claude-opus-5-5`). The session effort was selected as `ultracode`; `CLAUDE_EFFORT` reads `xhigh`, recorded as mode `subagents`. Two workflows ran read-only agents: four probes, one per lens (product shapes and exemptions; control files; links and anchors; write-backs and gate hygiene), then two refuters that tried to refute all seven candidate defects from an authority lens and a reproduction lens. The root verifier ran both gates, reproduced the consequential claims (F1, F2, F3) directly, and owns the judgment and this report. Harness readback: 6 subagents observed, 14 remaining against cap 20. The transport exposed no hard USD cap, so a USD 5 limit was unenforced.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.283","model":"claude-opus-5-5","effort":"xhigh","mode":"subagents","raw":"ultracode","source":"claude-session-readback"}

**Instruments under verification that recorded this work:** the subject's `docs-check` suite and its runner registration ran inside the `npm run test:docs` rows cited below. My criterion judgments rest on direct runs of `scripts/docs-check.mjs`, disposable fixture repositories and independent scanners, not on those rows' verdicts.

**Verdict:** **Fail.** Criterion 1 has two reproduced defects (F1, F2). Three reproduced defects in otherwise met criteria (F3, F4, F5) are routed to the same repair. [D008](../../evidence/WO-085/decisions.md#wo-085-d008) records the repair follow-up, and [D009](../../evidence/WO-085/decisions.md#wo-085-d009) boards the non-blocking observations O1–O14. The verifier changed no implementation.

## Evidence and checks

- `npm test` (outside any sandbox; the runner's probe found none in force), 04:16:21Z: 27 passed, 1 failed, 376.36 s, 72 fresh tasks. The failure was `console` › `WO-114 text host refreshes after atomic replacement` (`status watch timed out`, its 2 s budget, under 72 concurrent tasks). `npm test -- --only console` then passed alone (2 passed, 3.21 s). The order changes nothing under `packages/`. The full rerun at the same tree and code identity, 04:58:23Z: 28 passed, 0 failed, 299.52 s, 72 fresh tasks.
- `npm run test:docs`, 04:17:24Z on the post-dispatch tree: 14 passed, 9 failed. `docs-check` failed on one line, `docs/work-orders/README.md:89: missing file: ../../docs/verifications/WO-085/VER-001.md`; the other eight (`entropy`, `plan`, `registrations`, `plan-refutation-current`, `console-docs`, `skeleton-docs`, `lineage-fixtures`, `docs-check-fixtures`) did not run (`Required preflight ... failed ... docs-check`). All fifteen product rows were within their ceilings. See F3. Rerun after this report and D008/D009 were written and `npm run meta` refreshed the decisions index and register: 23 passed, 0 failed, 28.69 s, 23 fresh tasks. An intermediate run failed only `meta`, on this report's then-unfilled cost line.
- `node --test scripts/test-docs-check.mjs`: 21 passed, 0 failed (two lenses; writes only to the temporary directory, worktree status unchanged).
- `node scripts/docs-check.mjs`: its table is byte-identical to the executor's [transcript](../../evidence/WO-085/docs-check.txt); the only difference is the pending-report line above.
- A copy of checkpoint 3 (`git archive 4068d8e4`, fresh `git init`) checked with `checkDocs`: no failures, 40 historical link occurrences.
- `git diff --check` exit 0; the ten untracked files carry no whitespace errors (`git diff --no-index --check /dev/null <file>`). `package.json` and `package-lock.json` are unchanged; Prettier 3.9.6 is a pinned devDependency, and `prettier/plugins/markdown.mjs` resolves through its `./*` export. `prettier --check` passes on the 23 changed and new files; Markdown is prettier-ignored, so that judges the scripts and JSON.
- Disposable fixtures ran in fresh `git init` repositories under the session scratch directory; nothing ran `meta`, `release`, a `resume` transition or a Git write against this worktree before this report (read-only `resume status` ran).

## Criterion judgments

**Criterion 1:** unmet. The executor's fixtures fail for each listed case: over ceiling (`scripts/test-docs-check.mjs:86-94`), new receipt and new candidate (`:96-132`, `:487-493`), broken anchor and missing file (`:192-204`), and an unprefixed dispatch (`:249-277`). At the recorded subject the check passes. But it passes the current tree only because an undeclared rule admits 308 links that are missing in every renderer (F1), and a hand-written marker pair defeats the ceiling, receipt and candidate failures the criterion lists (F2).

**Criterion 2:** met. All fifteen `docs/product` documents are named, and none extra (a recursive listing finds fifteen). An independent byte count without the check's parser equals every `nonExemptBytesAtLanding`; for 06, 149,907 bytes less the 59,833-byte §Release boundary slice (line 21 to line 815) gives 90,074. Every ceiling equals `ceil(nonExemptBytesAtLanding × 1.02)` in float and integer arithmetic. An omitted document fails with `missing ceiling`, including a nested one. An activation-style paragraph under the real 06 §Release boundary with the real controls passes, and so does `release prepare`'s collision note in both of its placements.

**Criterion 3:** met. Product 07 §Documentation freshness and ownership keeps the edit-in-place rule and now names `scripts/docs-check.mjs` in `npm run test:docs` and the ceilings file. Product 08 §Freshness and ownership adds "Edit the sentence that changed in place" and names the check and both control files. `docs/README.md` links the check and both control files. One sentence there is inaccurate (F5).

**Criterion 4:** met. WO-085 was filed on 2026-09-08 (`6ab27c46`), so the ledger duty is discharged by the decisions file and index rows. The work-order index marks the substitution, and `docs/lineage/decisions-index.md` carries WO-085-D001 through D007. Both edition locks changed, and the `publication` suite passed. `FUP-87ed701db7d7209e` carries a `duplicate` disposition targeting `FUP-71fc2efc208f597a`, whose `deferred` disposition names the historical operator-chat sweep and the role-text question. `duplicate` is the only status the register schema lets target another FUP (`scripts/lib/planning-followups.mjs:290-299`), with the FUP-0054 precedent.

**Criterion 5:** met. `npm test` is green at the recorded code identity: 28 passed, 0 failed at 04:58:23Z, after one earlier run with a timing failure in an unchanged `console` test (O13). `npm run test:docs` failed on the post-dispatch tree only because of the dispatch's pending report link (F3). Rerun with this report and D008/D009 in place, it passed 23 of 23. `git diff --check` is clean, and there is no new dependency.

## Findings routed to repair

**F1 — criterion 1, medium. A final-review PR body's `docs/…` links are judged from the repository root, admitting 308 broken links outside the declared inventory.** Observed: `scripts/docs-check.mjs:247-259` resolves a `docs/`-prefixed href in any `docs/final-reviews/**/PR.md` against the repository root. A plain `grep` finds 308 such links in 24 PR bodies (WO-063, 064, 070, 100, 111, 114, 120, 131, 138, 147–161). An independent scanner and both refuters found all 308 missing relative to their file; `linkFailures()` reports none, and the historical inventory (39 pairs, 40 occurrences, all `missing anchor`) does not declare them. Consequence: `gh api` for the WO-070 pull request returns `href="docs/evidence/WO-070/decisions.md#wo-070-d003"` unchanged. Resolved against the pull-request URL it redirects to a new-pull-request page. In the repository view it resolves beside `PR.md` and returns 404. Neither the publish path (`scripts/worktree.mjs`, `scripts/github-body.mjs`) nor any decision rewrites or adopts the rule. The executor's fixture (`scripts/test-docs-check.mjs:216-220`) enshrines it, and D003's "No missing files were found" holds only because of it. Expected (Design, "as WO-090 D007"; D002 rejects wholesale exemptions): closed reports' broken links are declared historical exceptions, and a new one fails. Limit: no single relative form resolves in both the repository view and the PR page, so the repair has to choose which one the check judges and record that choice.

**F2 — criterion 1 and the Objective, medium. Any hand-written marker pair exempts its contents.** Observed: `productContent` treats any top-level `<!-- name:start -->` / `<!-- name:end -->` pair as generated (`scripts/docs-check.mjs:82-97`, `122-123`). Reproduction: a fixture document at its exact ceiling gains `<!-- notes:start -->`, `**WO-999 receipt (2026-09-27):** …`, `## Candidate — smuggled`, 5.8 KB of prose and `<!-- notes:end -->`. `checkDocs` returns no failures and 5,822 exempt bytes. The same text without markers fails for the ceiling, the receipt and the candidate. A refuter reproduced this on a copy of product 07 with the real controls (5,716 exempt bytes, no failures). No product document contains a marker today, and no script writes one into `docs/product`: the only product writer, `scripts/lib/release-preparation.mjs`, writes an unmarked 06 note. Expected (Cost: "Exempt: generated blocks between markers"; the evidence README and `docs/README.md` say "named generated blocks"): only a registered generator's markers are exempt.

**F3 — routed; criteria 1 and 5 hold at the recorded subject; medium. The document gate fails throughout every verifying and final-review window.** Observed: the generated index links an allocated report before it exists (`scripts/work-orders.mjs:436-438`, `527-528`), and control state sets the verification and final-review paths at their `Requested` events. `docs-check` refuses the link. Because it is a preflight, eight document suites wait on it and are skipped. Reproduction: the `test:docs` row above. At checkpoint 3 the index reads ready-to-verify and the check passes; creating the report clears the failure. The executor's green `test:docs` ran before any path was allocated, and no fixture covers a pending index row. Expected: the evidence README claims coverage of "generated indexes", and WO-090-D007 asked for a declared failure mode for generated links; the generated index should resolve in every lifecycle phase. Consequence: a verifier or reviewer who runs the document gate before filing sees red for their own pending report, and the dependent suites are masked. The first `npm run test:docs` of this verification is an instance.

**F4 — routed, low. A leading byte-order mark disables receipt and candidate detection.** Observed: with a leading U+FEFF, the pinned parser's offsets and the raw source slices disagree by one character, so the `**` and `##` prefix tests (`scripts/docs-check.mjs:126-143`) fail for every node in that document. Reproduction: on a copy of product 07 with the real controls, a new dated receipt and a new Candidate heading fail twice without the BOM and pass with it. `prettier --check` accepts and keeps the BOM (`*.md` is prettier-ignored), so no other suite catches it. No tracked Markdown file has a BOM today. D006 rejected leaving reproduced edge cases as parser limitations.

**F5 — routed, criterion 3 entry point, low. `docs/README.md` misstates the PR-body rule.** Line 75 says "Published PR bodies also resolve repository-root links". The check root-resolves only `docs/` paths: a fixture `PR.md` with `[x](README.md)` and `[y](scripts/tool.mjs)` fails as missing files while `[z](docs/a.md)` passes. Neither GitHub nor the publish path resolves PR-body links from the root (F1). Product 07 §Documentation freshness makes this the executor's fact to keep current.

## Observations (non-blocking)

Boarded by [D009](../../evidence/WO-085/decisions.md#wo-085-d009). Source: R = reproduced by the verifier or two lenses, L = one lens's reproduction, S = source reading.

- **O1 (Design gap, R).** The receipt shape the Design names matches few of the paragraphs the order's Observed gap counts. An AST count finds 192 dated bold-led paragraphs, 90 inside 06 §Release boundary. The check matches 28 of the other 102 (the Design's literal pattern would match 15) and baselines exactly those 28. New paragraphs in ten existing non-matching forms pass, for example `**Amendment (2026-09-17, vision-into-use pass).**` (07:745) and `**Near-term planning request, 2026-09-11:**` (05:1115). The per-document ceilings still bound their bytes.
- **O2 (Design gap, R).** Of the order's 64 counted Candidate headings, 59 use `## Candidate —` or `### Candidate —` and are baselined. `#### Candidate —` (03:415), `### Candidate role —` (05:737), `### Candidate extension —` (05:1627), `## Candidate first …` (11-protino, 11-proteino) and setext forms pass when added.
- **O3 (planning, R).** The 06 §Release boundary exemption is unconditional (`scripts/docs-check.mjs:98-114`). `doc-ceilings.json`'s policy string names WO-086, but WO-086's order text never names retiring the exemption. Removing its 545 exempt lines leaves 06's counted bytes at 90,074, so its criterion-4 ceiling cannot fall unless the exemption goes too. A simulated post-WO-086 roadmap with new handwritten receipts under the heading passes.
- **O4 (limit, R).** The control-file guards compare with `HEAD` or are absent. A raise of `ceiling` and `nonExemptBytesAtLanding` is refused uncommitted but passes once committed. Entries added to `doc-baseline.json` in the same change admit a new receipt, an unprefixed dispatch and a broken link. A renamed or new product document gets a fresh ceiling from its own declared size. Under "no branch commits before final review" the uncommitted comparison holds at every executor and verifier gate, so none of this falsifies a criterion.
- **O5 (scope, L).** Only root and `docs/` Markdown is scanned. 34 tracked Markdown files lie outside it, including 11 hand-written anchors from package READMEs into product documents. Those resolve today; a later heading rename would break them unnoticed. This matches WO-090-D007's scope but not the title's "every in-repo link".
- **O6 (latent, L).** Heading slugs are trimmed after character removal, unlike github-slugger, for headings that begin or end with an emoji or punctuation after a space (no current occurrence). File existence is judged on the local case-insensitive disk and includes ignored files. Raw HTML `href`/`src` is not checked (none today).
- **O7 (L).** Unused historical link exceptions are not reported, so a stale entry keeps admitting one occurrence of its pair.
- **O8 (L).** WO-090-D007's 41 reconciles: 40 closed-report occurrences plus one planning-document link this order repaired.
- **O9 (S).** The evidence README's gate figures are true for the executor's subject and stale for the verifying tree (F3; the console timing failure).
- **O10 (L).** `release prepare` looks for the activation paragraph across the whole roadmap. An activation paragraph outside §Release boundary would put the collision note outside the exemption.
- **O11 (L).** Render-equivalent labels (`***…***`, `__…__`) and out-of-Design placements (a table cell, HTML `<strong>`, a colon outside the bold) are not receipts.
- **O12 (L).** `operator direction` anywhere in a bold label is a receipt even when undated.
- **O13 (R).** The product gate's `console` › `WO-114 text host refreshes after atomic replacement` timed out once at its 2 s watch budget while 72 tasks ran. It passed alone and in the full rerun at the same code identity. This order does not change it; nominate it if it recurs.
- **O14 (S).** The two adjacent link repairs touch product 05 and a planning document, although "the planning documents" is a non-goal. The non-goal fences the check's scope. The repairs are one fragment each, recorded in the adjacent queue as `adjacent-0001` with its check-in and completed checks, and no closed report changed.

## Limits

- I did not observe client-side behavior on the GitHub PR page; F1's consequence rests on the served `href`, the redirect response and the repository-view 404.
- The order's counting method for "183 dated bold paragraphs" and "64 Candidate headings" is unknown; O1 and O2 compare shapes, not exact totals.
- Final-review behavior is inferred from the index generator and control state; I observed only the verifying window.

**Process cost:** entry 93,531 tokens; handoff 25,286,276 tokens; source claude-transcript-message-usage
