# WO-085 VER-002 — independent verification: pass

**Subject:** the uncommitted `wo-085` worktree at HEAD `6a5c323df4e2db298ec3ec0b16be61c6374b9cb8` (equal to `main` in both directions), after the 2026-09-26 `resume: fix` that repaired VER-001 F1–F5 and integrated `main` under D010. The `verify` dispatch allocated this report and checkpoint `refs/dotln/checkpoint/WO-085/8` (`22f8364c`). The judged working tree differs from that checkpoint only by the dispatch's own `VerificationRequested` event and its two projections (`docs/control/current.md`, `docs/work-orders/README.md`); a recursive comparison of `git archive` of the checkpoint against the tree finds no other tracked or untracked difference. I judged the five criteria of [WO-085](../../work-orders/WO-085-spec-receipt-boundary.md) against the original order, its Objective and Design, the cited WO-090-D007 and WO-153-D008 records, [VER-001](VER-001.md)'s findings and observations, the repair decisions [D010–D014](../../evidence/WO-085/decisions.md#wo-085-d010), the current implementation and fixtures, and disposable reproductions.

**Verifier:** Claude Code 2.1.283, Claude Fable 5.1 (`claude-fable-5-1`). The session effort was selected as `ultracode`; `CLAUDE_EFFORT` reads `xhigh`, recorded as mode `subagents`. One workflow ran read-only agents: four probes, one per lens (product shapes, exemptions and encodings; links, anchors and historical exceptions; lifecycle index, integration producer, imported path and gate registration; write-backs, controls and decision records), then two refuters that tried to refute every candidate from an authority lens and a reproduction lens. The root verifier ran the gates and the focused suites, reproduced the F1–F5 repairs directly, and owns the judgment and this report. Harness readback: 6 subagents observed (four probes, two refuters), 14 remaining against cap 20, no unresolved admissions; the harness attributes them as six unlinked children with the uncounted remainder unknown. The transport exposed no hard USD cap, so a USD 5 limit was unenforced.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.283","model":"claude-fable-5-1","effort":"xhigh","mode":"subagents","raw":"ultracode","source":"claude-session-readback"}

**Instruments under verification that recorded this work:** the subject's `docs-check` suite and its runner registration ran inside the `npm run test:docs` rows below. My criterion judgments rest on direct runs of `scripts/docs-check.mjs`, disposable copies of the real documents with the real controls, and independent scanners that do not use the check's parser, not on those rows' verdicts.

**Verdict:** **Pass.** All five criteria are met at the recorded subject. VER-001's five findings reproduce as fixed on copies of the real documents with the real controls, and the document gate is green on a live verifying window before this report existed. No finding routes to repair. [D015](../../evidence/WO-085/decisions.md#wo-085-d015) records the pass and boards the ten non-blocking observations V1–V10. The verifier changed no implementation.

## Evidence and checks

- `npm run test:docs` on the verifying tree before this report existed, 19:56:10Z to 19:56:42Z: 23 passed, 0 failed, 30.73 s, 23 fresh tasks. This is the F3 reproduction on a live verifying window; VER-001's first run of the same gate on the same window failed on the pending report link. The index row read `Verification: VER-002 (pending).` as plain text.
- `npm test` (outside any sandbox; the runner's probe found none in force), started 19:59:05Z: 28 passed, 0 failed, 333.30 s, 72 fresh tasks. The `console` timing failure VER-001 saw once (O13) did not recur.
- `npm test -- --review` (the product selection plus changed machinery), started 20:36:48Z: 34 passed, 0 failed, 597.60 s, 78 fresh tasks, including `configuration-root`, `worktree-integration` and `runner-fixtures`. The executor's run of the same selection preceded the D014 repair and failed only `configuration-root`.
- `node --test scripts/test-docs-check.mjs`: 24 passed, 0 failed. VER-001 judged 21; the three added cases cover handwritten marker pairs, one to three leading BOMs, and file-relative PR-body links with counted exceptions.
- `node scripts/docs-check.mjs`: PASS, 15 product documents, 412 declared historical link occurrences, 0 failures; its table is byte-identical to the executor's ignored scan record.
- `bash scripts/test-work-orders.sh`: 23 passed. `node --test scripts/test-worktree-integration.mjs`: 9 passed. `node --test scripts/test-configuration-root.mjs`: 13 passed. `node scripts/authority-evidence.mjs --check`: passed with no retained-evidence fallback and no re-mint. `npm run publication:check`: PASS, 276 of 276 headings indexed, both edition locks current.
- Independent byte count without the check's parser: every `nonExemptBytesAtLanding` equals the file's bytes, and for 06 the 152,451 total less the 62,377-byte §Release boundary slice gives 90,074; every ceiling equals `ceil(bytes × 1.02)`; fifteen files, fifteen entries.
- Link-exception integrity: `linkFailures()` on the tree yields 412 failures under 368 exact (file, href, reason) keys; the baseline declares the same 368 keys with identical counts, so 0 stale, 0 undeclared, 0 duplicate entries; all 44 exception files are tracked and unchanged against HEAD; the 34 owning orders are all `closed` in canonical status. The README's split reconciles: 40 anchor occurrences, 347 in stored PR bodies, 25 in the imported WO-115 release notes.
- Dispatch integrity: 789 decision rows; 743 fingerprinted, of which 127 have non-conforming shapes, all in committed unchanged files of 23 closed orders; the 46 unfingerprinted rows all pass `validDispatch`; no stale or orphan fingerprint. The two fingerprints added since VER-001's checkpoint are WO-115-D016 and WO-165-D007, both committed at HEAD and imported by the integration. D010–D015 all carry a control prefix and a paraphrase.
- Controls against VER-001's checkpoint 4: the baseline's product shapes are byte-identical; the ceilings differ only for 04 and 07, as D012 records. Verified at the bases: 04 is 51,582 bytes at `f73b7e18` and 59,662 at `6a5c323d`; 07 is 182,283 and 184,641, plus the order's 63-byte binding sentence, giving 182,346 and 184,704. The upstream diff under `docs/product/` touches 04, 06 and 07 only; the working tree differs from HEAD in 05 (one anchor), 06 (two notes inside the exempt section), 07 and 08 (the bound sentences).
- Disposable reproductions on a copy of `docs/` with the real controls in a fresh `git init` root: F1, a new PR body's `docs/…` href fails as `missing file`, its `../../…` form passes, a wrong anchor fails as `missing anchor`, and one extra occurrence of the declared WO-070 exception fails. F2, a handwritten marker pair holding a receipt, a candidate and 5 KB of prose on product 07 fails three times (1,420 bytes over ceiling, new candidate, new receipt) with 0 exempt bytes and the exact byte count. F4, a leading U+FEFF on product 07 with a new receipt and candidate still fails twice and counts the three BOM bytes; a probe confirmed a mid-file U+FEFF, CRLF and CR endings shift no offset. F5, each sentence of `docs/README.md` lines 62–79 was checked against the code path it describes.
- The lifecycle probe replayed a full cycle in a scratch control log: a pending verification and a pending final review render as plain text, a completed report links whether its verdict is pass or fail, and a linked report deleted afterwards fails as `missing file`; the resume tool refuses to record a completion without the report, so that is the only way the failure can arise. The integration decision stub emits `resume: fix; …` or `resume: final review; …` for exactly the two phases `integrateWorktree` admits, and the configured-root helper returns the previous literal in the default layout.
- `git diff --check` exits 0 for the staged and unstaged diffs; no untracked file has trailing whitespace or lacks a final newline. `package.json` and `package-lock.json` are unchanged. `git log main..wo-085` and `wo-085..main` are both empty. The named stash `aabda54a` and checkpoints 1–8 are present. The adjacent queue records items 0001–0003 completed with exit-0 checks.
- Nothing ran `meta`, `release`, a `resume` transition or a Git write against this worktree before this report (read-only `resume status` ran). The hook refused every non-read command while each gate ran; I waited for the gates instead of stopping them. The probes wrote only under the session scratch; `git status` was identical before and after.

## Criterion judgments

**Criterion 1:** met. On fixtures the check fails for a document over its ceiling (`scripts/test-docs-check.mjs:86-94`), a new dated receipt and a new candidate (`:96-133`), a broken anchor and a missing file (`:192-204`), and a dispatch without a control prefix (`:249-277`); 24 of 24 pass. It passes on the current tree for declared reasons: the 412 admitted occurrences are exact, counted, committed exceptions in closed orders' reports (F1), no marker pair exempts anything (F2), and a leading BOM run no longer disables detection (F4). Each was reproduced on a copy of the real documents.

**Criterion 2:** met. All fifteen `docs/product` documents are named and none extra. Every `nonExemptBytesAtLanding` equals an independent count and every ceiling equals `ceil(bytes × 1.02)`; the landing values were reconciled to the integrated base by D012 for 04 and 07 only, and both figures agree with Git at the two bases. An omitted document fails with `missing ceiling`. An activation-style paragraph under the real 06 §Release boundary passes, and so does `release prepare`'s collision note in both of its placements.

**Criterion 3:** met. Product 07 §Documentation freshness and ownership keeps the edit-in-place rule and names `scripts/docs-check.mjs` in `npm run test:docs` and the ceilings file. Product 08 §Freshness and ownership says "Edit the sentence that changed in place" and names the check and both control files. `docs/README.md` links the check and both controls, and its sentences now state what the code does: file-relative resolution including stored PR and release bodies, pending report IDs as plain text until a verdict, no marker exemption (F5). The product edits replace sentences in place; no dated paragraph or candidate was added.

**Criterion 4:** met. WO-085 was filed on 2026-09-08 (`6ab27c46`), so the ledger duty is discharged by the decisions file and index rows; `docs/lineage/decisions-index.md` carries WO-085-D001 through D014 with resolving anchors, and D015 joins them at this report. Both edition locks changed and `publication:check` passes. `FUP-87ed701db7d7209e` carries a `duplicate` disposition targeting `FUP-71fc2efc208f597a`, whose `deferred` disposition names the historical operator-chat sweep and the role-text question. The repair changed none of this.

**Criterion 5:** met. `npm test` is green at the recorded subject (28 passed, 0 failed). `npm run test:docs` is green on the verifying window before this report existed and again with this report in place (20:47:36Z: 23 passed, 0 failed, 30.82 s, 23 fresh tasks). `git diff --check` is clean, and there is no new dependency.

## Findings routed to repair

None.

## Observations (non-blocking)

Boarded by [D015](../../evidence/WO-085/decisions.md#wo-085-d015). Source: R = reproduced by the verifier or two lenses, L = one lens's reproduction, S = source reading. Each sits on a seam D009 already sends to planning or is a zero-occurrence variant of a boarded class; none touches the Design's literal shapes or a criterion.

- **V1 (O11 class, R).** An invisible prefix before the bold label (U+200B, U+2060, U+200D, U+00AD, NBSP, `&nbsp;`, a mid-file U+FEFF) or inline HTML before it makes the paragraph's first child a text or html node, so a new dated receipt passes; two ASCII spaces, a tab or an NBSP between `Candidate` and the dash, or U+2011 hyphens in the date, pass as new shapes. Reproduced on a copy of product 07 with the real controls; no such form exists in `docs/product`. "Renders identically" is inference from CommonMark's flanking rules and the parser's own AST; no renderer is installed.
- **V2 (O3 class, R).** Demoting or blockquoting the heading that terminates 06 §Release boundary (`06-roadmap.md:848`) extends the exemption over 23,150 bytes and reclassifies five baselined shapes as exempt; the printed table shows the exempt-bytes jump, but nothing refuses it.
- **V3 (O4 class, R, dormant).** `planningDecision()` accepts any resolving anchor under `docs/planning/`, including an untracked, gitignored or symlinked file and an unrelated heading, while `markdownFiles()` excludes ignored files. No raise guard runs on the current tree because the ceilings file is untracked at HEAD and every ceiling equals the formula; it governs every raise after landing.
- **V4 (O7 class, R).** Unused historical exceptions are still not reported. The inventory grew from 39 pairs to 368 pairs and 412 occurrences, all exact today, so a later deletion or rewrite of a closed report would leave a silent admission.
- **V5 (L).** The sixteen closed orders since WO-147 wrote PR bodies with root-style `docs/…` links, which the repaired rule refuses in a new body; only `docs/README.md` names the file-relative form. WO-085's own final review is the first affected window, and the refusal names the file and line. Neither form resolves on the GitHub PR page (VER-001 F1's limit); the file-relative form resolves in the repository view.
- **V6 (O6 class, L).** A link to an ignored or private file without a fragment passes when the file exists locally, and a root-absolute `/docs/…` href resolves against the repository root, which matches GitHub's blob view but is not stated in `docs/README.md`. Zero occurrences of either.
- **V7 (S).** The order's Cost and Design still describe an exemption for "generated blocks between markers"; the repaired check has none because no generator writes into `docs/product`, and today's byte count is identical either way. WO-086's generated roadmap history is the likely first producer; D011's reopening condition covers it.
- **V8 (S).** The repaired subject retains no fixture transcript: `fixtures.txt` and `docs-check.txt` describe the pre-verification subject and say so, and the 24-test result is a README table row. This report's live rerun stands as the transcript.
- **V9 (S).** The generated index shows only the latest verification, so VER-001 is no longer linked there once VER-002 is allocated (pre-existing at HEAD; the evidence README and D008 link it). The executor edited `docs/evidence/WO-085/README.md` thirty seconds after its last document gate, which the `RepairCompleted` advisory records; the gate is green on the verifying tree.
- **V10 (S).** `main` at `6a5c323d` fails its own `configuration-root` machinery suite on the WO-165 literal that D014 repairs; `npm test` is unaffected because that suite is machinery. The reviewer's integration carries the one-line fix to `main`.

## Limits

- I did not observe the GitHub PR page or blob view; V5 and V6 rest on VER-001's served-href observation and on GitHub's documented root-relative rule as recalled, not fetched.
- No CommonMark renderer is installed, so every "renders identically" claim in V1 is inference from the flanking rules and the pinned parser's AST.
- Criterion 2's "at landing" is judged on the uncommitted tree; a product edit before the commit that lands the controls changes the bytes the ceilings were set from. D012's reopening condition covers a further integration.
- The executor's "two read-only helpers, no descendants" is session-attested; the ignored subagent record for that session lists none, and Codex accounting is advisory.
- Final-review behavior of the index is replayed from the fold in a scratch control log, not observed on this order's own final-review window.

**Process cost:** entry 97,616 tokens; handoff 7,298,223 tokens; source claude-transcript-message-usage
