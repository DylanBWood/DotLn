# WO-121 VER-002 — independent verification of presence signals with origin

**Verdict: pass.** All six acceptance criteria are met and reproduce on the
current bytes. VER-001's single blocking finding, F1, is discharged: the release
fixture case that failed 3 of 3 runs then now passes for me in both environments
I ran it in, and the repair that fixed it changed no presence behavior, no
production release code and no component version. The presence work VER-001
found sound is still sound after the repair; the one fixture the repair touched
still carries criterion 2 without the assertion it removed.

Subject: [WO-121](../../work-orders/WO-121-presence-signals-with-origin.md),
branch `wo-121` at `1a2d309`, which is also the merge base with `main`. All work
is preserved uncommitted: `git log --oneline main..HEAD` is empty, so no branch
commits exist before final review. The subject is 51 changed files, +1413/-480
against its base. Checkpoint `refs/dotln/checkpoint/WO-121/7`
(`d088d8c24d9b1515ab4f3ac7626582be2f1b67ce`). I judged the acceptance criteria
of the order as filed; I made no edit to the order, the implementation, any
fixture or any earlier report.

Actor: this Claude Code session, harness `2.1.274` read back from
`claude --version`, model `claude-opus-5[1m]` as the session exposes it. No
effort selector was supplied to this dispatch and this harness gives no
effective-effort readback, so effort is `unknown`; I did not carry VER-001's
`ultracode` forward, because that was a different session's operator selection
and attesting it here would be an invented readback. The order recommends
verifier xhigh+; the completion command's advisory about that recommendation is
expected and correct. Under verification: the executor attestation (codex-cli
0.154.0, gpt-6-astra, `xhigh` with mode `subagents`, raw `ultra`, source
`codex-session-readback`) and the repair attestation, which is identical.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.274","model":"claude-opus-5[1m]","effort":"unknown","source":"self-reported"}

Instrument disclosure: the machinery under verification is installed in the
session that verified it, in two distinct ways.

First, the presence hooks. `.claude/settings.json` wires all four, so
`presence-pretooluse`, `presence-posttooluse`, `presence-userpromptsubmit` and
`presence-stop` executed on every tool call, prompt and stop of this dispatch.
They recorded nothing and could not: I read back `DOTLN_RESIDENT_STORE` and
`DOTLN_RESIDENT_EPISODE_ID` as unset in this session, and
`presence-heartbeat.ts:12-13` returns before any work when the store binding is
absent. This is the same disclosure VER-001 made, confirmed again by readback
rather than assumed from it.

Second, and new in this report: the gate-input guard under verification
governed my own reads. While my `npm test` runs were live,
`harness-host.ts:2391-2440` refused every shell command it could not classify as
read-only — `git`, `sed`, `awk`, `tail`, `wc`, recursive `grep`, anything with an
unresolvable `$TMPDIR` path or a pipeline it could not prove. That is the guard
working as designed, not a defect, and I record it because it shaped my method:
reads during a live gate went through the file-reading tool instead, and every
git-based check ran before or after a gate, never during one. No check was
skipped for this reason.

My writer reservation (`actorId 08febceb…`, owner pid 98682) is this worktree's
single reservation throughout, confirmed at entry and again at handoff.

## Executed checks

| Command or observation | Result |
| --- | --- |
| `npm test`, full gate, inside the command sandbox | **18 passed, 1 failed, 302.32 s**, 63 fresh tasks, exit 1 |
| `npm test`, full gate, outside the command sandbox | **19 passed, 0 failed, 261.12 s**, 63 fresh tasks, **exit 0** |
| `node scripts/harness.mjs evidence` | **Final evidence passed**, exit 0. The matched gate row is my own outside-sandbox run: exit 0, 261117 ms, 63 fresh suites, 0 reused, codeIdentity `02a82f95…`, recorded 2026-09-17T01:12:48.126Z — not a reused or inherited row |
| `release:case:runtime_refresh` — VER-001 F1's case | **PASS 31.08 s** in-sandbox; **PASS 22.00 s** outside. F1 does not reproduce |
| `skeleton` suite | `FAIL 180.05 s` in-sandbox (pass 293, fail 2, skipped 1); **`PASS 145.48 s` outside** |
| The two in-sandbox failures | cases 129/130, `WO-068 native script …` and `WO-068 fresh CLI once …`; both report actual `'script offline boundary unavailable: sandbox-exec refused its local probe'`, case 129 against expected `'native script fixture needs an outside-sandbox runner'` and case 130 against expected `null` — environmental, nested `sandbox-exec`; see L1 |
| Nine pre-existing hooks vs base `1a2d309`, whole-file comparison normalized for snapshot id, package versions and pin entries | **9/9 identical, 0 differing**; every one differs in raw bytes, none differs in substance |
| `.claude/settings.json` vs base | **+20 lines, additions only**: the four heartbeat registrations, one per event, no deletions |
| `.claude/` diff scope vs base | manifest + the nine hooks + settings only; four new `presence-*.mjs` added untracked; skills and instruction surfaces untouched |
| `node scripts/harness.mjs check` | **exit 0**, 28 generated surfaces |
| `compiler` suite (carries the WO-039 hook fixtures) | **PASS** in both gate runs |
| `node scripts/feedback-evidence.mjs --check` | **exit 0** — ten passing regressions, ten removal failures, 1192 fewer instruction bytes |
| `docs/evidence/current.json` | feedback and authority editions both `WO-121` revision `002` |
| `git diff main -- package-lock.json packages/*/package.json` | internal workspace bumps only: compiler 0.12.0→0.13.0, skeleton 0.21.0→0.22.0. **No external package added** |
| `git diff --check` | clean, exit 0, before and after the gate runs |
| `git log --oneline main..HEAD` | empty — no branch commits before final review |
| `grep -n 'git clone\|git init\|maintenance.auto\|receive.autogc' scripts/test-release.sh` | origin carries `maintenance.auto false` and `receive.autogc false` before any data; **all five** clone sites carry `-c maintenance.auto=false`; no unguarded clone remains |
| `docs/discovery/writing-worker-smoke-2026-09-14.md:18,34` | C-W10 observed: no hook-input field names a non-interactive origin. X-W10: no Codex prompt hook. The installed-profile fallback is sourced |
| `DOTLN_RESIDENT_STORE` / `DOTLN_RESIDENT_EPISODE_ID` in this session | both unset — the installed presence hooks were inert here |
| `node scripts/harness.mjs writer --show` | one reservation, this session, at entry and at handoff |
| `node scripts/harness.mjs usage` | unavailable; see L3 |

## Acceptance criteria

| # | Criterion | Verdict | Evidence |
| --- | --- | --- | --- |
| 1 | Fake clock; negative stamped and unstamped activity stream after `away` changes no phase and cancels nothing; explicit `back` and an unstamped human prompt each end `away`; a stamped scripted prompt does not; a launch-path-swap fixture proves classifier purity; all pinned | **met** | `presence-signals.test.ts:106-132` permutes 4 events × 3 interaction values × {stamp, no stamp} × 3 launch paths and asserts identical decisions — criterion 1(f) exactly. `:134-256` runs the negative stream for all four events stamped and, for the three activity events, unstamped, then asserts `present === false`, unchanged `state`/`generation`/`current` and `kills === 0`, across 6 combinations; the stamped `UserPromptSubmit` in that stream is the scripted-prompt case. `:532-606` repeats it through the real emitted hooks, asserting exactly 9 observations and that no prompt or tool bytes reach the log. Purity is structural, not merely tested: `classifyPresenceSignal` (`presence-signals.ts:29-39`) admits only `(signal, stamp)` and there is no launch-path or process input anywhere in the module |
| 2 | On return a discretionary in-flight episode is handled per the phase, and an explicitly requested foreground task continues to completion | **met** | The same loop drives kill / finish / foreground against both return signals. `:237` asserts `kills === 1` only for `kill`; `:238-239` assert the machine returns to `present` with `current === null`; `:247-250` assert the episode actor reaches `"stopped"`, which `resident-state.ts:394-395` sets only on an admitted outcome. In the foreground case the phase is `discretionary:false`, `kills === 0`, and the episode completes across the return. The compiler refuses `discretionary:false` with anything but `inFlightOnReturn:"finish"` (`presence-schema.ts:260-264`), which I re-read at the current bytes. This criterion no longer rests on the literal-only assertion VER-001 flagged as O2 — see O3 |
| 3 | A stalled actor is detected as a liveness failure, not a presence change | **met** | `:258-386` stalls the actor at t=35 while asserting `present === true` and `state === "present"`, then separates human idle at 40 and phase decay at 140 on independent clocks; `:388-437` asserts stall during `away` with `kill: () => assert.fail("stall is not a return")`. Replay is proven ambient-clock-free by throwing from `Date.now` (`:348-357`). In the fold, stalls are written by `expireActorHeartbeats` (`resident-state.ts:157-165`) into `actors[…].status` only; `present` changes at `:258-266` and `:308-317` on human origin and the human idle threshold alone |
| 4 | The regenerated bundle differs from the base only in the heartbeat hooks and pins; `harness check` passes; the WO-039 hook fixtures pass | **met** | Whole-file normalized comparison of all nine pre-existing hooks against the base leaves **0 differing**; the only changes are the runtime snapshot id, the two package versions, pin entries and their hashes. `settings.json` is additions only. `harness check` passes at 28 surfaces. The compiler hook fixtures (`harness.test.ts:299-302,372-373`, asserting the advisory text and that the reduced target bundle keeps only `presence-pretooluse.mjs`) pass inside the green `compiler` suite, and the generated hooks are additionally executed end-to-end at `presence-signals.test.ts:532-606` |
| 5 | Write-backs land: 03 §Operator-presence policy, ADR-0007 §Amendments, ledger entry | **met** | `03-architecture.md:1477-1517` states the three origins, the event-kind rule and the never-reads-launch-path/process-table/input-device rule, and cites C-W10/X-W10 for the installed-profile fallback — I checked those rows and they support the claim. `0007-presence-is-a-policy-input.md:105-114` carries the dated amendment. The ledger duty is discharged by the documented pre-2026-09-09 substitution: `docs/work-orders/README.md:130` marks it and `decisions-index.md:77-80` carries D001–D004, D004 being the repair's |
| 6 | `npm test` green; `git diff --check` clean; no new dependency; a fresh feedback evidence edition | **met** | `npm test` exits 0 outside the command sandbox: 19 suites passed, 0 failed, 261.12 s, with `release:case:runtime_refresh` and `skeleton` both passing. Inside the sandbox the same tree gives 18/1, the one failure being two WO-068 cases the nested sandbox cannot run (L1). `git diff --check` clean. No external dependency added; only the two internal workspace bumps. `feedback-002` is current and `feedback-evidence --check` exits 0 |

## Findings

**None.** VER-001's F1 is discharged, and I raise no new blocking finding.

F1's discharge, stated as a reproduction rather than a citation of the repair's
own log: VER-001 recorded `release:case:runtime_refresh` failing 3 of 3 — under
the full gate, isolated in-sandbox, and isolated outside it. On the current
bytes the case passes in both of my full gate runs, 31.08 s in-sandbox and
22.00 s outside. The mechanism VER-001 diagnosed is closed at its source rather
than at the one failing call site: `make_repo` now sets `maintenance.auto false`
and `receive.autogc false` on the bare origin before it receives any data, and
every one of the five `git clone` invocations in the file carries
`-c maintenance.auto=false`. I grepped the whole file; no unguarded clone
remains. The template path inherits the settings structurally, not incidentally:
`--prepare-template` calls `make_repo template` with `release_template` empty,
so it takes the same fresh branch that sets the config, and
`release-fixtures.mjs` copies `origin.git` and `project` wholesale, config
included. The change is confined to fixture setup; the diff to
`scripts/test-release.sh` adds only those config lines and the clone flag, plus
the diagnostic block VER-001 already accounted for, and touches no assertion and
no production release code.

## Observations

**O1 — the repair's scope is wider than F1 required, in the right direction.**
F1 reproduced on one case. The repair guards all five clone sites and both
fixture construction paths. That is what makes the discharge durable rather
than a case-specific patch, and it is why I did not treat the other four clone
sites as an open edge.

**O2 — `main` moved during this dispatch; it is not a finding.** At entry `main`
was `1a2d309`; at handoff it is `9ecedc5`, five commits fast-forwarded in at
21:03–21:06 local (01:03–01:06Z), while my first gate run was executing. They
are WO-119's merge and its records, and `890a20e "Prepare v0.26.0 with skeleton
0.22.0, regenerated runtime pins and fresh evidence editions"` — the same
application version and skeleton version WO-121 stages under D003. The project
floor is explicit that a sibling publishing the version an order staged is not a
finding and never routes to repair, and that final review integrates `main` and
retimes under the existing classification. I verified against the recorded
subject accordingly: HEAD and merge base are both `1a2d309`, and every diff in
this report is against that base, not against today's `main`. Reading the
subject against the moved `main` would have shown 85 files and −11106 lines of
WO-119 material that this order never deleted. Final review owns the
integration and the version collision; I flag it so it is not discovered late.

**O3 — VER-001 O2's dead assertion is gone, and criterion 2 did not depend on
it.** The literal-only `authorize(...)` block is removed from
`presence-signals.test.ts` along with its unused import, and the diagnostic now
reports the observed kill/completion result instead of overstating what was
checked. I re-read the remaining fixture at current bytes: both return signals,
all three dispositions, the cancellation count, the return state and the
completed-actor assertion all survive. The removal narrowed the test's claims to
what it actually executes, which is the correction D004 records.

**O4 — ingress still rejects a forged origin.** `decodePresenceObservation`
(`presence-signals.ts:102`) recomputes `classifyPresenceSignal` and refuses
any observation whose recorded `origin` disagrees, so a hand-written or replayed
event cannot assert `human`. The criteria do not ask for this; it materially
strengthens the objective's "presence is never inferred", and the repair left it
intact.

## Limits

**L1 — the command sandbox, and how I resolved it rather than worked around
it.** Cases 129 and 130 are WO-068 fixtures that need a real `sandbox-exec`;
nested inside this session's command sandbox the wrapper refuses its own probe.
Case 129's own expected message is `'native script fixture needs an
outside-sandbox runner'`; case 130 simply expects no unavailability at all and
receives the same refusal. VER-001 recorded this as its L1 and predicted an
outside-sandbox run would clear it. Rather than accept that prediction or the
executor's log, I ran the full `npm test` once outside the command sandbox and
it exits 0. I disclose that I disabled the sandbox for that single run, on the
evidence quoted above, and that both runs are recorded here rather than only the
green one. WO-121's own native case skips cleanly under the same condition via
`scriptAvailability()`, which is the better-behaved shape.

**L2 — I did not establish F1's pre-existence by executing the case at `main`.**
This carries VER-001's L2 forward unchanged. The repair added Trace2 evidence
that automatic maintenance and the clone overlap in the fixture — a mechanism
observation, which is more than the error-signature inference VER-001 had — but
no run at `main`. Nothing in this verdict depends on it: the case passes now,
and whether it also failed at `main` bears only on how the cost of the repair is
attributed, not on whether criterion 6 is met.

**L3 — process cost is unknown.** `node scripts/harness.mjs usage <session>`
needs a session identifier this session does not expose;
`CLAUDE_SESSION_ID` is unset in the command environment and the harness reported
"Begin the harness session before measuring usage" at entry. Tokens, commands,
steps and cost are therefore unknown for this dispatch and no reduction is
claimed. What I can report as measured: two full `npm test` executions, 302.32 s
in-sandbox and 261.12 s outside, no subagents, and no re-run of any check. The
second full gate was the deliberate cost of establishing criterion 6 first-hand
instead of inheriting it; a `skeleton`-only outside run would have been about
two minutes cheaper and would have left the criterion resting on a composed
argument rather than on its own command.

**L4 — a correction to my own method, recorded because it changed a result.**
My first normalized hook comparison read the current bytes with
`git show :<path>`, which returns the index. These files are modified but
unstaged, so the index still held the base content and the comparison was
vacuous — it reported 9/9 identical for the wrong reason. I caught it by testing
the assumption directly (`index===worktree: false`, `index===base: true`),
re-ran the comparison against the working-tree bytes, and the table above
reports that second run. The conclusion happens to be the same; the first run
was not evidence for it.

**L5 — the outside-sandbox log carries no per-suite skip count.** Passing suites
print only their `PASS` line, so I can state that `skeleton` passed there but
not that it ran with zero skips. The in-sandbox run shows 293 pass / 2 fail / 1
skip for that suite, the skip being WO-121's own native case under the condition
L1 describes.

**L6 — reads during my own live gate went through the file-reading tool.** The
gate-input guard refused most shell reads while `npm test` was running, as
disclosed above. Every claim in this report about `git`, `npm test`,
`harness check` and `feedback-evidence` was executed by me outside a live gate;
nothing was inferred from a blocked command.
