# WO-047 VER-002 — independent re-verification after the VER-001 F1 repair

**Verdict: WO-047 passes.** All five acceptance criteria are met and VER-001's single blocking finding is closed at the delivered tree. I reproduced every consequential claim myself rather than reading the executor's or the repairer's tests. `npm run publication:check` now exits 0 with exactly the output VER-001 recorded for the `origin/main` baseline, and the repair that achieved it is documentation-only: no implementation, test, script, package manifest or evidence artifact changed a single byte since VER-001 judged them. The product gate is green. Two document-gate suites (`plan`, `plan-refutation-current`) remain red; I proved they are inherited rather than this order's, by building a pristine `origin/main` tree and observing the identical failure there against a byte-identical planning subject hash. They are recorded as O1, not routed to repair. Nine further non-blocking observations follow.

Subject: [WO-047](../../work-orders/WO-047-replay-environment-projector.md), criteria 1–5, plus VER-001's finding F1 and its three-item repair checklist. The `resume: verify` dispatch was recorded by the session hook and allocated this path from checkpoint `refs/dotln/checkpoint/WO-047/7` (`f0e479d5`). The branch is `wo-047` with no branch commits; `HEAD`, `main` and `origin/main` are all `7fd94ae`, and the latest tag is `v0.21.1`, so the staged `v0.22.0` is unpublished and the entire subject is the uncommitted working tree. I hashed all 1,569 blobs in checkpoint 7 against the working tree: 1,567 are byte-identical and the only two that differ are `docs/control/current.md` and `docs/control/orders/WO-047.jsonl`, this dispatch's own lifecycle projections. Checks ran 2026-09-16T01:31Z–01:45Z; evidence cutoff 2026-09-16T01:43:41Z.

Verifier: Claude Code 2.1.273, observed with `claude --version`. Model `claude-opus-5[1m]` comes from the session's system prompt. Effort is **unknown**: the operator's `/model` selection in this session set the model only ("Opus 5 (1M context)") and no effort tier is exposed for readback, so unlike VER-001 — which ran at `xhigh` under the operator's `ultracode` selection — this session attests no effort value. The order recommends verifier `xhigh+`; that recommendation is not evidenced as met here, and is disclosed rather than assumed (see O9). The root agent was the sole registered writer for this worktree (`node scripts/harness.mjs writer --show`, actor `e01ab031`).

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.273","model":"claude-opus-5[1m]","effort":"unknown","source":"operator-selected-model-effective-effort-unavailable"}

Instrument disclosure. The regenerated hooks and manifest under verification (`session.mjs`, the PreToolUse/PostToolUse observers and the runtime snapshot) recorded this dispatch, observed this session's tool calls, and enforced the live-gate write guard, which refused a number of my own Bash calls while the two gates ran. The subject also includes `packages/skeleton/src/reactor.ts` and the two evidence scripts that the artifact-identity and feedback generators use, so the `--check` runs below regenerate the subject's outputs with the subject's own code; the committed baselines they compare against were produced by the previous (WO-046/WO-048) editions. I used no subagents. Every claim in this report is one I executed or read myself.

## Executed evidence

| Check | Observed result |
| --- | --- |
| `npm test` (product gate, current subject) | **`19 passed; 0 failed; 299.19 s; 62 fresh tasks`**, runner exit 0. Matches the executor's 19/62 and VER-001's 19/0. |
| `git diff --check` | Clean, exit 0. |
| Subject integrity vs dispatch checkpoint 7 | 1,569 blobs hashed; **1,567 byte-identical**, 2 differing — both this dispatch's lifecycle projections. No implementation, test or evidence file was edited during verification. |
| Repair scope vs VER-001's checkpoint 4 | 13 files, all documentation/lifecycle: roadmap, both TOC locks, capability table, decisions + index, followups, work-order index, evidence receipts, PR.md, control projections. **`packages/`, `scripts/`, `package-lock.json` and every `docs/evidence/WO-047` artifact are untouched.** |
| `node scripts/check-publication.mjs` (F1) | **Exit 0**: `PASS index coverage: 264/264 product headings indexed` / `PASS dual-voice links` / `CURRENT everyday-ai-user-toc.md: 30 linked source sections match` / `CURRENT software-engineer-toc.md: 45 linked source sections match` / `PASS publication bootstrap checks`. Identical to the `origin/main` baseline VER-001 recorded. |
| Roadmap form (F1 checklist item 2) | `docs/product/06-roadmap.md` now carries `**WO-047 replay projection (2026-09-15):**`, the bold delivery-note form used by the three WO-039 siblings immediately above it. It is no longer an ATX heading, so it no longer reads as a subsection of `## Post-1.0 horizons`. |
| Independent three-way replay identity sweep (own probe, built `dist`) | Walked the whole worktree: 138 `.jsonl` files; **65 replayable** through `seiriReactor`; **4,473 events**. For each stream I compared the entire `ReplayResult` (state **and** decisions) by `JSON.stringify` across three modes — no projector, `projectRuntimeEnvironment`, and `defaultEnvironmentProjection` passed explicitly. **0 mismatches.** Repeated against a second initial-state shape (`initialVerificationRuntime`): **0 mismatches.** |
| Frozen WO-003 trace oracle (own probe) | `packages/skeleton/fixtures/wo029-legacy-scenario.jsonl` replays to 21 decisions whose `trace` sequence deep-equals `packages/skeleton/fixtures/wo003-decision-traces.json` under all three projector modes. |
| Independent AC2 kernel probe (own probe) | A fixture keeping RNG at `state.random.seed` replays deterministically and repeatably with a projector — my own step function yields `rng:42 → rng:3397979675 → rng:3263785984` — and replays `rng:0` at every step without one, with `settings` correctly **not** promoted as `policy`. `NaN`, `Infinity` and `-Infinity` from a supplied projector each throw `Replay environment projector must return a finite rngState` **with the reactor never invoked** (verified by a spy). All seven non-number returns (`"7"`, `null`, `undefined`, `true`, `[]`, `[7]`, `{}`) are refused. |
| Documented default projection (own probe) | `{}` → `rng:0`, no `policy` key; `{rngState:"x"}` → `rng:0`; `{rngState:5,policy:null}` → `rng:5` with `policy` present and null. Exactly what product 02 and the kernel README state, including "(including null)". |
| Reserved-key census of `core.ts` (own patterns) | The only application-state reads of `rngState`/`policy` are the two `stateField(state, …)` calls at `core.ts:566-567` inside `defaultEnvironmentProjection`. Every other occurrence is a `KernelEnv` field (`env.rngState`), an `isClocklessKernelEnv` candidate check (`327-328`), an authority trace input (`494/496/539/541/543`) or the destructuring of the already-projected environment (`590`). `stateField` is not exported and has no other caller. |
| `replay` call-site census (own patterns) | Exactly **10** call sites outside tests — `beacon-observe.ts:103`, `scenario.ts:297/315/458`, `feedback-selfhost.ts:56/195`, `verification-host.ts:97`, `verification.ts:56`, `scripts/artifact-identity-evidence.mjs:278`, `scripts/feedback-evidence.mjs:57` — and all 10 pass `projectRuntimeEnvironment`. Confirms the executor's "eight runtime replay calls … plus two evidence-script calls". |
| Projector/state-shape safety (own read) | All 10 sites replay a genuine `RuntimeState`: `initialVerificationRuntime` spreads `initialState()`, so it carries a top-level numeric `rngState`. No site hands the skeleton projector a state shape lacking the fields it reads, so the new non-finite guard cannot fire on a well-formed call. |
| Live/replay identity (own read) | `LiveReactorDriver.append` and `.restore` both call `replay` with `projectRuntimeEnvironment` — the live path *is* the replay path, so identity is structural here, not merely tested. |
| Kernel export surface | `defaultEnvironmentProjection` is a function on the built package entry point; `replay.length === 5`. |
| `node scripts/artifact-identity-evidence.mjs --check` | Exit 0: `Verified 4 current artifact evidence files …; original semantic hashes and frozen oracle unchanged.` |
| `node scripts/verification-evidence.mjs --check` | Exit 0: `Verified 4 synthetic verification evidence files; planted defect, repair, staleness and replay are green.` |
| `node scripts/feedback-evidence.mjs --check` | Exit 0: `Verified ten passing regressions, ten removal failures, and 1192 fewer instruction bytes …` |
| `node scripts/harness.mjs check` | `24 generated surfaces; local-terms list: unavailable`. |
| Generated policy surfaces, normalized | All **10** changed `.claude` surfaces (nine hooks + `harness-manifest.json`) are textually identical to `origin/main` after normalizing `fnv1a64:` pins, `.runtime/harness/<snapshot>` ids and bare hashes. **0 substantively different.** No policy, permission or capability text changed. |
| Dependency and version scope | `package-lock.json` changes are exactly three values: `@dotln/kernel` `0.4.0`→`0.5.0`, `@dotln/skeleton` `0.18.1`→`0.18.2`, and the skeleton's exact kernel dependency. Both manifests agree. **No new dependency.** |
| Suppression scan | `git diff origin/main -- packages/*/src` adds no `eslint-disable`, `@ts-ignore`, `@ts-expect-error` or `as any`. |
| `npm run test:docs` (document gate) | **`15 passed; 2 failed; 97.92 s`**. `publication`, `index`, `console-docs`, `skeleton-docs`, `format`, `release-surfaces`, `meta` and all evidence suites pass. The 2 failures are `plan` and `plan-refutation-current` — see O1. |
| Planning staleness at the `origin/main` baseline (own probe) | I extracted `origin/main` with `git archive` into a scratch tree, made it a real repo, and ran `buildPlanSubject(root,"HEAD",{workspace:true})`. It throws the **identical** `Planning cost evidence is stale for the subject revision or dated acceptances`. Baseline sourceHash `sha256:16ad4933…` equals the current tree's `sha256:16ad4933…`; the stored `docs/planning/cost-table.json` records `sha256:34a5f8f2…` (observed 2026-09-15T05:15Z). **WO-047 leaves the planning subject byte-identical and cannot have caused this.** |
| Capability-table header at `origin/main` (own probe) | `origin/main` already carries the broken `Remaining gate` header at `capability-table.md:239`, while `packages/console/src/text-sources.ts:83` requires `Blocking-gate reassessment` and the file's two other tables already use it. The defect was inherited from WO-046; the repair's one-cell normalization restored it, and `console-docs` now passes. |

## Acceptance judgment

| Criterion | Judgment and evidence |
| --- | --- |
| 1 | **Met.** My own sweep compared the full `ReplayResult` — state as well as decisions — for 65 replayable streams and 4,473 events across three projector modes and two initial-state shapes, with zero mismatches. The frozen WO-003 oracle matches in all three modes. No committed fixture changed: the repair touched no `packages/` path at all, and the implementation is byte-identical to what VER-001 verified. Live and replay share one code path, so live/replay identity holds by construction. |
| 2 | **Met.** Reproduced independently and in full. An alternative-key fixture replays deterministically *and repeatably* with a projector and `rngState: 0` without one; a non-finite projected RNG refuses before the reactor, which I verified with a spy rather than inferring. The refusal is stricter than the type promises — every non-number is refused — which is the safe direction. |
| 3 | **Met, on the recorded interpretation.** My own patterns confirm the reserved application-state keys appear only in the exported `defaultEnvironmentProjection`, and a source-grep test pins it. D001's reading — that "reserved keys" means application-state lookups, not every `rngState`/`policy` token — was recorded before implementation and is legitimate, since `KernelEnv` fields and authority trace inputs carry those names and a literal token ban would be unsatisfiable. The test is materially narrower than the criterion's wording suggests (O2/O3), but the criterion asks for a grep test and a grep test is what it got. |
| 4 | **Met.** Product 02's Reactor row states the signature, replay's ownership of `now` and the predicate registry, the supplied-projector non-finite refusal, and the reserved-key fallback including `policy` promotion "(including null)"; I checked each assertion against `core.ts` and found none false. The kernel README documents the same contract at lines 69–83 and its component claim `0.5.0` matches `package.json`. The pre-2026-09-09 ledger duty is discharged by `docs/evidence/WO-047/decisions.md` — now D001–D004, all four with dispatch, evidence, alternatives and a reopening condition — with rows in `docs/lineage/decisions-index.md` and public follow-up records in `docs/planning/followups.json`. **F1's downstream publication surfaces are now green**, which is what VER-001 found missing. |
| 5 | **Met.** `npm test` green (19/0, 299.19 s, 62 fresh tasks); `git diff --check` clean; no new dependency; the lockfile is limited to workspace versions. No hash preimage changed: the semantic-hash inventory and frozen oracle verify unchanged, and the four artifact and four verification outputs are reproduced byte-identically by the changed code. The required fresh authority and feedback editions are present and pass `--check`. The regenerated bundle differs from `origin/main` only in runtime pins across all ten surfaces. |

## VER-001 finding F1 — closed

F1 required three things. All three are satisfied at the delivered tree, and I verified each against the command rather than the receipt.

1. **`npm run publication:check` exits 0.** Confirmed, with output identical to VER-001's `origin/main` baseline.
2. **The roadmap heading is demoted, or an index row is added.** The repair took the first branch — the form VER-001 judged "the smaller and more consistent of two ways" — so the delivery note now matches its three WO-039 siblings and no longer nests under `## Post-1.0 horizons`.
3. **Both TOC source locks refreshed, and the receipt states they were recomputed.** Both locks changed; `check-publication.mjs` reports both editions `CURRENT` (30 and 45 linked source sections). `docs/evidence/WO-047/repair-001.md` records the recomputation *and* separately records a factual review of both editions against the changed contract, explicitly noting that "a refreshed lock witnesses changed bytes" and does not replace independent verification — which is precisely the distinction product 08 line 180 and VER-001's checklist asked for.

No new blocking finding. I found no error in the kernel or skeleton change, and the repair introduced none.

## Non-blocking observations

- **O1 — two document-gate suites are red, and it is not this order's defect.** `plan` and `plan-refutation-current` fail with `Planning cost evidence is stale for the subject revision or dated acceptances` (`scripts/lib/plan-subject.mjs:393`). The gate builds its subject with `workspace: true`, so WO-047's uncommitted edits *do* feed the hash — "no branch commits" is not by itself an alibi, and I did not rely on it. I instead computed the hash both ways: the current tree yields `sha256:16ad4933…` and a pristine `origin/main` tree yields the same `sha256:16ad4933…`, against a stored cost table of `sha256:34a5f8f2…`. So the whole WO-047 change set, including the repair's capability-table edit, perturbs the planning subject not at all, and a pristine baseline throws the identical error. Repairing it here would mean either expanding into planning ownership or refreshing a hash to turn a check green without establishing the judgment the cost table exists to record — which D004 explicitly rejected, correctly. It is deferred to a planning continuation with validation commands `npm run plan -- check` and `npm run test:docs`. **This is a real, currently-red defect with a named owner; it is not a WO-047 finding and does not route to repair from here.**
- **O2 — the AC3 grep test is materially weaker than its criterion reads.** `packages/kernel/test/ac2-replay-store.test.ts:313-316` matches only a literal `stateField(state, "rngState"|"policy")` call or a `state.rngState` / `state["rngState"]` access on a variable *literally named* `state`. An alias (`const s = state; s.rngState`), destructuring, a computed key, a cast, an optional chain or a reflective accessor would reintroduce the coupling and still pass. The criterion's intent holds today — I confirmed that with my own census, not the test's — but the regression pin is narrower than "the only place the reserved keys appear in `core.ts`". Carried forward from VER-001 unchanged, because the repair did not touch tests.
- **O3 — the exempt region is carved by string anchors, not by scope.** `start = indexOf("export function defaultEnvironmentProjection(")` and `end = indexOf("export function replay<", start)` mean anything inserted between those anchors is exempt from the outside check, and reordering the two functions or repeating either anchor string in a comment moves the window. `assert.ok(start >= 0 && end > start)` is the only guard.
- **O4 — no stored stream exercises an evolving RNG.** My census is broader than VER-001's six-stream one: across **all 65** replayable streams, `rngState` is constant for the entire length and **zero** streams evolve it. So the byte-identity corpus proves byte preservation but is structurally insensitive to whether a projector delivers the RNG correctly step to step. That property is genuinely covered — but only by the new kernel fixture, which I reproduced independently (`rng:42 → 3397979675 → 3263785984`, repeatable).
- **O5 — explicitly passing `defaultEnvironmentProjection` is not byte-identical to omitting it.** For `{ rngState: NaN }` the omitted path delivers raw `NaN` while the same function passed as the fifth argument throws, because the guard at `core.ts:591` keys on supplied-vs-omitted, not on projector identity. Deliberate, recorded in D001, documented in 02 and the kernel README, and unreachable in the skeleton, where `draw()` returns a uint32. Recorded because the identity helper passes that function explicitly as one of its modes.
- **O6 — `packages/skeleton/src/scenario.ts` is at zero headroom against its ratchet.** 747 lines against `< 748` at `packages/skeleton/test/scenario.test.ts:199`. The bound literal is unchanged from `HEAD` — preserved, not relaxed — but the next line added to that file fails the gate.
- **O7 — the adjacent-work queue lives in an ignored path.** `docs/control/local/` is matched by `.gitignore:18`, so the queue entry deferring the planning reconciliation, and its prose `disposition.target`, will not survive merge. The durable record is D004 plus its `followups.json` entry, both on committed surfaces with a reopening condition, so the deferral is not lost — but the queue copy is local-only, and the `RepairCompleted` advisory separately notes that the deferral still wants a public FUP identifier as its target rather than prose.
- **O8 — `docs/work-orders/README.md` was stale at dispatch, as it was for VER-001.** `npm run test:docs` initially reported `index` stale at line 3, which cascaded four dependent suites into `Required preflight or fixture preparation failed` and made the gate read 12/5. The cause is this verification's own `VerificationRequested` event advancing the phase, not an executor or repairer omission. I discharged the standing duty with `npm run work-orders -- index` and re-ran the gate for the true 15/2 reading reported above. The first run's four cascade failures were artifacts of my own dispatch and are not findings.
- **O9 — this verification's effort is unknown, and the order recommends `xhigh+`.** VER-001 ran at `xhigh`; this session exposes no effort tier, so the attestation records `unknown` rather than inventing a readback. The recommendation is advisory and does not gate the verdict, but a reader comparing the two reports should know the second pass attests no effort value. Its compensating evidence is that the repair was documentation-only, so the implementation this report passes is the same bytes VER-001 examined at `xhigh`.
- **O10 — `docs/final-reviews/WO-047/PR.md` process-meter rows remain stale.** A generated projection the reviewer regenerates; recorded so the stale numbers are not quoted forward.

## Goal alignment and cost

The order serves the always-on runtime critical path: replay must stay dependable while the application, not the kernel, owns where its RNG and policy live, so WO-050's typed slices are not forced to keep two literal keys at the top level. The promised outcome is observed — ten call sites project explicitly and every stored stream is byte-identical either way — and the change is close to the smallest one that achieves it.

- **Rule beating** was again the trap worth pushing on, in two places. First, the evidence corpus: "byte-identical" can pass without the intended behavior occurring, and my census shows *no* stored stream evolves its RNG at all, so I built the evolving case myself rather than accepting corpus identity as proof (O4). Second, the repair itself: the cheapest way to turn `test:docs` green would have been to refresh the planning cost hash. That would have manufactured agreement the cost table is supposed to record. D004 declined it and I agree; I verified the staleness is inherited instead of accepting either the claim or the convenient fix.
- **Drift to low performance** is where I checked the repair hardest, because F1 was itself a drift finding. The publication control is now green at the delivered tree and matches the baseline exactly, and the repair reached that state by correcting the document rather than by relaxing the check.
- **Naive Interventionism / NoOp.** The repair was correctly bounded: documentation-only, no implementation touched, one inherited console defect fixed with a one-cell schema normalization that changed no capability claim, and one genuinely out-of-scope planning problem deferred to its owner with a reopening condition. Expanding this order into planning reconciliation would have been intervention without the provenance to do it correctly.
- **Shifting the burden** is genuinely improved: application code now owns its state layout instead of requiring a kernel edit per consumer.
- **Policy resistance, commons, escalation, success to the successful, seeking the wrong goal** are immaterial here: no new dependency, no new operator step, no new process, and the outcome claimed is replay independence rather than a claim that typed slices already exist.

Process cost. Entry observation at 2026-09-16T01:31:20Z: 109,713 total tokens, source `claude-transcript-message-usage`, scope `dispatch`. At handoff, 2026-09-16T01:43:41Z: **22,581,420 total tokens** (22,344,860 cached input, 164,482 cache-write, 71,758 output), 148 commands across 169 steps; dollar cost unavailable. No subagents were used, against VER-001's 3,783,019 subagent tokens — the saving is real but so is the coverage difference, and it was affordable here only because the repair was documentation-only, which let me spend the budget on reproducing the acceptance criteria and on the baseline probes that decided O1 rather than on re-fanning the search for implementation defects VER-001 already hunted at `xhigh`. I ran the full product gate (299.19 s) and the document gate twice (35.90 s + 97.92 s); the second document run was necessary because my own index refresh changed the first run's result. No cost reduction is claimed. These are meter observations, not a completion requirement.

## Limits

- The live feedback audit (`DOTLN_LIVE_WORKERS=1 … --transport codex-cli-exec --model gpt-6-astra --effort max`) needs a live model transport outside this sandbox and was not re-run. Only the committed edition's offline `--check` is claimed.
- My `origin/main` baselines ran from a `git archive` extraction that I initialized as a fresh repository. That is sound for the two questions I asked it — the capability-table header bytes and the planning sourceHash, both content-derived — but the fresh commit carries a new timestamp, so I did not evaluate the separate `Planning cost evidence predates its subject revision` check, which sits after the staleness throw.
- My identity sweep binds each stream to the first initial-state and predicate-registry pair that does not throw. Streams that refuse under every pair were counted as non-replayable rather than analyzed; I did not investigate why 73 of the 138 `.jsonl` files are not schema-1 replayable logs, which is a pre-existing classification question and projector-independent.
- Effective model and effort readback is not exposed; the attested model is operator-selected and the effort is unknown (O9).
- No trial merge was attempted, and none was needed: `HEAD` equals `origin/main`.
- Replay performance was not measured; the projector adds one call per event and I did not quantify it.
- I verified the two audience editions' *locks* and the checker's `CURRENT` verdict, and I read the repair's factual review of their claims, but I did not independently re-read both full editions against the changed 02 contract line by line.
