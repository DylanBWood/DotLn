# WO-161 VER-001 — independent verification: fail

**Subject:** [WO-161](../../work-orders/WO-161-sandbox-vocabulary.md) on branch `wo-161`, uncommitted over HEAD `852861d3` (`main`, tagged `v0.49.0`, and the order's activation tree `8e89fe50`). The `verify` dispatch allocated this report and checkpoint `refs/dotln/checkpoint/WO-161/3` (`fd8598ae`). The worktree judged here, hashed with untracked files, is tree `6274b49f`. It differs from that checkpoint only in the three lifecycle projections the dispatch wrote (`docs/control/current.md`, `docs/control/orders/WO-161.jsonl`, `docs/work-orders/README.md`). The product gate below recorded code identity `e6f9dfe0721b10d775090a917a0b2ab50b2794fdeb09fcb4e35adfc7e638a300`, the same identity as the executor's final gate. The order's text differs from its activation checkpoint only by the `(v0.50.0)` heading label, so the six criteria judged here are the original ones. The verifier made no implementation or product-document edit. It recorded [D006](../../evidence/WO-161/decisions.md#wo-161-d006) to [D008](../../evidence/WO-161/decisions.md#wo-161-d008), refreshed the decisions index and follow-up register with `npm run meta`, and wrote this report.

**Verifier:** Claude Code 2.1.282, Opus 5.5 (`claude-opus-5-5`), selected effort `xhigh` read from `CLAUDE_EFFORT` (the host's selected effort, not effective effort). The operator selected the session's `ultracode` mode. This root session was the sole writer. One workflow ran four read-only lenses: cold-start and generators, hand-written documents, detector and authority code, and scope and non-goals. The harness observed 4 admissions (`exact-observed`) against the cap of 20, with 16 remaining and no descendants. The lenses returned claims; every finding below was reproduced by this root session. No hard USD 5 provider cap was exposed or enforced.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-opus-5-5","effort":"xhigh","source":"claude-session-readback"}

**Instrument disclosure:** the role skill this verification followed is itself a WO-161 output: its gate sentence comes from the generator this order changed. The generated hooks this order regenerated judged every command in this session, and the session hook recorded this dispatch. The host-confinement detector under test wrote the `sandbox` field of this session's own gate row. The usage counter behind the cost line is DotLn's own. Criterion 3's host observation is therefore the instrument observing itself. It was corroborated with a scratch reproduction of both the entry and current runner code.

**Verdict:** **Fail.** All six criteria are met on the current subject. Every consequential executor claim reproduced: the grep, the byte table, suite selection, the confined-row identity, historical authority immutability, the editions and both gates. The order's heading and Design, however, promise that the cold-start text stops describing an approval path the host does not have. Six generated skills still carry that path's "need no Git escalation" clause (F1), and two hand-written surfaces still state a current sandbox or approval posture (F2). Six smaller record and naming defects are listed as F3 to F8. The operator accepted failing the order so these repairable defects are fixed ("im fine failing it to fix repairable defects", during this dispatch). The repair route and follow-up are [D006](../../evidence/WO-161/decisions.md#wo-161-d006) (`FUP-6f7834c355df7feb`). The branch remains for `resume: fix`.

## Findings

### F1 — major — the cold-start text keeps the removed approval path's escalation clause

**Criterion:** the order's heading ("the cold-start text stops describing a host sandbox and an approval path that are off in all three CLIs"), its Objective and the Design item "delete or scope the Codex approval sentence to `workspace-write` + `on-request`". Criterion 1's literal grep is met; this is the objective gap that the grep cannot see.

**Observed:** [`contributor.ts`](../../../packages/skeleton/src/loadouts/contributor.ts) line 70 replaced "State-changing resume commands require one-invocation outside-sandbox approval in Codex" but kept its dependent clause: "`status`, `times`, `briefing`, and `next` need no Git escalation." The clause is emitted, unscoped, into six cold-start files: `.claude/` and `.agents/` `dotln-executor/SKILL.md:32`, `dotln-verifier/SKILL.md:25` and `dotln-reviewer/SKILL.md:25`. The Git escalation it contrasts with is the one-invocation `.git` approval. [`PLAYBOOK.md`](../../PLAYBOOK.md) lines 113–121 and the security note now confine that approval to Codex `workspace-write` plus `on-request`, a mode the recorded posture does not use. Read in a full-access session, the clause implies that the other state-changing commands need an escalation that does not exist.

**Expected:** delete the clause with the approval sentence, or scope both to the retained mode, as the playbook now does.

**Reproduction:** `grep -n 'need no Git escalation' .claude/skills/dotln-{executor,verifier,reviewer}/SKILL.md .agents/skills/dotln-{executor,verifier,reviewer}/SKILL.md` returns six lines; `git --no-pager diff HEAD -- packages/skeleton/src/loadouts/contributor.ts` shows the retained clause.

**Limits:** the clause is not literally false. The defect is what it implies on the order's primary surface. Because `contributor.ts` is a registered source, the repair regenerates the bundle and skills. It re-pins `packages/skeleton/fixtures/wo161-role-baseline.json` and the cold-start evidence, and re-mints any edition its check marks stale.

### F2 — minor — two hand-written surfaces still state a current sandbox or approval posture

**Criterion:** the Objective (only true confinement statements) and the adjacent item D003 declared complete.

**Observed:** [`packages/skeleton/README.md`](../../../packages/skeleton/README.md) lines 820–821 say, as current fact, "Native sandbox and approval remain in force." That is the claim the order removed from the `CLAUDE.md` residue, in the Contributor-harness paragraph whose label this order corrected at line 807 (D003, adjacent-0001). The playbook's retained-mode paragraph ends (lines 127–128) with "The harness guide explains the warning, current Claude asymmetry". The asymmetry it points to is "Claude's recorded sandboxed posture" ([security note](../../AI-HARNESS-SECURITY.md) line 785), the retained 2026-09-01 posture, not the current one.

**Expected:** state the recorded posture (host permission settings decide; emission changes no host setting), or remove the README sentence. Write "recorded" or "retained" for "current" in the playbook.

**Reproduction:** `sed -n 816,822p packages/skeleton/README.md`; `sed -n 113,129p docs/PLAYBOOK.md`; `sed -n 783,789p docs/AI-HARNESS-SECURITY.md`.

**Limits:** criterion 2 remains met. The playbook's posture statement (lines 92–99) is correct, and the paragraph containing "current" opens by limiting itself to the retained mode.

### F3 — minor — the roadmap records a collision retiming of a target WO-161 never had

**Criterion:** product 07 §Release assignment (lines 1952–1957: "Complete a missing activation target under `06-roadmap.md` §Release boundary and record the base/classification … Retiming an existing target … follow[s] separate authority rules") and truthful durable records.

**Observed:** [`06-roadmap.md`](../../product/06-roadmap.md) line 23 reads "**WO-161 collision retiming (2026-09-25):** unpublished target `v0.49.0` is superseded by `v0.50.0`". At activation the order's heading carried no version. `v0.49.0` is WO-158's published tag at HEAD. D003 records that the executor seeded the heading with the README's `v0.49.0` so that `release prepare` would run. The prepare template at `scripts/lib/release-preparation.mjs:120` then wrote the retiming sentence. The roadmap has no WO-161 activation-completion note naming the base, classification and component bumps; every earlier order has one.

**Expected:** an activation-completion note recording `v0.50.0` as the next minor above the observed `v0.49.0`, with compiler 0.19.1 and skeleton 0.43.0, and a same-day correction to D003.

**Reproduction:** `git --no-pager log --tags --simplify-by-decoration --format='%d' -2` (`tag: v0.49.0` on HEAD); `git --no-pager diff HEAD -- docs/work-orders/WO-161-sandbox-vocabulary.md docs/product/06-roadmap.md`; `grep -n 'activation completion' docs/product/06-roadmap.md` (no WO-161 row).

### F4 — minor — the ADRs are edited in place beyond the Amendments mechanism, with no recorded decision

**Criterion:** product 07 line 962 ("update an ADR only through its permitted amendment mechanism") and lines 2055–2064 (reopen "as a new decision record proposal, never an in-place edit", except by appending to Amendments). Its Precedence rule also applies: a work-order override of a standing duty is noted.

**Observed:** the order directs "ADR-0003 gains an amendment (ADR-0004 and ADR-0005 a cross-reference)", and those Amendments entries are present and accurate. WO-161 also rewrites each ADR's Status line in place (ADR-0003:3–5, ADR-0004:3–6, ADR-0005:3–6). It renames each `## Decision` heading to `## Original decision — 2026-09-01` (0003:22, 0004:23, 0005:28). No decision records or justifies these edits. At HEAD, ADR-0003's Status stayed "Accepted (2026-09-01)" when ADR-0004 and ADR-0005 partially superseded decisions.

**Expected:** confine the change to the Amendments sections, or record a decision that notes the skipped duty and why the Status and heading edits help a reader.

**Reproduction:** `git --no-pager diff HEAD -- docs/decisions/0003-personal-ai-harness-security.md docs/decisions/0004-claude-sandboxed-bash-auto-allow.md docs/decisions/0005-claude-persistent-accept-edits.md`.

**Limits:** the original decision text is byte-identical, so nothing decided is falsified. `git grep` finds no inbound `#decision` anchor to these three ADRs outside historical records, so the heading rename broke no link.

### F5 — minor — the confined-row evidence has no recorded method and cannot come from the committed fixture

**Criterion:** criterion 3's evidence gate ("the fixture with the simulated confined host").

**Observed:** [`partial-before.json`](../../evidence/WO-161/partial-before.json) and [`partial-after.json`](../../evidence/WO-161/partial-after.json) are byte-identical and record `requiredSuites: ["document-barrier", "alpha"]`. The committed confined fixture (`scripts/test-runner.test.mjs:1407–1411`, with a `build` row) always yields `["build", "alpha"]`, as line 1547 asserts. `document-barrier` is chosen only when no selected row builds (`scripts/test-runner.mjs:1214–1218`). No file in `docs/evidence/WO-161` names the command, script or suite table that produced the rows. `needs` and `evidenceRefSuffix` are also assembled outside a check row.

**Expected:** record the method, or regenerate the pair from the committed fixture.

**Limits:** the claim itself holds. This verification's own reproduction under criterion 3 shows identical identity fields at entry and current code.

### F6 — minor — receipt 028's two known issues have no recorded disposition

**Criterion:** the planning map's WO-161 row ([`work-order-map.md`](../../planning/work-order-map.md) line 1976), "Receipt 028 known issues to carry into the executor's decisions". The first issue is to emit the confinement sentence from observed host posture, never from a constant. The second is that the residue states the posture rather than swapping interpretations. WO-158's final review (FINAL-001 line 52) treated its receipt 028 issues as each needing a decision.

**Observed:** `grep -n -i -E '028|known issue|observed host posture' docs/evidence/WO-161/decisions.md docs/evidence/WO-161/implementation.md` found nothing at handoff. Inference: the substance appears answered, since the generated sentences assert no posture constant and the residue uses the order's own wording. No decision says so.

**Expected:** one decision that disposes of each issue.

### F7 — minor — changed code keeps old names and an outdated reason

**Criterion:** Design, "rename … the fixture temp-root prefix".

**Observed:** `scripts/test-runner.test.mjs:1972` asserts with the message "a gate whose suites left a dotln-gate-sandbox root behind must fail". The fixture now creates `dotln-host-confinement-` roots. `scripts/test-runner.mjs:1229` comments "Gate-sandbox fixture roots". `scripts/authority-evidence.mjs:436–442` adds `componentDefinitions` to the Contributor's expected changed-field list. Its comment still gives only WO-099 D009's presence-policy reason, and nothing asserts which component definitions changed. The entropy branch above it does pin its one versioned component.

**Expected:** the new names, and a comment (or assertion) naming WO-161's three relabeled definitions.

**Reproduction:** `grep -n 'dotln-gate-sandbox\|Gate-sandbox' scripts/test-runner.test.mjs scripts/test-runner.mjs`; `sed -n 430,443p scripts/authority-evidence.mjs`.

### F8 — minor — no Config log line for the `CLAUDE.md` and `.claude/` change

**Criterion:** product 07 lines 2091–2095 and [`docs/README.md`](../../README.md) §Config log ("One line per `.claude/` or `CLAUDE.md` change").

**Observed:** WO-161 changes the `CLAUDE.md` residue line and regenerates `.claude/skills` and the hooks. Its docs-index work is a separate dated section (§Host-posture correction), and the newest Config log line is WO-144's (2026-09-19).

**Limits:** later orders that changed `CLAUDE.md` also added no line. The drift predates WO-161; the repair here is WO-161's one line.

## Acceptance criteria

**Criterion 1:** met. `git grep -n -i sandbox -- CLAUDE.md .claude/skills .agents/skills` exits 1 with no output, and a plain `grep -rn -i` over the same paths (untracked content included) agrees. At HEAD the same grep returns 11 lines, matching [`sandbox-before.txt`](../../evidence/WO-161/sandbox-before.txt). The residue line is now `CLAUDE.md:70` "Pre-effect envelope checks unavailable in this harness; host permission settings decide." Before and after bytes are recorded in [`vocabulary-before.json`](../../evidence/WO-161/vocabulary-before.json), [`vocabulary-after.json`](../../evidence/WO-161/vocabulary-after.json) and the implementation table. This verifier reproduced every figure in both skill roots, counting `CLAUDE.md` plus the role skill and, for the sandbox column, whole matching lines including terminators:

| Role | Before | After | Sandbox-line bytes before → after |
| --- | ---: | ---: | ---: |
| Executor | 25,819 | 25,789 | 1,350 → 0 |
| Verifier | 22,698 | 22,592 | 1,811 → 0 |
| Reviewer | 23,780 | 23,674 | 1,811 → 0 |
| Release close | 14,649 | 14,632 | 150 → 0 |
| Planner | 16,797 | 16,780 | 150 → 0 |
| Refuter | 16,372 | 16,355 | 150 → 0 |

The other every-session surfaces carry only true senses: the manifest and generated hooks name the discovery Seatbelt module and the retained `sandbox.disable` effect id. F1 is the objective gap this grep cannot see.

**Criterion 2:** met. The playbook (lines 92–99), `README.md` (lines 245–253), product 07 (lines 269–277 and the renamed §Host-confinement preflight at 2398–2401) and the three ADRs state the posture recorded in `docs/AI-HARNESS-SECURITY.md`. That posture is no host sandbox in Claude Code, Codex or Copilot, with DotLn's refusals plus the host permission mode as the boundary. Each distinguishes the retained `workspace-write` plus `on-request` procedure and names the discovery Seatbelt and the workers' `--sandbox workspace-write` separately. The ADR-0003 amendment names 2026-09-25 and the operator's direction, which explicitly includes Copilot. It cites the security note's 2026-09-17 section, which records Claude off and Codex `danger-full-access` plus `never`. ADR-0004 and ADR-0005 cross-reference it. Host settings are described by posture only; the clean-room screen of added lines found no copied settings, credentials or employer material. F2 and F4 are repairable defects on these surfaces.

**Criterion 3:** met. The detector is `scripts/lib/host-confinement.mjs`, exporting `detectHostConfinement`. The flag is `--confined-partial`, the fixture root prefix is `dotln-host-confinement-`, and product 07's heading is §Host-confinement preflight with a dated note. Suite selection is unchanged. On a scratch extraction of HEAD and on the current tree, `node scripts/test-runner.mjs --list` prints the same 27 suites. The partial lists (`--inside-sandbox` at HEAD, `--confined-partial` now) print the same 25, excluding `skeleton` and `portfolio`. On this host, this session's gate row records `sandbox: {marker: "claude-code", probe: written, inForce: false}` and ran all 27 suites. The executor's Codex row carries no marker. For the simulated confined host, one scratch script ran each tree's own `runGate` against a fake marker and a `0o555` directory. The partial rows match byte for byte in `checkId` (`npm test -- --inside-sandbox`), the evidence-reference suffix, `partial`, `excludedSuites` (`["outside"]`), `requiredSuites` (`["build", "alpha"]`) and the `sandbox` keys. The recorded rows, the suites that ran, and the unconfined full row also match. Only the refusal text differs, in its intended wording and printed flag. The committed runner fixtures passed in `runner-fixtures`. F5 and F7 are record and naming defects.

**Criterion 4:** met. `contributor.ts:222` reads `contributor.bounded`, with the constraint "DotLn refusals and host permission settings define the boundary" and the title "Bounded contributor authority". The `sandbox.disable` effect id is unchanged. The new WO-161/001 authority edition carries `contributor.bounded`. HEAD tracks 129 historical `authority.json` files, all carrying `contributor.sandboxed`. `git diff HEAD --name-only -- docs/evidence` lists only `docs/evidence/current.json`, so every one is byte-identical. At HEAD, `git grep` finds no assertion of the old label in any test. The writer test now asserts `contributor.bounded` with the unchanged effects, three outside-write grants and one-writer limit. The process-debt test pins the new gate sentence and a WO-161 role baseline chained to WO-158's. The historical hashes `fnv1a64:06245f5c581212f1` and `fnv1a64:87aa6e1263d6d74d` stay asserted by restoring exactly the three old labels. `skeleton` passed in `npm test`, and `process-debt` and `authority-evidence` passed in `npm run test:machinery`.

**Criterion 5:** met. `npm run harness -- check` passed: 31 generated surfaces. All five checks passed: `node scripts/authority-evidence.mjs --check`, `artifact-identity-evidence`, `verification-evidence`, `feedback-evidence` (carrying WO-159's live audit by reference) and `console-fixtures --check`. The current editions are WO-161/001. Every bounded cold start is within its ceiling in `docs/control/budgets.json`: executor 25,789 ≤ 29,246, verifier 22,592 ≤ 25,151, reviewer 23,674 ≤ 24,576, release close 14,632 ≤ 16,384 and planner 16,780 ≤ 24,576. The refuter has no ceiling, so there was no breach to record. These checks left the worktree tree hash unchanged (`6274b49f` before and after).

**Criterion 6:** met. `npm test` passed 27 suites with 0 failed in 302.33 s (71 fresh tasks, recorded 2026-09-25T17:24:15Z at tree `6274b49f` and the code identity above). `npm run test:docs` passed 21 with 0 failed in 26.71 s. `git diff --check` and `git diff --check --cached` are clean. The lens whitespace check over untracked and staged files found no trailing whitespace or missing final newline.

## Executed evidence and boarded items

Executed by this verifier on the current subject: the grep and byte inventory above; `npm run harness -- check`; the five `--check` commands; `npm test`; `npm run test:docs`; `npm run test:machinery`; both `git diff --check` forms; the two `--list` comparisons; the confined-host scratch reproduction; and `git`/`grep` reproductions of F1 to F8. Scratch copies lived only in the DotLn session scratch.

`npm run test:machinery` passed 18 suites and failed 1: `evidence-sources`, whose WO-154 pins-only test asserts that the current feedback edition is its own live audit. The same test fails the same way on a scratch copy of the entry subject, where WO-158's carried edition is current. The failure is inherited, not introduced. The reviewer's `--review` selection will not run it, because WO-161 changes none of its declared sources. It is boarded as [D007](../../evidence/WO-161/decisions.md#wo-161-d007) (`FUP-be91067a3842b44a`). `docs/product/02-domain-model.md` lines 913–915 still name "native sandbox/approval" as a control. The line lies outside this order's named surfaces and audit and is boarded as [D008](../../evidence/WO-161/decisions.md#wo-161-d008) (`FUP-422f1b3cdd4b7330`).

Notes, with no repair required:

- The race-probe directory `scripts/probes/gate-sandbox-race/` keeps its name; the order did not list it.
- The runtime file `packages/skeleton/src/harness-host.ts` says "host permissions decide", consistent with the new residue.
- The order's cited generator line numbers are off by a few lines at HEAD.
- `docs/final-reviews/WO-161/PR.md` carries a mid-execution meter snapshot that final review will refresh.
- A lens read, by posture only, that the user-level Claude setting enables the sandbox and that this linked worktree has no local override. This session's gate probe found no confinement (`inForce: false`). The effective Claude posture in linked worktrees rests on the operator's attestation, as the security note says. The operator's user-level setting is a non-goal.

## Judgment and process cost

The order serves the mission by removing a recurring operator rescue: agents reading confinement and approval instructions that the host does not have. Criteria 1 to 6 are the proxies, and each is met. Judged against the goal, NoOp on F1 and F2 would leave "need no Git escalation" in every executor, verifier and reviewer cold start. It would also leave "Native sandbox and approval remain in force" in the Contributor harness README. Those are the kinds of sentences the operator reported, so passing would seek the wrong goal and beat the rule of criterion 1's token grep. Policy resistance: an unscoped escalation clause invites agents to ask for an approval that full access never offers. Drift: passing known-false sentences normalizes partial correction. The repairs add no guard, refusal or approval step (escalation). They stay in the files the order already edits (shifting the burden, success to the successful). The only shared resource used was four batched read-only lenses (commons). Under the Naive Interventionism lens, product 02 and the inherited machinery failure are boarded rather than widened into this repair. The verifier edited no implementation.

**Process cost:** entry 95,275 tokens; handoff 15,511,120 tokens; source claude-transcript-message-usage

Both counters have `scope: dispatch`. The entry cutoff is 2026-09-25T17:17:14.671Z; the handoff cutoff is 2026-09-25T17:40:30.035Z, taken before this report and the result transition. USD cost is unavailable. The four lenses' own tokens are included in the transcript counter only as far as the host attributes them; the workflow reported 754,575 subagent tokens and 846.7 s wall-clock for the four lenses. The product gate took 302.3 s, the machinery inventory 362.6 s and the documentation gate 26.7 s, run sequentially.
