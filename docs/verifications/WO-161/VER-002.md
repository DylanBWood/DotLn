# WO-161 VER-002 — independent verification: pass

**Subject:** [WO-161](../../work-orders/WO-161-sandbox-vocabulary.md) on branch `wo-161`, uncommitted over HEAD `852861d3` (`main`, tagged `v0.49.0`). The `verify` dispatch allocated this report and checkpoint `refs/dotln/checkpoint/WO-161/7` (`db7b42a4`). The worktree judged here, hashed with untracked files, is tree `366a035c`. It differs from that checkpoint only in the three lifecycle projections the dispatch wrote (`docs/control/current.md`, `docs/control/orders/WO-161.jsonl`, `docs/work-orders/README.md`). The product gate below recorded code identity `17655ddca8c6450bac73e611c0d97632f3ac4eabb8e40bdb555cc4768381a590` at that tree. That is the same identity as the executor's final repair gate. The order's text still differs from HEAD only by the `(v0.50.0)` heading label, so the six criteria judged here are the original ones. This is the second verification. It judges the repair of [VER-001](VER-001.md) findings F1 to F8 ([repair evidence](../../evidence/WO-161/repair.md), [D009 to D011](../../evidence/WO-161/decisions.md#wo-161-d009)). The verifier made no implementation, product-document or decision edit and wrote only this report.

**Verifier:** Claude Code 2.1.282, Opus 5.5 (`claude-opus-5-5`), selected effort `xhigh` read from `CLAUDE_EFFORT` (the host's selected effort, not effective effort). This root session held the worktree's writer reservation for the whole dispatch and was the sole writer. It spawned no subagent or workflow: the harness observed 0 admissions (`exact-observed`) against the cap of 20, and every check below ran in this session. No hard USD 5 provider cap was exposed or enforced.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-opus-5-5","effort":"xhigh","source":"claude-session-readback"}

**Instrument disclosure:** the role skill this verification followed is a WO-161 output. Its product-gate sentence, and the absence of the removed escalation clause, both come from the generator this order changed. The generated hooks this order regenerated judged every command in this session. That includes four live-gate refusals of commands that were not on the read-only list. The session hook recorded this dispatch. The host-confinement detector under test wrote the `sandbox` field of this session's own gate row. The usage counter behind the cost line is DotLn's own. The criterion 3 host observation is therefore the instrument observing itself. It is corroborated by a scratch run of the entry and current runners against a simulated confined host.

**Verdict:** **Pass.** All six acceptance criteria are met on the current subject. Each of VER-001's eight findings is repaired, and the repair was reproduced on the files where VER-001 found the defect. This pass found no new implementation defect. The two items VER-001 boarded stay boarded ([D007](../../evidence/WO-161/decisions.md#wo-161-d007), `FUP-be91067a3842b44a`; [D008](../../evidence/WO-161/decisions.md#wo-161-d008), `FUP-422f1b3cdd4b7330`). This dispatch reproduced D007's inherited failure unchanged. One precedence note for final review is listed under Notes.

## VER-001 findings, rechecked

**F1 (major) — closed.** `grep -rn -i -E 'escalat|approval|outside command|confine' CLAUDE.md .claude/skills .agents/skills` returns only three kinds of line. The first is the new product-gate sentence in the verifier and reviewer skills (lines 35). The other two are the executor's Decision Receipts and Operator Check-In support sentences, which name no host approval path. "need no Git escalation" appears nowhere. [`contributor.ts`](../../../packages/skeleton/src/loadouts/contributor.ts) line 70 now reads "Host permission settings decide execution; inspect the exact command, package mapping, and lifecycle-script diff before a state-changing resume command." [`test-process-debt.mjs`](../../../scripts/test-process-debt.mjs) lines 4558–4562 assert the clause's absence in every generated role, and lines 7345–7357 assert the new gate sentence in both verifier and reviewer roots. Both ran green in `process-debt` below.

**F2 (minor) — closed.** [`packages/skeleton/README.md`](../../../packages/skeleton/README.md) lines 820–821 now say "Host permission settings decide execution; emission changes no host setting." The [playbook](../../PLAYBOOK.md) paragraph at lines 113–131 opens by limiting itself to Codex `workspace-write` with `on-request`, states that full-access sessions have no outside-approval path, and says "retained Claude asymmetry". `grep -n -i -E 'sandbox|approval' docs/PLAYBOOK.md` finds only the posture statement (line 94) and lines inside that scoped paragraph.

**F3 (minor) — closed.** [`06-roadmap.md`](../../product/06-roadmap.md) lines 23–28 carry a WO-161 activation-completion note: `v0.50.0`, the next minor above the observed `v0.49.0` tag, with compiler `0.19.1` and skeleton `0.43.0`. The collision-retiming sentence is gone. `git --no-pager log --tags --simplify-by-decoration` shows `v0.49.0` on HEAD. `packages/compiler/package.json` is `0.19.1`, `packages/skeleton/package.json` is `0.43.0`, and `COMPILER_PACKAGE_VERSION` is `0.19.1`. D009 carries a same-day correction to D003, appended below it; `git --no-pager diff refs/dotln/checkpoint/WO-161/4 refs/dotln/checkpoint/WO-161/7 -- docs/evidence/WO-161/decisions.md` shows additions only, so D003's bytes are unchanged.

**F4 (minor) — closed by VER-001's second route.** VER-001 expected the ADR changes either confined to the Amendments sections or a decision recording the skipped duty and why the Status and heading edits help a reader. D009 is that decision. Its rejected option gives the reader-facing reason: an unchanged "Accepted" label below a historical sandbox-on title would hide the current amendment. The ADR diffs against HEAD still change only the Status lines, the `## Decision` → `## Original decision — 2026-09-01` headings and text appended under each `## Amendments` heading (ADR-0003 line 94, ADR-0004 line 78, ADR-0005 line 79). The decision bodies are byte-identical. See Notes for the precedence wording.

**F5 (minor) — closed.** [`partial-method.mjs`](../../evidence/WO-161/partial-method.mjs) records the method. It is a no-build table (`alpha`, and `outside` declaring `needs: outside-sandbox`) under a fake marker and a `0o555` directory, and it runs `runGate` with `--confined-partial --serial`. `node docs/evidence/WO-161/partial-method.mjs` exited 0 here and asserted its projection equal to both saved files. The script imports only the current runner, so it cannot by itself show the entry side. The independent two-tree reproduction under criterion 3 covers that side.

**F6 (minor) — closed.** [D010](../../evidence/WO-161/decisions.md#wo-161-d010) disposes of both Receipt 028 issues named in the [map row](../../planning/work-order-map.md) (line 1976). The first issue asked to emit the confinement sentence from the observed host posture, never a constant. D010 declines a generator-time probe with reasons and keeps the compiled sentence conditional on the runner's runtime probe. The generated text asserts no posture constant, which this verifier confirmed in the grep above. The second issue asked that the residue state the posture. D010 records that the residue states the permission boundary and that the dated ADR records the host posture.

**F7 (minor) — closed.** `scripts/test-runner.test.mjs:1972` reads "a gate whose suites left a dotln-host-confinement root behind must fail", and `scripts/test-runner.mjs:1229` reads "Host-confinement fixture roots". [`authority-evidence.mjs`](../../../scripts/authority-evidence.mjs) lines 439–441 name WO-161's three relabeled Contributor definitions beside WO-099's presence policy. A search of `scripts` and the package sources and tests for `gate-sandbox|gateSandbox|inside-sandbox|insideSandbox` finds three kinds of match. The first is the preserved identity constant `CONFINED_PARTIAL_CHECK = "npm test -- --inside-sandbox"` and its assertion. The second is the race-probe directory name `scripts/probes/gate-sandbox-race/`, which VER-001 noted the order does not list. The third is that probe's own path comments. The probe's test-name pattern and root prefix were updated. Both of its loader anchors still exist in the test file, at lines 1380 and 1657.

**F8 (minor) — closed.** [`docs/README.md`](../../README.md) §Config log gains a 2026-09-25 WO-161 entry above WO-144's, covering the regenerated role skills, hook bundle and `CLAUDE.md` residue.

## Acceptance criteria

**Criterion 1:** met. `git grep -n -i sandbox -- CLAUDE.md .claude/skills .agents/skills` exits 1 with no output. A plain `grep -rn -i` over the same paths, which also covers untracked content, agrees. At HEAD the same grep returns 11 lines. The residue line is `CLAUDE.md:70` "Pre-effect envelope checks unavailable in this harness; host permission settings decide." The `.claude/harness-manifest.json` matches name only `packages/skeleton/dist/src/discovery-sandbox.js`, the discovery Seatbelt. `wc -c` on the current installed files reproduces the repaired cold starts in both skill roots:

| Role | Before (HEAD) | After (repaired) | Sandbox-line bytes before → after |
| --- | ---: | ---: | ---: |
| Executor | 25,819 | 25,723 | 1,350 → 0 |
| Verifier | 22,698 | 22,526 | 1,811 → 0 |
| Reviewer | 23,780 | 23,608 | 1,811 → 0 |
| Release close | 14,649 | 14,632 | 150 → 0 |
| Planner | 16,797 | 16,780 | 150 → 0 |
| Refuter | 16,372 | 16,355 | 150 → 0 |

The before column and the sandbox-line bytes were reproduced by VER-001 and are recorded in [`vocabulary-before.json`](../../evidence/WO-161/vocabulary-before.json). The after column matches [`vocabulary-after.json`](../../evidence/WO-161/vocabulary-after.json) and [`cold-start.json`](../../evidence/WO-161/cold-start.json), `CLAUDE.md` 6,096 bytes plus each skill.

**Criterion 2:** met. The [playbook](../../PLAYBOOK.md) lines 92–99, [`README.md`](../../../README.md) lines 245–253 and [product 07](../../product/07-execution-guide.md) lines 269–277 all state the posture recorded in [`docs/AI-HARNESS-SECURITY.md`](../../AI-HARNESS-SECURITY.md) lines 48–64: no host sandbox in Claude Code, Codex or Copilot, with DotLn's refusals plus the host permission mode as the boundary. So do product 07's renamed §Host-confinement preflight (lines 2398–2401) and ADR-0003 to ADR-0005. Each separates the retained `workspace-write` plus `on-request` procedure and names the discovery Seatbelt and the workers' `--sandbox workspace-write` as separate mechanisms. The [ADR-0003 amendment](../../decisions/0003-personal-ai-harness-security.md#amendments) names 2026-09-25 and the operator's direction, which includes Copilot. ADR-0004 and ADR-0005 cross-reference it. The anchors `PLAYBOOK.md#harness-safety-baseline`, `AI-HARNESS-SECURITY.md#current-mode-choices-and-settings-locations--2026-09-17` and `0003-…#amendments` resolve to existing headings. The clean-room screen of the added lines found posture descriptions only, with no copied settings, credentials, internal identifiers or employer material.

**Criterion 3:** met. The detector is `scripts/lib/host-confinement.mjs` and exports `detectHostConfinement`. The flag is `--confined-partial`, and `node scripts/test-runner.mjs --list --inside-sandbox` now prints the usage error. The fixture root prefix is `dotln-host-confinement-`, and product 07's heading is §Host-confinement preflight with a dated note. Suite selection is unchanged: `node scripts/test-runner.mjs --list` prints byte-identical output on a scratch extraction of HEAD and on the current tree (27 suites). The partial lists (`--inside-sandbox` at HEAD, `--confined-partial` now) are also byte-identical: 25 suites, excluding `skeleton` and `portfolio`. On this host, this session's gate row records `sandbox: {marker: "claude-code", probe: {code: "written", denied: false}, inForce: false}`, and all 27 suites ran. For the simulated confined host, one scratch script ran each tree's own `runGate` against a fake marker and a `0o555` directory in four cases:

- the committed fixture shape with a `build` row;
- the evidence file's no-build shape;
- a confined run with no flag;
- an unconfined run.

HEAD and current agree in every case on `checkId` (`npm test -- --inside-sandbox`), the evidence-reference suffix, `partial`, `excludedSuites` (`["outside"]`), `requiredSuites`, the `sandbox` keys, the suites that actually ran, and the exit code. The `requiredSuites` are `["build", "alpha"]` with the build row and `["document-barrier", "alpha"]` without it. The no-build projection equals [`partial-before.json`](../../evidence/WO-161/partial-before.json) and [`partial-after.json`](../../evidence/WO-161/partial-after.json). Only the refusal text differs, in its intended wording ("confined by the host", "Run outside host confinement") and printed flag. The committed runner fixtures passed in `runner-fixtures` below.

**Criterion 4:** met. `contributor.ts:222` reads `contributor.bounded`. The constraint is "DotLn refusals and host permission settings define the boundary" and the support title is "Bounded contributor authority". The `sandbox.disable` effect id is unchanged. The selected [authority edition](../../evidence/WO-161/authority/002/authority.json) `WO-161/002` carries `contributor.bounded`, and neither WO-161 revision carries `contributor.sandboxed`. The tree holds 129 historical `authority.json` files carrying `contributor.sandboxed`. `git --no-pager diff HEAD --name-only -- docs/evidence` lists only `docs/evidence/current.json`, so every tracked historical file is byte-identical. `git --no-pager diff refs/dotln/checkpoint/WO-161/4 refs/dotln/checkpoint/WO-161/7 --stat -- docs/evidence docs/verifications` shows that `authority/001` and VER-001 were untouched by the repair. [`writer.test.ts`](../../../packages/skeleton/test/writer.test.ts) asserts `contributor.bounded` with the unchanged effects, three outside-write grants and one-writer limit. At HEAD no test asserted the old label, as VER-001 found. The process-debt test pins the repaired role bytes through the chained `wo161-role-baseline.json`. The historical hashes `fnv1a64:06245f5c581212f1` and `fnv1a64:87aa6e1263d6d74d` remain asserted by restoring exactly the three old labels. `skeleton` passed in `npm test`, and `process-debt` and `authority-evidence` passed in `npm run test:machinery`.

**Criterion 5:** met. `npm run harness -- check` passed on 31 generated surfaces. All five edition checks passed: `node scripts/authority-evidence.mjs --check`, `artifact-identity-evidence`, `verification-evidence`, `feedback-evidence` (carrying WO-159's live audit by reference) and `console-fixtures --check`. `docs/evidence/current.json` selects authority `WO-161/002` and the other three editions at `WO-161/001`, as D011 records. `node scripts/harness-context.mjs --check` exited 0 with every profile in both roots `within`, and the refuter `unset`. Every bounded cold start is within its `docs/control/budgets.json` ceiling: executor 25,723 ≤ 29,246, verifier 22,526 ≤ 25,151, reviewer 23,608 ≤ 24,576, release close 14,632 ≤ 16,384 and planner 16,780 ≤ 24,576. The refuter has no ceiling, so there was no breach to record. These checks and every later one left the worktree tree hash unchanged: it was `366a035c` before them and after them, measured before this report was written.

**Criterion 6:** met. `npm test` passed 27 suites with 0 failed in 291.88 s (71 fresh tasks, recorded 2026-09-25T18:15:35.561Z at tree `366a035c` and the code identity above). `npm run test:docs` passed 21 with 0 failed in 25.46 s. `git diff --check` and `git diff --check --cached` are clean. The untracked files have no trailing whitespace and no missing final newline.

## Executed evidence and boarded items

This verifier ran the following on the current subject: `npm test`; `npm run test:docs`; `npm run test:machinery`; `npm run harness -- check`; the five `--check` commands; `node scripts/harness-context.mjs --check`; `npm run publication:check`; `npm run release -- check-surfaces --local` (each exit 0); both `git diff --check` forms; the criterion 1 greps and byte counts; the two `--list` comparisons against a `git archive` extraction of HEAD; `partial-method.mjs`; the four-case confined-host scratch reproduction; and `git`/`grep` reproductions of F1 to F8. Scratch copies lived only in the DotLn session scratch, and the confined-host temporary repositories were removed.

`npm run test:machinery` passed 18 suites and failed 1 in 333.38 s. The failure is `evidence-sources`, whose WO-154 pins-only test expected `docs/evidence/WO-161/feedback-001` and got `docs/evidence/WO-159/feedback-001`. That is the same assertion, with the same values, that VER-001 recorded and boarded as [D007](../../evidence/WO-161/decisions.md#wo-161-d007) (`FUP-be91067a3842b44a`). None of the suite's five declared sources (`scripts/test-runner.mjs` lines 213–219) differs from HEAD. The failure is therefore inherited, not introduced, and the reviewer's `--review` selection will not run it. `process-debt`, `runner-fixtures`, `authority-evidence`, `harness-fixtures` and `feedback-evidence` passed. [D008](../../evidence/WO-161/decisions.md#wo-161-d008) (`FUP-422f1b3cdd4b7330`, product 02 lines 913–915) is unchanged and stays boarded. This pass met no new defect, so it adds no decision.

## Notes, with no repair required

- **Precedence wording (F4).** D009 calls the ADR Status and heading annotations "the bounded WO-161 precedence exception". Product 07 §Precedence applies when a work order's authority clause conflicts with a standing duty. WO-161 directs only "ADR-0003 gains an amendment (ADR-0004 and ADR-0005 a cross-reference)". So the annotations are the executor's judgment, recorded by D009, rather than something the order directed. §Precedence also asks that a skipped duty be noted "in your result as an open question". The repair evidence notes the exception but does not frame it as one. The edits change no decision text and help a top-of-file reader. Whether to keep them is left open for final review and the operator, not routed to repair.
- The race-probe directory `scripts/probes/gate-sandbox-race/` keeps its name; the order does not list it.
- `docs/final-reviews/WO-161/PR.md` carries an executor-time snapshot that final review will refresh.
- The effective Claude posture in this linked worktree rests on the operator's attestation, as the security note says. This session's gate probe found no confinement (`inForce: false`). The operator's user-level setting is a non-goal.

## Judgment and process cost

The order serves the mission by removing a recurring operator rescue: agents reading confinement and approval instructions that the host does not have. VER-001 failed the order because that outcome was incomplete on its primary surface even though every criterion was met. This pass judges the same outcome. No generated cold start now names a sandbox or an approval or escalation path. The one conditional gate sentence is true on any host, and the hand-written surfaces VER-001 flagged state the recorded posture.

- **Seeking the wrong goal and rule beating.** The pass does not rest on criterion 1's token grep alone. The new `need no Git escalation` assertion and the direct approval/escalation grep check the implication the grep missed.
- **Policy resistance, fixes that fail, and drift.** Suite selection, the confined-row identity fields and the historical authority bytes are shown unchanged by the two-tree comparison, not by the executor's saved files.
- **Escalation and shifting the burden.** The repair added no refusal, approval step or host setting, and it fixed the generator rather than the generated text.
- **Success to the successful.** The detector was kept rather than removed because it remains useful on a genuinely confined host, which operator-review assumption 2 records.
- **Tragedy of the commons.** This verification spawned no agent and ran its gates sequentially.
- **Naive Interventionism.** The verifier changed nothing. The precedence note and the two boarded items are left to final review and the planner rather than widened into this pass.
- **NoOp.** Failing again, or holding, would leave a subject whose criteria and prior findings all reproduce as met on the same code identity the executor gated. No evidence supports another repair loop.

**Process cost:** entry 81,760 tokens; handoff 13,635,770 tokens; source claude-transcript-message-usage

Both counters have `scope: dispatch`. The entry cutoff is 2026-09-25T18:09:42.474Z; the handoff cutoff is 2026-09-25T18:23:27.891Z, taken before this report was written and before the result transition. USD cost is unavailable. The product gate took 291.9 s, the documentation gate 25.5 s and the machinery inventory 333.4 s, run sequentially.
