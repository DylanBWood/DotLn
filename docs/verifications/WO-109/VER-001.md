# VER-001 — WO-109 Re-mining Well: source census and first pilot draw

**Verdict: WO-109 passes.** All eight acceptance criteria and every item named
in the work order's Evidence gate are met, and I reproduced each of them from
the primary sources rather than from the executor's receipts. The draw's
strongest claim is a negative one — zero qualifying local yields against a
threshold of two — and a negative result is exactly the kind of claim a
verifier must be hardest on, because it is the cheapest one to fake by not
looking. I looked: I re-read the raw pilot units in the main checkout, viewed
four of the eight selected images at original resolution, re-read the ledger and
product sections the draw calls descendants, and re-derived every published
count. The zero is honest, and the reasoning that produced it is careful.

Two findings are recorded below. Neither falsifies an acceptance criterion.
**F-1 must be closed before final review**: the work order's H1 still carries the
`(version assigned at activation)` placeholder, so
`npm run release -- check-surfaces --local` exits 1 on this branch and takes
`npm test` with it. The version is an activation assignment the operator or
planner owns; I cannot supply it and neither could the executor.

- Subject work order: `docs/work-orders/WO-109-shape-first-source-remine.md`.
  The two appended operator paragraphs (§Operator scope amendments and
  §Operator preservation exception, both 2026-09-07) are this order's own
  changes; the objective, source contract, reading protocol, criteria, evidence
  gate, non-goals, and operator-review assumptions are unchanged from the
  activated authority.
- Subject state: the uncommitted working tree on branch `wo-109`, worktree
  `/Users/dylanwood/Projects/DotLn-wo109`, 2026-09-08. `HEAD` is
  `497a24f4498f792508f311605dbb65671d5e1581` (the WO-032 merge, #45).
  `git log main..wo-109` is empty, so the no-worktree-commit rule holds and the
  entire subject is the dirty tree: 4 tracked files modified, 15 untracked
  paths.
- Lifecycle: `WorkOrderActivated` 21:55:27.662Z; `ImplementationReady`
  2026-09-08T00:13:36.400Z (checkpoint `c46a23ba`, `refs/dotln/checkpoint/WO-109/2`)
  by Codex CLI 0.153.4 / `gpt-6-astra` / `max`, source `operator-attested`; this
  dispatch's `VerificationRequested` 00:17:56.775Z (checkpoint `2676501d`,
  `refs/dotln/checkpoint/WO-109/3`). No repair episode exists; this is a first
  verification.
- Subject integrity: I hashed all 652 paths in checkpoint 2 as Git blobs and
  compared them to the working tree. 649 are byte-identical and none is missing.
  Exactly three differ, all lifecycle bookkeeping:
  `docs/control/orders/WO-109.jsonl` (this dispatch's event),
  `docs/control/current.md` (its projection), and `docs/work-orders/README.md`
  (the index the executor regenerated after its own transition, per 07 §WO-026).
  No deliverable file moved between `ImplementationReady` and this report.

## Self-referential instruments, disclosed

Per 07 §We are driving the car while we are still building it.

1. **The draw's own conservation proof is the thing being verified.** The
   claim "every manifested source byte is unchanged" is asserted by a manifest
   the same session wrote. A manifest cannot vouch for itself. I therefore
   recomputed SHA-256 over all 150 manifested files from
   `/Users/dylanwood/Projects/DotLn/docs/intake/` with my own script and
   independently enumerated the current tree to find additions and deletions,
   rather than reading `output-delta-inventory.json` and believing it. I also
   opened the backup ZIP directly and re-hashed its members against the
   manifest instead of accepting the recorded comparison.
2. **The clean-room screen is judged by the actor bound by it.** The executor
   screened its own prose against the operator's private term list. I re-ran the
   screen myself against `docs/control/local/terms.txt`, over the whole
   committed surface, and then read the raw quarantined material so I could
   screen for its specific contents rather than only for the list. No term value
   appears in this report.
3. **`resume` allocated this report and will record its verdict.** That is
   ordinary dogfooding of an append-only log with a regenerated projection,
   which fails loudly rather than plausibly. It is disclosed, not avoided.

## What I did, independently

- Recomputed the source-input manifest (150 files), the current-tree delta, the
  product/decision snapshot (23 files), and the backup archive (148 members).
- Re-derived every published count from the registers with my own script:
  source register, stone register, coverage cells, lens and stratum
  cross-tabs, lineage links, visual receipts, founding reconciliation.
- Read the entire committed prose surface: all 14 files under
  `docs/lineage/remining/` (3,242 lines) plus the diffs to the work order, the
  planning map, and the generated index.
- Read the raw pilot units in place in the main checkout: both operator chats in
  full, both mixed notes in full (including the quarantined one), and four of
  the eight selected images at original resolution.
- Re-read the five ledger sections and three product sections the draw treats as
  descendants, and checked the lineage claims against them line by line.
- Verified all 31 distinct `path:line` citations in the research records against
  the files they name.
- Ran `git diff --check`, `publication:check`, `work-orders index --check`,
  `release check-surfaces --local`, and Prettier.

## Acceptance criteria

**1. Intake reconciled, backed up, manifest and delta proven — met.**
All 150 manifested files exist and are byte-identical under my own recompute;
zero deletions. The manifest excludes `docs/intake/remining/` (its single
`remining` string is a note *filename*, not a subtree entry). The current tree
holds 43 files beyond the manifest: 42 under the allowlisted
`remining/draw-001/` output subtree and exactly one elsewhere,
`notes/WO-039-expanded-ideation-2026-09-07.md`, which is precisely the addition
the operator's preservation exception names and which the receipts record as
unread. I did not read its content either. The backup ZIP
(`DotLn-intake-20260907T220701Z.zip`, 34 MB) contains 148 payload members; all
148 byte-match the manifest, and the one apparent mismatch is a filename
encoding artifact — the archive stores a narrow no-break space without the
Info-ZIP UTF-8 flag — which re-decodes to the manifest path with matching bytes,
exactly as the executor recorded. Founding reconciliation is honestly reported
as **partial**: 46 image references (42 resolved, 4 `missing-context` across two
filename families), 2 unidentified founding notes, 1 image with no founding
reference, each with a reopening condition.

The activation-refusal condition was hit and resolved rather than refused: the
worktree held a duplicate raw intake copy (the operator explains in the local
direction capture that they copied it in manually). The executor relocated the
whole duplicate tree, with recorded operator approval, to
`DotLn-intake-recovery-WO-109-20260907T220231Z/` (150 files, still present),
recreated the three tracked placeholders byte-identically, and confirmed main's
payload unchanged. I verified the outcome: the worktree's `docs/intake/` now
contains only the three tracked `.gitkeep` placeholders, so the single-copy
invariant holds and no reading happened from a divergent copy. The approval is
executor-attested in the checkpoint rather than captured in the operator's own
words; the final reviewer should note that, but the invariant the refusal
protects was restored before any source was read.

**2. Clean-room disposition, first-contact capsules, scales, orthogonal lens,
terminal disposition, whole-image and region passes — met.**
All 145 registered sources carry a `cleanRoom` value; all 20 roots carry a
capsule, declared scales, four lens passes, and one terminal disposition. The
first-contact-before-descendants ordering is not merely asserted: the twelve raw
capsules were written between 18:37 and 19:23, and all eight descendant capsules
at 19:59. Where isolation was impractical the capsules say so in their own
receipts ("This is not a blinded reading"), and the coverage report repeats the
disclosure rather than claiming independence.

Image treatment is the criterion most easily faked, and it is not faked. All
eight receipts record `requestedDetail: original` / `returnedDetail: original`,
whole-composition and normalized region coordinates, and `ocr: not-performed`.
I opened four myself. R-005 is the Ladder of Leadership at 750x1125, exactly the
recorded dimensions; the capsule's claim that the lower three bands separate
direction, observation and interpretation, the middle two a preferred action
from declared intent, and the upper two completed from ongoing work is correct
band by band. R-007 is a book excerpt whose four diagrams are the two serial
orders, a converging assembly and separate products, with dialogue below — as
described. R-010 is a generated search overview, visibly labelled, whose five
components and four policy families match the capsule's region notes item for
item. R-012 is a twelve-item intervention list, matching its record and its
caveat. No transcript of any of them appears in committed prose.

**3. Terminal status on every declared cell; counts reconcile; deferrals name
reopening conditions — met.**
416 cells, 416 unique ids, zero blanks. Every stone's declared
lens x scale product is present with no missing and no undeclared cell; every
cell's disposition agrees with its stone's. My cross-tabs reproduce the coverage
tables exactly: roots 15/2/2/1/0, cells 312/48/40/16/0, and each of the four
lenses indexing 104 cells as 78/12/10/4. Stratum grouping reconciles to the
published table (4 text roots x 6 scales, 8 image roots x 4 scales, 5 ledger and
3 product roots x 6 scales = 96+128+120+72 = 416). All 133 unselected payloads
are `deferred-with-reason` and all 133 carry a `reopenWhen`. The census
reconciles end to end: 22 chats (I confirmed they are 11 numbered
operator/model pairs), 74 notes, 47 images, 2 preservation artifacts = 145
payloads; plus 3 placeholders and 2 filesystem-metadata files = 150.

**4. Routing lines, at most three full candidate records, lowering or honest
stop, one complete down-and-back-up loop — met.**
Each of the ten atlas cards carries a Route naming a cheapest sufficient rung, a
rejected rung, and a consumer. Exactly three cards carry full candidate records
(F-01 on C-01, F-02 on C-03, F-03 on C-04), and I checked each against the
work order's required elements: stratum and carried shape, chosen and rejected
mechanic kinds, scope, activation and expiry, authority effect, observable
success and failure, evidence needed, lowering losses, and promotion condition
are all present in all three. Honest stops are used where warranted
(`not-warranted`, `unknown`, `inconclusive`, `not-yet-reducible` all appear).
C-01 shows the complete loop and says what the return changed: a static diagram
and a changed cost label "would have looked complete while concealing the
absence of executed comparison." That is a real change of reading, not a
formality, and it is the finding that drives the top-ranked next move.

**5. Bidirectional lineage; strata prevent circular corroboration; dissent and
losses visible — met.**
All 20 stones appear in `lineage-links.jsonl` (35 links, each with direction,
reverse, and a current destination). Forward navigation lives in
`research/source-comparison.md`; the reverse index lives in
`next-moves.md` §Bidirectional navigation, keyed by current destination and
explicitly noting it "does not imply that those files were changed." The
anti-circularity rule is stated in the README, restated in the atlas preamble
("These are retained intent plus original experiments, not independent
corroboration"), and applied in the records — Phoenix and The Goal are marked as
related lineage rather than independent votes; QU-08's own counterlens cites
Watts-Dodds against the source it is drawn from. Dissent survives: the two
tensions, the single no-yield, the losing hypotheses in OF-07, and the
"Preserve unsuccessful and uncertain trials" line in C-10.

**6. No raw reproduction, locator, hash, quarantined clue, forbidden term, or
unsupported literal claim; ledger and product byte-identical — met.**
The 23 snapshot files (16 ledger/product plus 7 ADRs) hash identical to
`product-snapshot.json` and `git status` shows them untouched. Machine screens
over the whole committed surface return zero absolute paths, zero SHA-like
strings, zero `docs/intake` references, zero raw source ids, zero raw
`chats|notes|images/...` filenames, and zero line locators.

The private-term screen returned three substring hits, all inside ordinary
English adverbs, and **zero word-boundary matches** — the executor's "passed" is
correct. Because a nine-entry list cannot cover everything a source contains, I
also read the raw quarantined note and its companion in full and screened the
committed surface for what they actually hold: the commercial tracker names, the
employer names, an internal story identifier, the predecessor rule-label
inventory, the model gateway, and the personal material in the second note. All
zero. Two apparent hits resolve as innocent: `launchpad` is an established
public term in this repository (41 uses in the committed ledger, plus product
03/06/12 and the planning map), and `teams` is generic English in the Team
Topologies discussion.

Literal claims hold up. The research register's "142 book-inspired idea/lead
headings and 24 interaction records: 166 addressable records" is exactly right —
I counted 166 unique record headings, 24 of them IX. The comparison record's
separate claim that the mixed note's rule-label inventory holds "142 named
topics" is also exactly right; the collision of the two numbers is a
coincidence, and I verified both independently. Of the 31 distinct `path:line`
citations, 30 resolve to the content they claim, including the load-bearing
ones: `EVALUABLE_CADENCE_KINDS` is precisely Once/After/Every/Gate/Until/Backoff
out of fourteen declared kinds; `EVALUABLE_PROGRAM_KINDS` is precisely
Done/Emit/Invoke/Await/Guard/Sequence with `All` and `Race` present as types but
excluded; `compile.ts:538` is exactly the one-active-mechanic,
one-participating-group, one-pipeline restriction; `scenario.ts:166` is
literally `class FakeScheduler`, supporting "fixture-driven scheduler"; and
`work.ts:87` carries the string "Observed actions only; the board cannot invoke
them", supporting "legal actions cannot be invoked". The thirty-first is F-2
below.

**7. Independent verifier reads raw units, samples every stratum and lens,
reviews every quarantine in full — met by this report.**
Sampled across all eight recorded strata and all four dispositions: both
`operator-text` chats (R-001, R-002, reinforcement), both mixed notes (R-003
`operator-text-with-uninspected-rule-labels`, R-004
`operator-text-with-unendorsed-quotations`), `image-gestalt` reinforcement
(R-005, R-012) and tension (R-007), `image-containing-unendorsed-model-text`
no-yield (R-010), `prior-interpretation` represented-elsewhere (R-013, R-015)
and tension (R-017), and the `current-interpretation` product sections. There
are no yields and no supersession proposals to sample, which is itself the
finding.

The comparisons hold against the sources. R-001's raw chat asks for browser and
Playwright as a universal interaction substrate and for per-turn session
disposal; the ledger's Chat 001 section shows both entries marked `transformed`
and `adopted` respectively — "narrowed by Resolution 5" and "Task/phase-scoped
sessions, not per-turn disposal" — so "deliberately narrowed", not lost, is
correct. R-013's `represented-elsewhere` is well founded: an 11-line raw chat
produced 28 ledger entries, most of them model-supplied implementation options.
R-015's claim that the Notes 001 ledger section is "four note-specific
additions" and not evidence the rest vanished is literally true — the section
has exactly four bullets. The R-007/R-017 tension is real and I read the text
that establishes it: the ledger's image entries convert a book analogy into
"primitives ... from which any agent pipeline topology can be composed" and
"universal scheduling rules", and describe generated overview content as
"ready-made, battle-tested vocabulary the blueprint can adopt wholesale" that
"would give the architecture rigor, a literature to draw on." Routing that to a
later ledger append rather than an in-place rewrite is the right call under
§Settled is settled.

**Quarantine review, in full.** Q-001 held the whole of source S-072 / root
R-003 and its descendant R-015 after the screen found employer-linked material
and private identifiers. I read that source in full and confirm the screen was
correct: it contains material this repository must never carry. The operator's
recorded disposition replaced whole-note exclusion with granular
generalization. I checked the result rather than the intent. The committed
output keeps the portable capability — importing a changing, formatted,
image-bearing external work artifact through interchangeable connectors, and
carrying questions and answers into work without the operator acting as courier
— expressed as C-04, IX-18 and AG-02, using the repository's existing generic
vocabulary. It carries none of the private specifics, and it does not derive a
design from any employer implementation; C-04's own text says "Private examples
were generalized under the operator's disposition; no private implementation is
a design source." Q-001 is the only quarantine record; its disposition record is
the second and final entry.

**8. Budget held; yield, threshold and recommendation stated; `git diff --check`
and the publication check clean — met.**
Budget: 20 roots (cap 20), 10 atlas cards (cap 12), 362 atlas lines (cap 600),
137 coverage lines (cap 150), 3 full candidate records (cap 3). All 55 local
links in the committed output resolve, anchors included, which matches the
executor's count exactly. The yield is stated plainly as 0 against a threshold
of 2 with the recommendation not to repeat an identical draw, and the report
refuses the easy overstatement in both directions — it does not inflate the
research inventory into local yield, and `next-moves.md` explicitly forbids
rewriting the zero as "nothing useful found." `git diff --check` is clean and
`publication:check` passes (239/239 product headings indexed, both audience
tables current).

Formatting needs a note. `npm run format:check` in this worktree reported 26
files, none of them WO-109 deliverables. The cause is environmental: this
worktree's `node_modules` is empty, so `npx prettier` resolved to a stale global
3.2.5 instead of the pinned 3.9.6. Run with the pinned binary from the main
checkout, the entire repository passes: "All matched files use Prettier code
style!" The executor's evidence records the same workaround, so its claim is
accurate and honestly labelled.

## Findings

**F-1 — The work order's H1 carries no strict version, and
`check-surfaces --local` fails on this branch. Close before final review.**
The heading still reads `# WO-109 — Re-mining Well: source census and first
pilot draw (version assigned at activation)`. `release check-surfaces --local`
exits 1 with "work-order heading must contain exactly one strict vX.Y.Z
version", because `checkSurfaces` reads the authority of whatever order the
control state selects, regardless of phase. `npm test` runs that check second,
so the repository's standard gate fails on `wo-109` today. This is not a
formality: the generated index records the rule that even an inferred no-release
close needs "a strict H1 version", and every activated or closed order in the
index carries one, while only drafts such as WO-107 keep the placeholder. The
work order's own activation preflight names "version" among the four things to
pin, and the local checkpoint shows the executor noticed ("The active work-order
heading still has no application target") without resolving or escalating it.
The executor's evidence lists format, `git diff --check`, `publication:check`,
`work-orders index --check` and the link/budget checks — `check-surfaces` and
`npm test` are absent, which is why this surfaced here rather than earlier.

I am not treating this as a criteria failure. Criterion 8 and the Evidence gate
name `git diff --check`, formatting and the publication check, and those are
green; the release classification itself was decided and recorded (internal
research and documentation, honest no-release close, no application or component
version change). What is missing is the H1 assignment, and assigning it is an
operator or planner act that neither the executor nor I may make unilaterally.
Before final review: assign the version in the heading and re-run
`release check-surfaces --local`. Note that a full `npm test` cannot run in this
worktree at all until dependencies are installed.

**F-2 — One stale line citation, caused by this order's own edit.**
`research/architecture-and-games.md` (AG-01) cites the parity/drag-equip
candidate at `docs/planning/work-order-map.md:233`. That was correct when
written; this same order then inserted its 11-line registry-candidate pointer
higher in the map's Preserved unallocated candidates section, and the cited
bullet now begins at line 244. The pointer is off by exactly those 11 lines. No
claim depends on it and every other citation resolves. This is a non-substantive
documentation correction of the kind 07 §Ideation breakout receipt permits
during the integration window; I am reporting rather than applying it so that
the artifact I certify stays the executor's, and because F-1 already requires an
edit pass before final review. Consider dropping the line number, since a
citation into a living planning map will drift again.

## Evidence gate

| Gate item                              | Result | How I established it                                                                                 |
| -------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------- |
| Frozen manifest and close comparison   | green  | Recomputed SHA-256 for all 150 files; 0 changed, 0 missing, 0 deleted                                |
| Allowlisted output-delta inventory     | green  | Enumerated the tree myself; 42 extras under the draw subtree, 1 approved addition, nothing else      |
| Register-to-coverage reconciliation    | green  | Own cross-tabs reproduce every published root, cell, lens and stratum count                          |
| Visual inspection receipts             | green  | 8/8 receipts whole+region at original detail, OCR none; I opened 4 and matched them region by region |
| Local term scrub plus verifier read    | green  | Re-ran the private-list screen (0 word-boundary hits) and read the full committed surface myself     |
| Sampling and quarantine review         | green  | Every stratum and disposition sampled against raw sources; Q-001 read in full and its result checked |
| Budget and yield report                | green  | 20/10/362/137/3 within caps; yield 0 against threshold 2 with recommendation stated                  |
| `git diff --check`                     | green  | Clean                                                                                                |
| Formatting                             | green  | Pinned Prettier 3.9.6: whole repository passes (see the worktree-environment note above)             |
| Publication check                      | green  | `publication:check` PASS                                                                             |
| `release check-surfaces --local`       | fails  | F-1; not a gate item, but it blocks `npm test` and the closeout path                                 |

## Actor

This verification was performed by Claude Code 2.1.263 running `claude-opus-5[1m]`
at `max` effort. The effort value is self-reported: the harness exposes it in
its own configuration output, not as a readback of effective reasoning, so I
claim the setting and not the behaviour. The executor was a different actor
(Codex CLI 0.153.4 / `gpt-6-astra` / `max`, operator-attested), so the
implementer-is-not-verifier rule holds, and the work order's `xhigh+` verifier
requirement is met.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}
