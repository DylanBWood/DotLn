# WO-151 VER-002 — Entropy Reducer dispatch

**Verdict: pass.** All nine acceptance criteria are met on the repaired
subject. VER-001's three major findings are closed by executable evidence: a
single-pointer guard now preserves pending review and refutation episodes,
authority revision 002 binds the current generated bundle and restores the
document gate, and REFUTATION-003 records the live refuter's post-episode
tracked status and frozen-copy inventory. No new substantive defect was found.

**Subject:**
[`docs/work-orders/WO-151-entropy-reducer-dispatch.md`](../../work-orders/WO-151-entropy-reducer-dispatch.md)
on branch `wo-151`, with repository HEAD `5b4b99cab19e` and the verification
dispatch's staged subject at `refs/dotln/checkpoint/WO-151/7`
(`9a813de38e70dc62c7e35b2c65e5a6bf6d517045`). This verifier changed no
implementation, product document or prior evidence. It added only this
allocated immutable report before recording the result.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-5.6-sol","effort":"xhigh","source":"codex-session-readback"}

Human actor: the operator dispatched `resume: verify` in an attended Codex
session. Canonical status and the dispatch briefing supplied the actor values
above. No subagent was launched: the observed count is 0 against the configured
cap of 20, with 20 remaining and an unknown unobserved remainder. This session
did not run through a transport with a hard provider-dollar cap, so a USD 5 cap
is unenforced rather than claimed.

**Process cost:** entry 32935 tokens; handoff 2463553 tokens; source codex-transcript-counter

Entry was observed at 2026-09-22T15:04:36.166Z and handoff at
2026-09-22T15:14:42.996Z, scope `dispatch`. Dollars are unavailable rather
than zero. The product gate took 276.59 s. The 2.46 s focused fixture and
27.98 s document gate were cheaper but not equivalent: the focused fixture
reproduced the repaired branches, the document gate exercised the repaired
authority preflight and entropy row, and criterion 9 independently required
the product gate.

## Goal-aligned judgment

The useful outcome is a dependable, externally consumable review interface
that replaces five manual host steps while preserving authority, evidence,
privacy and recoverability. The current subject achieves that outcome through
a command family, typed contracts, immutable receipts and a planning handoff;
the repair closes recovery and evidence gaps without adding a role, scheduler
or concurrency layer. NoOp would leave a paid episode strandable, the current
authority bundle unbound and a command-capable refuter without an after-state
witness, so it loses to the bounded repair.

The eight system traps support the pass rather than merely the activity count.
Policy resistance is checked by binding the final generated authority bytes.
Tragedy of the commons and drift to low performance are checked by the
single-pending guard, consume-before-produce routing, recorded live costs and
full gates. Escalation is bounded to one repair cycle and one necessary repeat
refutation, not a new workflow. Success-to-the-successful is checked by
retaining both transports and the manual fallback. Shifting the burden is
reduced because recovery no longer depends on finding an orphaned scratch
copy. Rule beating is checked by recording both episode inventories rather
than treating a successful worker return as confinement evidence. Seeking the
wrong goal is checked by judging recoverable, readable evidence rather than
receipt count. Naive Interventionism favors the implemented small probes and
guards: refuse a second pointer, observe the already-required after-state and
mint the edition only after bytes settle.

## Instrument disclosure

Several subject instruments also checked or recorded this verification:

- `scripts/resume.mjs` admitted the dispatch, allocated this report and will
  record its verdict.
- `scripts/harness.mjs` supplied procedure, writer state and usage counters;
  those counters support no product finding.
- `scripts/test-runner.mjs` selected both the document and product gates.
- `scripts/lib/entropy-review.mjs` implements the behavior under judgment and
  implements `entropy check`; source inspection, focused fixtures, live receipt
  bytes and the independent document/product gates therefore supplement its
  own `status: ok` result.
- REVIEW-002 and REFUTATION-003 are external model episodes under verification;
  their successful returns do not certify the host that launched them.

## VER-001 finding closure

### Finding 1 — pending explicit review could be orphaned. Closed.

**Expected:** A new dispatch cannot replace the only pointer while an episode
of that kind is pending, and refusal occurs before another frozen copy is made.

**Observed:** `requireNoPending()` runs before `freezeSubject()` for both
review and refutation and names the open episode, subject, copy and recovery
commands. The focused fixture changes tracked status between two explicit
reviews of the same commit, observes refusal, proves the first pointer and copy
remain addressable, proves no second copy or pending record appears, and then
proves `discard` releases that first copy.

**Reproduction and evidence:** `node scripts/test-entropy-review.mjs
--fixtures-only`, case `a dirty tree and a second pending dispatch are refused
by path`; 16/16 focused cases passed. Source:
`scripts/lib/entropy-review.mjs:304-311,899,1259` and
`scripts/test-entropy-review.mjs:364-426`.

**Severity and limits:** VER-001's major recovery defect is closed. The host
still intentionally supports one pending dispatch per kind, not a concurrent
addressable set; D015 records that smaller design and its reopening condition.

### Finding 2 — authority edition stale and document gate blocked. Closed.

**Expected:** The selected immutable authority edition describes the current
generated bundle, and the entropy document row runs behind a passing preflight.

**Observed:** `docs/evidence/current.json` selects WO-151 revision 002.
`authority-evidence --check` passes 34 bundle comparisons. `npm run test:docs`
passes 20/20 fresh tasks and includes `PASS entropy`; publication, feedback,
artifact identity and generated-harness checks also pass.

**Reproduction and evidence:** `node scripts/authority-evidence.mjs --check`;
`npm run test:docs`; `node scripts/feedback-evidence.mjs --check`; `node
scripts/artifact-identity-evidence.mjs --check`; `node scripts/harness.mjs
check`; `npm run publication:check`. The selected edition is
`docs/evidence/WO-151/authority/002/` and D018 records why revision 001 remains
immutable.

**Severity and limits:** VER-001's major identity-evidence defect is closed.
The checks establish byte identity and current projections, not a causal claim
that generated policy is behaviorally effective.

### Finding 3 — live refutation lacked post-episode confinement evidence. Closed.

**Expected:** A live refutation records source tracked status and frozen-copy
inventory before and after the episode, renders that evidence, and preserves
older immutable receipts.

**Observed:** The filing path recomputes tracked status and scratch inventory,
records both hashes and counts plus the delta, and conditionally renders the
new line only for receipts with that shape. The fixture adds a scratch path and
observes a one-path delta, exercises working-tree drift, and renders a legacy
shape. REFUTATION-003 records identical source hashes, a frozen inventory of
2,971 paths before and 3,811 after (delta 840), zero denied tools and four
independently attempted survivors. `entropy check` binds REVIEW-002,
REFUTATION-002 and REFUTATION-003 while preserving the two pre-mechanism pairs.

**Reproduction and evidence:** focused case `the refutation records tracked
status and the scratch delta on either side of its episode`; `npm run entropy
-- check`; `docs/instance/entropy-reducer/runs/REFUTATION-003.{json,md}`;
source at `scripts/lib/entropy-review.mjs:1232-1496`. `npm run entropy --
subject` selects REVIEW-002 with REFUTATION-003 and all four survivors.

**Severity and limits:** VER-001's major missing-witness defect is closed. The
receipt truthfully states that Claude file tools are restricted but Bash is
instructed rather than path-confined; the before/after observations witness
what moved, not a proof that escape was impossible.

## Criterion-by-criterion

### Criterion 1 — freeze, compile and retain one recoverable dispatch. Met.

The default route refuses a dirty tree, the explicit route binds a named
commit, the compiled episode is finite and carries the residue, lens briefs,
concern, frozen path and schema, and the repaired early guard prevents pointer
replacement for both dispatch kinds. The focused suite covers each refusal and
the canonical prompt.

### Criterion 2 — actor attestation. Met.

The focused fixture covers pinned, substitute, background and operator-attested
shapes without inventing effective readback. REVIEW-002 and REFUTATION-003
record launched Claude Code 2.1.278, `claude-fable-5-1` at `max`, source
`command-line-readback-and-invocation`, effective values unknown and identity
`entropy-reducer@1`.

### Criterion 3 — review validation and immutable filing. Met.

The host validates the exact WorkOrder and episode, retains rejected returns,
files the next numbered immutable pair and binds both hashes in the control
log. The default route refuses tracked drift. The explicit route follows the
recorded D009 adaptation: both repositories must resolve the same committed
tree and working-tree drift is recorded rather than hidden.

### Criterion 4 — blinded refutation and report binding. Met.

REFUTATION-003 carries only the four selected `{ findingId, command }`
subjects, with no reviewer conclusion, severity argument, packet or target
survival count. The bound report has measured denominator 4, by-inspection
`not-applicable`, four attempts and no refuted, blocked or unselected finding.
The zero-denominator and adverse-result branches pass in the fixture.

### Criterion 5 — disposition and planning handoff. Met.

The fixture accepts a survivor into the formal planning heading, files an
accepted packet, and refuses early, refuted, blocked and unknown acceptances.
The live `entropy subject` result is `consume` for REVIEW-002 with
REFUTATION-003; it does not buy a duplicate episode. Live dispositions remain
the operator's next planning act, as the criterion permits.

### Criterion 6 — immutable check and executable fixture. Met.

All 16 focused cases pass. They include JSON/Markdown edits, missing events,
fixture liveness, interrupted filing, the changed-status pointer regression,
the repaired after-state and the legacy receipt shape. `entropy check` returns
`status: ok`, and `npm run test:docs` executes `PASS entropy` within 20/20
passing rows.

### Criterion 7 — live pinned review and refutation evidence. Met.

REVIEW-002 is the pinned live review. Its explicit-commit binding and truthful
tracked drift are accepted under D009, as VER-001 already judged. REFUTATION-003
is the pinned live refutation at the repaired shape: 458.428 s, 21 turns, USD
2.8988545 list cost, four blinded attempts, byte-identical tracked status, an
840-path inventoried scratch delta and no denied tools. The repeated refutation
is necessary because REFUTATION-002 is immutable and its removed scratch copy
cannot supply an after-state retroactively.

### Criterion 8 — write-backs and current editions. Met.

The operator guide retains the five manual steps and describes the command,
single-pointer recovery and both episode inventories. Product 03 and 05,
PLAYBOOK, the planner procedure and the security document name the route and
substitute policy. Authority revision 002, feedback revision 001, both current
publication locks and the live self-host evidence pass their checks. The
document gate reports the entropy row against 20 fresh document tasks.

### Criterion 9 — product gate, whitespace, dependencies and identity. Met.

- `npm test`: **25 passed, 0 failed, 276.59 s, 69 fresh tasks**.
- `npm run test:docs`: **20 passed, 0 failed, 28.12 s, 20 fresh tasks**.
- Focused entropy fixtures: **16 passed, 0 failed, 2.46 s**.
- `git diff --check`: clean.
- Artifact identity, authority, feedback, harness and publication checks: pass.
- `packages/skeleton/src/loadouts/entropy-reducer.ts` and generated
  `RESIDUE.md`: unchanged from HEAD.
- No external dependency was added; package/lock changes add the root command,
  advance internal skeleton/console versions and update the console's exact
  workspace pin.
- No added lint, type, formatter or similar suppression directive appears in
  the source diff.

## Findings and limits

No new finding. D017 already records the one met-and-not-fixed observation:
both live refuters returned their typed attempts payload as the public
statement, so that field adds no evidence beyond the bound report. Its named
low-priority follow-up may tighten the instruction or remove the redundant
field in a future order; it does not violate this order's closed result schema,
blinding, receipt binding or acceptance criteria.

The live worker episodes are evidence inputs, not independent certification of
the dispatch host. Effective worker model and effort remain unknown. Dollar
figures are list-cost observations from the worker result envelope, while this
verifier's own dollar cost is unavailable. This report authorizes no commit,
publication, release or operator disposition. The next legal workflow on pass
is `resume: final review`.

## Reproduction

From `/Users/dylanwood/Projects/DotLn-wo151` on branch `wo-151`:

```text
npm run build --silent
node scripts/test-entropy-review.mjs --fixtures-only
npm run entropy -- check
npm run entropy -- subject
node scripts/authority-evidence.mjs --check
node scripts/artifact-identity-evidence.mjs --check
node scripts/feedback-evidence.mjs --check
node scripts/harness.mjs check
npm run publication:check
npm run test:docs
npm test
git diff --check
```
