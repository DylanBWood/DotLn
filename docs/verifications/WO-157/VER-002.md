# WO-157 VER-002 — independent verification: pass

**Subject:** The selected `wo-157` worktree after `resume: fix`, at the repaired checkpoint named by the `VerificationRequested` event for VER-002. This verdict judges the current uncommitted branch against [WO-157](../../work-orders/WO-157-closeout-followups.md), including its operator-authorized item 16 and criterion 20. It preserves [VER-001](VER-001.md) and judges its four findings again; final-review integration is a later, separate subject.

**Verifier:** Codex CLI 0.156.1, `gpt-6-sol`, effort `xhigh`, from the selected-session readback printed by this dispatch. One verifier owned the worktree and report. No subagents were admitted (0 of the budget of 20); the harness observed 0 attributable admissions, with unobserved coverage unknown. No hard USD 5 provider cap was exposed or enforced for this Codex session.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.156.1","model":"gpt-6-sol","effort":"xhigh","source":"codex-session-readback"}

**Verdict:** **Pass.** All twenty acceptance criteria have supporting evidence on this branch. There is no new implementation finding. Criterion 16's integrated-tree checks remain the final reviewer's obligation under product 07 §Independent workflows and integration; this report makes no claim about an integration that has not occurred.

## Executed checks and retained evidence

- `npm test` outside the harness sandbox: **27 suites passed, 0 failed, 71 fresh tasks, 284.45 s** at code identity `0c56e48bc34921e366489fa2caa639c1285eb4fa843baa3ad22bbc98f36657a1`. Its recorded gate row lists the integration, target-publication, resident-binding, portfolio, skeleton, derived-order and other product suites. Listing the system temporary directory immediately afterward found **zero** `dotln-gate-sandbox-*` roots. `git diff --check` passed.
- Explicit suites outside that `npm test` selection passed on the current tree: `node --test scripts/test-entropy-review.mjs` (**19/19**), `node --test scripts/test-evidence-sources.mjs` (**3/3**) and `node --test scripts/test-runner.test.mjs` (**40/40**). The import suite ran a real feedback edition check in a temporary repository copy for the moved protocol, excluded import and both VER-001 bypass forms. The runner suite exercised the tagged abandoned-root refusal and deterministic teardown.
- Current edition checks passed: `node scripts/authority-evidence.mjs --check`; `npm run evidence:artifact -- --check` (four current files in `artifact-identity/001`); `npm run evidence:verification -- --check` (four synthetic files); `node scripts/feedback-evidence.mjs --check` (ten passing regressions, ten removal failures, 1,192 fewer instruction bytes). The [current pointer](../../evidence/current.json) selects authority revision 002, artifact identity revision 001, verification revision 001 and feedback revision 002, all under WO-157. `node scripts/harness.mjs check --loadout contributor` passed on 31 generated surfaces; `node scripts/harness-context.mjs --check` found every set role ceiling within budget.
- `npm run plan -- check` passed and recognized the authorized WO-157 execution amendment ([D030](../../evidence/WO-157/decisions.md#wo-157-d030)); `npm run publication:check` passed; `node scripts/check-registrations.mjs` classified 317 JSONL paths (241 EventEnvelope streams, 76 classified) and found all 21 document-gate task stubs.
- With this report present, `npm run test:docs` passed **21 suites, 0 failures, 21 fresh tasks, 30.52 s**, including format, lineage, registrations, planning, edition and receipt checks.
- The executor's retained repair review gate, run after the last registered-source edit and re-mint, passed **41 suites, 0 failed, 85 fresh tasks, 525.16 s** ([implementation record](../../evidence/WO-157/implementation.md#repair-after-ver-001-resume-fix-2026-09-23-to-2026-09-24)). The retained [repair fail-before sweep](../../evidence/WO-157/fixtures/repair-fail-before-sweep.txt) fails on the assertions for F1–F4 and item 16 against the VER-001 subject. The [repair after transcript](../../evidence/WO-157/fixtures/repair-after.tap) and [post-mint edition fixture](../../evidence/WO-157/fixtures/repair-item-12-after.tap) pass. The first implementation's item-by-item before/after transcripts remain in `docs/evidence/WO-157/fixtures/` and were reviewed with the source and current gate, rather than treated as current execution by themselves.
- The retained feedback revision 002 verifier stream has one `WorkerAttemptStarted`, one `WorkerCompleted`, 169 `WorkerHeartbeat` events and no `WorkerInterrupted` event. Its `selfhost-verification.jsonl` is 2,141,113 bytes. Revision 001 remains retained as the two-attempt subject VER-001 judged. The [D036 record](../../evidence/WO-157/decisions.md#wo-157-d036) names the host-selected `claude-cli-print` model and effort and the separate usage source; effective model and effort were not observed.

## VER-001 findings reproduced against the repair

| Finding and severity at VER-001 | Expected and observed now | Reproduction and evidence |
| --- | --- | --- |
| F1, minor diagnostic defect; criterion 3 | The file-to-directory refusal names both removed path and configured ceiling. The current source reports `3 paths against the envelope's files ceiling of 3: D fixture.txt`; the over-ceiling and undeclared Sort refusals still hold. | `packages/skeleton/test/source-change-host.test.ts` runs the writer against a scratch Git target and asserts each refusal; the source's `checkChangeLimits` reports the count and ceiling. The focused fixture failed before and passed after; the current skeleton suite passed. [D031](../../evidence/WO-157/decisions.md#wo-157-d031). |
| F2, minor witness fidelity defect; criterion 9 | Distinct legal untracked path lists, including newline-bearing and leading-space names, now record distinct SHA-256 values and `identical: false`; the rendered witness says false. Scratch added/removed/resized path sets use the same unambiguous NUL framing. | `scripts/test-entropy-review.mjs` creates disposable Git repositories with both colliding filename pairs and files a receipt. `untrackedListing` reads untrimmed NUL-delimited Git output, and `pathListDigest` terminates each path with NUL. The focused fixture failed before and passed after; the current entropy suite passed. [D032](../../evidence/WO-157/decisions.md#wo-157-d032). |
| F3, medium attestation integrity defect; criterion 11 | With `CLAUDE_EFFORT=xhigh`, a `claude-code` lifecycle actor must record `xhigh` from `claude-session-readback`. A differing effort, equal effort from another source, or `unknown` is refused before an event is appended. Other harnesses remain independently attestable. | `scripts/test-resume.sh` exercises `parseActor` and the lifecycle refusal without an added event; `scripts/resume.mjs` enforces the same rule. The prior `high/operator-attested` case failed before and passed after the new assertion. The current resume suite passed. [D033](../../evidence/WO-157/decisions.md#wo-157-d033). |
| F4, medium evidence registration defect; criterion 12 | The edition import closure now names an unregistered sibling reached by chained dynamic import or semicolonless static import, as well as the original static form. An excluded import remains admitted, and moving `entropy-review-protocol.ts` stales feedback evidence. | `scripts/test-evidence-sources.mjs` checks both forms through a real `feedback-evidence --check` in a temporary repository copy and checks the token reader's runtime/type-only cases. The focused fixture failed before and passed after the re-mint; the current feedback edition check passed. [D034](../../evidence/WO-157/decisions.md#wo-157-d034). |

## Acceptance matrix

| Criterion | Judgment and current evidence |
| --- | --- |
| 1 — intent-to-add integration | **Pass.** The scratch Git fixture compares HEAD, index, refs, stash and receipt before and after refusal, then checks a fresh run after a pre-stash failure. The helper and product 07 integration rule agree. |
| 2 — deferred queue target | **Pass.** `retarget` changes only a deferred target to an existing FUP in final review; invalid target, missing FUP and extra field refuse. The generated executor sentence and 31-surface harness check agree. |
| 3 — file ceiling and deletion | **Pass.** F1 repair above; in-ceiling Standardize and declared Sort still pass; the `portfolio.ts` comment names the enforced ceiling. |
| 4 — target hooks | **Pass.** Both writer-profile probes and their limits are in the [probe record](../../evidence/WO-157/writer-profile-probe.md). The scratch target fixture proves that ordinary push runs its planted hook and guarded publication succeeds without the hook or target configuration running. |
| 5 — grant-bearing identity | **Pass.** A registry-bearing identity equips and reaches `SourceChangeHost`; a malformed registry refuses. Product 02 §Artifact identity v1 names the optional validated field. |
| 6 — registered binding profile | **Pass.** A hand-written mismatch refuses with repository and profile named; a loader-built binding passes in `scripts/test-resident-bind.mjs`. |
| 7 — default model and effort | **Pass.** The resident-binding fixture covers each transport default and explicit operator flags with `modelSource`/`effortSource`; product 06 states the shipped selections and reopening condition. This judges the configured values, not current external model availability. |
| 8 — typed refusal detail | **Pass.** Skeleton verification fixtures assert the closed detail vocabulary in `WorkerInterrupted` and CLI output, with raw model text excluded and unknown detail mapped to `unclassified`. |
| 9 — confinement witness | **Pass.** F2 repair above; the ordinary fixture also adds and removes paths and checks the rendered observation. |
| 10 — refutation statement | **Pass.** The entropy fixture files a refutation without a separate statement and renders typed attempt reasons; earlier receipt rendering remains valid. |
| 11 — Claude selected effort | **Pass.** F3 repair above; discovery and generated role text name the selected-session readback. |
| 12 — import graph | **Pass.** F4 repair above; all selected edition checks pass. The scanner's stated limit for ambiguous slash lexing and computed specifiers is recorded in D034; no current registered import was omitted by the old and new reader comparison. |
| 13 — document registration | **Pass.** The registration fixture plants an unregistered JSONL and removes a suite stub; the current check reports all 317 paths and 21 stubs classified. |
| 14 — allocation fold | **Pass.** The derived-order fixture folds a superseded section set and isolates corrupt authority to its order; the WO-113 map note is present. |
| 15 — sandbox teardown | **Pass.** The retained race diagnosis records 172/200 failures before and 0/200 after `maintenance.auto=false`; the planted tagged-root fixture fails the gate by name. This verifier's `npm test` passed and left no root. |
| 16 — edition re-mint | **Pass on branch.** D036 selects new immutable revisions after the repair's last registered-source edit; the live feedback episode completed under the tightened schema; current edition and harness checks pass. Integrated-tree checks belong to final review. |
| 17 — before/after and review gate | **Pass.** Original and repair fail-before transcripts, after transcripts and the executor's post-repair 41-suite review gate support the fixture claims. Changed existing expectations are named in D033 and D035. |
| 18 — write-backs | **Pass.** Products 02, 06 and 07, the closeout map/register, decisions and index carry the routes and reopening conditions. Publication and planning checks passed. |
| 19 — product gate and budget | **Pass.** Fresh `npm test`, `git diff --check` and context checks pass. The lockfile changes only package versions and the exact skeleton pin; no dependency was added. |
| 20 — admissible verifier schema | **Pass.** `verification.test.ts` checks twelve refused finding mutations against both schema and admission, plus a valid finding. With no failing witness the emitted schema has `findings.maxItems: 0` and no `fail` verdict; the live revision 002 episode completed on its first attempt under that schema. [D035](../../evidence/WO-157/decisions.md#wo-157-d035) names the control-character, duplicate and cross-field rules left to admission, and the untested Codex structured-output limit. |

## Judgment and limits

This order advances the critical path's independently verifiable source-to-deliverable loop: the repaired checks now expose the refused action's reason, bind witnesses without ambiguous path framing, refuse a false session effort and stale evidence on the reproduced imports. NoOp would leave those four observed failures and the verifier-schema gap for another operator pass. Rule beating and drift to low performance would be treating green suites alone as evidence for diagnostics and witness truth; the focused fail-before reproductions and source review address that risk. Policy resistance, escalation, tragedy of the commons and shifting the burden favor the bounded repair and single post-repair re-mint over further retry loops. Success to the successful does not privilege the earlier import pattern over its demonstrated bypass. Seeking the wrong goal would mark integration done before it occurs. Under Naive Interventionism, the retained first editions, historical receipts, Sort move and operator-authorized larger sandbox deferral remain explicit; the selected branch passes their existing checks. No additional repair is justified by the evidence reviewed here.

The writer-controlled test-code OS sandbox remains the separately boarded `FUP-92fd86e53b44fa39` under the operator's [D030](../../evidence/WO-157/decisions.md#wo-157-d030) scope choice. The `claude-cli-print` live episode establishes acceptance of the tightened schema there; Codex structured-output acceptance was not established by a live model episode. These limits do not change this order's acceptance verdict. The verifier made no implementation or product-document edit.

**Same-day correction:** The first report draft misread the 27-suite `npm test` total as including entropy and registration checks. The recorded gate row lists neither. This report names only its actual selected suites, and records the separately executed entropy, import-closure and registration checks above. The product-gate result and verdict did not change.

**Process cost:** entry 50,559 tokens; handoff 7,966,764 tokens; source codex-transcript-counter

The entry and handoff readings have `scope: dispatch`; the entry cutoff was 2026-09-24T14:26:14Z and the latest available handoff cutoff was 2026-09-24T14:36:53Z. USD cost is unavailable from this counter.
