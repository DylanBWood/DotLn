# WO-054 VER-003 — Independent verification of the VER-002 repair

**Verdict: pass.** VER-002's one finding, N1, is resolved by the documented
route, and nothing else in the subject changed.

- Both breached cold-start ceilings rise by one 4 KB step:
  - executor: 20,480 → 24,576;
  - release-close: 12,288 → 16,384.
- Each rise has a dated acceptance naming the rule that caused the growth. The
  rise follows the same convention as the three earlier cold-start acceptances.
- I measured both roles independently: executor 20,849 bytes and release-close
  12,437 bytes, identical in both skill roots. `harness-context --check` now
  reports every capped role within its ceiling.
- Since VER-002's subject, no non-document file changed, including untracked
  sources and generated hooks.
- Acceptance criteria 1–6 and the scope-expansion duties hold as VER-002
  recorded them.

There are no findings. Three observations (O7–O9) are recorded for planning.

This session also ran an operator `ideation:` breakout in the middle of the
verification, and I authored its documents. They are outside this verdict (see
"Ideation breakout in this session").

Verified by a Claude Code session. The session hook recorded dispatch
`resume: verify` and allocated this path. Its `VerificationRequested` row is
dated 2026-09-18T20:40:18.223Z.

- **Harness version.** 2.1.277, the output of `claude --version` in this
  session.
- **Model.** `claude-opus-5[1m]`, the identifier the harness supplied in this
  session's system context.
- **Effort.** `xhigh`, from `CLAUDE_EFFORT=xhigh` in this session's shell. That
  is the host-supplied selection, not effective-session readback, so the source
  is `operator-attested`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.277","model":"claude-opus-5[1m]","effort":"xhigh","source":"operator-attested"}

## Subject

- **Order.**
  [WO-054](../../work-orders/WO-054-verification-over-real-worktree.md), with
  its 2026-09-18 operator scope expansion and `Execution record`
  clarification. The order file has not changed since VER-002, which bound
  its bytes to the `WO-054-D005` `PlanExecutionAmended` row.
  `npm run plan -- check` exits 0.
- **Subject.** The `wo-054` worktree over `main` at `ce86fac6`, uncommitted,
  with a temporary-index tree of `8b375634` before the ideation breakout.
  - That tree differs from `refs/dotln/checkpoint/WO-054/11` (the verify
    transition) only in `docs/control/current.md` and the appended
    `VerificationRequested` row.
  - The repair delta, checkpoint 8 to checkpoint 10 (`RepairCompleted`), is
    documents only: `docs/control/budgets.json`, decision `WO-054-D006` with
    its index and follow-up rows, `repair-002.md`, one product 07 paragraph,
    the software-engineer source lock, the `PR.md` cost table and lifecycle
    projections.
  - `git diff --name-only refs/dotln/checkpoint/WO-054/7 8b375634 -- . ':!docs'`
    is empty. Checkpoint 7 is VER-002's verify transition. Its tree contains
    the untracked sources and generated surfaces: six paths matching
    `verification-worktree.ts`, `codex-continuation.mjs` or `.codex/` are
    present in it.
- **Earlier findings.** VER-002 N1, observations O5 and O6, and the VER-001
  dispositions that VER-002 checked. The repair record is
  [repair-002](../../evidence/WO-054/repair-002.md).

**Instrument disclosure.** Instruments from earlier orders observed this
verification:

- The generated Claude hooks reserved this session's writer (`6073b3d5…`) and
  wrote its session record.
- `harness usage` supplied the counts below.
- I reused the gate-evidence machinery's recorded `npm test` row; see
  "Product gate".
- `harness-context` and `meta` measured the cold-start budgets this repair
  changed. I cross-checked their byte counts with `wc -c`.

The Codex continuation adapter, which is under verification in this order, did
not observe this session.

**Fan-out.** Stated before any spawn: none. The repair delta is one budgets
file and documents. Budget: 20. Actual: 0. At 20:51:05Z, `harness usage`
reported `count 0, remaining 20, exact-observed`, with the uncounted
remainder unknown.

## Checks run by the verifier

| Check | Result |
| --- | --- |
| `node scripts/harness-context.mjs --check` | exit 0 with no breach advisory. Both roots: executor 20,849 ≤ 24,576; verifier 17,891 ≤ 20,480; reviewer 19,109 ≤ 20,480; release-close 12,437 ≤ 16,384; planner 13,509 ≤ 24,576; refuter 14,166, unset. |
| Independent `wc -c` of `CLAUDE.md` (5,474) plus each role skill in `.claude/skills` and `.agents/skills` | Identical to the tool in both roots; the executor skill files are byte-identical (`cmp`). |
| `npm run meta -- --check` | exit 0. All `coldStartBytes` rows are `within`. One advisory remains: `fastGateMs=344065 exceeds 120000` (O8). |
| `node --test --test-name-pattern="budgets keep unselected caps unset" scripts/test-process-debt.mjs` | 1/1 pass, 0.84 s. It reads the real `budgets.json`. |
| `node scripts/test-plan-refutation.mjs --fixtures-only`, run directly (the other reader of the real `budgets.json`) | 41/41 pass, exit 0, 107.9 s (20:50:48Z–20:52:36Z). This includes the two cases that copy the real budgets: 40 ("WO-126 legacy cost adoption…") and 41 ("WO-126 cost input holds…"). Its fixture repositories left this worktree's projections untouched. |
| `node --test scripts/test-codex-continuation.mjs` | 9/9 pass |
| `node --test packages/skeleton/dist/test/verification-worktree.test.js` | 8/8 pass |
| `node docs/evidence/WO-054/fixture.mjs --check` | exit 0: "worker pass, host contract failure, failing matrix" |
| `node scripts/{artifact-identity,authority,verification,feedback}-evidence.mjs --check`, `node scripts/console-fixtures.mjs --check` | all exit 0 |
| `npm run plan -- check`, `npm run publication:check`, `node scripts/harness.mjs check` (31 surfaces), `node scripts/release.mjs check-surfaces --local` | all exit 0 at the subject |
| `node_modules/.bin/prettier --check . --ignore-unknown` | exit 0 |
| `git diff --check`, `git diff --cached --check`, and `--no-index --check` of each of the 74 untracked files | no output |

**Product gate.** I did not run `npm test`.

- The current `gateCodeIdentity` is `db1db435…`. It equals the latest passing
  gate row: 2026-09-18T20:12:22.591Z, exit 0, 344,065 ms.
- The row's recorded tree (`c9c61451`) is not in the object store, so I could
  not compare it directly.
- VER-002's observation O5 still applies: code identity does not cover
  untracked sources. I bound them two ways instead:
  - the checkpoint-7 tree comparison above;
  - modification times: every changed or untracked non-document file was last
    modified by 20:00:48Z, before the gate started at 20:06:38Z.
- The repair changed `docs/control/budgets.json` after the gate. Tests in two
  machinery suites read the real file: one `process-debt` case and two
  `plan-refutation` cases. I re-ran that `process-debt` case and the whole
  `plan-refutation` fixture suite directly (table above).

## Acceptance criteria

The code for AC1–AC4 is byte-identical to VER-002's subject. In this subject I
re-ran their focused suites and the fixture check. I rely on VER-001 and
VER-002 for the broader reproductions.

| # | Result at this subject |
| --- | --- |
| 1 | Pass. The focused suite passes 8/8; the code is unchanged. |
| 2 | Pass. The focused suite passes; the code is unchanged. |
| 3 | Pass. The fixture check exits 0. |
| 4 | Pass. No WO-010 file or verification source changed. |
| 5 | Pass. The write-backs VER-002 found are present. Product 07 adds the dated ceiling paragraph, and D006 has its index row. |
| 6 | Pass. The gate is bound as above, the diff checks are clean, `package-lock.json` is unchanged since VER-001, the bundle check passes, and the editions check green. N1, which VER-002 raised against this criterion's bundle, is resolved. |

The scope-expansion duties are unchanged since VER-002, and so is the adapter
code. The continuation suite passes 9/9.

## Earlier findings

### N1 — resolved

- **Expected, from VER-002.** For each of `coldStartBytes.executor` and
  `coldStartBytes.release-close`, either raise the ceiling in this change
  under the documented route with the rule named, or record a dated
  acceptance. Do not trim rules, and leave no breach advisory.
- **Observed.** `docs/control/budgets.json` raises executor to 24,576 and
  release-close to 16,384. It adds two dated 2026-09-18 acceptances naming
  "the shared advisory-boundary and Codex compaction-continuation rule", the
  measured bytes and decision D006. The rule text and runtime are unchanged:
  no generated role file differs from checkpoint 7. `harness-context --check`
  and `meta` report both metrics `within`.
- **Convention.** Each earlier cold-start acceptance also rose from the
  previous ceiling by exactly 4,096 bytes:
  - reviewer: 16,384 → 20,480;
  - release-close: 8,192 → 12,288;
  - verifier: 16,384 → 20,480, under WO-139.

  This repair does the same. See O7 for the wording.
- **Authority.** D006 cites an operator direction during `resume: fix` to
  increment now and leave efficiency for a later pass. I did not observe that
  message; the repair record reports it. The documented route admits a raise
  without further operator choice, so the verdict does not depend on it.
- **Limits.** None beyond the gate binding above.

### VER-002 observations

- **O5 (code identity and untracked sources).** Handled above by tree
  comparison and timestamps. It remains an earlier order's instrument,
  outside this order.
- **O6 (same-turn typed stop).** Unchanged; operator-assigned, not a finding.

## Observations (not findings)

- **O7 — the step wording has two readings.** The `source` string in
  `budgets.json` says to raise a ceiling "to the measured bytes plus one 4 KB
  step". Read literally, that gives 24,945 and 16,533. Product 07 says "by one
  4 KB step above the measured bytes". All four acceptances, including this
  repair's, use the previous ceiling plus 4 KB, which also exceeds the
  measured bytes. The repair follows precedent. The planning pass may want one
  reading written down.
- **O8 — the gate-time advisory is not new.** `meta` reports
  `WO-054/fastGateMs 344,065 > 120,000`. The same metric was reported for
  WO-050 at 715,892 in its FINAL-001. Product 07's same-change route covers
  cold-start ceilings, not this metric, and repair-002 discloses it as a
  separate efficiency observation.
- **O9 — no new cost-table effect.** A future planning refutation compares
  `docs/planning/cost-table.json` acceptances with `budgets.json`. The table
  already differs from `main`, which has 5 acceptances to the table's 4 after
  WO-139, so this change adds no new staleness. The next planning pass
  re-measures it by design.

## Ideation breakout in this session

At 20:44Z the operator sent an `ideation:` message about an out-of-project
write that this session caused.

- **The write.** One of my check commands carried a stray stderr redirect to
  `$PWD/../.x`. It created a 75-byte file in the directory above the worktree.
  I removed it at once. It held `meta`'s advisory output.
- **The intake.** I captured the message in main's ignored intake. The
  synthesis is in [the breakout receipt](../../evidence/WO-054/ideation-2026-09-18.md),
  the ledger entry, a dated paragraph in product 03 §Candidate — DotLn-owned
  authority, and correction [WO-054-D007](../../evidence/WO-054/decisions.md#wo-054-d007).
- **My failure in handling it.** I first investigated sandbox settings, which
  nobody requested. I then reported a capture-only intake as sufficient. The
  operator had to correct me three times to get the documented default. D007
  records this as a failure under shifting the burden to the intervenor.
- **Effect on the subject.** I wrote these documents after defining the
  subject (tree `8b375634`), and I refreshed both publication source locks for
  the 03 paragraph. This report does not judge them. Final review must inspect
  the receipt, the ledger entry, the 03 paragraph and D007 as the receipt
  directs.
- **Checks after the breakout.** I regenerated the follow-up register and
  decision index with `npm run meta`. `publication:check`, `plan check` and
  `meta --check` then exit 0. Full Prettier and `git diff --check` are clean.

## Limits

- **Codex.** I did not run Codex. The native evidence is as VER-002 recorded
  it.
- **Product gate.** Reused, not re-run. The binding for untracked files uses
  the checkpoint-7 tree comparison and modification times, not a
  cryptographic gate identity.
- **Machinery suites.** Of these, I re-ran only the `process-debt` budget case
  and the `plan-refutation` fixtures.
- **Operator authority.** The operator direction behind D006 is reported by
  the repair record; I did not observe it.

## Goal alignment at handoff

- **Outcome.** The order's outcome still holds at fixture level. Host-run
  tests reach the matrix independently, and a contract failure survives worker
  success. The Codex continuation restores unfinished work after compaction
  in its suite and in the recorded native probe, which ran in a scratch
  repository.
- **The repair.** It closed the one procedural gap VER-002 found. The breach
  was settled through the recorded route, without trimming rules, so
  **policy resistance** and **rule beating** are answered.
- **Drift to low performance.** The ceilings grew and no efficiency gain is
  claimed. The operator reserved efficiency for a later pass, which D006's
  reopening condition tracks.
- **Seeking the wrong goal.** Absent: the fix raised limits to fit reviewed
  rules rather than editing rules to fit limits.
- **NoOp.** It would have carried the advisory into the next order.

This session's own handling of the ideation breakout is the clearest trap
instance here: **shifting the burden to the intervenor**, recorded in D007.
The stray write it concerned was also mine.

## Process cost

- **Tokens.** At 20:54:27Z: 11,605,136 total tokens, of which 11,379,047 were
  cached input, 165,389 cache writes and 60,520 output. Dispatch scope,
  `claude-transcript-message-usage`.
- **Activity.** 110 steps and 90 commands.
- **Unknown.** USD and reasoning tokens.
- **Subagents.** None.
- **Waiting.** Not re-running `npm test` avoided about 344 s of gate time. The
  two targeted budget-reader runs took about 109 s together: 0.84 s and
  107.9 s.
- **Ideation.** The breakout, including the three corrections, added steps
  that no verification check needed. The counts above include them.
