# WO-132 VER-005 — independent verification after repair 004

**Verdict: pass.** Repair 004 closes VER-004 F1 at the current source identity. The redirect parser treats shell-special output operands as opaque, and the generated live-gate hooks deny the reported `!` redirects into built gate inputs. The product gate passed at this identity in 252.17 s with 19 suites, 62 fresh tasks and zero reuse. No blocking acceptance defect remains in the tested subject.

Subject: [WO-132](../../work-orders/WO-132-machinery-stand-down.md), criteria 1–14, with its 2026-09-15 execution amendment superseding criterion 14's eight-hour cutoff. This `resume: verify` dispatch allocated `docs/verifications/WO-132/VER-005.md` from checkpoint `refs/dotln/checkpoint/WO-132/18` (`a23b46c0`) and created checkpoint 19 (`227cea26`). The branch is `wo-132`; no implementation file changed against checkpoint 18 during this verification. Code identity before and after the checks was `8e24bc8c482b8a8f02830b201ac6d2b02c53c1baeb14660decd304ed678e2a43`. No branch commit or publication was made.

Verifier: Codex CLI 0.154.0, observed with `codex --version`; model `gpt-6-astra` and effort `max` follow the repository's operator-attested default. Effective-session model and effort readback are unavailable. The verifier used three read-only analysis workers for independent runner, release and hook review; the root agent alone owned writes and held a registered live writer reservation before the product gate and report.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.154.0","model":"gpt-6-astra","effort":"max","source":"operator-attested"}

Instrument disclosure: the changed runner recorded the `npm test` success row, the changed writer guard registered this verifier, and the changed lifecycle allocated this report. I checked each through focused tests or independent fixture behavior rather than treating its own record as proof.

## Executed evidence

| Check | Observed result |
| --- | --- |
| `npm test` at the repaired code identity | 19 passed, 0 failed; 252.17 s; 62 fresh tasks, 0 reused. Gate row: `host-gate:8e24bc8c482b8a8f02830b201ac6d2b02c53c1baeb14660decd304ed678e2a43:npm test`, recorded 2026-09-15T16:31:50.165Z, exact tree `102da4ee47205846831d6b3d9cee95a629997d68`. |
| Focused `VER-004 F1` parser and generated live-gate hook tests | 2 passed, 0 failed in 54.97 s. The parser regression covers the reported shell-special operands; the generated fixture denies writes to gate inputs and the success record while admitting descriptor-only reads. |
| Goal-review and historical-disposition fixtures | 2 passed, 0 failed in 18.53 s. A constructed failure becomes a known issue; observed failure or vision contradiction can hold; accepted criterion text binds the disposition. |
| Wrapped-Cost and missing-measurement fixtures | 2 passed, 0 failed in 5.47 s. Complete removal text reaches the printed reconciliation; a measured shortfall becomes a planning input, and unavailable duration remains unknown. |
| `npm run test:docs` | 17 passed, 0 failed in 71.40 s, including generated bundle, authority, publication, format, meta, work-order index and document-sensitive checks. |
| Independent release fixture `composed_evidence` and code-identity fixture | Passed at current source. The lifecycle fixture has one gate run, distinct reviewed and merge trees, report edits after the gate, no gate row on main, and cleanup as best effort; the identity fixture keeps document/generated edits stable and moves on source or dependency edits. |
| Independent runner and main-writer fixtures | Runner classification/concurrent release cases passed 2/2; main reservation/build/bootstrap/history/release and second-writer/dead-holder fixture passed; delegated advisory/journal fixture passed. |
| `npm test -- --list`; checkpoint comparison; `git diff --check` | 19 product/lifecycle suites each printed `protects:`. Source files matched checkpoint 18; diff whitespace check was clean. |

The current 252.17 s gate is below the 360 s acceptance limit. The three consecutive fresh calibration runs in `docs/evidence/WO-132/product-gates.json` were 254.908, 250.500 and 246.160 s against the 605 s baseline; VER-001 independently measured 240.42, 243.16 and 243.62 s. Those series predate repair 004 and are retained at their own source identities, not relabeled as current runs.

## Acceptance judgment

| Criterion | Judgment and evidence |
| --- | --- |
| 1 | Met. The `resume` suite exercises four legal completions without gate/session prerequisites and illegal-phase refusal; the current completion path runs inline `git diff --check` and validates the report actor. |
| 2–3 | Met. Current product row has code and exact-tree identities; independent cross-process identity and full lifecycle release fixtures passed. Final review still owns the required single release gate. The next real order's one-gate control segment is a future observation, as the execution record states. |
| 4–5 | Met. The runner has no replica/sandbox modules or shared success cache, lists 19 protected product/lifecycle suites and 15 on-demand machinery suites, and retains document-sensitive checks in `test:docs`. Its release cases span lanes 0–3. |
| 6 | Met with the timing cutoff above. Three recorded fresh runs and this current run are below 360 s; the release cases use at least two lanes. |
| 7 | Met. The `resume` and worker fixtures exercise unrecorded versions and effort, `ultra`/`ultra code` normalization, retained raw spelling and Codex subagent mode without discovery or minimum-effort refusal. Required field presence remains a syntax check. |
| 8 | Met. The feedback v2 main-branch fixture and generated hook fixture admit one main writer for build, bootstrap, history and release, refuse a second live writer, and reclaim a dead holder. This verifier's reservation was observed live. |
| 9 | Met for the kept invariants. The current parser and generated-hook matrix close VER-002, VER-003 and VER-004 redirect admissions; delegated tools and advisories pass their fixtures. Claude/Codex role skills are byte-identical. Codex automatic project-hook enforcement remains unavailable, as the manifest records. |
| 10 | Met in the local lifecycle fixture: one reviewed row is consumed without a main gate, close excludes installation/suite/smoke, and cleanup follows publication as best effort. Fixture close took 2.612 s excluding transport. Live remote publication remains separately authorized and unobserved. |
| 11–12 | Met. The goal-review and reconciliation fixtures above reproduce the claimed result and advisory shortfall behavior. Historical planning receipts retain their identities. |
| 13 | Met. The cited product/playbook/security/refutation/publication write-backs and D001–D008 decision/correction records are present; the historical WO-126–WO-131 evidence is unchanged. Current product and document gates pass, diff check is clean, and no dependency was added. |
| 14 | Superseded by the operator's recorded execution amendment. Optional rollback commands remain recorded in D001 and were not executed. |

No new finding is filed. The prior root-level Markdown exclusion noted in VER-001 O1 and conservative refusal of opaque shell reads remain scope observations; neither was expanded by repair 004. The `changedMachinery` selector uses tracked Git diffs, so a newly created source file must be staged before final review to select its machinery suite. Live remote publication and the next real order's control history are outside this verification's observation cutoff.

The order's mission contribution is to restore the local-first platform's operator flow by replacing repeated transition gates with one reviewed product gate while retaining its writer and live-gate protections. NoOp would keep the recorded 605 s fresh gate and repeated refusals. Policy resistance, commons cost, drift and escalation are checked against the measured 252.17 s run and removal of recurring transitions; success to the successful is addressed by deleting invested replica machinery; shifting the burden is checked by executable release close; rule beating is checked by the redirect regression rather than aggregate green alone; seeking the wrong goal is checked against operator flow and product/lifecycle coverage. Naive Interventionism favors preserving the product suites and the two kept invariants. This verifier's optional full `npm test` added 252 s to verification; final review must still run its own gate, so that choice incurred extra work beyond the order's one-required-gate design.

Process-cost observation at 2026-09-15T16:33:53.858Z: 4,933,578 total tokens, source `codex-transcript-counter`, scope `dispatch`; 4,860,416 were cached input. Command count and dollar cost were unavailable. These are meter observations, not a completion requirement. Final counters remain in ignored receipts and the response.
