# WO-132 VER-003 — verification after redirect repair

**Verdict: fail.** The earlier findings are repaired. One new defect in that repair fails criterion 9: zsh append redirects to numeric or `-` filenames are treated as descriptor operations, so the generated hooks admit those writes during a live product gate. F1 below is the sole repair item; its practical reach is narrow.

Subject: [WO-132](../../work-orders/WO-132-machinery-stand-down.md), criteria 1–14 with the recorded execution amendment superseding criterion 14. Dispatch: `resume: verify`, allocated VER-003 from the ready-to-verify state after checkpoint 10. Code identity throughout verification: `dee83ba1012ce048ce7ec05cfb2834c744d5dbcaa71463b2067877396548a288`. The pre-report exact tree was `4d87346905ea52e7b88ebc030ecd9cb84564a2a1`. No implementation was changed by this verifier.

Verifier: Codex CLI 0.154.0, observed with `codex --version`; GPT-6 Astra/max follows the repository's operator-attested session default. Effective model/effort readback is unavailable. Two read-only workers independently inspected the repair and acceptance evidence; this verifier reproduced the finding directly.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.154.0","model":"gpt-6-astra","effort":"max","source":"operator-attested"}

Instrument disclosure: DotLn's changed lifecycle, identity, writer and usage machinery also recorded this work. Codex did not automatically enforce the generated hooks. Hook evidence below comes from explicitly invoked generated hooks in isolated temporary repositories. The root session was the sole registered writer; workers made no repository writes. Reservation liveness was reported as unavailable in this sandbox.

## Executed evidence

| Check run by this verifier | Observation |
| --- | --- |
| `npm run build` | Passed; all four packages built |
| `node --test --test-name-pattern='WO-132\|VER-001' scripts/test-process-debt.mjs scripts/test-harness.mjs` | 23 passed, 0 failed, 56.58 s; includes the new parser/live-gate tests, four completion actions, main writer behavior, delegation, document/source identity and wrapped Cost reconciliation |
| Current generated-hook reproduction | Six repaired pathname cases: 54 denials; five ordinary-read/descriptor controls: 45 admissions; seven numeric/dash append cases: 63 admissions, all incorrect for the protected fixture destinations |
| Checkpoint-9 adapter in otherwise-current fixture runtime, regenerated through that fixture's generator | The same seven cases: 63 denials. This isolates the repair's DENY → ADMIT regression |
| `/bin/zsh -f -c` in independent scratch repositories | `printf probe >>&1`, `printf probe >>&-`, and `printf probe >>& 1` changed their named files, exit 0; each destination was a gate input, but the adapter returned `[]` |
| `node scripts/harness.mjs check`; `node scripts/authority-evidence.mjs --check` | Passed: 24 generated surfaces; current authority revision validated |
| `npm run publication:check`; `node scripts/meta.mjs --check` | Passed; both editions current, cost reconciliation remains advisory |
| `npm test -- --list` | 19 product/lifecycle suites, each printing its `protects:` statement |
| `git diff --check`; `git diff --cached --check` | Clean |
| Source/directive and immutability comparisons | No new lint/type suppressions or dependency entries. Earlier VERs and repair evidence preserved; D006 appends the requested corrections |

The focused-test transcript is retained locally at `/tmp/wo132-ver003-targeted.log`. Direct hook reruns are `/tmp/wo132-ver003-hook-reproduction.log` and `/tmp/wo132-ver003-old-hook-reproduction.log`. Both finished successfully; the table above preserves their consequential results without depending on scratch retention.

## F1 — append operands are discarded as descriptors

**Severity: blocking acceptance defect, criterion 9; narrow practical exposure.** In `packages/skeleton/src/harness-command.ts:323`, `/^\d*>>?&(.*)$/` matches both `>&` and `>>&`. Line 334 skips a numeric or `-` operand for both. For zsh's `>>&`, those operands name files. The resulting empty destination array lets `activeGateWriteRefusal` at `packages/skeleton/src/harness-host.ts:2289` admit the command without classifying its actual destination.

Observed with zsh 5.9 on this host:

| Command | Checkpoint-9 adapter | Current adapter | Old hooks | Current hooks |
| --- | --- | --- | --- | --- |
| `echo marker >>&1` | `["&1"]` | `[]` | DENY | ADMIT |
| `echo marker >>&-` | `["&-"]` | `[]` | DENY | ADMIT |
| `echo marker >>& 1` | `["&"]` | `[]` | DENY | ADMIT |
| `echo marker >>& -` | `["&"]` | `[]` | DENY | ADMIT |
| `echo marker 1>>&1` | `["&1"]` | `[]` | DENY | ADMIT |
| `ls scripts >>&1` | `["&1"]` | `[]` | DENY | ADMIT |
| `head -1 fixture.ts >>&-` | `["&-"]` | `[]` | DENY | ADMIT |

Each hook row was reproduced across `permissions`, `concurrent-work-requires-worktrees` and `write-observer`, using `Bash.command`, `exec_command.command` and `exec_command.cmd`. The fixture tracked files named `1` and `-`; `gateInputPath` returned true for both. Its ordinary `>&` pathname controls were denied, while `2>&1`, `2>&-`, `ls`, `head` and a literal `grep` pipeline remained admitted.

Reproduction from the existing `scripts/test-harness.mjs` fixture:

1. Add tracked fixture files `1` and `-` containing `seed\n` before opening the fixture gate. Confirm `gateInputPath(root, name) === true`.
2. Call `shellWritePaths('echo marker >>&1')` and `shellWritePaths('echo marker >>&-')`: both incorrectly return `[]`.
3. Open `beginGateRun(root, 'npm test')` and pass those commands through each generated pre-tool hook using the fixture session. Expected: `permissionDecision: deny`. Observed: no denial on every surface above. Release the fixture gate in `finally`.
4. In scratch, execute `/bin/zsh -f -c 'echo marker >>&1'` and `/bin/zsh -f -c 'echo marker >>&-'`. Each exits 0 and changes its file from `seed\n` to `seed\nmarker\n`.
5. For the old comparison, transpile the checkpoint-9 adapter into the fixture's copied runtime and regenerate hooks using the fixture-local generator. The same payloads are denied. A complete historical build is not claimed.

The added unit test at `scripts/test-process-debt.mjs:259` covers append-to-path and single-`>` descriptor forms separately, but never combines append with a numeric/dash operand. The live-gate test at `scripts/test-harness.mjs:2918` has the same gap. Both therefore pass on this defect.

Repair within the existing adapter: distinguish append redirection from descriptor duplication. Numeric/`-` operands may be skipped for `>&`; literal `>>&` operands must be retained as destinations, or conservatively remain opaque. Pin numeric/dash append cases in both existing tests, preserving the repaired pathname denials and ordinary-read admissions. Correct the repair's generalized descriptor explanation in its next append-only decision record. No broader shell classifier is needed.

This worktree currently contains **zero tracked files with numeric or `-` basenames**, and no real gate corruption was observed. The old arbitrary-dist-path bypass is closed. Those facts limit exposure; they do not satisfy the retained invariant for a supported shell spelling. This is a new, executed regression within the changed branch, independently reproduced by both readers and the verifier, and it meets D006's stated reopening condition.

## Acceptance disposition

| Criteria | Judgment and evidence |
| --- | --- |
| 1 | Met: current focused fixtures exercise all four completions without gate/read/usage prerequisites and retain illegal-transition refusal |
| 2 | Met with VER-002 O8's existing limits: identity tests pass; the repair's passing product row still resolves at the unchanged code identity after this session's dispatch/index edits |
| 3 | Prior support retained: the unchanged release fixture exercises one gate across the full lifecycle. The next real order's segment remains a future observation |
| 4–5 | Met: runner/removed modules unchanged since VER-002; current listing confirms the 19-suite default and protection statements |
| 6 | Prior support retained: VER-001's independent three fresh runs were 240.42, 243.16 and 243.62 s. Repair-002's current-identity row is 249.08 s. This session did not repeat that series |
| 7–8 | Met: unchanged attestation behavior; current completion, main-reservation, second-writer and dead-holder fixtures pass |
| 9 | **Not met: F1.** VER-002 F1/F2's original cases now pass; the new numeric/dash append regression remains |
| 10–11 | Prior support retained: release/refutation implementation and tests unchanged. No live publication or new planning judgment was attempted |
| 12 | Met: current wrapped-Cost and invalid-duration integration fixtures pass; historical promise/outcome limits remain as VER-002 recorded |
| 13 | Met for delivered checks/write-backs, with F1's correction required: generated/authority/publication checks pass, historical evidence is immutable, diff clean, no dependency added; current product-gate evidence is retained below |
| 14 | Superseded by the order's recorded operator amendment; no fallback was executed |

Checkpoint 7 → 10 changed implementation only in the adapter and its two test files. Of 20 pre-existing WO-132 evidence/verification files, 19 are byte-identical and `decisions.md` preserves the complete old prefix. All nine generated hooks differ only in snapshot location and adapter identity. WO-126–WO-131 evidence and verification directories are unchanged against HEAD. VER-002 O1–O8 remain disclosed observations and are not new repair items.

The available passing current-identity product row was recorded by the repair session at **2026-09-15T14:55:33.976Z**, duration **249,078 ms**, exit 0, exact tree `c92f235069b57f8c711fe87d44d05a93f92c2b92`, reference `host-gate:dee83ba1012ce048ce7ec05cfb2834c744d5dbcaa71463b2067877396548a288:npm test`. This verifier observed that row and matching code bytes, but did not execute another product gate. A passing aggregate cannot resolve the reproduced F1; the final reviewer still owns its required gate.

## Judgment, cost and limits

The mission contribution is reliable release evidence while removing recurring machinery work. NoOp leaves a newly admitted write in one of the two protections the order keeps. A bounded operand correction removes that defect without changing the lifecycle or adding a recurring mechanism. Rule beating applies because the new tests pass despite the failed behavior; shifting the burden applies because close consumes the single reviewed gate. Policy resistance and escalation favor preserving ordinary reads and limiting repair to append semantics. Commons cost and drift favor these focused checks over another full gate after a decisive failure. Success to the successful gives no preference to the existing parser; seeking the wrong goal forbids substituting a green fixture for the required behavior. Naive Interventionism favors the smallest correction that preserves the repaired pathname handling and host delegation.

All write effects were executed in scratch, after releasing fixture gate markers. Hook decisions were observed during fixture gates. No source, success record or installed input was changed during a real worktree gate. Baseline timings, three-run freshness, complete lifecycle/refutation coverage and live publication retain the source/cutoff limits stated above; they were not re-measured by this dispatch.

Entry usage was unavailable. The later collector returned **1,946,201 total tokens**, source `codex-transcript-counter`, scope `dispatch`, cutoff **2026-09-15T15:05:23.206Z**; cost and wrapped command count were unknown. This is the collector's reported total, including repeated input context, not unique context size. Inclusion of read-only worker usage is unknown. Final observations remain in ignored receipts and the response.

## Repair checklist

- [ ] **F1:** distinguish `>>&` append filenames from `>&` descriptor operands; add numeric/dash parser and live-hook regression cases; append the corrected explanation. Preserve all original repaired denials and ordinary-read admissions.
