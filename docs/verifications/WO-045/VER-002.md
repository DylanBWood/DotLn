# WO-045 VER-002 — independent verification after repair

**Verdict: pass.** The repair discharges VER-001 F1. Published `v0.19.0` (WO-067, `5bcbd63`) is integrated as the base. The order is retimed to application `v0.20.0` (tag absent), and every component moves forward: compiler `0.11.0 → 0.11.1`, kernel `0.2.1 → 0.3.0`, skeleton `0.16.0 → 0.17.0`, console unchanged. Criteria 1–6 hold at the current subject. One stale sentence remains: the WO-045 note in 06 §Release boundary still names `v0.19.0` and compiler `0.10.1`. Product 07 classes that as final-review integration bookkeeping, not a repair event. Final review must correct it before publication (O7).

Subject: [WO-045](../../work-orders/WO-045-store-and-hook-input-decoders.md), criteria 1–6 with the operator's 2026-09-15 AC4 scope expansion, after the `resume: fix` repair of [VER-001](VER-001.md) F1. The session hook recorded the `resume: verify` dispatch and allocated this path at checkpoint `refs/dotln/checkpoint/WO-045/7` (`3a1172a5`). Branch `wo-045` has `HEAD` = merge-base with `main` = `5bcbd63` (the `v0.19.0` commit) and no branch commits. I edited no implementation file. My only repository writes are this report and the regenerated work-order index projection. Evidence cutoff: 2026-09-15T20:21Z.

Verifier: Claude Code 2.1.272, observed with `claude --version`. Model `claude-opus-5[1m]` comes from the operator's `/model` selection and the session system prompt. Effort is not shown by that selection or any readback, so it is recorded as `unknown`. No effective-session readback is claimed. The root agent was the sole registered writer (`harness writer --show`: alive, reserved 20:15:22Z). No subagents were used.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.272","model":"claude-opus-5[1m]","effort":"unknown","source":"operator-selected"}

Instrument disclosure: the regenerated hooks under test (snapshot `.runtime/harness/902393d5d4254be5`) recorded this dispatch and observed this session's tool calls. I judged hook behavior from the generated-process fixtures, the skeleton decoder tests and this session's transcript, not from those hooks' own records.

## Carried-forward claims

Checked blob by blob against checkpoint 3 (the VER-001 subject):

- `packages/kernel/src/*` and `packages/skeleton/src/harness-host.ts` are unchanged.
- The `store-decode`, `store-history` and `harness-input` tests and both fixtures under `packages/kernel/test/fixtures/` are unchanged.
- The compiler entry hunk is identical: `git diff 7aeccb1 ckpt3` equals `git diff HEAD` on `packages/compiler/src/harness.ts`, compared with `cmp`.

VER-001's decoder judgments therefore apply to the same bytes, including its independent probes and observations O1–O6. The re-runs below confirm them on the integrated tree. Nineteen original evidence files and `VER-001.md` are byte-identical to checkpoint 4. The retained preservation stash `fda7492f` ("WO-045 repair preserve before v0.19.0 integration") exists.

## Executed evidence

| Check | Observed result |
| --- | --- |
| `npm run build`, then `node --test` over dist: kernel `store-decode`, `store-history`, `ac2-replay-store`, `ac7-readme-map`, `kernel`; skeleton `harness-input`, `evidence-editions` | 39 passed, 0 failed. History: 66 event streams / 3,968 events byte-identical; 39 files classified as other JSONL protocols. VER-001 counted 62 / 3,857 and 38; the increase is WO-067's integrated streams and control segment. |
| `node --test --test-name-pattern="WO-039\|WO-045\|WO-127\|WO-067" scripts/test-harness.mjs` | 14 passed, 0 failed, 26 s: every WO-039 generated-hook fixture, WO-045's malformed generated payloads, WO-127 stdin shapes and WO-067's compiler-only fresh snapshot. |
| `node --test --test-name-pattern=WO-131 scripts/test-process-debt.mjs` | 6 passed, 0 failed, 12 s. Recovery still precedes runtime, state, Git, gate and writer checks, and survives missing runtime, damaged state and malformed input. |
| WO-101 `wo101-program-corpus` and `wo101-id-corpus` | 16 passed, 0 failed. |
| `artifact-identity-evidence.mjs --check`, `verification-evidence.mjs --check`, `feedback-evidence.mjs --check` | All exit 0 on the WO-045 revision `001` editions selected in `docs/evidence/current.json`; authority is revision `002`. The artifact check reports original semantic hashes and the frozen WO-003 oracle unchanged. |
| `node scripts/harness.mjs check`; `node scripts/harness-evidence.mjs` | Pass: 24 generated surfaces; historical role and writer smokes remain valid at their historical pin. An advisory (release-close cold start 8,600 > 8,192 bytes) is outside this subject; the skills and floor are untouched. |
| Bundle scope against `v0.19.0` | [`bundle-check.mjs`](../../evidence/WO-045/bundle-check.mjs) now compares against `v0.19.0`. The recorded [comparison](../../evidence/WO-045/repair-bundle-comparison.json) shows manifest and hook configs equal after removing pins. My textual `git diff HEAD -- .claude/hooks` shows three kinds of change: snapshot `f1f7fc0e… → 902393d5…`, derived hashes and version pins, and the entry lines (`rawInput`, guarded `recoveryInput`/`operatorControl`, `runHarnessHook(..., input, rawInput)`). |
| This session's native hooks | The transcript records 132 PreToolUse, 60 PostToolUse and 2 UserPromptSubmit events, with zero hook outputs containing `DOTLN_HARNESS_INPUT_REFUSED`. Four transcript lines contain the string: three are my own diff and grep results, one is my grep command. |
| `node scripts/release.mjs check-surfaces --local` | Exit 0. |
| `git diff --check HEAD` | Clean. |
| Versions and tags | `v0.19.0` is the latest local tag and an ancestor of `HEAD`; `refs/tags/v0.20.0` is absent. The package versions, lockfile, skeleton dependency pins, `COMPILER_PACKAGE_VERSION`, contributor runtime pins and `HARNESS_HOST_VERSION` agree. |

I did not run `npm test` or the full `scripts/test-harness.mjs` suite. The executor's repair gate (19 passed, 62 fresh tasks) and harness run (27 passed) are recorded in [the repair transcripts](../../evidence/WO-045/repair.md). Final review's `npm test -- --review` is the gate that PR publication consumes. A verifier run would have duplicated about eight minutes of wall-clock without being consumed; the focused runs above cover the claims the integration changed. The operator-run live feedback audit was not re-run; its committed edition passes `--check`. Origin's tag state was not queried because sandbox egress to github.com was denied.

## VER-001 F1 — resolution

| F1 repair item | Observed |
| --- | --- |
| Integrate `main` at or after `5bcbd63` | Done by fast-forward; `HEAD` is `5bcbd63`. |
| Next free application version; compiler above `0.11.0` | `v0.20.0` in the heading, README release block and kernel/skeleton READMEs; compiler `0.11.1`. |
| Regenerate bundle and manifest; re-run bundle check against the new base | Done; see executed evidence. |
| Fresh evidence editions and live feedback audit | WO-045 revision `001` artifact, verification and feedback editions and authority `002` are generated and selected, and pass `--check`. D005 records the selector change that enables same-order numbered revisions. |
| Re-record console fixtures | `packages/console/fixtures/*` are regenerated (see the repair record). This report did not re-check them independently beyond the edition checks. |
| Correct release surfaces and D003 | Mostly done. D004 explicitly supersedes D003's staged versions, and D003 stays as a dated historical record. The heading, README and component READMEs are corrected. **06 §Release boundary is not corrected (O7).** |
| Re-run `npm test` and harness fixtures | Executor transcripts; focused harness re-run here. |

On retiming authority: product 10 §Separate version axes requires explicit operator authorization and a dated migration note. The dated note is present as "Repair migration (2026-09-15)" in the work-order header. Product 07 §Independent workflows and integration permits retiming an unpublished release under its existing classification. It also permits retiming a component bump that upstream consumed. Operator authorization is observed only in the executor's [`repair.md`](../../evidence/WO-045/repair.md) record of operator corrections; I did not observe the operator's words.

## Acceptance judgment

| Criterion | Judgment and evidence |
| --- | --- |
| 1 | Met. The corpus bytes are unchanged: 22 malformed lines with distinct `{code, path}`, and physical lines 1 and 2 are named. The corpus test re-ran green. `decodeLog` and `appendEvent` throw the same message. |
| 2 | Met. 66 committed EventEnvelope streams / 3,968 events round-trip byte-identically on the integrated tree. The WO-003 oracle is unchanged and the WO-101 corpora pass. The WO-009/010/011 streams are within the strict history set, per VER-001 O6. |
| 3 | Met. The skeleton test refuses more than eight malformed payloads. The generated fixture refuses ten piped payloads with the PreToolUse deny shape. All WO-039 generated-hook fixtures and the historical live-record check pass over the regenerated `0.11.1` bundle. |
| 4 (amended) | Met against the integrated activation-base equivalent `v0.19.0`: only pins, the manifest and the authorized entry fix differ, and `harness check` passes. Recovery stays ahead of runtime imports (WO-131 cases). |
| 5 | Met. The 02 Event store row, kernel/skeleton/compiler READMEs and the `kernel.jsonl-replay` capability row (L2, dated 2026-09-15) are present. The ledger duty is discharged by D001–D005 and their index rows. No product heading changed. |
| 6 | Met for this dispatch. `git diff --check` is clean. No new dependency: the lockfile changes only workspace versions. The executor's repair `npm test` is green. Fresh editions are selected and pass their checks, and the fresh live feedback audit is recorded. The consumed gate is final review's `--review` run. |

## Findings

None blocking.

## Observations

- **O7 — stale roadmap note; required before publication.** `docs/product/06-roadmap.md:31-36` still reads "application `v0.19.0` is the next minor above local `v0.18.0` … advances compiler to `0.10.1`". The note directly above it records WO-067 taking `v0.19.0` with compiler `0.11.0`. Expected: `v0.20.0` above published `v0.19.0`, with compiler `0.11.1`, matching D004 and the heading. Reproduce with `sed -n 23,37p docs/product/06-roadmap.md`.
  - `release check-surfaces --local` passes with the stale note in place.
  - I did not run `release prepare`, because it writes files. Inference from `scripts/release.mjs:1993`: prepare edits the roadmap only when a retime plan exists. With `v0.20.0` free, it would report "remains current; no files changed", so the stale sentence would survive unless someone edits it.
  - Severity: false product text about two release identities that would ship to `main`. It is not behavioral, and product 07 assigns the retiming note to integration bookkeeping, which "must not create a new VER-NNN, a failed FINAL-NNN, or a repair event". The final reviewer should rewrite this one note before PR publication.
- **O8 — evidence selector widening (checked).** `evidence-editions.mjs` now accepts numbered revisions for artifact-identity and verification editions and resolves them to `<kind>/<revision>`. The three-digit non-`000` pattern still applies. The new test rejects `000`, `1`, `0001`, `../001` and `001/next`, and shows the immutable writer refuses replacement while the original directory survives. This is bounded to D005's stated purpose.
- **O1, O2 carried forward unchanged.** O1: `appendEvent` validates the existing log but not the new draft. O2: `tool_response` is object-only, and MCP tool response shapes remain unobserved. Neither is inside the order's criteria.
- **O5 carried forward.** The kernel minor (`0.3.0`) remains the right axis because callers observe the stricter `decodeLog`; the reviewer decides.

## Goal alignment and cost

The order serves the always-on runtime critical path with one typed boundary for persisted events and one for hook input. The promised outcome holds on the integrated tree: malformed lines and payloads refuse with a code and path, valid history is byte-stable, and recovery stays reachable. NoOp would leave casts at both boundaries.

- **Policy resistance / fixes that fail** was the F1 collision between parallel lanes. It is resolved by integration, and O7 is its residue.
- **Escalation:** failing the order for O7 would add a repair and verification cycle for bookkeeping that product 07 assigns to the integrating reviewer. That is the process growth the operator corrected in `repair.md`.
- **Rule beating:** checked with generated-process fixtures and blob identity, not only aggregate green. `check-surfaces` passing with O7 present is a small rule-beating gap in release-surface checking. It is recorded here, not repaired here.
- **Commons, drift, success to the successful, shifting the burden and seeking the wrong goal:** immaterial. The repair adds no dependency or process, and O1 remains a recorded later-store item.
- **Naive Interventionism:** the decoders are correct and unchanged, and no re-implementation is warranted.

Process cost at 2026-09-15T20:21:13Z: 1,089,941 total tokens, source `claude-transcript-message-usage`, scope `dispatch`. Of these, 917,721 were cached input and 22,781 were output; 34 commands were run, and dollar cost was unavailable. Dropping the verifier's full `npm test` and harness suites in favor of focused runs (38 s) avoided about eight minutes of duplicated gate time. The final reviewer's `--review` gate still runs once.
