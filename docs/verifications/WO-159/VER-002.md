# WO-159 VER-002 — independent verification: pass

**Subject:** The selected `wo-159` worktree at the `VerificationRequested`
checkpoint `eafca1596a9cb426e2b587b9e8b2d69596ea9534`, judged against the
original [WO-159](../../work-orders/WO-159-codex-episode-isolation.md) and the
current uncommitted implementation. This report preserves
[VER-001](VER-001.md)'s failed judgment of its earlier subject. I made no
implementation or product-document edit.

**Verifier:** Codex CLI 0.156.1, `gpt-6-sol`, session effort `xhigh` from the
dispatch readback. No subagent was launched: manual and observed admissions
were 0 against the shared cap of 20; unobserved coverage remains unknown.
The transport exposed no hard USD 5 provider cap for this session, so that
limit was unenforced.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.156.1","model":"gpt-6-sol","effort":"xhigh","source":"codex-session-readback"}

**Verdict:** **Pass.** The two VER-001 findings are repaired on this subject,
and the original order's seven acceptance criteria are supported by the
current code, retained live evidence and passing gates. This verdict judges
the selected branch; integration remains the final review's responsibility.

## Earlier findings and reproduction

**F1 — major, repaired.** Expected: each distinct Codex invocation in a
writing-worker probe row has its own home and digest record. Observed:
[`writing-worker-probe.mjs`](../../../scripts/lib/writing-worker-probe.mjs)
starts an episode inside `isolated(label, launch)` for each invocation,
including each of the three concurrent processes and the fresh recovery
process, and finishes it in `finally`. The row retains labelled
`codexEpisodes` in launch order. The [probe fixture](../../../scripts/test-harness-probe.mjs)
logs the home seen by each stub invocation, asserts 12 distinct homes for
12 Codex invocations, and matches the 11 row-launch home hashes to 11
records with equal pairs and removed homes. That fixture passed in `npm test`.
The executor's [repair evidence](../../evidence/WO-159/repair.md) also
records a negative control against checkpoint 5: the same assertion found
only 9 distinct homes for 12 invocations. I did not rerun a live
multi-process model probe; the fixture exercises the defective control flow.

**F2 — minor, repaired.** Expected: the live receipt's stream digest, row
count, launch and terminal facts come from the retained verifier stream.
Observed: [`receipt.mjs`](../../evidence/WO-159/receipt.mjs)
`matchSharedStream` rebuilds the feedback edition's verifier stream by value,
derives the receipt through `buildReceipt`, and compares all nine top-level
fields. `--check` now requires that stream and exited 0 on the committed
receipt. I independently changed `eventStream.value.sha256` **in memory** to
`sha256:` plus 64 zeroes: `validateReceipt` accepted its shape, while
`matchSharedStream` refused it with `eventStream: the retained stream records
another eventStream`. The receipt fixture also covers false count, launch,
terminal and episode fields. No filed receipt was changed.

## Acceptance matrix

| Criterion | Judgment and evidence |
| --- | --- |
| 1 — fake Codex fixture | **Pass.** [`codex-episode.test.ts`](../../../packages/skeleton/test/codex-episode.test.ts) runs a fake CLI that writes trust into the isolated home. It asserts the fixture's user config remains byte-identical, equal file and trust-table pairs, removed home, and no fixture auth bytes in record, store or stderr. It passed in the product gate. |
| 2 — one argv builder and adopted callers | **Pass.** `git grep -l -e '"--ephemeral"' -- ':(glob)packages/*/src/**' scripts` returned only `packages/skeleton/src/worker-transport.ts`. The five named exec sites call `codexExecArgv` and `startCodexEpisode`; the help-only `probe-worker-hosts.mjs` uses the launcher's shared flags and isolates its Codex invocations. The writing-worker site's multi-launch paths are covered by F1's fixture. The seventh live-model site, `probe-codex-effort.mjs`, also adopts the launcher. The order's literal `packages/*/src` pathspec does not reach source files; [D004](../../evidence/WO-159/decisions.md#wo-159-d004) records the corrected glob. |
| 3 — live Codex worker | **Pass for the retained single-process episode.** [`live-codex.json`](../../evidence/WO-159/live-codex.json) records isolated `codex login status` exit 0 and one completed Codex CLI 0.156.1 feedback verifier episode. Both removed homes have equal before/after config and trust-table digests. The [trust probe](../../evidence/WO-159/trust-probe.json) observed a trust entry in the isolated home under WO-111's source-change launch prefix while the user-level pairs remained equal. This is not a WO-111 portfolio rerun. |
| 4 — protected pairs and refusal | **Pass.** The live receipt lists both launches' digest pairs under `protectedSurfaces`; the receipt fixture refuses unequal pairs and missing homes. F2's independent false-hash reproduction confirms the retained stream is now required. |
| 5 — WO-054 correction and capability | **Pass.** The dated note is appended below the unchanged original bytes of [`codex-continuation.md`](../../evidence/WO-054/codex-continuation.md). The capability table reassesses both Codex launch containment and WO-054 continuation without rewriting the historical rows. |
| 6 — editions | **Pass on this branch.** [`current.json`](../../evidence/current.json) selects WO-159/001 for authority, verification and feedback. The product gate's edition checks passed. The repair changed `writing-worker-probe.mjs`, its test and the WO-159 receipt checker, not the registered edition source files; the feedback edition retains the live self-host episode minted after the implementation's registered source edits settled. |
| 7 — gates and dependencies | **Pass.** `npm test`: 27 suites passed, 0 failed, 71 fresh tasks, 365.54 s. With this report present, `npm run test:docs` passed 21 suites, 0 failed, 21 fresh tasks, 25.53 s. `git diff --check` was clean. The manifest and lockfile diff bumps the skeleton component and console pin without adding a dependency. |

## Judgment, limits and process cost

The order advances the critical path to worker activity on operator-owned
repositories by demonstrating Codex configuration containment at a real
launch and covering each probe invocation in a fixture. NoOp on F1 or F2
would retain a false per-launch claim or a receipt that could pass with a
false stream hash. F1 addresses drift to low performance; F2 addresses rule
beating. Equal digest pairs reduce the operator's recurring trust cleanup
burden, while the fixture avoids more live model spend and shared compute.
The live login and worker row test the risk that an isolated home blocks
authentication; the retained receipt and existing launch arguments preserve
the useful worker behavior. Additional process, preference for the existing
mechanism, policy resistance and pursuing a passing label over the operator's
actual configuration are checked by the independent reproduction and the
unmodified historical evidence. This is the smallest useful verification
probe under product 07's Naive Interventionism lens.

The WO-159 receipt builder and checker are themselves part of the instrument
under verification. The independent in-memory false-hash reproduction and
the fixture's per-invocation home log supply checks separate from its success
claim. Other host paths and network effects were not measured by this order.

**Process cost:** entry 38,735 tokens; handoff 3,833,856 tokens; source codex-transcript-counter

Both counters have `scope: dispatch`. Entry cutoff was
2026-09-25T05:24:54.743Z; the latest handoff readback cutoff when this report
was drafted was 2026-09-25T05:33:22.616Z. USD cost is unavailable.
