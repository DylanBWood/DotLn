# WO-159 VER-001 — independent verification: fail

**Subject:** The selected `wo-159` worktree at the `VerificationRequested`
checkpoint `68f4bd0120275337b495737941c5db855d6063dd`, judged against the
original [WO-159](../../work-orders/WO-159-codex-episode-isolation.md) and
its current uncommitted implementation. The verifier made no implementation
or product-document edit. The defects and repair follow-ups are retained in
[D016](../../evidence/WO-159/decisions.md#wo-159-d016) and
[D017](../../evidence/WO-159/decisions.md#wo-159-d017).

**Verifier:** Codex CLI 0.156.1, `gpt-6-sol`, selected effort `ultra`
(normalized to `xhigh`, mode `subagents`) from the dispatch readback. Two
read-only subagents reviewed grouped code/fixture and live/edition evidence;
this root session was the sole writer. Manual admission count: 2 against the
shared cap of 20. The Codex harness observed 0 attributable admissions and
does not observe all child tool calls, so its remaining coverage is unknown.
No hard USD 5 provider cap was exposed or enforced.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.156.1","model":"gpt-6-sol","effort":"xhigh","mode":"subagents","raw":"ultra","source":"codex-session-readback"}

**Verdict:** **Fail.** The named writing-worker probe does not give every
Codex exec launch its own home and before/after digest record. The current
live single-process row, fixture, corrected WO-054 note, and re-minted
editions are supported. A separate minor receipt-check defect also needs
repair. The branch remains for `resume: fix`; this report does not judge an
integrated or repaired subject.

## Findings

### F1 — major — per-launch isolation and evidence are absent in multi-process probe rows

**Criterion:** The order's heading and design, especially lines 106–122, and
criterion 2's adoption by the six named sites. Expected: each distinct
`codex exec` in a probe receives a fresh `CODEX_HOME`; its own before/after
configuration and trust-table pairs are retained. Observed:
[`writing-worker-probe.mjs`](../../../scripts/lib/writing-worker-probe.mjs)
creates one episode and environment per *row* at lines 1128–1130. The
`concurrent` row starts three `runProcess` calls with that same environment
(lines 845–851, 1176–1187). The `resident-kill` row makes a second launch
explicitly labelled a fresh recovery with the first launch's environment
(lines 824–830, 1307–1322). The row writes only one `codexIsolation` record
(lines 1342–1349).

**Reproduction:** Follow `env` from the single `startCodexEpisode` through
the three `Promise.all` calls and the recovery call; there is no intervening
home allocation. The existing [probe fixture](../../../scripts/test-harness-probe.mjs)
at lines 1000–1025 checks one `homeRemoved` field per row, so it cannot
distinguish these subprocesses. A stub recording `env.CODEX_HOME` for each
`runProcess` in those rows would observe the same value for distinct launches.
No real model invocation is needed to reproduce the control-flow defect.

**Impact and limit:** The per-episode isolation and per-launch receipt claim
are unmet for this required caller. Sharing one isolated home still keeps
these processes away from the user `config.toml` under the observed launcher;
this finding does not assert an observed user-file change or a credential
leak. [D016](../../evidence/WO-159/decisions.md#wo-159-d016) names the repair
and a distinct-home fixture.

### F2 — minor — the live receipt accepts a false event-stream digest

**Criterion:** The live evidence's observed stream identity supporting
criteria 3–4. Expected: `--check` binds its claimed `eventStream.sha256`, row
count, and launch selection to the retained feedback verifier stream.
Observed: [`receipt.mjs`](../../evidence/WO-159/receipt.mjs) checks only the
digest's syntax at lines 321–334 and matches the terminal event's isolation
record at lines 389–409. I changed the receipt's stream digest **in memory**
to `sha256:` followed by 64 zeroes; `validateReceipt` and
`matchSharedStream` both returned `true`.

**Current evidence and limit:** Resolving the committed feedback edition's
references in memory rebuilt the original 2,203,530-byte verifier log. Its
SHA-256 is the receipt's `8916ed6f…`, its 29 rows match, and its start event
matches the claimed launch. The committed projected file has different bytes
and a different hash by design. Thus the current live observation remains
supported; the check would admit a later false claim. [D017](../../evidence/WO-159/decisions.md#wo-159-d017)
names the bounded checker and fixture repair. This finding alone would not
negate the current live episode.

## Acceptance matrix and executed evidence

| Criterion | Judgment on this subject |
| --- | --- |
| 1 — fake Codex fixture | **Pass.** `codex-episode.test.ts` runs the fake CLI that writes trust into its isolated home; the user file is byte-identical, both pairs are equal, the home is removed, and fixture auth bytes are absent from the record, store and stderr. Its subtest passed in the fresh product gate. |
| 2 — one launch site and six callers | **Fail in operation.** The corrected `git grep -l -e '"--ephemeral"' -- ':(glob)packages/*/src/**' scripts` returns only `packages/skeleton/src/worker-transport.ts`. All six sites import the launcher, but F1 shows one site applies one episode to several exec launches. The literal `packages/*/src` pathspec in the order reaches no source file; D004 records the corrected search. Bare `codex --version` observations are explicitly outside episode scope in D005. |
| 3 — live Codex row | **Pass for the recorded single-process launch.** [`live-codex.json`](../../evidence/WO-159/live-codex.json) records isolated `codex login status` exit 0, a completed `codex-cli-exec` feedback verifier episode, distinct removed homes, and equal user-config/trust-table pairs. The retained terminal event carries the same isolation record; the separate writer-shape trust probe observed one trust entry in its isolated home. This is not a WO-111 source-change rerun. |
| 4 — protected pairs and unequal-pair refusal | **Pass for the stated pair check.** The live receipt lists both launches' pairs under `protectedSurfaces`; `codex-isolation-receipt.test.ts` refuses unequal pairs and other malformed fixtures. F2 identifies a separate stream-identity check gap. |
| 5 — WO-054 correction and capability | **Pass.** The dated note at `codex-continuation.md` lines 134–156 is appended below its unchanged original bytes. The capability table's 2026-09-25 section reassesses both Codex launch containment and WO-054 continuation. |
| 6 — editions | **Pass on the selected branch.** `current.json` selects WO-159/001 for authority, verification and feedback. Read-only `--check` commands for all three passed; the feedback edition's retained live self-host episode and projected stream select the current source. Integration remains a later final-review duty. |
| 7 — gates and dependencies | **Pass.** `npm test` passed 27 suites, 0 failed, 71 fresh tasks in 295.21 s. `npm run test:docs` passed 21 suites, 0 failed, 21 fresh tasks in 22.65 s. `git diff --check` was clean. The lockfile changes component versions and the console's skeleton pin, with no added dependency. |

Executed by this verifier: `npm test` (exit 0); `git diff --check` (exit 0);
`node docs/evidence/WO-159/receipt.mjs --check` (exit 0, terminal event
matched); the source-only shared-flag search above (one file); an in-memory
false-hash reproduction (both checks incorrectly true); and in-memory
reconstruction of the retained verifier stream (claimed hash and 29 rows
match). A read-only reviewer ran the authority, verification and feedback
edition checks; each exited 0. `npm run meta` refreshed the decision index
after D016–D017. `npm run test:docs` passed with this report present.

## Judgment and process cost

This order advances the critical path by keeping DotLn's own Codex launches
from adding trust entries to the operator's configuration. The current live
row supports that benefit for one transport episode. NoOp on F1 would leave
multi-process probes sharing state and reporting one aggregate pair while the
order claims per-launch evidence. Passing solely on green gates would beat
the rule and drift below the stated outcome; treating the current live row
as proof of every probe shape would seek the wrong goal. The bounded fixture
and checker repairs protect the existing live evidence without repeating a
model episode. They reduce recurring operator inspection and avoid consuming
shared compute for a test that cannot exercise F1. Other policy-resistance,
escalation and success-to-the-successful concerns do not outweigh the direct
source evidence; the current single-process launcher and authentication path
remain useful while repair addresses the two identified gaps. This is the
smallest useful intervention under the Naive Interventionism lens.

**Process cost:** entry 40,386 tokens; handoff 5,546,037 tokens; source codex-transcript-counter

Both counters have `scope: dispatch`; entry cutoff 2026-09-25T04:51:43.464Z,
latest available handoff cutoff 2026-09-25T05:00:46.894Z. USD cost is
unavailable. The two read-only agents reviewed grouped paths concurrently;
the harness recorded no attributable child admissions, so the manual count
above supplies the actual fan-out for this session.
