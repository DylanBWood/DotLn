# WO-158 VER-002 — independent re-verification: pass

**Subject:** The uncommitted `wo-158` worktree at HEAD `1f88850975c5aa7c2e8169c7267017d06d34a302`, after the repair of [VER-001](VER-001.md). The `verify` dispatch allocated this report and checkpoint `refs/dotln/checkpoint/WO-158/7` (`b74de50ae15e34eae3c80ed3d09d2e088ea55e40`). I judged the nine criteria of [WO-158](../../work-orders/WO-158-lifecycle-off-ramps.md) against the current source, fixtures, product documents and generated outputs. The passing product gate recorded code identity `1172bcf9c23c07e60ca2dd9e1e8ccd1de8260af74a287868633490f3c575e8f7`.

**Verifier:** Codex CLI 0.156.1, GPT-6 Sol, selected xhigh. The current-session readback supplied the model, effort and CLI version. I used no subagents (zero planned and observed against cap 20; the unobserved remainder is unknown). The transport exposed no hard dollar-cap control, so a USD 5 cap was unenforced.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.156.1","model":"gpt-6-sol","effort":"xhigh","source":"codex-session-readback"}

**Verdict:** **Pass.** The four VER-001 defects have bounded repairs and passing regressions. No implementation was changed during this verification.

## Evidence and checks

- `npm test` passed 27 of 27 suites in 292.10 s, with 71 fresh tasks, at the code identity above. The `resume` suite exercised `scripts/test-off-ramps.mjs`; the `skeleton`, work-order and derived-order suites covered the generated-hook and withdrawn-dependency fixtures. The WO-143 restart case that timed out in VER-001's separate gate passed in this run.
- F1: `scripts/resume.mjs` round-trips reactivation dates through the UTC calendar before admitting a changed authority. The route fixture refuses both `9999-99-99` and `2026-02-30`, then activates a changed authority with a valid new note.
- F2: verification and final-review completion events record the report's SHA-256. A corrected path must name the same report id and bytes. The fixture refuses a different `sub/VER-002.md` while retaining the passing verdict's original path, then admits an identical copy and projects its correction ordinal.
- F3: the generated session hook creates override captures with exclusive file creation and a numbered suffix on collision. A frozen-clock hook fixture performs two exits in one second; each event's capture path and digest still match its own operator words.
- F4: the live-gate classifier requires `git --no-pager` or `git -P` for its listed Git reads. The hook fixture admits the flagged reads across three pre-tool hooks and refuses bare Git reads, redirects and unlisted programs with the published list in the refusal. D025 records the unchanged WO-142 `git --no-pager status` metadata exception under configured `core.fsmonitor` and its existing follow-up; this report does not claim that exception was removed.
- `npm run harness -- check` passed 31 generated surfaces. `node scripts/harness-context.mjs --check` measured both installed roots: executor 25,819/29,246 bytes, verifier 22,698/25,151, reviewer 23,780/24,576, release-close 14,649/16,384, planner 16,797/24,576; refuter 16,372 with no ceiling. `node scripts/authority-evidence.mjs --check`, `npm run evidence:artifact -- --check`, `npm run evidence:verification -- --check` and `node scripts/feedback-evidence.mjs --check` passed. `docs/evidence/current.json` selects WO-158 authority revision 002 and artifact-identity, verification and carried-feedback revision 001. The feedback check is carried evidence, not a new live episode.
- `npm run plan -- check`, `npm run publication:check`, `npm run release -- check-surfaces --local` and `git diff --check` passed. The lockfile diff changes only compiler/skeleton versions and workspace pins; it adds no dependency. `npm run work-orders -- index` ran after dispatch.
- `npm run test:docs` passed 21 of 21 suites in 23.94 s after the report was filed. Its format, receipt, registration, lineage, plan, edition and publication checks were green.

## Criterion judgments

**Criterion 1:** met. The four actor-bearing routes, phase refusals and status/current projections have passing lifecycle fixtures. The correction route now binds a relocated result to its judged report bytes.

**Criterion 2:** met. The route fixture checks contained, ignored capture files and their hashes, rejects an executor waiver, and distinguishes a waived unmet criterion from an unwaived unmet criterion in a passing verification.

**Criterion 3:** met. Withdrawal is terminal except for activation of a changed authority with a new valid dated note. The fixture rejects impossible dates and verifies the index's disposition, sequence settlement and an unmet typed dependency.

**Criterion 4:** met. A correction cannot change a verdict or report bytes; report-path correction requires the recorded digest (or the current bytes for a pre-digest result). The fixture rejects a different same-id report and the source retains the `CLAUDE_EFFORT` completion readback refusal.

**Criterion 5:** met. Generated-hook fixtures cover both a successful override-exit record and the advisory command when its runtime cannot record one. The same-second regression proves the two captures stay distinct and hash-bound; Codex's role text carries its explicit command duty.

**Criterion 6:** met. The live-gate fixture admits the fixed read-only list with no-pager Git reads and refuses redirecting or unlisted commands, naming the list. The prior configured-pager execution path is closed by the required flag. The pre-existing WO-142 status/fsmonitor exception remains separately boarded in D025.

**Criterion 7:** met. Generated roles and product 07 name the four routes; the harness check is green and every measured role with a cold-start ceiling is within it.

**Criterion 8:** met. Authority, artifact-identity, verification and carried-feedback edition checks pass at the current pointer. No live episode is claimed.

**Criterion 9:** met. The current 27-suite product gate and 21-suite document gate passed, `git diff --check` is clean, and the lockfile has no new dependency.

## Judgment and limits

The order's promised benefit is a durable, typed route for recurring operator decisions. The repaired probes now reject false reactivation dates and report substitutions, preserve operator-word provenance, and keep a pager out of the listed live-gate Git reads. Those are the material rule-beating and evidence-integrity risks raised by VER-001. The bounded regressions avoid an additional redesign (Naive Interventionism); NoOp would leave the reproduced failures. The unchanged WO-142 metadata exception and the absence of a new live episode limit what this pass claims. Policy resistance is checked where correction, report judgment and projection agree. One full gate accounts for shared compute (tragedy of the commons) without escalating to repeated gates; passing behavior rather than a weaker baseline guards against drift to low performance. The prior green fixtures did not get a preferred verdict merely from investment in them (success to the successful). Typed refusals and truthful status reduce later operator rescue (shifting the burden), while the operator-flow outcome, rather than process or fixture count, guards against seeking the wrong goal.

**Process cost:** entry 44,639 tokens; handoff 1,758,471 tokens; source codex-transcript-counter

The entry readback was dispatch-scoped at 2026-09-25T14:11:19.243Z; the handoff figure is the latest available dispatch-scoped readback before the result transition at 14:18:51.792Z. USD cost and wrapped command count were unavailable. The comparison is one independent verifier and one complete product gate after the bounded repair, with no added agent wait.
