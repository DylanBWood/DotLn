# WO-158 VER-003 — independent re-verification after FINAL-001: pass

**Subject:** [WO-158](../../work-orders/WO-158-lifecycle-off-ramps.md) on branch `wo-158`, uncommitted over HEAD `0c727915a0582120cbe12d41f48ada53ddd7e5ce` (`main` after WO-159, integrated by FINAL-001), after [repair 002](../../evidence/WO-158/repair-002.md) of [FINAL-001](../../final-reviews/WO-158/FINAL-001.md) F1. The `verify` dispatch allocated this report and checkpoint `refs/dotln/checkpoint/WO-158/14` (`7b94b834`). No source path under `packages/`, `scripts/` or the hook generator differs from that checkpoint. The product gate below recorded code identity `50f0c0565cad8d08212c08f30db6b5e43a3ff58d0f8fcf9c65aa5ae54c578f41`, the identity repair 002's gate recorded, and tree `24d74a3937f26fb022c6243e2f75282d22d1bedc`. The order's text differs from its activation checkpoint only by the `(v0.49.0)` release label, so the nine criteria judged here are the original ones.

**Verifier:** Claude Code 2.1.282, Opus 5.5, selected effort `xhigh`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-opus-5-5","effort":"xhigh","source":"claude-session-readback"}

Human actor: the operator dispatched `resume: verify` in a Claude Code session and made no other choice during this verification. The harness version is from `claude --version`. The model is the session's selected model. Effort `xhigh` is the host's `CLAUDE_EFFORT` value read by this session: selected effort, not effective. The order asks for verifier `xhigh+`. No subagent was planned or spawned: 0 of the cap of 20 (`harness usage`: exact-observed). Claude Code exposes no hard dollar-cap control, so a USD 5 limit was unenforced.

**Instrument disclosure:** the generated pre-tool hooks this order changes judged every command of this session, including the live-gate classifier criterion 6 judges. The session hook recorded this dispatch, and the usage counter behind the cost line is DotLn's own. The live-session observations under criterion 6 are therefore the instrument observing itself. I corroborated them with a scratch probe of the built classifier and with the fixture suites.

**Verdict:** **Pass.** FINAL-001 F1 is repaired. Every recognized Git prefix sequence terminates, and a write chained after a listed read is still refused. I reproduced the original hang on a scratch copy of the pre-repair line. All nine criteria are met on the current subject. This verification made no implementation edit and found no new defect.

## Checks of FINAL-001 F1

- **Repair reading.** `liveGateGit` in [harness-command.ts](../../../packages/skeleton/src/harness-command.ts) now consumes the recognized prefix on every loop iteration (`if (args[index++] !== "--no-optional-locks") unpaged = true;`). The loop condition requires `args[index]` to be one of three prefix tokens. `index` rises on every pass, and an index past the end reads `undefined`, which is not a prefix token. So the loop ends for every finite argument list. The rebuilt `packages/skeleton/dist/src/harness-command.js` carries the same line.
- **Exhaustive probe against the built runtime.** I generated every prefix sequence of length 0 to 4 over `--no-pager`, `-P` and `--no-optional-locks` (121 sequences). Each was crossed with eight subcommand forms (`diff`, `log -1`, `show --stat HEAD`, `status`, `stash list`, none, `-c core.pager=x log`, `blame f`) and three tails (none, `; echo x > fixture.ts`, `| head -3`). All 2,904 inputs returned within 0.32 ms under a 20 s alarm. Each result matched the rule "at least one pager-disabling token, a listed read and no chained write": 1,160 admitted, 0 mismatches. FINAL-001's four hanging forms now return. `git --no-pager --no-optional-locks status`, `git -P --no-pager log` and `git --no-pager --no-pager log` are admitted. The `-c core.fsmonitor=false status --short` form returns `null` from the list and reaches the WO-142 metadata vocabulary, as D025 and D030 record.
- **Mutation check of the regression.** A scratch copy of the built module with the pre-repair `unpaged ||= args[index++] !== "--no-optional-locks"` line ran `git --no-pager --no-optional-locks status` and `git -P --no-pager log` until a 5 s alarm killed it (exit 142). The same copy still returned `git --no-optional-locks --no-pager status` in 0.35 ms. That is FINAL-001's reproduction, and it shows that the new regression's inputs catch the defect.
- **Guard abbreviations.** On Git 2.55.0 in a scratch repository, `--outp=`, `--outpu=`, `--ext-d`, `--textc` and `--show-sig` are rejected by Git itself (exit 128 or 129), so the list's `--output`, `--ext-diff`, `--textconv` and `--show-signature` guards cannot be bypassed by abbreviation. The full `--output=` wrote a file, and the classifier refuses it.
- **Live gate in this session.** While this session's own `npm test` gate ran, the real Claude Code pre-tool hooks acted as follows:
  - Admitted immediately: `git --no-pager --no-optional-locks status --short | head -3`, `git -P --no-pager log -1 --format=%h` and `node scripts/harness.mjs writer --show`.
  - Refused with the refusal text naming the list: `git --no-pager --no-optional-locks status --short; touch package.json`, the paged `git log -1 --format=%h` and the unlisted `node -e 1`.
  - Commands I wrote with `cut`, `echo` or an unquoted glob were also refused, as the list states.
- **Fixtures.** `node --test --test-name-pattern 'WO-158' scripts/test-harness.mjs`: 5 passed, 0 failed, 22.47 s. That includes the new `WO-158 FINAL-001 F1` case, which runs 44 classifier inputs under a five-second child-process bound and 48 hook judgments through the three generated pre-tool hooks under a live gate. The full `harness-fixtures` suite, which is not among `npm test`'s 27 required suites, is reported below.

## Evidence and checks

| Check (executed 2026-09-25) | Result |
| --- | --- |
| `npm test` (outside the harness sandbox; runner `sandbox.inForce: false`) | 27 passed, 0 failed; 298.78 s; 71 fresh tasks; recorded 15:53:18.503Z at the code identity above |
| `node scripts/test-runner.mjs --only harness-fixtures` | build and `harness-fixtures` passed, 0 failed; the suite took 190.35 s (190.91 s with the build). The suite contains the WO-158 live-gate, override-record and scratchpad cases |
| `npm run harness -- check` | 31 generated surfaces current |
| `node scripts/harness-context.mjs --check` (both installed roots) | executor 25,819/29,246; verifier 22,698/25,151; reviewer 23,780/24,576; release-close 14,649/16,384; planner 16,797/24,576; refuter 16,372, no ceiling |
| `node scripts/authority-evidence.mjs --check` | verified at `WO-158/004`; the regenerated `authority.json` and `bundle-diff.json` equal the recorded bytes (no retained-evidence path) |
| `npm run evidence:artifact -- --check`; `npm run evidence:verification -- --check` | verified at `WO-158/001` |
| `node scripts/feedback-evidence.mjs --check` | verified at `WO-158/002`, carrying WO-159's live audit; no new live episode |
| `npm run evidence:console -- --check` | selfhost, control, refutations and missing match |
| `npm run plan -- check`; `npm run publication:check`; `npm run release -- check-surfaces --local` | exit 0; publication PASS with both editions CURRENT; 44 PASS, no FAIL |
| `git diff --check`; `git diff --cached --check`; `git diff main --check` | clean |
| `npm run test:docs`, after this report | 21 passed, 0 failed; 24.15 s; 21 fresh tasks (format, receipt, registration, lineage, plan, edition and publication checks); a rerun after this row was filled also passed 21 of 21 (24.09 s) |

**Changes since VER-002's pass.** Outside `docs/` and the generated surfaces, the files that differ between VER-002's result checkpoint `/8` and `/14` are `main`'s WO-159 file set plus `packages/skeleton/src/harness-command.ts` (the repair), with one exception. `README.md` is in `main`'s set but is identical at both checkpoints. `main` changed only its release label (`v0.47.1` to `v0.48.0`), and WO-158's projection already reads `v0.49.0`. Per file, the changed lines from `/8` to `/14` equal the lines `main`'s WO-159 changed from base `1f888509` to `0c727915`, with two exceptions. The first is the skeleton and compiler version retime FINAL-001 recorded in D026 (`package-lock.json` and two package manifests). The second is `scripts/test-harness.mjs`, whose lines through checkpoint `/12` equal `main`'s exactly and which gains only the 109-line F1 regression by `/14`. `scripts/resume.mjs`, `scripts/lib/`, `packages/skeleton/src/harness-host.ts` and `packages/compiler/src/harness.ts` did not change after VER-002. So criteria 1 to 5 rest on the source VER-002 and FINAL-001 judged, and this gate re-ran their fixtures: `resume` (which runs `scripts/test-off-ramps.mjs`), `worktree`, `work-orders-fixtures`, `derived-orders` and `skeleton`.

**Dependencies.** The lockfile and manifests change only `@dotln/compiler` `0.18.0` to `0.19.0` and `@dotln/skeleton` `0.41.0` to `0.42.0`, with their workspace pins. No dependency is added.

## Criterion judgments

**Criterion 1:** met. The four routes, their phase refusals and the `status --json` and `current.md` projections are unchanged since VER-002 and FINAL-001 judged them met. Their fixture matrix (`scripts/test-off-ramps.mjs`: legal and illegal phases per route, and a withdrawn order that a typed dependency reads as unmet) passed in this gate's `resume` suite. This session's own status projects `legalOffRamps`, `waivedCriteria`, `withdrawal`, `corrections` and `overrideRecords`.

**Criterion 2:** met. The capture-hash checks, the executor-role `waive` refusal, and the pass-with-waiver versus fail-without-waiver fixtures are unchanged, and they passed in this gate. The Codex environment evasion stays boarded under [D029](../../evidence/WO-158/decisions.md#wo-158-d029) (a). It records `recordingSession.role: unobserved` and does not defeat the refusal for an observed session.

**Criterion 3:** met. The terminal `withdrawn` phase, calendar-validated reactivation, index disposition and sequence settlement are unchanged since VER-002. Their fixtures passed in this gate (`resume`, `work-orders-fixtures`, `derived-orders`). The publication gap stays with `FUP-3a0c4ea52f8d6d08`.

**Criterion 4:** met. `correct` still refuses a verdict and never writes a report. A path correction is bound to the result's recorded `reportHash`, and the `CLAUDE_EFFORT` disagreement refusal is in place. The fixture passed in this gate. The recorded `reportHash` values of VER-002 and FINAL-001 equal those reports' current SHA-256, so neither filed report changed.

**Criterion 5:** met. The generated session hook's `operator override: off` record, its advisory command and the same-second capture regression passed in the focused WO-158 run and in the full `harness-fixtures` suite. The hook generator is unchanged since VER-002, and Codex carries the duty in role text.

**Criterion 6:** met. The FINAL-001 F1 loop is gone: every recognized prefix order terminates promptly, and the listed reads are admitted in any prefix order. A redirecting, chained-write, paged or unlisted command is refused with the list in the refusal text. The evidence is the exhaustive probe, the new fixture through three generated hooks under a live gate, and this session's own gate. The `-c core.fsmonitor=false status` form is admitted by the WO-142 vocabulary, not by the list, and remains the boarded exception `FUP-9e2be6bfac0708fe`.

**Criterion 7:** met. All twelve installed role skills carry the `Off-ramps (WO-158)` sentence. Product 07's resume-phrase table carries four WO-158 rows (waive, withdraw, correct, `operator override: off`). Its route list and live-gate paragraph name the routes and the list, in the same text as `LIVE_GATE_READ_LIST` and the hook refusal. `npm run harness -- check` is green, and every measured role with a cold-start ceiling is within it, at the figures above. No breach needs the standing 2026-09-17 route.

**Criterion 8:** met. `docs/evidence/current.json` selects authority `WO-158/004`, artifact identity `WO-158/001`, verification `WO-158/001` and feedback `WO-158/002`. All four checks pass, and the authority check regenerates the recorded bytes exactly. The carried feedback audit claims no live episode.

**Criterion 9:** met. `npm test` passed 27 of 27 at the current code identity, and `npm run test:docs` passed (above). The three whitespace checks are clean, and no dependency was added.

## Findings

None. No defect was met that this report leaves unrecorded. FINAL-001's boarded observations ([D028](../../evidence/WO-158/decisions.md#wo-158-d028), [D029](../../evidence/WO-158/decisions.md#wo-158-d029)) and D005's `FUP-3a0c4ea52f8d6d08` keep their existing register rows and are not required by any criterion.

## Goal-aligned judgment and limits

The order is record-truth and live-gate throughput machinery, not a lettered critical-path gate. It succeeds if sessions stop improvising: they run a route, and they can keep reading while a gate runs without opening a write path. The repair is the smallest change that restores termination, and this verification checked termination by exhaustion rather than by the fixture's chosen spellings.

The eight traps and the two reference cases, as they applied here:

- **Rule beating:** the earlier F4 fixture passed because it chose the one spelling the loop survived. That is why I judged every prefix sequence up to length four and checked that the new regression fails on the old line.
- **Seeking the wrong goal:** the target is prompt read admission with continued write refusal, not a green suite. So both halves were observed through real hooks.
- **Policy resistance:** the repair keeps the no-pager requirement and every guard, so the pager, output-file and signature paths D025 closed stay closed. The abbreviation probe confirms that Git does not reopen them.
- **Shifting the burden to the intervenor:** the product 07 prefix that sessions are taught is now admitted, so no operator rescue is needed during a gate.
- **Drift to low performance:** the criteria text is unchanged, and no ceiling moved.
- **Escalation:** one repair pass and one verifier, with no new process.
- **Tragedy of the commons:** one product gate and one hook-heavy suite; no subagents.
- **Success to the successful:** the earlier green verification and review did not carry criterion 6. It was re-judged on the new subject.
- **Naive Interventionism:** a parser rewrite or a narrower list would change behavior without evidence of need; the one-line repair preserves every consumer.
- **NoOp:** leaving the repair unverified would block the order's close on a now-green subject.

Limits:

- Claude Code's own outcome on a hook timeout was not observed. It no longer arises for these inputs.
- The Copilot and Codex hosts were not exercised live.
- The `harness-fixtures` suite sits outside `npm test`'s required set, so its result above is separate evidence, not part of the product-gate row.
- No new live episode was run or claimed.

**Process cost:** entry 81831 tokens; handoff 9433296 tokens; source claude-transcript-message-usage

Both readings are `dispatch` scope from `node scripts/harness.mjs usage dbe28d46-5c7b-457f-a5c6-08be5921f49f`. The entry reading was observed at 2026-09-25T15:44:24.160Z. The handoff reading was observed at 2026-09-25T15:57:26.082Z (87 steps, 81 commands, 0 subagents of cap 20, exact-observed), after the judgment text was complete and before the result transition. Both include reused cached input. Reasoning tokens and dollar cost are unavailable from this counter, which means unknown, not zero. The largest waits were the product gate (298.78 s) and the `harness-fixtures` suite (190.91 s).
