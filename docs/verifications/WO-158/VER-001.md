# WO-158 VER-001 — independent verification: fail

**Subject:** The uncommitted `wo-158` worktree at HEAD `1f88850975c5aa7c2e8169c7267017d06d34a302`, after `resume: next` and before repair. The `verify` dispatch allocated this report and checkpoint `refs/dotln/checkpoint/WO-158/3`. I judged the nine criteria of [WO-158](../../work-orders/WO-158-lifecycle-off-ramps.md) against its cited sources, current implementation, fixtures and outputs. The product-gate code identity is `e070e7f260745b57503be53fa25c60e81dbecc891dc7694ff3ff111bd493977e`.

**Verifier:** Codex CLI 0.156.1, GPT-6 Sol, selected `ultra` (recorded `xhigh`, mode `subagents`). Two read-only agents each reviewed a group of criteria; the root verifier owns the judgment and report. The provider transport exposed no hard USD cap, so a USD 5 limit was unenforced.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.156.1","model":"gpt-6-sol","effort":"xhigh","mode":"subagents","raw":"ultra","source":"codex-session-readback"}

**Verdict:** **Fail.** Criteria 3, 4, 5 and 6 have reproduced defects. [D021](../../evidence/WO-158/decisions.md#wo-158-d021) records the bounded repair follow-up. The verifier changed no implementation.

## Evidence and checks

- The order's route fixture ran in the green `resume` suite, and work-order and derived-order fixtures ran in the green product gate at 2026-09-25T05:33:23.922Z. That gate passed 27 of 27 suites in 341.92 s and recorded the code identity above. The verifier's full `npm test` attempt on the same code identity passed 26 suites and timed out one unchanged WO-143 restart case at its 240 s test limit, with no assertion failure. That case passed alone in 161.67 s. The red attempt is retained, not represented as a pass.
- `npm run harness -- check` passed 31 generated surfaces. `node scripts/harness-context.mjs --check` measured both installed roots: executor 25,819/29,246 bytes; verifier 22,698/25,151; reviewer 23,780/24,576; release-close 14,649/16,384; planner 16,797/24,576; refuter 16,372 with no ceiling. `node scripts/authority-evidence.mjs --check`, `npm run evidence:artifact -- --check`, `npm run evidence:verification -- --check` and `node scripts/feedback-evidence.mjs --check` passed; [current.json](../../evidence/current.json) selects WO-158 revision 001 for all four editions.
- `npm run plan -- check`, `npm run publication:check`, `npm run release -- check-surfaces --local` and `git diff --check` passed. The lockfile changes only workspace versions and pins; no new dependency was found. `npm run work-orders -- index` ran after dispatch and `npm run meta` refreshed the D021 decisions index.
- `npm run test:docs` passed 21 of 21 suites in 26.41 s after this report and D021 were written. Its receipt, format, registrations, lineage, plan, edition and publication checks were green.

## Criterion judgments

**Criterion 1:** met. Four typed routes, phase refusals, actor-bearing events, status/current projections and legal/illegal fixture matrix are present. The defects below concern the safety of particular admitted inputs.

**Criterion 2:** met. `waive` and `withdraw` check a contained ignored capture and its stated SHA-256 before append; the executor-role waiver refusal and waived/unwaived verdict fixtures pass. The override-capture finding concerns a different route.

**Criterion 3:** unmet. The terminal withdrawal, index and dependency fixtures pass, but activation accepts an impossible reactivation date (F1).

**Criterion 4:** unmet. A report-path correction can redirect an unchanged passing verdict to different, unvalidated report bytes (F2).

**Criterion 5:** unmet. Two same-second override exits overwrite the first event's operator-word capture, invalidating its recorded hash (F3).

**Criterion 6:** unmet. An admitted `git log` can run a configured pager during a live gate (F4).

**Criterion 7:** met. Generated roles and product 07 name the routes, the harness check passes, and every measured cold-start role with a ceiling is within it.

**Criterion 8:** met. Authority, artifact-identity, verification and carried-feedback edition checks pass at the pointer in `current.json`; the carried feedback check reports no live episode.

**Criterion 9:** met. The 27-suite green gate at the current code identity, green isolated rerun of the verifier's timed-out unchanged case, 21-suite green document gate, clean whitespace check and version-only lockfile change support this criterion. The verifier's separate red gate remains a limit of this judgment.

## Findings for repair

### F1 — criterion 3: an impossible date reactivates a withdrawn order

**Severity:** medium control-integrity defect. **Expected:** activation from `withdrawn` requires a changed authority with a dated reactivation note on or after withdrawal. **Observed:** a disposable WO-099 fixture changed its authority to add `**Reactivation (9999-99-99):** impossible calendar date.`; `activate` exited 0 and status became `active`. The parser in [resume.mjs](../../../scripts/resume.mjs) checks the digit shape and compares date strings, without calendar validation. The fixture was removed. The existing tests cover an old and a valid date, not an impossible one. Repair is named in D021.

### F2 — criterion 4: correction rebinds a passing verdict to different evidence

**Severity:** high evidence-integrity defect. **Expected:** correcting a mistaken report location preserves the content the recorded verdict judged; a different report requires its own judgment. **Observed:** in a disposable fixture, `VER-001.md` passed with valid actor and cost lines, then `correct --set reportPath=docs/verifications/WO-099/sub/VER-001.md` exited 0 for a distinct file lacking those lines and declaring a criterion unmet. Status retained `latestVerdict: pass` while projecting the alternate path. The original and alternate SHA-256 values were `9f097c301a5c5ada3d31ec262a4ca73e6d6471c81cde85062891394fa8ca8720` and `5d476c851b07acebf6285e3d12eb48690409ec3760370b7db3793185a67d656b`; neither file's bytes changed. [resume.mjs](../../../scripts/resume.mjs) checks basename and containment but neither content identity nor the original report validations; [control.mjs](../../../scripts/lib/control.mjs) then projects the new path. This contradicts D008's claim that a verdict never rests on another report. Repair is named in D021.

### F3 — criterion 5: same-second override exits replace captured words

**Severity:** high operator-provenance defect. **Expected:** each `OperatorOverrideRecorded` event continues to name the words whose hash it stored. **Observed:** a disposable generated-hook fixture froze the clock within one second and completed two override/off cycles. Both successful events (ordinals 2 and 3) named the same ignored capture path. The second write replaced the first words: the first event's `captureHash` no longer matched the file, while the second did. [harness-host.ts](../../../packages/skeleton/src/harness-host.ts) forms a filename from date, session prefix and HHMMSS, then writes with overwrite semantics. The existing hook fixture covers one exit. Repair is named in D021.

### F4 — criterion 6: admitted Git read executes an unlisted pager

**Severity:** high live-gate boundary defect. **Expected:** a listed read during `npm test` cannot run an unlisted program that may write gate inputs. **Observed:** a disposable live-gate hook fixture admitted `git log -1` with `core.pager` configured. In a PTY, after removing this host's `GIT_PAGER=cat` override, Git ran that pager and wrote a harmless marker in system temp. [harness-command.ts](../../../packages/skeleton/src/harness-command.ts) admits plain `git log`; [harness-host.ts](../../../packages/skeleton/src/harness-host.ts) rejects several configured Git programs but omits pager settings and environment. No gate input was changed in the probe. Repair is named in D021.

## Judgment and limits

The intended outcome is less operator rescue through typed events and truthful projections. F1 and F2 let an event pass while its intended condition is false; F3 loses captured authority; F4 crosses the live-gate read boundary. Rule beating and drift to low performance would result from treating green fixtures as proof of those missing behaviors. Policy resistance matters because correction and waiver must agree with the verdict checks. Escalation and tragedy of the commons favor four focused regressions and one repair pass over a broad route redesign. Success to the successful does not make the current classifier or fold authoritative beyond their evidence. Shifting the burden to the intervenor and seeking the wrong goal would leave an operator to discover the damaged record later. Under Naive Interventionism, repair should preserve valid historical events and current read admissions while closing only the reproduced gaps. NoOp retains the false records and pager execution path; it does not meet the order's objective. The verifier made no implementation edit, so a fresh VER must judge any repair.

**Process cost:** entry 133,190 tokens; handoff 6,282,289 tokens; source codex-transcript-counter

The entry counter is the dispatch-scoped readback at 2026-09-25T05:42:29.727Z; the handoff counter is the latest available dispatch-scoped readback before the result transition at 05:59:05.236Z. USD cost is unavailable. The root explicitly admitted two read-only subagents, zero descendants, against cap 20; the Codex harness observed zero attributable child admissions, so its unobserved remainder is unknown.
