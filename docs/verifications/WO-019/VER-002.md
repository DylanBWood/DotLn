# WO-019 verification report VER-002

**Subject:** branch `wo-019`, uncommitted working tree, 2026-09-03. `HEAD` and
`origin/main` are both `5d20c6274475a85e501e9842024e3901a607d4c2` (the WO-025
merge, published as annotated `v0.3.3`); the branch still carries no local
commits, so the entire subject is the dirty tree — 27 modified tracked paths
(2010 insertions, 192 deletions) plus the untracked immutable `VER-001.md`.
Three of the changed lines are this order's own `RepairRequested`,
`RepairCompleted`, and this verification's `VerificationRequested` appends, plus
the regenerated projection.

**Authority:** `docs/work-orders/WO-019-effort-truth.md` — nine acceptance
criteria, a five-item evidence gate, a nine-clause scope fence, a dated
implementation-clarifications block, an explicit non-goal list, a direct-draft
provenance note, and now two ideation breakout receipts whose §Required review
sections impose named duties on this report.

**Prior reports:** `docs/verifications/WO-019/VER-001.md` — **fail**, one
blocking finding (F1, acceptance criterion 7) with a six-item bounded repair
checklist confined to `scripts/test-resume.sh`, and a stated re-verification
scope: criterion 7 plus green `npm test` plus confirmation that no production
file changed, with criteria 1–6 and 8 returning to scope only if the repair
touched `scripts/resume.mjs`.

**Verifier:** Claude Opus 5 (1M context) in Claude Code 2.1.259, **effort
`max`**, macOS 15.6 (darwin 24.6.0, build 24G84, arm64), node v22.2.0, npm
10.8.0, git 2.55.0, Prettier 3.9.6. WO-019 permits any capable model and
requires verifier effort `xhigh+`; `max` is one rung above that minimum. The
value is **self-reported**, and this session makes the epistemic limit unusually
concrete: the harness surfaced two successive effort selections to me, first
`ultracode` and then `max`, the later of which is operative and is the one I
attest. `ultracode` is not one of the five documented ladder values; had it been
operative, the honest attestation would have been `unknown (raw: ultracode)`,
which the shipped ladder check would have **refused** against `verifier xhigh+`.
I record that here rather than quietly resolving it, because a source-labeled
claim about a selection — not an observed effective setting — is exactly what
this order says the log may contain. Claude Code 2.1.259 exposes no
effective-effort readback, as this order's own probe records, so
`harness-readback` is unavailable and would be refused by the mechanism under
test. I ran every command in this report first-hand from the work-order
worktree; nothing is quoted out of the executor's transcript. No subagents were
used.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.259","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}

**Verdict: WO-019 passes.** The repair is exactly the bounded checklist F1
asked for and nothing else: 24 added lines in `scripts/test-resume.sh`, and
`scripts/resume.mjs` is **byte-identical** to the VER-001 subject
(`git diff --quiet refs/dotln/checkpoint/WO-019/4 -- scripts/resume.mjs` exits
0). All six mutants that survived VER-001's sweep are now killed, including the
one that mattered on the merits — an unconstrained `--source` label. `npm test`
is green and `git diff --check` is clean. The repair window also carried an
operator-opened ideation breakout, which added a second receipt to the work
order and promoted three product surfaces; that addition is purely additive
prose that lowers no criterion, and I screened it under §Ideation breakout.

## Self-referential instruments — mandatory disclosure

07-execution-guide.md §Discipline and the order's own **Effort** line require me
to name the instruments that are part of the deliverable I am judging.

1. **The transition that dispatched me is the deliverable.** `npm run resume --
   verify` allocated this report's path from the same `scripts/resume.mjs` the
   order rewrites, and the `verification-result` command that records this
   verdict is the new actor-gated code path.
2. **My own attestation is produced by the mechanism under test.** The header
   above is compared byte-for-byte by `requireReportActor` — code this report
   evaluates. A successful append is evidence about the subject, not
   independent of it.
3. **I am also re-verifying my own prior report.** VER-001 was mine. The
   specific risk is that I would accept the repair because it matches the
   checklist I wrote. I addressed this by not scoring the checklist: every
   claim below rests on mutants I constructed against copies of the shipped
   script in disposable fixtures under `$TMPDIR`, and I extended the sweep from
   VER-001's 29 mutants to 68 single mutants plus 4 multi-guard combinations,
   deliberately probing surfaces my earlier report had not touched. Two of the
   three residual clusters below were found by that extension, not by the
   checklist.
4. **What I could not close.** The order's own `WorkOrderActivated` predates
   the shipped parser and carries no `effortDeclarationValidated` marker. That
   is designed for — WO-019 is hard-coded as the strict migration boundary and
   receives no legacy fallback, which I re-confirmed against a synthetic
   pre-WO-019 `WO-019` activation — but the order's activation was still not
   itself validated by the mechanism it ships. Criterion 8's full inventory
   remains the final reviewer's duty.

## Evidence gate

| Gate item                                             | Result                                                                                                                                            |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test-resume.sh` output                               | green standalone and inside `npm test`; `resume tests passed`                                                                                     |
| probe transcript per harness with version             | `docs/discovery/environment.md` §WO-019 addendum and `environment.json` `effortReadbackProbe`; re-read first-hand this session                     |
| `current.md` before and after one attested transition  | the projection carries `- Latest attestation:` and `- Effort drift: xhigh -> max` from three real attested completions; fixture before/after re-run |
| refusal transcripts                                   | reproduced first-hand; see §Criterion 7 and §Shipped-behavior probes                                                                              |
| the order's own `resume.jsonl` events showing actors   | three completion events, all with canonical-key-order actors; enumerated below                                                                    |

`npm test` from the worktree: exit 0, all ten shell/Node suites reported
passed, compiled suite `# tests 78 / # pass 78 / # fail 0`. `git diff --check`
exits 0. `node scripts/release.mjs check-surfaces` reports four PASS lines
including `release-block: observed v0.3.4; expected v0.3.4 (work-order target
v0.3.4; latest published v0.3.3)`. `package.json` and `package-lock.json` are
unchanged, so no dependency was added, and no `.claude/` or personal-settings
path appears in `git status --porcelain -uall`.

## Criterion 7 — the blocking finding is closed

F1 required six things, all in `scripts/test-resume.sh`. The repair delivered
all six, and I verified each by mutation rather than by reading the diff.

| F1 checklist item                                  | Delivered as                                                                        | Mutant that proves it is load-bearing                             |
| -------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| `final-review-result` refusal, no header            | `FINAL-002.md` written bare before the first `final-review-result`                  | deleting the `final-review` `requireReportActor(...)` call — killed |
| `final-review-result` refusal, header ≠ flags       | `FINAL-003.md` carrying the `human` header, called with the `codex-cli` actor       | same call site, plus `lines[0] !== expectedHeader` — both killed    |
| out-of-vocabulary `--source`                        | `--source observed` on `implementation-ready`                                       | deleting the `--source` vocabulary check — killed                   |
| out-of-vocabulary and malformed `--harness`         | `--harness made-up` and `--harness other:`                                          | deleting the check, and loosening the `other:` regex — both killed  |
| empty and flag-shaped actor values                  | `--model ''` and `--model --typo`                                                   | deleting `!value.trim()`, deleting `value.startsWith("--")` — killed |
| blank line between Model and Effort still activates | the `WO-099` fixture declaration now separates the two fields with a blank line     | deleting the blank-line skip — killed                              |

Each new case goes through the existing `assert_refusal` helper, which asserts a
non-zero exit, the expected message, no leaked stack trace, **and an unchanged
event count** — the "no event appended" assertion criterion 7 names.

One thing the sixth item could have cost, and did not: converting the fixture
from adjacent fields to blank-line-separated fields might have removed positive
coverage of the adjacent form the scope fence also promises. It did not. I
built the inverse mutant — making one blank line mandatory between the complete
`**Model:**` and `**Effort:**` fields — and the suite kills it, because the
malformed-Effort fixture would then refuse with the adjacency message instead of
the malformed-Effort message.

Criterion 7's other named duties hold: the suite covers criteria 1–6 in the
existing non-git fixture, asserts in a Node block that every `WO-099` completion
event carries an `actor` and that no event's `schemaVersion` changed, and
carries four drift assertions including the `unknown (raw: …)` rendering and the
distinct-pair rule.

## Mutation evidence

Three sweeps, 68 single mutants, each verified to apply exactly once, to leave
valid JavaScript, and to differ from the shipped file. 50 killed, 18 survived.
Four additional multi-guard mutants were run against `parseActor`.

**Killed (50)** — every rule VER-001 killed, re-run unchanged against the new
fixture, plus the six it did not: the ladder check and its `unknown` handling,
both report-header call sites, header equality, exactly-one-header, drift
rendering and dedupe, raw-pair normalization, the WO-019 boundary, the
control-character guard, the `raw` label, the recognized-effort evidence gate,
version scoping of that evidence, the `harness-readback` gate and its
`observed` classification, the `documented locally` selector class, the
`effortDeclarationValidated` marker, the `next` briefing, header adjacency in
both directions, the header region, duplicate fields, the field-paragraph
terminator, verification report containment and `realpathSync` containment,
activation validation, canonical key order, both projection lines, the
attestation source label, the `none` attestation case, the effort-pairs reset,
`VER`/`FINAL` id allocation from disk, the reopened-fix branch of
`legalActions`, the failure-source fields on `fix`, the ladder ordering, the
`+`-suffix strip, `renderEffort`, actor omission from `RepairCompleted` and
`FinalReviewCompleted`, the `--source` and `--harness` vocabularies, the
`other:` label shape, the empty-value and flag-shaped-value guards, and the
malformed-Model check.

**Survived (18)**, every one classified and none blocking:

- **Equivalent — no reachable behavior change (5).** Removing
  `actor.effort === "unknown" ||` from `validateEffort` (`indexOf("unknown")` is
  `-1`, so the comparison still refuses); removing the `effortDeclarations` Set
  membership check (the regex alternation encodes the identical six tokens); and
  each of the three `parseActor` structural guards taken alone — the unknown-flag
  check, the duplicate-flag check, and the exact-argument-count check. Those
  three are mutually redundant: with the count check intact, five pairs must be
  five distinct allowed flags, and with the other two intact the count is forced
  to ten.
- **Unreachable through the CLI (2).** The `fold` work-order guard on
  attestation, and `fold`'s actor field type checks. Both defend a hand-edited
  control log; `parseActor` gates every write the tool makes.
- **Pre-existing code outside this order's surface (4).** The final-review
  report containment guard and its message, the `activate` basename/id-prefix
  check, and the `verification-result` phase guard — all unchanged by WO-019 and
  present at `HEAD`. Their coverage is not criterion 7's subject; I note the
  final-review containment gap because its verification-side twin *is* covered,
  which makes the asymmetry look accidental.
- **New surface, behavior-changing, untested — but incapable of a forged
  acceptance (7).** An odd argument count would print an internal `TypeError`
  instead of the usage string (it still refuses and still appends nothing); a
  non-`observed` probe `version.classification` is never exercised because the
  fixture records both harnesses as `observed`; the declaration grammar is
  exercised only for `high+` and `any`, so dropping `xhigh+` from the level
  alternation goes unnoticed; the malformed-title guard, the `(?:\s|$)` word
  boundary on field labels, the column-one boundary of the actor header, and the
  literal role names `executor`/`verifier`/`reviewer` in the Effort grammar all
  lack a case.

**Multi-guard mutants (4, all survived; 2 exploitable).** Removing any two of
the three `parseActor` structural guards leaves a green suite. Two of the three
pairs are genuinely exploitable: without the unknown-flag and count checks a
mistyped `--bogus x` is silently swallowed, and without the duplicate-flag and
count checks a repeated `--model honest … --model forged` records the last
value. The third pair is another equivalent mutant. I am not failing the order
over this — no *single* guard removal produces a forged acceptance, which is the
bar VER-001 set and applied when it exempted its own two equivalent survivors —
but §Observations carries it forward, because WO-018 refactors this exact file.

## Shipped-behavior probes

Because several guards are untested, I confirmed the shipped behavior by hand
rather than inferring it. Against a disposable fixture, all four of these refuse
with the usage string and append zero events: an unknown `--bogus` flag, a
duplicated `--model` (no last-wins), an odd argument count, and a duplicated
`--source` attempting to launder `self-reported` into `operator-attested`.
Against the declaration parser: a wrong role name (`planner`) and reordered
roles both refuse as malformed; `**Model:**glued` with no separating space
refuses as missing; a file with no `# ` title refuses as a malformed header; and
`xhigh+`, adjacent fields, one blank line, and two blank lines all activate.

I also re-confirmed VER-001's Observation 1 first-hand rather than restating it,
because it constrains the very next session. Exactly one column-one
`**Actor attestation:**` line is required: a report that quotes the canonical
example at column one inside a fenced block **refuses**, while the same quote
indented two spaces or blockquoted is accepted, and a report whose only header
is indented refuses. This report is written to that rule.

## Criterion 8 — the order's own transitions

`scripts/resume.mjs` is byte-identical to the VER-001 subject, so criteria 1–6
did not return to scope under VER-001's stated re-verification rule. I
nevertheless re-ran the declaration parse across the whole real corpus and the
`next` briefing against the amended authority, since both work-order files and
the fixture changed. Twelve orders activate (WO-016 through WO-026 and WO-109);
twenty-one refuse with `missing **Effort:** line`, the intended forward-only
boundary. `resume -- next` against the real, now-longer WO-019 still prints the
complete multi-line `**Model:**` and `**Effort:**` source fields verbatim and
appends nothing — the appended second ideation receipt sits behind a `##`
boundary and introduces no duplicate metadata label.

The order's three completion events, read from the real log:

| Event                  | Actor                                                                | Role     | Declared | Evidence for the value                                              |
| ---------------------- | -------------------------------------------------------------------- | -------- | -------- | --------------------------------------------------------------------- |
| `ImplementationReady`  | codex-cli 0.152.1, gpt-5.6-sol, `xhigh`, self-reported               | executor | `xhigh+` | `persistedEffortSelector` recorded `observed` with value `xhigh`      |
| `VerificationCompleted` | claude-code 2.1.259, claude-opus-5[1m], `max`, self-reported          | verifier | `xhigh+` | `sessionEffortSelector` `documented locally`, values include `max`    |
| `RepairCompleted`      | codex-cli 0.152.1, gpt-5.6-sol, `xhigh`, self-reported               | executor | `xhigh+` | same persisted selector                                               |

All three serialize in the canonical `harness, harnessVersion, model, effort,
source` order that only the shipped `parseActor` produces, all carry
`schemaVersion: 1`, and each satisfies its declared minimum. The projection
reads `- Effort drift: xhigh -> max`, which is the correct distinct-pair set:
the executor's repeat of `xhigh` is deduplicated exactly as criterion 5
requires.

## Ideation breakout — 2026-09-03 §Required review

The repair window carried an operator-opened ideation breakout. Its effect on
the subject is one appended `##` receipt in the work order plus 47 ledger lines,
19 lines in `00-vision.md`, and 40 lines in `05-pattern-library.md`. **No
acceptance criterion, objective, scope-fence clause, deliverable, evidence-gate
item, non-goal, version, release classification, or dependency was touched** —
the work-order diff across the repair is purely additive after the non-goals
paragraph.

Discharging the named duties:

- **Clean-room rewriting.** Both ignored intake captures exist and are
  gitignored; only `.gitkeep` files under `docs/intake/` are tracked. I ran a
  verbatim-overlap screen between each capture and the committed added lines:
  **zero shared 8-, 10-, or 12-word shingles** for both captures, and zero even
  at 5 and 6 words for the 2026-09-03 capture. The 2026-09-02 capture shares
  exactly two 6-word sequences with committed prose: its own filename, which the
  receipt is required to cite, and the phrase "do not enter remote model
  prompts", which sits inside an otherwise fully rewritten ledger sentence and
  is the ordinary technical phrasing of that constraint. This is synthesis, not
  lifting.
- **No recollection asserted as fact.** The ledger labels the scientific and
  fictional references "operator recollections and familiar lenses" and states
  that "no literal physics or canon claim is load-bearing"; the pattern entry
  says the viewing "does not make a physical claim or import physics as the
  implementation"; `00-vision.md` files the nominated series as "nominated, not
  yet mined" and defers every ontology claim until "an authorized source is
  reviewed with provenance".
- **Four response shapes open and distinct.** Band-gated, cumulative,
  bounded-history, and self-governed are separately defined, and the prose
  states they "may later compose" and "are not assumed to exhaust the space".
- **No conflation, and precedence retained.** Source, provenance, `Voice`,
  urgency, evidence, trust, timing, and policy result are named as separate
  facts; "repetition does not become truth, message force does not create
  authority, and aggregation cannot weaken a hard constraint"; the self-governed
  shape "can never bypass hard constraints, authority, consent, or evidence
  obligations"; and any number of lower-band inputs stays distinct from one
  qualifying urgent signal.
- **No identity drift or fake precision.** The candidate is explicitly "not the
  existing `AttentionPolicy`", it is a versioned policy rather than "a fixed
  personality score", and it declines to "pretend to measure hidden human
  judgment". The cumulative shape names the operator's democratic intuition as
  "a felt shape, not a guarantee of fairness".
- **Fictional vocabulary stays optional source content.** "No series-specific
  noun or rule becomes kernel ontology"; the ledger entry is marked `preserved`,
  not `adopted`.
- **Nothing presented as implemented; no acceptance expansion.** Both the
  ledger and pattern sections are headed as candidates, the receipt's own scope
  paragraph enumerates what is *not* built, and the work-order diff confirms it.
- **Ledger discipline.** The new section is inserted newest-first above the
  2026-09-02 entry with 47 insertions and zero deletions; nothing earlier was
  rewritten.

Forbidden-term screen over all 2010 added lines: no commercial tracker or ALM
product name, no predecessor-system reference (the single `v1` hit is
`schema-v1`, the control-log schema version), no managed-host, corporate-gateway,
or employer-policy description beyond the repo's own clean-room screening
language, and no credential, endpoint, private identifier, or address shape. The
one URL is the project's own public GitHub Releases page in `README.md`,
pre-existing.

## Observations — not defects, for the final reviewer

1. **Two of three `parseActor` structural guards can be removed together
   without turning the suite red.** Each is individually redundant, so nothing
   is wrong today and the shipped script refuses all four probe cases correctly.
   The reason to carry it forward is dated and specific: **WO-018 refactors
   `scripts/resume.mjs`**, and a rewrite of `parseActor` is exactly the change
   that could drop two guards at once. Two `assert_refusal` lines — one unknown
   flag, one duplicated flag — would close it permanently. I did not require
   them here because no single-guard mutant produces a forged acceptance, which
   is the bar VER-001 set.
2. **The reviewer's header can still collide with its quoting duty.** Confirmed
   first-hand above. `docs/final-reviews/README.md` asks the reviewer to list
   each verifier's report header beside its control actor, and there are now
   **two** verifier headers to list. FINAL-001 must indent or blockquote every
   quoted header; only its own may start at column one. The column-one boundary
   itself has no test, so a future refactor could silently flip it.
3. **The declaration grammar is exercised at only two of its six levels.** The
   fixture uses `high+` and `any`; `low+`, `medium+`, `xhigh+`, and `max+` never
   appear in a test declaration, and neither does a wrong or reordered role name.
   The shipped parser handles all of them correctly — I probed each — but WO-019's
   own `executor xhigh+` line depends on a token no test pins.
4. **The final-review report containment check has no refusal case while its
   verification twin does.** Pre-existing code, so outside this order, but the
   asymmetry reads as accidental and is one line to fix whenever
   `scripts/test-resume.sh` is next touched.
5. **The committed probe still pins one personal-settings value.**
   `environment.json` records codex-cli's persisted `model_reasoning_effort` as
   `xhigh` — only the level, as the order permits. It is load-bearing: it is
   what makes both of this order's executor attestations acceptable, and it goes
   stale the moment the operator changes that setting or upgrades the harness.
6. **`"WO-019"` remains a permanent string literal in production code.** As
   specified by the scope fence, inert after close, and re-confirmed by the
   synthetic boundary fixture. Noting it so a WO-018 refactor does not mistake
   it for an accident.
7. **This verifier verified its own prior report.** VER-001 and VER-002 share an
   author. §Self-referential instruments item 3 states how I tried to break that
   circularity; whether it was broken well enough is a fair question for the
   final reviewer, and an independent reviewer harness would settle it.

## Re-verification scope

None. The order passes as it stands. If any later change touches
`scripts/resume.mjs` before final review, criteria 1 through 8 return to scope
for a fresh `VER-NNN`; a documentation-only change does not.
