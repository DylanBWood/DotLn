# WO-013 verification report VER-001

**Subject:** branch `wo-013`, uncommitted working tree, 2026-09-02. `HEAD` and
`origin/main` are both `df67601751cf2946f9e2b53dcb8650abb2e3d4fd`; the branch
carries no local commits, so the entire subject is the dirty tree — twelve
modified tracked paths (115 insertions, 55 deletions) plus two new untracked
files, `scripts/test-temp-root.sh` (233 lines) and
`scripts/test-fixture-temp-root.sh` (361 lines). Three of the modified lines are
this verification's own `VerificationRequested` append and its regenerated
projection.

**Authority:** `docs/work-orders/WO-013-portable-fixture-temp-roots.md` — ten
acceptance criteria, a seven-item evidence gate, a six-clause scope fence, and an
explicit non-goal list.

**Prior reports:** none. This is the first independent verification of WO-013.

**Verifier:** Claude Opus 5 (1M context) in Claude Code 2.1.258, **effort `max`**,
macOS 15.6 (darwin 24.6.0, build 24G84, arm64), bash 3.2.57(1)-release
(`/bin/bash`, the PATH bash), node v22.2.0, npm 10.8.0, git 2.55.0, Prettier
3.9.6. WO-013 permits any capable model. I ran every command in this report
first-hand from the work-order worktree; nothing is quoted out of the executor's
transcript. No subagents were used.

**Verdict: WO-013 passes.** Nine of ten acceptance criteria are confirmed
first-hand and in full. AC8 is confirmed for its in-sandbox half — the headline
requirement — and its outside-sandbox half could not be reproduced by this
session because harness policy disables unsandboxed execution; that is a
disclosed limitation of the verification, not an observed defect, and it is
detailed below. The defect the order was cut to fix is gone: an unchanged
`npm test` now exits 0 inside the harness sandbox with no relocation, no source
mirror, and no approval prompt. Two governance observations are recorded for the
final reviewer; neither is a behavioral or evidence defect.

## Self-referential instruments — mandatory disclosure

07-execution-guide.md §Discipline requires me to name the instruments that are
themselves part of the deliverable I am judging.

1. **`scripts/test-fixture-temp-root.sh` is both the deliverable and the
   evidence** for acceptance criteria 2–6. Its 33 `PASS` lines are the
   executor's proof that the base matrix, the cleanup guard, and the traps
   behave. Trusting them would be verifying the instrument with itself.
   Mitigation: every criterion it covers, I re-derived with probes I wrote,
   which either source `scripts/test-temp-root.sh` directly or drive the six
   real suites end-to-end. **No probe in the "Independent re-derivation" section
   below invokes `test-fixture-temp-root.sh`.** Where the two disagree I report
   my own result.
2. **`scripts/test-temp-root.sh` is the shared safety logic** and is exercised by
   every other suite, so a green `npm test` is partly the new helper certifying
   itself. Mitigation: probes B and D drive it into its refusal paths with
   sentinel files planted outside the target, so a silent over-broad delete would
   show as a missing sentinel rather than as a passing test.
3. **`scripts/resume.mjs` allocated this report's `VER-001` path** and appended
   the `VerificationRequested` event. It is *not* under judgment here: AC9
   requires it byte-identical to `origin/main` and I confirmed that with `cmp`
   before using it. It is also a fail-loud instrument — an append-only log with a
   regenerated projection — which the guide names as safe to dogfood.

## Dispatch and subject integrity

`docs/control/current.md` named WO-013 at phase `ready-to-verify` with `verify`
as the only legal action. `npm run resume -- verify` allocated
`docs/verifications/WO-013/VER-001.md` and moved the phase to `verifying`;
checkpoint `b22064a543cf481301b4b8764c1029abc13c9fea`
(`refs/dotln/checkpoint/WO-013/3`). The command ran inside the ordinary sandbox
and needed no approval escalation. `docs/control/resume.jsonl` is append-only
across the subject: the diff adds exactly three lines
(`WorkOrderActivated`, `ImplementationReady`, `VerificationRequested`) and
rewrites none.

I ran no destructive git command in this worktree. Nothing was restored from a
checkpoint; no rollback was needed. `git status --porcelain` is identical before
and after the entire verification apart from this report and the control append.

## Evidence gate

Every line below is output I produced in this session.

### Before/after reproduction in a restricted writable temporary root

The harness sandbox permits writes only within its session-supplied `$TMPDIR`
(`/tmp/claude-501`, physically `/private/tmp/claude-501`) and the working
directory. The exact pre-change operation still fails there:

```text
$ mktemp -d /private/tmp/dotln-publication-test.XXXXXX
mktemp: mkdtemp failed on /private/tmp/dotln-publication-test.9SKGfE: Operation not permitted
exit: 1
```

That is the WO-013 §Observed problem verbatim, reproduced live — the defect is
real and still reproducible at this harness version. With the change in place,
the unchanged `npm test` from this worktree:

```text
$ npm test
All matched files use Prettier code style!
fixture temp-root tests passed
publication checker tests passed
backup-intake tests passed
resume tests passed
checkpoint tests passed
worktree tests passed
release tests passed
# tests 77
# pass 77
# fail 0
npm test exit status: 0
```

Three full `npm test` runs, all exit 0, all 77/77. Wall clock ≈ 37 s. No
permission prompt appeared, no persistent allow rule was accepted, no mirror or
relocated copy was used, and the `package.json` `test` chain was not edited for
the run.

### Residue check bracketing the full run

Same command shape before and after the final `npm test`, against the resolved
base `/private/tmp/claude-501`:

```text
BEFORE: dotln-final-mirror, dotln-probe.D6dhna, dotln-resume-test.K7YNMg   (3)
AFTER:  dotln-final-mirror, dotln-probe.D6dhna, dotln-resume-test.K7YNMg   (3)
```

Identical. The three entries are pre-existing residue from earlier sessions
(dated 2026-09-01), and per the work order's evidence gate they are **recorded as
an observation, not deleted**. `dotlntmp` also sits beneath that base and does
not match `dotln-*`. Separately, 74 `dotln-*` entries remain under
`/private/tmp` from sessions predating this order; I touched none of them. This
run created and removed its own fixture roots and left nothing.

### `git diff --check`

Clean — no whitespace errors, no conflict markers.

### Harness deviation, disclosed — and its converse

`docs/verifications/WO-006/VER-001.md`, `VER-002.md`, `VER-003.md`, and
`FINAL-001.md` each disclosed running the suites from a relocated mirror because
of this defect. **This report discloses no such deviation.** The suites ran in
place, from the work-order worktree, on the first attempt. That is the
observable state change WO-013 was cut to produce.

## Independent re-derivation

Seven probes, all written by me, none using `test-fixture-temp-root.sh`.

### Probe A — the TMPDIR matrix through a real suite (14/14)

Drove `scripts/test-checkpoint.sh` under each base, with a sentinel file planted
where an over-broad delete would destroy it. `ok` = suite exit 0; `refuse` =
non-zero exit with the base named on stderr.

| Base                             | Expected | Result | Message                                                              |
| -------------------------------- | -------- | ------ | -------------------------------------------------------------------- |
| ordinary directory               | ok       | rc=0   | —                                                                     |
| directory with spaces            | ok       | rc=0   | —                                                                     |
| symlink to a directory           | ok       | rc=0   | —                                                                     |
| trailing `////`                  | ok       | rc=0   | —                                                                     |
| nonexistent                      | refuse   | rc=1   | `rejected temporary base <path>/nope: not an existing directory`      |
| a regular file                   | refuse   | rc=1   | `rejected temporary base <path>/not-a-dir: not an existing directory` |
| unwritable (mode 500)            | refuse   | rc=1   | `rejected temporary base <path>/unwritable: directory is not writable`|
| `/`                              | refuse   | rc=1   | `rejected temporary base /: filesystem root is not a fixture base`    |
| `.`                              | refuse   | rc=1   | `rejected temporary base .: path must be absolute`                    |
| `..`                             | refuse   | rc=1   | `rejected temporary base ..: path must be absolute`                   |
| `relative-base`                  | refuse   | rc=1   | `rejected temporary base relative-base: path must be absolute`        |
| `<path>/seg/../target`           | refuse   | rc=1   | `rejected temporary base <...>: path is not a safe fixture base`      |
| `TMPDIR=""`                      | refuse   | rc=1   | `rejected temporary base /tmp: directory is not writable`             |
| `TMPDIR` unset                   | refuse   | rc=1   | `rejected temporary base /tmp: directory is not writable`             |

Every sentinel survived. No `dotln-*` residue anywhere under the probe lab.

The last two rows are worth reading carefully: with `TMPDIR` unset or empty the
suites **do** fall back to `/tmp` as AC3 specifies, and inside this sandbox that
resolved base is not writable, so they fail loudly and name it — which is exactly
the second half of AC3. The fallback is not silently swallowed.

### Probe B — the cleanup guard, driven directly (14/14)

Sourced `scripts/test-temp-root.sh` and called the guard myself, with a
`BASE-SENTINEL` in the base and a `keep` file inside every decoy:

- created root sits physically beneath the resolved base (`/private/tmp/...`
  even though I passed the `/tmp/...` symlinked form) — AC2's "resolved form";
- ownership marker written;
- refuses an empty `root`, the base itself, a mismatched prefix, a root under a
  different parent, an unowned same-shape sibling
  (`dotln-probe-test.BBBBBB`, correct name, never created by this process), and
  a symlink standing where the owned root was;
- every decoy and sentinel intact after all six refusals;
- the one genuinely owned root, with nested content, removed exactly;
- the unowned same-shape sibling still present at the end.

That last pair is the ledger rule "tests clean only artifacts they created inside
owned fixtures" made executable: the guard keys on provenance (an in-process
registry plus a per-root ownership token), not on filename familiarity. A
correctly named stranger is refused.

### Probe C2 — INT and TERM on suites the fixture never touches (4/4)

`test-fixture-temp-root.sh` exercises signals against `test-release.sh` only. I
raced four *other* suites: started each with a controlled `TMPDIR`, polled until
its `dotln-<suite>-test.??????` root appeared, then signalled.

| Suite                | Signal | Exit | Residue | Sentinel |
| -------------------- | ------ | ---- | ------- | -------- |
| `test-worktree`      | INT    | 130  | none    | intact   |
| `test-checkpoint`    | INT    | 130  | none    | intact   |
| `test-backup-intake` | INT    | 130  | none    | intact   |
| `test-resume`        | INT    | 130  | none    | intact   |
| `test-worktree`      | TERM   | 143  | none    | intact   |
| `test-checkpoint`    | TERM   | 143  | none    | intact   |
| `test-publication`   | TERM   | 143  | none    | intact   |

**A correction to my own first attempt.** My initial INT probe reported exit 0
and I nearly wrote it up as a defect. It was my harness, not the deliverable: a
background job started in a non-interactive shell has SIGINT set to `SIG_IGN`, so
the suites ran to completion and exited 0 legitimately. Re-running with `set -m`
(job control, default signal dispositions) produced the 130s above. Recorded
because a later reader reproducing AC6 will hit the same trap.

### Probe D — AC4's named behavior change, end-to-end (2/2)

The order requires `test-backup-intake.sh` and `test-publication.sh` — which
previously printed a refusal and still exited 0 — to fail closed. I tripped the
guard inside the running suites by moving the base out from under them and
planting a same-named decoy:

```text
test-backup-intake   exit=1  error: refusing fixture-root cleanup without its ownership marker: .../dotln-intake-backup-test.r8H5Dt
test-publication     exit=1  error: refusing unsafe fixture-root cleanup target .../dotln-publication-test.l7oiBc
```

Both decoys intact; both genuinely owned roots preserved under the moved base.
Non-zero exit, nothing removed. Confirmed against the `origin/main` source: the
old `cleanup()` in these two suites printed to stderr and returned, and an EXIT
trap's return value does not change the script's exit status — so the pre-change
behavior really was exit 0. The behavior change is real, in scope, and named in
the work order.

### Probe E — quoting, past the required "spaces" case (11/11)

`test-checkpoint.sh` against bases named with `*`, `?`, `[`, `]`, `$`, backticks,
single and double quotes, `;`, `&`, non-ASCII (`ünïcode`, `空`) — and an embedded
**newline**. All eleven exit 0, no residue, sentinel intact. The scope clause
asked for "quote every expansion"; it holds under adversarial input well beyond
what the criterion required.

### Probe F — symlinked base over a directory of precious files (pass)

`TMPDIR` pointed at a symlink whose target held six files and a nested
subdirectory. After a full `test-checkpoint.sh` run, `find` output before and
after is byte-identical, and no `dotln-*` residue remains. An adversarial base
cannot redirect cleanup outside the generated directory (AC5).

### Probe G — every suite × every base (49/49)

The matrix above, generalized to all seven scripts:

```text
suite                   spaces      symlink     trailing    missing     notadir     unwritable  unset
test-fixture-temp-root  ok(0)       ok(0)       ok(0)       ok(1)       ok(1)       ok(1)       ok(1)
test-publication        ok(0)       ok(0)       ok(0)       ok(1)       ok(1)       ok(1)       ok(1)
test-backup-intake      ok(0)       ok(0)       ok(0)       ok(1)       ok(1)       ok(1)       ok(1)
test-resume             ok(0)       ok(0)       ok(0)       ok(1)       ok(1)       ok(1)       ok(1)
test-checkpoint         ok(0)       ok(0)       ok(0)       ok(1)       ok(1)       ok(1)       ok(1)
test-worktree           ok(0)       ok(0)       ok(0)       ok(1)       ok(1)       ok(1)       ok(1)
test-release            ok(0)       ok(0)       ok(0)       ok(1)       ok(1)       ok(1)       ok(1)

residue anywhere under lab: []
```

AC2 and AC3 hold for *each* suite, not just a sampled one.

### Flake check

`test-fixture-temp-root.sh` run four consecutive times standalone: four passes,
33 `PASS` lines each. Its signal fixtures carry a 15 s deadline and a 10 ms poll,
which is the only timing-sensitive machinery in the deliverable; it did not drift
across seven total executions (four standalone plus three inside `npm test`).

### Scope and byte-identity

`git diff -U0` hunk ranges for the six modified suites are confined to the
preamble in every case — lines 5–20, the base-resolution/`mktemp`/guard/trap
region and nothing else:

```text
test-publication    @@ -5 +5,7 @@   @@ -13,8 +18,0 @@
test-backup-intake  @@ -5 +5,7 @@   @@ -10,8 +15,0 @@
test-resume         @@ -5 +5,7 @@   @@ -7,2 +12,0 @@
test-checkpoint     @@ -5 +5,7 @@   @@ -8,2 +13,0 @@
test-worktree       @@ -5,3 +5,7 @@
test-release        @@ -5,3 +5,7 @@
```

No assertion line is touched. The three `stat -f` call sites the scope clause
fenced off are byte-identical to `origin/main` (they only shift line number:
`test-backup-intake.sh:27→25`, `test-resume.sh:101→105`, `:103→107`). No suite
re-registers a `trap` after the shared installer, and no suite enumerates
`$test_root`'s entries in a way the new `.dotln-test-root-owner` marker would
disturb — the one `case` guard on `"$test_root"/*/node_modules` in
`test-release.sh:275` is pre-existing, unchanged, and passes.

AC9 by `cmp` against `origin/main`:

```text
IDENTICAL scripts/resume.mjs
IDENTICAL scripts/release.mjs
IDENTICAL scripts/worktree.mjs
IDENTICAL scripts/check-publication.mjs
IDENTICAL scripts/backup-intake.sh
```

`package.json` differs from `origin/main` in exactly one line — the `test` chain,
which gains `bash scripts/test-fixture-temp-root.sh` ahead of the six suites, as
the deliverables clause requires. Dependencies, order of the existing six, and
every other field are unchanged. No `.claude/` path and no `docs/decisions/` body
appears in `git status`.

## Acceptance criteria

| #   | Criterion                                             | Verdict | First-hand basis                                                              |
| --- | ----------------------------------------------------- | ------- | ----------------------------------------------------------------------------- |
| 1   | No hardcoded `/private/tmp` or absolute temp base      | **met** | `grep -rn /private/tmp scripts/` returns nothing; only `${TMPDIR:-/tmp}` × 6   |
| 2   | Honors valid `$TMPDIR` incl. spaces and symlink        | **met** | Probe G (7 suites × 3 valid bases); probe B proved physical placement          |
| 3   | Defined for unset/empty/missing/not-dir/unwritable     | **met** | Probe A (4 refusal cases + both fallback forms); probe G (7 suites × 4)        |
| 4   | Cleanup fail-closed, removes only the generated root   | **met** | Probe B (6 refusals, 1 exact removal); probe D (end-to-end, both suites)       |
| 5   | Adversarial base cannot redirect cleanup; sentinels    | **met** | Probes A, B, E, F — every sentinel and decoy survived                          |
| 6   | INT/TERM/exit traps leave no residue                   | **met** | Probe C2 (7 signal runs across 5 suites, exits 130/143, zero residue)          |
| 7   | Each suite passes; diffs touch only the named lines    | **met** | 7/7 individually green; `-U0` hunks confined to lines 5–20                     |
| 8   | Unchanged `npm test` passes in-sandbox and unsandboxed | **in-sandbox met; unsandboxed not independently reproduced** | 3× exit 0 in-sandbox, transcript above, harness named and versioned; see below |
| 9   | Five named scripts byte-identical                      | **met** | `cmp` against `origin/main` — all five identical                               |
| 10  | No settings, allow rule, dependency, API, or schema     | **met** | one-line `package.json` diff; no `.claude/` or `docs/decisions/` path changed  |

### AC8, precisely

The first half — "passes inside a harness sandbox that permits its supplied
temporary root, without source mirroring, relocation, or outside-sandbox
approval, [with] the in-sandbox transcript captured with the harness name and
version" — is **confirmed first-hand**: Claude Code 2.1.258, macOS 15.6, three
green runs, transcript above, no prompt and no relocation. This is the half the
order exists for.

The second half — "and also passes outside any sandbox" — I could **not**
reproduce. This verification session runs under a policy that disables
unsandboxed execution outright (`dangerouslyDisableSandbox` is refused), so no
command of mine can leave the sandbox. I am not treating this as a failure, for
three reasons, and I am naming it so the final reviewer can weigh it rather than
inherit it silently:

1. The code path is base-agnostic. Nothing in `resolve_test_tmp_base` or
   `create_test_temp_root` branches on sandbox state; leaving the sandbox only
   makes `/private/tmp` writable again. Probe G exercised the identical path
   across 49 suite × base cells, and probe E across eleven exotic base names.
2. The one behavior that genuinely differs unsandboxed is the `TMPDIR`-unset
   fallback: outside a sandbox it would create and remove a fixture root beneath
   `/tmp` → `/private/tmp` instead of refusing. That is the documented AC3
   fallback, and it is the same `mktemp`/guard/trap code proven everywhere else.
3. `docs/AI-HARNESS-SECURITY.md` records the executor's outside-all-sandboxes run
   (exit 0, 77/77, empty before/after residue listings). I have no evidence
   contradicting it and every claim in that note I *could* check was accurate —
   see below.

If the final reviewer wants AC8 closed to the letter, the missing artifact is one
unsandboxed `npm test` transcript witnessed by the operator; it does not require
re-verification of anything else in this report.

## Documentation write-back

**`docs/AI-HARNESS-SECURITY.md`** gains `### Portable shell-fixture evidence
(2026-09-02)` under `## Current posture`. I checked every falsifiable claim in it
against this session:

- "Claude Code 2.1.258" — `claude --version` reports exactly `2.1.258`. ✓
- "all seven shell suites passed" — confirmed, and the note's excerpt lists them
  in the chain's actual order. ✓
- "77 tests, 77 passes, and zero failures" — confirmed, three times. ✓
- "the Bash tool reported its session-provided `$TMPDIR`" — confirmed
  (`/tmp/claude-501`); the note correctly sanitizes the literal path. ✓
- "three older `dotln-*` entries already beneath the Claude temp base ... observed
  and left untouched" — confirmed: exactly three match `dotln-*`, all still
  present after my runs. Precisely counted, not rounded. ✓
- "Claude Sonnet 5 at low effort", the supplied-root-only macOS policy run, and
  the outside-all-sandboxes run are executor self-reports I cannot re-derive.
  The note attributes them plainly rather than presenting them as machine
  evidence, which is the honest form.

The note's closing sentence — "This note updates the fixture-path evidence only;
it does not replace the broader dated posture snapshot above" — is accurate and
prevents it from being read as a fresh whole-posture claim.

**`docs/planning/work-order-map.md`**: the WO-013 row moves from `draft; version
unassigned` to `v0.3.1` with implementation evidence captured and "independent
verification pending" — accurate as written. The "Legal now" bullet is correctly
restated as a dated snapshot deferring to the control projection.

**Ledger duty, skipped and declared.** The work order states that the ledger's
fixture-hygiene rule ("tests clean only artifacts they created inside owned
fixtures") is unchanged by this order and no entry is due.
`docs/lineage/idea-ledger.md` is untouched. I confirm the skip is correct: the
deliverable *implements* the existing rule more strictly rather than amending it —
cleanup moved from a literal-prefix match to a provenance check (in-process
ownership registry plus a per-root token file), which is what the rule already
said. No ledger entry is due.

## Findings

**None.** No acceptance criterion is unmet and no behavioral defect was found in
40 probe assertions across seven independently written probes, 49 suite × base
matrix cells, seven signal runs, and three full `npm test` executions.

## Observations — recorded, not scored

### 1. The executor edited the work order, which is the scope authority

`docs/work-orders/WO-013-portable-fixture-temp-roots.md` is modified in the
subject: the H1 becomes `..., v0.3.1` and the release-classification paragraph is
rewritten from the planner placeholder to a concrete assignment. The executor
disclosed the edit in place, dated it, and stated it forward-corrected an
activation preflight miss without replaying or editing the append-only
activation event — which I confirmed: `resume.jsonl` is append-only and its
`WorkOrderActivated` line is untouched.

Assessed rather than merely flagged:

- **It was necessary.** `scripts/release.mjs:255-261` reads the work order's
  first line and throws unless it contains exactly one strict `vX.Y.Z`. The old
  heading contained zero. Left alone, `resume: release close` would have thrown.
- **It is correct.** The latest published tag is `v0.3.0`; `v0.3.1` is the patch
  above it, matches the work-order map's first suggested patch target, and
  matches the roadmap class the order itself names. `release.mjs` sources the
  release version from the work-order heading (`application: authority.version`),
  not from `package.json`, so the heading is the right place for it.
- **It changes no scope.** The diff is a single hunk, `@@ -1,17 +1,18 @@`. All ten
  acceptance criteria, the scope fence, the evidence gate, and the non-goals are
  byte-identical.
- **But** 07-execution-guide.md §Discipline says the original work order remains
  scope authority and that "new authority comes only from the operator or an
  amended/new work order," and `.prettierignore` classes
  `docs/work-orders/*.md` with `docs/decisions/` as surfaces that "retain
  reviewable bytes." An executor amending its own authority is a real
  governance question even when the amendment is right on the merits.

This is for the final reviewer and the operator to adjudicate, not for me to
resolve. Nothing is published on this basis alone: merge authority and a fresh
release-close phrase both remain with the operator.

### 2. Nothing in the committed surface records the executor's model and effort

07-execution-guide.md §Model-specific notes requires the executor to state the
model *and effort* it ran at, so evidence is attributable to the configuration
that produced it. The only place any of that survives in the repository is the
runbook note's "Claude Sonnet 5 at low effort," which describes the final
evidence session rather than declaring the executor's own configuration. The work
order carries `Model: any capable model`, so there is no constraint to violate —
this is an attribution gap, not a breach. It is the same gap
`docs/verifications/WO-015/VER-001.md` recorded as its observation 2; recording it
again is evidence the gap is structural rather than incidental, and a candidate
for whichever future order gives the control log a model/effort field.

### 3. Two portability notes, neither a defect

- Everything above ran on **bash 3.2.57**, macOS's system `/bin/bash` and the
  PATH `bash`. The helper stays inside 3.2 features (`${!array[@]}`, `case`,
  `[[ ]]`), so it is portable to the oldest bash the repo is likely to meet.
  Worth recording because it was not stated anywhere and a future contributor
  reaching for bash 4+ syntax (`declare -A`, `${var,,}`, `mapfile`) in this
  helper would break the suites on stock macOS.
- `scripts/test-temp-root.sh` is named `test-*.sh` like the six suites but is a
  sourced library, not a suite; running it directly is a harmless no-op exit 0.
  Both new files are mode 644, matching `test-publication.sh` and
  `test-release.sh` and unlike the four 755 suites — the chain invokes all of
  them as `bash scripts/...`, so mode is cosmetic. Pre-existing inconsistency,
  not introduced here.

## Required next actions

None for the executor. WO-013 is ready for final review.

For the final reviewer specifically: adjudicate observation 1 (executor amendment
of the scope authority), and decide whether AC8's unsandboxed half needs an
operator-witnessed transcript before close or is adequately carried by the
runbook note plus this report's in-sandbox evidence.

## Open questions and caveats

1. **AC8 unsandboxed half not independently reproduced** — harness policy, not a
   defect; scoped and argued above.
2. **Three executor claims in the runbook note are unreproducible by design**
   (model/effort, the supplied-root-only macOS policy run, the
   outside-all-sandboxes run). Every checkable claim in that note was accurate,
   which is the strongest indirect signal available to me.
3. **Timing-sensitive fixtures.** `exercise_suite_signal` polls at 10 ms with a
   15 s deadline against `test-release.sh`. Seven executions showed no drift, but
   this is the one part of the deliverable whose pass depends on machine speed. A
   much slower or heavily loaded CI host is the plausible future flake source.
4. **Pre-existing residue is untouched by design** — three `dotln-*` entries under
   the session base and 74 under `/private/tmp`, all predating this order. The
   work order forbids treating them as this order's cleanup target, and the new
   provenance guard makes such cleanup impossible even if a later session tried:
   a correctly named stranger is refused (probe B).
