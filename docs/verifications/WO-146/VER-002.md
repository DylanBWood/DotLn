# WO-146 VER-002 — independent re-verification

**Verdict:** pass. VER-001 N1 is resolved; no new substantive finding.
The current subject meets the order with the observation limits recorded below.

**Subject:** the working tree on `wo-146`, based on integrated main `19e706ad`,
captured by the verification dispatch at `refs/dotln/checkpoint/WO-146/7`.
The original order remains [WO-146](../../work-orders/WO-146-copilot-cli-harness.md),
including its authorized worker-version exception. No implementation was edited
by this verifier. Earlier reports and recovery refs remain preserved.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-6-astra","effort":"xhigh","source":"codex-session-readback"}

The operator dispatched `resume: verify` in a fresh attended Codex session.
Briefing and status supplied the actor values above. No subagents were launched;
usage reports zero observed admissions against cap 20, with unobserved coverage
unknown.

**Process cost:** unknown; cause harness-no-readback

The entry measurement at 2026-09-20T15:38:35.781Z had no counters, after explicit
session entry at 15:38:35.678Z. A later attempt to reconstruct that entry cutoff
also yielded no eligible measurement. Counters subsequently became available:
3,678,940 total tokens, source `codex-transcript-counter`, scope `dispatch`,
cutoff 2026-09-20T15:44:28.652Z. This is the available pre-report measurement,
not an invented entry total or the final turn cost; report writing, final
checks and handoff follow that cutoff. Dollars remain unknown. Final readback
stays in ignored receipts and the operator response.

## Repair judgment and evidence continuity

[VER-001](VER-001.md) failed only N1: formatting in two order-owned scripts
made the grouped documentation gate fail and suppressed five dependent checks.
Expected: both files match the pinned formatter and all grouped tasks execute.
Observed: `npm run test:docs` now passes **19/19**, including those five tasks.
N1 is closed, with no remaining severity or repair action.

The checkpoint comparison from VER-001's result (`.../4`) to this dispatch
(`.../7`) contains exactly eight changed paths: the two scripts, decisions,
repair evidence, decisions index, order index, control projection and control
segment. All other source, generated and live-evidence bytes are unchanged.
For each repaired script, I read its checkpoint-4 bytes, passed them through
the installed `node_modules/.bin/prettier --stdin-filepath <path>`, and asserted
byte equality with the current file. Both assertions pass. Thus the repair is
exactly formatter output, not an unreviewed behavioral edit.

I also compared current package, script and configuration blobs with checkpoint
7, including the new source files absent from the index: all match. The current
tracked code identity matches the full passing product row recorded at
2026-09-20T15:13:20.612Z (21 suites, 251.932 seconds). That product result is
carried forward from [repair-001](../../evidence/WO-146/repair-001.md), not claimed
as a new `npm test` run by this verifier. Fresh machinery tests exercise the
new probe and qualification sources as well as the shared runtime.

VER-001 L1 is also resolved: D014 preserves and annotates D012's superseded
figures. Direct byte counts reproduce executor **23,154**, verifier **21,061**,
reviewer **22,279**, release-close **13,967**, planner **15,033** and refuter
**15,690**. Both skill roots are byte-identical. The existing D008 ceiling
acceptance remains sufficient.

## Acceptance checks

| Criteria | Current evidence and judgment |
| --- | --- |
| 1: bounded probe and H1–H12 | Met with the stated observation limits. Source guards require opt-in, system-temp scratch and a disposable CLI home, reserve launches before execution, cap scripted launches at twelve and supply each launch's credit limit. The retained record contains ten launches plus bare I1/I2. I independently read P7/P10/I2 denial records and I2's model change. The fresh stub suite checks the bounds, trust correction, shapes-only reduction and preservation of older observations. Existing probe modes change only by the new dispatch branch. |
| 2–4: one profile, one registration, compatibility | Met. The profile cites dated H anchors and uses an existing skill root. Compiler fixtures compare complete Claude/Copilot hook outputs; harness fixtures assert unique generated paths and commands. The two worker profiles retain their normalized baseline fingerprint. P7 records one invocation per correlated event for the shipped registration. The unchanged compatibility inventory names every permitted generated-path change and its protecting fixture. |
| 5: labeled controls | Met. The security table contains all fifteen required rows and an allow-all column. Its four enforced predicates combine P7/P10/I2's observed refusal channel with generated-handler tests for writer, planning, gate and literal outside-write paths. Missing child identities, read ranges and delivery evidence are explicitly advisory. These are bounded predicates, not arbitrary shell confinement. |
| 6–7: harness identity and readback | Met. Fresh process-debt fixtures exercise real completions with Claude and GPT model IDs while retaining `copilot-cli`; discovery and planning actors admit that harness independently. Reader cases cover latest model selection, unresolved auto/null effort, absent/corrupt/foreign logs, operator attestations, partial request counters, shutdown totals and credit-only records. Source review confirms worktree/session matching and explicit unknown causes. |
| 8, 11: setup and qualification limits | Met. The seven-item setup record separates documented behavior from observed enforcement, and proposed personal settings include undo steps. Sandbox-off, allow-all, outward channels and cross-session memory limits are explicit. The playbook marks Copilot final review and release close untested. No personal settings were changed in this verification. |
| 9: four live workflow episodes | Met on unchanged retained evidence, not new live launches. Independent assertions checked four reservations, four passes against their expected outcomes, phases `ready-to-verify / needs-fix / ready-to-verify / verified`, fixture exits `0 / 1 / 0 / 0`, the planted finding, Copilot actor/readback and writer release. Recomputed totals are 196.35181 credits, 5,299,696 tokens and 1,088,345 ms. Collector source and fresh fixtures check session freshness, phase/report/test evidence and attempt bounds. |
| 10, 12: suites, write-backs and gates | Met. No suite, lane or external dependency was added; the runner diff only adds three source selectors. Product 03, product 07, the security table and playbook contain the required write-backs. Publication and generated-bundle checks pass. The grouped docs failure is repaired, and the existing product pass still applies as described above. |

New evidence produced in this session:

- `npm run test:docs`: **19 passed, 0 failed**, **18.13 s**.
- `npm run test:machinery`: **16 passed, 0 failed**, **290.90 s**.
- `node --test packages/compiler/dist/test/harness.test.js`: **7 passed, 0 failed**.
- `npm run publication:check`: pass, **272/272** headings covered.
- `node scripts/harness.mjs check`: **31** generated surfaces match.
- `git diff --check` and `git diff --cached --check`: clean.

The machinery run is 6.85 seconds longer than repair-001's 284.05 seconds;
the docs run is 0.56 seconds longer than its final 17.57-second run. These are
same-selection observations across sessions, not isolated performance effects
of the formatting repair. No product-gate duration is attributed to this turn.

## Limits, instruments and disposition

VER-001 L2–L6 remain disclosures. The generated PR placeholder is not a final
review; timestamp-only maxima in the other probe rows are not duplicate-handler
counts; bare entry, trust and approval observations are operator attestations;
once-only instruction loading remains untested, and H6/H7 did not demonstrate
message delivery. Session counters have no independent provider cross-check.
The qualified workflow remains CLI 1.0.86 / Claude Sonnet 5 / xhigh; the separate
GPT model change does not qualify that complete workflow. No live session was
repeated and no broader guarantee is inferred.

The lifecycle, generated harness, usage reader, probe and qualification helpers
are themselves under verification. I checked source, independent assertions and
fixtures instead of treating their successful messages as proof. Codex supplied
no automatic hook receipts here: I used explicit session/observation adapters
and manually invoked the generated writer preflight before authoring this
report. It reserved this session's live ancestor; this is not evidence of a
native Codex hook firing.

The judgment preserves the planning rationale and D013/D014: the mission benefit
is a usable third operator harness with honest limits, not a critical-path
runtime dependency. NoOp would leave a repaired finding unresolved. Policy
resistance and naive interventionism favor unchanged implementation and shared
hooks; commons and escalation favor existing deterministic checks and carrying
unchanged product/live evidence; drift and rule beating require the previously
red grouped gate to execute; success to the successful requires the same
evidence standard across harnesses; shifting the burden favors completing this
verification here; seeking the wrong goal is avoided by judging the workflow
and its limits rather than profile count. The observed repair meets that
promise without new machinery or operator episodes.

No defect was encountered and deferred, so no new follow-up decision is needed.
Next legal workflow: `resume: final review`. Final review owns integration,
its product gate and publication decisions; this verdict grants no publication
authority.
