# WO-146 VER-001 — independent verification

**Subject:** `docs/work-orders/WO-146-copilot-cli-harness.md` at the staged and
unstaged working tree on branch `wo-146`, base commit `19e706ad` (integrated
main, WO-145). The branch carries no commits of its own; the whole order lives
in the index and working tree, as the no-branch-commits rule requires.

**Verdict:** fail. One substantive defect (N1) routes to repair: the
repository's `npm run test:docs` gate is red because two files this order adds
or changes fail the pinned Prettier `format` check, and that failure suppresses
five further checks in the same gate. Criteria 1 through 11 are met on
reproduced evidence, and every other executable claim in criterion 12 is met.
Six limits are recorded below; none of them routes to repair.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"unknown","source":"self-reported"}

Human actor: the operator dispatched `resume: verify` in an attended Claude
Code session. Harness version is live readback from `claude --version`. The
model id is this session's own reported identity. Effort is `unknown`: this
harness supplies no effective-effort readback and nobody supplied a value for
this dispatch, so the order's `verifier xhigh+` recommendation is not recorded
as an observation.

**Process cost:** entry 83577 tokens; handoff 50625613 tokens; source claude-transcript-message-usage

Scope `dispatch`; entry cutoff `2026-09-20T14:45:25.347Z`, handoff cutoff
`2026-09-20T15:01:53.323Z`, both taken before this report was written and
before the result transition, so both remain uncounted. `costUsd` and
`reasoningOutputTokens` are unavailable from this counter. Subagents: 0
observed against a cap of 20. No agent was spawned; the fan-out plan stated at
entry was to reproduce every claim in this session, so the whole budget remains
and the unobserved remainder is unknown.

## Instrument disclosure

Several instruments used as evidence here are themselves part of the subject.
The regenerated `.claude/hooks/*.mjs` carry the staged skeleton `0.31.0`
runtime; they judged this session throughout and refused a substantial number
of my own commands (below). `scripts/harness.mjs usage` reads through the
changed `packages/skeleton/src/usage-observation.mjs`, which is the order's own
readback, so the process-cost figures above come from code under verification.
`scripts/lib/copilot-probe.mjs` and `scripts/lib/copilot-qualification.mjs`
produced the probe and qualification records I judge, so for those I checked
their guards in source rather than trusting the records alone, and I
recomputed the qualification's arithmetic independently.

Hook refusals observed against this session, all correct behaviour:

- While each of my two gates was live, the generated pre-tool hook refused
  every `git`, `sort`, `awk`, `diff`, `shasum` and `node -e` invocation as a
  possible write to gate inputs. I deferred those checks until the gates ended
  rather than stopping a gate.
- One outside-write refusal for a malformed scratch path I constructed
  (`/tmp/../private/tmp/...`), correctly reported as lacking a verifier grant.
  I reran it against the DotLn session scratch printed at dispatch.

## N1 — the docs gate is red on this order's own files

**Criterion:** 12, and the order's own evidence claim that the planning,
publication, release-surface and whitespace checks passed.

**Observed:** `npm run test:docs` reports **13 passed; 6 failed; 4.00 s**. The
`format` task fails:

```
FAIL format 3.28 s
  [format] Checking formatting...
  [format] [warn] scripts/lib/copilot-probe.mjs
  [format] [warn] scripts/test-harness-probe.mjs
  [format] [warn] Code style issues found in 2 files. Run Prettier with --write to fix.
```

`format` is a declared preflight for five further tasks, so `plan`,
`plan-refutation-current`, `console-docs`, `skeleton-docs` and
`lineage-fixtures` did not execute at all: each reports `Required preflight or
fixture preparation failed for <task>: format`.

**Expected:** a green standing gate. Both named files belong to this order —
`scripts/lib/copilot-probe.mjs` is new, `scripts/test-harness-probe.mjs` gains
459 lines — so the failure is introduced here, not inherited.

**Reproduction:** `npm run test:docs`, and directly
`npx prettier --check scripts/lib/copilot-probe.mjs scripts/test-harness-probe.mjs`
(exit 1). The binary is the repository's own pin, `prettier@3.9.6` in
`package.json`, and `prettier --version` confirms 3.9.6, so this is not tool
drift. Diffing each file against `npx prettier <file>` shows four hunks, all
mechanical line-wrapping of hand-written multi-line expressions
(`copilot-probe.mjs` around the `usage` ternary and `renderCopilotProbe`'s two
predicates; `test-harness-probe.mjs` around the credit-source `assert.ok` and
the `interactive` array literal). No semantic change is involved. Both files'
mtimes (10:13 and 10:15) fall after the order's last recorded formatting pass,
which matches the "probe renderer/accounting correction" the implementation
evidence describes as its final edit.

**Why the executor's gates were green:** `format` is not part of `npm test` or
`npm run test:machinery`, both of which I reproduced green. The implementation
evidence never names `npm run test:docs`; its "Publication, planning, release
surfaces, cold-start budget and whitespace" row reports individual checks,
which do pass in isolation. The gate that runs them as a group does not.

**Severity:** moderate. The repair is mechanical and the risk of the fix is
very low, but a standing repository gate is red on this order's files and it
hides five checks from execution, so this is not a limit to record — it is work
the order has not finished. I did not run `prettier --write` myself: a verifier
may not edit the implementation to turn its own verdict green.

**Bounding evidence.** So the finding stays narrow, I ran each suppressed check
standalone against the current tree. All five pass: `node
scripts/refute-plan.mjs check` exit 0; `node --test
scripts/test-plan-refutation.mjs --check-only` exit 0; `node --test
scripts/test-lineage.mjs` exit 0; console fixtures 21 passed, 0 failed;
skeleton fixtures 384 passed, 0 failed. Nothing behind the preflight is broken.
The repair is the formatting alone, followed by a green `npm run test:docs`.

## Criterion 1 — the probe, its bounds and its record

Met. I read the guards in source rather than trusting the record.
`runCopilotProbe` asserts `env.DOTLN_LIVE_HARNESS === "1"` ("explicit live
harness probe required"); the scratch repository and CLI home are asserted to
sit under `realpathSync(tmpdir())`; every scripted launch carries
`--max-ai-credits 30` in its argument list; and the launch bound is enforced as
`previousLaunches + launches.length <= 12` across all retained dated records.
`prepareCopilotScratch` sets `COPILOT_HOME` to the disposable scratch home, so
no scripted launch touches the operator's real CLI home.

The retained record `docs/discovery/copilot-cli-2026-09-20.md` carries H1
through H12 as HTML anchors with a result and an explicit limit per row, and
reports 10 of 12 launches on CLI 1.0.86. Its companion JSON keeps field-name
trees and enumerated values only. H1, H5 and H12 each carry an operator
interactive row: I1 blocked on `trust-required`, and I2 collected from a bare
trusted allow-all session with 49 hook invocations, `claude-deny` and
`claude-error` both `denialObserved: true`, and a mid-session change from
`claude-sonnet-5`/`xhigh` to `gpt-5.6-sol`/`medium`.

The other probe modes are untouched: `git diff HEAD -- scripts/harness-probe.mjs`
is a single three-line `else if (process.argv[2] === "copilot")` dispatch, and
`git status` shows no change to any existing `docs/discovery/` record. The stub
test exists at `scripts/test-harness-probe.mjs:34` (opt-in refusal plus the
shape reduction, asserting key names survive and values become type strings)
and `:131` (the stub launch). The whole `harness-probe` suite passed in my
machinery run.

## Criterion 2 — one profile entry, no new surface

Met. `packages/skeleton/src/loadouts/contributor.ts:688-722` adds exactly one
entry: `harness: "copilot-cli"`, `profileId: "copilot-cli-1.0.86"`. Every
declared availability cites a probe anchor (`#H5`, `#H3`, `#H1`, `#H10`), and
each `true` has an observed row behind it; `Stop` and `UserPromptSubmit` carry
explicit `reason` strings recording that H7's message visibility and H6's
context delivery were not observed. `refusal: "claude-command-json-v1"` is
named on the strength of H5's observed denial, as the criterion requires.
Skills root is `.agents/skills`, one of the two existing roots.

No new tracked surface: `.github` does not exist, `git status` lists no new
`SKILL.md`, instruction, agent definition or plugin, and
`diff -rq .claude/skills .agents/skills` exits 0 with no output, so the six
role bodies are byte-identical across roots.

## Criterion 3 — no handler runs twice

Met, by a deterministic fixture per form plus the live row for the shipped
configuration.

- `packages/compiler/test/harness.test.ts:108` lowers the same program for the
  Claude and Copilot profiles and asserts the `.claude/settings.json` and
  `.claude/hooks/**` file sets are deeply equal, that `.agents/skills/` files
  appear, and that no `.github/` file is emitted. It also asserts the invariant
  refuses a Copilot profile with `refusal: "unavailable"` and refuses one as a
  target worker.
- `scripts/test-harness.mjs:511` asserts installation paths are unique and that
  within every hook event group in the emitted settings the command strings are
  unique — a direct check that no handler is registered twice per event.
- `scripts/lib/harness.mjs:145-154` merges shared outputs and throws
  `Harness profiles disagree on shared output: <path>` on any byte conflict, so
  the single emission is structural, not incidental.
- The probe stub itself throws `duplicate fixture registration` if either
  registration form holds more than one hook for an event, so every stub row is
  also a per-form single-registration assertion.
- Live: P7, the trusted Claude-form-only row, records
  `maxInvocationsPerCorrelatedEvent: 1`. That is the configuration DotLn ships.

Branch B is not taken, so its extra obligations do not apply. See L3 for the
ambiguity in the other rows' maxima, which the record and WO-146-D005 already
disclose.

## Criterion 4 — existing behaviour intact

Met. `git status` reports no change at all to `.claude/settings.json` or
`.codex/**`. The changed generated paths are the `.claude/hooks/*.mjs`, both
skill roots, `CLAUDE.md` and `.claude/harness-manifest.json`; each is listed by
path with a reason and a protecting fixture in
`docs/evidence/WO-146/compatibility.md`, which is what the criterion allows
beyond its own "expected changes" list. The manifest carries three profiles.

`targetWorkerProfiles` filters `copilot-cli` out explicitly, and
`scripts/test-harness.mjs:511` pins the two remaining workers to the SHA-256
`9f41bb7c…` after deleting only `runtime.skeletonVersion` — an executable
statement of exactly the exception the operator authorized. That amendment to
criterion 4's text is bound in the planning log: `docs/control/plan-refutations.jsonl`
holds a `PlanExecutionAmended` row for `WO-146-D009`, alongside one for
`WO-146-D001`. `npm run plan -- check` exits 0 and classifies the current order
source as an `authorized-execution-amendment`.

## Criterion 5 — the control table

Met. `docs/AI-HARNESS-SECURITY.md#copilot-control-table` publishes all fifteen
required rows — session and worktree identity; writer reservation; presence
heartbeat and origin class; live-gate input protection and `evidence --stop`;
the planning-branch write boundary; subagent admission and descendant
accounting; the outside-write grant; write observation and authorship; read
observation; the observed-facts block; usage counters; model and effort
readback; completion and handoff; compaction continuation; `analysis:` and
`operator override:`. Each row carries a classification, its probe anchor or
fixture, and a "With allow-all" column.

The four rows that say enforced each have both halves. Live denial: P7 and P10
(P10 under allow-all) and bare interactive I2 all record `claude-deny` and
`claude-error` as `denialObserved: true`. Fixture:
`scripts/test-harness.mjs:559` drives the generated `permissions` handler with
`COPILOT_PROJECT_DIR` set and Copilot's native `path` payload, and observes the
outside-write grant, the planning-branch boundary and the live-gate refusal;
`:672` does the same for freeform patch input and adds the writer reservation;
`:621` pins `patchWriteTargets`. `HARNESS_BOUNDARIES` in
`packages/compiler/src/harness.ts:1041` states Copilot's position once, in the
same clause form used for Codex, and that sentence is present in the generated
`CLAUDE.md` and in all six role skills.

## Criterion 6 — harness recorded apart from model

Met. `scripts/test-process-debt.mjs:706` runs the real lifecycle twice, once
per model family: it copies `resume.mjs`, `harness.mjs`, `scripts/lib` and the
three package sources into a scratch repository and executes
`resume implementation-ready --harness copilot-cli … --model claude-sonnet-5`
and the same with `gpt-6-astra`, then asserts the recorded actor's harness,
model, effort and source, and that `resume status` prints `harness copilot-cli`
and the model. `docs/discovery/environment.json:710` records the observed
`1.0.86` and version line `1.0` from `npm run discover -- harness copilot-cli`.
`scripts/lib/plan-receipts.mjs:800` admits `copilot-cli` in the planning-receipt
actor list, and `:668`'s fixture proves it for both model families and rejects
an unknown harness. No model-id literal appears anywhere in
`packages/skeleton/src`, `packages/compiler/src`, `scripts/resume.mjs`,
`scripts/harness.mjs`, `scripts/lib/harness-runtime.mjs`,
`scripts/lib/receipt-cost.mjs` or `scripts/lib/executor-handoff.mjs`, so no code
path maps a model to a harness or the reverse.

## Criterion 7 — the session-log reader

Met. `scripts/test-process-debt.mjs:345` covers a named model with an effort;
a mid-session change reporting the latest; `auto` with a null effort yielding
`model: null`, `effort: null` and causes `["model-auto-unresolved",
"effort-unreported"]`; a missing log (`session-log-missing`); an unidentified
session (`session-unidentified`); a traversal id; a foreign `cwd`; a foreign
`gitRoot`; and a corrupt log. It also asserts the result leaks no session id,
path or model name. `:706` supplies the operator-attested case: a log reporting
`high` does not overwrite a supplied `xhigh`, and the recorded source stays
`operator-attested`. `harness usage` never throws for a Copilot session:
`measureHarnessUsage` returns an observation with
`cause: "active-dispatch-unavailable"` when no dispatch is open, asserted in the
same fixture, and `:528` proves a credit-only or partially populated shutdown
record is still collectable and recordable with
`cause: "session-token-counters-unavailable"` and null tokens.

## Criterion 8 — the seven-item checklist and proposed settings

Met. `docs/AI-HARNESS-SECURITY.md:691-757` answers all seven items: settings
locations and precedence with `config.json` versus `settings.json` and
`COPILOT_HOME`; read and write boundaries; approval behaviour with its
untested exception matrix named as untested; network and the built-in GitHub
MCP server; fail-open or fail-closed when sandboxing is unavailable; enable,
verify and undo; and the tested date and version. It states that command
sandboxing is experimental and off by default in 1.0.86, what is left
unconfined with it off, and that DotLn requires neither setting. The
"Proposed operator settings" block gives file, key, observed value, proposed
value, reason and undo for seven settings, and opens by saying none is applied.
No tracked file writes under `~/.copilot`: the only `COPILOT_HOME` assignment
in the order's new code points at the disposable scratch home, and I2 records
`operatorSettingsWritten: false`.

## Criterion 9 — the live qualification

Met. `docs/evidence/WO-146/qualification.json` records four episodes with the
required roles and outcomes: executor to `ready-to-verify`; a fresh verifier to
`needs-fix` with `plantedDefectReported: true` and `fixtureExitCode: 1`; a fixer
to `ready-to-verify`; a fresh verifier to `verified`. All four record
`writerReleased: true`, harness id, CLI 1.0.86, model, effort, source, hook
counts by event, an empty denial list, an approval observation, AI credits and
wall-clock. `attemptsReserved` is 4 against the four-plus-two bound.

The record is not self-certifying. `collectCopilotQualification` computes
`passed` from the actual control-log phase, an actor whose harness is
`copilot-cli`, the real fixture exit status, the planted-defect sentence
appearing in the episode's own `VER` report, and a released writer; it refuses
a reused session id, a session that predates its reservation, and a
non-attested bare launch; and `publish` sets `qualified` only when
`nextEpisode === 4`, which is incremented only on a pass. I reproduced the
record's arithmetic independently: the four `aiCredits` sum to 196.35181, the
four `totalTokens` to 5,299,696 and the four `durationMs` to 1,088,345, all
matching the implementation evidence exactly. The fixture order is the
synthetic WO-999 in a system-temporary repository, never DotLn's control log.

## Criterion 10 — suites and lanes

Met. The new tests are in `packages/compiler/test/harness.test.ts`,
`scripts/test-harness.mjs`, `scripts/test-harness-probe.mjs` and
`scripts/test-process-debt.mjs` — the four existing suites. The only change to
`scripts/test-runner.mjs` is three source paths added to the existing
`harness-probe` machinery declaration at lines 80-82; no suite, lane or
classification is added. No gate invokes a live CLI: the live paths all sit
behind `DOTLN_LIVE_HARNESS=1` and the suite drives a stub executable. The
executor reported both gate wall-clocks.

## Criterion 11 — what is not qualified

Met. The `docs/PLAYBOOK.md` role table gives Copilot a row reading
"Final review and release close: **untested**; keep those on a qualified
harness", and the paragraph below names the built-in GitHub MCP server,
`/delegate`, `--remote`, `--fleet` and autopilot as outside this loop's
qualification and helper governance. None of them is handled by a launch
argument, and a persisted off switch for the built-in server
(`disabledMcpServers`) is on the proposed-settings list. Cross-session memory
is named as the one channel that can carry implementer context into a fresh
verifier, in the playbook, the security document, the qualification record and
the operator runbook, each time with the statement that a fresh session
identifier is not mechanical independence.

## Criterion 12 — write-backs and gates

Partly met; N1 is the exception.

Write-backs land: product 03 at line 465 (the third runtime target and what was
observed, with a reopening condition), product 07 at line 1934 (the readback
source, the attestation line and the completion command), the five-refusal
paragraph through the generator, the `docs/PLAYBOOK.md` role table, the README
and the roadmap. The capability table needs no dated reassessment because
WO-146-D008 records that no level is promoted by this profile.

Gates I reproduced myself, from this worktree:

- `npm test` — 21 passed, 0 failed, 254.95 s runner.
- `npm run test:machinery` — 16 passed, 0 failed, 290.88 s runner.
- `npm run publication:check` — pass, including index coverage 272/272.
- `node scripts/harness.mjs check` — 31 generated surfaces.
- `git diff --check` — clean, staged and unstaged.
- `npm run plan -- check` — exit 0.
- `npm run test:docs` — **13 passed, 6 failed** (N1).

No new dependency: `package-lock.json` changes only the internal
`@dotln/compiler` 0.16.0 to 0.17.0 and `@dotln/skeleton` 0.30.0 to 0.31.0 pins.

Cold-start totals reproduce exactly. `CLAUDE.md` is 6,113 bytes; with the role
bodies that gives executor 23,154, verifier 21,061 and reviewer 22,279, which
are the implementation evidence's figures. The verifier ceiling was raised to
25,151 through the one route product 07 names, with a dated acceptance in
`docs/control/budgets.json` citing WO-146-D008. Every other role stays inside
its existing ceiling.

## Further limits recorded

- **L1.** WO-146-D012's prose states cold-start totals of 23,156 / 21,063 /
  22,281; the current bytes are 23,154 / 21,061 / 22,279, which is what
  `implementation.md` reports. The decision text is two bytes stale per role. No
  ceiling is affected. Worth a same-day correction in the decisions file rather
  than a repair episode.
- **L2.** `docs/final-reviews/WO-146/PR.md` exists, untracked, containing only a
  generated process-meter block. It is written by `scripts/release.mjs:2021` as
  a side effect of the release-preparation retime the order's integration
  decision authorized. Nothing was published, nothing is committed, and the
  reviewer regenerates it. Recorded so a reviewer does not read it as a
  pre-empted final review.
- **L3.** `maxInvocationsPerCorrelatedEvent` reads 2 in P8, P9 and P10. For P9
  and P10 two registrations were installed, so two is the expected total; P8
  installed only the native form and still reads 2. The record's H1 limit and
  WO-146-D005's third rejected option both state that timestamp-only
  correlation cannot distinguish a duplicate handler from two calls sharing a
  timestamp. The affirmative evidence for criterion 3 is therefore P7 plus the
  fixtures, not these maxima. The metric should not be quoted as a duplicate
  count.
- **L4.** Bare entry, folder trust, permission mode and approval counts are
  operator attestations throughout the probe and the qualification, not
  mechanical observations; episode 2's permission mode is honestly recorded as
  `unknown` because the operator changed it mid-session, and I2 discloses that
  prior permission changes included `/yolo`. The helpers enforce the attestation
  flags but cannot observe the launch.
- **L5.** `instructionLoadedOnce` is `untested`, and H6 (prompt context) and H7
  (Stop message visibility) were not observed. The profile and the control table
  both carry these as limits rather than availability claims, which is correct,
  but it means "the symlinked instruction pair loads once" in criterion 1's H10
  is answered as availability only.
- **L6.** The usage counters in the process-cost line above were produced by
  `usage-observation.mjs`, which this order changes. I have no independent
  counter to cross-check them against; the figures are reported as the
  instrument gives them.

## Reproduction

From `/Users/dylanwood/Projects/DotLn-wo146` on branch `wo-146`:

```
npm test
npm run test:machinery
npm run test:docs                                    # 13 passed; 6 failed
npx prettier --check scripts/lib/copilot-probe.mjs scripts/test-harness-probe.mjs
npm run publication:check
node scripts/harness.mjs check
npm run plan -- check
git diff --check && git diff --cached --check
diff -rq .claude/skills .agents/skills               # no output
git status --porcelain -- .claude/settings.json .codex   # empty
git diff HEAD -- scripts/harness-probe.mjs           # three added lines
git diff HEAD -- package-lock.json                   # internal pins only
wc -c CLAUDE.md .claude/skills/dotln-verifier/SKILL.md   # 6113 + 14948 = 21061
node scripts/refute-plan.mjs check
node --test scripts/test-plan-refutation.mjs --check-only
node --test scripts/test-lineage.mjs
node --test 'packages/console/dist/test/*.test.js' --fixtures-only
node --test 'packages/skeleton/dist/test/*.test.js' --fixtures-only
```

The formatting deltas were read by diffing each file against `npx prettier
<file>` written into the DotLn session scratch printed at dispatch. The
qualification sums were recomputed from `docs/evidence/WO-146/qualification.json`.

## Severity and disposition

One finding routes to repair. N1 is mechanical and its fix is a single
`npx prettier --write` over the two named files, followed by a green
`npm run test:docs`; nothing behind the failed preflight is itself broken, as
the five standalone runs show. It is nonetheless unfinished work rather than a
recorded limit, because a standing repository gate is red on files this order
introduces and the order's own evidence presents its check set as discharged.

Everything else the order set out to do is done and independently reproduced: a
third operator harness compiled from the existing loadout, the one generated
hook set reused with no second registration, fifteen DotLn controls labelled
against a dated probe with live denials behind every "enforced", and four live
operator episodes carrying the real lifecycle from implementation through a
failing independent verification and a repair to a passing one. The limits
above are disclosure, not doubt about those results.

Reopening observations stay as the order states them: a Copilot session denied
every tool call in a DotLn worktree, a DotLn handler observed twice for one
event, or a control the table calls enforced admitting what it refuses.
