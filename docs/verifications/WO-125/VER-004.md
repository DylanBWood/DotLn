# WO-125 VER-004 — independent verification

- Work order: [WO-125 — Codex effort selection](../../work-orders/WO-125-codex-effort-selection.md), including its `## Execution record` repair expansion.
- Subject: branch `wo-125` working tree after `RepairCompleted` (checkpoint `refs/dotln/checkpoint/WO-125/14`, `53ed730`) and the verify dispatch (`refs/dotln/checkpoint/WO-125/15`, `2ac2a48`). The dispatch changed only the three lifecycle projections. Since VER-003's dispatch, the only source changes are the repair's: one guard in `packages/skeleton/src/gate-evidence.mjs` and new cases in `scripts/test-runner.test.mjs` and `scripts/test-harness.mjs`, with their regenerated pins, evidence and documentation.
- Earlier reports: [VER-001](VER-001.md) (fail: F1–F3), [VER-002](VER-002.md) (fail: F1–F3) and [VER-003](VER-003.md) (fail: F1).
- Dispatch: operator `resume: verify`, recorded 2026-09-12T14:14:21Z.
- Verdict: **pass**. VER-003 F1 is resolved and the earlier findings stay resolved. No new finding; four observations.

Verifier: Claude Code `2.1.269` (`claude --version`), model `claude-opus-5` running as the session's 1M-context variant `claude-opus-5[1m]`, effort `max` as selected by the operator's `/model` command at the start of this session. The source is `self-reported`; neither value is effective-session readback. The order requires a verifier at `xhigh` or above.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.269","model":"claude-opus-5","effort":"max","source":"self-reported"}

## Summary

The repair closes VER-003 F1 with one conservative check. `gateInputPath` now treats an existing non-directory destination with more than one link as a gate input, because its other names cannot be proven to be scratch (`gate-evidence.mjs:84–88`). Every in-place write VER-003 admitted through a pre-existing hard link is now refused with the active run named, and the tracked file keeps its bytes. The same holds for the Write, Edit and NotebookEdit tools, for case-variant, symlinked and directory-symlinked spellings of the link, and for a hard link to an installed input. This session's own hooks refused a redirect and a Write into a multiply-linked scratch pair while a marker was held, and admitted a single-link scratch write.

The new runner regression detects the gap: my replica of its assertions fails against the inherited classifier and against each of four mutations of the guard. The transport half is unchanged since VER-002. It still forwards exactly the five observed levels, now checked against each whole recorded vector.

The conservative rule costs nothing in this worktree today: no file in it, `node_modules` included, has more than one link. The canonical gate on this report's first bytes failed only `console` test 18, the load-dependent `release:list` failure VER-003 O3 recorded, and product 07's retry at the same tree passed. This amendment records those runs, so the final bytes need their own gate record before the pass is recorded (AC5).

## Acceptance criteria

- **AC1 — one discovery row per declared level.** Met. [`codex-effort-2026-09-11.json`](../../discovery/codex-effort-2026-09-11.json) has five rows, `low` through `max`. Each carries the complete argument vector, exit `0`, result `{"ok":true}`, `effortFields: []`, `effectiveEffort: "unknown"` and classification `observed`. The live launches were not repeated (O4).
- **AC2 — observed levels forward exactly and record a launch claim; `unknown` forwards nothing and fixtures are unchanged; unavailable levels refuse naming the row.** Met. I reran VER-001's HEAD-versus-current probe on the current build and extended it to compare each forwarded vector with its whole discovery row, after substituting the request's model, schema path and root for the row's placeholders.
  - On `0.154.0`, each of the five levels reaches the process with the recorded vector. HEAD refused all five.
  - `minimal`, `none`, `MAX`, `""`, `null` and the newline-plus-`sandbox_mode` injection refuse before launch and name the discovery record. `max` refuses on `0.153.4` and with the version omitted. `codex-cli 0.154.1` and `0.155.0` refuse at construction.
  - `unknown` equals HEAD's arguments on `0.153.4`, on `0.154.0` and with the version omitted, and HEAD's arguments equal `codex-unknown-args.json`.
  - `worker.test.js`: 20/20, including the `host-launch` claim records.
- **AC3 — `npm run plan -- refute --transport codex-cli-exec --model gpt-6-astra --effort max` constructs the transport.** Met with a double; the live run is the operator's. "WO-125 plan CLI accepts Codex GPT-6 Astra/max and records only a launch claim" passed (1/1, 62.9 s).
- **AC4 — write-backs: refutations README, 07 §Model-specific notes, `environment.md` addendum, ledger entry.** Met. The README sentence, 07 §Model-specific notes, the `environment.md` addendum and the runtime-map table agree with the rows and are unchanged since VER-003. The ledger duty is discharged by [the decisions file](../../evidence/WO-125/decisions.md) (D001–D005) and its five index rows, and the work-order index marks the substitution. 07's gate paragraph now carries the D005 sentence, which matches the code.
- **AC5 — `npm test` green; `git diff --check` clean; no new dependency; regenerated pins; fresh feedback edition.** Met, after one diagnosed retry of `console`.
  - Dependencies are unchanged. The lockfile and the skeleton manifest change only the skeleton version, and `package.json` changes only the `harness` script.
  - The hooks and manifest pin skeleton `0.15.2` snapshot `.runtime/harness/52e7c7bffd5a00b7`. Its `gate-evidence.mjs`, `harness-host.js`, `harness-command.js` and `worker-transport.js` are byte-identical to the current build, and its classifier contains the new guard.
  - `docs/evidence/current.json` selects WO-125 authority `004` and feedback `001`. Revision 004 has four compatibility, four widening and nine narrowing entries and 27 bundle files over 24 generated surfaces, matching the repair record's counts.
  - `git diff --check` was clean at dispatch, and the targeted suites listed under Reproduction passed.
  - The first `npm run harness -- evidence`, on this report's pre-amendment bytes (tree `b5ac6bd`), recorded `npm run test:full`: 36 passed, 1 failed, 838.97 s, 78 fresh tasks (`host-check:94ee33d8…`, 2026-09-12T14:51:30Z). `git diff --check` passed (`host-check:9f4873aa…`).
  - The failure was `console` test 18, "host collection reads current sources…": `release:list` came back `unavailable` after 89.3 s. That is VER-003 O3's signature, in code this order does not change. After that diagnosis, product 07's retry at the same tree reran `console` (35.72 s, passed) and nine other tasks, reused 68, and recorded 37 passed and 0 failed (2026-09-12T14:55:42Z).
  - This amendment changes the tree again. `verification-result pass` is refused unless the gate records passing `npm run test:full` and `git diff --check` for the final bytes (`scripts/lib/lifecycle-evidence.mjs:36–46`). I record the verdict only after that run.
- **Execution record (F3 scope).** Met. Live refusal naming the active runs, ignored-scratch admission, dead-owner recovery and generated-hook fixtures that execute attempted writes are all demonstrated, now including hard-link aliases.

## Earlier findings

- **VER-003 F1 — resolved.**
  - Classifier, in a temporary repository: `scratch/input-link.ts`, hard-linked to tracked `input.ts`, and `scratch/via-symlink.ts` now return `true`. So do the case variant `SCRATCH/Input-Link.ts`, the path through a directory symlink, the absolute path and a hard link to an ignored `node_modules` file. A single-link scratch file, a new scratch file and scratch directories return `false`.
  - Generated hooks, by VER-003's fixture method with a held marker. These attempts through a hard link from ignored scratch to tracked `fixture.ts` were refused, each naming the run:
    - Write, Edit and NotebookEdit;
    - `printf … >`, `>>`, `| tee` and `| tee -a`;
    - the Codex `exec_command` redirect, including one from the scratch directory as working directory;
    - `touch` and `rm`;
    - case-variant, symlinked and directory-symlinked spellings;
    - a hard link to an installed input, a multiply-linked scratch-only pair, and `ln`.
  - `fixture.ts` ended as `export const value = 1;`. Four single-link scratch controls were admitted and executed, and neither the tree fingerprint nor the installed fingerprint changed.
  - Live: this session's hooks refused `printf probe > docs/control/local/ver004/pair-b.txt` and a Write to the same file while my bounded holder kept a marker. Each refusal named `ver004 live probe` with run `69ad19f7…` and PID `55587`. `printf probe > docs/control/local/ver004/single.txt` was admitted, and `pair-a.txt` kept its bytes.
  - Regression strength: my replica of the new runner test's assertions fails against the inherited checkpoint-13 classifier and against four mutations. Removing the guard, or raising it to `nlink > 2`, fails the three hard-link assertions. Applying it to directories fails the `scratch` assertion. Checking the lexical path instead of the physical one fails the symlink assertion. The generated-hook fixture executes an admitted Write in place, so an admitted hard-link Write would change `fixture.ts` and fail its fingerprint assertion.
- **VER-002 F1–F3 and VER-001 F1–F3 — remain resolved.**
  - `scripts/test-runner.test.mjs`: 19/19, including the VER-002 case-alias and link-traversal tests.
  - `node --test --test-name-pattern=WO-125 scripts/test-harness.mjs`: 4/4 in 120.8 s (runner 40.1 s, evidence 27.3 s, entry 52.4 s, killed owner 0.9 s), with test-owned stage release.
  - The AC2 reproduction covers VER-001 F1 and F2.
  - The new check only adds refusals, so it cannot re-admit a destination the earlier repairs refused.

## Findings

None.

## Observations

- **O1 — hard links outside the worktree.** The guard runs before the worktree test, so an existing multiply-linked file outside the worktree now classifies as an input and is refused during a live gate. In my matrix, `<outside>/b.txt` returns `true` where the inherited classifier returned `false`. VER-003 O4 described outside destinations as non-inputs left to the outside-worktree authority. The new behavior is stricter, matches 07's unqualified sentence, and that authority already refused this session's Read outside the root. No change requested.
- **O2 — cost of the conservative rule.** D005 accepts refusing writes to multiply-linked scratch files while a gate runs, including `touch` and `rm` of such a name, which cannot change another name's bytes. `find -links +1` finds no such file in this worktree, `node_modules` included, so the rule refuses nothing here today. D005's reopening condition covers observed cost.
- **O3 — read-observer on deduplicated reads.** Twice, Claude Code answered a repeated Read with "file unchanged since your last Read", and the PostToolUse read-observer then failed with "host facts or pinned runtime unavailable": once while a marker was held and once with none. WO-125's added gate check returns immediately for events other than `PreToolUse` (`harness-host.ts` `activeGateWriteRefusal`), so this order's change is not the cause. I did not trace it further. Neither read was of an output this session authored.
- **O4 — carried forward.**
  - Unresolvable destinations refuse with "host facts or pinned runtime unavailable" instead of naming the run (VER-003 O1). In the fixture, a `MultiEdit` payload refuses the same way with or without a gate.
  - Read-only shell stays limited during a live gate: this session's hooks refused `ls` of the marker directory while the marker was held (VER-003 O2; D003's adapter limit).
  - VER-003 O3's console timing recurred in this pass's first gate (AC5) and remains nominated separately.
  - Admission is by CLI version, not by model; there is no negative acceptance control; and the live rows were not re-observed (VER-001 O1, O3 and O4).
  - The clean-room screen of the repair delta, `refs/dotln/checkpoint/WO-125/13` to `/15`, found no home paths, e-mail addresses, URLs, credentials or key material.

## Instruments under verification

- This session's hooks load the subject's pinned snapshot, which is byte-identical to the current build for the four changed runtime modules. They produced the live refusals and the O3 events, and they supply the usage meter.
- The live marker came from the subject's `beginGateRun`, called by a scratch holder that released it after 240 s; the marker directory was empty afterwards.
- The fixture probe uses the subject's `emitHarness`, generated hooks and `gateTreeHash`. The runner tests, fixtures and gate are the subject's own code.
- The HEAD comparison transpiles HEAD's adapter independently. The mutation driver loads the checkpoint-13 classifier and altered in-memory copies of the current one from `data:` URLs.
- As in VER-003, the compiled authority refused this session's Read of a background-task output outside the worktree root and of the gate's retained failure logs, so I read those with `cat`. My scratch drivers live in the ignored `docs/control/local/ver004/`.

## Reproduction

Run from the worktree root after `npm run build`. The scratch drivers are local and ignored.

1. `node --test scripts/test-runner.test.mjs`: 19/19. `node --test packages/skeleton/dist/test/worker.test.js`: 20/20. `node --test --test-name-pattern=WO-125 scripts/test-harness.mjs`: 4/4. `node --test --test-name-pattern=WO-125 scripts/test-plan-refutation.mjs`: 1/1.
2. `node docs/control/local/ver004/transport.mjs`: the AC2 comparison.
3. `node docs/control/local/ver004/mutation.mjs .`: the classifier matrix for the current classifier, the inherited checkpoint-13 classifier and the four mutations.
4. `node docs/control/local/ver004/probe-hooks.mjs .`: 23 generated-hook attempts, first without and then with a held marker. Admitted attempts execute inside the temporary fixture only.
5. `node docs/control/local/ver004/live-holder.mjs 240` in the background, then the two redirects and the Write listed under the live bullet above.
6. `shasum -a 256` of the four modules under `packages/skeleton/dist/src/` and under `.runtime/harness/52e7c7bffd5a00b7/packages/skeleton/dist/src/`.
7. `npm run harness -- evidence` on this report's bytes, retried once after diagnosis as product 07 prescribes. The runner's gate records hold the results.

## Goal alignment

WO-125 lets a Codex refuter or worker launch at `max`, and its repair keeps verification inputs stable. Both serve the independently verified source-to-deliverable loop on the critical path. The repair is proportionate: one check in a classifier the order already owns, a regression that fails without it, and generated-hook cases that execute attempted writes.

- **NoOp and the alternative verdict:** a fail needs a supported defect, and I found none in the order's scope. Holding the order open would spend another repair cycle, a gate and operator attention without a demonstrated benefit. Reopening condition: a multiply-linked file refused in real work, a supported filesystem that misreports link counts (D005), or an alias outside the kinds swept here: case, symlinks, directory symlinks, working directories, hard links and installed inputs.
- **Naive Interventionism:** the change only adds refusals. Valid markers, single-link scratch, the transport and the final input comparison are unaffected, and the change is locally reversible.
- **System traps:**
  - Rule beating: checked by mutation; the shipped assertions fail without the guard.
  - Policy resistance: the rule refuses a few operations that cannot change inputs (O2). That is acceptable while no such file exists.
  - Escalation and tragedy of the commons: this pass swept the alias category once more instead of opening a fourth cycle, and ran one full canonical gate instead of two (see Process cost).
  - Shifting the burden: the guard refuses the write up front instead of leaving the final comparison, or the operator, to catch it after the gate's cost is spent.
  - Drift to low performance: the gate keeps its refusal claim instead of normalizing a documented exception.
  - Success to the successful and seeking the wrong goal are immaterial: the check targets the stated behavior.

## Process cost

- **At entry:** 40,907 tokens (source `claude-transcript-message-usage`, scope `dispatch`, 2026-09-12T14:13:06Z).
- **Before this report:** 2,324,376 tokens over 67 steps and 40 commands (2026-09-12T14:29:59Z):
  - 2,038,376 cached input
  - 210,481 cache-write input
  - 75,099 output
  - 420 uncached input
- **What that includes:** useful work and waiting, including the 120.8 s fixture run, the 62.9 s plan case, the probes and the 240 s held marker. It excludes both gate runs and this amendment.
- **Tradeoff:** VER-001 to VER-003 each ran the canonical gate during verification and again on their finished bytes. I ran targeted suites and probes first, then the full gate once on the report's first bytes (838.97 s) and its 46.88 s retry. Reporting that outcome needed this amendment and one more gate record on the final bytes, which reuses suites whose inputs are unchanged.
- **Not measured:** no equivalent alternative verification was measured, so no efficiency comparison is claimed.
