# WO-056 VER-001 — Live blinded verification and repair

**Verdict: pass.** All six acceptance criteria are met at the recorded
subject, including the two runtime repairs the operator's 2026-09-20 scope
expansion added. Criteria 1, 2 and 3 are met by `claude-live-3`; criteria 1
and 2 additionally by `codex-live-3`.

Dispatch: `resume: verify`, 2026-09-20, recorded by the harness.
Subject: the uncommitted `wo-056` worktree at `37a729ca` (WO-140 integrated
during execution), staged as application `v0.33.2` with skeleton `0.29.2`.
No branch commit exists, as the phase requires. The working tree was
byte-identical before and after this verification (`git status --short`
unchanged); nothing here edited the subject.

Order: [WO-056](../../work-orders/WO-056-live-verification-and-repair.md).
Executor evidence: [README](../../evidence/WO-056/README.md),
[implementation](../../evidence/WO-056/implementation.md),
[decisions](../../evidence/WO-056/decisions.md).

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"xhigh","source":"operator-attested"}

The human operator dispatched this verification and ran no part of it; the
model above is the one the operator selected for this session and the effort
is the session's `CLAUDE_EFFORT`. Neither is effective readback. The model
differs from the executor's (`claude-fable-5-1`), so the judgment below is
not the implementing model reviewing itself.

## Criterion 1 — a live finding names the violated clause while the superficial test passed

**Pass.** Expected: at least one harness's live verifier returns a finding
naming the violated contract clause with expected, observed, reproduction and
evidence references, while the superficial test passed.

Observed, in [claude-live-3.json](../../evidence/WO-056/claude-live-3.json):
at planted commit `08ad3d69` (`sum.mjs` = `left + Math.abs(right)`) the host's
confined witnesses are `superficial` (`node focused-test.mjs`) **exit 0,
outcome pass** and `contract` (`node contract-test.mjs`) **exit 1, outcome
fail**; the planted double took the focused test from exit 1 to exit 0 with a
`completed` envelope. The verifier evaluated `AC-positive` pass and
`AC-signed` fail and returned one **blocking** finding
`finding_AC-signed_contract_1` on `AC-signed`, with `observed: "exit 1"`,
`expected: "exit 0"`, `reproductionSteps` = the adverse witness's step plus
the exact named command `node contract-test.mjs`, `evidenceRefs` = only
`08ad3d69…:host-test:1` (that criterion's adverse witness), and
`likelySurface: ["sum.mjs"]`. The receipt's `clause` field carries
"add(left, right) returns the numeric sum when either argument is negative.",
which is `AC-signed`'s description verbatim. The envelope summary names the
cause independently: `Math.abs(right)` discards the sign, so `add(4,-7)=11`
and `add(-2,-3)=1`.

[codex-live-3.json](../../evidence/WO-056/codex-live-3.json) carries the same
finding shape (`finding_1`, blocking, `AC-signed`, exit 1 against exit 0, the
adverse witness referenced, `sum.mjs` blamed, the same clause).

Reproduction: `sed -n '156,215p' docs/evidence/WO-056/claude-live-3.json`;
the same finding bytes appear in the published log at `evt_29`/`evt_30`
(receipt lines 357-358), so the stated facts are not a separate assertion.
Evidence: both receipts; `docs/evidence/WO-056/receipt.mjs:684-754`, whose
`findingPass` invariants I read and which require exactly these properties.

## Criterion 2 — the repair touches only the finding's surfaces and re-verification passes from the original contract hash

**Pass.** Observed in `claude-live-3`: a round-1 order for that finding with
`surfaces: ["sum.mjs"]`, `tests: ["node contract-test.mjs"]`,
`grantIds: []`, `contractHash: fnv1a64:6d92217e2b53cd40` and
`executionBaseCommit` = the planted commit. The repair commit `d753f73c`'s
`diffPaths` are exactly `["sum.mjs"]`; the focused test went exit 1 → exit 0;
the host's own reproduction exited 0; `workerAttempts: 1`.

Re-verification is a distinct episode (`…_verify_1_…` against `…_verify_0_…`)
at `subjectRevision d753f73c`, with the same `contractHash`,
`contractUnchanged: true`, both rows `verified`, both witnesses exit 0,
`openFindings: []`, loop `complete` at round 1 and host exit 0.
`codex-live-3` records the same five facts.

Reproduction: `sed -n '225,296p' docs/evidence/WO-056/claude-live-3.json`.

## Criterion 3 — the event log replays negative against an injected implementer success event

**Pass, reproduced first-hand.** I rebuilt (`npm run build`) and re-executed
`node docs/evidence/WO-056/replay.mjs docs/evidence/WO-056/claude-live-3.json`
myself: **exit 0**, `control` = `{"genuineResultAdmitted":true,
"erasureHonoredFromVerifier":true}` (both controls live, so the result is not
vacuous), 2 appended injections (`implementer-success-event`,
`implementer-episode-forged-pass`) each `unchanged: true`, 6
pending-position injections each `matrixUnchanged: true` and
`tailEqualsRecorded: true`, and `negativeRetained: true`. `AC-signed` stays
`failed` with its finding open in every projection.

I then compared my re-executed replay with the receipt's recorded
`foldReplay` by JSON equality: **identical**. The suite's regression
re-executes the same replay in a child pinned to the recorded compiler
identity and additionally requires a tampered log to throw
(`packages/skeleton/test/live-verification-receipt.test.ts:275-344`).

`codex-live-3` withholds its log (absolute Node path in the hashed capsule),
claims no `foldPass`, and I did not count it toward this criterion.

## Criterion 4 — every claim labeled

**Pass.** Every receipt fact carries `epistemic` of `observed`,
`launch-claim` or `unknown`; launch identity is the only `launch-claim` and
its `effectiveReadback` is `unknown`. Withheld facts are `unknown` with a
null value and a reason — `codex-live-3`'s `eventLog` ("the absolute test
command embeds a machine path in the hashed capsule") and `foldReplay`. The
Codex finding's reproduction step carries the `<node-executable>` placeholder
rather than the machine path. `validateReceipt` refuses an unlabeled fact, a
claim label on anything but launch identity, and a value on an `unknown`.

## Criterion 5 — write-backs land

**Pass.** Verified in place: roadmap 06 the vertical rung's verification
sentence (`docs/product/06-roadmap.md:1624-1634`) and the WO-056 activation
note (`:22-34`); the dated `runtime.independent-verification` reassessment in
`docs/planning/capability-table.md:271-280`, which states level 1 and names
the remaining gate honestly, including "as executor evidence awaiting
independent verification"; the ledger duty discharged by 11 WO-056 rows in
`docs/lineage/decisions-index.md` (the order predates 2026-09-09). The
expansion's added write-backs are present: product 02 §Independent
verification v1's WO-056 paragraph (`:1029-1049`) and
`packages/skeleton/README.md:674-688`. Each matches the code I read.

## Criterion 6 — gate, clean diff, and the amended change bound

**Pass as amended.**

- `npm test`, run by me on the integrated tree outside the harness sandbox
  (no `outside-sandbox` refusal was raised): **21 passed, 0 failed, 254.08 s,
  65 fresh tasks, exit 0.** This reproduces the executor's claim of 21/0.
- `git diff --check`: exit 0.
- No dependency added. The only manifest change is skeleton `0.29.1` →
  `0.29.2` with the console pin and lockfile following.
- Generated configuration is regenerated pins only: normalizing hashes in
  `git diff HEAD -- .claude/hooks/`, the 14 hook files and the manifest
  differ solely in the runtime snapshot id, the pinned file hashes and
  `skeletonVersion`. No hook logic changed.
- `npm run harness -- check` exit 0 (31 generated surfaces);
  `npm run publication:check` exit 0;
  `npm run release -- check-surfaces --local` exit 0.
- All four evidence editions verify against the changed source:
  `verification`, `artifact-identity`, `authority` and `feedback` `--check`
  each exit 0, with `authority` and `feedback` selected at WO-056 in
  `docs/evidence/current.json`, as D009 records.
- `npm run plan -- check` exit 0, 11 passes, and WO-056 appears as
  `authorized-execution-amendment` bound to `WO-056-D008` — the binding the
  role requires when an operator scope expansion changes a judged order.

## Scope-expansion conformance

The expansion authorizes two runtime surfaces only, and keeps "the admission
and derivation rules, the finding shape and the event schema" and "every
existing inspection, writer and `verification-snapshot-v1` argument vector"
at their exact bytes. I checked each:

- The `verification-protocol.ts` diff is confined to a new `findingContract`
  helper, `evidenceResultSchema` and the verifier branch of
  `transportPrompt`. The admission rule `"finding observed versus expected"`
  (`:320-331`) is unchanged, and `repair.ts` is not modified at all, so
  derivation (`:220-226`) keeps its bytes.
- The claim that this states a rule the host **already** enforced is true and
  predates the order: product 02 has required since WO-010 that "the
  observed/expected pair must match an adverse host witness"
  (`docs/product/02-domain-model.md:925`).
- The schema constraint cannot exclude an admissible finding: `observed` and
  `expected` become an enum of the adverse witnesses' strings, which is a
  superset of the single-witness pair admission requires, and the enum is
  omitted entirely when no adverse witness exists. It also leaks nothing —
  those strings are already in the capsule the verifier reads.
- The `reproductionSteps` constraint is scoped by `subject.snapshot`, which
  is exactly the `worktree-snapshot` profile: `WorktreeSnapshot.profile` is
  the literal `"worktree-snapshot"`
  (`packages/compiler/src/verification.ts:79-85`) and a pre-existing check
  binds `profile.profileId` to it or to the legacy default
  (`verification-protocol.ts:457-460`).
- `--skip-git-repo-check` is emitted only for an evidence request in that
  profile (`worker-transport.ts:305-315`). The legacy verifier, inspection
  and writer vectors are asserted free of it
  (`verification.test.ts:780-797`, `worker.test.ts:168-169`,
  `verification-worktree.test.ts:598-635`).
- Three regressions hold the repairs and are not vacuous: the finding-contract
  test ties the enums to the witnesses, checks the instruction text, admits a
  finding drawn from the stated values, and requires the exact 2026-09-20
  live shape to be refused by the unchanged admission rule and a paraphrased
  step to yield `NeedsHuman`.

## Observations (non-blocking; not routed to repair)

1. **Product 03 will read stale after merge.**
   `docs/product/03-architecture.md:837` and `:1698` still frame live model
   verification as the pending, separate WO-056 proof. Those sentences are
   true while this order is open and unmerged, and 03 is outside criterion
   5's write-back list, so this is not a criterion failure. The executor
   disclosed it in `implementation.md` §Limits but did not board it up in
   `decisions.md` with a named follow-up, which the role's own rule asks for
   rather than leaving a met-and-unfixed item as a report sentence. Final
   review, which integrates `main` and retimes, is the right place to decide
   whether to write it back now or record a follow-up.

## Limits of this verification

- I judged the **filed receipts**, not the vendor wire. The live episodes
  need the operator's outside terminal and authenticated CLIs; I did not and
  could not re-run them. The receipt contract is a consistency contract, not
  authentication: the log's launch fields are not hash-bound, so a deliberate
  forger with write access could still fabricate a receipt. Filing remains
  the operator's act.
- One synthetic two-clause repository, one passing episode per harness. The
  capability stays at level 1; nothing here evidences dependability.
- `codex-live-3`'s log is withheld, so its criterion 3 is unverifiable by
  anyone but its own machine; I counted it for criteria 1 and 2 only.
- Criterion 2's repair and re-verification facts are host observations with
  no published log behind them, as the executor discloses.
- I verified the operator's scope expansion only through the quoted record in
  `decisions.md#wo-056-d008` and the amended order text; I have no
  independent copy of the operator's message.
- The four earlier failed attempts are filed and claim no passes; I confirmed
  that rather than re-diagnosing them.

## Instrument disclosure

An instrument under verification also recorded and constrained this work. The
subject includes the regenerated `.claude/hooks/*` and
`.claude/harness-manifest.json` pinned to skeleton `0.29.2`, and those hooks
enforced DotLn's refusals on this session: during the live `npm test` gate
they refused several of my read commands as possible gate-input writes, and
one scratch redirect was refused for lacking an outside-write grant for this
role. No judgment above depends on that: every refused command was re-run in
an allowed form after the gate finished, and the gate itself ran to
completion and exit 0. The subject also includes
`packages/skeleton/src/verification-protocol.ts`, the DotLn verification
runtime — this role's own workflow does not execute it; it was exercised only
inside the suites and the replay I re-ran.

**Process cost:** entry 83765 tokens; handoff 16840309 tokens; source claude-transcript-message-usage
