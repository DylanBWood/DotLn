# WO-100 VER-003 — preauthorized portfolio after VER-002 repair

**Verdict:** pass

Verifier: Codex CLI 0.155.1, GPT-6 Sol at xhigh, judging the uncommitted WO-100 subject selected by `resume: verify` on 2026-09-22. The active session supplied the model, effort and CLI version readback. No verifier subagent was launched: 0 observed of the configured cap of 20, with any unobserved remainder unknown. This transport exposes no hard dollar-cap control, so a USD 5 limit was unenforced. WO-100 changes harness probes and generated hooks; this judgment uses the current implementation, work-order fixture, current prose and executed gate, rather than those instruments' own claims alone.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-6-sol","effort":"xhigh","source":"codex-session-readback"}

**Process cost:** entry 99346 tokens; handoff 2615205 tokens; source codex-transcript-counter

The entry reading was dispatch scoped at 2026-09-22T19:56:28Z and the handoff reading at 20:03:32Z. The counter reports 2,524,544 cached input tokens at the later cutoff; USD and command count are unavailable. One full product gate took 282.21 s, dominated by the skeleton suite (279.43 s); the document gate took 29.20 s. No paid external worker or separate scratch reproduction was launched in this pass. This is observed work and waiting, not a claimed saving against another method.

## Acceptance judgment

| Criterion | Judgment and evidence |
| --- | --- |
| 1 — bounded derivation and durable records | Pass. `packages/skeleton/src/portfolio.ts` derives one result per WO-119 candidate, checks mechanics, paths, phase effects, file ceiling and the phase envelope before creating an order. The `portfolio` unit fixture checks the six candidates and bounds; `scripts/test-portfolio.mjs` materializes admitted orders through WO-120 and checks their identities and index rows, while suggestions and `NeedsHuman` allocate no order. |
| 2 — activation, source change, verification and curve | Pass. The end-to-end suite records the `host-policy` grant before the WO-052 change, uses the real WO-052/WO-054 hosts with worker doubles, advances on an admitted pass and resets on a failed check. Its Sort cases require an exact move; a copy fails before verifier dispatch. The pathless `npm test` cases reach WO-054 using files in both snapshots and show one pass and one failed named check. Source inspection confirms `verificationSurfaces` and the all-criteria, no-escalation verdict rule. |
| 3 — budget refusal | Pass for the declared admission behavior. The resident fold checks episodes, wall time and reported tokens before selection. The fixture records two episodes, then one `ScriptEpisodeRefused` with the exhausted-episodes reason and no third portfolio dispatch. Charging wall time and tokens at episode end means the final admitted episode can exceed a remaining balance; D001 and D006 record that limit. |
| 4 — write-backs | Pass. Product 06 allocates the portfolio and records the always-on model candidate; product 03 states progressive authority and the open follow-ons; product 07 explains portfolio declaration. The pre-2026-09-09 ledger duty is discharged by `docs/evidence/WO-100/decisions.md` and its generated index row. The authorized model-migration guide now states the same Opus 5.5 and GPT-6 Sol `xhigh` defaults as `scripts/refute-plan.mjs`, while explicitly keeping effective selection unknown without independent readback. |
| 5 — checks, pins and edition | Pass. This verifier ran `npm test`: **26 suites passed, 0 failed, 282.21 s, 70 fresh tasks**. `npm run test:docs` passed **20 suites, 0 failures, 29.20 s, 20 fresh tasks**. `git diff --check` is clean. The lockfile retimes skeleton and its internal console pin without a new external dependency. `docs/evidence/current.json` selects WO-100 feedback-001, authority-002 and artifact-identity editions; the product gate accepted their checks and regenerated bundle. |

## Earlier findings and current subject

**VER-001 F1, major — resolved.** Expected: an admitted Sort move checked by a command such as `npm test` reaches WO-054 and advances only after a pass. Previously, neither the moved paths nor that command supplied a shared snapshot surface, so verification threw before dispatch. The current `verificationSurfaces` selects the shared tree when its narrower selection is empty; the end-to-end fixture exercises an exact move with `npm test`, checks WO-054's criterion surfaces, and observes both a passing and failing named check. The host still requires an exact relocation and WO-054 still receives real paths present in both trees. The earlier verifier's scratch Git reproduction is in VER-002; this pass reproduced the behavior through the current end-to-end fixture and inspected the source.

**VER-002 F1, minor — resolved.** Expected: the active planning-refutation guide states the current external CLI defaults and keeps the effective-readback limit. Previously it said Claude Fable 5.1 at max and GPT-6 Astra at unknown. The current guide's §Execution and preservation states `claude-opus-5-5` and `gpt-6-sol`, both at `xhigh`, matching the default expressions in `scripts/refute-plan.mjs`; its last sentence still says effective model and effort are unknown unless independently observed. A sweep of active prose and source found remaining old-model mentions as previous selections, historical release/run evidence or migration comparisons, not an active conflicting default. The script and guide comparison reproduces the correction without launching an external CLI worker.

## Judgment and limits

WO-100 connects executable discovery to the later unattended hour through bounded, independently verified work. **NoOp** would keep VER-002's active operator guide inconsistent with the command. The narrow correction and the existing portfolio fixtures advance the selected outcome. **Naive Interventionism:** the prose repair leaves the command and effective-readback caveat intact; the earlier host repair preserves snapshot validation and exact relocation, with the 100-file verification snapshot limit still applicable.

**Rule beating** is checked by the failing twin of the pathless Sort case and by reading guide bytes against the executable defaults, rather than treating green tests as proof of prose accuracy. **Policy resistance** is checked at portfolio admission and phase authority. **Drift to low performance** and **seeking the wrong goal** are checked against the verified change and correct operator instruction, not order or suite counts. **Tragedy of the commons** weighs the full gate's 282.21 s against its required application evidence and records the cost above. **Escalation** and **shifting the burden to the intervenor** are limited by durable host behavior instead of routine operator rescue. **Success to the successful** was considered by comparing the guide and command independently and retaining the accepted common-surface repair only where narrow paths are absent.

No live unattended hour or external model worker ran here; the order accepts fixture hosts and the recorded feedback edition. The gate establishes the current application checks, while the source and guide comparison establish the repaired instruction. Effective model and effort of external workers are not inferred from requested CLI defaults.
