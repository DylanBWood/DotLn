# WO-100 VER-001 — preauthorized portfolio and work derivation

**Verdict:** fail

Verifier: Codex CLI 0.155.1, GPT-6 Sol at xhigh, independently judging the uncommitted WO-100 subject selected by `resume: verify` on 2026-09-22. The current Codex session supplied model, effort and CLI version readback. This order also changes harness probes and generated Claude hooks; this report uses source inspection, a separate scratch Git reproduction and the executed product gate as evidence, rather than treating that instrument's own claims as proof. No verifier subagents were launched; the observed count is 0 of the configured cap of 20. The transport exposes no hard USD cap here, so a USD 5 limit was unenforced.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-6-sol","effort":"xhigh","source":"codex-session-readback"}

**Process cost:** entry 46083 tokens; handoff 5249399 tokens; source codex-transcript-counter

The entry counter was dispatch scoped at 2026-09-22T19:05:30.175Z; the handoff counter was dispatch scoped at 2026-09-22T19:14:06.759Z. USD cost was unavailable. The full product gate ran once in this verifier session and took 286.65 s; the document gate passed 20 suites in 33.07 s. The focused scratch probe cost one local Git reproduction; there was no paid external worker launch. Waiting for the broad skeleton suite dominated observed wall time. These are observed activities, not an estimate of savings against another verifier method.

## F1 — a valid Sort move with a pathless check cannot reach independent verification

- **Criterion:** acceptance 2: an in-portfolio misplaced-file candidate derives a bounded order that the resident executes through WO-052, verifies through WO-054 and advances only after a pass.
- **Severity:** major. The order is admitted and the source change can be correct, but the host fails before the required verifier runs for a common valid command shape.
- **Expected:** A portfolio may declare `"misplaced-file": ["npm test"]`; after an exact relocation, the host prepares a valid WO-054 snapshot criterion, runs the named check and admits its actual verdict.
- **Observed:** `decodePortfolio` accepts `npm test`. For a move from `loose/guide.md` to `docs/guide.md`, neither order path exists in both snapshots; neither token in `npm test` names a tracked path. `verificationSurfaces` returns `[]`. `portfolioExecution.verify` then throws `portfolio verification has no surface in both trees` before opening WO-054. The portfolio actor converts that host error to a failed episode, so the phase resets even when the move and check could pass.
- **Reproduction:** In a system-temp scratch Git repository, commit `loose/guide.md`, move the unchanged blob to `docs/guide.md` and commit, then call the built `relocationHolds` and `verificationSurfaces` functions with those two commits, the move, order surfaces `["loose/guide.md", "docs/guide.md"]` and tests `["npm test"]`. Observed JSON: `{"relocationHolds":true,"verificationSurfaces":[]}`. `packages/skeleton/src/portfolio-host.ts` lines 226–233 show the consequent throw before verifier setup.
- **Why the gate missed it:** the existing Sort fixture declares `node checks/test.cjs`; `checks/test.cjs` is a tracked file in both snapshots, so that command supplies a criterion surface. It does not exercise an admitted check with no shared path token. The current gate and fixture success therefore do not establish this required case.
- **Repair follow-up:** [WO-100-D009](../../evidence/WO-100/decisions.md#wo-100-d009) records the named `resume: fix` work and a regression fixture. Preserve WO-054's real shared-surface requirement while making admitted Sort verification commands usable.

## Acceptance judgment

| Criterion | Judgment and evidence |
| --- | --- |
| 1 — bounded derivation and durable records | The WO-119 six-candidate fixture and the real WO-120 materializer pass in the current `portfolio` and skeleton suites. Source inspection confirms candidate paths, size, phase effects and `host-policy` grant in the derived order. The Sort variant in F1 limits the claim that every in-portfolio candidate can complete its required verification. |
| 2 — activation, change, verification and curve | **Fail, F1.** The existing e2e fixture demonstrates activation before source change, an admitted pass advancing `widen` to `peak`, and a failed check resetting to `probe`; the scratch reproduction demonstrates an admitted Sort check that never reaches WO-054. |
| 3 — budget refusal | Pass for the declared admission behavior: both WO-100 suites show two episodes followed by one reasoned `ScriptEpisodeRefused` and no third portfolio dispatch. The executor's recorded limitation remains: wall time and reported tokens are charged when an episode ends, so the last admitted episode may exceed its remaining balance. |
| 4 — write-backs | Product 06 allocates the portfolio candidate; product 03 records progressive-authority status; product 07 adds the portfolio declaration under operator resume phrases; product 05 and the skeleton README describe it. WO-100's pre-2026-09-09 ledger duty is represented by its decisions file and generated index row. |
| 5 — checks, pins and edition | This verifier ran `npm test`: **26 suites passed, 0 failed, 286.65 s, 70 fresh tasks**. `git diff --check` was clean. The package changes retime skeleton and its internal console pin; no external dependency was added. The current evidence selector points to WO-100 feedback-001, authority-002 and artifact-identity editions; the product gate accepted the regenerated bundle and evidence checks. These passing checks do not override F1. |

No earlier WO-100 VER existed in canonical status. D006's earlier read-only review fixes were checked against the current source and passing Sort fixture; F1 is a separate command-shape case.

## Judgment rationale and limits

WO-100 is the link between the executable candidate producer and the later unattended hour, so a stranded in-portfolio Sort order blocks the promised verified loop. The smallest useful probe was an exact move in a scratch repository with the accepted `npm test` command. **NoOp** would keep that admitted order unable to advance. **Naive Interventionism:** the repair must keep the existing snapshot validation and host relocation check, and should change only the common-surface selection needed for this case; broadening the verifier's claims without a real shared surface would be second-order harm.

The concrete **rule-beating** signal is a green fixture with a path-bearing command while another admitted command fails. **Drift to low performance** and **seeking the wrong goal** favor the executable loop over counting green suites or allocated orders. **Policy resistance** requires the repair to preserve the compiled floor and WO-054 snapshot rules. **Tragedy of the commons** supports one regression and one required gate rather than repeated broad runs; process cost above accounts for this gate. **Escalation** and **shifting the burden to the intervenor** favor a host repair over routine operator rescue. **Success to the successful** was checked by comparing the existing host's shared-surface approach with the accepted command grammar; the current fixture's success is not a reason to exclude the latter.

The verifier did not run a live external model or a live unattended hour; both are outside the fixture-based acceptance of this order. External CLI model-selection changes were inspected in active source sites and exercised by the product suite, while historical recorded selections were preserved. The host failure in F1 is established without relying on an external worker's judgment.
