# WO-100 VER-004 — portfolio after FINAL-001 repair

**Verdict:** pass

Verifier: Codex CLI 0.155.1, GPT-6 Sol at xhigh, independently judging the current uncommitted WO-100 subject selected by `resume: verify` on 2026-09-22. The active session supplied the model, effort and CLI version readback. No verifier subagents were launched: 0 observed of the cap of 20, with any unobserved remainder unknown. This transport exposes no hard dollar-cap control, so a USD 5 limit was unenforced. WO-100 changes harness probes and generated hooks, which also recorded this work; the judgment below uses current source, the independently executed gates and fixture behavior rather than the instrument's own assertion alone.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-6-sol","effort":"xhigh","source":"codex-session-readback"}

**Process cost:** entry 39049 tokens; handoff 4936334 tokens; source codex-transcript-counter

Both readings are dispatch scoped from `node scripts/harness.mjs usage 01a0cae4-90a9-7cd3-843b-85beef127b1b`, observed at 20:53:31Z and 21:08:50Z. The later reading includes 4,783,488 cached input tokens; dollar cost and command count are unavailable. This verification ran plain `npm test` (282.60 s), the review selection (466.02 s), and `npm run test:docs` before and after the report's first write (29.25 and 29.57 s). The second product gate was needed because FINAL-001's failing `runner-fixtures` check is selected in review; the final cost update passed Prettier and was read back. No external model worker or separate scratch reproduction was launched. These are observed costs, not a saving estimate.

## Acceptance judgment

| Criterion | Expected and observed evidence |
| --- | --- |
| 1 — bounded derivation and durable records | Pass. `deriveWorkOrders` maps WO-119 candidates to one bounded outcome each and checks mechanics, surfaces, file count, phase effects and authority before creating an order. The current `portfolio` unit and end-to-end fixtures exercise the six-candidate producer, materialize admitted orders through WO-120, find their index rows and show that suggestions and `NeedsHuman` allocate no order. The derived grant records `grantedBy: "host-policy"`. |
| 2 — activation, change, verification and curve | Pass. The end-to-end fixture uses the real WO-052 and WO-054 hosts with worker doubles. An order fails verification in `widen` and resets to `probe`, where a pass would have advanced to `peak`; a second `widen` order passes and advances. The Sort cases require an exact relocation, including with a pathless command; a copy fails before verifier dispatch. `portfolio-host.ts` admits a pass only after all criteria pass without a finding or human escalation. |
| 3 — budget refusal | Pass for admission. With an eligible Sort candidate still available in `peak`, the fixture records a reasoned `ScriptEpisodeRefused` after two episodes and no third portfolio dispatch. The unit fixture separately checks exhausted wall time and reported tokens while episodes and an eligible candidate remain. Charging wall time and tokens at episode end can let the last admitted episode exceed its remaining balance, as D001 and D006 record. |
| 4 — write-backs | Pass. Product 06 allocates the portfolio candidate and records the always-on model candidate; product 03 states progressive authority and its open follow-ons; product 07 explains declaration and the outside-only gate cause. WO-100's pre-2026-09-09 ledger duty is represented by its decisions file and generated index row. The active refutation guide matches `scripts/refute-plan.mjs` for Opus 5.5 and GPT-6 Sol at xhigh while keeping effective selection unknown without readback. |
| 5 — gates, pins and evidence | Pass. This verifier ran `npm test`: **27 passed, 0 failed, 282.60 s, 71 fresh tasks**; `npm test -- --review`: **38 passed, 0 failed, 466.02 s, 82 fresh tasks**; and `npm run test:docs` twice: **20 passed, 0 failed** each time, in 29.25 and 29.57 s. The second document gate ran after this report was written. The review gate passed `runner-fixtures`, the suite that failed in FINAL-001. Both `git diff --check` and `git diff --cached --check` are clean. The lockfile retimes skeleton and console without adding an external dependency. The selected WO-100 feedback, authority and artifact-identity editions passed their review and document checks; generated harness checks passed. |

## Earlier findings at the current subject

**VER-001 F1 — resolved.** Expected: an exact Sort move with a named command such as `npm test` reaches WO-054. `verificationSurfaces` now falls back to paths shared by both trees when no narrow path survives, and the current end-to-end Sort fixture exercises passing and failing pathless checks. WO-054 still receives real shared files, and the host still checks the exact relocation. The current `portfolio` suite passed in both product gates.

**VER-002 F1 — resolved.** Expected: the active planning-refutation guide states the command's current defaults. Its §Execution and preservation names `claude-opus-5-5` and `gpt-6-sol` at `xhigh`, matching `scripts/refute-plan.mjs`; it separately says effective selection is unknown without independent observation. [D019](../../evidence/WO-100/decisions.md#wo-100-d019) corrects an unrelated nonexistent section citation in the older decisions and immutable VER-002 without changing their bytes.

**FINAL-001 F1 — resolved.** Expected: a suite declared outside-only has an evidenced environmental cause and the gate inventory names it. [D018](../../evidence/WO-100/decisions.md#wo-100-d018) ties the declaration to the in-profile [Seatbelt receipt](../../evidence/WO-100/repair-003-portfolio-in-seatbelt.tap), where both end-to-end tests stop at the refused nested discovery launch; the outside [suite receipt](../../evidence/WO-100/repair-003-portfolio-e2e.tap) passes three of three. The runner comment, inventory expectation and product 07 §Gate sandbox preflight now name `portfolio` beside `skeleton`. I reproduced the decisive gate outcome: `runner-fixtures` and the full review gate pass on the current subject. The in-profile receipt itself was produced by the repair session; I inspected its source and did not claim an independent in-profile rerun.

**FINAL-001 F2 and F3 — resolved.** The two current release labels say the Fable 5.1 pin lasted through `v0.43.0`, matching the tag/source evidence in [D020](../../evidence/WO-100/decisions.md#wo-100-d020). D019 records that the older security-document citation meant its Entropy Reducer launch-line paragraph, which states the same default values but describes a different command. The binding comparison remains the guide against `refute-plan.mjs` itself.

**FINAL-001 F4 — resolved.** Both curve fixtures now fail in non-last `widen`, so a return to `probe` distinguishes reset from an advance. They refuse a third order with an in-phase candidate still available, and the unit fixture covers wall-time and reported-token refusals separately. `decodePortfolioBinding` accepts only a 40-hex base, the form WO-054 prepares. [Repair-003](../../evidence/WO-100/repair-003.md) records negative controls under which the new assertions fail; source inspection and the passing current gates support the repaired behavior.

## Judgment and limits

WO-100 connects executable discovery to the later unattended hour through bounded, verified work. **NoOp** after FINAL-001 would leave the reviewer gate red. The repair closes its gate record and makes reset and budget tests discriminate the intended behavior. **Rule beating** is checked by running the review selection, where the earlier invariant failed, and by checking an eligible candidate remains at budget refusal. **Policy resistance** is checked at floor and phase admission; **drift to low performance** and **seeking the wrong goal** are checked against the verified change rather than suite counts alone. **Tragedy of the commons** is the cost of two broad gates, reported above; **escalation** and **shifting the burden to the intervenor** favor the durable inventory and fixtures over operator rescue. **Success to the successful** is addressed by judging the current suite declaration against its own environmental evidence, not assuming the old single-suite inventory was complete. **Naive Interventionism:** the repair retains the existing outside-only declaration and source-change and snapshot rules while updating its evidence, fixtures and the impossible 64-hex binding admission.

The verifier did not run a live unattended hour or external model worker; the order accepts fixture hosts and a recorded feedback edition. The in-profile sandbox comparison and negative-control mutations are repair receipts, independently checked here against the current source and passing gates rather than rerun. [D016](../../evidence/WO-100/decisions.md#wo-100-d016) boards the inherited WO-052 committed-file-count limit before WO-111 uses a portfolio unattended on an operator repository; [D017](../../evidence/WO-100/decisions.md#wo-100-d017) boards the integration-helper intent-to-add defect. Neither is a new WO-100 repair finding. No new substantive defect was met.
