# WO-100 VER-002 — preauthorized portfolio after VER-001 repair

**Verdict:** fail

Verifier: Codex CLI 0.155.1, GPT-6 Sol at xhigh, independently judging the current uncommitted WO-100 subject selected by `resume: verify` on 2026-09-22. The active thread supplied the model, effort and CLI version readback. No verifier subagent was launched (0 observed of the configured cap of 20; unobserved remainder unknown). This transport exposed no hard dollar-cap control, so a USD 5 cap was unenforced. The order also edits harness probes and generated hooks; this judgment relies on source inspection, an independent scratch Git reproduction and the executed product gate, rather than an instrument's own assertion.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-6-sol","effort":"xhigh","source":"codex-session-readback"}

**Process cost:** entry 43176 tokens; handoff 3857541 tokens; source codex-transcript-counter

The entry reading was dispatch scoped at 2026-09-22T19:37:02Z and the handoff reading at 19:45:03Z. USD was unavailable. One full product gate ran in 286.15 s; two document gates passed in 29.50 s and 29.18 s after the report was written and updated. The long skeleton suite dominated waiting. The independent scratch reproduction was one local Git repository. These are observed costs; no saving against a different verification method is claimed.

## F1 — the active planning-refutation guide states the old CLI defaults

- **Criterion and scope:** criterion 4's write-backs, as expanded by the operator-authorized WO-100 model migration. The order expressly includes the planning-refutation command defaults and documents that state them.
- **Severity:** minor, but a required documentation surface remains incorrect. An operator following the guide sees model and effort values that the command no longer selects by default.
- **Expected:** `docs/planning/refutations/README.md` §Execution and preservation states Claude Opus 5.5 at xhigh and GPT-6 Sol at xhigh, while separately retaining the limit that effective model and effort have no independent readback.
- **Observed:** that active guide still states Claude Fable 5.1 at max and GPT-6 Astra at unknown. `scripts/refute-plan.mjs` lines 262–272 select `claude-opus-5-5` or `gpt-6-sol` and xhigh. `docs/AI-HARNESS-SECURITY.md` §Planning refutation already states the new defaults, so the two current guides contradict each other.
- **Reproduction:** compare the default expression in `scripts/refute-plan.mjs` with lines 112–119 of `docs/planning/refutations/README.md`; no external model launch is needed to observe the mismatch. `npm test` passed, but no semantic check in that gate caught the stale prose.
- **Repair follow-up:** [WO-100-D011](../../evidence/WO-100/decisions.md#wo-100-d011) names the bounded guide correction and its checks.

## Acceptance judgment

| Criterion | Judgment and evidence |
| --- | --- |
| 1 — bounded derivation and durable records | Pass for the declared fixture: the `portfolio` suite runs WO-119's candidates through `deriveWorkOrders` and WO-120 materialization, checks indexed identities and bounds, and leaves outside candidates as a suggestion or `NeedsHuman` with no allocation. Source inspection confirms the derived surfaces, phase envelope and `host-policy` grant. |
| 2 — activation, change, verification and curve | Pass for the fixture and VER-001 F1 repair. The end-to-end suite runs the real WO-052 and WO-054 hosts with doubles over the workers, observes a pass advancing the phase and a failing check resetting it. Its new pathless `npm test` Sort runs reach WO-054 and pass or fail with the check. In my separate system-temp Git reproduction, an exact `loose/guide.md` to `docs/guide.md` move returned `relocationHolds=true` and `verificationSurfaces=[".dotln/discovery.json","package.json"]` for `npm test`, both real shared files. |
| 3 — budget refusal | Pass for the declared admission behavior: the fixture records two episodes and one reasoned `ScriptEpisodeRefused`, with no third dispatch. Wall time and reported tokens are charged at episode end, so the last admitted episode can overrun the remaining balance, as the implementation record states. |
| 4 — write-backs | **Fail, F1.** Product 06 allocates the portfolio candidate, product 03 records progressive authority, product 07 explains declaration, and the pre-2026-09-09 ledger duty is represented by the decisions file and index row. The model-migration scope includes current documents stating command defaults; the planning-refutation guide remains stale. |
| 5 — checks, pins and evidence | The current `npm test` passed **26 suites, 0 failures, 286.15 s, 70 fresh tasks**; both `npm run test:docs` runs passed 20 suites, in 29.50 s and 29.18 s. `git diff --check` was clean at the current subject. The lockfile retimes skeleton and console without adding an external dependency; `docs/evidence/current.json` selects WO-100 feedback-001, authority-002 and artifact-identity editions. The gate's evidence checks passed. Green checks do not establish the correctness of the guide's prose. |

The earlier [VER-001 F1](VER-001.md#f1--a-valid-sort-move-with-a-pathless-check-cannot-reach-independent-verification) is resolved at this subject. The independent scratch result exercises the same command shape, and the end-to-end regression shows both a passing and a failing named check after WO-054 dispatch. The repair preserves the shared-snapshot surface rule and the host's exact relocation check. The verifier's snapshot still has the existing 100-file limit; this order's fixture remains inside it.

## Judgment rationale and limits

WO-100 links the executable candidate producer to the later unattended hour. The repaired verified Sort loop advances that critical path; the stale operator guide undermines the same order's authorized model migration. **NoOp** would leave a current instruction contradicting executable behavior. **Naive Interventionism:** the named follow-up changes one current guide sentence and preserves its useful distinction between requested defaults and unobserved effective selection; historical receipts and fixtures remain evidence of what ran.

The material **rule-beating** signal is that all 26 suites pass while the active guide is false. **Policy resistance** favors keeping the guide consistent with the command and the other current security guide. **Drift to low performance** and **seeking the wrong goal** favor an accurate operator-facing default over a green test count. **Tragedy of the commons** favors one bounded prose correction rather than another broad implementation pass. **Escalation** and **shifting the burden to the intervenor** favor repairing the guide once instead of relying on operators to reconcile contradictory instructions. **Success to the successful** was checked by comparing both active guides with the executable default, rather than treating the already-updated one as proof that all write-backs landed.

No live external CLI worker or unattended hour ran in this verifier session; the order's acceptance uses fixture hosts and the recorded feedback edition. Effective model and effort of external workers are not inferred from invocation defaults. The stale guide finding is established by current source bytes and does not depend on that unknown readback.
