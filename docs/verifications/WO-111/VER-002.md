# WO-111 VER-002 — independent re-verification of the documentary repair after VER-001

**Verdict: fail.** The repair fixed what a documentary repair can fix. The
document gate is green again. The planning map answers R2. The corrected
receipt editions rebuild byte for byte from the retained stores. The living
claims now match what the windows observed. One blocker remains, and the subject
itself records it: the live runs changed the operator's user-level Codex
configuration, which lies outside the portfolio. Both corrected receipts label
criterion 2 `unmet; no operator exception recorded`, and no operator judgment
changes that criterion (D012, D013 and D014 each decline to treat the scope
answer as a waiver). Every acceptance criterion is required, so WO-111 cannot
pass on this subject. Two new minor documentary defects are boarded as
[D015](../../evidence/WO-111/decisions.md#wo-111-d015).

Subject: [WO-111](../../work-orders/WO-111-unattended-live-proof.md), branch
`wo-111` at `4b6a19cc50e98a437d35b2adf2d392fbf5b12139`. All work is uncommitted,
as required before final review. I snapshotted every tracked and untracked file
into a scratch index (tree `907a05c8`) and compared it with the RepairCompleted
evidence tree `c3b43779` (checkpoint `refs/dotln/checkpoint/WO-111/6`). Only the
lifecycle projections differ: `docs/control/current.md`,
`docs/work-orders/README.md` and the two appended control rows. The order text
still differs from `main` only in the title's `v0.46.3`. I judged the order as
filed.

I edited no implementation, receipt, script or write-back. My writes are:

- this report;
- decision [WO-111-D015](../../evidence/WO-111/decisions.md#wo-111-d015);
- `npm run meta`'s regenerated D015 row in `docs/lineage/decisions-index.md`
  and its sync of `docs/planning/followups.json`, which minted
  `FUP-7dbf4e832a8fb124`;
- the role-mandated `npm run work-orders -- index` refresh;
- gate receipts in the ignored local lane;
- a census script in the session scratch directory, outside the repository.

I did not relaunch any live episode and did not touch the operator's Codex
configuration.

Actor: this Claude Code session. Harness `2.1.282` is read back from
`claude --version`. The model is `claude-opus-5-5[1m]` as the session exposes
it. Effort `xhigh` is read back from `CLAUDE_EFFORT`; that is the selected
effort, not a measured effective effort. I planned no subagents and spawned
none: the harness reports 0 of the 20-agent cap, exact-observed. No provider
dollar cap was enforced, because this transport exposes no hard dollar control.

Under verification is the repair attestation: codex-cli `0.156.1`,
`gpt-6-astra`, `xhigh`, source `codex-session-readback`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-opus-5-5[1m]","effort":"xhigh","source":"claude-session-readback"}

## Instrument disclosure

`repair-receipts.mjs check` replays the retained logs through the runtime under
examination (`replayResident`, `residentMachine`, `portfolioSelection` and
`residentRefusal` from the built `packages/skeleton`). As in VER-001, that is the
right instrument for a claim about this runtime's fold. It is not an independent
policy model.

The host's auto-mode classifier denied my two attempts to run that check. The
first asked to disable the sandbox; the second, sandboxed, was treated as the
same request. The operator then ran it in this session with the `!` prefix, and
its output is what I cite. The classifier also denied a read-only census of the
user Codex configuration as credential exploration. I did not pursue it by any
other route, so the repair's census counts stay unreproduced here. While my
`npm test` gate was live, the DotLn harness refused my shell calls (run
`f910a001`). I waited for the gate to finish rather than stop it.

## Criteria

**1 — producer discovery, at least three derived orders in ascending phase,
verified, first touching one file, reset on a failed verification. Met.**
VER-001 established this from the raw stores. The repair changed no raw store
or original receipt:

- `events.jsonl` still hashes to `b354e3e4…3660` and `return-events.jsonl` to
  `367c4237…a924`, as VER-001 recorded.
- `receipt.json` hashes to `833b83af…f2f0` and `return-receipt.json` to
  `3b178d9a…db6df`, the `originalSha256` values the v2 editions cite.

The v2 progression repeats the original values: 1/1/2 changed paths, three
`pass` verdicts, commits `a4dcfbab`, `e8413d5c` and `eb046ebc`. The operator-run
check rebuilt both v2 editions exactly from the retained stores.

**2 — main checkout hash, sentinel tree and every path outside the portfolio
unchanged; no remote effect. Not met (B1, carried).** The measured protected
surfaces still hold, and both v2 receipts now say what they cover: tracked and
non-ignored bytes only, with the observed ignored writes listed. But both
receipts record `outsidePortfolioChange.changed: true` for the user Codex
configuration, with `criterion2: "unmet; no operator exception recorded"`. The
README, product 06, the capability table, the critical-path R2 answer and the
planning map say the same.

**3 — mission check fired with its verdict recorded; return stopped dispatch
within one tick with the in-flight episode handled per the phase. Partially
met, not blocking, unchanged from VER-001.**

- **Mission half: met.** One `drift` verdict is recorded. The v2 receipt now
  names the judged subject (the WO-111 DotLn worktree), the separate store and
  the hold's scope. The first-window stream carries 898 mission rows beside
  833 portfolio rows.
- **Return half: met in the second window.** The v2 receipt adds the no-return
  control. With `back`, `due()` is null at `dueAt − 1`, `dueAt` and
  `dueAt + 1000`. Without it, `due()` returns `1790286813701` at and after
  `dueAt`, and the control dispatch is accepted. These match VER-001's
  independent replay.
- **In-flight clause: not exercised.** Both receipts now say so, with
  `pendingEpisodeCount: 0`, the compiled kill/finish rules and the fake-actor
  test they rest on. Whether a live in-flight return is still required remains
  the operator's or reviewer's call, as VER-001 M1 left it.

**4 — every claim labeled `observed`, `launch-claim` or `unknown`; budget
consumed reported with its method. Met.** Every top-level field in both v2
receipts carries a label.

- Both budgets state the replay method and take the ceiling from the recorded
  portfolio definition (3 and 4 episodes, 3,600,000 ms).
- Each receipt projects six per-episode launch claims (three `fresh-cli`
  writers and three `fresh-transport` verifiers), each with effective identity
  `unknown`. The first also projects the mission launch.
- Pre-back eligibility, the due time and the in-flight state are computed and
  asserted from the log prefix, not written as literals.
- Presence authorship is a `launch-claim`.
- The one remaining content slip is n2.

**5 — write-backs; replan checkpoint answered in the planning map. Met, with
n1.**

- **README:** "What runs today" (README.md:112-121) scopes the mission resident
  apart from the portfolio and names the unmet containment criterion.
- **Product 06:** the "WO-111 live rung observation (2026-09-24)" paragraph
  (06-roadmap.md:982-1000) is dated and scoped the same way.
- **Capability table:** "WO-111 dated reassessment (2026-09-24)" reassesses
  `runtime.resident` and `worker.source-change` at level 1 within stated
  bounds.
- **Ledger:** the decisions-file substitution still applies. The dated
  clarification now sits in newest-first position, and `lineage` passes.
- **Planning map:** `work-order-map.md` now carries a dated "WO-111 R2
  checkpoint disposition" that links the combined answer in
  `critical-path-2026-09-08.md` §Stop and replan points. That answer compares
  the audit's four-to-twelve-hour spans (critical-path.md:385) with the
  windows' measures, finds no comparable token or dollar baseline, and
  reassesses the integration-package growth trigger (n1).

**6 — `npm test` green; `git diff --check` clean; no runtime source, generated
configuration or dependency change. Met.**

- **`npm test`:** 27 passed, 0 failed, 71 fresh tasks, 305.50 s, exit 0, run
  outside the sandbox. The gate row was recorded at 2026-09-24T23:49:46Z.
- **`git diff --check`:** exits 0.
- **Changed paths:** the only change outside `docs/` and `README.md` is two
  added entries in `packages/kernel/test/fixtures/jsonl-protocols.json`. That
  is the test fixture registry VER-001 B3 offered as a route, not runtime
  source, generated configuration or a dependency.

## Findings

### Blocking

**B1 (carried from VER-001) — criterion 2: the live runs changed a path
outside the portfolio.**

- **Expected:** every path outside the portfolio unchanged.
- **Observed:** the subject's own v2 receipts record the user Codex
  configuration as changed and criterion 2 as unmet. The repair disclosed the
  change, as VER-001 routed. It did not and could not remove it: the runtime
  fix is fenced out of WO-111 and is owned by
  [D011](../../evidence/WO-111/decisions.md#wo-111-d011) and
  `FUP-3c34a8ffbf61376f`, which D014 keeps independent of this order's outcome.
- **Reproduction:** `receipt-v2.json` and `return-receipt-v2.json`,
  `outsidePortfolioChange`. The operator-run check printed
  `"criterion2":"unmet"` for both editions.
- **Severity:** blocking. Every criterion is required.

**Route: WO-111 repair** (corrected after filing, 2026-09-24, at the operator's
direction; [D016](../../evidence/WO-111/decisions.md#wo-111-d016)). The filed
text routed criterion 2 to three operator decisions outside the lifecycle. That
was wrong: a repair can close it within WO-111's authority.
`worker-transport.ts:92-103` launches each Codex child with an environment built
from `process.env`, so the WO-111 caller (`run.mjs`, evidence tooling, not
runtime source) can supply an isolated Codex home. The repair:

- reruns the window with an isolated Codex home for every worker and verifier
  launch;
- adds a before/after digest of the user Codex configuration (hashes only) to
  the receipt's protected surfaces;
- supplies Codex credentials to the isolated home without copying them into
  evidence;
- fixes D015's two documentary defects.

The operator runs the window outside the sandbox (assumption 1). The isolation
is untested; the before/after digest decides it. `FUP-3c34a8ffbf61376f` remains
the independent runtime fix for every other launch path (D014).

### Minor (boarded as [D015](../../evidence/WO-111/decisions.md#wo-111-d015), follow-up `FUP-7dbf4e832a8fb124`)

- **n1 — the R2 growth count mixes two enumerations.**
  - Claimed: `critical-path-2026-09-08.md:324` and its risk-register row
    (:454) call the audit's 46 "top-level" files and say "the same
    enumeration" now finds 93.
  - Audit enumeration: `source-verification-2026-09-08.md:36-37` counts
    `packages/skeleton/src` recursively, as 39 `.ts` (4 under `loadouts/`)
    plus 7 `.mjs`.
  - Recount: `git ls-tree -r` at `33e2c25` and at `HEAD` gives 46 → 102
    recursively (89 `.ts`, 9 of them under `loadouts/`, plus 13 `.mjs`) and
    42 → 93 at top level.
  - Effect: the disposition (no split from count alone) does not depend on the
    difference.
- **n2 — the second window's receipt names the first window's mission store.**
  `return-receipt-v2.json` `snapshotCoverage.excludedObservedWrites` includes
  "mission store in the first window's ignored DotLn control lane". The cause
  is that `repair-receipts.mjs:182-192` writes one list for both windows. The
  second window ran no mission resident: its stream has 435 portfolio rows and
  no mission rows.

## VER-001 findings, re-checked

| Finding | Status at this subject | How I checked |
| --- | --- | --- |
| B1 | Disclosed, still blocking. | v2 receipts, write-backs, D011–D014 |
| B2 | Resolved. | `work-order-map.md` R2 line; critical-path R2 answer |
| B3 | Resolved. | `npm run test:docs` 21/0 (includes `lineage` and `registrations`) |
| M1 | Resolved as disclosure; live in-flight return still unexercised. | `inFlightReturn` in both v2 receipts |
| M2 | Resolved. | v2 `mission` block; README, 06 and capability-table text |
| M3 | Resolved. | budget method, computed pre-back fields, 6+6 launch claims, authorship label |
| m1, m2 | Resolved. | `runnerReturn` block; README and planning candidate link `return-receipt-v2.json` |
| m3, m4 | Resolved as disclosure. | `snapshotCoverage`, `generatorProvenance` (n2 aside) |
| m5 | Resolved. | critical-path cost/session and growth paragraphs (n1 aside) |
| m6 | Resolved in the v2 arrays. | refusal keeps its reason; `ResidentConfigured` offsets are null; the offset bases are documented |
| m7, m8 | Resolved. | `implementation.md:50-52, 99-104` |

## New repair claims, checked

- **Codex documentation.** Both cited pages exist and say what the diagnosis
  quotes. `--ignore-user-config`: "Do not load `$CODEX_HOME/config.toml`.
  Authentication still uses `CODEX_HOME`." `--ephemeral`: "Run without
  persisting session rollout files to disk." Project trust is recorded under
  `projects.<path>.trust_level`, and untrusted projects skip project-scoped
  `.codex/` layers.
- **Launch arguments.** `worker-transport.ts:516-550` launches
  `codex -a never exec --ephemeral --ignore-user-config --sandbox
  workspace-write …` and passes no isolated Codex home, consistent with the
  diagnosis.
- **Trust census (43 trusted, 36 DotLn scratch, 34 absent, 2 present).**
  `unknown` here: host-denied and not reproduced. The verdict does not rest on
  it.
- **Stream integrity.** `events-v2.json` hashes to `30c90380…6f65` with 1,731
  rows, and `return-events-v2.json` to `eed8729e…6b80` with 435 rows. Both
  match their receipts. The only refusal row keeps its reason ("budget
  exhausted: 3 of 3 episodes").
- **Privacy.** The committed WO-111 evidence, verification and final-review
  files, the planning candidate and all added diff lines contain no
  `/Users/`, `/private/`, `/var/folders`, email, key or token pattern. The
  only matches are the guard regex in `repair-receipts.mjs` and VER-001's
  description of its own scan.

## Checks I ran

- **`npm test`:** 27/0, 71 fresh tasks, 305.50 s, exit 0.
- **`npm run test:docs`:** 21/0, 21 fresh tasks, 24.89 s, before D015 and this
  report; 21/0, 23.84 s, after both.
- **Other checks:** `npm run plan -- check`, `npm run publication:check` and
  `git diff --check` each exit 0.
- **`npm run meta`:** exit 0.
- **`node docs/evidence/WO-111/repair-receipts.mjs check`:** run by the
  operator in this session. It printed `check: true` for both editions with
  empty stderr; the exit code was not displayed to me.

## Limits of this verification

- I judged retained evidence and did not relaunch live episodes, which this
  dispatch does not authorize.
- The trust census and B1's historical timestamp are not re-observed here.
  B1's standing does not depend on either, because the subject records the
  change itself.
- Other host paths and network effects remain `unknown`, as both receipts say.
- The human authorship of the presence edges cannot be observed from this
  session.
- Goal alignment: the order's outcome is dependable bounded unattended work
  that leaves the operator's own settings alone. A pass over a receipt that
  says `criterion2: unmet` would be rule beating. The fail keeps that
  observation load-bearing without adding a gate or an agent. Routing it to a
  repair with a concrete isolated rerun gives the lifecycle a path to an
  honest pass (corrected route, D016).

**Process cost:** entry 80,966 tokens; handoff 7,629,143 tokens; source claude-transcript-message-usage

Dispatch scope. The entry read was observed at 2026-09-24T23:43:00.120Z, and the
handoff read at 2026-09-24T23:54:04.950Z. At handoff the counters were
inputTokens 118, cachedInputTokens 7,430,179, cacheWriteInputTokens 163,341 and
outputTokens 35,505. `reasoningOutputTokens` and `costUsd` are unavailable
counters, so they are `unknown`. No subagents ran. The session's one
product-gate row is `npm test` at 305,495 ms.
