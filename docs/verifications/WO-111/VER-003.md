# WO-111 VER-003 — re-verification after the operator's D020 disposition

**Verdict: pass.** Criteria 1, 3, 4, 5 and 6 are met on the evidence below.
The live runs met criterion 2's measured parts: the main checkout, the
sentinel tree and scratch Git remotes. They did not meet its outside-portfolio
clause, because the runs added scratch trust entries to the user Codex
configuration. The operator accepted that specific, disclosed deviation
([D020](../../evidence/WO-111/decisions.md#wo-111-d020)) and confirmed the pass
in this session on 2026-09-25. Under product 07, "only an explicit operator
decision lowers a standard". This pass rests on that decision. It does not
claim the clause was observed. Prevention stays with the independent runtime
follow-up `FUP-3c34a8ffbf61376f` (D011, D014).

Subject: branch `wo-111` at `4b6a19cc`, all work uncommitted. The worktree
snapshot (tree `06894c39`) matches the RepairCompleted tree `1677a4b5`
(checkpoint `refs/dotln/checkpoint/WO-111/10`) apart from the verify dispatch's
lifecycle projections (`docs/control/current.md`, `docs/work-orders/README.md`,
one control row). Since VER-002, the repair added D016–D020, `repair-002.md`,
the operator-acceptance sentences in the README and product 06, and the
`v0.47.1` retiming. It changed no receipt, event stream, capability-table row
or planning-map line.

My writes are this report and the role's index refresh. I spawned no
subagents (0 of 20, exact-observed). No provider dollar cap was enforced,
because this transport exposes no hard dollar control.

Actor: this Claude Code session, harness `2.1.282` (`claude --version`), model
`claude-opus-5-5[1m]` as the session exposes it, effort `xhigh` from
`CLAUDE_EFFORT` (selected, not effective). Under verification is the repair
attestation: codex-cli `0.156.1`, `gpt-6-sol`, `xhigh`,
`codex-session-readback`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-opus-5-5[1m]","effort":"xhigh","source":"claude-session-readback"}

## Criteria

1. **Met.** VER-001 established from the raw stores that the producer
   discovered all six seeded candidates. It also established three derived
   orders per window in probe → widen → peak, each independently verified,
   with the first change touching one file (1/1/2). The v2 streams still hash
   `30c90380…` and `eed8729e…`, as VER-002 recorded. No verification failed,
   so no reset was needed.
2. **Measured parts met; the outside-portfolio clause was not met and is
   waived by the operator.** The checkout, sentinel and zero-remote results
   hold under the receipts' stated tracked/non-ignored coverage. Both v2
   receipts record the Codex configuration change (`changed: true`), and D020
   accepts it for this synthetic proof. Other host paths and network effects
   remain `unknown`.
3. **Met.** One mission `drift` verdict is recorded. The second window's
   return cancelled an eligible fourth dispatch in retained-log replay against
   a no-return control. No episode was in flight at either return, and both
   receipts say so.
4. **Met.** Every receipt field is labeled. The budget states its replay
   method, and per-episode launch claims carry `unknown` effective identity.
5. **Met.** README "What runs today", product 06's dated rung, the capability
   table's WO-111 reassessment and the planning map's R2 disposition are
   present. The ledger duty is discharged by the decisions file and index
   (the pre-2026-09-09 substitution).
6. **Met.** `npm test` passed: 27 passed, 0 failed, 71 fresh tasks,
   291.34 s, exit 0. `git diff --check` exits 0. The only change outside
   `docs/` and `README.md` is the two evidence entries in the kernel test
   fixture `jsonl-protocols.json`.

## Checks I ran

- `npm test`: 27/0, 291.34 s, exit 0.
- `npm run test:docs`: 21/0, 22.82 s, exit 0.
- `npm run plan -- check`: exit 0. The reverted D017 amendment row stays in
  the append-only log as history, and the check accepts the restored order.
- `git diff --check`: exit 0.
- The `v0.47.1` retiming matches the local tags, whose newest is `v0.47.0`.

## Limits

I judged the retained evidence and did not relaunch live episodes. VER-002's
minor n2 (the second window's snapshot list names the first window's mission
store) stays open under `FUP-7dbf4e832a8fb124`. D018 records the corrected
46 → 102 count without rewriting the historical planning pass. Goal alignment:
the order's outcome, bounded unattended scratch work that is independently
verified and stops on return, was observed. The one side effect is disclosed,
waived by the operator and owned by a separate follow-up, so no evidence
claims containment that did not happen.

**Process cost:** entry 37,763 tokens; handoff 1,658,439 tokens; source claude-transcript-message-usage

Dispatch scope; entry cutoff 2026-09-25T00:22:08.584Z, handoff read
2026-09-25T00:30:34.726Z. `reasoningOutputTokens` and `costUsd` are unavailable
counters, so they are `unknown`.
