# WO-111 VER-001 — independent verification of the live scratch portfolio windows, their receipts and write-backs

**Verdict: fail.** The live proof itself is largely real. From the retained raw
stores, WO-119's producer discovered all six seeded candidates. In each of two
windows three derived orders completed in probe → widen → peak, each with its
own independent live verification pass. The first change touched one file. A
counterfactual replay of the retained log confirms that the human `back`
cancelled the pending fourth dispatch. The protected checkouts, sentinel and
remote counts are unchanged, and `npm test` is green. Three defects still block
a pass:

- **B1:** the run changed a path outside the portfolio, the operator's
  user-level Codex configuration, which the receipts label `unknown`.
  Criterion 2 is therefore not met.
- **B2:** criterion 5's replan checkpoint is not answered in the planning map.
- **B3:** the subject turns the repository's document gate red.

Criteria 3 and 4 are partially met (M1, M3), and several write-backs overstate
what the windows show (M2 and the minor findings).

Subject: [WO-111](../../work-orders/WO-111-unattended-live-proof.md), branch
`wo-111` at `4b6a19cc50e98a437d35b2adf2d392fbf5b12139`, which is also the merge
base with `main`. `git rev-list --count main..HEAD` is `0`, so all work is
uncommitted, as required before final review. I compared every tracked and
untracked file with checkpoint `refs/dotln/checkpoint/WO-111/3`
(`2c111b6c90b64051aa4fd81f45206ab0117946cc`). The subject bytes are identical.
The only differences are the verify dispatch's own lifecycle projections
(`docs/control/current.md`, `docs/work-orders/README.md` and the appended
`VerificationRequested` row). I judged the order as filed; its text differs from
`main` only in the title's `v0.46.3`.

I edited no implementation, receipt, script or write-back. My writes are:

- this report;
- decision [WO-111-D011](../../evidence/WO-111/decisions.md#wo-111-d011), which
  boards up B1's runtime defect as the role requires;
- `npm run meta`'s regenerated `docs/lineage/decisions-index.md` row for D011,
  and its sync of `docs/planning/followups.json`, which minted open follow-up
  `FUP-3c34a8ffbf61376f` for D011;
- the role-mandated `npm run work-orders -- index` refresh, which regenerated
  identical bytes;
- gate receipts in the ignored local lane.

I did not relaunch any live episode, and I did not touch the operator's Codex
configuration.

Actor: this Claude Code session. Harness `2.1.282` is read back from
`claude --version`. The model is `claude-opus-5-5[1m]` as the session exposes
it. Effort `xhigh` is read back from the host's `CLAUDE_EFFORT`; that is the
selected effort, not a measured effective effort. The operator selected ultracode
for the session, so I orchestrated four subagents in one workflow: three
independent checkers split by criterion and one adversarial judge over all of
their verdicts. The harness observed 4 of the 20-agent cap, exact-observed, with
16 remaining and no descendants. The workflow reported 1,150,647 subagent tokens,
486 tool calls and 2,451 s. No provider dollar cap was enforced, because this
transport exposes no hard dollar control. The judge withdrew nothing and
re-graded several findings. I reproduced every blocking finding and each
load-bearing major claim myself before recording it.

Under verification is the executor attestation: codex-cli `0.156.1`,
`gpt-6-sol`, `xhigh`, source `codex-session-readback`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.282","model":"claude-opus-5-5[1m]","effort":"xhigh","source":"claude-session-readback"}

## Instrument disclosure

The return-cancellation judgment replays the retained log through the runtime
under examination: `replayResident` and `residentMachine` from the built
`packages/skeleton`. That is the right instrument for the claim, which concerns
what this runtime's fold does. It is not an independent model of the policy. The
DotLn harness hooks, which are not part of this subject, refused several of my
Bash calls while my `npm test` gate was live. They named the run id and pid each
time. I waited for the gate to finish rather than stop it.

## Criteria

**1 — producer discovery, at least three derived orders in ascending phase,
verified, first touching one file, reset on a failed verification. Met.**
Observed in the first window's raw portfolio store:

- **Discovery:** a `discovery` script episode ran the WO-119 program
  (`discovery-cli.js`) over the target and was observed with exit 0. It
  returned six candidates with per-check exit codes, digests, a corpus hash and
  seven scanned files, so this is a discovery run, not a copied fixture list.
- **Derivation:** each `PortfolioOrderActivated` carries that discovery
  episode's id and a `portfolio:gardener-5s@1:<base>:<candidate>` source. The
  launchpad holds the materialized derived orders WO-900 to WO-902.
- **Workers and verifiers:** separate `source-WO-90x` and `verification-WO-90x`
  stores record live `codex-cli-exec` workers and fresh-transport verifiers. The
  verifiers' authority denies `repo.write`, their effective model is `unknown`,
  and each verdict is `pass` with no findings.
- **Changed paths:** read-only `git diff base..commit` gives `src/main.js`,
  `src/main.js`, and `docs/guide.md` plus `loose/guide.md` (a 100% rename). Each
  set is inside its order's surfaces.
- **Commits:** the three target commits remove the lint marker, fix the test,
  and move the guide.
- **Second window:** repeats the pattern with counts 1/1/2. No verification
  failed, so no reset was required.

`run.mjs` binds `CodexCliExecWorkOrderTransport` for both worker and verifier,
and no test double is on the path. Limits are in N2.

**2 — main checkout hash, sentinel tree and every path outside the portfolio
unchanged; no remote effect. Not met (B1).** The measured parts hold:

- The main worktree's HEAD is still `4b6a19cc…`, and its recomputed snapshot is
  byte-identical to both baselines.
- The sentinel digests match.
- Each target's `main` is at its seed base, and every episode branch changes
  only its declared surfaces.
- Both scratch repositories and both launchpads have zero remotes.

But "every path outside the portfolio" is contradicted: the operator's
user-level Codex configuration gained trust entries for both scratch targets
(B1). The receipts never measured such paths and label them `unknown`. Their
checkout snapshots also exclude ignored paths (m3).

**3 — the mission check fired with its verdict recorded; return stopped dispatch
within one tick, with the in-flight episode handled per the phase. Partially
met.**

The mission half is met. In the separate mission store, one `mission-check`
episode was dispatched on the 900 s cadence after human away. It returned
`drift` over `contract:surfaces` (the DotLn worktree's
`docs/lineage/decisions-index.md`), and `MissionDriftObserved` was appended. M2
covers what that check judged.

The return half is met in the second window, reproduced here:

- The human `back` is row 432 of 435. At that edge the replayed machine moves
  from `probe` to `present`.
- Extending the retained log with clock samples to and past the pending
  `dueAt` 1790286813701 leaves `due()` null.
- A probe dispatch folded there is rejected: "invalid or repeated resident
  dispatch".
- The control is the same log without `back`. It is due at exactly
  1790286813701, no refusal applies, and the same dispatch folds `ACCEPTED`.

So the resident's own state, not only the caller's abort (m1), cancelled the
eligible fourth order at the edge itself. In the first window, return causality
is correctly `unknown`: the portfolio had already stopped on its exhausted
three-episode budget.

The in-flight clause was not exercised (M1).

**4 — every claim labeled `observed`, `launch-claim` or `unknown`; the budget
consumed reported with its method. Partially met (M3).** Every top-level claim in
both receipts carries a label. `receipt.json`'s budget carries an accurate
method: the resident folds wall time as the sum of `observation.at −
activation.at`, and the three windows sum to 182,200 ms. Several `observed`
labels sit on reducer literals or on uncommitted inputs, the return receipt has
no method, and per-episode launch claims are absent (M3).

**5 — write-backs. Not met (B2).**

- **README "What runs today"** (README.md:112-118), **06's dated rung**
  (the "WO-111 live rung observation (2026-09-24)" paragraph) and **the
  capability table** (§WO-111 dated reassessment, which reassesses both
  `runtime.resident` and `worker.source-change`) all land. Their wording
  problems are M2, m1 and m2.
- **Ledger:** WO-111 was filed before 2026-09-09, so the duty is discharged by
  the decisions file and its decisions-index rows, as
  `docs/work-orders/README.md:96` marks. The operator-authorized ledger
  follow-up added under D004 breaks the ledger's ordering rule (B3).
- **Replan checkpoint:** not answered in the planning map (B2).

**6 — `npm test` green; `git diff --check` clean; no runtime source, generated
configuration or dependency change. Met.** I ran the gate myself, outside the
sandbox: `npm test` passed 27 suites with 0 failed and 71 fresh tasks in
300.50 s, exit 0, recorded 2026-09-24T22:25:47Z. This matches the executor's
27/0/71.

`git diff --check` exits 0. Every changed or new path is under `docs/` or is
`README.md`:

- no `packages/*/src` or runtime `scripts/` file changed;
- no `.claude/`, `.agents/` or harness manifest changed;
- no `package.json` or lockfile changed.

The new `docs/evidence/WO-111/*.mjs` callers import the built runtime and are
evidence tooling, not runtime source. The document gate is outside this
criterion's text and is B3.

## Findings

### Blocking

**B1 — criterion 2: the live run changed the operator's user-level Codex
configuration, outside the portfolio, and the receipts do not report it.**

Expected: every path outside the portfolio unchanged, or any observed change
recorded.

Observed:

- The operator's user-level Codex configuration holds two
  `trust_level = "trusted"` project entries. They are keyed on
  `<system-temp>/dotln-wo111-JslDMR/target` and `…-QRM9qa/target`, random
  roots only WO-111's seed created. One earlier WO-053 and one WO-056 scratch
  target carry the same kind of entry.
- An independent checker read the file's modification time at 22:40Z as
  1790286441.611, before an unrelated later rewrite at 1790289893. That is 40 ms
  after the second-window peak worker's `CommandReceipt` at 1790286441.571. I
  reproduced that timestamp from its `source-WO-902` store. It was the only
  file under the Codex home modified in that window. I read the checker's
  recorded tool output; I cannot re-observe the mtime now.
- `worker-transport.ts:517-527` launches `codex -a never exec --ephemeral
  --ignore-user-config --sandbox workspace-write --cd <worktree>` with no
  isolated `CODEX_HOME`, and no DotLn source writes the Codex home.

Attribution to the Codex CLI's own trust persistence at worker launch is an
**inference** from the keys, that timing and those arguments. No log line naming
the writer was found. `receipt.json` and `return-receipt.json` label
`outsideHostFilesystem` `unknown`, and `docs/evidence/WO-054/codex-continuation.md:116-117`
says no user configuration or trust setting is changed.

Reproduction: `grep -n -F dotln-wo111- <user Codex config>` gives lines 209 and
212, each followed by `trust_level = "trusted"`. The per-scratch-target entry
count shows wo111 ×2, wo053 ×1 and wo056 ×1. The raw `source-WO-902` rows give
`WorkerAttemptStarted` 1790286441556 and `CommandReceipt` 1790286441571.

Severity: blocking. The criterion's literal claim is contradicted, and the
effect touches operator tool settings.

Route:

- **Repair within WO-111** can only make the receipts, `implementation.md`
  and the write-backs record this as an observed outside-portfolio change.
  Name the Codex home as a checked location.
- **The runtime fix** is outside WO-111's authority and is boarded up as
  [D011](../../evidence/WO-111/decisions.md#wo-111-d011) with a named planner
  follow-up, registered as `FUP-3c34a8ffbf61376f`.
- **Closing criterion 2** then needs that fix and a rerun (operator-review
  assumption 2 keeps a failed run's receipt and the order open), or a dated
  operator judgment.
- **The operator decides** whether to remove the four existing entries. I did
  not.

**B2 — criterion 5: the replan checkpoint is not answered in the planning map.**

Expected: "the replan checkpoint is answered in the planning map". Product 07
names the human planning map as the source of the work-order index's proposed
sequence, which is `docs/planning/work-order-map.md`. WO-044 answered its
checkpoint there (`work-order-map.md:33`), and WO-053 answered there too
(`work-order-map.md:35`).

Observed: `work-order-map.md` is unmodified. Its line 35 still reads "R2's
combined prioritization decision awaits WO-111's receipt". The R2 disposition
exists only in `critical-path-2026-09-08.md:320` and D005.

Also incomplete (m5): R2 question 1 asks for a comparison with the audit's
measures, and the risk register's "Integration-package growth" row names R2 as
its trigger. The disposition answers neither.

Reproduction: `git status` and `git diff` list no change to
`work-order-map.md`; `grep -n WO-111 docs/planning/work-order-map.md` finds
line 35's pending text.

**B3 — repository document gate: `npm run test:docs` is red because of two
subject defects.**

Observed in my run: 13 passed, 8 failed, exit 1.

- `lineage` fails: "Line 8205: section below the founding corpus boundary".
  The new "Presence-curve clarification — 2026-09-24" section was appended after
  Chat 001. `idea-ledger.md:23-32` requires new sections directly below the
  header and never below the founding corpus. The generated
  `docs/lineage/README.md` was not regenerated.
- Seven dependent suites refused on that preflight, among them `registrations`.
  Run directly, `node scripts/check-registrations.mjs` exits 1. It reports
  `docs/evidence/WO-111/events.jsonl` and `return-events.jsonl` as unregistered
  JSONL: they are not `EventEnvelope` streams (`UNKNOWN_FIELD at $.stream` /
  `$.index`), and `jsonl-protocols.json` does not classify them.

`npm test` does not run document suites, so the executor's green gate did not
cover them. Criterion 6 names only `npm test`, but final review cannot publish
over a red document gate.

Route: move the ledger section into newest-first position and regenerate the
lineage index. Either register the two sanitized streams in the kernel's
protocol registry (a test fixture; record the decision) or store them in a
non-JSONL form. Then rerun `npm run test:docs`.

### Major

**M1 — criterion 3: "the in-flight episode handled per the phase" was not
exercised, and neither receipt says so.** The replayed state immediately before
`back` has `machine.current` null and no pending episodes in both windows
(rows 829 and 431). `return-receipt.json` records `preBackInFlight: false`, and
D003 deliberately returned while nothing ran. The compiled phases declare
`discovery` kill and `probe`/`widen`/`peak` finish. The only evidence for that
rule is `resident.test.ts:163-224`: fake script actors, no portfolio and no
Codex worker. The `skeleton` suite that runs it passed in my `npm test` run.
With nothing in flight the
clause is vacuous rather than violated, so I do not fail criterion 3 on it
alone. Route: state the non-exercise and cite the phase rule and test. Whether
a live in-flight return is still required is the operator's or reviewer's call.

**M2 — the mission check's subject and hold are misdescribed, and the objective's
composition is overstated.** The mission check ran as a separate resident
(`WO-111-1`, bound by `resident-bind` to the WO-111 DotLn worktree). It judged
that worktree's diff against surfaces the executor typed, not the scratch
portfolio's derived orders, which it never saw.

Its hold lives only in that store. `resident-state.ts:393` exempts the mission
actor itself, and that store has no other actor, so it refused nothing. The
portfolio store's `dispatchHeld` is null throughout; its only refusal is
"budget exhausted: 3 of 3 episodes" at +189,211 ms, about 355 s before the
drift.

Several texts misstate this:

- `implementation.md:21-23`, `receipt.json`'s `returnCausality` reason and
  D003's rejected-option reason call the hold a competing cause of the stop.
- README.md:112-115 folds "recorded a mission verdict" into the portfolio
  resident's sentence.
- The capability table scopes `runtime.resident` as including "mission cadence
  … over a scratch target".
- Product 06 calls the verdict a "fixture-surface `drift`", and
  `implementation.md:18-21` and D003 call it a "fixture declaration error".
  The binding was the real WO-111 branch, and the drift is an accurate finding
  about the declared contract.

Criterion 3's literal clause still holds. But no window shows a mission check
supervising unattended portfolio work, which is the objective's "the resident …
fires the mission check on its cadence". Route: name the judged subject in the
receipt, drop the hold as a competing cause, and scope the write-backs to what
ran.

**M3 — criterion 4: `observed` labels on literals, uncommitted inputs, a missing
method and missing per-episode launch claims.**

- **Literals labeled `observed`:** `return-receipt.mjs:193` writes
  `preBackCandidateEligible: true` as a literal, and `receipt.mjs:232` writes
  the budget ceiling as a literal.
- **Uncommitted input:** the other pre-back fields (`phase`, `armedAt`/`dueAt`,
  episodes 3/4, `nextCandidate`, `inFlight`) are copied from
  `return-preback.json`, whose generator is not committed. My checker
  independently recomputed every value from the log prefix, including
  `portfolioSelection` returning the repeated-repair order and `armedAt +
  300000 = dueAt`, so no value is false.
- **Missing method:** `return-receipt.json` reports `reportedWallMs`,
  `reportedTokens` and episodes with no method.
- **Missing launch claims:** the order's **Model** line asks for "launch
  claims recorded per episode". `receipt.json` has one aggregate worker
  `launch-claim` and no verifier launch claim, and the return receipt has no
  identity fields. The per-episode records exist only in the ignored raw stores.
- **Presence edges:** the human-origin away/back are labeled as observed human
  edges. The classifier assigns `human` by signal kind alone
  (`presence-signals.ts:33-35`), and the edges were entered through the
  executor session at the operator's messages (D003). Their human authorship
  is a launch claim.

Route: compute every `observed` value from the log (or commit the pre-back
generator), add the method, project per-episode worker and verifier launch
claims with `unknown` effective identity, and label edge authorship.

### Minor

- **m1 — return cancellation wording and the caller's abort.** `run.mjs:157`
  aborts the host within one pulse of seeing `present`; the runners stopped
  1,313 ms and 348 ms after `back`. So "0 post-back dispatches/activations" is
  not probative for a dispatch due 252 s later. The cancellation is true by the
  replay under criterion 3, but no receipt discloses the abort or names that
  evidence. Roadmap "causally observable" and product 05 "proves" should say
  what was observed and replayed.
- **m2 — wrong receipt cited.** README.md:432-434 cites `receipt.json` for
  "return cancellation" and the "immediate peak reset". That receipt labels
  return causality `unknown` and records no reset; both come from
  `return-receipt.json`. The planning candidate's lines 8-10 do the same.
- **m3 — the containment snapshots' coverage is unstated.** `checkoutSnapshot`
  (`WO-053/fixture.mjs:175-208`) excludes ignored paths. During the windows the
  host wrote six harness target installation records, two exclude records and
  the mission store into the DotLn worktree's ignored control lane, and appended
  a target-worker block to each target's `.git/info/exclude`. `git status
  --ignored` on both target mains is clean. The objective's "or worktree"
  wording admits worktree writes, but "selected checkout unchanged" needs that
  qualification.
- **m4 — generator provenance.** Window 1 was produced by earlier revisions of
  `seed.mjs`, `run.mjs`, `baseline.mjs` and `mission.mjs`: their mtimes follow
  the window or its runner start. It also used a manual "Add synthetic grant
  registry" launchpad commit that the committed seed does not produce. The
  `location` files' store fields, the bind commands and the presence commands
  are unscripted and unrecorded. Window 2 ran the committed seed, run and
  baseline.
- **m5 — incomplete R2 disposition.** R2 question 1's comparison with the
  audit's measures and the risk-register R2 trigger (B2) are both missing.
- **m6 — sanitized-stream gaps.** `ResidentConfigured` rows carry offset
  −`awayAt`. The mission stream's offset base (`missionAwayAt`) is undocumented.
  The budget refusal keeps only `{refused:true}`, without its reason.
  `return-events.jsonl` projects fewer fields than `events.jsonl`, and its
  receipt has no field description.
- **m7 — two inaccurate statements.** `implementation.md:41` calls the folded
  wall time "actor-reported", but the resident computes it from its own
  timestamps; only tokens are actor-reported, and none were. D010 and
  `implementation.md:77-78` say release prepare changed no files, but it
  rewrote `docs/final-reviews/WO-111/PR.md`.
- **m8 — follow-ups reopened without mention.** The scope-expansion edits moved
  FUP-0083, FUP-0108 and FUP-0110 back to needs-review, and
  `implementation.md` does not mention it. `followups.json` is otherwise
  consistent, and FUP-441469088613e52a is well formed.

### Notes, not findings

- **N1 — duration.** No numbered criterion sets a duration, and the objective
  asks for "one bounded window" whose window and budget are the portfolio's.
  The windows lasted 648,313 ms and 536,565 ms, about 10.8 and 8.9 minutes. The
  title's "hour", the WO-099 dependency reason and the 3,600,000 ms ceiling
  name an hour. D003's reading is supported by the criteria text; whether the
  title binds is for the reviewer or operator.
- **N2 — what "ascending" shows.** Widen's ceiling equals probe's, one file, so
  scope widens only at peak. Nothing was merged, so the post-peak re-discovery
  reports all six candidates again. WO-902's 2/2 criteria are one host
  relocation check plus one model judgment. Worker and verifier share the
  launch model, so independence is per episode, not per model. The
  repeated-repair candidate never ran.
- **N3 — split windows.** Criterion 3's halves come from different windows, and
  the operator supplied both.
- **N4 — Blackjack figures.** The illustrative $5/+$5/$45 figures peak near 8.5
  losses, not 12. The ADR amendment and the ledger map the minute examples
  differently. All are disclaimed as illustrative. The time-away-only mapping,
  the future automatic-selection placement and the `v1` naming are consistent
  across every changed file. The clean-room screen found no employer, internal
  or private identifier.
- **N5 — correction shape.** D003 and D006-D009 nest
  `correction: {misread, meant, changed}` rather than the established
  `kind: "correction"` shape. `meta --check` passes.
- **N6 — staged PR body.** `docs/final-reviews/WO-111/PR.md` is
  executor-staged; the reviewer owns and re-derives it.

## Checks I ran

- **`npm test`:** 27/0, 71 fresh tasks, 300.50 s, exit 0.
- **`npm run test:docs`:** 13 passed, 8 failed, exit 1 (B3).
- **`node scripts/check-registrations.mjs`:** exit 1 (B3).
- **Other checks:** `git diff --check`, `npm run plan -- check`,
  `npm run publication:check` and `npm run work-orders -- index` (identical
  bytes) each exit 0.
- **Event streams:** SHA-256 of `events.jsonl` `b354e3e4…3660` (1,731 rows) and
  `return-events.jsonl` `367c4237…a924` (435 rows) match both receipts. My
  checker also rebuilt both receipts field by field from the raw stores
  without running the committed reducers, which write with `wx`.
- **Privacy scan:** no `/Users/`, `/private/`, `/var/folders`, email, key or
  token pattern in the committed WO-111 evidence or in the added diff lines.

## Limits of this verification

- I judged the retained raw stores and receipts. I did not relaunch live
  episodes, which is outside this dispatch's authority.
- B1's attribution is an inference, labeled as such. Its in-window timestamp
  was read by a subagent before the file was rewritten again at 1790289893, and
  I read that recorded output rather than re-observing it.
- Whether other host paths or network endpoints changed remains `unknown`. B1
  shows the receipts' `unknown` hid at least one real change; it does not
  establish that there are no others.
- The presence edges' human authorship cannot be observed from this session.
- Goal alignment: the order's outcome is an honest proof that the resident can
  do bounded unattended work without operator rescue. Passing it over B1 would
  normalize a hidden side effect on operator tool settings (rule beating). The
  fail routes three concrete repairs and one boarded runtime order. NoOp, a pass
  with notes, would let WO-112 and WO-118 inherit the same side effect
  unrecorded.

**Process cost:** entry 44,326 tokens; handoff 16,698,782 tokens; source claude-transcript-message-usage

Dispatch scope. The entry cutoff is 2026-09-24T22:18:57.962Z and the handoff
read was observed at 2026-09-24T23:13:28.212Z. At handoff the counters were
inputTokens 184, cachedInputTokens 16,359,153, cacheWriteInputTokens 256,546 and
outputTokens 82,899. The root counters exclude the four
subagents' 1,150,647 workflow-reported tokens. `reasoningOutputTokens` and
`costUsd` are unavailable counters, so they are `unknown`. The session's one
product-gate row is `npm test` at 300,500 ms.
