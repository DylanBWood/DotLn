# WO-139 VER-002 — Independent verification after the FINAL-001 repair

**Verdict: pass.** Both FINAL-001 findings are repaired at the current subject,
reproduced independently below. All seven acceptance criteria still hold. The
required review gate, which FINAL-001 failed, now passes: 32 suites, zero
failed. One pre-existing gap of F1's class is recorded as a non-blocking
observation (N1). WO-139 introduced none of it, and every runtime module this
order changed or added is pinned.

Verified by a Claude Code session, dispatch `resume: verify` recorded by the
session hook at 2026-09-18T17:13:25Z. Harness version 2.1.276 is the harness
host's own `versionObservation` for this session (channel
`ancestor-executable-lsof`). Model `claude-opus-5[1m]` is the model identifier
the harness supplied in this session's system context. Effort `xhigh` is the
`effort` value carried in this session's hook input and recorded in its session
journal. These are the actor's readback of harness-supplied values, not an
independent effective-session attestation.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.276","model":"claude-opus-5[1m]","effort":"xhigh","source":"self-reported"}

## Subject

- Order: [WO-139](../../work-orders/WO-139-subagent-cap.md). The planning check
  reports its current source hash as `sha256:a9cd1921…74111`, the same bytes
  VER-001 judged.
- Subject: the `wo-139` worktree over `main` at `494e682`, uncommitted. All
  tracked, staged and untracked files equal checkpoint
  `refs/dotln/checkpoint/WO-139/9` (`509bbf0`), except:
  - the verify-transition projections: `docs/control/current.md` and the
    appended `VerificationRequested` row;
  - the post-dispatch index refresh of `docs/work-orders/README.md`, which
    changed only the phase lines.

  Checked by `git diff` against the checkpoint and per-file blob hashes of
  untracked and staged files, before and again after the gate.
- Change since VER-001's subject (checkpoint 3). From checkpoint 3 to 7 only
  review artifacts were added: VER-001, FINAL-001 and its evidence, and the
  PR/release drafts. The repair (checkpoints 7 → 8, `RepairCompleted` at
  17:08:03Z) changed four authored source/test files:
  - `packages/skeleton/src/harness-host.ts`
  - `scripts/lib/harness.mjs`
  - `scripts/test-harness.mjs`
  - `scripts/test-process-debt.mjs`

  It also regenerated the hooks and manifest, and added authority revision 003,
  D006, [repair-001](../../evidence/WO-139/repair-001.md) and the index rows.
  `docs/product/07-execution-guide.md`, `docs/AI-HARNESS-SECURITY.md`, the
  budgets file, the contributor loadout, `subagent-budget.ts` and the package
  metadata are byte-identical to VER-001's subject.

**Instrument disclosure.** The instruments under verification also observed
this verification:
- The generated hooks reserved this session's writer.
- The pre-existing gate refusal refused this session's shell calls during its
  own `npm test`.
- The hooks initialized this session's subagent counter.
- `harness usage`, which is under verification, supplied the counts quoted
  below.
- The gate-evidence machinery recorded this session's `npm test` row.

**Fan-out.** Plan stated before any work: no subagents, against the root budget
of 20 (observed 0 at entry). Actual: none. At 17:39:33Z the counter file holds
no direct admissions, children or workflows, and `harness usage` reports
`count 0, cap 20, remaining 20, exact-observed`, with the uncounted remainder
unknown.

## FINAL-001 findings

### F1 — cap module pinned in the installed runtime: repaired

Code: `scripts/lib/harness.mjs:95` now declares
`packages/skeleton/dist/src/subagent-budget.js` among `runtimeFiles`. The
branch's only change to that file is this line.

Reproduction: FINAL-001's own
[reproduction](../../evidence/WO-139/final-review-runtime-reproduction.md),
inverted to assert the repaired behavior. It ran in disposable copies of the
four packages' `dist` under this session's scratch directory, not the system
temp directory.

| Probe | FINAL-001 observed | VER-002 observed |
| --- | --- | --- |
| Cap module in `runtimeFiles` | absent | present |
| Snapshot after a module-only 20 → 21 change | unchanged | `.runtime/harness/bb9522b84d7a517d` → `.runtime/harness/a63f7a3663b7a914` |
| Built / installed default after re-emit | 21 / 20 | 21 / 21 |
| Prior snapshot's module bytes | — | unchanged (original) |
| `harness check`, installed module altered | passed (28) | throws `harness drift: pinned snapshot missing or changed (packages/skeleton/dist/src/subagent-budget.js)` |
| `harness check`, installed module deleted | passed (28) | same throw, module named |
| Generated permission hook, module deleted | `runtime-unavailable`, no deny | `snapshot-missing` advisory, no deny |

The initial snapshot id computed from the worktree's current `dist` equals the
installed `bb9522b84d7a517d` referenced by `.claude/hooks/permissions.mjs`. The
built and installed `subagent-budget.js` and `harness-host.js` have identical
SHA-256 prefixes (`15fff614…`, `378aa197…`), checked both before and after the
gate's build step.

- **Altered-bytes probe.** With the installed module altered, the permission
  hook in the non-Git scratch copy answered with the `runtime-unavailable`
  advisory ("Git host observation unavailable"), not `pins-differ`. That
  scratch copy is not a repository. The repair's fixture, which is a
  repository, asserts `pins-differ` for the same damage, and it passes. Both
  responses are advisories without a deny.
- **Regression.** The new fixture "WO-139 cap-module-only changes refresh the
  runtime and snapshot damage is detected" (`scripts/test-harness.mjs:529`)
  passes in a focused run (3/3 WO-139 cases, 13.9 s) and within the gate's
  harness-fixtures suite.

**Runtime import closure.** I computed the transitive relative-import closure
of the 38 pinned modules from the built `dist`. The only runtime import WO-139
adds is `harness-host.ts` → `./subagent-budget.js`, and that module imports
only `node:` built-ins. Of this order's changed runtime sources
(`harness-host`, `subagent-budget`, `version`, compiler `harness` and
`artifact-identity`), all are pinned. The contributor loadout is not reachable
from the runtime. See N1 for the pre-existing remainder of the closure.

### F2 — budget and classification advisories keep separate throttles: repaired

Code:
- `harness-host.ts:2365` adds a module-level `WeakSet`. The only two
  budget-advisory responses are tagged where they are created:
  - SessionStart counter initialization (`2587`);
  - permission admission (`2962`).
- The throttle bucket at `3186` now keys on set membership, not the substring
  "subagent".

`runHarnessHook` assigns the awaited `evaluateHarnessHook` result directly
(`3104`), so object identity survives to the throttle. The other paths that
touch the counter return no budget advisory:
- UserPromptSubmit initialization discards its warning (`2651`);
- the observer's `linkSubagentResult` (`2808`);
- Stop's summary, which does not start with `DotLn advisory:` and so is never
  throttled;
- the CLI `stderr` messages (`1486`, `1530`).

The `WeakSet` is never serialized, so host responses carry no new field.

Reproduction: FINAL-001's focused command,
`node --test --test-name-pattern='WO-132 spawn and classification advisories|WO-132 all four completions' scripts/test-process-debt.mjs`,
now passes 2/2 (FINAL-001: 0/2).

The repaired regression (`test-process-debt.mjs:470–499`) asserts four things:
- the missing-counter budget advisory is delivered once;
- its repetition is suppressed, with the journal row retained;
- the remote-agent classification advisory ("Remote subagents") is still
  delivered;
- it suppresses the subsequent unclassified-tool message.

This is the behavior FINAL-001 required. The completion fixture (`911–912`)
asserts `^Subagents: 0/20` and, multiline, `^Observed facts`. It keeps its
no-deny and writer-release assertions.

## Acceptance criteria at the current subject

| # | Expected | Observed | Result |
| --- | --- | --- | --- |
| 1 | Probe row with field shapes; design record names the counter key | [Probe 2](../../evidence/WO-139/subagent-probe-2.json) is byte-identical to VER-001's subject. The [evidence README](../../evidence/WO-139/README.md) repair gained only a leading repair pointer and an edition sentence; its identity paragraph still names root `session_id`, child `agent_id` and direct `tool_use_id`. | Pass |
| 2 | Cap 3 refusal wording, zero-budget Workflow refusal, `null` disables, and missing/unreadable counter advisories with fixtures | `subagent-budget.ts` and its unit test are byte-identical to VER-001's subject; the skeleton suite passes in the gate. The generated-hook case "WO-139 generated permission, observer, Stop and usage share the root subagent cap" passes (focused and in-gate). The repair adds only advisory provenance; the refusal path (`protocolRefusal` on `admission.refusal`) is unchanged. | Pass |
| 3 | Attributable children count at their first call and are refused beyond the cap; unattributable ones are reported uncounted | Same source and fixtures as VER-001. The Stop summary still reports the uncounted remainder as unknown. | Pass |
| 4 | Stop advisory and `harness usage` report count and cap | Live `node scripts/harness.mjs usage <session>` at 17:39:33Z: `{"count":0,"cap":20,"remaining":20,"countKind":"exact-observed",…,"uncountedRemainder":"unknown"}`. Generated Stop output is asserted as `^Subagents: 0/21` after the module-only change (F1 fixture) and `^Subagents: 0/20` in the completion fixture. | Pass |
| 5 | Batching rule; bundle/manifest regenerate; `harness check`; 07 §Discipline and runbook write-backs | `node scripts/harness.mjs check`: 28 generated surfaces, exit 0, before and after the gate. `harness-context --check`: exit 0, bytes unchanged from VER-001 in both roots: executor 20,461/20,480, verifier 17,503/20,480, reviewer 18,721/20,480, release-close 12,049/12,288, planner 13,121/24,576. Authority revision 003 lists 28 generated surfaces, and the gate's `authority-evidence` check passes. 07 and the runbook are byte-identical to VER-001's subject, which verified the four-refusal text. After the F1 repair, a change to the cap module now changes the installed runtime identity. | Pass |
| 6 | Per-harness, per-path admission table; 07 candidate | The D003 table and 07 §Candidate — total subagent cap are unchanged; `decisions.md` only appends D006. | Pass |
| 7 | `npm test` green; `git diff --check` clean; no new dependency | See the gate details below. | Pass |

**Criterion 7 details.**
- `npm test -- --review`: started 17:18:12Z. Result: 32 passed, 0 failed,
  1,177.68 s, 76 fresh suites, 0 reused. This includes harness-fixtures
  (408.75 s), process-debt (179.95 s), skeleton, harness-probe,
  plan-refutation and all evidence checks.
- Gate row recorded 17:37:52.807Z: exit 0, code identity `24742080…1140d`,
  tree `1d676fe`.
- `git diff --check main` and `git diff --cached --check`: exit 0.
- Package metadata and lockfile are byte-identical to VER-001's subject:
  compiler 0.13.2 and skeleton 0.25.4 only.

Other checks: `node scripts/test-plan-refutation.mjs --check-only` exits 0. It
reports receipt 017 with WO-139's `release-assignment` v0.29.5 and the
preserved WO-053 amendment and capability reassessment.

## Earlier observations

VER-001's O1–O6 and FINAL-001's note on the 07 wording about foreground Agent
joins concern files the repair did not touch. They remain as recorded:
non-blocking, and with no new evidence here.

## New observation (non-blocking)

- **N1 — low, pre-existing, same class as F1; nominate separately.** The pin
  list is hand-maintained, and the snapshot copies whole `dist` trees, so
  identity and integrity cover only the listed modules.
  - **Reach.** The relative-import closure from the 38 pinned modules reaches
    57 modules; 19 are unpinned. Examples:
    - `skeleton/dist/src/loadouts/feedback.js`, imported directly by
      `harness-host.js`;
    - compiler `normalize.js`, `verification.js` and `operator-control.mjs`,
      imported by the pinned compiler `harness.js`;
    - the beacon and control codebook modules reached from `reactor.js`.
  - **Reproduction.** In a disposable copy, a change to `loadouts/feedback.js`
    alone left the snapshot id unchanged. `harness check` still passed (28),
    and the installed copy stayed stale. Deleting that installed module still
    passed `harness check`.
  - **Provenance.** `main` has the same list minus `subagent-budget.js`, and
    the source of `loadouts/feedback.ts` is unchanged on this branch, so this
    is not WO-139's defect. It lies outside FINAL-001 F1's scope, which named
    the newly imported cap module.
  - **Candidate.** Derive the pin list from the entry points' import closure,
    or assert closure coverage in the harness fixtures, so that a future
    import cannot reopen F1. D006's reopening condition ("a required module
    changes without changing the installed runtime identity") describes this
    latent state for those modules.

## Limits

- **Codex.** Codex paths were not exercised. The order records the cap there
  as role text without enforcement. The repair's executor ran in Codex; its
  helper count is the executor's own report.
- **Live paths.** The live Agent, Task and Workflow paths were not re-run, and
  this session spawned no agents. Criteria 2–4 rest on the unchanged source,
  the fixtures and the gate, plus VER-001's live join.
- **Gate scope.** The gate ran the review set: 32 suites and 76 fresh tasks.
  `npm run test:docs` was not run separately.
- **Lifecycle field.** `RepairCompleted` carries
  `sourceVerificationId: "VER-001"`, although `RepairRequested` names
  FINAL-001. It is set from `state.latestVerificationId` in `scripts/resume.mjs`,
  and the same line exists on `main`. This is recorded as pre-existing
  lifecycle behavior, not judged here.
- **Process cost.** At entry (17:13:41Z): 122,362 total tokens. At 17:39:33Z:
  5,260,893 total tokens, mostly cached input. Both figures are dispatch scope
  from `claude-transcript-message-usage`; USD and reasoning tokens are unknown.
  The gate's 1,177.68 s was waiting time.
