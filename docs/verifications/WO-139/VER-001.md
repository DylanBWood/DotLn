# WO-139 VER-001 — Independent verification of the subagent admission cap

**Verdict: pass.** All seven acceptance criteria are met at the current
subject, reproduced independently below. The two operator-authorized scope
expansions recorded after activation (WO-139-D002 planning amendment route,
WO-139-D005 executor writer release) work as their fixtures and checks claim.
Six non-blocking observations are recorded for final review; none is an
acceptance-criterion failure.

Verified by a Claude Code session, dispatch `resume: verify` recorded by the
session hook at 2026-09-18T15:48:18Z. Harness version 2.1.276 is the harness
host's own observation for this session (`versionObservation`, channel
`ancestor-executable-lsof`). Model `claude-opus-5[1m]` is the model identifier
the harness supplied in this session's system context. Effort `xhigh` is the
`effort.level` value carried in this session's hook input and recorded in its
session journal. These are the actor's readback of harness-supplied values,
not an independent effective-session attestation.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.276","model":"claude-opus-5[1m]","effort":"xhigh","source":"self-reported"}

## Subject

- Order: [WO-139](../../work-orders/WO-139-subagent-cap.md), read in full at
  `sha256:a9cd1921…74111` (bytes 0–9015). Its only change from `main` is the
  release label `(v0.29.5)` in the title.
- Subject: the `wo-139` worktree over `main` at `494e682`, uncommitted. Every
  tracked file and all 47 untracked files equal checkpoint
  `refs/dotln/checkpoint/WO-139/3` (`6a06343`, tree `0c30705`) except the
  verify-transition projections (`docs/control/current.md`, the appended
  `VerificationRequested` row) and the index refresh this verifier ran after
  dispatch (`docs/work-orders/README.md`: phase and pending VER-001 lines
  only). Checked by `git diff` against the checkpoint and per-file blob
  hashes of the untracked files.
- Work after `ImplementationReady` (checkpoint 2, `b48d705`, 15:18:53Z): the
  operator-expanded D005 writer release, feedback/authority edition 002,
  lock-era evidence and the D005 decision were added before checkpoint 3. No
  lifecycle event represents that addition (D005 says so); this report judges
  it as part of the current subject.

**Instrument disclosure.** The instruments under verification also observed
this verification. The generated hooks reserved this session's writer,
refused this session's Git and shell reads during its own `npm test` (the
pre-existing WO-135 gate refusal), and counted this session's one helper
spawn. `harness usage`, which is under verification, supplied the subagent
counts quoted here. The gate-evidence machinery recorded this session's
`npm test` row.

**Fan-out.** Plan stated before the first spawn: at most two read-only helpers,
each judging a group of questions, no nested spawns, against the root budget
of 20 (observed 0 at entry). Actual: one read-only general-purpose helper
judged the D002 questions as one group. Observed count after the spawn: 1/20,
`exact-observed`, unresolved overlap 0/0.

## Acceptance criteria

| # | Expected | Observed | Result |
| --- | --- | --- | --- |
| 1 | Probe row with field shapes; design record names the counter key | [Probe 2](../../evidence/WO-139/subagent-probe-2.json) (Claude Code 2.1.276, 2026-09-18T14:18:34Z) records shapes only; all 22 hooks carry the root `session_id`; child hooks carry `agent_id`/`agent_type`; the direct child's `agent_id` equals Agent `tool_response.agentId` (identity-5) against `tool_use_id` identity-4; workflow children identity-12 and identity-16 are distinct. [Evidence README](../../evidence/WO-139/README.md) lines 3–11 name `session_id`, `agent_id` and `tool_use_id`. | Pass |
| 2 | Cap 3: third Agent admitted, fourth refused with named message; Workflow at zero remaining refused; `null` disables; missing/unreadable counter admits with named advisory and a fixture covers it | Unit suite `subagent-budget.test.js` 9/9. The generated-hook case "WO-139 generated permission, observer, Stop and usage share the root subagent cap" passes within the 51/51 harness fixtures. Refusal text: `DOTLN_SUBAGENT_CAP_REFUSED: count 3, cap 3; docs/control/budgets.json subagentCap would be exceeded…` (`subagent-budget.ts:255`). `null` admitted 22 and still counted; absent key → 20; missing, malformed, directory, locked counter and invalid budgets each admit with their named cause. | Pass |
| 3 | Attributable subagent calls count at first call and are refused beyond cap; unattributable reported uncounted in Stop | Unit case 2: children count once at their first attributed call, and a fourth child is refused on every attempt; case 6: the child observation persists when its own spawn is refused. The Stop summary states `uncounted remainder unknown (agents without attributable hooks; …)` (`subagent-budget.ts:238–240`). Live: this session's helper's first tool call recorded its hashed `agent_id`. | Pass |
| 4 | Stop advisory and `harness usage` report count and cap | Live `node scripts/harness.mjs usage <session>` returned `{"count":1,"cap":20,"remaining":19,"countKind":"exact-observed",…,"uncountedRemainder":"unknown"}`. The Stop hook output `Subagents: 3/3 … uncounted remainder unknown` is asserted by the generated-hook fixture; host wiring is at `harness-host.ts:2891–2895` and `1545–1563`. | Pass |
| 5 | Batching rule in Contributor instruction; bundle/manifest regenerate; `harness check` passes; 07 §Discipline and runbook record the refusal, source and count | Rule at `contributor.ts:427`, present in both generated skill roots (this session's delivered verifier skill carries it). `node scripts/harness.mjs check`: 28 generated surfaces, exit 0. `harness-context --check` exit 0; cold-start bytes: executor 20,461/20,480, verifier 17,503/20,480, reviewer 18,721/20,480, release-close 12,049/12,288, planner 13,121/24,576, in both roots. The verifier ceiling was raised 16,384 → 20,480 with a dated acceptance naming the rule. 07 §Discipline (lines 1575–1587), the runbook §DotLn hook boundary (lines 209–223) and the compiler boundary text (`harness.ts:855`) state four refusals, sourced to WO-135 and WO-139. The host's hard-refusal sites match: gate `2590`, planning `2592`, subagent `2919`, foreign writer `3034`. The other refusal-shaped sites are pre-existing input-contract, dispatch-admission, commit-message and target-worker paths. | Pass |
| 6 | Per-harness, per-path admission table; 07 candidate confirmed/updated | The [D003 table](../../evidence/WO-139/decisions.md) classifies each path. Agent: before creation. Task: the same branch, fixtures only. Workflow: the call needs one remaining unit and descendants exist before their first hook. Workflow `agent()`: at the first attributable tool call. Codex `spawn_agent`: not at all, carried as role text. 07 §Candidate — total subagent cap (lines 1178–1204) now carries the probe observation, the remaining gaps and a reopening condition. | Pass (see O1) |
| 7 | `npm test` green; `git diff --check` clean; no new dependency | `npm test`: 19 passed, 0 failed, 378.92 s (started 15:49:51Z; gate row 15:56:10.952Z). `git diff --check` against `main`: exit 0. Package metadata changes only bump compiler 0.13.1 → 0.13.2 and skeleton 0.25.3 → 0.25.4. | Pass |

Code read for criteria 2–4: `packages/skeleton/src/subagent-budget.ts` in full
and the host's session, permission, observe and Stop branches
(`harness-host.ts:2564–2580`, `2640–2642`, `2794–2799`, `2891–2925`). The
overlap computation is a maximum matching of unresolved direct admissions to
children observed at or after each admission. It therefore yields the minimum
distinct count, labeled `minimum-observed`, and never assigns a parent.

**Live reproduction.** This session's one background Agent spawn went through
the installed generated hooks. The counter file is keyed by
`sha256(session_id)` of this root session. It recorded one direct admission
with `childrenBefore: 0`, joined it to the hash of the agent identity the Agent
tool returned, and recorded the same hash as the child's first attributed
call. Both equalities were checked by recomputing the hash. Under background
execution the Agent result carried the identity immediately, so no
`minimum-observed` interval was observed.

## Operator-authorized scope expansions

**WO-139-D002, planning execution amendment and capability-history repair.**
Reproduced the inherited failure the expansion cites. In a scratch clone at
`494e682`, `node scripts/refute-plan.mjs check` exits 1 with "changed criterion
has no text-bound disposition". Caveat: the clone used this worktree's
`node_modules`, whose workspace links resolve to this branch's packages; the
plan scripts import no workspace package. At the current subject,
`node scripts/test-plan-refutation.mjs --check-only` exits 0. It reports
`authorized-execution-amendment` (WO-053-D002), committed
`pending-capability-history-repair` (`worker.source-change`), the workspace
dated reassessment, and WO-139's `release-assignment` v0.29.5. The planning
fixtures pass 41/41 (299.4 s).

The independent read-only helper recomputed the recorded event's bindings
against receipt 017. The source order hash, order hash and length (7,929) and
decision hash all match, and receipt 017 has no holds. The helper found that
exact-byte binding holds, that amendments cannot discharge holds, and that the
history repair admits only same-ID rows preserved byte-for-byte before commit.
This verifier checked the helper's defect claims against source before
recording them as O2–O4.

**WO-139-D005, executor/fix completion releases the Codex writer.** Code read:
`scripts/lib/executor-handoff.mjs` and `scripts/resume.mjs:861–879,
1092–1152`. The release is prepared before the append. A pre-append failure
propagates before the `try`, so ownership is retained. The release runs in
`finally` after the projection and index refresh, and only for the current
session's own actor. The case "WO-139 Codex executor and fix completion
automatically release after the final index" passes within the 51/51 harness
fixtures (15:57:36Z–16:05:41Z).

The path was not exercised live for this order: `ImplementationReady`
(15:18:53Z) preceded D005. The writer journal shows the executor actor
released at 15:47:31Z with no lifecycle transition at that time. The mechanism
of that release is not identified by the journal and is recorded as unknown.

## Other checks

- `npm run test:docs`: the first run failed only on the stale work-order index
  (the verify dispatch had changed the phase) plus four checks gated behind it.
  After the post-dispatch `npm run work-orders -- index` the verifier procedure
  requires: 17 passed, 0 failed, 126.05 s.
- Probe and authority-probe fixtures: 33/33, including "WO-139 subagent probe
  confines reads and parent fan-out structurally".
- `docs/final-reviews/WO-139/PR.md` holds only the generated process-meter
  projection, not a reviewer-authored body.

## Observations (non-blocking)

- **O1 — low, design-record completeness.** Probe 2 observed `SubagentStart`
  events carrying `session_id` and `agent_id` for the direct child and both
  workflow children, each before that child's first tool call (probe lines
  108–127, 494–513, 648–667). The implementation registers no `SubagentStart`
  hook. The evidence README, the D003 table and the 07 candidate describe
  tool-free agents as unseen but do not record that this observed event could
  count them. Whether `SubagentStart` can deny is untested. Candidate for the
  open total-cap requirement: reassess counting at `SubagentStart`.
- **O2 — low, theoretical.** `executionAmendmentSource`
  (`plan-continuation.mjs:64–70`) uses the multiline flag. If an order's title
  still carries the placeholder, a later `# … (vX.Y.Z)` line would be
  normalized, hiding a change to that version. `releaseAssignment` (lines
  22–38) anchors to the string start. No work order currently has a second H1
  line; `TEMPLATE.md` does, and it is not in the judged horizon.
- **O3 — low–medium, design provenance.** The `PlanExecutionAmended` event
  (`plan-receipts.mjs:1188–1201`) carries no actor or operator-capture binding.
  `amend-order` admits any order in the latest judged horizon that has a
  matching decision in its own decisions file. Its authority therefore rests
  on role text and an agent-written decision. That matches `plan dispose` and
  is weaker than `override` (capture hash plus actor). 07 lines 782–783 call it
  "an actor-attested authorization record", although the event has no actor
  field. Final review or the operator should confirm the intended provenance,
  or reword the claim.
- **O4 — low, documentation precision.** 07 lines 781–782 say unrecorded changes
  "fail with their order/criterion named". A non-criterion edit fails with
  "existing work-order bytes changed" (`plan-continuation.mjs:43–46`), which
  names no path, and no caller adds one.
- **O5 — low, test coverage.** The generated-hook case's title names the
  observer, but it never drives a PostToolUse Agent result through the
  generated observer. `linkSubagentResult` is covered by unit cases and by
  this session's live join.
- **O6 — cosmetic.** The verifier acceptance in `docs/control/budgets.json`
  records 17,477 bytes; the current measurement is 17,503 after D005's role
  text, still within 20,480. The refusal lists in the runbook (lines 211–215)
  and 07 (lines 1575–1579) contain a doubled "and".

## Limits

- Codex paths were not exercised; the order records the cap there as role text
  without enforcement.
- The live Task and Workflow paths were not re-run by this verifier; they rest
  on probe 2 and the fixtures. The Stop advisory was observed through the
  fixture, not in this live session.
- The operator messages behind D002 and D005 are not available to this
  session. Their authority is taken from the decisions record and the adjacent
  queue entry it cites.
- Neither `npm test -- --review` nor the full machinery set was run. The
  machinery suites run were the harness fixtures, probe fixtures, planning
  fixtures, current plan check, `harness check` and `harness-context --check`.
- Process cost: at entry (15:48:48Z) 292,897 total tokens; at 16:10:06Z,
  17,664,569 total tokens. Both are dispatch scope from
  `claude-transcript-message-usage`, mostly cached input; USD and reasoning
  tokens unknown. The helper reported 123,352 tokens of its own.
