# WO-154 — VER-002

**Verdict:** pass. The repaired subject satisfies criteria 1–8, including the
rebuilt compiler release path that failed VER-001. No new implementation
finding.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.156.1","model":"gpt-6-sol","effort":"xhigh","source":"codex-session-readback"}

Independent Codex verifier, dispatched by `resume: verify` on 2026-09-24.
The model, selected effort and CLI version are the current-session readback;
effective provider settings are unobserved. One writer owned the worktree.
No subagent or live model episode was launched by this verifier (0 observed
admissions of a 20-agent cap; unobserved coverage remains unknown). The
transport exposes no hard USD 5 provider cap, so that limit was unenforced.

**Process cost:** entry 41993 tokens; handoff 4208833 tokens; source codex-transcript-counter

Both readings have dispatch scope and include reused input tokens. Entry
cutoff: 2026-09-24T18:12:23.333Z; handoff sample cutoff:
2026-09-24T18:20:46.008Z. Dollar cost is unavailable. No further agent
work or waiting was purchased for a second judgment.

## Subject and method

The selected [WO-154](../../work-orders/WO-154-evidence-editions-by-reference.md)
is based on `75743fd6`; this verification's entry checkpoint is
`refs/dotln/checkpoint/WO-154/7` (`759eaf10`). The repair's live feedback
revision 002 and the three deterministic WO-154 revision 001 editions are
selected in [current.json](../../evidence/current.json). VER-001 remains the
immutable judgment of feedback revision 001. I made no implementation,
product-document or branch-commit change.

I read the order and its cited REVIEW-002 ER2-001/ER2-002, REFUTATION-003,
both filed packets, WO-147 D010, the product 03 and 07 sections, and the
receipt subject's validation and contract. I inspected the repaired compiler
and reactor paths, recorder and reader, regression tests, WO-154 decisions,
repair evidence, both feedback editions and the prior verification. The
feedback checker contributes evidence to the document gate, so I also
compared its projected stream with the separate saved runtime stream and
inspected the current event and manifest shapes directly.

## Acceptance findings

| Criterion | Observed result and evidence |
| --- | --- |
| 1 — references and resolution | **Pass.** Feedback revision 002 is schema 2. Its opening has 52 subject and 49 baseline `{path, blobHash, bytes}` references without `contents`; the persisted capsule also carries references. Direct resolution reproduced all 2,196,293 bytes of the saved verifier stream exactly. The focused skeleton cases exercise uncommitted, committed, synthesized and missing-path resolution. |
| 2 — behavior, pins and live-audit rule | **Pass.** The edition records distinct behavior and pins identities. The focused end-to-end fixture changes compiler source, package and lock labels, rebuilds, observes `--check` name the deterministic carry, mints it with no live episode, selects it and checks again. A skeleton or console label change needs no action. D001 enumerates all 51 judged files and the regenerated report as behavioral inputs and explains why nonjudged registered sources are covered through the report. |
| 3 — D010 refusal | **Pass.** The focused fixtures refuse a judged `worker-store.ts` change through both check and carry. The skeleton regression also forges the behavior identity and bypasses that layer: the live verifier's per-file comparison still refuses it. Its subject-only report comparison models the field difference D010 recorded; that comparison is synthetic, so the direct judged-file refusal is the consequential control. |
| 4 — immutable history and console | **Pass.** `git diff` shows no changes to tracked older feedback editions or planning receipts; feedback revision 001 matches all seven files in the pre-repair checkpoint 5 by Git blob identity. Schema 1 remains accepted by the source test. The console's pinned current case and stored-stream replays passed in `npm test`. The new WO-154 identity manifest chains to the unchanged WO-050 oracle, with reasons for changed and removed rows. |
| 5 — measured size and rule | **Pass.** Direct size measurement totals 189,014 bytes for revision 002, including pins; D013's same-episode by-value comparison is 2,250,286 bytes (91.6% smaller). The projected verifier stream is 115,186 bytes. D009/D013 and product 03 record the before/after values, corrected planning premise and live-episode rule. The by-value counterfactual is attributed to the executor's measurement. |
| 6 — receipt subject | **Pass via the order's split alternative.** D003 gives the separate planning contract and cost reason, names the patch-order follow-up, and leaves prior receipt bytes and the current subject-hash check unchanged. |
| 7 — re-mint and gate | **Pass.** The current pointer selects the four new editions. The feedback verifier stream records a live `claude-cli-print` verifier, and its reconstructed replay has a complete matrix with two verified rows. The saved audit stream equals the recorded runtime audit byte for byte. The four current edition `--check` commands pass. `npm test` passed 27 suites, 0 failed, 71 fresh tasks in 287.12 s; `git diff --check` is clean. The lockfile changes versions and exact workspace pins only; no dependency was added. The product suite count and fresh task count are unchanged from VER-001. |
| 8 — rebuilt-label repair | **Pass.** The focused `WO-154` source fixture passed both cases, including a rebuild before check and carry, carried feedback and verification stream replay, and a post-build judged-file refusal. Compiler assertions lower a recorded program or capsule at its recorded release and compare every other field. The verification reactor adopts a persisted capsule only when that label differs and the remaining command is identical; its test refuses changed subject and hash drift. Historical 0.5.0, 0.6.0 and 0.17.0 streams replay in the passing skeleton gate. The pin snapshot is offered as a working-tree candidate; no `git hash-object -w` remains in the recorder. |

## VER-001-F1 on the repaired subject

**Earlier severity:** major. **Expected:** a rebuilt compiler label can
retain or carry the old live audit without a new episode; any behavioral or
persisted-command drift is refused. **Observed now:** the end-to-end fixture
first runs a new process loading the rebuilt `@dotln/compiler`, then sees
`--check` request a deterministic carry because the console binds the moved
policy hash. `--carry` writes only a new edition record and feedback report;
its check succeeds. The same fixture replays the gate's stored feedback and
verification streams (7/7 subtests) and refuses a later judged-file edit in
both check and carry. The test runs in a disposable copy with its own workspace
links, the missing condition in the VER-001 fixture. The current compiler and
verification unit tests separately refuse a changed mechanism, changed
work-order subject, incorrect derived hash and malformed release. F1 is
resolved for the recorded cases; its D010 follow-up need not remain open.

## Executed checks and limits

- `npm test`: 27 passed, 0 failed, 71 fresh tasks, 287.12 s.
- `npm run test:docs`: 21 passed, 0 failed, 21 fresh tasks, 30.30 s with
  this report present; format, current evidence, planning and receipt checks
  passed.
- `node --test --test-name-pattern=WO-154 scripts/test-evidence-sources.mjs`:
  both cases passed in 42.25 s, including the rebuilt-label reproduction.
- Direct reference reconstruction matched the 2,196,293-byte saved verifier
  stream; replay returned `complete` with two `verified` rows. The recorded
  audit stream also matched its saved runtime stream byte for byte.
- The four current authority, artifact identity, verification and feedback
  edition checks passed. The feedback check reported ten passing regressions,
  ten removal failures and 1,192 saved instruction bytes.
- `git diff --check` passed; tracked older evidence and receipts have no
  diff, and revision 001 matches checkpoint 5 by blob identity.

The one live episode used to mint revision 002 is recorded in D013 and the
saved event stream; this verifier inspected it and ran no provider episode.
The provider's effective model and effort are unknown. The resolver requires
the referenced Git objects or matching working-tree files, the order's
explicit reader assumption. Final-review integration with main has not yet
been judged; product 07 assigns that assessment to the reviewer.

This passes the critical-path enabling claim: the repaired rule removes
repeat live audits for unchanged judged behavior while preserving D010's
refusal. NoOp would leave VER-001-F1 at the next rebuilt compiler release.
Policy resistance mattered because replay used to undo the label-normalized
check; the direct replay and rebuilt fixture now agree. Commons cost and
shifting the burden favor the smaller edition and deterministic carry, while
the repair did pay one authorized live episode. Drift to low performance and
rule beating are addressed by the changed-source refusals and by rebuilding
inside the regression. Escalation is limited to one named carry when the
console's policy hash moves. Success to the successful is checked by the
recorded receipt split and declined storage alternatives. Seeking the wrong
goal would count a green checker while stored streams failed; both were
tested. Under Naive Interventionism, the broader historical replay admission
is the material side effect; the chained oracle and historical-stream tests
cover the recorded cases. Reopen D011 if a recorded compiler release changes
behavior through another field or a consumer binds another report identity.
