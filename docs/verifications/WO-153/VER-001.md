# WO-153 verification — VER-001

**Verdict:** fail.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.156.1","model":"gpt-6-sol","effort":"xhigh","source":"codex-session-readback"}

Dispatch: `resume: verify`, 2026-09-24. Subject: worktree `wo-153` at
`75743fd64031e83bb13c3873411fd5ca56cda1c1`; current authored
`scripts/resume.mjs` SHA-256
`7fda4bd85ef139f1df39339d0f20c9d30d8b421bfc7fc7234cd2e7745157f86e`
and `scripts/test-process-debt.mjs` SHA-256
`d5ba42405da26fec0a9e8559dd34a4001d97abd14f5ad03352d80bc4fb3fbd1b`.
The implementation was not edited during this verification. The isolated probe
used a temporary repository and removed it afterward.

## Finding F1 — partial begin reports the wrong cause

**Criterion:** 1 (and the objective's named-cause requirement). **Severity:**
medium; substantive repair finding.

**Expected:** When session entry fails after a lifecycle transition, the
dispatch admits the transition, prints its briefing and names a cause that
agrees with the following usage readback. Criterion 1 specifically requires
`unknown; cause no-session` from that readback under a forced begin failure.

**Observed:** The new catch prints `cause no-session` for every error escaping
`beginHarnessSessionOnce`. The host writes the session JSON before appending its
observation row. If that append fails, the session exists. In an isolated Git
repository, I precreated the session-key `.jsonl` observation path as a
directory and called `beginHarnessSessionOnce(root, session, "verifier")`. It
threw `Host observation log is not a regular file`; the session `.json` existed;
`measureHarnessUsage(root, session)` returned
`cause: "session-counters-unavailable"` with null total tokens. The catch would
therefore give the operator `no-session` while the usage command sees a
session and returns another cause. On a repeat dispatch, the existing JSON
also takes the adapter's silent already-began branch. The new fixture injects
an invalid role, which fails before the session JSON write and cannot detect
this case.

**Reproduction and sources:** `packages/skeleton/src/harness-host.ts`
`beginHarnessSession` writes state before `record`; `record` refuses a non-file
observation path; `scripts/resume.mjs` catches and unconditionally writes the
`no-session` advisory; `scripts/test-process-debt.mjs` covers the pre-write
invalid-role path. The temporary probe printed
`{"error":"Host observation log is not a regular file","stateExists":true,"usage":{"cause":"session-counters-unavailable","totalTokens":null,"source":"unavailable"}}`.
The later readback is an executed observation. The dispatch's exact stderr in
this partial case is inferred from the checked catch, rather than observed by
a full fixture dispatch. The repair follow-up and reopening condition are in
WO-153-D005 in `docs/evidence/WO-153/decisions.md`; D001 had already named this
partial case as an inference and reopening condition. Preserve the admitted
dispatch and briefing while repairing cause consistency.

## Criteria and checks

| Criterion | Judgment | Evidence and limit |
| --- | --- | --- |
| 1. Five dispatches and cause readback | **Fail** | The focused WO-153 fixture passes its invalid-role path for all five actions, but F1 reproduces another begin failure for which the readback differs from `no-session`. |
| 2. Repeat, no-thread and successful begin | Pass for tested paths | The focused fixture asserts silence, unchanged repeat session JSON, no-thread behavior and successful begin; both unchanged WO-149 cases pass. F1 shows an incomplete session can also take the silent repeat branch. |
| 3. Receipt cause grammar | Pass for `no-session` | `no-session` is in `usageCauseCodes` and the fixture calls `judgeCostLine`. F1's observed readback cause is different. |
| 4. Decisions and WO-149 boundary | Pass | WO-153 D001–D005 record sources and reopening conditions; `npm run meta` regenerated the decisions index. `docs/evidence/WO-149/decisions.md` has no diff. |
| 5. Gate, diff, dependencies, step count | Pass on recorded evidence | The executor's product gate row reports 27 suites, 71 fresh tasks, 0 failures and 294,546 ms; I ran the focused check (3/3) and `git diff --check` (clean). No package or lockfile changed. The added `test()` is inside the existing `process-debt` machinery suite: zero new product gate suites or fresh tasks. I did not rerun the full gate; the execution guide places its one current-subject run at final review. |

Focused command: `node --test --test-reporter=spec
--test-name-pattern='WO-149|WO-153' scripts/test-process-debt.mjs` → 3 pass,
0 fail, 1,821 ms. `npm test -- --list` enumerated the 27 product suites.
The D005 write-back was accepted by `npm run meta`; `npm run test:docs`
passed 21/21 fresh tasks in 31.88 s. No earlier WO-153 verification exists.

## Goal and limits

The catch removes the blocked-briefing path demonstrated by the invalid-role
fixture, improving operator flow. F1 means the receipt can still claim a cause
that its own usage reader contradicts, a rule-beating and drift risk. A bounded
repair should keep the existing admitted dispatch and avoid retries or new
workflow. The other system-trap lenses have no material new resource, authority
or process change in this finding: the current one-catch design adds no gate or
subagent work, and its alternatives are recorded in D001. NoOp leaves F1's
false cause and silent repeat. The absence of a live Codex failure remains a
limit; this verdict rests on the executable isolated probe and current source.

**Process cost:** entry 81113 tokens; handoff 2221326 tokens; source codex-transcript-counter

Entry counter scope: dispatch, cutoff 2026-09-24T16:03:04.037Z. Handoff cutoff
2026-09-24T16:08:40.185Z. Dollar cost is unknown because the
counter supplies no USD value. No subagents were spawned; observed count 0 of
cap 20, with unobserved remainder unknown. This verification required one
temporary failure probe in addition to the existing focused test; a full gate
rerun would duplicate the recorded gate and would not resolve F1.
