# WO-153 verification — VER-002

**Verdict:** pass.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.156.1","model":"gpt-6-sol","effort":"xhigh","source":"codex-session-readback"}

Dispatch: `resume: verify`, 2026-09-24. Subject: the current `wo-153` worktree after the VER-001 repair, with `scripts/resume.mjs` SHA-256 `7fda4bd85ef139f1df39339d0f20c9d30d8b421bfc7fc7234cd2e7745157f86e`, `scripts/test-process-debt.mjs` SHA-256 `7a5999ed0fb4a7c9d103522814b2c6336f80e3108e2a581dcfc239a695845384`, and `packages/skeleton/src/harness-host.ts` SHA-256 `576b9c391f50335c63d38335ca84f9d1e8c8289b560119894f368a7f73c08d29`. I made no implementation edits. This report judges the original order, the current subject, and VER-001 F1.

## Judgment

| Criterion | Result and evidence |
| --- | --- |
| 1. Five failed dispatches, briefing, transition and cause | Pass. The WO-153 fixture forces both a pre-write invalid role and VER-001's post-write observation-log failure for `next`, `verify`, `fix`, `final-review` and `release-close`. For each, it asserts exit 0, the briefing and allocated path where applicable, the named stderr error and `cause no-session`, the expected control event, absent session JSON, and a subsequent `harness usage` readback of `no-session` with null tokens. I reran the fixture as part of the focused 3/3 check below. |
| 2. Silent and unchanged paths | Pass. The same fixture checks a successful begin, byte-identical session JSON and no advisory on repeat, and no entry or advisory without a thread. The two adjacent WO-149 cases pass without edits. After a post-write failure, repeating `next` names the failure again; removing the obstruction lets the next dispatch begin silently. |
| 3. Closed cost cause | Pass. `no-session` is in `usageCauseCodes`; the fixture passes the observed readback cause to `judgeCostLine`, which accepts `unknown; cause no-session`. |
| 4. Decisions and boundary | Pass. WO-153 D001–D008 record the sources, the VER-001 repair and reopening conditions. The decisions index contains the repair follow-up's settled state. `docs/evidence/WO-149/decisions.md` has no worktree diff. |
| 5. Gate, diff, dependencies and count | Pass. The retained `npm test -- --review` row at 2026-09-24T16:40:24.929Z matches the current `gateCodeIdentity` (`529919eb8d7cc42b2218c6126510a1c7e494e84919935bf5d5b7f901b12d129e`): 35 required suites, 79 fresh tasks, 0 failed cases, 629,089 ms. `git diff --check` is clean. The package changes bump the existing skeleton version and console pin to 0.38.1; no dependency is added. The fixture remains one test inside the existing `process-debt` machinery suite, adding zero product-gate suites and zero product-gate fresh tasks. |

Independent focused command: `node --test --test-reporter=spec --test-name-pattern='WO-149|WO-153' scripts/test-process-debt.mjs` passed 3/3 in 4,149 ms, including the WO-153 case in 3,237 ms. `npm test -- --list` enumerated the 27 product suites. The matching 35-suite row includes the product suite and the selected machinery checks at these current code bytes, so another full run would repeat the recorded gate without resolving a remaining concern.

VER-001 F1 is closed. In the current host, `beginHarnessSession` removes the session JSON and rethrows when its entry observation append fails. The fixture reproduces F1 through the real dispatch table for all five actions and observes both the `no-session` readback and a non-silent retry. The catch in `resume.mjs` remains byte-identical to VER-001's subject; the changed host behavior makes its cause accurate for this reproduced failure. The host also exposes an explicit begin caller; the rollback occurs inside the shared host function. I did not run a separate live Codex failure episode, which the order does not require. An unlink failure after the session JSON is written remains unexercised, as D006 records; no such failure was observed here.

This patch advances operator flow by keeping a recorded lifecycle transition and its report path available after measurement fails. The fixture and matching gate address policy resistance, drift and rule beating: the dispatch proceeds and its cause agrees with readback. The existing single test case and one-line failure advisory limit shared compute and process growth; the host rollback removes repeated operator rescue after a partial begin. The repair was checked against the prior failure rather than accepted because the fixture alone is green. NoOp would retain F1's false cause and silent repeat. The intervention is reversible and does not change the authority or dispatch set; the remaining system-trap lenses reveal no separate material risk at this scope.

**Process cost:** entry 43465 tokens; handoff 621450 tokens; source codex-transcript-counter

Counter scope: dispatch. Entry cutoff 2026-09-24T16:43:57.718Z; handoff-read cutoff 2026-09-24T16:45:21.259Z. USD cost is unknown because the counter supplied no dollar value. No subagents were spawned; observed count 0 of cap 20, with unobserved remainder unknown. The focused check and gate reuse met this verification's evidence need without a second full run.
