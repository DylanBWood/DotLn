# WO-069 VER-002 — pass

**Date:** 2026-09-21
**Work order:** [WO-069 — Configuration root](../../work-orders/WO-069-configuration-root.md)
**Verdict:** pass. All five acceptance criteria hold at the repaired subject, and each of VER-001's five findings reproduces as repaired by an independent counterexample. One bounded residue is recorded as [WO-069-D016](../../evidence/WO-069/decisions.md#wo-069-d016) rather than as a repair finding; no criterion depends on it.

**Actor:** independent verifier, Claude Code 2.1.278, claude-opus-5[1m], xhigh; the session hook recorded `effort: xhigh` on every hook row of this dispatch and observed the CLI version through `ancestor-executable-lsof`. No subagents were used: the plan at entry was zero fan-out against the remaining cap of 20, and the harness usage readback reports `count 0` (`exact-observed`, `remaining 20`).
**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"xhigh","source":"claude-hook-readback"}
**Process cost:** entry 179869 tokens; handoff 11242408 tokens; source claude-transcript-message-usage

## Subject, sources and limits

The operator dispatched `resume: verify`; the harness had already recorded that dispatch and allocated this path. Canonical status selected WO-069 in `verifying` with `verification-result` as the only legal action. The judged source is the uncommitted repaired implementation preserved at `refs/dotln/checkpoint/WO-069/7` (`7bb85d09605467d56e66a06f86b3021cc2b8cdd5`), above activation base `010b3ba52fd3a3532ffe222904b0720d16d3ed87`. The working tree held 84 changed or new paths at entry and the same 84 at handoff: no implementation was edited during verification, and no verifier fixture wrote inside the repository.

Read for this judgment: the original order; VER-001 and the repair records it produced; `scripts/lib/config.mjs` in full; the changed regions of `lineage.mjs`, `plan-direct.mjs`, `plan-subject.mjs`, `plan-receipts.mjs`, `planning-followups.mjs`, `writing-worker-probe.mjs`, `license-surfaces.mjs`, `work-orders.mjs`, `harness-context.mjs`, `intake-reconciliation.mjs`; `scripts/test-configuration-root.mjs`; the product 07 and 10 write-backs; the decisions index and follow-up register rows; and the executor's [evidence](../../evidence/WO-069/configuration-root.md) and [decisions](../../evidence/WO-069/decisions.md). Unrelated final-review artefacts present in the worktree were not used as evidence.

The loader, lifecycle commands, work-order index, test runner and gate recorder under verification also recorded this session. Independent observations come from matched base/current editions in a temporary clone, from fixtures whose launchpad is a different directory from the running scripts, and from adversarial mutation of a copied source tree. Every fixture lived in this role's granted session scratch or in system temp; no live transport, publication or main integration was performed. The `2099-01-01` report date in the writing-worker probe is synthetic.

## Acceptance results

| Criterion | Judgment and evidence |
| --- | --- |
| 1 — unchanged suites and byte identity | **Pass.** Seven matched outputs are byte-identical between the activation-base scripts and the current scripts over the same control history, refs and tags. The whole suite table runs green: the runner reports 22, 19 and 17 tasks passing and none failing for the product, document and machinery selections, which list 22, 18 and 16 suites. `product` is defined as `!machinery && !document`, so those three selections are the complete table, and `--review` adds only changed machinery already run. |
| 2 — lifecycle under configured roots | **Pass, with the recorded package-lane limit.** An independent launchpad whose roots are `vault/...`, driven by a **separate** tool checkout through `DOTLN_LAUNCHPAD`, carried `activate`, `implementation-ready`, `verify`, `verification-result`, `final-review` and `final-review-result` to `closed`, writing every lifecycle document under its configured roots. The single non-configured write is the Git-ignored `docs/control/local/harness/checks.json` lane that [D005](../../evidence/WO-069/decisions.md#wo-069-d005) reserves for WO-070 and product 07 states. `worktree start`/`finish` under configured roots passed from a copied tree. |
| 3 — one root derivation, literal-root guard, malformed refusal | **Pass.** The guard was mutated-tested, not merely read: five injected offences in a copied tree — string literal, template literal, regular-expression fragment, `new URL("../", import.meta.url)` and the two-step `fileURLToPath(new URL("./", import.meta.url))` — were all named by the failing assertion. Fourteen malformed configurations each refuse with the configuration path. No document-root derivation remains in the non-test sources. |
| 4 — write-backs | **Pass.** Product 07 gains §Where the control plane finds its documents (discovery order, `DOTLN_LAUNCHPAD`, the eighteen root names, positive validation, the package-lane limit); product 10 gains the launchpad configuration schema axis; the pre-2026-09-09 ledger duty is discharged by the decisions file and its fifteen decisions-index rows, and the work-order index marks that substitution. |
| 5 — npm test, diff check, dependencies | **Pass.** `npm test` exit 0 at code identity `027fbe3a…`; `git diff --check` exit 0; `package.json`, the lockfile and `packages/` are untouched; `devDependencies` are unchanged and no dependency was added; no new lint or type suppression appears in the script diff. |

## Executed evidence

- `npm test`: **22 passed, 0 failed**, 266.35 s, 66 fresh tasks. Recorded gate row: `exit 0`, `executed true`, `durationMs 266351`, `treeHash 469a5b05…`, `codeIdentity 027fbe3af9c29ff57ec371b6b4446fb61c44c0f2d0435dae98e385e1a044326a`, at `2026-09-21T05:27:12.027Z`. The WO-143 filesystem-boundary case that failed once during the executor's first repair gate ([D015](../../evidence/WO-069/decisions.md#wo-069-d015)) passed here inside the same 66-task selection; that is one more observation of a concurrency-sensitive case, not proof of its cause.
- `npm run test:machinery`: **17 passed, 0 failed**, 292.31 s, 17 fresh tasks — including the `harness-fixtures` suite whose cases failed at VER-001.
- `npm run test:docs`: **19 passed, 0 failed**, 22.43 s, 19 fresh tasks.
- `node --test scripts/test-configuration-root.mjs`: **12 passed, 0 failed**, 2.21 s.
- `node --test scripts/test-license-surfaces.mjs`: **9 passed, 0 failed**, 5.86 s.
- `node --test --test-name-pattern='compiler-only release installs' scripts/test-harness.mjs`: **1 passed, 0 failed**, 0.66 s.
- `npm run format:check`: clean. `npm run publication:check`: `PASS publication bootstrap checks`. `git diff --check`: exit 0.

### Independent byte comparisons

A temporary clone was checked out at the activation base, its `refs/dotln/*` checkpoint refs and tags fetched, and copied; the copy received only the current `scripts/` overlaid. Both editions therefore read the same committed control history and refs and differ only in script bytes. `current.md` was deleted and re-rendered by `refreshControlProjection(root)` in each edition rather than copied, and the work-order index was regenerated in each edition, so both are products of the two script editions.

| Output | Command / producer | Bytes per edition | `cmp` exit |
| --- | --- | ---: | ---: |
| `status.json` | `node scripts/resume.mjs status --json` | 24,122 | 0 |
| `status.txt` | `node scripts/resume.mjs status` | 1,103 | 0 |
| `times.txt` | `node scripts/resume.mjs times` | 122,266 | 0 |
| `usage.txt` | `node scripts/resume.mjs usage` | 85,872 | 0 |
| `manifest.json` | `node scripts/release.mjs manifest-from-tag v0.37.1` | 7,435 | 0 |
| `current.md` | `refreshControlProjection(root)` after deleting the file | 983 | 0 |
| `index.md` | `node scripts/work-orders.mjs index` | 316,182 | 0 |

A first pass produced an empty `times` transcript in both editions because the clone lacked the local checkpoint refs; the refs were fetched into both editions and the measurement above is the corrected one. That prerequisite is a fixture property, not a subject failure.

### VER-001 findings, rechecked against the repaired subject

Each counterexample was rebuilt from the finding's own description, not from the executor's test.

- **F1 — lineage and planning consumers.** Matched default and configured fixtures hold the repository's real ledger under `docs/lineage/` and `records/lineage/` respectively. `DOTLN_LAUNCHPAD=<fixture> node scripts/lineage.mjs index` now exits 0 in both, printing `Generated docs/lineage/README.md` and `Generated records/lineage/README.md`; the two indexes are byte-identical (17,489 bytes) and the configured fixture never creates `docs/`. **Repaired.**
- **F2 — follow-up validation.** Physical temporary Git roots carry the same `00-fixture.md` candidate under `docs/product` and, with `roots.docs = "records"`, under `records/product`. `plan followups --sync` now exits 0 in both; the configured run writes `records/planning/followups.json` with its reference intact as `records/product/00-fixture.md#candidate--configured-root-follow-up`. **Repaired.**
- **F3 — wrong-checkout report write.** With the scripts copied to a separate tool checkout and `DOTLN_LAUNCHPAD` naming a different launchpad, `renderReport({date:"2099-01-01"})` writes `records/discovery/writing-worker-smoke-2099-01-01.{md,json}` into the launchpad, and the tool checkout gains no `docs/` directory at all. The guard now recognizes the two-step derivation shape, as the mutation probe above confirms. **Repaired.**
- **F4 — legal-document error contract.** `licenseSurfaceRules(<empty dir>)` returns the structured failure `FAIL license-surfaces docs/LEGAL.md pins: observed docs/LEGAL.md is missing or is not a contained regular file; expected the three decided SHA-256 pins`, and the configured fixture returns the same shape naming `records/LEGAL.md`. No `ReferenceError`. **Repaired.**
- **F5 — harness fixture dependency graph.** The named case passes alone, and the full machinery gate passes all its suites. **Repaired.** VER-001's causal uncertainty about the operator-release barrier case is resolved in the same direction the repair record claims: it passes once the fixture's module graph is complete.

## Observation recorded, not a finding

`config.mjs` documents `TOOL_ROOT` as the owner of kit paths and the launchpad as the owner of documents. Several self-measurement and benchmark scripts bind one root to `findLaunchpad()` and then read kit paths under it. Observed directly: with `DOTLN_LAUNCHPAD` naming an unrelated launchpad, `scripts/harness-context.mjs` exports `fixtureTree` pointing at `<launchpad>/scripts/fixtures/harness-context/tree`, which does not exist, while the same file's evidence output correctly follows the launchpad. In the default layout the two roots coincide, which the byte-identity table above demonstrates; no acceptance criterion names these scripts, and the failure mode is a loud missing-path read rather than a wrong-checkout write. `npm run meta` was checked in the same separate-launchpad fixture and works. The residue is boarded up as [WO-069-D016](../../evidence/WO-069/decisions.md#wo-069-d016) with a named follow-up, alongside the kit/plane boundary D005 already assigns to WO-070; it does not route to repair.

## Judgment and handoff

WO-069 is the prerequisite that lets a launchpad's document layout be data rather than code, which the export kit and the target-repository children of WO-033 consume. The repaired subject delivers that: a separate tool checkout drove a whole lifecycle into a configured launchpad, while the defaults reproduce the activation base byte for byte across seven outputs and the entire suite table.

Goal-alignment comparison. Policy resistance and rule beating were tested rather than assumed: the guard was mutated until it failed, and the configured-root claims were re-derived from the findings' own descriptions instead of from the executor's suite. Drift to low performance and seeking the wrong goal are checked by judging behaviour under a launchpad that is not the tool checkout — the case the copied-tree fixtures cannot distinguish — rather than by counting removed literals. Escalation is bounded: one product gate, one machinery gate, one document gate and small fixtures, with no subagent fan-out against the cap of 20. Commons cost is the three gates, about 10 minutes of machine time, justified because the document and machinery selections are where VER-001's F5 lived and the product-only gate did not see it. Shifting the burden is avoided by recording the one residue as an indexed follow-up with a reproduction rather than as an operator inspection step. Success to the successful is countered by not treating a green product gate as the judgment. Naive Interventionism favours accepting a repair whose default behaviour is provably unchanged and boarding up the one out-of-criteria residue; NoOp would withhold a prerequisite that five reproduced repairs and the full suite table support.

Usage readback was available at both ends of this dispatch (`claude-transcript-message-usage`, scope `dispatch`): entry 179,869 tokens at `2026-09-21T05:17:42.185Z`, handoff 11,242,408 tokens at `2026-09-21T05:34:39.669Z`; totals include the three gates' waiting time. No token reduction is claimed.

Record `verification-result pass`; the next legal operator dispatch is `resume: final review`. Final review owns integration with `main` and any retiming under the existing patch classification, including the `v0.37.2` assignment this order's release preparation recorded. No implementation defect was silently fixed by the verifier.
