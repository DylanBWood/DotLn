# WO-110 VER-002 — independent verification after repair

**Verdict:** pass. All four acceptance criteria are satisfied at this subject.
VER-001 F1–F3 are repaired, and the release-component correction in
WO-110-D010 is supported by the pinned source comparison. No new blocking
finding was observed. This verifier changed no implementation.

**Subject:** the `wo-110` working tree based on
`920c5fd2d42ce8f636cc91241a3b170e89e91db6`, captured at verification dispatch
by `refs/dotln/checkpoint/WO-110/7`
(`3856220f872be8351f6ca1d2ab1be32de32c96d6`). Authority:
[WO-110](../../work-orders/WO-110-local-model-transport.md). Earlier report:
[VER-001](VER-001.md). Eighteen transport, integration, test, dependency and
evidence-selection files were compared with the checkpoint's Git blobs and
remain unchanged. Checks below ran on 2026-09-20 in this attended session.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-6-astra","effort":"xhigh","source":"codex-session-readback"}

Human actor: the operator dispatched `resume: verify`; canonical status admitted
that action and allocated this report. The briefing supplied the actual actor
values above. No subagents were planned or launched: explicit count zero;
the session usage observation also reported zero against a cap of 20, with
unobserved coverage unknown. The verifier registered its session and explicitly
invoked the existing writer guard; this is not automatic Codex hook evidence.

**Process cost:** unknown; cause no-session

The entry usage command reported that the harness session had not begun, so
the entry total is unknown. After registration, counters were initially
unavailable and subsequently became readable from `codex-transcript-counter`,
scope `dispatch`. The handoff total and observation cutoff stay in the ignored
usage receipt and operator response. No entry-to-handoff token delta or dollar
cost is inferred. The measured product and document gate durations below
include waiting within those commands and are not performance-improvement
claims.

## Earlier findings and the release correction

| Item | Expected after repair | Observed evidence | Judgment |
| --- | --- | --- | --- |
| VER-001 F1: seven unformatted files | Current formatter passes without suppressions | Fresh `npm run test:docs` passed `format`; reviewed source adds no lint/type suppression | Resolved |
| VER-001 F2: stale authority edition | Current generated surfaces have a matching immutable edition | Fresh `authority-evidence` and `harness` checks pass; `docs/evidence/current.json` selects WO-110 authority revision `001` | Resolved |
| VER-001 F3: stale feedback edition | Current actor/transport source matches the selected evidence | Fresh `feedback-evidence` passes against WO-110 revision `001`, including its retained self-host streams | Resolved |
| WO-110-D010: missing skeleton component bump | Changed component source carries a new version and matching workspace pin | Skeleton `0.32.0`, console pin `0.32.0`, matching lockfile and host version; fresh `release-surfaces` passes | Resolved |

The repairs and their reopening conditions are recorded in
[WO-110-D008–D010](../../evidence/WO-110/decisions.md#wo-110-d008--ver-001-f1-the-seven-order-owned-files-are-formatted-not-exempted).
The new authority and feedback editions leave the old evidence intact: all
24 retained files under `docs/evidence/WO-146` match the dispatch checkpoint's
blob identities. VER-001 itself is byte-identical to that checkpoint.
Superseded WO-110 editions remain under the already-recorded D009 follow-up.

**Correction to the previous verdict's explanation.** VER-001 called the
release-surface failure inherited from base. That classification is not
supported for its component-version failure. `componentVersionRules` in
`scripts/release.mjs` compares the previous release with the working tree
unless a committed revision is supplied. The pinned comparison
`git diff v0.35.1 920c5fd2d42ce8f636cc91241a3b170e89e91db6 -- packages/skeleton/src`
is empty. By contrast, checkpoint `WO-110/3` changes skeleton source while
retaining the tag's `0.31.0` version. The subject therefore needed the bump.
This independently supports D010; it does not claim to have rerun VER-001's
entire clean-clone experiment. The current complete document gate is green,
including the previously reported dependent suites.

## Acceptance criteria

### 1. Validated inspection envelope and typed unavailability — pass

**Expected:** a double endpoint returns the six-field envelope and the result
validates; an unavailable endpoint produces typed unavailability.

**Observed:** the real loopback doubles exercise request construction, response
reading, completion decoding and host validation. The envelope contains exactly
`episodeId`, `requiresHuman`, `resultId`, `status`, `summary` and `workOrderId`.
The completed result passes `parseWorkerResult` again. Connection refusal and
an explicit missing-model response produce `WorkerFailure("model-unavailable")`;
an unrelated HTTP fault produces `transport-failed`. The actor adapter retains
the result or typed failure in its checked observation, and a local completion
never becomes independent verification.

**Reproduction:**
`npm run build --silent && node --test packages/skeleton/dist/test/local-model.test.js scripts/probes/local-model-transport-smoke.test.mjs`
passed **15/15 tests**. These include foreign episode identifiers, truncation,
prose and empty content, request-kind and endpoint fences, cancellation,
cross-kind actor declarations and the declared catalog availability row.

Additional verifier-authored inline Node probes used independent loopback
servers: a response larger than 262,144 bytes produced `output-limit`; a stall
before headers and a stall after partial response content both produced
`deadline-exceeded`; a 307 redirect failed without contacting its target; and
an `evidence-worker` request was refused before network access. All passed.
Every completed failure probe left `alive()` false.

**Evidence:** `local-model-transport.ts`, `local-model-contract.ts`,
`local-model-actor.ts`, the actor catalog/contract, `worker-transport.ts`,
`worker-protocol.ts`, the two test files and this session's probe output.
`resident-host.ts` maps an unavailable adapter reason to `ActorUnavailable`;
the full gate includes the existing WO-068 resident test exercising the shipped
local-model unavailable branch with no dispatch or fallback.

**Severity:** none. **Limits:** doubles establish the adapter contract, not
model quality or a qualified runner. Cancellation evidence here establishes
client settlement; server-side inference cancellation is not requalified.

### 2. Retained live smoke with shapes only — pass

**Expected:** an operator-endpoint smoke records a validated envelope with
shapes only, or an unavailable row.

**Observed:**
[the retained 18:38 packet](../../discovery/local-model-transport-live-2026-09-20.json)
records HTTP 200 reachability, `outcome: "envelope"`, a 14,558.64 ms dispatch,
the exact six envelope fields, a 161-character summary length and one candidate
with three evidence strings. Its projection omits summary text, candidate
paths, classifications and evidence text. The earlier
[18:10 unavailable packet](../../discovery/local-model-transport-2026-09-20.json)
is preserved.

**Reproduction:** independently parsed the retained live JSON and asserted its
outcome, exact field-name list, allowed shape keys and unavailable readiness
label. The four smoke tests passed, including content exclusion and connection
refusal. Reviewed `runTransportSmoke`: it records an envelope only after the
transport validates the result. No new live model run was needed or made.

**Severity:** none. **Limits:** the retained observation is n=1 on a warm
runner and one fixture. Its runner version is `unknown` in the packet; the
dated narrative supplies installed-version provenance separately. The requested
model name does not identify the answering artifact. The model-identity and
egress limits remain recorded in WO-110-D007 with the WO-138 follow-up; `L-U1`
stays unavailable and WO-137's readiness contract remains inconclusive.

### 3. Write-backs and inherited ledger duty — pass

**Expected:** architecture Ports, the environment addendum and the inherited
ledger duty are complete.

**Observed:** product 03 Ports and Runtime primitive catalogs describe the
third transport, inspection fence, host validation, catalog NoOp and dated
limits. `docs/discovery/environment.md` contains both time-indexed smoke rows.
The work-order index explicitly substitutes the order's decisions file and
decisions-index rows for a lifecycle ledger append on this pre-2026-09-09 order.
All ten decisions are indexed, including the repair and correction.

**Reproduction:** refreshed the work-order index after dispatch; fresh
`npm run test:docs` passed publication, index, lineage, meta and the remaining
document checks. Relevant authored write-backs were read directly; generated
publication and console outputs were judged through their passing checks.

**Severity:** none. **Limits:** no new product decision or publication is
authorized by this verification; the dated readiness limits remain in force.

### 4. Product gate, whitespace and dependency constraint — pass

**Expected:** `npm test` green, `git diff --check` clean and no new dependency.

**Observed and reproduction:**

- Attended unrestricted-host `npm test`: **21 passed, 0 failed, 254.40 s,
  65 fresh tasks**. Skeleton passed in 251.53 s, including the repaired WO-143
  atomic result-publication fixture and the WO-110 tests.
- `npm run test:docs`: **19 passed, 0 failed, 19.64 s, 19 fresh tasks**.
- `git diff --check` and `git diff --cached --check`: exit 0.
- Independent parsed-lockfile comparison found the same package-key set and
  exact equality after accounting for only the skeleton version and its console
  workspace pin. Root `package.json` is unchanged. No dependency was added.

**Severity:** none. **Limits:** gate durations describe this run only. The
report and generated lifecycle projections are completed after the passing
product gate; implementation remains at the tested identity.

## Instruments, judgment and handoff

The test selector and generated harness surfaces are part of this subject.
The focused tests were also invoked directly, and the response-bound, timeout,
redirect and profile probes independently exercised the adapter. The smoke
generator is under verification; its retained packet was parsed independently.
Passing harness/evidence checks establish their declared bindings, not that
Codex automatically enforced hooks or that a local model is qualified.

Two verifier instrumentation corrections were resolved before judgment: a
checkpoint-versus-worktree `git diff` treated the still-untracked VER-001 as
deleted, so a direct saved-blob/current-file comparison established that it was
present and unchanged. Reading a large old evidence blob exceeded the child
process output buffer; per-file Git blob identity comparisons then established
all 24 files' preservation. Neither was an implementation failure.

The selected action advances the local-first, model-agnostic runtime by
independently checking a consumable inspection transport and its repair.
NoOp would leave the repaired order without a current verdict. Policy resistance
and shifting the burden favor the existing availability/NoOp contract; commons
and escalation favor one full product gate plus focused checks and retained
live evidence; drift and rule beating require current source-bound checks and
host validation; success to the successful is addressed by testing the new
adapter independently of the established CLI transports; seeking the wrong
goal is avoided by preserving the explicit qualification limits. Naive
Interventionism favors this bounded verification and unchanged implementation
over further runner or architecture changes. The observed benefit is a valid
third inspection transport with all required checks green, not a newly qualified
local model or an inferred cost reduction.

No repair finding remains for this subject. The next workflow is
`resume: final review`; this pass grants no publication authority.
