# WO-110 VER-001 — independent verification

**Verdict:** fail. The runtime acceptance behavior and the order's named
`npm test` gate pass, but three subject-caused documentation-gate defects remain:
seven order-owned files are not formatted, and both the authority and feedback
evidence checks are stale at the current subject. Repair is required. No
implementation was edited by this verifier.

**Subject:** the WO-110 working tree on branch `wo-110`, based on integrated
main `920c5fd2d42ce8f636cc91241a3b170e89e91db6`, captured by the verification
dispatch at `refs/dotln/checkpoint/WO-110/3`
(`26707d8d44e68d075f98185aa799ac5cdb553067`). The authority is
[WO-110](../../work-orders/WO-110-local-model-transport.md). This is the first
verification report for the order.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-5.6-sol","effort":"xhigh","source":"codex-session-readback"}

Human actor: the operator dispatched `resume: verify` in an attended Codex
session. The dispatch briefing supplied the actor values above. No subagents
were planned or launched; the explicit session count is zero against the cap of
20. Harness observation of descendants is unknown because no session journal
was available.

**Process cost:** unknown; cause no-session

At entry and again before report writing,
`node scripts/harness.mjs usage 01a0c027-0985-7113-b132-dac4afdfaf0d`
reported that the harness session had not begun. Total tokens, dollar cost,
source, scope and cutoff are therefore unknown; no counts are inferred. The
same command is the handoff readback.

## Findings requiring repair

### F1 — seven order-owned implementation files fail the repository formatter

**Criterion:** verification completion for criteria 3 and 4 requires the
current authored code and write-backs to pass the standing document/format
gate. **Severity:** major; repair required.

**Expected:** `node scripts/test-runner.mjs --document --only format` passes at
the current subject.

**Observed:** the suite fails and names seven files:

- `packages/skeleton/src/local-model-actor.ts`
- `packages/skeleton/src/local-model-transport.ts`
- `packages/skeleton/src/worker-protocol.ts`
- `packages/skeleton/test/fixtures/worker-lock-process.ts`
- `packages/skeleton/test/local-model.test.ts`
- `scripts/probes/local-model-transport-smoke.mjs`
- `scripts/probes/local-model-transport-smoke.test.mjs`

**Reproduction:** `node scripts/test-runner.mjs --document --only format` →
**1 passed, 1 failed, 3.23 s**. Prettier reports code-style issues in exactly
the seven paths above.

**Evidence:** fresh suite output in this verification session and the current
working-tree bytes.

**Limit:** this is mechanical formatting, not a demonstrated runtime behavior
failure. It still leaves a required current-subject check red and cannot be
waived by the green product gate.

### F2 — current generated harness surfaces invalidate authority evidence

**Criterion:** verification completion requires current generated surfaces to
preserve the repository's immutable authority-evidence editions or select a
new reviewed edition/revision. **Severity:** major; evidence integrity requires
repair.

**Expected:**
`node scripts/test-runner.mjs --document --only authority-evidence` passes
without rewriting retained evidence.

**Observed:** the check fails with `stale WO-146 revision 002 evidence:
bundle-diff.json; select a new edition or revision to preserve existing
evidence`. The changed hashes include `.claude/harness-manifest.json` and the
generated hook surfaces present in this subject.

**Reproduction:** the isolated authority-evidence suite fails **1/1** at the
WO-110 subject. A clean local clone of base
`920c5fd2d42ce8f636cc91241a3b170e89e91db6` passes authority-evidence, so the
failure is introduced by the working subject rather than inherited from main.

**Evidence:** fresh isolated-suite output, `git status --short -- .claude`, and
the clean-base comparison.

**Limit:** this finding does not say the generated hooks enforce the wrong
behavior. It says the current changed bytes are not covered by the retained
immutable authority-evidence revision.

### F3 — actor/transport changes leave feedback evidence stale

**Criterion:** verification completion requires changes inside the declared
feedback behavior source set to retain or revise its immutable evidence under
the repository's edition rules. **Severity:** major; evidence integrity
requires repair.

**Expected:**
`node scripts/test-runner.mjs --document --only feedback-evidence` passes at the
current source identity.

**Observed:** it fails with `feedback evidence is stale`. The declared source
set in `packages/skeleton/src/feedback-audit.ts` includes the WO-110-modified
`actor-catalog.ts`, `actor-contract.ts`, `worker-transport.ts` and
`worker-protocol.ts`, but no matching evidence edition/revision was selected.

**Reproduction:** the isolated feedback-evidence suite fails **1/1** at the
WO-110 subject. The same suite passes on a clean local clone of the base
revision, establishing that the stale subject is caused by the current changes.

**Evidence:** fresh isolated-suite output, `FEEDBACK_SOURCE_PATHS` in
`packages/skeleton/src/feedback-audit.ts`, the four modified source paths and
the clean-base comparison.

**Limit:** the present/removal feedback fixtures still execute; this finding is
about the required subject/evidence binding, not a claim that one of the ten
feedback mechanisms changed behavior.

The full `npm run test:docs` result is **10 passed, 9 failed, 4.32 s**. A clean
base clone reports **13 passed, 6 failed, 3.80 s**: `release-surfaces` and five
dependent suites are inherited, while F1-F3 are the three additional
current-subject failures. Repair should record the findings and any named
follow-up in `docs/evidence/WO-110/decisions.md` as required by the verifier
workflow; this read-only verifier writes only the allocated report.

## Criterion 1 — double endpoint, six-field envelope and typed unavailability

**Judgment:** pass. **Severity:** none.

**Expected:** a double endpoint returns a result whose envelope has exactly
`episodeId`, `requiresHuman`, `resultId`, `status`, `summary` and
`workOrderId`, and host validation accepts it; an unavailable endpoint yields a
typed unavailability.

**Observed:** `LocalModelWorkOrderTransport` makes one bounded request to an
explicit IPv4 loopback origin, reads the response under a 262,144-byte bound,
decodes the completion JSON and calls `parseTransportResult`, which reaches
`parseWorkerResult` for the inspection request. A refused connection yields
`WorkerFailure("model-unavailable")`. The actor adapter carries the validated
result or typed failure into a checked local observation; the shipped `L-U1`
row makes the resident record a reasoned NoOp, while a fixture-injected `ready`
row exercises the episode. A local completion is explicitly not independent
verification.

**Reproduction:** after `npm run build --silent`, direct Node test execution of
`packages/skeleton/dist/test/local-model.test.js` and
`scripts/probes/local-model-transport-smoke.test.mjs` passed **15/15**. The
cases include the exact six fields, a second host parse, connection refusal,
missing-model and unrelated HTTP errors, truncated/prose/empty/foreign results,
profile fences, cancellation, actor persistence and catalog NoOp behavior.

**Evidence:** `packages/skeleton/src/local-model-transport.ts`,
`packages/skeleton/src/local-model-contract.ts`,
`packages/skeleton/src/local-model-actor.ts`, `actor-contract.ts`,
`actor-catalog.ts`, `packages/skeleton/test/local-model.test.ts` and the smoke
test. The doubles are real loopback HTTP servers, not stubbed clients.

**Limit:** this proves the transport and validation contract against doubles;
it is not a model-quality, determinism or runner-readiness claim.

## Criterion 2 — retained live smoke is shape-only

**Judgment:** pass. **Severity:** none.

**Expected:** a live smoke against the operator's endpoint records either a
validated envelope with shapes only or an `unavailable` row.

**Observed:** the retained 18:38 UTC packet has `outcome: "envelope"`, HTTP
200 reachability and a 14,558.64 ms dispatch. Its projection contains the six
envelope field names, status, types, character/count totals and beacon claim,
but no summary, candidate path, classification or evidence text. I parsed the
packet independently and asserted the envelope outcome and exact six-name list.
The earlier 18:10 `unavailable` observation remains preserved rather than
being overwritten.

**Reproduction:** the double-backed smoke suite passed its exclusion checks for
model-authored text and its unavailable branch. A new live run was not made:
the recorded reproduction requires an operator-started and loaded endpoint,
and the accepted retained packet already captures that authorized run.

**Evidence:**
`docs/discovery/local-model-transport-live-2026-09-20.json`, the earlier
`local-model-transport-2026-09-20.json`, their narrative availability record,
and `scripts/probes/local-model-transport-smoke.mjs`.

**Limits:** the live result is n=1 on one warm runner and one fixture. The
endpoint served `required-model` while another alias was loaded, so the packet
does not establish which model answered. No attributable non-local-egress
boundary exists; `L-U1` correctly remains `unavailable`, and working inference
does not discharge WO-137's readiness contract.

## Criterion 3 — write-backs and inherited ledger duty

**Judgment:** pass. **Severity:** none.

**Expected:** product 03 Ports and runtime-catalog write-backs, the environment
addendum and the order's ledger duty all land.

**Observed:** product 03 names the third transport, its one-request boundary,
wire shape, validation, inspection-only fence, declared availability row and
model-identity limit. `docs/discovery/environment.md` carries both time-indexed
smoke rows. Because the order predates 2026-09-09, its decisions file and seven
generated decisions-index rows discharge the inherited ledger duty; the
work-order index states this substitution explicitly.

**Reproduction:** `npm run publication:check` passed with **272/272** product
headings indexed, three identical claim links per voice, and both publications
current. `npm run work-orders -- index` completed after dispatch.

**Evidence:** `docs/product/03-architecture.md` §§Runtime primitive catalogs
and Ports, `docs/discovery/environment.md`,
`docs/evidence/WO-110/decisions.md`, `docs/lineage/decisions-index.md` and
`docs/work-orders/README.md`.

**Limit:** the availability and readiness statements remain dated evidence and
carry explicit reopening conditions; they are not promoted to general runner
guarantees.

## Criterion 4 — named product gate, whitespace and dependencies

**Judgment:** the criterion's named checks pass, but F1-F3 prevent verification
completion. **Severity:** none against the three literal clauses; major findings
are recorded above against the standing current-subject evidence checks.

**Expected:** `npm test` is green, `git diff --check` is clean and no new
dependency is introduced.

**Observed and reproduction:** the attended unrestricted-host `npm test` run
passed **21 suites, 0 failed, 255.40 s, 65 fresh tasks**. The skeleton suite
passed at 252.77 s, including WO-110 and the adjacent WO-143 atomic fixture
publication. `git diff --check` and `git diff --cached --check` both exited 0.
`package.json`, `package-lock.json` and `npm-shrinkwrap.json` have no diff.
The changed WO-110 files introduce no lint/type suppression directive.

**Evidence:** the fresh gate output in this verification session, the manifest
comparison and the current working-tree diff.

**Limit:** elapsed times are observations of this run, not performance claims.

## Instruments, goal alignment and disposition

The changed `scripts/test-runner.mjs` is part of the subject and also selected
the smoke test in the full gate. I therefore ran both WO-110 test files directly
outside that selector before relying on the full product gate. The smoke
generator is likewise under verification; its projection behavior is checked
by doubles, and the retained live JSON was parsed independently. The lifecycle
and generated harness files are also in the staged subject, so their messages
were not treated as sole proof of the transport behavior.

The result advances the critical path by independently establishing that the
third transport is consumable through the existing catalog without claiming a
qualified local writer or verifier. NoOp would leave that implementation
without a verdict. Policy resistance and shifting-the-burden favor the declared
row and resident NoOp over recurring operator rescue; commons and escalation
favor the existing gate and one bounded live packet; drift and rule beating
require host validation and the fresh product gate; success-to-the-successful
is avoided by testing the HTTP adapter independently of the CLI adapters; and
seeking-the-wrong-goal is avoided by preserving the model-identity and egress
limits. Naive Interventionism favors the separate adapter and reversible row
injection over changing the CLI transports or starting the operator's runner.

F1-F3 route the order to repair. NoOp would preserve a deceptively green
runtime gate while leaving current source unformatted and two immutable
evidence bindings stale; the smallest useful intervention is the bounded repair
of those exact failures, followed by their isolated checks and the required
gate. The next legal workflow after this result is `resume: fix`; this verdict
grants no publication authority.
