# WO-090 VER-001 — independent verification

**Verdict:** fail. One finding, N1: acceptance criterion 1's first clause —
"the directed-load total per role … is lower after than before for every role" —
is not met. I reproduced the measurement independently and confirmed the totals
are exactly equal. The defect is in the order's premise, not in the
implementation: **N1 is not repairable in the subject**, and the route is an
operator amendment of criterion 1, not a `resume: fix` dispatch. Every other
clause and criterion is met.

**Subject:** the uncommitted working tree on `wo-090` at base `f24b5d72`
(HEAD is that commit; no branch commit, staged change, push, tag or Release
exists), captured by the verification dispatch at
`refs/dotln/checkpoint/WO-090/3`. The original order remains
[WO-090](../../work-orders/WO-090-shorter-cold-start.md). No implementation,
evidence or measurement file was edited by this verifier; earlier reports and
recovery refs are preserved.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"unknown","source":"operator-attested"}

The operator dispatched `resume: verify` in this session; the harness recorded
the dispatch and allocated this path, and I did not repeat the command.
`claude --version` on the operator's PATH reports `2.1.278 (Claude Code)`. The
model is `claude-opus-5[1m]`, selected by the operator with `/model` at session
start ("Opus 5 (1M context)"). Effort is `unknown`: Claude exposes no
effective-effort readback and nobody supplied a value; the order recommends
verifier xhigh+. No subagents were planned or launched — usage reports 0
observed admissions against cap 20 (`countKind: exact-observed`), with
unobserved coverage unknown.

**Process cost:** entry 82578 tokens; handoff 2903275 tokens; source claude-transcript-message-usage

Entry measured at 2026-09-20T16:37:28.369Z and handoff at 2026-09-20T16:44:11.972Z,
both `scope: dispatch`. Dollars, reasoning-output tokens and context remain
unknown (counters unavailable). This handoff figure is the pre-report
measurement; report writing and the result transition follow that cutoff and
stay in ignored receipts and the operator response.

## N1 — criterion 1, first clause: the directed-load totals are equal, not lower

**Severity:** substantive against the order as written; **not repairable in the
subject**. **Criterion:** "The directed-load total per role, measured by
WO-039's method, is lower after than before for every role."

**Expected:** a strictly lower per-role directed total after the relocation.
**Observed:** identical totals, to the byte.

I did not read this off the executor's records. I called
`measureHarnessContext()` from `scripts/harness-context.mjs` twice from session
scratch:

1. Against the current tree.
2. Against the same tree with **all twelve changed tracked files overridden to
   their `f24b5d72` bytes** (`git show f24b5d72:<path>` fed through the
   function's `overrides` map), which reconstructs the pre-edit directed load
   without touching the worktree.

Both runs return, for all eight profiles (four roles × two skill roots):

| Role          | Before (bytes / lines) | After (bytes / lines) |
| ------------- | ---------------------- | --------------------- |
| executor      | 32,855 / 300           | 32,855 / 300          |
| verifier      | 30,762 / 295           | 30,762 / 295          |
| reviewer      | 36,666 / 373           | 36,666 / 373          |
| release-close | 22,504 / 239           | 22,504 / 239          |

**Cause, independently confirmed.** I dumped the union of directed input paths
across all four roles. `docs/product/07-execution-guide.md` enters every role's
set through exactly one directive — the generated skills' Goal Alignment line —
as the `#Goal-aligned decisions` subtree: 6,205 bytes and 83 lines, before and
after, moving only from lines 39–121 to 46–128. Neither
`docs/AI-HARNESS-SECURITY.md` nor `docs/PLAYBOOK.md` appears in any role's set,
before or after. Product 07 is the only changed file that is a directed input at
all. §Model-specific notes and the Codex sandbox-approval paragraph were
therefore never in any role's directed read, so relocating them cannot move any
total. The order's Cost line — "Removes context bytes from every role's directed
reads, by the amount criterion 1 measures" — rests on a premise the measurement
falsifies.

**Why a repair cannot close it.** The only guide bytes in any directed set are
the goal card. I read §Goal-aligned decisions (lines 46–128) in full: it is the
vision and critical-path framing, the 2026-09-11 operator direction, the
correctness-over-sycophancy paragraph, the eight-lens table, Naive
Interventionism/NoOp, the 2026-09-17 platform lens and the decision-recording
rule. The generated skills carry a one-line summary that *points at* this
section; they do not carry the lenses, so retiring it would leave rules
homeless. Lowering any role's total requires shortening that text, which the
order's non-goal ("dropping any rule"), its design ("removal before pruning")
and operator-review assumption 1 ("every rule keeps a home") all forbid.
Relocating the goal card elsewhere would not help either: the read directive
names the section and would follow it. So no implementation that respects this
order can satisfy the clause.

**A vacuous reading exists and I do not rest on it.** In WO-039's method
`before` and `after` are also field names: `before` is the frozen legacy
activation read, `after` is the current tree, and the method's own `lower` flag
compares them. That flag is `true` for all eight profiles in my run (e.g.
executor 71,165 → 32,855 bytes). Read that way the clause is satisfied — but it
was already satisfied at `f24b5d72` before a single byte changed, so it would be
met by the empty change and asserts nothing about this order. The order's title,
objective and Cost line all intend the before/after of the relocation. I judge
the clause under the reading that gives it content, and record the other reading
here only because it bears on how the operator amends.

**Disposition.** The executor measured honestly, refused to manufacture a lower
number by pruning the goal card, and recorded the fact in
[WO-090-D001](../../evidence/WO-090/decisions.md#wo-090-d001--the-relocated-sections-were-never-in-any-roles-directed-set-relocate-anyway-prune-nothing-and-put-the-criterion-1-premise-to-the-operator)
with a named `followup` for the operator, harvested as `FUP-ac9c2b1271e1dc05`.
I agree with that judgment and with the refusal; the fail verdict records the
order's unmet clause, not a fault in the work. Dispatching `resume: fix` would
be futile. The operator's route is to amend criterion 1 — for example to "not
higher, with the relocated sections shown absent from every role's directed set"
— and bind it with
`npm run plan -- amend-order WO-090 WO-090-D001 "operator authorization and bounded scope"`,
after which a fresh verification can judge the amended clause against this same
evidence.

## Acceptance checks

| Criteria | Current evidence and judgment |
| --- | --- |
| 1, clause 1: lower directed total per role | **Not met.** N1 above: reproduced equal to the byte in both directions, with the structural cause confirmed from the directed-input union and the goal-card text. |
| 1, clause 2: no relocated paragraph newly directed | **Met.** Both destinations are absent from the union of directed inputs across all four roles, in both skill roots, before and after; the guide's only directed subtree is unchanged in bytes. |
| 1, clause 3: no role's cold-start bytes rise | **Met.** I re-ran `measureColdStarts` on the current tree; it equals the recorded `cold-start-after.json` exactly, and the recorded before/after files are byte-identical (4,213 bytes each). Executor 23,154, verifier 21,061, reviewer 22,279, release-close 13,967, planner 15,033, refuter 15,690, identical in both skill roots. Nothing rose. |
| 2: the table accounts for every removed paragraph | **Met.** The guide has exactly three hunks: +7 lines at §Read order, 244–256 → an 8-line pointer, and 1908–2016 → a 23-line stub. I read the removed regions at `f24b5d72`: their line ranges map one-to-one onto table rows 1–10, with row 10 kept in place ("lives here" → "lives in this repository") as stated. I then ran my own normalized sentence-home check over both removed regions against the current guide, security document, playbook, `CLAUDE.md` and the generated executor skill. 46 of 55 model-notes sentences matched verbatim; every unmatched one resolved on inspection to a splitter artifact across bullet boundaries or to a rewording the table itself declares (rows 5, 7, 8 and the retired row 1). |
| 2, row 1 (retired, the strictest case) | **Met.** All seven rules of the retired sandbox paragraph have homes: first-invocation approval, `.git`/worktree protection, the read-only command list (now including `briefing`), "never repeat a sandboxed transition", "approval covers the whole npm process", "inspect command/mapping/diff, never persist an allow rule" and the rollback pointer are in the playbook §Harness safety baseline; "neither needs Git escalation" is carried by the generated role skills' state-changing-resume line, which I read in my own loaded skill text. |
| 3: write-backs land | **Met.** 07 §Read order carries the homes and the measured fact; the security document gains §Harness version, model and effort readback and the Copilot §Selected-session readback subsection with relative links correctly rebased from `../` to the `docs/` level; the playbook gains `briefing`, the home sentence and the WO-028/WO-126 control-time paragraph. The ledger duty is substituted per the pre-2026-09-09 rule: four `WO-090-D00*` rows are in the decisions index and the work-order index marks the substitution at its line 142. `npm run publication:check`: PASS, 272/272 headings, both editions CURRENT after the two lock refreshes. |
| 4: `npm test` green, `git diff --check` clean | **Met, re-run by me.** `npm test`: **21 passed, 0 failed, 266.89 s**, 65 fresh tasks, exit 0 (external `/usr/bin/time -p real` 267.28 s; the gate row records 266,893 ms). `git diff --check` and `git diff --cached --check`: clean. |
| Anchors behind the new pointers | **Met.** I extracted all 24 links added across the changed documents and resolved each target file and heading anchor. The only two flags were false positives: four decisions-index links that my slugifier mis-slugged at the em dash (126 pre-existing generated links share the identical pattern), and the work-order index's `(pending)` link to this report, which this write resolves. |

New evidence produced in this session, beyond the criteria:

- `node scripts/harness.mjs check`: **31** generated surfaces agree.
- Consequence figures confirmed by direct count: the guide 2,016 → 1,932 lines
  and 147,292 → 141,127 bytes (−84 / −6,165); the security document 815 → 914
  lines and 52,856 → 59,277 bytes; the playbook 638 → 654 lines and 41,452 →
  42,589 bytes. Destinations gain 115 lines and 7,558 bytes against the guide's
  6,165, the difference being the new headings, pointers and provenance
  sentences. These are consequences, not the criterion's measure, and the
  evidence README presents them that way.

## Limits, instruments and disposition

- **Scope of "every role" in criterion 1.** WO-039's directed-load method
  defines four roles (`roles` in `scripts/harness-context.mjs`: executor,
  verifier, reviewer, release-close). Planner and refuter have no directed-load
  profile under it, although both appear in the cold-start measure. "Every role"
  in clause 1 is therefore judgeable only for those four. This does not change
  N1 — all four are equal — but an amendment should say which set it means.
- **One imprecision in the executor's evidence, low severity, no criterion
  affected.** The README states "every cold-start verdict stays `within` its
  ceiling". Precisely, ten rows report `within` and the two refuter rows report
  `unset`, because `docs/control/budgets.json` sets no refuter ceiling. No row
  rose, so clause 3 holds either way. Recorded as a limit, not a finding, and no
  repair is requested for it.
- **Fixture scope.** The measurement's task is the synthetic WO-999 fixture. The
  directed load a real order's cold start pulls depends on that order's own
  Cites block; the evidence README says so, and I make no broader claim. I note
  that WO-090's own Cites block includes §Model-specific notes, so under a
  self-referential measurement the relocated bytes would leave one cited input
  and enter two others — which is why the fixture, not this order's citations,
  is the right instrument, as D001's second rejected option argues.
- **Text-level check of harness claims.** The relocated Codex and Copilot
  paragraphs were verified as text arriving intact at their named homes. I ran
  no live Codex or Copilot session and re-probed no harness behavior; those
  observations remain the immutable records of WO-126, WO-132 and WO-146.
- **Instruments under description that also recorded this work.** The subject is
  documentation about this harness, and I used that harness to judge it.
  `scripts/harness-context.mjs` is both criterion 1's named method and my
  measuring instrument, so I read its source and reconstructed the "before" side
  myself rather than trusting the recorded JSON. The usage counters that produce
  the process-cost line are the WO-126 channel the relocated text describes.
  During the live `npm test` the writer/gate hook refused my read-only Bash
  calls with `DOTLN_HARNESS_REFUSED` — the same refusal the retained and
  relocated text documents; I waited for the gate rather than stopping it with
  `evidence --stop`, and used read-only tools meanwhile. None of this is
  independent proof of those mechanisms; it is disclosure that the described
  system and the observing system are the same one.
- **Goal alignment.** Mission and critical path: this order is operator-flow
  documentation hygiene, not runtime progress, and the verification's value is
  an honest measured fact rather than a cleared phase. Rule beating is the
  live trap here and the reason for the verdict: a pass on the vacuous
  `lower`-flag reading, or on the equal totals, would let the evidence pass
  without the intended behavior occurring. Drift: the explicit standard is the
  order's own criterion, and equality is recorded as equality. Shifting the
  burden: the operator receives one bounded decision — amend the clause or
  accept equality — instead of a repair cycle that cannot converge. Naive
  Interventionism: I changed no implementation to turn my own verdict green, as
  the role forbids. NoOp: passing the order would bury a falsified planning
  premise in a closed record, where the next cold-start order would inherit it.
- This verdict grants no publication, integration or release authority. The
  `v0.35.1` assignment, the roadmap activation note and the README release claim
  are recorded and consistent, and remain final review's and release close's to
  consume.

Next step is the operator's: amend criterion 1 and re-verify, or direct
otherwise. No defect of mine was encountered and deferred, so this report adds
no new follow-up decision; N1's follow-up already exists as D001's `followup`
and `FUP-ac9c2b1271e1dc05`.
