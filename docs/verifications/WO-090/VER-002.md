# WO-090 VER-002 — independent verification of the repaired subject

**Verdict:** pass. No findings. All four acceptance criteria are met at the
current subject, including criterion 1's first clause, which
[VER-001](VER-001.md) found unmet. I reproduced the consequential claims from
the instrument and the base bytes rather than from any recorded file, and I
checked VER-001's finding directly.

**Subject:** `wo-090` at HEAD `03e6f9e9` plus the uncommitted working tree,
captured by the verification dispatch at `refs/dotln/checkpoint/WO-090/7`
(`8347ed0f`). The activation base is `f24b5d72`; three local commits sit
between them (`8454883d`, `247e4c6c`, `03e6f9e9`). No push, tag, pull request
or Release exists. The original order remains
[WO-090](../../work-orders/WO-090-shorter-cold-start.md); its only change since
activation is the `v0.35.1` title assignment, so every acceptance criterion is
judged exactly as written. I edited no implementation, evidence, measurement or
earlier report; VER-001, the four original measurement records and all recovery
refs are preserved.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"unknown","source":"operator-attested"}

The operator dispatched `resume: verify` in this session; the harness recorded
the dispatch and allocated this path, and I did not repeat the command.
`claude --version` reports `2.1.278 (Claude Code)`. The model is
`claude-opus-5[1m]`, selected by the operator at session start. Effort is
`unknown`: Claude exposes no effective-effort readback and nobody supplied a
value; the order recommends verifier xhigh+. No subagents were planned or
launched — usage reports 0 observed admissions against cap 20
(`countKind: exact-observed`), with unobserved coverage unknown.

**Process cost:** entry 82123 tokens; handoff 2495584 tokens; source claude-transcript-message-usage

Entry measured at 2026-09-20T17:11:35.927Z and handoff at 2026-09-20T17:18:56.508Z,
both `scope: dispatch`. Dollars and reasoning-output tokens remain unknown
(counters unavailable). The handoff figure is the pre-report measurement;
report writing and the result transition follow that cutoff and stay in ignored
receipts and the operator response.

## VER-001's finding, rechecked

VER-001 N1 had two parts. Its **measurement** was correct and I reproduce it:
at `f24b5d72` the directed totals were equal after the relocation, because
product 07 enters every role's directed set through exactly one directive, as
the `#Goal-aligned decisions` subtree, and neither relocation destination is a
directed input at all. Its **second-order conclusion** — that no implementation
respecting this order could lower any total, so the route was an operator
amendment rather than `resume: fix` — was too broad and is falsified by the
repaired subject. The order's objective expressly includes "retiring any
paragraph a generated skill or compiled unit already carries", and the goal
card's **All phases** introduction was such a paragraph. Retiring it lowers
every measured profile while keeping every rule. The correction is recorded by
the executor in [D005](../../evidence/WO-090/decisions.md#wo-090-d005--retire-the-duplicated-goal-alignment-procedure-and-preserve-its-unique-limits),
which reopens D001 on the same day; I agree with it against my own evidence.
No criterion amendment was needed or made.

## Criterion 1, clause 1 — reproduced independently

I called `measureHarnessContext()` from the unchanged
`scripts/harness-context.mjs` twice from session scratch:

1. Against the current tree.
2. Against the same tree with **all thirteen changed tracked files that exist
   at `f24b5d72` overridden to their base bytes** (`git show f24b5d72:<path>`
   through the function's `overrides` map). The three files added since the
   base are not directed inputs of any role, so their absence cannot affect a
   total.

| Role          | Activation (bytes / lines) | Current       | Delta        | Strictly lower |
| ------------- | -------------------------- | ------------- | ------------ | -------------- |
| executor      | 32,855 / 300               | 32,603 / 299  | −252 / −1    | yes            |
| verifier      | 30,762 / 295               | 30,510 / 294  | −252 / −1    | yes            |
| reviewer      | 36,666 / 373               | 36,414 / 372  | −252 / −1    | yes            |
| release-close | 22,504 / 239               | 22,252 / 238  | −252 / −1    | yes            |

Identical in both skill roots — eight profiles, all strictly lower in bytes and
in lines. The activation column equals VER-001's equal-totals table to the
byte, so the two runs agree about the base. The reduction is entirely the guide
subtree: 6,205 bytes / 83 lines at base lines 39–121, now 5,953 bytes / 82
lines at lines 48–129. My live run is deep-equal to the recorded
[harness-context-repair.json](../../evidence/WO-090/harness-context-repair.json),
and the recorded before/after pair is byte-identical as VER-001 found.

I did not rest the verdict on the method's own frozen-legacy `lower` flag,
which was already true at `f24b5d72`; the judgment is the matched
activation-to-current comparison the order's title and Cost line intend.

## Criterion 1, clauses 2 and 3

**Clause 2 — met.** I dumped the union of directed input paths across all four
roles in both roots, before and after: 21 paths, identical lists per profile
(`samePaths: true` for all eight). Of the sixteen files changed since
`f24b5d72`, exactly one — `docs/product/07-execution-guide.md` — is a directed
input. `docs/AI-HARNESS-SECURITY.md` and `docs/PLAYBOOK.md` appear in no role's
set in either direction, so nothing relocated entered a directed read.

**Clause 3 — met.** I re-ran `measureColdStarts` on the current tree. All
twelve profiles are deep-equal to both the recorded activation file and
[cold-start-repair.json](../../evidence/WO-090/cold-start-repair.json):
executor 23,154, verifier 21,061, reviewer 22,279, release-close 13,967,
planner 15,033, refuter 15,690 bytes in each root. Nothing rose. Ten rows
report `within`; the two refuter rows report `unset`, because
`docs/control/budgets.json` sets no refuter ceiling — the wording VER-001 asked
for, now corrected in the evidence README.

## Criterion 2 — the table accounts for every removed paragraph

The relocation table carries eleven rows, row 11 being the repair's retirement.
I ran my own normalized sentence-to-home check over the three removed regions
at `f24b5d72`, against the current guide, security document, playbook,
`CLAUDE.md` and all six generated role skills:

- **Step-2 sandbox region (base lines 244–256), 8 sentences, 0 verbatim.** The
  table declares row 1 retired and reworded. I located all seven rules in the
  playbook §Harness safety baseline: first-invocation approval, the resolved
  Git-directory protection, the read-only list *including* `briefing`, "do not
  run a transition sandboxed and then retry it", the whole-`npm`-process scope
  of the approval, inspect-command/mapping/diff with no persistent allow rule,
  and the pointer to the harness guide. That paragraph now names itself the
  rule's home. "Neither needs Git escalation" is carried by the generated role
  skills' state-changing-resume line, which I read in my own loaded skill text.
- **§Model-specific notes region (base lines 1908–2012), 57 sentences, 44
  verbatim.** Every one of the 13 unmatched sentences resolves to a disposition
  the table already declares — the Copilot bold label becoming a heading plus a
  provenance sentence (row 5), rebased relative links (rows 7 and 9), or my
  splitter merging a bullet boundary — or to row 8's partial relocation, which
  I checked line by line in the playbook §Resume command surface: schema-1
  optionality, "timing cannot order events or grant a legal action", no
  recovered-time substitution, the dated discovery observation with its 120
  committer times and 15 unknowns, the public-profile sentence, and the WO-126
  usage channel with "never raw transcript text or a fabricated price" are all
  present. The status/`times` sentences the row calls already-carried are in
  the preceding playbook paragraph, including the refusal on a missing or
  mismatched local ref.
- **Goal-card All phases paragraph (base lines 56–64), 5 sentences, 1
  verbatim.** Row 11 and the repair's seven-row
  [rule-homes table](../../evidence/WO-090/repair-001.md#rule-homes) account
  for every clause, and I checked each: the material-choice/critical-path
  record, all eight traps with Naive Interventionism and NoOp, and "revisit
  changed evidence/scope; judge outcomes at handoff" are in the generated Goal
  Alignment line; evidence of benefit, intervention risk, the
  verification/handoff comparison and the legal/release-authority and NoOp
  limits are retained verbatim in the same guide section, immediately after the
  unchanged NoOp paragraph; the lens table, mission, critical path, accuracy
  rule, platform lens, decision surfaces and mechanism limits are untouched.
  For the equipping claim I checked the files rather than the prose: `Goal
  Alignment:` and `Process Cost:` each occur exactly once in all six role
  skills in **both** roots — twelve files, twenty-four lines.

No rule lost a home, so the non-goal "dropping any rule" and operator-review
assumption 1 both hold.

## Criteria 3 and 4

| Criteria | Evidence and judgment |
| --- | --- |
| 3: write-backs land | **Met.** 07 §Read order carries the homes paragraph and the measured fact, now also naming that the harness-observation moves alone left the totals unchanged. `docs/AI-HARNESS-SECURITY.md` §Harness version, model and effort readback (line 309) and §Selected-session readback, completion and counters (line 843) exist and hold the relocated text; the playbook's §Harness safety baseline (line 81) and §Resume command surface (line 533) hold theirs. Every anchor behind the new pointers resolves. Ledger duty substituted under the pre-2026-09-09 rule: six `WO-090-D00*` rows in the decisions index, marked at work-order index line 142. `npm run publication:check`: PASS, 272/272 headings, both editions CURRENT after the goal-card edit. Release write-backs carry `v0.35.1` in the order title, `README.md:96` and `06-roadmap.md:23`. |
| 4: `npm test` green, `git diff --check` clean | **Met, re-run by me.** `npm test`: **21 passed, 0 failed, 258.52 s**, 65 fresh tasks (external `/usr/bin/time -p real` 259.21 s), run outside the harness sandbox. `git diff --check` and `git diff --cached --check`: clean, exit 0, before and after the gate. |

Additional checks beyond the criteria: `npm run test:docs` **19 passed, 0
failed, 19.44 s**, 19 fresh tasks, including `plan` and
`plan-refutation-current` — the two planning checks D006's route exists to
satisfy. `node scripts/harness.mjs check`: **31** generated surfaces agree. The
D001 follow-up `FUP-ac9c2b1271e1dc05` is settled in `docs/planning/followups.json`
with a reason and a reopen condition, and D006's follow-up is open and named.

## Limits, disclosures and disposition

- **Magnitude.** Criterion 1 sets no threshold and is met, but the whole
  measured improvement is 252 bytes and one line per role, 0.7–1.1% of these
  totals, and it comes entirely from the one retired paragraph. The order's
  headline work — relocating §Model-specific notes and the sandbox paragraph —
  still contributes exactly zero to the directed totals, as VER-001 measured.
  Both the evidence README and repair-001.md say so, and neither infers a
  latency or token saving from bytes. I record it so final review judges the
  outcome, not only the clause.
- **The evidence README's criterion-1 section retains the superseded account.**
  Under `## Measurement (criterion 1)` it still reads "Lower after than before
  for every role: not met. The totals are equal." That describes the subject
  VER-001 judged, which the README's opening paragraph states explicitly and
  the relocation table's row 11 corrects, with the current measurement in
  repair-001.md. The document is accurate once read in order; I record the
  split presentation as a readability limit, not a defect, and request no
  repair for it.
- **The guide no longer enumerates the six equipped roles.** The retired
  sentence named executor/fixer, verifier, reviewer, planner, refuter and
  release-close; the pointer says only "the generated role skills". The fact is
  unchanged and I verified it directly in all twelve skill files, so no rule
  lost a home, but the enumeration is now observable only in the generated
  text.
- **Three local commits exist before final review.** `8454883d`, `247e4c6c` and
  `03e6f9e9` carry the guide edit, the decision records, the refreshed planning
  cost table and the planning refutation receipt. The executor attests explicit
  operator authorization for this exception in
  [D006](../../evidence/WO-090/decisions.md#wo-090-d006--operator-authorized-local-commits-refresh-the-planning-judgment),
  which also nominates the general executor/refutation conflict for planning. I
  can observe the authorization only as that attestation, not as the operator's
  utterance; final review integrates these commits rather than an uncommitted
  tree.
- **The planning receipt is not this verification.** The 2026-09-20 pass-scoped
  refutation records aligned-with-findings with no holds, but its own scope
  judged WO-146 and the sequence and did not independently verify WO-090. Its
  two known issues — unproven net recurring savings and the declared
  cross-session-memory limitation on verifier independence — stand.
- **Independence and instruments under description.** This is a fresh session
  with no inherited conversation, but the same harness and model line as
  VER-001 (`claude-code` 2.1.278, `claude-opus-5[1m]`); the repair ran under
  `codex-cli` 0.155.1 / `gpt-6-astra`. I re-derived the activation side from
  `f24b5d72` bytes and re-ran both measures rather than relying on VER-001 or
  on the recorded JSON, and the agreement with both is a result, not an input.
  The subject is documentation about this harness and I used that harness to
  judge it: `scripts/harness-context.mjs` is both criterion 1's named method
  and my instrument, and the usage counters behind the process-cost line are
  the WO-126 channel the relocated text describes. That is disclosure, not
  independent proof of those mechanisms.
- **Text-level scope for harness claims.** I verified the relocated Codex and
  Copilot paragraphs as text arriving intact at their named homes. I ran no
  live Codex or Copilot session and re-probed no harness behavior; those remain
  the immutable records of WO-126, WO-132 and WO-146.
- **Fixture scope.** The measurement's task is the synthetic WO-999 fixture; a
  real order's directed load depends on its own Cites block. The guide's new
  §Read order paragraph now says this, which it did not at VER-001.
- **Goal alignment.** Mission and critical path: operator-flow documentation
  hygiene, not runtime progress; the verification's value is a checked fact.
  Rule beating was the live trap and the reason I rebuilt the activation side
  myself and refused the frozen `lower` flag — the same standard that produced
  VER-001's fail, applied to a subject that now meets it. Drift: the explicit
  standard is the unamended criterion, and I report the small magnitude rather
  than letting a passed clause stand in for the promised benefit. Shifting the
  burden: the operator receives a closed criterion instead of an amendment
  request. Naive Interventionism: I changed no implementation to turn my own
  verdict green, and I preserved VER-001 and every original record. NoOp:
  failing again over the magnitude would substitute my judgment for the
  criterion the operator set.
- This verdict grants no publication, integration or release authority. The
  `v0.35.1` assignment, the roadmap activation note and the README release
  claim are recorded and consistent, and remain final review's and release
  close's to consume.

No defect of mine was encountered and deferred, so this report adds no new
follow-up decision.
