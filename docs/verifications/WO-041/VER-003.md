# VER-003 — WO-041 Plan refutation as a mechanism (re-verification after the FINAL-001 repair and the operator-authorized breakout)

**Verdict: WO-041 passes.** All three of FINAL-001's blocking findings are
resolved on the integrated tree, and the four acceptance criteria the operator's
2026-09-07 breakout added are met. The release target is retimed to `v0.13.3`
with a dated roadmap note, `@dotln/skeleton` carries `0.12.1` in its manifest and
the lockfile, the branch is fast-forwarded onto the WO-038 merge, and the feedback
evidence edition is re-recorded live on that integrated base. `npm run release --
check-surfaces` passes every rule against origin, where FINAL-001 observed two
failures. The two new executable mechanisms — the ancestral release-surface
baseline and the labeled feedback source projection — are bound by negative
evidence this report produced independently of the executor's fixtures.

The plan-refutation deliverable VER-002 passed is byte-identical here: all nine
of its modules, the receipt convention, and the live receipt pair hash the same
as at `refs/dotln/checkpoint/WO-041/8`. Criteria 1, 2, 3 and 5 are carried forward
on that identity, as FINAL-001's §Required repair step 5 directs, and §Criteria 1,
2, 3 and 5 names exactly what was re-run rather than re-derived.

- Subject work order: `docs/work-orders/WO-041-plan-refutation-mechanism.md`.
  Two changes since VER-002: the heading target `(v0.13.2)` → `(v0.13.3)`, and the
  appended §Operator-authorized breakout — 2026-09-07 with criteria 8–11. Criteria
  1–7 and every other line are unchanged. The first is FINAL-001 §Required repair
  step 2; the second rests on the [ideation receipt](../../evidence/WO-041/ideation.md).
- Subject state: the uncommitted working tree on branch `wo-041`, worktree
  `DotLn-wo041`, 2026-09-07. `HEAD` is
  `a360d04ac865e6a55c64f06cd0467f9e69acf9f7` — the WO-038 merge (#43) that the
  annotated tag `v0.13.2` names — and equals `main` and `origin/main`.
  `git log main..wo-041` is empty, so the no-worktree-commit rule still holds and
  the entire subject is the dirty tree. `git status --porcelain -uall` showed 60 entries at review
  open — 34 tracked files modified against `HEAD` (746 insertions / 172
  deletions) and 26 untracked paths — and 61 once this report exists.
- Repair episode: `RepairRequested` at 16:50:30.268Z against `FINAL-001`
  (checkpoint `04a3c72`, `refs/dotln/checkpoint/WO-041/11`); `RepairCompleted` at
  17:46:26.253Z (checkpoint `40eb9a3`, ref 12) by Codex CLI 0.153.4 /
  `gpt-6-astra` / `max`, source `operator-attested`. This verification's request
  checkpoint is `a593f7380bade517571600f8f247a649e5537dde`
  (`refs/dotln/checkpoint/WO-041/13`), recorded at 17:49:12.203Z.
- Subject integrity: comparing every path in checkpoint 12 and in the working
  tree — 573 paths — exactly three moved between `RepairCompleted` and this
  report: `docs/control/orders/WO-041.jsonl` (this dispatch's event),
  `docs/control/current.md`, and the `docs/work-orders/README.md` index I
  regenerated after dispatch. No substantive file moved. (`AGENTS.md` also
  differed under a naive content hash; it is an unmodified `120000` symlink to
  `CLAUDE.md`, identical in `git ls-files -s` and in checkpoint 12.)
- Prior reports: `VER-001` (fail), `VER-002` (pass), `FINAL-001` (fail). All three
  are byte-identical to their verdict checkpoints and untouched by this repair;
  §Historical evidence gives the hashes.
- Implementer and verifier remain structurally separate: Codex CLI /
  `gpt-6-astra` wrote the implementation, both repairs, and the breakout code;
  this verification is Claude Code / Opus 5 and wrote no subject file.

## Actor

Verification ran on the Claude Code CLI, version `2.1.263` (`claude --version`
observed in this session; that value is on this harness's observed `versions`
list in `docs/discovery/environment.json`), model `claude-opus-5[1m]` (Opus 5, 1M
context), at reasoning effort `max` selected by this session's model control. The
value is self-reported: `max` is on this harness's `sessionEffortSelector.values`,
whose `effectiveEffortReadback` for `claude-code` is `not found` with
`harnessReadbackEligible: false`, so `harness-readback` is unavailable and is not
claimed. `CLAUDE_EFFORT=max` is visible in the shell as a launch selection, not a
recorded readback. The work order declares `verifier xhigh+`; `max` meets it and
the projection reports `Effort drift: none`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.263","model":"claude-opus-5[1m]","effort":"max","source":"self-reported"}

This is the same harness and model that recorded VER-001 and VER-002. That is
disclosed, not concealed: it is a repeated verifier, not a self-verification,
because the implementer and both repairers are the Codex executor recorded in the
control segment. FINAL-001 adjudicated the same fact for its own subject. Nothing
in this report relies on a VER-001 or VER-002 conclusion without either re-running
the check or proving the bytes identical.

## Self-referential instruments, disclosed

1. `scripts/resume.mjs` allocated this report and will record its verdict. WO-041
   changes one line of it — `parseActor` becomes an export — read here and inert
   for the CLI path; the file is byte-identical to VER-002's subject.
2. `scripts/work-orders.mjs index` was regenerated after dispatch and will be
   again after the verdict; `index --check` is inside the gate this report runs.
3. `scripts/release.mjs` `check-surfaces --local` is both a WO-041 deliverable and
   the first step of the `npm test` this report relies on. §Criterion 9 does not
   accept that self-report: it defeats the new filter and shows the fixture
   failing.
4. `packages/skeleton/src/feedback-audit.ts` is both the changed instrument and a
   pinned input to its own subject. §Criterion 10 replicates its claims on a
   fixture this report wrote, not the executor's.
5. The plan refuter judged WO-041 `thesis-advancing`; that verdict is advisory
   under the work order's criterion 4 and is used nowhere here.

## What the repair changed

Against `HEAD` the repair is 34 modified tracked files and 26 untracked paths.
Classified:

- **FINAL-001 finding 1** — the work-order heading, the README release block, and
  a dated roadmap retiming note.
- **FINAL-001 finding 2** — `packages/skeleton/package.json` and the lockfile's
  `packages/skeleton` entry, plus the roadmap, evidence README and ledger
  sentences that said component versions were unchanged.
- **FINAL-001 finding 3** — the fast-forward onto `a360d04`, the four regenerated
  projections, the three authored conflict resolutions, and a re-recorded live
  feedback edition under `docs/evidence/WO-041/feedback/`.
- **Breakout criterion 8** — 07 §Independent workflows and integration and the
  §Discipline rewrite; PLAYBOOK; the concurrent plan's lane rules and comparison
  table; the phase-two plan's procedure, trial status and first-pair receipt; the
  map's operator correction; WO-033's sync-helper scope; the ledger section.
- **Breakout criterion 9** — `scripts/lib/release-preparation.mjs` (new, 168
  lines), `release prepare` and `check-surfaces --local` in `scripts/release.mjs`,
  `scripts/test-release-preparation.mjs` (new), two new fixtures and one new
  negative assertion in `scripts/test-release.sh`, and both new gate steps in
  `package.json`.
- **Breakout criterion 10** — `feedbackSourceFile` in
  `packages/skeleton/src/feedback-audit.ts`,
  `packages/skeleton/test/feedback-audit-source.test.ts` (new, and pinned into
  `FEEDBACK_SOURCE_PATHS`), 02 §WO-041 integration correction, and the skeleton
  README's runbook paragraphs.

`packages/kernel`, `packages/compiler`, and the dependency set have no diff
against `HEAD`; the only lockfile change is the skeleton's `0.12.0` → `0.12.1`.

## FINAL-001 finding 1 — release target and README block: **met**

`head -1` of the work order now reads `… on the vision (v0.13.3)`; at
checkpoint 8 it read `(v0.13.2)`. The README release block carries exactly one
strict version, `v0.13.3`, and describes "a control-plane patch above the
published license-posture release" — the two-strict-version defect is gone. The
roadmap keeps the original activation completion verbatim and adds beneath it a
dated note naming the superseded target, the successor, the classification, and
the reason, which is what 06 §Release boundary requires.

`npm run release --silent -- check-surfaces` (origin observation) now reports
`PASS release-block: observed v0.13.3; expected v0.13.3 (work-order target
v0.13.3; latest published v0.13.2)`, where FINAL-001 recorded a `FAIL`.

The retime was produced by the new command rather than by hand, and the command's
arithmetic is checkable: `planReleasePreparation` reads the single declared
`**Release classification:** patch`, increments the patch axis of the latest
observed tag `v0.13.2` and zeroes nothing below it, giving `v0.13.3`. It refuses
unless the heading holds exactly one strict version and the README block holds
exactly one that equals it. `strictVersionsIn` returns every match rather than a
unique set, so a block containing the same version twice raises the count to two
and refuses instead of silently rewriting only the first occurrence.

## FINAL-001 finding 2 — component version: **met**

`packages/skeleton/package.json` is `0.12.1` and `package-lock.json`'s
`packages/skeleton` entry matches. `check-surfaces` reports `PASS
component-version @dotln/skeleton: src changed; observed 0.12.1; previous v0.13.2
0.12.0; expected a different version`, and compiler `0.6.0` / kernel `0.2.1` pass
as unchanged. FINAL-001's observation 2 is discharged too: the roadmap's
activation completion now reads "Skeleton component `0.12.1` carries the source
changes", the evidence README's opening says "prepares application `v0.13.3` with
skeleton `0.12.1`", and the ledger's first-pair entry states the bump and marks
the earlier framing superseded. No true-when-written sentence is left beside a
contradicting manifest.

## FINAL-001 finding 3 — base sync and re-recorded evidence: **met**

`wo-041` is `a360d04`, equal to `main` and `origin/main`; `git log main..wo-041`
is empty, so the branch was fast-forwarded, not merged with a work-order commit.
The four generated conflicts were regenerated rather than hand-merged: both
edition locks are `CURRENT` (30 and 45 linked sections), `index --check` is green
inside `npm test`, and `docs/control/current.md` agrees with the fold — `status`
issued no projection warning.

The feedback edition is genuinely fresh and genuinely on the integrated base. The
verifier capsule's `subject.baseCommit` is
`a360d04ac865e6a55c64f06cd0467f9e69acf9f7`; its records span
2026-09-07T17:15:41.916Z to 17:16:04.492Z, inside the repair window that opened at
16:50:30.268Z and closed at 17:46:26.253Z. `feedback.json` pins subject
`sha256:bc7e948d…`, and `feedback-evidence.mjs --check --edition WO-041` is green
in the gate on this tree.

FINAL-001's observation 1 (one edition per order) was answered by its first honest
route, and the supersession is verifiable rather than asserted. The preserved
stash `827132315b7d5d8664b0fa9f15c10545b011906e` exists as `stash@{0}` with the
message the evidence README names; its untracked-tree parent `0972c6e` holds the
old `docs/evidence/WO-041/feedback/feedback.json`, whose recorded subject is
`sha256:f0df21329af323d93b3c9f5eb3a94ef19fca4e1ac0543092ff590972f6d5988d` —
byte-for-byte the hash FINAL-001 recorded for the edition it read. The superseded
edition was never committed and remains recoverable; no published edition and no
numbered report was altered.

## Criterion 8 — independent progress and evidence-scoped integration: **met**

The contract lands in 07 §Independent workflows and integration and is echoed,
without contradiction, in `docs/PLAYBOOK.md`, the concurrent plan (lane rules 3–5
and the admission-control row), the phase-two plan, the work-order map, and
WO-033's sync-helper scope. Read end to end against the ideation receipt, each of
the operator's three instructions is present and none is exceeded:

- No sibling-phase prerequisite is introduced anywhere. The concurrent plan's
  waiting-order verification reserve and the playbook's "do not start a third
  implementation while two orders wait" are both struck and replaced with
  available actors plus one writer per worktree.
- The voluntary final-review-through-release-close serialization is preserved and
  is labeled discipline rather than a gate in all five surfaces; a gate is named
  as an open option, not implemented.
- The integration/acceptance line is drawn the same way each time, and the
  implementer ≠ verifier rule survives inside it: "A reviewer never writes a
  behavioral fix and certifies it," with repair plus fresh independent
  verification retained for behavior, contracts, acceptance, schema,
  compatibility and authority.

"Historical evidence stays immutable" holds structurally, not just in prose —
see §Historical evidence. "Without changing the marked sequence" holds exactly:
the map's `dotln-work-order-sequence` block hashes
`sha256:22be356dd989a3279de0e289fd10c888eeef82371b1e8f778fc83cc9868e9ac9` at
checkpoint 8, at `HEAD`, and in the working tree.

## Criterion 9 — release preparation and ancestral release surfaces: **met**

The seven `test-release-preparation.mjs` cases cover every fixture the criterion
names: ordinary retiming of exactly three surfaces with planning proven not to
write; idempotence (a second `plan()` returns no edits, and the CLI prints "no
files changed"); classification-following for minor and major and skipping an
already-available target; refusal of malformed and ambiguous inputs with the
snapshot unchanged after every throw; prerelease examples preserved; wrong
worktree, escaped source and stale-plan refusals; and byte recovery after an
injected write failure. `applyReleasePreparation` re-reads each file and refuses
if it changed since planning, then restores in reverse on any write error,
including the file being written when it failed.

"Preserves unrelated files and all control events" and "operation without
sibling-phase inspection" are proven by the `prepare_independent` real-Git
fixture: it plants three independent control segments — WO-099 open on the
checked-out branch, WO-100 open, WO-101 folded to closed — then asserts `diff -r`
over `docs/control` and `cmp` over `git for-each-ref` after the retime. The
command reads only the branch-selected order's state and appends nothing.

The ancestry claim is the one I would not take on the executor's word, because the
gate that would catch a regression is the same code under test. I defeated it: with
`(options.localOnly ? ancestralLocalTags(root) : localTags(root))` at
`scripts/release.mjs:552` replaced by `localTags(root)` on both arms and nothing
else changed, `bash scripts/test-release.sh` exits 1 at the `surfaces_local_snapshot`
fixture with

> `FAIL release-block: observed v0.2.1; expected exactly one v0.2.2 (work-order target v0.2.1; latest local tag v0.2.2)`

— the sibling's non-ancestor `v0.2.2` becoming the verification baseline, which is
exactly the failure mode the criterion forbids. With the filter restored the suite
passes. The mutation was reverted immediately and `scripts/release.mjs` is
byte-identical to its pre-mutation state (`b15dbd24756fa674b59263ab93277787c28869d0`
before and after); §Disclosures records the episode.

The converse assertion is already negative by construction: `assert_surface_failure
'FAIL component-version @dotln/kernel: … expected a different version' --local`
requires the `--local` check to keep failing a missing component bump against its
own ancestral baseline. Publication is unaffected — `checkSurfaces` at
`scripts/release.mjs:1532`, the release-close and `worktree publish` path, passes
explicit origin-fetched `local`/`remote` maps and never `localOnly`, and the
`--local` CLI prints "publication checks origin" on every run.

## Criterion 10 — feedback source projection: **met**

`feedbackSourceFile` rewrites only `package-lock.json` and
`packages/skeleton/package.json` into a capsule at a logical `.feedback-source/`
path carrying `projection: "feedback-package-projection-v1"`, the physical
`sourcePath`, and an explicit `omitted` pointer list. It deletes `version` and
`license` at the manifest root, at the lockfile root, and in the four known
workspace entries, and nothing else; malformed input throws rather than degrading.
The label reaches the verifier capsule: `selfhost-verification.jsonl`'s
`VerificationOpened` payload carries both projected files under `subject.files`
with their `projection`, `sourcePath` and `omitted` fields intact, so a reader
cannot mistake them for raw manifests.

Rather than re-run the executor's fixture, I replicated the criterion on a fixture
I wrote, driving the built `readFeedbackSource` over a synthetic repository shaped
like the real collision. All fourteen claims hold:

| Mutation                                                                                        | Required | Observed |
| ----------------------------------------------------------------------------------------------- | -------- | -------- |
| WO-038's license fields plus skeleton `0.12.0`→`0.12.1` across manifest, lockfile root and all four workspace entries | stable   | stable   |
| lockfile whitespace reformat only                                                                | stable   | stable   |
| `node_modules/typescript` `integrity`                                                            | invalid  | invalid  |
| `node_modules/typescript` `version`                                                              | invalid  | invalid  |
| root workspace `devDependencies` selection                                                       | invalid  | invalid  |
| manifest `scripts.prepublishOnly`                                                                | invalid  | invalid  |
| manifest `dependencies` constraint                                                               | invalid  | invalid  |
| manifest `private`                                                                               | invalid  | invalid  |
| each of `reactor.ts`, `worker-transport.ts`, `verification-protocol.ts`, `worker-store.ts`, `feedback-audit.ts` | invalid  | invalid  |
| `test/feedback-audit-source.test.ts` (the projection's own fixture pin)                          | invalid  | invalid  |

The first row is the criterion's substance: the exact upstream change that forced
this repair no longer moves the feedback subject, while every behavioral and
dependency input still does. The executor's own suite adds four more cases —
lockfile version, `exports`, an unknown `extension` field, a bumped
`lockfileVersion`, and malformed-projection refusals — and passes inside `npm
test`. The live run required by the criterion is the one recorded in finding 3.

## Criterion 11 — the full gate on the repaired subject: **met**

Run on this tree, all read-only or fixture-scoped:

- `npm test`: **exit 0**, zero `not ok` lines. Group totals: 7 release-preparation,
  9 license-surface, 285 runtime package, 8 identity-corpus, 21 mutation
  self-tests. It opens with `check-surfaces --local` (`Tag observation: local
  ancestors of HEAD only; publication checks origin.`, then `PASS release-block
  … latest local tag v0.13.2` and the three component rules), and includes
  prettier, the shell suites, publication, `index --check`, `tsc -b --force`, all
  eleven plan-refutation fixture groups, and the artifact, verification and
  feedback evidence checks against the WO-041 edition.
- `npm run release --silent -- check-surfaces` (origin): **exit 0**; 33 rules
  reported, 0 FAIL, including both rules FINAL-001 recorded as FAIL.
- `npm run plan --silent -- check`: **exit 0**,
  `{"receipts":1,"passes":0,"enforcement":"pending introduction commit","localTerms":"unavailable"}`.
- `node scripts/check-publication.mjs`: **exit 0**, 235/235 product headings
  indexed, 3 dual-voice claim links per voice, both audience locks `CURRENT`.
- `git diff --check` and `git diff --cached --check`: **clean**.

The criterion's second half — that the sync record, first-pair observation and
breakout evidence identify what was measured and what remains manual — holds, and
the numbers are checkable. The phase-two plan's first-pair receipt quotes five
elapsed spans; I recomputed each from the control segment's timestamps and all
five match to the millisecond (implementation 36,409,941; VER-002 verification
1,109,388; the VER-001 repair 1,344,824, correctly the latest *completed* repair
at the receipt's 17:16:36Z observation; FINAL-001 557,663 — the receipt's "9
minutes 17.663 seconds"; and the FINAL-001 repair 3,355,985). The receipt is also
honest about its limits: "There is no demonstrated time saving from this pair,"
wall-clock spans include waiting, and overlapping spans are not summed. FINAL-001
observation 7 is discharged.

## Criteria 1, 2, 3 and 5 — carried forward on identical bytes

FINAL-001 §Required repair step 5 asks that these be left as VER-002 left them
"unless the sync touched their files." The sync touched none of them. Every module
these criteria rest on hashes identically at `refs/dotln/checkpoint/WO-041/8` and
in the working tree: `scripts/lib/plan-subject.mjs`, `scripts/lib/plan-receipts.mjs`,
`scripts/lib/terms.mjs`, `scripts/refute-plan.mjs`, `scripts/test-plan-refutation.mjs`,
`packages/skeleton/src/loadouts/plan-refuter.ts`, `plan-refutation-protocol.ts`,
`plan-refutation-host.ts`, `plan-refutation-fake.ts`, and
`docs/planning/refutations/README.md`. The five shared surfaces VER-001 adjudicated
— `reactor.ts`, `verification-protocol.ts`, `worker-store.ts`, `worker-transport.ts`
and `scripts/resume.mjs` — are likewise unchanged, as is `scripts/feedback-evidence.mjs`.

The behavior was still re-run rather than assumed: all eleven plan-refutation
fixture groups execute inside this tree's `npm test` on the integrated base, and
their labels in the log cover AC1's five stale-source families, AC2's authority
refusals and pinned questions, AC3's closed schema and local-terms screen, AC5's
override, disposition-chain, re-roll and three-hold-stop cases, and the
forward-only gate. VER-001 and VER-002 established the binding power of the two
repair groups by module swap; that evidence stands on identical bytes and is not
repeated here.

## Criteria 4, 6 and 7 — re-derived on the integrated tree

**Criterion 4.** The live receipt pair is byte-identical to checkpoint 8. Its
recorded revision `01c2e321268bf9cb9842b58b3e95fc8ed5a09d67` is still an ancestor
of `HEAD` (`git merge-base --is-ancestor` succeeds), so the subject remains
rebuildable after integration, and `plan check` validates the pair end to end on
this tree. The header records transport `claude-cli-print`, harness version
`2.1.263`, model `claude-fable-5-1`, effort `max`, `pass`, zero holds, and
`selectionSource: host-launch` with `effectiveModel`/`effectiveEffort` honestly
`unknown`. The self-referential disclosure is in the rendered receipt and the
verdict on WO-041 is treated as advisory throughout this report.

**Criterion 6.** All eight write-backs land and say something true about the
mechanism on this tree: 07 §Operator-opened planning pass (the mandatory step, the
hold/override rules, the forward-only boundary, and the receipt pair as a standard
artifact); the 13 tester paragraph and the roles table's showrunner and tester
rows; `docs/README.md`'s map lines for `refutations/` and the separate
`plan-refutations.jsonl`; the README "What runs today" block; the dated
`control.plan-refutation` capability row; the publication index rows plus both
refreshed edition locks; and the ledger's two 2026-09-07 sections. FINAL-001's
observation 3 is discharged — the capability row now reads "**1 — demonstrable**,
original mechanism passed VER-002; integrated repair pending review" instead of
the stale "staged pending independent verification", and names what would promote
it. Two further write-backs belong to the breakout: 02 §WO-041 integration
correction and 07 §Independent workflows and integration, both indexed and locked.

**Criterion 7.** `npm test` green and `git diff --check` clean, both above. No new
dependency: `package.json`'s `devDependencies` are untouched and the only
`package-lock.json` change is the skeleton version. Kernel unchanged:
`git diff HEAD --stat -- packages/kernel packages/compiler` is empty, and
`check-surfaces` confirms both components' `src` unchanged at their recorded
versions.

## Historical evidence

Immutability is verified, not assumed. `docs/verifications/WO-041/VER-001.md` and
`VER-002.md` hash identically at checkpoint 8 and now.
`docs/final-reviews/WO-041/FINAL-001.md` hashes `47ec48a9…` at its own verdict
checkpoint 10, at the `RepairRequested` checkpoint 11, at the `RepairCompleted`
checkpoint 12, and in the working tree. `docs/evidence/WO-041/fixtures.txt` and
`inspection-continuity.json` are unchanged. The six pre-mechanism manual receipts
under `docs/planning/refutations/` have no diff against `HEAD`.

## Clean-room screen

A token screen over all 60 changed and new paths present at review open for commercial ticketing/ALM
product names, predecessor-system names, managed-host, corporate gateway, proxy
and internal-hostname shapes, private address ranges, and credential shapes
returned nothing. I additionally read in full the two surfaces that are model
output landing in committed files — `docs/evidence/WO-041/README.md` and
`docs/evidence/WO-041/ideation.md` — plus the live receipt's free text; all three
use only this repository's own vocabulary (orders, theses, exclusions, roles,
rows, transports) and the generic equivalents the repository already standardizes.
The ideation receipt states its own screen, names the capture's SHA-256 in ignored
intake without reproducing it, and records that no stop condition was found.
`docs/intake` was not read.

## Non-blocking observations

1. **`prepare --local` and `check-surfaces --local` observe different tag sets.**
   `prepare` uses the whole local snapshot; `check-surfaces` uses only ancestors of
   `HEAD`. This is correct — a retime must avoid the entire shared tag namespace,
   while a verification baseline must not inherit a sibling's history — and 07
   describes them separately. It reads as an inconsistency at a glance; a future
   reader benefits from the reason being stated where the code is, not only in 07.
2. **The projection's workspace list is fixed.** `feedbackSourceFile` strips
   `version`/`license` from `""`, `packages/kernel`, `packages/compiler` and
   `packages/skeleton` only. A fourth workspace's release metadata would stay in
   the subject. This fails safe — it over-invalidates, never under-invalidates —
   but it will surprise whoever adds a package.
3. **A lightweight ancestral release tag would throw rather than FAIL.**
   `publishedTag` is called outside `componentVersionRules`' try/catch, so a
   non-annotated `vX.Y.Z` tag reachable from `HEAD` would abort `check-surfaces`
   with an exception instead of a report line — and `--local` now puts that path
   inside `npm test` for every checkout. The behavior predates this order on the
   origin path; the change widens where it can be reached.
4. **VER-001 O4 stands and this repair enlarged its reach.** Once a planning pass
   dated after the introduction commit exists, edits to the capability table, a
   sequence order file, a thesis section or the judged roles table turn `npm test`
   red until a fresh refutation runs. This repair edits the capability table, the
   roles table, and both the WO-033 and WO-041 order files. All are inert today
   (`enforcement: pending introduction commit`); the operator should meet this
   deliberately at the next planning pass.
5. **FINAL-001's observations 5, 6 and 8 are untouched and remain non-blocking:**
   same-day planning-pass resolution, the `.prettierignore` asymmetry that leaves
   only the first manual redirect receipt formatter-owned while its five numbered
   siblings and the mechanized receipts are ignored, and `gh` configuration inside
   the Claude Code sandbox.
6. **Ledger spacing.** The new 2026-09-07 sections carry two- and three-line blank
   gaps. `docs/lineage/idea-ledger.md` is in `.prettierignore` as an append-only
   record, so this is cosmetic and the formatter is not wrong.

## Disclosures

1. **A source file was temporarily mutated and restored.** To establish
   §Criterion 9's binding I replaced one expression in `scripts/release.mjs`, ran
   `scripts/test-release.sh`, and restored the file from a copy under `$TMPDIR`.
   `git hash-object` is `b15dbd24756fa674b59263ab93277787c28869d0` before and
   after; `git status --porcelain -uall` is 60 entries before and after, and
   `git diff --check` is clean. The mutant suite's fixture repositories were
   created and removed under `$TMPDIR` by the suite's own temp-root traps. No
   other subject file was written by this session.
2. **No destructive Git command ran.** No `reset`, `checkout .`, `clean`, `stash`
   push, `pop` or `drop`. The preserved stash was inspected read-only through
   `git cat-file`, `git rev-list --parents` and `git ls-tree`; `stash@{0}` is
   still present.
3. **Self-referential instruments** are listed in §Self-referential instruments.
4. **Effort evidence limit.** `CLAUDE_EFFORT=max` is a launch selection visible to
   the process; no effective-session readback exists for this harness, so the
   attestation is `self-reported`.
5. **What I did not re-derive.** The module-swap binding for VER-002's two repair
   groups, and VER-001's throwaway-repository reproduction of the five
   subject-hash families. Both rest on files proven byte-identical here, and the
   fixtures that encode them run green in this tree's gate.
6. **Repeated verifier.** Same harness and model as VER-001 and VER-002; see
   §Actor.

## Method

Commands run, in order: `npm run resume --silent -- status --json`;
`npm run resume -- verify`; `npm run work-orders -- index`; `npm test` (full gate,
log under `$TMPDIR`); `npm run release --silent -- check-surfaces`;
`npm run plan --silent -- check`; `node scripts/check-publication.mjs`;
`git diff --check`, `git diff --cached --check`; `git diff HEAD --stat` and
`git diff HEAD -- <each changed file>`; `git hash-object` comparisons against
`refs/dotln/checkpoint/WO-041/8`, `/10`, `/11` and `/12` across 573 paths;
`git merge-base --is-ancestor`; `git cat-file`, `git rev-list --parents`,
`git ls-tree` and `git stash list` over the preserved stash; the sequence-block
digest at three revisions; the elapsed-span recomputation; an independent
`readFeedbackSource` probe over a synthetic repository under `$TMPDIR`; and the
`scripts/release.mjs` mutation run of `scripts/test-release.sh` described in
§Disclosures.

Files read in full: the work order, `VER-001`, `VER-002`, `FINAL-001`, the control
segment, the executor evidence README, `ideation.md`, `integration-checks.txt`,
`scripts/lib/release-preparation.mjs`, `scripts/test-release-preparation.mjs`,
`packages/skeleton/test/feedback-audit-source.test.ts`, the `scripts/release.mjs`,
`packages/skeleton/src/feedback-audit.ts`, `scripts/test-release.sh`,
`package.json`, `.prettierignore`, `scripts/feedback-evidence.mjs`,
`scripts/resume.mjs` and `packages/skeleton/README.md` diffs, every documentation
diff, the live receipt JSON header and the feedback capsule's `VerificationOpened`
payload, `FEEDBACK_SOURCE_PATHS`, `componentVersionRules`, `publishedTag`,
`releaseBlockRule`, `strictVersionsIn`, and the relevant sections of 07, the
playbook, the phase-two plan, the concurrent plan, the map and WO-033.
