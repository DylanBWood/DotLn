# WO-012 verification report VER-001

**Subject:** branch `wo-012`, uncommitted working tree, 2026-09-01. Base
`49a3841` (`origin/main`); subject diff before this report: 204 insertions and
39 deletions across 10 files.
**Authority:** `docs/work-orders/WO-012-release-gate-path-quoting.md`, including
its ideation-breakout receipt.
**Prior reports:** none.
**Verifier:** Codex, GPT-5 family; reasoning-effort setting was not surfaced to
this session. WO-012 permits any capable model.

**Verdict: WO-012 passes.** All six acceptance criteria pass, the full evidence
gate is green, the regression fixtures fail against the pre-fix scripts, and
the required documentation and ideation write-backs are complete. There are no
blocking or non-blocking findings.

## Dispatch and subject integrity

`npm run resume -- verify` allocated this exact report path and advanced the
control state to `verifying`. The transition could not create its optional
recovery checkpoint because this environment exposes `.git` read-only; it
failed loudly, recorded `checkpointUnavailable`, and proceeded as designed.
No older checkpoint was used.

The subject diff hashed
`7a8adae99db0f2210b9f0653c8b62415ca773ef79d952ff03d1ddd75320073cb`
with `git diff --binary | shasum -a 256` immediately before and after the full
gate. The verifier made no lasting change to the implementation or its
documentation before writing this report. During review, the verifier briefly
paraphrased the quoted operator term `"fix in place"`; the operator clarified
that the exact quotation was deliberate, and the edit was fully restored. The
subject returned to the same diff hash shown above.

## Evidence gate

`npm test` from the repository root exited 0 on the final subject:

```text
backup-intake tests passed
resume tests passed
checkpoint tests passed
worktree tests passed
release tests passed
# tests 68
# pass 68
# fail 0
```

Additional checks all exited 0:

```text
node --check scripts/release.mjs
node --check scripts/worktree.mjs
bash -n scripts/test-release.sh
bash -n scripts/test-worktree.sh
git diff --check
```

For mutation sensitivity, the current `test-release.sh` and
`test-worktree.sh` were run against copies of the corresponding scripts from
base `49a3841`. Both exited 1. The new U+202F fixtures therefore distinguish
the repair from the pre-fix behavior rather than passing without exercising
it. All copied repositories and probes lived under isolated `/private/tmp`
directories and were removed afterward.

## Required before/after classification capture

A read-only probe with `core.quotePath=true` reproduced the reported pathname
classification against both Git encodings. U+202F was independently confirmed
as UTF-8 bytes `e2 80 af`:

```text
before_candidate="docs/intake/images/Screenshot 2026-08-30 at 10.01.24\342\200\257PM.jpg"
before_allowed=false

after_candidate=docs/intake/images/Screenshot 2026-08-30 at 10.01.24 PM.jpg
after_candidate_hex_suffix=3234e280af504d2e6a7067
after_allowed=true
```

The before arm uses the former line-delimited reading: Git wraps the path in a
literal quote, so the intake-prefix predicate fails. The after arm uses the
new NUL-delimited reading: the literal path begins with `docs/intake/`, and the
real U+202F byte sequence survives. `test-release.sh` then proves that release
close advances past this guard to the lower-version disposition while leaving
the protected intake file present.

## Acceptance criteria

| # | Verdict | Evidence |
|---|---|---|
| 1 | **pass** | `scripts/test-release.sh:169-181` creates the screenshot-style U+202F intake path, reaches the post-guard lower-version result, and proves the file remains. The independent capture above reproduces the original false classification and repaired classification. |
| 2 | **pass** | `scripts/test-release.sh:139-145` creates an ignored non-ASCII dotenv path, requires refusal, compares the entire message to the literal unquoted path, proves the file remains, and proves no candidate tag exists. |
| 3 | **pass** | `scripts/test-worktree.sh:106-135` requires exact literal refusal and preservation for a U+202F intake note, then removes a worktree whose U+202F paths cover `node_modules`, `dist`, `.DS_Store`, and `*.tsbuildinfo`. |
| 4 | **pass** | `scripts/test-release.sh:226-282` proves a tracked U+202F path and a leading-space path appear literally in `changedFiles`, still trigger the anchored workflow note, survive tag extraction, and pass `release validate`. Lines 303-321 separately prove the first-release `ls-tree` arm. |
| 5 | **pass** | Both shell suites construct U+202F with octal UTF-8 bytes, require hex `e280af`, and call the byte assertion for every new non-ASCII fixture. |
| 6 | **pass** | `npm test` is green. The exact screenshot-style failure now reaches the allowed branch, while foreign ignored material still refuses closed. |

## Implementation audit

- `scripts/release.mjs:31-36` and `scripts/worktree.mjs:13-18` add dedicated
  path-list readers that never trim, require a trailing NUL, remove exactly
  that delimiter, and split only on `\0`.
- `ensureNoIgnoredInfluence` uses `git ls-files -z` at
  `scripts/release.mjs:42-45`; `ensureNoIgnoredMaterial` does the same at
  `scripts/worktree.mjs:26-29`. Their existing predicates and refusal policy
  are unchanged and now receive literal paths.
- `changedFiles` uses `-z` for both `git diff --name-only` and
  `git ls-tree --name-only` at `scripts/release.mjs:195-198`. The resulting
  literal paths flow into the manifest and anchored `criticalNotes` checks.
- `ensureClean` in both scripts is unchanged. It asks only whether porcelain
  output is empty, so pathname quoting cannot invert its decision.
- No dependency, release authority, cleanup classification, tag-targeting
  behavior, or other non-goal changed.

The changed lifecycle scripts were used by their own fixture suites, so this
verification does not trust their successful execution by itself. Source
inspection, the independent Git-output capture, and the pre-fix mutation runs
provide separate evidence for the behavior under judgment.

## Documentation and ideation review

- `docs/product/06-roadmap.md:457-473` names WO-004 and WO-012 at `v0.2.1`,
  records the dated U+202F deferral, states that no tag was created, and keeps
  the release obligation open through WO-012.
- `docs/product/07-execution-guide.md:162-178` now accurately says the helper
  tags synchronized `origin/main` HEAD, including any later contained commits;
  the tagging implementation did not change.
- `docs/lineage/idea-ledger.md:26-38` records the general NUL-delimited pathname
  lesson. Lines 40-59 preserve the release-close recovery idea while clearly
  separating WO-012's current manual orchestration from future executable
  capability.
- The breakout receipt is complete and traceable. Its raw input is
  operator-authored and contains no employer material, internal identifier,
  proprietary API shape, or managed-host detail. The operator explicitly
  confirmed during verification that the quoted phrase `"fix in place"` is
  intentional product wording and must remain verbatim. The surrounding
  roadmap and ledger text synthesizes its meaning, authority limits, and
  unresolved design choices without expanding WO-012's executable scope or
  acceptance criteria.

## Deviations and open questions

- Deviation: the verification-request checkpoint is unavailable because the
  sandbox denied `.git` writes; the control log records this honestly.
- Open questions: none within WO-012.
