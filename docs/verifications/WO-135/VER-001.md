# WO-135 VER-001 — independent verification of the three planning-gate corrections, their fixtures and the write-backs

**Verdict: pass.** All six acceptance criteria are met on the current bytes.
Criteria 1, 2 and 3 are reproduced by my own probes against the real receipts,
the real capability table and the real sequence revisions — not read from the
executor's account and not taken from the shipped fixtures: the two historical
sections that blocked WO-068 and WO-052 are admitted against the receipts that
preceded their purpose-built passes and classified `dated-capability-addition`,
the four malformed forms still refuse with their pre-existing messages, and the
2026-09-16 sequence refuses through the new checker naming both typed hard edges
while the corrected and current sequences pass. Criterion 4 is reproduced by a
full run of the harness fixture suite at the final source — the run the
execution record says was not made there — which passes 49 of 49 including the
generated planning-hook case. Criterion 5's write-backs all land, and criterion
6's gates are green in my own runs: `npm test` 19-of-19 and `npm run test:docs`
17-of-17, both with zero failures, `git diff --check` clean and no dependency
added. No blocking or substantive finding survived verification. Two
informational notes and the order's disclosed limits are recorded below.

Subject: [WO-135](../../work-orders/WO-135-capability-id-admission.md), branch
`wo-135` at `8471dc7e42aadfa64206771dae09541d6b1942b6`, which is also the merge
base with `main`. `git log --oneline main..HEAD` is empty, so no branch commit
exists before final review and all work is preserved uncommitted: 59 tracked
files changed, +1527/−559 against that base, plus 3 untracked paths
(`docs/control/orders/WO-135.jsonl`, `docs/evidence/WO-135/`,
`docs/final-reviews/WO-135/`). Checkpoint `refs/dotln/checkpoint/WO-135/3`
(`9eaafa68e9297a9e2a00e8ce0ca3a14be7186a90`). I judged the acceptance criteria
of the order as filed. I made no edit to the order, to any implementation file,
fixture, receipt, product document or evidence artifact; `git status --porcelain`
at handoff lists exactly the 62 entries this dispatch opened with, plus this
report. The only other write I caused is the role-mandated `work-orders index`
refresh — stale at entry solely because the recorded `verify` dispatch moved
WO-135 from `queued` to `verifying` — and it is a projection that reproduces
identical bytes on a repeat run (`75a97d305cd9ab42`).

Actor: this Claude Code session, harness `2.1.275` read back from
`claude --version`, model `claude-opus-5[1m]` as the session exposes it, Node
`v22.2.0`. No effort was supplied to this dispatch and this harness provides no
effective-effort readback, so effort is recorded `unknown` rather than inferred
from the order's `verifier any` recommendation, which is a recommendation and
not a supplied value. Under verification: the executor attestation (codex-cli
`0.154.0`, `gpt-6-astra`, `xhigh` with mode `subagents`, raw `ultra`, source
`codex-session-readback`).

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.275","model":"claude-opus-5[1m]","effort":"unknown","source":"claude-session-readback"}

## Instrument disclosure

Instruments that recorded or governed this verification are part of the subject.
`packages/skeleton/src/harness-host.ts` is modified by this order — it carries
the new `planningWriteRefusal` — and the generated `.claude/hooks/*.mjs` that
governed every tool call in this session resolve to the pinned runtime snapshot
`.runtime/harness/5896e4e2ed8ac675`, whose `harness-host.js` contains that same
rule. The rule never fired here: this worktree is on `wo-135`, not a `planning/`
branch, and the branch test precedes every path decision in the function, so a
non-`planning/` branch returns before any classification runs.
`packages/skeleton/src/gate-evidence.mjs` is also modified, but only to export
`prospectiveRealpath`; its body is unchanged, and it is the module that recorded
my own gate rows. `scripts/work-orders.mjs` is modified by this order and is
also the command whose index projection I refreshed and re-checked.
`scripts/test-runner.mjs` is **not** modified by this order, so the two gate
summaries below come from an instrument outside the subject; I nonetheless read
the per-suite PASS lines and the fixture suites' own TAP counters rather than
resting on a summary line alone.

My writer reservation (`actorId a259a8f4e2456fe8…`, owner pid 81928, source
`CLAUDE_PID`, `alive: true`) is this worktree's single reservation from entry
through handoff; at entry it reclaimed a dead reservation
(`ca3590a482bb68c8…`, pid 41019, source `ancestor`), which is the documented
reclaim path. The live-gate input guard refused nothing, because I ran no other
command while either gate was live.

## Executed checks

| Command or observation | Result |
| --- | --- |
| `npm test` | **exit 0 — 19 passed; 0 failed; 417.08 s; 63 fresh tasks** (gate row recorded `2026-09-18T00:13:10.174Z`, tree `5d19d4b2c2b7`) |
| `npm run test:docs` | **exit 0 — 17 passed; 0 failed; 279.99 s**, including `plan` (43.45 s) and `plan-refutation-current` (43.31 s) |
| `node scripts/test-plan-refutation.mjs` | **38 passed; 0 failed; 330.52 s**, including all five WO-135 cases |
| `node scripts/test-harness.mjs` | **49 passed; 0 failed; 448.65 s**; `ok 45 — WO-135 generated planning hooks refuse repository code paths and preserve documents, scratch and override` |
| `work-orders-fixtures` suite inside `npm test` | PASS, 52.78 s (the index-check limb of criterion 3) |
| `compiler` suite inside `npm test` | PASS (carries the merged-instruction assertions for criterion 4) |
| `node scripts/refute-plan.mjs check` | exit 0; 17 receipts; 8 passes; workspace updates `execution-record`, `release-classification-format`, `release-assignment` |
| `node scripts/work-orders.mjs index --check` | stale at entry (`README.md` line 3, from the recorded `verify` dispatch) → **PASS** after the role-mandated `index` refresh; refresh is byte-idempotent |
| `node scripts/harness-entry.mjs check` | `harness check: 28 generated surfaces; local-terms list: unavailable` |
| Independent replay probe (receipts 014/015, real sections) | both admitted as `dated-capability-addition`; judged subjects byte-identical after the call |
| Independent refusal probe (4 malformed forms) | all four refused with the pre-existing messages |
| Independent topology probe (3 sequence revisions + 5 synthetic groupings) | 2026-09-16 refused naming both edges; 2026-09-17 and current admitted; pair/triple rules as specified |
| Independent `shellWriteTargets` probe | `rm` and `touch -h` do not follow the final link; `node arbitrary.mjs` classifies to `null` (host-delegated) |
| `git diff --check` (worktree and against `main`) | rc 0, no output |
| `git log --oneline main..HEAD` | empty |
| `npm ls @dotln/compiler --json` | exit 0; `0.13.1` at the root and under `@dotln/skeleton` |
| `git diff main -- package-lock.json` | only the three workspace version strings; no dependency added |
| `git status --porcelain docs/planning/refutations docs/planning/capability-table.md docs/planning/sequence.md` | empty — no judged planning document changed |
| `claude --version` / `node --version` | `2.1.275 (Claude Code)` / `v22.2.0` |
| `node scripts/harness.mjs usage <session>` at entry | 76 steps, 74 commands; inputTokens 136; cachedInputTokens 6,859,625; cacheWriteInputTokens 152,319; outputTokens 35,229; totalTokens 7,047,309; `costUsd` unavailable; source `claude-transcript-message-usage`, scope `dispatch`, cutoff `2026-09-17T23:59:03.064Z` |

## Criterion findings

**1. Historical sections admitted against receipts 014/015 as additions;
receipts unchanged; `plan check` prints the kind — met, reproduced.**
Expected: WO-068's `runtime.resident` section and WO-052's
`worker.source-change` section are each admitted against the receipt that
preceded their purpose-built pass, recorded as `dated-capability-addition`, with
receipts 001–016 still admissible against unchanged judged subjects.
Observed, from my own replay of `reassessments` over the real committed bytes:
against receipt `2026-09-16-planning-8f6dda8f556e7e98-014`, whose subject does
not know `runtime.resident`, the real `## WO-068 dated addition (2026-09-16)`
section returns
`[{kind: "dated-capability-addition", workOrderId: "WO-068", date: "2026-09-16",
ids: ["runtime.resident"]}]`; against `2026-09-16-planning-496d3a8cf6dab3e9-015`
the real `## WO-052 dated addition (2026-09-17)` section returns the same kind
for `worker.source-change`. `JSON.stringify(judgedSubject)` is byte-identical
before and after each call, so the checker mutates no judged subject. Note that
both real headings say **addition** — the word the old regex refused — so both
of the two failures FINAL-001 §B1 isolated for WO-068 (heading form, then
unknown id) are cleared by this change, not just the second.
Receipt integrity: `docs/planning/refutations` is unchanged against `main` (23
files), `scripts/lib/plan-subject.mjs` is unmodified, so subject composition and
the subject hash inputs are untouched; `plan check` re-validates all 17 receipts
and 8 passes at exit 0, and the document gate's `plan-refutation-current` limb
passes. Kind reporting: `checkPlanGate` returns `continuation.committedUpdates`
and `continuation.workspaceUpdates`, each row carrying `kind`, and the CLI
prints that object — I read the three kinds it prints on the current tree. I did
not append a section to the committed capability table to print
`dated-capability-addition` live, because that table is a judged planning
document and a verifier does not write it; the shipped fixture "WO-135 dated
additions are execution updates in plan check" asserts that kind in the same
`plan check` result, and my replay shows the value the CLI would print.
Severity: none. Limit: my replay exercises `reassessments` directly; the
end-to-end path through `checkPlanContinuation` is covered by the fixtures and
by `plan check` on the current tree.

**2. Malformed dated sections still refuse with the current messages — met,
reproduced.** Against receipt 014's judged subject and the real capability
table, each of the four forms refuses, prefixed by the unchanged gate message
`planning pass needs a receipt matching the current subject`:

- heading naming an order absent from the judged subject (`WO-999`) →
  `capability reassessment needs a reviewed order and a valid date`;
- invalid date (`2026-02-30`) → the same message;
- a stray `## Stray` heading appended after a valid section →
  `capability additions must be appended dated reassessment sections`;
- an empty observation cell → `missing capability observation probe.new`.

A well-formed section naming an unknown id is admitted as
`dated-capability-addition`, which is the intended new behavior. The three
message strings are byte-identical to `main`; the diff removes only the
`new capability id ${id}` assertion and widens the heading alternation.
The fixture suite additionally replaced the two now-obsolete negative rows
(unknown id; `dated addition` heading) with an unknown-order addition, an
invalid-date addition and a stray heading, so the negative inventory did not
shrink. Severity: none.

**3. Both public gates refuse the two typed reversals and pair-internal blocking
edges; larger groups order-only — met, reproduced.** Expected: `plan check` and
`work-orders index --check` fail on the 2026-09-16 sequence and on a two-entry
pair containing a blocking edge, naming the orders and the edge; both pass on
the sequence the 2026-09-17 pass committed; groups of three or more are checked
for order only. Observed, driving `checkSequenceTopology` with the current
repository's dependency projections:

- `docs/planning/sequence.md` at `ec502c9` (2026-09-16) → refused:
  `WO-114 -> WO-120 (hard): dependency follows its dependent;
  WO-115 -> WO-100 (hard): dependency follows its dependent`. WO-120 and WO-100
  are both `draft` with no verdict, so both edges are genuinely unmet today.
- at `4576594` (the corrected 2026-09-17 sequence) → admitted, 16 groups,
  14 pairs; the current workspace sequence → admitted, 17 groups, 15 pairs.
- synthetic groupings over the same rows: `[WO-120, WO-114]` as a pair →
  `blocking edge inside a two-entry pair`; `[WO-120, WO-114, WO-115]` as a
  three-entry group → admitted; `[WO-114, WO-120, WO-115]` → `dependency
  follows its dependent`; the same two orders in separate one-entry groups
  follow the order rule in both directions.

Both public entry points carry the check at live scale: `node
scripts/refute-plan.mjs check` exits 0 and `node scripts/work-orders.mjs index
--check` prints `PASS docs/work-orders/README.md is current` on the current
sequence, and the shipped fixtures drive both commands to failure on the frozen
historical one. The relation coverage matches the criterion: `blocking` is
`state === "unmet"`, which is `hard` and `satisfied-by-close` while the target
is not closed-pass, `satisfied-by-release` while the release is absent, and
`planning-deferral` while `until` is not closed; the fixture asserts each of the
four relations refuses when unmet and admits when satisfied. The historical
fixture freezes the control sources at `4576594` so a future close cannot erase
the regression, which I confirmed by reading it.
Severity: none. **Informational note (a):** the criterion describes the
committed 2026-09-17 sequence as "fourteen two-entry pairs and one serial run",
and the fixture pins exactly those fourteen pairs and the 36-entry tail, plus
sensitivity to a lost or added blank separator. The document actually parses as
16 groups — a leading 24-entry run of already-sequenced orders, then the 14
pairs, then the 36-entry tail — so the leading run is not pinned by the
`slice(1, -1)` assertion. The criterion's substance is discharged; the unpinned
leading group is worth one sentence to the reviewer, not a repair.

**4. Planning-branch write refusal in the generated hook, with Codex role text
and a current bundle — met, reproduced.** The execution record states that the
full harness suite passed 49 cases *before* the final no-follow symlink
refinement and that only a focused fixture ran at the final source, so I ran the
whole suite myself at the current bytes: **49 passed, 0 failed**, with `ok 45`
being the WO-135 case. That case drives three generated hooks (`permissions`,
`concurrent-work-requires-worktrees`, `write-observer`) over nine denied and
thirteen admitted requests on a `planning/2030-01-02-fixture` branch: `Write`,
`Edit` and `NotebookEdit` to code paths, redirects and `touch`/`touch -h` to
code paths, a `docs/` symlink whose physical target is `scripts/`, and a write
reached through a scratch-side symlink all deny, each naming the path and
`operator override:`; `docs/`, root Markdown, an out-of-repository scratch path,
a relative path above the root, an opaque `node arbitrary.mjs`, `rm docs/source`
and `touch -h docs/source` are admitted; `operator override:` admits the denied
request and `operator override: off` restores the refusal; switching off the
`planning/` branch admits it. I separately confirmed the shell classifier's
semantics by calling `shellWriteTargets` directly: `rm` and `touch -h` report
`followFinalSymlink: false` (an entry removal, so the `docs/` entry stays
admitted) while an ordinary `touch` follows the link, and an unrecognized
program returns `null`. The compiled sentence is asserted by
`packages/compiler/test/harness.test.ts` (the `compiler` suite passes), all six
`.agents/skills/*/SKILL.md` and all six `.claude/skills/*/SKILL.md` carry the
three-refusal sentence with the `planning/` clause, `CLAUDE.md` and `AGENTS.md`
carry it, and `harness check` reports 28 generated surfaces current.
Severity: none. **Limit, disclosed by the order and its write-backs:** the rule
classifies known tool destinations only. An opaque shell program — `node x.mjs`,
`git checkout main -- scripts/`, an interpreter — returns no write set and stays
host-delegated, so this is a classified-effect boundary, not an OS sandbox. The
original register-item-10 failure mode (an agent editing `scripts/*.mjs` on a
planning branch through the write tools) is inside the classified set. The rule
is keyed on the branch prefix, so a detached HEAD during a planning pass is
outside it; operator-review assumption 2 admits the keying.

**5. Write-backs land — met, read at source.** Each named surface carries the
change: product 07 §Operator-opened planning pass now says the continuing-work
gate admits "appended dated capability additions or reassessments for orders in
that reviewed sequence, including new ids" and that "Verification, final review
and the next planning receipt judge their level claims", plus one sentence on
the topology check and one on the planning-branch write rule, and a paragraph
recording the `release-classification-format` admission; product 07 §Discipline
now reads "DotLn has three hook refusals (WO-135)" and names all three;
`docs/AI-HARNESS-SECURITY.md` §DotLn hook boundary is retitled "WO-132 and
WO-135" and opens "DotLn's generated hooks refuse three conditions (WO-135)"
with the external-scratch and override carve-outs; the compiled instruction
sentence in `packages/compiler/src/harness.ts` is the three-refusal text and is
current in `CLAUDE.md`/`AGENTS.md` under `harness check`; the planning map
carries an **Execution disposition (2026-09-17, WO-135)** under candidate 1 and
under register item 10; `docs/evidence/WO-135/decisions.md` records D001–D006
with sources, alternatives, the eight traps and reopening conditions, and
`docs/lineage/decisions-index.md` gained its rows. Severity: none.

**6. Gates, whitespace, dependencies and capability neutrality — met,
reproduced.** `npm run test:docs`: 17 passed, 0 failed, including `plan` and
`plan-refutation-current`. `npm test`: 19 passed, 0 failed, 63 fresh tasks.
`git diff --check` is silent in the worktree and against `main`. The lockfile
diff is exactly the three workspace version strings, `npm ls @dotln/compiler`
resolves `0.13.1` at the root and under skeleton, and no dependency is added.
`docs/planning/capability-table.md` is unchanged against `main`, so the order
introduces no capability id and promotes no level. Severity: none.

## Scope beyond the criteria, and what I did not reproduce

The execution record and D003/D005 record an operator-expanded scope: a personal
SSH/SCP/SFTP `forbidden` rules file installed at
`~/.codex/rules/personal-remote-deny.rules` and a substantial rewrite of
`docs/AI-HARNESS-SECURITY.md` covering both harnesses' sandbox and permission
modes. That expansion is outside acceptance criteria 1–6 and I judged it only
for the floor it must not breach: the repository surface it added carries no
credential contents, no employer material and no internal identifier — the diff
has no credential-shaped string, and its citations are public vendor
documentation and a tagged open-source source file. The rules file itself lives
outside the repository, and I did not run `codex execpolicy check`, so the
runbook's "twelve bare/argument checks returned forbidden, with `git status` and
`ls` as non-forbidden controls" remains the executor's recorded observation,
reported and not reproduced here. The runbook states Claude's current mode as
operator-attested rather than measured, which is the honest label.

I also did not re-run the recorded evidence editions under
`docs/evidence/WO-135/` (`authority` 002, `feedback` 002, `artifact-identity`
001, `verification` 001) or the live feedback audit; they are recorded
artifacts, they are not named by any acceptance criterion, and re-running them
would consume live worker capacity this dispatch holds no authority to spend.
`docs/final-reviews/WO-135/PR.md` exists already as an untracked draft with a
process-meter table full of `unavailable` cells; it is a reviewer-owned surface
regenerated at final review and I left it untouched.

**Informational note (b):** `work-orders index --check` was stale at entry. The
cause is this dispatch itself — the recorded `verify` transition moved WO-135
from `queued` to `verifying` and the projection had not been refreshed — and the
role-mandated `index` refresh cleared it and reproduces identical bytes on
repeat. It is not a defect in the order's work, and I record it only so the
reviewer is not surprised by the one projection change in this dispatch's diff.

## Findings

None. No blocking finding, no substantive implementation defect, and nothing
that routes to repair. The two informational notes above are recorded for the
reviewer's judgment, not as findings, and the criterion-4 classification limit
is the order's own disclosed boundary rather than a gap between the order and
its implementation.
