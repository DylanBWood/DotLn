# WO-140 VER-001 — independent verification of sandbox preflight and receipt cost lines

**Verdict: pass.** All six acceptance criteria are met, with the source correction in criterion 3 explained below. No substantive implementation defect was reproduced. This is WO-140's first verification; no earlier WO-140 findings exist.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-6-astra","effort":"xhigh","source":"codex-session-readback","accountLabel":"not-applicable"}

Actor: Codex CLI 0.155.1, gpt-6-astra, xhigh, from the active-session readback. Dispatch: `resume: verify`, 2026-09-20. One verifier; no subagents.

**Process cost:** unknown; cause harness-no-readback

The entry usage command initially found no begun harness session. After explicit session initialization, its 04:18:29Z observation still had unavailable counters, source `unavailable`, scope `dispatch`, and no counter cutoff. Counter readback became available later. The entry-to-handoff difference remains unknown; the final available handoff counters, source and cutoff are reported in the response and ignored usage receipts. This cause describes the missing entry reading, not a claim that later counters never became available.

## Subject and evidence boundary

Judged the original [work order](../../work-orders/WO-140-gate-sandbox-preflight.md), its cited implementation and historical evidence, and the current uncommitted subject above HEAD `295766dd11b12d0cd819161a7c4f9ec58a82d7f2`. Read the changed source and fixtures, product 07's cited discipline and goal-alignment sections, shared Process Cost support, generated role changes, and the executor's decisions and evidence. The additional historical input was WO-121 FINAL-001 §L1, selected by [D001](../../evidence/WO-140/decisions.md#wo-140-d001), to resolve the order's hooks-denial attribution.

The independent product gate recorded code identity `49f6b472e92f873dd9218ad6bcd2c29fc8e0e7bc61611ce146b4899e73086a4e`, tree `0dfe9e60d739ec2cefd6148d57e3cafd5d881050`, at **2026-09-20T04:27:06.694Z**. Its evidence reference is `host-gate:49f6b472e92f873dd9218ad6bcd2c29fc8e0e7bc61611ce146b4899e73086a4e:npm test`.

Code identity covers tracked source. Two new modules remain untracked and were directly read and exercised at these SHA-256 digests:

- `scripts/lib/gate-sandbox.mjs`: `c26eb37639ade1a3a84e436241a06665e16e70c05db5b66767ba2b58dc079d06`.
- `scripts/lib/receipt-cost.mjs`: `d623d28d11898b0e169c959cf2c24ff419f9c2a24b25a041112beeec47acab29`.

The reviewer must stage intended new sources before its publication gate, as the existing reviewer procedure requires. This report does not substitute this earlier tracked-source identity for that obligation.

## Acceptance results

| Criterion | Expected and observed |
| --- | --- |
| 1 — preflight and partial selection | **Met.** The runner fixtures passed. An additional independent temporary-repository probe invoked the real `runGate` with build, inside and outside tasks, each appending its name to a sentinel. Real macOS Seatbelt profiles denied writes to `.claude/hooks` under `CLAUDECODE=1`, and separately `.git` under `CODEX_SANDBOX=1`. Both refused before the sentinel existed, named the excluded task and printed the outside command. The Codex-marker partial run executed exactly build and inside, recorded `npm test -- --inside-sandbox`, `partial: true`, and the outside exclusion. With both markers inherited but no profile, all three tasks ran and the full result was accepted. |
| 2 — evidence consumers | **Met.** `runner-fixtures` passed the same-code-identity partial/full regression. It exercises `findGateCheck`, lifecycle evidence, and committed-history `reviewedProductGate`; source inspection confirmed that both worktree publication and release close call the latter. Partial rows, including relabelled rows and exclusions without the partial flag, are rejected as product evidence. A full row is accepted. The independent native partial probe also returned no `findGateCheck(..., "npm test", ...)` match. Lifecycle completion itself remains advisory about absent gate evidence; it does not attach a partial row as a product gate. |
| 3 — declarations from environmental evidence | **Met with the existing sourced correction.** Only `skeleton` declares the outside requirement; `release` does not. WO-121 VER-001's executed-check table identifies the environmental failure as the native skeleton cases, and F1 as a release fixture race reproduced outside too. WO-068 FINAL-001 confirms the nested-sandbox cause. WO-121 FINAL-001 §L1 attributes the hooks denial to merge/checkout/emission commands, not a failing suite. D001 already records this error in the order's wording and a named planning follow-up. No unsupported suite exclusion was added to match that wording. The fake-marker regression passed. |
| 4 — briefing, skills and cause codes | **Met.** This dispatch printed the session id and exact usage command; the command worked after explicit session initialization. The four generated verifier/reviewer skills contain the attended outside-gate and resident partial-result instructions. The process-debt fixtures passed their briefing and role-text assertions. Product 07 §Process budget records the same three causes as `receipt-cost.mjs`. |
| 5 — new and historical receipts | **Met.** An independent scratch control segment allocated a stamped VER and FINAL. Both bare-unknown reports were refused by the document and completion validators, and `metaMain(["--check"], root)` refused them. A counters line and a single-cause line passed. Removing the stamps admitted the historical bare-unknown reports. `process-debt` and `resume` passed the repository regressions, including refusal before a result event is appended. The actual document gate passed all 19 suites before this report was filed; this report's cost line is checked again before its result transition. |
| 6 — complete checks and dependencies | **Met.** My `npm test -- --review` passed **29 suites, 0 failures, 73 fresh tasks, 488.55 s**, exit 0. The review selection includes the changed runner, harness and process machinery. `npm run test:docs` passed **19 suites, 0 failures, 19 fresh tasks, 16.59 s**, exit 0. Direct `node scripts/harness.mjs check` passed **31 generated surfaces**. `git diff --check` passed. Package and lockfile diffs add no external dependency. No implementation was changed during verification. |

## Reproduction and limits

Run `npm test -- --review`, `npm run test:docs`, `node scripts/harness.mjs check`, and `git diff --check` from the selected worktree outside a harness sandbox. The targeted retained fixtures are `node --test --test-name-pattern=WO-140 scripts/test-runner.test.mjs` and the WO-140 cases in `scripts/test-process-debt.mjs`; these were exercised through the full gate, not rerun merely for a second count.

For the independent native probe, create an isolated temporary Git repository and inject three real subprocess tasks into `runGate`. Give the third task `needs: "outside-sandbox"`. Use `(version 1)(allow default)(deny file-write* (subpath "<fixture>/.git"))` with `CODEX_SANDBOX=1`, or the equivalent hooks path with `CLAUDECODE=1`. Check the sentinel before/after refusal, then run `--inside-sandbox --serial` and query the recorded row. Repeat without the profile while retaining both markers. The scratch repositories were removed after assertions passed; project source was untouched.

The native probes establish behavior under the declared markers and protected paths on this macOS host. They do not establish every real harness launch configuration or Linux marker propagation. Unrecognized sandboxes intentionally fail open. A permitted probe with a different environmental restriction can likewise fail open, as D002 already records. Actual savings across operator sessions were not measured.

Self-referential instruments: `resume.mjs` allocated and will record this report; the runner, evidence consumers, briefing and receipt validator are themselves under verification. Independent temporary-repository probes, retained negative fixtures, and direct source checks support the verdict rather than relying solely on those instruments' positive output.

## Goal alignment

The contribution is fewer environmental gate failures while preserving trustworthy review evidence on the path to verified deliverables. I retained D001/D003/D004/D006's bounded choices. Policy resistance/fixes that fail is checked by accepting inherited markers outside; commons by one full gate and zero fan-out; drift by retaining the full suite requirement; escalation by using existing checks; success to the successful by independently probing behavior rather than inheriting the executor's green row; shifting the burden by actionable refusal and cause text; rule beating by rejecting relabelled partial rows; and seeking the wrong goal by judging behavior, not receipt volume. Naive Interventionism favors unchanged outside behavior and historical receipts. NoOp would retain the documented false starts and ambiguous cost lines. The observed probes support the intended behavior, without claiming measured recurring savings.
