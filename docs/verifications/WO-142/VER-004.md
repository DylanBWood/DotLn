# WO-142 VER-004 — independent verification of repair 003

**Verdict: pass.** Repair 003 resolves VER-003's blocking finding and both of
its non-blocking findings. It holds in fixtures, under mutation, under replay
of local transcripts, and live on Claude Code 2.1.278.

- **VER-003 F1 (row B2 ★) is resolved.** This session reproduced the trigger
  live: a completed background task, then a failed `TaskStop` on it,
  reconstructed as an `unknown` row. The task stayed `completed`, and its
  elapsed time stayed at 2,926 ms in every later prompt.
  - Replaying 185 local transcripts (632 terminal tasks) through the current
    observer gives 0 wrong terminal states or times.
  - Repair 002's observer, replayed the same way, gives 41 wrong states and 62
    tasks with a wrong state or time.
- **VER-003 N1 is resolved.** The document gate's notice reached this session's
  prompt hook before the transcript held it. The prompt facts already read
  `completed`. One further boundary added the earlier transcript time exactly
  once.
- **VER-003 N2 is resolved.** Reversing the earliest-dispatch selection, or
  removing reconstruction's journal scope, now fails the observer suite.
- **Gates, under Node 26.9.0:**
  - `npm test -- --review` passes 35/35.
  - `npm run test:docs` passes 19/19.
  - Both `git diff --check` forms are clean.
- **No new finding.** Nothing routes to repair, and no defect was met that
  needs boarding.
- **One item is outstanding for the operator, not for repair.** Criterion 5's
  operator-run prune listing on the real checkout is still not recorded (O3).
  Final review must see it recorded before close.

Subject:

- the order: [WO-142](../../work-orders/WO-142-outstanding-cleanup.md) as
  amended by Part E;
- branch `wo-142`, with `HEAD` = `main` = `c5b2b0e7`. All work is uncommitted;
- the tree judged: `refs/dotln/checkpoint/WO-142/15` (`dd9d84b8`, tree
  `6e6b7028`) plus this dispatch's own projections (`current.md`,
  `WO-142.jsonl`, the work-order index). I compared a temporary-index tree of
  the worktree with checkpoint 15, and only those three files differ.
- Repair 003's change set is checkpoint 13 → 14: 32 files. Four are
  hand-written source or tests: `observed-facts.ts`, `harness-host.ts` and
  their two test files. The rest are the regenerated bundle, evidence and
  projections.

This `resume: verify` allocated VER-004 at `2026-09-19T16:32:31.181Z`. The
prompt hook recorded the dispatch; none was repeated.

This verifier:

- changed no implementation;
- made no branch commit;
- ran no live model and published nothing;
- ran no real prune;
- wrote only this report and the generated projections that the completion
  refreshes.

Verifier details:

- **Harness:** Claude Code 2.1.278, read with `claude --version`.
- **Model:** `claude-opus-5[1m]`, as this session exposes it.
- **Effort:** `CLAUDE_EFFORT` reads `xhigh`, and all 609 effort-bearing journal
  rows of this session record `xhigh`. No `ultra` or `ultracode` selection was
  made.
- **Effective readback:** no effective-model or effective-effort readback
  channel is claimed.
- **Repair attestation under verification:** codex-cli 0.155.1, `gpt-6-astra`,
  `xhigh`, `codex-session-readback`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"xhigh","source":"operator-attested"}

## Instrument disclosure

Several instruments under verification also recorded this verification. Every
claim that depends on one says so.

- **The observed-facts block (row B2).** It journaled this session's three
  background tasks and delivered the prompt facts quoted below as F1 and N1
  live evidence. Each live claim is checked against the journal rows and
  transcript timestamps, not only against the rendered facts.
- **The live-gate guard (row B3).** It judged none of my commands. Both of my
  gates ran while my turn was idle, so no command of mine ran during a live
  gate.
- **The prompt hook (row A4).** It recorded this dispatch, and the document
  gate's `index` check passed.
- **The subagent counter (WO-139).** I planned at most two read-only helpers
  against the cap of 20 and used none: 0 of 20, exact-observed.

**Self-disclosure.** To compare the worktree with checkpoint 15, I staged the
whole tree into a temporary index file in my scratchpad (`GIT_INDEX_FILE`).
That wrote loose blob objects into the shared object store. No ref, no real
index and no worktree file changed. The mutation runs used a scratchpad copy
made with `rsync`, excluding `.git`, `docs/intake/`, local control state,
`.runtime` and the beacon directory. They restored every mutated file.

## Method

- **Root checks, all in this session.** I ran the review gate and the document
  gate under Node 26.9.0, and both whitespace checks. I also checked
  `harness check`, the follow-up feed, and preservation against the
  repair-entry checkpoint.
- **Reverted-subject runs.** I patched the built observer (`dist`) in a
  scratchpad copy, one mutation at a time. For each, I ran
  `scripts/test-observed-facts.mjs`, and for the prompt path also the
  generated-hook tests in `scripts/test-harness.mjs`, whose fixtures copy that
  `dist`.
- **Transcript replay.** I loaded the observer sources of checkpoint 15
  (current) and checkpoint 13 (repair 002) directly as TypeScript under Node
  26. I then replayed every top-level transcript in local DotLn project
  directories that holds a native terminal notice, in disposable roots, with
  each copy's `cwd` rewritten to its root. The expected result per task is its
  earliest explicit terminal notice: the queue enqueue, the queued-command
  attachment or the user row.
- **Incremental replay (N1).** At each user-row notice (an idle delivery), I
  ran a boundary with that notice's own rows and everything after them
  removed from the transcript, and the notice passed as the prompt. This
  simulates delivery before the transcript flushes. A full-transcript boundary
  then closed each run.
- **Live boundaries.** This session backgrounded three tasks: a 3 s probe, the
  review gate and the document gate. I called `TaskStop` on the finished
  probe, then read the facts each prompt delivered, the session journal and
  the transcript's own timestamps.
- **Output limits.** Scans printed only counts, states, row kinds, timestamps
  and envelope shapes. They excluded any project directory whose name
  contains "intake".

## Executed checks

| Command or observation | Result |
| --- | --- |
| **`npm test -- --review`, Node 26.9.0 (dispatched 16:38:39.571Z)** | **35 passed, 0 failed, 303.06 s, 79 fresh tasks, exit 0.** Gate row: 303,064 ms, recorded 16:43:43.254Z. `scripts/test-runner.mjs` and the package scripts are unchanged since VER-003's subject. VER-003 compared this selection with plain `npm test` (all 21 suites included) |
| **`npm run test:docs` (dispatched 16:44:18.346Z)** | **19 passed, 0 failed, 15.08 s, 19 fresh tasks**, including `index`, `publication`, `meta`, `plan`, `format`, `authority-evidence` (revision 004) and the other evidence checks |
| `git diff --check`; `git diff --cached --check` | both clean, exit 0 |
| `node --test scripts/test-observed-facts.mjs` (scratchpad copy) | 41 passed, 0 failed |
| `--test-name-pattern='VER-003 N1 generated\|WO-142 repair B2' scripts/test-harness.mjs` | 2 passed, 0 failed |
| `node scripts/harness.mjs check` | "31 generated surfaces" |
| `npm run plan -- followups` | 428 total, 91 pending, 10 untriaged. This matches repair 003; this report adds none |
| Preservation, checkpoint 13 → now, every file under `docs/verifications/WO-142`, `docs/evidence/WO-142` and `docs/final-reviews/WO-142` | 53 byte-identical, including VER-001–003, repair 001–002 and the PR draft. 3 changed: the evidence README, `rows.md` and `decisions.md`, as repair 003 states |
| Append-only surfaces, checkpoint 13 → 15 | `decisions.md`, `followups.json` and `decisions-index.md` remove no line |
| Unchanged since checkpoint 13 | Role text (`.claude/skills`, `.agents/skills`), `harness-prune.mjs`, `planning-followups.mjs`, `plan-direct.mjs`, `harness-command.ts`, every `package.json` and the lockfile |
| Suppression directives, `.skip`, `.only` or `.todo` added by repair 003 | 0 |
| D020's source claim (`scripts/release.mjs:2016-2040`) | Confirmed. `release prepare` writes `docs/final-reviews/<order>/PR.md` unconditionally, then prints "no files changed" when `plan.edits` is empty |

**Reverted-subject runs.** Each mutation was applied alone to the built
observer.

| Mutation | Suite | Result |
| --- | --- | --- |
| Fold restored to repair 002's chronology | observed-facts | 35/41: all four VER-003 native sequences, the 120-order test and the prompt-reconciliation test fail |
| A later terminal replaces an earlier terminal | observed-facts | 39/41 (killed → stopped; 120 orders) |
| An older terminal cannot replace a newer non-terminal | observed-facts | 40/41 (120 orders) |
| Non-terminal: first append wins | observed-facts | 40/41 (N2 chronology test) |
| Latest `dispatchedAt` instead of the earliest (VER-003 N2) | observed-facts | 39/41 |
| Reconstruction's journal left unscoped (VER-003 N2) | observed-facts | 38/41 (phase, order and start tests) |
| Prompt ingestion dropped (VER-003 N1) | observed-facts; generated hook | 40/41; 1/2 |
| `harness-host` prompt forwarding removed | generated hook | 1/2 |
| Earlier transcript time not reconciled | observed-facts | 40/41 |
| A later duplicate delivery appends | observed-facts | 26/41 |
| Prompt accepts a non-terminal status | observed-facts | 39/41 |
| Prompt's start-time check removed | observed-facts | **41/41, survives** (O1: equivalent) |
| Envelope closing-tag check removed | observed-facts | **41/41, survives** (O1) |

**Transcript replay (VER-003 F1 class).** 185 transcripts hold native terminal
notices, covering 632 terminal tasks. Every replay ingested at least one
notice, so none failed silently (VER-003 O3).

| Observer | Wrong terminal state | Right state, wrong time | Non-terminal after its notice |
| --- | --- | --- | --- |
| Repair 002 (checkpoint 13) | 41: 33 completed → unknown, 3 failed → unknown, 5 killed → stopped | 21 | 36 |
| **Current (checkpoint 15)** | **0** | **0** | **0** |

**Incremental replay (VER-003 N1 class).** There were 223 idle-delivery
boundaries, each with its notice not yet in the transcript.

| Observer | Terminal at its own prompt boundary | Wrong after the closing boundary | Notice event IDs with more than two rows |
| --- | --- | --- | --- |
| Repair 002 | 4 of 223 | 24 | 0 |
| **Current** | **223 of 223** | **0** | **0** |

The corpus differs slightly from repair 003's (647 terminal tasks), because
this replay keys tasks through the journal's invocation map. Its error counts
for repair 002 (41 states; 62 states or times) equal repair 003's.

## Live observations (Claude Code 2.1.278)

| Task | Sequence (journal and transcript timestamps) | Delivered prompt facts |
| --- | --- | --- |
| 1: 3 s probe | Dispatched 16:37:52.595Z. Notice enqueued and journaled 16:37:55.521Z. `TaskStop` at 16:38:04.830Z returned "No task found with ID", and reconstruction journaled `unknown` at 16:38:04.833Z, which is VER-003 F1's trigger | At 16:43:43.431Z and 16:44:33.755Z: "last observed state completed; elapsed to terminal observation 2926 ms". Before the repair, this sequence showed `unknown` with growing elapsed time (VER-003) |
| 2: review gate | Transcript enqueue 16:43:43.313Z, already flushed when the hook ran | At 16:43:43.431Z: `completed`, 303,742 ms (transcript path) |
| 3: document gate | Transcript enqueue 16:44:33.667Z, **not yet visible** to the hook. The prompt path journaled `completed` at the hook's own time, 16:44:33.755Z | At 16:44:33.755Z: `completed`, 15,409 ms. Inference: repair 002 reads only the transcript, so it would have shown `dispatched` here, as in VER-003 N1. The incremental replay supports this |

**Reconciliation, live data.** I replayed a copy of this session's journal and
transcript through two more boundaries. Exactly one row was added, for task 3
at 16:44:33.667Z. Task 3 then reads 15,321 ms, shortened by 88 ms, not
restarted. Tasks 1 and 2 were unchanged.

## Acceptance criteria

| # | Criterion | Verdict | Evidence |
| --- | --- | --- | --- |
| 1 | Rows file complete, one state each, reverted-subject evidence, returns bounded | **met** | `rows.md` B2 ★'s repaired claim now holds, both live and under replay. Its checks fail under each repaired subject's reversal (table above). Every other row is unchanged since VER-003, which confirmed them, and their owning suites pass in this review gate. The single return (B12(b), D002) is unchanged |
| 2 | Feed fixture both forms; B17 rule for three roles; untriaged count recorded | **met** | Role text is unchanged since checkpoint 13, and `harness check` passes. The feed reads 428 / 91 / 10, matching repair 003. The merged-tree figure belongs to final review |
| 3 | A title-stamp-only order stays carried | **met** | `plan-direct.mjs` is unchanged; `plan-refutation` passes in the review gate |
| 4 | `harness check`; two-advisory and read-list fixtures; four refusals still refuse | **met** | 31 surfaces. The guard source is unchanged since VER-003. The harness suites, including the second-writer, live-gate, planning-branch and cap tests, pass in the review gate |
| 5 | Prune preview deletes nothing; apply removes exactly the listed paths; real listing recorded | **met for the implementation; the operator clause is outstanding (O3)** | `harness-prune.mjs` is unchanged since VER-002, and its fixtures pass in the review gate. No operator-run real-checkout listing, with before and after sizes, is recorded yet |
| 6 | Write-backs land; `publication:check` passes | **met** | I read repair 003's 07 addition (§Discipline observed facts). It states the terminal chronology and the prompt-time/transcript-time distinction exactly as the code behaves. The evidence README's changes and D019/D020 are accurate. `publication` passes in the document gate |
| 7 | `npm test`; `test:docs`; `git diff --check`; no new dependency except E1's; no new suppression | **met at this subject** | Both gates are green under Node 26.9.0, and the diff checks are clean. Manifests and the lockfile are unchanged since checkpoint 13, and repair 003 adds no suppression. Final review still owes its own gate |

**Part E.** E1 and E2 keep VER-002's verdicts (met). The runtime and compiler
pins are unchanged, and both gates pass under Node 26.9.0.

## VER-003 findings

| VER-003 item | Status now | Evidence |
| --- | --- | --- |
| **F1 (B2 ★)** | **resolved** | Live: task 1 stays `completed` at 2,926 ms after a reconstructed `unknown` row. Replay: 0 of 632 wrong, against repair 002's 41 states and 62 states or times. Four targeted fold mutations fail |
| N1 (idle delivery) | resolved | Live, task 3: `completed` at its own prompt before the transcript had the notice; later reconciliation adds one earlier row. Replay: 223 of 223. Both prompt-path mutations fail |
| N2 (unpinned behaviors) | resolved | The latest-dispatch mutation fails 2 tests; the unscoped-journal mutation fails 3 |
| D018 | owned | unchanged |
| O1–O4 | as recorded | Unchanged subjects |
| O5 (PR draft) | reviewer-owned, now with D020 | `PR.md` is byte-identical to checkpoint 13. D020 boards the helper that rewrote it, with a follow-up |
| O6 (real prune listing) | still outstanding | See O3 below |
| L1–L5 | carried | See Limits |

## Findings

None. Nothing routes to repair.

## Observations

- **O1 (B2 test pins).** Two guards in the shared notice parser (prompt and
  transcript) survive their removal:
  - **The start-time check is equivalent.** The prompt's time is the
    boundary's time, which is never earlier than the scope's start, and the
    transcript loop already skips earlier rows.
  - **The closing-tag check is unpinned.** All 1,748 native envelope rows in
    local transcripts (1,299 of them in row kinds the parser reads) are one
    envelope that ends with its closing tag, so no real input reaches the
    check. It guards correctly, so this is a coverage note, not a defect.
- **O2 (N1 timing).** Two idle deliveries were sampled live. One was already
  flushed when the hook ran (task 2) and one was not (task 3). Both showed
  `completed` at their own prompt. The pre-flush case depends on host timing.
  Its live observation here is one sample, backed by the generated-hook fixture
  and the 223-boundary replay.
- **O3 (criterion 5, carried from VER-002 O11 and VER-003 O6).** The operator
  has not yet run the real-checkout listing, and no decisions entry records
  operator-run before and after sizes. D006 records the executor's preview.
  This is an operator action, not an executor defect, so it does not route to
  repair. Final review must see it recorded, or explicitly waived by the
  operator, before close.
- **O4 (gate time).** The review gate took 303.06 s, against VER-003's
  304.20 s and repair 003's 307.99 s. The document gate took 15.08 s.
- **O5 (evidence README).** The README's actor attestation changed its effort
  from `ultra` to `xhigh`. That matches repair 003's own attestation and the
  latest attestation `resume status` reports. The README is a current surface,
  not an immutable record.

## Limits

- **L1 (no live spend).** No worker, planning refutation or feedback audit was
  run. Feedback revision 002 was judged through the passing
  `feedback-evidence` check. Neither changed source is a feedback source input.
- **L2 (replay scope).** The replay covers top-level transcripts in DotLn
  project directories, not subagent transcripts, and it has no live hook rows.
  Its counts describe reconstruction. The live rows are limited to the three
  tasks above.
- **L3 (`TaskOutput`, `killed`).** Claude Code 2.1.278 does not offer
  `TaskOutput`, and a native `killed` notice could not be produced on demand.
  Both were judged through the native-sequence fixtures and replay.
- **L4 (Codex).** Repair 003 changes only Claude's prompt path and the shared
  fold. Codex live task observation was not exercised; the fold's tests are
  harness-neutral.
- **L5 (carried).** Live acceptance of B4's schema and the worker-host probe
  remain unobserved (VER-002 L2, L3).
- **L6 (clean room).** No `docs/intake/` content was opened. Transcript scans
  printed counts, states, row kinds and timestamps. They excluded
  intake-named project directories. No transcript text is quoted here.
- **L7 (usage).** Root dispatch usage at 16:46:02Z (source
  `claude-transcript-message-usage`, scope dispatch):
  - input 106, cached input 7,848,032, cache writes 179,767, output 44,320,
    total 8,072,225 tokens;
  - reasoning tokens and cost unknown;
  - 54 steps and 44 commands;
  - subagents 0 of cap 20, exact-observed.

  Report writing and completion happen after this cutoff.
