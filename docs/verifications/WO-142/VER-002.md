# WO-142 VER-002 — independent verification of repair 001

**Verdict: fail.** Two blocking findings route to repair. Both are residues of
VER-001 findings the repair addressed only in part:

- **F1 (row B2 ★).** Claude Code's native `killed` terminal status is not
  recognized. A task whose `killed` notice arrives keeps state `dispatched`,
  and its elapsed time keeps growing. That is the symptom row B2(b) names.
  Local transcripts hold 14 such notice deliveries.
- **F2 (row B3, VER-001 F3).** Thirteen read-only command forms that the
  live-gate guard admitted at activation are still refused. Examples:
  `echo ---`, `grep -rA3`, `grep -3`, `grep --exclude-dir=…`, `ls --all` and
  `cat -`. The repair restored the seven forms VER-001 quoted, not the class.

Everything else that VER-001 found is repaired or explicitly owned:

- **VER-001's blocking findings.** F1 (the index refresh) and F4 (the kernel
  coupling assertion) hold in real use.
- **VER-001's non-blocking findings.** N1–N6 and N8–N14 are resolved, except
  N13's B3 claim (criterion 1); N3 keeps a low residual (N9). N7 is owned by
  WO-142-D012.
- **Part E.** Node 26 and TypeScript 7 hold on every executable check this
  verification ran.
- **Gates.**
  - Under Node 26.9.0, `npm test` passes 21/21 and `npm test -- --review`
    passes 35/35.
  - `npm run test:docs` passes 19/19, and `git diff --check` is clean.
- **Findings for the same repair.** Ten non-blocking findings (N1–N10) and
  twelve observations go to the same repair.
- **Boarded defect.** One pre-existing defect outside the order's rows is
  recorded as
  [WO-142-D015](../../evidence/WO-142/decisions.md#wo-142-d015--board-up-the-optional-chaining-gap-in-the-kernel-coupling-tripwire-met-during-ver-002).

Subject:

- the order: [WO-142](../../work-orders/WO-142-outstanding-cleanup.md) as
  amended by Part E (D010, D011 and D014 are bound in
  `docs/control/plan-refutations.jsonl`);
- branch `wo-142`, with `HEAD` = `main` = `c5b2b0e7`. All work is uncommitted;
- the tree judged: `refs/dotln/checkpoint/WO-142/7` (`a0961935`) plus this
  dispatch's own projections (`current.md`, `WO-142.jsonl`, the work-order
  index).

This `resume: verify` allocated VER-002 at `2026-09-19T14:30:11.945Z`. The
prompt hook recorded the dispatch; none was repeated.

This verifier:

- changed no implementation;
- made no branch commit;
- ran no live model and published nothing;
- wrote only this report, decision WO-142-D015 and the generated projections
  that the completion refreshes.

Verifier details:

- **Harness:** Claude Code 2.1.278, read with `claude --version`.
- **Model:** `claude-opus-5[1m]`, as this session exposes it.
- **Effort:** `CLAUDE_EFFORT` reads `xhigh`, and every hook journal row of this
  session records `effort: xhigh`. No `ultra` or `ultracode` selection was made
  in this session.
- **Effective readback:** no effective-model or effective-effort readback
  channel is claimed.
- **Executor attestation under verification:** codex-cli 0.155.0,
  `gpt-6-astra`, `xhigh` (subagents, raw `ultra`), `codex-session-readback`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"xhigh","source":"operator-attested"}

## Instrument disclosure

Several instruments under verification also recorded this verification. Every
claim that depends on one says so.

- **The prompt hook (row A4).** Its dispatch of this `verify` is the primary
  live evidence for VER-001 F1's repair.
- **The observed-facts block (row B2).** It journaled this session's own
  background tasks. It is the live evidence for B2(a) and (c), and for this
  report's F1 and N2.
- **The live-gate guard (row B3).** It judged my read commands during my own
  gates. Those admissions and refusals are quoted as live evidence.
- **The subagent counter (WO-139).** It counted this verification's four
  helpers: 4 of cap 20, exact-observed.
- **Prune (row D1).** I ran it on the real checkout, in preview mode only.

The harness flagged two helper reports for instruction-shaped text: quoted
`task-notification` tags and a settings path, cited as evidence. That text
was treated as data, and nothing in it was acted on.

## Method

- **Root checks.** This session ran both product gate selections and the
  document gate under Node 26.9.0. It probed the live-gate guard during its
  own gates and read its own journal. It also stopped one background task on
  purpose, ran the real-checkout prune preview with full listings before and
  after, and reviewed the repair's change set: 129 files between checkpoints
  3 and 7.
- **Helpers.** Four read-only helpers (H1–H4), one per area, re-observed each
  repaired claim in their own copies of a snapshot, which equals checkpoint 7.
  They ran the named checks and mutated each repaired subject to show that its
  check fails. Comparison builds used the activation commit `cb932c84` and
  VER-001's subject, checkpoint 3 (`320ef91e`).
- **Isolation.** No helper wrote to the real worktree, and none spawned
  descendants.
- **Transcript scans.** Scans of local Claude Code transcripts printed only
  keys, discriminators, status values, versions and counts. They excluded the
  intake-image project directory.
- **My own reproductions.** I reproduced each blocking finding before filing
  it: F1 with the real build and a status count across 2,101 transcripts, and
  F2 with the activation and current guard side by side. I also reproduced
  N1, N2, N4, N5, N6, N7, N8 and D015 myself. Findings that rest on helper
  evidence alone say so.

## Executed checks

| Command or observation | Result |
| --- | --- |
| This session's `resume: verify` dispatch, recorded by the hook (journal) | `{"action":"verify","recorded":true}` at 14:30:12.427Z. The UserPromptSubmit hook took 1,343.78 ms against the 12 s spawn limit |
| `node scripts/work-orders.mjs index --check`, real worktree, right after the dispatch | PASS "is current", 0.475 s. VER-001 measured 15.60 s |
| **`npm test`, Node 26.9.0 (14:33:00Z–14:34:23Z)** | **21 passed, 0 failed, 84.02 s, 65 fresh tasks, exit 0** |
| **`npm test -- --review`, Node 26.9.0 (14:34:58Z–14:40:10Z)** | **35 passed, 0 failed, 310.47 s, 79 fresh tasks, exit 0.** Harness 137.28 s, process-debt 57.91 s, skeleton 63.75 s, worktree 64.17 s |
| **`npm run test:docs`** | **19 passed, 0 failed, 15.78 s** |
| `git diff --check`; `git diff --cached --check` | both clean, exit 0 |
| Live-gate probes during the `--review` gate | Admitted: `grep -R -l --color=never`, `grep -C1 -m 1 … \| wc -l`, `git --no-pager --no-optional-locks -c core.fsmonitor=false status --short`, and the same prefix with `log --no-ext-diff --no-textconv`. Refused: `-c core.fsmonitor=true`; `diff … --output=<worktree path>`; `log -p`; `sed -n '1w <path>'`; `tee <path>`; a redirect into the worktree. No probe file was created |
| Live-gate probes during the plain gate | Admitted: `grep -rn`, `grep -A3 -m 1`, `head -n5`, `head --lines=2`, `ls --color=never`, `cat --number`, `sed -n 1,2p`. Refused, matching 07's documented bounds: `git --no-optional-locks …` without `--no-pager`, and `git --no-pager log` without `--no-ext-diff --no-textconv` |
| `node scripts/harness.mjs prune` (real checkout, preview), with a full `find` + `stat` listing (path, size, mtime, inode) of `.runtime/harness`, `docs/control/local/harness` and `<git-common>/dotln` before and after | Exit 0. 5,089 entries, identical listings, identical JSON. Candidates: 1 unpinned snapshot (2,747,865 bytes), 15 advisory markers from earlier sessions and the dead cache (97,838,994 bytes); 100,589,334 bytes in total. This session's 4 markers (14:31–14:40Z) were retained as "session is current, reserved or live" (O10) |
| External lockfile diff, `c5b2b0e7` → subject | Only `typescript` 5.4.5 → 7.0.2, its 20 optional `@typescript/typescript-<platform>` 7.0.2 packages (all Apache-2.0), `@types/node` 22.20.1 → 26.6.2 and its `undici-types` 6.21.0 → 8.9.0 |
| The order's bytes against the last `PlanExecutionAmended` row | The raw file (53,467 bytes) differs from the row's `orderLength` 53,416 because the binding hashes normalized text. H1 reproduced the normalized length and hash match for D014, and a one-word edit fails `plan check` |
| `node scripts/harness.mjs usage <session>` at 15:05:50Z | see Limits L6 |

The helper runs (Node 26.9.0; all counts are pass/total):

| Helper | Result |
| --- | --- |
| **H1: lifecycle and integration** | `test-process-debt` 76/76, `test-plan-refutation` 42/42, `test-work-orders.sh` 19/19, `test-resume.sh` 10/10 embedded, `test-worktree.sh` 6/6, `test-release.sh` prepare + 44/44 cases, `test-runner.test.mjs` 29/29, `test-license-surfaces` 9/9, `meta --check`, `publication:check`, `refute-plan check` (19 receipts), `check-surfaces --local` 44 PASS, `release prepare --local`: "v0.32.0 remains current" |
| **H2: harness observation** | `test-observed-facts` 16/16, `test-harness.mjs` 73/73, subagent-budget 10/10, D1 fixtures 6/6, `harness check` "31 generated surfaces" |
| **H3: guard and test pins** | kernel 102/102, resident 19/19, entropy-reducer 11/11, compiler 26/26, feedback 5/5, worker 23/23 |
| **H4: Node 26 and TypeScript 7** | Beacon, control-beacon, senses and worker tests 58/58. Parser consumers 40/40. Evidence `--check` for feedback, verification, authority, artifact identity and console fixtures (5/5): all pass. Build: exit 0, 0 bytes of stderr |

## Acceptance criteria

| # | Criterion | Verdict | Evidence |
| --- | --- | --- | --- |
| 1 | Rows file complete, one state each, reverted-subject evidence, returns bounded | **not met** | All 51 groups plus E1 and E2 are listed. The single return (B12(b)) is justified and owned (D002). But B2 and B3 are recorded `fixed` while F1 and F2 hold. rows.md B3's "unsafe options remain refused" is inaccurate for the thirteen harmless forms. rows.md A16's dead-import claim fails again for a new import (N8). Every other repaired row's check fails when its subject is reverted (see the next section) |
| 2 | Feed fixture both forms; B17 rule for three roles; untriaged count recorded | **met** | The B17 sentence is byte-identical in all six generated role files and names the JSON `followup` string or `reopens` object. Restoring the prose wording fails the fixture (H2). `plan followups` read 424 total, 87 pending, 6 untriaged before this report (H1), matching D014. D015 adds one follow-up by design: after `npm run meta` the register reads 425 total, 88 pending, 7 untriaged. The merged-tree figure belongs to final review |
| 3 | A title-stamp-only order stays carried | **met** | Unchanged since VER-001; the plan-refutation suite passes 42/42 in both gates |
| 4 | `harness check`; two-advisory and read-list fixtures; four refusals still refuse | **met, with F2** | 31 surfaces. The B1 and N5 fixtures pass, and reverting either identity rule fails its test (H2). The B3 fixture pairs pass. The second-writer, live-gate, planning-branch and cap tests pass (73/73). The read-list regression is F2 |
| 5 | Prune preview deletes nothing; apply removes exactly the listed paths; real listing recorded | **met** | Fixtures 6/6. H2 compared a whole-copy listing of 4,792 entries before and after a preview: identical. My real-checkout preview left an identical 5,089-entry listing. Live session markers were protected (N8 of VER-001, observed live). O11 applies |
| 6 | Write-backs land; `publication:check` passes | **met, with N6** | The 07, 03, 05 and component README write-backs are present. `publication:check` exits 0 (H1 and the document gate) |
| 7 | `npm test`; `test:docs`; `git diff --check`; no new dependency except E1's; no new suppression | **met at this subject** | Both gates and the document gate are green under Node 26.9.0, and the diff checks are clean. The lockfile adds only E1's packages. H3 scanned all 31,732 added lines: six hits, all quoted evidence data in `selfhost-verification.jsonl`, no directive. No `tsconfig*.json` changed. No `.skip`, `.only` or `.todo` added. Final review still owes its own gate |

**Part E.** E1 is met, with N6, N7 and O1–O4. E2 is met:

- WO-084's ledger, Resolutions, lineage README and `scripts/lineage.mjs` hash
  equal to `c5b2b0e7`.
- Outside WO-142's own directories, only `docs/evidence/current.json` and the
  C7 refutations README changed.
- All 418 register entries from `main` and all 419 from checkpoint 3 are
  present. 413 are byte-identical, and 5 carry appended revisions only.
- `docs/control/plan-refutations.jsonl` is append-only: `main`'s nine lines
  are an exact byte prefix.

## VER-001 findings

| VER-001 item | Status now | Evidence |
| --- | --- | --- |
| **F1 (A4 ★)** | **resolved** | The live dispatch was recorded in 1.34 s and the index is current. Real-data index output is byte-identical with VER-001's readers swapped in, with 68 Git calls against 374. The hook's exact `spawnSync` call takes 1.25–1.29 s for verify, fix and final-review (5.2–5.5 s at 40 ms per Git call). With checkpoint 3's readers at 40 ms, the same hook times out at 12.5 s with the event appended. Reverting the batching fails the dispatch fixture: 18/19 (H1) |
| **F2 (B2 ★)** | **partly resolved; see F1** | (a) Live: the journal holds `backgroundTask` rows for my three background Bash tasks and four helper agents. Every agent reached `completed` in the prompt facts. (b) Queue and attachment notices complete tasks live, but `killed` does not (F1). (c) The scope stamp is live (every row carries WO-142 / verification), and deleting it fails the generated-hook regression (H2) |
| **F3 (B3)** | **partly resolved; see F2** | The seven quoted forms are `[]` again, and copying checkpoint 3's `harness-command.ts` back fails both B3 tests (H3). Thirteen further activation-admitted forms are still refused |
| **F4 (B14)** | **resolved** | The exact original regex is back. All seven VER-001 and alias/destructure mutants planted in `core.ts` fail the current test; at activation the alias and destructure forms passed (H3). One pre-existing form is boarded as D015 |
| N1 | resolved | 35 imports in `scripts/worktree.mjs`, none unused (H1). A new unused import elsewhere is N8 |
| N2 | resolved | The roadmap sentence matches the equal-version path. O6 notes a wider pre-existing fetch |
| N3 | resolved, with N9 | Setext, list, blockquote, indent and closing-hash forms are refused. Checkpoint 3's `release-notes.mjs` fails `test-worktree.sh` (H1) |
| N4 | resolved | Restoring the inline predicate fails the "ledgers" fixture, and `npm run meta` refreshes a stale index (H1) |
| N5 | resolved | Putting the hook kind back into the advisory identity fails the repair test. Stop output never enters suppression (H2) |
| N6 | resolved | The `version.ts` mask is live (disabling it fails 3 tests), and the dead masks are gone. Removing `.env` fails the credentials-advisory pin (H3) |
| N7 | owned by D012, extended by N4 | — |
| N8 | resolved | Live: this session's markers were retained. Removing `CLAUDE_CODE_SESSION_ID` fails the repair fixture (H2) |
| N9 | resolved | Moving the class back fails the structural test (31/32); the 751-line bound is unchanged (H3) |
| N10 | resolved | `UltraCode`, `Ultra`, `ULTRA CODE` and `ultracode` all normalize to xhigh/subagents with the raw spelling kept. A case-sensitive revert fails 2 tests (H3). O9 |
| N11 | resolved | The kernel, compiler and skeleton README headings are version-free and carry the B11 and B16(b) sentences (H3) |
| N12 | resolved | rows.md labels its `/tmp` records as session-local and lists what is source inspection only |
| N13 | resolved except B3 | The B2, A16 and B14 claims are corrected. B3's claim is inaccurate (criterion 1) |
| N14 | resolved | See criterion 2 |

The other rows keep VER-001's verdicts. Their owning suites pass in both
gates and in H1–H4's direct runs. The repair's documentation edits (the
README setup line, products 02, 06 and 07, the component READMEs) were read.
The C1 block holds 13 sentences and names four refusals.

## Blocking findings (route to repair)

### F1 — Row B2 ★: a native `killed` notice leaves the task `dispatched` with growing elapsed time

- **Criterion:** row B2(b)'s required result, "corrected with a regression",
  for the defect "a task whose completion notice arrived keeps state
  `dispatched` with growing elapsed". Also criterion 1, and D013's reopening
  condition: "a real envelope … does not satisfy its new regression".
- **Observed:**
  - The notice parser accepts only
    `/<status>(completed|failed|stopped)<\/status>/`
    (`packages/skeleton/src/observed-facts.ts:465`). The terminal lists at
    `:130`, `:181` and `:201` also omit `killed`. No test or document mentions
    `killed`.
  - Claude Code emits `killed`. I counted notice statuses across 2,101 local
    DotLn transcripts, counts only: completed 1,201, failed 27, stopped 6,
    **killed 14**, and 134 with no status (event notices, correctly ignored).
    The `killed` deliveries carry versions 2.1.270, 2.1.272, 2.1.274 and
    2.1.277; nine are queue rows, which carry no version. H2 attributed them
    to 9 distinct tasks: Agent 4, Bash 4, Monitor 1.
- **Reproduction** (real build, synthetic notices of the observed shapes; the
  script is H2's `h2-probe.mjs`, run by me against the worktree's `dist`):
  - Bash dispatch journaled, then a queue-enqueue `killed` notice: at +1 h the
    result is `dispatched/elapsed=3600000`.
  - Agent dispatch, then an attachment `killed` notice: the same.
  - Control: the same Bash case with `stopped` gives `stopped/elapsed=60000`.
- **Expected:** `killed` is a terminal state, elapsed time freezes at the
  notice, and a regression covers both envelope forms.
- **Severity:** blocking. A starred row's named defect still occurs for a real
  native form.
- **Repair direction** (not prescribed): treat every terminal status the
  harness emits as terminal, and pin each with a fixture. Consider N2 in the
  same change.

### F2 — Row B3: read-only forms admitted at activation are still refused during a live gate

- **Criterion:** the order's Design ("B3 extends an existing allowlist"),
  operator-review assumption 2 ("widens"), VER-001 F3's expected result
  ("read-only forms admitted at activation stay admitted"), and D013's
  reopening condition ("an old accepted read form still regresses").
- **Observed:** `shellWriteTargets` from the activation build (`cb932c84`)
  against the worktree's current build, run by me:

  ```text
  echo ---                                 act=[]  cur=REFUSED(null)
  echo '- item'                            act=[]  cur=REFUSED(null)
  printf '---\n'                           act=[]  cur=REFUSED(null)
  true x                                   act=[]  cur=REFUSED(null)
  grep -rA3 foo docs                       act=[]  cur=REFUSED(null)
  grep -3 foo a.md                         act=[]  cur=REFUSED(null)
  grep --exclude-dir=node_modules -r foo . act=[]  cur=REFUSED(null)
  grep -r --include=x foo docs             act=[]  cur=REFUSED(null)
  grep --regexp=foo a.md                   act=[]  cur=REFUSED(null)
  grep -ve foo a.md                        act=[]  cur=REFUSED(null)
  grep foo -                               act=[]  cur=REFUSED(null)
  ls --all                                 act=[]  cur=REFUSED(null)
  cat - a.md                               act=[]  cur=REFUSED(null)
  ```

  - The restored forms (`grep -rn`, `grep -A3`, `head -n5`, `ls --color=never`,
    `cat --number`) are `[]` in both builds. `echo hi > a.md` names its
    destination in both, and `sed -n 1w out a.md` is refused in both.
  - H3 confirmed eight of the forms end to end through the generated
    permissions hook during a synthetic live gate: admitted at activation,
    denied now. H3 also found `ls -l@`, `ls -D %s -l`, `cat -l`, `grep -Z` and
    `printf -v`.
  - None of these options writes or runs a program. `echo ---` is a common
    separator inside compound read commands.
  - `scripts/test-harness.mjs:4365-4369` asserts that unrecognized `echo` and
    `printf` options return `null`. The repair will need to change that
    assertion deliberately.
  - `tail -n5` (attached count) is refused while `head -n5` is admitted. tail
    was not admitted at activation, so this is not a regression; it belongs
    to the same repair.
- **Expected:** every form the activation build admitted stays admitted
  unless it can write or execute, and the new programs keep bounded flags.
- **Severity:** blocking, because VER-001 F3 (blocking) is still not fixed as
  a class.
- **Repair direction:** derive the admitted vocabulary from the activation
  classifier's behavior for `echo`, `printf`, `cat`, `pwd`, `true`, `false`,
  `ls`, `head` and `grep` (including combined short flags and flags that take
  values). Add an activation-parity fixture that cannot pass with a narrower
  list, and correct rows.md B3 and 07's read-list paragraph.

## Non-blocking findings (route to the same repair: fix in the row, or board in decisions.md with a `followup`)

- **N1 (B2(a)): a Bash command moved to the background by its timeout is
  never journaled.**
  - Its result carries `backgroundTaskId` and `timedOutAfterMs` but no
    `run_in_background` input. `observeBackgroundTool` requires
    `run_in_background` or `isAsync` (`observed-facts.ts:112-120`).
  - The later notice creates a task with no dispatch time:
    `completed/elapsed=null` (probe 3, reproduced).
  - H2 counted 9 such results and 16 notices in local transcripts.
- **N2 (B2): a successful `TaskStop` is recorded as `unknown`, and elapsed
  time keeps growing.**
  - Live: at 14:57:03Z I started a background `sleep 900` and stopped it with
    `TaskStop`. Claude Code 2.1.278 wrote no task notification.
  - The PostToolUse row sets state `unknown`: the result keys are `message`,
    `task_id`, `task_type` and `command`, with no status.
  - Every later prompt's facts read "task 9 … last observed state unknown;
    elapsed since dispatch …", still growing at 588,374 ms.
  - Related edge (H2 probe 7, reproduced): a task dispatched before the
    scope's `startedAt` whose notice also precedes it stays `dispatched`
    forever, because `observedFacts` does not scope tasks.
- **N3 (B2): the cost of each hook boundary grows with notices × journal
  rows** (helper evidence only).
  - `journalRows` is re-read, and one SHA-256 recomputed per journal entry,
    for every notice (`observed-facts.ts:468-473`). This happens before the
    `seen` check, so it recurs on every UserPromptSubmit and Stop.
  - H2 measured 2.93–2.99 s per boundary at 16,636 journal rows (the largest
    real journal here) with 200 notices, and 7.41–7.45 s with 500 notices.
  - The hook's own timeout is 15 s, shared with the dispatch's 12 s spawn.
- **N4 (B3/D012): the documented Git prefix does not stop `git diff` from
  writing the index, and admitted Git forms run programs from repository
  config.**
  - 07 (lines 1658-1661) says the prefix avoids the index refresh and
    fsmonitor "for status/log/diff".
  - Reproduced here with Git 2.55.0: after a `touch`, the prefixed
    `git diff --no-ext-diff --no-textconv --stat` rewrote `.git/index`. The
    mtime moved from 1789830409 to 1789830410, while the prefixed `status`
    left it unchanged. The same form is admitted as `[]`, and `.git/index` is
    a gate input.
  - H3 also observed that a `.gitattributes` clean filter runs under the
    prefixed `diff`, and that `gpg.program` runs under admitted `log` forms
    when `log.showSignature=true`.
  - Correct 07. Either close these paths or extend D012's follow-up to name
    them; its proposed prefix alone does not cover `diff`.
- **N5 (B17 evidence): the cold-start evidence is stale.**
  - `docs/evidence/WO-142/harness-context.json` and the evidence README
    (lines 72-75) record 21,076 / 18,118 / 19,336 bytes (+227).
  - `measureColdStarts` now reads 21,176 / 18,218 / 19,436 (+327), matching
    `repair-001.md`. Headroom: executor 3,400, verifier 2,262,
    reviewer 1,044.
  - rows.md B17 cites the stale file.
- **N6 (E1): active documents still describe the TypeScript 5 dependency.**
  - `docs/LEGAL.md:22` names TypeScript `5.4.5` as the skeleton runtime
    dependency and records license observations only for the old package
    set.
  - ADR-0002 (`docs/decisions/0002-kernel-first-agentic-core.md:58-59`,
    `:108-117`) records TypeScript 5.4.5, `@types/node` 22.20.1 and "no new
    version".
  - `packages/skeleton/README.md:673` cites ADR-0002 as the record of the
    7.0.2 parser dependency.
  - A dated note in each, citing D010 and D011, would close this. The
    reviewer's boy-scout rule would also admit it at final review if nothing
    else routes it.
- **N7 (E1, C10): a manual corpus check listed in `corpus/README.md:38-40`
  can no longer pass on any toolchain.**
  - `node --test corpus/harness/wo101-generators.test.mjs` fails test 5 under
    Node 26.9.0 (`v22.2.0` pinned by `corpus/harness/manifest-lib.mjs:22-29`),
    reproduced.
  - H4 found it fails under Node 22 as well, on TypeScript `5.4.5` against
    `7.0.2`. Checkpoint 3 under Node 22 passed 5/5.
  - Either mark the pin as the recorded WO-101 toolchain or handle it
    explicitly.
- **N8 (A2/A16 class): row A2 left an unused import.**
  `scripts/lib/plan-direct.mjs:6` imports `hashParts` and never uses it. At
  `cb932c84` the file used it (2 occurrences). rows.md A16's "dead
  exports/imports removed" is inaccurate again.
- **N9 (VER-001 N3 residual, low)** (helper evidence only). The
  reserved-heading guard still admits these generated titles:
  - a multi-line setext heading;
  - a backslash-escaped `WO\-123` title;
  - an `&mdash;` entity title;
  - an emphasized `**Derived from the diff**` heading.

  A 4-space-indented code line is over-refused, as it was at checkpoint 3.
- **N10 (evidence prose).** `docs/evidence/WO-142/README.md:25-26` ends a
  sentence with a semicolon before "The original executor…", and repeats
  "Verification and final review remain separate dispatches" in the next
  sentence.

## Boarded defect (met here, outside WO-142's rows)

- [WO-142-D015](../../evidence/WO-142/decisions.md#wo-142-d015--board-up-the-optional-chaining-gap-in-the-kernel-coupling-tripwire-met-during-ver-002)
  covers the kernel AC3 coupling check, which misses an optional-chained
  reserved-state read.
  - A planted `(context.state as { policy?: unknown } | undefined)?.policy`
    passes the current test (20/20) and the activation test (18/18).
  - The cast and computed forms fail the current test.
  - Some other forms H3 reported as missed were caught in my run, so only the
    reproduced form is boarded.

## Observations

- **O1 (E1 beacon).**
  - On Node 26, 5,955 of 6,013 accepted writes had mtime above the target
    (0–499 ns), and 0 did on Node 22 (H4).
  - For those beacons, `beaconAge` reports clock skew within the written
    millisecond. The reactor's round-up cadence origin moves the stale
    boundary 1 ms later.
  - Product 02 and D011 say this "can" happen; on Node 26 it happens on about
    99% of writes.
  - H4 ran 6,013 and 20,013 random writes: no refusals and no reader
    mismatches. The failure-path tests discriminate under four mutations.
- **O2 (E1 runner).** Node 26's default reporter is `spec`, so suites started
  through `node()` rather than `nodeTests()` show "waiting for the next case
  report". It appears 7 times in my `--review` gate log. This affects the
  progress display only.
- **O3 (E1 parser).** TypeScript 7's native parse is slower on very large
  files: 10 MB takes 2.8 s against 0.55 s, and 21 MB takes 11.4 s against
  1.1 s (H4). The boundary parses both texts. A 29-case tricky-input corpus
  matched TypeScript 5, except a conflict-marker file where 7.0.2 is stricter.
- **O4 (E1 tests).** The three test consumers call the deprecated
  `updateSnapshot({ openProject })`; the product uses `openProjects`.
- **O5 (A4).**
  - A missing batch object reports "Git blob batch has invalid framing"
    without naming a tag or path.
  - The batch shares one 16 MiB buffer across all tags (about 13× headroom on
    real data). A 2×9 MiB scratch case fails behind a pre-existing
    Buffer-`trim` TypeError.
  - Dispatch still costs one `ls-tree` per tag, about 260 tags at 40 ms per
    Git call (H1).
- **O6 (N2).** Every close path fetches the latest remote tag into
  `refs/tags` (`release.mjs:1763-1768`, pre-existing since `cb932c84`). The
  docs attribute a local tag restoration only to equal-version recovery.
- **O7 (feedback evidence).** Revision 002's independent verifier episode used
  the same harness and model family (`codex-cli-exec`, `gpt-6-astra`) as the
  repair actor; its effective model and effort are recorded `unknown`.
  Revision 001's bytes before 002 existed cannot be checked, because both
  first appear in checkpoint 6.
- **O8 (PR body).** The executor pre-wrote `docs/final-reviews/WO-142/PR.md`
  (meter cutoff 14:15:25Z). Nothing treats it as a final-review record: the
  index shows none, and `nextFinalReview` counts only `FINAL-NNN.md`. The
  reviewer's procedure owns that file.
- **O9 (B15).** Effort normalization lowercases but does not trim
  (`" ultra "` stays raw). Launch and attestation agree.
- **O10 (prune listing).** My first before/after digest pair differed, with
  equal entry counts, while four helpers' hooks were writing to the session
  journal. I did not keep that listing. The second pair, full listings
  diffed line by line, was identical. The non-mutation claim rests on that
  second pair and on H2's fixture copy.
- **O11 (criterion 5).** The operator has not yet run the real-checkout
  listing. The executor and two verifiers ran previews only, with 0 bytes
  deleted.
- **O12 (instrument).** During my gates the guard also refused helpers'
  opaque commands in the scratchpad ("write may change gate inputs"). This is
  pre-existing conservative behavior; helpers waited for the gate to end.

## Limits

- **L1 (no live spend).** No worker, planning refutation or feedback audit
  was run. Revision 002 was judged through its `--check` and recorded
  episodes.
- **L2 (B4 schema).** Live acceptance of B4's `anyOf` schema by either CLI
  is still unobserved (VER-001 L4).
- **L3 (worker probe).** `scripts/probe-worker-hosts.mjs` was not run.
- **L4 (helper evidence).** Findings and observations marked "helper
  evidence only" (N3, N9, O1, O3, O5) were not re-run by me. Their scripts
  are session-local under the scratchpad and are not committed reproduction
  commands.
- **L5 (clean room).** No `docs/intake/` content was opened. Transcript scans
  printed shapes and counts only, and excluded the intake-image project
  directory.
- **L6 (usage).** Root dispatch usage at 15:05:50Z (source
  `claude-transcript-message-usage`, scope dispatch):
  - input 198, cached input 17,855,138, cache writes 295,656, output 92,423,
    total 18,243,415 tokens;
  - reasoning tokens and cost unknown;
  - subagents 4 of cap 20, exact-observed.

  The helpers reported 1,043,949 subagent tokens in total (H1 300,864,
  H2 229,811, H3 231,118, H4 282,156) and 546 tool calls.
