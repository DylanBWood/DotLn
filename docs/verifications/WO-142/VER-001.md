# WO-142 VER-001 — independent verification of the outstanding cleanup

**Verdict: fail.** Four blocking findings route to repair. Two of them are
starred rows that pass their fixtures but do not hold in real use:

- **F1 (row A4 ★).** The index refresh now runs on every transition. That
  pushes the harness's phrase dispatches past their 12 s timeout. This
  verification's own `verify` dispatch was killed after its event was
  appended, and the work-order index is still stale.
- **F2 (row B2 ★).** Background Bash dispatches and completion notices are
  still never journaled in Claude Code. The fixtures use a result shape and a
  notice form that the harness did not emit here.
- **F3 (row B3).** Seven read-only forms that the live-gate guard admitted at
  activation are now refused, including `grep -r`, `grep -A`, `head -n5` and
  `ls --color`.
- **F4 (row B14).** The replacement AC3 coupling check accepts reserved-state
  reads that the removed assertion refused, so "none weakened" does not hold.

Everything else holds and reproduces, with some caveats:

- **Other rows.** The other 44 of the 51 row groups meet their required result.
  Three are partly met, with non-blocking findings: A14, A16 and B6. Four hold
  in substance but have only non-blocking gaps in their negative evidence:
  A5, A13, B13 and A17. A7's not-reproduced state and B12(b)'s return are
  justified.
- **`npm test`.** Green under Node 22.2.0: 21 suites, 0 failed, 451.76 s.
  Under Node 26.9.0 it fails three suites, and so does the activation commit
  (boarded as WO-142-D007).
- **`npm run test:docs`.** Red on the subject as it stands. The only cause is
  F1's stale index.
- **Non-blocking findings.** N1–N14 go to the same repair.
- **Boarded defects.** Three pre-existing defects met here are recorded as
  [WO-142-D007 to D009](../../evidence/WO-142/decisions.md#wo-142-d007--board-up-the-node-26-beacon-timestamp-failure-met-during-verification).

Subject: [WO-142](../../work-orders/WO-142-outstanding-cleanup.md), branch
`wo-142`. `HEAD` is the activation commit `cb932c84` (equal to `main`); all
work is uncommitted and preserved by `refs/dotln/checkpoint/WO-142/3`
(`320ef91e186c3e487f6cbcab9460bab026efd625`). The subject is:

- the order's rows A1–A23, B1–B17, C1–C10 and D1, as filed;
- acceptance criteria 1–7;
- the executor's evidence (`docs/evidence/WO-142/rows.md`, `README.md`,
  `decisions.md` D001–D006 and the counterfactual summaries).

This `resume: verify` allocated VER-001 at `2026-09-19T05:35:35.556Z`.
Allocation happened in an earlier session whose dispatch the harness killed
(see F1). This session reclaimed that session's writer reservation, and the
prompt hook reported the dispatch as already recorded, so none was repeated.

This verifier:

- changed no implementation;
- made no branch commit;
- ran no live model and published nothing;
- wrote only this report, decisions WO-142-D007 to D009 and the generated
  indexes they refresh.

Every gate below ran at code identity
`ecb7a7b91a83c5fbf709df78cab10fa119915a71190ce00f7d2afa5c7c28610d`. No
executor `npm test` row exists; the executor reserved the product gate for
final review.

Verifier details:

- **Harness:** Claude Code 2.1.278, read with `claude --version`.
- **Model:** `claude-opus-5[1m]`, as this session exposes it. The operator
  selected "Opus 5 (1M context)".
- **Effort:** the operator selected `ultracode` for this session.
  `CLAUDE_EFFORT` reads `xhigh`. The resume command records it as `xhigh`,
  mode subagents.
- **Effective readback:** no effective-model readback channel is claimed.
- **Executor attestation under verification:** codex-cli 0.155.0,
  `gpt-6-astra`, `xhigh` (subagents, raw `ultra`), `codex-session-readback`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"xhigh","mode":"subagents","raw":"ultracode","source":"operator-attested"}

## Instrument disclosure

Several instruments under verification also recorded this verification. Every
claim that depends on one says so.

- **The hooks.** They are the regenerated WO-142 bundle. They journaled this
  session, and their observed-facts block (row B2) reached every prompt
  boundary.
- **The live-gate guard (row B3).** It refused several of my read-only
  commands while my own gates ran. Those refusals are quoted as live evidence
  in F3 and O4.
- **My gate rows.** Three `npm test` rows were written by the unchanged
  gate-evidence path.
- **The prompt hook's dispatch (row A4).** Its failure in the previous session
  is F1's primary live evidence.
- **The subagent counter (WO-139).** It counted this verification's 10
  workflow agents: exact-observed, 10 of cap 20.

The harness flagged one helper's structured output for instruction-shaped
text. That helper quoted `task-notification` tags and settings keys as
evidence for B2. The text was treated as data, and nothing from it was acted
on.

## Method

- **Root checks.** This session ran the product gate three times and probed
  the live-gate guard during its own gates. It inspected its transcript
  structure and ran the real-checkout prune preview.
- **Helpers.** Five helper agents, one per row group, re-observed each defect
  at the activation commit. Each checked every clause of the required result,
  ran the named check, and reverted each fixed subject in its own disposable
  clone. Each clone was the activation commit overlaid with the current tree,
  excluding `docs/intake/` and local control state.
- **Refuters.** Five further agents tried to refute every non-observation
  finding in a fresh clone. They either confirmed it, downgraded it, or refuted
  it (one refuted, F9 of the harness group).
- **Node and reads.** All helper runs used Node 22.2.0. The real worktree was
  only read by helpers.
- **My own reproductions.** I reproduced each blocking finding myself before
  filing it: F1 with the real journal and timing, F2 with this session's
  transcript and facts blocks, F3 with the activation and current classifier,
  and F4 with the diff of the removed assertion.

## Executed checks

| Command or observation                                                                                                                                                    | Result                                                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm test`, Node 26.9.0, inside the OS sandbox (05:39:38Z–05:40:51Z)                                                                                                      | 18 passed, 3 failed (resume, worktree, skeleton), 72.18 s, 65 fresh tasks, exit 1                                                                                                                                                                               |
| `npm test`, Node 26.9.0, outside the sandbox (05:41:35Z–05:42:49Z)                                                                                                        | 18 passed, 3 failed, 73.55 s, exit 1. Every failing case is a beacon size/mtime check (L1)                                                                                                                                                                      |
| `node --test packages/skeleton/dist/test/beacon-fs.test.js` on the activation commit                                                                                      | Node 26.9.0: 1/7 pass, 6 fail with "filesystem did not preserve the exact beacon size/mtime". Node 22.2.0: 7/7 pass                                                                                                                                             |
| `utimesSync` probe, Node 26.9.0                                                                                                                                           | `1726700000123` ms reads back as `1726700000122` ms. Whole-second values are exact                                                                                                                                                                                         |
| **`npm test`, Node 22.2.0, outside the sandbox (05:44:58Z–05:52:30Z)**                                                                                                    | **21 passed, 0 failed, 451.76 s, 65 fresh tasks, exit 0**                                                                                                                                                                                                       |
| `node scripts/work-orders.mjs index --check` (real worktree, read-only)                                                                                                   | `docs/work-orders/README.md is stale at line 3`. Took 15.60 s real time                                                                                                                                                                                              |
| `npm run test:docs` (helper copy of the subject as it stands)                                                                                                             | 12 passed, 5 failed: `FAIL index`, plus four suites not run behind it. After one uncapped index refresh, `index --check` passes                                                                                                                     |
| `node scripts/harness.mjs prune` (real checkout, preview), with a SHA-256 inventory of `.runtime/harness`, retained lanes, advisory markers and `git-common/dotln/suite-success` taken before and after | Exit 0. Before and after: 3,315 entries, 108,938,295 bytes, digest `a7c8def0…a218a3`, identical. Candidate: the dead cache, 97,838,994 bytes. Retained: four snapshots (three pinned; one "another live or unknown writer", which is this session's reservation) and seven advisory markers |
| `node scripts/harness.mjs check` (helper copy)                                                                                                                            | exit 0, 31 generated surfaces                                                                                                                                                                                                                                   |
| `git diff --check`                                                                                                                                                        | clean, exit 0                                                                                                                                                                                                                                                   |
| `git log --oneline main..HEAD`                                                                                                                                            | empty                                                                                                                                                                                                                                                           |
| Fenced paths (`reactor.ts`, the ledger files, `docs/lineage/README.md`, `scripts/lineage.mjs`, the capability table, reports and receipts)                                     | no diff. The only change nearby is `docs/planning/refutations/README.md` (C7)                                                                                                                                                                                         |
| `npm run publication:check`, `npm run meta -- --check`, `release check-surfaces --local` (helper copy)                                                                    | all exit 0                                                                                                                                                                                                                                                      |
| Helper suites (Node 22.2.0)                                                                                                                                               | `test-harness.mjs` with `test-observed-facts.mjs` 78/78; runner 29/29; work-orders 19/19; license 9/9; subagent-budget 10/10; kernel 102/102; board 21/21; resident 8/8; the scenario and reactor-slices tests 31/31; all shell suites exit 0 |
| `npm run plan -- followups` (helper copy)                                                                                                                                 | 419 total, 83 pending, 2 untriaged. This matches decisions.md D006                                                                                                                                                                                                       |

## Acceptance criteria

| #   | Criterion                                                                                                                                   | Verdict                   | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Rows file complete, one state each, reverted-subject evidence per fixed row group; returns bounded and owned                                | **not met**               | The rows file lists all 51 groups (B12 as (a) and (b)): 50 fixed, 1 not-reproduced, 1 returned. The single return (B12(b)) is not starred and is owned by D002 / FUP-4f8cd7989607ad3f. But four "fixed" states are false in real use or break their required result (F1–F4). The negative evidence is also thinner than the file implies (N12). The fixtures for A4 and B2 pass because they exercise conditions the harness does not produce. |
| 2   | Feed fixture both forms; B17 rule for three roles; untriaged count recorded                                                                 | **met**                   | Reverting the collector fails "ordinary decisions are records, not actions" (expected 0, actual 1). The B17 sentence appears in all six generated role files, and the fixture fails with the activation `contributor.ts`. The count reproduces: 419 total, 83 pending, 2 untriaged. The merged-tree observation belongs to final review. Adding D007–D009 adds three pending follow-ups by design; after `npm run meta` the register reads 422 total, 86 pending, 5 untriaged. O5 notes that the role text does not name the `followup` key |
| 3   | A title-stamp-only order stays carried; nothing carried is evicted                                                                          | **met**                   | Real-data probe in a helper clone: activation code judges `["WO-142"]` and the current code judges `[]`, with 93 carried. A direct receipt filed there carries WO-142 with receipt 019's verdict, and `refute-plan.mjs check` admits 20 receipts. The fixture's legacy assertion does not discriminate (O1); the real-tree gate does |
| 4   | `harness check`; two-advisory and read-list fixtures; the four refusals still refuse                                                        | **met**, with F3          | 31 surfaces. B1 fixture: two identities × 20 repeats, each shown once. B3 fixture pairs pass. Second-writer, live-gate, planning-branch and cap tests pass (78/78). The criterion is met, but the read-list change also regresses admitted reads (F3) |
| 5   | Prune preview deletes nothing; apply removes exactly the listed paths; real listing recorded                                                | **met**                   | Fixtures: 5/5. A helper's own temp-root scenario gives an identical 35-entry listing across the preview, and `--apply` removed exactly the 4 listed paths. My real-checkout preview shows an identical 3,315-entry inventory. D006 records the executor's real preview; the criterion says the operator runs it (O8) |
| 6   | Write-backs land; `publication:check` passes                                                                                                | **met**, with N11         | 07 has the two candidate "Implemented in WO-142" paragraphs, the Discipline advisory-key and read-list text, and the Documentation-freshness rule. Products 03 and 05 are cleared for B9, and the skeleton and compiler READMEs are updated. `publication:check` exit 0. The kernel README still names v0.5.0 (N11) |
| 7   | `npm test` green; `test:docs` green; `git diff --check` clean; no new dependency; no new suppression                                        | **not met at this subject** | `npm test` is green under Node 22.2.0 (L1). `test:docs` is red because of F1's stale index. `git diff --check` is clean. The lockfile's `packages` key set is unchanged, with only workspace entries edited. No suppression is added in diff lines or the two new files |

## Row outcomes

Verdicts are this verification's; the executor's states are in the
[rows file](../../evidence/WO-142/rows.md). The "Revert fails" column says
whether the named check fails when its subject is reverted.

| Row                | Verdict                         | Revert fails         | Key evidence                                                                                                                                                                                  |
| ------------------ | ------------------------------- | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1 ★               | met                             | yes                  | Collector revert fails the "records, not actions" fixture. Removing the history guard fails the byte-equality check. 412 of 417 register entries are byte-identical; the other 5 gain appended revisions only |
| A2 ★               | met                             | yes (fixture + gate) | The raw-title hash fails the WO-902 carried fixture. Removing legacy acceptance fails the real 19-receipt gate                                                                                  |
| A3                 | met                             | yes                  | Independent slugger: 47/235 fragments unresolved at activation, 241/241 resolved now. Removing the resolution loop fails `meta --check`                                                                         |
| A4 ★               | **not met in real use (F1)**    | fixture only         | The fixture spawns resume with no timeout, in a repository with no tags                                                                                                                                  |
| A5                 | met                             | links yes; predicate no (N4) | Existence-gated links render correctly. Restoring the drifted inline predicate fails nothing                                                                                           |
| A6                 | met                             | yes                  | Unassigned title fixture                                                                                                                                                                      |
| A7                 | not-reproduced, confirmed       | n/a                  | All five references exist at `cb932c84` and name the owning order. The retained WO-042 tool's check was already broken (D008)                                                                        |
| A8 ★               | met                             | yes                  | Stray and disposable pair. The skipped set is exactly `paths.mjs`'s activation basenames                                                                                                           |
| A9                 | met                             | yes (all three)      | The length pin catches a dropped 24th row. The matcher is the generic prefix (O3)                                                                                                                |
| A10                | met                             | yes                  | The final-review repair records `FINAL-002`, not `VER-003`. No code reader of the field exists                                                                                                           |
| A11                | met                             | 5 of 7 pinned        | Cascade, progress, ordinal, checkpoint and stale-temp messages are pinned; the `--work-order` hint and the candidate wording are not (O6)                                                                       |
| A12                | met                             | yes                  | Both halves fail on revert                                                                                                                                                                       |
| A13                | met                             | yes                  | Symlink fixture. Two corpus guards remain outside the ten (D009)                                                                                                                         |
| A14                | partly met (N2, N3)             | yes                  | FAIL line, advisory and 18 ATX heading cases. The roadmap sentence is stale, and setext and list-item headings pass                                                                                     |
| A15                | met                             | yes (11 of 12 reverted) | Each reverted item fails its intended assertion                                                                                                                                             |
| A16                | partly met (N1)                 | no committed check   | The named unused `readFileSync` import is still unused. "Source-definition count assertions" do not exist                                                                                             |
| A17                | met                             | yes                  | The compiler keeps its own narrower pattern (O7)                                                                                                                                              |
| A18                | met                             | yes                  | The hint is printed but not asserted (O6)                                                                                                                                                               |
| A19                | met                             | no committed check   | Keys sort identically either way. The source predicate reproduces                                                                                                                                         |
| A20                | met                             | (c) yes; (a)(b)(d) source only | Exact pins, staged builds and `engines.node >=22.1.0`. The flag is absent from `node --help` on 20.10.0 and 21.4.0 and present on 22.2.0                                                                   |
| A21                | met                             | yes                  | Caption fixture                                                                                                                                                                               |
| A22                | met                             | baseline yes; rest source only | The worker probe was not run because it would launch live CLIs (L3)                                                                                                                     |
| A23                | met                             | yes                  | The runner refuses generic protection text for every suite                                                                                                                                              |
| B1 ★               | met, with N5                    | yes                  | Live: 24 journal rows read "observer input unavailable"; none fell into `runtime-unavailable`                                                                                                     |
| B2 ★               | **not met (F2)**                | fixture only         | (d) and (e) are met. (a) and (b) are not live. (c) works live but has no discriminating regression                                                                                                      |
| B3                 | **regresses admitted reads (F3)** | yes                  | The new forms work live during a gate. The narrowed forms are refused                                                                                                                                      |
| B4                 | met                             | yes                  | Live acceptance of the `anyOf` schema is untested (L4)                                                                                                                                       |
| B5                 | met                             | yes (4 mutants)      | The cap-3 concurrency case discriminates where cap 1 did not                                                                                                                                  |
| B6                 | partly met (N6)                 | yes                  | Masks are inert, and the `.env` pin asserts non-deny only                                                                                                                                              |
| B7 ★               | met                             | yes                  | The edition does not encode the bound (O2)                                                                                                                                                      |
| B8                 | met                             | yes                  | Lockfile-derived workspace list. `discovery-cli.ts` is in all three inventories                                                                                                             |
| B9                 | met                             | yes                  | The historical hash reproduces. Exactly four Shape-First changes                                                                                                                                         |
| B10                | met                             | yes                  | The loop-ran assertion fails when the second attempt is removed                                                                                                                                      |
| B11                | met                             | yes                  | D005's NaN divergence is pre-existing and boarded                                                                                                                                             |
| B12(a) ★           | met                             | yes                  | The colliding budget is refused                                                                                                                                                                        |
| B12(b)             | returned, justified             | n/a                  | Dedupe reads the folded `state.lastNoOp`. Keying it by phase and arm generation changes the replay fold. The follow-up is FUP-4f8cd7989607ad3f                                                            |
| B13                | met (N9)                        | incidental only      | A class-only revert passes 31/31. A full revert fails through an ENOENT                                                                                                                                                    |
| B14                | **weakened (F4)**               | yes for new pins     | The new pins discriminate. A removed assertion was weakened                                                                                                                                                  |
| B15                | met, with N10                   | yes                  | Unknown CLI argument refused. `ultracode` normalized                                                                                                                                                 |
| B16 ★              | met                             | yes                  | The seven WO-023 strings refuse, a bare `*` refuses, `Assisted-by` refuses, and a human `Co-authored-by` passes                                                                                               |
| B17 ★              | met                             | yes                  | +227 bytes per role, identical in both roots, within the ceilings                                                                                                                                                  |
| C1–C10             | met                             | predicates rerun     | C1 has 13 sentences and four refusals. C8 matches `claude` 2.1.278 and `codex-cli` 0.155.0. C4's outcomes match control state                                                                               |
| D1                 | met, with N8                    | yes (4 mutants)      | Preview and apply are exact in fixtures and in a helper's own CLI scenario                                                                                                                                          |

## Blocking findings (route to repair)

### F1 — Row A4 ★: the index refresh pushes harness phrase dispatches past their 12 s timeout, so the transition is recorded but reported failed, and the index stays stale

- **Criterion:** row A4's required result, "every state-changing transition
  refreshes it". Also criterion 7 (`test:docs` green) and criterion 1 (a false
  "fixed" state).
- **Observed:**
  - `scripts/resume.mjs:1102-1112` now runs `refreshExecutorIndex` for every
    action except `status`, `times`, `usage` and `briefing`. That includes the
    read-only `next`.
  - Regenerating the index takes 15.6–28 s on this repository. My read-only
    `index --check` in the real worktree took 15.60 s. It makes 369 git
    subprocess calls, measured in helper copies.
  - The Claude prompt hook runs `resume: next`, `fix`, `verify` and
    `final review` as `spawnSync(node scripts/resume.mjs <action>, {timeout: 12_000})`
    (`packages/skeleton/src/harness-host.ts:602-612`; the same value was at
    `cb932c84`). The child is killed after it appends the event and projects
    `current.md`, and before it writes the index.
- **Expected:** the transition refreshes the index, and the hook reports it
  as recorded.
- **Live reproduction** (the dispatch that allocated this report):
  - The last line of `docs/control/orders/WO-142.jsonl` is
    `VerificationRequested` at `05:35:35.556Z`.
  - The previous session's harness journal records
    `{"event":"UserPromptSubmit","dispatch":{"action":"verify","recorded":false}}`
    at `05:35:46.819Z`, with hook duration 12,541 ms.
  - `docs/work-orders/README.md:3` still reads "ready-to-verify" (mtime
    01:33:43 local), while `docs/control/current.md` says "verifying" (mtime
    01:35:35).
- **Helper and refuter reproductions** (Node 22.2.0, same `spawnSync` call):
  - current `resume.mjs verify` → `SIGTERM` at 12,004 ms, event appended,
    index stale;
  - the refresh moved back under `releaseExecutorWriter` → exit 0 in
    1.1–1.4 s;
  - `next` in the active phase → killed at 12 s now, against 274–435 ms at
    activation.
- **Why the fixture misses it:** it spawns resume with no timeout, in a
  repository without tags.
- **Severity:** blocking. It is a starred row not fixed in real use, and it
  regresses `resume: next`.
- **Repair direction** (not prescribed): keep the refresh off the timed
  dispatch path, make it fast enough, or give the hook's lifecycle call an
  honest budget. Test through the hook's own dispatch path or its time bound.

### F2 — Row B2 ★: background Bash dispatches and completion notices still never reach the session journal in Claude Code

- **Criterion:** row B2's required result, "each corrected with a regression",
  for items (a), (b) and (c). Also criterion 1.
- **Observed (a):**
  - `observeBackgroundTool` reads the id from
    `agentId/agent_id/task_id/task_name` or `input.task_id/agent_id/id`
    (`packages/skeleton/src/observed-facts.ts:100-110`).
  - Claude Code's background Bash result is
    `{backgroundTaskId, interrupted, isImage, noOutputExpected, stderr, stdout}`,
    so the function returns `null`.
  - A helper scanned local transcript structure (keys only, no content): 451
    background-Bash results across 2.1.251–2.1.278 all carry
    `backgroundTaskId`, and none carries `task_id`. Codex exposes no Bash tool.
- **Observed (b):**
  - The new notice parser (`observed-facts.ts:436-481`) reads only
    `type:"user"` rows whose content is a string.
  - This session's three notices were stored as `queue-operation` rows and
    `attachment` rows (`attachment.type` `queued_command`).
  - So were all 11 agent notices of the 2026-09-19 planning session that the
    row cites as its reproduction.
- **Observed (c):** the PostToolUse scope stamp
  (`harness-host.ts:2847-2848`) works live: 259 rows carry WO-142 and
  verification. But deleting it fails no test. The only scope assertion covers
  rows that `appendObservation` already stamped at activation.
- **Expected:** a background Bash dispatch and its completion are journaled,
  and each item has a regression that fails when its subject is reverted.
- **Live reproduction:**
  - This session started three background Bash tasks (`bc680qvzn`,
    `bir4b8d3k`, `bsd1w2wb5`) and received three completion notices.
  - Every observed-facts block, from 05:37:56Z to 06:52:50Z, read
    "Background tasks: unknown (no-session-task-observation)".
  - Refuter count: the session journal holds 0 `backgroundTask` rows out of
    12,053 rows.
  - The fixture shape `{ task_id: "task-one" }` and the user-string notice are
    the only forms the tests exercise.
- **Severity:** blocking. (a) and (b) are a starred row not fixed in real use.
  (c) is a required regression that does not discriminate.
- **Items (d) and (e):** met.

### F3 — Row B3: the new flag allowlists refuse read-only forms that the live-gate guard admitted at activation

- **Criterion:** the order's Design, "B3 extends an existing allowlist", and
  operator-review assumption 2, "B3 widens an enforcement allowlist". Also the
  Cost line's promise to remove live-gate refusals of read-only commands, and
  the regression rule.
- **Observed:** I ran `shellWriteTargets` from the activation build and the
  current build side by side:

  ```text
  "grep -rn foo docs"    HEAD: []  CURRENT: null
  "grep -A3 foo a.md"    HEAD: []  CURRENT: null
  "grep -m 5 foo a.md"   HEAD: []  CURRENT: null
  "head -n5 a.md"        HEAD: []  CURRENT: null
  "head --lines=5 a.md"  HEAD: []  CURRENT: null
  "ls --color=never"     HEAD: []  CURRENT: null
  "cat --number a.md"    HEAD: []  CURRENT: null
  ```

  - `null` is refused during a live gate. Helpers confirmed this end to end
    through the generated permissions hook for `grep -rn`, `grep -R`,
    `grep -A2`, `grep -C2`, `grep -m 1`, `head --lines=2` and
    `ls --color=never`.
  - `grep -R`, `-L`, `-P` and `--color` behave the same way.
  - None of these flags can write, so refusing them adds no safety.
  - The new forms do work live. During my gate, `git status --short`,
    `git --no-pager status`,
    `git --no-pager log --no-ext-diff --no-textconv`, `sed -n 1,3p`, `wc -l`,
    `ps -p 1 -o pid,comm` and `tail -5` were admitted, and plain `git log` and
    `git diff` were refused, as 07 documents.
- **Expected:** read-only forms admitted at activation stay admitted, and the
  new programs are added with bounded flags.
- **Severity:** blocking, as a regression of admitted read-only behavior
  against the order's stated direction.
- **Recorded disagreement:** the harness-group refuter judged this
  non-blocking. Its reasoning is that "each program with a flag allowlist"
  itself entails refusing some previously admitted flags. The docs-group
  refuter confirmed it blocking. I side with blocking for these reasons:
  - the excluded flags are all read-only;
  - the Design and assumption 2 say "extends" and "widens";
  - no decision record documents the narrowing;
  - `rows.md`'s "Read commands treated as opaque" is inaccurate for the five
    programs admitted at activation with any flags.

  The fix is bounded: widen the grep, head, ls and cat allowlists and add
  fixture pairs.

### F4 — Row B14: the replacement AC3 coupling check accepts reserved-state reads that the removed assertion refused

- **Criterion:** row B14's required result, "each assertion or fixture added;
  none weakened".
- **Observed:**
  - `packages/kernel/test/ac2-replay-store.test.ts` removed
    `assert.doesNotMatch(outside, /stateField\([^)]*,…|\bstate\s*(?:\.\s*(?:rngState|policy)\b|\[…\])/u)`
    and replaced it with `assert.deepEqual(reservedStateReads(outside), [])`
    (line 397). The diff's only removed lines in that file are this assertion.
  - The AST detector flags a read only when the receiver is a bare identifier
    in its alias set.
  - Two helpers probed the detector. The removed regex refuses each form
    below, and the new check accepts it:
    - `context.state.policy`
    - `this.state.rngState`
    - `x.state.policy`
    - `context.state['policy']`
    - `obj.stateField(state, 'policy')`
  - A planted `return context.state.policy;` in `packages/kernel/src/core.ts`
    passes the current test and fails the activation test.
  - `core.ts` already reads `context.state` at :368, :394 and :537, so the
    form is realistic.
- **Expected:** the new alias and destructure coverage (a real gain) sits
  beside the old coverage, not in place of it.
- **Severity:** blocking, as an unmet required-result clause.
- **Executor claim:** `rows.md`'s "pins added without weakening existing
  assertions" is inaccurate for this assertion.

## Non-blocking findings (route to the same repair: fix within the row, or board in decisions.md with a `followup`)

- **N1 (A16).** The named unused import (`readFileSync`, `scripts/worktree.mjs:3`,
  formerly :7) is still unused. `rows.md` says "dead exports/imports removed",
  and its "source-definition count assertions" do not exist in the repository.
- **N2 (A14).** `docs/product/06-roadmap.md:421-422` still says close without
  `--publish` "prepares and validates without creating a tag or Release". The
  equal-version path can still create a local tag ref.
- **N3 (A14).** The reserved-heading guard (`scripts/release-notes.mjs:51-60`)
  matches only ATX lines. A setext level-one heading or a list-item ATX
  heading with a generated title passes, although the row says "at every
  level".
- **N4 (A5).**
  - Restoring the drifted inline predicate fails no check. A fixture word such
    as "ledgers" would discriminate.
  - Existence-gated decision links make the index stale the moment an order's
    first decision is written. `npm run meta` does not refresh the index, so
    this is a new source of the stale-index `test:docs` failure that A4
    targets.
- **N5 (B1).** The same advisory emitted by two hooks for one call is shown
  twice, because the marker key includes the hook kind. 07's "repeating the
  same advisory is quiet" is inaccurate across hook kinds.
- **N6 (B6).**
  - The new `version.ts` mask is read by no evidence or machinery comparison,
    and the dead `harness-host.ts` and `contributor.ts` masks were kept.
  - The `.env` delegation pin asserts only non-deny. Removing `.env` from the
    credentials classifier still passes.
- **N7 (B3).**
  - The admitted git forms refresh `.git/index`, which `gateInputPath` treats
    as a gate input.
  - They also run a configured `core.fsmonitor` program.
  - This existed at activation for the metadata-list forms (`git status --short`,
    `git diff --check`). The new `--no-pager` forms inherit it.
  - `--no-optional-locks` or `GIT_OPTIONAL_LOCKS=0` would close the index
    write.
- **N8 (D1).** Current-session protection reads `CLAUDE_SESSION_ID`
  (`scripts/lib/harness-prune.mjs:163`). Claude Code 2.1.278 exports
  `CLAUDE_CODE_SESSION_ID` instead, which I confirmed equals this session's id.
  Live markers stay protected only through owner liveness, or as an unknown
  owner.
- **N9 (B13).** Only an incidental ENOENT guards the extraction: moving the
  class back into `scenario.ts` passes 31/31. A structural assertion would
  pin it.
- **N10 (B15).** Launch normalizes `ultracode` case-sensitively
  (`packages/skeleton/src/worker-protocol.ts:24`), while attestation
  lowercases (`scripts/resume.mjs:494`). `UltraCode`, `Ultra` and
  `ULTRA CODE` reach the transport raw.
- **N11 (criterion 6).** `packages/kernel/README.md:1` still reads v0.5.0,
  although the order bumped kernel to 0.6.0. The kernel README omits B11's
  full-candidate validation, and the compiler README omits the bare-`*`
  refusal (B16(b)).
- **N12 (criterion 1 evidence).**
  - `counterfactuals.md` and `package-counterfactuals.*` cite scripts and logs
    that exist only in `/tmp`.
  - For A16, A19, A20(a)(b)(d) and the A22 non-baseline items, no committed
    check fails when the subject is reverted.
  - This verification reproduced the source predicates, and its own
    counterfactuals now stand as the reproducible record. The repair should
    either commit the checks it names or name what it actually has.
- **N13 (evidence accuracy).** `rows.md` overstates four rows:
  - B2 ("fixed", "scoped hashed dispatch/completion observations");
  - B3 ("Read commands treated as opaque");
  - A16 (see N1);
  - B14 (see F4).
- **N14 (B17 wording).** The role text says "a named follow-up", but A1
  admits only a JSON `followup` string or `reopens` object. A writer who
  follows the role text alone could board a prose follow-up that never
  reaches the feed. The key is documented in 07 and `followups.md`, not in
  the role text.

## Boarded defects (met here, not WO-142's rows)

- **[WO-142-D007](../../evidence/WO-142/decisions.md#wo-142-d007--board-up-the-node-26-beacon-timestamp-failure-met-during-verification) —
  Node 26 beacon timestamp failure.** Under v26.9.0, `npm test` fails the
  skeleton, resume and worktree suites at both the activation commit and the
  current tree. Row A20's `engines.node >=22.1.0` admits Node 26. Final
  review must run its gate under Node 22 until this is fixed.
- **[WO-142-D008](../../evidence/WO-142/decisions.md#wo-142-d008--board-up-the-retained-wo-042-mutation-tools-broken-check) —
  broken check in a retained tool.** `authority-mutation-evidence.mjs --check`
  exits 1 at the activation commit with "mutation evidence executable subject
  drift".
- **[WO-142-D009](../../evidence/WO-142/decisions.md#wo-142-d009--board-up-two-lexical-module-entry-guards-outside-row-a13s-ten-scripts) —
  lexical module-entry guards.** `corpus/mutation/mutate.mjs` and
  `corpus/harness/generator-runtime.mjs` keep the lexical guard.

## Observations

- **O1 (A2).** The A2 fixture's legacy-hash assertion does not discriminate,
  because its title starts without a parenthetical. The real-tree 19-receipt
  gate does discriminate.
- **O2 (B7).** The regenerated verification edition does not encode B7's
  schema bound or prompt sentence; its `--check` passes with B7 reverted. The
  unit test is the causal check.
- **O3 (A9).** The forged-`orderLength` matcher is the generic gate prefix. A
  matcher-less `assert.rejects` remains at
  `scripts/test-plan-refutation.mjs:2191`, outside the row's range.
- **O4 (B3).**
  - Plain `git status --short` was already admitted at activation through the
    metadata exact list (`harness-host.ts:1882`), which 07's new sentence does
    not mention.
  - Any word containing a glob or brace character is opaque regardless of
    quoting (`harness-command.ts:423`). That is why my
    `grep -o '…{[^}]*…}' file` was refused.
  - The shared adapter also widens the target-worker and planning-branch
    guards for the new read forms.
- **O5 (criterion 2).** Row B17's rule plus the A1 collector depend on the
  `followup` key (see N14).
- **O6 (A11, A18).** Two A11 hints (`--work-order`, "resolve <until>") and the
  A18 DCO hint are present in source but asserted by no test. Each row group
  still has one discriminating pin.
- **O7 (A17).** `packages/compiler/src/harness.ts:317` keeps `[a-z-]+`. A
  runtime file with a capital, digit or underscore is still refused at emit;
  the widened reader is reachable only through a hand-written manifest.
  Declaring the two files under the skeleton product suite does not change
  `--review` selection, because the product suite always runs.
- **O8 (criterion 5).** The criterion says the operator runs the
  real-checkout listing. D006 records the executor's preview, and this report
  records mine. Both are preview-only, and 0 bytes were deleted.
- **O9 (fences).** The Design fence says no row changes a worker's input, but
  rows B4, B7 and B15 require exactly that:
  - B4: plan-refuter `anyOf` schema; a `.git` directory in every scratch cwd,
    for Claude as well as Codex;
  - B7: prompt sentence and `maxLength`;
  - B15: `ultracode` launch argv.

  The order is internally inconsistent here. The executor followed the rows.
- **O10 (B11).** The WO-142-D005 live and replay NaN divergence reproduces
  identically at activation and now. It is boarded with a follow-up
  (FUP-75563edf56be23b5).
- **O11 (C4, C9).**
  - The rewritten WO-051 and WO-052 outcomes carry no evidence link; both
    orders are closed with a pass.
  - The downtime runbook's historical marker dates it 2026-09-01, while its
    content was last revised 2026-09-04.
- **O12 (release).** The minor classification and the component bumps are
  correct: kernel 0.6.0, compiler 0.15.0, skeleton 0.28.0, console 0.1.7, and
  `check-surfaces --local` passes. D006's "four newly refused inputs" also
  leaves out the narrowed compiler attribution input (B16(c)) and two
  tooling refusals (A12, A14).

## Limits

- **L1 (Node).** The operator upgraded this host from Node 22 to Node 26.9.0
  between implementation and verification. The green gate is under Node
  22.2.0, the version the executor used. Under 26.9.0 three suites fail,
  exactly as at the activation commit (D007).
- **L2 (no live spend).** No planning refutation, feedback edition or worker
  launch was run. The executor's one live feedback edition was checked only
  through its `--check` and source digests.
- **L3 (worker probe).** `scripts/probe-worker-hosts.mjs` was not run,
  because it launches live CLIs. A22's four added flags were checked in
  source only.
- **L4 (B4 schema).** Whether Claude's `--json-schema` and Codex's
  `--output-schema` accept B4's new `anyOf` result schema is unobserved.
  Neither transport schema used one at activation. The next paid refutation
  is the first live test. A reviewer can check the CLI documentation before
  spending.
- **L5 (C rows).** C1–C10 were checked with this verification's own content
  predicates and link checks. For documentation, the file is the evidence.
- **L6 (clean room).** One helper's early recursive grep over `docs/` ran
  before it excluded `docs/intake/`. Its output was filtered. No intake path
  or content was displayed, quoted or copied, and every clone excluded
  `docs/intake/`.
- **L7 (usage).** Root dispatch usage at 06:58:01Z, source
  `claude-transcript-message-usage`, scope dispatch:
  - input 284, cached input 36,062,371, cache writes 401,220, output 120,285,
    total 36,584,160 tokens;
  - reasoning tokens and cost unknown;
  - subagents 10 of cap 20, exact-observed.

  The workflow reported 2,214,311 subagent tokens, 1,290 tool calls and
  3,483 s of wall time.
