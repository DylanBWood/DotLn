# WO-150 VER-001 — independent verification

**Verdict:** pass. The current subject meets all five acceptance criteria. I
found no substantive implementation defect and no unresolved earlier finding.
The behavioral reopening observation, the 249-byte executor margin and the
unavailable session counters remain explicit limits rather than repair items.

**Subject:**
[`docs/work-orders/WO-150-tinkerer-economy-default.md`](../../work-orders/WO-150-tinkerer-economy-default.md)
on branch `wo-150`, with repository HEAD `cd361f17fd2d`; the verification
dispatch captured the staged working tree at
`refs/dotln/checkpoint/WO-150/3` (`f5de3b9051f85ed9613660da2a260c4f6a6ff592`).
This verifier changed no implementation or product decision.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-5.6-sol","effort":"xhigh","source":"codex-session-readback"}

Human actor: the operator dispatched `resume: verify` in an attended Codex
session. Canonical status and the dispatch briefing supplied the actor values
above. No subagent was launched; the explicit session count is zero against the
configured cap of 20. Harness observation of background tasks is unavailable
because no harness session journal was established.

**Process cost:** unknown; cause no-session

The required entry usage command and a second attempt after the product gate
both returned `Begin the harness session before measuring usage`. Total tokens,
source, scope, cutoff and dollars are therefore unknown rather than zero. No
equivalent counter was available to reconstruct either entry or handoff.

## Goal-aligned judgment

The order removes the manual equipment step that let the third economy trial
run unequipped while preserving the executor's opt-out and decline paths. NoOp
would retain that ambiguity. The intervention is one default switch plus the
missing entry-briefing projection; it adds no authority, gate, agent, schedule
or external dependency. The shared-cost risk is measured directly: 1,173 bytes
per ordinary executor dispatch and at most one optional experiment inside 900 s.
Rule beating is handled by the recorded post-merge behavioral observation,
rather than treating the enabled switch as proof that executors use it. The
other system traps are bounded by unchanged authority and outcome standards,
the reversible opt-out, retained alternatives, and the absence of new operator
rescue or process. These observations support the order's operator-flow goal
without claiming critical-path runtime progress.

## Instrument disclosure

`scripts/lib/harness.mjs`, the generated skills and the product gate are part of
the subject they helped check. I therefore did not treat their success messages
alone as proof. I read the source and tests, reproduced the configuration and
historical-baseline cases, and independently recomputed each role's generated
bytes with `harnessInstallation()` against the checked-in budget and evidence
records. The lifecycle and decisions generators are also under the broader
generated bundle; their projections were checked against current bytes and the
durable control records.

## Criterion 1 — default equipment, opt-out and role isolation

**Expected:** defaults include `tinkerer-economy`; the ordinary Contributor
program equips it; `{ "tinkerer-economy": false }` removes only that support;
both generated skill roots are byte-identical.

**Observed:** `executorSupportDefaults["tinkerer-economy"]` is `true`, so
`defaultExecutorSupportIds` includes it. The ordinary program contains the
support in its component manifest and executor procedure; the opt-out contains
neither. Authority envelope, work order, non-procedure facets, all five other
roles and every other executor support are unchanged across the switch. The
dispatch path now also names `Tinkerer — Economy is equipped` and the 900 s
bound, closing the objective's briefing clause.

**Reproduction and evidence:** the focused compiled test
`WO-145 economy equipment adds only executor guidance and no authority or host
checks` passed. The focused process-debt briefing case also passed, including
presence under defaults and absence under the opt-out. An independent
`harnessInstallation()` comparison found all six `.claude` role files
byte-identical to their `.agents` twins and each installed file identical to
the default-on generation.

**Judgment:** pass. Severity: none.

## Criterion 2 — cold-start measurement and budget route

**Expected:** measure every role in both roots against the checked-in ceilings;
state the executor verdict; if a role breaches, use the standing one-step route
and record its dated acceptance rather than trimming reviewed instructions.

**Observed:** independent UTF-8 byte counts reproduce
[`cold-start.json`](../../evidence/WO-150/cold-start.json) exactly:

| Role | Default cold start | Opt-out | Delta | Ceiling | Verdict |
| --- | ---: | ---: | ---: | ---: | --- |
| executor | 24,327 | 23,154 | 1,173 | 24,576 | within by 249 |
| verifier | 21,061 | 21,061 | 0 | 25,151 | within |
| reviewer | 22,458 | 22,458 | 0 | 24,576 | within |
| release-close | 13,967 | 13,967 | 0 | 16,384 | within |
| planner | 15,033 | 15,033 | 0 | 24,576 | within |
| refuter | 15,690 | 15,690 | 0 | unset | unset |

Each row is identical in both roots. `CLAUDE.md` contributes 6,113 unchanged
bytes. No ceiling was breached, so the empty `ceilingChanges` and
`acceptancesAdded` arrays are correct and no budget edit was due.

**Judgment:** pass. Severity: none.

## Criterion 3 — current and historical role baselines

**Expected:** the default-on build matches a new current oracle, the opt-out
matches the previous default, and the WO-145 release snapshot remains immutable;
the Origin comment distinction must remain explicit.

**Observed:** `wo150-role-baseline.json` records all twelve current role hashes
and pins `wo079-role-baseline.json` by SHA-256. The process-debt case walks that
fixture chain, also preserving the separate v0.33.2 WO-145 snapshot. The
default-on build matches all twelve new hashes. The opt-out matches all twelve
previous-default bytes, including Origin comments. Across default-on versus
opt-out, only the executor gains instruction text; non-executor bodies match
after removing the one whole-loadout Origin line, whose semantic hash correctly
changes when equipment changes.

**Reproduction and evidence:** the focused
`WO-145 optional economy support preserves historical snapshots through WO-079
and changes only executor instructions on` case passed. It also confirmed the
support paragraph appears exactly once in the executor and that both generated
manifests select the expected support.

**Judgment:** pass. Severity: none.

## Criterion 4 — product and decision write-backs

**Expected:** product 05 records default equipment, opt-out and history start;
the order's decisions carry the three trials and operator acceptance; the
decisions index is refreshed.

**Observed:** product 05 has the dated WO-150 application record. WO-150-D001
records the accepted default and alternatives; D002 records the baseline
strategy; D003 records the cold-start decision; D004 carries the three adopted
trials with `lastAdoptedImprovementAt: 2026-09-20`,
`experimentsSinceAdoption: 0` and `regression: false`; D005 defines the
behavioral reopening observation; D006 records the regenerated evidence
editions; and D007 records the briefing repair. The record preserves that two
trials were equipped and WO-099 was unequipped, and makes no per-order saving
claim because no trial counted iterations.

`npm run meta` refreshed the decisions projection, where all seven decisions
appear with their reopening conditions. `npm run publication:check` passed with
273/273 headings indexed and both reader editions current. The authority and
feedback evidence selectors point to WO-150 revision 001; generated-bundle
checking passed for all 31 surfaces.

**Judgment:** pass. Severity: none.

## Criterion 5 — product gate, whitespace and dependencies

**Expected:** `npm test` is green, `git diff --check` is clean and no dependency
is added.

**Observed and reproduced:**

- `npm test`: **22 passed, 0 failed, 262.69 s, 66 fresh tasks**.
- `node scripts/harness.mjs check`: **31 generated surfaces**, exit 0.
- `npm run publication:check`: pass, **273/273** headings indexed.
- `npm run release -- check-surfaces --local`: every surface passed; application
  `v0.39.0` and skeleton `0.34.0` match the declared minor release, while the
  compiler, kernel and console source versions remain unchanged.
- `npm run format:check`: all matched files use the pinned Prettier style.
- `git diff --check`: clean.

The manifest and lockfile diff adds no external package. It changes only the
internal `@dotln/skeleton` version from 0.33.1 to 0.34.0 and the console's exact
workspace pin. The diff adds no `eslint-disable`, `@ts-ignore`,
`@ts-expect-error`, `prettier-ignore` or `biome-ignore` directive.

**Judgment:** pass. Severity: none.

## Limits and disposition

The fixtures prove equipment and isolation, not that future executors will find
or run a useful economy experiment. That is why WO-150-D005 reopens after ten
equipped dispatches and requires each to record an experiment or explicit
kept-current decline. The executor margin is only 249 bytes; the next reviewed
executor instruction may trigger the standing 4 KB ceiling route. The trial
record supports the default decision but still does not support a per-order
time or token saving. These limits are disclosed in the order's durable
decisions and need no new follow-up.

No defect was encountered and deferred, so no follow-up decision or repair
route is required. Next legal workflow on this pass is `resume: final review`;
that role owns integration and publication, and this verdict grants neither.

## Reproduction

From `/Users/dylanwood/Projects/DotLn-wo150` on branch `wo-150`:

```text
npm run build --silent
node --test --test-reporter=tap --test-name-pattern='^WO-145 economy equipment adds only executor guidance and no authority or host checks$' packages/skeleton/dist/test/executor-supports.test.js
node --test --test-reporter=tap --test-name-pattern='^WO-145 optional economy support preserves historical snapshots through WO-079 and changes only executor instructions on$' scripts/test-process-debt.mjs
node --test --test-reporter=tap --test-name-pattern='^bare executor next/fix project installed defaults and completion advises about unfinished adjacent work$' scripts/test-process-debt.mjs
npm test
node scripts/harness.mjs check
npm run publication:check
npm run release -- check-surfaces --local
npm run format:check
git diff --check
```
