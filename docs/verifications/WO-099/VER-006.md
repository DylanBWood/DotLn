# WO-099 VER-006 — independent verification after the fifth repair

**Verdict:** pass. VER-005's only finding is repaired at the current subject:
an ordinary build no longer moves baselined ignored directory roots off the
mission pin, so the cadence stays unheld across consecutive pulses. The new
source- and resident-level regressions reach the prior failure mechanism, the
real worktree reproduces the repair, all earlier mission-check regressions stay
green, and the product and documentation gates pass. I found no blocking,
major or minor defect.

**Subject:** the uncommitted `wo-099` worktree at base
`479107530e35304bd2b39a4bfeaa09cd0011b103` (`main`, WO-110), captured by this
verification dispatch at `refs/dotln/checkpoint/WO-099/23`
(`251edfa82a9b9e9bd64d05cb8e0b8e47ff889fa5`). Authority is
[WO-099](../../work-orders/WO-099-mission-check.md); the finding rechecked here
is [VER-005 F1](VER-005.md), and its repair record is
[repair-005](../../evidence/WO-099/repair-005.md). This verifier changed no
implementation or test file, made no commit and published nothing. Its only
authored repository output is this allocated report; lifecycle commands updated
their owned control projections.

Current session: Codex CLI 0.155.1, `gpt-5.6-sol`, xhigh, from canonical
session readback. No subagents were planned or launched: explicit session count
0 against `docs/control/budgets.json` cap 20; harness-observed remainder is
unknown because no harness session was begun.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-5.6-sol","effort":"xhigh","source":"codex-session-readback"}

**Process cost:** unknown; cause no-session

The entry and handoff usage commands both returned `Begin the harness session
before measuring usage`; total tokens, dollar cost, source, scope and cutoff are
therefore unknown. Usage unavailability does not control the verdict. Executed
subject evidence below has a cutoff of 2026-09-20T22:55:42Z. Observed waiting
and work include the 260.52 s product gate, the 19.32 s documentation gate and
the 5.76 s focused suite; no total workflow time or token reduction is claimed.

## Findings

None. Severity: none. The retained collapsed-directory visibility boundary is
an explicit product limit, not an undisclosed finding: work added, removed or
rewritten beneath a baselined ignored directory is invisible until the design
is reopened toward local provenance. New, removed, kind-changed or mode-changed
roots still move; ignored files, links and other leaf entries retain
kind/mode/size/mtime comparison.

## VER-005 finding recheck

**Expected.** A directory Git collapses as one ignored entry is fingerprinted
without its volatile size or mtime. Creating and removing `.runtime/build-*`
and publishing a direct child into `dist/` therefore leave an existing baseline
quiet on this pulse and the next. The resident raises no hold or correction.
Leaf ignored files and the appearance or disappearance of collapsed roots
remain observable.

**Observed.** Source inspection at
`packages/skeleton/src/mission-check-source.ts:140-169` selects `[kind, mode]`
for directories and `[kind, mode, size, mtimeMs]` for other entry kinds. The
new fixture performs the same direct-child create/remove and rename operations
as `scripts/build.mjs:48-69`, asserts the directory mtime actually moved, and
then observes two empty `ignoredEntries` lists. Its negative cases still drift
for a new `.env.private`, a rewritten baselined `.env`, and a removed baselined
directory; it also asserts the disclosed blind spot beneath `.runtime/`.

The resident regression performs the build effects after `away`, advances two
cadence pulses, and records neither `dispatchHeld` nor
`MissionDriftObserved`. Independently, I pinned this real worktree, observed
eight ignored baseline entries, ran `npm run build --silent`, and observed
ignored-entry counts `0 -> 0 -> 0` across quiet, post-build and next-pulse
observations. The repaired worktree still exposed 107 ordinary changed paths.

**Reproduction evidence.** `npm run build --silent && node --test
packages/skeleton/dist/test/mission-check.test.js` passed 16/16 in 5.76 s,
including both VER-005 regressions and all earlier repair cases. The full
outside-sandbox `npm test` passed 21 suites, 65 fresh tasks and 0 failures in
260.52 s. These observations close VER-005 F1.

## Acceptance judgment

| Criterion | Expected and observed judgment |
| --- | --- |
| 1 — four verdict cases | **Pass.** The focused suite reproduces out-of-surface `drift`, a mid-episode clause `drift` naming the clause, an on-contract `on-mission`, and unavailable-verifier `unknown`; the bound, untracked, symlink, ignored-entry and decision-history regressions also pass. |
| 2 — correction, hold, refusal and clearance | **Pass.** The resident tests record the drift correction and hold, refuse dispatch while held, clear on a human answer, and clear on a fresh passing judgment over changed work. Host-proved drift survives an unavailable or invalid judge. The new ordinary-build resident case raises no false hold or correction across two pulses. |
| 3 — operator-run unattended row | **Pass with retained limit.** `docs/evidence/WO-099/live.json` records operator away before and after, a live Codex `gpt-6-astra` drift over one host and one model finding, a hold, one correction, a refused next dispatch and human clearance in 18.758 s. I inspected rather than repeated the paid row. It remains one synthetic worktree and predates the later ignored-inventory capsule extension; deterministic current-source tests cover that extension. |
| 4 — write-backs | **Pass.** Product 02 names `MissionDriftObserved` as the distinct non-operator correction; product 03 states the kind-dependent ignored-inventory rule and its widened boundary; the dated capability row remains live-evidenced with its remaining gate; D034 plus the same-day corrections to D031 and repair-004 name the misread claim, intended behavior, chosen rule, alternatives and reopening condition. |
| 5 — gates, dependency and generated evidence | **Pass.** `npm test` and both diff checks pass; no new suppression appears; the only package changes are internal `@dotln/skeleton` 0.32.0 to 0.33.0 pins. The 31 generated harness surfaces, selected authority and feedback editions, 272/272 publication headings and both voice locks validate. `release -- prepare --local` reports v0.37.0 current with no files changed. |

## Executed evidence and limits

| Check | Observed result |
| --- | --- |
| Focused mission suite | 16 passed, 0 failed, 5.76 s |
| Real-worktree build probe | 8 baseline ignored entries; 0 before build, 0 after build, 0 next pulse; 107 ordinary changed paths |
| `npm test` outside the harness sandbox | 21 passed, 0 failed, 65 fresh tasks, 260.52 s |
| `npm run test:docs` | 19 passed, 0 failed, 19 fresh tasks, 19.32 s |
| `git diff --check` / staged check | Clean |
| Harness / authority / feedback checks | 31 generated surfaces; 34 bundle comparisons; ten passing feedback regressions and ten removal failures |
| Publication check | 272/272 headings; 30 and 45 linked sections current |
| Dependency and suppression comparisons | No external dependency and no new lint/type/format suppression |

The runtime under verification also produced the mission fixtures, the retained
live row, authority and feedback editions, generated harness, and test gate
observations. Those instruments are disclosed rather than treated as independent
oracles; the source trace and separately executed real-worktree probe establish
the repaired join. I did not rerun the paid live model episode, did not verify
WO-110's separate claims, and make no hostile-filesystem-monitor guarantee.

## Goal alignment

Passing now advances Gate R's read-only supervision while preserving the
operator-flow outcome: real drift holds unattended work, but the repository's
own build no longer creates a permanent false hold. **Policy resistance** and
**fixes that fail** are answered by reproducing the exact directory-mtime
mechanism. **Rule beating** is answered by the real-worktree probe and the two
new regressions, not the green full gate alone. **Seeking the wrong goal** and
**drift to low performance** would either accept build-noise holds or hide the
widened boundary; the implementation and product text now agree on both.
**Shifting the burden to the intervenor** is reduced because a human answer is
not followed by the same false hold. **Escalation** is bounded to the existing
fingerprint join. **Success to the successful** grants no exemption to the
earlier live row, which is why its later regression was checked independently.
**Tragedy of the commons** favors deterministic current-source tests and one
full gate over repeating a paid live episode whose transport was unchanged.

Naive Interventionism supports the small kind-dependent fingerprint: it keeps
the privacy boundary and leaf-file detection while avoiding new capsule fields,
re-pinning rituals or reads of ignored names and bytes. NoOp would preserve the
reproduced permanent hold and three false write-back claims. At handoff, the
repair's observed benefit and disclosed cost match the work order: dependable
cadence over ordinary builds, with nested work below a collapsed ignored root
remaining a named reopening condition rather than an implied guarantee.
