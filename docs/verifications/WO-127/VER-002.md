# WO-127 — VER-002

**Verdict:** PASS

**Actor:** Independent Codex verifier; CLI 0.154.0 observed with `codex --version`. Model and effort use the repository's operator-attested default.
**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.154.0","model":"gpt-6-astra","effort":"max","source":"operator-attested","accountLabel":"not-applicable"}

Judged the original order, all seven current acceptance criteria, complete subject diff, cited source/tests and VER-001/FINAL-001. The repair was handed off at checkpoint `refs/dotln/checkpoint/WO-127/8`. No substantive findings remain. No implementation edits were made in verification.

| Criterion | Observed against expected | Evidence |
| --- | --- | --- |
| 1 | Retained binary files, nested and empty directories are archived; main's active state and terms retain their bytes. Gate records use their separate handoff. | Four independent closeout cases; inspected actual teardown/evidence-handoff fixture and its passing executor workload. |
| 2 | Preview enumerates intake and control destinations without creating directories or changing source/main bytes. Receipt objects and rendered text omit private contents and content digests. Integration assertions preserve refs and subject HEAD. | Closeout preview reproduction and worktree integration assertions. |
| 3 | Differing and cross-type collisions preserve both versions; identical retries reuse bytes. Partial failure, corrupt destinations and changed sources remain recoverable. Symlinks, escapes, unignored destinations and tracked literal-name collisions refuse; unknown ignored material remains protected. | Independent closeout failure cases and final pre-removal checks. |
| 4 | Execution-only commits and a merge retain cost evidence. Changed cost inputs, mismatched acceptances, invalid/non-ancestor revisions and observations older than their recorded subject refuse. | Independent planning-cost case and current plan check. |
| 5 | Real authored-output receipts survive commits, become stale when current bytes change, and recover after rereading. Generated processes accept manifest-sized and chunked multibyte messages and refuse truncated JSON. The 20-second deadline remains. | Independent cross-commit and transport cases; FINAL-001 F1 disposition below. |
| 6 | Closeout documentation, generated instructions, sourced decisions and explicit authority revision 002 agree with the repair. Prior reports and authority snapshots retain their bytes. | Complete diff review and passing executor workload; final current-tree gate identity is retained by this verification's completion event. Later final review and post-merge release close remain required. |
| 7 | Owned projections are prepared before fingerprinting; repeated preparation preserves unchanged bytes and mtimes. Immutable observations and explicit publication inputs survive. Packages and fixtures wait for successful preflights; actual failing application evidence remains failed. | Independent preparation/idempotence and scheduler-barrier cases. |

**FINAL-001 F1 — resolved:** The retained trace `docs/control/local/hook-trace/83052.jsonl` ends at 29.958 ms entering synchronous stdin `readFileSync`, before evaluation, with no return. The production hook now awaits asynchronous stdin consumption. This repairs the captured blocking path; it does not establish a general Node defect or guarantee that every possible timeout is eliminated. The former artificial adoption of all 29 generated outputs is replaced by actual Write/Read observations, with a separate generated-process transport regression. The executor's 15 repetitions all passed; the verifier independently reproduced both behavioral cases.

**FINAL-001 F2 — resolved:** Architecture now declares its publication validator. Independent verifier/reviewer fixtures reject missing, unrelated, failing and stale output checks, require current report delivery, and permit a failed verdict without a green application gate. Passing-result requirements remain intact; no lifecycle exemption was added.

Independent reproductions, from the selected worktree:

```sh
node --test --test-name-pattern='closeout ' scripts/test-process-debt.mjs
node --test --test-name-pattern='WO-126 cost input holds' scripts/test-plan-refutation.mjs --fixtures-only
node --test --test-name-pattern='WO-039 completion tracks outputs across commits|WO-127 hook stdin' scripts/test-harness.mjs
node --test --test-name-pattern='failed verification and review validate|evidence preparation repairs' scripts/test-process-debt.mjs
node --test --test-name-pattern='full inventory retains|package tests and fixtures wait' scripts/test-runner.test.mjs
```

All passed. TAP totals: preservation 4/4 in 1492.205 ms; planning fixture 825.251 ms, invocation 5937.820 ms including the current plan check; harness 2/2 in 1731.074 ms; preparation 2/2 in 2294.551 ms; runner 2/2 in 100.756 ms. Logs: `docs/control/local/wo127-verifier-002-{preservation,planning,harness,preparation,runner}.log`. The inspected executor `wo127-repair-full-gate.log` records 37/37 suites in 488156 ms and a passing 221 ms diff check for tree `9ebbb4e3fdbc0496981f38b58e3f21d45b1c47f5`. The final report/control tree receives canonical `npm run harness -- evidence`; its actual result is recorded with completion and in `docs/control/local/wo127-verifier-002-gate.log`. Meter inspection through `collectMeta` at 2026-09-11T04:42:56.007Z recorded verifier token and dollar observations as null.

VER-001, FINAL-001 and the original WO-127 authority snapshots compare byte-for-byte with checkpoint 6; WO-126 evidence/reports have no diff. The gate inventory and generated procedures under verification also support this work. Their changes were independently inspected and consequential behavior reproduced. Authorship, writer reservation and current-byte delivery use explicit Codex adapters; automatic hooks and effective-session readback are not claimed. Inherited implementation was not adopted as verifier authorship. Fixture evidence does not claim live publication or worktree cleanup has occurred.
