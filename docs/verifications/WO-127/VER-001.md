# WO-127 — VER-001

**Verdict:** PASS

**Actor:** Independent Codex verifier; CLI 0.154.0 observed with `codex --version`. Model and effort use the repository's operator-attested default, not effective-session readback.
**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.154.0","model":"gpt-6-astra","effort":"max","source":"operator-attested","accountLabel":"not-applicable"}

Judged the original work order, complete subject diff and cited implementation, tests, closeout documentation, decisions and generated-surface contracts. No previous WO-127 verification exists. No substantive implementation findings; no implementation edits were made in verification.

| Criterion | Observed against expected | Evidence |
| --- | --- | --- |
| 1 | Binary retained files, nested and empty directories survive archival. Main's active queue, terms and harness state retain their bytes. The worktree fixture performs teardown and retains both subject and main gate records. | Four independently executed closeout fixtures; reviewed `test-worktree.sh` assertions and its passing full-workload output. |
| 2 | Preview enumerates intake/control destinations without copying or creating archive directories. Integration assertions retain refs and subject HEAD. Receipt objects and rendered text omit private contents and content digests. | Closeout preview fixtures, including unchanged source/main snapshots; worktree preview assertions. |
| 3 | Differing and cross-type collisions preserve prior bytes; identical retries reuse copies. Injected partial failure, corrupt destinations and late source arrivals retain recoverable sources. Source/destination symlinks, dangling links, unignored destinations and tracked literal-name collisions refuse. Unknown ignored material remains outside the preservation allowlist. | Independently executed closeout failure fixtures; inspected final ignored-material and byte checks immediately before removal. |
| 4 | Later execution-only commits and a merge retain valid cost evidence. Changed costs, mismatched acceptances, missing/non-ancestor revisions and observations predating their subject refuse. The current planning gate also passes. | Independently executed planning-cost fixture and current planning check. |
| 5 | Cross-commit receipts survive the commit, then fail after current bytes change; rereading restores readiness. All 29 output files pass through generated-hook processes. The 20-second deadline remains unchanged. | Independently executed affected harness fixture; complete release workload passed all 37 suites. |
| 6 | Closeout docs, generated instructions, sourced decisions, publication locks and the new authority edition are present. Historical authority evidence stays intact. This independent verification supplies the next lifecycle handoff. | Reviewed diff; executor gate 004; canonical final-tree evidence recorded with the verification result. |

Reproductions from the selected worktree:

```sh
node --test --test-name-pattern='closeout ' scripts/test-process-debt.mjs
node --test --test-name-pattern='WO-126 cost input holds' scripts/test-plan-refutation.mjs --fixtures-only
node --test --test-name-pattern='WO-039 completion tracks outputs across commits' scripts/test-harness.mjs
```

All passed. TAP observations: closeout 4/4, 1452.502 ms total; planning fixture 838.025 ms, 5837.947 ms invocation including the current plan check; harness 1/1, 2972.077 ms total. Logs are local `docs/control/local/wo127-verifier-{preservation,planning,harness}.log`. The inspected executor `full-gate-004.log` records 37/37 suites, 475679 ms, and a passing 147 ms diff check for source tree `5c4b51a09b3f1c89e0173f502761726bac7521db`. Final report/control bytes receive canonical `npm run harness -- evidence`; the completion event retains that evidence's actual identity. Meter inspection at 2026-09-11T03:33:36.688Z reported verifier token and dollar observations as null.

The earlier timeout was not reproduced here. Successful diagnostics and this passing workload establish exercised behavior, not a proven runtime timeout repair. The operator asked for useful testing and supplied no ban on reruns or in-process tests. Final review, merge, live publication and worktree cleanup remain the later acceptance-6 obligations; this report does not claim they occurred.

The gate inventory and generated role procedures under review also support this verification. I independently inspected their changes and reproduced consequential behavior. Codex session authorship, writer reservation and current-byte delivery used the existing explicit adapters; automatic Codex hooks are not claimed. Inherited implementation outputs were not adopted as verifier authorship.
