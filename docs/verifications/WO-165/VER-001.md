# WO-165 VER-001 — independent verification: pass

**Subject:** the uncommitted `wo-165` worktree at HEAD `f73b7e184b38de9cab97b4e86c718b6006f6b19d` (equal to `main`), after `resume: next` and before any repair. The `verify` dispatch allocated this report and checkpoint `refs/dotln/checkpoint/WO-165/3` (`ac470a07`). I judged the five criteria of [WO-165](../../work-orders/WO-165-entropy-review-route-agreement.md) against the original order, its cited sources (REVIEW-003 ER3-003, REFUTATION-004, the loadout, the protocol, the transport, the receipt renderer and REFUTATION-PLAN.md), the current implementation, its fixtures and disposable probes. The product-gate code identity is `66ead9288690e51c0e9c5627b2c02445e8f775025c505016dd7c4e1853bc209a`, the same identity the executor's final gate recorded at 2026-09-26T03:59:04Z. No earlier verification report exists for this order.

**Verifier:** Claude Code 2.1.283, Claude Opus 5.5 (`claude-opus-5-5`). The operator selected session effort `ultracode`; `CLAUDE_EFFORT` reads `xhigh`, recorded as mode `subagents`. One workflow ran four read-only agents without descendants. Three were lenses that each judged a group (criteria 1–2; criteria 3–4; criterion 5, write-back, non-goals and records), and one refuted all three results and looked for anything missed. The root verifier ran both gates and the consequential probes, owns the judgment, and is the only writer of this report and of [WO-165-D006](../../evidence/WO-165/decisions.md#wo-165-d006). Harness readback: 4 subagents observed, 16 remaining against cap 20. The transport exposed no hard USD cap, so a USD 5 limit was unenforced.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.283","model":"claude-opus-5-5","effort":"xhigh","mode":"subagents","raw":"ultracode","source":"claude-session-readback"}

**Instruments under verification that recorded this work:** this order changes three instruments I also relied on: `scripts/test-entropy-review.mjs`, `scripts/authority-evidence.mjs` and `scripts/artifact-identity-evidence.mjs`. My judgments do not rest on their success alone. Probes outside those scripts compiled every route from `dist` and scanned the whole result. A probe independent of the executor's scripts rebuilt the before identity from the HEAD source. Receipts filed on the background and fake routes were rendered directly. The refuter read the 170-line authority-evidence change and found no weakening: the expected graph is rebuilt through named deltas only, and unrelated drift still fails. The gate runner and this session's hooks recorded the gate rows; this order does not change them.

**Verdict:** **Pass.** All five criteria are met by independent reproduction. Two minor defects (M1, M2) do not falsify a criterion. [WO-165-D006](../../evidence/WO-165/decisions.md#wo-165-d006) boards them, with the non-blocking notes N1–N12, under a named follow-up, and records the correction to D001. The verifier changed no implementation.

**Process cost:** entry 44465 tokens; handoff 9219449 tokens; source claude-transcript-message-usage

Counters are dispatch-scoped root-session observations, with entry cutoff 2026-09-26T04:04:02.780Z and handoff cutoff 04:42:13.105Z. They count repeated cached input, not unique context. Reasoning tokens and dollars are unknown. The four workflow agents reported 742,912 subagent tokens of their own. The harness lists them as 4 unlinked children, so no complete combined total is claimed. Process note: my full `npm test` repeated the executor's passing gate at the same code identity, 415.66 s of duplicate cost. WO-070 VER-001 records an operator correction against exactly that repetition. The gate's value here was confirming that identity, not new coverage.

## Evidence and checks

- `npm test`: 28 passed, 0 failed, 415.66 s, 72 fresh tasks, recorded 2026-09-26T04:12:25.793Z, evidence `host-gate:66ead928…:npm test`.
- `npm run test:docs`: 21 passed, 0 failed, 30.06 s, recorded 04:13:08.655Z at the same code identity. `git diff --check` exit 0.
- `node scripts/authority-evidence.mjs --check` twice: exit 0 both times, with identical `Verified … WO-165 serial-route migrations …` output. Neither run printed the `Retained` line, so the edition took the strict byte-comparison path. `node scripts/feedback-evidence.mjs --check`: `Retained live feedback audit docs/evidence/WO-070/feedback-001: component release labels moved since it was recorded; the judged behavior is unchanged.` The worktree status hash (`f292bf64…`) was the same before and after both runs.
- A direct probe of `compileReviewerWorkOrder` from `dist`, for claude-cli-print, codex-cli-exec, background and fake, reproduces every after-hash in `route-identities.json`: `e3505eb7f111ba22`, `30c4e134587e445f`, `e091eca53bad2e11` and `051216f1a90c7998`. Each has `resourceLimits {"probes":32}`. A scan of each whole result for `delegat|fan-out|fanOut|parallel|"All"` found only the N1 phrase.
- Agent reproductions (read-only, scratch outside the worktree, worktree status unchanged):
  - The HEAD `entropy-reducer.ts` with types stripped compiles to the recorded before hash `79151d7baebc5262` with the recorded before envelope.
  - `sibling-identities.mjs`, plus an independent three-input comparison, gives plan-refuter `e9f7e0080fcb9810` and mission-check `4c09a5d98f6bc433` unchanged.
  - `node scripts/test-entropy-review.mjs` passes 21 of 21, and the loadout test passes 15 of 15.
  - `artifact-identity-evidence`, `verification-evidence` and `feedback-evidence --check` each pass twice.
  - `authority-evidence --check --edition WO-070 --revision 001` fails as stale, which shows a new edition was required.
  - `console-fixtures.mjs --check` matches all five cases.
  - `scripts/entropy.mjs check` returns `ok`.
  - `meta.mjs --check` exits 0.

## Criterion judgments

**Criterion 1:** met. For both pinned routes, the compiled envelope's `allowedEffects` are `repo.read*`, `probe.run:scratch*`, `intake.capture` and `report.emit`. `resourceLimits` is `{probes: 32}`, the work order's allowed operations name no delegate effect, the lens node is a `Sequence` of `repo.read.lens` invocations with no resource, and `deferredProgramKind` is `null` (`packages/skeleton/src/loadouts/entropy-reducer.ts:983, 1144-1152`). The residue is byte-identical across routes and to `docs/instance/entropy-reducer/RESIDUE.md`. It says `Lenses are worked serially by the reviewer` and carries no delegate or parallel wording; N1 judges the one remaining `side routines` phrase. Receipts for background and fake episodes, filed end to end by an agent probe, render `Compiled review confinement (execution rule): lenses worked serially by the reviewer; no delegate grant or delegate resource limit. Checklist completion is not independently observed; worker result: <status>.` (`scripts/lib/entropy-review.mjs:645-650`). The live pinned routes take their compile inputs from the same `pending.compileInputs` path (`:1046-1047, 1257-1259`). The actual argv agrees: Claude gets `--tools`/`--allowedTools Bash,Read,Glob,Grep`, and Codex gets `--disable multi_agent` and `multi_agent_v2`, even at effort `ultra`. The WO-165 fixture (`scripts/test-entropy-review.mjs:1058-1200`) asserts the envelope, the residue and the receipt for each pinned route. M2 records that its residue negative control is narrow.

**Criterion 2:** met. `renderEntropyReducerResidue` emits `## Reviewer lens checklist` with one `- [ ]` item per brief, followed by Files, Questions, Output shape, Word budget and No-fix lines (`RESIDUE.md:17-39`). For every route, a request built as the fixture builds it yields an `entropyReviewPrompt` whose residue carries all four items, and 12 of 12 per-brief field comparisons are true. The prompt still carries the `lensBriefs` array. With `lensBriefs: []`, the unchanged protocol throws `profile-refused: invalid-result: entropy review lens briefs` (`packages/skeleton/src/entropy-review-protocol.ts:237-241`). `entropy-review-protocol.ts` and `worker-transport.ts` have no diff against HEAD. Multiline custom brief text is escaped onto one line, and sparse or non-array briefs are refused. See N3 for compile-time empty briefs.

**Criterion 3:** met. `docs/evidence/WO-165/route-identities.json` records the entry-source before hash `79151d7baebc5262` and envelope, plus the after hash and envelope for all four routes. The authority edition WO-165/001 records `routeMigration.previousSemanticHash` and `semanticHash`. The historical REVIEW-001 compile inputs recompute to `ecb0eca5675332f7`, the current test assertion, and to `114f13ac51e1c2f9` from HEAD source (N5). The REVIEW-001 receipt hash is unchanged. On the current tree, `entropy review --transport fake` runs review, receipt, refute, refutation receipt and dispose end to end (`the fake transport drives … end to end`, which passed in the root gate and in two agent runs).

**Criterion 4:** met. The authority edition re-mints deterministically: `--check` rebuilds `authority.json` and `bundle-diff.json` and compares them byte for byte (`scripts/authority-evidence.mjs:1076-1086`). The executor minted it at 23:31, it still matches, and two runs gave identical output. No feedback behavior source changed. Two listed feedback files, `package-lock.json` and `packages/skeleton/package.json`, changed only release labels (0.43.2 to 0.43.3, including the console pin). `evidenceSourceContent` normalizes those labels, and the feedback behavior identity `sha256:9e7a325d…` is equal on HEAD and working-tree content. The protocol and transport are unchanged, so the retained WO-070 live audit stands. D003 records why and claims no live episode, and the order's cost line names exactly those two files as the trigger for one (N6).

**Criterion 5:** met. `npm test` 28/0 and `npm run test:docs` 21/0 at code identity `66ead928…`; `git diff --check` clean. The full diff and every untracked file contain no newly introduced lint or type suppression and no attribution trailer.

## Write-back and non-goals

The write-back duty is discharged. D001–D005 parse and pass the `meta.mjs` validator, and their index rows match. `docs/instance/entropy-reducer/README.md:108-121` adds the route statement, and each of its claims matches the code. The non-goals hold:

- No new review route: all four names in `ENTROPY_REDUCER_ROUTES` were existing dispatch routes at HEAD.
- The lens content is unchanged: `entropyReducerLensBriefs` and its schema are byte- and JSON-equal to HEAD.
- The refutation selection rule is unchanged: no edit to the selection functions, and REFUTATION-PLAN.md changes wording only.

Release labels agree: v0.51.2 appears in the order title, README, roadmap, the work-order index and the release notes. Skeleton is 0.43.3 in its manifest, the lockfile and the console pin. No v0.51.2 tag exists, and the patch classification follows local precedent. The adjacent plan-refuter and mission-check pins are necessary: removing any pin changes those loadouts' complete compilations. They are minimal and grant no delegate authority (N12).

## Minor defects boarded in D006

**M1 — unsupported claim that the background route lacks a delegate tool (minor).** Criterion: none unmet; this is a records and comment accuracy defect in the order's objective area.

- Observed: `packages/skeleton/src/loadouts/entropy-reducer.ts:894-895` says `Every admitted route lacks a delegate tool`, over a list that includes `background`. D001's first rejected-option reason says `All supported routes lack a delegate tool`. The background route prints the prompt for a worker the session spawns (`scripts/lib/entropy-review.mjs:1122-1134`), and its attested tools are copied from `ENTROPY_REVIEW_TOOLS`, not observed. All five filed receipts with an actor line ran `route launched; transport claude-cli-print`; no background receipt exists. Planning refutation `docs/planning/refutations/2026-09-25-planning-c93346fb3a92bb7f-030.json` recorded this exact unpinned-route known issue for criterion 1, and no WO-165 record engages it.
- Expected: the claim is either evidenced or stated as unknown, with background compiling serial lenses by rule.
- Receipts do not overclaim: they label the sentence an execution rule and leave completion unobserved. D006 records the correction to D001.
- Reproduction: `grep -h -o -E "route \`[a-z-]+\`; transport \`[a-z-]+\`" docs/instance/entropy-reducer/runs/*.md | sort | uniq -c` prints `5 route \`launched\`; transport \`claude-cli-print\``.

**M2 — the fixture's residue negative control matches only the removed wording (minor).** Criterion 1 (the fixture clause is still met).

- Observed: `scripts/test-entropy-review.mjs:1084-1087` asserts that the residue does not match `/fan-out|four-delegate|delegate reads|delegated|Program.All/iu`. That regex matches the HEAD residue, so a revert is caught. It matches none of `Delegate up to four read-only lenses`, `delegate.readonly` or `4 delegates-maximum`.
- Expected: a control that rejects delegate wording generally.
- The envelope assertions use `startsWith("delegate")`, so authority drift is still caught.
- Reproduction: apply the regex to `git show HEAD:docs/instance/entropy-reducer/RESIDUE.md` (true) and to the three strings above (false each).

## Notes (non-blocking)

- **N1:** `RESIDUE.md:7` keeps the shared `entropy-reducer@1` identity invariant `side routines stay within declared budgets` (`entropy-reducer.ts:865`). It does not name a fan-out. `side routines` is the pattern library's general Lazzi vocabulary (`docs/product/05-pattern-library.md:385`), and Lazzi now names the lens checklist. Its retention is not recorded. Inference: changing it would version the identity that plan-refuter and mission-check share.
- **N2:** the receipt confinement sentence is keyed on the presence of `compileInputs.route` and does not read the envelope. It agrees now because every admitted route compiles serial; a future delegating route must make it read the envelope.
- **N3:** `compileReviewerWorkOrder` accepts `lensBriefs: []` and renders an empty checklist, as HEAD did. Only the protocol refuses, and no production caller passes custom briefs.
- **N4:** `route-identities.json` records neither the compiler version nor the generating command. Its values reproduce with the current compiler, 0.19.1, which is unchanged from HEAD.
- **N5:** the historical REVIEW-001 compile-input hash pair (`114f13ac…` to `ecb0eca5…`) is recorded only in the test diff.
- **N6:** criterion 4 holds in the repository's behavior-identity sense. Read literally, two feedback-listed files changed bytes; only the pins identity moved (`4157276d…` to `6c9dbd47…`).
- **N7:** the console loadouts input absorbed older WO-100 drift in its entropy entry. Its plan-refuter entry still differs from the current export, which predates this order. This was reproduced by a verifier agent; no test compares that input with current exports.
- **N8:** D004's evidence line naming plan-refuter and mission-check as all `entropyReducerLoadout` callers is imprecise. Evidence scripts and the harness test also call it.
- **N9:** the route-aware compile is degenerate. The route enters only the admission list and one known fact, so a later delegating route must reintroduce a support instead of selecting one. This is consistent with operator-review assumption 1 and D001's reopening condition.
- **N10:** refutation requests still carry the reviewer work order, whose constraint now reads `Work at most four read-only lenses serially as the reviewer`. The shape predates the order, and the refutation receipt renders no confinement sentence.
- **N11:** the sparse-array tightening in `requiredStringArray` reaches its 15 callers. JSON-parsed input cannot produce sparse arrays, so behavior on valid input is unchanged.
- **N12:** the derived judges keep the `delegate` tag as vocabulary only; the compiler reads tags for support compatibility. The private package exports change shape (`fanOutLens` becomes `lensChecklist`, and `deferredProgramKind` becomes `null`), with no in-repo consumer of the old name.

## Goal alignment and limits

Mission and critical path: the corrected envelope, residue and receipt remove a standing misdescription from every review and refutation episode on the pinned routes. Nothing new is granted, and there is no gate or operator rescue. My comparison of pass-with-boarding against fail-for-repair, the eight system traps, Naive Interventionism and NoOp is recorded in D006. The deciding evidence is that no criterion is at stake and receipts do not overclaim.

Limits:

- No live Claude or Codex review was launched. Argv construction was checked, not model behavior.
- Per-lens completion stays unobserved, as the receipt says.
- The background worker's tool surface remains unknown (M1).
- The executor's three-agent adversarial pass and its timing are attributed to executor evidence, not rerun.
- Counters exclude the subagents' own totals, as stated above.
