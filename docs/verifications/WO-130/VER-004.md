# WO-130 VER-004 — independent verification after the third repair

**Verdict: pass.** No blocking findings remain in the reviewed subject. VER-001 F1/F2, VER-002 F1 and VER-003 F1 are closed by current executable evidence. The accepted replica residual and the recorded limits below remain.

Subject: [WO-130](../../work-orders/WO-130-declared-suite-inputs-replica.md), branch `wo-130`, after the repair completed at 2026-09-13T20:10:30.150Z and preserved by `refs/dotln/checkpoint/WO-130/14`. This operator's `resume: verify` allocated VER-004 at 2026-09-13T20:31:37.334Z with checkpoint `/15`. The subject includes the original replica deliverable and the D007–D015 repair expansions. This verifier changes no implementation and creates no branch commit.

Verifier: Codex CLI 0.154.0, observed with `codex --version`; GPT-6 Astra at max under the repository's operator-attested default, meeting the order's xhigh-or-above minimum. No effective-session readback is claimed.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.154.0","model":"GPT-6 Astra","effort":"max","source":"operator-attested"}

## Earlier findings

- **VER-001 F1/F2: closed.** All 26 suite-evidence cases pass with synthetic injected `GIT_CONFIG_COUNT`, `GIT_CONFIG_KEY_0=safe.directory`, a candidate-root `GIT_CONFIG_VALUE_0` and `GIT_CONFIG_PARAMETERS` carrying the original inert proxy-method configuration class. The projection omits that family from both execution and identity. Refused narrowing has a named, conservative candidate-tree key distinct from replica execution. The deterministic-repository fixture requires both replica roots before invoking Git. The three-role composition regression also passes with the same injection.
- **VER-002 F1: closed.** The current generated-hook fixture runs against the real lifecycle. A live gate and a live foreign writer each refuse the prompt with the equivalent ordinary command's refusal reason, before changes to events, the control projection, checkpoint refs, report allocations or phase. An uncontended dispatch records once and owns the writer. Read-only `next`, illegal prompts and manifest-policy mismatch retain their checked behavior.
- **VER-003 F1: closed.** I extracted the executable reproduction from [VER-003](VER-003.md), inverted its two missing-delivery assertions and added assertions that the resumed session names the same supports and takes no reservation. Both sessions now receive Adjacent Repair, Intent to Act, Decision Receipts, Operator Check-In and Follow-up Queue, plus the opening-intent instruction. After releasing the first synthetic session's writer, the second `resume: fix` leaves events, the control projection and checkpoint refs byte-identical, stays in `repairing` and reserves nothing. The current real-lifecycle fixture additionally checks byte-identical briefings and report allocations. The stub fixture refuses a resumed dispatch whose briefing command is unavailable.

The D015 mechanism was read in `scripts/resume.mjs` and `harness-host.ts`: the lifecycle shares briefing builders between dispatch and read-only projection, and both hook branches share delivery. A direct `npm run resume -- briefing` in this worktree names VER-004 and leaves the candidate tree and writer record unchanged. These checks establish hook output and lifecycle behavior; they do not observe the desktop renderer or prove compliance by later chat text.

## Acceptance evidence

| Criterion | Observed result and evidence |
| --- | --- |
| 1 — replica construction and execution boundary | Current fixtures reproduce declared contents, preserved regular-file modes and an in-set link; one verified read-only installed copy; unlistable parents; and no candidate-root path in child cwd, arguments or environment. The constructor, projection, installed-input inventory and runner dispatch were read. The operator's accepted unprojected absolute-path/same-user residual is unchanged. |
| 2 — guarded reads and escaping link | Current fixtures pass for existence, regular-file and declared-flag guards across absent, present, directory and changed undeclared inputs. Outcomes and keys stay equal; flipping the declared flag changes the key while the fresh replica still cannot read the undeclared file. Escaping links refuse narrowing. This covers the WO-130 holds selected from planning receipts 009 and 010. |
| 3 — migrated scopes | The retained host measurement has 78 passing task rows, including 47 replica executions and all 40 release cases. Kernel, compiler, skeleton, worktree, planning fixtures, process-debt and release preparation are explicitly recorded in replicas. The original and repair execution records name the outcomes; none of these scopes was retained whole-tree in that measurement. |
| 4 — identity and invalidation | Current cases reproduce package reuse after an unrelated script change, invalidation after declared additions, mode/name changes and command changes, and diagnostics naming changed paths. Source inspection confirms declaration/mechanism, selected entries, installed inputs, toolchain, host configuration and projected environment participate. Candidate execution after refusal cannot reuse replica evidence. |
| 5 — loud failure without retry | The unconditional read passes in the candidate and fails in the replica, naming `undeclared.txt`. Release preparation cannot import its undeclared guarded input. Setup/execution errors do not retry in the candidate. Cleanup fault cases also pass. |
| 6 — operator-host comparison | Independently validated the retained measurement's rows, restored tree, package source keys against the gate store, and exclusion of the probe path from all three package declarations. Fresh full: 798.414 s; one verified 50,685,411-byte/1,099-file copy in 240.348 ms; replica setup 22,665.920 ms. The source-change `npm test` passes in 51.917 s with three packages reused, retaining 199.889 s of original package execution. These are retained executor measurements, not new timings from this verifier; different inventories do not establish a wall-clock speedup ratio. |
| 7 — write-backs and checks | Product 07, decisions D001–D015, the decisions index and follow-up register carry the changes and reopening conditions. The dependency diff only changes skeleton's version to 0.15.8; no dependency or source suppression directive was added. Targeted tests and pre-filing `git diff --check` pass. The canonical completion binds the final full-suite and diff evidence to this report's final bytes, as described below. |

## Executed checks and final evidence

- `npm run build --silent`: passed for compiler, console, kernel and skeleton.
- `node --test scripts/test-suite-evidence.mjs` with the synthetic Git injection above: **26 passed, 0 failed; 89.820 s**. Local transcript: `docs/control/local/wo130-ver004-suite-tests.txt`.
- `node --test --test-name-pattern='three-role|WO-130' scripts/test-process-debt.mjs` with the same injection: **3 passed, 0 failed; 23.553 s**. Local transcript: `docs/control/local/wo130-ver004-process-tests.txt`.
- Corrected VER-003 reproduction: **exit 0**, both support lists and intent instructions present, lifecycle unchanged and writer unreserved. Local executable/result: `docs/control/local/wo130-ver004-resumed-repair-probe.mjs` and `.json`. Its base executable remains in the immutable prior report; only the two defect expectations and the two additional closure assertions differ.
- Direct selected-order briefing: **exit 0**, VER-004 named, candidate tree and writer unchanged; local result `docs/control/local/wo130-ver004-briefing.json`.

The final command is `npm run harness -- evidence`, after this report is authored. Its transcript is `docs/control/local/wo130-ver004-harness-evidence.txt`. The `VerificationCompleted` event for VER-004 supplies the exact report-bearing tree and requires successful `npm run test:full` and `git diff --check` records at that tree before recording this pass. Final gate timing and reuse counts belong to those observations, avoiding an invented measurement or a report edit after the gate. No separate fast-gate timing is claimed for this dispatch.

## Limits, judgment and process cost

The runner, replica constructor, observer and cache under verification produced the suite and retained host evidence. The built host and generated hooks under verification produced the dispatch observations; assertions compared their outputs with actual synthetic lifecycle bytes. The lifecycle under review also allocated this report. Codex uses explicit begin/observe/delivered calls, not automatic hooks. One writer was registered through the generated adapter; its short-lived parent-process observation is not evidence of durable liveness. Synthetic guard cases use an explicitly live owner.

VER-001 O3's conservative directory-mode difference and O6's per-session environment limitation remain documented. No cross-role reuse improvement or kernel filesystem sandbox is claimed. Newly required inputs for D015 were the shared lifecycle briefing builders, hook delivery/classification, Contributor procedures, generated hook consumers and executor-readiness helper. Prior findings, cited product sections, the selected planning holds and retained measurement sources supplied the remainder of the scope. No clean-room stop condition was observed.

Mission and critical path: dependable evidence and resumed-session delivery support the independently verified source-to-deliverable loop. Observed replica behavior and package reuse support the original benefit; the two-session reproduction now supports D013/D015's delivery promise. NoOp on implementation is appropriate for the verifier. NoOp on the prior finding is rejected because its closure is now reproduced; no new control or repair is justified by the remaining documented limits.

All eight traps were compared at this scope: **policy resistance** requires preserving guards and no-repeat behavior; **commons depletion** favors focused reproductions before one final gate; **drift to low performance** requires both initial and resumed delivery; **escalation** argues against another operator step; **success to the successful** does not privilege the new helper over its observed outputs; **shifting the burden** is reduced when the replacement session receives its briefing automatically; **rule beating** is checked by real lifecycle equality and adversarial guard cases; **seeking the wrong goal** is judged by delivered information and sound reuse rather than receipt counts. Naive Interventionism preserves the useful isolation, checkpoints and conservative refusal behavior. Reopen on a same-key fresh/reused contradiction, an unprojected candidate read, a guard bypass or a supported resumed dispatch missing its applicable briefing.

Entry collection initially ran before registration and was repaired by beginning this session and collecting successfully: **51,696 total tokens**, including 48,128 cached input, source `codex-transcript-counter`, scope `dispatch`, observed 2026-09-13T20:31:22.466Z. At report preparation the same source/scope reports **2,655,905 total tokens**, including 2,538,112 cached input, observed 2026-09-13T20:39:42.624Z. The window starts at explicit registration after routing reads, includes useful work and waiting, and is cumulative usage rather than context occupancy. Completion recollects a later cutoff. Wrapped command count and dollar cost are unavailable. The two focused test processes total 113.373 s; no equivalent alternative was measured, so no numerical efficiency claim is made.
