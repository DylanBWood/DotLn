# WO-152 VER-002 — independent verification

**Verdict:** pass. The repair discharges VER-001 F1: the authority and
feedback editions are re-minted at the current subject, the feedback edition
retains one completed live self-host result after two recorded refused
attempts, and both edition checkers pass. The schema repair, regression,
component bump, real `claude-cli-print` mission-check row and full product gate
also pass. I found no current substantive defect.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-5.6-sol","effort":"xhigh","source":"codex-session-readback"}

Human actor: the operator dispatched `resume: verify` in an attended Codex
session. The harness supplied the current model, effort and CLI version. The
fan-out plan was one verifier and one writer in this worktree; no subagents
were spawned, so observed use is 0 of the cap of 20 and any unobserved remainder
is unknown.

**Process cost:** entry 39617 tokens; handoff 3230265 tokens; source codex-transcript-counter

Both readings have dispatch scope. Their cutoffs are
2026-09-22T14:02:02.351Z and 2026-09-22T14:15:01.246Z. Cost in USD is
unavailable. These are observed cumulative counters, not limits; unavailable
values did not block the verdict.

## Subject

- Work order: `docs/work-orders/WO-152-mission-check-schema-ids.md`.
- Branch `wo-152`; `HEAD`, `origin/main` and their merge base are
  `5b4b99cab19eacaeb3d09775480c7ad2fa6dff5e`. The implementation and evidence
  remain uncommitted as required before final review.
- Verification dispatch: `VER-002`, checkpoint 7. VER-001 failed only F1 and
  routed the missing criterion-4 evidence to repair; the current control phase
  was `verifying` with `verification-result` as its sole legal action.
- I read the order, its cited product and evidence sections, VER-001, the
  implementation and repair receipts, D001–D009, the authored source and tests,
  the current live row and the current edition outputs before judgment.
  Oversized generated logs were parsed for their event types, attempts and
  terminal rows and were validated by their repository checkers.
- This verifier changes no implementation or decision record. Its only authored
  output before the result transition is this immutable report.

## Goal-aligned outcome

The subject advances the critical path through an interface consumed outside
the repository: the emitted JSON Schema now names each contract-clause id once,
the Claude print transport accepts it, and the resulting live receipt is usable
without this session. The repair also restores the evidence contract VER-001
enforced, instead of treating a passing staleness check as a substitute for a
required re-mint and live episode. NoOp would leave a required deliverable
absent and keep the order in repair, so pass now wins only because the missing
work exists and validates.

The systems-trap comparison supports that judgment. Policy resistance between
D004's optimization and criterion 4 was resolved by D008's explicit correction.
Passing retained evidence before repair would have enabled rule beating, drift
to a lower completion standard and seeking lifecycle completion rather than the
selected outcome. The repair does not privilege the already-invested retained
edition under success-to-the-successful, and it removes rather than shifts the
burden of reconciling the criterion to final review or the operator. Tragedy of
the commons and escalation are material because the live feedback evidence took
three paid attempts; the append-only record accounts for all three, the accepted
attempt ends the requirement, and this verification launches no fourth attempt.
No new guard or local workaround competes with another guard, so no additional
policy-resistance loop is introduced.

Under Naive Interventionism, the useful release-label normalization remains
unchanged, the repair adds evidence editions and fixture projections rather
than another behavioral source edit, and the current selectors can be replaced
by a later edition without rewriting history. The smallest sufficient probe is
the current strict edition check plus the already-recorded live stream; repeating
either paid external episode would add cost without testing a changed subject.

## Instrument disclosure

- The WO-152 schema regression is part of the subject. I inspected its generic
  seven-enum walk, literal first-seen lists, renumbered-contract union and
  validator assertions, then ran it independently after a fresh build.
- `scripts/evidence-resident-binding.mjs` produced the filed mission-check row
  and is unchanged by the order. The row establishes the recorded launch; the
  focused regression and product gate establish the current schema behavior.
- The authority and feedback evidence generators are the mechanisms whose
  outputs criterion 4 requires. I did not rely only on their green result: I
  compared the new editions to WO-149's selected editions and parsed the fresh
  feedback streams. The authority JSON is byte-identical, its bundle record
  changes only the comparison label, and the feedback JSON changes only its
  subject hash.
- The live feedback verifier is also an instrument under verification. Its
  append-only stream contains three starts, two `invalid-result` interruptions
  and one completed result with both criteria passing; all attempts and their
  usage remain disclosed rather than only the successful one.

## Executed checks

| Check | Result |
| --- | --- |
| Fresh build and focused WO-152 regression | Pass: 1/1 in 221.34 ms; no duplicate item across 7 emitted enums for unchanged and renumbered contracts. Source inspection confirms the previous concatenation would fail the exact six-id assertion before the generic scan. |
| Authority edition | Pass: two unchanged programs, both named migrations, four widening rejections, nine runtime denials, grant restoration and 34 bundle comparisons. Independent comparison confirms `authority.json` is byte-identical to WO-149 revision 002 and `bundle-diff.json` changes one comparison-label line. |
| Feedback edition | Pass: 10 passing regressions, 10 removal failures and 1,192 saved instruction bytes. The checker no longer reports retained immutable evidence; the new edition's JSON differs from WO-149 feedback-002 only at `subject`. |
| Fresh self-host stream | Parsed 642 events: 3 `WorkerAttemptStarted`, 2 `WorkerInterrupted` with reason `invalid-result`, 1 completed result and both criteria passing. The three ignored usage rows independently total 1,740,588 tokens, USD 4.2363984 and 631,183 ms of launch wall-clock, matching the repair record. |
| Console evidence | Pass: all five JSON, terminal and HTML cases match after the self-host re-pin. |
| Live mission-check row | Parsed successfully: one exit-0 `claude-cli-print` run, model `claude-haiku-4-5-20251001`, selected effort `xhigh`, CLI 2.1.278, validated `drift` verdict, no failure or block, 14 changed paths, 0 omitted paths and 0 changed ignored entries. |
| Full product gate | `npm test` passes **25 suites, 0 failed, 319.53 s, 69 fresh tasks**. |
| Release and repository checks | `check-surfaces --local` passes all 44 rows at v0.41.1 / skeleton 0.35.1; console evidence, publication, metadata and formatting checks pass; `git diff --check` is clean. |
| Dependency and suppression review | No third-party dependency changes. Manifest and lockfile changes only move the skeleton version and console/workspace pins. No new lint, type or formatter suppression appears in the authored source/test diff. |

## VER-001 F1 — resolved — required evidence re-mint and live self-host episode

**Criterion and expected result.** Criterion 4 requires a skeleton component
bump, new authority and feedback editions with one live self-host episode, the
reason each edition changed, decision sources and reopening conditions, and no
edit to `docs/evidence/WO-148/decisions.md`.

**Prior observation and severity.** VER-001 found the versions and write-backs
present but both editions and the live episode absent. It classified that gap
moderate because the behavioral schema repair was correct while a required
deliverable remained knowingly incomplete.

**Current observation.** D008 corrects the earlier waiver and the repair receipt
names the reason each edition changed. `docs/evidence/current.json` selects
WO-152 revision 001 for both authority and feedback. The authority and feedback
files exist, their strict checkers pass, the feedback logs contain the live
episode, console fixtures are re-pinned, and `docs/evidence/WO-148/decisions.md`
has no worktree diff.

**Reproduction.** Compare each WO-152 edition with WO-149's prior selected
edition, parse the feedback JSONL for attempt and terminal events, run both
edition checkers, and inspect `docs/evidence/current.json`. Those steps produce
the observations above and remove F1's reproduction condition.

**Disposition.** Resolved; there is no current finding or remaining criterion-4
repair.

## Acceptance criteria

| # | Verdict | Independent evidence |
| --- | --- | --- |
| 1 | **Met.** | The contract-clause branch uses first-insertion-order `Set` deduplication. The focused regression verifies six unchanged ids and the pinned-first seven-id union after renumbering; thesis and exclusion outputs are unchanged. |
| 2 | **Met.** | The generic scan reaches all 7 enums and finds no duplicate in either subject. Literal list assertions prevent a vacuous empty-list pass. Source comparison and the recorded negative control establish that restoring concatenation fails the exact list assertion. |
| 3 | **Met.** | The filed real-store row records one accepted print-transport invocation with CLI/model/selected-effort provenance, a returned validated verdict, no failure or block and bounded timing. Activation records authorization for the launch. |
| 4 | **Met after repair.** | Skeleton is 0.35.1; both WO-152 revision-001 editions exist and are selected; the live self-host stream and measured reasons are recorded; D008/D009 carry sources, correction, follow-up and reopening conditions; WO-148's decisions file is untouched. |
| 5 | **Met.** | The independent product gate is green, `git diff --check` is clean, no dependency or suppression is added, and the regression adds one test step to the existing mission-check file without adding a suite or fresh gate task. |

## Limits and handoff

- I did not repeat either paid external launch. Their immutable rows establish
  the historical launches; current repository checks and the full gate establish
  the subject now being passed.
- The mission-check row's 46.8-second episode duration is a bound from store
  samples, not an exact duration. Its effort is selected/attested rather than an
  effective readback, as the filed evidence states.
- The first two feedback verifier attempts preserve only `invalid-result`, not
  the typed contract detail. This diagnosability defect is outside WO-152's
  transport non-goal and is already owned by WO-152-D009 and public follow-up
  `FUP-ed431cc1c8ca9a52`; it is not evidence that the schema was refused. The
  adjacent queue's generic `planning` target remains a recorded advisory, while
  the public follow-up exists and `meta --check` passes.
- The authority and feedback re-mints are identity changes, not evidence of new
  authority or feedback behavior. Final review must still judge the integrated
  subject and may re-mint again if integration changes a registered source.
