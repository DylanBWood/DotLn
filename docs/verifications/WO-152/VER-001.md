# WO-152 VER-001 — independent verification

**Verdict:** fail. The schema repair, regression, component bump, real
`claude-cli-print` row and full product gate all pass. One substantive finding
(F1) routes to repair because required acceptance criterion 4 was knowingly
left incomplete: neither evidence edition was re-minted and the required live
self-host episode was not run.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-5.6-sol","effort":"xhigh","source":"codex-session-readback"}

Human actor: the operator dispatched `resume: verify` in an attended Codex
session. The harness supplied the current model, effort and CLI version. No
subagents were spawned; the explicit fan-out plan was one verifier and one
writer in this worktree, so observed use is 0 of the cap of 20 and any
unobserved remainder is unknown.

**Process cost:** entry 40668 tokens; handoff 2031074 tokens; source codex-transcript-counter

Both readings have dispatch scope. Their cutoffs are
2026-09-22T13:10:45.853Z and 2026-09-22T13:22:42.829Z. Cost in USD is
unavailable. These counters are observations, not limits, and the unavailable
remainder did not block the verdict.

## Subject

- Work order: `docs/work-orders/WO-152-mission-check-schema-ids.md`.
- Branch `wo-152`; `HEAD` and `origin/main` were both
  `5b4b99cab19eacaeb3d09775480c7ad2fa6dff5e`. The implementation and evidence
  remain uncommitted as required before final review.
- Verification dispatch: `VER-001`, checkpoint 3. No prior WO-152 verification
  report exists.
- I read the order, its cited product/evidence sections, the complete subject
  diff, implementation and tests, D001–D006, the live row and current authored
  outputs before judgment. This report adds no implementation change. D007
  only boards F1 and its bounded repair ownership; generated indexes follow.

## Goal-aligned outcome

The code change advances the critical path: the emitted schema is the interface
consumed by the external CLI, the repository exercises it in a regression, and
the filed live row is usable without session context. NoOp would leave every
print-transport mission check rejected before a model call, so the repair itself
is warranted.

The decision to fail is also goal-aligned. Policy resistance is present because
the executor's evidence-retention optimization conflicts with an explicit
required criterion. Passing would normalize lower performance and permit rule
beating: green staleness checks would stand in for a re-mint and live episode
that never occurred. It would shift the burden to final review or the operator
to discover and resolve the waived requirement, and would seek completion as a
proxy instead of the selected order's whole outcome. Success-to-the-successful
does not justify privileging the retained-edition machinery over the activated
contract. Tragedy-of-the-commons and escalation concerns are real—the live
episode consumes shared compute, operator time and model spend—but the order
budgeted that work and the verifier cannot silently trade it away. The current
schema fix neither widens authority nor creates a guard likely to undo another
guard, so the remaining policy-resistance risk is the recorded contract
conflict itself.

Under Naive Interventionism, the passing retained-edition checks are useful
functions to preserve, the prior comparable live audit cost 320.6 seconds, and
the smallest reversible next step is either the already-authorized criterion-4
repair or an explicit operator scope expansion that amends the order. NoOp
leaves a required deliverable absent and therefore loses. This comparison does
not authorize the verifier to choose between repair and amendment for the
operator.

## Instrument disclosure

- The new WO-152 regression is part of the subject and produced both the
  focused and full-gate results. I checked its generic enum walk, non-vacuous
  seven-enum guard, literal first-seen lists, changed-contract union and
  validator assertions in source rather than relying only on the green row.
- `scripts/evidence-resident-binding.mjs` produced the filed live row. It is not
  changed by this order. I validated the filed row's required success fields,
  counts-only limits and absence of a physical path; the row remains evidence
  of that recorded launch, not proof of the current report or this verdict.
- The authority and feedback checkers are the mechanisms D004 relies on. Their
  passing results establish behavior-equivalent retained editions after
  release-label normalization; neither command performs the re-mint or live
  self-host episode that criterion 4 separately names.

## Executed checks

| Check | Result |
| --- | --- |
| `npm test` | Pass: **25 suites, 0 failed, 280.12 s, 69 fresh tasks**. |
| Focused WO-152 test | Pass: 1/1 in 221.95 ms; the step reports no duplicate item across 7 enums for unchanged and renumbered contracts. |
| Regression strength | Source inspection confirms the exact six-id assertion is evaluated before the schema scan; restoring the prior concatenation would return the two identical six-id lists and fail that assertion. The executor's recorded negative control observed that failure. |
| Live row | Parsed successfully: one exit-0 `claude-cli-print` run, model `claude-haiku-4-5-20251001`, effort `xhigh`, CLI 2.1.278, verdict `drift`, no failure/block, 14 changed paths, 0 omitted and 0 changed ignored entries. No physical path is filed. |
| Edition checks | Both pass. Authority verifies 34 bundle comparisons. Feedback explicitly retains the immutable audit because behavior source is unchanged apart from component release labels, then verifies ten passing and ten removal regressions. |
| Release and diff | `check-surfaces --local` passes all rows at v0.41.1 / skeleton 0.35.1; `git diff --check` is clean. |
| Dependency and suppression review | No third-party dependency changes. The manifest changes only move the skeleton version and console/workspace pins. No new lint, type or formatter suppression is introduced. |

## F1 — moderate — required evidence re-mint and live self-host episode are absent

**Criterion and expected result.** The order labels all five criteria required.
Criterion 4 says the skeleton component version moves; the authority and
feedback editions are re-minted with one live self-host episode and the reason
each changed; the decisions file records sources and reopening conditions; and
WO-148's decisions file remains untouched.

**Observed.** The version moves and the decision/write-back portions are
present. D004 and the implementation report explicitly state that neither
edition was re-minted and no live self-host episode was run. The repository has
no operator `scope expand:` authorization or `amend-order` event that changes
criterion 4.

The executor's factual premise is supported: `evidenceSourceContent` normalizes
component release labels, and both edition checks pass while reporting retained
behavior. That proves the existing editions are not stale under the current
checker. It does not prove that the required re-mint and live episode happened;
the reports expressly confirm they did not.

**Reproduction.** Read criterion 4, then D004 and
`docs/evidence/WO-152/implementation.md` §Editions. Run the two edition check
commands: both return success for retained evidence, and neither creates a new
WO-152 edition or records a WO-152 self-host episode.

**Expected.** Repair must satisfy criterion 4 as written and record the new
edition/live evidence, or an explicit operator scope expansion must amend the
order through the required planning receipt before a fresh verification judges
the amended contract. A verifier cannot substitute behavioral equivalence for
a named required deliverable.

**Why consequential.** Passing would claim all required obligations are
discharged when the subject itself says one is not. That breaks the lifecycle's
no-partial-completion guarantee even though the implementation behavior and
current retained evidence are green.

**Severity:** moderate. The delivered schema behavior is correct and the
existing evidence checks pass, but the order cannot advance to final review
with a required acceptance obligation missing.

**Ownership:** boarded in
[WO-152-D007](../../evidence/WO-152/decisions.md#wo-152-d007), which names the
bounded repair and the explicit-amendment alternative.

## Acceptance criteria

| # | Verdict | Independent evidence |
| --- | --- | --- |
| 1 | **Met.** | The contract-clause branch uses first-insertion-order `Set` deduplication. The focused regression verifies the six unchanged ids and the pinned-first seven-id union after renumbering; thesis and exclusion lists are asserted unchanged. |
| 2 | **Met.** | The generic walk reaches all 7 enums and finds no duplicates for either subject. Literal list assertions prevent an empty-list false pass. The prior concatenation necessarily fails the first exact list assertion, matching the executor's negative control. |
| 3 | **Met for the required live row.** | The filed real-store row records one accepted print-transport invocation with CLI/model/effort readback, a returned validated `drift` verdict, null failure/block and bounded timing. The activation records the launch authorization. |
| 4 | **Not met (F1).** | The skeleton version, decisions and WO-148 immutability portions are met. D004 and the implementation report explicitly state that both edition re-mints and the live self-host episode were omitted. |
| 5 | **Met at this code subject.** | `npm test` passes 25/25, `git diff --check` is clean, no dependency is added, and the new test adds one step to the existing mission-check file without adding a suite or fresh gate task. |

## Limits and handoff

- I did not repeat the paid external mission check. The filed receipt and the
  unchanged collector/source path establish criterion 3's historical launch;
  the independent focused test and full gate establish the current schema.
- The live row's `drift` verdict does not fail criterion 3, which requires a
  returned verdict. Its criterion-4 finding agrees with F1; the executor also
  recorded that agreement rather than concealing it.
- This finding does not request a schema, validator, resident-host or transport
  change. The repair boundary is the missing criterion-4 evidence or an
  operator-authorized amendment of that criterion.
- After repair, rerun the edition checks, focused mission-check regression,
  full product gate and `git diff --check`, then verify the original or amended
  criterion 4 from a fresh immutable report.
