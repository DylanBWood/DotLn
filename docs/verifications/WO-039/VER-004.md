# VER-004 — WO-039 Harness lowering (v0.15.0)

**Verdict: pass.** The current subject satisfies the eight acceptance criteria. The fourth repair closes VER-003's two reservation races and failed live-evidence finding; the earlier repairs remain covered by passing regressions. No new substantive finding was established. No implementation change was made by this verification.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.153.4","model":"gpt-6-astra","effort":"max","source":"operator-attested"}

Codex CLI reported `0.153.4`. Model and effort use the repository's operator-attested GPT-6 Astra / max selection; no effective-session readback is claimed. This satisfies the order's verifier xhigh+ minimum. No account label was supplied.

## Subject and instrument

- Authority: [WO-039](../../work-orders/WO-039-harness-lowering.md), its cited sections, the incremental-private-list amendment and [ideation receipt](../../evidence/WO-039/ideation.md). The current repair claim is the [fourth repair receipt](../../evidence/WO-039/repair-004.md). [VER-001](VER-001.md), [VER-002](VER-002.md) and [VER-003](VER-003.md) remain immutable inputs.
- Subject: the dirty `wo-039` worktree at `HEAD` `8b55eca3b3427146e98d34c68f67f679dc59bea5`. Physical cwd and Git root agree, Git registers the selected worktree once, and its old writer reservation reports a dead owner. This verification used one writable agent. No branch commit was made.
- Allocation: `VerificationRequested` at `2026-09-08T07:03:45.954Z`, checkpoint `8acb1d6566de02123741c922b62d76bce30d497f`, `refs/dotln/checkpoint/WO-039/17`.
- Reviewed host source SHA-256: `9088bb18667602fee9808338d28ee8b28935120f1513db215bdca7550423a6e9`. Executed built host SHA-256: `0eeb17193909408f59ac46f8263e7e3a0a7ff6fa28908b9f30c60a4e2c75e4eb`. Installed manifest SHA-256: `869f036579eb857f3e93af19bf640fd7aeb0ab3b8d6d5a75d5a36701ea40da23`.

**Instrument disclosure.** The generated Codex verifier skill and shared instruction file are part of the subject. Codex hooks remain declared unavailable, so this session is not a live Claude enforcement episode and claims no Claude output-read receipts. The subject's `harness evidence` runner executed the fixed checks; I inspected its implementation and results alongside source, existing boundary tests, direct regeneration and independent comparisons. Canonical `resume.mjs` is unchanged and allocated this report. Only this report and required control/index bookkeeping were written in the selected worktree; builds and evidence receipts are local generated state. No real writer reservation, implementation, user setting or previous report was changed.

## Earlier findings

| Finding | Current observation and reproduction | Judgment |
| --- | --- | --- |
| VER-003 F1 — stale reclaim removes refreshed facts | The generated-hook regression pauses B after it classifies A's old owner dead, admits A's refresh, then resumes B. Refresh writes a new nonce-named instance; B cannot retire that instance through the old name and is refused. The inverse schedule also passes: a refresh landing after another holder acquires withdraws and refuses, preserving the replacement. | Closed. The observed schedules now preserve the outstanding authorization. |
| VER-003 F2 — operator release judges one holder and retires another | The public CLI regression pauses release of an observed dead holder while a generated hook admits a replacement. Unforced release re-observes and refuses the live holder; forced release refuses the changed reservation. No misleading `operator-released` row is recorded. The uncontended release still retires and journals the holder it judged. | Closed. Decision, retirement and journal are bound to one observed instance. |
| VER-003 F3 — latest live-holder smoke fails | The checker selects the newest schema-4 records against the repaired runtime, including `writer-foreign-live-003`. That record proves the scenario's required writer refusal, with no acquisition or reclaim and the foreign holder preserved; it also happened to reach the attribution refusal. The four role records and dead-holder record still require attribution refusal. The evidence README links these current records. | Closed. The scenario-specific required effect remains a denied effect under criterion 4; the separate attribution guard is still exercised. Failed attempt 002 remains unchanged. |
| VER-002 — concurrent dead-owner recovery admits two writers | The generated-hook regression holds the first admitted writer's work outstanding while a second stale reclaimer resumes. The second cannot retire the newly named instance and is refused. | Closed; regression passes together with the refresh and operator-release schedules. |
| VER-001 — aggregate finish parity, constrained smoke, incomplete read receipts and drift coverage | Passing fixtures exercise aggregate finish against the equipped boundary and after removing each eligible unit; observation-only native Read accounting with alternate routes bounded; complete current-byte receipts across gaps, stale deliveries, inherited output counts and oversized Unicode lines; and content, missing, unexpected, manifest, ownership and path-safety drift cases. | Closed within the documented host scope. Current live records have no refused native Reads or outside reads/attempts. |

These reproductions ran through the generated hooks or public CLI in `scripts/test-harness.mjs` during this verification's full gate. I also reviewed the reservation observation, refresh, retirement and release paths and their fixtures. The protocol remains same-user bookkeeping with the filesystem, process-liveness and interrupted-refresh limits stated in the repair receipt; this verdict does not assert hostile-process isolation or exhaustive concurrency proof.

## Acceptance evidence

| Criterion | Expected and observed |
| --- | --- |
| 1 — observed harness capabilities | The [phase-zero report](../../discovery/harness-smoke-2026-09-07.md), JSON index and per-harness observations record Claude C1–C8 and Codex X1–X3. Consumed profile capabilities cite those rows. Claude's four events, refusal semantics and project skill were observed; Codex project skills were observed while hooks/settings remain unavailable. Bare authentication and skill resolution are not claimed as successful. |
| 2 — pure lowering and identity | Compiler tests pass for frozen-input determinism, origin and manifest identity, all ten units' lowering/residue, single-unit change locality and unsupported-capability refusal. Direct re-emission verified both profiles' manifests and matched all 24 installed files byte for byte. Seiri and Entropy Reducer retain their exact semantic hashes. Package/lock changes add no runtime dependency. |
| 3 — generated-hook parity | All 17 host fixture tests pass, including synthetic generated-hook decisions compared with the existing boundaries and unit-removal checks. Aggregate completion covers its three eligible feedback units. Missing built adapters fail before effects. Reservation regressions additionally test that concurrent observations preserve the ownership the predicate judges. |
| 4 — current live evidence | The four current role smokes resolve their role skill, refuse the fixture attribution effect, finish their session-wide observer, and record zero native Read refusals, outside reads or outside attempts. The two writer scenarios pass on the same runtime. The correction token is unconfirmed; each role reaches the correction unit through its skill and names `operator.correction-token.confirmed` as unavailable. Current records state Claude Code 2.1.263, the `claude-fable-5` launch selection, observed models and effective `xhigh`. No new live model episode was launched by this verification. |
| 5 — installed self-host bundle | Independent emission to a fresh temporary directory produced 24 files and zero byte differences, including the entire instruction file and manifest. `harness check` passes in `npm test`; drift and refusal fixtures pass. The config log and dated ADR-0005 amendment are present. Emission stays in project scope; no user-scope mutation was made or required. The reviewed installation remains pending the normal final-review commit. |
| 6 — directional context | `harness-context --check` recomputes the stored measurement from the instruction/skill directives, including late reads. All eight profile/role rows are strictly lower in bytes and lines. The instruction alone falls from 3,878 bytes / 78 lines to 3,593 / 64. Late-directive and unaccounted-read fixtures pass. The comparison uses the disclosed matched synthetic task; it does not claim to measure this verification's total reading cost. |
| 7 — durable write-backs | Required sections in product 02, 03, 07 and 13, interface/cold-start instructions, README, dated capability rows, publication index and both edition locks, and the ledger are present and consistent with the implemented scope. Publication checks pass. The private-list prerequisite is explicitly superseded by the operator's incremental-list amendment; its future editor and further Stop/controller work remain candidates. |
| 8 — complete gate and boundaries | The full `npm test` and `git diff --check` pass. The kernel and lifecycle implementation are unchanged. The TypeScript dependency remains 5.4.5 and no runtime dependency is added. The local-terms list is present; the evidence checker passes its 78 selected generated/discovery/live/context surfaces and the synthetic-term refusal test passes without exposing the term. Direct source-comment comparison across 33 changed/new code surfaces found no new suppression directives. |

Current live records: [executor-014](../../evidence/WO-039/harness-live/executor-014.json), [verifier-010](../../evidence/WO-039/harness-live/verifier-010.json), [reviewer-009](../../evidence/WO-039/harness-live/reviewer-009.json), [release-close-009](../../evidence/WO-039/harness-live/release-close-009.json), [writer-foreign-dead-003](../../evidence/WO-039/harness-live/writer-foreign-dead-003.json) and [writer-foreign-live-003](../../evidence/WO-039/harness-live/writer-foreign-live-003.json). These are bounded synthetic role-entry episodes, not full implementation, publication or release lifecycles. Alternate shell routes remain guarded; zero native Read refusals does not mean every tool route was unrestricted. The checker validates the current bundle/runtime pins and complete recorded read projections. The sanitized records retain no raw transcript, so this verification revalidates recorded observations rather than claiming fresh observation of the harness sessions.

The recomputed context rows are:

| Role | Before bytes / lines | Claude after bytes / lines | Codex after bytes / lines |
| --- | ---: | ---: | ---: |
| Executor | 71,165 / 994 | 10,960 / 144 | 12,125 / 150 |
| Verifier | 71,165 / 994 | 10,526 / 142 | 11,691 / 148 |
| Reviewer | 71,503 / 1,004 | 13,255 / 182 | 14,420 / 188 |
| Release close | 71,203 / 993 | 8,439 / 130 | 9,604 / 136 |

## Executed checks and limits

`npm run harness -- evidence` exited 0 in this worktree. Its fresh `npm test` completed at `2026-09-08T07:15:25.843Z`: 288 rebuilt runtime tests, 17 harness tests, 7 release-preparation tests, 9 license tests, 8 identity-corpus tests and 21 mutation tests passed, with zero failures or skips in those TAP totals. Formatting, lifecycle/recovery/worktree/release fixtures, publication and work-order index checks, generated-file drift, context and live-evidence validation, plan-refutation fixtures, and artifact/verification/feedback evidence checks also completed successfully. Its subsequent `git diff --check` exited 0.

The host receipt binds both checks to `sha256:8a7d861c1b4787fb9ef07a65db46402e89a6d24603fd5d800258ef7bf0088e8a`. The captured local gate transcript is 111,131 bytes with SHA-256 `4817da4755dcf7ae668c67bca1220c34911149aad64f2f4f3f9945dfb9dd9658`. Verification/control/index surfaces are excluded from that host subject by its documented implementation; this report is not evidence for its own test execution.

The independent regeneration/comparison also checked all three earlier VER files and failed live-holder attempt 002 against the allocation checkpoint: all are unchanged. The local terms list was not read, copied, printed or hashed by this verifier; the configured checker consumed it privately. No private intake was mined. The locked Clean Room floor, ADR controls, deferred Codex hooks, unconfirmed correction token and non-kernel scope remain intact.

This is a verification pass for the current subject. Final review remains the next role and owns any branch commit or publication decision.
