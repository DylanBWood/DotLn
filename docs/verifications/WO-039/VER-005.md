# VER-005 — WO-039 Harness lowering (v0.15.0)

**Verdict: pass.** The integrated subject satisfies all eight acceptance criteria. The fifth repair closes FINAL-001's integration finding; prior verification findings remain covered by passing regressions. No new substantive finding was established.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.153.4","model":"gpt-6-astra","effort":"max","source":"operator-attested"}

Codex CLI reported `0.153.4`. Model and effort use the repository's operator-attested GPT-6 Astra / max selection, satisfying the verifier xhigh+ minimum. No effective-session readback or account label is claimed.

## Subject and method

Authority is [WO-039](../../work-orders/WO-039-harness-lowering.md), its cited sections and incremental-private-list amendment, and the [ideation receipt](../../evidence/WO-039/ideation.md). Required inputs include the [fifth repair](../../evidence/WO-039/repair-005.md), VER-001 through [VER-004](VER-004.md), and FINAL-001's finding and integration-bookkeeping sections selected by that repair.

The reviewed `wo-039` worktree has integrated `HEAD` `1937b023187a813f3b8053a221f819a15cdad244`. Physical cwd and Git root agree; Git registers this worktree once. One writable agent performed this verification. Allocation at `2026-09-08T15:34:09.874Z` created checkpoint `cfa52b01267e8be3ace6936ac2e70a986a8ec7ef`, `refs/dotln/checkpoint/WO-039/23`.

I inspected current implementation, existing tests and required write-backs, reproduced emission and integration claims independently, and ran the complete gate. A direct comparison of all 833 checkpoint files found only the required control/index changes before this report was written. Earlier VER reports and FINAL-001 are unchanged.

## Acceptance evidence

| Criterion | Expected and observed |
| --- | --- |
| 1 — observed capabilities | The [phase-zero record](../../discovery/harness-smoke-2026-09-07.md) records Claude events, payload shapes, refusals, skill resolution and bare-mode limits, plus Codex observations. Every consumed profile observation resolves to its cited row; unobserved Codex hooks/settings remain unavailable. |
| 2 — pure lowering | Compiler fixtures pass deterministic/frozen-input lowering, origin and manifest validation, all ten units' lowering or reasoned residue, unit-local changes and unchanged Seiri/Entropy Reducer semantic hashes. Independent emission verified each profile bundle's manifest. No runtime dependency was added. |
| 3 — boundary parity | All 17 harness fixtures pass. Generated hooks agree with the equipped boundary on synthetic host facts and allow after unit removal, including the three eligible aggregate Stop policies. Missing built adapters refuse before effects. |
| 4 — live smoke | The checker validates four current role smokes and two writer scenarios against the current runtime/bundle. Each role resolves its skill and refuses attribution; the live-holder scenario proves writer refusal. All six finish their observers with zero native Read refusals or outside reads/attempts. The unconfirmed correction token remains named residue, with the unit reached through the role skill. |
| 5 — installed configuration | Independent emission into a fresh temporary directory matched all 24 installed files byte for byte, including the whole instruction file and manifest. In-gate drift tests cover content, missing/unexpected files, manifest, ownership and path safety. The config log and dated ADR-0005 amendment are present. Installation awaits the ordinary final-review commit. |
| 6 — directional context | Mechanical recomputation from instruction/skill directives, including late reads, passes for all eight role/profile rows. The whole instruction falls from 3,878 bytes / 78 lines to 3,593 / 64. For example, the Codex verifier falls from 71,165 / 994 to 11,691 / 148. Late-directive and unaccounted-read negative controls pass. The [measurement](../../evidence/WO-039/harness-context.json) uses a disclosed matched synthetic task with frozen task inputs on both sides. |
| 7 — write-backs | Required sections in product 02, 03, 07 and 13, README, cold-start instructions, dated capability rows, publication index and both edition locks, and the ledger are present and consistent. Publication checks pass. The operator's incremental-list amendment and explicit gate-serialization supersession preserve their rejected alternatives; the future list editor and broader console/projection work remain candidates. |
| 8 — gate and boundaries | Full `npm test` and `git diff --check` pass. Kernel and lifecycle implementation are unchanged; the TypeScript dependency remains 5.4.5. The configured private-list checker passes all 78 selected generated/discovery/live/context surfaces; its synthetic refusal passes. Independent source-comment comparison across 36 changed/new code surfaces found no new suppression directives. |

Current live records under [harness-live](../../evidence/WO-039/harness-live/) are `executor-014`, `verifier-010`, `reviewer-009`, `release-close-009`, `writer-foreign-dead-003` and `writer-foreign-live-003`. They record Claude Code 2.1.263, the `claude-fable-5` launch selection, observed models and effective `xhigh`. Relevant harness source, generated configuration and observations remain byte-identical to checkpoint 21; the integration changes the console edition and shared document/package inputs.

## Repair judgments

**FINAL-001 F1: closed.** The current feedback source reproduces `sha256:3546ed4fd50367f10831fb5510a60efc21df7d9b5f4c959379c88333eb03fb40` across 18 files, matching the new audit under policy `fnv1a64:f78e381ffafce1e9`. Replaying its verifier stream yields a complete matrix with both criteria verified. The console's pinned `selfhost` fixture selects WO-039 and links accepted episode `ep_verifier_1_attempt_1`; its context cell is verified and not stale. The full gate independently validates the audit and verifier inputs. All 18 console tests pass, including host collection at 39,309 ms under its unchanged 60-second command budget.

**Earlier findings: remain closed.** Generated-hook/CLI regressions reproduce concurrent dead-holder reclaim, owner refresh in both schedules, and forced/unforced release observing a replaced holder. Nonce-named instances preserve the newly admitted owner. Aggregate finish parity/removal, observation-only native reads, complete current-byte output receipts and expanded drift negative controls also pass. Historical failed smokes remain intact.

## Executed checks and limits

On 2026-09-08, `npm run harness -- evidence` exited 0: 288 kernel/compiler/skeleton tests, 18 console tests, 17 harness tests, 7 release-preparation tests, 9 license tests, 8 identity-corpus tests and 21 mutation tests passed without failures or skips. Formatting, lifecycle/recovery/worktree/release fixtures, publication/index checks, context/live validation, plan-refutation and artifact/verification/feedback evidence checks also completed successfully. The subsequent whitespace check exited 0.

Both host checks are recorded as executed with exit code 0 at subject `sha256:9cf5d7bd4bbaa1ab2cde3fe8bbe6c982bafad5512195600e559b84f12f6b3e9a`. The local gate transcript contains 117,784 bytes, SHA-256 `42514365e3c07671361492493f4e9aff9b4a023ea6de29fed39dec90f22b4d57`.

The generated Codex skill/instruction file and `harness evidence` runner are themselves under verification; their implementations and outputs were inspected alongside independent comparisons. Codex hooks are unavailable, so this session claims no Claude read receipts or fresh live model episode. The sanitized smokes are bounded role-entry observations, not complete delivery lifecycles; context savings do not measure this verification's total reading cost. Reservation tests establish the exercised same-user schedules, not exhaustive concurrency or hostile-process isolation.

Only this report and required control/index bookkeeping were written as verification outputs. Build/check receipts are local generated state; verification/control/index files are outside the host check subject. No implementation, real writer reservation, previous report or user setting was changed. The private terms list was consumed only by its checker, never printed, copied or hashed into evidence; no private intake was mined. Final review is the next role.
