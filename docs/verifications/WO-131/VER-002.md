# WO-131 VER-002 — independent verification after the repair: the cross-role reuse row

**Verdict: pass.** VER-001's blocking F1 is repaired and the repair is
independently reproduced. This verifier session's first gate — a second role
session at the executor's declared bytes, with the kernel denial unavailable —
ran **62.377 s with 17 fresh and 62 reused tasks**, 37 suites passed, against
the **807.236 s with 79 fresh and 0 reused** the same class of session paid
before the repair. Every one of the 63 declared tasks reused; the 17 fresh are
exactly the build, preparation, the live checks and the current-tree checks.
That row is the measurement criteria 7 and 11 reserved for this session, and
[amendment 15](../../work-orders/WO-131-remaining-suites-under-replica.md)'s
cross-phase handoff comparison now rests on a real cross-phase handoff. F1, F2
and F3 are closed. F1 (this report) is a recording gap, not an implementation
defect, and does not block the verdict.

Subject: [WO-131](../../work-orders/WO-131-remaining-suites-under-replica.md),
branch `wo-131`, uncommitted working tree at gate tree
`f7347d1667708707487357237396136d6e7117b5`, preserved by
`refs/dotln/checkpoint/WO-131/13`
(`33f7a5310b334e28fcc968251c946d95e36ffca0`). The subject is the reviewed
criteria 1–11, the mandatory operator scope amendments 12–15 of 2026-09-14 and
the repair amendments 16–17 of the same day. This operator's `resume: verify`
allocated VER-002 at 2026-09-14T16:42:22.930Z. This verifier changed no
implementation, wrote no evidence outside this report and created no branch
commit.

Verifier: Claude Code 2.1.270, observed with `claude --version`; Opus 5 at max
effort, the 1M-context variant this session exposes as `claude-opus-5[1m]`,
exceeding the order's verifier xhigh minimum. Effort and model are the values
this session exposes; no effective-session readback is claimed.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.270","model":"claude-opus-5","effort":"max","source":"self-reported"}

## The row VER-001 could not obtain

VER-001 F1 measured a sandboxed verifier role session at a document-only delta
and found no reuse at all, because D009 had made a declared success recordable
and reusable only under an applied `sandbox-exec` denial that no sandboxed
Claude or Codex session on this host can start. Amendment 16 and
[D019](../../evidence/WO-131/decisions.md#wo-131-d019) withdrew that condition;
[D020](../../evidence/WO-131/decisions.md#wo-131-d020) made the replica PATH
canonical after diagnosing that per-shell PATH entries forked every narrowed
key between sessions. This session is the first independent test of both.

| Row | Tree | Elapsed | Fresh / reused | Suites |
| --- | --- | ---: | ---: | ---: |
| VER-001's later-phase gate, before the repair | `0fa17674…` | 807.236 s | 79 / 0 | 37 passed |
| Executor's sandboxed document-only composition | `4dd64c8a…` | 63.389 s | 17 / 62 | 37 passed |
| **This verifier session's first gate** | **`f7347d16…`** | **62.377 s** | **17 / 62** | **37 passed / 0 failed** |

Recorded 2026-09-14T16:44:19.840Z,
`host-gate:f7347d1667708707487357237396136d6e7117b5:npm run test:full`, exit 0.
The kernel-denial probe in this session answered
`{"available":false,"reason":"host refused sandbox startup","applied":false}` —
the same answer that produced zero reuse before the repair.

Against VER-001's pre-repair row: **744.859 s (12.414 minutes) saved, 92.27 %
reduction, 12.94×**. Against WO-130's 409.653-second composed median: 347.276 s
saved, 84.77 % reduction, 6.57×. Against WO-130 D009's 78-fresh cross-role
baseline: 17 fresh, none of them a declared suite. The executor's sandboxed
document row and mine differ by 1.012 s in different sessions at different
trees, which is the reproducibility this order was measured on.

The fresh set is exactly the allowed one — `build`, `format`,
`release-surfaces`, `license-surfaces`, `publication`, `index`,
`authority-evidence`, `harness`, `harness-context`, `harness-evidence`,
`artifact-evidence`, `verification-evidence`, `feedback-evidence`, `meta`,
`release:prepare`, `plan-refutation:current`, `console:current`: the 16 table
retentions that `--full` schedules plus `release:prepare`, which carries a
declaration but is preparation and is never keyed. No package or replica suite
appears. The reused successes came from two earlier sessions — 16:33:30.885Z
(`harness-fixtures` 141.71 s, `process-debt` 118.48 s, `worktree` 56.28 s,
`resume` 37.99 s, `console:fixtures`) and 16:23:39.630Z (`skeleton`, `kernel`,
`compiler`, `artifact-corpus`).

That this is a genuinely distinct session is not an assertion: the six
whole-tree current-tree checks went fresh naming
`environment: CLAUDE_PID, GIT_SSH_COMMAND` among their changed inputs. The
session's per-invocation values did change; the narrowed suites reused anyway,
which is precisely what D020 and criterion 7 require.

## Findings

### F1 — the cross-role row is not written into `docs/evidence/WO-131/` (low; recording, not implementation)

Criterion 7 requires the second-role-session row "recorded in
`docs/evidence/WO-131/` beside D009's 78-fresh rows"; criterion 11 repeats the
location for all four of its rows. `gate-performance.json` carries the fresh,
document-transition, changed-session and repair rows, and its final `limits`
entry states that "the second-role-session row of criteria 7 and 11 is produced
by the next verification session, not by the repair session." That row now
exists — above, and in `docs/control/local/harness/checks.json` (local,
ignored) — but not in `docs/evidence/WO-131/`.

Observed: `gate-performance.json` has no `crossRoleSession` row and
`README.md` §Repair defers it. Expected by criteria 7 and 11: the row, its
fresh task list and its wall-clock beside the existing rows.

This is not an implementation defect and it is not something the executor could
have produced: the row is by construction a later session's, exactly as
operator-review assumption 3 arranges for criterion 10's live release-close row.
The criterion's own stated failure condition — "a row outside its band fails the
criterion" — is not met: 62.377 s is inside the band. So the verdict is not held
on it. What is owed is a bounded write-back of the numbers above into
`docs/evidence/WO-131/gate-performance.json` and `README.md`, citing this
report. Reproduction: read the two evidence files and search for the row.

## Earlier findings

| VER-001 | Status |
| --- | --- |
| **F1** — reuse conditional on a kernel denial no role session can obtain (blocking) | **Closed.** The four guards are gone: `suiteInputHash` no longer returns `null` on an unavailable denial (`scripts/lib/suite-evidence.mjs:820-833`), `replicaPlan` no longer refuses first (`scripts/lib/suite-replica.mjs:187-199`), and `valid()` records rather than requires the provenance (`:886-902`). Reproduced end to end by the 62.377 s gate above with the probe answering `host refused sandbox startup`. |
| **F2** — an unavailable denial produced no key at all | **Closed**, by withdrawal rather than by amendment 13's wording: D019 removes the condition entirely, so the case falls through to the ordinary replica key. VER-001's exact reproduction now passes — `node scripts/test-runner.mjs --only kernel` twice at a byte-identical tree gave `2 fresh / 0 reused` (4.74 s) then `1 fresh / 1 reused` (3.90 s), against `2 fresh / 0 reused` both times before. |
| **F3** — the measurement evidence did not carry its precondition | **Closed.** `gate-performance.json` `limits` now states that the accepted-fresh, document-transition and changed-session rows required the available denial, that `changedSession` varied three values inside one shell and was not a second role session, and that the repair rows ran without the denial. `README.md` §Repair says the same in prose. |
| **O1** — a session cannot stop its own gate | Unchanged, correctly filed to planning with the recovery-helper candidate (engineering review R6/"filed elsewhere"). I hit the same guard: every read-only Bash call was refused for the 90 s my gate held the lock. |
| **O2** — a failed verdict pays a full gate for a check it does not need | Still true in shape, but its cost basis has collapsed: the completion gate at a document delta now costs about a minute rather than 807 s, which is exactly the reasoning engineering-review R6 gives for not repairing it here. Correctly named for planning. |

## Acceptance evidence

| Criterion | Observed result and evidence |
| --- | --- |
| 1 — declarations or named retentions | I enumerated every scheduled task against the table: **80 tasks, 63 with declared `paths`, 17 with an explicit `retention` string, 0 with neither**, and no aggregate suite name missing a table row (`scripts/lib/suite-evidence.mjs:181-394`). The live checks and current-tree checks carry retention text naming them as such. **Met.** |
| 2 — declared suites execute in their replicas | The executor's accepted fresh gate built **59 replicas** for its 63 declared tasks (the other four reused), with a verified read-only installed copy of 833 files / 48,789,359 bytes; all six named shell suites are among them (`resume`, `checkpoint`, `work-orders-fixtures`, `publication-fixtures` at `git: replica-repo`; `backup-intake`, `fixture-temp-root` at `git: none`). I reproduced replica execution independently in this session: forced-fresh `runner-fixtures` (358 candidate files, `replica-repo`, 111.99 s) and `process-debt` (317 files, `replica-repo`, 86.80 s) both built replicas and passed, with the denial unavailable. No narrowing refusal is recorded for any declared task. **Met, independently reproduced.** |
| 3 — WO-129 three-role fixture | `scripts/test-process-debt.mjs` drives three full gates through `ImplementationReady`, `VerificationCompleted` and `FinalReviewCompleted` under three distinct session environments and asserts exact counts and the exact reused name list; its later sessions now run without the denial. Executed fresh in this session as part of `process-debt`: subtest "WO-131 three-role full gates compose through lifecycle transitions and distinct session environments" passed. **Met.** |
| 4 — operator-host document-only row | Recorded twice in `gate-performance.json` beside the 243–419 s composed and 41–47 s identical-tree bands: 57.403 s / 17 fresh (denial available) and 63.389 s / 17 fresh (sandboxed). My own 62.377 s gate is a third instance of the same class. Fresh task lists recorded. **Met.** |
| 5 — kernel denial where available | One probe per gate (`scripts/test-runner.mjs:765`), recorded on the persisted gate row; `applied` is `Boolean(probe.available && narrowed)` (`scripts/lib/suite-sandbox.mjs:64-68`), carried on every live result row including reused ones (`:802`, `:857`) and sealed into each success record's `source.kernelDenial`. On the persisted gate row the per-task value is derivable without ambiguity from the probe plus `replicaSetup.suites`. The wrapper is applied only when available (`:833-835`) and unavailability changes no reuse decision — proved by the gate above. Both host answers are on record: `sandbox-host-tests.txt` (4 passed, including the real host denying literal absolute candidate reads in parent and descendant processes) and `sandbox-tests.txt` (3 passed, the host case `SKIP host refused sandbox startup`). `scripts/test-suite-sandbox.mjs` proves the wrapping, the seal and the unavailable case with a stub; it ran fresh here inside `runner-fixtures`. **Met**, as amended by 16. |
| 6 — write-backs and checks | Product 07 §Discipline carries the gate-execution-and-reuse bullet with the denial-is-an-addition rule, the accepted absolute-path residual and the canonical-PATH rule, citing D019 and D020. `docs/evidence/WO-131/decisions.md` carries D001–D021 with dispatches, rejected alternatives and reopening conditions; the decisions index carries all 21 WO-131 rows; the follow-up register carries 21 WO-131 entries. `npm run test:full`: **37 passed, 0 failed**. `git diff --check`: clean (recorded 2026-09-14T16:44:20.126Z, exit 0; re-run by hand, clean). Dependency diff is `@dotln/compiler` 0.9.1→0.9.2 and `@dotln/skeleton` 0.15.10→0.15.11 with its workspace pin — no third-party dependency added. **Met.** |
| 7 — per-suite environment declarations | `fixtureEnvironment` (`:139-180`) names the shell, Node, npm and Git variables a narrowed suite may observe; `CLAUDE_PID`, `GIT_SSH_COMMAND`, `CODEX_HOME` and `CLAUDE_CODE_EXECPATH` are absent and caller `TMPDIR` is replaced by a runner-owned root. `declaredSuiteEnvironment` filters execution and identity together. I checked the three key properties directly against the live tree rather than through a fixture: changing a declared byte (`scripts/test-process-debt.mjs`) changes the key; setting `CLAUDE_PID`, `GIT_SSH_COMMAND`, `CODEX_HOME` and `CLAUDE_CODE_EXECPATH` to new values leaves it byte-identical; changing an undeclared candidate document leaves it byte-identical. Each declared suite's key covers its own test source and `scripts/lib/suite-replica.mjs`. **The live row is now present**: 62 of 62 declared tasks reused in a second role session. **Met.** |
| 8 — release-close admission | `scripts/test-process-debt.mjs:3984` runs the real `scripts/resume.mjs release-close`, extracts the spelling it prints, asserts it does not begin with `cd`, and asserts that the printed handoff, its `--dry-run` variant and `npm run release -- close WO-999 --publish` are admitted by both `permissions.mjs` and `concurrent-work-requires-worktrees.mjs` from main; a `--force` spelling is denied with the writer-isolation reason, takes no writer reservation and leaves the control segment byte-identical. Executed fresh in this session. **Met.** |
| 9 — installed links resolve through the copied graph | `throughCopiedLinks` follows only copied links one hop at a time and refuses a hop leaving the copied graph; the copy is built under a private name and published by one rename; a failure removes it and names its cause. The real case exists in this checkout — `node_modules/.bin/dotln -> ../@dotln/skeleton/dist/src/dotln.js` through the workspace link `node_modules/@dotln/skeleton -> ../../packages/skeleton` — and every replica I built here copied it as part of a verified read-only 833-file installed copy. The fixture in `scripts/test-suite-evidence.mjs` ran fresh inside `runner-fixtures`. The `npm ci` host leg is not reproduced here (see Limits). **Met in fixture and in live replica construction; `npm ci` leg not reproduced.** |
| 10 — release close reuses the reviewed gate | `scripts/test-release-composition.mjs` merges a reviewed fixture branch, hands off its evidence, closes from main under a changed session environment **with the probe stubbed unavailable**, and asserts that the reused set equals exactly the declared suites (`paths` and not `release:prepare`) and the fresh set exactly the remainder. I re-executed it fresh: `release:case:cached_evidence` passed in 22.97 s within a 42-fresh-task run of the whole release suite (123.89 s, 0 failures). The live row is the closeout role's duty under operator-review assumption 3. **Fixture met and independently reproduced; live row pending closeout.** |
| 11 — measured bands | Fresh gate: the accepted row is 801.849 s with 79 tasks, inside WO-130's 780–811 s band; the sandboxed repair fresh row is 513.023 s, and the executor correctly declines to claim that difference as a speedup. Document-only / post-transition: 57.403 s, 63.389 s and my 62.377 s, all within the 41–47 s identical-tree band plus the seven added current-tree checks the criterion allows. Second role session at identical declared bytes: **every declared suite reused** — the row above, the one item VER-001 recorded as not met. Release close at the reviewed tree: pending closeout. No averaged speedup is claimed anywhere. **Met for the three rows available pre-merge**, subject to F1's recording gap. |
| 12 — prompt submission always open | Every generated hook wraps its body in `try { … } catch`, emits an advisory `systemMessage` and never `decision: block`; `scripts/test-process-debt.mjs` covers missing runtime, damaged state, malformed input, illegal phase, unavailable briefing, a live gate and a foreign writer, and asserts that a refused automatic dispatch mutates no lifecycle state. `scripts/bootstrap.mjs` imports only Node builtins; `scripts/worktree.mjs` runs it before the launch handoff. Executed fresh here. **Met.** |
| 13 — protected narrowed successes | Superseded by amendment 16 where it required the denial as a reuse condition. What remains — the probe, the wrapper, the sealed provenance and the absolute-path counterexample where the denial is available — is present and passing in both host modes, and the residual is recorded with its reopening condition in D019 and in product 07. **Met as amended.** |
| 14 — operator controls reach the agent first | `operatorControl` is inlined at the top of every generated hook ahead of all imports and repository reads, keyed per session under a per-uid temporary directory so no other session inherits a mode; `scripts/operator-control.mjs` needs no build or Git. Covered by `process-debt`, executed fresh here, including the dependency-free source-adapter case. The ideation receipt presents the general recovery helper as an unimplemented candidate. **Met.** |
| 15 — measured return on the emergency work | The before/after now has its missing leg. Fresh gate, document-only change and — with this report — the cross-phase handoff are all measured on the operator's host with build, preparation, live checks, failed attempts and measurement overhead included: the retained archive carries the 27.472 s preflight refusal and the 547.973 s failed attempt, and `grossMeasurementInvestment` is 6,625.898 s with break-even at 18.81 later-phase gates and 9.47 work orders. The host condition that decides the outcome — kernel-denial availability — is now stated in `limits`, and after D019 it no longer decides it. I re-derived the arithmetic of this report's own comparisons; the executor's earlier figures were re-derived in VER-001 and are unchanged. Cache-hit counts are nowhere substituted for elapsed time. **Met**, with F1's recording gap against the same rows. |
| 16 — denial is an addition, never a condition | Directly reproduced. Probe unavailable in this session; 62 declared tasks reused; two forced-fresh suites narrowed, executed in their replicas and recorded successes with `applied: false`; a third session's records were reused by a fourth. The cache holds 119 replica records with `applied: false` beside 469 with `applied: true` — both forms valid, as `valid()` now records rather than requires the seal. The replica PATH is canonical (`canonicalPathEntries`, `scripts/lib/suite-replica.mjs:81-112`), which is what let this session match the executor's keys. **Met.** |
| 17 — engineering review of WO-125 to WO-131 | [`engineering-review.md`](../../evidence/WO-131/engineering-review.md) records what the six orders built, ranks R1–R8, names what is sound, applies five bounded repairs and files R6 and the recovery helper to planning. It is candid about the line's central failure — every reuse key was measured inside one shell, so each order shipped a key carrying a value of that session — and R7 names the durable procedural correction, which this session is the first execution of. **Met.** |

## Executed checks and final evidence

- `npm run work-orders -- index` after dispatch: regenerated, no content change.
- Kernel-denial probe in this session: `{"available":false,"reason":"host refused sandbox startup"}`.
- `npm run harness -- evidence` (first gate of this session, the criteria 7/11 row): **exit 0**; `npm run test:full: 37 passed; 0 failed; 62.38 s; 17 fresh / 62 reused tasks` at tree `f7347d1667708707487357237396136d6e7117b5`, recorded 2026-09-14T16:44:19.840Z; critical path 60.906 s.
- `npm run harness -- evidence` (completion gate, after this report was authored): **exit 0**; `npm run test:full: 37 passed; 0 failed; 60.90 s; 17 fresh / 62 reused tasks` at tree `cd1761c6c0cef05355c26d1cc4a0867999d8a12e`, recorded 2026-09-14T16:57:40.591Z; `git diff --check` exit 0 at 2026-09-14T16:57:40.706Z. This is a second composition in this session and a direct instance of criterion 4's class — a document-only change composing the full gate — at 60.90 s against the 807.236 s the same class cost before the repair. Recorded rather than smoothed: that gate ran before this bullet and the process-cost paragraph were added, so a further completion gate ran at the final tree immediately before `verification-result` was recorded; the gate whose result binds this report is that last one, and the 60.90 s row above is the substantive second composition. No gate result is claimed for a run that did not complete.
- `node scripts/test-runner.mjs --only runner-fixtures --fresh`: **2 passed, 0 failed, 116.76 s**; the suite itself 111.99 s in a `replica-repo` replica of 358 candidate files.
- `node scripts/test-runner.mjs --only process-debt --fresh`: **2 passed, 0 failed, 91.62 s**; the suite itself 86.80 s in a `replica-repo` replica of 317 candidate files.
- `node scripts/test-runner.mjs --only release --fresh`: **2 passed, 0 failed, 123.89 s, 42 fresh / 0 reused**, including `release:case:cached_evidence` at 22.97 s.
- `node scripts/test-runner.mjs --only kernel` twice at an unchanged tree: `2 fresh / 0 reused` then `1 fresh / 1 reused` — VER-001 F2's reproduction, now passing.
- Direct key checks against the live tree: declared byte change invalidates; `CLAUDE_PID`, `GIT_SSH_COMMAND`, `CODEX_HOME`, `CLAUDE_CODE_EXECPATH` changes do not; an undeclared candidate document change does not.
- Declaration-table enumeration: 80 scheduled tasks, 63 declared, 17 retained, 0 uncovered.
- `git diff --check`: clean, at gate tree `f7347d16…` both in the gate row and by hand.
- Dependency inspection of `package.json` and `packages/*/package.json`: workspace version bumps only.
- Source read for the judgments above: `scripts/lib/suite-evidence.mjs`, `scripts/lib/suite-replica.mjs`, `scripts/lib/suite-sandbox.mjs`, `scripts/test-runner.mjs`, `scripts/test-release-composition.mjs`, the cited regions of `scripts/test-process-debt.mjs`, product 07 §Discipline, and the WO-131 work order, decisions, README, `engineering-review.md`, `gate-performance.json` and the recorded fixture transcripts.

## Observations

- **O1 — the success cache carries 899 unusable records.** 899 of the 1,664 records are version 3 and can never validate after the version-4 bump, occupying 69.6 MB of the cache's 116.9 MB. D019's bound prunes to the newest 24 files per suite at save time and orders by mtime, so the stale records are pruned first as each suite next saves; a suite that has not saved since the bump still holds them. Self-clearing, no correctness effect, recorded so the disk figure in `README.md` is read correctly.
- **O2 — read-only inspection is still refused while a gate holds the lock.** As in VER-001, every `cat`, `head`, `sed` and `grep` was refused with `DOTLN_HARNESS_REFUSED: write may change gate inputs during active gate` for the duration of my own gate, so all inspection during it went through file-reading tools. `packages/skeleton/src/harness-command.ts` is unchanged in this subject; this is WO-125-D003 behavior, not a finding against this order.

## Limits, judgment and process cost

The instruments are under verification: the runner, the replica constructor,
the kernel-denial probe, the suite cache and the generated hooks produced every
observation above, and the lifecycle under review allocated this report. That
circularity is why I did not let the verdict rest on the composed gate alone:
62 of its 79 tasks were reused rather than executed, so I re-executed the four
groups that carry the order's own claims — `runner-fixtures` (the replica,
sandbox, PATH and pruning fixtures), `process-debt` (release-close admission,
the three-role composition, the prompt and operator-control boundaries, the
spawn admission), the whole `release` suite (the release-close composition) and
`kernel` twice — fresh, in this session, 340.9 s in total. All passed.

This session is sandboxed and `dangerouslyDisableSandbox` is disabled in it, so
I could not run any gate with the kernel denial available. Three things are
recorded but not independently reproduced here: the denial-applied execution
path, which has its own executed coverage in both host modes; criterion 9's
`npm ci` host leg, which would require reinstalling the shared dependency tree
and re-running a full fresh gate; and criterion 10's live release-close row,
which operator-review assumption 3 assigns to the authorized closeout role after
merge. Where a fixture stubs the probe, it proves composition and lifecycle
behavior, not the real denial.

The forced-fresh reproductions ran through `node` directly rather than through
`npm`, so their projected environment differs from the gate's in `PATH` and the
`npm_config_*` variables, and the runner named exactly those variables as the
reason they went fresh. That is the mechanism reporting correctly, and it is why
those runs are evidence of correctness rather than additional reuse rows.

Mission and critical path: carrying a paid proof from the session that earned it
to the session that needs it is this order's whole purpose, and this report is
the first time that has been demonstrated across a real phase boundary in the
six-order line. Across the eight traps: **rule beating** and **seeking the wrong
goal** are the live risks, and both are answered by judging elapsed time in the
phase that pays it and by checking key invalidation against the live tree rather
than through the fixtures that ship with the change; **drift to low performance**
is answered by holding amendment 15's standard rather than accepting a
same-session proxy, which is what VER-001 refused and what this row supplies;
**policy resistance** argued for D009's denial condition and lost to a measured
outcome, so I checked that the protection it bought is still recorded and still
exercised where available rather than quietly dropped — it is; **commons cost**
and **escalation** argue against failing a passing order over a write-back, which
is why F1 is a finding and not a verdict; **shifting the burden** is why F1 names
the exact files and numbers rather than returning an open question; **success to
the successful** gives the executor's recorded rows no claim over a contradicting
measurement, and none contradicted; **Naive Interventionism** is respected — I
changed nothing. NoOp on the findings is rejected because criteria 7 and 11 name
a location this row is not yet in. Reopen if a later-phase gate fails to compose
at unchanged declared bytes, if a suite is found reading an undeclared candidate
file by a literal absolute path in a session without the denial, or if the
release-close row at the reviewed tree does not reuse every declared suite.

Process cost: token collection was not registered at entry, so no entry reading
exists; this is a collection gap in this session, not a repaired one. At report
preparation, `node scripts/harness.mjs usage <session>`
reports **8,803,150 total tokens** (130 input, 8,571,470 cached input, 196,036
cache write, 35,514 output), source `claude-transcript-message-usage`, scope
`dispatch`, observed 2026-09-14T16:53:29.668Z, across 78 steps and 64 commands;
this is cumulative session usage dominated by cache reads, not context
occupancy. Dollar cost and wrapped command count are unavailable. Gate cost for
this verification: 340.910 s of targeted forced-fresh reproduction, the
62.377 s criteria 7/11 gate, and every completion gate this session ran — three
of them, at 60.900 s, 63.680 s and the one recorded with the verdict, each in
the 60–64 s band because each re-composed after an edit to this report. Counting
all of them at the top of that band, the total is under 530 s, against
VER-001's two full gates alone at 1,614.472 s. More than a third of this
verification's gate time went to re-composing after report edits, which at
807 s a gate would have been prohibitive and is the concrete shape of the
payoff. The reduction this order delivers is the reason
this verification cost what it did, and it is the cleanest available measurement
of the payoff: the same role, on the same host, at the same class of delta, paid
807.236 s before the repair and 62.377 s after it.

Publication redaction, final review 2026-09-14: the local harness session identifier in the usage command is replaced by `<session>`. The original report is retained in ignored recovery storage; verdict, measurements and reproduction are unchanged.
