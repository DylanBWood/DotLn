# WO-144 VER-002 — independent verification of the repaired outside-project write grants

**Verdict: pass.** No blocking finding. Every acceptance criterion is met at
the width the order states, and all four VER-001 findings (F1, F2, N1, N2) are
repaired and reproduced live in this Claude Code session under the regenerated
hooks — not only in fixtures. Five non-blocking observations are recorded
below; none of them is a defect against this order's criteria, and each names
the decision or reopening condition that already covers it.

Subject: [WO-144](../../work-orders/WO-144-outside-project-write-grant.md),
branch `wo-144`, repaired under `resume: fix`
([repair.md](../../evidence/WO-144/repair.md), decisions D004–D006).

- `HEAD` is `99714b94`, equal to `main`. All work is uncommitted; no branch
  commit, no remote branch, no publication.
- The work is preserved by `refs/dotln/checkpoint/WO-144/7`
  (`6ed6d6b24690b416ff566c29c374dcde3b322f46`). Against that checkpoint the
  working tree differs in exactly three paths — `docs/control/current.md`,
  `docs/work-orders/README.md` and `docs/control/orders/WO-144.jsonl` — all
  three rewritten by this `verify` dispatch. Every other tracked file matches,
  and 24 of the 25 untracked WO-144 evidence, verification and review files
  match byte for byte (`git hash-object` against the checkpoint tree); the
  25th is that same `WO-144.jsonl`.
- Code identity is `d4476480c8558c338af123c23651aa4398aa724c3114100bdaf5e9fbc6693d34`,
  unchanged since the repair's passing product gate.

The subject also includes the executor's evidence:
[implementation](../../evidence/WO-144/implementation.md),
[repair](../../evidence/WO-144/repair.md),
[decisions D001–D006](../../evidence/WO-144/decisions.md),
[fixture transcripts](../../evidence/WO-144/fixture-transcripts.md) and the
[inventory script](../../evidence/WO-144/inventory.mjs), and the prior
[VER-001](VER-001.md), whose findings are re-checked here.

This `resume: verify` allocated VER-002. The prompt hook recorded the dispatch
and it was not repeated. This verifier changed no implementation, made no
branch commit, spawned no subagents (0 of the 20 allowed, `exact-observed`),
published nothing, and wrote only this report and the projections the result
refreshes.

Probe files created during this verification: two under the granted DotLn
session scratch root (left in place, outside the repository), and one
in-project file at the worktree root, created and removed within two calls
(`git status` untracked count returned to its pre-probe value of 4). Every
refused probe created nothing.

Verifier details:

- **Harness:** Claude Code 2.1.278, read with `claude --version`.
- **Model:** `claude-opus-5[1m]`, as this session exposes it. The operator
  selected "Opus 5 (1M context)" with `xhigh` effort.
- **Effort:** `CLAUDE_EFFORT` reads `xhigh`. No `ultra` mode was selected.
- **Effective readback:** no effective-model readback channel is claimed.
- **Executor attestation under verification:** codex-cli 0.155.1,
  `gpt-6-astra`, `xhigh` (subagents, raw `ultra`), `codex-session-readback`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"xhigh","source":"operator-attested"}

## Instrument disclosure

The regenerated hooks under verification were live in this session. They
judged, refused and journaled every probe below, and the same host code
recorded this session's `npm test` gate row. The findings depend on that
instrument by design: the order's objective is the hooks' behavior in a Claude
session. The session journal
(`docs/control/local/harness/<session-key>.jsonl`) is the hooks' own record;
row numbers below cite it. Where a claim could rest on the instrument alone, an
independent check is named beside it: the product gate was also re-run and its
suites pass on their own assertions, and the code paths were read directly.

Paths below are shapes, not the operator's absolute paths, except where the
refusal message itself is quoted:

- `<tmpdir>` is this host's `os.tmpdir()`, a per-user directory under
  `/var/folders/…/T`; `<scratch>` is `<tmpdir>/dotln/<sha256(session id)>/scratch`.
- `<scratchpad>` is the Claude Code session scratchpad under `/private/tmp`.

## Method

1. Read the order, VER-001 and the repair evidence (repair.md, D004–D006,
   D001–D003).
2. Read the repair diff directly: `harness-command.ts` (the `redirect` marker),
   `harness-host.ts` (`knownWriteDestinations` and its null-device exemption,
   `outsideWriteResponse`, the `observe` ownership, `permissionEffect`'s
   `outsideEffect`, `harnessFeedbackFacts`' outside skip, and the split of
   `evaluateHarnessHook` from `evaluateExistingHarnessHook`),
   `compiler/src/harness.ts` (role text, boundary text), `scripts/harness.mjs`
   (`scratch`), and the WO-144 fixtures.
3. Re-ran every executable claim in repair.md, plus the full product gate.
4. Probed the live guard with the shapes VER-001 found broken and with the
   order's own criterion-2 shapes: discard redirects, a non-redirect device
   mutation, the granted scratch root, `/tmp`, the host scratchpad, the parent
   directory (the recorded incident's shape), an outside removal, and an
   in-project write and removal. Then read the journal rows each produced.
5. Checked work preservation against the checkpoint and the recorded gate row
   against the current code identity.

## Executed checks

| Check | Result |
| --- | --- |
| `node scripts/harness.mjs check` | pass: 31 generated surfaces; local-terms list unavailable |
| `git diff --check` | clean (exit 0) |
| `npm run format:check` | "All matched files use Prettier code style!" |
| `node --test --test-name-pattern='WO-144' scripts/test-harness.mjs` | 6/6 pass, 14,540.7 ms |
| `node --test scripts/test-harness.mjs` | 106/106 pass, 148,670.1 ms (matches repair.md) |
| `npm test` (Node v26.9.0) | **21 passed, 0 failed, 254.32 s, 65 fresh tasks**; gate row recorded `2026-09-19T21:48:33.618Z`, exit 0, code identity `d4476480…`, mode `fresh` |
| Current code identity vs gate row | equal (`d4476480c855…`); the recorded gate applies to this subject |
| `node scripts/harness-context.mjs` | executor 22,020/24,576; verifier 19,062/20,480; reviewer 20,280/20,480; release-close 13,286/16,384; planner 14,352/24,576; refuter 15,009 (unset). Identical in `.claude/skills` and `.agents/skills`; every metric `within` |
| `node docs/evidence/WO-144/inventory.mjs` | cutoff `2026-09-19T21:50:51.556Z`; main 61 journals / 43,128 rows, 0 malformed, 0 command or tool-destination fields; worktree 5 journals / 2,232 rows with 238 judgment rows (199 unobserved, 24 verifier refused, 15 verifier `system-temp` granted) |
| `node scripts/harness.mjs scratch <session>` | prints `<scratch>` exactly, byte-equal to the path this dispatch printed |
| `git diff package-lock.json` | 5 changed lines, internal pins only (compiler 0.16.0, skeleton 0.29.0); no new dependency |
| Live probes | 12 calls, below |

## Live probes (this session, role `verifier`, darwin)

| Journal row | Time (UTC) | Call | Result |
| ---: | --- | --- | --- |
| 305 | 21:37:18.296 | `true 2>/dev/null` | **admitted**, no judgment row |
| 317 | 21:37:19.976 | `echo … >/dev/null 2>&1` | **admitted**, no judgment row |
| 329 | 21:37:21.587 | `echo … &>/dev/null` | **admitted**, no judgment row |
| 339 | 21:37:23.428 | `touch /dev/null` | **refused** before execution |
| 361 | 21:37:50.974 | `mkdir -p <scratch>` | **granted**, `system-temp` |
| 374 | 21:37:56.460 | `printf … > <scratch>/probe.txt` | **granted**, `system-temp` |
| 387 | 21:37:59.527 | `printf … > /tmp/…` | **refused** (physical `/private/tmp/…`) |
| 396 | 21:38:02.188 | `printf … > <scratchpad>/…` | **refused** |
| 405 | 21:38:06.702 | `printf … > ../…` | **refused** (physical `/Users/dylanwood/Projects/…`) |
| 414, 416 | 21:38:12.745 | Write tool → `<scratch>/ver002-write-probe.txt` | **granted**, `system-temp`; permission effect `outside.write:system-temp`, allowed |
| 790 | 21:49:24.713 | `rm -f /Users/dylanwood/Projects/wo144-ver002-absent.txt` | **refused** |
| (none) | 21:49:3x–4x | `touch ./wo144-…` then `rm -f ./wo144-…` (in project) | **not judged**: no row, no advisory |

Each refusal carried the full message, for example:
`DOTLN_HARNESS_REFUSED: outside-project write to ../wo144-ver002-parent-probe.txt (physical destination /Users/dylanwood/Projects/wo144-ver002-parent-probe.txt) lacks an equipped outside-write grant for role verifier (WO-144). Use operator override: for authorized recovery.`
— destination, missing grant and recovery control, as the objective requires.

The three admitted discard redirects were parsed, not skipped: the same
extractor names `/dev/null` in `touch /dev/null` (row 339, refused), and an
unparsable command records `{"status":"unobserved","cause":"destination-not-extractable"}`,
which none of the three produced. Parsed, unrefused and unjournaled is the
exemption path, and the granted row for `<scratch>` on the very next call
shows the guard was live throughout.

## Acceptance criteria

| # | Criterion (abridged) | Verdict | Evidence |
| --- | --- | --- | --- |
| 1 | Decisions record the journal inventory by role and root kind, defaults chosen from it, ungranted destinations named | **met** | D001's inventory reproduces at a later cutoff: 61 main journals / 43,128 rows with zero command or tool-destination fields, so historical destinations are unknown and are labeled as missing evidence, never as zero writes. D005 adds the judgment-row summary VER-001's O1 asked for; my rerun reports it by role, root kind and status. Every root kind is named, and main intake, home folders and sibling projects are explicitly ungranted |
| 2 | Default grants admit a write tool and a redirect into scratch and temporary roots; refuse parent, sibling and documents-like targets; symlink escape and removal judged | **met, at fixture and real-session width** | Fixtures 6/6, including the darwin-shaped `TMPDIR`/`/tmp` split VER-001 asked for, the symlink escape and the removal. Live: the granted scratch root admits both forms; the parent directory, `/tmp`, the host scratchpad and an outside removal refuse before execution. VER-001's two real-session failures no longer reproduce |
| 3 | Equipping a support with an operator-named root admits under it only; unequipping refuses; manifest lists roots and sources | **met** | Fixture passes (exact root admitted, `-beside` refused, expired/denied/missing-evidence envelopes refused, unequip refused, unadmitted declaration rejected at lowering). `.claude/harness-manifest.json` lists all six roles' kinds with `originId`, `source` and `authorityGrantId` in both profiles |
| 4 | Guard failure admits with one advisory and journals the cause; the four existing refusals unchanged; in-project writes not judged | **met** | Fixture passes across the four pre-tool hooks with the grant table nulled, and the stale-pin case still delegates with `pins-differ`. The writer, live-gate, planning-branch and subagent refusals still fire (106/106 harness fixtures). Live: an in-project write and removal produced no judgment row and no advisory |
| 5 | Documents state the width | **met** | Product 03 (dated note, with the repair's scratch route), product 07 §Discipline, `docs/AI-HARNESS-SECURITY.md` and the generated `CLAUDE.md`/role text all name five refusals, the four root kinds, the `/dev/null` discard rule, the printed scratch path and the `node scripts/harness.mjs scratch` retrieval, and state that native scratch and `/tmp` need a separate grant. VER-001's gap ("the docs do not state F1's or F2's real-session consequences") is closed. Map carry-in rows for order-named roots remain in place |
| 6 | `harness check` passes, `npm test` green, `git diff --check` clean, no new dependency, cold-start totals recorded and any ceiling met handled by the one route | **met** | See Executed checks. 31 surfaces; 21 suites green at the current code identity; diff clean; lockfile changes internal pins only; all six role totals recorded and `within`. No ceiling was reached or raised |

## VER-001 findings, re-checked

- **F1 (discard redirects refused) — repaired.** The extractor marks redirect
  operands (`harness-command.ts`), and `knownWriteDestinations` skips exactly
  one shape: a redirect whose literal operand is `/dev/null` **and** whose
  `lstat` reports a character device. Live: the three discard forms are
  admitted; `touch /dev/null` is refused, so the exemption does not leak to a
  device mutation. Read of the code confirms it cannot leak to `rm`, to a
  symlink named `/dev/null` (`lstat` does not follow), to `/dev/null/child`,
  to other `/dev` paths or to any other spelling — those all fall through to
  the ordinary judgment, which refuses them for the default roles. The role
  text and both documents state the rule.
- **F2 (scratch unreachable, `/tmp` and host scratchpad refused) — repaired
  by the second route VER-001 offered.** The operator's recorded choice of a
  DotLn-managed scratch stands; discoverability was the defect and it is
  fixed. This dispatch printed the concrete path in its own prompt context,
  `node scripts/harness.mjs scratch <session>` printed the identical path, and
  writes there are admitted by both the shell and the write tool. The role
  text, product 03, product 07 and the security document now say plainly that
  native scratch and `/tmp` need a separate grant. `<session-key>` is no
  longer something an agent has to resolve.
- **N1 (four identical judgment rows per call) — repaired.** Every pre-tool
  boundary still judges and still refuses; only the permission hook records.
  This session's journal holds **one** row per judged call: 19 judgment rows at
  the mid-session check (356 journal rows) and 56 at the final check (959
  rows), with no duplicate group — no two rows share a timestamp and payload.
  The fixture asserts exactly one row for a granted destination.
- **N2 (a granted write reported as not permitted) — repaired.** The granted
  Write-tool probe recorded `"effect":"outside.write:system-temp","allowed":true`
  (row 416) instead of the old `settings.user` advisory, and its PostToolUse
  observation carried no "observer input unavailable" message (row 423, `factKinds: []`). Credential classification keeps precedence: the fixture
  still returns `credentials.access` for a `.env` path inside the scratch root.
- **O1 (inventory blind to the new rows) — addressed** by D005; reproduced.
- **O2 (reviewer headroom)** — headroom is now 200 bytes (20,280 of 20,480),
  down from 349. Still `within`; no ceiling was raised. It is a real constraint
  on the next role-text change, not a defect here.
- **O3 (stale gate row)** — the gate was re-run; the recorded row's code
  identity equals the current one.
- **O4 (correction handling)** — the typed-correction merge now unions the
  previous destructive set with `outside.write:*` effects drawn from both the
  saved and configured envelopes; the dedicated fixture ("typed correction
  removes outside-write grants, including reused correction state") passes, and
  I read the merge: a reused correction state can no longer drop earlier
  destructive effects.

## Observations (non-blocking, no repair routed)

- **O1 — an exempted discard leaves no record at all.** A judged call whose
  only destination is a `/dev/null` discard produces neither a `granted` nor an
  `unobserved` row (live rows 305, 317, 329 are absent from the judgment
  series). Admission is correct; the silence means a later journal-based
  inventory cannot distinguish "exempted discard" from "no write tool used".
  Covered by D005's reopening condition (a stronger metric awaits a stable
  call identity).
- **O2 — write-capable tools outside the compiled inventory are neither
  judged nor journaled.** The inventory names `Edit`, `Write`, `NotebookEdit`,
  `apply_patch` as writes and `Bash`, `Monitor`, `exec_command`, `write_stdin`
  as shells. A tool outside that list that takes a destination argument (an MCP
  file writer, for example) is not judged, and unlike an opaque shell command it
  records no `unobserved` row. The documents state the guarantee as "recognized
  write tools", so this is within the stated width, not a criterion failure.
  Not probed live; inferred from the compiled `tools` map and the code path
  (`knownWriteDestinations` returns an empty list for an unrecognized tool).
- **O3 — the printed scratch directory is not created.** `harnessSessionScratch`
  computes the path; nothing in `beginHarnessSession` or the dispatch creates
  it. A first use needs `mkdir -p`, which is granted (row 361). Worth one
  sentence in the role text at the next touch of that surface; not a defect.
- **O4 — main's ignored intake is a declared kind granted to no role.** The
  order's own observed gap says "a planner in a worktree writes main's ignored
  intake", while the inventory could name no destination, so operator-review
  assumption 2 left it unequipped. The common routes are unaffected —
  `npm run backup:intake` and `cp`/`mv` are opaque to the extractor and remain
  admitted under host permissions — but a planner in a worktree that writes
  main's intake with a redirect, `tee`, `touch` or the Write tool will be
  refused. That is exactly D001's reopening condition ("A legitimate known
  outside write is refused") and it needs a real planner dispatch to observe.
  I could not exercise the planner role from this verifier session.
- **O5 — prose: a doubled conjunction in the five-refusal lists.**
  `docs/AI-HARNESS-SECURITY.md:217-222` (the second conjunction at `:220`) and `docs/product/07-execution-guide.md`
  §Discipline both read "…; and an observable subagent admission …; and a known
  … destination …", a leftover from inserting the fifth item into a
  four-item list. The list is still unambiguous and counts five. A copy edit
  for whoever next touches those lines.

## Goal alignment

- **Mission:** stop accidental file effects in the operator's folders while
  keeping authorized local work usable (the order, D001, D004).
- **Observed against that promise:** the recorded incident's shape (a stray
  redirect into the directory above the worktree) is refused before execution,
  as are `/tmp`, the host scratchpad, an outside removal and a device mutation;
  the two legitimate-work refusals VER-001 reproduced are gone; a granted write
  is now reported as granted on every surface that sees it.
- **Traps:** *Shifting the burden* — recovery no longer means `operator
  override:` or command rewriting for ordinary discards and scratch work, the
  two shapes an agent meets first. *Rule beating* — the incentive to hide a
  redirect in an opaque form is reduced, not removed; opaque effects remain a
  named non-goal. *Seeking the wrong goal* — the repair was judged by which
  legitimate writes stay usable, not by refusal counts. *Naive
  Interventionism* — the exemption is one shape checked against the device
  itself, not a `/dev` or `/tmp` grant. *Policy resistance* — the other four
  refusals are preserved under both grant failure and stale pins, by fixture.
  *Escalation* — no new hook, gate, key or classifier. *Commons* — one writer,
  zero subagents, and the journal volume for opaque calls fell four-fold.
  *Success to the successful* — the operator's recorded scratch convention was
  kept because it is the operator's decision, and made reachable rather than
  replaced.
- **NoOp,** passing VER-001's subject unchanged, would have shipped two
  reproduced refusals of legitimate work into every Claude role session;
  failing again now would cost a repair cycle for observations that are
  bounded, recorded and, in O4's case, not observable from this role.

## Limits

- Live probes ran on darwin only, for the verifier role only, and through the
  Bash and Write tools. Linux, where `os.tmpdir()` is commonly `/tmp`, was not
  probed; the fixtures cover the split with a synthetic `TMPDIR`.
- `operator override:` admission and its `off` restoration for this refusal
  were exercised by fixture, not live.
- Refusals into the operator's Documents/Desktop-shaped folders were not
  probed live, deliberately; the fixtures cover them with fixture homes.
- The planner and reviewer roles, `main-intake` and `operator-root` grants, and
  Codex behavior (a non-goal) were verified by fixture and code reading only.
- Whether subagent tool calls inherit the session's role was not exercised;
  this session spawned none.
- Process cost at handoff (`node scripts/harness.mjs usage <host session id>`):
  - source `claude-transcript-message-usage`, scope dispatch, observed
    `2026-09-19T21:51:05.546Z`;
  - totalTokens 7,167,842: input 132, cached input 6,998,378, cache write
    130,717, output 38,615;
  - reasoning tokens and cost in dollars unavailable (`null`), therefore
    unknown;
  - 73 steps, 71 commands, subagents 0 of cap 20 (`exact-observed`).
  - The same command keyed by the writer actor id returns "Begin the harness
    session before measuring usage"; the host session id is the working key.

  Final counters are in the response.
