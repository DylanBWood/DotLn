# WO-144 VER-003 — verification after the FINAL-001 repair

**Verdict: pass.** No new blocking finding. FINAL-001 F1 and F2 are repaired
at the documented boundary. The earlier VER-001 repairs remain effective.
This verdict covers known destinations, not arbitrary shell effects or Codex
hook enforcement.

Verifier: Codex CLI 0.155.1, gpt-6-astra, xhigh, reported by this session's
canonical briefing. One verifier; zero subagents. The `resume: verify`
dispatch allocated this report on 2026-09-20 UTC (2026-09-19 locally).

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-6-astra","effort":"xhigh","source":"codex-session-readback"}

## Subject and method

The original [WO-144](../../work-orders/WO-144-outside-project-write-grant.md)
and its acceptance criteria govern this judgment. I read its cited product,
planning, security and ideation sections; the relevant implementation and
tests; [VER-001](VER-001.md), [VER-002](VER-002.md), the cited FINAL-001
findings, and the [repair record](../../evidence/WO-144/repair-final-001.md).
[Decisions D001–D009](../../evidence/WO-144/decisions.md) supply the recorded
choices and remaining follow-ups.

The source matches repair checkpoint `refs/dotln/checkpoint/WO-144/12`.
Separately, all 31 evidence, verification and final-review files present in
that checkpoint matched their current bytes before this report. Dispatch
changed the control segment and its projections. The tested code identity is
`edf80fb619113dd69088b4ac12401a51950b0f60d9f04b71cb36b5a1686013f5`.
I changed no implementation, older report, work-order contract or product
decision. No branch commit or publication was made.

The new verification work consists of fresh generated-hook regressions,
compiler/authority tests, document checks, and independent input probes of
the installed generated hooks. I checked the repair's existing successful
product-gate row against the current code identity; I did not rerun `npm test`.
The next final review retains its own product-gate duty.

## Executed evidence

| Check | Observed result |
| --- | --- |
| `node --test scripts/test-harness.mjs` | 108 passed, 0 failed; 176,066.889 ms. Includes all eight WO-144 cases and the writer, live-gate, planning and subagent regressions. |
| `node --test packages/compiler/dist/test/harness.test.js packages/skeleton/dist/test/executor-supports.test.js scripts/test-authority-grants.mjs` | 13 passed, 0 failed; 1,031.232 ms. |
| Installed `permissions` and `write-observer` input probes | 32/32 expected decisions, cutoff 2026-09-20T00:35:38.627Z; matrix below. |
| `npm run test:docs` | 19 passed, 0 failed; 16.96 s. Includes build, source-pinned authority/artifact/verification/feedback evidence, generated harness, release surfaces, indexes and planning checks. |
| `node scripts/harness.mjs check` | 31 generated surfaces match; local-terms list unavailable. |
| `npm run publication:check`; `npm run plan -- check`; `npm run format:check`; `git diff --check` | All passed. Publication coverage 272/272 headings. |
| `node docs/evidence/WO-144/inventory.mjs` | Reproduced the retained-evidence limits and current judgment counts below. |
| `node scripts/harness-context.mjs` | Both harness roots have identical totals; every configured ceiling is met. |
| Existing `npm test` row, checked with `findGateCheck` and `gateCodeIdentity` | Executed, exit 0, recorded 2026-09-20T00:27:30.478Z, 259,808 ms, exact current code identity. The repair transcript records 21 suites passed, 0 failed. This is retained evidence, not a fresh verifier product run. |

The lockfile diff changes internal component version pins only. No dependency
was added. Cold-start bytes, including shared instructions:

| Role | Bytes | Ceiling |
| --- | ---: | ---: |
| executor | 22,174 | 24,576 |
| verifier | 19,216 | 20,480 |
| reviewer | 20,434 | 20,480 |
| release-close | 13,440 | 16,384 |
| planner | 14,506 | 24,576 |
| refuter | 15,163 | unset |

No ceiling was raised. D009 already records the reviewer's 46-byte headroom
as a planner follow-up.

## Independent installed-hook probes

These were manual subprocess invocations of the actual installed generated
hooks with synthetic `PreToolUse` inputs and this session's registered
verifier role. They were **not** automatic Codex hooks or a live Claude
session. Each case ran through both hooks; no requested file operation or
shell command was executed. Refusal messages named the destination, missing
verifier grant and `operator override:` recovery.

| Input | Expected and observed |
| --- | --- |
| Literal `printf` redirect to an ungranted absolute path, from the project root, `docs/`, and an outside directory | Refused in all three cases. |
| Relative redirect from the project's parent directory to a file there | Refused. |
| Absolute Write, relative escaping Write, and `rm` from `docs/` | Refused in all three cases. |
| `npm run meta 2>../<probe>`, `node -e 0 2>/<outside-probe>`, and `git status > <outside-probe>` | Refused, including the moved-directory `node` case. |
| Relative in-project Write from `docs/` | Admitted. |
| Write to the declared scratch root; `node` redirect there from `docs/` | Admitted. |
| `node -e 0 2>/dev/null` | Admitted. |
| `touch /dev/null` | Refused. |
| `npm run meta 2>$PWD/../.x` | Admitted as opaque, as the repaired documents now state. |

The permission journal independently recorded 16 judgment rows: 11 refused,
2 granted and 3 unobserved. Seven refusals and one grant were marked
`workingDirectory: moved`; a partial moved command also recorded an
unobserved row. The granted Write reported `outside.write:system-temp` with
`allowed: true`. Four moved-directory stand-down advisory rows were retained.
Journal cutoff: 2026-09-20T00:37:31.627Z. These are judgment rows, not executed
effects or unique tool-call counts.

## Acceptance and earlier findings

| Criterion | Judgment and evidence |
| --- | --- |
| 1 — inventory and default grants | Met as an exhaustive scan of retained evidence, with unavailable history explicitly unknown. At 2026-09-20T00:33:46.074Z: main 61 journals/43,128 rows; worktree 8 journals/5,492 rows; no malformed rows and no retained command/tool-destination fields. The worktree had 398 judgment rows: 350 unobserved, 25 verifier refusals, 15 verifier temporary grants, 2 executor refusals, 5 executor temporary grants, 1 reviewer refusal. D001/D005 state the defaults and ungranted roots without inventing historical destinations. |
| 2 — scratch/temp admissions and physical-path refusals | Met. Fresh generated fixtures cover Write and redirect forms, parent/sibling/Documents-shaped paths, symlink escapes, dangling links, removal semantics and unchanged sentinels. The installed-hook probes independently reproduce root and moved-directory decisions. |
| 3 — support grant, unequip and provenance | Met. Fresh compiler and generated-hook fixtures reject unadmitted declarations, accept the exact operator root, reject adjacent roots and expired/denied/missing-evidence envelopes, and revoke on unequip. The checked manifest records roots, declaring origins, sources and authority grant IDs. |
| 4 — failure behavior and preserved refusals | Met. The full hook suite checks once-only advisories and journal causes for unreadable grants, all four existing refusals, in-project bypass of the grant table, stale pins and typed-correction narrowing. Moved-directory and missing-runtime fixtures keep journal/marker writes in the installed hook's own project. |
| 5 — honest documented width | Met. Product 03, product 07 Discipline, security, README and generated instructions describe known destinations, literal redirects, expansion/opaque limits and Codex's lack of enforcement. The detailed documents state the four older refusals' root-only behavior. Map carry-ins remain on WO-056, 072, 075, 077, 111, 112 and 118. |
| 6 — executable and generated evidence, dependencies and context | Met with the fresh checks and matching retained product-gate row above. No dependency or ceiling change. |

**FINAL-001 F1:** repaired. `runHarnessHook` verifies the input root against
the installed runtime and otherwise derives the hook's own root.
`outsideWriteResponse` resolves relative paths against the supplied working
directory. The new fixture covers a project subdirectory, a non-repository
directory and another repository, including the generated fallback when its
runtime is unavailable. The independent installed-hook probes reproduce
refusal for moved-directory Write, redirect and removal inputs. The four
older refusals deliberately retain their existing scope; D007/D008 board that
separate question.

**FINAL-001 F2:** repaired. `shellRedirectTargets` is used only by the
outside-write path when whole-command extraction is opaque. The original
`shellWriteTargets` contract remains the input to the live-gate and planning
checks. Fresh regressions and installed-hook probes cover redirects on
`npm`, `node` and `git`. The original `$PWD` spelling remains opaque and is
now stated plainly. FINAL-001 allowed this documented boundary; its finding
does not require expansion evaluation. D009 records the original order's
broader incident wording for planning rather than silently amending it.

**VER-001 F1/F2/N1/N2:** remain repaired. The fixtures and installed probes
retain the null-device redirect exception, scratch discovery/admission,
permission-owned judgment rows and the actual granted Write effect.
Credentials retain precedence in the fixture. VER-002 O5's doubled
conjunction is corrected on the touched prose surfaces. Its other bounded
observations remain covered by the existing decisions and reopening
conditions; this pass found no new defect requiring a decision entry.

## Judgment, instrument disclosure and limits

The mission contribution is fewer accidental outside-folder effects while
authorized temporary work stays usable. That reduces a risk on the unattended
delivery path; it does not establish confinement. I chose fresh boundary tests
and installed-hook probes over simply accepting earlier reports (NoOp), and
used the exact matching product-gate row instead of duplicating that run.

Policy resistance/fixes that fail: check the four existing refusals alongside
the new one. Commons: one writer, no delegated agents, and reuse only evidence
whose code identity was checked. Drift to low performance and seeking the
wrong goal: judge destinations and usable legitimate writes, not passing-test
counts. Escalation: no new mechanism or approval loop. Success to the
successful: prior passes do not determine this verdict. Shifting the burden:
recheck both reported failure cases before handing the result forward. Rule beating:
explicitly retain the admitted expansion case rather than treating a fixture
pass as universal protection. Naive Interventionism: probe decisions without
executing ungranted effects or changing the implementation. The observed
outcome supports the bounded benefit; broader protection remains open.

The generated hooks and harness host under verification also recorded these
manual probes, writer ownership and observations. The same machinery supplies
the retained gate's identity check. Independent evidence consists of direct
source inspection, subprocess response assertions, fixture sentinel assertions
and the separately executed compiler/authority tests. No automatic Codex
enforcement or new live Claude observation is claimed.

Remaining limits are the recorded ones: shell expansions, unsupported syntax
and programs' own effects; missing historical input data; Codex enforcement;
native scratch discovery; automatic order-to-grant propagation; the older
refusals away from the root; and tight reviewer context headroom. Subagent
role inheritance and other operating systems were not newly exercised.
This report neither authorizes publication nor changes those boundaries.

Entry usage at 2026-09-20T00:32:51.226Z: source unavailable, scope dispatch;
total tokens, component counters and dollar cost unknown. Zero subagents were
spawned. Final counters belong in the ignored usage receipt and handoff.
