# WO-084 VER-001 — Independent verification of the ledger order and index

**Verdict: pass.** All four acceptance criteria hold at the current subject.

- **Sections.** All 142 original sections survive: 141 in the ledger and
  Resolutions on its own surface. Every heading is byte-identical. The bodies
  differ only by the six listed lead-tag edits and trailing blank separators.
- **Planning passes.** The 13 planning-pass IDs are the same set before and
  after.
- **Index.** The generated counts equal an independent Markdown parse on every
  status and all 141 section rows.
- **Check.** The check refuses every misplacement, unlabeled entry and unknown
  status I built against copies of the real ledger.
- **Gates.** `npm test` is green (21/21). `npm run test:docs` is green (19/19)
  with both lineage suites in its chain. `git diff --check` is clean and no
  dependency changed.

There are no findings. Five observations (O1–O5) go to the final reviewer.
O1 and O2 are judgments the executor made and disclosed; the operator may want
to confirm them. None of them is a defect to repair.

Verified by a Claude Code session. The session hook recorded dispatch
`resume: verify` and allocated this path. Its `VerificationRequested` row is
dated 2026-09-19T04:59:27.130Z.

- **Harness version.** 2.1.278, the output of `claude --version` in this
  session.
- **Model.** `claude-opus-5[1m]`, the identifier the harness supplied in this
  session's system context. The operator selected "Opus 5 (1M context)" with
  `/model` at session start.
- **Effort.** `xhigh`, from `CLAUDE_EFFORT=xhigh` in this session's shell. That
  is the host-supplied selection, not effective-session readback, so the source
  is `operator-attested`.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"xhigh","source":"operator-attested"}

## Subject

- **Order.** [WO-084](../../work-orders/WO-084-ledger-order-and-index.md) at
  `v0.31.2`. `npm run plan -- check` exits 0 and reports the order's only
  workspace update as a `release-assignment` to `v0.31.2`.
- **Subject.** The `wo-084` worktree over `main` at `cb932c84`, uncommitted.
  A temporary index of the whole worktree, including untracked files, has tree
  `dbe58362`.
  - Against `refs/dotln/checkpoint/WO-084/2` (`ImplementationReady`, tree
    `87c2be66`, which equals that event's recorded `treeHash`), it differs
    only in `docs/control/current.md`, the appended `VerificationRequested`
    row and `docs/work-orders/README.md`. These are lifecycle projections. No
    ledger, script, test, evidence or product-document byte changed after the
    executor's handoff.
  - The `docs/work-orders/README.md` difference includes my own index refresh
    (see O3).
- **Baseline.** `HEAD:docs/lineage/idea-ledger.md` at `cb932c84` (7,744
  lines). Resolutions sat at line 6279, and 31 sections sat below it. The
  order measured 6115 of 7,580 at `3b3533f8`; the ledger grew in between.
- **Earlier findings.** None. This is the first verification of WO-084.

**Instrument disclosure.**

- The generated Claude hooks reserved this session's writer. While my gates
  ran, the live-gate write guard refused every Bash call, including read-only
  `git diff`. I waited for each gate to finish.
- The gate-evidence machinery recorded my `npm test` row: 594,508 ms, recorded
  2026-09-19T05:15:53.836Z.
- `scripts/test-runner.mjs` is part of this subject, because it registers the
  two new suites, and it also ran both gates. I ran `scripts/test-lineage.mjs`
  and `scripts/test-runner.test.mjs` directly with `node --test` as well.
- `scripts/lineage.mjs` is under verification. I did not use its parser as an
  oracle.
  - Counts came from prettier's bundled Markdown (remark) parser.
  - Preservation came from a separate `## `-boundary split of the baseline and
    current files.
  - Planning-pass IDs came from the unchanged `planningPasses` in
    `scripts/lib/plan-subject.mjs`, the derivation the criterion names.

**Fan-out.** Stated before any spawn: none. Budget: 20. Actual: 0. At
05:19:03Z, `harness usage` reported `count 0, remaining 20, exact-observed`,
with the uncounted remainder unknown.

## Checks run by the verifier

| Check | Result |
| --- | --- |
| `npm test` (default selection, run by this session, 05:05:58Z–05:15:53Z) | exit 0: `npm test: 21 passed; 0 failed; 594.51 s; 65 fresh tasks`. Neither lineage suite is in this selection. |
| `npm run test:docs`, first run | exit 1: 13 passed, 6 failed, 34.22 s. `index` failed with `docs/work-orders/README.md is stale at line 3`, and five dependent suites were skipped as failed preflight. Line 3 still read `ready-to-verify` after the verify dispatch. See O3. |
| `npm run work-orders -- index` | Changed four projection lines: `ready-to-verify` became `verifying` three times, and the VER-001 pending link was added. |
| `npm run test:docs`, rerun (05:16:41Z–05:18:54Z) | exit 0: `19 passed; 0 failed; 132.35 s; 19 fresh tasks`. `lineage` took 0.12 s and `lineage-fixtures` 0.90 s. |
| `node scripts/lineage.mjs index --check` | `PASS lineage index: 141 sections`, 0.05 s real. |
| `node --test scripts/test-lineage.mjs` | 7/7 pass, 577 ms. |
| `node --test scripts/test-runner.test.mjs` | 29/29 pass, 15.2 s. Registered as the machinery `runner-fixtures` suite, outside the default selection. |
| Preservation script (scratch; baseline vs current ledger plus `resolutions.md`) | See criterion 2. |
| Count oracle (scratch; prettier's Markdown AST) | See criterion 1. |
| Refusal fixtures (scratch; copies of the real ledger and index, run through `main(["index","--check"], root)`) | See criterion 1. |
| Stale-reference grep (`git grep -i resolutions` plus untracked files) | See criterion 3. |
| `npm run plan -- check` | exit 0: 19 receipts, 10 enforced passes. The only workspace update is WO-084's `release-assignment`. |
| `npm run publication:check` | exit 0. Both editions are current: 30 and 45 linked sections. |
| `git diff --check`, and `git diff --no-index --check /dev/null` for every untracked file | No output. |
| `git diff -- package.json package-lock.json packages/*/package.json` | Empty. No new dependency. |
| Newly added suppression directives (`eslint-disable`, `@ts-*`, `prettier-ignore`, and similar) in the new scripts and generated files | None. |
| Clean-room scan of newly authored text: all non-ledger diffs, the ledger header, and every untracked file | Only false positives ("internal whitespace", and "internal contradictions" in the verbatim Resolutions text). The moved ledger bodies were not rescanned; they are byte-identical to `main`. |

## Acceptance criteria

| # | Result at this subject |
| --- | --- |
| 1 | **Pass.** The check passes on the reset ledger. Against copies of the real ledger and index it refused: a dated session appended at end of file ("below the founding corpus boundary"); the old Resolutions section re-added at the bottom ("belongs in resolutions.md"); a 2026-09-20 session inserted just above Images ("out of newest-first date order"); an entry whose only `adopted` sat in nested evidence ("unlabeled entry"); an unknown `approved` tag ("unknown status or provenance tag"); and a valid new session without regeneration ("Stale docs/lineage/README.md"). In every case the index bytes stayed unchanged. The executor's seven fixture tests also pass. **Counts.** The remark parse finds 970 top-level entries and no untagged entry: adopted 533, preserved 319, raw 16, superseded 4, transformed 63, rejected 26, deferred 1, candidate 7, open 3, specified 4. These equal the index. All 141 index rows match: each row's line holds exactly `## <heading>`, and its entry and tag counts agree. A naive single-line grep undercounts (for example, adopted 372), because many leads wrap their tag onto the next line. A grep for this criterion must therefore read whole leads, as the committed test's regex oracle does. |
| 2 | **Pass.** The heading multisets are equal (142 = 141 + 1) and no heading is duplicated. Of the 142 section bodies, 138 are byte-identical, 2 differ only in trailing blank separators, and 2 differ only by the six edits in `migration.json`, which I reproduced line by line. The 13 `planningPasses` IDs are the same set. Before the founding corpus, dates are non-increasing, and every same-date group keeps its baseline relative order. Images and Chat 011 to 001 are last. The header gains the dated migration note and also changes three existing paragraphs (O2). Three of the six tag edits add an inferred lifecycle status (O1). Neither changes entry prose. `npm run plan -- check` passes. |
| 3 | **Pass.** The live pointers name the new path: product 07 §Operator-opened ideation mode item 5 and §Discipline; the planning map's do-not-relitigate row; WO-109's citation; the `docs/README.md` map (which also lists the generated index); and the ledger header's Provenance and migration note. A grep of the tracked and untracked tree finds no live document still pointing at an in-ledger Resolutions section. `CLAUDE.md` and the role skills never mentioned it, which matches the order's observed gap. The remaining mentions are historical or immutable: WO-035's umbrella measurements, the phase-two and 2026-09-19 planning documents, refutation receipts, final reviews and evidence. Two more are ledger entry prose (lines 2328 and 2766), which criterion 2 forbids changing. **Ledger entry.** For this pre-2026-09-09 order, the duty is discharged by [WO-084-D001](../../evidence/WO-084/decisions.md#wo-084-d001), its row in the decisions index and its follow-up row. The work-order index marks the substitution. |
| 4 | **Pass.** `npm run test:docs` is green with `lineage` (`index --check`, a document preflight) and `lineage-fixtures` in the chain. The committed test asserts that both suites are `document: true, product: false`, and neither appears in `npm test`'s 21 suites. `npm test` is green, `git diff --check` is clean and there is no new dependency. **Cost line.** The order defers the check's duration to verification: 0.12 s inside `test:docs` (0.11 s in the first run), 0.05 s standalone, and 0.90 s for its fixture suite. |

## Observations (not findings)

- **O1 — three entries gain an inferred `adopted` lifecycle tag.**
  - **The edits.** `**Recovered:**` became `` `adopted` `recovered`: `` and
    `**Implementation choice:**` became
    `` `adopted` **Implementation choice:** ``, both in the 2026-09-06 WO-022
    section. The 2026-09-08 entry "Two NoOps of the first result are reversed
    by operator direction" went from `` `operator-directed` `` to
    `` `adopted` `operator-directed` ``.
  - **Outside the named set.** The third entry already had a backtick tag and
    was not one of the bold-lead sections the order names.
  - **Why the check needs them.** It requires a lifecycle tag, not just any
    tag. That matches the header's founding rule ("gets one entry and a
    lifecycle status"), so each of these entries needed some status.
  - **Evidence.** The chosen status is the executor's inference, disclosed in
    D001 and `migration.json`. The entries' own text describes implemented or
    operative decisions. The metadata finding is the implemented boundary in
    `docs/evidence/WO-022/README.md` (the 0111 and mode-000 rows).
  - **For review.** The operator may want to confirm `adopted` for these three.
    Reverting them would require weakening the check.
- **O2 — the header changes beyond the dated note.**
  - **Statuses.** The declaration grows from five statuses to ten. The
    executor wrote the definitions for `rejected`, `deferred`, `candidate`,
    `open` and `specified`, and added a sentence saying the declaration does
    not change recorded dispositions.
  - **Provenance.** The paragraph now links `resolutions.md` and adds
    `operator-directed` and `operator-authorized` as provenance.
  - **Order.** The paragraph drops the sentence placing Resolutions at the
    bottom, which the move made false. It adds the date-format, same-day,
    founding-boundary and lead-tag rules the check enforces.
  - **Why.** The objective's "status outside the declared set" and criterion
    1's pass on the reset ledger require the extension, because 41 existing
    entries carry the five undeclared statuses. None of this is entry prose,
    and the migration note names the move.
  - **For review.** The five new definitions are executor-authored wording the
    operator has not yet seen.
- **O3 — the first document-suite run failed on a stale lifecycle
  projection.**
  - **Cause.** The verify dispatch had moved the phase, but
    `docs/work-orders/README.md` still said `ready-to-verify`.
  - **My omission.** The verifier procedure says to run
    `npm run work-orders -- index` after dispatch, and I did not do that first.
    The failed run cost 34.22 s.
  - **Result.** After the refresh, all 19 suites pass. This is not a subject
    defect.
- **O4 — some edited lines are wider than the file's wrap.**
  - **Where.** Product 07 line 610, WO-109 line 84 and three normalized lead
    lines in the WO-022 section run past the surrounding 80-column wrap. The
    edited cell in the planning map's do-not-relitigate row breaks that
    table's column alignment.
  - **Impact.** The `format` suite passes. This is legibility only.
- **O5 — the index adds a step to every ledger write.**
  - **Links.** Index rows use GitHub `?plain=1#L<n>` source-line anchors. In a
    local viewer they open the file without jumping to the line.
  - **Every write.** Line numbers shift on every insertion, so each ledger
    write must regenerate the index. `--check` enforces this with the "Stale"
    refusal.
  - **Trade.** This replaces the hand decision about placement with one
    mechanical command, which is the intended trade.

## Limits

- **Status semantics.** I checked that the three inferred statuses (O1) are
  consistent with their entries and the WO-022 evidence. Whether `adopted` is
  the operator's intended disposition is an operator judgment I cannot
  observe.
- **Executor-side claims.** I did not observe the executor's read-only audit
  agent or the ordering of its gate runs. My own gates at the unchanged
  implementation tree supersede its transcripts for criterion 4.
- **Rendering.** I did not render the index on GitHub. The link format is
  judged from its syntax.
- **Machinery suites.** Beyond `runner-fixtures`, I did not run
  `npm run test:machinery` or `--review`.

## Goal alignment at handoff

- **Outcome against the promise.** Delivered. The ledger now reads
  newest-first with nothing below the founding corpus. Resolutions has one
  address, and a misplaced or unlabeled insertion is an executable refusal in
  the document suite. This is documentation upkeep beside the critical path,
  as D001 says. It does not advance the runtime.
- **Rule beating.** Guarded. A tag counts only in an entry's lead. Nested
  evidence and fenced examples cannot satisfy the check, and my nested-`adopted`
  fixture confirmed that.
- **Seeking the wrong goal.** Avoided. Counts are presented as navigation, not
  as progress.
- **Drift to low performance.** Addressed. The date-order and boundary checks
  stop the ledger re-growing at both ends.
- **Escalation.** Bounded. One generator, one check and one fixture suite,
  with no hook, product-gate entry or approval step.
- **Shifting the burden.** Answered. The insertion choice becomes a stale-index
  diagnostic with the exact fix command.
- **Policy resistance.** Nothing was routed around. The stand-down's
  document/product split holds.
- **Tragedy of the commons.** Answered by keeping the check out of the product
  gate. Its 0.12 s cost is negligible.
- **Success to the successful.** Answered. The stable ledger address and every
  heading are kept, so receipts and citations still resolve.
- **Naive Interventionism.** Respected. Whole-section moves are reversible, and
  heading bytes (and so receipts) are untouched.
- **NoOp.** It would have left 31 sections, two of them operator ideation from
  2026-09-18, where a newest-first reader never meets them.

## Process cost

- **Tokens.** At 05:19:03Z: 5,887,086 total tokens, of which 5,700,628 were
  cached input, 150,892 cache writes, 35,454 output and 112 uncached input.
  Dispatch scope, `claude-transcript-message-usage`.
- **Activity.** At that cutoff: 61 steps and 51 commands.
- **Unknown.** USD and reasoning tokens.
- **Subagents.** None.
- **Waiting.** `npm test` took 594.51 s, and `test:docs` 34.22 s (failed, O3)
  plus 132.35 s. My earlier estimate of about seven minutes for `npm test` came
  from the executor's 421.18 s run; the observed run was 594.51 s.
- **Trade-off.** I chose a fresh `npm test` over binding the executor's gate
  row, because the order's evidence gate names `npm test`. The implementation
  tree equals the `ImplementationReady` tree, so the executor's row may have
  sufficed. I did not check whether a gate record matched this code identity,
  so the saving I gave up is unknown. Refreshing the index before the first
  `test:docs` run would have saved 34.22 s.
