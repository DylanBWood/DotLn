# WO-044 VER-003 — fail

**Actor:** Independent verifier, Codex CLI 0.154.0, GPT-6 Astra at max under the
repository's operator-attested selection; no effective-session readback claimed.
**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.154.0","model":"gpt-6-astra","effort":"max","source":"operator-attested","accountLabel":"not-applicable"}

**Dispatch:** `resume: verify`, allocated VER-003 by `VerificationRequested`.
**Observation cutoff:** 2026-09-15T01:41:58Z. The subsequent
`VerificationCompleted` event in `docs/control/orders/WO-044.jsonl` binds this
report to its checked tree. Final gate counters and usage remain in ignored
receipts and the handoff response.

**Subject:** the original order, execution amendments 9–16, cited product and
discovery sections, current source/tests, VER-001 and VER-002, repair evidence
and decisions D012–D016. Additional inputs were the preservation/writer,
release/worktree, suite-key/cancellation, CLI parsing and permission helpers.
Two read-only reviewers checked the discovery and teardown portions; this
verifier reproduced their counterexamples. No implementation was repaired.

## Blocking finding

### F1 — Medium: the documented failure-gate command rejects its own flag

Criterion 15 fails in the normal operator workflow. Running exactly
`npm run harness -- evidence --fail` builds successfully, prints usage and
exits 1 without recording the required diff check. No synthetic provider or
unusual repository state is needed.

At `scripts/harness.mjs:109–114`, the generic options loop permits only
`--loadout`, `--profile` and `--out`, each with a value. It rejects `--fail`
before the evidence branch recognizes that flag at line 118. Consequently the
new shared lifecycle check selection cannot be reached through its advertised
command, and a failed verification still needs the existing full-gate route.

Expected: the exact documented command records only `git diff --check` and
exits successfully when that check passes. Its internal function test at
`scripts/test-process-debt.mjs:2502` calls `runHarnessEvidence(root, "fail")`
directly, so it does not exercise this argument-parser failure. Reopen F1 when
the actual package command passes and its recorded checks exclude `test:full`;
the default/pass path must retain the full gate.

## Acceptance and earlier repairs

| Criteria | Observed result |
| --- | --- |
| 1–2 | 22 retained run files produce 33 rows: 23 observed, eight unavailable, two ambiguous. Matched Claude settings control and hook refusal exist; Codex hook absence and attempted profile are explicit. Scratch regeneration matches the record and index. |
| 3–4 | Inside/sibling effects and instruction markers are retained. Successful sibling writes do not establish confinement; marker echo proves visibility, not automatic loading. The record states these limits. |
| 5–6 | Both harnesses have operator and detached resident SIGKILL evidence, worktree state and fresh recovery. Authentication lifetime, scheduler, idle cost and conversation/store recovery remain explicitly bounded or unavailable. Historical concurrent launches all succeeded. |
| 7 | Discovery pointer, environment addendum, planning disposition and decisions index exist; the legacy ledger substitution applies. |
| 8 | Compiler pin and lockfile now agree: `npm ls` exits 0. Only first-party component versions changed in the lockfile. Final executable evidence is bound by the completion event; no new user-setting or dependency change was made. |
| 9 | Git owner-executable normalization and its 0600/group/other-bit regressions pass. The release fixture includes the 0600 copy. Full release reuse composition was not independently rerun here. |
| 10 | Earlier saved-ref, staged-only, dangling-object and broken-HEAD counterexamples are rejected; feedback cleanup preserves them. Ordinary nested preservation and blocker fixtures pass. The repeated-collision limit below is nonblocking under the operator's practical-scope direction. |
| 11–12 | Reachability-before-local-prerequisites and actual worktree cleanup fixtures pass. Live/unknown writer reservations prevent removal; registration and teardown exclude each other. |
| 13–14 | Generated harness check passes all 24 surfaces. The shared hatch names its allowed reads/commands; compound-command and never-guess projections are present in both harnesses. |
| 15 | **Fails F1:** internal check selection is implemented, but the public command rejects its flag. |
| 16 | Workflow is classified as spawn in source and emitted permissions; its effect regression and remote-spawn refusal remain in the suite. |

VER-002 F1 is repaired by the matching compiler pin and clean `npm ls`.
VER-002 F2 now uses bounded teardown retries with default Git maintenance left
enabled; the executed suite result is recorded below. VER-001 F1–F3/F7 have
passing preservation, writer, cancellation and mode regressions. F4–F6/F8 have
matched operation evidence, argument-derived selectors, spool cleanup and
resident kill/recovery tests and retained observations. These conclusions do
not extend to every possible provider or filesystem failure.

## Practical scope and nonblocking limits

During this verification the operator directed progress on DotLn and rejected
pedantic failures based on an expanding universe of theoretical support. This
verdict therefore rests solely on the directly broken documented command F1.

Two synthetic stress probes established narrower limits, not additional repair
gates: preserving distinct nested repositories at the same retained path
merges their directories and renames conflicting internal files; byte
verification passes, but the second repository is not intact as a separate
unit. A synthetic concurrent-limit error also produces the overly confident
row text "no limit was reached" despite zero successful launches. No loss of
retained bytes or false result in the six historical live concurrency launches
was demonstrated. These limits are disclosed without turning them into new
support requirements or blocking this order's normal-use progress.

Reproductions are retained only in the ignored local lane:
`wo044-ver003-repro.mjs` and `wo044-ver003-concurrency-repro.mjs`. Their exit 0
means the described counterexamples reproduced, not that the order passed.

## Executed evidence and judgment

- The real `npm run harness -- evidence --fail`: build succeeded, command
  exited 1 at parsing, no diff-check receipt produced.
- `npm ls`: exit 0; compiler 0.9.3 is coherently declared.
- `node scripts/harness.mjs check`: 24 generated surfaces passed.
- Probe suite: six tests passed. Scratch regeneration matched discovery
  Markdown/JSON, environment addendum and planning disposition exactly.
- WO-044 mode/cancellation tests: six passed; preservation/writer tests:
  three passed. `bash scripts/test-worktree.sh` and release `unreachable`
  fixture passed.
- `node --test scripts/test-process-debt.mjs`: 62 tests passed with default
  Git maintenance enabled, including the earlier teardown reproduction and
  internal failure-gate/Workflow checks.
- `git diff --check`: clean before final report filing. The final canonical
  gate and current-tree diff receipt are cited by the completion event.

Goal: dependable operator flow and empirical inputs for WO-049/051/068. The
prior repairs advance that goal. F1 defeats the specific promised reduction
in failed-handoff cost, so pass/NoOp would certify a command that does not run.
Policy resistance and shifting the burden appear directly in that broken
handoff. Commons cost and escalation favor one bounded finding and one final
gate, not further speculative exploration. Rule beating and drift would be
accepting an internal-function test as proof of its unreachable CLI. Success
to the successful does not privilege the existing green test over execution;
seeking the wrong goal would optimize hypothetical coverage instead of usable
DotLn progress. Naive Interventionism favors preserving the implementation and
reporting the small, concrete repair without expanding its architecture.

The broken fail selector requires the existing default evidence command for
this handoff. Source and report freeze before that final gate; its counters do
not cause another report edit or gate. This is a disclosed cost of F1, not a
claim that a failing verdict requires green application code.

The harness under verification also records this work: explicit Codex
begin/observe/delivered, writer reservation, usage collection and evidence
runner. Those instruments are not independent proof of their own behavior.
Entry usage needed a fresh transcript counter before collection succeeded.
No live provider launches, long-duration authentication checks or account
changes were performed in this verification. No gate requirement was bypassed.
