# WO-044 VER-005 — pass

**Actor:** Independent verifier, Claude Code 2.1.272, Opus 5 (1M context) at
`xhigh`. The effort is recorded **self-reported**, which is the honest label
here. Two session-local signals agree on `xhigh` — the installed PreToolUse hook
recorded `effort: "xhigh"` on every one of this session's own 889 tool-call
records between 2026-09-15T03:13:29Z and 03:18:24Z
(`docs/control/local/harness/d1610b8b….jsonl`), and the environment exposes
`CLAUDE_EFFORT=xhigh` — but neither is a vendor effective-effort API, and the
lifecycle refused `harness-readback` for this version because the discovery
registry holds no observed effective-effort readback for 2.1.272 (its recorded
observed versions are 2.1.259, 2.1.260, 2.1.261 and 2.1.263). No
effective-session readback is therefore claimed. The harness version is
`claude --version`; the model identifier is the session's own. The order's
`verifier xhigh+` minimum is met.
**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.272","model":"claude-opus-5[1m]","effort":"xhigh","source":"self-reported"}

**Dispatch:** `resume: verify`, allocated VER-005 by `VerificationRequested`
(`docs/control/orders/WO-044.jsonl`, 2026-09-15T03:13:30Z). The harness recorded
the dispatch from the operator's phrase; it was not repeated here.
**Observation cutoff:** 2026-09-15T03:20Z. The subsequent `VerificationCompleted`
event binds this report to its final checked tree. Final gate and usage counters
stay in ignored receipts and the handoff response.

**Bypassed requirements: none.** No operator control was opened during this
verification.

**Subject:** the original order, its 2026-09-14 amendments (items 9–14 and the
second expansion's 15–16), the current implementation and record, and
[VER-001](VER-001.md)–[VER-004](VER-004.md) with the repair records
([repair.md](../../evidence/WO-044/repair.md),
[repair-003.md](../../evidence/WO-044/repair-003.md),
[repair-004.md](../../evidence/WO-044/repair-004.md)) and decisions D012–D018.
No implementation was repaired by this verification.

**Operator direction received mid-verification:** "the issue was an out of date
readme. just look at the readme." The charged defect is the README; this report
judges that repair and the order's criteria, and did not widen into a further
sweep of already-verified ground.

## Charged finding: VER-004 F1 — repaired

Expected: the prepared release block names the components this tree ships, or a
decision records the divergence. Observed at these bytes: `README.md:87`, inside
`<!-- DOTLN-RELEASE-BEGIN -->`, reads "This source prepares DotLn `v0.17.7`. …
Compiler `0.9.3` and skeleton `0.15.12` lower the [Contributor build] …". That
pair now matches every other declaration of the same components:

| Surface | Compiler | Skeleton |
| --- | --- | --- |
| `README.md:87` release block | 0.9.3 | 0.15.12 |
| `packages/*/package.json` | 0.9.3 | 0.15.12 |
| `packages/compiler/src/artifact-identity.ts:15` (`COMPILER_PACKAGE_VERSION`) | 0.9.3 | — |
| `packages/skeleton/package.json:17` (declared dependency) | 0.9.3 | — |
| `.claude/harness-manifest.json` (both generated profiles) | 0.9.3 | 0.15.12 |
| `package-lock.json` | 0.9.3 | 0.15.12 |

Reproduction: manifest and lockfile reads, the identity constant, both generated
profiles, and `git diff main --word-diff -- README.md`, which is exactly three
token changes — `v0.17.6`→`v0.17.7`, `0.9.2`→`0.9.3`, `0.15.11`→`0.15.12` — and
nothing else in the block. The repair is the smallest edit that could fix the
defect, and it introduced no new claim. Decision
[D018](../../evidence/WO-044/decisions.md#wo-044-d018) records it with its
reopening condition. **F1 is closed.**

Because the charged category is README currency, I read the whole file rather
than the one line. Its remaining version and date references are line 90
(`v0.2.0` first shipped), lines 146–147 (the v0.2.0 notes and compatibility
manifest), lines 135/288/292/447 (dated planning, probe and licence records) —
all historical statements that are accurate for their own cutoffs — and the gate
description at lines 139–142 (`npm test` fast gate, `npm run test:full`,
`npm run test:docs`), which matches `package.json`'s current script definitions.
No other stale claim was found in the README's 488 lines.

The audit that accompanied the repair also found stale current-version headings
in `packages/compiler/README.md` and `packages/skeleton/README.md`. Those are
**not** this order's doing and are correctly deferred: on `main` the compiler
package README already documented `compilerPackageVersion: "0.6.0"` against a
shipped 0.9.2, so the staleness predates the bump by several releases. D018 and
adjacent queue item `adjacent-0008` record them for a documentation planning
pass, and do not rest on pre-existing origin alone.

## Earlier findings rechecked at these bytes

| Finding | State | Evidence at this tree |
| --- | --- | --- |
| VER-004 F1 — release block declares components the release does not ship | **repaired** | The six-surface agreement above; the one-line word diff. |
| VER-003 F1 — the documented failure-gate command rejects its own flag | **rejected** | `lifecycleRequiredChecks` is reached through the public command; the source is unchanged since VER-004 proved it live, and the regression at `scripts/test-process-debt.mjs:2502` drives the real `npm run harness -- evidence --fail`, executed by this report's canonical gate. |
| VER-002 F1 — skeleton declares a compiler the workspace no longer contains | **rejected** | `packages/skeleton/package.json:17` is `0.9.3`, equal to `packages/compiler`; the lockfile entry agrees. The whole `package-lock.json` diff against `main` is six lines, all first-party component versions; no third-party entry changed. |
| VER-002 F2 — process-debt green decided by git's detached auto-gc | **rejected** | Unchanged bytes since VER-004 executed the suite (62/62, no `hookFailed` teardown); the suite re-executes in this report's canonical gate. |
| VER-001 F1–F8 | **rejected** | Unchanged bytes since VER-004 re-ran `docs/control/local/wo044-ver002-repro.mjs` (exit 0) and re-read the retained run files; I independently re-read the run files for the rows those findings touch (see criteria 2, 3, 5 below). |

The subject bytes are exactly those the repair gated: `git diff --stat` between
the `RepairCompleted` tree (`fcb7c425`) and a synthetic tree of the current full
working state (`2d620b8f`, built in a temporary index so untracked work is
included) is three files — `docs/control/current.md`, two appended control
events, and the refreshed work-order index. The repair's own delta against
VER-004's checked tree (`55ff44e5`) is the README line plus evidence, decisions,
index, PR and follow-up write-backs; **no source file changed.**

## Acceptance criteria

| Criteria | Observed result |
| --- | --- |
| 1 | 33 rows, counted from the record: 23 observed, 0 blocked, 8 unavailable, 2 ambiguous, matching its own header; 17 Claude Code 2.1.270 rows (C-W1…C-W10, C-K1, C-U1…C-U6) and 16 Codex CLI 0.154.0 rows (X-W1…X-W8, X-W10, X-K1, X-U1…X-U6). Every question the order lists maps to a labeled row, and every row names its command shape. I re-rendered the record from the 22 saved runs into a scratch tree (`--writing-worker report --date 2026-09-14 --out <scratch>`): the record, the JSON index and the `environment.md` addendum came back **byte-identical** by `diff`, so the record is derived from the runs — which proves derivation, not the truth of each run. My own privacy scan of the record, index and all 22 run files for the home path, username, `/Users`, `/home`, `/private`, `/var/folders`, `/tmp`, UUIDs, 32+ character hex and this worktree's name found **zero** hits; the only `.local` matches are the instruction filename `CLAUDE.local.md`. **Met.** |
| 2 | `claude-allowed-tools-repair-1.json` records `hooks.refusals: 1` with a `PreToolUse`/Bash event carrying `decision: "deny"`, the four print-mode events `UserPromptSubmit, PreToolUse, PostToolUse, Stop`, and `hook-fixture` denied in `stream.operations`. The settings deny is a controlled pair: `claude-settings-deny-repair-1.json` has `settings-fixture` **denied** with `settingsDeniedFileAbsent: true`, and its matched control `claude-settings-control-repair-1.json` has the same operation **succeeded** with the file present. X-W3/X-W4/X-W5/X-W10 state explicitly that no Codex hook event fired and what was launched. **Met.** |
| 3 | `claude-allowed-tools-repair-1.json` and `codex-workspace-write.json` both record `insideWritten: true` and `siblingWritten: true`; C-W6/X-W6 report the sibling write as **not** confined, name the matched sibling request counts (1 and 0), and refuse a containment claim because the sibling lies under the OS temporary directory. `codex-named-profile.json` supplies the contrast the order asked for: under the named write profile `siblingWritten: false`. **Met.** |
| 4 | C-W7 lists four echoed markers and the marked block's visibility; X-W7 lists five, adding `INSTRUCTION_MARKER_AGENTS`. The rows claim marker echo, i.e. visibility, not automatic loading versus an explicit read. **Met.** |
| 5 | Four kill rows, two per harness, distinguished by `detached` in the run files: operator-launched `claude-kill.json` and `codex-kill.json` (`killed: true`, `signal: SIGKILL`, `detached: false`, worktree status codes), and resident-launched `claude-resident-kill-repair-1.json` and `codex-resident-kill-repair-1.json` (`detached: true`) each carrying a `recovery` block, `mode: "fresh-episode-on-preserved-worktree"`, exiting 0 on the preserved worktree. Vendor-conversation and worker-store recovery are named unavailable. **Met.** |
| 6 | Per harness: detached launch observed with authentication lifetime explicitly `unknown` from one launch; no-terminal permission behaviour (Claude `observed` with a matched denial, Codex `ambiguous` with no matched request); scheduler `unavailable` for both with `--help` as the command and the worktree-settings question explicitly untested; idle cost `unavailable` with its blocker; concurrent `observed` at 3 of 3, with the three per-launch records in `claude-concurrent.json`. Each row carries a label and a stated blocker. **Met.** |
| 7 | Discovery pointer at `docs/product/03-architecture.md:377`; the `environment.md:589` addendum; the map disposition line at `docs/planning/work-order-map.md:33`, which answers the checkpoint in one line; 18 `WO-044-D…` rows in `docs/lineage/decisions-index.md` through D018; the work-order index marks the pre-2026-09-09 ledger substitution. **Met.** |
| 8 | `git diff --check` clean. The `package-lock.json` diff is six lines of first-party component versions only. No user-settings file is in the diff, and the order's own receipt is explicit that the session's edit of `~/.claude/settings.json` was **denied** by the host classifier and that the operator applies that one-line change (D009; evidence README, Limits). Every changed source path falls inside the paths items 9–16 name — I spot-checked the two least obvious, `packages/compiler/src/harness.ts` (item 13's hatch text) and `scripts/resume.mjs` (item 11's printed handoff). The new `"stop"` tool class reclassifying `KillShell`/`TaskStop` is not unscoped: it is receipted at D011 with its own regression. **Met.** |
| 9 | `canonicalMode` (`scripts/lib/suite-evidence.mjs:46`) keys on Git's owner-executable bit alone. I checked it against Git directly across this checkout: **1305 tracked files, 0 disagreements** between the canonicalised on-disk mode and the canonicalised index mode, so a 0600 copy canonicalises to the same 0644 key as the 0644 original. The `cached_evidence` release case, which reproduces main's 0600 copy, executes in this report's canonical gate. **Met.** |
| 10 | `describeIgnoredMaterial` (`scripts/lib/paths.mjs:163-200`) returns one classified row per ignored entry with its lane, disposability and that lane's remedy; an empty nested repository is classified `empty nested repository (fixture scaffolding; only .git, no commit)` and disposable, a non-empty one is preserved as a directory unit, and intake/control lanes are always preserved. `scripts/worktree.mjs:129` reports **every** blocker at once with the note that nothing there deletes ignored material; `removeMountScaffolding` (`packages/skeleton/src/feedback-selfhost.ts:389`) is called from the feedback verifier's `finally`. **Met**, with VER-003's disclosed same-path collision limit carried forward unchanged. |
| 11 | `ensureOriginReachable` is the **first** statement of the dry-run branch (`scripts/release.mjs:1623`), ahead of `ensureClean` and `ensureNoIgnoredInfluence`, and precedes `updateMainAndFinish` in the publish path (`:1666`); the dry run prints "Origin reachable: … publication also needs gh authentication there." The requirement is stated in `docs/product/07-execution-guide.md:897-901`, in the release-close role skill, and in the `resume.mjs:1110` handoff text. The `unreachable` case asserts both refusal strings. **Met.** |
| 12 | `scripts/worktree.mjs:185-203` collects a derived worktree's blockers (writer holder, uncommitted changes, active gate run, removal error), removes it when there are none, and otherwise keeps it and prints the blocker with the exact `git worktree remove --force` command; `scripts/release.mjs:1415-1423` settles derived worktrees even when the subject is already gone. The `derived` case covers the clean-removed and dirty-kept pair. **Met.** |
| 13 | The generated denial text names the admitted read tools and the exact commands — "Still admitted while the adapter is unavailable: Read, Glob and Grep, and in this checkout exactly pwd; git status --short; git status --short --branch; git rev-parse --show-toplevel; node scripts/bootstrap.mjs" — in the deny reason itself (`.claude/hooks/permissions.mjs:234`) and in the identical advisory at `:243`. Compound admission is `segments.every(...)` over the command split on `&&` and `;` (`:239-242`), so a compound is admitted only when **every** segment is allowlisted. Observed live, unprompted: this session's `for` loop was refused as "Shell control syntax requires an explicit effect adapter" and its `$(…)` substitution as "Dynamic shell substitution requires an explicit effect adapter". `node scripts/harness.mjs check`: **24 generated surfaces**, exit 0. **Met.** |
| 14 | All twelve generated role skills — six under `.claude/skills`, six under `.agents/skills` — carry the never-guess rule **exactly once** each, in identical words. **Met.** |
| 15 | One shared `lifecycleRequiredChecks(verdict)` (`packages/skeleton/src/gate-evidence.mjs:739-742`) returns `["git diff --check"]` for a failing verdict and `["npm run test:full", "git diff --check"]` otherwise, and is imported by both the lifecycle gate (`scripts/lib/lifecycle-evidence.mjs:35-39`) and the harness evidence runner (`packages/skeleton/src/harness-host.ts:57,1607`), so the two cannot drift. An absent verdict keeps the full set, so the default gate is unchanged. **Met.** |
| 16 | `Workflow: "spawn"` at `packages/skeleton/src/harness-command.ts:34`, beside `Agent` and `Task`, carried into the installed `.claude/hooks/permissions.mjs:168`; `harness check` confirms the installed surfaces equal the emitted set. **Met.** |

## Executed evidence

- Record re-render from the 22 saved runs into a scratch tree, then three `diff`
  comparisons against the committed record, index and `environment.md`
  addendum: identical.
- Privacy scan of the record, index and 22 run files (10 identifier patterns):
  no home path, username, absolute path, UUID or long hex.
- `canonicalMode` against `git ls-files -s` over 1305 tracked files: 0
  disagreements.
- `node scripts/harness.mjs check`: 24 generated surfaces, exit 0.
- `claude --version`: 2.1.272; the hook's `effort` field across 889 of this
  session's records and `CLAUDE_EFFORT`, both `xhigh`, neither registry-validated.
- `git diff --check`: clean. `git diff main --word-diff -- README.md`: three
  token changes. `git diff --stat` between the repair tree and a synthetic tree
  of the full current working state: three files, no source.
- Direct reads of the run files behind criteria 2, 3, 5 and 6, including the
  matched settings-deny control pair and the four kill/recovery records.
- The canonical `npm run harness -- evidence` gate for the final full suite run
  and diff check at the exact tree, cited by this report's completion event.

## Judgment and limits

The order's contribution is empirical harness truth for WO-049, WO-051 and
WO-068 plus a dependable release-close and gate path, and that work stands: the
33-row record is reproducibly derived from its runs and honestly labelled — its
`unavailable` and `ambiguous` rows say what was tried and what is missing rather
than reaching for a value — and every VER-001 through VER-004 finding is
answered at these bytes. The one charged defect was a false sentence on the
release surface; it is now true, by the smallest edit that could make it true,
with its own decision and reopening condition. Nothing about the fix created a
new claim to check.

Bounds on this verdict, stated rather than implied:

- **No live provider launch was rerun.** Authentication lifetime, scheduler
  behaviour and effective effort inside the probed launches remain as the record
  labels them; I verified the record's derivation, labelling and privacy
  reduction, not the vendor behaviour behind each run.
- **Criteria 9–12 rest on the source I read plus the fixtures the canonical gate
  executes at this tree**, not on separately re-run targeted cases; VER-004 ran
  those cases at the same source bytes, which are unchanged.
- **The operator's personal settings file was not read.** Whether
  `sandbox.allowUnsandboxedCommands` is currently applied there is `unknown` to
  this verification. It is outside the checked tree, and the repository's own
  receipt records that the session's edit was denied and the operator applies
  it, so criterion 8's "no user setting change" holds for the order's diff.
- VER-003's two disclosed nonblocking limits are carried forward, not
  re-litigated: distinct nested repositories retained at the same path merge as
  directories, and the concurrency row's phrasing under a synthetic launch
  error. The gate-reuse contract question the operator routed to planning
  (`docs/evidence/WO-044/README.md`) is out of scope here and unaffected by this
  verdict.

Disclosure: the harness under verification also recorded this work. Its
generated hooks observed my reads and writes, refused two of my shell forms —
which is itself criterion 13's evidence — supplied the session effort field this
report records as self-reported, and the amended evidence runner recorded my
checks. None of that is treated as
independent proof of the behaviour it implements; every claim above rests on a
reproduction I ran or a file I read.
