# WO-156 VER-002 — independent verification: fail

**Subject:** Uncommitted WO-156 repair on HEAD `a403671dd2c92b7423be4410f4ea6fb0862818b2`, after D005/D006 scope amendments. Judged against [WO-156](../../work-orders/WO-156-plan-check-sub-second.md) and [VER-001](VER-001.md). Product gate identity: `99e24442ffba7d2a5b8fd978df9a51a93a03b8cb9aa520c350f4f5792024ab3a`.

**Verifier:** One fresh-context, read-only verifier agent, gpt-6-astra / xhigh, independently inspected implementation and ran the probes below. The executor root files its judgment and records lifecycle transitions as sole writer. One manually counted agent of cap 20, zero descendants; automatic coverage remains unknown. Provider dollar cap is unenforced because the transport has no hard cap. The modified planning code checked state during verification but did not record this work.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.156.1","model":"gpt-6-astra","effort":"xhigh","source":"codex-session-readback"}

**Verdict:** **Fail.** VER-001 latency is repaired, but two new batching regressions reject previously valid inputs. [D009](../../evidence/WO-156/decisions.md#wo-156-d009) records both findings and the repair follow-up under the operator's continuing authority.

## Evidence

| Criterion | Judgment and evidence |
| --- | --- |
| 1 — identical outputs and passing suite | Same-tree stdout matches exactly; 65/65 fixtures pass in 26.696 s. Accepted-input preservation fails F1/F2. |
| 2 — under 2 seconds | Pass: current 1.601 / 1.594 / 1.584 s; original HEAD sources 17.364 s, all exit 0. Subject original/current 0.649 / 0.102 s. |
| 3 — meaningful regressions | All four WO-156 cases fail against original sources and pass currently: normalization 12 versus 1 call, immutable reader 12 versus 0 launches, receipts 15 versus 4 launches, statSync 543 versus 10. F1/F2 expose missing boundary cases. |
| 4 — recorded decisions and product timing | Pass: D001–D008 and product 07 record measurements, sources, alternatives and reopening conditions. |
| 5 — gate, diff, dependencies and task count | Pass: independently matched current code identity to npm test exit 0, 292100 ms, 71 fresh tasks, recorded 2026-09-24T20:01:05.195Z. Clean diff check, no new dependency or runner task; four additive fixture cases. |

Same-tree subject stdout: 227825 bytes, SHA-256 `2eccf9e2858f85b24c658ed3d826eca29716b8b893e3ce1baf9978868051a9ed`. Check stdout: 3407 bytes, SHA-256 `ca8fe28afd1bf3ad434b595351ea493c49cf8daed9d010af033d84ad0d5995a2`. Every paired output is equal. Original sources were loaded through a scratch Node hook; Git status stayed unchanged.

## F1 — optional prefetch rejects an unused legacy symlink

**Severity:** blocking correctness regression; validation preservation required by the amended design.

**Expected:** A valid canonical sequence and receipt remain accepted when an unused legacy map is a symlink. Required canonical paths remain subject to regular-file validation.

**Observed:** A fixture with `docs/planning/sequence.md` and `docs/planning/work-order-map.md` symlinked to `sequence.md` passes `buildPlanSubject` and `validateReceipt`. Original `readReceipts` returns one receipt; current `readReceipts` rejects with `plan subject requires a committed regular file: docs/planning/work-order-map.md`.

**Reproduce:** Create that fixture, commit the symlink, build a valid subject and direct receipt, commit it, then call `readReceipts`. Prefetch calls `requireBlob` for an unused hint.

## F2 — aggregate receipt batch exceeds individual-read capacity

**Severity:** blocking correctness regression.

**Expected:** Individually readable committed receipts remain readable when their aggregate size exceeds the existing Git buffer.

**Observed:** Six valid receipts sharing a subject with one 3 MiB thesis produce JSON blobs approximately 3,152,367 bytes each. Original reads accept all six; current aggregate batch fails ENOBUFS / SIGTERM at 16,822,641 stdout bytes, above the unchanged 16 MiB limit. Existing error formatting then includes partial stdout. That amplification is a consequence of the newly triggered overflow, not a separate finding against the unchanged helper.

**Reproduce:** Enlarge one fixture thesis to 3 MiB, create six valid evidence receipts, commit, and call `readReceipts`. Repair must cover receipt batches and cross-revision prefetch.

## Limits and judgment

Five malformed receipt shapes remain rejected; four produce earlier TypeErrors rather than the closed-shape diagnostic. No acceptance bypass was found. Timings describe this host, not a universal bound. Independent probes are retained in system scratch `wo156-independent-i2wMrR`: `hook.mjs`, `unused-symlink.mjs`, `large-batch.mjs`, `malformed.mjs`, and `timing-rows.json`; fixture names must be changed before recreating them. Full product gate was reused because its identity matches; root did not alter source to obtain this verdict.

The speedup supports operator flow, but treating speed as sufficient would be rule beating and seeking the wrong goal. D009 compares all eight system traps, Naive Interventionism and NoOp for the bounded repair. Independent judgment remains failed until the accepted-input regressions are corrected.

**Process cost:** entry 9,055,937 tokens; handoff 11,607,298 tokens; source codex-transcript-counter

Root cumulative dispatch counters, entry cutoff 2026-09-24T20:03:19.054Z and handoff cutoff 2026-09-24T20:11:44.647Z; USD cost unknown. These are available root counters, not a separately metered verifier bill.
