# Writing-worker and unattended-launch harness truth — 2026-09-14 (WO-044)

These rows precede any target-bundle, transport-profile or resident-launcher rule. `scripts/harness-probe.mjs --writing-worker <harness>` created a fresh scratch Git worktree outside this checkout for every launch, with one fixture file, one trivial test script, a project `permissions.deny` rule, one generated-shape PreToolUse hook that imports its runtime by an absolute `file://` URL from outside the worktree and refuses a fixture command, and the instruction surfaces `CLAUDE.md` (with a marked block), `CLAUDE.local.md`, `.claude/CLAUDE.md` and `AGENTS.md`. Launches used the operator's own authentication through the harness's unsandboxed command request from the executor session. Hook inputs and result messages were reduced to field names and value shapes at collection; run files retain no raw transcripts, absolute paths, session identifiers or host names. Repaired detached launches delete their temporary raw spools in finally; the original detached observations retained spools at collection; repair removed the two identified owned spools and records that correction in their run files. Labels are `observed`, `blocked`, `unavailable` or `ambiguous`; every row names the command shape it ran and its run file.

Installed harnesses: Claude Code 2.1.270; Codex CLI 0.154.0. Selectors derived from each saved launch or step: claude: claude-fable-5 at xhigh; claude: claude-fable-5 at low; codex: gpt-6-astra at max; codex: gpt-6-astra at low; effort labels are launch selectors, not effective-session readback. Row counts: 23 observed, 0 blocked, 8 unavailable, 2 ambiguous.

| Row | Harness | Question | Label | Observation and limit | Command shape |
| --- | --- | --- | --- | --- | --- |
| <a id="C-W1"></a>C-W1 | Claude Code 2.1.270 | Which tool-allowlist form admits editing and a bounded shell: `--tools` names alone | ambiguous | with only --tools and no allow rule, the edit did not happen, the write did not happen and the bounded shell command did not run; 6 aggregate refusal signals do not establish a particular operation's cause | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read,Edit,Write --permission-prompts none --output-format stream-json --verbose --include-hook-events` |
| <a id="C-W2"></a>C-W2 | Claude Code 2.1.270 | Which tool-allowlist form admits editing and a bounded shell: `--tools` plus `--allowedTools` with an exact Bash pattern | observed | with --allowedTools Edit,Write,Read,Bash(node fixture-test.mjs) the edit happened, the write happened and the allowed command ran | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read,Edit,Write --allowedTools Edit,Write,Read,Bash(node fixture-test.mjs) --permission-prompts none --output-format stream-json --verbose --include-hook-events` |
| <a id="C-W3"></a>C-W3 | Claude Code 2.1.270 | Does the scratch project's `permissions.deny` apply in print mode | observed | the exact otherwise-allowed command was requested and denied; the matched control without permissions.deny created the file, establishing the settings rule's effect; [matched control](writing-worker-smoke-2026-09-14/claude-settings-control-repair-1.json) | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read --allowedTools Read,Bash(touch settings-denied.txt) --permission-prompts none --output-format stream-json --verbose --include-hook-events Attempt exactly 'touch settings-denied.txt' once. Do not retry or change settings. Finish TURN_DONE.` |
| <a id="C-W4"></a>C-W4 | Claude Code 2.1.270 | Does a generated-shape PreToolUse hook under the worktree fire and refuse in print mode | observed | hook events UserPromptSubmit, PreToolUse, PostToolUse, Stop fired; the refusal held and the fixture file was not created | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read,Edit,Write --allowedTools Edit,Write,Read,Bash(node fixture-test.mjs) --permission-prompts none --output-format stream-json --verbose --include-hook-events` |
| <a id="C-W5"></a>C-W5 | Claude Code 2.1.270 | Can a hook under the worktree import a runtime by an absolute `file://` path | observed | the hook imported the runtime from outside the worktree by file:// URL; CLAUDE_PROJECT_DIR was set | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read,Edit,Write --allowedTools Edit,Write,Read,Bash(node fixture-test.mjs) --permission-prompts none --output-format stream-json --verbose --include-hook-events` |
| <a id="C-W6"></a>C-W6 | Claude Code 2.1.270 | Is a write inside the worktree admitted and a write to a sibling directory refused | observed | inside write happened; sibling write happened (not confined; the sibling lies under the OS temporary directory, which a harness sandbox may treat as writable by default); matched sibling requests 1, matched denial false; an absent effect alone does not establish containment | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read,Edit,Write --allowedTools Edit,Write,Read,Bash(node fixture-test.mjs) --permission-prompts none --output-format stream-json --verbose --include-hook-events` |
| <a id="C-W7"></a>C-W7 | Claude Code 2.1.270 | Which instruction surfaces load; is there a worktree-local surface for a bundle's marked block | observed | markers echoed: INSTRUCTION_MARKER_ROOT, INSTRUCTION_MARKER_BLOCK, INSTRUCTION_MARKER_LOCAL, INSTRUCTION_MARKER_DOTCLAUDE; the marked block in CLAUDE.md was visible | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read,Edit,Write --allowedTools Edit,Write,Read,Bash(node fixture-test.mjs) --permission-prompts none --output-format stream-json --verbose --include-hook-events` |
| <a id="C-W8"></a>C-W8 | Claude Code 2.1.270 | What does the structured result envelope look like with tools enabled | observed | message types system:hook_started, system:hook_response, system:init, rate_limit_event, system:thinking_tokens, assistant, user, system:permission_denied, result:success; result fields duration_api_ms, stop_reason, session_id, total_cost_usd, usage, modelUsage, permission_denials, terminal_reason, fast_mode_state, fast_mode_disabled_reason, subagent_stats, is_error, num_turns, subtype, api_error_status, result, ttft_ms, type, duration_ms, uuid, ttft_stream_ms, time_to_request_ms, first_content_frame_ms, queued_turn_count, result_index | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read,Edit,Write --allowedTools Edit,Write,Read,Bash(node fixture-test.mjs) --permission-prompts none --output-format stream-json --verbose --include-hook-events` |
| <a id="C-W9"></a>C-W9 | Claude Code 2.1.270 | Does `--no-session-persistence` leave no session file for the worktree | observed | no session file appeared under the user session store | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read,Edit,Write --allowedTools Edit,Write,Read,Bash(node fixture-test.mjs) --permission-prompts none --output-format stream-json --verbose --include-hook-events` |
| <a id="C-W10"></a>C-W10 | Claude Code 2.1.270 | Which hook events fire for the scripted initial prompt, and does the hook input mark a non-interactive session | observed | UserPromptSubmit fired with fields session_id, transcript_path, cwd, prompt_id, permission_mode, hook_event_name, prompt; permission_mode default; no field names a non-interactive origin | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read,Edit,Write --allowedTools Edit,Write,Read,Bash(node fixture-test.mjs) --permission-prompts none --output-format stream-json --verbose --include-hook-events` |
| <a id="C-K1"></a>C-K1 | Claude Code 2.1.270 | SIGKILL mid-episode (operator-launched): harness exit and worktree state | observed | killed after the first write signal; signal SIGKILL; fixture edited true; inside written false; status codes  M | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read,Edit,Write --allowedTools Edit,Write,Read,Bash(node fixture-test.mjs) --permission-prompts none --output-format stream-json --verbose --include-hook-events` |
| <a id="C-U1"></a>C-U1 | Claude Code 2.1.270 | Detached launch with no terminal and the operator's stored authentication; authentication lifetime | observed | the detached process completed (exit 0); lifetime before a login is demanded is not measurable in one launch and stays unknown | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools  --output-format json Reply with exactly the single word DETACHED_OK and nothing else. Use no tools.` |
| <a id="C-U2"></a>C-U2 | Claude Code 2.1.270 | Permission or approval request with no terminal: refused, hung or auto-denied | observed | the exact fixture command was requested and a matched denial was recorded (exit 0 after 7 s); the file effect did not occur | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read --permission-prompts host --output-format stream-json --verbose --include-hook-events Run exactly 'node fixture-test.mjs' once, then reply with the word TURN_DONE. If the command is refused, reply TURN_DONE anyway.` |
| <a id="C-U3"></a>C-U3 | Claude Code 2.1.270 | Scheduled-task or cron surface; whether a scheduled launch loads the worktree's settings and hooks | unavailable | no scheduler subcommand in the installed help; background session launch exists; a daemon or app-server surface is listed; whether a scheduled launch loads worktree settings is untested | `claude --help` |
| <a id="C-U4"></a>C-U4 | Claude Code 2.1.270 | Idle cost of a session left open | unavailable | Original launcher token was unconfirmed; resident SIGKILL was not established. See the repair resident-kill attempt for external detached lifecycle evidence. | `claude --bg ...; agents --json` |
| <a id="C-U5"></a>C-U5 | Claude Code 2.1.270 | Concurrent session limit observed | observed | 3 of 3 concurrent launches completed; rate-limit status messages 3 (routine status, not a refusal); no limit was reached at this count | `claude --print --model claude-fable-5 --effort low --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools  --output-format json Reply with exactly the single word CONCURRENT_OK and nothing else. Use no tools.` |
| <a id="C-U6"></a>C-U6 | Claude Code 2.1.270 | SIGKILL of a resident-launched episode and its recovery | observed | the external detached parent delivered SIGKILL; exit SIGKILL; worktree edit true, inside write false, status  M; fresh recovery from the preserved worktree exited 0, edit after recovery true. Vendor conversation and transport worker-store recovery remain unavailable in this ephemeral profile. | `claude --print --model claude-fable-5 --effort xhigh --no-session-persistence --setting-sources project,local --strict-mcp-config --mcp-config {"mcpServers":{}} --no-chrome --max-budget-usd 3.00 --tools Bash,Read,Edit,Write --allowedTools Edit,Write,Read,Bash(node fixture-test.mjs) --permission-prompts none --output-format stream-json --verbose --include-hook-events` |
| <a id="X-W1"></a>X-W1 | Codex CLI 0.154.0 | `workspace-write` sandbox: does exec mode edit and write inside the worktree | observed | the edit happened, the write happened and the fixture command ran under approval never | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json` |
| <a id="X-W2"></a>X-W2 | Codex CLI 0.154.0 | Named filesystem permission profile granting write on the workspace roots | observed | under the named write profile the edit happened and the sibling write did not happen | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json -c default_permissions="dotln-writer" -c permissions.dotln-writer.filesystem={":minimal"="read",":workspace_roots"="write"} -c permissions.dotln-writer.network.enabled=false` |
| <a id="X-W3"></a>X-W3 | Codex CLI 0.154.0 | Does a project hook refuse a fixture command in exec mode | unavailable | no hook event was recorded; the fixture file was created | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json` |
| <a id="X-W4"></a>X-W4 | Codex CLI 0.154.0 | Which hook events fire in exec mode | unavailable | no hook event fired for the launch | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json` |
| <a id="X-W5"></a>X-W5 | Codex CLI 0.154.0 | Can a hook under the worktree import a runtime by an absolute `file://` path | unavailable | no hook ran, so the import was not exercised | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json` |
| <a id="X-W6"></a>X-W6 | Codex CLI 0.154.0 | Is a write inside the worktree admitted and a write to a sibling directory refused | observed | inside write happened; sibling write happened (not confined; the sibling lies under the OS temporary directory, which a harness sandbox may treat as writable by default); matched sibling requests 0, matched denial false; an absent effect alone does not establish containment | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json` |
| <a id="X-W7"></a>X-W7 | Codex CLI 0.154.0 | Which instruction surfaces load; is there a worktree-local surface for a bundle's marked block | observed | markers echoed: INSTRUCTION_MARKER_ROOT, INSTRUCTION_MARKER_BLOCK, INSTRUCTION_MARKER_LOCAL, INSTRUCTION_MARKER_DOTCLAUDE, INSTRUCTION_MARKER_AGENTS; the marked block in CLAUDE.md was visible | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json` |
| <a id="X-W8"></a>X-W8 | Codex CLI 0.154.0 | What does the JSONL result look like with tools enabled | observed | message types thread.started, turn.started, item.completed, item.started, turn.completed; final agent message captured | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json` |
| <a id="X-W10"></a>X-W10 | Codex CLI 0.154.0 | Which hook events fire for the scripted initial prompt, and does the hook input mark a non-interactive session | unavailable | no UserPromptSubmit event fired; events: none | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json` |
| <a id="X-K1"></a>X-K1 | Codex CLI 0.154.0 | SIGKILL mid-episode (operator-launched): harness exit and worktree state | observed | killed after the first write signal; signal SIGKILL; fixture edited true; inside written true; status codes  M ?? | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json` |
| <a id="X-U1"></a>X-U1 | Codex CLI 0.154.0 | Detached launch with no terminal and the operator's stored authentication; authentication lifetime | observed | the detached process completed (exit 0); lifetime before a login is demanded is not measurable in one launch and stays unknown | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json Reply with exactly the single word DETACHED_OK and nothing else. Use no tools.` |
| <a id="X-U2"></a>X-U2 | Codex CLI 0.154.0 | Permission or approval request with no terminal: refused, hung or auto-denied | ambiguous | the fixture effect did not occur and no matched request/denial establishes permission behavior; the launch exited 0 | `codex -a on-request exec --ephemeral --ignore-user-config --sandbox read-only --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json Run exactly 'node fixture-test.mjs' once, then reply with the word TURN_DONE. If the command is refused, reply TURN_DONE anyway.` |
| <a id="X-U3"></a>X-U3 | Codex CLI 0.154.0 | Scheduled-task or cron surface; whether a scheduled launch loads the worktree's settings and hooks | unavailable | no scheduler subcommand in the installed help; no background launch; a daemon or app-server surface is listed; whether a scheduled launch loads worktree settings is untested | `codex --help` |
| <a id="X-U4"></a>X-U4 | Codex CLI 0.154.0 | Idle cost of a session left open | unavailable | exec mode leaves no resident session open; the app-server daemon surface was not exercised | `codex --help` |
| <a id="X-U5"></a>X-U5 | Codex CLI 0.154.0 | Concurrent session limit observed | observed | 3 of 3 concurrent launches completed; rate-limit status messages 3 (routine status, not a refusal); no limit was reached at this count | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="low" --cd <scratch> --json Reply with exactly the single word CONCURRENT_OK and nothing else. Use no tools.` |
| <a id="X-U6"></a>X-U6 | Codex CLI 0.154.0 | SIGKILL of a resident-launched episode and its recovery | observed | the external detached parent delivered SIGKILL; exit SIGKILL; worktree edit true, inside write true, status  M, ??; fresh recovery from the preserved worktree exited 0, edit after recovery true. Vendor conversation and transport worker-store recovery remain unavailable in this ephemeral profile. | `codex -a never exec --ephemeral --ignore-user-config --sandbox workspace-write --model gpt-6-astra -c model_reasoning_effort="max" --cd <scratch> --json` |

Each launch's complete field/shape tree is in its run file under the dated directory. A `blocked` row records a launch that could not run (no executable or no authentication); an `unavailable` row records a surface the installed harness does not expose or a signal the launch did not produce; an `ambiguous` row records contradictory or insufficient signals. WO-049, WO-051 and WO-068 design from these labels; an unavailable row is a valid result.
