# Harness truth — 2026-09-07 (WO-039 phase 0)

These observations precede the harness lowering implementation. `scripts/harness-probe.mjs` ran a fresh inert scratch checkout for each row, using the existing Claude user sandbox posture or Codex workspace-write with on-request approval. No hook trust bypass, user settings mutation, fallback model, or external effect was requested. Hook inputs were reduced to field names and value shapes at collection; raw transcripts, paths, session identifiers, and host names are not retained. The JSON companion indexes the individual sanitized observations.

| Row               | Harness             | Capability           | Observation and limit                                                                                                                                                                                                                                                                 |
| ----------------- | ------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <a id="C1"></a>C1 | Claude Code 2.1.263 | PreToolUse           | Observed on Skill, Read, Edit, and Bash. A command hook returned `hookSpecificOutput.permissionDecision: deny`; the fixture effect did not execute. Empty output delegated normal permissions.                                                                                        |
| <a id="C2"></a>C2 | Claude Code 2.1.263 | PostToolUse          | Observed on Skill, Read, and Edit. The edit changed the fixture bytes; input included tool input and response. This observes an effect that already happened; it cannot undo that effect.                                                                                             |
| <a id="C3"></a>C3 | Claude Code 2.1.263 | Stop                 | Observed a `decision: block` continuation, then another Stop with `stop_hook_active: true`. This is a completion affordance, not an unbreakable lifecycle lock.                                                                                                                       |
| <a id="C4"></a>C4 | Claude Code 2.1.263 | UserPromptSubmit     | Observed; `hookSpecificOutput.additionalContext` reached the response. The probe did not classify a correction or confirm an operator token.                                                                                                                                          |
| <a id="C5"></a>C5 | Claude Code 2.1.263 | Project skills       | `.claude/skills/dotln-probe/SKILL.md` resolved through Skill by name; its fixture procedure ran. No skill permission grants were used.                                                                                                                                                |
| <a id="C6"></a>C6 | Claude Code 2.1.263 | Settings permissions | A project `permissions.deny` Bash matcher refused the separate harmless settings fixture. Generated allows remain unavailable: no allow was probed.                                                                                                                                   |
| <a id="C7"></a>C7 | Claude Code 2.1.263 | Instruction file     | The scratch `CLAUDE.md` was installed; auto-discovery is locally documented, and the active repository session already receives that file. Exact fresh-session byte accounting is a separate self-host measurement.                                                                   |
| <a id="C8"></a>C8 | Claude Code 2.1.263 | `--bare`             | No hooks fired; authentication was unavailable and no fixture effect ran. Installed help says it skips hooks, auto-memory, attribution, keychain access and instruction auto-discovery, while explicit skills can still resolve. Skill resolution under bare remains unobserved here. |
| <a id="X1"></a>X1 | Codex CLI 0.153.4   | Project skills       | `.agents/skills/dotln-probe/SKILL.md` resolved by explicit name and its fixture edit ran.                                                                                                                                                                                             |
| <a id="X2"></a>X2 | Codex CLI 0.153.4   | Project hooks        | None of the four hooks in the scratch `.codex/hooks.json` fired. Both harmless fixture commands executed. Cause is not established: the runner did not bypass trust or alter personal configuration. All hook capabilities are unavailable in this profile.                           |
| <a id="X3"></a>X3 | Codex CLI 0.153.4   | Instruction file     | This executor received the existing `AGENTS.md` symlink to `CLAUDE.md`. Project skills and the shared instruction block are the available lowering surfaces; settings permission lowering is unobserved.                                                                              |

The Claude launch selected `claude-fable-5` at `xhigh`; its stream named that primary model and an ancillary `claude-haiku-4-5-20251001` model. The Codex launch selected `gpt-6-astra` at `max`; that stream exposed no effective model/effort readback. Effort labels are launch selectors, not effective-session readback. All three processes terminated; the bare comparison exited 1 for unavailable authentication, and the two ordinary probes exited 0.

Each event's complete field/shape tree is in the per-run JSON. Shared fields observed by Claude include `session_id`, `transcript_path`, `cwd`, `permission_mode`, and `hook_event_name`; tool events add tool name, input, use identifier, and (afterward) response. Stop carries a continuation flag and assistant-message shape. No record retains operator wording or tool-input values.

Sources establish syntax only; the rows above establish local behavior: [Claude hooks](https://code.claude.com/docs/en/hooks), [Claude skills](https://code.claude.com/docs/en/skills), [Codex skills](https://learn.chatgpt.com/docs/build-skills), [Codex hooks](https://learn.chatgpt.com/docs/hooks). Current documentation was retrieved through Context7 and official pages; installed CLI help supplied version-specific launch options.

The executor is running under the hand-written configuration that existed at activation. The self-host bundle and its later live read trace are separate instruments and must not be inferred from these phase-zero fixtures.
