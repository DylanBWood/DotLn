# Accepted Entropy Reducer findings — REVIEW-003

Generated by `npm run entropy -- dispose`. Each item below is a finding the operator accepted after it survived its blinded refutation; the follow-up collector reads these headings, and `npm run meta` lists them as register candidates. Nothing here is activated work: promotion to a work order remains a planning act.

## Candidates — accepted Entropy Reducer findings, review REVIEW-003 (recorded 2026-09-25)

- **ER3-001 — Worker launches keep host-side effects inside the declared portfolio and scratch roots, and a recorded precondition that gates further live runs is enforced whe**
  Surface `packages/skeleton/src/worker-transport.ts (runWorkerProcess, Codex argv builders); scripts/lib/authority-probe.mjs; scripts/lib/writing-worker-probe.mjs; scripts/harness-probe.mjs; scripts/probe-worker-hosts.mjs; scripts/target-worker-smoke.mjs; scripts/probes/local-model-role-qualification.mjs`, severity major, altitude 5, evidence measured.
  Observed: Seven tracked files build a Codex exec argv: packages/skeleton/src/worker-transport.ts, scripts/harness-probe.mjs, scripts/lib/authority-probe.mjs, scripts/lib/writing-worker-probe.mjs, scripts/probe-worker-hosts.mjs, scripts/probes/local-model-role-qualification.mjs and scripts/target-worker-smoke.mjs. None mentions CODEX_HOME. Six of them copy the argv (--ephemeral, --ignore-user-config) outside
  Expected: Every Codex launch, the product transport and the probes alike, goes through one launcher. That launcher supplies an isolated Codex home, preserving authentication without copying credentials into evidence. It digests the user Codex configuration before and after each launch, and it refuses or reports when the digest changes. D011's precondition is then enforced at the launch site instead of being
  Reproduction: `cd <frozenSubjectPath> && echo 'Codex launch files:' && git grep -l -e '"--ephemeral"' -- 'packages/*/src/*.ts' 'scripts/*.mjs' 'scripts/lib/*.mjs' 'scripts/probes/*.mjs' && echo "mention CODEX_HOME: $(git grep -l -e '"--ephemeral"' -- 'packages/*/src/*.ts' 'scripts/*.mjs' 'scripts/lib/*.mjs' 'scripts/probes/*.mjs' | xargs grep -l CODEX_HOME | wc -l)" && echo "argv copies outside the transport: $(git grep -l -e '"--ignore-user-config"' -- 'scripts/*.mjs' 'scripts/lib/*.mjs' 'scripts/probes/*.mjs' ':!scripts/test-*.mjs' | wc -l)" && sed -n 96,103p packages/skeleton/src/worker-transport.ts`
  Operator disposition: accepted — Re-checked at HEAD fa9957f1 on 2026-09-25: seven files build a Codex argv and none sets CODEX_HOME; WO-111 VER-001 B1 observed the trust writes, D020 accepted the deviation and FUP-3c34a8ffbf61376f is undisposed. Filed as WO-159 (one launcher, a per-episode home seeded for authentication only, digest pairs as protected receipt surfaces), paired with WO-158 at the head of the sequence; the packet i
  Source: `docs/instance/entropy-reducer/runs/REVIEW-003.md`; survived its blinded refutation.

- **ER3-002 — A deferred follow-up is reopened when its recorded observation occurs. FUP-7f9a27e6ed6c44b3 (docs/planning/work-order-map.md:1171-1176) reopens on 'a measured w**
  Surface `packages/console/src/collect.ts (collectSources controlStatus and command runner); scripts/resume.mjs (status); scripts/release.mjs (list); packages/console/test/board.test.ts:992; scripts/test-runner.mjs console-docs task`, severity major, altitude 10, evidence measured.
  Observed: On the frozen subject, collectSources measures 18.4-19.1 s, and the console board --json CLI takes 19.73 s. Most of that is 101 sequential `node scripts/resume.mjs status --json --work-order` forks, one per work order ever seen in the control log (packages/console/src/collect.ts:117-139, 934 events). They take 12.8-13.3 s at about 0.12 s each. One `release list` adds 5.2-5.6 s through 1,050 git sp
  Expected: Console collection issues a constant number of processes regardless of order and release count. That means one batched status fold over all orders, plus a release listing whose per-tag results are cached by immutable tag object id. The board and the document gate then return to the few-second band as history grows, and FUP-7f9a27e6ed6c44b3 is reopened on this measurement.
  Reproduction: `cd <frozenSubjectPath> && mkdir -p .runtime/tmp && export TMPDIR=$PWD/.runtime/tmp && node scripts/build.mjs >/dev/null && node --input-type=module -e 'import cp from "node:child_process";import {syncBuiltinESMExports} from "node:module";const o=cp.execFileSync;const t=new Map();cp.execFileSync=function(f,a){const k=[f.split("/").pop(),...(a||[]).slice(0,2).map(x=>x.split("/").pop())].join(" ");const s=performance.now();try{return o.apply(this,arguments)}finally{const e=t.get(k)||{n:0,ms:0};e.n++;e.ms+=performance.now()-s;t.set(k,e)}};syncBuiltinESMExports();const {collectSources}=await import(process.cwd()+"/packages/console/dist/src/collect.js");const s=performance.now();await collectSources(process.cwd(),[]);for(const [k,v] of t)console.log(v.n,Math.round(v.ms)+"ms",k);console.log("total",Math.round(performance.now()-s)+"ms")'`
  Operator disposition: accepted — Measured by the review on the frozen subject and reproduced by the refuter: collectSources 18.4 to 19.1 s from 101 sequential resume status forks plus a 5.2 to 5.6 s release list over 1,050 Git spawns; console-docs is 20.06 s of the 24.81 s test:docs gate. FUP-7f9a27e6ed6c44b3 reopening observation (above ten seconds) has occurred. Filed as WO-164 (one batched status fold, a per-tag cached release
  Source: `docs/instance/entropy-reducer/runs/REVIEW-003.md`; survived its blinded refutation.

- **ER3-003 — A compiled grant and its runtime route agree. The loadout should not compile an authority or cost that the pinned transport cannot exercise, and the residue and**
  Surface `packages/skeleton/src/entropy-review-protocol.ts (ENTROPY_REVIEW_TOOLS); packages/skeleton/src/worker-transport.ts (entropyArgs); packages/skeleton/src/loadouts/entropy-reducer.ts (fan-out-lens support, delegates resource)`, severity minor, altitude 6, evidence measured.
  Observed: The compiled reviewer grants delegate.readonly with resourceLimits {delegates: 4} (packages/skeleton/src/loadouts/entropy-reducer.ts:35, 969, 1020, 1133) and throws 'compiled authority lacks the delegates resource' without it (:1257). The dispatch protocol refuses a request without lens briefs (packages/skeleton/src/entropy-review-protocol.ts:238). However, the review profile names only Bash, Read
  Expected: There are two consistent options. The pinned route can admit a read-only delegate tool under the same confinement, with the receipt recording delegates used against the four-delegate maximum. Alternatively, the fan-out support is not compiled for a route that cannot delegate, and the lens briefs are presented as the reviewer's own checklist. Either way, the authority envelope, the residue and the 
  Reproduction: `cd <frozenSubjectPath> && grep -n 'ENTROPY_REVIEW_TOOLS =' packages/skeleton/src/entropy-review-protocol.ts; grep -n '"delegate.readonly"\|delegates: 4\|compiled authority lacks the delegates' packages/skeleton/src/loadouts/entropy-reducer.ts; grep -n 'lensBriefs.length > 0' packages/skeleton/src/entropy-review-protocol.ts; grep -n '"multi_agent",' packages/skeleton/src/worker-transport.ts; grep -c 'Zero of four lens delegates used' docs/instance/entropy-reducer/runs/REVIEW-002.json`
  Operator disposition: accepted — Verified against the cited lines: the compiled reviewer requires the delegates resource while both pinned routes deny delegation, so every receipt since REVIEW-002 describes a fan-out the episode could not perform. Filed as WO-165 (the route and the compiled support agree; preferred repair stops compiling fan-out-lens for routes without a delegate tool and renders the lens briefs as the reviewer c
  Source: `docs/instance/entropy-reducer/runs/REVIEW-003.md`; survived its blinded refutation.
