# R1 replan — the three orders after WO-044's record, the receipts since the stand-down, and the repository at v0.22.0 (2026-09-16)

The operator opened this pass on the main checkout after the v0.22.0 close
with three parts: design WO-049, WO-051 and WO-068 at the mandatory R1
checkpoint the 2026-09-08 critical path placed after WO-044's record; review
every receipt and planning suggestion since the last pass or the last 72
hours, whichever is longer; and take a meta view of the repository, its
gaps, its organization and its debt. Two mid-turn messages added a fourth
part: fix the hook advisory that repeated on every tool call of this
session, and arrange the sequence in pairs of two so two lanes run in
parallel under the existing one-at-a-time final review and release close.
The dispatch and both follow-ups are captured verbatim in ignored intake
(`docs/intake/notes/2026-09-16-r1-replan-planning.md`, SHA-256
`07ffad87a856637579b6a40ba7f7bf77a533ccbd5e0622784086bc33a2bab279`).

Method: one session, read-only until the planning branch, no background
workers. Sources: canonical status, the sequence, the last planning document
and its ledger section, the three orders, WO-044's writing-worker record and
its 33 rows, the decisions, verifications and final reviews of the seven
orders closed since the stand-down pass, the ignored gate rows and hook
journals, the follow-up register's first page, and the code surfaces the
three orders name. Entry process cost is unknown: `node scripts/harness.mjs
usage` answered "Begin the harness session before measuring usage", because
this session's hooks ran in their fallback until the runtime was rebuilt (§4).
Observation and inference are labeled; an unobserved value is unknown.

## 1. R1 decided from the record

The checkpoint asked two questions of WO-044's rows
([writing-worker-smoke-2026-09-14.md](../discovery/writing-worker-smoke-2026-09-14.md);
23 observed, 0 blocked, 8 unavailable, 2 ambiguous).

| Question | Rows | Answer | Consequence |
| --- | --- | --- | --- |
| Do a target worktree's own hooks and settings apply to a print-mode or exec-mode worker? | C-W3 (project `permissions.deny` denied an otherwise-allowed command, matched control created the file), C-W4 (a generated-shape PreToolUse hook fired and refused), C-W5 (the hook imported its runtime by an absolute `file://` URL), C-W7 (root, marked-block, `CLAUDE.local.md` and `.claude/CLAUDE.md` surfaces loaded), C-W9 (no session file) | Yes for Claude Code print mode with `--setting-sources project,local`. | WO-049 keeps its hook design for Claude and is not converted to a host-side containment order. |
| Same question for Codex exec mode | X-W3, X-W4, X-W5, X-W10 (no hook event fired; the fixture file was created), X-W7 (root, marked block, local, `.claude/CLAUDE.md` and `AGENTS.md` loaded), X-W2 (a named filesystem permission profile confined the sibling write) | No hooks; instruction surfaces yes; containment by launch profile yes. | For Codex the target bundle carries the instruction block only; governance is WO-051's named write profile. |
| Is the harness sandbox the containment boundary? | C-W6, X-W6 (an inside write happened and a sibling write under the OS temporary directory also happened; "an absent effect alone does not establish containment") | No, for either harness. | Neither order claims sandbox containment. Containment is the target hook's write check (Claude), the named profile (Codex) and the host's diff check (WO-052, WO-053). |
| Which tool-allowlist form admits editing and a bounded shell? | C-W1 ambiguous (`--tools` alone: no edit, no write, no command), C-W2 observed (`--allowedTools Edit,Write,Read,Bash(<exact command>)`: all three happened) | Exact `--allowedTools` patterns beside `--tools`. | WO-051's Claude shape carries three exact Bash patterns: the declared test command, `git add -A`, `git commit -F <host-written path>`. No wildcard pattern was observed, so none is used. |
| Can a detached parent launch either harness without an interactive login? | C-U1, X-U1 (detached launch with stored authentication exited 0; lifetime before a login is demanded unknown) | Yes; lifetime unknown. | WO-068's `cli-worker` kind is available in principle; WO-122 fills it. The resident's own proof still uses `script`. |
| What happens to a permission request with no terminal? | C-U2 (the request was auto-denied, exit 0 after 7 s), X-U2 ambiguous | Claude: auto-denied. Codex: unknown under `on-request`. | Unattended episodes carry exact allowlists and `--permission-prompts none`; Codex runs only under `-a never`. |
| Is there a harness scheduler the resident could use? | C-U3, X-U3 (no scheduler subcommand; daemon or app-server surfaces listed, untested) | No. | The resident is its own process. It also runs as a single `--once` tick so an outside scheduler can drive it; no harness is the runtime. |
| What does a kill mid-episode leave behind? | C-K1, X-K1, C-U6, X-U6 (SIGKILL after the first write left the fixture edited and uncommitted, status ` M`; a fresh recovery from the preserved worktree completed) | An edited, uncommitted worktree; recovery from it works. | WO-052's recovery-by-commit-identity holds: a missing commit means reset and re-dispatch; the resident's `kill` on return is viable. |
| Concurrent sessions? | C-U5, X-U5 (3 of 3 completed; rate-limit status messages were routine) | No limit at three. | The resident's first proof dispatches one episode at a time; the policy's concurrency is a later input. |

Sizing at R1 (the critical path measured the first merges here): executor
spans since the stand-down are 22 to 87 minutes (WO-046 1,329 s, WO-048
1,692 s, WO-050 1,870 s, WO-045 5,194 s over two attempts, WO-047 1,034 s);
whole lifecycles 0.8 to 2.3 hours. The four-hour split rule is met with
room; none of the three orders is split again.

## 2. What changed under the three orders since 2026-09-08

| Landed | What it changes for WO-049, WO-051, WO-068 |
| --- | --- |
| WO-132 (v0.18.0): one product gate per order, hooks refuse two things and delegate the rest with an advisory, `Effort:` lines are recommendations, Cost lines are reconciled | Each order gets a real Cost line; effort lines are recommendations; the Contributor hook fallback is advisory, which a print-mode worker in a target must not inherit: WO-049's target hook fails closed. |
| WO-067 (v0.19.0): the compiled presence statechart (`present`, phases, `expired`; `phase-ready`, `returned`, `interrupt-phase` guards; `After(idleMs)` expiry) with a fixture interpreter | WO-068 executes that statechart and reuses the interpreter's logic as the resident's evaluator; it does not write a second one. |
| WO-050 (v0.21.2): `SkeletonState` as typed slices with one fold per event; a reserved `sourceChange` slot; hosts read through selectors | WO-068 adds a `resident` slice and fold; WO-052 later fills `sourceChange`. |
| WO-047 (v0.22.0): replay projects its environment through the application's projector | WO-068's byte-identical replay with a fake clock uses it; `ClockSampled` events make the tick an input. |
| WO-045, WO-046, WO-048: decoders at the store, hook-input, continuation and on-disk-state boundaries | The resident's new event types decode under schema 1; the `script` actor's program is in the executable subset; the resident's lock and pending dispatch are inspected before recovery. |
| WO-044 (v0.17.7): the rows in §1, egress-first close, derived-worktree settlement | The designs above. |

## 3. Receipts since the last pass

The last pass committed at 2026-09-15T05:46Z; 72 hours before this pass is
2026-09-13T03:30Z, the longer window. WO-130, WO-131 and WO-044 closed inside
it and were the stand-down pass's own subject (§1 to §3 of that document);
they are not re-digested. Seven orders closed after it, all on 2026-09-15
and 2026-09-16 UTC.

| Order | Version | Verdicts | Nominations and suggestions in its receipts | This pass |
| --- | --- | --- | --- | --- |
| WO-132 | v0.18.0 | VER-001 to VER-004 fail (Cost paragraph truncation; redirect-operand misparses admitting gate-input writes), VER-005 pass, FINAL-001 pass | `test-worktree.sh` now checks the PR body by containment, losing its counterexample; the next real order's one-gate segment and a live close from main are the reopening observations | Both observed: every order since ran one reviewer gate and published from main. The weakened assertion is a boy-scout item for the next order that touches that script (§5.9). |
| WO-067 | v0.19.0 | VER-001 pass, FINAL-001 pass | D003: a compiler-only release reused a snapshot that admitted the old compiler; the artifact-identity module is now pinned | Consumed by WO-068's design (§2). |
| WO-045 | v0.20.0 | VER-001 fail (integration and release evidence after a sibling published), VER-002 pass, FINAL-001 pass | `appendEvent` validates the log it extends, not the draft (nominated to WO-048, not taken); `tool_response` must be a plain object; version assignment collides by construction across lanes; the verifier classified a sibling's publication as a defect | The collision routing is settled as the two-lane workflow (§5.7). The draft validation joins the kernel hygiene pair (§5.10). |
| WO-046 | v0.21.0 | VER-001 pass, FINAL-001 pass | `stepProgram`'s default branch lost its kind-naming diagnostic and has no pin (a one-line test for the next kernel order); deferred cadence kinds decode inside `Await` timeouts; publication locks staled for the second consecutive order with no phase running the check | Kernel hygiene pair (§5.10). The publication check is a boy-scout line in WO-133's role-text edit (§5.4). |
| WO-048 | v0.21.1 | VER-001 pass, FINAL-001 pass | The read-inventory tripwire misses a module-local rebinding; WO-046 bumped the package without its runtime literals | The literal gap is the mechanism behind §3.1's finding. The tripwire gap stays nominated for the next host order (WO-068 does not own that inventory). |
| WO-050 | v0.21.2 | VER-001 pass, FINAL-001 pass | A stale Codex writer reservation refused every shell command including the diagnosis of itself; attestation stopped recording the operator's model and effort from 2026-09-15T18:22Z; `scenario.ts` is two lines under a ratchet satisfied by deleting blank lines | Attestation is WO-133 (§5.4). The reservation hazard is a candidate (§5.11). The ratchet is decided (§5.8). |
| WO-047 | v0.22.0 | VER-001 fail (publication freshness), VER-002 pass, FINAL-001 pass | D004 deferred the planning cost-table reconciliation to this pass; D007 raised the ratchet to 751 at the operator's direction and deferred the extraction question; the integrated tree's bound change has no independent verification | Reconciled here: the cost table is refreshed at this pass's subject and the `plan` document suites are green again. Extraction is decided (§5.8). |

### 3.1 WO-132's Cost line against the observed rows

WO-132 promised a fresh product gate of about 250 s against 605 s and
measured 246 to 255 s at its own close. The 38 `npm test` rows recorded on
this host since then (ignored `docs/control/local/harness/checks.json`)
split cleanly:

| Rows | Suites | Wall-clock | Cause |
| --- | --- | --- | --- |
| 27 passing rows with the 19 product suites only | 19 | 191 to 413 s, most 236 to 310 s | The promise holds when only product suites run; the spread above 300 s is host load from parallel lanes. |
| 6 rows where the reviewer's selection added the machinery suites | 27 or 31 | 625, 663, 716, 807, 840 s (and 450 s once) | `harness-fixtures` 254 to 493 s and `process-debt` 139 to 164 s were selected because a file under their declared sources changed since the base. |
| 1 row with the evidence editions but no heavy suite | 24 | 227 s | WO-046 changed no runtime literal. |

The selecting files are `packages/skeleton/src/harness-host.ts` and
`packages/skeleton/src/loadouts/`, which carry `HARNESS_HOST_VERSION` and
both profiles' `skeletonVersion` literals; every skeleton release bumps them
(WO-048 D003, WO-047 D005), so five of the seven reviewer gates since the
stand-down paid about 400 s for a version bump. This is a measured shortfall
of the Cost line and a planning input, not a gate. WO-133 moves the literals
into one module outside every machinery source list (§5.4).

### 3.2 The advisory flood and the stale built runtime (the fourth part)

Observed in this session's ignored hook journal: 344 rows carrying "DotLn
advisory: built adapter unavailable" between 2026-09-16T00:08Z and 03:03Z,
two per Bash call, from every hook kind (feedback 54, observe 36, permission
18 timed rows in the sample). Cause, verified by rebuilding: the checkout's
`packages/*/dist` was built at 23:29Z on 2026-09-15 and main was
fast-forwarded by three release closes after it (00:06Z, 01:41Z, 02:50Z);
each close regenerated the hooks' runtime pins, `assertHarnessRuntime`
found the built bytes differed, and the generated prelude's catch printed
the advisory on every invocation. `harness check` reported drift at
`commit-msg.mjs` until a 3.9 s `npm run build`, after which it passed.
Release close builds a missing `dist` (WO-132 criterion 10) and never a stale
one, and no hook remembers that it already said so. Because the session hook
also fell back, this session took no writer reservation and recorded no usage
counters until the rebuild. WO-133 carries the fix: rebuild after a
fast-forward that changes the pins, one advisory per session per cause, none
from PostToolUse observers, and a SessionStart line that names the cause.

### 3.3 Attestation regression

WO-050 VER-001 bisected it: the last operator-attested Codex completion is
WO-132 at 16:37Z; from WO-067 at 18:22Z every completion on both harnesses
records `effort unknown` although the operator selected one, with `source`
degrading through `session-tools-no-effective-readback`,
`session-observation` and `unavailable`. The canonical projection for WO-047
reads `effort unknown; source operator-selected-model-effective-effort-unavailable`
at Claude Code 2.1.273. The role text (contributor loadout, line 57) says
values are logged as given and the Codex default is operator-attested; the
sessions supply `unknown` instead. WO-133 restores the operator-attested
fallback in role text and refuses a downgrade of a supplied value.

### 3.4 Two lanes as the normal workflow

Every order since the stand-down ran in parallel with a sibling and every
integration was handled by hand a little differently: WO-045 went back
through repair because its verifier read a sibling's publication as a
defect; WO-048 and WO-047 retimed at final review under D003 and D005;
followups.json needed a union merge twice; the generated bundle, both
publication editions, the README release block and the control projections
conflicted every time and were regenerated; two orders bought `scenario.ts`
headroom from the same blank lines and the merge broke a ratchet neither
breached alone. The operator's direction today is recorded as a decision:
two lanes are the normal workflow, one order at a time passes final review
and release close, and the integrating final review owns a fixed list of
mechanical steps (§5.7, product 07 §Independent workflows and integration).

## 4. The repository at v0.22.0 — meta review

**Numbers (observed 2026-09-16).** 1,205 tracked documentation files; the
tracked working set is 74 MB with a 13 MB pack. The largest documents are
the append-only idea ledger (664 KB), the generated work-order index (229
KB), the planning map (199 KB) and four product documents above 100 KB. Source
is 74,000 lines across packages and scripts; the eight largest files are
`scripts/test-process-debt.mjs` (5,406), `scripts/test-harness.mjs` (3,235),
`packages/skeleton/src/harness-host.ts` (3,040), `scripts/test-plan-refutation.mjs`
(2,737), `packages/skeleton/src/reactor.ts` (2,572),
`loadouts/entropy-reducer.ts` (2,133), `scripts/release.mjs` (2,067) and
`scripts/lib/writing-worker-probe.mjs` (1,902). The default gate is 19
product suites, each with a `protects:` line, 191 to 413 s on this host; the
machinery run is 16 checks in 547 s and the document run 17 checks in 71 s.
Hook latency today: 108 timed rows, mean 296 ms, peak 1,100 ms, so a Bash
call pays about 0.6 s of hooks (WO-130 measured 267 ms; unchanged by the
stand-down). Local, ignored lanes: 13 immutable runtime snapshots (34 MB),
hook journals 36 MB, retained close lanes 31 MB, with no retention rule.
The follow-up register holds 297 entries, 283 untriaged; 24 of them arrived
today because every per-order decision record is harvested as a follow-up;
three passes have selected nothing from it.
`npm run meta` also reports one advisory budget breach on the branch: the
release-close role text is 8,865 cold-start bytes against an 8,192 ceiling,
inherited from WO-132's rewrite of that role; WO-133 regenerates the role
text and can trim it, and the breach refuses nothing.

**What improved.** Seven product and codec orders merged in one day after
five days of machinery, each with one gate and one close; the kernel moved
for the first time since 2026-09-09; every close published. The stand-down's
promise holds on the product-only rows (§3.1).

**Gaps worth filling, and the disposition of each.**

1. The built runtime does not follow main, and a broken runtime floods
   instead of speaking once. WO-133 (§5.4).
2. Attestation discards what the operator selected. WO-133.
3. Machinery suites are selected by version literals. WO-133.
4. Two-lane integration is a habit, not a procedure. Product 07 write-back
   (§5.7); no code.
5. The follow-up register is a write-only queue: decision records are not
   follow-ups, the migration rows are unread, and the feed has yielded no
   selection in three passes. Candidate in product 07 (§5.11): settle the
   migration rows as historical in one pass and stop harvesting decision
   records that carry no `reopenWhen` action, so the feed shows live items.
   Not allocated now; it is planning machinery.
6. Local lanes grow without bound. Candidate in product 07 (§5.11): a
   retention rule for snapshots that no manifest pins and for retained close
   lanes older than the order's published release. Not allocated now.
7. A stale writer reservation cannot be diagnosed from the session it blocks.
   Candidate in product 07 (§5.11); the stand-down declined a shell
   classifier, and the fix is a metadata-list edit that needs an observed
   second occurrence before it is worth an order.
8. `scenario.ts` sits at a ratchet satisfied three times by deleting blank
   lines. Decided (§5.8): the next order that needs a line extracts the live
   driver; the bound is not raised again.
9. Kernel hygiene: `appendEvent` validates the log but not the draft; the
   stepper's backstop is untested. Decided (§5.10) as one boy-scout pair for
   the next order that touches `packages/kernel`.
10. Publication locks stale whenever a product document changes and no phase
    before final review runs the check. A role-text line in WO-133's
    contributor edit (§5.4).

**Reorganization considered and declined (NoOps, §6).** Splitting
`harness-host.ts`; moving evidence out of the repository; consolidating the
70 scripts; splitting the 5,406-line process-debt test. Each would touch the
machinery the operator just stood down, for no product outcome this horizon
names; WO-133's 20-line version module removes the one recurring cost that
the file's size causes (§3.1) without reorganizing it.

**Tech debt to pay now versus later.**

| Debt | Now | Later, with its trigger |
| --- | --- | --- |
| Stale dist, advisory flood, attestation, literal selection | WO-133 | |
| Two-lane integration steps | Product 07 decision | A `worktree sync` helper when a third lane appears (WO-079's territory) |
| `scenario.ts` growth | Decision recorded | Extraction at the next needed line (WO-068 is the likely order) |
| Kernel draft validation and stepper pin | Boy-scout pair recorded | The next kernel order |
| Follow-up register migration | Candidate | A planning pass that spends one session settling it |
| Local lane retention | Candidate | Disk pressure or a snapshot count above twenty |
| Documentation weight (ledger, generated index, 100 KB product documents) | None; the planner reads the ledger tail and scoped sections | WO-084 to WO-090, deferred behind WO-053 by the operator's dated deferral |

**Antifragility added by this pass.** The resident runs as a `--once` tick
so a kill costs nothing and any outside scheduler restarts it (WO-068). The
target bundle imports the launchpad's content-addressed snapshot, so a
launchpad rebuild cannot break a running target (WO-049); it refuses to
modify a tracked file, so a target with its own settings is a refusal and
not a silent merge. A broken runtime degrades to one line and a named cause
(WO-133). Version collisions and generated-surface conflicts become a
checklist the second lane runs, not a judgment (§5.7). The machinery suites
are selected by content, not by version (WO-133).

## 5. Decisions of this pass

Each names its sources and the observation that reopens it; the goal and
critical-path contribution, the NoOp baseline and the trap lenses are stated
where they are material and grouped where they are not.

1. **R1 is decided as §1 states.** WO-049 keeps hooks for Claude, carries
   the instruction block for both, fails closed, and claims no sandbox
   containment; WO-051 uses exact `--allowedTools` patterns and the named
   Codex write profile, and the host writes the commit message; WO-068 is
   its own process with a `--once` mode, `cli-worker` deferred to WO-122.
   Sources: the rows cited in §1. Reopen: a new writing-worker record whose
   rows contradict a cited one, or a live smoke (WO-049 criterion 6, WO-053)
   that fails a claim the rows supported.
2. **The three orders are rewritten as stable contracts at this
   checkpoint**, with real Cost lines, recommendation effort lines, updated
   typed dependencies (WO-044, WO-050, WO-067 closed) and observed gaps
   dated at `b7914ed`. The mandatory replan authorizes the rewrite; they
   were never activated. Reopen: activation of any of them, after which
   only execution records and dated reassessments are appended.
3. **The sequence is ordered in lane pairs** (§7): adjacent entries with
   disjoint primary surfaces and no hard edge inside the pair. WO-051 moves
   ahead of WO-121 and WO-119 so the external-change chain is contiguous;
   WO-133 goes first. Sources: the operator's third message; the surfaces
   each order names; the critical-path graph edges. Reopen: an integration
   conflict inside a pair, or R2.
4. **WO-133 is filed, first, as one order holding four residue repairs of
   WO-132's own changes**: the runtime follows main and a broken runtime
   speaks once (§3.2); attestation keeps the operator's values (§3.3); a
   version literal alone selects no machinery suite (§3.1); and, as an
   admitted boy-scout line inside the same role-text edit, run the
   publication check after touching a product document. One order rather
   than four because each is a one-file edit with an observed row, all four
   are residue of the same order, and the 2026-09-12 pass showed that four
   small machinery orders pay four lifecycles; one order rather than
   boy-scout items on WO-049 because two of the four are outside WO-049's
   paths and the operator asked for the fix now. Traps weighed: escalation
   (this is a machinery order the day after the stand-down; it adds no
   mechanism and removes three recurring costs with rows), rule beating (the
   advisory fixture counts rows, not the absence of a message), shifting the
   burden (the rebuild leaves the operator's memory), drift (the gate
   shortfall is named, not normalized). NoOp: the flood recurs after every
   close on main, attestation stays unknown, and every skeleton order's
   reviewer gate stays near 700 s. Reopen: any of its criteria observed
   failing after merge, or the next reviewer gate of a skeleton order above
   360 s with only a literal changed.
5. **WO-047 D004's deferral is settled here.** The planning cost table is
   refreshed at this pass's subject and the `plan` and
   `plan-refutation-current` document suites are green on the branch; the
   follow-up record for D004 is disposed `settled`. Reopen: never for this
   subject; a future stale table is that pass's own duty.
6. **The stand-down's cost reconciliation is recorded** (§3.1) as planning
   input with its cause; no ceiling is raised and no gate is added.
7. **Two lanes are the normal workflow.** Recorded in product 07
   §Independent workflows and integration as the operator's 2026-09-16
   direction with the integration checklist: preserve, merge main, regenerate
   the bundle, index, meta and publication locks, union the follow-up
   register, retime an unpublished release under its recorded
   classification with a dated decision, run the affected checks on the
   integrated tree, and never treat a sibling's publication, a text conflict
   or a version collision as a finding. One order at a time passes final
   review and release close; that is the operator's discipline, not a gate.
   The verifier and reviewer skills already cite that section, so no code
   changes. Reopen: a third lane, or an integration that needs a step the
   list does not name.
8. **`scenario.ts` is extracted, not ratcheted.** The next order that needs
   a line in it moves `LiveReactorDriver` to its own module and the WO-016
   bound follows the file that keeps the fixture tree; the bound is not
   raised again. Named as WO-068's admitted boy-scout item because it is the
   likely next order to need the line. Sources: WO-047 D007 and FINAL-001.
   Reopen: an operator direction to raise the bound instead.
9. **`test-worktree.sh`'s weakened containment assertion** is a boy-scout
   item for the next order that touches that script; it is not WO-133's
   path. Source: WO-132 FINAL-001 item 2.
10. **Kernel hygiene pair**: validate the assigned envelope in `appendEvent`
    before writing (WO-045 O1, WO-045 FINAL-001) and pin `stepProgram`'s
    default branch (WO-046 O1). Recorded as one admitted boy-scout pair for
    the next order that touches `packages/kernel`; neither has a current
    caller that trips it, so neither earns an order.
11. **Three candidates enter product 07**, with no allocation: follow-up
    register settlement, local lane retention, and stale writer reservation
    self-diagnosis (§4 gaps 5 to 7). Each names its reopening observation.
12. **The three open follow-ups (FUP-0091, FUP-0111, FUP-0132) remain open.**
    FUP-0111 gains a dated measurement in its candidate: this pass consumed
    the feed's first page and selected nothing from it, because 283 of 287
    pending rows are untriaged migration; orientation needed the reads listed
    in the method paragraph. The candidate stays open until the register
    settlement lets the feed be judged on live rows.

## 6. Declined alternatives — the NoOp register of this pass

1. _Convert WO-049 to a host-side containment order._ Declined: C-W3, C-W4
   and C-W5 show the target's hooks and settings apply to a Claude print-mode
   worker. Reverse: a live smoke where the emitted hook does not fire.
2. _Wait for WO-133 before WO-049 activates._ Declined: no behavior depends
   on it; WO-049 integrates on WO-133's merge (both edit the hook template).
   Reverse: none.
3. _Bundle the residue into WO-049 as boy-scout items._ Declined: the
   rebuild-after-close and the attestation fallback are outside WO-049's
   paths, and the operator asked for the fix now. Reverse: none.
4. _Four residue orders._ Declined: four lifecycles for four one-file edits;
   the 2026-09-12 pass paid exactly that. Reverse: the executor finds one of
   the four needs more than its named file, in which case it is deferred to
   its own order with a named follow-up.
5. _Have the worker compose its commit message._ Declined: no wildcard
   Bash pattern was observed (C-W2 is exact), and a host-written message
   file keeps attribution and camouflage in the host. Reverse: an observed
   row for a wildcard pattern and a reason to want model-authored messages.
6. _Let the resident use a harness scheduler._ Declined: none exists (C-U3,
   X-U3). Reverse: a later record with a scheduler row.
7. _Write a second statechart interpreter in the resident._ Declined:
   WO-067's fixture interpreter already executes the emitted table with the
   real kernel functions; the resident moves that logic, not copies it.
8. _Split `harness-host.ts` (3,040 lines) now._ Declined: it is the hook hot
   path and every order that touches it pays the machinery suites; WO-133's
   version module removes the recurring cost without the churn. Reverse: a
   product order that must change its structure.
9. _Move evidence out of the repository or prune it._ Declined: immutable
   evidence and the publication contract depend on it; the pack is 13 MB.
   Reverse: none this horizon.
10. _Consolidate the 70 scripts or split the 5,406-line process-debt test._
    Declined: machinery churn the day after the stand-down, for no product
    outcome. Reverse: a machinery pass the operator opens.
11. _Settle the 283 untriaged follow-ups in this pass._ Declined: a session
    of planning machinery; recorded as a candidate. Reverse: the next
    planning pass that has the session to spend.
12. _Add a retention rule for local lanes now._ Declined: 101 MB of ignored
    material is not a constraint today. Reverse: disk pressure or more than
    twenty snapshots.
13. _Pair WO-068 with WO-051 or WO-133 with WO-049._ Declined: both pairs
    share a file (`execution-environment.ts`; the hook template). Reverse:
    none.
14. _Add a cross-order gate for the one-at-a-time final review._ Declined
    again (product 07 already records it as an open option); the operator's
    discipline is preserved in handoff text. Reverse: an observed second
    order entering final review while one is between review and close.

## 7. Lane pairs

Pairs are adjacent entries in the sequence, separated by a blank line.
Inside a pair: disjoint primary surfaces and no hard dependency edge. The
second lane integrates main at its final review by the checklist in product
07; one order at a time passes final review and release close.

| Pair | Orders | Why they do not collide |
| --- | --- | --- |
| 1 | WO-133, WO-068 | Residue edits `harness-host.ts`, the hook template, `contributor.ts`, `release.mjs`, `resume.mjs`, the runner; the resident adds `resident-host.ts`, `actor-catalog.ts`, a slice in `reactor.ts` and a `dotln` command. |
| 2 | WO-049, WO-051 | The bundle edits the compiler's harness lowering and the emit helper; the profile edits `worker-protocol.ts`, `worker-transport.ts`, `execution-environment.ts`. Both depend only on closed orders. |
| 3 | WO-121, WO-119 | Presence origin edits the presence fold and hook signals; the discovery producer is a new `script`-actor module. Both need WO-068. |
| 4 | WO-052, WO-122 | The source-change host fills `sourceChange` and adds a host; the actor catalog's second slice fills `cli-worker` and `human-handoff`. Both need pairs 1 to 3 merged. |
| 5 | WO-053, WO-099 | Two live proofs on different chains. |
| 6 | WO-114, WO-069 | A status projection and a configuration root. |
| 7 | WO-070, WO-120 | Beacon portability and derived work identity. |
| 8 | WO-054, WO-115 | Verification over a real worktree and the console parity contract. |
| 9 | WO-055, WO-116 | Repair continuation and the served audit projection. |
| 10 | WO-056, WO-100 | Live blinded verification and the portfolio (its inputs, WO-054, WO-119, WO-120, WO-052, are closed by then). |
| 11 | WO-111, WO-117 | The unattended hour and the live console host. |

The remaining horizon keeps its serial reading order; R2 after WO-053 and
WO-111 cuts the next pairs on the measurements.

## 8. Reversal conditions for this plan

Reopen at a planning pass when: a live smoke contradicts a row §1 relied on;
an integration conflict lands inside a lane pair; WO-133's criteria fail
after merge or a skeleton order's reviewer gate exceeds 360 s with only a
literal changed; a third lane appears; or the resident's `--once` mode is
found insufficient for an outside scheduler on the operator's host.

## 9. Evidence of this pass

Entry and handoff process cost: unknown (the harness session began in the
hooks' fallback; no usable counters). The pass ran `npm run build` once
(3.9 s) to verify §3.2's diagnosis, no code suite, and `npm run test:docs`
at its end. The refutation is one background goal review recorded under
`docs/planning/refutations/`. The follow-up feed's first page was read;
one record (WO-047 D004) is disposed.
