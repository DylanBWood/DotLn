# Plan refutation — 2026-09-06 phase-two redirect (receipt 006, manual, pre-mechanism, final for the pass)

**Disclosure.** As in the earlier receipts: a manual blinded dispatch through
the planning session's harness, the same reading list as receipt 005, the
plan, the ledger, the earlier receipts, and every planning narrative
excluded, the fixed verdict rules supplied. The subject was the working tree
after receipt 005's dispositions, committed unchanged with this receipt. The
planning session wrote the disposition block; the self-referential
instrument is disclosed. The refuter's verdict on WO-041 itself is advisory.
This is the final run of the pass; no order was changed after it.

| Field         | Value                                                                                        |
| ------------- | -------------------------------------------------------------------------------------------- |
| Subject       | the marked sequence block and the ten order files it names, after receipt 005's dispositions |
| Subject hash  | `sha256:b52dceb84feb69115c7fb34379183ae9ab0fb9ad7a9dcde7b31518b257f30484`                    |
| Transport     | manual dispatch, Claude Code Agent tool (fresh general-purpose session; no DotLn transport)  |
| Harness       | Claude Code 2.1.263                                                                          |
| Model, effort | claude-fable-5-1, max                                                                        |
| Dispatched    | 2026-09-07T02:05Z (2026-09-06 in the operator's day)                                         |
| Verdict       | **pass**; no holds; no drift; two open risks short of a hold; one open clean-room item       |

## Result (verbatim JSON in the sibling file)

| Order  | Verdict              | Thesis                                                                                                        | Capability row                                            |
| ------ | -------------------- | ------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| WO-032 | thesis-advancing     | The core bet's legible half of the loop; every view compiles to the same program                              | `projection.uifa-board` (new)                             |
| WO-033 | thesis-advancing     | A build, not a biography, carried into forks; without prescribing that loadout; Horizon 1 across repositories | `launchpad.starter` (new)                                 |
| WO-034 | thesis-advancing     | Horizon 1's one outcome across repositories; the Mission's babysitting measures                               | `consumer.angular` (new; level 0 unless the run succeeds) |
| WO-035 | machinery            | none                                                                                                          | none                                                      |
| WO-036 | machinery            | none                                                                                                          | none                                                      |
| WO-037 | thesis-advancing     | Every pattern compiles into exact mechanics; Horizon 2                                                        | `compiler.five-s-set` (new); widens `compiler.seiri-v1`   |
| WO-038 | machinery            | none                                                                                                          | none                                                      |
| WO-039 | thesis-advancing     | A session gets a build, not a biography                                                                       | `compiler.harness-v1`, `harness.self-hosted` (new)        |
| WO-040 | thesis-advancing     | The roughly 140 rules become typed mechanisms; the core bet's incident, mechanism, fixture triad              | `feedback.migration` (new)                                |
| WO-041 | machinery (advisory) | none; its subject is documents                                                                                | `control.plan-refutation` (new)                           |

The refuter's rationale, quoted:

> Six of ten orders move a named thesis into code with a capability row
> [...]. The four machinery orders (WO-035, WO-036, WO-038, WO-041) are
> honest about being process; none drifts. I held WO-041 to machinery
> deliberately: it is built from the platform's own actor primitives, but
> its subject is documents, and admitting it under Mission would admit
> every process improvement.
>
> I looked hard for the traps named in the brief. The "actor UI that shows
> no person" is closed by WO-032 criterion 2. The "starter with no compiled
> behavior or a forced loadout" is closed by WO-033 criterion 5's export
> smoke and its `build: none` fixture. "Operator sessions governed by prose
> with no required reduction" is closed three times over (WO-039 c6, WO-040
> c4, WO-035 c5), each strictly directional with a mechanically derived
> directed-read set and a before value fixed at the activation base.
> After-the-fact denominators are pre-registered (WO-034) or labeled
> attestations (WO-040). Term lists never enter committed files, refusals
> never echo the match, and every model-authored committed surface is gated,
> except `CONTRIBUTING.md`, which gets only a human read.
>
> Two open risks fall short of holds: WO-034 c4's "observed baseline"
> wording contradicts its own "never called observed" (fix: say
> "attested"), and WO-041's override is attributable but not refusable, as
> its assumptions disclose. The largest gap, the authoring half of the
> differentiated interface, is deferred to named preserved candidates; the
> loop's "declare intent" step and the v1.0 stranger remain unnamed.

## Disposition (2026-09-06, planning session)

**Verdict accepted; no order changed.** The planning session declared this
run final before it ran and does not edit the subject after it. The two open
risks and the unnamed gap are recorded here for the activation preflight and
the next pass, not fixed:

1. **WO-034 criterion 4 wording.** The direction clause compares run counts
   with "the observed baseline" while the same criterion says attested
   baselines are never called observed. The intended reading is that the
   direction test runs against the attested baseline wherever one exists
   and yields `unverified` where none does; the one-word change ("attested
   baseline") is left for the activation preflight, which may amend a draft
   order with a dated note, and a wrong reading can only produce an honest
   `unverified`, never a false pass.
2. **WO-041's override is attributable, not refusable.** The override event
   records its actor and cites a capture hash the committed tree cannot
   verify; a planner that overrides is visible, not blocked. This is the
   limit of what a local control plane can enforce without an external
   identity, and WO-041's assumption 1 discloses it. The next pass may
   decide whether an operator-only signing step is worth its ceremony.
3. **The unnamed secondary gap.** The loop's first step, declaring a bounded
   intent in prose and receiving a receipt, and the v1.0.0 stranger test
   have no order and had no recorded candidate. The planning map's
   preserved candidates now carry "Intent declaration and the stranger
   test" beside the authoring candidate; naming a candidate changes no
   order and leaves this receipt's subject intact.

**Open clean-room item.** `CONTRIBUTING.md` (WO-038) gets a final-reviewer
read against the clean-room floor and no mechanical check, because it lands
before the local-terms check exists; low exposure, disclosed in the order.

**Standing verdict for the horizon: pass.** The planning pull request may
open. The next planning pass runs the refuter first, through WO-041's
command if it has shipped.
