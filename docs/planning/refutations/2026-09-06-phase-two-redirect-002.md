# Plan refutation — 2026-09-06 phase-two redirect (receipt 002, manual, pre-mechanism)

**Disclosure.** As in receipt 001: a manual blinded dispatch through the
planning session's harness, not a DotLn transport; the same reading list plus
`CLAUDE.md` so the refuter could judge clean-room risk; the plan, the ledger,
the earlier receipt, and every planning narrative excluded; the fixed verdict
rules from WO-041 supplied in the brief. The subject was the working tree
after receipt 001's dispositions, whose bytes are committed unchanged with
this receipt. The planning session wrote the disposition block; the
self-referential instrument is disclosed. The refuter's verdict on WO-041
itself is advisory.

| Field                            | Value                                                                                        |
| -------------------------------- | -------------------------------------------------------------------------------------------- |
| Subject                          | the marked sequence block and the ten order files it names, after receipt 001's dispositions |
| Subject hash (as refuted)        | `sha256:e2e4bd88b55340a488f431eb3330580b145c37cffb8cf6760508641ff68c0406`                    |
| Subject hash (after disposition) | `sha256:58c5652c1d4a88fc1c47d464a157cc8ff7fec0cbe0f4d023398a063d76d19e6f`                    |
| Transport                        | manual dispatch, Claude Code Agent tool (fresh general-purpose session; no DotLn transport)  |
| Harness                          | Claude Code 2.1.263                                                                          |
| Model, effort                    | claude-fable-5-1, max                                                                        |
| Dispatched                       | 2026-09-07T00:55Z (2026-09-06 in the operator's day)                                         |
| Verdict                          | **hold**, four reasons, nine self-certification risks, five clean-room risks, no drift       |

## Result (verbatim JSON in the sibling file)

Seven orders thesis-advancing (WO-032, WO-033, WO-034, WO-037, WO-039,
WO-040, WO-041), three machinery (WO-035, WO-036, WO-038), none drift. The
refuter's rationale, quoted:

> The horizon does move the vision: seven of ten orders put a named thesis
> into code with a capability row, and no order works against a "What DotLn
> is not" item [...]. The hold rests on criteria, not direction. WO-040's
> headline number, shapes governed "by mechanism versus prose," is a column
> the executor fills in, and the order defers the actual prose removal to
> WO-035; a live session can read exactly the same bytes after the batch.
> WO-039's context measurement is directional, which is right, but it never
> says the generated role skill counts in the after value; relocating the
> guide into `SKILL.md` would pass. WO-034 can close on synthetic fixtures
> under assumption 4 with its objective's central claim unproven, and its
> baseline comparison requires no direction where the Mission clearly implies
> one. WO-041, the instrument meant to catch all this, hashes the orders but
> not the vision it judges them against, and its hold is dischargeable by the
> refuted planner's own line.
>
> The largest gap is the differentiated interface's authoring half: after ten
> orders the shelf holds one compiled pattern and every build is TypeScript
> the author wrote; nothing lets anyone mix and match agents through an
> analogy, and the deferrals name no work order.
>
> On clean room, the shared design choice, a committed list of SHA-256 hashes
> of forbidden terms, deserves a second look: hashes of well-known product
> names are reversible by guessing, the refusal fixtures need a plaintext
> match, and WO-033 propagates the list into every fork.

## Disposition (2026-09-06, planning session; every hold accepted and changed)

1. **WO-040 criterion 4: the "by mechanism" count is a classification column;
   the same bytes can load after the batch.** Accepted; changed. Governance
   mode is now derived, not classified: a shape is `mechanism` only when its
   unit is equipped **and** no always-on sentence restating it remains,
   proven by a fixture that pins the retired sentence's hash and asserts its
   absence; each batch removes its own sentences in the same order instead
   of nominating them; the always-on bytes and every role's loaded total
   must fall strictly; `--check` refuses a `mechanism` row whose sentence is
   still present.
2. **WO-039 criterion 6 does not count the role skill in the after value;
   criterion 4 is unmeetable on the assumed path.** Accepted; changed.
   Criterion 6 now counts everything the session loads for the role
   (always-on bytes, the residue block, the skill as loaded, and every
   reference the skill instructs the session to read) and requires that sum
   strictly lower than the base's always-on bytes plus mandatory read, with
   relocation into skills stated as counting; criterion 4's correction-event
   leg is conditional on the operator's token and the skill path is the
   evidenced alternative.
3. **WO-034 can close on synthetic fixtures with its central claim unproven;
   criteria 4 and 6 have no direction.** Accepted; changed. A partial real
   run closes with criterion 3 recorded as failed, creates no
   `consumer.angular` row above level 0, enters the sibling registry as "not
   evidenced", and moves nothing in product 12; every observed baseline
   measure must be beaten or is recorded failed, and an unknown baseline
   yields `unverified`, never a pass; zero hand restatements is the pass
   condition for "the build is the priming" and any other count records
   that claim as failed.
4. **WO-041's subject hash omits the standard, and the refuted planner can
   discharge a hold with its own line.** Accepted; changed. The subject hash
   now covers the vision's thesis sections, the roles table, and the
   capability rows beside the sequence and the orders, with a fixture per
   surface; a hold is answered only by a change plus a fresh receipt, or by
   an operator override that cites a captured operator instruction by hash,
   which the planner session may not author; the gate fails a
   planner-authored override.

Self-certification risks: all nine are answered by the four changes above
and by WO-032's criterion 5, where the product lead's question now maps to
a refutation-verdict cell on the Blueprint panel rather than to a status
label and the verifier reviews the mapping. WO-040's whole-set denominator
is unverifiable from committed files by construction; it is now pinned to
the intake capture's hash and disclosed as an operator attestation.

Clean-room risks: accepted; changed. The committed hash list is gone. WO-039
adds a local-terms check whose plaintext list lives only in ignored local
state, is never committed or hashed into any file, refuses matches, and
reports `unavailable` when absent; its fixture uses a synthetic term. WO-033
splits the deny list into public launchpad vocabulary (committed) and the
instance's local terms (never committed), exports no term list in any form,
runs the check over kit text, and adds a reviewer read of kit text for
descriptions of a specific host, gateway, or policy. WO-034's transcripts run
through the check and its baseline is counts only. WO-039's discovery record
reduces paths, session ids, and host names to shapes. WO-040 consumes the
same check.

**Largest gap.** Accepted and now named: the planning map's preserved
candidates record "Console parity contract and drag-equip authoring" as the
wave-5 candidate the next planning pass files once WO-032 and WO-037 have
merged and WO-034's shell exists, and WO-032's and WO-037's non-goals defer
to it by name. The runner-up gap (the floor, the trimmed guide, and later
batches) is answered by the derived count and by one batch per wave from
wave 4.

**Re-run.** Every disposition changed an order file, so the subject hash
changed; receipt 003 is the third blinded run over the changed subject and
carries the horizon's standing verdict.
