# Plan refutation — 2026-09-20-planning-1a0fb634704921a3-021

Subject: `sha256:2cfd506c73cab99a91a5ea456243f56139b9dd29b06bf7e1aadf9a66ef8bcdcf` at committed revision `247e4c6c144ceabc5141fa7d80fe6dd505b63eb0`.

Pass: `planning-1a0fb634704921a3` (planning). Verdict: **aligned-with-findings**.

Review source: direct harness session. Harness version, model and effort: unknown; settings verification: unverified. No CLI launch or transport acceptance is claimed.

Judgment frozen: 2026-09-20T17:08:39.296Z. Frozen result hash: `sha256:15ff58025db7ee0db1eb168ebd8664bb8524fd862e77a21f23fbddadc97896b5`. Local-terms list: **unavailable**.

Judgment basis: canonical subject and plan-goal-review-v1 protocol. Independence is session-attested; context isolation was not enforced and model tools were available. Session statement: I read all 179350 characters of docs/control/local/wo090-refute-prompt.json in nine contiguous, untruncated parts, including its supplied vision, roles, goal review, cost evidence, WO-146 contract, sequence, output instructions, closed result schema and worker instructions. I verified the working directory and Git root as read-only metadata, then independently judged only that canonical subject through the Contra-Auguste architecture-and-semantics lens. I read no repository source, planner narrative or prior receipt, spawned no agents, made no Git writes, and wrote only the frozen result and this statement in the granted system-temp scratch directory. My verdict is aligned-with-findings with no hold; I did not verify an implementation or file a receipt.

This verdict evaluates a planning horizon; it does not verify implementation or confer operator decision authority.

Scope: pass; dispatch-to-file 413625 ms. Judged 1 orders and the sequence; carried 93 unchanged verdicts by order and receipt hash.

## Validated result

```json
{
  "schemaVersion": "plan-goal-review-v1",
  "orders": [
    {
      "workOrderId": "WO-132",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Supports every gate by reducing lifecycle overhead for the showrunner and devops roles. NoOp retains the recorded repeated gates, 605-second release-close gate and override episodes. These supplied records were not independently rerun.",
      "systemTraps": "Policy resistance: removes competing refusals. Commons: reduces suite execution and context. Drift: sets an absolute fresh-gate target. Escalation: removes recursive process checks. Success to the successful: explicitly retires invested machinery. Intervenor burden: removes override and closeout rescue. Rule beating: retains product evidence tied to code identity. Wrong goal: redirects effort toward product outcomes; speed alone must not substitute for correctness.",
      "removalBalance": "Yes, the Cost line names substantial recorded removals and no new recurring procedure. The estimated gate reduction remains a prediction until measured.",
      "failureBehavior": "Unavailable adapters and missing process measurements become advisory. Writer and live-gate protections still refuse. The eight-hour fallback requires operator intervention rather than automatic degradation.",
      "findings": [
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "The code-identity exclusion list is sound only while excluded content cannot independently change the behavior covered by the reused gate.",
          "evidence": null,
          "reopenWhen": "An excluded-file change alters tested behavior while retaining the same successful gate identity."
        }
      ]
    },
    {
      "workOrderId": "WO-126",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Cross-cutting lifecycle support for the critical path. Its recorded NoOp includes 5,305,410 inherited/generated read bytes and a 184,668-byte planner read. WO-132 subsequently replaces several mechanisms introduced here.",
      "systemTraps": "Policy resistance: advisory Stop reduces competing guards. Commons: bounded reads reduce shared attention. Drift: absolute budgets expose growth. Escalation: hard budget enforcement could create more process and is subsequently demoted. Success to the successful: evidence reuse challenges repeated execution. Intervenor burden: automates closeout. Rule beating: attribution and meaningful evidence remain distinct from receipt volume. Wrong goal: cost measurement serves operator flow.",
      "removalBalance": "The stated removal exceeds the proposed additions, but the later stand-down records show that the original gate architecture did not deliver all promised savings.",
      "failureBehavior": "Stop is advisory; its original lifecycle and budget checks refuse. Assess those as historical mechanisms superseded by WO-132, not as instructions to reinstate them.",
      "findings": []
    },
    {
      "workOrderId": "WO-042",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate A enables unattended authority, target bundles and instance overlays. NoOp leaves supports able to widen authority and inspection able to disagree with enforcement; no monetary or time baseline is supplied.",
      "systemTraps": "Policy resistance: one effective envelope reconciles grants and restrictions. Commons: compile-time checking avoids repeated judgment. Drift: pinned behavior preserves the floor. Escalation: bounded grants avoid ad hoc exceptions. Success to the successful: provenance is independent of actor vendor. Intervenor burden: removes repeated authority inspection. Rule beating: tooltips derive from enforcement data. Wrong goal: protects authority rather than maximizing dispatch.",
      "removalBalance": "Unknown: the legacy Cost line claims no measured removal. This is a product authority prerequisite, not demonstrated process savings.",
      "failureBehavior": "Invalid widening refuses deliberately. Removing a grant restores the earlier envelope and semantic hash; unsafe authority is not a permissible fallback.",
      "findings": []
    },
    {
      "workOrderId": "WO-043",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate B enables dependency truth, stable contracts and derived identities. NoOp retains token-based dependency interpretation and manual activation diagnosis; its cost is unmeasured.",
      "systemTraps": "Policy resistance: separates blockers from citations. Commons: shares one projection. Drift: explicit relations replace ambiguous token inference. Escalation: typed reasons avoid repeated clarification. Success to the successful: legacy files remain readable. Intervenor burden: computes activation eligibility. Rule beating: passing closure matters, not merely closed status. Wrong goal: supports showrunner sequencing without becoming a scheduler.",
      "removalBalance": "Not established by the legacy Cost line. The objective removes duplicated interpretation but provides no comparative measurement.",
      "failureBehavior": "Untyped orders retain a labeled legacy view without blocking. Malformed declarations and unmet blocking dependencies refuse explicitly.",
      "findings": []
    },
    {
      "workOrderId": "WO-125",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate B repairs the transport used by Codex refuters and workers. NoOp leaves declared effort selection unavailable through that adapter; the cost is unmeasured.",
      "systemTraps": "Policy resistance: aligns declared effort with observed launch syntax. Commons: adds a bounded observation matrix. Drift: distinguishes requests from readback. Escalation: avoids user-configuration troubleshooting. Success to the successful: enables another actor transport. Intervenor burden: removes manual launch adaptation. Rule beating: launch claims are not effective-effort proof. Wrong goal: adapter usability, not effort-label compliance, is the outcome.",
      "removalBalance": "Unknown: the legacy Cost line supplies no measured comparison.",
      "failureBehavior": "Unknown effort preserves the existing launch. The original unavailable-level refusal is subsequently demoted by WO-132; it should not be treated as current horizon policy.",
      "findings": []
    },
    {
      "workOrderId": "WO-128",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Cross-cutting gate reliability supports all product orders. NoOp retains twelve recorded failed fresh gates totaling 8,387 seconds, plus their reruns.",
      "systemTraps": "Policy resistance: deadlines account for scheduler load. Commons: measurement series consume substantial host time. Drift: first-pass success is explicit. Escalation: prohibits automatic retries masking defects. Success to the successful: exclusivity is reconsidered empirically. Intervenor burden: diagnostics replace manual reconstruction. Rule beating: load failures remain distinct from product defects. Wrong goal: reliability must be assessed alongside total gate time.",
      "removalBalance": "A credible recurring removal is named, but the one-time measurement series is expensive. The supplied WO-132 diagnosis records that the faster scheduling configuration was not retained; WO-132 corrects that choice.",
      "failureBehavior": "Out-of-class load and actual defects still fail with diagnostics. This improves failure explanation rather than providing a general fallback; later stand-down removes much of the affected workload.",
      "findings": []
    },
    {
      "workOrderId": "WO-129",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Historical gate-cost support across the critical path. NoOp was five transition-following fresh runs totaling 3,876 seconds and a separate 476-second release gate.",
      "systemTraps": "Policy resistance: narrower keys reduce irrelevant invalidation. Commons: shared records reduce duplicate execution. Drift: miss explanations expose regressions. Escalation: declaration/cache machinery itself adds maintenance. Success to the successful: WO-132 later removes that investment. Intervenor burden: reuse reduces repeated commands. Rule beating: seals and fresh execution protect evidence. Wrong goal: aggregate gate obligations limited the benefit.",
      "removalBalance": "The original line predicts larger removals, but WO-132's supplied diagnosis records continuing aggregate misses. Its replacement removes this machinery instead of requiring further investment.",
      "failureBehavior": "Malformed or unusable cache records are ignored and execution runs fresh. This is a valid old-behavior fallback; the cache is subsequently retired by WO-132.",
      "findings": []
    },
    {
      "workOrderId": "WO-130",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Historical suite-reuse support. NoOp reruns package suites after unrelated source changes, including recorded skeleton runs of 89–162 seconds. WO-132 removes the replica mechanism.",
      "systemTraps": "Policy resistance: enforced declarations avoid ambiguous cache claims. Commons: copying installed roots consumes disk and time. Drift: measured setup cost matters. Escalation: replica failures can generate declaration repairs. Success to the successful: subsequent retirement avoids protecting sunk investment. Intervenor burden: intended reuse competes with replica diagnosis. Rule beating: fixtures test actual read isolation. Wrong goal: reuse is useful only if total work falls.",
      "removalBalance": "The line names a recurring removal, but does not establish its net size against replica overhead. Later supplied measurements motivated deletion.",
      "failureBehavior": "Replica failures explicitly refuse instead of using the candidate tree. That historical choice is removed by WO-132; this review does not reopen its settled replacement.",
      "findings": []
    },
    {
      "workOrderId": "WO-131",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Historical document-only and release-close gate optimization. NoOp was 243–419-second composed runs against a 41–47-second identical-tree baseline.",
      "systemTraps": "Policy resistance: environment declarations reduce session-dependent misses. Commons: narrowing aims to save execution. Drift: per-case bands prevent averaged claims. Escalation: extra declarations and probes add maintenance. Success to the successful: WO-132 retires the mechanism. Intervenor burden: printed handoffs address observed closeout rescue. Rule beating: reuse must follow matching declared inputs. Wrong goal: release completion, not cache-hit counts, is the outcome.",
      "removalBalance": "The declared removal is substantial, but the later supplied diagnosis records cold release execution and weak reuse. WO-132 supplies the current correction.",
      "failureBehavior": "Unavailable kernel sandboxing does not change reuse decisions. Replica and installed-copy failures still refuse under the historical design, subsequently removed by WO-132.",
      "findings": []
    },
    {
      "workOrderId": "WO-044",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate C2 supplies observed launch contracts for R and D. NoOp leaves target bundles and unattended launchers designed from assumptions; no measured NoOp cost is supplied.",
      "systemTraps": "Policy resistance: observations precede adapter guarantees. Commons: probes are bounded evidence work. Drift: unknowns remain labeled. Escalation: no runtime change is bundled with discovery. Success to the successful: both harnesses receive independent rows. Intervenor burden: reusable rows replace repeated investigation. Rule beating: actual hooks and containment are observed. Wrong goal: evidence enables concrete product consumers.",
      "removalBalance": "Unknown: the legacy line does not price discovery or avoided rework.",
      "failureBehavior": "Blocked, unavailable and ambiguous observations remain valid results. The order can close without inventing support or forcing settings changes.",
      "findings": []
    },
    {
      "workOrderId": "WO-067",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate R enables the resident and bounded unattended work. NoOp leaves presence policy uncompiled; supervision savings are not measured.",
      "systemTraps": "Policy resistance: phase authority can only narrow. Commons: phase budgets bound unattended resource use. Drift: progress requires verified success. Escalation: failures reset the curve. Success to the successful: policies remain optional graph data. Intervenor burden: return and decay are mechanized. Rule beating: activity alone cannot advance phases. Wrong goal: supports operator availability rather than perpetual agent utilization.",
      "removalBalance": "Unknown: the legacy Cost line supplies no comparison. The addition is a consumed product contract.",
      "failureBehavior": "Unavailable adapters emit NoOp. Invalid authority or policy shapes refuse. Programs without presence preserve their existing hashes and behavior.",
      "findings": []
    },
    {
      "workOrderId": "WO-045",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate C1 protects persisted and hook inputs before gate V touches retained work. NoOp keeps malformed-state trust and late failures; its cost is unmeasured.",
      "systemTraps": "Policy resistance: one typed boundary clarifies failures. Commons: decoding avoids later recovery work. Drift: existing logs remain accepted. Escalation: bounded malformed corpora avoid universal proof demands. Success to the successful: protocol shapes remain actor-independent. Intervenor burden: path diagnostics reduce diagnosis. Rule beating: valid decoding is not verification of behavior. Wrong goal: protects recoverable execution.",
      "removalBalance": "Not established: the legacy line claims no measured saving.",
      "failureBehavior": "Valid old inputs remain unchanged; malformed inputs intentionally refuse before execution. Falling back to unchecked casts would discard the useful safety function.",
      "findings": []
    },
    {
      "workOrderId": "WO-046",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate C1 makes persisted continuations executable before the end-to-end loop. NoOp retains deferred-kind failures after dispatch; no priced baseline is supplied.",
      "systemTraps": "Policy resistance: authoring and executable grammars become explicit. Commons: early rejection avoids wasted episodes. Drift: round trips preserve existing behavior. Escalation: no deferred evaluator is added. Success to the successful: types describe capability rather than vendor. Intervenor burden: path errors localize recovery. Rule beating: serialization must retain actual executable steps. Wrong goal: dependable continuation, not type coverage, is the outcome.",
      "removalBalance": "Unknown: legacy cost measurement is absent.",
      "failureBehavior": "Executable old continuations replay identically. Unsupported or malformed programs refuse early; they cannot safely degrade into an executable equivalent.",
      "findings": []
    },
    {
      "workOrderId": "WO-047",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate C1 enables WO-050's state slices and therefore R/D. NoOp leaves kernel replay coupled to application state keys; the cost is unmeasured.",
      "systemTraps": "Policy resistance: explicit projection removes implicit state conventions. Commons: one seam avoids duplicate replay implementations. Drift: complete Decisions remain equal. Escalation: optionality limits migration. Success to the successful: applications can choose state layouts. Intervenor burden: removes repeated key coordination. Rule beating: semantic replay comparisons exceed source-layout checks. Wrong goal: preserves one kernel across consumers.",
      "removalBalance": "No measured larger removal is declared; the architectural removal is implicit application coupling.",
      "failureBehavior": "Yes: omitting the projector preserves the documented default. An explicitly invalid non-finite projection refuses rather than silently changing replay.",
      "findings": []
    },
    {
      "workOrderId": "WO-048",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate C1 protects recovery stores before gate V. NoOp retains trusted disk shapes that can affect dispatch, locks and acceptance; no measured baseline is supplied.",
      "systemTraps": "Policy resistance: shared positive decoding clarifies host boundaries. Commons: avoids costly partial recovery. Drift: existing valid traces remain unchanged. Escalation: scoped inventories constrain the work. Success to the successful: existing storage contracts remain usable. Intervenor burden: errors name paths. Rule beating: byte-identical failure proves no partial acceptance. Wrong goal: recoverability rather than decoder count.",
      "removalBalance": "Unknown: the legacy Cost line supplies no addition/removal comparison.",
      "failureBehavior": "Malformed trusted state refuses without mutation. Valid stores preserve old behavior; executing malformed state is not a safe fallback.",
      "findings": []
    },
    {
      "workOrderId": "WO-050",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Prerequisite to gates R and D through the resident and source-change host. NoOp grows existing reactor branches instead of providing a bounded extension seam; cost is unmeasured.",
      "systemTraps": "Policy resistance: one decider avoids competing folds. Commons: shared selectors reduce duplicated knowledge. Drift: complete trace identity preserves behavior. Escalation: no package split or new host is introduced. Success to the successful: existing consumers remain supported. Intervenor burden: local slices simplify later changes. Rule beating: trace equality matters more than grep conformity. Wrong goal: prepares a named immediate consumer.",
      "removalBalance": "Not established quantitatively. The legacy line does not price reduced coupling or the refactor.",
      "failureBehavior": "No runtime fallback is added; the refactor must preserve existing behavior. Trace comparison and ordinary rollback provide the recovery route.",
      "findings": []
    },
    {
      "workOrderId": "WO-133",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Supports the product gates by making main usable after closeout. NoOp retains 344 advisory journal rows, remembered rebuilds and version-only machinery selections recorded in the order.",
      "systemTraps": "Policy resistance: stale runtime advice stops flooding. Commons: avoids repeated context and heavy suites. Drift: rebuilt pins preserve the working baseline. Escalation: no new check layer is introduced. Success to the successful: selection follows changed behavior rather than version literals. Intervenor burden: automatic rebuilding removes rescue. Rule beating: supplied attestation remains labeled as such. Wrong goal: usable sessions and faster review are concrete outcomes.",
      "removalBalance": "Yes as declared: a conditional 3.9-second build replaces repeated manual rebuilding, advisories and unnecessary machinery runs. Actual net results still require closeout observations.",
      "failureBehavior": "Runtime unavailability yields one advisory per session while the two retained invariants still refuse. Supplied model and effort remain claims, not fabricated readback.",
      "findings": []
    },
    {
      "workOrderId": "WO-068",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate R is the resident foundation for unattended proof, discovery, actors, status and composition. NoOp leaves those consumers without a host and retains the operator's predecessor cron practice.",
      "systemTraps": "Policy resistance: one lock and one event fold coordinate dispatch. Commons: cadences, idle expiration and budgets limit activity. Drift: replay and restart identity define dependability. Escalation: no separate scheduler service is required. Success to the successful: actor kinds share a catalog. Intervenor burden: recovery and return handling are automatic. Rule beating: dispatch identity prevents duplicate effects. Wrong goal: delivers the immediate work platform.",
      "removalBalance": "No existing DotLn removal is claimed. The Cost line honestly justifies a new product primitive by its consumers; practice-level savings are unmeasured.",
      "failureBehavior": "Unavailable actors emit NoOp; restarts recover without duplicate dispatch. Competing residents and unauthorized effects refuse to preserve authority and state.",
      "findings": []
    },
    {
      "workOrderId": "WO-049",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate D enables WO-052/053; gate S also consumes the bundle. NoOp leaves no governed target flow. The supplied row records 12,413,364 ms elapsed and three operator corrections, not a counterfactual saving.",
      "systemTraps": "Policy resistance: target and launchpad profiles have explicit boundaries. Commons: the measured machinery share is 0.3774; marginal fixture cost is not total lifecycle cost. Drift: emit/check/remove preserve target cleanliness. Escalation: existing settings are not merged speculatively. Success to the successful: both harnesses receive supported surfaces. Intervenor burden: host-managed installation removes manual setup. Rule beating: real hooks are exercised. Wrong goal: enables an actual external change.",
      "removalBalance": "No larger removal is claimed. About five seconds of fixtures is not comparable to the order's aggregate gate time; new capability is the justification.",
      "failureBehavior": "Target-runtime loss intentionally refuses because governed writing cannot retain its guarantee. Removal restores exactly the prior files; the Contributor profile remains unchanged.",
      "findings": []
    },
    {
      "workOrderId": "WO-051",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate D supplies the writer transport used by WO-052, WO-053 and WO-122. NoOp leaves inspection-only dispatch; no measured opportunity cost is supplied.",
      "systemTraps": "Policy resistance: separate validators avoid widening inspection. Commons: process doubles bound development cost. Drift: launch claims follow observed rows. Escalation: no wildcard shell permissions are added. Success to the successful: both transports implement one request contract. Intervenor burden: standard launch construction removes repeated setup. Rule beating: observed commit and denials remain distinct from worker claims. Wrong goal: enables bounded source change.",
      "removalBalance": "No: the line explicitly claims no existing removal and names a critical-path product addition with bounded fixtures.",
      "failureBehavior": "The existing inspection path remains byte-identical. An unsupported writer profile refuses construction instead of silently launching with weaker authority.",
      "findings": []
    },
    {
      "workOrderId": "WO-121",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate R and the gate V exit require presence independent of actor activity. NoOp permits autonomous tool activity to imply human return. The row records 1,451,292 tokens, but no comparative baseline.",
      "systemTraps": "Policy resistance: origin separates cancellation from liveness. Commons: heartbeat overhead needs measurement. Drift: actor activity cannot normalize false presence. Escalation: avoids process-table heuristics. Success to the successful: classification follows signals, not launcher prestige. Intervenor burden: prevents needless cancellation rescue. Rule beating: active tools do not prove human presence. Wrong goal: preserves foreground intent. Recorded machinery share 0.4843 does not establish causation.",
      "removalBalance": "Unknown: the legacy Cost line gives no promised removal. The aggregate execution row does not measure benefit.",
      "failureBehavior": "The design separates stalled actors from presence changes. Missing-signal behavior relies on the declared idle policy; it must not invent a human return. No general fallback is specified.",
      "findings": []
    },
    {
      "workOrderId": "WO-119",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate R enables portfolio derivation and the unattended hour. NoOp leaves the resident without evidence-backed work candidates. Its row records 5,587,715 ms elapsed, not avoided discovery time.",
      "systemTraps": "Policy resistance: declared conventions constrain discovery. Commons: target commands consume resources even in inspection. Drift: candidates require observable evidence. Escalation: discovery does not itself authorize repair. Success to the successful: no model ranking is privileged. Intervenor burden: removes repeated manual scanning. Rule beating: seeded candidates test meaningful observations. Wrong goal: useful bounded work, not candidate count; machinery share 0.4712 alone proves neither success nor failure.",
      "removalBalance": "Unknown: the legacy Cost line supplies no comparison.",
      "failureBehavior": "An observation failure must remain an unavailable observation rather than proof of no imperfections. The supplied criteria do not define a general error fallback; no observed failure is established.",
      "findings": []
    },
    {
      "workOrderId": "WO-052",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate D enables the first external change and verification spine E. NoOp leaves source-change episodes without durable recovery. The observed 44,949,279 ms elapsed is execution cost, not a NoOp baseline.",
      "systemTraps": "Policy resistance: one host owns dispatch and receipts. Commons: focused checks add bounded work. Drift: commits and host tests define success. Escalation: no remote delivery is added. Success to the successful: writer transports remain interchangeable. Intervenor burden: commit recovery removes duplicate-work rescue. Rule beating: a worker envelope alone cannot prove a commit. Wrong goal: actual target change; the 0.1021 machinery share is descriptive, not benefit proof.",
      "removalBalance": "Unknown: legacy cost declaration has no measured removal.",
      "failureBehavior": "A committed effect recovers by identity; a pre-commit interruption has bounded redispatch. No commit produces a typed refusal, rather than a fabricated success.",
      "findings": []
    },
    {
      "workOrderId": "WO-122",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate R supplies real workers and decision packets for gate V. NoOp leaves actor kinds unavailable and handoffs unstructured. The row records 13 steps and 21,494.836 ms across 81 hook runs, without a comparison.",
      "systemTraps": "Policy resistance: origin stamping preserves presence semantics. Commons: actor launches and hooks consume measurable resources. Drift: availability is explicit. Escalation: packets stop repeated prompting for the same decision. Success to the successful: catalog kinds share contracts. Intervenor burden: only genuine questions await humans. Rule beating: live envelopes distinguish launches from fixture claims. Wrong goal: progress under authority; low machinery share alone is not outcome evidence.",
      "removalBalance": "Unknown: no comparative removal is recorded in the legacy Cost line.",
      "failureBehavior": "Unavailable launch paths yield NoOp. Human handoff holds only the affected order until an answer arrives; that is an intentional authority boundary.",
      "findings": []
    },
    {
      "workOrderId": "WO-141",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "No gate in the supplied critical-path table lists WO-141; it is process work, and its Cost line names a mission outcome (fewer operator corrections of unmeasured claims) rather than a blocked gate. NoOp cost in the records, per the order's own Cost line: one correction on 2026-09-17 (a 'roughly 10 minutes ago' claim while the session held 14:06 and 14:32) and nine operator corrections in one 2026-09-16 session (defect register items 7 and 13). These are the subject's claims; no supplied observation id carries them. The supplied meter observation (docs/planning/cost-table.json#trap:shifting-the-burden-to-the-intervenor) already reports source session-journal with per-phase and per-unit maps and totals of 0 for WO-135, WO-139, WO-054, WO-055 and WO-137. Inference from that shape: the counter of criterion 3 has already landed. The order's status is not stated in the subject and its cost row is null (subject.costTable.rows[24]), so its own process cost is unknown.",
      "systemTraps": "Policy resistance: the hedge scan and the equipped accuracy rule pull against each other; the goal standard requires stating material uncertainty, while the scan journals a number only when an estimate marker accompanies it, so dropping the marker is the cheap way to a clean row (finding 1). Tragedy of the commons: the observed-facts block is printed at the briefing, at every prompt-submit and at Stop, listing every background task and every gate row, with no byte bound in criterion 1; hook cost fields (hookRuns, hookMs) are null in every supplied row, so the total is unaccounted (finding 3). Drift to low performance: replacing self-report with a journal derivation raises the standard; no drift seen. Escalation: low; measurement only, no consequence attached, no new receipt. Success to the successful: not material; no alternative excluded by prior investment is visible in the subject. Shifting the burden: per-unit attribution exists only when the operator or the agent names a unit, so a meaningful per-unit row depends on operator typing or agent self-report, which is what the order says it replaces. Rule beating: material, finding 1; an unhedged wrong number journals nothing. Seeking the wrong goal: a count named operatorCorrections that adds agent hedges to operator corrections measures a proxy under the operator's name (finding 2); every supplied total is 0 with empty unit maps, which cannot distinguish no corrections from no capture.",
      "removalBalance": "Named removal: recurring operator corrections of unmeasured claims (ten cited across two sessions) and a self-report derivation. Named addition: one facts block in three places, one scan under 100 ms, one counter, fixtures; no refusal, receipt, key or ritual. On the Cost line's own terms the removal recurs and the addition is mostly fixed, so the line does name a removal larger than the addition. Two qualifications: the removal is delivered only for hedged guesses (finding 1), and the block's recurring context bytes are not counted in the addition (finding 3). The order's own wall-clock, tokens and bytes are unknown (row null); unknown, not a hold.",
      "failureBehavior": "By construction the mechanism never holds a turn: criterion 2 requires the Stop hook to return without blocking and with no re-entry in every listed case, and criterion 1 prints unknown with a cause code for a missing counter. The supplied rows show stopRefusals 0 and guardRefusals 0 for all five metered orders, consistent with no hold, though those zeros do not isolate this hook. Two gaps: no criterion fixtures an unreadable or malformed transcript_path or a failed journal write, so fail-open under scan error is asserted in the objective but not pinned; and criterion 3 removes the decision-record derivation, so a missing journal degrades to unknown rather than to the old value. Unknown is the more honest degradation, but the series across the derivation change is not comparable and the meter's delta across it carries no meaning.",
      "findings": [
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "The scan is lexical: it journals a number or duration only when an estimate marker (roughly, about, approximately, around, probably, I think, should be, ~) is attached. A handoff that states an unmeasured or wrong number with no marker journals nothing, while an honest hedged estimate is journaled and counted. The cheapest way to a clean row is to delete the hedge, not the guess, which works against the goal standard's requirement to state material uncertainty. Criterion 2 fixtures only the marked cases and the exempt cases.",
          "evidence": null,
          "reopenWhen": "A post-landing handoff states a number or duration that disagrees with the journal's observed value for that quantity, carries no estimate marker, and no hedge row is journaled for it; or hedge rows fall to zero while a review or operator record still names an unmeasured claim in the same order."
        },
        {
          "criterionId": "criterion:3",
          "kind": "known-issue",
          "reason": "operatorCorrections is defined as typed operator corrections plus the agent's own journaled hedges (fixture: two corrections and one hedge yield 3), and the shifting-the-burden row reads the total as operator corrections, so the label no longer names what is counted. Separately, the supplied observation already shows source session-journal with per-phase and per-unit maps, and every total is 0 with empty maps across five orders and 22 recorded attempts; a measurement-only sensor with no liveness signal cannot distinguish 'no corrections occurred' from 'nothing was captured'. The subject records an operator direction during WO-054's resume: fix on 2026-09-18 while that order's count is 0; whether that direction was a correction is unknown.",
          "evidence": null,
          "reopenWhen": "Any order whose decisions file, review report or acceptance record names an operator correction while its cost-table row reports operatorCorrections 0; or a row whose nonzero total consists only of hedge rows and is presented as operator corrections."
        },
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "The observed-facts block lists every background task with dispatch time, state and elapsed, every gate row's duration, the usage counters and the clock, and is printed at the briefing, at every prompt-submit and at Stop. Criterion 1 sets no byte bound, so the always-on context it adds grows with session length. The Cost line bounds only the scan's latency. Hook cost fields are null in every supplied row, so the addition is unmeasured. The content is task state, which the one-paragraph story wants in front of the agent; the open point is the unbounded size, not the idea.",
          "evidence": null,
          "reopenWhen": "The block's bytes per prompt-submit, once recorded, grow with the number of background tasks or gate rows in a session; or a cold-start or context acceptance is raised citing this block; or hookMs, once populated, rises after this order."
        }
      ]
    },
    {
      "workOrderId": "WO-136",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Not a typed prerequisite of any order in the supplied critical-path table. Its declared consumer is a decision: which operating mode each harness may be offered in, taken at the planning checkpoint after close and confirmed at R2 (the table places R2 after WO-053 and WO-111), and the product 03 candidate for DotLn-owned authority. NoOp cost in the records, per its Cost line: the 2026-09-17 enforcement table keeps four unknown cells, one untested row and no sandbox-off row in either harness, so any sandbox-off or hook-mediated mode would be offered on unmeasured guarantees. No supplied observation id measures that cost; the cost row is null (subject.costTable.rows[25]), so the order's own process cost is unknown.",
      "systemTraps": "Policy resistance: low; the order changes no setting, hook or bundle, so it cannot fight an existing guard. Tragedy of the commons: material; it draws on the scarcest shared resource, operator attention, with a declared budget of one session of up to 120 minutes and up to forty approvals, plus forty launches; the budget is declared and recorded, which is the accounting the lens asks for. Drift to low performance: the matrix raises the evidence standard from inferred to host-observed cells. Escalation: the decision section may call for mediation orders; further process by design, bounded by the non-goal that the mode is decided at the checkpoint. Success to the successful: the three candidate modes are named without preferring the current one. Shifting the burden: the probe depends on operator presence for up to forty approvals; acceptable once, a problem if the matrix must be re-run per harness version. Rule beating: criteria 1 and 2 close the main route (no inferred cell, no worker-reported effect); the residual is generalizing a label from one command shape (finding 2). Seeking the wrong goal: the matrix is a proxy only if no mode decision follows it (finding 1).",
      "removalBalance": "The Cost line names a removal that is an unknown (unmeasured cells blocking a candidate and later claims), not a recurring measured cost, and an addition that is quantified and falls on the operator: up to 120 minutes, forty approvals, forty launches. Taken literally the line does not show a removal larger than the addition; the benefit is decision information whose value depends on a later mode that removes recurring prompts or stalls (row 10). No gate, hook or recurring step is added, so the addition is one-time. Own wall-clock and tokens unknown.",
      "failureBehavior": "Favourable. The order adds no gate, hook or recurring step and applies no configuration; the packet ends as ready, negative or inconclusive, and a negative or inconclusive outcome leaves today's mode and settings untouched, which is the old behaviour. Criterion 6 keeps live launches out of npm test. The residual risk is in the probe rather than in its failure: sandbox-off launches attempt the very effects a limit should stop, and criterion 4's claim that nothing outside the scratch roots is touched is host-checked only where a sentinel, listener or fixture remote exists.",
      "findings": [
        {
          "criterionId": "criterion:5",
          "kind": "known-issue",
          "reason": "The removal named is an unknown; the addition is a measured operator burden (up to 120 minutes and forty approvals). The order pays back only if the checkpoint uses the matrix to choose a mode that removes recurring prompts or stalls. If no decision follows, the order is receipt volume, which the goal standard says is not progress by itself.",
          "evidence": null,
          "reopenWhen": "The planning checkpoint after this order's close, or R2, records no mode decision citing the matrix; or the matrix has to be re-run for a new harness version at comparable operator cost."
        },
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "Each cell is labelled from the command shape it ran. 'prevented' for one shape does not establish prevention for the limit; the decision section then states per-harness guarantees for a mode from those cells. The non-goal bars claims beyond the labelled cells, but criterion 5's 'guarantees' wording invites exactly that step.",
          "evidence": null,
          "reopenWhen": "A later host-observed effect crosses a limit whose cell is labelled prevented, through a command shape the matrix did not run."
        }
      ]
    },
    {
      "workOrderId": "WO-135",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Not listed at any gate. It serves the flow of capability-introducing orders through gates R and D: the Cost line records that WO-068 (R) and WO-052 (D) each paid a purpose-built planning pass, a refutation dispatch (546 s observed on 2026-09-17) and a final-review diagnosis after final review, twice in two days, and that a hand-run 40-line script found two dependency violations an earlier pass and its receipt missed. NoOp: every later capability-introducing order pays the same again and sequence violations stay invisible. Its own process cost is observed (docs/planning/cost-table.json#row:WO-135): 7,699,647 ms elapsed, 3 attempts, 1,828,895 ms of gates, machinery share 0.577, 73 gate steps, no corrections or refusals.",
      "systemTraps": "Policy resistance: part (c) adds a hard refusal on planning branches; a planning pass that legitimately has to touch a non-document path now routes through operator override:, and planning sessions are not metered rows, so the policy-resistance signal cannot see these refusals (finding 1). Tragedy of the commons: the order removes a 546 s dispatch per recurrence; its own 58 percent machinery share consumed shared gate time (finding 3). Drift: the topology check raises the standard from a hand script to a checked rule. Escalation: it removes a pass rather than adding one; the third refusal raised the hook-boundary count that three documents must restate, a documentation tax WO-142 later pays (two or three against four). Success to the successful: not material. Shifting the burden: removes a post-review planning chore; adds an override route that needs the operator. Rule beating: a dated section is admitted whenever its heading names an order in the judged subject, new capability ids included; the level claim is deferred to verification, final review and the next receipt, so a level claim can sit in the table unjudged between a final review and the next planning receipt. Seeking the wrong goal: criterion 3's fixture pins a historical parse (fourteen pairs, one serial run) and blank-line semantics rather than the dependency truth itself (finding 2).",
      "removalBalance": "The Cost line names a recurring removal (a planning pass, a 546 s refutation and a diagnosis per capability-introducing order, plus the hand-run dependency script) against a fixed addition (one helper branch, one check in two commands, one write-time rule, fixtures). The addition is now observed at 7,699,647 ms elapsed, of which 0.577 was machinery. Only the 546 s dispatch of the removal is measured; on that figure alone break-even needs about fourteen recurrences, fewer if the planning pass and diagnosis were measured. The line does name a removal larger than its addition over the remaining horizon, but its size is unmeasured.",
      "failureBehavior": "Mixed. Part (a) widens admission and falls back to the old refusal messages (criterion 2), which is the old behaviour. Parts (b) and (c) are refusals by design: plan check and the index check refuse a sequence with a violated blocking edge, and the generated hook refuses a non-document repository write on a planning/ branch. The subject does not say what the hook does when the branch cannot be determined (detached HEAD, Git unavailable) or a path cannot be resolved against the repository root; whether that case admits or refuses is unknown. Paths outside the repository root stay admitted and operator override: is the named recovery. The five metered rows show guardRefusals 0, but they are work-order sessions, not planning sessions, so they do not observe this rule.",
      "findings": [
        {
          "criterionId": "criterion:4",
          "kind": "known-issue",
          "reason": "The planning-branch rule is a hard refusal whose behaviour under an undetermined branch or unresolvable path is unspecified, and whose firings are invisible to the supplied meter because planning sessions have no cost-table row. A planning pass that must regenerate a surface outside docs/ and root Markdown is pushed to operator override:, which is operator rescue.",
          "evidence": null,
          "reopenWhen": "A planning pass records an operator override: taken to complete a write the pass itself required, or a refusal on a planning/ branch for a path the planning commands generate; or a hook error with an undetermined branch is observed to refuse."
        },
        {
          "criterionId": "criterion:3",
          "kind": "known-issue",
          "reason": "Concurrency semantics live in the blank lines of a Markdown sequence (two-entry pairs against groups of three or more, which are checked for order only), and a fixture pins the committed 2026-09-17 sequence as fourteen pairs and one serial run. A formatter or an edit that adds or drops a blank line changes which edges are checked without changing any dependency, and a group of three escapes the pair rule entirely.",
          "evidence": null,
          "reopenWhen": "A sequence edit that changes only whitespace flips plan check between pass and fail; or a blocking edge inside a group of three or more entries is found that the check admitted."
        },
        {
          "criterionId": "criterion:6",
          "kind": "known-issue",
          "reason": "Observed process cost for a process-correction order: machinery share 0.577 of 7,699,647 ms, 3 attempts, 1,828,895 ms in gates. More than half of the order's elapsed time went to machinery. The meter reports worsening [] and reopenCandidate false and no threshold is supplied, so this is recorded cost, not a failure.",
          "evidence": null,
          "reopenWhen": "The seeking-the-wrong-goal trap sets reopenCandidate true, or the next two process-order rows again record machineryShare above 0.5."
        }
      ]
    },
    {
      "workOrderId": "WO-053",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate D, the end of the external-change chain (WO-042, WO-044, WO-049, WO-052, WO-053). It unblocks R2, WO-111 (gate R), WO-112 (gate V) and, through the dated planning-deferral, the deferred documentation set that includes WO-084 and WO-090. NoOp: no worker has committed in a repository outside DotLn, the first external proof is absent and everything behind gate D waits. The capability list carries worker.source-change at '1 - demonstrable, live-evidenced'; inference: criterion 5's write-back has landed, but the order's status is not stated in the subject. Process cost: the Cost line declares the legacy figure unavailable and asks this refutation to judge the gap; the cost-table row is still null (subject.costTable.rows[27]). Judgment: unknown, and unknown is not a hold.",
      "systemTraps": "Policy resistance: the 2026-09-17 operator override folds bounded writer runtime repairs into a proof order; the guards it crosses are suspended by the operator's own authority, not by the plan. Tragedy of the commons: a smoke per installed harness, one live episode and one kill episode consume model spend that no supplied row measures. Drift: every claim must be labelled observed, launch-claim or unknown, which holds the standard. Escalation and success to the successful: not material. Shifting the burden: the receipt is filed from an outside terminal, an operator step, acceptable for a first proof and not recurring. Rule beating: the target is a scratch repository with one wrong function and one failing test, so the evidence can pass on the easiest possible change; host-run red and green, the unchanged sentinel tree and envelope-only transcript growth close the narrative route. The live episode is required for at least one harness only (finding 2). Seeking the wrong goal: this is the product outcome the critical path names, not a proxy.",
      "removalBalance": "No removal is claimed and none is measured; the Cost line says so. The addition is a live episode, a kill episode, a receipt-shape fixture, write-backs and, by override, runtime repairs with regression tests. For a critical-path proof the balance question is answered by the blocked outcome rather than by a removal. The order's cost row is null while five later-versioned orders have rows; why is unknown.",
      "failureBehavior": "A failed worker episode produces a receipt with labelled claims and no commit in the target; the main checkout hash and the sentinel tree are host-checked; the kill episode recovers by commit identity with exactly one commit above base. Failure leaves the old state (no external change) and refuses nothing the operator does. Criterion 6 requires the inspection profile to keep its pinned behaviour, so the older path is preserved.",
      "findings": [
        {
          "criterionId": "criterion:6",
          "kind": "known-issue",
          "reason": "The Cost line hands this refutation the missing cost evidence to judge. It is still missing: the cost-table row for WO-053 has metrics null while WO-135, WO-139, WO-054, WO-137 and WO-055 have rows. The cost of the first external change, the critical path's headline proof, is therefore unknown. Unknown cost is not a hold.",
          "evidence": null,
          "reopenWhen": "The cost table gains a WO-053 row: compare its elapsedMs, attempts and machineryShare with the WO-054 and WO-055 rows and reopen if machinery share is above 0.5; or the next receipt's cost table still carries a null row for it."
        },
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "Every installed harness gets a writer-profile smoke, but the full clean episode is required for at least one harness only. The core bet treats Claude, Codex, a human and a script as interchangeable occupants of one actor slot; the first external proof leaves that unevidenced for the second harness.",
          "evidence": null,
          "reopenWhen": "The second installed harness's clean episode is attempted and fails, or WO-112 or R2 is reached with the live episode still recorded for one harness only."
        }
      ]
    },
    {
      "workOrderId": "WO-139",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Not listed at any gate. It protects a shared resource rather than unblocking an order: the Cost line records an operator-observed fan-out on 2026-09-17 from a five-agent guideline to more than a hundred (5 + 15 + 45 before any repair loop) and the operator's manual arithmetic. NoOp: fan-out stays uncapped on every path and the operator keeps counting. Own process cost is observed (docs/planning/cost-table.json#row:WO-139): 11,431,171 ms elapsed, 6 attempts, 5,178,587 ms of gates (the largest gate time of the five rows), machinery share 0.501, 76 gate steps (the series maximum, +3), no corrections or refusals.",
      "systemTraps": "Policy resistance: the refusal names the count, the cap and the key, and the key lives in docs/control/budgets.json; nothing in the supplied criteria protects that key from the refused session, and the subject's own planning-branch rule admits docs/ writes (finding 1). Tragedy of the commons: this is the order's purpose and it is partly delivered; agents created before any hook fires are counted late or not at all, which the order states openly. Its batching rule went into every role's shared instruction and pushed the verifier cold start to 17,477 bytes against 16,384; the ceiling was raised (finding 3). Drift: that raise is the drift pattern, a standard relaxed to fit the addition, and the drift trap cannot see it because coldStartBytes is null in every row. Escalation: a cumulative per-session count with no window means a long legitimate session meets the cap and needs a key change or an override (finding 2). Success to the successful: not material. Shifting the burden: removes manual arithmetic; adds a possible rescue at the cap. Rule beating: per-item trees can move to paths the hook cannot attribute; the Stop advisory reports an uncounted remainder, a reported minimum rather than a bound. Seeking the wrong goal: spawn count is a proxy for compute; no token or cost field is populated in any row.",
      "removalBalance": "Removal: uncapped fan-out on admitted paths (one observed incident above a hundred agents) and manual arithmetic. Addition: a counter, a key, a role-text rule, a probe row, fixtures and one hard refusal. The observed addition is 11,431,171 ms with six attempts and about 86 minutes of gates, plus a recurring context cost the Cost line lists only as 'one role-text rule': the verifier ceiling breach and raise in subject.costTable.acceptances[6]. One avoided hundred-agent fan-out plausibly outweighs this, but no supplied row measures tokens or cost, so the size of the removal is unknown. The line names a removal larger than its addition and undercounts the addition.",
      "failureBehavior": "Favourable on the counter: an unreadable or missing counter admits the spawn with an advisory naming the cause (criterion 2 fixtures it), null disables the cap, and Codex is advisory only. That is degradation to the old behaviour. The refusal itself is a hard stop by design at cap plus one. guardRefusals is 0 for WO-139 and the three later rows, so no refusal is recorded in metered work so far; whether guardRefusals counts cap refusals is not stated in the subject.",
      "findings": [
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "The refusal message names the key, and the key is a field in a docs/ file. No criterion requires the key to be protected from the session it limits, so a refused session can raise subagentCap or set it to null and retry. The cap then binds only sessions that choose to be bound.",
          "evidence": null,
          "reopenWhen": "A session's diff or journal shows subagentCap changed by an agent session after a cap refusal in the same order, or a guardRefusals count above zero followed by a budgets.json edit with no recorded operator decision."
        },
        {
          "criterionId": "criterion:3",
          "kind": "known-issue",
          "reason": "The count is cumulative per root session with no window or reset. The supplied rows show orders running 5,288,212 to 24,848,112 ms with 3 to 7 attempts; a long session that stays inside the batching rule can still reach the twenty-first spawn and be refused mid-order, which hands the operator a key change or an override to finish authorized work.",
          "evidence": null,
          "reopenWhen": "A cost-table row records a cap refusal in an order whose fan-out was planned within the batching rule, or an operator override: or key change is recorded to finish authorized work after the twentieth spawn."
        },
        {
          "criterionId": "criterion:5",
          "kind": "known-issue",
          "reason": "The batching rule was added to the Contributor's shared instruction, which every role loads. The cost table records the result: WO-139 measured 17,477 bytes in both generated roots against the 16,384 verifier ceiling, and the ceiling was raised one 4 KB step under standing operator direction. An always-on prose rule beside a typed guard runs against the direction of the one-paragraph story (mechanisms attached only where relevant). The operator's acceptance stands; the finding is that the Cost line did not name this cost and the drift meter cannot see it (coldStartBytes null for every row).",
          "evidence": null,
          "reopenWhen": "Another cold-start acceptance is raised citing a shared instruction rule, or coldStartBytes becomes populated and shows any role above its ceiling as it stood before 2026-09-17."
        }
      ]
    },
    {
      "workOrderId": "WO-054",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate E (WO-054, WO-055, WO-056); prerequisite WO-052. In the typed graph it also blocks WO-100 and WO-111 (gate R) and WO-124 (gate G), and through WO-056 the loop proof WO-112 (gate V). NoOp: verification stays on fixtures, no independently verified source-to-deliverable loop exists and the critical path's named target cannot be met. The Cost line declares the legacy cost unavailable and asks this refutation to judge it; the cost is now observed (docs/planning/cost-table.json#row:WO-054): 24,848,112 ms elapsed, 7 attempts, 2,807,030 ms of gates, machinery share 0.240, 73 gate steps, no corrections or refusals. It is the most expensive of the five rows and has the lowest machinery share.",
      "systemTraps": "Policy resistance: the capsule refuses any worker-envelope field by path while the diff, commit text and snapshot are implementer-authored, so blinding by field and openness by content pull against each other (finding 1). Tragedy of the commons: about 6.9 hours and seven attempts of shared session and gate time; tokens and cost are null. During this order a shared advisory-boundary and Codex compaction-continuation rule entered CLAUDE.md and the role skill, the executor and release-close cold starts breached their ceilings and both ceilings were raised (finding 2). Drift: that is the drift pattern; the drift trap shows coldStartBytes null and cannot see it. Escalation: host-run witnesses add a test run before every verifier episode, a recurring step that replaces trust in the implementer's claim, which is the point. Success to the successful: the existing profile is preserved unchanged (criterion 4), not displaced. Shifting the burden: removes the operator's checking of unsupported completion claims, the mission's named babysitting cost. Rule beating: criterion 3 is proven with a double verifier, which shows the fold keeps a negative result negative, not that a verifier finds the defect; the live proof is WO-056's. Seeking the wrong goal: on the named product outcome.",
      "removalBalance": "No removal is claimed. Judged now that a row exists: the addition is 24,848,112 ms, seven attempts and a recurring host test run per verification, plus role-text growth that was no part of the stated objective. The benefit is a critical-path capability rather than a removal, and the mission-level removal (operator checking of completion claims) is not measured in any supplied row. Unknown size, not a hold.",
      "failureBehavior": "Integrity failures refuse by design (an injected envelope field refuses with the path; a snapshot tampered after sealing is refused by the input hash), which is correct for a verifier input. The old behaviour survives beside the new: every WO-010 fixture passes unchanged under the existing profile. What happens when the host cannot run the named tests in a confined checkout (confinement unavailable, a test that needs the outside) is not stated; the fold's rule that no implementer event relabels a negative result points in the safe direction, but it is not a criterion.",
      "findings": [
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "Blinding is by field: the capsule excludes the worker envelope, transcript and narrative. The diff, the commit message and the snapshot are written by the implementer and can carry narrative (a comment or document asserting the change is verified). The title's 'never sees the implementer's narrative' is stronger than what criterion 1 tests.",
          "evidence": null,
          "reopenWhen": "A live verifier episode (WO-056 or later) cites implementer-authored comment, commit-message or document text from the diff or snapshot as support for a passing judgment."
        },
        {
          "criterionId": "criterion:6",
          "kind": "known-issue",
          "reason": "The cost evidence the Cost line asked this refutation to judge now exists: 24,848,112 ms and 7 attempts. In the same order, shared rules were added to CLAUDE.md and the role skill, VER-002 N1 measured 20,849 against 20,480 (executor) and 12,437 against 12,288 (release-close), and both ceilings were raised with efficiency work left 'for a later pass'. That growth is outside the objective and criteria supplied for WO-054, and no order in this pass owns the later pass.",
          "evidence": null,
          "reopenWhen": "A further acceptance raises the executor ceiling above 24,576 or the release-close ceiling above 16,384, or the next runtime order's row again records seven or more attempts."
        }
      ]
    },
    {
      "workOrderId": "WO-137",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Feeds gate R's WO-110 (local-model transport) and WO-138; WO-110 has no typed prerequisite in the table, so WO-137 is risk reduction whose blocked outcome is named: whether WO-110 is written from a live row. NoOp in the records, per the Cost line: WO-110 and the product 06 candidate held since 2026-09-03 because WO-027 recorded n=0 after the only permitted launch crashed the service, plus a repeated setup explanation. Own cost is observed (docs/planning/cost-table.json#row:WO-137): 5,875,968 ms elapsed, 3 attempts, 2,716,747 ms of gates, fast gate 388,833 ms, machinery share 0.377, 66 gate steps, no corrections or refusals.",
      "systemTraps": "Policy resistance: none; no setting, hook or package change. Tragedy of the commons: up to 90 minutes of operator time, a 10 GB download cap without a decision, and live runs kept away from running product gates, all declared; yet 2,716,747 ms of gates were spent on an order that changes nothing under packages/ (finding 2). Drift: client-recorded provenance holds the standard. Escalation: none added. Success to the successful: the order probes the installed runner only and makes another runner an operator decision; a fair bound for a readiness question, though a negative result says nothing about local inference in general. Shifting the burden: it records the exact setup steps so the explanation stops recurring. Rule beating: criterion 2 lets ready stand on 'byte-identical outputs or the recorded diff', so the determinism the question asks for is not required for ready (finding 1). Seeking the wrong goal: aligned with the local-first, model-agnostic purpose; no capability-level claim is made.",
      "removalBalance": "Removal: an unknown open since 2026-09-03 that holds a gate R order and a product candidate, and a repeated explanation. Addition: one probe client with double-endpoint tests, one packet, a bounded operator session; no gate, hook or recurring step. Observed addition: 5,875,968 ms, of which 2,716,747 ms were gates. The removal is a one-time unknown rather than a measured recurring cost, so 'larger' cannot be shown numerically; the addition is bounded and one-time.",
      "failureBehavior": "Favourable and explicit: negative or inconclusive yields a failure artifact naming the attempted path, error shapes, ruled-out causes, remaining blocker and reopening condition, and the order closes without marking readiness, so WO-110 keeps waiting exactly as before. The live smoke never runs inside npm test, so a dead runner cannot fail the product gate.",
      "findings": [
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "The objective asks whether the runner gives deterministic output at temperature 0 and a fixed seed across three repeats. Criterion 2 accepts ready with either byte-identical outputs or a recorded diff, so ready does not mean deterministic. A consumer reading only the outcome would take more than was shown.",
          "evidence": null,
          "reopenWhen": "WO-137's packet records ready with a non-empty determinism diff and WO-110 or WO-138 then relies on repeat-identical local output."
        },
        {
          "criterionId": "criterion:4",
          "kind": "known-issue",
          "reason": "The order declares no change under packages/ and keeps its live smoke out of npm test, yet its row records 2,716,747 ms of gates over 3 attempts, more than the runtime order WO-055 (2,115,681 ms). For a probe and documentation order that is gate time with little to guard. No threshold is supplied, so it is recorded cost, not a failure.",
          "evidence": null,
          "reopenWhen": "The next order that declares no change under packages/ records gateMs at or above a runtime order's (WO-055: 2,115,681 ms)."
        }
      ]
    },
    {
      "workOrderId": "WO-055",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate E; prerequisites WO-054, WO-052 and WO-042. It blocks WO-056 (gate E), WO-066 (gate H) and, through WO-056, the loop proof WO-112 (gate V). NoOp: a negative verification ends in operator rescue, which is the recovery burden the mission names. The Cost line declares the legacy cost unavailable and asks this refutation to judge it; it is now observed (docs/planning/cost-table.json#row:WO-055): 5,288,212 ms elapsed, 3 attempts, 2,115,681 ms of gates, fast gate 1,070,798 ms, machinery share 0.582 (the highest of the five rows), 72 gate steps, no corrections or refusals.",
      "systemTraps": "Policy resistance: the derivation is bounded by the WO-042 envelope, so the repair loop and the authority floor reinforce rather than undo each other. Tragedy of the commons: each round is a fresh worker episode plus a re-verification; the round limit bounds it. Drift: re-verification always runs from the original contract, which stops the contract eroding round by round. Escalation: bounded by RepairExhausted. Success to the successful: not material. Shifting the burden: removes routine rescue and keeps NeedsHuman for out-of-authority findings, which is the right point for judgment. Rule beating: material; the objective puts 'the tests it cites' inside the repair order's surfaces while criterion 1 equates surfaces with referenced files and tests with reproduction commands, so whether a repair worker may edit the cited test is ambiguous (finding 1). Criteria 2 to 4 are proven with doubles, so they show the continuation's control flow, not repair quality. Seeking the wrong goal: on the product outcome; its own process ran at 0.582 machinery share (finding 2).",
      "removalBalance": "No removal is claimed. Observed addition: 5,288,212 ms, three attempts, more than half of it machinery. The benefit is a critical-path capability; the mission-level removal (operator rescue after a failed verification) is not measured in any supplied row. Unknown size, not a hold.",
      "failureBehavior": "Favourable. A finding outside the original surfaces or commands yields NeedsHuman naming it, derives no order, dispatches nothing and leaves the persisted envelope byte-identical; the round limit ends in a terminal RepairExhausted naming the last finding; a kill between repair commit and re-verification resumes from the persisted program without a second dispatch. Each failure hands back to a human, which is the old behaviour, and none widens authority.",
      "findings": [
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "The objective defines the repair order's surfaces as the files the finding names plus the tests it cites; criterion 1 says surfaces equal the referenced files and tests equal the reproduction commands. If a cited test file is a writable surface, a repair worker can turn re-verification green by weakening the test, and criterion 2's path assertion would pass because the test is a declared surface. Re-verification from the original contract is the defence, and the subject does not show it checks the test against the contract.",
          "evidence": null,
          "reopenWhen": "A derived repair order's diff modifies a test file the finding cites and the re-verification from the original contract records green."
        },
        {
          "criterionId": "criterion:6",
          "kind": "known-issue",
          "reason": "Observed machinery share 0.582 of 5,288,212 ms, the highest of the five rows and a rise of 0.342 over WO-054, yet the trap reports worsening [] and reopenCandidate false. More than half of a critical-path order's elapsed time went to machinery, and the meter's own flag did not move. No threshold is supplied, so this is recorded cost, not a failure.",
          "evidence": null,
          "reopenWhen": "The seeking-the-wrong-goal trap sets reopenCandidate true, or the next gate E row (WO-056) again records machineryShare above 0.5."
        }
      ]
    },
    {
      "workOrderId": "WO-142",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate: none. The supplied critical-path table (gates A, B, C2, C1, R, D, U, S, E, F, G, H, V, P, deferred) does not list WO-142, and the order names no blocked critical-path outcome although the goal standard requires prerequisite or risk-reduction work to name one. It sits in the sequence between WO-055 (gate E) and WO-084 (deferred). Its claimed contribution is indirect: lower planning, refutation and review overhead for every later order (the showrunner and devops questions), not a product gate. NoOp cost: the Cost line carries dated figures measured at 3b3533f8 on 2026-09-19: the follow-up feed grew 359 to 399 in two days with 235 of 399 entries being decision records and three passes selecting nothing; eight orders re-judged on 2026-09-19 for a version stamp; one paid refutation lost on 2026-09-17; the first test:docs run failing after every verify or final-review dispatch (four reports); live-gate refusal of read-only commands (nine reports; seventeen reviewer gates of 303 to 1,178 s, median 793 s); two full gates lost to a cascade message; 176 MB of residue. None of these figures appears in the supplied observation set: the cost-table rows and the five trap series carry no feed size, re-judge count, lost-result count or residue size, and guardRefusals reads 0 or null where the order reports nine refusals. The NoOp cost is therefore asserted by the order with dates and a commit and is not verifiable from the supplied records; it is treated as claimed, not observed. What the records do hold is the order's own run: elapsedMs 20,615,865, attempts 9, gateMs 3,130,429, machineryShare 0.53, coldStartBytes 21,176, gateStepCount 79, operatorCorrections 0, and a PR body of 8,650 bytes.",
      "systemTraps": "Applied to the order's own process cost, using row WO-142 where a value exists. Policy resistance: the order relaxes one guard (live-gate refusal of read-only commands) while keeping the rest; the relaxation needs a read-only classifier, and a misclassified write reopens the race the refusal exists for. guardRefusals is null for this order, so the meter can show neither the old cost nor the new behavior. Tragedy of the commons: one order bundles every unowned defect and nomination since WO-003; the record shows the largest elapsed time (20.6M ms) and attempt count (9) of the five measured rows and 3.13M ms of gate time; tokens and costUsd are null, so model spend is unknown. Drift to low performance: criterion 1 tolerates up to six returned rows and any number of not-reproduced rows; row B17 adds always-on role text to three roles while the supplied acceptances show five 4 KB cold-start ceiling raises on 2026-09-17 and 2026-09-18; the row's coldStartBytes 21,176 is above the 20,480 that was the executor ceiling until 2026-09-18. Escalation: the Cost line counts two new assertions, but criterion 1 requires a named check per fixed row, and gateStepCount reads 79 here against 66 before and 72 and 74 after; the meaning of that metric is not defined in the subject, so this is a question, not a failure. Success to the successful: three passes selected nothing from the follow-up feed, which is evidence the feed has no demonstrated consumer; the order repairs it and adds a writer for it rather than testing its retirement. Shifting the burden: harness prune is on demand and operator-run, and criterion 5 needs an operator run on the real checkout, so residue control depends on the operator remembering; operatorCorrections is 0 for the recorded run. Rule beating: reverted-subject proof is once per row group, not per row; a not-reproduced state closes a logged defect with an observation only; the register count reaches zero through the planning pass's settlement rather than through this order's mechanism; readAmplification, stopRefusals and reAnnouncements are null. Seeking the wrong goal: machineryShare 0.53 on an order that is itself machinery, with no critical-path gate; the success measures are counts (untriaged entries, MB, report counts), and no criterion measures a later planning pass or reviewer gate getting cheaper, except the before and after observations criterion 6 asks the decisions file to hold. All five supplied trap series report worsening [] and reopenCandidate false, largely because their indicator values are null.",
      "removalBalance": "On recurring cost, yes as written: the Cost line adds one on-demand command, one equality assertion, one anchor-resolution assertion, one feedback edition and one role sentence for three roles, and names seven recurring costs removed with dated magnitudes. Three qualifications. First, the removals are in mixed units (entries, reports, seconds, MB) and are absent from the supplied observations, so larger is the order's claim, not a recorded comparison. Second, the Cost line says the order's own wall-clock, tokens and context bytes are unknown until run, yet the supplied row records the run: 20,615,865 ms elapsed over 9 attempts with 3,130,429 ms of gate time, the most expensive of the five measured rows; the Cost line is stale against the record, and the one-time cost is not set against any removal expressed in time per order, so payback is unknown. tokens and costUsd are null: unknown, not zero. Third, the additions are undercounted relative to criterion 1 if a fixed row's named check is new rather than existing; the subject does not say which.",
      "failureBehavior": "Mixed. Carried set: if the version-parenthetical normalization fails to match, the order is evicted and re-judged, which is the old behavior; if it matches too much, a materially changed order is carried and skips judgment, which is not the old behavior, and criterion 3 proves only the first direction. Register: the old failure was over-harvest; a filter that does not recognize a real follow-up produces no pending row, a silent loss that is worse than the old behavior, and criterion 2 covers fixture shapes only. Hooks: a read-only classifier that fails closed returns the old refusal, which is acceptable; one that fails open admits a write during a live gate; criterion 4 shows one live-gate write still refused, and names four refusals where the supplied sequence (WO-144) and WO-146 criterion 12 indicate five. The new assertions in release check-surfaces and meta --check are refusals by construction: a defect in the anchor resolver fails a check that passed before. harness prune without --apply deletes nothing, which is the old behavior; with --apply it is irreversible, and no criterion makes it refuse a path that a live writer or a recovery record still reads; the statement that nothing writes .git/dotln/suite-success since WO-132 is asserted in the Cost line, not checked by a criterion. The lost-refutation-result repair is covered only through the generic rows file, so its failure behavior is unknown from the subject.",
      "findings": [
        {
          "criterionId": "criterion:6",
          "kind": "known-issue",
          "reason": "The Cost line says the order's own wall-clock, tokens and context bytes are unknown until run, but the supplied row records a run with a PR body: elapsedMs 20,615,865, attempts 9 (the highest of the five measured rows), gateMs 3,130,429, machineryShare 0.53. The Cost line is stale against the record, and none of the seven named removals is expressed in a unit the row carries, so whether the one-time cost is repaid cannot be read from the records. tokens and costUsd are null: unknown.",
          "evidence": null,
          "reopenWhen": "The decisions file's before and after observations (criterion 6) omit an after-value for any of the seven named removals, or the next two measured orders show no fall in attempts or gateMs against the rows before WO-142."
        },
        {
          "criterionId": "criterion:4",
          "kind": "known-issue",
          "reason": "The removal named as live-gate refusal of read-only commands (nine reports) is invisible to the meter: guardRefusals is 0 for WO-137, WO-084 and WO-143 and null for WO-142 and WO-144. The NoOp cost is not in the supplied series and the after-state cannot be shown by it. The relaxation also introduces a read-only classification at the hook; criterion 4 proves one live-gate write is still refused, not that the classification is complete.",
          "evidence": null,
          "reopenWhen": "A command that changes a gate input is admitted as read-only during a live gate, or guardRefusals stays null for the next measured order so the removal remains unmeasurable."
        },
        {
          "criterionId": "criterion:4",
          "kind": "known-issue",
          "reason": "Criterion 4's still-refused list names four refusals (second writer, live-gate write, planning-branch source write, cap breach), and the objective speaks of four hook refusals. The supplied sequence has WO-144 adding a refusal of outside-project writes, and WO-146 criterion 12 refers to a five-refusals paragraph. A hook set regenerated under this order on a tree that contains the fifth refusal passes criterion 4 without showing it still refuses, and the document correction to four would itself be stale.",
          "evidence": null,
          "reopenWhen": "The hook set is regenerated under WO-142 on a tree that contains WO-144's refusal and the still-refused fixture list or the corrected documents name four."
        },
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "The evidence can pass without the intended behavior: reverted-subject proof is required once per row group, not per row, so a fixed row inside a group may have a named check that does not fail on its own revert; a not-reproduced state closes a logged defect with a pre-edit observation only and has no cap; up to six rows may be returned.",
          "evidence": null,
          "reopenWhen": "A row recorded as fixed or not-reproduced in docs/evidence/WO-142/rows.md is reported again by a later verification or final review."
        },
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "The untriaged count goes from 364 to zero through the planning pass's settlement, not through this order's mechanism, so the count cannot show the mechanism works. The fixture covers a decision with and without a follow-up in fixture shape only; a real decision whose follow-up is not recognized creates no pending row, a silent loss that is worse than the old over-harvest. Three passes selecting nothing from the feed is evidence it has no demonstrated consumer, yet the order adds always-on role text (row B17) so the feed has a writer instead of testing its retirement.",
          "evidence": null,
          "reopenWhen": "Two planning passes after merge select nothing from the feed, or a decision record that carries a follow-up is found absent from npm run plan -- followups."
        },
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "Row B17 adds a sentence to the executor, verifier and reviewer role text against headroom of 3,727, 2,589 and 1,371 bytes. The supplied acceptances record five 4 KB cold-start ceiling raises on 2026-09-17 and 2026-09-18, and this order's recorded coldStartBytes 21,176 (dispatch not stated) is above the 20,480 that was the executor ceiling until 2026-09-18. The standard is being moved to fit the text; the trap series has a single value and cannot flag it.",
          "evidence": null,
          "reopenWhen": "Another coldStartBytes acceptance is recorded, or the next recorded coldStartBytes value exceeds 21,176."
        },
        {
          "criterionId": "criterion:3",
          "kind": "known-issue",
          "reason": "Criterion 3 proves one direction: an order with only a version parenthetical changed is carried and no previously carried order is evicted. It does not prove the converse, that an order whose title outside the parenthetical or whose criteria changed is still evicted. Separately, WO-142 is in this pass's judged set although the cost table records its run; whether that reflects a substantive edit or the eviction this criterion removes is unknown from the subject.",
          "evidence": null,
          "reopenWhen": "npm run plan -- refute lists as carried an order whose title text outside the version parenthetical, or whose criteria, changed since its latest receipt; or the pass diff shows WO-142's only change was a version stamp."
        },
        {
          "criterionId": "criterion:5",
          "kind": "known-issue",
          "reason": "harness prune is on demand and operator-run, so residue control depends on the operator remembering; with 27 snapshots against a candidate threshold of twenty, regrowth is the expected state. With --apply the deletion is irreversible, and no criterion makes prune refuse a path that a live writer or recovery record still reads; that nothing writes .git/dotln/suite-success since WO-132 is asserted, not checked.",
          "evidence": null,
          "reopenWhen": "Residue again exceeds the named threshold with no prune run recorded, or a path that prune lists is later found to be read by a gate or a recovery command."
        },
        {
          "criterionId": "criterion:7",
          "kind": "known-issue",
          "reason": "The Cost line says the order adds no recurring step, while gateStepCount reads 79 for WO-142 against 66 for the two rows before it and 72 and 74 for the two after, all above the earlier baseline. The subject does not define gateStepCount and the trap reports worsening [], so this is not an established failure.",
          "evidence": null,
          "reopenWhen": "A step-list comparison of the full gate before and after WO-142 shows steps introduced by WO-142 that run on later orders."
        },
        {
          "criterionId": "criterion:6",
          "kind": "known-issue",
          "reason": "The equality assertion in release check-surfaces and the anchor-resolution assertion in meta --check are new refusal paths inside existing checks. A defect in either refuses documents that passed before, so failure of this part of the mechanism refuses rather than degrading to the old behavior.",
          "evidence": null,
          "reopenWhen": "meta --check or release check-surfaces fails on a tree whose documents are correct, during an order that did not touch those documents."
        }
      ]
    },
    {
      "workOrderId": "WO-084",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Critical-path row 'deferred' (WO-084 to WO-090 docs reset): 'Not the product bottleneck; each child floats after the first external change or a dated waiver', with a planning-deferral on WO-053. It unblocks no gate. In the supplied sequence it sits at entry 33, ahead of WO-056 (gate E), WO-110 and WO-099 (gate R, the first unattended proof). NoOp in the records, re-observed 2026-09-19 at 3b3533f8: 31 sections below the Resolutions boundary (line 6115 of 7,580) dated 2026-09-05 to 2026-09-18, two of them operator ideation of 2026-09-18 that a newest-first reader does not meet, and a hand placement decision at every ideation and planning write. No observation id carries these; the cost row is null.",
      "systemTraps": "Policy resistance: 31 sections landed below the boundary in two weeks against a header that says never at the end, so appending is the writers' actual behaviour; a check that refuses the result, with no insertion mechanism, sets a guard against a habit and will be paid as repair churn (finding 2). Tragedy of the commons: one more check in test:docs, a suite WO-142's Cost line already reports failing first time after verify and final review dispatches; duration unknown until built. Drift: enforcing the header's rule restores a standard. Escalation: the check lives in the document suite, not the product gate, which limits it. Success to the successful: not material. Shifting the burden: surfaces operator ideation a newest-first reader misses, a real intake benefit for the product lead role. Rule beating: criterion 2 demands identical planning-pass ids while the objective normalizes bold-lead sections to the backtick tag; pass ids derive from heading bytes, so if any planning-pass heading carries the lead the two requirements cannot both hold (finding 1). Seeking the wrong goal: generated counts equal to a grep measure the index, not whether readers find the ideation; and a documentation order now precedes two critical-path proofs (finding 3).",
      "removalBalance": "Addition: one generated index and one check in test:docs, duration unknown. Removal: 31 misplaced sections (a one-time correction) and a recurring hand decision. The recurring half is the larger claim, and the mechanism in the criteria does not deliver it: a check detects a wrong placement after the write, it does not make the placement for the writer. So the line names a removal larger than its addition, but only the one-time part is secured by the criteria. Own cost unknown.",
      "failureBehavior": "The check is a refusal inside test:docs: a section below the boundary, an unlabelled entry or an unknown status fails the document suite. It is kept out of the product gate, so a failure does not block npm test, but it does not degrade to the old behaviour; the old behaviour (append at the end) is exactly what it refuses. Once Resolutions move to their own file, the 'boundary' the check enforces needs a new definition, which the subject does not give. Failure of the generator itself (index drift) is not addressed.",
      "findings": [
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "The order normalizes bold-lead sections to the backtick tag and also requires every planning-pass id to be unchanged. The subject states that a pass id is derived from the bytes of the dated heading, so a moved section keeps its receipt only if its heading is byte-identical. If a bold lead sits in any planning-pass heading, normalizing it changes the id and orphans a filed receipt; leaving it breaks the normalization. Whether any such heading exists is unknown from the subject.",
          "evidence": null,
          "reopenWhen": "The executor finds a planning-pass section whose heading carries the bold lead; then either normalization is skipped for it or the computed pass-id set changes and a filed receipt loses its subject."
        },
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "The Cost line claims to remove the hand decision of where a section goes. The criteria deliver an index and a refusing check, not an insertion command. The observed behaviour is 31 end-appended sections in about two weeks, so the likely result is a check that fires after writes and is repaired by hand: the decision remains, with a failure added.",
          "evidence": null,
          "reopenWhen": "After landing, test:docs failures or repair commits record sections filed below the boundary again; the count of such failures per planning or ideation write is the measure."
        },
        {
          "criterionId": "criterion:4",
          "kind": "known-issue",
          "reason": "The critical-path table calls this order 'not the product bottleneck', yet the sequence lists WO-142, WO-084 and WO-140 before WO-056 and WO-099. The goal standard says prerequisite work must name the blocked outcome it enables; this Cost line names none. Whether sequence entries run in parallel lanes is not stated in the subject, so the delay to the gate E and gate R proofs is unknown rather than shown.",
          "evidence": null,
          "reopenWhen": "The next cost table carries WO-142, WO-084 and WO-140 rows and no WO-056 or WO-099 row, with machinery share not falling across them."
        }
      ]
    },
    {
      "workOrderId": "WO-143",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate R. The resident (WO-068) feeds the first unattended proof (WO-099), the unattended hour (WO-111) and the product exit WO-118, whose title requires surviving a resident restart; the capability list holds runtime.resident at level 1, fixture-evidenced. Of the five judged orders this is the only one that serves a listed gate, and it is sequenced before WO-099. The typed prerequisites in the supplied table do not name WO-143 for any R order, so if it truly blocks the unattended proof the graph does not say so. NoOp cost: by the Cost line's own figures a lock transaction lasts 0.004 to 0.017 s and recurs on every 0.020 s poll while an episode runs, so a resident killed during an episode is inside the window roughly 20% to 85% of the time (inference from those figures, one transaction per poll); the guard then refuses every later start until a person deletes it. That is the mission's 'recovers stalled work' cost in its plainest form. The window measurement is attributed to WO-068 VER-001, which this review cannot see; no supplied observation measures it.",
      "systemTraps": "Policy resistance: the guard exists because lock recovery is not atomic; reclaiming the guard is a second recovery with the same problem, and criteria 1 and 2 pull against each other inside the new window (finding). Commons: a real-subprocess SIGKILL fixture with stepped windows and thirty rounds joins the resident suite inside npm test; its duration is unknown and every later gate pays it; WO-128's title records wall-clock-sensitive gate children as a past defect class (finding). Drift: the reassessment toward level 2 is left to verification and final review, which is right, but the quadratic append and the torn-log refusal stay, so 'dependable' for an unattended hour is unshown (finding). Escalation: low; no new step, key or receipt. Success to the successful: the order repairs recovery and keeps the exposure; criterion 4 removes lock cycles only after a kill is issued, and the 50-per-second cycle during an episode remains with no recorded comparison against not locking on a poll that appends nothing. Shifting the burden: this is the order's purpose and it does remove a human rescue; a released-version ownerless guard still needs one, and criterion 3 makes the refusal name the directory. Rule beating: thirty rounds prove little unless each is stepped into the window; criterion 4 is vacuous if 'a kill' means the resident's own SIGKILL (finding). Wrong goal: none seen; the goal is the unattended runtime itself.",
      "removalBalance": "Yes. The addition is one small file per acquisition and one fixture; the removal is a permanent-stop failure on the runtime the vision's first horizon names, plus the recorded blocker for runtime.resident level 2. Two limits on the claim: the removal is 'the one recorded way', and the order's own non-goals keep a second, unrecorded way (a torn log still refuses for inspection); and the fixture's gate cost is unknown until built. Order wall-clock, tokens and context bytes are unknown, which is unknown and not a fault.",
      "failureBehavior": "Mostly yes, with one case that is worse than the old behavior. An alive owner or an unreadable owner record refuses for inspection exactly as today, and a released-version ownerless guard still refuses. So a failure of the owner record degrades to the old behavior, and the old behavior here is itself a refusal that needs a person. The case that does not degrade: a live owner judged dead. The old code never reclaimed, so it could never put two residents on one store; the new code can, if the liveness probe errs toward 'dead' (an owner under another user, container or host). Criterion 2 tests concurrent starts against a dead guard only.",
      "findings": [
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "Criteria 1 and 2 meet inside the new window. Criterion 1 requires a reclaim after a kill 'between each pair of system calls that create and remove' the guard. Criterion 2 requires a guard with a 'missing, partial or malformed owner record' to refuse for inspection. Between the call that creates the guard directory and the completion of the owner-file write, the guard exists with a missing or partial owner. Both criteria hold only if the guard and its owner record appear in one atomic step (for example a populated temporary directory renamed into place). With create-then-write, a kill in that sub-window strands the store for a person again: the same defect, moved by one system call. The order does not name the construction, and the easy way out is to weaken criterion 2 or 3.",
          "evidence": null,
          "reopenWhen": "The stepped fixture's kill between guard creation and owner-record completion ends in a refusal for inspection; or the implementation settles the conflict by reclaiming an ownerless or partial-owner guard."
        },
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "'Killed at any instant' is wider than the order. The non-goals keep 'a torn log, which still refuses for inspection'. If SIGKILL can land inside an append (a multi-call write, or a rewrite after decoding the whole log), the next start refuses and a person is needed, which the title and objective say cannot happen. The fixture steps only the guard window. The thirty rounds add little unless each is stepped: against a 0.004 to 0.017 s window, randomly timed kills may never enter it. Whether an append can be torn by a kill is unknown from the subject.",
          "evidence": null,
          "reopenWhen": "Any kill-and-restart round, in the fixture or in a WO-099 or WO-111 run, ends with a torn-log refusal; or the README or capability note repeats 'any instant' without the torn-log exception."
        },
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "The one failure that is worse than the old behavior is untested. The old code never reclaimed a guard, so it could never put two residents on one store. The new code can if the liveness inspection errs toward 'dead': an owner running under another user (a permission error on the probe read as absence), in another container or process namespace, or on another host sharing the store. Criterion 2 tests two concurrent starts against a guard whose owner is dead; it has no case for a live owner the probe cannot see. The order reuses 'the same inspection a dead owner's lock gets', so the risk may already exist for the lock; that is unknown from the subject.",
          "evidence": null,
          "reopenWhen": "A start reclaims a guard whose owner process is still running, seen as two resident processes appending to one log or as a reclaim journaled while the owner's process id is alive."
        },
        {
          "criterionId": "criterion:4",
          "kind": "known-issue",
          "reason": "Criterion 4 is ambiguous and possibly vacuous. 'After a kill is issued the polling loop performs no lock cycle' is meaningful if the kill is the resident stopping an episode's child. If it means the SIGKILL sent to the resident, the process cannot act after it, and counting zero acquisitions proves nothing. Separately, the criterion leaves the exposure in place: by the Cost line the resident takes the lock on every 0.020 s poll while an episode runs, and no comparison with skipping the lock on a poll that appends nothing is recorded.",
          "evidence": null,
          "reopenWhen": "The criterion 4 fixture counts acquisitions in a process that has already received SIGKILL; or the verifier cannot tell from the fixture which kill is meant."
        },
        {
          "criterionId": "criterion:5",
          "kind": "known-issue",
          "reason": "The level-2 reassessment will be written while two limits remain. The window was measured at 0.004 to 0.017 s per transaction (WO-068 VER-001, per the Cost line); the log size at that measurement is unknown. The non-goals keep the quadratic append (each append decodes the whole log), and WO-068's title has the resident record each clock sample as an event, so transaction time grows with uptime. By the Cost line's own figures the resident already spends about 20% to 85% of an episode inside the window (inference: one transaction per 0.020 s poll). Once a transaction outlasts the poll interval the resident is in the window continuously. Recovery makes a kill survivable; it does not by itself make an unattended hour dependable.",
          "evidence": null,
          "reopenWhen": "A resident log from WO-099 or WO-111 shows a lock transaction longer than the 0.020 s poll interval, or per-tick time rising with log length; the level note for runtime.resident is then reopened."
        },
        {
          "criterionId": "criterion:6",
          "kind": "known-issue",
          "reason": "The fixture's gate cost is unknown and permanent. A real-subprocess SIGKILL fixture with stepped windows and thirty rounds joins the resident suite inside npm test, and every later order pays it at each full gate. WO-128's title records wall-clock-sensitive gate children under full load as a past defect class, and this fixture is of that kind. Metered gates already total 1,828,895 to 5,178,587 ms per order.",
          "evidence": null,
          "reopenWhen": "The order's receipt shows the fixture's standalone duration as a visible share of a full gate; or the fixture fails once under full-gate load while passing alone."
        }
      ]
    },
    {
      "workOrderId": "WO-144",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate: none. WO-144 is in no row of the critical-path table and no order's typed prerequisites; its theme is gate A's (authority), which is closed. It is sequenced ahead of WO-056, the next E-gate order, and its own criterion 5 expects later orders to need an order-named root, so it adds a step to the critical path rather than removing one. NoOp cost in the records: one incident, a stray stderr redirect that created one file in the directory above a worktree (WO-054 VER-003, per the Cost line); no harm is recorded, and the case aimed at the operator's documents is hypothetical. The supplied observations show guardRefusals 0 and operatorCorrections 0 for all five metered orders. The nearest metered analog for the order's own cost is WO-139, another hook refusal: 6 attempts, 11,431,171 ms elapsed, 5,178,587 ms of gate. NoOp leaves host permissions and a sandbox the Cost line says the operator has relaxed and product 03 records as not confining a sibling write.",
      "systemTraps": "Policy resistance: the refusal covers only destinations the extractor can name; the cheapest reply to a refusal is the same write through an interpreter or script, which the guard admits by design, and bypassTools and adHocScripts are null in every metered row, so the meter would not see it (finding). Commons: one more evaluation on every write tool call and every shell command with a known target; hookRuns and hookMs are null in every row, so the cost is unknown. Drift: default grants are chosen from what the retained journals show agents already wrote outside, so the present habit becomes the standard, and the inventory sees only extractor-known writes. Escalation: a fifth refusal, a grant vocabulary, a manifest field, catalog rows for every order needing a root, and another always-on sentence through CLAUDE.md for every role; criterion 6 pre-authorizes the ceiling route, which the acceptances show raised ceilings five times on 2026-09-17 and 2026-09-18 (finding). Success to the successful: the hook guard is extended because it exists; the grant is not shown to pass through WO-042's envelope, which already owns GRANTS and provenance (finding). Shifting the burden: contract roots are not carried into the grant (non-goal), so each order that works outside needs an operator-named root equipped by hand (finding). Rule beating: the title promises 'can no longer' while the objective promises known destinations only (finding). Wrong goal: protecting the operator's folders serves flow only if refusals on authorized work stay near zero; that is the number to watch.",
      "removalBalance": "Not shown. The additions are permanent: a fifth refusal judged on every write and shell call with a known target, role text for every role, a second place where write authority is declared, and a per-order root declaration the Cost line does not count. The removals are one incident class with one harmless recorded instance, and 'the operator's reliance on the native sandbox for this case', which holds only for known destinations; reliance for opaque effects, Codex sessions and a failed guard remains by the order's own width statement. The size of the avoided harm is unknown, which is unknown and not a hold. Order wall-clock, tokens and hook latency are unknown.",
      "failureBehavior": "Yes for the named case: criterion 4 has an unreadable configuration admit with one advisory and a journaled cause, which is the old behavior. Not settled for the rest. The parenthesis names one failure; an extractor exception or a path-resolution error (a destination that does not exist yet, a symlink loop, a permission error on an ancestor) is not covered, and the new code sits inside the pre-tool guard that carries the four existing refusals, so its failure mode is theirs too. Root aliasing (a temporary root reached through a symlink) fails toward refusing authorized scratch writes, which is a new refusal, not the old behavior. The message names the operator's recovery control, so recovery from a wrong refusal depends on the operator.",
      "findings": [
        {
          "criterionId": "criterion:3",
          "kind": "known-issue",
          "reason": "A second authority vocabulary. WO-042's title fixes the rule: a linked support may only narrow a compiled build's authority, widening needs an explicit provenance-bearing grant, and GRANTS and RESTRICTIONS are projected from the effective envelope. WO-144 has a support widen write authority by granting an operator-named root, with a manifest that 'lists the effective roots and their sources'. The subject does not say the root grant is lowered through that envelope or bound by its provenance rule. Criterion 3 has no case where a support without a provenance-bearing grant tries to add a root and is refused, and no ceiling case. If roots live in hook configuration beside the envelope, DotLn has two authority models and the tooltip's GRANTS stop being the whole truth. WO-115's title names the standard: 'no second authority path'.",
          "evidence": null,
          "reopenWhen": "For any build, the manifest's effective outside roots differ from the GRANTS its compiled envelope projects; or a support with no provenance-bearing grant adds a root and `harness check` passes."
        },
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "The guard selects for the writes it cannot see. It refuses only destinations the extractor names and leaves programs with opaque effects to host permissions. After a refusal the cheapest next move is the same write through an interpreter or a script, which is admitted by design and is less inspectable than the redirect it replaces. The meter would not notice: bypassTools and adHocScripts are null for all five metered orders.",
          "evidence": null,
          "reopenWhen": "The hook journal shows an outside-grant refusal followed in the same session by an opaque command that creates the refused destination; or bypassTools or adHocScripts, once measured, rise after this order."
        },
        {
          "criterionId": "criterion:5",
          "kind": "known-issue",
          "reason": "The Cost line leaves out a recurring step that criterion 5 admits. It says the order adds 'no new hook, gate, key, receipt or classifier', yet criterion 5 lists 'the map's catalog rows for the orders that will need an order-named root', and the non-goals exclude carrying a work order's contract roots into the active grant. The critical-path orders that follow work outside the project by definition (WO-056 'a real repository', WO-111 'a scratch repository', WO-112 'a scratch target', WO-118, WO-083). Each will need a root named and equipped by hand or will route its writes through opaque commands. That is a per-order step and an operator dependency on the critical path. operatorCorrections reads 0 for all five metered orders today.",
          "evidence": null,
          "reopenWhen": "A critical-path order's journal records an outside-grant refusal on its own authorized target root, or an operator equip or override step to pass one; or operatorCorrections rises from 0 with this guard as the recorded cause."
        },
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "Root aliasing and unresolvable destinations. The objective judges where a destination 'resolves'. Granted roots must be resolved the same way or authorized writes are refused: on a host where the temporary root is reached through a symlink (on macOS, /tmp and /var point into /private), a grant spelled one way and a destination resolved the other never match. If roots are not resolved, criterion 2's rule about a symlink inside a granted root has a twin in a granted root that is itself a symlink. A destination that does not exist yet cannot be resolved directly; resolution has to walk to the nearest existing ancestor. Fixture roots under a fixture home will not reproduce either case. The host platform is not stated in the subject.",
          "evidence": null,
          "reopenWhen": "Under the merged hooks a write to the temporary or scratch root through its symlinked spelling is refused, or a write through a symlinked granted root to an outside target is admitted; or guardRefusals rises on writes the default grants were meant to admit."
        },
        {
          "criterionId": "criterion:4",
          "kind": "known-issue",
          "reason": "Fail-open is specified for one failure only. Criterion 4 names 'a guard failure (unreadable configuration)'. An exception in the extractor, or a resolution error (missing ancestor, symlink loop, permission error), is not covered, and the new code sits inside the pre-tool guard that carries the four existing refusals. 'The four existing refusals are unchanged' does not say they were tested while the new refusal was failing. A fault here can therefore fail open for all five or block writes that pass today.",
          "evidence": null,
          "reopenWhen": "With the outside-grant configuration unreadable or a resolution error forced, a second writer or a live-gate write is admitted, or an in-project write is refused."
        },
        {
          "criterionId": "criterion:5",
          "kind": "known-issue",
          "reason": "The title promises more than the objective. The title says a mistaken path 'can no longer create or delete a file in the operator's folders'. The objective covers known destinations only; opaque commands, Codex sessions and a failed guard (criterion 4 admits) all still can. Criterion 5 corrects the documents but not the title, and the title is what reaches the index row, the release title and pull-request text. The platform lens asks that a result be usable by a stranger to the session; a stranger reads 'can no longer' as a total guarantee.",
          "evidence": null,
          "reopenWhen": "The release title, index row or generated CLAUDE.md sentence states the guarantee without the words 'known destination'; or an opaque command in a fixture creates a file in the documents-like folder under the merged hooks."
        },
        {
          "criterionId": "criterion:6",
          "kind": "known-issue",
          "reason": "Always-on prose beside a compiled guard. The refusal message already names the destination, the missing grant and the recovery control at the point of need. The order also adds a sentence to always-on text for every role through CLAUDE.md. The vision's first thesis names always-on prose as how the predecessor failed and 'attached only where relevant' as the answer; this is a tension with it, not a contradiction, because the mechanism itself is compiled. The cost table's acceptances show ceilings raised five times on 2026-09-17 and 2026-09-18, reviewer headroom is 1,371 bytes by WO-142's Cost line, criterion 6 pre-authorizes the same route, and the drift series for coldStartBytes is null for all five metered orders, so the meter does not see the growth.",
          "evidence": null,
          "reopenWhen": "During WO-142, WO-144 or WO-145 any role's cold-start total meets its ceiling and the route raises the ceiling again instead of removing bytes; or coldStartBytes is still null in the drift series after these orders merge."
        },
        {
          "criterionId": "criterion:5",
          "kind": "known-issue",
          "reason": "The stale claim WO-142 cleans comes straight back. WO-142's objective corrects live documents that describe 'four hook refusals as two or three'. Two orders later WO-144 makes the number five across product 03, product 07, the security document and CLAUDE.md. Unless the count is derived from the generated hook set (WO-142's equality assertion may do this; the subject does not show it), the same hand-kept number goes stale at once.",
          "evidence": null,
          "reopenWhen": "After WO-144 merges, any live surface still says four refusals while `release check-surfaces` passes."
        }
      ]
    },
    {
      "workOrderId": "WO-140",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Supports attended verification on E/V and machinery review. NoOp retains recorded sandbox-caused failed gates costing 191–515 seconds each and uninformative cost fields.",
      "systemTraps": "Policy resistance: preflight avoids doomed runs, while receipt validation reintroduces process refusal. Commons: a subsecond probe can save full suites. Drift: partial evidence remains explicitly partial. Escalation: outside-session routing can create handoffs. Success to the successful: causes follow observations, not all sandbox failures. Intervenor burden: attended execution may remain necessary. Rule beating: cause-code presence does not prove measurement effort. Wrong goal: complete verification must remain the outcome.",
      "removalBalance": "The gate preflight names a credible larger removal. The mandatory receipt code has a separate, unmeasured benefit and recurring maintenance cost.",
      "failureBehavior": "Preflight refuses instead of running the known-incompatible selection; an explicit partial mode is available. New receipt cost omissions also refuse, contrary to the stand-down's advisory direction.",
      "findings": [
        {
          "criterionId": "criterion:5",
          "kind": "known-issue",
          "reason": "Mandatory cost-line syntax creates tension with WO-132's rule that process-cost measurement is never a completion requirement. A structural inconsistency alone is not an observed failure or supplied vision contradiction.",
          "evidence": null,
          "reopenWhen": "An otherwise valid completion, publication or release is blocked solely by missing counters or the absence of an accepted cause code."
        },
        {
          "criterionId": "criterion:4",
          "kind": "known-issue",
          "reason": "Resident-launched verification produces partial evidence, while attended sessions obtain the full gate. The effect on the autonomous product exit is not established by this subject.",
          "evidence": null,
          "reopenWhen": "A representative resident-owned loop needs an operator handoff solely to obtain the full product gate under this policy."
        }
      ]
    },
    {
      "workOrderId": "WO-056",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Closes gate E's live blinded verification and repair proof before V. NoOp leaves independence and repair demonstrated only with doubles.",
      "systemTraps": "Policy resistance: tests implementer/verifier separation in operation. Commons: one bounded live loop contains cost. Drift: original-contract verification sets the standard. Escalation: runtime defects become separate work. Success to the successful: fresh actors can replace predecessors. Intervenor burden: validates automated repair. Rule beating: planted superficial success must remain negative. Wrong goal: proves meaningful correctness, not receipt production.",
      "removalBalance": "Unknown: the legacy Cost line contains no measurement. The value is evidence for a necessary product behavior.",
      "failureBehavior": "Failure remains a failed proof with labeled evidence; it does not promote capability or authorize bypassing independence.",
      "findings": []
    },
    {
      "workOrderId": "WO-146",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "No critical-path gate in the supplied table names WO-146 as a prerequisite. Its bounded contribution is operator-launched harness substitution, consistent with the-core-bet: the existing compiled role and lifecycle can have another occupant. It does not supply the resident, source-change transport or independent runtime verification gates; those are explicit non-goals. NoOp retains the two existing harnesses and leaves the Cost line's stated hand translation and uncertainty about Copilot reading the existing hook registration. The supplied records do not count the incidence or duration of that manual work, so its opportunity cost and any acceleration of a selected gate are unknown. The product lead's intended experience is clear; the showrunner should treat this as bounded enabling work, not claim a critical-path gate has been cleared by adding a third harness.",
      "systemTraps": "Policy resistance / fixes that fail: one hook registration, at-most-once handler invocation and compatibility fixtures address competing controls; observed availability labels avoid promising enforcement the host lacks. Tragedy of the commons: scripted-launch and retry bounds account for finite probes, but shared role text is paid by existing harness sessions too, and credit limits do not quantify total interactive or recurring context cost. Drift to low performance: criterion 5 retains the live-denial-plus-fixture standard; raising a cold-start ceiling records acceptance, not evidence of improved efficiency. Escalation: using four existing suites, no new gate and bounded qualification limits procedural growth; the added profile still creates maintenance work whose recurring cost is unknown. Success to the successful: another occupant of the same loadout preserves a credible harness alternative without forking the role system; criteria 4 and 6 preserve the existing choices and separate harness from model identity. Shifting the burden to the intervenor: the intended removal is repeated hand translation, while operator-started qualification and fresh verifier sessions remain explicit costs; recurring rescue reduction is not measured. Rule beating: live episodes and fixtures test the actual lifecycle, but freshness of a session alone does not exclude the cross-session memory channel expressly named in criterion 11. Seeking the wrong goal: reusable role delivery serves operator flow; harness count, green fixtures and receipt volume cannot substitute for saved supervision or progress on the selected product outcome. The supplied trap series report no reopening candidate, and their many nulls establish neither success nor failure.",
      "removalBalance": "The Cost line names removals: manual role-procedure translation and uncertainty about how the installed third CLI interacts with existing hooks. It also names a profile entry, probe mode, event-log reader, fixture work, shared cold-start text, generated residue and live qualification. It does not establish that the removals are larger. The supplied WO-146 cost row records elapsedMs=37639592, attempts=5 and machineryShare=0.0663632060623824; tokens, costUsd and gateMs are null, with no comparable measured savings. The supplied verifier-ceiling acceptance additionally records 974 added bytes and a measured 21055-byte cold start. These are real costs, not an observed failure or a basis for inventing a return on investment. For the UIFA engineer and devops roles, reusing one loadout and existing suites is a coherent interface extension that avoids a separate role implementation; its net economy remains unknown.",
      "failureBehavior": "The contract generally preserves the previous operating route: criterion 4 retains Claude and Codex behavior, criterion 2 marks unobserved refusal support unavailable, criterion 7 makes missing usage data unknown with a cause rather than blocking, and criterion 9 turns an incomplete qualification episode into a finding within the retry bound. Criteria 5, 8 and 11 label unavailable or untested guarantees and retain qualified harnesses for final review and release close. Existing authority and ownership refusals remain intentional boundaries, not new fallback failures. This supports the intended degradation to existing behavior; the supplied subject contains no live malfunction evidence that would justify claiming the implementation actually demonstrated every fallback. For the UIFA tester, fresh-session qualification is useful evidence with the declared memory limitation, not proof of mechanical independence.",
      "findings": [
        {
          "criterionId": "criterion:12",
          "kind": "known-issue",
          "reason": "The planned removal is qualitative while shared cold-start growth and an elapsed-cost record are present. Neither a named critical-path dependency nor measured recurring savings establishes that this harness extension removes more cost than it adds. This is an unresolved economy claim, not a structural hold for unknown cost.",
          "evidence": null,
          "reopenWhen": "Observed routine Copilot episodes show that the additional translation or rescue work persists after qualification, or observed recurring context and maintenance costs exceed the manual work removed; alternatively, a recorded selected critical-path delay is caused by maintaining this optional harness."
        },
        {
          "criterionId": "criterion:9",
          "kind": "known-issue",
          "reason": "Criterion 9 calls for a fresh independent verifier, while criterion 11 explicitly names CLI cross-session memory as a channel for implementer context and excludes mechanical independence. A new session can therefore satisfy the launch procedure without establishing a blinded judgment. No supplied observation shows that contamination occurred; the constructible case is not a hold.",
          "evidence": null,
          "reopenWhen": "An observed fresh verifier context or session-memory record contains implementer narrative, the planted-defect answer or the repair conclusion from another episode before the verifier independently inspects and judges the subject."
        }
      ]
    },
    {
      "workOrderId": "WO-145",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate: none. WO-145 is in no row of the critical-path table and unblocks no order; the goal standard itself says the adaptive Tinkerer mechanics keep candidate scope. It is sequenced after WO-056 and directly before WO-090, the order that shortens the cold start this order lengthens. NoOp cost in the records: none. No supplied observation or Cost-line figure records a loss from the support's absence; NoOp leaves the candidate a candidate and costs nothing measured. The case for acting is the operator's dated direction (2026-09-11 and 2026-09-19) as the Cost line reports it; this review cannot see that direction and takes the citation as the subject's claim.",
      "systemTraps": "Policy resistance: the objective lets the experiment target 'a recurring repository step' while the non-goals forbid exploration outside the order and any authority the order lacks; one of the two must give (finding). Commons: up to 900 s per trial order is 17% of WO-055's whole elapsed time and 15% of WO-137's, and the operator must choose two trial orders; machineryShare is already 0.50 to 0.58 in three of five metered orders (finding). Drift: no decision rule is fixed before the trials, so any of the three outcomes can be read from three records (finding). Escalation: accepted improvements either land inside an unrelated order's diff or become new follow-ups, so process begets process either way; one more paragraph of executor text arrives one order before WO-090 removes text. Success to the successful: low; the support is optional and byte-identical when off, the best NoOp guarantee in the five orders. Shifting the burden: criterion 4 waits on the operator's choice of trial orders, and nothing unequips the support after the third trial (finding). Rule beating: a declined record satisfies a trial, and criteria 2 and 3 disagree on whether a declined record is even admissible (finding); the effect is measured once by the actor who proposed it. Wrong goal: 'seconds, tokens or commands' is a proxy; tokens and commandsRun are null in all five metered rows, so seconds alone will carry the verdict (finding).",
      "removalBalance": "No, by the order's own words: 'Names no removal in advance.' The additions are a paragraph of executor text while equipped, one record kind in the decisions generator (permanent, with two refusal conditions), and up to 900 s per trial order. The subject reports a dated operator acceptance for cost without removal; this review cannot see it and does not dispute it. The absence of a removal answers question 3 in the negative; it is not an observed failure or a vision contradiction and is not a hold. Whether any removal appears is the experiment's question, and the design is small and noisy for answering it (findings). Order wall-clock and tokens are unknown.",
      "failureBehavior": "Yes when off: criterion 1 makes every role's text byte-identical with the support unequipped, so withdrawal restores the old behavior exactly. Partly when on. The 900 s budget has no enforcer (the non-goals exclude a gate, schedule or hook), so an overrun is only recorded. The one hard mechanism refuses rather than degrades: the decisions generator rejects an experiment record without a measured cost or an effect command, so an executor with no counter, or an honest decline, can fail the host order's decisions check. That check would then stop an order for the sake of an optional experiment.",
      "findings": [
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "Criteria 2 and 3 disagree about a declined record. Criterion 3 accepts one experiment record 'run or declined'. Criterion 2 has the generator refuse a record 'without a measured cost or with an effect that names no command'. A declined experiment has neither. Either the generator refuses the record criterion 3 allows, so an honest decline fails the host order's decisions check, or declined records are exempt, and declining becomes the free way to complete all three trials with no information gained.",
          "evidence": null,
          "reopenWhen": "The generator refuses a declined record; or all three trial records are declined; or a run record is accepted with a zero or placeholder cost."
        },
        {
          "criterionId": "criterion:3",
          "kind": "known-issue",
          "reason": "The instrument is coarse for the question. Each trial is one unrepeated measurement with no control, taken by the actor who proposed the change. Two of the three units the objective names are not recorded today: tokens and commandsRun are null in all five metered rows. Seconds remain, against run-to-run spreads like the 303 to 1,178 s range WO-142's Cost line gives for one gate. A saving smaller than that spread cannot be told from noise, and three such numbers are to decide whether the support stays equipped. WO-140 is sequenced earlier and may supply counters; that is unknown.",
          "evidence": null,
          "reopenWhen": "A trial record claims a saving smaller than the observed spread of the command it measured; or verification re-runs the named command and does not reproduce the effect; or trial records carry null tokens and commands."
        },
        {
          "criterionId": "criterion:4",
          "kind": "known-issue",
          "reason": "No decision rule is fixed before the trials, and no removal is named. The title says 'the record decides whether it stays equipped', but nothing states in advance what record means keep, default or withdraw; WO-138 in the same pass pre-registers its floors. With three records and no threshold, any of the three decisions can be read from any result. The Cost line says plainly that it names no removal and rests on a dated operator acceptance that this review cannot see.",
          "evidence": null,
          "reopenWhen": "The planning pass that decides cites no threshold written before the first trial; or the support is kept or made default while the summed measured cost of the trials exceeds the summed measured saving."
        },
        {
          "criterionId": "criterion:4",
          "kind": "known-issue",
          "reason": "The order closes before its question is answered and cannot close without the operator. Criterion 4 needs 'the two further trial orders the operator chose', so the executor cannot satisfy it alone. Two of three trials then happen inside other orders, and the answer arrives at 'the next planning pass'. Nothing unequips the support after the third trial, so an optional cost can become a standing one with no owner.",
          "evidence": null,
          "reopenWhen": "WO-145 waits in execution or final review for the operator's choice of trial orders; or the support is still equipped on an order after the third trial with no recorded planning decision."
        },
        {
          "criterionId": "criterion:3",
          "kind": "known-issue",
          "reason": "Objective and non-goals pull apart on scope. The objective allows an experiment on 'a recurring repository step'; the non-goals forbid 'exploration outside the order being executed' and 'any authority an order does not already carry'. An accepted improvement to a gate step or script, made inside a trial order, lands in that order's diff outside its contract, and that order's verifier must judge machinery it was not given. If accepted improvements are only recorded and not landed, each becomes a new follow-up. The mission thesis bars flow from silently authorizing scope expansion; here it is recorded, not silent, so this is a known issue and not a contradiction.",
          "evidence": null,
          "reopenWhen": "A trial order's diff touches a file outside its declared surfaces with an experiment record as the reason; or a verification finding is filed against experiment residue."
        },
        {
          "criterionId": "criterion:5",
          "kind": "known-issue",
          "reason": "The 900 s budget is large against observed orders and has no enforcer. It equals 17% of WO-055's whole elapsed time (5,288,212 ms) and 15% of WO-137's (5,875,968 ms), and machineryShare is already 0.50 to 0.58 in three of five metered orders. The non-goals exclude a gate, schedule or hook, so the cap is policed only by the executor that spends it. Criterion 5 records the cold-start total; no criterion records a budget overrun.",
          "evidence": null,
          "reopenWhen": "A trial record's measured cost exceeds 900 s; or a trial order's elapsed time or machineryShare rises against these rows with the experiment as the recorded cause."
        }
      ]
    },
    {
      "workOrderId": "WO-090",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Same 'deferred' row as WO-084; unblocks no gate; sequence entry 40. NoOp in the records: the guide stands at 1,772 lines on 2026-09-19 against 804 at the 2026-09-08 sweep, so every role's directed reads have grown; the per-role directed-load totals are not recorded and will be measured at activation. The supplied cost table adds context: five cold-start ceiling acceptances in two days (reviewer to 20,480, release-close to 12,288 and then 16,384, verifier to 20,480, executor to 24,576), each raising a ceiling rather than trimming. The drift-to-low-performance observation shows coldStartBytes null for every metered order, so the meter does not see that series. Cost row null.",
      "systemTraps": "Policy resistance: every process order in this pass writes more into product 07 (WO-141 criterion 5, WO-135 criterion 5, WO-139 criterion 5, WO-142 criterion 6) while this order trims the guide once; with no ratchet the local goals undo one another (finding 1). Tragedy of the commons: this is the one order that returns shared context; its amount is unmeasured until activation. Drift: the guide more than doubled in eleven days and five ceilings were raised in two; the order improves a standard once and sets none going forward. Escalation: adds no step, check, hook or receipt. Success to the successful: not material. Shifting the burden: none added. Rule beating: directed-load can fall by relocation while what a role actually reads is unchanged, or a relocated rule is simply not read when needed; bytesReadIntoContext is null in every row, so the intended effect is unobservable today (finding 2). Seeking the wrong goal: the title says shorter cold start, the non-goals exclude the generated role text, and the recorded ceiling breaches are all in generated role text (finding 3). Criterion 1's guard that no generated cold-start bytes rise is a floor, not a reduction.",
      "removalBalance": "Addition: pointers only; no step, check, hook or receipt. Removal: context bytes from every role's directed reads, amount unknown until criterion 1 measures it. Any positive measured reduction exceeds the addition, so the line's shape is right; its size is unknown, and it is a one-time cut against a guide that grew by 968 lines in eleven days.",
      "failureBehavior": "There is no runtime mechanism to fail: the order moves paragraphs and leaves pointers, drops no rule and adds no refusal. If criterion 1 cannot be met for some role the order does not close and the guide stays as it is, which is the old behaviour. The risk is silent rather than refusing: a relocated sandbox-approval or model-specific paragraph that a role no longer meets, which is the predecessor's 'vanished when needed most' failure in miniature.",
      "findings": [
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "Criterion 1 is a single before-and-after comparison. The guide grew from 804 to 1,772 lines in eleven days, and four orders in this pass add to product 07. Nothing in the order stops the total returning to its old value, so the removal is one-time against a recurring addition.",
          "evidence": null,
          "reopenWhen": "By the next planning receipt after WO-090 closes, the guide's line count or any role's directed-load total is back at or above its pre-WO-090 measurement."
        },
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "The relocation table accounts for where each paragraph went, not for whether the role that needs it still reaches it. Directed-load falls when a paragraph moves behind a pointer; the actual bytes read may not fall (the role follows the pointer) or the rule may go unread at the moment it matters. bytesReadIntoContext is null in every supplied row, so neither effect is visible.",
          "evidence": null,
          "reopenWhen": "A post-WO-090 review, journal or operator correction records a sandbox-approval or model-specific error that a relocated paragraph covered; or bytesReadIntoContext, once populated, does not fall for the roles whose directed-load fell."
        },
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "The recorded context pressure is in the generated role text: five ceiling acceptances on 2026-09-17 and 2026-09-18 for installed CLAUDE.md plus role skill, two of which defer efficiency changes to 'a later pass'. WO-090 is titled shorter cold start and lists reducing the generated role text as a non-goal. No order in this pass owns that later pass.",
          "evidence": null,
          "reopenWhen": "Another cold-start acceptance raises any role's ceiling after WO-090 closes, or the later pass that the 2026-09-18 acceptances defer efficiency to is still unallocated at the next receipt."
        }
      ]
    },
    {
      "workOrderId": "WO-110",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Adds a local actor option to gate R. NoOp leaves inspection dependent on existing transports; no comparative cost or quality baseline is supplied.",
      "systemTraps": "Policy resistance: implements the existing transport contract. Commons: local compute has real but unmeasured cost. Drift: envelope validity does not imply model quality. Escalation: inspection-only scope limits expansion. Success to the successful: creates a credible non-remote alternative. Intervenor burden: typed availability avoids setup rescue. Rule beating: live results and doubles remain distinct. Wrong goal: interchangeable actors, not model novelty.",
      "removalBalance": "Unknown: no larger removal is declared; this is a bounded extension of an existing port.",
      "failureBehavior": "Yes: endpoint unavailability becomes typed unavailability and resident NoOp. It does not silently substitute another actor or authority.",
      "findings": []
    },
    {
      "workOrderId": "WO-099",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate R's first unattended mission proof, also required by WO-111 and WO-117. NoOp leaves unattended drift without this scheduled check; cost is unmeasured.",
      "systemTraps": "Policy resistance: a global hold can conflict with independent work. Commons: recurring model checks consume budget. Drift: contract and thesis evidence define drift. Escalation: holds can require follow-up verification. Success to the successful: unavailable verifiers must remain distinguishable from wrong work. Intervenor burden: early detection competes with hold clearance. Rule beating: findings need evidence references. Wrong goal: useful authorized progress, not universal verifier availability.",
      "removalBalance": "Unknown: the legacy line does not compare recurring checks with avoided supervision.",
      "failureBehavior": "No: unavailable verification yields unknown and holds; drift holds every unattended dispatch. This deliberately fails closed rather than retaining prior dispatch behavior.",
      "findings": [
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "An unavailable verifier can stop all unattended work, including work unrelated to the uncertain episode. Whether that scope improves flow while preserving authority remains unobserved.",
          "evidence": null,
          "reopenWhen": "Verifier unavailability blocks an otherwise authorized independent order and requires operator rescue without evidence that the order is affected by drift."
        }
      ]
    },
    {
      "workOrderId": "WO-079",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate S integration support removes second-lane handwork. NoOp retains an eight-step checklist and the recorded variation across sibling integrations.",
      "systemTraps": "Policy resistance: regenerates shared projections consistently. Commons: one command reduces repeated diagnosis. Drift: retained checkpoints and stashes preserve recovery. Escalation: no lifecycle events or automatic acceptance are added. Success to the successful: authored conflicts remain explicit judgments. Intervenor burden: removes remembered integration steps. Rule beating: printed checks are not passing evidence. Wrong goal: integration completion rather than helper activity.",
      "removalBalance": "A larger procedural removal is named. The observed review-duration medians are associations, not a demonstrated seven-minute causal saving.",
      "failureBehavior": "Precondition failures leave the tree unchanged. Authored conflicts remain recoverable through retained stash/checkpoint state and explicit resolution; the helper does not guess.",
      "findings": []
    },
    {
      "workOrderId": "WO-069",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate S enables export, repository configuration and WO-120. NoOp retains the recorded repeated document roots and eleven ad hoc root derivations.",
      "systemTraps": "Policy resistance: one resolver prevents root disagreement. Commons: shared configuration reduces repeated context. Drift: default-output equality protects current users. Escalation: registration semantics stay separate. Success to the successful: external layouts become possible. Intervenor burden: avoids per-script relocation fixes. Rule beating: alternate-root lifecycle fixtures prove portability. Wrong goal: externalizable control-plane behavior.",
      "removalBalance": "The architectural removal is explicit, but its time/context value is not measured in the legacy Cost line.",
      "failureBehavior": "Yes: absent configuration reproduces today's layout. Malformed explicit configuration refuses rather than silently writing into an unintended root.",
      "findings": []
    },
    {
      "workOrderId": "WO-138",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate: none directly. Gate R lists WO-110 (the local-model transport); no row or typed prerequisite names WO-138, and WO-111 and WO-118 do not wait on it. It informs a catalog decision for the local actor kind. WO-110 is sequenced before it and its title already has the local transport join the catalog for the inspection profile, so admission precedes qualification (finding). NoOp cost in the records: none measured. NoOp leaves the local kind inspection-only with no measured floor; nothing in the supplied observations or the Cost line records a loss from that. The order's own spend is at least 38 live episodes across two models if each extra cell is one episode, a 30-minute operator session, a permanent evaluation harness and its tests inside npm test. Its dependency WO-137 is metered: 5,875,968 ms elapsed, 3 attempts.",
      "systemTraps": "Policy resistance: an activation condition lives in an acceptance criterion instead of a typed edge, so the graph WO-043 and WO-135 built cannot enforce it (finding). Commons: 30 minutes of operator attention for the T2 ranking, remote-model spend for half the episodes (tokens and costUsd are null in every metered row, so unknown), and harness tests added to every later gate; gateStepCount was falling, 76 to 66 (finding). Drift: five repeats of one input per cell cannot support a reliability floor; 5 of 5 is compatible with a true rate near 55% (finding); accepting that as 'qualified' sets a low bar for every later qualification. Escalation: the outcome names 'what the next qualification must show', so the pilot's product is partly another pilot. Success to the successful: one remote comparator and one local build; the non-goals exclude a second remote model, which is reasonable for cost and limits what 'compared' means. Shifting the burden: the held-out ranking is a one-time, recorded operator cost; acceptable if it stays one-time. Rule beating: floors are 'as written at activation' and absent from the judged subject; one-factor cells have no stated repeats (findings). Wrong goal: local roles serve the vision's privacy and model-swap theses, yet criterion 1 qualifies no role that reads private material, so the roles it can admit are the ones where local inference matters least; T3's journal input strains the public-material rule (finding).",
      "removalBalance": "No in cost terms. The named removal is an assumption ('local episodes stay inspection-only indefinitely, and its opposite'), not a recurring cost, and the order cannot remove it: all three tasks are read-only inspection tasks, and criterion 4 and the non-goals forbid qualifying implementation or verification. What it can deliver is a measured floor for inspection roles. The additions are concrete and partly permanent: an evaluation harness with tests inside npm test, an evidence directory, at least 38 live episodes and up to 30 operator minutes. The information may still be worth more than it costs; that is unknown until run and is not a hold.",
      "failureBehavior": "Yes. Live runs never enter npm test; an episode that fails leaves a typed failure; a blocked pilot reports `inconclusive` with the blocker; nothing in the packet promotes a level. Failure therefore leaves the catalog where WO-110 left it. Two edges: the harness's deterministic tests run inside npm test, so a defect in them refuses every later gate rather than degrading; and if criterion 1's first clause proves false after activation the order has no defined state to return to, because the condition is not a typed dependency.",
      "findings": [
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "An activation condition is written as an acceptance criterion. 'The order is not activated otherwise' depends on the outcome row in WO-137's execution record. WO-043's title says only an unmet hard dependency or a planning deferral blocks activation, and WO-135's title has the sequence checked against every typed hard edge. A condition on another order's outcome, in prose inside a criterion, is invisible to both, and acceptance criteria are judged after execution, when the condition can no longer prevent anything.",
          "evidence": null,
          "reopenWhen": "Status offers WO-138 for activation while WO-137's record has no successful live inference row; or the executor finds the first clause of criterion 1 false after activation and has no defined state to return the order to."
        },
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "T3's input strains the public-material rule and the clean-room floor. T3 classifies hook-journal rows; criterion 1 requires every episode input to be committed public material or the seeded scratch repository, and the same input goes to a remote transport for comparison. Hook journals are session records. WO-142's non-goals in this same pass list 'a home-path screen for reports' and 'the absolute store path in the worker prompt' as not addressed, so absolute local paths are known to occur in neighboring material and no screen is named. To satisfy criterion 1 the labeled T3 sample must be committed, which could place journal rows with local paths, session ids or command text in the public tree and in a remote model's input; to avoid that, T3 must read uncommitted rows, which breaks criterion 1. What the journal rows actually contain is unknown from the subject.",
          "evidence": null,
          "reopenWhen": "The committed T3 sample or any recorded envelope contains an absolute home path, user name, host name, session id or other local identifier; or a T3 episode reads journal rows that are not in the committed tree."
        },
        {
          "criterionId": "criterion:2",
          "kind": "known-issue",
          "reason": "The design cannot support 'at what reliability'. The budget is three tasks by two models by five repeats: five repeats of one input per cell. Five successes in five supports, at 95% one-sided confidence, a true rate no lower than about 55% (0.05 to the power one fifth is 0.549), and the schema-valid rate moves in 20-point steps. Each task has a single instance (one receipt, one seeded repository, one labeled sample), so variation across inputs, which is what a role meets in use, is unmeasured. The title's 'representative' rests on one case per task.",
          "evidence": null,
          "reopenWhen": "The packet states a reliability figure with no n or interval; or a role admitted as `ready` fails its schema or oracle check on a second input of the same task."
        },
        {
          "criterionId": "criterion:4",
          "kind": "known-issue",
          "reason": "The floors that decide the outcome are not in the judged subject. The outcome is `ready`, `negative` or `inconclusive` 'against the floors as written at activation'. This review cannot judge whether `ready` will mean anything. Nothing requires the floors to be fixed before any episode runs or by someone other than the executor who runs them, and WO-137 has already produced live inference rows, so the local model's behavior is partly known when the floors are written.",
          "evidence": null,
          "reopenWhen": "The commit that writes the floors is later than the first recorded baseline envelope; or the packet's floors differ from the activation text."
        },
        {
          "criterionId": "criterion:3",
          "kind": "known-issue",
          "reason": "The one-factor cells have no stated repeats. The baseline measures variability across five repeats; 'five one-factor cells' reads as one episode each. A single-episode delta cannot be separated from the repeat variability the baseline itself measures, so 'how much does one prompt or support change move it' may be answered with noise.",
          "evidence": null,
          "reopenWhen": "A one-factor delta is reported as an effect while lying inside the baseline's five-repeat range for the same task and model."
        },
        {
          "criterionId": "criterion:5",
          "kind": "known-issue",
          "reason": "A pilot's harness becomes permanent gate load with no named consumer. The deterministic tests over recorded envelopes run inside npm test, so every later order pays them. gateStepCount has been falling (76 at WO-139 to 66 at WO-137). The non-goals make the pilot one-shot (no leaderboard, no second remote model), and the platform lens asks that a capability arrive as an interface something else consumes; the next consumer of this harness is not named.",
          "evidence": null,
          "reopenWhen": "gateStepCount rises with WO-138 and no order filed after it names the evaluation harness as an input."
        },
        {
          "criterionId": "criterion:6",
          "kind": "known-issue",
          "reason": "Admission precedes qualification. WO-110 is sequenced before WO-138 and its title already has the local transport 'join the actor catalog for the inspection profile'. WO-138 then decides which inspection roles the local kind may fill. For a `negative` or `inconclusive` role the write-backs name only 'the local kind's qualified roles, dated'; no write-back restricts what WO-110 already offered.",
          "evidence": null,
          "reopenWhen": "WO-138 reports `negative` or `inconclusive` for a role while the catalog entry WO-110 shipped still offers the local kind for that inspection role with no recorded restriction."
        }
      ]
    },
    {
      "workOrderId": "WO-071",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate S enables target lifecycle, profiles and cross-repository workstreams. NoOp leaves repository identity and authority implicit; costs are unmeasured.",
      "systemTraps": "Policy resistance: registration flows through the monotone authority floor. Commons: profiles avoid repeated declarations. Drift: activation records retain base identity. Escalation: no publishing machinery is added. Success to the successful: absent metadata preserves self-repository operation. Intervenor burden: reduces target-selection ambiguity. Rule beating: an authority profile cannot silently grant effects. Wrong goal: usable multi-repository work.",
      "removalBalance": "Unknown: the legacy Cost line does not quantify the addition or removed manual interpretation.",
      "failureBehavior": "Absent Repository metadata preserves self behavior. Unknown repositories and malformed authority refuse before activation, preventing work against the wrong target.",
      "findings": []
    },
    {
      "workOrderId": "WO-120",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate B's identity seam enables portfolio derivation and gate V composition. NoOp leaves runtime/UI work outside the existing lifecycle and index; cost is unmeasured.",
      "systemTraps": "Policy resistance: one lifecycle prevents parallel status systems. Commons: materialization replaces duplicated adapters. Drift: replay preserves identity. Escalation: draft intake does not auto-authorize work. Success to the successful: handwritten and derived work share contracts. Intervenor burden: durable identity removes restart rediscovery. Rule beating: a filed draft is not an activated order. Wrong goal: preserves operator intent through execution.",
      "removalBalance": "Unknown: the legacy line provides no measured removal, though it avoids a second work-order system.",
      "failureBehavior": "Ambiguous intake remains a draft; exhausted identity ranges refuse explicitly. Existing handwritten orders continue through the same commands.",
      "findings": []
    },
    {
      "workOrderId": "WO-063",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate H provides pre-publish artifact checks. NoOp retains manual outward-text and privacy review; no measured baseline is supplied.",
      "systemTraps": "Policy resistance: formatting rules can conflict with target conventions. Commons: a pure lint avoids repeated review. Drift: unavailable privacy screening is not a pass. Escalation: style refusals can cause rewriting. Success to the successful: conventional commits must remain a selected policy. Intervenor burden: automates deterministic checks. Rule beating: syntax does not establish safe or useful content. Wrong goal: protect delivery and privacy rather than enforce arbitrary style.",
      "removalBalance": "Unknown: the legacy Cost line does not show a larger removal.",
      "failureBehavior": "The linter refuses policy violations and labels a missing local list unavailable. It does not degrade privacy screening into a false pass; publish handling is a downstream responsibility.",
      "findings": [
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "A fixed conventional-commit policy may conflict with a registered target's legitimate publication policy. The subject does not show whether this lint is selectable at that boundary.",
          "evidence": null,
          "reopenWhen": "A target-compliant, authorized artifact is blocked solely because the generic lint imposes a conflicting style requirement."
        }
      ]
    },
    {
      "workOrderId": "WO-100",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate R enables the unattended hour and gate V's authorized intake. NoOp leaves discovery candidates needing manual scope and authority assignment; costs are unmeasured.",
      "systemTraps": "Policy resistance: portfolio ceilings reconcile candidate work and authority. Commons: episode, token and time budgets bound activity. Drift: phases advance only after verification. Escalation: outside work becomes a suggestion. Success to the successful: mechanics are selected policy. Intervenor burden: routine derivation becomes automatic. Rule beating: candidate existence is not authorization. Wrong goal: useful work within intent, not utilization.",
      "removalBalance": "Unknown: the legacy Cost line gives no comparison, although manual derivation is a clear intended replacement.",
      "failureBehavior": "Budget exhaustion yields NoOp; outside-portfolio work yields suggestions or NeedsHuman. No fallback widens authority.",
      "findings": []
    },
    {
      "workOrderId": "WO-064",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate H enables authorized delivery and the pull-request loop. NoOp leaves completed source changes requiring manual publication; cost is unmeasured.",
      "systemTraps": "Policy resistance: explicit grants separate local writing from remote effects. Commons: artifact generation reuses existing evidence. Drift: acceptance data drives publication text. Escalation: no merge or release authority is added. Success to the successful: title generation avoids privileged worker narrative. Intervenor burden: removes routine publishing steps. Rule beating: lint alone cannot supply the grant. Wrong goal: deliver verifiable work.",
      "removalBalance": "Unknown: no measured larger removal appears in the legacy Cost line.",
      "failureBehavior": "Missing grants or failed lint refuse before remote effects. This intentional authority boundary must not degrade into unapproved publication.",
      "findings": []
    },
    {
      "workOrderId": "WO-111",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate R's unattended-hour proof joins the external-change chain for R2. NoOp leaves progressive autonomous work and stop-on-return unproven live.",
      "systemTraps": "Policy resistance: verifies presence, mission checks and portfolio together. Commons: one bounded window and reported budget limit work. Drift: verified changes define advancement. Escalation: failures reset rather than expand effort. Success to the successful: scratch tasks test capability without claiming universal usefulness. Intervenor burden: interventions are observable. Rule beating: unchanged external surfaces and no remote effects are checked. Wrong goal: unattended useful completion.",
      "removalBalance": "Unknown: this proof has no measured savings claim.",
      "failureBehavior": "Return stops dispatch; verification failure resets the phase. Failed evidence prevents the claimed rung, while runtime repair remains separately scoped.",
      "findings": []
    },
    {
      "workOrderId": "WO-114",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate U exposes the resident to text and future graphical consumers. NoOp requires manual reconstruction of actors, holds and orders from logs; cost is unmeasured.",
      "systemTraps": "Policy resistance: a derived view avoids competing state. Commons: per-tick projection writes have a real cost. Drift: restart equality checks freshness. Escalation: no command authority is added. Success to the successful: multiple UI hosts consume one contract. Intervenor burden: clear status reduces polling and rediscovery. Rule beating: displayed activity is not completion. Wrong goal: legible authorized progress.",
      "removalBalance": "Unknown: the legacy Cost line does not compare projection overhead with avoided inspection.",
      "failureBehavior": "The projection can be rebuilt from the log. Atomic writes protect readers; behavior on persistent projection-write failure is not specified and cannot be inferred as either refusal or graceful degradation.",
      "findings": []
    },
    {
      "workOrderId": "WO-070",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate S enables a build-free exported control plane. NoOp retains duplicate module identities and skeleton-source dependence; costs are unmeasured.",
      "systemTraps": "Policy resistance: one module identity avoids divergent imports. Commons: shared leaves avoid redundant build dependencies. Drift: byte-identical Beacon fixtures preserve semantics. Escalation: no new Beacon design is added. Success to the successful: control-plane consumers can stand alone. Intervenor burden: removes packaging workarounds. Rule beating: source-absent fixtures prove portability. Wrong goal: externalizable functionality.",
      "removalBalance": "Architectural duplication is removed, but no larger measured cost removal is declared.",
      "failureBehavior": "Existing Beacon behavior must remain identical. No runtime fallback is added; ordinary rollback handles an unsuccessful relocation.",
      "findings": []
    },
    {
      "workOrderId": "WO-115",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate U enables console parity and the gate V product exit. NoOp leaves UI clients without a shared authorized command interface; costs are unmeasured.",
      "systemTraps": "Policy resistance: terminal and UI share authorization. Commons: one implementation avoids duplicate command machinery. Drift: byte/event parity is explicit. Escalation: absent commands are not invented. Success to the successful: text and graphical hosts are equal clients. Intervenor burden: reduces surface-specific troubleshooting. Rule beating: interface presence is insufficient without identical effects. Wrong goal: accessible operation under one authority path.",
      "removalBalance": "Unknown: the legacy line does not quantify avoided duplicate implementations.",
      "failureBehavior": "Terminal commands remain the underlying route when the UI surface is unavailable. Unauthorized callers and commands refuse; replay must reproduce records without repeating external effects.",
      "findings": [
        {
          "criterionId": "criterion:3",
          "kind": "known-issue",
          "reason": "Replay equality for effectful commands must mean reconstructing recorded results, not executing those effects again. The supplied criterion does not make that distinction explicit.",
          "evidence": null,
          "reopenWhen": "Replaying a recorded console invocation creates a second external or repository effect instead of reconstructing its observation."
        }
      ]
    },
    {
      "workOrderId": "WO-060",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate G supplies portable intake for contracts and source adapters. NoOp leaves downstream work without a screened, immutable source boundary; cost is unmeasured.",
      "systemTraps": "Policy resistance: one decoder and screen provide consistent input rules. Commons: content addressing avoids repeated source copies. Drift: resolving spans preserve provenance. Escalation: adapters remain separate. Success to the successful: sourceKind supports multiple origins. Intervenor burden: structured intake reduces manual reconstruction. Rule beating: a pattern screen is not universal secret detection. Wrong goal: usable intent with protected provenance.",
      "removalBalance": "Unknown: the legacy Cost line provides no comparison.",
      "failureBehavior": "Malformed or screened content refuses before storage. This protects privacy; falling back to unscreened intake would discard the boundary's purpose.",
      "findings": []
    },
    {
      "workOrderId": "WO-116",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate U makes audit accessible to the live console and gate V. NoOp requires separate access to canonical audit projections; costs are unmeasured.",
      "systemTraps": "Policy resistance: canonical audit remains the sole source. Commons: reuses existing projections. Drift: fidelity labels prevent overstated interpretation. Escalation: no new audit semantics are added. Success to the successful: any parity client can consume the result. Intervenor burden: reduces evidence navigation. Rule beating: rendering does not improve underlying evidence. Wrong goal: inspectable outcomes for unfamiliar users.",
      "removalBalance": "Unknown: the legacy line does not price accessibility gains against the serving layer.",
      "failureBehavior": "The underlying audit projection remains available as the baseline. Serving failure must not fabricate a complete audit; no contrary behavior is specified.",
      "findings": []
    },
    {
      "workOrderId": "WO-065",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate H enables post-delivery resolution in WO-066 and V. NoOp leaves pull-request state to manual polling and classification; costs are unmeasured.",
      "systemTraps": "Policy resistance: classifications separate human and automated review. Commons: identical observations append nothing. Drift: current head identity anchors state. Escalation: no separate daemon is introduced. Success to the successful: typed events keep the adapter replaceable. Intervenor burden: removes routine polling. Rule beating: local status cannot substitute for observed remote state. Wrong goal: delivery closure rather than observation volume.",
      "removalBalance": "Unknown: no measured larger removal is declared.",
      "failureBehavior": "Failed reads cannot establish resolved state; secret-bearing content refuses before storage. Manual observation remains possible, but stale results must remain distinguishable from fresh evidence.",
      "findings": []
    },
    {
      "workOrderId": "WO-117",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate U provides the operator-visible runtime and supports V's exit. NoOp leaves supported commands and projections without the witnessed live experience.",
      "systemTraps": "Policy resistance: one parity contract prevents UI authority drift. Commons: refresh costs belong to the client. Drift: live witnessed behavior complements fixtures. Escalation: no drag-and-drop scope is added. Success to the successful: the text host remains one of multiple possible clients. Intervenor burden: integrates status, action and audit. Rule beating: screenshots or transcripts alone do not prove unseen functionality. Wrong goal: strengthens the smallest useful loop.",
      "removalBalance": "Unknown: the legacy Cost line does not quantify reduced navigation.",
      "failureBehavior": "Terminal commands remain the underlying interface if the live console fails. Unsupported connections should remain visibly unavailable, not imply resident progress.",
      "findings": []
    },
    {
      "workOrderId": "WO-066",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate H completes the post-PR loop required by V. NoOp retains manual triage, repair, verification, push and re-observation; costs are unmeasured.",
      "systemTraps": "Policy resistance: human review remains outside automatic disposition. Commons: bounded repair limits episode cost. Drift: verification precedes push. Escalation: incorrect suggestions can be rejected. Success to the successful: reviewer origin does not make a suggestion correct. Intervenor burden: routine resolution is automated. Rule beating: flipping a resolved bit cannot pass. Wrong goal: actual observed closure rather than reply activity.",
      "removalBalance": "Unknown: the legacy line does not measure the removed handoff chain against added episodes.",
      "failureBehavior": "Outside-scope items yield NeedsHuman; failed verification prevents push. Persisted continuations resume without repeated publication, preserving manual handling for unresolved decisions.",
      "findings": []
    },
    {
      "workOrderId": "WO-057",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate F provides browser evidence for WO-059 and V. NoOp leaves installation, isolation, replay and cleanup assumptions untested; costs are unmeasured.",
      "systemTraps": "Policy resistance: environment observations precede adapter promises. Commons: browser probes consume bounded resources. Drift: actual screenshot behavior defines comparison rules. Escalation: no dependency lands during discovery. Success to the successful: tests operation without a privileged connected server. Intervenor burden: records reusable setup truth. Rule beating: process observation checks cleanup. Wrong goal: enables evidence for real visual criteria.",
      "removalBalance": "Unknown: the legacy line does not quantify avoided integration mistakes.",
      "failureBehavior": "Unavailable capabilities can be recorded without implementing the adapter. Unequal screenshots are observations, not grounds for inventing determinism.",
      "findings": []
    },
    {
      "workOrderId": "WO-058",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate F supplies visual/network witness rules for V. NoOp permits unsuitable witness types to appear sufficient; costs are unmeasured.",
      "systemTraps": "Policy resistance: typed witness rules clarify acceptance but broad error propagation can conflict with independent criteria. Commons: browser-free fixtures bound cost. Drift: missing evidence stays unverified. Escalation: unrelated errors could trigger extra repair. Success to the successful: DOM evidence is not privileged as universal proof. Intervenor burden: deterministic folding reduces judgment repetition. Rule beating: screenshots must bind to criteria. Wrong goal: correct claims rather than witness counts.",
      "removalBalance": "Unknown: the legacy Cost line does not quantify the benefit.",
      "failureBehavior": "Missing required witnesses yield unverified; negative witnesses cannot be relabeled by implementers. Existing behavior claims remain compatible.",
      "findings": [
        {
          "criterionId": "criterion:3",
          "kind": "known-issue",
          "reason": "A console error fails every criterion in its scenario without a stated causal distinction. This may be an intentional scenario contract, but its wider burden is unobserved.",
          "evidence": null,
          "reopenWhen": "A recorded unrelated or expected console error causes otherwise satisfied criteria to fail and generates unnecessary repair."
        }
      ]
    },
    {
      "workOrderId": "WO-059",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate F implements the browser port consumed by V. NoOp leaves visual and network claims without independent runtime witnesses; costs are unmeasured.",
      "systemTraps": "Policy resistance: witness contracts isolate adapter specifics. Commons: browser and dependency costs stay outside the kernel. Drift: saved scenarios compare reproducible evidence. Escalation: one pinned dependency bounds expansion. Success to the successful: no connected server is required. Intervenor burden: automated capture and cleanup replace manual collection. Rule beating: console failures remain negative. Wrong goal: evidence for application behavior.",
      "removalBalance": "Unknown: the legacy Cost line does not establish a larger removal; the adapter is a product capability.",
      "failureBehavior": "Browser failure cannot produce verified witnesses. Kill recovery cleans up processes; unavailable evidence remains unverified while non-browser contracts remain usable.",
      "findings": []
    },
    {
      "workOrderId": "WO-061",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate G turns source artifacts into contracts consumed by V. NoOp retains manual interpretation and revision tracking; costs are unmeasured.",
      "systemTraps": "Policy resistance: explicit supersession reconciles reversals. Commons: selective invalidation avoids restarting unrelated work. Drift: source spans preserve the acceptance basis. Escalation: ambiguity remains open rather than generating assumptions. Success to the successful: inferred statements cannot overwrite rules silently. Intervenor burden: durable understanding reduces restatement. Rule beating: model inference is never relabeled deterministic. Wrong goal: product lead intent survives lowering.",
      "removalBalance": "Unknown: no comparative measurement appears in the legacy line.",
      "failureBehavior": "Unresolved interpretation stays open; invalid inference refuses. Valid unchanged source retains its derived items rather than forcing whole-contract regeneration.",
      "findings": []
    },
    {
      "workOrderId": "WO-124",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate G enables bounded implementation orders in V. NoOp requires repeated manual surface/test selection; costs are unmeasured.",
      "systemTraps": "Policy resistance: profile and contract evidence share one derivation. Commons: scoped tests avoid indiscriminate work. Drift: unresolved paths do not become guesses. Escalation: confidence handoff can create operator work and must remain material. Success to the successful: supplied inference stays labeled. Intervenor burden: automates resolvable cases. Rule beating: confidence alone is not correctness. Wrong goal: express intended change within authority.",
      "removalBalance": "Unknown: the legacy line does not compare automated derivation and handoffs with the manual baseline.",
      "failureBehavior": "Low confidence yields NeedsHuman with candidates, preserving a manual decision path. It does not dispatch an invented scope.",
      "findings": []
    },
    {
      "workOrderId": "WO-062",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate G supplies the first external source adapter for V. NoOp leaves issue intake manual; costs are unmeasured.",
      "systemTraps": "Policy resistance: one SourceBundle boundary separates tracker behavior. Commons: stable hashes avoid duplicate storage. Drift: revisions retain source identity. Escalation: the adapter remains read-only. Success to the successful: GitHub is a port implementation, not kernel doctrine. Intervenor burden: removes copy/paste intake. Rule beating: screening precedes persistence. Wrong goal: usable intent rather than connector coverage.",
      "removalBalance": "Unknown: no larger measured removal is declared.",
      "failureBehavior": "Fetch or screening failure cannot produce a trusted bundle. Existing stored bundles retain their identities; failure must not masquerade as a fresh unchanged source.",
      "findings": []
    },
    {
      "workOrderId": "WO-123",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate V composes the existing primitives into one intent-to-delivery continuation. NoOp leaves the operator coordinating each primitive manually; the cost is unmeasured.",
      "systemTraps": "Policy resistance: one authority-checked admission joins existing contracts. Commons: persisted steps prevent duplicate work. Drift: terminal outcomes require each receipt. Escalation: failures stop at named steps. Success to the successful: command and resident entries share implementation. Intervenor burden: removes ordinary phase handoffs. Rule beating: a draft cannot stand in for authorization. Wrong goal: delivers the mission's smallest complete loop.",
      "removalBalance": "Unknown quantitatively, but the objective names a substantial procedural replacement without adding a separate primitive.",
      "failureBehavior": "Ambiguous or unauthorized intents remain drafts or NeedsHuman. Named step failures stop safely; individual primitives remain available for authorized manual operation.",
      "findings": []
    },
    {
      "workOrderId": "WO-112",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate V proves the integrated source-to-deliverable loop from core. NoOp leaves component success without demonstrated replacement of the predecessor workflow.",
      "systemTraps": "Policy resistance: exercises the complete authority and evidence chain. Commons: records context, tokens, time and cost. Drift: item-by-item parity prevents weakened standards. Escalation: repairs remain bounded by prior mechanisms. Success to the successful: compares against an actual predecessor baseline. Intervenor burden: interventions and touch time are measured. Rule beating: every representative item must be observed-met. Wrong goal: verified delivery rather than activity.",
      "removalBalance": "The legacy Cost line is unknown; this order is where the proposed replacement's comparative benefits are measured.",
      "failureBehavior": "Unknown or unmet parity remains unverified or failed. No runtime fixes are smuggled into the proof, and the prior workflow remains the practical baseline.",
      "findings": []
    },
    {
      "workOrderId": "WO-074",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate S enables a starter instance consumed by V and P. NoOp leaves the control plane tied to this repository; costs are unmeasured.",
      "systemTraps": "Policy resistance: export portability conflicts with inherited version refusals. Commons: manifest-listed contents bound the kit. Drift: in-place suites verify usable output. Escalation: discovery chores can burden fresh users. Success to the successful: local/offline distribution avoids marketplace dependence. Intervenor burden: templates remove setup reconstruction. Rule beating: file export alone does not prove a working instance. Wrong goal: a stranger should obtain a usable platform.",
      "removalBalance": "Unknown: the legacy line does not quantify avoided bootstrap work.",
      "failureBehavior": "Nonempty destinations refuse to protect user files. The inherited unknown-version attestation refusal does not gracefully degrade and conflicts with WO-132's later advisory treatment.",
      "findings": [
        {
          "criterionId": "criterion:5",
          "kind": "known-issue",
          "reason": "The export requires a version refusal that WO-132 removes from the source workflow. This is unresolved horizon residue, not evidence that a produced export has already failed.",
          "evidence": null,
          "reopenWhen": "A generated starter rejects an otherwise authorized completion solely because its installed harness version lacks a discovery row."
        }
      ]
    },
    {
      "workOrderId": "WO-075",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate S makes the starter executable and enables the instance proof in V. NoOp exports control-plane files without the required runtime and governed bundle.",
      "systemTraps": "Policy resistance: runtime pins and bundle checks align. Commons: directed-load totals cannot exceed core. Drift: byte identity and smoke tests define the baseline. Escalation: no package publication is introduced. Success to the successful: local distribution remains complete. Intervenor burden: consumers need not rebuild the platform from session knowledge. Rule beating: a denied-effect smoke checks actual enforcement. Wrong goal: usable externalized capability.",
      "removalBalance": "Unknown: the legacy line does not quantify setup work removed by the bundled runtime.",
      "failureBehavior": "Drift refuses harness validation; existing source operation remains unaffected. An unavailable exported runtime cannot truthfully claim the governed profile works.",
      "findings": []
    },
    {
      "workOrderId": "WO-072",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate S enables governed target worktrees and gate P's real consumer. NoOp leaves target lifecycle and report routing manual; costs are unmeasured.",
      "systemTraps": "Policy resistance: explicit launchpad selection prevents split control state. Commons: a local registry avoids repeated discovery. Drift: target cleanliness and no-release behavior are verified. Escalation: publication remains separate. Success to the successful: registered targets use the same lifecycle. Intervenor burden: standard handoffs replace remembered paths. Rule beating: ignored files must also stay out of commits. Wrong goal: safe cross-repository work.",
      "removalBalance": "Unknown: legacy Cost supplies no measured comparison.",
      "failureBehavior": "Missing or ambiguous target context must not select another repository. Cleanup is limited by containment; existing self-repository commands retain their route.",
      "findings": []
    },
    {
      "workOrderId": "WO-073",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate S supplies on-demand repository knowledge for target work and gate P. NoOp requires repeated repository priming; its cost is unmeasured.",
      "systemTraps": "Policy resistance: explicit policy layers expose conflicts. Commons: profiles load only for active target work. Drift: demonstrated architecture replaces guesses. Escalation: missing profiles can require setup, but no ambient cold-start expansion occurs. Success to the successful: classes are chosen compositions. Intervenor burden: durable commands reduce restatement. Rule beating: profile presence is not proof of correctness. Wrong goal: the engineer's authored build carries behavior.",
      "removalBalance": "Unknown: unchanged cold-start totals constrain addition, but the legacy line does not measure removed priming.",
      "failureBehavior": "A missing required target profile refuses activation with its path. Existing non-target workflows remain usable; policy specificity cannot supersede the authority floor.",
      "findings": []
    },
    {
      "workOrderId": "WO-076",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate S enables instance-owned builds and V's starter proof. NoOp leaves consumers editing the shipped Contributor build directly; costs are unmeasured.",
      "systemTraps": "Policy resistance: overlays make instance choices explicit. Commons: reuse avoids copying entire builds. Drift: manifest differences are bounded. Escalation: build:none provides a minimal option. Success to the successful: bundled identity and units can be removed. Intervenor burden: re-emission automates customization. Rule beating: widening still requires admitted provenance. Wrong goal: platform sovereignty rather than prescribed organization.",
      "removalBalance": "Unknown: the legacy line does not price avoided fork maintenance.",
      "failureBehavior": "Absent customization preserves the kit build; build:none intentionally emits the floor. Invalid widening refuses while valid narrowing remains available.",
      "findings": []
    },
    {
      "workOrderId": "WO-077",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate S sustains starter instances after initial export. NoOp requires manual comparison and update of kit files; costs are unmeasured.",
      "systemTraps": "Policy resistance: manifest ownership separates kit and instance edits. Commons: selective updates avoid copying everything. Drift: pinned upstream identity remains inspectable. Escalation: mechanical actions require explicit opt-in. Success to the successful: local modifications remain protected. Intervenor burden: automates safe updates and prints needed actions. Rule beating: rewriting a manifest cannot justify overwriting authored files. Wrong goal: maintainable independent instances.",
      "removalBalance": "Unknown: the legacy Cost line does not quantify the manual update baseline.",
      "failureBehavior": "Modified kit files are retained and reported; instance files remain unchanged. Missing manifests refuse because ownership is unknown. Resident continuity is explicitly tested.",
      "findings": []
    },
    {
      "workOrderId": "WO-078",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate S supplies export provenance for the starter and consumer in P. NoOp requires one checkout lookup per sibling whenever versions or builds must be established.",
      "systemTraps": "Policy resistance: generated rows avoid competing bookkeeping. Commons: small receipts trade disk for repeated reads. Drift: current receipt hashes expose stale entries. Escalation: no separate export step is added. Success to the successful: entries include not-evidenced states. Intervenor burden: removes remembered sibling versions. Rule beating: registry presence cannot certify a consumer run. Wrong goal: useful provenance rather than registry completeness.",
      "removalBalance": "A concrete manual removal is named, but its magnitude versus generator and check cost remains to be measured.",
      "failureBehavior": "A stale render refuses its check; receipts and manifests remain the underlying evidence. The criteria do not require preventing otherwise valid export solely because presentation is stale.",
      "findings": []
    },
    {
      "workOrderId": "WO-118",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate V is the resident-owned product exit leading to R3. NoOp leaves the system dependent on manual phase prompting despite component proofs.",
      "systemTraps": "Policy resistance: exercises all authority and handoff boundaries together. Commons: measures full-loop expenditure. Drift: strict representative outcomes prevent diluted replacement claims. Escalation: only material decisions return to humans. Success to the successful: exported instances must work beyond the author's checkout. Intervenor burden: ordinary coordination must disappear. Rule beating: actor death and restart test actual durability. Wrong goal: one intent reaching verifiable delivery.",
      "removalBalance": "Unknown in the legacy Cost line; the order measures whether the proposed workflow actually removes supervision.",
      "failureBehavior": "Ambiguous control intent yields NeedsHuman; ordinary failures recover under the resident. Unmet replacement criteria remain failed rather than being relabeled as successful partial automation.",
      "findings": []
    },
    {
      "workOrderId": "WO-113",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate B stabilizes authority files consumed by WO-120 and the lifecycle. NoOp mixes contracts, state and receipts, increasing repeated interpretation; cost is unmeasured.",
      "systemTraps": "Policy resistance: separate surfaces clarify ownership. Commons: smaller contracts reduce repeated reads. Drift: historical bytes remain preserved. Escalation: syntactic bans can create relocation chores. Success to the successful: existing history is grandfathered. Intervenor burden: machine-kept state replaces manual edits. Rule beating: section shape is not semantic stability. Wrong goal: legible authority rather than immaculate filing.",
      "removalBalance": "Unknown: the legacy line does not show that the check and migration cost are smaller than removed reading and maintenance.",
      "failureBehavior": "New prohibited sections refuse the index check; there is no advisory fallback. Existing closed records remain intact.",
      "findings": [
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "Date- and heading-based detection can confuse a stable contract's dated evidence or assumptions with a lifecycle receipt.",
          "evidence": null,
          "reopenWhen": "A legitimate stable-contract provenance statement is rejected solely because it contains a date or matching heading."
        }
      ]
    },
    {
      "workOrderId": "WO-080",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate P represents outcomes spanning repositories. NoOp leaves member compatibility and staleness to manual coordination; costs are unmeasured.",
      "systemTraps": "Policy resistance: member state remains independently authoritative. Commons: one grouping reuses existing headers and logs. Drift: stale bases stay visible. Escalation: no new event schema is introduced. Success to the successful: no repository's green status dominates siblings. Intervenor burden: reduces cross-repository bookkeeping. Rule beating: grouped green cannot erase a stale member. Wrong goal: outcome delivery rather than individual order throughput.",
      "removalBalance": "Unknown: the legacy Cost line does not quantify the coordination removed.",
      "failureBehavior": "Stale or failing members remain visible independently. Existing order-level information remains usable when grouping is absent.",
      "findings": []
    },
    {
      "workOrderId": "WO-081",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate P exposes workstream coordination in the board. NoOp leaves the index as the only grouped view; costs are unmeasured.",
      "systemTraps": "Policy resistance: board and index share data. Commons: one additive projection avoids duplicated state. Drift: existing panels remain unchanged. Escalation: no new workflow authority is added. Success to the successful: older capsules still render. Intervenor burden: reduces navigation among member orders. Rule beating: visual grouping does not prove delivery. Wrong goal: showrunner clarity.",
      "removalBalance": "Unknown: no larger measured removal appears in the legacy Cost line.",
      "failureBehavior": "Yes: capsules without the extension still render. Failure of the new view does not change underlying member status.",
      "findings": []
    },
    {
      "workOrderId": "WO-082",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Gate P qualifies multi-repository work before the operator's real run. NoOp leaves six coordination behaviors unproven outside isolated components.",
      "systemTraps": "Policy resistance: collisions and independent progress are tested together. Commons: synthetic repositories bound experiment cost. Drift: stale claims cannot inherit green status. Escalation: blocked edges name manual handoffs. Success to the successful: pinned contracts allow independent members to proceed. Intervenor burden: restart behavior reduces re-priming. Rule beating: six demonstrations require actual state transitions. Wrong goal: coordinated delivery.",
      "removalBalance": "Unknown: this is an evidence-producing pilot with no quantified savings declaration.",
      "failureBehavior": "Unavailable edges expose a manual route while unrelated members continue. The precise scope of the collision refusal needs to remain the worktree, as in the shared writer rule.",
      "findings": [
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "The objective describes collision on one repository, whereas writer isolation is one writer per worktree. The fixture must distinguish an actual shared-worktree conflict from safe sibling worktrees.",
          "evidence": null,
          "reopenWhen": "The pilot or implementation refuses independent writers in distinct worktrees solely because they belong to the same repository."
        }
      ]
    },
    {
      "workOrderId": "WO-083",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Gate P proves the platform in the operator's own consumer after the starter exit. NoOp leaves real workflow replacement and priming reduction unobserved.",
      "systemTraps": "Policy resistance: the fork's emitted build must govern the actual run. Commons: elapsed phases and interventions are counted. Drift: baselines are pinned before activation. Escalation: every manual handoff remains visible. Success to the successful: author-checkout success is insufficient. Intervenor burden: zero vision restatements tests priming. Rule beating: unknown baseline cannot pass. Wrong goal: demonstrated user benefit.",
      "removalBalance": "Unknown before execution; independent baseline comparisons are the intended evidence of removal.",
      "failureBehavior": "Missing baseline yields unverified, and non-improving measures fail their claim. The prior workflow remains the comparison rather than being declared obsolete without evidence.",
      "findings": []
    },
    {
      "workOrderId": "WO-096",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred migration support after the first external change, enabling WO-097/098. NoOp retains uncertain shape coverage and always-on prose; costs are unmeasured.",
      "systemTraps": "Policy resistance: one derived classification avoids competing inventories. Commons: whole-set enumeration has bounded but real cost. Drift: pinned denominator prevents denominator drift. Escalation: no mechanisms are compiled here. Success to the successful: exclusions and reference status remain legitimate outcomes. Intervenor burden: reusable classification reduces rediscovery. Rule beating: counts cannot substitute for behavior. Wrong goal: selective context reduction, not migration volume.",
      "removalBalance": "Unknown: legacy Cost does not quantify inventory work against future savings.",
      "failureBehavior": "Missing local terms yields unavailable rather than a clean privacy claim. Classification failures prevent a misleading render; existing mechanisms remain usable.",
      "findings": []
    },
    {
      "workOrderId": "WO-097",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred migration work supports the compiled-rules thesis after the product bottleneck. NoOp retains the six selected always-on sentences; byte costs await selection and measurement.",
      "systemTraps": "Policy resistance: mechanisms must cover the retired rule's behavior. Commons: fewer prompt bytes trade against hook execution. Drift: regression fixtures preserve useful corrections. Escalation: six new mechanisms can add maintenance. Success to the successful: duplicates are prohibited. Intervenor burden: checks aim to remove repeated correction. Rule beating: sentence removal alone is insufficient. Wrong goal: cheaper dependable behavior rather than unit count.",
      "removalBalance": "Not established by the legacy Cost line. Instruction-byte comparisons provide partial evidence, not total runtime/process savings.",
      "failureBehavior": "The criteria prove coverage when mechanisms are present, but do not specify each mechanism's unavailable-state fallback. Failure behavior must be judged for the selected units, not presumed safe.",
      "findings": []
    },
    {
      "workOrderId": "WO-098",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred migration completes a representative batch and measures directed-load reduction. NoOp retains the remaining selected prose and lacks whole-set evidence.",
      "systemTraps": "Policy resistance: reverse mappings preserve correction intent. Commons: every role must load fewer bytes, while cadence cost remains relevant. Drift: restated rules revert to prose status. Escalation: batch scope limits migration expansion. Success to the successful: references remain valid alternatives. Intervenor burden: fewer restatements are tested. Rule beating: unit counts cannot prove reduced supervision. Wrong goal: useful compiled behavior at lower total cost.",
      "removalBalance": "The acceptance text requires a real context reduction, but the legacy Cost line does not establish total additions versus removals.",
      "failureBehavior": "A failed behavioral migration is labeled prose again rather than falsely counted as a mechanism. Individual hook/cadence failures still need their own contracts.",
      "findings": []
    },
    {
      "workOrderId": "WO-091",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred workshop work enables richer compositions after the first external change. NoOp retains single-active groups; its opportunity cost is unmeasured.",
      "systemTraps": "Policy resistance: authority and precedence remain per active. Commons: shared supports avoid duplicated loadouts. Drift: old hashes remain exact. Escalation: six-link overflow is a decomposition diagnostic. Success to the successful: multiple active mechanics become possible. Intervenor burden: explicit pipelines replace guessed interactions. Rule beating: compiling alone cannot prove intended behavior. Wrong goal: engineer-authored composition, not metaphor-driven kernel growth.",
      "removalBalance": "Unknown: this is a new authoring capability with no measured larger removal.",
      "failureBehavior": "Single-active programs retain existing semantics. Noncommuting compositions require an explicit pipeline; overflow is described as a diagnostic rather than a universal refusal.",
      "findings": []
    },
    {
      "workOrderId": "WO-092",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred workshop extension enables WO-094's set bonuses. NoOp lacks a declarative set relationship; costs are unmeasured.",
      "systemTraps": "Policy resistance: bonuses use existing emission kinds. Commons: shared graph data avoids view-specific implementations. Drift: absent sets preserve hashes. Escalation: fixture sets bound scope. Success to the successful: no mandatory 5S doctrine enters here. Intervenor burden: inspection shows arming conditions. Rule beating: round-trip equality must preserve mechanics. Wrong goal: meaningful composition rather than decorative metaphor.",
      "removalBalance": "Unknown: the legacy Cost line names no measured replacement.",
      "failureBehavior": "Yes for absence: sets normalize to empty and old graphs remain unchanged. Bonuses below threshold stay dark.",
      "findings": []
    },
    {
      "workOrderId": "WO-093",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred workshop data supplies the 5S example set. NoOp leaves five mechanics uncompiled; no measured cost is supplied.",
      "systemTraps": "Policy resistance: terms lower into explicit envelopes and seeds. Commons: reuse of existing definitions avoids duplication. Drift: divergences are recorded. Escalation: bonuses and runtime scenarios stay separate. Success to the successful: examples remain optional compositions. Intervenor burden: authored mechanics reduce repeated mapping. Rule beating: tooltip completeness is not behavioral proof. Wrong goal: exact authoring semantics rather than ontology by resemblance.",
      "removalBalance": "Unknown: the legacy line does not quantify reused definitions or avoided prompting.",
      "failureBehavior": "Unequipped mechanics leave existing programs unchanged. Compilation alone does not claim runtime behavior; later scenarios provide that evidence.",
      "findings": []
    },
    {
      "workOrderId": "WO-094",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred workshop lowering enables the full-set scenario. NoOp leaves bonuses descriptive rather than executable; costs are unmeasured.",
      "systemTraps": "Policy resistance: bonus emissions remain within authority. Commons: integrity checks and reevaluation add recurring compute. Drift: bonuses arm only at explicit thresholds. Escalation: reevaluation is bounded. Success to the successful: the optional set does not prescribe all builds. Intervenor burden: standardization can remove repeated repair. Rule beating: semantic hashes change only for armed effects. Wrong goal: useful behavior rather than piece collection.",
      "removalBalance": "Unknown: the legacy Cost line does not compare recurring verification/cadence costs with avoided repair.",
      "failureBehavior": "Missing pieces leave bonuses dark. Destructive actions intentionally refuse without the selected safety conditions; old unmodified loadouts remain unaffected.",
      "findings": []
    },
    {
      "workOrderId": "WO-095",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred workshop proof demonstrates the compiled set without advancing it ahead of the work-platform exit. NoOp leaves combined behavior and inspection unproven.",
      "systemTraps": "Policy resistance: one scenario tests bonus interactions. Commons: deterministic fakes bound resource cost. Drift: complete Decisions and the old oracle remain equal. Escalation: no graphical authoring is added. Success to the successful: the existing scenario remains supported. Intervenor burden: visible bonuses aid inspection. Rule beating: rendered hashes alone are supplemented by behavior. Wrong goal: tester evidence for the authored composition.",
      "removalBalance": "Unknown: the legacy line makes no quantified removal claim.",
      "failureBehavior": "The prior scenario remains unchanged. Return cancels the bounded cadence, and destructive authority stays refused rather than inferred from the full set.",
      "findings": []
    },
    {
      "workOrderId": "WO-085",
      "verdict": "aligned-with-findings",
      "criticalPathAndNoOp": "Deferred documentation support for clearer product authority. NoOp retains embedded receipts, banners and broken-link risk; the reading cost is unmeasured.",
      "systemTraps": "Policy resistance: receipt separation can conflict with legitimate capability evidence. Commons: smaller product documents reduce reading. Drift: moved text is preserved exactly. Escalation: broad bans may create filing chores. Success to the successful: evidence remains linked rather than discarded. Intervenor burden: navigation improves only if pointers remain usable. Rule beating: clean formatting is not current product truth. Wrong goal: understandable specifications.",
      "removalBalance": "Unknown: the legacy Cost line does not compare removed reading with migration and recurring checks.",
      "failureBehavior": "The proposed checker refuses new forbidden text and broken references. No advisory fallback is specified, although evidence remains available at preserved destinations.",
      "findings": [
        {
          "criterionId": "criterion:1",
          "kind": "known-issue",
          "reason": "A blanket dated-receipt detector must remain compatible with legitimate dated capability additions and reassessments accepted by WO-135.",
          "evidence": null,
          "reopenWhen": "A verified capability write-back is rejected by docs-check solely because its evidence is dated."
        }
      ]
    },
    {
      "workOrderId": "WO-086",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred release-document maintenance. NoOp retains hand-kept retiming layers and duplicated release history; costs are unmeasured.",
      "systemTraps": "Policy resistance: tags and headers become one source. Commons: generation replaces repeated table edits. Drift: stale marker checks expose divergence. Escalation: no new release policy is introduced. Success to the successful: historical retiming remains preserved. Intervenor burden: removes manual joins. Rule beating: table freshness does not prove release quality. Wrong goal: legible release history for the showrunner.",
      "removalBalance": "The objective names manual maintenance removed, but the legacy Cost line provides no comparative measurement.",
      "failureBehavior": "A stale table refuses its check; annotated tags and preserved records remain available. No lifecycle refusal beyond presentation checking is established by the subject.",
      "findings": []
    },
    {
      "workOrderId": "WO-087",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred roadmap organization. NoOp keeps candidate policies mixed with release rungs; the navigation cost is unmeasured.",
      "systemTraps": "Policy resistance: retained slugs limit link disruption. Commons: focused documents reduce irrelevant reads. Drift: text remains unchanged. Escalation: this is a bounded move without new policy. Success to the successful: candidates retain their status. Intervenor burden: clearer navigation reduces rediscovery. Rule beating: a split alone does not prove lower directed load. Wrong goal: an understandable ladder rather than more documents.",
      "removalBalance": "Unknown: the legacy line does not measure navigation or context reduction.",
      "failureBehavior": "This is a reversible document relocation. Preserved text and checked links retain the old information; no new execution refusal is introduced.",
      "findings": []
    },
    {
      "workOrderId": "WO-088",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred documentation consistency for the operator command surface. NoOp retains multiple hand-maintained phrase tables; cost is unmeasured.",
      "systemTraps": "Policy resistance: one emitted table prevents divergent instructions. Commons: repeated edits become generation. Drift: stale copies are detectable. Escalation: no new phrase or workflow is introduced. Success to the successful: consumers share the same command source. Intervenor burden: reduces command-spelling diagnosis. Rule beating: byte equality cannot certify command usability. Wrong goal: accessible ordinary operation.",
      "removalBalance": "Manual duplication is removed in the objective, but a larger cost removal is not established by the legacy line.",
      "failureBehavior": "Stale-copy validation refuses its check. The command implementation remains the source of truth, and existing phrases do not change.",
      "findings": []
    },
    {
      "workOrderId": "WO-089",
      "verdict": "aligned",
      "criticalPathAndNoOp": "Deferred capability-truth maintenance. NoOp retains duplicate addenda and the missing verification row; costs are unmeasured.",
      "systemTraps": "Policy resistance: one row per capability reconciles updates. Commons: less repeated history reading. Drift: promotions require cited passing evidence. Escalation: no new capability work is added. Success to the successful: old levels are not automatically promoted. Intervenor burden: reduces reconstruction of current status. Rule beating: table consolidation cannot create evidence. Wrong goal: truthful product understanding.",
      "removalBalance": "Unknown: the legacy Cost line does not price the one-time consolidation or subsequent reading savings.",
      "failureBehavior": "Unsupported levels stay where they were, and historical assessments remain preserved. Failure to substantiate promotion does not erase prior evidence.",
      "findings": []
    }
  ],
  "planVerdict": "aligned-with-findings",
  "holdReasons": []
}
```

## Addressed holds

```json
[]
```

## Dispositions carried from earlier receipts

```json
[]
```

Only evidence-backed misalignment holds. Criterion-text-bound dispositions in the planning control log settle findings without another judgment; known issues name reopening observations. This receipt and all historical receipts remain immutable.

Receipt hash: `sha256:199ca32a514749e6c900c8a67a1bf90cc7eb9ba57047fa6d48afe457a3add15d`.
