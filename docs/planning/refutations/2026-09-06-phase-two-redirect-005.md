# Plan refutation — 2026-09-06 phase-two redirect (receipt 005, manual, pre-mechanism)

**Disclosure.** As in the earlier receipts: a manual blinded dispatch through
the planning session's harness, the same reading list as receipt 004, the
plan, the ledger, the earlier receipts, and every planning narrative
excluded, the fixed verdict rules supplied. The subject was the working tree
after receipt 004's dispositions, committed unchanged with this receipt. The
planning session wrote the disposition block; the self-referential
instrument is disclosed. The refuter's verdict on WO-041 itself is advisory.

**Correction to receipt 004.** Receipt 004 said this run would be the last of
the pass and that a hold would stay open. This run held on two exact
criterion defects, and the planning session accepted and changed both
rather than leaving them open, because the rule the orders now carry says an
accepted change requires a fresh receipt and because both fixes are one
criterion each. Receipt 006 is the run that rule requires and is final for
this pass: any hold it returns stays open for the operator's control-log
override or the next pass, with no further change by the planner.

| Field                            | Value                                                                                         |
| -------------------------------- | --------------------------------------------------------------------------------------------- |
| Subject                          | the marked sequence block and the ten order files it names, after receipt 004's dispositions  |
| Subject hash (as refuted)        | `sha256:121b548779367dfffd96c448a657fff70949e131974f0471ac5678430b5ca0b9`                     |
| Subject hash (after disposition) | `sha256:b52dceb84feb69115c7fb34379183ae9ab0fb9ad7a9dcde7b31518b257f30484`                     |
| Transport                        | manual dispatch, Claude Code Agent tool (fresh general-purpose session; no DotLn transport)   |
| Harness                          | Claude Code 2.1.263                                                                           |
| Model, effort                    | claude-fable-5-1, max                                                                         |
| Dispatched                       | 2026-09-07T01:50Z (2026-09-06 in the operator's day)                                          |
| Verdict                          | **hold**, two reasons, four open self-certification risks, one open clean-room item, no drift |

## Result (verbatim JSON in the sibling file)

Seven orders thesis-advancing (WO-032, WO-033, WO-034, WO-037, WO-039,
WO-040, WO-041 advisory), three machinery (WO-035, WO-036, WO-038), none
drift. The refuter's rationale, quoted:

> The horizon does move the vision. Seven of ten orders are
> thesis-advancing; the one-paragraph story is the best-served thesis
> (WO-039 and WO-040 carry a mechanically derived, directional cold-start
> reduction pinned to the activation base, with sentence retirement proven
> by hash and a reverse-mapping fixture), the differentiated interface gets
> its first fully compiled shelf entry (WO-037), and Horizon 1 gets fixtures
> for the multi-repository outcome (WO-034) plus a starter that ships an
> enforced build the fork can unequip or omit (WO-033). No order drifts.
>
> I hold on two exact criterion defects. WO-034 criterion 4 presents
> baseline counts as "observed" when they are computed over the operator's
> private records, committed only as SHA-256 names; from committed evidence
> they are an attestation, and the order does not label them as one the way
> WO-040 labels its denominator. [...] WO-032 criterion 2 pins only two model
> sessions as actor rows; nothing requires a person to appear as an actor,
> so the "user interface for actors" can close as a model-session view plus
> two dashboard panels, with the human occupant of the core bet's actor slot
> unevidenced.
>
> Open risks short of a hold: WO-039's reduction is measured only before a
> session's first effect, WO-040's mechanism label is proven by one
> sentence's absence, WO-041's gate can be re-rolled, and no order says the
> local-terms refusal output avoids echoing the matched term into committed
> transcripts.

## Disposition (2026-09-06, planning session; every hold accepted and changed)

1. **WO-034 criterion 4: baseline counts over private records are called
   observed; no counting rule.** Accepted; changed. The baseline half is
   labeled an operator attestation in the receipt and never called
   observed; a pinned counting rule defines each of the four measures, and
   the independent verifier applies it to the committed sanitized
   transcripts and control log for the run-side counts.
2. **WO-032 criterion 2: no human actor row is required.** Accepted;
   changed. At least one person appears as an actor from the control log's
   recorded roles under the WO-031 actor attribution, pinned as a fixture
   row with role, last recorded action, and evidence links; a board with no
   human row in that fixture fails the criterion.

Open self-certification risks: accepted; changed. WO-039's read-observing
hook now runs for the whole smoke session and the directed-read set counts
every read the skill directs anywhere in the role's procedure; WO-040's
`mechanism` label now requires the independent verifier's recorded search
of the whole always-on set for any restatement, with a found restatement
reverting the shape to `prose`; WO-041's gate now admits a fresh receipt
only when it dispositions every prior hold and its subject changed inside a
held criterion, refuses a subject changed only elsewhere, and stops the loop
after the third consecutive hold in one pass with the holds left open.

Open clean-room item: accepted; changed. The local-terms check's refusal
output prints only file, line, and count, never the matched text.

**Largest gap.** Unchanged, named, and by the rules not a hold.
