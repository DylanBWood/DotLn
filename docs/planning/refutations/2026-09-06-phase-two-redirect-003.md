# Plan refutation — 2026-09-06 phase-two redirect (receipt 003, manual, pre-mechanism)

**Disclosure.** As in receipts 001 and 002: a manual blinded dispatch through
the planning session's harness, the same reading list including `CLAUDE.md`,
the plan, the ledger, the earlier receipts, and every planning narrative
excluded, and the fixed verdict rules supplied. The subject was the working
tree after receipt 002's dispositions, committed unchanged with this receipt.
The planning session wrote the disposition block; the self-referential
instrument is disclosed. The refuter's verdict on WO-041 itself is advisory.

| Field                            | Value                                                                                           |
| -------------------------------- | ----------------------------------------------------------------------------------------------- |
| Subject                          | the marked sequence block and the ten order files it names, after receipt 002's dispositions    |
| Subject hash (as refuted)        | `sha256:58c5652c1d4a88fc1c47d464a157cc8ff7fec0cbe0f4d023398a063d76d19e6f`                       |
| Subject hash (after disposition) | `sha256:7519af4190b44f80d5f36e1076998e91eba05748c0db9e8d7c2317b317d49580`                       |
| Transport                        | manual dispatch, Claude Code Agent tool (fresh general-purpose session; no DotLn transport)     |
| Harness                          | Claude Code 2.1.263                                                                             |
| Model, effort                    | claude-fable-5-1, max                                                                           |
| Dispatched                       | 2026-09-07T01:15Z (2026-09-06 in the operator's day)                                            |
| Verdict                          | **hold**, two reasons, two open self-certification risks, three open clean-room items, no drift |

## Result (verbatim JSON in the sibling file)

Six orders thesis-advancing (WO-032, WO-033, WO-034, WO-037, WO-039,
WO-040), four machinery (WO-035, WO-036, WO-038, WO-041), none drift. The
refuter's rationale, quoted:

> The horizon moves the vision: six orders are thesis-advancing, four are
> machinery, none drifts. WO-039 is the load-bearing one [...]; WO-040 gives
> the one-paragraph story its first count with a required direction; WO-037
> is the first full shelf entry; WO-032 is a genuine actor view rather than
> only a dashboard, though two panels are repository process. The
> prescribed-loadout trap is answered (`build: none`, overlay fixtures), and
> the prose-with-no-required-reduction trap is answered in principle by
> WO-039 crit 6 and WO-040 crit 4.
>
> The hold rests on two exact measurement defects, both one-sentence fixes.
> WO-039 crit 6 defines the after value as instruction bytes plus skill bytes
> plus references "the skill instructs", a guide read still directed by the
> hand-written CLAUDE.md floor is not in that sum, and crit 7 leaves the
> cold-start read order in place unless the (thus-biased) measurement
> shortens it. WO-035 crit 5 counts one file's lines and is met by relocation
> with pointers; nothing in that order measures the cold-start total its own
> objective names. Both are the predecessor's failure mode measured on the
> wrong denominator, which is the vision passage most at stake in this
> horizon.
>
> Open clean-room items: WO-039 names the local-terms check but no criterion
> requires it; WO-041's committed model-authored receipt has no check at all.

## Disposition (2026-09-06, planning session; every hold accepted and changed)

1. **WO-039 criterion 6 omits reads the instruction file's hand-written
   floor still directs; criterion 7 keeps the read order unless the biased
   measurement shortens it.** Accepted; changed. The after value now sums
   the whole instruction file, the role skill, and every file that either
   the instruction file or the skill directs before acting, with the
   directed-read set derived mechanically by a fixture rather than listed
   by the executor; the floor's read order is rewritten per role to name
   the skill and only counted files, and a directed read the measurement
   does not count fails the criterion. Criterion 7 now writes that read
   order back into `CLAUDE.md` §Start here and the guide. WO-040 criterion 4
   inherits the corrected method by name.
2. **WO-035 criterion 5 counts one file's lines; relocation with pointers
   passes.** Accepted; changed. The criterion now requires the mandatory
   cold-start total per role, by WO-039's corrected method, to be lower
   after than before, forbids a relocated paragraph from entering any
   role's directed-read set, makes the 450-line figure a consequence rather
   than the measure, and requires the relocation table to say for each
   paragraph whether a skill or unit retired it or it became a reference.

Open self-certification risks: both were these two holds. WO-034 criterion 4's
baseline half now comes from the operator's own pre-run records, and a
recalled number is `unknown`. The refuter marked the remaining risks as
addressed by the criteria that carry them.

Open clean-room items: accepted; changed. WO-039 criterion 8 now requires the
local-terms check to have run over every generated skill, the residue block,
and the discovery record with the list present and reported; WO-041's
receipt writer runs the check over the validated result before writing and
refuses a match (design and criterion 3); WO-033's exported first order asks
the fork to register its own local list before any prose is committed, and
WO-034's receipt records that the fork's list was present when `WS-001` and
the profile were committed.

**Largest gap.** Unchanged and named: the authoring half of the differentiated
interface is the recorded wave-5 candidate. The runner-up the refuter found
unnamed, the roughly 120 shapes after batch one, is now the recorded
candidate "Rule migration, batches two onward" (one batch per wave from wave
4, named in the migration ledger before filing), and WO-037's "their own
order" for the rest of the shelf is the recorded candidate "Pattern workshop
v1, remaining shelf entries"; WO-040's and WO-037's non-goals defer to them
by name.

**Re-run.** Every disposition changed an order file, so the subject hash
changed; receipt 004 is the fourth blinded run and carries the standing
verdict. If it holds, its holds are left open for the operator, because the
planner may not override its own refutation.
