# Plan refutation — 2026-09-06 phase-two redirect (receipt 004, manual, pre-mechanism)

**Disclosure.** As in the earlier receipts: a manual blinded dispatch through
the planning session's harness; the reading list adds only the map's
preserved-candidates section so a deferral to a recorded candidate can be
judged; the plan, the ledger, the earlier receipts, and every planning
narrative excluded; the fixed verdict rules supplied. The subject was the
working tree after receipt 003's dispositions, committed unchanged with this
receipt. The planning session wrote the disposition block; the
self-referential instrument is disclosed. The refuter's verdict on WO-041
itself is advisory.

| Field                            | Value                                                                                           |
| -------------------------------- | ----------------------------------------------------------------------------------------------- |
| Subject                          | the marked sequence block and the ten order files it names, after receipt 003's dispositions    |
| Subject hash (as refuted)        | `sha256:7519af4190b44f80d5f36e1076998e91eba05748c0db9e8d7c2317b317d49580`                       |
| Subject hash (after disposition) | `sha256:121b548779367dfffd96c448a657fff70949e131974f0471ac5678430b5ca0b9`                       |
| Transport                        | manual dispatch, Claude Code Agent tool (fresh general-purpose session; no DotLn transport)     |
| Harness                          | Claude Code 2.1.263                                                                             |
| Model, effort                    | claude-fable-5-1, max                                                                           |
| Dispatched                       | 2026-09-07T01:35Z (2026-09-06 in the operator's day)                                            |
| Verdict                          | **hold**, three reasons, five open self-certification risks, one open clean-room item, no drift |

## Result (verbatim JSON in the sibling file)

Seven orders thesis-advancing (WO-032, WO-033, WO-034, WO-037, WO-039,
WO-040, WO-041 advisory), three machinery (WO-035, WO-036, WO-038), none
drift. The refuter's rationale, quoted:

> The horizon does move the vision: six of ten orders are thesis-advancing,
> and the strongest chain (WO-039 → WO-040 → WO-033 → WO-034) turns "a
> build, not a biography" from prose into enforced hooks, a measured
> directed-load reduction, an omittable exported actor, and a witnessed
> multi-repository run. No order drifts [...]. The traps I was told to hunt
> are mostly closed by the orders' own criteria [...] and every term list
> stays uncommitted and unhashed.
>
> The hold rests on three narrow measurement defects, not on direction.
> WO-032's product-lead cell can be `unavailable` in every pinned fixture;
> WO-034's "observed baseline" has no selection rule and need not be cited;
> WO-040's mandatory byte fall can be met by deleting a rule no unit
> carries, and its governance-mode vocabulary is inconsistent with its
> derivation. Each is a one-sentence criterion fix. The largest gap, the
> authoring half of the differentiated interface, is honestly deferred to a
> named candidate and is not a hold reason.

## Disposition (2026-09-06, planning session; every hold accepted and changed)

1. **WO-032 criterion 5 with criterion 1: the product lead's cell can be
   `unavailable` in every pinned fixture.** Accepted; changed. The committed
   refutation receipts are now a pinned fixture source in criterion 1, and
   the fixture that carries them must render the product lead's verdict
   cell populated, never `unavailable`.
2. **WO-034 criterion 4: the baseline has no pre-run selection rule and the
   receipt need not cite its records.** Accepted; changed. The launchpad
   records a pre-run baseline block naming each record by SHA-256 and the
   measure it supports, appended before the real run's first order
   activates; a record not named there is not a baseline; criterion 3's
   receipt names the block, its hashes, and the activation event that
   follows it.
3. **WO-040 criterion 4: the byte fall can be met by deleting a correction
   no unit covers; the governance derivation never yields `none`.**
   Accepted; changed. Governance is derived by one three-valued rule
   (`none` for reference and declined shapes, `mechanism` when equipped and
   no sentence remains, otherwise `prose`); removal is mapped in both
   directions and a reverse-mapping fixture fails the batch on any removed
   always-on sentence with no covering unit; assumption 3 now points at
   that fixture instead of standing alone.

Open self-certification risks: accepted; changed. WO-033 criterion 5 now
measures the export's directed-load total per role by WO-039's method and
requires it not larger than core's; WO-039 criterion 4's smoke now records
every file the session actually read before its first effect through a
generated read-observing hook and fails on a read outside the directed set
plus the order's cited sections; WO-041's override is no longer a line in
the receipt but a control event appended under the acting session's
recorded actor label with a capture hash, read by the gate from the log
only, so a planner that overrides is visible as the actor that did. The
refuter marked the remaining risks as addressed by the criteria that carry
them.

Open clean-room item: accepted; changed. WO-038 gains a criterion that the
final reviewer reads `CONTRIBUTING.md` against the clean-room floor and
records that read, since the order lands before the local-terms check
exists.

**Largest gap.** Unchanged, named, and by the rules not a hold: the
authoring half of the differentiated interface is the recorded wave-5
candidate, and the remaining shapes after batch one are the recorded
"batches two onward" candidate.

**Re-run.** Every disposition changed an order file; receipt 005 is the
fifth blinded run and carries the standing verdict. This is the last run of
the pass: if it holds, its holds stay open for the operator's control-log
override or for the next pass, because the planner may not override its own
refutation.
