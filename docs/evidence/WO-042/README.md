# WO-042 executor evidence

Compiler `0.8.0` rejects linked supports that widen the supplied active
mechanic's authority. Explicit repository-bound grants require a matching
host registry entry and retain their provenance through the program, trace
and harness manifest. Inspection projects the effective envelope and labels
authored permission notes as non-enforcing. The application target is
`v0.16.0`; the compiled-program contract remains `"1"`.

The operator's [ideation breakout](ideation.md) also equips five independent
executor supports, repairs the planning gate's handling of required execution
records, and repairs the actor board's stale default edition. Skeleton advances
to `0.14.0` and console to `0.1.2`; kernel remains `0.2.1`. The breakout receipt
records the scope expansion, decisions, rejected options and review obligations.

| Acceptance                                                              | Executed evidence                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1: four widening paths reject                                           | [Authority transcript](authority.json), `widening`; [compiler fixtures](../../../packages/compiler/test/authority.test.ts) assert diagnostic ids, both base lists and both corrections.                                                                                                                                                                                                                             |
| 2: nine layers narrow; higher precedence restores only a base allowance | [Authority transcript](authority.json), `narrowing` and `restoration`; runtime `authorize` agrees with the compiled lists and trace.                                                                                                                                                                                                                                                                                |
| 3: separate admission, provenance, validation and reversal              | [Authority transcript](authority.json), `explicitGrant`; [compiler fixtures](../../../packages/compiler/test/authority.test.ts) cover each mismatched admission field, all provenance values, malformed grants and three view codecs; [host fixtures](../../../scripts/test-authority-grants.mjs) exercise both registry files.                                                                                     |
| 4: unchanged saved programs and frozen fixtures                         | [Activation baseline](authority-baseline.json), [compatibility transcript](authority.json), [artifact identity inventory](artifact-identity/semantic-hash-inventory.json). All four full program and authored inspection digests match activation, and artifact identities differ only in `compilerPackageVersion`.                                                                                                 |
| 5: honest tooltip projection                                            | [Authority transcript](authority.json), `projections`; compiler fixtures reject exact contradictory notes in both directions and verify non-enforcing authored notes. Console expected JSON, text and HTML are regenerated through the existing command.                                                                                                                                                            |
| 6: bundle envelope and provenance                                       | [Bundle comparison](bundle-diff.json) checks all 24 surfaces in two stages: the unequipped saved build against activation, then the declared support equipment. Authority, host behavior and other-role procedures remain unchanged; executor fragments and origins are explicit. Compiler/skeleton pins and derived identities are classified separately. The grant fixture retains the envelope-mismatch refusal. |
| 7: two historical survivors killed                                      | [Runner transcript](mutations/reproduce.log) and [subject, policy, sites and results](mutations/summary.json). Both current unmutated baselines pass 328/328 package tests; each mutant compiles and fails its named test.                                                                                                                                                                                          |
| 8: durable write-backs                                                  | Products 02, 03, 04, 06 and 10; dated capability rows; README; publication index and both current edition locks; ledger; allocated planning-map candidate.                                                                                                                                                                                                                                                          |
| 9: complete gate and fresh editions                                     | Artifact identity, verification and feedback receipts below. The local-terms screen ran on all changed product/planning/publication prose; the local list was **unavailable**.                                                                                                                                                                                                                                      |

The mutation kills are `WO-042 AC7 F-00001 incompatible tags reject even when
every capability exists` and `WO-042 AC7 F-00002 equal-precedence incompatible
claims reject independent of source order`. The unchanged mutation runner
received this order's current public files through an isolated fixture Git
snapshot. Its temporary commit identifies that fixture only; it is not a
commit on the order branch. The historical WO-108 campaign is unchanged.

The expanded support fixtures exercise all 32 combinations of the five default
switches, three editable-view round-trips, conflict and malformed-switch
refusals, and generated executor instructions. Removing the equipment restores
the saved Contributor hash `fnv1a64:06245f5c581212f1`; the installed build is
`fnv1a64:709272ae4905aaa6`. These are instruction supports. Equipment tests do
not prove model judgment, chat delivery or cross-harness hard enforcement.

Five queue test groups exercise persistence, ordering, revision checks, veto,
scope changes, deferrals, communication levels, stale check-in refusal and
completion evidence. The console repair was itself queued and completed through
that workflow; chat and message-channel observations are actor-attested.
Planning fixtures admit only the specified append-only execution records while
retaining rejection of changed planning contracts and forged records, including
changes after a fixture commit. Historical planning receipts remain immutable.

The complete `npm test` gate passed on 2026-09-09 UTC: 316 package tests,
18 console tests, five queue test groups, authority and harness checks,
planning continuation and refusal fixtures, artifact identity, verification,
feedback and mutation-runner self-tests. The separate current-source mutation
reproduction passed both 316-test baselines and killed both named mutants.

The fresh editions are [artifact identity](artifact-identity/audit.json),
[verification matrix](verification/matrix.json) and [feedback report](feedback-002/feedback.json),
with the [audit stream](feedback-002/selfhost-audit.jsonl) and
[verifier stream](feedback-002/selfhost-verification.jsonl). The feedback audit
completed through the operator-approved outside-sandbox CLI invocation on
2026-09-09 UTC: ten passing regressions, ten failing mechanism removals and a
matched instruction projection 2,393 bytes smaller. The live transport was
`codex-cli-exec`, model `gpt-6-astra`, transport effort `unknown` as required
by that adapter's observed profile. This fixture audit does not replace
independent verification of WO-042.

The current harness edition contains passing
[executor](harness-live/executor-003.json),
[verifier](harness-live/verifier-002.json),
[reviewer](harness-live/reviewer-003.json) and
[release-close](harness-live/release-close-002.json) role-entry observations,
plus [dead-writer reclaim](harness-live/writer-foreign-dead-002.json) and
[live-writer refusal](harness-live/writer-foreign-live-002.json). These bounded
fixtures ran with the installed Claude Code `2.1.263` binary, model
`claude-fable-5` and observed effort `xhigh`. The first reviewer attempt
[failed](harness-live/reviewer-001.json) because it read the whole publication
document instead of the directed section; its immutable receipt is retained.
The next attempt passed without changing the skill or weakening the check.
The first attempt with the support overlay also
[failed](harness-live/executor-002.json) because the host version constant still
named skeleton `0.13.0`. Correcting that pin and regenerating the bundle restored
the runtime check; all six final observations passed. Both failures remain
recorded. The existing harness checker selects the latest numbered observations
and verified all 24 generated surfaces. Its matching
[context measurement](harness-context.json) retains the frozen activation
instruction and execution guide while giving both sides the current shared
task guidance. The historical WO-039 measurement remains intact. This also
binds the reviewer comparison to the current publication-section range
observed by the live fixture.

Reproduce after the pinned dependency install and build:

```sh
npm test
node scripts/authority-evidence.mjs --check
node scripts/authority-mutation-evidence.mjs --check
node scripts/authority-mutation-evidence.mjs --write
npm run evidence:console -- --check
```

The mutation `--check` validates the recorded transcript against the current
executable source inventory; `--write` reruns both fresh baselines and both
mutants. Artifact identity, verification, feedback and installed bundle
checks are included in `npm test`. The recorded live audit is checked against
its source subject; regenerating it requires another explicit live audit.

The supplied active definition and host registry remain trusted inputs.
Reasons are explanatory rather than admission fields; the registry hash is
an equality receipt, not authentication. An exact grant cannot override a
broader wildcard denial under the unchanged kernel, so that graph rejects.
The contradiction check matches effect-id tokens; it does not infer arbitrary
natural-language implications. No personal authority is added: the committed
instance registry is empty.

The default collector, recorded fixture and feedback gate select WO-042's
`feedback-002` edition. The console regression requires both default streams
to replay and project successfully; missing requested stores still report
unavailable. Earlier feedback streams remain intact.

Executor attestation: `codex-cli` `0.153.4`; `gpt-6-astra`; effort `max`;
source `operator-attested` under the repository's Codex default. Effective
session readback is not exposed. Verification, final review and publication
remain their separate lifecycle dispatches.

The [VER-001 F1 repair record](repair-001.md) records the subsequent Unicode
punctuation correction, its failing-then-passing regression cases and the
renewed evidence gate. The original verification report remains immutable.
