# WO-043 — VER-002 repair

Operator dispatch: `resume: fix`, 2026-09-11. This repairs F2–F5; independent
re-verification remains a separate dispatch. [D014](decisions.md#wo-043-d014)
records the decision, correction, alternatives and goal/trap comparison.

| Finding | Repair and executed evidence                                                                                                                                                                                                                                                                                                                                                               |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F2      | Every generated Contributor role carries both session prefixes. The new output assertion failed before the change and passes in both harnesses. Removing optional supports retains the mandatory commands. Existing session fixtures confirm side questions and scope additions preserve the running lifecycle session.                                                                    |
| F3      | `git diff --exit-code -- CLAUDE.md` passes: the entire hand-written floor matches HEAD. Generated release-close wording uses the existing helper and retains its limits. `harness-context --check` passes; Codex release-close is 6,132 bytes against its 6,144-byte ceiling.                                                                                                              |
| F4      | Product 07's table names the goal-card read for every working role, separates the refuter's purpose input from its sole subject evidence, and retains the executor's read-only status/times shortcut.                                                                                                                                                                                      |
| F5      | Independent counter subtraction reproduces both original implementation readings and its full completion window: 16,773,516 tokens. Append-only reconciliation preserves the originals. The prior repair contributes 23,933,250 at completion, or 24,580,001 including its recorded post-handoff work. [The public reconciliation](usage-reconciliation-002.json) records both boundaries. |

The saved loadout semantic identity remains unchanged; the existing harness
target identity covers the new generated instructions. All three executor-support
tests pass. Three focused process fixtures also pass: current-session counter
collection/refusals, session continuity, and source-reconciled usage aggregation.
Authority edition `002` records the changed harness targets while preserving
edition `001`: four unchanged programs, four widening rejections, nine runtime
denials, admitted/reverted grants and 27 bundle comparisons pass. The first full
gate stopped in preflights after 25.08 seconds on document formatting and the
stale authority snapshot; its expensive suites did not run.
No release helper, dependency meaning, event schema or publication authority
changes in this repair. Application `v0.17.1`, compiler `0.9.1`, skeleton
`0.15.1` and console `0.1.4` retain their assigned patch versions.

The two historical executor handoffs total 40,706,766, correcting the prior
22,860,785 snapshot. Retained post-handoff observations bring those completed
dispatches to 41,353,517. The refreshed meter and prepared PR table also include
this active repair at their explicit observation cutoff; they are snapshots,
not predictions of subsequent work. The completion-time measurement is followed
by a final meter/PR refresh and evidence check so the completed interval is
included; later post-handoff work retains its own timestamped observation.

At 17:41:08.785Z, this session measured 3,333,409 tokens from
`codex-transcript-counter`: 267,527 during entry diagnostics and 3,065,882 after
the explicit harness entry. The two disjoint intervals reproduce the complete
session counter exactly. The first successful entry reading was 113,733 for the
harness interval; its first immediate attempt preceded a fresh counter and was
repaired by collecting after the next recorded response. Handoff recollects
through the same required adapter. VER-002's previous repair completion used
23,933,250 tokens on its broader scope; these different scopes do not establish
a causal efficiency improvement.

The final `npm run harness -- evidence` gate runs after authored outputs and
release preparation. Its current-tree full/diff result is recorded by the
repair-completion event. Historical live feedback evidence is retained; this
repair does not claim a new independent model audit or verification verdict.

Actor: Codex CLI `0.154.0`, observed with `codex --version`; GPT-6 Astra at max
under the repository's operator-attested default, not effective-session readback.
