# WO-043 command and token-usage breakout

On 2026-09-11, during `resume: fix`, the operator opened ideation for
first-party `scope expand:` and `conversation only:` commands, required actual
Codex and Claude token measurements, and clarified that `conversation only:`
is a side question that does not interrupt the current work order.

Source: `docs/intake/notes/WO-043-expanded-ideation-2026-09-11.md`. The sandbox
permits this checkout, so the capture is provisional worktree-local staging;
the canonical closeout reconciliation must preserve it in main before removal.
The clean-room screen found only operator workflow preferences, with no
employer material or secrets. Treatment is Shape-First Synthesis, rewritten
into product language; no Direct Draft Fidelity exception is used.

The scope-expansion authority and additional acceptance obligations are in
WO-043's dated execution record. The ledger's same-date command and usage
entry and product 07 carry the durable meaning. `conversation only:` answers
the side question without changing the work's scope, state or continuation;
`scope expand:` authorizes the stated addition and its receipt while retaining
the original obligations and effect boundaries. These are session commands,
not new lifecycle events.

The usage correction addresses optional collection and swallowed reader
errors. Both harnesses already record counters. Measurement must identify
the actual session, respect the dispatch window, count cached tokens according
to each source's semantics, and retain numeric totals with their source.
Missing counters are a collection failure to resolve before handoff. Prices
and historical absent observations are separate from this requirement and
must never be invented.

The verifier and final reviewer must digest this receipt, the promoted
documents, generated instructions, collector and completion integration, and
the counter fixtures. They must check source treatment, operator traceability,
side-question continuity, correct session attribution, deduplication, numeric
totals, privacy, release compatibility and publication locks. Existing VER-001
remains immutable; fresh independent verification covers the expanded subject.

The operator also reports that default Adjacent Repair and Intent to Act do
not appear until explicitly named. The source equips them, but inserts their
fragments late and checks queue transitions only after manual entry. The
worktree's empty queue and product 05's earlier WO-126 observation reproduce
that gap. The additional execution record authorizes moving equipped duties to
entry, projecting current equipment/queue on bare executor/fix dispatch, and
checking unresolved queue items at completion. Review must distinguish these
mechanical guarantees from model judgment and actor-attested chat delivery;
neither an emitted phrase nor a successful queue fixture proves that a model
recognizes every defect. Existing switch removal and authority boundaries stay
part of the regression subject.

The operator then asked about the Tinkerer and its increasing experiment
selection pressure since the last application improvement, explicitly directing
that it remain outside this repair if still proposed. Product 05 and existing
`FUP-fc4158d3207e495d` establish candidate status. The product candidate now carries
the operator's near-term planning request; no implementation or new allocation
is claimed. Both source messages are preserved in the same intake capture.

The operator accepted Process Cost as a first-class support across all six
roles, asked when the eight-trap/intervention/NoOp comparisons are recorded,
then required goal and critical-path alignment in every phase. The same intake
captures these messages. D009/D010 and product 07 record the selected shared
supports, the scoped mission/path source, decision timing and existing evidence
destinations. The compiler's additive explicit role-set adapter is in the
expanded review subject, with a patch bump to 0.9.1; skeleton remains 0.15.1.
General causal evaluators and Tinkerer activation remain outside this repair.
