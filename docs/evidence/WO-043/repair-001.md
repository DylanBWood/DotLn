# WO-043 repair 001

This repair addresses VER-001 F1 and the operator's same-session expansions.
The original typed dependency implementation and immutable verification report
remain retained. The mixed navigation section is again `planned`; refreshed
publication locks preserve its least-mature status.

`scope expand:` and `conversation only:` are first-party commands. A side
question leaves the ongoing work, phase and obligations intact. Actual Codex
and Claude counters are now required through the shared collector, explicit
adapter, Claude Stop observation and all four lifecycle completion checks.
The collector verifies session/worktree attribution, dispatch window and
source-specific cached-token semantics. Missing counters refuse measurement;
the Stop hook advises once and releases the writer. Scope and provenance travel
with numeric totals; prices and historical gaps are separate.

Ordinary executor next/fix projects the installed supports and current queue.
Equipped duties occur at entry, with an initial Intent to Act announcement and
fresh announcements for queued actions. Completion refuses unresolved queued
or running items; a deferral still needs a live public destination. Process
Cost and Goal Alignment are independent shared supports projected once into
every role in both harnesses. The additive explicit role-set adapter retains
the legacy single-role shape. The goal card ties material decisions to operator
flow and the current runtime critical path, compares all eight system traps,
Naive Interventionism and NoOp before selection, and records reconsideration
and handoff judgment in existing phase evidence.

The live queue exercised the default process. Adjacent-0001 covers the support
repair; adjacent-0002 was discovered while reviewing the meter, without a new
operator prompt. The latter corrects overlapping Claude observations after
every original counter vector reproduced from one exact source session. The
append-only [reconciliation](usage-reconciliation.json) replaces their aggregate
with 10,029,006 measured tokens while retaining all seven original local rows.
The earlier [both-harness probe](usage-probes.json) uses a shorter Claude window;
its 5,278,902 tokens do not include the later continuation through 15:00:22 UTC.

The Tinkerer remains a candidate. Existing `FUP-fc4158d3207e495d` and product 05
now carry the operator's request to prioritize its adaptive exploration policy
soon; the operator explicitly excluded implementing it here.

## Validation

- `node --test scripts/test-process-debt.mjs`: 48/48 passed in 63.07 seconds,
  including source-reconciled overlap and every mandatory-token completion path.
- `node --test packages/skeleton/dist/test/executor-supports.test.js packages/compiler/dist/test/harness.test.js`:
  6/6 passed, including both harnesses, all roles, independent switches and
  empty/duplicate/unknown/ambiguous target refusals.
- `npm run test:docs`: 5/5 passed in 63.46 seconds after correcting the new
  audience-index row's formatting. No refutation receipt was replaced.
- Installed role plus floor measurements meet all selected byte ceilings;
  Codex release-close is 6,135 bytes against 6,144. No budget acceptance or
  invented token ceiling was added. This source-byte measure excludes the
  separately selected goal card and work-order documents.
- `npm run release -- prepare --local` retains application `v0.17.1`;
  compiler is `0.9.1`, skeleton `0.15.1`, console `0.1.4`; kernel stays fixed.

The current evidence selector is centralized in `docs/evidence/current.json`.
Four evidence commands and the console consume one resolver; the gate and
package command carry no current work-order literal. A manifest-switch fixture
changes the selected order without changing source and retains explicit
historical selection. The prior 37-command inventory assertion still passes.
The new resolver plus shared-support/compiler checks passed 7/7. Existing
Correctness over Sycophancy is asserted on every role in both harnesses; the
goal card makes accuracy over agreement explicit for operator and agent premises.

The initial full-gate attempt found four stale compatibility preflights;
other blocked suites were skipped, not independent failures. The executor-only
support comparison now explicitly selects the executor-only equipment set.
Artifact and verification records use fresh current editions; prior evidence
bytes remain retained. The live feedback check is separate from whole-order
verification. The observed Claude CLI `2.1.268` is added to the existing exact
transport profile with its tool, settings and budget restrictions preserved.
This reopens D010's earlier no-additional-model-episode assumption: the existing
gate requires one fresh live audit of the changed source, explicitly authorized
by the operator. It adds measured execution cost to this repair.

The final-tree application gate is `npm run harness -- evidence`; it records
the full inventory and diff check and is required by repair-complete. This
receipt precedes that gate rather than claiming an unexecuted result. Current
numeric repair-session usage is in the [meter snapshot](meta.json); the handoff
recollects it after output review. D005–D013 retain the decisions and actual
action/NoOp, intervention and eight-trap rationale.

Limits: deterministic projection, queue state and completion checks do not
prove model judgment or actual chat/inbox delivery. Those observations remain
actor-attested; generalized counterweight and causal-evaluator mechanisms stay
candidate work. The raw ideation capture is provisional worktree-local staging
and must reconcile into main before eventual teardown. The clean-room screen
found only personal workflow material; no source transcript text or private
session identifiers are published.

Actor: Codex CLI `0.154.0`, GPT-6 Astra, `max`, operator-attested under the
repository default; CLI version observed, no effective-effort readback claimed.
Independent verification and final review remain separate dispatches.
