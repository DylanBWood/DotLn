# WO-131 — operator recovery controls

The operator expanded execution scope to unconditional `analysis:` and
`operator override:` entry, then opened ideation about immediate recovery to a
usable repository. WO-131 remains active; its existing evidence obligations
remain assigned. This is the ideation breakout receipt, not a planning verdict.

Source: `docs/intake/notes/WO-131-expanded-ideation-2026-09-14.md`.
The main control-plane checkout was resolved with `git worktree list
--porcelain`. Its intake lies outside this session's writable roots. This batch
is provisionally staged in this worktree, backed up through the existing intake
helper, and must be reconciled into main before worktree removal. No other
intake was mined or reproduced.

The source describes personal repository controls and contains no employer
material or secrets. The effective treatment is the locked clean-room floor,
Shape-First Synthesis, public vocabulary, provenance and breakout review. No
direct-filing exception or extra review support applies. The Westworld analogy
supplies the interaction shape: suspend the current routine, become available
for diagnosis, explain the observed state, and accept operator direction. It
does not introduce fictional mechanics or authority outside this repository.

Promoted surfaces:

- `docs/lineage/idea-ledger.md`, dated operator recovery section.
- `docs/product/07-execution-guide.md`, Operator recovery controls.
- `docs/work-orders/WO-131-remaining-suites-under-replica.md`, scope criteria.
- `docs/evidence/WO-131/decisions.md`, D007 and its reopening conditions.

The unconditional entry and hook bypass are authorized WO-131 implementation.
A general command that repairs arbitrary corrupted lifecycle data into a
working state is a preserved design requirement: scripts versus assisted ad
hoc recovery, concurrent process handling, and the exact recovery event remain
open. Recovery must preserve work and distinguish operator acceptance from an
executed passing check. It must not require the broken workflow to approve its
own repair. This ideation does not authorize publication, deletion of evidence,
or invented verification results.

Verifier and final reviewer must inspect this receipt and the promoted
surfaces, execute the generated-hook recovery tests, and check that the
unimplemented general recovery helper is presented as a candidate. The
dependency-free operator-control adapter introduced by this scope is executable
work-order code and receives the same independent review as the other changes.
