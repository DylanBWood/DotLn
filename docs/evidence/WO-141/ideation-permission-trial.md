# WO-141 ideation breakout — reversible permission-mode trials

Authority: the operator's `ideation:` message on 2026-09-17 during
`resume: next` expands the documentary subject. Preserve the implementation
and continue WO-141 after this receipt. This captures an experiment the operator
is considering; it does not select a new default or apply a setting.

Source: `docs/intake/notes/WO-141-expanded-ideation-2026-09-17.md`, captured
unedited. The main control-plane checkout was resolved with
`git worktree list --porcelain`; its ignored intake is outside the writable
roots. This worktree-local capture is provisional, backed up outside the
repository, and must be reconciled into main before worktree removal.
Shape-First Synthesis; clean-room review found no stop condition. No
direct-filing exception applies.

Promoted surfaces: the ledger's dated permission-mode trial entry and product
03's existing DotLn-owned authority candidate. The operator may try different
permissions or automation modes for a few work orders and expects likely
reversion. Exact modes, trial length and evaluation criteria remain open.

Evidence: WO-136 is an evidence-only sandbox-on/off probe; its non-goals exclude
runtime capability and setting changes. Current `currentCodexSession` projects
model, effort and CLI version, not a permission-mode field. This session's
supplied host policy directly declares workspace-write, restricted network and
protected paths; this is not an inference from a rejection or proof that a
particular app mode label is selected. An actual rejection establishes that
operation's outcome, not the complete effective configuration. Automatic mode
readback needs an explicit, sourced field if the harness exposes one; otherwise
retain unknown or an operator-attested trial label separately from outcomes.

The [official Auto-review documentation](https://learn.chatgpt.com/docs/sandboxing/auto-review)
was checked on 2026-09-17: automated review changes the reviewer of eligible
boundary requests while keeping the main agent's sandbox limits. Therefore a
sandbox field alone cannot identify manual versus automatic approval routing.

Goal alignment: comparing completed authorized work, stalls and interventions
can reduce supervision on the critical path. Policy resistance and rule beating
require distinguishing declared policy, effective readback and effects. Commons
and escalation favor a short reversible comparison over new recurring gates.
Drift and wrong-goal risks require useful completed work, not fewer denials alone.
Success to the successful permits competing modes; shifting the burden measures
operator intervention. Naive Interventionism preserves a return path. NoOp would
lose the experiment's provenance; immediately changing defaults would decide an
open question. Preserve the candidate with no implementation expansion.

Reopen when WO-136's packet lands, a concrete mode is selected for trial, or the
host exposes effective mode readback. No ADR, schema, runtime or settings change
arises here. The eventual verifier and reviewer must read this receipt, the
ledger entry and product paragraph, checking intent, clean-room treatment,
evidence limits, cross-references and speculative status. Publication and document
checks cover these surfaces; no executable ideation helper was introduced.
