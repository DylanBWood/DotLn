# WO-141 decisions

## WO-141-D001

```json
{
  "id": "WO-141-D001",
  "date": "2026-09-17",
  "dispatch": "resume: next for WO-141",
  "decision": "Project session observations and lexical hedge advisories through a shared runtime module; count journal events with recorded phase, order and explicitly named unit. Preserve nonblocking Stop and lifecycle behavior.",
  "evidence": [
    "WO-141 acceptance criteria 1–6",
    "harness-host.ts finish reads usage but not final assistant text; observe journals tool boundaries without task state",
    "scripts/lib/meta.mjs derives operatorCorrections from decision records",
    "usage-observation.mjs already selects exactly the current session transcript and worktree",
    "FUP-0130 already has an allocated disposition targeting WO-141 dated 2026-09-17"
  ],
  "rejected": [
    "NoOp or another instruction line retains the demonstrated operator correction burden.",
    "A truth classifier cannot establish semantic accuracy; use lexical observations with explicit unknown values.",
    "Blocking Stop or lifecycle admission would hold operator turns and conflicts with the order.",
    "Infer a quantity from an ambiguous task, infer the violated unit from tone, or count cumulative correction arrays: each invents evidence."
  ],
  "reopenWhen": "Fixtures or observed operation show false attribution, missing boundaries, excessive scan cost or repeated estimates after an advisory."
}
```

This supports the critical path by reducing recurring operator rescue during
execution and review. Policy resistance and escalation are addressed by keeping
observations advisory. Commons cost is bounded transcript reading and fixture
measurement; no model call is added. Drift is measured against explicit unknown
and observed values. Success to the successful is checked against the rejected
NoOp, prose-only and semantic alternatives. Shifting the burden is measured from
events, not authored decisions. Rule beating remains possible through unmarked
guesses; this is lexical measurement, not truth certification. Seeking the wrong
goal is avoided by adding no refusal or ritual. Naive Interventionism: preserve
existing permissions, correction effects and transcript privacy, use the existing
session selector and ignored journal, and validate missing/corrupt inputs as
nonblocking unknowns before integration.

Claude observes prompt and Stop boundaries automatically. Codex has no Stop
hook; its briefing and lifecycle handoff can scan only assistant messages already
present in the current session transcript, plus their own handoff output. A final
answer written after the last lifecycle command is observable only at a later
boundary. Untyped operator corrections remain outside the counter.

## WO-141-D002

```json
{
  "id": "WO-141-D002",
  "date": "2026-09-17",
  "dispatch": "resume: next; announced adjacent-0001 revision 2 with asynchronous steering opportunity",
  "decision": "Repair three assertions in scripts/test-process-debt.mjs that conflate observational presence hooks with governance hooks; retain all governance assertions and check the observers' empty nonblocking response explicitly.",
  "evidence": [
    "The full process-debt run failed the complete SessionStart/UserPromptSubmit equality, universal operator-control systemMessage, and universal bootstrap advisory assertions.",
    "packages/compiler/src/harness.ts heartbeatHook writes empty JSON and an optional stderr diagnostic, while SessionStart is wired only to session.mjs.",
    "The repair stays in the existing subject test file and changes no hook implementation."
  ],
  "rejected": [
    "Change the presence runtime to match the obsolete fixture expectation: expands behavior without a product requirement.",
    "Delete or skip the affected tests: loses the governance protections those fixtures establish.",
    "NoOp: leaves the encountered harness verification failing on the observed behavior."
  ],
  "reopenWhen": "The presence hook contract changes or a fixture no longer distinguishes observational output from governance decisions."
}
```

This adjacent repair removes a false failure on the same verification path.
Policy resistance, drift and rule beating favor preserving governance checks
and adding the observer assertion. Commons, escalation and shifting the burden
favor the single bounded repair. Success to the successful and wrong-goal risks
are limited by judging the current source contract instead of a historical
fixture. Naive Interventionism favors the reversible test-only change.

## WO-141-D003

```json
{
  "id": "WO-141-D003",
  "date": "2026-09-17",
  "dispatch": "resume: next; standing classified-release default",
  "decision": "Stage application v0.29.1 and skeleton 0.25.1 as patch releases; keep other components unchanged.",
  "evidence": [
    "WO-141 declares a patch classification and initially has no activation version.",
    "Local tag observation lists v0.29.0 as latest; skeleton package and harness host are 0.25.0 at the baseline.",
    "The runtime session hook owns the standard correction-token measurement and the effect hook records that ownership, without changing compiled configuration.",
    "Changes repair existing evidence behavior and add no refusal, new dependency or public event schema."
  ],
  "rejected": [
    "Reuse the published application version: conflicts with the next-release duty.",
    "Bump unrelated components: no changed component behavior supports it."
  ],
  "reopenWhen": "Final review observes a newer published version or a compatibility impact that changes the existing classification."
}
```

Release assignment preserves the existing publication controls and independent
review. The integration version remains reviewable and reversible before
publication; no extra release mechanism is introduced.

Correction, 2026-09-17: D002 originally called the process-debt fixture a
required bare `npm test` row. The runner selects it as an affected machinery
check under `--review`; the encountered failure and bounded repair are unchanged.
D003 initially included a compiler configuration field and patch bump. The full
gate exposed persisted-compilation drift in historical verification and feedback
streams. Runtime ownership of the standard correction count removes the need
for that compiler change, so its source and version are retained. Rewriting live
evidence or weakening replay checks would widen the repair without necessity.

## WO-141-D004

```json
{
  "id": "WO-141-D004",
  "date": "2026-09-17",
  "dispatch": "resume: next; current-evidence refresh required by the document gate",
  "decision": "Record new WO-141 authority and feedback revision 001 editions, using the existing live CLI verifier for the changed feedback source; advance only their current-evidence selections and the console selfhost fixture pins.",
  "evidence": [
    "The document gate found authority bundle-diff.json stale after generated runtime bindings changed.",
    "readFeedbackSource includes usage-observation.mjs, whose exported session selector changes the exact source subject.",
    "The existing feedback recorder requires an actual CLI verifier, a complete verified matrix and matching source/report bytes."
  ],
  "rejected": [
    "Overwrite historical editions or relabel their verifier logs: would replace the recorded subject without a new observation.",
    "Relax current source checks: would remove the evidence obligation introduced by the changed dependency.",
    "NoOp: leaves the document gate failing on stale selected evidence."
  ],
  "reopenWhen": "The exact source or compiled bundle changes again before handoff, or the live verifier cannot establish its existing criteria."
}
```

The existing refresh path preserves the mission's evidence-before-claims floor.
Policy resistance, drift and rule beating favor exact recorded subjects; commons,
escalation and shifting the burden favor one refresh through existing tools.
Success to the successful and wrong-goal risk are checked by retaining unchanged
artifact-identity and verification selections. Naive Interventionism favors no
new compatibility policy or evidence mechanism. This feedback evidence worker
does not replace WO-141's separate verification dispatch.
