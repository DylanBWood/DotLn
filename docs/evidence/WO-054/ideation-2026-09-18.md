# WO-054 ideation breakout — out-of-project file effects

**Authority.** The operator sent an explicit `ideation:` message during
`resume: verify` (VER-003) on 2026-09-18. It adds documents only to this
branch's subject. It does not expand WO-054's implementation scope, amend the
order text, or authorize changes to settings, permissions or publication.

**Source.** The capture is
`docs/intake/notes/WO-054-expanded-ideation-2026-09-18.md`, written unedited to
the main control-plane checkout's ignored intake (resolved with
`git worktree list --porcelain`). No worktree-local copy exists, so nothing is
pending reconciliation. The capture holds the ideation message and the
operator's follow-up corrections. No raw text is copied into committed
documents.

**Strategy.** Shape-First Synthesis with public vocabulary. The clean-room
screen found no employer material, credentials, private identifiers or other
stop condition. The direct-filing exception does not apply.

**Promoted surfaces:**

- the dated ledger entry "Out-of-project file effects as an explicit grant";
- one dated paragraph in product 03 §Candidate — DotLn-owned authority with
  minimal native harness restrictions;
- correction [WO-054-D007](decisions.md#wo-054-d007), which records how this
  intake was first mishandled.

No ADR, schema, implementation, generated surface, settings or release
classification changes. No work order is filed here.

**Open choices:**

- the grant's shape: a support, a role, or both;
- how allowed outside roots are declared;
- the enforcement point for shell redirections and subprocesses;
- how the grant relates to today's planning-branch admission of external paths.

The operator set this for priority at the next planning pass. The operator's
recollection that ordinary sandbox rules would have prevented the write
remains unverified, by the operator's direction.

**Goal alignment.** The mission contribution is fewer silent side effects
outside the authorized subject.

- **Shifting the burden.** The trap is present twice: in the incident, which
  relied on the operator noticing, and in this intake's first handling
  (D007).
- **Policy resistance and rule beating.** Both argue for an enforced grant
  rather than more role prose.
- **Escalation.** Favors reusing the existing authority candidate and WO-136's
  sibling-write evidence over a parallel mechanism.
- **Drift and seeking the wrong goal.** Both require that authorized
  outside-path work, such as intake and scratch, keeps working.
- **Commons and success to the successful.** Both favor one grant model across
  roles.
- **Naive Interventionism.** Nothing changes in settings or runtime now.
- **NoOp.** Loses a direction the operator stated explicitly.

**Review.** The author of these surfaces is the WO-054 VER-003 verifier. VER-003
does not judge them independently, so final review must inspect this receipt,
the ledger entry, the product 03 paragraph and D007. It checks clean-room
treatment, traceability to intent, consistency with the four refusals and the
authority candidate, and that nothing speculative is presented as settled. No
executable ideation tool was introduced.
