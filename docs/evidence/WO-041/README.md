# WO-041 executor evidence

The executor is Codex CLI 0.153.4, GPT-6 Astra at max (the operator-attested selection recorded in execution guide 07, not effective-session readback). This is implementation evidence; independent verification and final review remain separate.

The implementation adds a committed-only subject builder, a compiled read-only Entropy Reducer / Contra-Auguste loadout, a closed result validator, immutable receipts, captured actor-attributed overrides, and a forward-only planning gate. The repaired source prepares application `v0.13.3` with skeleton `0.12.1`; compiler `0.6.0`, kernel `0.2.1`, dependencies, and lifecycle event schemas are unchanged. The shared local-terms module implements the already-specified WO-039 contract early because WO-041's receipt writer requires it. The [operator-authorized breakout](ideation.md) adds independent integration policy, release preparation/early checks, and feedback evidence scoped to behavioral dependencies.

[Fixture transcript](fixtures.txt) covers criteria 1, 2, 3 and 5: deterministic committed inputs and all five stale-source families, excluded planner narrative, authority denials, exhaustive closed output, structural holds, synthetic local terms, captured overrides, criterion-bound dispositions, and the three-hold stop. Both actual transport adapters also run with injected process fixtures to prove launch shape and output binding; these adapter fixtures are not live model evidence.

The local operator terms list is unavailable in this checkout; the screen reports that absence explicitly, while the synthetic-list fixture proves refusal. Neither a list nor hashes of its terms are written to evidence.

Self-reference disclosure: the new subject builder, receipt writer and gate are themselves part of this deliverable. The fixtures use independent expected values, synthetic Git histories, injected transport results and negative mutations to challenge them. The live actor judges the committed activation-base horizon, which includes WO-041; its verdict on WO-041 is advisory and cannot establish the instrument's correctness. The receipt is staged for the final reviewer to commit with the deliverable under the work-order workflow.

## Live plan-refuter receipt

The [live receipt](../../planning/refutations/2026-09-07-wo041-live-001.md) records Claude Code 2.1.263, `claude-fable-5-1`, `max`, through `claude-cli-print`: **pass**, zero holds. It judged committed revision `01c2e321268bf9cb9842b58b3e95fc8ed5a09d67`, subject `sha256:dc02ed397a3a4963f40f340367f62f64ca466ba69afbf942e44ecf74c7a59ec8`. The current marked sequence has the same ten orders; the later activation title and staged capability row are explicitly outside this frozen judgment. The receipt is an evidence-only instrument run, not a reopened historical planning pass.

The first call exhausted a 600-second deadline without a result and produced no receipt. The successful retry used the recorded twenty-minute bound and unchanged $5 cap, dispatched at 12:59:32.930 UTC and completed at 13:09:21.372 UTC on 2026-09-07. These are host launch selections, not effective-session readback. The live compiled hash records that episode. Subsequent inspection-label cleanup removed inherited scratch/delegation claims from the loadout's display metadata; its dispatched questions, read-only envelope and model-tool boundary did not change. The [continuity record](inspection-continuity.json) reconstructs the recorded live hash by replacing only the current active inspection metadata with the original Entropy Reducer metadata, then asserts identical compiled WorkOrders and authority envelopes.

The refuter's largest remaining gap is the route from a new correction in a governed session to a compiled, fixture-backed unit: the horizon files one batch and leaves the continuing route manual. It recorded this as a gap, not a hold, because the thesis is touched. Its suggested extra capability promotion for WO-041 is advisory; this implementation promotes only the bounded `control.plan-refutation` row to staged demonstrable evidence, as the work order requires. Other orders' activation notes remain input for their own dispatches.

## Feedback evidence edition

The shared reactor/transport edits correctly invalidated the earlier feedback check. The current check selects [this report](feedback/feedback.json), [audit stream](feedback/selfhost-audit.jsonl), and [independent verifier stream](feedback/selfhost-verification.jsonl) under the WO-041 edition. The published WO-011 edition on the synchronized `v0.13.2` base remains byte-for-byte untouched by this repair. The stable logical report label inside the verification capsule remains `docs/evidence/WO-011/feedback.json`; the physical edition is the linked WO-041 directory.

The prior, never-committed WO-041 edition judged raw source subject `sha256:f0df21329af323d93b3c9f5eb3a94ef19fca4e1ac0543092ff590972f6d5988d` and was the edition VER-001/002 and FINAL-001 read. It was moved aside before recording its replacement because the writer accepts one immutable edition per work-order identifier. It remains recoverable in stash `827132315b7d5d8664b0fa9f15c10545b011906e` (the untracked-tree parent), named `WO-041 sync 2026-09-07 after FINAL-001 and workflow ideation`, and in the pre-repair checkpoints. This is explicit supersession of uncommitted executor evidence, not alteration of a published edition or numbered review. The changed projection requires a fresh live audit of its own; future release-only metadata changes do not.

`feedback-evidence.mjs` adds an explicit `--edition WO-NNN` destination and refuses overwriting an edition with different bytes. Its current projected-source, policy, report, real-transport, matrix and replay checks remain in force. The root `evidence:feedback` command and full gate select WO-041. Reproduce with `npm run evidence:feedback -- --check`; a changed behavioral source subject needs a new live run and an explicitly preserved evidence edition.

The replacement live audit completed on its first attempt through Codex CLI `0.153.4`, model `gpt-6-astra`, effort `unknown` under the observed transport profile. It judged source `sha256:bc7e948d120d528e783f6201c468149b125592358e1b5c111933745d3cda2766`; both `AC-causal-fixtures` and `AC-context` are verified with host-admitted verifier provenance and no stale evaluation. The fixed executor witnessed ten passing regressions, ten failures with their mechanisms removed, and 2,393 fewer instruction bytes in the matched projection. The saved streams pass current-source and replay validation. This is evidence for the bounded feedback audit, not independent acceptance of the whole WO-041 breakout.

## Initial executor validation (before integration)

On 2026-09-07, `npm test` exited 0: 281 runtime tests, 8 corpus tests and 21 mutation self-tests passed, alongside all ten plan-refutation fixture groups, lifecycle/recovery/release fixtures, formatting, publication locks, the work-order index, and artifact/verification/feedback evidence checks. `npm run plan -- check` validated the live receipt and reported forward enforcement pending the introduction commit, with the local terms list unavailable. `git diff --check` passed. The kernel, lockfile, WO-011 evidence, and six historical manual refutation receipts have no diff.

The only additions outside WO-041's named primary surfaces are the shared-reactor authorization helper required by the existing one-decider test, the small shared local-terms module required by the writer, and immutable feedback-edition selection required to keep the pre-existing source-pinned gate green after transport changes. None changes lifecycle legality or replaces independent work-order verification.

## Repair of VER-001 — 2026-09-07

This repair addresses the two required findings in [VER-001](../../verifications/WO-041/VER-001.md) under the original WO-041 scope. The repair actor is Codex CLI 0.153.4 / `gpt-6-astra` / `max`, source `operator-attested` under execution guide 07. Independent re-verification remains required.

The local-terms screen now checks contiguous token spans up to the longest normalized term's character length. It handles listed phrases of three or more words, separators inserted within a word, and phrases spanning line breaks. Unicode/case normalization and complete token boundaries are retained; separate surfaces stay separate. Refusals report only the surface, starting line and match count. Synthetic fixtures assert the exact diagnostic bytes and that a refused receipt leaves neither evidence files nor a writer lock; a subsequent clean receipt succeeds.

Deferrals now require a committed order strictly later in the same marked sequence. Fixtures reject an outside-sequence draft, an outside-sequence order whose control history folds to closed, self-deferral, an earlier order, a missing order, and an unknown thesis. Each is checked against workspace inputs, committed inputs, and the planning gate. The existing valid later-order fixture still discharges the structural gap hold. The [receipt convention](../../planning/refutations/README.md) states both repaired boundaries; its existing execution-guide and UIFA entry points remain accurate.

Before the fixes, the new deferral assertion reproduced the missing refusal, and a separate three-word synthetic-term assertion reproduced the screen's missing refusal. The expanded fixture also reproduced the cross-line miss. After repair, `node scripts/test-plan-refutation.mjs` exited 0 with all eleven fixture groups and the existing live-receipt gate green; its output is appended to the [fixture transcript](fixtures.txt). The gate still reports forward enforcement pending the introduction commit and the local operator terms list unavailable. VER-001 and the live receipt remain unchanged.

The full repair gate, `npm test`, exited 0: 281 runtime tests, 8 corpus tests, 21 mutation self-tests, all eleven plan-refutation fixture groups, formatting, lifecycle/recovery/release checks, publication locks, the work-order index, and artifact/verification/feedback evidence checks passed. `git diff --check` is clean. No dependency, kernel or compiler changes were needed; the repair introduces no scope deviation or new design decision.

## Repair of FINAL-001 and workflow breakout — 2026-09-07

The repair actor is Codex CLI 0.153.4 / `gpt-6-astra` / `max`, source `operator-attested` under execution guide 07. FINAL-001 and both numbered verification reports remain unchanged. The original plan-refutation modules and receipts remain as VER-002 verified them; the integration and the [breakout](ideation.md) are the additional subject for the next independent verification.

The sync fetched origin and confirmed the WO-038 merge was contained. It preserved all tracked and untracked work in `WO-041 sync 2026-09-07 after FINAL-001 and workflow ideation`, stash `827132315b7d5d8664b0fa9f15c10545b011906e`, and retained that stash after applying it. `wo-041` fast-forwarded from `01c2e321268bf9cb9842b58b3e95fc8ed5a09d67` to `a360d04ac865e6a55c64f06cd0467f9e69acf9f7` without creating or rewriting a work-order commit. Ignored local intake contained only scaffold files; the three new operator captures were written directly to surviving main intake.

Seven files conflicted. Authored resolutions combined the README descriptions into one release claim, retained both roadmap activation notes, and combined the plan/feedback scripts with WO-038's license fields and checks in `package.json`. The four generated conflicts were regenerated: control projection, work-order index, and both publication locks. The selected repairing order cannot run `resume next`, so the documented projection refresh through the known closed WO-038 was used; no repair-completion event was fabricated for that purpose. The control segments did not conflict.

`release prepare --local` then retimed the unpublished target from `v0.13.2` to `v0.13.3` and wrote its dated roadmap note. Skeleton's manifest and lockfile entry were bumped to `0.12.1` before the fresh audit. The separate release-preparation helper validates every source before editing, refuses ambiguity or a closed/mismatched order, preserves strict-version boundaries and unrelated text, and restores prior bytes on a write failure. It never appends a control event or alters Git refs. Local release-surface validation now runs early in `npm test`, using only releases in the subject's `HEAD` ancestry so a shared tag from an unintegrated sibling cannot change its verification baseline; publishing retains the origin check. A real-Git fixture with a newer non-ancestor tag failed before this filtering and passes after it, while a missing component bump against the ancestral baseline still fails.

The feedback source reader now keeps release-only workspace version/license metadata outside its behavior subject, using clearly labeled dependency projections in the verifier capsule. Runtime/fixture bytes, scripts, exports, dependency constraints, publication controls, external dependency versions/resolutions/integrity, and unknown metadata remain inputs. The projection implementation and its fixture file are themselves pinned. Tests independently vary these inputs and verify both stable and invalidating cases; the existing self-host/recovery tests still pass. This is the bounded correction to the coupling that caused the license merge and version bump to require another live audit, not a general evidence-impact analyzer.

The completed repair passed `npm test`: 7 release-preparation tests, 9 license-surface tests, 285 runtime tests, 8 identity-corpus tests, 21 mutation self-tests, all eleven plan-refutation fixture groups, lifecycle/recovery/worktree/release/index suites, formatting, publication, and all artifact/verification/feedback evidence gates. The [validation summary](integration-checks.txt) captures the relevant output and additional checks. The standalone plan gate and origin release-surface preflight passed; publication covers 235/235 headings with both edition locks current, and both working/index diffs are whitespace-clean. The original numbered reviews and plan receipt pair match the preserved pre-sync bytes; published WO-011 evidence and all compiler/kernel files have no diff from the integrated base. The current repair adds behavior and therefore still needs independent verification; integration bookkeeping alone will not require a repeat cycle under the corrected contract.
