# WO-041 parallel-workflow correction — 2026-09-07

The operator opened this breakout during the repair of FINAL-001 and explicitly directed an immediate correction to the repeated review cycle. A second message rules out phase dependencies between parallel workflows; a third retains the operator's voluntary discipline of finishing one final review → PR → merge → release-close window before beginning another. These instructions expand WO-041 to the bounded changes below; the original plan-refutation deliverable and its evidence remain in scope.

## Capture and clean-room synthesis

The three unedited messages are preserved in the main control-plane checkout's ignored `docs/intake/notes/2026-09-07-wo041-parallel-workflow-correction.md`, SHA-256 `8ade09bc4c735915516f14fb3fb754f1db9a69a2f21efa29b48f7aa1d1ab742c`. The main checkout was resolved through Git's worktree inventory. No worktree-local intake reconciliation is pending.

Effective Clean Room supports: the locked employer/credential exclusion floor, Shape-First Synthesis, public vocabulary, provenance, and breakout review. The messages concern this repository's observed workflow and contain no employer material, credentials, internal services, or proprietary implementation. No stop condition was found. The durable text is synthesized; there is no direct-filing exception.

The intended interaction is independent progress with integration handled by the machinery. Parallel execution has failed its purpose when an unrelated merge creates a mandatory extra fix, verification, and review cycle. Moving that cost into a requirement that one order wait for another's phase is also rejected. Serial publication of changes to one Git branch remains distinct from ordering the work orders' lifecycles.

## Authorized correction

- Preserve phase-independent lifecycle legality for every order. No sibling-phase barrier, synchronized wave, admission queue, or default return to serial execution is introduced. Implementation and verification overlap freely; the operator voluntarily serializes each final-review-through-release-close window. A gate for that window is an open option only.
- Distinguish integration bookkeeping from a changed acceptance subject. A reviewer may incorporate independently reviewed upstream changes, regenerate projections, and retime an unpublished release under its existing classification, then check and disclose the integrated result. Those operations alone do not manufacture a failed final review or require a fresh semantic verification. An actual acceptance defect or a resolution that changes behavior still needs independent evidence for the affected claims; no historical report is rewritten or represented as certifying bytes it never saw.
- Automate the bounded release chores now: an explicit release-preparation command retimes a colliding unpublished target and its source claim, and the ordinary evidence gate checks release surfaces against local releases in the subject's own ancestry before expensive suites. An unintegrated sibling's tag cannot change that verification baseline. Publication keeps its authoritative remote check. Neither command reads or gates on another order's phase.
- Give feedback evidence a declared dependency projection: workspace release versions and license metadata do not invalidate the feedback behavior audit; source, executable configuration, dependency selection, and external dependency versions/integrity still do. Record a new live edition for the projection change itself and prove both unchanged and invalidating cases with fixtures.

The broader sync helper remains within WO-033's existing scope, corrected to preserve lifecycle independence and to classify evidence impact instead of automatically returning an integrated order through repair. This breakout does not claim a general semantic conflict resolver or a new scheduler.

## Durable homes and review

The ledger entry is **2026-09-07 independent workflows and evidence-scoped integration**. Product homes are execution guide 07 (verification and final-review authority, independent integration) and domain model 02 (feedback source projection). The playbook, concurrent-work-orders plan, phase-two plan, work-order map, and WO-033 draft must agree with that rule; the marked order sequence is unchanged. WO-041's scope amendment names the implementation and tests. Publication index/locks and the work-order index are regenerated.

The independent verifier and final reviewer must read this receipt and the changed contracts, inspect the release command and dependency projection, execute their negative fixtures, and check the new live feedback edition. FINAL-001 and VER-001/002 remain immutable, time-indexed evidence. The current repair still addresses FINAL-001's real release-surface defects; this correction does not erase them.

The executable additions are `scripts/lib/release-preparation.mjs` and its `scripts/release.mjs` entry points, `scripts/test-release-preparation.mjs`, the local/ancestry cases in `scripts/test-release.sh`, and the projection plus `packages/skeleton/test/feedback-audit-source.test.ts`. The root test command registers their checks. These are self-referential instruments: the release helper prepares this order and the new source reader binds its live evidence. Real-Git refusal fixtures, independent expected edits, write-failure recovery, and positive/negative dependency mutations establish the instruments' behavior separately from their successful use here. Full acceptance remains with the independent verifier and final reviewer.

Unresolved scope: general evidence-impact analysis and arbitrary authored merge conflicts are not automated by this patch. They require an explicit affected-claim assessment, with independent verification where behavior changes; merely observing a new base or another workflow's phase is insufficient.
