# WO-039 ideation receipt — incremental exclusions and future list UI

On 2026-09-07 the operator corrected the assumption that a private exclusion list
must be populated in full before handoff, then explicitly dispatched ideation for
an open item covering how to view, add and remove entries through a future UI.
A subsequent instruction authorized an initial private seed. The ongoing
`resume: next` remains the implementation dispatch.

## Capture and clean-room treatment

The correction and ideation messages were captured unedited at
`docs/intake/notes/WO-039-expanded-ideation-2026-09-07.md`. The main control-plane
checkout was resolved from Git's worktree registry. The worktree-local capture
was copied without overwriting an existing destination, verified by byte
comparison in the canonical ignored intake, and backed up. Its provisional copy
is preserved; this batch has no pending canonical reconciliation. The backup is
an operational snapshot, not an evidence dependency.

The public synthesis uses Shape-First Synthesis under the saved Clean Room
loadout: preserve the interaction and progressive maintenance requirement,
rewrite the wording, retain provenance, and keep the employer/secret floor.
No additional review support or direct-draft filing exception is selected.
The ideation messages contain no clean-room stop condition. The separately
supplied exclusion values are private checker configuration, not ideation inputs:
they exist only in the ignored local list in the active and main checkouts and
are neither reproduced nor hashed here. No private values enter product text,
the ledger, generated configuration, discovery or evidence.

## Durable outcome and scope

- The ledger's **WO-039 ideation — incremental exclusions and a private list
  editor (2026-09-07)** section records the adopted incremental-list correction
  and the candidate UI with source provenance. It explicitly supersedes the
  earlier list-present prerequisite without rewriting that historical entry.
- WO-039's design and criterion 8 now allow an honestly reported unavailable
  screen when no terms are known. They still require a successful screen when
  the list is configured and a refusing synthetic fixture. The existing
  checker already behaves this way; no executable code changes in this breakout.
  A present empty or malformed file still refuses, so the current unconfigured
  representation is an absent file.
- [Domain model 02](../../product/02-domain-model.md#harness-compiler-v1) and the
  skeleton README explain incremental private data and the unchanged locked
  boundary. [Interfaces 04](../../product/04-interfaces.md#candidate--private-exclusion-list-management)
  proposes Clean Room → Excluded terms, with inspection, add/remove actions
  and No terms configured. The planning map and roadmap carry the open item.
- The publication index and both edition locks include the new candidate.
  The executor README records the actual configured-list screen. The earlier
  full test transcript retains its then-unavailable observation.

The future UI host, final layout, storage reconciliation and work-order
allocation remain open. No UI, schema, contract, dependency, lifecycle rule,
model configuration or additional activation is introduced. This candidate does
not change the planning sequence or turn the ideation dispatch into a planning
pass. Existing WO-039 implementation and immutable evidence are preserved.

## Required review and validation

The eventual independent verifier and final reviewer must read this receipt,
the amended criterion, new ledger section and promoted product documents as part
of WO-039. Check faithful synthesis, the explicit supersession, privacy of the
local data, honest unavailable/present reporting, links and edition freshness,
and that the UI remains a candidate. No initial list is claimed to be exhaustive.

The [executor evidence](README.md#validation) records the successful full gate
before this documentation-only breakout and the subsequent affected checks.
Generated-surface identity, all four live role observations and the runtime
source remain unchanged; they do not need new live episodes for these doc edits.
