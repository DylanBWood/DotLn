# WO-114 implementation evidence

Dispatch: `resume: next`, 2026-09-24. Subject: the current WO-114 worktree
after the `runtime-status-v1` implementation and evidence edition refresh.

Historical implementation receipt. VER-001 found defects in this subject;
the repair changes and current evidence are recorded in [repair.md](repair.md).
In particular, the syncs described below were removed, index selection is now
shared across writers, and read/publication failures are contained.

The resident projects its folded event log and the generated work-order index
to `runtime-status-v1.json` under the store directory. Each append publishes
through a temporary file, file sync, atomic rename and directory sync while
the append lock is held. A read-only transaction on restart rebuilds the same
file. The projection selects facts rather than copying event payloads or actor
specifications. Missing or malformed index data is marked unavailable.

The console decodes the versioned file, renders actors, live episodes,
presence and cadences, holds, budget and open orders, and watches the store
directory for atomic replacement. The existing actor board command and its
five recorded fixture cases remain intact. The published JSON schema lives at
`packages/console/runtime-status-v1.schema.json`; the console exports the
matching type and decoder.

The fake-clock resident fixture pins the SHA-256 of the status file at start,
after a presence event, during a live episode, after a clock tick, and after
the outcome, in that order:

```text

```

The restart assertion deletes the projection, starts a new resident host from
the same log, and compares the complete regenerated bytes to the final file.
The privacy assertion checks that the file contains no fixture store path,
command, working directory, endpoint, session-id or host-name fields. The
console fixture checks every section and observes a replacement through its
watcher. The WO-032 board fixture check preserves its existing bytes.

Executed checks on this subject:

| Check | Result |
| --- | --- |
| `npm test` | 27 suites passed, 0 failed; 344.96 s; 71 fresh tasks |
| `npm run test:docs` | 21 passed, 0 failed; 24.25 s |
| `node scripts/console-fixtures.mjs --check` | Five existing cases matched JSON, terminal and HTML |
| Authority and feedback evidence `--check` | WO-114 revision 001 current; live feedback audit judged the changed presence policy |
| `npm run harness -- check` | 31 generated surfaces matched |
| `npm run publication:check` | 275 product headings covered; both audience tables current |
| `npm run release -- check-surfaces --local` | v0.47.0 and changed component pins passed; local tag observation only |
| `npm run format:check`; `git diff --check` | Passed |

The new feedback edition records one live `codex-cli-exec` verifier episode,
ten passing fixtures and ten corresponding removal failures. A scratch-path
alias stopped the first attempt before worker launch; the same store resumed
through its physical path and completed. The selected authority edition
captures the regenerated harness snapshot. The existing board fixtures retain
their historical evidence inputs as required by WO-114 criterion 2.

The resident status file remains a disposable read-only projection; it grants
no command or authority. The Angular or loopback consumer, audit projection,
independent verification, final review and publication are separate work.
