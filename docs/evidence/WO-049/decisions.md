# WO-049 decisions

## 2026-09-16 — repair boundary and housekeeping

`resume: fix` selects VER-001 F1 and the operator-authorized F5. F1's stored
substring owns a separator that user appends now need, and accepts duplicate
blocks. Locate one exact marker pair, validate its current bytes before any
mutation, preserve the separator when a suffix follows, and restore the original
EOF state only for an unchanged terminal separator. Refresh the exclude baseline
when an inactive installation restarts. Regression probes must prove the user's
appended ignore rule still hides its output and that duplicate blocks refuse
without mutation. F5 adds the real unspaced effort selector to the existing
attestation normalization, preserving old reports and events.

This repairs the external-worker prerequisite for WO-052/WO-053 without widening
worker authority. NoOp leaves a demonstrated loss of user ignore semantics and
incorrect actor records; deleting all matching substrings could erase user
content; widening shell permissions would not address either cause. The smallest
useful intervention is marker ownership plus the existing selector mapping.
Policy resistance, drift and rule beating are addressed by Git-visible effects
and refusal-before-write checks. Commons and escalation favor targeted probes
and one integrated gate; success-to-the-successful does not justify keeping the
faulty substring method. Intervenor burden falls because removal preserves user
rules automatically. The goal is a safe target lifecycle, not receipt volume.
Naive Interventionism favors keeping shared-worktree union behavior, Contributor
semantics and original verification evidence. Reopen if a new ownership case
cannot be resolved without storing user content or changing authority.

The operator also reported WO-051's merge to main and requested standard
housekeeping. Local main contains that merge and local v0.24.0; it consumes
skeleton 0.20.0 while compiler remains 0.11.2. Integrate the preserved pending
work, retain the existing minor classifications, and prepare application
v0.25.0 / skeleton 0.21.0 / compiler 0.12.0. This is integration bookkeeping,
not a new finding against VER-001. Assess and test the integrated bytes before
handoff. Historical reports and evidence keep their original cutoffs.

## WO-049-D007

```json
{
  "id": "WO-049-D007",
  "date": "2026-09-16",
  "dispatch": "resume: fix; operator main-merge housekeeping direction",
  "decision": "Repair exclude-block ownership and the unspaced effort selector; integrate WO-051 and retime the unpublished minor release while preserving prior evidence.",
  "evidence": [
    "docs/verifications/WO-049/VER-001.md F1 and operator-authorized F5",
    "Operator housekeeping direction during this repair",
    "Local main includes WO-051 and v0.24.0; its skeleton is 0.20.0 and compiler 0.11.2"
  ],
  "rejected": [
    { "option": "NoOp", "reason": "Leaves demonstrated ignore-rule damage and incorrect selector records." },
    { "option": "Remove every matching substring", "reason": "Ambiguous ownership must refuse before mutation." },
    { "option": "Rewrite historical evidence after integration", "reason": "Prior reports judge their recorded subject, not the integrated bytes." }
  ],
  "reopenWhen": "New ownership evidence or another merged release changes the bounded repair or release baseline."
}
```

## WO-049-D008

```json
{
  "id": "WO-049-D008",
  "date": "2026-09-16",
  "dispatch": "resume: fix; Adjacent Repair",
  "decision": "Correct target capability provenance, reject unloadable backslash import roots, and document the existing opaque-command boundary without weakening it.",
  "evidence": [
    "docs/verifications/WO-049/VER-001.md F2, F3 and F4",
    "docs/discovery/writing-worker-smoke-2026-09-14.md C-W4, X-W3, X-W4, X-W10",
    "packages/skeleton/src/worker-transport.ts source-change allowlist still loads project hooks"
  ],
  "rejected": [
    { "option": "NoOp", "reason": "Leaves contradicted capability provenance and roots known to generate unloadable hooks." },
    { "option": "Broaden shell authorization or add a capability schema", "reason": "Unnecessary authority and compatibility changes for these bounded corrections." }
  ],
  "reopenWhen": "WO-052/WO-053 supplies a tested host-authorized opaque-command route or new capability observations change the target profile."
}
```

These adjacent changes share the target/compiler checks and advance the same safe
external-worker outcome. Policy resistance and rule beating favor explicit scope
reasons and early import-root refusal. Drift and wrong-goal risks favor accurate
claims over apparent feature completeness. Commons, escalation, successful-
incumbent bias and intervenor burden favor the existing profile shape and a
bounded validation rule over a new capability model or per-launch rescue. Naive
Interventionism keeps valid URL characters, existing authority and Contributor
behavior. F2 stays an explicit integration limit: the merged transport's Bash
allowlist does not override target permission denials; a host execution route
needs its own source-change integration evidence.

## 2026-09-16 — evidence before claims

Operator dispatch: `resume: next`, followed by `scope expand:` requiring an
immediate end to guessing, illustrated by a recurring conversational phrase
that admits it. The existing executor skill says never to guess, but the hand-written
shared instruction did not carry that rule for every role. Added it to
`CLAUDE.md`, which `AGENTS.md` shares, outside the generated block.

The chosen rule requires checking available evidence before dependent claims
or actions, identifying unknowns, and labeling supported inference. Corrections
must name the error and checked evidence directly. A new compiled support would
add machinery without improving this instruction's coverage; NoOp would leave
the reported behavior unaddressed. This supports reliable operator flow and
reduces repeated operator correction. It does not claim mechanical detection of
guessing or guarantee model obedience.

System traps: policy resistance and rule beating are addressed by specifying
observable evidence duties instead of only banning a phrase. Drift and wrong-goal
risks favor factual accuracy over agreeable wording. Commons, escalation and
intervenor-burden risks favor one shared rule over extra process or repeated
operator rescue. Success-to-the-successful does not favor a compiled support
without evidence of added value. Naive Interventionism: preserve the clean-room
floor and generated behavior; this reversible instruction is the smallest useful
change. Reopen if observed failures show a specific enforceable mechanism is
needed. This scope expansion authorizes the hand-written floor addition; it does
not change WO-049's byte-identity obligation for the generated Contributor block.

Same-day correction: I misread the example phrase as a separate behavior to
prohibit. The operator meant to prevent the unsupported reasoning that produces
it. A phrase ban could conceal that failure without correcting it, so I removed
the ban. The rule instead requires evidence before dependent claims and actions;
changing wording cannot discharge that duty. No mechanical detection is claimed.

## 2026-09-16 — target installation approach

WO-049 and the cited writing-worker observations require a reduced target
profile, immutable launchpad runtime imports, off-target state and exact local
ownership. This enables the external source-change path (WO-052, WO-072, WO-075
and WO-053). NoOp leaves that path blocked. Keep the Contributor lowering's
existing default output unchanged; target behavior is explicitly selected.

Reject copying runtime bytes into the target, committing target configuration,
publishing a package, unavailable Codex hooks and merging existing settings, for
the reasons in the work order. Exclude and manifest ownership must be checked
before mutation; fail closed on unavailable target runtime. Existing Contributor
advisories remain useful for its interactive recovery and are not target policy.

System traps: policy resistance is bounded by the compiled envelope; commons and
escalation by one small installation and no target skills or lifecycle observers;
drift by real decision and tree fixtures; successful-incumbent bias by explicitly
comparing local emission with the declined alternatives; intervenor dependence
by reusable emit/check/remove; rule beating by executing generated hooks and
checking Git visibility; wrong-goal risk by measuring the external-worker
prerequisite. Naive Interventionism favors an opt-in profile and scratch-repo
probes, preserving existing consumers. Reopen from contrary live observations or
a separately authorized settings-merge or runtime-retention design.

Operator clarification: criterion 4 permits required version, pin and hash
updates while preserving Contributor behavior and relative-import generation.
Literal generated-byte equality is judged at equal activation inputs; new
runtime identities must be regenerated under criterion 8.

Actor readback for this dispatch: Codex CLI 0.154.0, GPT-6 Astra, raw effort
`ultra` (`xhigh` + workflows), source `codex-session-readback`. The active
thread's session metadata and latest turn context provide these values. The
entry usage observation lacked a fresh token counter at its cutoff; that did
not make model, effort or CLI metadata unavailable.

Same-day correction: I inserted the operator's Codex preference into the shared
Claude instruction floor and claimed effective readback was unavailable without
inspecting the accessible session record. Both were wrong. Removed that default
and made existing Codex briefings, status and harness begin/usage output report
the active thread's properties automatically. Reporting is independent of token
counter freshness and adds no admission check, refusal or approval. The
operator explicitly requested information delivery without additional process.
Generated role text now points to actual readback instead of a hardcoded model
default; this separately authorized correction changes that text while retaining
Contributor hook behavior and relative-import generation.

## WO-049-D001

```json
{
  "id": "WO-049-D001",
  "date": "2026-09-16",
  "dispatch": "resume: next; scope expand: evidence before claims",
  "decision": "Put the no-guessing rule in the shared hand-written instruction, requiring checked evidence before dependent claims and actions.",
  "evidence": [
    "Operator scope expansion on 2026-09-16",
    "CLAUDE.md",
    "Existing executor Never guess duty"
  ],
  "rejected": [
    {
      "option": "NoOp",
      "reason": "Leaves the reported repeated unsupported claims unaddressed."
    },
    {
      "option": "New compiled support",
      "reason": "No observed mechanical detection to justify added machinery."
    }
  ],
  "reopenWhen": "Observed failures identify a specific enforceable mechanism beyond shared instructions."
}
```

## WO-049-D002

```json
{
  "id": "WO-049-D002",
  "date": "2026-09-16",
  "dispatch": "operator correction during resume: next",
  "kind": "correction",
  "decision": "Remove the phrase ban and address unsupported reasoning directly.",
  "misread": "Treated an example admission of guessing as a separate wording defect.",
  "meant": "Prevent guessing; the phrase is evidence of the reasoning failure.",
  "changed": "Removed the phrase ban and stated that wording changes cannot correct unsupported claims.",
  "evidence": [
    "Operator correction on 2026-09-16",
    "CLAUDE.md"
  ],
  "rejected": [
    {
      "option": "Keep phrase ban",
      "reason": "Could conceal guessing without correcting it."
    }
  ],
  "reopenWhen": "Evidence shows the instruction needs a concrete additional evidence-checking behavior."
}
```

## WO-049-D003

```json
{
  "id": "WO-049-D003",
  "date": "2026-09-16",
  "dispatch": "resume: next; operator pin-update clarification",
  "decision": "Add opt-in target lowering and fail-closed execution with immutable launchpad imports and off-target state; preserve Contributor behavior, allowing required version/pin/hash updates.",
  "evidence": [
    "docs/work-orders/WO-049-target-worktree-bundle.md",
    "docs/discovery/writing-worker-smoke-2026-09-14.md",
    "Operator permits required pin updates while preserving behavior"
  ],
  "rejected": [
    {
      "option": "Copy runtime or commit bundle into target",
      "reason": "Violates target bundle boundary."
    },
    {
      "option": "Reuse Contributor fallback",
      "reason": "Its advisory behavior does not enforce target denials."
    },
    {
      "option": "Codex hooks or settings merge",
      "reason": "No supporting observations; outside this order."
    },
    {
      "option": "NoOp",
      "reason": "Leaves external source-change prerequisites blocked."
    }
  ],
  "reopenWhen": "Contrary live observations or authorized settings-merge/runtime-retention work changes the boundary."
}
```

## WO-049-D004

```json
{
  "id": "WO-049-D004",
  "date": "2026-09-16",
  "dispatch": "resume: next",
  "decision": "Stage application v0.24.0 with compiler 0.12.0 and skeleton 0.20.0; retain kernel and console versions.",
  "evidence": [
    "Local latest tag v0.23.0",
    "WO-049 minor classification",
    "Changed compiler and skeleton source; additive APIs"
  ],
  "rejected": [
    {
      "option": "Bump unchanged packages",
      "reason": "No changed component source requires it."
    },
    {
      "option": "No release",
      "reason": "Standing classified release preparation applies."
    }
  ],
  "reopenWhen": "Final-review integration observes a newer published version and retimes under minor classification."
}
```

## WO-049-D005

```json
{
  "id": "WO-049-D005",
  "date": "2026-09-16",
  "dispatch": "operator correction during resume: next",
  "kind": "correction",
  "decision": "Automatically report accessible active Codex session metadata in existing workflow output without adding a progress gate.",
  "misread": "Put a Codex preference in the shared Claude instruction floor and claimed effective readback was unavailable without inspecting the current session record.",
  "meant": "Report the model, effort and CLI version the current Codex harness actually exposes; add no red tape.",
  "changed": "Removed the misplaced default; read active-thread session_meta and latest turn_context independently of token counters; include their values in existing briefings, status and harness begin/usage output; replace stale generated role defaults with readback guidance.",
  "evidence": [
    "Operator correction on 2026-09-16",
    "Active local session metadata: CLI 0.154.0, gpt-6-astra, raw effort ultra",
    "scripts/test-codex-session.mjs"
  ],
  "rejected": [
    {
      "option": "Hardcode the operator preference or infer readback from token availability",
      "reason": "Neither reports the actual current session properties."
    },
    {
      "option": "New admission gate or approval",
      "reason": "The operator requested reporting and explicitly rejected additional progress gates."
    }
  ],
  "reopenWhen": "Observed changes to local Codex session records require updating the informational reader."
}
```

## WO-049-D006

```json
{
  "id": "WO-049-D006",
  "date": "2026-09-16",
  "dispatch": "operator correction during resume: next",
  "kind": "correction",
  "decision": "Keep the operator's GPT-6 Astra/ultra preference scoped to this Codex harness; separately launched DotLn workers follow their task assignments.",
  "misread": "Extended the operator's Codex harness preference to the separately launched feedback-audit verifier and supplied --effort ultra.",
  "meant": "The preference applies within the Codex harness, not to DotLn's independent worker-selection decisions.",
  "changed": "Corrected the interpretation for subsequent worker calls. No DotLn worker-selection default was changed; completed audit evidence retains the actual invocation. Future smoke records label the supplied xhigh selector directly and leave unobserved workflow mode unknown.",
  "evidence": [
    "Operator scope clarification on 2026-09-16",
    "Completed feedback-audit CLI invocations with --effort ultra"
  ],
  "rejected": [
    {
      "option": "Apply the harness preference to every child Codex invocation",
      "reason": "Exceeds the operator's clarified scope."
    }
  ],
  "reopenWhen": "The operator explicitly changes the preference's scope or a worker's task assignment."
}
```
