# WO-126 ideation — context continuity and observation before caps

The operator's 2026-09-09 `ideation:` dispatch asks for automatic context
monitoring and evidence that actors can continue the same task through
automatic compaction. Two budget answers direct leaving the unspecified token,
dollar and PR-body limits unset and measuring token usage first. The breakout
is document-only; the eighteen authorized WO-126 implementation criteria
continue, with that explicit budget clarification.

The four source messages are preserved unedited in ignored
`docs/intake/notes/WO-126-expanded-ideation-2026-09-09-context-continuity.md`,
SHA-256 `f8457865bed5b49c4708359afd874bdeef731f2a0b6d9cac29577de8b2855741`.
The worktree inventory resolved the main control-plane checkout before capture.
The active write boundary permits provisional worktree-local staging; the
capture is preserved and backed up with the existing intake backup command.
Canonical reconciliation into main's ignored intake remains required before
removing this worktree, through WO-126's closeout helper when available. No
source or backup is discarded by this breakout.

Clean-room review found ordinary personal workflow feedback and no employer,
credential or private-service content. The treatment is Shape-First Synthesis
under the locked floor; this is rewritten product understanding, not an
operator-authored public draft filed verbatim. The ledger's dated context
continuity section and product 05 §Candidate — Context Continuity are the
promoted surfaces. Product 03's runtime projection and the planning map's
unallocated candidate link to the same model.

The candidate distinguishes context occupancy, dispatch token usage and cost,
requires observed availability and timestamps, and tests resumed behavior
against a pre-compaction oracle. Repeated compactions, in-flight operations,
late steering and stale/missing recovery records are named cases. A summary
without a continuation test, fabricated host percentages, arbitrary caps and
an unqualified zero-loss claim were rejected because they do not prove the
operator's requested outcome. Existing authority and verification boundaries
are recovery obligations, too.

Host binding, warning cadence and implementation allocation remain open. Reopen
the candidate when available signals and a bounded fixture can test a useful
improvement. Reopen numeric token/cost limits after usage observations support
a choice and the operator selects it. No compiler schema, runtime behavior,
support equipment or separate version change is claimed by this ideation.

The verifier and final reviewer must read this receipt and the promoted
sections, check traceability, source treatment, candidate labeling and the
observation-first budget implementation, and confirm publication links and
edition locks. The original WO-126 checks still govern implementation. This
breakout creates no executable helper and requires no independent planning
refutation because it allocates no order or sequence change.

The follow-up capture `docs/intake/notes/WO-126-context-continuity-followup-2026-09-09.md`
records seven further messages: the 81% display observation, prior v1 experience,
the difference between host capacities, a public discovery lead and the exact
experimental setting. The product candidate now includes both collection routes
and the enabled-mode test condition. The 21:10:41.964Z Codex log reported
217,114 last-request tokens and a 258,400-token capacity (84.02% by division);
the operator reported 81% at 21:11:14Z. The timestamps and meanings differ, so
this is a comparison observation, not a calibrated occupancy measurement.
No transcript text or private session identifier is promoted. The exact local
experimental flag was observed enabled; no setting was changed. Claude's
reported one-million-token setup remains operator testimony pending a matching
session payload. This clarification removes an assumption that percentages are
directly comparable or that another recovery mechanism is necessarily needed.
The last two messages place continuity in DotLn's future offline queue and
shared state, with bounded single-use calls as the destination. Product 05 and
product 03 now assign durable work ownership to DotLn and host-specific context
signals to adapters, composing with the already planned resident/work-derivation
work rather than inventing a second queue. This remains an unallocated candidate.

Capture integrity: `docs/intake/notes/WO-126-context-continuity-followup-2026-09-09.md`, SHA-256 `3b2784b9a6d1bfb6c4077b1775a0c4e6af10b03da88acaff961c098fc04c611e`. The existing intake backup command preserves this batch; main reconciliation remains the closeout helper's duty.

Capture integrity: `docs/intake/notes/WO-126-support-behavior-followup-2026-09-09.md`, SHA-256 `eea7654cc0c4d595e25d8f93aa3fc12bd25f146f311ff30e8df377ff33eebd80`. The existing intake backup command preserves this batch; main reconciliation remains the closeout helper's duty.

The later support-behavior discussion is recorded in the same documentation
breakout and [WO-126-D003](decisions.md#wo-126-d003). The operator confirmed the
activation diagnosis, distinguished a separate failure to interpret deliberate
chat intent, required changes to survive sessions and compaction, and left the
executor judgment about whether a sufficiently supported repair fits WO-126.
The chosen disposition preserves the observed gap for planning rather than
claiming another prompt fragment is an executable fix. No order or sequence is
allocated. The prototype and queue history remain retained locally.

The coverage review found that WO-096 classifies governance, WO-097 replaces
six rules with checks/hooks and proves their prose absent, and WO-098 requires
lower directed context per role and detects restatement. These address the
accumulation risk; none explicitly requires the default executor to notice a
missed adjacent task or infer the purpose of operator chat. Proposed next
planning actions: reproduce those omissions in a fresh session and after
compaction; map each behavior to an existing executable host or scoped model
judgment; test removal of the mechanism and measure context, commands and
operator interventions. Extend an existing order only if that coverage is
explicitly selected; otherwise allocate a bounded gap. Product 05 and the map
retain this as an unallocated candidate, not implemented behavior.
