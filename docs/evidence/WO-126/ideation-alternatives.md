# WO-126 ideation — recurring review of implementation alternatives

The operator's 2026-09-09 `ideation:` dispatch asks for a method to surface
useful alternatives by default, automatically or periodically. The motivating
case is the Node/Python publication comparison during `resume: fix`. This
document-only breakout preserves that general question; the authorized repair
continues to its evidence gate and ready-to-verify handoff.

The unedited message is captured in ignored
`docs/intake/notes/WO-126-recurring-alternative-review-2026-09-09.md`, SHA-256
`1ef47aa028bd3b3ae31732a13f9f2eb452e7b2661097dda95ea612d00d21b3f0`.
The worktree inventory resolved the main control-plane checkout. The capture
is provisionally staged in this writable worktree and backed up with the
existing intake helper; reconcile it into main's ignored intake before this
worktree is removed. No raw source or recovery copy is discarded.

The source is personal product/workflow ideation; clean-room review found no
employer, credential or private-service material. Shape-First Synthesis rewrites
it into the ledger entry “Surface useful alternatives without waiting for an
objection” and product 07's matching candidate. This is not direct filing of
operator-authored public prose. The follow-up register collects that candidate
through the existing metadata command.

The candidate compares default, event-triggered and periodic review against the
existing four process questions, meter and Entropy Reducer. It preserves the
purpose, makes changed guarantees explicit, and evaluates discovery benefit
alongside the cost of asking. A blanket no-dependency rule, a new always-on
gate, and more prompt prose without observed behavior are not selected.
Trigger, cadence, host binding and allocation remain open; planning may choose
them when a bounded experiment can test benefit and overhead. There is no new
runtime behavior, schema, schedule, version change or executable helper.

The WO-126 verifier and final reviewer must digest this receipt, the appended
ledger entry and product candidate. Check source treatment, traceability,
candidate status, overlap with existing mechanisms, internal links and the
retained follow-up. The document gate validates this breakout. It allocates no
order or sequence change and therefore needs no new planning refutation.

The operator's follow-up is captured unedited in
`docs/intake/notes/WO-126-experiment-support-followup-2026-09-09.md`, SHA-256
`f2f7404c350577acf51f5ae991b3c59121a68b36e4fb372d2b216d00f469f00f`, with the same
provisional intake reconciliation and clean-room treatment. Product 05's
Tinkerer / Scientist candidate and a second appended ledger entry preserve the
experimental bias and reuse the established term Support modifier for a possible
activation-rate control. Product 07 links to that behavioral proposal. A/B is
one candidate experiment shape; names, trigger/rate policy and allocation remain
open. The verifier and final reviewer also read that product 05 section and
ledger entry, including the distinction between proposed general modifiers and
the already-compiled independent support switches.

The second follow-up is captured unedited in
`docs/intake/notes/WO-126-historical-comparison-followup-2026-09-09.md`, SHA-256
`8d1dafccbbd7ee9a4b438fa0e31c20ba5932ff68a481c005724bee6e19ff84df`, with the same
capture, backup, reconciliation and clean-room treatment. Its distinct product
05 candidate and ledger entry require the future behavior to collect and retain
comparisons when a decision forces another method with roughly the same purpose,
for historical value even after the choice is settled. Automatic activation or
equipped support remains open. Required review includes that distinction from
occasional experimentation, the Node/Python source example, and honest comparison
limits rather than assumed equivalent guarantees. No automatic collector is
implemented by this ideation breakout.

The emoji question is captured unedited in
`docs/intake/notes/WO-126-emoji-substitution-2026-09-09.md`, SHA-256
`571463598a36d6798b06f7044a861256eaa8b16c6a7f3bcdf8d0b435493804fd`, with the same
source treatment and reconciliation duty. Product 05's emoji-shorthand candidate
and the appended ledger entry preserve it as an experiment, with no selected
substitution policy. The read-only probe used existing Node 22.2.0
`Buffer.byteLength` on these literal pairs:

| Word      | Emoji | Word UTF-8 bytes | Emoji UTF-8 bytes |
| --------- | ----- | ---------------: | ----------------: |
| done      | ✅    |                4 |                 3 |
| package   | 📦    |                7 |                 4 |
| test      | 🧪    |                4 |                 4 |
| scientist | 👩‍🔬    |                9 |                11 |

No executable helper was created. None of the probed project tokenizers
(`tiktoken`, `js-tiktoken`, `@dqbd/tiktoken`) was available, and no dependency was
installed. Actual model-token, scanning and behavioral measurements remain
open. The primary tokenizer documentation linked from the candidate supports
the distinction between characters/bytes and vocabulary-based tokens; it is
not an emoji task-performance study. Required review includes that candidate,
ledger entry, source treatment and evidence limits.

The browser-runtime question and Electron/3D follow-up are captured unedited in
`docs/intake/notes/WO-126-browser-runtime-2026-09-09.md`, SHA-256
`37ca0f46e4c4f32dc0caa0943c5ae94a365f9723e1b9749d65e4c2c63c86db29`, and
`docs/intake/notes/WO-126-desktop-3d-runtime-2026-09-09.md`, SHA-256
`abb2a0b3e51657a6844520bcd365667f84d626e3c84dc01896fb5ab916c8d58c`.
The same clean-room synthesis, backup and main-intake reconciliation duties
apply. Two appended ledger entries and product 04's resident-3D-host candidate
preserve browser execution, desktop packaging and graphics as related but
separate choices within the existing plural-host contract. No old ledger entry,
framework decision or work allocation is replaced.

Current MDN and Electron documentation was resolved and fetched through
Context7; direct primary documentation from MDN, Electron and Tauri supplied
the linked lifecycle and architecture facts. Electron as the quickest resident
desktop route for the existing TypeScript/Node project is an executor inference,
not a benchmark or operator selection. A browser scene prototype, browser plus
resident companion, Electron and a viable Tauri configuration remain comparable
options. Rendering performance, footprint and recovery are unmeasured. No app,
new dependency, service or experimental helper was created. Required review
includes the product 04 candidate, both ledger entries, source treatment,
unattended-work requirements, provisional status and future comparison criteria.

The full eight-system-traps dispatch is captured unedited in
`docs/intake/notes/WO-126-eight-system-traps-2026-09-09.md`, SHA-256
`ea530b902ce83cf5fb53832b18a1ed7f4a9757a345196c7d5063401486eb10bc`, including
the operator-supplied source links. The same clean-room screen, synthesis,
backup and main-intake reconciliation duties apply. A new product 05 candidate,
product 07 cross-reference and appended ledger entry preserve all eight traps,
possible escapes and candidate applications. The existing five implemented
meter signals are not represented as coverage of all eight or as causal proof.

The supplied secondary pages were inspected; the YouTube page was unavailable
to the text reader, so no video viewing or transcript verification is claimed.
Meadows' primary essays linked in the candidate ground the general feedback,
rules and goals approach. Project-specific examples and development-process
diagnoses remain hypotheses. Proposed whole-file/changed-region review and
evidence-invalidation comparisons collect usefulness and cost; neither waives
the current gates. No executable helper, guard change, meter extension or work
allocation is part of this ideation. Required review covers all eight preserved
relationships, source treatment, speculative status, the five/eight distinction
and the retained follow-up. The document gate validates the write-back.

The adaptive Tinkerer dispatch is captured unedited in
`docs/intake/notes/WO-126-adaptive-improvement-pressure-2026-09-09.md`, SHA-256
`ba46ff9ee7f30d28fb74827546698ce31a5c8fb6f581fd0c4529bcb75cb1d0f5`, with the
same source handling, backup and main-intake reconciliation duty. It extends
the existing product 05 Tinkerer candidate and adds a ledger entry. Increasing
pressure since a demonstrated gain, a recent-gain quiet period, eventual
broader discovery beyond the current order, and more inventive experimentation
are preserved; the example duration and daily gain remain unselected.

The linked developer explanation describes PoE's historical evasion entropy
counter, while James Clear's own article supplies the small-gain compounding
analogy. Neither source proves this proposed activation policy will improve a
repository. Required review covers the distinction between opportunity and
outcome, clocks and reset evidence, valuable no-change findings, budget/cooldown
behavior, authority for broader work, and resistance to cosmetic improvement
or escalating overhead. No scheduler, experiment runner, modifier schema or
default equipment is implemented. The existing candidate remains retained in
the follow-up register with its updated source.

The unchosen-alternatives dispatch and its immediate comparison context are
captured unedited in
`docs/intake/notes/WO-126-unchosen-alternatives-2026-09-09.md`, SHA-256
`1be662df898ab0a2b7098e8d84657a60fa1144f332f7bd9d1ff9a342bb71fd9e`.
The same clean-room screen, Shape-First Synthesis, backup and main-intake
reconciliation duties apply. Product 05's retained-alternatives candidate and
an appended ledger entry preserve the request across the operator's named
judgment, comparison, intervention and experiment scenarios. The follow-up
retains the earlier outcome-first comparison direction.

Required review covers considered alternatives and reasons, evidence and
uncertainty, distinct dispositions, both sides of action/NoOp, declined
experiments, and conditions for later reconsideration. An untested option is
not represented as an empirical loser, and historical rejection is not a
permanent ban. The proposal reuses decision/comparison evidence and evaluates
retrieval benefit against recording and review cost. Placement, granularity,
activation and retention remain open; no collector, schema, ranking system,
helper or new gate is implemented. The existing follow-up collector retains
the new candidate, and the document gate validates the write-back.
