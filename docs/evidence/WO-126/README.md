# WO-126 execution evidence

WO-126 replaces repeated lifecycle work with authored-output observations,
reusable evidence for an exact Git tree, a staged Node-only build, scripted closeout,
scoped planning refutation and a meter. This is executor evidence; verification
and final review remain separate dispatches.

The executor is Codex CLI 0.153.4, GPT-6 Astra at max, operator-attested.
There is no effective-session effort readback. The source checkout is the
WO-126 worktree at the same activation commit used for the baseline run.
No dependency was added, and no branch commit or publication occurred.

[The VER-001 repair receipt](repair-001.md) records the nine findings, the
operator's amended build-publication boundary, recurring observer cost and
the retained Node/Python comparison. Earlier timings below remain observations
of their named trees; they are not relabeled as repaired-tree measurements.

## Executable coverage

| Criteria                          | Evidence                                                                                                                                                                                                                                                                                                                                                                     |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1: attribution                    | Compiler predicate and process fixtures cover arbitrary agent session trailers and session URLs; six worktree-publication cases reject each form in title and body before push. Removing the settings block makes harness check fail.                                                                                                                                        |
| 2: attestation                    | Resume fixtures retain the old usage/privacy cases and accept a newer patch on an observed line. Process fixtures exercise the bounded discovery probe and one warning for an off-line version. WO-042's control segment remains unchanged.                                                                                                                                  |
| 3–5: outputs, Stop, evidence      | Process fixtures observe two authored files, inherited changes, generated output and an oversized file; exercise all four completion actions; end a turn with unmet work and release the writer; reuse a successful tree check across sessions and invalidate it after a byte changes. Concurrent writers retain all cache rows.                                             |
| 6: gates and build                | Runner fixtures map all 37 previous chain commands, exercise bounded concurrency, serialization, suite selection, expected negative output, timeout reporting and the document-only route. The atomic-build fixture invokes an installed hook continuously and at every replacement boundary.                                                                                |
| 7: closeout                       | Release and worktree fixture transcripts cover main and subject harness state, private terms, new intake, differing-byte collisions, unchanged dry-run state and modern tag evidence reused at the same tree. Every original intake byte remains.                                                                                                                            |
| 8: planning and permissions       | Planning fixtures create a branch only from clean main. Process fixtures distinguish invocation effects from quoted search data and heredoc content, and refuse tools without an effect adapter.                                                                                                                                                                             |
| 9: feedback v2                    | The fresh feedback edition has ten successful present tests and ten failing removal tests. Four units deliver prose, three advise at Stop, and v1 definitions retain their original replay tests. Every generated role carries the correction category rule and worked example.                                                                                              |
| 10–12: budgets, meter, headlines  | Process fixtures cover unset ceilings, dated acceptance and independence from product prose. The generated cold-start table compares installed floor plus skill with v0.16.0. Usage fixtures project actual envelopes and tool metadata without transcript text or private identifiers. Publication prints recent title lengths without refusing a numeric length.           |
| 13–14: decisions and write-back   | Decisions fixtures require dispatch sources and reopening conditions, index a repair choice, recognize inherited ledger duties and preserve the existing ledger. Product guides, playbook, template, receipt README and both profiles were updated. Existing ledger sections remain unchanged.                                                                               |
| 15–16: direct refutation and cost | The 23 planning fixtures include direct prompt-to-committed-receipt execution, malformed/stale refusal, unchanged verdict carry-forward, full scope, missing/unbalanced cost holds, dated acceptance, stale observations and a dispatch clock that cannot be reset by repeating the prompt. The exact unavailable legacy header cannot admit a substantive authority change. |
| 17: systems traps                 | Every trap includes all specified indicators with availability and previous-order deltas; a fixture requires three consecutive worsening deltas before reopening. Unobserved interventions remain unknown.                                                                                                                                                                   |
| 18: final evidence                | Full-gate results, fast-gate timing, the immutable baseline, current meter snapshot, cold-start table and fresh feedback edition accompany this receipt. The verifier and reviewer supply their own later phase and read observations.                                                                                                                                       |

## Measured scope and limits

[Before-gate observations](before-gate.json) measure all 37 original steps on
the operator's machine in an isolated copy of the activation source: 359.147 s.
That run continued through two failures in the old context/evidence checks;
it is a complete timing baseline, not a claimed green historical gate. The
runner fixture compares its command ownership against that commit's package
entry. [Gate timings](gate-timings.json) retain each executed tree and every
suite's duration:

| Gate                  | Seconds | Result                                            |
| --------------------- | ------: | ------------------------------------------------- |
| Original serial chain | 359.147 | 37 measured steps; 2 historical metadata failures |
| Fast gate             | 106.905 | 12 suites passed; under the 120-second budget     |
| Full gate             | 497.559 | 37 suites passed; no failures                     |

The expanded full suite is slower on this observed run. The measured fast
route and exact-tree reuse remove repeated full runs; this receipt does not
claim a faster full gate. Release, planning, worktree and skeleton suites are
reported as split candidates by the runner. Final handoff checks cover the
later receipt and metadata writes without changing these named measurements.

Six existing scenario runners now use named `node:test` cases. Existing
scenario counts are preserved: control segments 7, control beacons 5,
concurrent control 4, work-order index 13, GitHub body 12 and planning 19.
Planning now has 23 cases. The new runner has 6 cases and process debt has 18;
feedback v2 adds 9 package tests. Lifecycle shells retain their assertions.

[Cold-start measurement](harness-context.json) counts the installed floor and
role skill, including generated provenance, for each profile. It excludes
task-specific reads and system prompts. It is a bound on instruction size,
not proof of behavioral compliance or complete session context reduction.

[The first meter observation](meta-baseline.json) is immutable. Later
observations belong in `meta.json`. Unknown historic token, command, read and
intervention values are null; no retrospective counts are invented. Token,
dollar and PR-body caps remain unset by the operator's explicit direction.
The reviewer read comparison starts with WO-042's 135 files and 5,305,410
bytes; WO-126's reviewer has not run yet, so its count and bytes are pending.

[Feedback edition 002](feedback-002/feedback.json) pins the changed source.
Edition 001 remains the earlier immutable source observation. The console now selects edition 002 and distinguishes hard prevention from prose/advice. Its independent live Codex CLI audit completed with every acceptance row
verified. The existing worker profile declares effort unknown; that limited
worker observation is distinct from the executor's operator-attested max
setting. The sandboxed launch failed; the authenticated outside-sandbox
retry completed and its event streams are retained. This edition's matched
feedback projection saves 1,348 instruction bytes. Prose/advisory fixtures
claim delivery or advice, not behavioral prevention.

The historical harness live receipt continues to validate its original
v0.16.0 bundle. Current hook behavior is covered by the changed-source
fixtures and generated pins; it is not relabeled as a new live hook survey.
The authority, artifact-identity and synthetic verification generators write
new WO-126 editions for compiler 0.9.0. The authority check also verifies that
all ten WO-042 evidence files retain their v0.16.0 bytes. The four compiled
fixture programs retain their semantic hashes and authority behavior.
An installed snapshot survives runtime replacement. A fresh checkout first
builds to rehydrate a matching snapshot; no pre-build availability is claimed.

The planning archive retains the original rationale bytes; its link
concordance supplies the new relative locations. Existing receipt subjects
continue to read their committed paths. The narrow WO-126 cost migration
preserves historical receipt bytes and makes no historical cost claim.

The cost-header migration also exposes a remaining output-review cost. An
exact comparison of all 123 legacy orders proves that removing the one added
header and separator reproduces the activation bytes: 1,151,495 original body
bytes are unchanged. The executor still owes whole-file reads for those
authored paths under this order's file-level contract. A later verifier or
reviewer inherits these changes and owes no output read unless it edits them.
The proposed follow-up is to compare reviewed change ranges plus an exact
unchanged-byte proof against whole-file delivery, including stale-source and
substantive-edit refusal. No range exemption or new order is introduced here;
WO-126-D005 records the decision and reopening condition.

[Decisions](decisions.md) record the operator's budget choice, authorized
floor update, compatibility migration and support-behavior deferral.
[Ideation receipt](ideation.md) records the separately authorized context
continuity and support follow-up synthesis. Those candidates do not claim
implemented session or compaction behavior in WO-126.

### Planning follow-ups

The operator expanded WO-126 to close the gap between stored suggestions and
planning entry. [The retained register](../../planning/followups.json) contains
133 formal source records. Planning startup returns a bounded feed; metadata
and document gates check its freshness. Five process-debt fixtures exercise
source change/removal, retained dispositions, stale requests, allocation
targets, duplicate cycles, complete pagination, planning entry, the checked
local-to-public deferral and discovery order across sibling worktrees.

[The measured feed](planning-followups.json) has a 4,441-byte first page,
collected in 32.741 ms. All 127 pending records are reachable once across 16
pages totaling 55,589 bytes; the largest measured page is 4,441 bytes. The
16 source documents total 936,120 bytes. Those document bytes are an inventory
comparison, not a measurement of the prior planner startup. Every page is
bounded to eight rows and 8,192 bytes. Detailed disposition instructions are
a scoped lookup rather than part of the required startup read.

The migration retains 123 records as untriaged rather than inferring historical
allocation or rejection. New formal candidate and decision sources are
collected automatically; archive snapshots, private intake, refutation receipts
and unstructured legacy prose are outside the coverage claim. Unresolved items
persist; current source changes return prior dispositions to review. Committed
history cannot be removed or rewritten through the checked path.

The current planning front page includes Context Continuity (FUP-0091), the
remaining planner-context comparison (FUP-0111), change-range output review
(FUP-0132), and the broader support-behavior deferral (FUP-0130). The existing
local adjacent deferral now points to FUP-0130. This repairs its durable handoff;
it does not claim the broader support behavior is implemented. A later planning
observation can settle FUP-0111 if this feed sufficiently reduces startup reads.
