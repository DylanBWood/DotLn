# WO-126 repair of VER-003

The operator's `resume: fix` selects VER-003 and its four findings. The original
order, prior reports and evidence remain in force. Codex CLI 0.154.0 was observed
through the bounded discovery command; GPT-6 Astra at max is the repository's
operator-attested selection, with no effective model or effort readback.

| Finding | Repair                                                                                                                                                                                                                                                                                    |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F17     | Preserve pipeline inputs, including heredoc producers. Shell input and inline or piped interpreter programs containing literal denied invocations require an explicit adapter. Data-program controls remain allowed.                                                                      |
| F18     | Refuse inline Git alias configuration; classify push plumbing and the supported GitHub API, comment, review and issue writes as remote effects. Read-method and inspection controls remain allowed.                                                                                       |
| F19     | Atomically publish complete session records. Retry a parse failure once and name persistent session corruption. Four concurrent processes completed 400 observer/feedback evaluations without refusal; the installed hook separately refused a damaged record with the correct diagnosis. |
| F20     | Accept a version basename only on the verified CLAUDE_PID ancestor. The installed-hook fixture uses the macOS shape and a different PATH version; unrelated and unverified PIDs fall back correctly.                                                                                      |

The recorded D014 duty is handled by adjacent-0005: the harness wrapper and Stop
consumer accept an existing successful full gate at the exact current tree for
the fast obligation, retaining its original identifier, time and reference. A
fixture executes the gate, changes documentation, records the current full run,
and observes no duplicate test execution. Stale, failed and unexecuted evidence
still executes checks. A separate three-entry-point npm experiment found no
projected-environment differences; environment handling was left unchanged.

Nine focused command, version and concurrency tests passed in 5.955 seconds;
the wrapper regression passed separately in 3.946 seconds. The cold canonical
`npm test -- --fresh` passed all twelve suites in **102.317 seconds**, with
17.683 seconds of margin under the unchanged 120-second budget. The
[measurement](repair-003-results.json) identifies that tree, before the final
pipeline-input correction, documentation and discovery write-backs. Its two input observations took a
combined 1.417 seconds, hashed 414,224,082 bytes and ran 24 subprocesses. These
are filesystem costs, not delivered context. Final exact-tree completion
evidence belongs to the control transition and host gate records.

Output review exposed the same F17 bypass with programs piped into Node and
Python. The correction now screens their inherited input too; its regression
passed in 80.068 ms, including harmless controls and a nested shell. A second
cold fast attempt took 104.966 seconds: eleven suites passed and publication
failed because the product write-back had stale edition locks. Those locks were
refreshed before the final full gate. The failed attempt remains in the
measurement; it is not passing evidence.

The repair adds no dependency, hook or recurring command. Interpreter screening
is in-process; atomic publication adds a temporary-file rename, and the basename
channel uses the existing process observation. Total session token and dollar
costs are unavailable. The permission fixtures classify synthetic commands;
they execute no remote write. Version behavior is proven with process doubles,
not a new live Claude session. This remains a bounded literal-command adapter,
not a complete interpreter or general proof of program effects.

The feedback audit's declared source set is unchanged, so its immutable
WO-126 feedback-002 edition remains applicable. The changed harness has a new
pinned generated snapshot and executable regressions. Decisions D015–D018 and
the operator's conversation/continuation correction D019 are retained in
[decisions.md](decisions.md), with their product write-backs and reopening
conditions. Independent re-verification remains a separate dispatch.
