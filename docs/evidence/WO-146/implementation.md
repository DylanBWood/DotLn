# WO-146 implementation evidence

Status: implementation complete, ready for independent verification. Both bare interactive observations and all four workflow episodes are collected. Product and machinery gates passed; later isolated reporting/status-prose corrections passed their affected checks as detailed below. This report is not independent verification or final review.

**Actor attestation:** {"harness":"codex-cli","harnessVersion":"0.155.1","model":"gpt-6-astra","effort":"xhigh","source":"codex-session-readback"}

**Process cost:** entry unknown; cause harness-no-readback (no counters at the 2026-09-20T07:43:15.268Z entry observation; later counters and final cutoff remain in ignored usage receipts and the handoff).

The continuation resumed inherited Copilot work and integrated requested main at 19e706ad (WO-145), preserving the previous 67b4b188 base, named recovery stashes, checkpoint and separately archived ignored intake. Twenty generated/document conflicts were reconciled; both orders' process-debt fixtures remain. No WO-146 branch commit, publication or verification/final-review dispatch occurred.

## Deliverable and acceptance

| Criteria | Evidence and current result |
| --- | --- |
| 1, 3 | [Dated probe](../../discovery/copilot-cli-2026-09-20.md) retains ten scripted launches. P1–P6 have invalid trust setup and are not trusted-hook evidence; P7–P10 establish corrected callbacks and denials. Claude-form registration is reused once in generated output; complete shared hook bytes and unique settings commands are fixture-checked. I1 records a trust-required blocked start after the operator declined trust. I2 records trusted bare allow-all behavior with no approval prompts, Claude-form denials, native JSON denial failure and the in-session Claude-to-GPT model change; prior personal permission changes are disclosed. |
| 2, 4 | One Copilot contributor profile; six identical role bodies across existing roots, no third tree or copied registration. [Compatibility inventory](compatibility.md) enumerates every changed generated path, reason and protecting fixture. Exactly two worker profiles compare equal to integrated main except the authorized runtime.skeletonVersion field. Existing settings and Codex surfaces are byte-unchanged. |
| 5, 8, 11 | [Security table](../../AI-HARNESS-SECURITY.md#copilot-control-table), [playbook](../../PLAYBOOK.md) and product write-backs distinguish observed enforcement, advisory duties, unavailable telemetry and completed bounded bare qualification. Personal settings remain proposals. Final review, release close, resident Copilot workers and outward/autonomous channels are unqualified. |
| 6, 7 | Existing suites exercise model-independent Copilot identity, both model families' actual lifecycle commands, discovery, named/auto/missing/foreign session readback and preserved operator attestations. Usage separates partial utility tokens, complete shutdown counters and independent checkpoint/shutdown credits. A reproduced incomplete-token/valid-credit recording failure was repaired with a collection-to-recording regression; missing complete tokens remain unknown and do not block known credits. |
| 9 | [Qualification record](qualification.md): 4/4 live episodes collected with expected outcomes: executor implementation-ready, fresh verifier found the planted import defect and recorded needs-fix, fixer recorded repair-complete, and a second fresh verifier recorded pass. All four released their writer after exit. CLI 1.0.86 / Claude Sonnet 5 / xhigh was selected and attested in every episode. Episode 2 changed from manual to allow-all mid-session; its record retains that timeline rather than claiming one session-wide mode. The old unused scratch remains preserved; synthetic fixture transitions do not qualify live behavior. |
| 10, 12 | Tests stay in the four existing suites; no suite, gate, lane or dependency added. Latest focused WO-146/WO-140 checks: 18 passed in 13.180 s. Compiler harness plus executor-support tests: 12 passed. Full-gate/machinery observations below. Publication locks and generated bundle refreshed from sources. |

Fresh authority (revision 002), artifact-identity, synthetic verification and complete live feedback editions are selected under this order; historical editions remain immutable. The operator-authorized Codex gpt-6-astra / xhigh feedback audit completed in 43.83 s external wall time, verified both acceptance rows, and passed the evidence check: ten passing regressions, ten removal failures and 1,192 fewer matched instruction bytes. Its recorded streams judge the current source. Synthetic verification evidence remains labeled synthetic. The live feedback audit is separate product evidence and discharges neither criterion 9 nor WO-146 independent verification.

## Validation and cost

| Check | Latest substantiated result |
| --- | --- |
| Build and generated harness | Passed; 31 generated surfaces. |
| Compiler harness and executor-support fixtures | 12 passed, 1.461 s, after the final neutral status-clause correction; 13 focused harness/process-debt/context checks also passed in 2.605 s. |
| Existing harness-probe suite | 37 passed, 49.987 s (external 50.015 s). |
| Existing harness-fixtures suite | 112 passed, 163.606 s (external 163.63 s), after repairing the recovery-command path. |
| Existing process-debt suite | 88 passed, external 62.840 s. After the final role-path correction, all 12 WO-146/WO-140/WO-145 targeted tests passed in 2.278 s; the separate context/economy pair also passed. |
| Full npm test | Passed: 21 suites, 0 failures, 254.22 s runner / 254.64 s external; 65 fresh tasks. This followed the live feedback audit and dependent console capture refresh. The later probe-only renderer/accounting correction passes its two affected tests and is included in the full machinery run. |
| Full npm run test:machinery | Passed: 16 checks, 0 failures, 299.71 s runner / 300.10 s external; 16 fresh tasks. Includes the latest probe renderer/accounting correction. The earlier stale-feedback attempt cost 2.99 s / 3.35 s externally and was not a pass. |
| Authority, artifact identity and synthetic verification evidence | Current selected editions passed their existing checks. |
| Compatibility | Both worker profiles differ from integrated main only in the authorized skeleton version; both skill roots match; existing settings and Codex surfaces remain byte-unchanged. |
| Publication, planning, release surfaces, cold-start budget and whitespace | Passed their existing checks; no publication performed. |

The harness suite initially had one failure caused by the missing `scripts/` prefix in the newly generated recovery instruction. The corrected source and current role snapshot pass the full rerun above. No test or gate was skipped or weakened to obtain that result. Raw command logs remain in ignored session scratch.

Activation baseline: npm test 305.84 s (external 306.71 s); test:machinery 272.63 s (external 273.16 s). This comparison includes intervening WO-056 and WO-145 integrations and cannot attribute all timing changes to Copilot. One interrupted after-change gate cost 231.3 s (external 231.70 s), explicitly stopped to repair the demonstrated credit-recording defect; it recorded no passing check and is additional execution cost, separate from any future successful-run delta. The successful product run is 51.62 s faster by runner time (52.07 s externally). Machinery is 27.08 s slower by runner time (26.94 s externally). These are observations across the combined integrations, not Copilot-only performance claims. The failed runs are not equivalent outcomes: a premature feedback selection cost 250.22 s / 250.60 s external, and the missed console capture refresh cost 254.64 s / 255.20 s external. Their cost remains additional work, not part of that successful-run delta.

The prior Copilot parent's verified shutdown records 7,128.82367 AI credits and 28,450,845 session-cumulative tokens at 2026-09-20T07:42:10.301Z, superseding the earlier 4,971.48-credit checkpoint. These are neither this continuation's counters nor the total for all scripted probe sessions. Dollars remain unknown. D011 in [decisions](decisions.md) records the source and correction.

The operator reports **8,000 / 20,000 monthly Copilot AI credits used (40%)**, with WO-146 as the only Copilot workload, and reports switching to Codex mid-implementation after a Copilot handoff. This is an operator-attested account-level reading as of 2026-09-20, not an independently reconciled session sum. It includes earlier Copilot implementation work. It overlaps the individual session figures above and below, so those totals must not be added to it. Codex usage remains separate; dollars are unknown. This substantial cost is part of the outcome, alongside the completed capability.

Cold-start totals in each identical root: executor 23,154 bytes, verifier 21,061, reviewer 22,279, release-close 13,967, planner 15,033, refuter 15,690; shared instruction 6,113 bytes. The verifier ceiling is 25,151 under D008's authorized adjustment; other applicable ceilings are met. These are source bytes, not tokens.

## Release and handoff

Application v0.35.0 and skeleton 0.31.0 follow integrated WO-145's v0.34.0 / 0.30.0 under the existing minor classification. Compiler 0.17.0 adds the third target; kernel 0.6.0 and console 0.1.7 retain their versions and behavior, with internal pins synchronized. No publication action is authorized by this evidence.

The mission contribution is an operator-selected third harness using one shared policy source and observed limits. Both bare probes and all four workflow episodes are collected. The [operator runbook](operator-qualification.md) retains the procedure and limits; the four session records total 196.35181 AI credits, 5,299,696 tokens including cached input and 1,088.345 seconds of session wall time, excluding time between sessions. These distinct-session sums exclude parent and scripted-probe usage.

The full product pass predates the isolated probe-report correction and neutral generated status sentence; the full machinery pass includes the probe correction. The final status sentence removes a stale pending claim without changing role procedure, adapters or hook behavior. It passes the 25 affected checks above, current generated-output/context checks and authority revision 002; artifact, synthetic verification and live feedback checks remain green. The live qualification snapshot predates only that neutral status sentence. No full-gate rerun or new live model is inferred from these narrower checks.

All implementation obligations are discharged with the stated coverage limits. WO-146 independent verification and final review remain separate dispatches. No commit or publication was performed. Reopen on duplicate handlers, denial mismatch, wrong session/counter attribution or an operator episode contradicting the control table.
