# WO-146 repair of VER-001

`resume: fix`, 2026-09-20. Repairs the single finding N1 from
[VER-001](../../verifications/WO-146/VER-001.md) and records its limit L1 as a
same-day correction. Decisions are [D013 and D014](decisions.md). The original
[implementation evidence](implementation.md) and VER-001 remain preserved; this
report records the repaired subject. No other finding routed to repair, and
VER-001's L2 to L6 are disclosure the repair does not touch.

**Actor attestation:** {"harness":"claude-code","harnessVersion":"2.1.278","model":"claude-opus-5[1m]","effort":"unknown","source":"self-reported"}

Harness version is live readback from `claude --version`. The model id is this
session's own reported identity. Effort is `unknown`: this harness supplies no
effective-effort readback and nobody supplied a value with the `resume: fix`
dispatch, so the order's `executor xhigh+` recommendation is not recorded as an
observation. One writable executor, zero subagents against a cap of 20; the
fan-out plan at entry was to repair in this session and spawn nothing, so the
whole budget remains and the unobserved remainder is unknown. No branch commit
and no publication.

| Finding | Repair and executed evidence |
| --- | --- |
| N1: `npm run test:docs` red because `scripts/lib/copilot-probe.mjs` and `scripts/test-harness-probe.mjs` fail the pinned Prettier `format` task, suppressing five preflight dependants | Ran the repository's pinned Prettier (3.9.6, the `package.json` pin) over exactly those two files. The rewrite is the four mechanical hunks VER-001 named — the `usage` ternary and `renderCopilotProbe`'s two predicates in `copilot-probe.mjs`, the credit-source `assert.ok` and the `interactive` array literal in `test-harness-probe.mjs` — line-wrapping and trailing commas, no semantic change. `npx prettier --check` on both files moved from exit 1 to exit 0, and `npm run test:docs` from **13 passed / 6 failed** to **19 passed / 0 failed**, so `plan`, `plan-refutation-current`, `console-docs`, `skeleton-docs` and `lineage-fixtures` now execute instead of reporting a failed preflight. |
| L1 (recorded limit, not routed to repair): WO-146-D012's prose carried cold-start totals two bytes high per role | Recorded as correction D014 and annotated in place under D012's paragraph. Measured from the current bytes: `CLAUDE.md` 6,113 plus 17,041 / 14,948 / 16,166 gives executor **23,154**, verifier **21,061**, reviewer **22,279**. D012's figures were correct for the edition it described; its own continuation's status-clause correction later removed two bytes per role. The original sentence is preserved, not rewritten. |

Executed checks, all from this worktree after the repair:

- `npm run test:docs`: **19 passed, 0 failed**, 17.57 s, on the final tree
  including the decisions edits. The first post-fix run was 19 passed, 0 failed
  in 18.12 s.
- `npx prettier --check scripts/lib/copilot-probe.mjs scripts/test-harness-probe.mjs`:
  exit 0, "All matched files use Prettier code style!".
- `node scripts/test-runner.mjs --machinery --only harness-probe` immediately
  after the rewrite: **2 passed, 0 failed**, 51.37 s. This is the suite that
  executes both rewritten files, so the formatter's output is observed to run,
  not assumed neutral.
- `npm test`: **21 passed, 0 failed**, 251.93 s runner, 252.31 s external.
- `npm run test:machinery`: **16 passed, 0 failed**, 284.05 s runner, 284.56 s
  external.
- `npm run publication:check`: pass, index coverage 272/272, both voice tables
  current.
- `node scripts/harness.mjs check`: **31 generated surfaces**, unchanged count.
- `npm run plan -- check`: exit 0.
- `git diff --check` and `git diff --cached --check`: clean.
- `npm run meta`: refreshed `docs/lineage/decisions-index.md` with D013 and
  D014; its own trap readout reports `coldStartBytes 23,154`, which is the
  figure D014 corrects to.

Gate wall-clock, as criterion 10 requires. `npm test` 251.93 s runner here
against VER-001's 254.95 s and the implementation pass's 254.22 s: **-3.02 s**
and **-2.29 s**. `npm run test:machinery` 284.05 s here against VER-001's
290.88 s and the implementation's 299.71 s: **-6.83 s** and **-15.66 s**. Both
deltas are within ordinary host-load variation across three sessions on one
machine; nothing in this repair changes what either gate executes, and no lane,
suite or classification changed.

Scope actually touched: two script files reformatted, `decisions.md` extended
with D013, D014 and the in-place D012 annotation, and `docs/lineage/decisions-index.md`
regenerated by `npm run meta`. No generated bundle, profile, fixture
expectation, probe record, qualification record, release version or dependency
changed; `node scripts/harness.mjs check` reporting the same 31 surfaces is the
evidence for that. Cold-start bytes are unchanged by this repair at 23,154 /
21,061 / 22,279 for executor, verifier and reviewer, so no ceiling moves.

Limits. The two rewritten files are working-tree sources —
`scripts/lib/copilot-probe.mjs` untracked and `scripts/test-harness-probe.mjs`
partly unstaged — which is how every new file of this order stands; the repair
is in the same tree VER-001 judged, and final review still owns committing it.
The `harness-probe` suite exercises both files through the stub executable, not
a live CLI, so this repair adds no new observation of Copilot behavior and
changes none of the order's labels. VER-001's L2 to L6 stay as recorded: the
untracked `docs/final-reviews/WO-146/PR.md` process-meter block, the ambiguous
`maxInvocationsPerCorrelatedEvent` maxima, the operator-attested launch and
approval rows, the untested `instructionLoadedOnce` with H6 and H7 unobserved,
and the absence of an independent cross-check for the usage counters. None of
them is a defect this repair could close, and none was widened.

Follow-up queue: revision 0, empty at entry and empty at handoff; no item was
added, because both repairs were completed in this episode rather than
deferred. No defect was met and left unfixed. Independent re-verification and
final review retain their separate dispatches.
