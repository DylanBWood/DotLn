# WO-051 ideation breakout — local model use

The operator's `ideation:` message on 2026-09-16 authorizes capture, synthesis,
ledger append and product write-back during `resume: next`. It asks the next
planning pass to prioritize local LLM experiments, with possible operator setup
in LM Studio, followed by a larger controlled comparison program. It does not
activate WO-110, reorder the sequence, authorize installation/configuration or
extend WO-051's source-writing implementation to local models.

Source: `docs/intake/notes/WO-051-expanded-ideation-2026-09-16.md` (unedited
operator message). `git worktree list --porcelain` resolved the main checkout;
the current host's write scope covers this worktree, so the capture is staged
locally, backed up outside the repository, and must be reconciled into main's
ignored intake before worktree removal. No backup filename is a durable input.
Clean-room screen: no stop condition; personal product intent, no employer or
secret material. Strategy: Shape-First Synthesis with the saved public
destination loadout; no direct-filing exception or added support.

Promoted surfaces:

- `docs/lineage/idea-ledger.md`, 2026-09-16 local-model experiments and guided
  operator work-order entries.
- `docs/product/06-roadmap.md`, Candidate — local-model usefulness experiments.
- `docs/product/07-execution-guide.md`, Candidate — guided operator work orders.
- The WO-051 scope-expansion receipt points verifier/reviewer to this document.

Existing context: product 06's counterfactual profiling contract and dated
WO-027 deferral; WO-110's inspection-only local transport and fresh availability
row. Preserve those observations. Current LM Studio readiness is unknown; the
next planning pass should identify the concrete setup needed with the operator
before a new bounded availability smoke. Then plan matched comparisons of
models, within-model repeated runs, and prompt/support changes. The goal is
evidence about useful local responsibilities and limits, including hybrid use,
not a universal ranking or an output-volume target.

Unresolved: installed runner/model readiness, setup steps, hardware capacity,
which work orders represent intended use, sample sizes, evaluators, budget,
transport capability differences and the plan's placement. Reopen at the next
planning pass or an operator report that local setup is ready. No new schema,
implementation, version or work-order allocation follows from this breakout.

Goal/trap comparison: useful local inference advances model-agnostic operation
and can reduce recurring operator/provider dependence. Policy resistance and
drift: keep authority and correctness criteria fixed across comparable cells.
Commons and escalation: start with availability and a bounded pilot before the
larger experiment; include setup and evaluation cost. Success to the successful:
include local and remote candidates without assuming the incumbent wins.
Shifting the burden: identify concrete setup needs once and judge operator
intervention in the experiments. Rule beating: preserve failed runs and
within-cell variation; use independent outcome checks. Seeking the wrong goal:
choose viable local responsibilities, not the highest token count or score.
Naive Interventionism: reuse profiling contracts, keep current observations,
and change one factor at a time. NoOp preserves the absence of practical local
use evidence, so durable next-pass priority wins; immediate configuration and
large fan-out are deferred until availability and pilot evidence exist.

Required independent review: verifier and final reviewer inspect the raw-capture
provenance, synthesis fidelity, candidate status, retained WO-027 deferral,
cross-references, lack of implied setup/launch authorization and pending intake
reconciliation. Document checks and publication screening cover these surfaces.

Second dispatch, same day: the operator wants the setup/readiness work itself
to be a normal work-order type, with guided trial and error toward an observable
result or a failure artifact. Capture message 2 and synthesize a general guided
operator-work candidate in product 07. This supplies the missing workflow for
the first candidate's setup prerequisite; it does not start that setup now.
Unresolved choices include outcome status, human-handoff interaction and the
failure-artifact contract. Apply the goal/trap comparison above: ordinary
continuation and evidence should remove repeated explanation, retain failures
and avoid a parallel process. NoOp leaves diagnostic setup outside the durable
workflow; prematurely adding a new state machine or schema lacks a consuming
example. Reopen at the next planning pass. The verifier/reviewer also judge that
a completed investigation is never mislabeled as a successful setup.
