# WO-119 ideation breakout — authority and continuity

Authority: the operator's explicit `ideation:` message during `resume: next`
on 2026-09-16 expands the documentary subject. Existing implementation remains
preserved and resumes after this breakout. It does not authorize changing
Claude/Codex settings or removing any current permission boundary.

Source: `docs/intake/notes/WO-119-expanded-ideation-2026-09-16.md`, captured
unedited in this worktree. `git worktree list --porcelain` resolved the main
control-plane checkout; its intake is outside this session's writable roots.
The capture is provisional, backed up outside the repository, and must be
reconciled into main's ignored intake before this worktree is removed. No raw
source is copied into committed documentation.

Effective strategy: Shape-First Synthesis with public vocabulary and provenance;
clean-room screen found no employer material, credentials, private identifiers
or other stop condition. No direct-filing exception applies. The first message ended mid-sentence;
message 2 explicitly confirms faithful runtime enforcement plus adversarial
tests. Message 3 adds that intermittent approval gates on basic calls prevent
useful unattended work. Both requirements are now operator-confirmed; the
proposed mediation mechanisms and detailed test matrix remain synthesis.

Promoted surfaces: the dated ledger entry “DotLn-owned authority with minimal
native harness restrictions” and product 03's candidate of the same name.
No ADR, schema, implementation, account setting, publication or release
classification changes arise from this breakout. The work-order scope note
adds this receipt and product section to verifier/reviewer reads.

Goal alignment: central policy could reduce recurring operator supervision and
duplicated configuration. Policy resistance and rule beating require checking
that declarations reach actual effect mediation; today's advisory hooks do not
establish that. Commons and escalation favor one policy source with bounded
probes over another parallel permissions framework. Drift and wrong-goal risk
require successful useful work and observed denied effects, not merely empty
settings. Success to the successful permits replacing native boundaries when
proved; shifting the burden rejects relying on repeated operator rescue.
Naive Interventionism preserves working protections until their useful functions
are accounted for and favors reversible, isolated experiments. NoOp would keep
current protection but fail to record the requested product direction; immediate
settings removal would outrun evidence. Capture as a candidate wins now.

Reopen at the next planning pass to find the earliest evidence-backed path or
when new runtime mediation evidence arrives. Open choices: runtime mechanism, acceptable residual host responsibility, measured coverage
threshold and transition/rollback criteria. No new order is filed here.

Independent verification and final review must inspect this receipt, ledger and
product candidate for clean-room treatment, intent traceability, consistency
with current authority and correct separation of future goals from current
capability. Document/publication checks are required; no settings helper or
executable ideation tool was introduced.

Clarification incorporated before resuming execution: runtime enforcement must
survive adversarial tests, and authorized basic calls must not stall on approval
while the operator is absent. A representative sustained unattended workflow
must therefore test useful completion as well as refusal of excess authority.
The receipt includes all three source messages; existing host permissions still
apply to this session. No immediate sandbox/settings mutation was requested.

Follow-up message 4, 2026-09-16: the operator reports post-compaction replies
to stale chat followed by indefinite idle work without a running tool or wake
event. The same ignored intake capture now contains this unedited message.
Clean-room review found no stop condition; ordinary synthesis remains the
strategy. The dated ledger entry “Continuation after compaction and premature
turn completion”, product 05's existing Context Continuity candidate and product
03's session lifecycle now preserve this requirement. Those surfaces join the
verifier/reviewer reads. No executable helper or continuity implementation was
added.

Observed correction: the executor's preceding final response answered the
conversation-only discovery question and ended the turn while WO-119 still
needed gate collection, document checks and its completion transition. Product
07 §Operator resume phrases and §Discipline already require continuing the
active work. The compacted handoff retained those obligations, so missing task
state is not established by this case. The assistant's interpretation was
wrong; the resumed executor collected the successful product gate and continued.
The broader claim that compaction causes stale-message replay or that a harness
will remain idle forever needs a bounded live probe and event evidence.

The official [Codex app-server documentation](https://learn.chatgpt.com/docs/app-server),
checked on 2026-09-16, distinguishes context-compaction item completion,
turn completion and starting generation with `turn/start`. It supplies protocol
discovery evidence, not a guarantee of autonomous task completion in this
interactive session. An independent read-only review identified the missing
zero-running-tool case; existing resident ticks do not prove interactive-thread
wake capability.

Goal alignment: continuity advances unattended work by removing repeated
operator rescue. NoOp leaves an observed conduct failure and an untested
liveness gap. Reuse the existing candidate instead of adding another queue or
prompt reminder: this limits commons cost, policy resistance and escalation.
Drift, rule beating and wrong-goal risks require observed progress within a
declared fixture deadline, not a good summary or turn-completed event. Success
to the successful leaves native recovery and a DotLn host as testable options;
shifting the burden rejects operator nudges as the normal scheduler. Naive
Interventionism preserves pause, completion, authority and one-writer behavior;
probe before selecting a mechanism. Reopen at planning or on actual compaction
and continuation evidence. The reviewer should challenge any inferred causal
claim or implied implementation promotion.
