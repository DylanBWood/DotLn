# WO-142 — B8/B9/B10/B14/B15 focused evidence

Repair clarification (N12): this is a dated observation record. Temporary
helper scripts and logs named below were session-local and are not committed
or promised to remain available. Source predicates are inspections, distinct
from repository regression tests. Current repair commands and their outcomes
are recorded in [repair-001.md](repair-001.md).


These observations were made in the selected worktree. Proposal and counterfactual material lived outside the repository; the executor remained the only repository writer. Counterfactuals changed only an external test/source copy and did not alter recorded historical evidence. The commands below use repository-relative or generic temporary paths and contain no host-specific identifiers.

| Group | Current check and observed result | Required negative observation |
| --- | --- | --- |
| B8 | `node --test packages/skeleton/dist/test/entropy-reducer.test.js packages/skeleton/dist/test/feedback-audit-source.test.js` — 16/16 combined B8/B9 tests pass. Feedback tests cover linked workspaces (including console and a future workspace), retained unknown metadata, and discovery CLI registration. | `node /tmp/wo142-reverted-check.mjs` (run before parent applied B8/B9): substitute then-original feedback source into the external check and restore the proposal afterward. 3/5 pass, 2 fail: workspace release/license projection and discovery source inventory. |
| B9 | Same combined command — 16/16 pass, including version 2, relationship-first residue, unchanged authority changes, and component-manifest participation. Current compiler fixture hash is `fnv1a64:04b7ff37e73a6cd0`; current compilation with historical review inputs is `fnv1a64:843ab5458a939d25`. The recorded review hash remains `fnv1a64:b81d0609ee2e9fed`. | Same original-source command: original Entropy Reducer yields 10/11 pass, 1 fail at the new versioned Shape-First assertion. The later residue-renderer correction also removes its separate contradictory literal-first instruction. |
| B10 | `node --test --test-name-pattern='WO-032 AC2 executor, verifier attempts, authority, hashes and actual accepted episode remain distinct' packages/console/dist/test/board.test.js` — 1/1 pass using the current feedback edition. The fixture appends its second attempt only to an in-memory copied log. | `node /tmp/wo142-b10-counterfactual.mjs` — restores only the original single-attempt fixture construction while retaining the loop-executed assertion. The test fails specifically with `the expired-attempt assertions must execute`; no persisted-compilation-drift failure. |
| B14 | `node --test --test-name-pattern='WO-142 (?!B11)|WO-047 reserved|WO-048 inventory|WO-050 hosts' /tmp/wo142-b14-*.test.mjs` — 10/10 pass against the rebuilt product modules. Covers state aliases/destructuring, local file-reader rebindings, the typed feedback-selfhost exemption, audit validation/verdict/adjacency, F-00005–F-00007, and non-discretionary presence. | `node /tmp/wo142-b14-counterfactual.mjs` — both original alias detectors fail their new fixtures. Separate external mutants for F-00005 socket-budget validation, F-00006 ordered multiplicity, F-00007 update laws, and audit schema/time/failed/unknown/adjacency each fail the intended assertion (10 negative checks). |
| B15 | `node --test --test-name-pattern='WO-142' packages/skeleton/dist/test/cli.test.js packages/console/dist/test/board.test.js` — 3/3 pass: unknown CLI argument refusal, healthy loadouts retained beside a throwing factory, and underlying store parse cause retained. `node /tmp/wo142-check-small.mjs` also observed `ultracode` normalize to effort `xhigh`, mode `subagents`, raw `ultracode`. Other small B15 changes are included in the executor's package/process fixture validation. | `node /tmp/wo142-b15-counterfactual.mjs` — original HEAD CLI fails the new unknown-argument rejection assertion: `--beacons=fixture` returns exit 0 where nonzero is required. |

Transcripts: `wo142-b8-b9-current.log`, `wo142-feedback-audit-source-reverted.log`, `wo142-entropy-reducer-reverted.log`, `wo142-b10-current.log`, `wo142-b10-counterfactual.log`, `wo142-b14-focused.log`, `wo142-b14-counterfactual.log` and its individually named negative logs, `wo142-b15-current.log`, `wo142-b15-counterfactual.log` (all currently in `/tmp/`). Raw process logs can contain runtime stack paths; this concise observation record omits those host details.

B9 generation follow-up: historical/current compatibility in the authority tool was checked read-only with `node /tmp/wo142-authority-b9-readonly-check.mjs --check`. It reconstructs historical entropy from the frozen baseline revision, verifies historical semantic/program/inspection/full-identity claims, admits exactly the four explicit Shape-First field changes, and keeps authority, ambient effects, the full work order and other component definitions equal. Transcript: `wo142-authority-b9-readonly-check.log`. Generation and the live edition are owned by the executor, not claimed by this helper's check.
