{
  "name": "dotln",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "rm -rf packages/kernel/dist && tsc -b --force && ls packages/kernel/dist/test/*.test.js >/dev/null && node --test packages/kernel/dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "5.4.5"
  }
}
